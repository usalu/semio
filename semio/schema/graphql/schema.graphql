type AddedAttributeToConceptInput {
	attribute: Attribute!
}

type AddedAttributeToPortInput {
	attribute: Attribute!
}

type AddedAttributeToQualityInput {
	attribute: Attribute!
}

type AddedAttributeToTagInput {
	attribute: Attribute!
}

type AddedAttributesToConceptInput {
	attributes: AttributeConnection!
}

type AddedAttributesToPortInput {
	attributes: AttributeConnection!
}

type AddedAttributesToQualityInput {
	attributes: AttributeConnection!
}

type AddedAttributesToTagInput {
	attributes: AttributeConnection!
}

type Alternative implements Version {
	id: ID!
	hash: String!
	owner: Graph
	name: String!
	start: Checkpoint!
	checkpoints: [Checkpoint!]!
	store: Kit!
	checkpoint: CheckpointConnection!
	latestWipCheckpointAncestor: Checkpoint
	savedChanges: ChangeConnection!
	unsavedChanges: ChangeConnection!
	kit: Kit!
}

type AlternativeCommand {
	version: ID!
	integrateIntoTheKit: ID!
}

type AlternativeConnection {
	edges: [AlternativeEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type AlternativeEdge {
	cursor: String!
	node: Alternative!
}

type Attribute {
	id: ID!
	hash: String!
	owner: AttributeOwner!
	key: String!
	value: String
	definition: String
}

type AttributeConnection {
	edges: [AttributeEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type AttributeEdge {
	cursor: String!
	node: Attribute!
}

"""
🏷️ Hand union for `Attribute.owner` (subset of carriers Attribute can hang off of).
"""
union AttributeOwner = Piece | Connector | Representation | Connection | Kit | Design | Type | Concept | Tag

type Author {
	id: ID!
	name: String!
	email: String!
	role: String
	rank: Int
	hash: String!
}

type AuthorConnection {
	edges: [AuthorEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type AuthorEdge {
	cursor: String!
	node: Author!
}

type BackboneCommand {
	attach(uri: String!): ID!
	detach(uri: String!): ID!
	status: BackboneStatus!
	setActiveCheckpoint(id: ID!): ID!
	syncNow: ID!
}

"""
@emoji 🗄️ Backbone materialization: dev JSON file, local `.semio/` SQLite+blobs, or remote hub (reserved).
"""
enum BackboneKind {
	DEV
	LOCAL
	REMOTE
}

type BackboneStatus {
	attachedUri: String
	kind: BackboneKind
}

type Benchmark {
	id: ID!
	name: String!
	min: Float
	max: Float
	minExcluded: Boolean
	maxExcluded: Boolean
	hash: String!
}

type BenchmarkConnection {
	edges: [BenchmarkEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type BenchmarkEdge {
	cursor: String!
	node: Benchmark!
}

"""
🧩 Blueprint union (Type | Design) — what a Piece references and what a Piece's owner is.
"""
union Blueprint = Type | Design

type BlueprintConnection {
	edges: [BlueprintEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type BlueprintEdge {
	cursor: String!
	node: Blueprint!
}

type Change {
	id: ID!
	hash: String!
	owner: ChangeOwner!
	edits: EditConnection!
	startedAt: Timestamp!
	savedAt: Timestamp
	saved: Boolean!
	description: String!
	origin: String!
	"""
	@emoji 🔗 Ordered  operation record ids constituting the forwards side (bundle `OperationLog` ids) when persisted.
	"""
	forwardOperationRecordIds: [ID!]!
	"""
	@emoji 🔗 Ordered  operation record ids for backwards / inverse application when persisted separately from `OperationKind`.
	"""
	backwardOperationRecordIds: [ID!]!
}

type ChangeConnection {
	edges: [ChangeEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type ChangeEdge {
	cursor: String!
	node: Change!
}

union ChangeOwner = Alternative | Checkpoint

type ChangedDescription implements Operation {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	entity: Kit!
}

type Checkpoint {
	id: ID!
	hash: String!
	timestamp: Timestamp
	authors: AuthorConnection!
	"""
	@emoji 🔗 SDL `Checkpoint.parent` — prior checkpoint in the spine (none for the seed checkpoint).
	"""
	parent: Checkpoint
	"""
	@emoji 🔗 SDL `Checkpoint.ancestors` — ordered walk up `parent` (immediate parent first).
	"""
	ancestors: [Checkpoint!]!
	"""
	@emoji 🌱 SDL `Checkpoint.initial` — graph-level [`Graph::initial_kit`] baseline (saved once per graph, not duplicated on each checkpoint).
	"""
	initial: Kit
	"""
	@emoji 📦 SDL `Checkpoint.kit` — materialized kit at this checkpoint; until checkpoint-owned `changes` are wired, matches `initial`.
	"""
	kit: Kit
	changes: ChangeConnection!
	change(id: ID!): Change
	edits: EditConnection!
	edit(id: ID!): Edit
	message: String!
}

type CheckpointConnection {
	edges: [CheckpointEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type CheckpointEdge {
	cursor: String!
	node: Checkpoint!
}

type Concept {
	id: ID!
	hash: String!
	owner: ConceptOwner!
	name: String!
	description: String
	icon: String
	order: Int
	attributes: AttributeConnection!
	attribute(id: ID!): Attribute
}

type ConceptConnection {
	edges: [ConceptEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type ConceptEdge {
	cursor: String!
	node: Concept!
}

type ConceptOperationInput {
	rename(newName: String!): ID!
	changeDescription(newDescription: String!): ID!
	changeIcon(newIcon: String!): ID!
	addAttribute(key: String!, value: String!, definition: String!): ID!
	removeAttribute(id: ID!): ID!
	removeAttributes(ids: [ID!]!): ID!
}

"""
@emoji 🔗 SDL `union ConceptOwner`.
"""
union ConceptOwner = Kit | Type

type Conflict {
	id: ID!
	hash: String!
	backboneTip: String
	reason: String!
	createdAt: Timestamp!
}

type ConflictConnection {
	edges: [ConflictEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type ConflictEdge {
	cursor: String!
	node: Conflict!
}

type Connection {
	id: ID!
	hash: String!
	owner: Design!
	name: String!
	description: String!
	icon: String!
	connected: Side!
	connecting: Side!
	gap: Float
	shift: Float
	rise: Float
	rotation: Float
	turn: Float
	tilt: Float
	u: Float
	v: Float
	attributes: AttributeConnection!
	attribute(id: ID!): Attribute
}

type ConnectionConnection {
	edges: [ConnectionEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type ConnectionEdge {
	cursor: String!
	node: Connection!
}

type Connector {
	id: ID!
	hash: String!
	owner: Type!
	name: String!
	code: String!
	description: String!
	icon: String!
	port: Port
	qualities: QualityConnection!
	attributes: AttributeConnection!
	quality(id: ID!): Quality
	attribute(id: ID!): Attribute
}

type ConnectorConnection {
	edges: [ConnectorEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type ConnectorEdge {
	cursor: String!
	node: Connector!
}

type ConnectorOperationInput {
	rename(newCode: String!): ID!
	changeDescription(newDescription: String!): ID!
	changeIcon(newIcon: String!): ID!
}

"""
@emoji 📍 WeakEntity + entity shell for [`Coordinate`] (SDL `Coordinate`).
"""
type Coordinate implements Node {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	u: Float!
	v: Float!
}

type CoordinateEdge implements EntityEdge {
	cursor: String!
	node: Coordinate!
}

input CoordinateInput {
	u: Float!
	v: Float!
}

type CreatedConceptInput {
	concept: Concept!
}

type CreatedConceptsInput {
	concepts: ConceptConnection!
}

type CreatedFixedPiece implements Operation {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	piece: Piece!
}

type CreatedPortInput {
	port: Port!
}

type CreatedPortsInput {
	ports: PortConnection!
}

type CreatedQualitiesInput {
	qualities: QualityConnection!
}

type CreatedQualityInput {
	quality: Quality!
}

type CreatedTagInput {
	tag: Tag!
}

type CreatedTagsInput {
	tags: TagConnection!
}

type DeletedConceptInput {
	hasInput: Boolean!
}

type DeletedConceptsInput {
	hasInput: Boolean!
}

type DeletedPortInput {
	hasInput: Boolean!
}

type DeletedPortsInput {
	hasInput: Boolean!
}

type DeletedQualitiesInput {
	hasInput: Boolean!
}

type DeletedQualityInput {
	hasInput: Boolean!
}

type DeletedTagInput {
	hasInput: Boolean!
}

type DeletedTagsInput {
	hasInput: Boolean!
}

type Design {
	id: ID!
	hash: String!
	owner: DesignOwner!
	name: String!
	description: String
	icon: String
	image: String
	location: Location
	unit: String
	createdAt: Timestamp
	updatedAt: Timestamp
	pieces: PieceConnection!
	piece(id: ID!): Piece
	connections: ConnectionConnection!
	connection(id: ID!): Connection
	layers: LayerConnection!
	groups: GroupConnection!
	authors: AuthorConnection!
	qualities: QualityConnection!
	props: PropConnection!
	attributes: AttributeConnection!
	stats: StatConnection!
	qualitySum(qualityId: ID!): Float!
	references: DesignConnection!
	referencedBy: PieceConnection!
}

type DesignConnection {
	edges: [DesignEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type DesignEdge {
	cursor: String!
	node: Design!
}

type DesignOperationInput {
	rename(newName: String!): ID!
	changeDescription(newDescription: String!): ID!
	flatten: ID!
	addAttribute(key: String!, value: String!, definition: String!): ID!
	removeAttribute(id: ID!): ID!
	removeAttributes(ids: [ID!]!): ID!
	addFixedPiece(blueprintId: ID!, position: PositionInput!, name: String, description: String): ID!
	addChildPieceWithParentConnection(blueprintId: ID!, parentPieceId: ID!, parentConnector: String!, childConnector: String!, name: String, description: String, position: PositionInput, scale: Float): ID!
	addHangingChildPieceWithParentConnection(blueprintId: ID!, parentPieceId: ID!, parentConnector: String!, childConnector: String!, position: PositionInput!, name: String, description: String, scale: Float): ID!
	piece(id: ID!): PieceOperationInput!
	pieces(ids: [ID!]!): PiecesOperationInput!
	deletePiece(id: ID!): ID!
	deletePieces(ids: [ID!]!): ID!
	deletePiecesAndConnections(pieceIds: [ID!]!, connectionIds: [ID!]!): ID!
}

"""
@emoji 🏠 SDL `union DesignOwner = Kit`.
"""
union DesignOwner = Kit

type DraggedPiece implements Operation {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	pieces: [Piece!]!
}

type Edit {
	id: ID!
	hash: String!
	owner: EditOwner
	forwards: OperationConnection!
	backwards: OperationConnection!
	sequenceNumber: Int!
	startedAt: Timestamp!
	finishedAt: Timestamp
	finished: Boolean
	description: String!
	origin: String!
	changes: [Change!]!
}

type EditConnection {
	edges: [EditEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type EditEdge {
	cursor: String!
	node: Edit!
}

union EditOwner = Alternative | Checkpoint

interface EntityEdge {
	cursor: String!
}

"""
@emoji 🧷 Kit [`Family`] SDL shell — Artifact [`name`]/[`description`]/[`icon`] are persisted kit fields.
"""
type Family {
	id: ID!
	name: String!
	description: String
	icon: String
	hash: String!
}

type FamilyConnection {
	edges: [FamilyEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type FamilyEdge {
	cursor: String!
	node: Family!
}

type File {
	id: ID!
	url: String!
	mime: String
	size: Int
	description: String
	created: Timestamp
	updated: Timestamp
	hash: String!
	tag(id: ID!): Tag
	quality(id: ID!): Quality
	attribute(id: ID!): Attribute
}

type FileConnection {
	edges: [FileEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type FileEdge {
	cursor: String!
	node: File!
}

type FixedPiece implements Operation {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	piece: Piece!
}

type Folder {
	id: ID!
	path: String!
	description: String
	hash: String!
	file(id: ID!): File
	subFolder(id: ID!): Folder
	family(id: ID!): Family
	type(id: ID!): Type
	design(id: ID!): Design
}

type FolderConnection {
	edges: [FolderEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type FolderEdge {
	cursor: String!
	node: Folder!
}

"""
@emoji 🌐 Global `node` / `entity` resolution union (Relay `Node` stand-in until full `Entity` interface wiring).
"""
union GqlNode = Graph | Kit | Design | Piece | Session | Conflict | Tag | Concept | Quality

type Graph {
	id: ID!
	hash: String!
	owner: GraphOwner!
	theKit: Version!
	initialKit: Kit
	alternative(id: ID!): Alternative
	alternatives: AlternativeConnection!
	checkpoint(id: ID!): Checkpoint
	checkpoints: CheckpointConnection!
	release(id: ID!): Checkpoint
	releases: CheckpointConnection!
}

"""
@emoji 🔗 `Graph.owner` — target SDL `union GraphOwner = Session`.
"""
union GraphOwner = Session

type Group {
	id: ID!
	name: String!
	description: String
	color: String
	icon: String
	hash: String!
	pieces: PieceConnection!
	piece(id: ID!): Piece
}

type GroupConnection {
	edges: [GroupEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type GroupEdge {
	cursor: String!
	node: Group!
}

type Kit {
	id: ID!
	hash: String!
	owner: KitOwner!
	checkpoint: Checkpoint
	name: String!
	description: String
	icon: String
	image: String
	preview: String
	remote: String
	homepage: String
	license: String
	uri: String
	createdAt: Timestamp
	updatedAt: Timestamp
	version: String
	design(id: ID!): Design
	designs: DesignConnection!
	type(id: ID!): Type
	types: TypeConnection!
	files: FileConnection!
	folders: FolderConnection!
	families: FamilyConnection!
	authors: AuthorConnection!
	concepts: ConceptConnection!
	tags: TagConnection!
	qualities: QualityConnection!
	props: PropConnection!
	attributes: AttributeConnection!
	stats: StatConnection!
	file(id: ID!): File
	folder(id: ID!): Folder
	family(id: ID!): Family
	author(id: ID!): Author
	concept(id: ID!): Concept
	tag(id: ID!): Tag
	quality(id: ID!): Quality
	prop(id: ID!): Prop
	attribute(id: ID!): Attribute
	stat(id: ID!): Stat
}

"""
@emoji 🔗 SDL `union KitOwner = Graph | Checkpoint | Alternative`.
"""
union KitOwner = Graph | Checkpoint | Alternative

type Layer {
	id: ID!
	name: String!
	description: String
	"""
	@emoji 🏷️ Artifact `icon` on SDL `Layer`.
	"""
	icon: String!
	color: String
	order: Int
	visible: Boolean
	locked: Boolean
	hash: String!
}

type LayerConnection {
	edges: [LayerEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type LayerEdge {
	cursor: String!
	node: Layer!
}

"""
@emoji 🌍 WeakEntity shell for [`Location`] (SDL `Location`).
"""
type Location implements Node {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	longitude: Float!
	latitude: Float!
	altitude: Float!
}

type LocationEdge implements EntityEdge {
	cursor: String!
	node: Location!
}

type Mutation {
	"""
	@emoji 🎛️ Kit-changing commands — navigate via nested fields per `target.schema.graphql` `#region Commands`.
	"""
	session: SessionCommand!
}

interface Node {
	id: ID!
}

type Offset implements Node {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	u: Float!
	v: Float!
}

type OffsetEdge implements EntityEdge {
	cursor: String!
	node: Offset!
}

input OffsetInput {
	u: Float!
	v: Float!
}

"""
🪄 GraphQL `interface Operation` (each variant is an Arc-shared operation entity).
"""
interface Operation {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
}

type OperationConnection {
	edges: [OperationEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type OperationEdge {
	cursor: String!
	node: Operation!
}

type OperationInput {
	rename(newName: String!): ID!
	changeDescription(newDescription: String!): ID!
	createTag(name: String!, description: String, icon: String, order: Int): ID!
	tag(id: ID!): TagOperationInput!
	deleteTag(id: ID!): ID!
	deleteTags(ids: [ID!]!): ID!
	createConcept(name: String!, description: String, icon: String, order: Int): ID!
	concept(id: ID!): ConceptOperationInput!
	deleteConcept(id: ID!): ID!
	deleteConcepts(ids: [ID!]!): ID!
	createQuality(key: String!, value: String, unit: String, definition: String, description: String, icon: String): ID!
	quality(id: ID!): QualityOperationInput!
	deleteQuality(id: ID!): ID!
	deleteQualities(ids: [ID!]!): ID!
	createType(name: String!, description: String, icon: String, image: String, unit: String): ID!
	type(id: ID!): TypeOperationInput!
	deleteType(id: ID!): ID!
	deleteTypes(ids: [ID!]!): ID!
	createDesign(name: String!, description: String, icon: String, image: String, unit: String): ID!
	design(id: ID!): DesignOperationInput!
	deleteDesign(id: ID!): ID!
	deleteDesigns(ids: [ID!]!): ID!
}

"""
@emoji 🔗 SDL `OwnedEntity` subset for non-empty `owns` edges.
"""
union OwnedEntity = Kit | Design | Piece | Position

type OwnedEntityConnection {
	edges: [OwnedEntityEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type OwnedEntityEdge {
	cursor: String!
	node: OwnedEntity!
}

"""
@emoji 🔗 SDL `OwnerEntity` subset (grow toward full target union).
"""
union OwnerEntity = Edit | Kit | Type | Representation | Connector | Design | Piece | Graph | Session | Checkpoint | Alternative | Conflict | ReadVersion | WriteVersion | Position | Coordinate | Plane | Point | Vector | Place | Offset | Location

type PageInfo {
	hasNextPage: Boolean!
	hasPreviousPage: Boolean!
	startCursor: String
	endCursor: String
}

type Piece {
	id: ID!
	hash: String!
	owner: Blueprint!
	name: String
	description: String
	position: Position
	scale: Float
	blueprint: Blueprint!
	connectionKind: PieceConnectionKind
	flatPosition: Position!
	replaceableBlueprints: BlueprintConnection!
	parentConnection: Connection
	childConnections: [Connection!]!
	parentPiece: Piece
	childPieces: [Piece!]!
	depth: Int!
	path: [Piece!]!
	props: [Prop!]!
	prop(id: ID!): Prop
	attributes: [Attribute!]!
	attribute(id: ID!): Attribute
	childConnection(id: ID!): Connection
	childPiece(id: ID!): Piece
}

type PieceConnection {
	edges: [PieceEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

enum PieceConnectionKind {
	FIXED
	CONNECTED
}

type PieceEdge {
	cursor: String!
	node: Piece!
}

type PieceOperationInput {
	rename(newName: String!): ID!
	changeDescription(newDescription: String!): ID!
	drag(offset: OffsetInput!): ID!
	move(position: PositionInput!): ID!
	fix: ID!
	changeBlueprint(blueprintId: ID!): ID!
	addAttribute(key: String!, value: String!, definition: String!): ID!
	removeAttribute(id: ID!): ID!
	removeAttributes(ids: [ID!]!): ID!
}

type PiecesOperationInput {
	drag(offset: OffsetInput!): ID!
	move(offset: OffsetInput!): ID!
	fix: ID!
	changeBlueprint(blueprintId: ID!): ID!
}

type Place {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
}

type Plane implements Node {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	origin: Point!
	xAxis: Vector!
	yAxis: Vector!
}

type PlaneEdge implements EntityEdge {
	cursor: String!
	node: Plane!
}

input PlaneInput {
	origin: PointInput!
	xAxis: VectorInput!
	yAxis: VectorInput!
}

type Point implements Node {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	x: Float!
	y: Float!
	z: Float!
}

type PointEdge implements EntityEdge {
	cursor: String!
	node: Point!
}

input PointInput {
	x: Float!
	y: Float!
	z: Float!
}

type Port {
	id: ID!
	hash: String!
	owner: Type!
	code: String
	label: String
	order: Int
	attribute(id: ID!): Attribute
}

type PortConnection {
	edges: [PortEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type PortEdge {
	cursor: String!
	node: Port!
}

type PortOperationInput {
	rename(newCode: String!, newLabel: String): ID!
	changeDescription(newDescription: String!): ID!
	changeIcon(newIcon: String!): ID!
	addAttribute(key: String!, value: String!, definition: String!): ID!
	removeAttribute(id: ID!): ID!
	removeAttributes(ids: [ID!]!): ID!
}

type Position implements Node {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	center: Coordinate!
	plane: Plane!
}

type PositionEdge implements EntityEdge {
	cursor: String!
	node: Position!
}

input PositionInput {
	center: CoordinateInput!
	plane: PlaneInput!
}

type Prop {
	id: ID!
	key: String!
	value: String!
	unit: String
	hash: String!
	"""
	@emoji 🔎 SDL `Prop.attribute(id)` — props carry no attribute bag yet; reserved for kit snapshots.
	"""
	attribute(id: ID!): Attribute
}

type PropConnection {
	edges: [PropEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type PropEdge {
	cursor: String!
	node: Prop!
}

type Quality {
	id: ID!
	hash: String!
	owner: QualityOwner!
	key: String!
	value: String
	unit: String
	definition: String
	description: String
	icon: String
	benchmarks: BenchmarkConnection!
	attributes: AttributeConnection!
	attribute(id: ID!): Attribute
}

type QualityConnection {
	edges: [QualityEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type QualityEdge {
	cursor: String!
	node: Quality!
}

type QualityOperationInput {
	rename(newKey: String!): ID!
	changeDescription(newDescription: String!): ID!
	changeIcon(newIcon: String!): ID!
	addAttribute(key: String!, value: String!, definition: String!): ID!
	removeAttribute(id: ID!): ID!
	removeAttributes(ids: [ID!]!): ID!
}

"""
@emoji 🔗 SDL `union QualityOwner`.
"""
union QualityOwner = Connector | Representation | Type | Design | Kit

type Query {
	"""
	@emoji 🏪 Canonical target-schema read root.
	"""
	store: Store!
	"""
	@emoji 🔎 Relay-style global `node` lookup (WIP + authoritative + sessions + conflicts).
	"""
	node(id: ID!): GqlNode
	"""
	@emoji 🔎 Alias of [`Query::node`] for SDL `entity` entry point (`hash` merkle id).
	"""
	entity(hash: ID!): GqlNode
}

type ReadVersion {
	id: ID!
	hash: String!
}

type RemovedAttributeFromConceptInput {
	hasInput: Boolean!
}

type RemovedAttributeFromPortInput {
	hasInput: Boolean!
}

type RemovedAttributeFromQualityInput {
	hasInput: Boolean!
}

type RemovedAttributeFromTagInput {
	hasInput: Boolean!
}

type RemovedAttributesFromConceptInput {
	hasInput: Boolean!
}

type RemovedAttributesFromPortInput {
	hasInput: Boolean!
}

type RemovedAttributesFromQualityInput {
	hasInput: Boolean!
}

type RemovedAttributesFromTagInput {
	hasInput: Boolean!
}

type RenamedConceptInput {
	name: String!
}

type RenamedKit implements Operation {
	id: ID!
	requestId: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	kit: Kit!
}

type RenamedPortInput {
	code: String!
	label: String!
}

type RenamedQualityInput {
	key: String!
}

type RenamedTagInput {
	name: String!
}

type Representation {
	id: ID!
	hash: String!
	owner: Type!
	name: String!
	url: String!
	description: String!
	icon: String!
	file: File
	tags: TagConnection!
	qualities: QualityConnection!
	attributes: AttributeConnection!
	tag(id: ID!): Tag
	quality(id: ID!): Quality
	attribute(id: ID!): Attribute
}

type RepresentationConnection {
	edges: [RepresentationEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type RepresentationEdge {
	cursor: String!
	node: Representation!
}

type Session {
	id: ID!
	hash: String!
	startedAt: Timestamp
	"""
	@emoji 🌐 Same navigation as WIP [`Graph`] — resolved via [`crate::worker::ParentStore::wip_graph`] for the active runtime.
	"""
	alternatives: AlternativeConnection!
	alternative(id: ID!): Alternative
	theKit: Version!
}

type SessionCommand {
	start: ID!
	end: ID!
	login(username: String!, passwordHash: String!, hubUrl: String): ID!
	logout: ID!
	backbone: BackboneCommand!
	theKit: VersionCommand!
	alternative(id: ID!): AlternativeCommand!
	startAlternative(name: String): ID!
}

type Side {
	id: ID!
	hash: String!
	owner: SideOwner!
	piece: Piece!
	port: Port
	designPiece: Piece
	connector: Connector
}

"""
@emoji 🔗 SDL `union SideOwner = Connection`.
"""
union SideOwner = Connection

type Stat {
	id: ID!
	key: String!
	value: String!
	unit: String
	description: String
	hash: String!
	"""
	@emoji 🔎 SDL `Stat.attribute(id)` — stats carry no attribute bag yet; reserved for kit snapshots.
	"""
	attribute(id: ID!): Attribute
}

type StatConnection {
	edges: [StatEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type StatEdge {
	cursor: String!
	node: Stat!
}

type Store {
	"""
	@emoji 🧭 First active [`crate::vcs::Session`] on this runtime.
	"""
	session: Session!
	"""
	@emoji 🌐 Writable in-progress graph head.
	"""
	wip: Graph!
	"""
	@emoji 🧾 Authoritative graph head when available.
	"""
	authoritative: Graph
	"""
	@emoji ⚔️ Current conflict registry as a relay connection.
	"""
	conflicts: ConflictConnection!
}

type Subscription {
	"""
	@emoji 📡 Live-query mirror of [`Query::store`] — re-emits the explicit Store root on matching event-bus ticks.
	"""
	store: Store!
	"""
	@emoji 📡 Live-query mirror of [`Query::session`] — re-emits on each outbound [`EventBus`] tick.
	"""
	session: Session!
	"""
	@emoji 📡 Live-query mirror of [`Query::wip`].
	"""
	wip: Graph!
	"""
	@emoji 📡 Live-query mirror of [`Query::authoritative`].
	"""
	authoritative: Graph!
	"""
	@emoji 📡 Live-query mirror of [`Query::conflicts`].
	"""
	conflicts: ConflictConnection!
	"""
	@emoji 📡 Live-query mirror of [`Query::node`].
	"""
	node(id: ID!): GqlNode
	"""
	@emoji 📡 Live-query mirror of [`Query::entity`].
	"""
	entity(hash: ID!): GqlNode
}

type Tag {
	id: ID!
	hash: String!
	owner: TagOwner!
	name: String!
	description: String
	icon: String
	order: Int
	attributes: AttributeConnection!
	attribute(id: ID!): Attribute
}

type TagConnection {
	edges: [TagEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type TagEdge {
	cursor: String!
	node: Tag!
}

type TagOperationInput {
	rename(newName: String!): ID!
	changeDescription(newDescription: String!): ID!
	changeIcon(newIcon: String!): ID!
	addAttribute(key: String!, value: String!, definition: String!): ID!
	removeAttribute(id: ID!): ID!
	removeAttributes(ids: [ID!]!): ID!
}

"""
@emoji 🔗 SDL `union TagOwner`.
"""
union TagOwner = Kit | Type | Representation

type TheKit implements Version {
	id: ID!
	hash: String!
	owner: Graph
	checkpoint: CheckpointConnection!
	latestWipCheckpointAncestor: Checkpoint
	savedChanges: ChangeConnection!
	unsavedChanges: ChangeConnection!
	kit: Kit!
}

scalar Timestamp

type Type {
	id: ID!
	hash: String!
	owner: Kit!
	name: String!
	description: String!
	icon: String!
	image: String!
	unit: String!
	created: Timestamp
	updated: Timestamp
	connectors: ConnectorConnection!
	ports: PortConnection!
	port(id: ID!): Port
	connector(id: ID!): Connector
	representations: RepresentationConnection!
	representation(id: ID!): Representation
	bestRepresentation(tagIds: [ID!]!): Representation
	authors: AuthorConnection!
	concepts: ConceptConnection!
	tags: TagConnection!
	qualities: QualityConnection!
	props: PropConnection!
	attributes: AttributeConnection!
	stats: StatConnection!
	author(id: ID!): Author
	concept(id: ID!): Concept
	tag(id: ID!): Tag
	quality(id: ID!): Quality
	prop(id: ID!): Prop
	attribute(id: ID!): Attribute
	stat(id: ID!): Stat
}

type TypeConnection {
	edges: [TypeEdge!]!
	pageInfo: PageInfo!
	hash: String!
}

type TypeEdge {
	cursor: String!
	node: Type!
}

type TypeOperationInput {
	rename(newName: String!): ID!
	changeDescription(newDescription: String!): ID!
	changeIcon(newIcon: String!): ID!
	addAttribute(key: String!, value: String!, definition: String!): ID!
	removeAttribute(id: ID!): ID!
	removeAttributes(ids: [ID!]!): ID!
	createPort(code: String, label: String, description: String, icon: String, order: Int): ID!
	port(id: ID!): PortOperationInput!
	deletePort(id: ID!): ID!
	deletePorts(ids: [ID!]!): ID!
	addConnector(code: String!, description: String, icon: String, portId: ID): ID!
	connector(id: ID!): ConnectorOperationInput!
	removeConnector(id: ID!): ID!
	removeConnectors(ids: [ID!]!): ID!
}

type UnsavedChangeCommand {
	kit: OperationInput!
	save: ID!
}

type UpdatedConceptDescriptionInput {
	description: String!
}

type UpdatedConceptIconInput {
	icon: String!
}

type UpdatedPortDescriptionInput {
	description: String!
}

type UpdatedPortIconInput {
	icon: String!
}

type UpdatedQualityDescriptionInput {
	description: String!
}

type UpdatedQualityIconInput {
	icon: String!
}

type UpdatedTagDescriptionInput {
	description: String!
}

type UpdatedTagIconInput {
	icon: String!
}

type Vector implements Node {
	id: ID!
	hash: String!
	owner: OwnerEntity
	owns: OwnedEntityConnection
	x: Float!
	y: Float!
	z: Float!
}

type VectorEdge implements EntityEdge {
	cursor: String!
	node: Vector!
}

input VectorInput {
	x: Float!
	y: Float!
	z: Float!
}

interface Version {
	id: ID!
	hash: String!
	checkpoint: CheckpointConnection!
	latestWipCheckpointAncestor: Checkpoint
	savedChanges: ChangeConnection!
	unsavedChanges: ChangeConnection!
	kit: Kit!
}

type VersionCommand {
	startNewChange: ID!
	unsavedChange(id: ID!): UnsavedChangeCommand!
	save: ID!
	createCheckpoint(message: String!): ID!
}

type WriteVersion {
	id: ID!
	hash: String!
}

"""
Directs the executor to include this field or fragment only when the `if` argument is true.
"""
directive @include(if: Boolean!) on FIELD | FRAGMENT_SPREAD | INLINE_FRAGMENT
"""
Directs the executor to skip this field or fragment when the `if` argument is true.
"""
directive @skip(if: Boolean!) on FIELD | FRAGMENT_SPREAD | INLINE_FRAGMENT
schema {
	query: Query
	mutation: Mutation
	subscription: Subscription
}
