{
  "name": "semio/graphql",
  "bundleKind": "schema",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "cwd": "semio/schema/graphql",
        "command": "bun scripts/export-schema.ts"
      }
    }
  }
}
