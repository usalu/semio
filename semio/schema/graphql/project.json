{
  "name": "semio/graphql",
  "bundleKind": "schema",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["@semio/rs:build-wasm"],
      "cache": false,
      "options": {
        "command": "bun semio/schema/graphql/scripts/export-schema.ts"
      }
    }
  }
}
