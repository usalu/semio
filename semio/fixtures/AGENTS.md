---
technology: semio
bundle:
  name: fixtures
  emoji: 🧪
  description: Test and story JSON for semio algorithms and UI; not shipped in product bundles.
  kind: fixtures
---

# 🧾 Specification

Import via `@semio/fixtures` from tests, storybook, and dev harnesses only. Builtin kits, meshes, and icons live under `@semio/assets`.
