{
  "name": "@semio/icon",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "semio/asset/icon",
        "command": "bun -e \"console.log('@semio/icon delegates to @ui/asset; run build on ui/asset.')\"",
        "forwardAllArgs": true
      }
    }
  }
}
