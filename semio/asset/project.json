{
  "name": "@semio/asset",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "semio/asset",
        "command": "bun -e \"console.log('@semio/asset is a source-only workspace and does not require a build step.')\"",
        "forwardAllArgs": true
      }
    }
  }
}
