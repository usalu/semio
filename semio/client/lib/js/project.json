{
  "name": "@semio/js",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["@semio/rs:build"],
      "options": {
        "cwd": "semio/client/lib/js",
        "command": "bunx tsc --noEmit",
        "forwardAllArgs": true
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["@semio/rs:build"],
      "options": {
        "cwd": "semio/client/lib/js",
        "command": "bunx vitest run --config vite.config.ts",
        "forwardAllArgs": true,
        "env": {
          "SEMIO_JS_RUN_EMBEDDED_TESTS": "1"
        }
      }
    }
  }
}
