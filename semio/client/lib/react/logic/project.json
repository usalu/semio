{
  "name": "@semio/react",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["@semio/rs:build"],
      "options": {
        "cwd": "semio/client/lib/react/logic",
        "command": "bunx tsc --noEmit -p tsconfig.json",
        "forwardAllArgs": true
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["@semio/rs:build"],
      "options": {
        "cwd": "semio/client/lib/react/logic",
        "command": "bunx vitest run --config vite.config.ts --sequence.concurrent false index.tsx",
        "forwardAllArgs": true,
        "env": {
          "SEMIO_REACT_RUN_EMBEDDED_TESTS": "1",
          "SEMIO_JS_RUN_EMBEDDED_TESTS": "0",
          "NODE_OPTIONS": "--max-old-space-size=16384"
        }
      }
    }
  }
}
