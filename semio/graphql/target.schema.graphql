# Custom semio extension of the [relay specs](https://relay.dev/docs/guides/graphql-server-specification)

#region Scalars

scalar Timestamp

#endregion Scalars

#region Entities

interface Node {
  id: ID!
}

interface Entity implements Node { # Our own extension ontop of the relay specs
  id: ID!
  hash: String!
  owner: Entity
  owns: EntityConnection
}

interface EntityEdge {
  cursor: String!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String # start at this entity
  endCursor: String # end at this entity
}

interface EntityConnectionInterface {
  # edges: [ENTITYEdge]
  pageInfo: PageInfo!
  hash: String! # Our own extension ontop of the relay specs
}

union EntityConnection =
  | AlternativeConnection
  | AttributeDiffConnection
  | AttributeListingConnection
  | AuthorConnection
  | BridgeConnection
  | ChangeConnection
  | CheckpointConnection
  | ConnectorConnection
  | CoordinateConnection
  | DesignConnection
  | DraftConnection
  | DiffsConnection
  | FileConnection
  | FolderConnection
  | GraphConnection
  | KitConnection
  | LocationConnection
  | LocationDiffConnection
  | LocationModificationConnection
  | OffsetConnection
  | PieceListingConnection
  | PlaneConnection
  | PlaneDiffConnection
  | PlaneModificationConnection
  | PlaceConnection
  | PlaceDiffConnection
  | PlaceModificationConnection
  | PointConnection
  | PointDiffConnection
  | PositionConnection
  | PositionModificationConnection
  | RepresentationConnection
  | SessionConnection
  | TransactionConnection
  | TypeConnection
  | VectorConnection

#region Weak // Weak entities have hash as id

# interface WeakEntity implements Entity {
#   id: ID! # hash
# }

#region Diffs

# interface Modification implements WeakEntity {
# FIELD: NULLABLE FIELD // if the field was modified then return the modified value
# removeNULLABLEFIELD: Boolean // use this if the field is by default nullable to indicate that it was removed
# }

union Modification = PlaneModification | AttributeModification

union DiffOwner = Diffs | Change | Operation

interface Diff implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # DiffOwner
  owns: EntityConnection

  before: Entity!
  modification: Modification!
  after: Entity!
}

union DiffConnection = DiffsConnection | PlaneDiffConnection | PointDiffConnection | AttributeDiffConnection | LocationDiffConnection | PlaceDiffConnection

union DiffsOwner = Diff | Operation | Change

#endregion Diffs

type Diffs implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # DiffsOwner
  owns: EntityConnection

  removed: EntityConnection
  diffs: DiffConnection
  added: EntityConnection
}

type DiffsEdge implements EntityEdge {
  cursor: String!
  node: Diffs!
}

type DiffsConnection implements EntityConnectionInterface {
  edges: [DiffsEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#region Vector

input VectorInput {
  x: Float!
  y: Float!
  z: Float!
}

union VectorOwner = Plane | PlaneModification
union VectorOwned = Plane | PlaneModification

type VectorEdge implements EntityEdge {
  cursor: String!
  node: Vector!
}

type Vector implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # VectorOwner
  owns: EntityConnection # VectorOwned
  x: Float!
  y: Float!
  z: Float!
}

type VectorConnection implements EntityConnectionInterface {
  edges: [VectorEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Vector

#region Point

input PointInput {
  x: Float!
  y: Float!
  z: Float!
}

union PointOwner = Plane | PlaneModification
union PointOwned = Plane | PlaneModification

type PointEdge implements EntityEdge {
  cursor: String!
  node: Point!
}

type Point implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # PointOwner
  owns: EntityConnection

  x: Float!
  y: Float!
  z: Float!
}

type PointConnection implements EntityConnectionInterface {
  edges: [PointEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Point

#region Coordinate

input CoordinateInput {
  u: Float!
  v: Float!
}

union CoordinateOwner = Position | PositionModification
union CoordinateOwned = Position | PositionModification

type CoordinateEdge implements EntityEdge {
  cursor: String!
  node: Coordinate!
}

type Coordinate implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # CoordinateOwner
  owns: EntityConnection # CoordinateOwned
  u: Float!
  v: Float!
}

type CoordinateConnection implements EntityConnectionInterface {
  edges: [CoordinateEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Coordinate

#region Offset

input OffsetInput {
  u: Float!
  v: Float!
}

union OffsetOwner = Position | PositionModification
union OffsetOwned = Position | PositionModification

type OffsetEdge implements EntityEdge {
  cursor: String!
  node: Offset!
}

type Offset implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # OffsetOwner
  owns: EntityConnection # OffsetOwned
  u: Float!
  v: Float!
}

type OffsetConnection implements EntityConnectionInterface {
  edges: [OffsetEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Offset

#region Plane

input PlaneInput {
  origin: PointInput!
  xAxis: VectorInput!
  yAxis: VectorInput!
}

union PlaneOwner = Piece | PieceModification | PlaneDiff
union PlaneOwned = Point | Vector

type PlaneEdge implements EntityEdge {
  cursor: String!
  node: Plane!
}

type Plane implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # PlaneOwner
  owns: EntityConnection # PlaneOwned
  origin: Point!
  xAxis: Vector!
  yAxis: Vector!
}

type PlaneConnection implements EntityConnectionInterface {
  edges: [PlaneEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union PlaneModificationOwner = PieceModification | PositionModification | LocationModification | PlaceModification | AttributeModification | PlaneDiff
union PlaneModificationOwned = Point | Vector | Plane

type PlaneModificationEdge implements EntityEdge {
  cursor: String!
  node: PlaneModification!
}

type PlaneModification implements Entity { # implements Modification
  id: ID! # hash
  hash: String!
  owner: Entity # PlaneModificationOwner
  owns: EntityConnection

  origin: Point
  xAxis: Vector
  yAxis: Vector
}

type PlaneModificationConnection implements EntityConnectionInterface {
  edges: [PlaneModificationEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

type PlaneDiff implements Diff {
  id: ID! # hash
  hash: String!
  owner: Entity # DiffOwner
  owns: EntityConnection

  before: Plane!
  modification: PlaneModification!
  after: Plane!
}

type PlaneDiffEdge implements EntityEdge {
  cursor: String!
  node: PlaneDiff!
}

type PlaneDiffConnection implements EntityConnectionInterface {
  edges: [PlaneDiffEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Plane

#region Position

input PositionInput {
  center: CoordinateInput!
  plane: PlaneInput!
}

union PositionOwner = Piece | PositionModification | Plane | PositionDiff

type PositionEdge implements EntityEdge {
  cursor: String!
  node: Position!
}

type Position implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # PositionOwner
  owns: EntityConnection

  center: Coordinate!
  plane: Plane!
}

type PositionConnection implements EntityConnectionInterface {
  edges: [PositionEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union PositionModificationOwner = PieceModification | PositionDiff

type PositionModificationEdge implements EntityEdge {
  cursor: String!
  node: PositionModification!
}

type PositionModification implements Entity { # implements Modification
  id: ID! # hash
  hash: String!
  owner: Entity # PositionModificationOwner
  owns: EntityConnection
  center: Coordinate
  plane: Plane
}

type PositionModificationConnection implements EntityConnectionInterface {
  edges: [PositionModificationEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

type PositionDiff implements Diff {
  id: ID! # hash
  hash: String!
  owner: Entity
  owns: EntityConnection
  before: Entity! # Position
  modification: PositionModification!
  after: Entity! # Position
}

type PointDiffEdge implements EntityEdge {
  cursor: String!
  node: PositionDiff!
}

type PointDiffConnection implements EntityConnectionInterface {
  edges: [PointDiffEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Position

#region Location

input LocationInput {
  longitude: Float!
  latitude: Float!
  altitude: Float
}

union LocationOwner = Place | Kit | Design

type LocationEdge implements EntityEdge {
  cursor: String!
  node: Location!
}

type Location implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # LocationOwner
  owns: EntityConnection
  longitude: Float!
  latitude: Float!
  altitude: Float!
}

type LocationConnection implements EntityConnectionInterface {
  edges: [LocationEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union LocationModificationOwner = PlaceModification | LocationDiff

type LocationModificationEdge implements EntityEdge {
  cursor: String!
  node: LocationModification!
}

type LocationModification implements Entity { # implements Modification
  id: ID! # hash
  hash: String!
  owner: Entity # LocationModificationOwner
  owns: EntityConnection
  longitude: Float
  latitude: Float
  altitude: Float
}

type LocationModificationConnection implements EntityConnectionInterface {
  edges: [LocationModificationEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

type LocationDiff implements Diff {
  id: ID! # hash
  hash: String!
  owner: Entity
  owns: EntityConnection
  before: Entity! # Location
  modification: LocationModification!
  after: Entity! # Location
}

type LocationDiffEdge implements EntityEdge {
  cursor: String!
  node: LocationDiff!
}

type LocationDiffConnection implements EntityConnectionInterface {
  edges: [LocationDiffEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Location

#region Attribute

input AttributeInput {
  key: String!
  value: String
  definition: String
}

union AttributeOwner = Piece | Connector | Representation | Kit | Design | Type | AttributeDiff | PositionModification | PieceModification

type Attribute implements Entity { # implements WeakEntity
  id: ID! # hash
  hash: String!
  owner: Entity # AttributeOwner
  owns: EntityConnection
  key: String!
  value: String!
  definition: String!
}

union AttributeModificationOwner = PieceModification | Connector | Representation | Kit | Design | Type | AttributeDiff

type AttributeModification implements Entity { # implements Modification
  id: ID! # hash
  hash: String!
  owner: Entity # AttributeModificationOwner
  owns: EntityConnection
  key: String
  value: String
  definition: String
}

type AttributeDiff implements Diff {
  id: ID! # hash
  hash: String!
  owner: Entity
  owns: EntityConnection
  before: Entity! # Attribute
  modification: AttributeModification!
  after: Entity! # Attribute
}

type AttributeEdge implements EntityEdge {
  cursor: String!
  node: Attribute!
}

type AttributeDiffConnection implements EntityConnectionInterface {
  edges: [AttributeEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

type AttributeListingEdge implements EntityEdge {
  cursor: String!
  node: Attribute!
}

type AttributeListingConnection implements EntityConnectionInterface {
  edges: [AttributeListingEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union AttributesDiffOwner = Diff | Operation | Change

type AttributesDiff implements Diffs {
  id: ID! # hash
  hash: String!
  owner: Entity # AttributesDiffOwner
  owns: EntityConnection
  removed: EntityConnection # Attribute
  diffs: DiffConnection # AttributeDiffConnection
  added: EntityConnection # Attribute
}

#endregion Attribute

#endregion Weak

#region Strong

# interface StrongEntity implements Entity {
#   id: ID! # uuidv7
#   hash: String!
#   owner: Entity # ENTITYKIND
#   owns: EntityConnection # ENTITYKIND
# }

union Input = RenamedKitInput | ChangedDescriptionInput | CreatedFixedPieceInput | FixedPieceInput | DraggedPieceInput

union OperationOwner = Change

interface Operation implements Entity { # implements StrongEntity
  id: ID! # uuidv7
  hash: String!
  owner: Entity # OperationOwner
  owns: EntityConnection # Input
  diff: Diff!
  input: Input
}

#region Kit

#region Place

union PlaceOwner = Kit | Checkpoint | Graph

type PlaceEdge implements EntityEdge {
  cursor: String!
  node: Place!
}

type Place implements Entity { # implements StrongEntity
  id: ID!
  hash: String!
  owner: Entity! # PlaceOwner
  owns: EntityConnection
  name: String
  location: Location
}

type PlaceConnection implements EntityConnectionInterface {
  edges: [PlaceEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union PlaceModificationOwner = PlaceDiff | Place

type PlaceModificationEdge implements EntityEdge {
  cursor: String!
  node: PlaceModification!
}

type PlaceModification implements Entity { # implements Modification
  id: ID! # hash
  hash: String!
  owner: Entity # PlaceModificationOwner
  owns: EntityConnection
  name: String
  location: LocationModification
  removeLocation: Boolean
}

type PlaceModificationConnection implements EntityConnectionInterface {
  edges: [PlaceModificationEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

type PlaceDiff implements Diff {
  id: ID! # hash
  hash: String!
  owner: Entity
  owns: EntityConnection
  before: Place!
  modification: PlaceModification!
  after: Place!
}

type PlaceDiffEdge implements EntityEdge {
  cursor: String!
  node: PlaceDiff!
}

type PlaceDiffConnection implements EntityConnectionInterface {
  edges: [PlaceDiffEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Place

#region Folder

union FolderOwner = Kit | Folder
union FolderOwned = Folder | File | Design | Type

type FolderEdge implements EntityEdge {
  cursor: String!
  node: Folder!
}

type Folder implements Entity { # implements StrongEntity
  id: ID!
  hash: String!
  owner: Entity! # FolderOwner
  owns: EntityConnection # FolderOwned
  name: String!
  path: String!
  description: String!
}

type FolderConnection implements EntityConnectionInterface {
  edges: [FolderEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Folder

#region File

union FileOwner = Kit | Folder

type FileEdge implements EntityEdge {
  cursor: String!
  node: File!
}

type File implements Entity { # implements StrongEntity
  id: ID!
  hash: String!
  owner: Entity! # FileOwner
  owns: EntityConnection # Nothing
  url: String!
  mime: String
  size: Int
  description: String
  created: Timestamp
  updated: Timestamp
}

type FileConnection implements EntityConnectionInterface {
  edges: [FileEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion File

#region Author

union AuthorOwner = Kit | Checkpoint | Graph | Session

type AuthorEdge implements EntityEdge {
  cursor: String!
  node: Author!
}

type Author implements Entity {
  id: ID!
  hash: String!
  owner: Entity # AuthorOwner
  owns: EntityConnection
  name: String!
  email: String!
  role: String
  rank: Int
  drafts: [Draft!]!
  transactions: [Transaction!]!
  checkpoints: [Checkpoint!]!
}

type AuthorConnection implements EntityConnectionInterface {
  edges: [AuthorEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Author

#region Prop

type Prop {
  id: ID!
  key: String!
  value: String!
  unit: String
  quality: Quality
}

#endregion Prop

#region Benchmark

type Benchmark {
  id: ID!
  name: String!
  min: Float
  max: Float
  minExcluded: Boolean
  maxExcluded: Boolean
}

#endregion Benchmark

#region Quality

type Quality {
  id: ID!
  key: String!
  value: String
  unit: String
  definition: String
  description: String
  benchmarks: [Benchmark!]!
}

#endregion Quality

#region Tag

type Tag {
  id: ID!
  name: String!
  order: Int
}

#endregion Tag

#region Concept

type Concept {
  id: ID!
  name: String!
  description: String
  order: Int
}

#endregion Concept

#region Stat

type Stat {
  id: ID!
  key: String!
  value: String!
  unit: String
  description: String
}

#endregion Stat

#region Type

union ConnectorOwner = Type

type ConnectorEdge implements EntityEdge {
  cursor: String!
  node: Connector!
}

type Connector implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # ConnectorOwner
  owns: EntityConnection
  code: String!
  description: String
  portId: ID
  qualities: [Quality!]!
  attributes: [Attribute!]!
}

type ConnectorConnection implements EntityConnectionInterface {
  edges: [ConnectorEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union RepresentationOwner = Type

type RepresentationEdge implements EntityEdge {
  cursor: String!
  node: Representation!
}

type Representation implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # RepresentationOwner
  owns: EntityConnection
  url: String!
  description: String
  file: File
  tags: [Tag!]!
  qualities: [Quality!]!
  attributes: [Attribute!]!
}

type RepresentationConnection implements EntityConnectionInterface {
  edges: [RepresentationEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union TypeOwner = Kit

type TypeEdge implements EntityEdge {
  cursor: String!
  node: Type!
}

type Type implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # TypeOwner
  owns: EntityConnection
  name: String!
  description: String
  icon: String
  image: String
  unit: String
  created: Timestamp
  updated: Timestamp
  connectors: [Connector!]!
  connector(id: ID!): Connector
  representations: [Representation!]!
  representation(id: ID!): Representation
  bestRepresentation(tagIds: [ID!]!): Representation
  authors: [Author!]!
  concepts: [Concept!]!
  tags: [Tag!]!
  qualities: [Quality!]!
  props: [Prop!]!
  attributes: [Attribute!]!
  stats: [Stat!]!
}

type TypeConnection implements EntityConnectionInterface {
  edges: [TypeEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Type

#region Design

#region Layer

type Layer {
  id: ID!
  name: String!
  description: String
  color: String
  order: Int
  visible: Boolean
  locked: Boolean
}

#endregion Layer

#region Group

type Group {
  id: ID!
  name: String!
  description: String
  color: String
  icon: String
  pieces: [Piece!]!
}

#endregion Group

#region Piece

enum PieceConnectionKind {
  FIXED
  CONNECTED
}

union Blueprint = Type | Design

union PieceOwner = Type | Design | PieceModification | Piece

type PieceListingEdge implements EntityEdge {
  cursor: String!
  node: Piece!
}

type Piece implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # PieceOwner
  owns: EntityConnection
  name: String
  description: String
  position: Position
  scale: Float
  blueprint: Blueprint!
  connectionKind: PieceConnectionKind
  flatPosition: Position!
  replaceableBlueprint: [Blueprint!]!
  parentConnection: Connection
  childConnections: [Connection!]!
  parentPiece: Piece
  childPieces: [Piece!]!
  depth: Int!
  path: [Piece!]!
  props: [Prop!]!
  attributes: [Attribute!]!
}

type PieceListingConnection implements EntityConnectionInterface {
  edges: [PieceListingEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union PieceBridgeOwner = Design

type BridgeEdge implements EntityEdge {
  cursor: String!
  node: Connection!
}

type Connection implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # PieceBridgeOwner
  owns: EntityConnection
  connected: Side!
  connecting: Side!
  gap: Float
  shift: Float
  rise: Float
  rotation: Float
  turn: Float
  tilt: Float
  u: Float
  v: Float
  description: String
  attributes: [Attribute!]!
}

type BridgeConnection implements EntityConnectionInterface {
  edges: [BridgeEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#region Operations

type CreatedFixedPieceInput {
  designId: ID!
  blueprintId: ID!
  position: Position!
  name: String
  description: String
}

type CreatedFixedPiece implements Operation & Entity {
  id: ID!
  hash: String!
  owner: Entity! # CreatedFixedPieceOwner
  owns: EntityConnection
  input: CreatedFixedPieceInput!
  diff: Diff!
  piece: Piece!
}

union CreatedFixedPieceOwner = Change

type FixedPieceInput {
  designId: ID!
  pieceId: ID!
}

type FixedPiece implements Operation & Entity {
  id: ID!
  hash: String!
  owner: Entity! # FixedPieceOwner
  owns: EntityConnection
  input: FixedPieceInput!
  diff: Diff!
  piece: Piece!
}

union FixedPieceOwner = Change

type DraggedPieceInput {
  designId: ID!
  pieceIds: [ID!]!
  offset: Offset!
}

type DraggedPiece implements Operation & Entity {
  id: ID!
  hash: String!
  owner: Entity! # DraggedPieceOwner
  owns: EntityConnection
  input: DraggedPieceInput!
  diff: Diff!
  pieces: [Piece!]!
}

union DraggedPieceOwner = Change

#endregion Operations

#endregion Piece

#region Connection

#region Side

type Side {
  id: ID!
  piece: Piece!
  port: Connector
  designPiece: Piece
  connector: Connector
}

#endregion Side

#endregion Connection

union DesignOwner = Kit

type DesignEdge implements EntityEdge {
  cursor: String!
  node: Design!
}

type Design implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # DesignOwner
  owns: EntityConnection
  name: String!
  description: String
  icon: String
  image: String
  place: Place
  unit: String
  created: Timestamp
  updated: Timestamp
  pieces: [Piece!]!
  piece(id: ID!): Piece
  connections: [Connection!]!
  connection(id: ID!): Connection
  layers: [Layer!]!
  groups: [Group!]!
  authors: [Author!]!
  concepts: [Concept!]!
  tags: [Tag!]!
  qualities: [Quality!]!
  props: [Prop!]!
  attributes: [Attribute!]!
  stats: [Stat!]!
  qualitySum(qualityId: ID!): Float!
  references: [Design!]!
  referencedBy: [Piece!]!
}

type DesignConnection implements EntityConnectionInterface {
  edges: [DesignEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#endregion Design

union KitOwner = Graph | Checkpoint | Alternative

type KitEdge implements EntityEdge {
  cursor: String!
  node: Kit!
}

type Kit implements Entity {
  id: ID!
  hash: String!
  owner: Entity # KitOwner
  owns: EntityConnection
  checkpoint: Checkpoint
  draft: Draft
  transaction: Transaction
  name: String!
  description: String
  icon: String
  image: String
  preview: String
  remote: String
  homepage: String
  license: String
  uri: String
  created: Timestamp
  updated: Timestamp
  version: String
  design(id: ID!): Design
  designs: [Design!]!
  type(id: ID!): Type
  types: [Type!]!
  files: [File!]!
  folders: [Folder!]!
  authors: [Author!]!
  concepts: [Concept!]!
  tags: [Tag!]!
  qualities: [Quality!]!
  props: [Prop!]!
  attributes: [Attribute!]!
  stats: [Stat!]!
}

type KitConnection implements EntityConnectionInterface {
  edges: [KitEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

#region Operations

type RenamedKitInput {
  name: String!
}

type RenamedKit implements Operation & Entity {
  id: ID!
  hash: String!
  owner: Entity! # RenamedKitOwner
  owns: EntityConnection
  input: RenamedKitInput!
  diff: Diff!
  kit: Kit!
}

union RenamedKitOwner = Change

type ChangedDescriptionInput {
  entityId: ID!
  description: String!
}

type ChangedDescription implements Operation & Entity {
  id: ID!
  hash: String!
  owner: Entity! # ChangedDescriptionOwner
  owns: EntityConnection
  input: ChangedDescriptionInput!
  diff: Diff!
  entity: Entity!
}

union ChangedDescriptionOwner = Change

#endregion Operations

#endregion Kit

#endregion Entities

#region VCS

union ChangeOwner = Transaction | Draft | Checkpoint

type ChangeEdge implements EntityEdge {
  cursor: String!
  node: Change!
}

type Change implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # ChangeOwner
  owns: EntityConnection
  description: String
  origin: String
  forwards: [Operation!]!
  backwards: [Operation!]!
  sequenceNumber: Int!
}

type ChangeConnection implements EntityConnectionInterface {
  edges: [ChangeEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union TransactionOwner = Draft

type TransactionEdge implements EntityEdge {
  cursor: String!
  node: Transaction!
}

type Transaction implements Entity {
  id: ID!
  hash: String!
  owner: Entity # TransactionOwner
  owns: EntityConnection
  changes: [Change!]!
}

type TransactionConnection implements EntityConnectionInterface {
  edges: [TransactionEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union DraftOwner = Author

type DraftEdge implements EntityEdge {
  cursor: String!
  node: Draft!
}

type Draft implements Entity {
  id: ID!
  hash: String!
  owner: Entity! # DraftOwner
  owns: EntityConnection
  parentCheckpoint: Checkpoint
  targetAlternative: Alternative
  openTransaction: Transaction
  finalizedTransactions: [Transaction!]!
  redoTransactions: [Transaction!]!
  changes: [Change!]!
  canUndo(steps: Int!): Boolean!
  canRedo(steps: Int!): Boolean!
}

type DraftConnection implements EntityConnectionInterface {
  edges: [DraftEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union CheckpointOwner = Alternative | Graph | Kit | Author

type CheckpointEdge implements EntityEdge {
  cursor: String!
  node: Checkpoint!
}

type Checkpoint implements Entity {
  id: ID!
  hash: String!
  owner: Entity # CheckpointOwner
  owns: EntityConnection
  timestamp: Timestamp
  authors: [Author!]!
  root: Kit
  parentCheckpoint: Checkpoint
  message: String
  isRelease: Boolean!
  changeCount: Int!
}

type CheckpointConnection implements EntityConnectionInterface {
  edges: [CheckpointEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union AlternativeOwner = Graph

type AlternativeEdge implements EntityEdge {
  cursor: String!
  node: Alternative!
}

type Alternative implements Entity {
  id: ID!
  hash: String!
  owner: Entity # AlternativeOwner
  owns: EntityConnection
  name: String!
  start: Checkpoint!
  checkpoints: [Checkpoint!]!
  store: Kit!
  draft: Draft
  transaction: Transaction
}

type AlternativeConnection implements EntityConnectionInterface {
  edges: [AlternativeEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union GraphOwner = Session

type GraphEdge implements EntityEdge {
  cursor: String!
  node: Graph!
}

type Graph implements Entity {
  id: ID!
  hash: String!
  owner: Entity # GraphOwner
  owns: EntityConnection
  theKit: Kit
  alternative(id: ID!): Alternative
  alternatives: [Alternative!]!
  checkpoint(id: ID!): Checkpoint
  checkpoints: [Checkpoint!]!
  release(id: ID!): Checkpoint
  releases: [Checkpoint!]!
}

type GraphConnection implements EntityConnectionInterface {
  edges: [GraphEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

union SessionOwner = Graph | Checkpoint | Alternative

type SessionEdge implements EntityEdge {
  cursor: String!
  node: Session!
}

type Session implements Entity {
  id: ID!
  hash: String!
  owner: Entity # SessionOwner
  owns: EntityConnection
  startedAt: Timestamp
  drafts: [Draft!]!
}

type SessionConnection implements EntityConnectionInterface {
  edges: [SessionEdge!]!
  pageInfo: PageInfo!
  hash: String!
}

type ReadVersion {
  checkpoint: Checkpoint
  change: Change
  operation: Operation
}

type WriteVersion {
  draft: Draft
  transaction: Transaction
  checkpoint: Checkpoint
  change: Change
  operation: Operation
}

type Conflict {
  id: ID!
  authoritativeVersion: ReadVersion
  transactionVersion: WriteVersion
  reason: String!
  createdAt: Timestamp!
}

#endregion VCS

#region Schema

type Query {
  session: Session!
  wip: Graph!
  authoritative: Graph
  conflicts: [Conflict!]!
  node(id: ID!): Node
  entity(id: ID!): Entity
}

type Mutation {
  renameKit(draftId: ID!, transactionId: ID!, name: String!): ID!
  changeDescription(draftId: ID!, transactionId: ID!, description: String!): ID!
  createFixedPiece(draftId: ID!, transactionId: ID!, designId: ID!, position: PositionInput!, name: String, description: String): ID!
  fixPiece(draftId: ID!, transactionId: ID!, designId: ID!, pieceId: ID!): ID!
}

type Subscription {
  commandSucceeded: Command!
  operationSucceeded: Operation!
  operationFailed: Error!
  kitRenamed: RenamedKit!
  descriptionChanged: ChangedDescription!
  createdFixedPiece: CreatedFixedPiece!
  fixedPiece: FixedPiece!
  draggedPiece: DraggedPiece!
  error: Error!
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

#endregion Schema
