{
  "title": "Fix Puzzle 2D Performance Regression",
  "emoji": "🐢",
  "status": "closed",
  "description": "Puzzle 2D board used to be very performant at the `premigration` git tag; after the migration from the bespoke `puzzle/2d/react/index.tsx` renderer to the generic framework shell + Rust/WASM BoardHost pipeline, zoom and selection now take multiple seconds. Root-cause refactor per plan at /Users/ueli/.claude/plans/puzzle-2d-used-to-piped-balloon.md: (1) restore rAF-coalesced rendering in framework/renderer/react/components/puzzle-2d-board-host.tsx (no more synchronous renderFrame()/dispatch per raw pointer/wheel event), (2) stop hover setters from bumping content_scene_generation in mathematical/graph/port/directed/normal/rs/lib.rs, (3) stop puzzle/plugin/rs/lib.rs from re-stringifying the whole fixture 3x per action, (4) parallelize framework/renderer/react/os-shell.tsx refreshUi()'s sequential awaits, (5) add spatial partitioning to hit-testing in the graph engine.",
  "summary": "Root-caused the puzzle 2D zoom/selection regression to four compounding issues introduced/left over from the premigration→framework-shell migration: (1) puzzle-2d-board-host.tsx called session.renderFrame() and dispatch(\"setCamera\") synchronously on every raw wheel/pointermove event with no rAF-coalescing or settle-debounce (premigration had this via scheduleInputInvalidate()); (2) every dispatched action re-ran os-shell.tsx's refreshUi(), sequentially awaiting plugin.render() for all 3 puzzle2d panes + every panel tab + tools + engagements + measures + labels; (3) puzzle/plugin/rs's puzzle2d_board_scene() unconditionally re-serialized the whole fixture to JSON on every pane render (3x per action); (4) BoardHost's set_hovered_id/set_hovered_kind/set_hovered_id_silent/set_highlighted_ids bumped content_scene_generation on every hover change, forcing a full icon/base-layer rebuild even though hover only affects the always-fresh overlay layers (confirmed inconsistent with the already-correct, already-tested selection-does-not-bump-generation behavior).\n\nFixed: added scheduleRender() rAF-coalescing plus a pendingCameraDispatchRef debounced through the existing 350ms camera-settle timer in puzzle-2d-board-host.tsx; parallelized os-shell.tsx refreshUi()'s render/tools/engagements/measures/labels calls via Promise.all; added an fnv1a-hash-keyed fixture_json cache (cached_fixture_json) in puzzle/plugin/rs/lib.rs so the 3 panes of one action reuse one serialized string; removed the four hover/highlight bump_content_scene_generation() calls in mathematical/graph/port/directed/normal/rs/lib.rs. Added a puzzle2d_board_scene cache-reuse test in puzzle/plugin/rs/lib.rs and a hover-does-not-bump-generation test in puzzle/2d/rs/lib.rs (mirroring the existing selection test). Verified: mathematical_graph_port_directed_normal compiles clean; framework/renderer/react's full suite (97 tests) and lint pass; scoped tsc check shows no new type errors. Full workspace cargo build is currently blocked by unrelated concurrent changes from other live sessions (vcs/framework-os API mismatch, a missing protocol/plugin/rs workspace member) — not caused by this ticket's changes, left untouched per the no-fix-others'-WIP rule.\n\nSpun off a follow-up (spatial index for BoardHost hit-testing — pre-existing O(n)/O(n·m) linear scans, not part of this regression) as a separate suggested task rather than bundling it into this ticket.",
  "github": {
    "issue": "https://github.com/usalu/semio/issues/1958"
  },
  "goal": "🎯r2602🎯runningsketchpad",
  "sessions": [
    "⚪025e91b8ed06440d8fc4d3bee6eed9b8"
  ]
}