{
  "title": "Pack Binary Document Layer Across All Apps",
  "emoji": "📦",
  "status": "open",
  "description": "Introduce a new binary document layer (\"pack\") used by every app to store the initial document and for export/import, while keeping the existing text DSL layer (debugging, LLMs) with guaranteed bidirectional dsl\u003c-\u003epack conversion. The op-log based VCS materialization (vcs::OpText, .ops files, replay) stays completely unchanged.\n\nFull design is at /Users/ueli/.claude/plans/introduce-a-new-layer-valiant-diffie.md (already approved by the user in plan mode). Summary:\n\n- New crate family under pack/ (pack_core, pack_format, pack_value, pack_io, pack_async, pack_http, pack_index, pack facade, pack_testkit, pack_cli) implementing a general-purpose, gigabyte-scale binary document container: superblock/header, footer-based root discovery, self-describing segment framing with 64-bit lengths, canonical encoding mode, CRC-32C + blake3 content hashing, unknown-field/segment preservation, chunked blobs, streaming write, deflate compression, three verification levels, corruption-hardened decode, and a testkit law suite.\n- pack_value encodes/decodes dsl_schema's (RecordSpec, RecordValue) value model (stable u16 field ids) directly, so dsl text and pack bytes are two projections of the same RecordValue -- bidirectionality is structural, not just tested.\n- vcs gains a new `DocumentPack` trait (mirroring the existing `DocumentDsl` trait) plus a `serde_json::Value` bridge impl, DocumentPackFiles, print_document_pack/parse_document_pack, FolderTextStorage/FolderSqliteStorage pack support, and a runtime DocumentCodec registry used to fix framework/sync's type-erased FolderEndpoint.\n- dsl_derive's DslDocument derive macro is extended to also emit `impl ::vcs::DocumentPack`, so all ~49 existing document kinds across 33 plugins gain pack support purely by recompiling -- zero per-app codec code.\n- framework/plugin, the WIT ABI, the wasmtime host, the browser jco bridge, the OS shell (export/import commands), and framework/sync all get pack-aware wiring.\n- Every app (~24 family groupings covering 49 document kinds) gets: codec registration, new assert_dsl_pack_equivalence/assert_document_pack_round_trip test calls beside its existing DSL round-trip tests, and any remaining JSON example fixtures hand-converted to DSL text.\n\nExecution is a 4-wave parallel-agent rollout (wave 0: pack crates in parallel; wave 1: single-agent critical-section integration into vcs/dsl_derive/framework/script.ts; wave 2: ~24 parallel per-app-family agents with zero file overlap; wave 3: single-agent verification sweep). See the plan file for the full file-by-file breakdown, byte-level container format spec, API signatures, and per-agent task templates.",
  "github": {
    "issue": "https://github.com/usalu/semio/issues/2356"
  },
  "goal": "🎯r2602🎯runningsketchpad",
  "sessions": [
    "⚪9a55d6cc0da141c2b5b9c521897ea7af"
  ]
}