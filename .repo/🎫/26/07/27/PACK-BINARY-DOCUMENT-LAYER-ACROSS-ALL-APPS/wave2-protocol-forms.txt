WAVE 2 — Family: protocol-forms
================================================================================
Directories touched: protocol/**, forms/**

--------------------------------------------------------------------------------
1. DOCUMENT KINDS COVERED
--------------------------------------------------------------------------------

ProtocolSpec (protocol/rs/lib.rs)
  App: ProtocolPlayApp (protocol/plugin/rs/lib.rs)
  Schema: PROTOCOL_DOCUMENT_SCHEMA = "protocol.program"
  Codec registered: register_document_codec_for_app::<ProtocolPlayApp>(PROTOCOL_DOCUMENT_SCHEMA)
    in new fn register_protocol_play_exports(), called at the top of protocol_play_bundle()
    (protocol/plugin/rs/lib.rs had no semio_plugin!{setup:...} hook before this — it used the
    bare plugin_exports!(protocol_play_bundle) form, so a small register fn was added following
    the sibling-app pattern already used in note/animate/imperative/fem/lowpoly/layout plugins).

FormSpec (protocol::ProtocolSpec re-exported under forms' name, forms/rs/lib.rs — forms has no
own document type, it rides protocol's, per family brief)
  App: FormsPlayApp (forms/plugin/rs/lib.rs)
  Schema: FORMS_DOCUMENT_SCHEMA = "forms.form"
  Codec registered: register_document_codec_for_app::<FormsPlayApp>(FORMS_DOCUMENT_SCHEMA)
    added inside the ALREADY-EXISTING (previously empty) register_forms_exports() fn, which is
    already wired as semio_plugin!{ setup: register_forms_exports, ... }'s setup hook.

ModuleRenderPayload / procmodule (protocol/module/procedural/rs/lib.rs)
  App: ModuleApp
  Schema: MODULE_DOCUMENT_SCHEMA = "protocol.module.procedural.payload"
  Codec registered: register_document_codec_for_app::<ModuleApp>(MODULE_DOCUMENT_SCHEMA) in new
    fn register_module_exports(), called at the top of module_bundle() (same reasoning as
    protocol-play: no setup hook existed here before).

--------------------------------------------------------------------------------
2. TEST LAW CALLS ADDED (sibling to every existing assert_dsl_round_trip /
   assert_document_text_round_trip call for these document kinds)
--------------------------------------------------------------------------------

protocol/rs/lib.rs (mod tests, //#region 🔖DslAndOpText):
  - empty_protocol_projection_dsl_round_trips: added assert_dsl_pack_equivalence(&empty_protocol_projection())
  - sample_spec_dsl_round_trips: added assert_dsl_pack_equivalence(&sample_spec())
  - document_text_round_trips_after_applied_operations: added assert_document_pack_round_trip(&store)

forms/rs/lib.rs (mod tests):
  - building_component_fixture_dsl_round_trips: added assert_dsl_pack_equivalence(&spec) (same
    include_str!-loaded building-component.forms fixture as the existing assert_dsl_round_trip
    call); widened the `use vcs::test_support::{...}` import to bring in assert_dsl_pack_equivalence.

protocol/module/procedural/rs/lib.rs (mod tests, //#region 🔖DslAndOpText):
  - module_render_payload_dsl_round_trips: added assert_dsl_pack_equivalence(&default_payload())
    (module_payload_operation_op_text_round_trips is an OpText-only test, not a DSL/document-text
    round trip, so per the brief it gets no pack-law sibling call)

--------------------------------------------------------------------------------
3. FIXTURES
--------------------------------------------------------------------------------

forms/example/building-component.forms.json -> already absent; only forms/example/building-
component.forms (DSL text) exists and is the fixture `forms/rs/lib.rs` and `forms/plugin/rs/
lib.rs`'s BUILDING_COMPONENT_EXAMPLE_TEXT both include_str! from. No JSON fixture was present to
convert — this conversion evidently happened before this session (wave 0/1 or concurrent churn).
Nothing left to do here.

--------------------------------------------------------------------------------
4. PACK-COMPLETENESS LINT ALLOWLIST — family entries now satisfied
--------------------------------------------------------------------------------

Remove these 3 entries from root script.ts's POLICY_PACK_COMPLETENESS_ALLOWLIST (wave 3):
  forms/rs/lib.rs
  protocol/module/procedural/rs/lib.rs
  protocol/rs/lib.rs

(protocol/plugin/rs/lib.rs and forms/plugin/rs/lib.rs were never in the allowlist — they are app-
plugin crates with no DSL round-trip law tests of their own, only the codec-registration call was
needed there.)

--------------------------------------------------------------------------------
5. CARGO TEST STATUS
--------------------------------------------------------------------------------

cargo check -p protocol -p protocol-plugin -p protocol-module-procedural -p forms -p forms-plugin: GREEN, no errors.

cargo test -p protocol: 21 passed, 0 failed.
cargo test -p protocol-plugin: 9 passed, 0 failed.
cargo test -p forms: 5 passed, 0 failed.
cargo test -p protocol-module-procedural: 11 passed, 3 FAILED (pre-existing, unrelated).
cargo test -p forms-plugin: 21 passed, 10 FAILED (pre-existing, unrelated).

The 13 failures above are NOT caused by this ticket's changes. All but one panic at
ui/wgpu/rs/../../../ui/asset/icon/generated/icon_name.rs:1380 with "invalid catalog icon name"
(e.g. icon id "building" used in protocol/module/procedural's ProtocolBlockKind contribution isn't
present in the generated icon catalog at all — zero occurrences of "building" in icon_name.rs).
git status shows ui/styling/**, ui/wgpu/rs/lib.rs, and other icon/token generation files are
currently dirty/mid-edit in the working tree from another concurrent session (matches this repo's
known "concurrent cargo workspace churn" pattern) — this is an icon-catalog desync bug, entirely
unrelated to DocumentPack, pre-existing in code this session did not touch, and outside this
family's allowed directories (ui/asset/**) to fix. One remaining forms-plugin failure
(try_wizard_gates_navigation_and_reports_inline_errors, asserting `"disabled":true` is present in
rendered JSON) is also unrelated to any diff in this session — no try-wizard/disabled-state code
was touched.

--------------------------------------------------------------------------------
6. FILES TOUCHED
--------------------------------------------------------------------------------
  protocol/rs/lib.rs
  protocol/plugin/rs/lib.rs
  protocol/module/procedural/rs/lib.rs
  forms/rs/lib.rs
  forms/plugin/rs/lib.rs
