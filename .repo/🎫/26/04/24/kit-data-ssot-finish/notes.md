# Kit data SSOT finish

- Added read commands: design replaceable catalog, included design ids, piece parent connection full DTO.
- React: replaceable + explodeable hooks use rs reads only; sketchpad SQL symbols re-exported from `@compose/react`.
