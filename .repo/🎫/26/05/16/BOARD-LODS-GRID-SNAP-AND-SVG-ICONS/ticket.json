{
  "title": "Board Lods Grid Snap And Svg Icons",
  "emoji": "🗺️",
  "status": "closed",
  "description": "Board: four discrete LODs (minimap/overview/normal/detail) with configurable zoom thresholds as props; per-LOD grid (none / 10 / 10+5 / 10+5+1), node stroke/fill, edge weight, handles, labels vs abbreviated vs vello_svg icons; grid snap to visible grid lines with toolbar toggle; integrate vello_svg for detail node icons from metabolism example SVGs.",
  "summary": "Runtime node iconKind: Rust resolves catalog keys or inline SVG/XML strings before usvg/Vello paint; WASM path unchanged for cache key. TypeScript: four-band LOD thresholds (BoardLodZoomThresholds), resolveBoardLodLabelFromThresholds, BoardRenderer lod/grid WASM sync (setLodZoomThresholdsJson, setGridSnapEnabled), descriptor iconKind, overlay captions by LOD (minimap off, overview abbreviated, detail skips when icon set), Node iconKind + fixture parse. React: BoardCanvas props, play toolbar grid snap + DEFAULT thresholds. Restored WASM fixture layout API: layoutBoardFixtureForceGraph via boardRedrawLayoutFixtureJson; exported layoutBoardFixtureRedrawNodes/Handles and redraw option kinds for play. Vitest 54 passed; play Vite build succeeded. Native cargo test on Windows still blocked by wgpu/windows duplicate (pre-existing).",
  "goal": "🎯r2602🎯runningsketchpad",
  "sessions": [
    "⚪5c9f973215db4264aa7cf18c27dd77d5"
  ]
}