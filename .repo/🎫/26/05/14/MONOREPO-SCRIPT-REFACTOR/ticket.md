# Monorepo Script Refactor

**Status:** In progress

- Root `*.ts` runners replace inline `package.json` / `project.json` command strings.
- Platform `setup.*` / `start.*` scripts for zero-touch bootstrap and IDE session hooks.
- Removed unused `clean` target defaults from `nx.json`.
- Fixed `@semio/3dm-ui` dev command path depth to repo-root runners.

Repo MCP unavailable; ticket tracked manually.
