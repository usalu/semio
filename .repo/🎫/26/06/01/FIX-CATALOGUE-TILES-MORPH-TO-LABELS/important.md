## Picture auto-animate (cover, no stretch)

- `figureTileBackgroundStyle` now cover-fills each frame from crop + frame aspect ratio (uniform zoom, center focal point).
- `<img class="presentation-media-figure">` uses `object-fit: cover` on `section[data-auto-animate]` slides.

Catalogue many-to-one morph / settle ordering is still open per ticket description.
