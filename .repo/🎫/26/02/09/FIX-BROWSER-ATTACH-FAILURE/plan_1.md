# Plan

1. Reproduce and isolate the browser attach failure source in workspace launch/debug config.
2. Replace legacy VS Code browser debugger configuration with supported built-in debugger type.
3. Update root developer documentation (`README.md`, `AGENTS.md`) with the enforced debugger requirement.
4. Validate configuration changes and finalize ticket summary with touched files.
