# Plan

1. Reopen and continue the same ticket after persistent attach failures.
2. Replace browser-attach debug launch profiles with attach-free Node launches.
3. Use `serverReadyAction` with `openExternally` for one-click URL opening once dev servers are ready.
4. Update root docs to codify attach-free launch behavior for remote/devcontainer sessions.
5. Finalize ticket summary and close with all touched files.
