1. Remove leftover alternate toolbar-path implementation and keep the existing group-based toolbar flow.
2. Adjust Selection dropdown placement to render directly above the Selection toolbar tool as a fixed-width vertical list.
3. Keep Selection subtools as `select` and `hand` while showing additive/subtractive controls in the Tool Setting Bar content for both Selection subtools.
4. Update README.md and AGENTS.md toolbar documentation for the dropdown placement contract.
5. Record summary in ticket.md and close the ticket with touched files.
