# Ticket

## Todos

## Changes

## Log

## Summary

Fixed goal tree to display parent-child hierarchy from slash-separated goal IDs and sort by due date then creation date. Added createdAt field to Goal GraphQL schema. Rewrote formatGoalTree to build goal hierarchy, sort goals, and render nested tree with proper connectors.
