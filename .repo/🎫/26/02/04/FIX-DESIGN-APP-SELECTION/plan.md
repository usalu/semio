# Plan

- Reproduce and trace scene window model selection path (R3F Select + mesh/group ancestry metadata).
- Identify why model/object picks are not resolving to selected piece ids in scene selection sync.
- Implement a focused fix in `js/compose/sketchpad/Design.tsx` so scene model picks consistently map to piece guids.
- Validate with tests/checks.
- Update `README.md` and `AGENTS.md` documentation for finalized scene selection behavior.
- Update ticket log/summary and close the ticket with touched files.
