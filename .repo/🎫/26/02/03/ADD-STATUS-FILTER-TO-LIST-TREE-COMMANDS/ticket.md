# Ticket

## Summary

Added status filter flags (--open, --closed, --status) to all list and tree commands (ticket, goal, bundle). For bundles, 'open' filters to bundles that have at least one open ticket. Also added 'bundle' field to Goal struct with --bundle flag in goal open command and GraphQL schema support.
## Changes

## Log

## Todos

## Plan
