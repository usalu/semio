import re

FILE = "/workspaces/semio/semio/js/sketchpad/shared.ts"

with open(FILE, "r") as f:
    lines = f.readlines()

section_summaries = {
    "Imports": "// MUST import XState, Y.js, and semio core types for shared sketchpad infrastructure.",
    "YPath Types": "// MUST define path segment and path types for navigating Y.js document structures.",
    "Granular Hook Types": "// MUST define hook result tuples and field abstractions for granular reactive state access.",
    "Standard Empty Constants": "// MUST provide frozen singleton constants for empty collections and default panel visibility.",
    "Generic Diff Types": "// MUST define generic array and selection diff types with apply and inverse operations.",
    "Enums": "// MUST enumerate theme, expertise, mode, store status, tool, window, and panel kinds.",
    "File Provider": "// MUST define file storage provider interfaces for upload, download, and delete operations.",
    "App IDs": "// MUST define identifier interfaces for design, kit, type, and quality app scopes.",
    "Panel": "// MUST define panel kind configurations, visibility, sizing, sections, and definition interfaces.",
    "App Registry": "// MUST define route segment and app configuration interfaces for app registration.",
    "Sketchpad State": "// MUST define mutable and immutable sketchpad state interfaces with diff types.",
    "Commands": "// MUST define command context and result interfaces for kit and sketchpad operations.",
    "Store": "// MUST define store state, app step, edit, diff, and command result interfaces.",
    "Complete State": "// MUST define the complete aggregated state interface for the entire sketchpad.",
    "Window": "// MUST define window configuration, control, layout parsing, and default layout creation.",
    "Tool": "// MUST define tool interfaces for selection, lasso, connector, and hand interactions.",
    "Focus": "// MUST define the focus item interface for search and navigation targets.",
    "Footer": "// MUST define the footer item interface for status bar entries.",
    "Panel Props": "// MUST define resizable panel props interface for panel width management.",
    "XState Types": "// MUST define XState machine context and event type interfaces for sketchpad, kit, and app machines.",
    "Y.js-XState Bridge": "// MUST bridge Y.js document observation to XState machine events.",
    "Machine Factories": "// MUST define machine input and transaction configuration interfaces for state machine creation.",
    "YPath Helpers": "// MUST provide path segment constructors, value retrieval, and observation functions for Y.js paths.",
    "Derived Store": "// MUST provide reactive derived computation nodes with dependency tracking and caching.",
    "Store Factory Registry": "// MUST manage registration and retrieval of app-specific store factory functions.",
    "App Plugin Registry": "// MUST manage plugin registration, retrieval, and contribution composition for app extensions.",
    "Dynamic Event Dispatch Registry": "// MUST manage dynamic event handler and guard registration with namespace-based dispatch.",
    "App Event Handler Factories": "// MUST provide factory functions for creating standard app event handlers for panels, hover, selection, and windows.",
    "Transaction Handler Factory": "// MUST provide factory functions for creating undo/redo transaction event handlers.",
    "Selector Factory Pattern": "// MUST provide factory functions for creating property selectors with app key scoping.",
    "App Hooks Registry": "// MUST manage registration and retrieval of design and kit app hook implementations.",
    "App Registry Exports": "// MUST provide docs registry port interface and registration for documentation section access.",
}

def_summaries = {
    "YPathSegment": "// A single segment in a Y.js document path, either a map key, array index, or array item by ID.",
    "YPath": "// An ordered sequence of YPathSegment values describing a path through a Y.js document.",
    "HookResult": "// A readonly tuple of value, optional setter, and canSet flag for granular hook access.",
    "HookNoSetResult": "// A readonly tuple of value, undefined setter, and canSet flag for read-only hook access.",
    "READONLY_SETTER": "// Sentinel undefined value indicating that a hook result has no setter.",
    "READONLY_CAN": "// Sentinel false value indicating that a hook result is read-only.",
    "readonlyHookResult": "// Wraps a value into a read-only HookResult tuple with no setter.",
    "writableHookResult": "// Wraps a value and setter into a writable HookResult tuple.",
    "conditionalHookResult": "// Wraps a value into a HookResult tuple with a setter conditional on canSet.",
    "Field": "// A reactive field with a value, canSet flag, and setter function.",
    "ActionField": "// A reactive action field with canExecute flag and execute function.",
    "createField": "// Constructs a Field with a value, setter, and canSet flag.",
    "createReadonlyField": "// Constructs a read-only Field with a fixed value and no-op setter.",
    "createAction": "// Constructs an ActionField with a guarded execute function.",
    "fieldToHookResult": "// Converts a Field to a HookResult tuple.",
    "hookResultToField": "// Converts a HookResult tuple back to a Field.",
    "EMPTY_ARRAY": "// Frozen empty array singleton for default array values.",
    "EMPTY_OBJECT": "// Frozen empty object singleton for default record values.",
    "EMPTY_GUID_ARRAY": "// Frozen empty Guid array singleton for default guid collections.",
    "EMPTY_STRING_ARRAY": "// Frozen empty string array singleton for default string collections.",
    "EMPTY_PANEL_VISIBILITY": "// Frozen default panel visibility with only toolbar visible.",
    "ArrayDiff": "// Describes added and removed items for an array diff operation.",
    "SelectionDiff": "// Maps selection keys to their corresponding array diffs.",
    "inverseArrayDiff": "// Inverts an array diff by swapping added and removed items.",
    "inverseSelectionDiff": "// Inverts all array diffs within a selection diff.",
    "applyArrayDiff": "// Applies an array diff to a current array, removing then adding items.",
    "applySelectionDiff": "// Applies a selection diff to a partial selection state.",
    "Url": "// A string alias representing a URL.",
    "Subscribe": "// A callback subscription function that returns an unsubscribe disposer.",
    "Disposable": "// A cleanup function that disposes of a resource.",
    "Transact": "// A function that executes a mutation within a transaction with optional origin.",
    "Unsubscribe": "// A function that unsubscribes a previously registered callback.",
    "YProviderFactory": "// A factory function that creates a Y.js document provider for a given ID.",
    "AppKind": "// A string alias identifying the kind of an app.",
    "Device": "// Union type for desktop, tablet, or mobile device contexts.",
    "PanelKey": "// Union of all panel identifier strings including side and HUD panels.",
    "SidePanelKey": "// Union of left and right side panel keys.",
    "HudPanelKey": "// The HUD panel key literal type.",
    "HotkeyPath": "// A string alias for a hotkey path identifier.",
    "HotkeyValue": "// A string alias for a hotkey binding value.",
    "HotkeyOverrides": "// A record mapping hotkey paths to their override values.",
    "FileProviderFactory": "// A factory function that creates a FileProvider for a given kit ID.",
    "YUuid": "// A string alias for a Y.js-compatible UUID.",
    "YUuidArray": "// A Y.js array of UUID strings.",
    "YConcept": "// A string alias for a Y.js concept name.",
    "YConcepts": "// A Y.js array of concept name strings.",
    "YStringArray": "// A Y.js array of strings.",
    "YLeafMapString": "// A Y.js map with string leaf values.",
    "YLeafMapNumber": "// A Y.js map with number leaf values.",
    "YAttributes": "// A Y.js array of Y.js maps representing attribute key-value pairs.",
    "Theme": "// Available UI theme options: system, light, or dark.",
    "Expertise": "// User expertise levels: beginner, normal, or expert.",
    "Mode": "// Application modes: user or dev.",
    "StoreStatus": "// Store lifecycle states: idle, loading, error, or ready.",
    "ToolKind": "// Available tool kinds for selection, lasso, connector, and hand interactions.",
    "WindowKind": "// Window content kinds: table, scene, diagram, or custom.",
    "PanelPosition": "// Panel layout positions: left, right, middle, or bottom.",
    "PanelKind": "// Panel kinds: workbench, tools, toolbar, HUD, stats, details, chat, settings, params, or console.",
    "FileProvider": "// Interface for file upload, download, delete, and URL retrieval operations.",
    "MemoryFileProviderConfig": "// Configuration interface for in-memory file provider.",
    "LocalFileProviderConfig": "// Configuration interface for local IndexedDB file provider.",
    "RemoteFileProviderConfig": "// Configuration interface for remote file provider with base URL and headers.",
    "CompositeFileProviderConfig": "// Configuration interface combining memory, local, and remote file providers.",
    "RemoteProviders": "// Interface for remote Y.js document and file provider factories.",
    "FileOperation": "// Describes a file operation with type, kit ID, file ID, path, and optional blob.",
    "DesignAppId": "// Identifier for a design app scope with kit and design GUIDs.",
    "KitAppId": "// Identifier for a kit app scope with a kit GUID.",
    "TypeAppId": "// Identifier for a type app scope with kit and type GUIDs.",
    "QualityAppId": "// Identifier for a quality app scope with kit and quality GUIDs.",
    "PanelKindConfig": "// Configuration for a panel kind including icon, position, group, and hotkey.",
    "panelKindConfigs": "// Registry mapping each PanelKind to its PanelKindConfig.",
    "SidePanelPosition": "// Side panel positions: left or right.",
    "SidePanelTab": "// A tab entry for a side panel with ID, icon, order, and content.",
    "HudPanelTab": "// A tab entry for the HUD panel with ID, icon, order, and content.",
    "SidePanelVisibility": "// Visibility flags for left and right side panels.",
    "HudPanelVisibility": "// Visibility flag for the HUD panel.",
    "PanelVisibility": "// Optional visibility flags for all panel kinds.",
    "PanelSizes": "// Numeric sizes for all panel dimensions including widths and heights.",
    "PanelSection": "// A collapsible section within a panel with content, actions, and toolbar group.",
    "PanelSections": "// Collections of panel sections and tabs organized by panel kind.",
    "SidePanelTabs": "// Left and right arrays of side panel tabs.",
    "HudPanelTabs": "// Array of HUD panel tabs.",
    "PanelDefinition": "// Definition of a panel with ID, kind, hotkey, and tooltip.",
    "EnrichedPanelDefinition": "// Extended panel definition with resolved icon, position, group, and transparency.",
    "createPanelDefinition": "// Constructs a PanelDefinition from a kind, ID, hotkey, and tooltip.",
    "enrichPanelDefinition": "// Enriches a PanelDefinition with resolved config properties from panelKindConfigs.",
    "PanelConfig": "// Configuration for a panel instance with ID, key, label, order, and content.",
    "AppPanels": "// Container for an array of panel configurations.",
    "RouteSegment": "// A URL route segment with path, optional param name, and scope provider.",
    "AppConfig": "// Full app configuration with ID, component, routes, panels, and order.",
    "AppRegistration": "// App registration entry extending AppConfig.",
    "MobileDevice": "// Mobile device state with navbar and footer expansion flags.",
    "SketchpadChangableState": "// Mutable fields of sketchpad state including navigation, theme, device, and settings.",
    "SketchpadState": "// Full sketchpad state extending changeable state with ID and persistence flag.",
    "SketchpadDiff": "// Partial diff of sketchpad state fields for incremental updates.",
    "InitialStateKit": "// Initial kit state with kit data and local/remote flags.",
    "ExtendedInitialState": "// Extended initial state combining partial sketchpad state with initial kits.",
    "WindowEvents": "// Callback functions for window minimize, maximize, and close events.",
    "SketchpadScope": "// Scoped sketchpad context with ID, optional remote providers, and window events.",
    "KitCommandContext": "// Context for kit commands including kit data, file URLs, and origin.",
    "KitCommandResult": "// Result of a kit command with optional diff, files, and origin.",
    "SketchpadCommandContext": "// Context for sketchpad commands including sketchpad state and origin.",
    "SketchpadCommandResult": "// Result of a sketchpad command with optional diff and origin.",
    "Synchronizable": "// Interface for objects that support change subscription and snapshot retrieval.",
    "StoreState": "// Wrapper for store status, data, and error.",
    "AppStep": "// A single app step with optional selection diff.",
    "AppEdit": "// An undoable edit consisting of do and undo app steps.",
    "AppDiff": "// A diff containing selection, presence, hover, fullscreen, and panel visibility changes.",
    "AppCommandResult": "// Result of an app command with optional diff and origin.",
    "KitDiffAppStep": "// An app step extended with an optional kit diff.",
    "KitDiffAppEdit": "// An undoable edit with kit diff-aware do and undo steps.",
    "KitDiffAppCommandResult": "// An app command result extended with an optional kit diff.",
    "CompleteState": "// Full aggregated state containing sketchpad, kits, and all app states.",
    "WindowConfig": "// Configuration for a window with ID, title, icon, component, and default size.",
    "WindowControl": "// A window control with kind, ID, icon, options, and change handler.",
    "WindowKindDefinition": "// Definition of a window kind with label, icon, component, controls, and variants.",
    "AppWindowConfig": "// App-level window configuration with window kinds and default layout.",
    "parseWindowLayout": "// Parses a window layout from a string, object, or undefined input.",
    "deduplicateWindowLayout": "// Removes duplicate and disallowed window components from a layout.",
    "stringifyWindowLayout": "// Serializes a window layout to a JSON string.",
    "AppWindowProps": "// Props for an app window component with kind, children, and className.",
    "createDefaultLayout": "// Creates a default GoldenLayout configuration from window IDs and direction.",
    "Tool": "// A tool with ID, icon, and render function returning scene, diagram, and table nodes.",
    "ToolMode": "// A tool mode with ID, icon, label, and tooltip.",
    "ToolDefinition": "// Definition of a tool with ID, default mode, and available modes.",
    "ToolRenderContext": "// Context passed to a tool's render function containing the current state.",
    "ToolGroupProps": "// Props for a tool group component with tools, active tool, and change handler.",
    "FocusItem": "// A focusable item with ID, label, optional description, and category.",
    "FooterItem": "// A footer status bar item with ID, icon, text, content, and click handler.",
    "ResizablePanelProps": "// Props for a resizable panel with visibility, width, and width change handler.",
    "YjsSyncContext": "// Base context for Y.js-synced machines with dirty flag and cache.",
    "SketchpadMachineContext": "// XState context for the sketchpad machine with navigation, theme, kits, and refs.",
    "SketchpadMachineEvent": "// Union of all events the sketchpad machine can receive.",
    "KitMachineContext": "// XState context for a kit machine with GUID, kit data, types, designs, and files.",
    "KitMachineEvent": "// Union of all events the kit machine can receive.",
    "AppMachineContext": "// XState context for an app machine with panels, selection, hover, and transaction state.",
    "AppMachineEvent": "// Union of all events an app machine can receive.",
    "KitDiffAppMachineContext": "// Extended app machine context with a kit GUID for kit-diff-aware apps.",
    "createYjsSyncActor": "// Creates an XState callback actor that observes a Y.js map and sends Y_UPDATE events.",
    "createYjsFieldSyncActor": "// Creates an XState callback actor that observes a single field in a Y.js map.",
    "yTransact": "// Executes a function within a Y.js document transaction.",
    "createYjsUpdateAssign": "// Creates an XState assign action that marks dirty and caches Y_UPDATE event data.",
    "createYjsSelector": "// Creates a memoized XState selector that rebuilds only when context is dirty.",
    "AppMachineInput": "// Input for creating an app machine with Y.js map and transact function.",
    "KitDiffAppMachineInput": "// Extended app machine input with a kit GUID.",
    "TransactionMachineConfig": "// Configuration for transaction handling with apply and inverse functions.",
    "yPathMapKey": "// Creates a YPathSegment for accessing a map key.",
    "yPathArrayIndex": "// Creates a YPathSegment for accessing an array element by index.",
    "yPathArrayItemById": "// Creates a YPathSegment for accessing an array item by its ID field.",
    "getValueAtPath": "// Traverses a Y.js map or array along a YPath and returns the value at the end.",
    "createPathObserver": "// Sets up deep observers along a YPath and calls subscribe when the leaf value changes.",
    "BaseDependency": "// A dependency on a store path used by DerivedNode for change tracking.",
    "DerivedNode": "// A reactive computation node that recomputes when its dependencies change.",
    "DerivedStore": "// A keyed collection of DerivedNode instances with lifecycle management.",
    "DesignAppStoreFactory": "// Factory function type for creating a design app store.",
    "KitAppStoreFactory": "// Factory function type for creating a kit app store.",
    "TypeAppStoreFactory": "// Factory function type for creating a type app store.",
    "QualityAppStoreFactory": "// Factory function type for creating a quality app store.",
    "registerDesignAppStoreFactory": "// Registers the design app store factory.",
    "registerKitAppStoreFactory": "// Registers the kit app store factory.",
    "registerTypeAppStoreFactory": "// Registers the type app store factory.",
    "registerQualityAppStoreFactory": "// Registers the quality app store factory.",
    "getDesignAppStoreFactory": "// Retrieves the registered design app store factory or throws if not registered.",
    "getKitAppStoreFactory": "// Retrieves the registered kit app store factory or throws if not registered.",
    "getTypeAppStoreFactory": "// Retrieves the registered type app store factory or throws if not registered.",
    "getQualityAppStoreFactory": "// Retrieves the registered quality app store factory or throws if not registered.",
    "AppMachineContribution": "// Plugin contribution of event types, actions, guards, handlers, selectors, and default state.",
    "AppPlugin": "// An app plugin with ID, namespace, machine contribution, and lifecycle hooks.",
    "registerAppPlugin": "// Registers an app plugin, invoking its store registration and onRegister hooks.",
    "getAppPlugins": "// Returns all registered app plugins.",
    "getAppPlugin": "// Returns the registered app plugin with the given ID, or undefined.",
    "hasAppPlugin": "// Checks whether an app plugin with the given ID is registered.",
    "composePluginContributions": "// Merges actions, guards, event handlers, and selectors from all registered plugins.",
    "getPluginDefaultStates": "// Collects default states from all registered plugins.",
    "EventHandlerConfig": "// Configuration for a dynamic event handler with optional guard and action.",
    "registerEventHandler": "// Registers a dynamic event handler for a given event type.",
    "unregisterEventHandler": "// Removes a registered event handler for a given event type.",
    "hasEventHandler": "// Checks whether an event handler is registered for a given event type.",
    "getEventHandler": "// Retrieves the event handler configuration for a given event type.",
    "executeEventHandler": "// Executes the registered event handler for the given event, applying guard and action.",
    "registerGuard": "// Registers a named guard function.",
    "unregisterGuard": "// Removes a registered guard function by name.",
    "getGuard": "// Retrieves a registered guard function by name.",
    "hasGuard": "// Checks whether a guard with the given name is registered.",
    "executeGuard": "// Executes a registered guard and returns its boolean result.",
    "getEventTypesForNamespace": "// Returns all registered event types matching a given namespace prefix.",
    "getRegisteredNamespaces": "// Returns all unique namespaces from registered event types.",
    "getRegisteredEventTypes": "// Returns all registered event type strings.",
    "AppEventHandlerConfig": "// Configuration for an app event handler with namespace, app key, and default state factory.",
    "createTogglePanelHandler": "// Registers a toggle panel event handler for the given app config.",
    "createSetPanelVisibilityHandler": "// Registers a set panel visibility event handler for the given app config.",
    "createSetHoverHandler": "// Registers a set hover event handler with a mapper for the given app config.",
    "createClearHoverHandler": "// Registers a clear hover event handler with a guard for the given app config.",
    "createSetWindowLayoutHandler": "// Registers a set window layout event handler for the given app config.",
    "createClearSelectionHandler": "// Registers a clear selection event handler for the given app config.",
    "KeyedAppEventHandlerConfig": "// Extended app event handler config with a getKey function for keyed state.",
    "createKeyedTogglePanelHandler": "// Registers a keyed toggle panel event handler for multi-instance app state.",
    "createKeyedSetPanelVisibilityHandler": "// Registers a keyed set panel visibility event handler for multi-instance app state.",
    "createKeyedSetHoverHandler": "// Registers a keyed set hover event handler for multi-instance app state.",
    "createKeyedClearHoverHandler": "// Registers a keyed clear hover event handler for multi-instance app state.",
    "createKeyedSetSelectionHandler": "// Registers a keyed set selection event handler for multi-instance app state.",
    "createKeyedClearSelectionHandler": "// Registers a keyed clear selection event handler for multi-instance app state.",
    "createKeyedSetWindowLayoutHandler": "// Registers a keyed set window layout event handler for multi-instance app state.",
    "createKeyedSetCameraHandler": "// Registers a keyed set camera event handler for multi-instance app state.",
    "createKeyedSetActiveToolHandler": "// Registers a keyed set active tool event handler for multi-instance app state.",
    "createKeyedSetFullscreenWindowHandler": "// Registers a keyed set fullscreen window event handler for multi-instance app state.",
    "createKeyedInitHandler": "// Registers a keyed init event handler that sets initial keyed app state.",
    "createKeyedSyncHandler": "// Registers a keyed sync event handler that merges state for keyed app state.",
    "registerStandardAppEventHandlers": "// Registers all standard event handlers for a non-keyed app.",
    "registerKeyedAppEventHandlers": "// Registers all standard event handlers for a keyed multi-instance app.",
    "SingleKeyAppEventHandlerConfig": "// Configuration for single-key event handlers with namespace, app key, key field, and default state.",
    "createSingleKeyInitHandler": "// Registers a single-key init event handler.",
    "createSingleKeySyncHandler": "// Registers a single-key sync event handler.",
    "createSingleKeyTogglePanelHandler": "// Registers a single-key toggle panel event handler.",
    "createSingleKeySetPanelVisibilityHandler": "// Registers a single-key set panel visibility event handler.",
    "createSingleKeySetHoverHandler": "// Registers a single-key set hover event handler with a mapper.",
    "createSingleKeyClearHoverHandler": "// Registers a single-key clear hover event handler.",
    "createSingleKeySetSelectionHandler": "// Registers a single-key set selection event handler.",
    "createSingleKeyClearSelectionHandler": "// Registers a single-key clear selection event handler.",
    "createSingleKeySetWindowLayoutHandler": "// Registers a single-key set window layout event handler.",
    "createSingleKeySetFullscreenWindowHandler": "// Registers a single-key set fullscreen window event handler.",
    "registerSingleKeyAppEventHandlers": "// Registers all standard event handlers for a single-key app.",
    "KeyedTransactionHandlerConfig": "// Configuration for keyed transaction handlers with namespace, app key, key fields, and default state.",
    "AppTransactionState": "// Transaction state with active flag, current stack, past stack, and redo stack.",
    "createKeyedTransactionHandlers": "// Registers all transaction event handlers for keyed app state.",
    "SingleKeyTransactionHandlerConfig": "// Configuration for single-key transaction handlers with namespace, app key, key field, and default state.",
    "createSingleKeyTransactionHandlers": "// Registers all transaction event handlers for single-key app state.",
    "createAppPropertySelectorFactory": "// Creates a factory for selectors that read a property from a non-keyed app state.",
    "createKeyedAppPropertySelectorFactory": "// Creates a factory for selectors that read a property from a keyed app state.",
    "getAppKey": "// Joins scope strings into a colon-separated app key.",
    "getOrCreateAppState": "// Retrieves existing app state or creates it from a default factory.",
    "DesignAppHooks": "// Interface for design app hook functions including commands, diff, hover, and selection.",
    "KitAppHooks": "// Interface for kit app hook functions including commands.",
    "registerDesignAppHooks": "// Registers design app hook implementations.",
    "registerKitAppHooks": "// Registers kit app hook implementations.",
    "getDesignAppHooks": "// Returns registered design app hooks or defaults.",
    "getKitAppHooks": "// Returns registered kit app hooks or defaults.",
    "DocsRegistryPort": "// Port interface for retrieving documentation section trees and pages.",
    "registerDocsRegistry": "// Registers a docs registry implementation.",
    "getDocsRegistry": "// Returns the registered docs registry or null.",
}

func_requirements = {
    "readonlyHookResult": "// MUST return a frozen readonly tuple with undefined setter and false canSet.",
    "writableHookResult": "// MUST return a tuple with the setter included only when canSet is true.",
    "conditionalHookResult": "// MUST return a tuple with the setter conditional on the canSet flag.",
    "createField": "// MUST use the provided setter when canSet is true, otherwise a no-op setter.",
    "createReadonlyField": "// MUST set canSet to false and use a no-op setter.",
    "createAction": "// MUST guard execute behind canExecute, logging a warning in dev mode when disabled.",
    "fieldToHookResult": "// MUST convert the field's canSet and set properties into the hook result format.",
    "hookResultToField": "// MUST reconstruct a Field from the tuple, using a no-op setter when undefined.",
    "inverseArrayDiff": "// MUST swap added and removed arrays to produce the inverse diff.",
    "inverseSelectionDiff": "// MUST apply inverseArrayDiff to each key in the selection diff.",
    "applyArrayDiff": "// MUST remove items first, then add non-duplicate items.",
    "applySelectionDiff": "// MUST apply the array diff for each key present in the selection diff.",
    "createPanelDefinition": "// MUST use the panelKindConfigs hotkey as fallback when no explicit hotkey is provided.",
    "enrichPanelDefinition": "// MUST resolve all config properties from panelKindConfigs for the panel's kind.",
    "parseWindowLayout": "// MUST return undefined for null, empty, or unparseable inputs and parse valid JSON strings.",
    "deduplicateWindowLayout": "// MUST remove duplicate component entries and filter out disallowed window IDs.",
    "stringifyWindowLayout": "// MUST return undefined when serialization fails.",
    "createDefaultLayout": "// MUST generate a GoldenLayout config with one stack per window ID.",
    "createYjsSyncActor": "// MUST observe the Y.js map deeply and send Y_UPDATE events on every change.",
    "createYjsFieldSyncActor": "// MUST observe a specific field in the Y.js map and send Y_FIELD_UPDATE events.",
    "yTransact": "// MUST delegate to the Y.Doc transact method with the given origin.",
    "createYjsUpdateAssign": "// MUST return an XState assign that sets dirty to true and caches event data.",
    "createYjsSelector": "// MUST return cached snapshot when not dirty, rebuilding only when dirty.",
    "yPathMapKey": "// MUST return a mapKey segment with the given key.",
    "yPathArrayIndex": "// MUST return an arrayIndex segment with the given index.",
    "yPathArrayItemById": "// MUST return an arrayItemById segment with the given ID and idKey.",
    "getValueAtPath": "// MUST traverse each path segment, returning undefined when a segment cannot be resolved.",
    "createPathObserver": "// MUST set up nested observers along the path and notify when the leaf value changes.",
    "registerDesignAppStoreFactory": "// MUST replace any previously registered design app store factory.",
    "registerKitAppStoreFactory": "// MUST replace any previously registered kit app store factory.",
    "registerTypeAppStoreFactory": "// MUST replace any previously registered type app store factory.",
    "registerQualityAppStoreFactory": "// MUST replace any previously registered quality app store factory.",
    "getDesignAppStoreFactory": "// MUST throw if no design app store factory has been registered.",
    "getKitAppStoreFactory": "// MUST throw if no kit app store factory has been registered.",
    "getTypeAppStoreFactory": "// MUST throw if no type app store factory has been registered.",
    "getQualityAppStoreFactory": "// MUST throw if no quality app store factory has been registered.",
    "registerAppPlugin": "// MUST store the plugin and invoke registerStores and onRegister hooks if present.",
    "getAppPlugins": "// MUST return all registered plugins as an array.",
    "getAppPlugin": "// MUST look up the plugin by ID in the registry.",
    "hasAppPlugin": "// MUST check the registry for the given plugin ID.",
    "composePluginContributions": "// MUST iterate all plugins and merge their contributions into single records.",
    "getPluginDefaultStates": "// MUST call createDefaultState on each plugin that defines it.",
    "registerEventHandler": "// MUST store the handler config in the registry keyed by event type.",
    "unregisterEventHandler": "// MUST remove the handler for the given event type.",
    "hasEventHandler": "// MUST check the registry for the given event type.",
    "getEventHandler": "// MUST return the handler config or undefined.",
    "executeEventHandler": "// MUST run the guard before the action, returning empty context when guard fails.",
    "registerGuard": "// MUST store the guard function keyed by name.",
    "unregisterGuard": "// MUST remove the guard function by name.",
    "getGuard": "// MUST return the guard function or undefined.",
    "hasGuard": "// MUST check the guard registry for the given name.",
    "executeGuard": "// MUST return false when the guard is not registered.",
    "getEventTypesForNamespace": "// MUST filter event types by the namespace prefix.",
    "getRegisteredNamespaces": "// MUST extract unique namespace prefixes from all registered event types.",
    "getRegisteredEventTypes": "// MUST return all event type strings from the registry.",
    "createTogglePanelHandler": "// MUST register a handler that toggles the specified panel in panelVisibility.",
    "createSetPanelVisibilityHandler": "// MUST register a handler that replaces the entire panelVisibility.",
    "createSetHoverHandler": "// MUST register a handler that sets hover using the provided mapper.",
    "createClearHoverHandler": "// MUST register a handler with a guard that only clears non-empty hover state.",
    "createSetWindowLayoutHandler": "// MUST register a handler that sets the windowLayout from the event.",
    "createClearSelectionHandler": "// MUST register a handler that sets selection to undefined.",
    "createKeyedTogglePanelHandler": "// MUST register a keyed handler that toggles the panel for the resolved key.",
    "createKeyedSetPanelVisibilityHandler": "// MUST register a keyed handler that replaces panelVisibility for the resolved key.",
    "createKeyedSetHoverHandler": "// MUST register a keyed handler that sets hover for the resolved key.",
    "createKeyedClearHoverHandler": "// MUST register a keyed handler that clears hover for the resolved key.",
    "createKeyedSetSelectionHandler": "// MUST register a keyed handler that sets selection for the resolved key.",
    "createKeyedClearSelectionHandler": "// MUST register a keyed handler that clears selection for the resolved key.",
    "createKeyedSetWindowLayoutHandler": "// MUST register a keyed handler that sets windowLayout for the resolved key.",
    "createKeyedSetCameraHandler": "// MUST register a keyed handler that sets camera for the resolved key.",
    "createKeyedSetActiveToolHandler": "// MUST register a keyed handler that sets activeTool for the resolved key.",
    "createKeyedSetFullscreenWindowHandler": "// MUST register a keyed handler that sets fullscreenWindow for the resolved key.",
    "createKeyedInitHandler": "// MUST register a keyed handler that initializes state for the resolved key.",
    "createKeyedSyncHandler": "// MUST register a keyed handler that merges state for the resolved key.",
    "registerStandardAppEventHandlers": "// MUST register toggle panel, set panel visibility, hover, clear hover, window layout, and clear selection handlers.",
    "registerKeyedAppEventHandlers": "// MUST register init, sync, and all standard keyed handlers including camera, tool, and fullscreen.",
    "createSingleKeyInitHandler": "// MUST register a handler that initializes state for the event's key field value.",
    "createSingleKeySyncHandler": "// MUST register a handler that merges state for the event's key field value.",
    "createSingleKeyTogglePanelHandler": "// MUST register a handler that toggles the panel for the event's key field value.",
    "createSingleKeySetPanelVisibilityHandler": "// MUST register a handler that replaces panelVisibility for the event's key field value.",
    "createSingleKeySetHoverHandler": "// MUST register a handler that sets hover for the event's key field value.",
    "createSingleKeyClearHoverHandler": "// MUST register a handler that clears hover for the event's key field value.",
    "createSingleKeySetSelectionHandler": "// MUST register a handler that sets selection for the event's key field value.",
    "createSingleKeyClearSelectionHandler": "// MUST register a handler that clears selection for the event's key field value.",
    "createSingleKeySetWindowLayoutHandler": "// MUST register a handler that sets windowLayout for the event's key field value.",
    "createSingleKeySetFullscreenWindowHandler": "// MUST register a handler that sets fullscreenWindow for the event's key field value.",
    "registerSingleKeyAppEventHandlers": "// MUST register init, sync, and all standard single-key handlers.",
    "createKeyedTransactionHandlers": "// MUST register start, commit, abort, undo, redo, and record edit handlers for keyed state.",
    "createSingleKeyTransactionHandlers": "// MUST register start, commit, abort, undo, redo, and record edit handlers for single-key state.",
    "createAppPropertySelectorFactory": "// MUST return a factory that creates selectors reading from the given app key.",
    "createKeyedAppPropertySelectorFactory": "// MUST return a factory that creates keyed selectors reading from the given app key.",
    "getAppKey": "// MUST join all scope strings with colon separators.",
    "getOrCreateAppState": "// MUST return existing state or call the default factory to create it.",
    "registerDesignAppHooks": "// MUST store the provided hooks, replacing any previously registered.",
    "registerKitAppHooks": "// MUST store the provided hooks, replacing any previously registered.",
    "getDesignAppHooks": "// MUST fall back to default no-op hooks when none are registered.",
    "getKitAppHooks": "// MUST fall back to default no-op hooks when none are registered.",
    "registerDocsRegistry": "// MUST store the given docs registry, replacing any previous one.",
    "getDocsRegistry": "// MUST return the registered docs registry or null when none is registered.",
}

class_requirements = {
    "DerivedNode": "// MUST lazily initialize observers and recompute only when dependency values change.",
    "DerivedStore": "// MUST manage DerivedNode lifecycle including creation, retrieval, and disposal.",
}

new_lines = []
i = 0
added_count = 0
seen_synchronizable = False

while i < len(lines):
    line = lines[i]
    stripped = line.rstrip("\n")

    section_match = re.match(r"^// #region 🔖(.+)$", stripped)
    if section_match:
        section_name = section_match.group(1)
        if section_name in section_summaries:
            new_lines.append(line)
            i += 1
            if i < len(lines):
                next_stripped = lines[i].rstrip("\n")
                if not next_stripped.startswith("//"):
                    new_lines.append(section_summaries[section_name] + "\n")
                    added_count += 1
                else:
                    new_lines.append(lines[i])
                    i += 1
            continue
        else:
            new_lines.append(line)
            i += 1
            continue

    export_match = re.match(
        r"^export (type|const|function|interface|enum|class) (\w+)", stripped
    )
    if export_match:
        kind = export_match.group(1)
        name = export_match.group(2)

        if name == "Synchronizable":
            if seen_synchronizable:
                if name in def_summaries:
                    prev_stripped = new_lines[-1].rstrip("\n") if new_lines else ""
                    if not prev_stripped.startswith("//"):
                        new_lines.append(def_summaries[name] + "\n")
                        added_count += 1
                new_lines.append(line)
                i += 1
                continue
            else:
                seen_synchronizable = True

        if name in def_summaries:
            prev_stripped = new_lines[-1].rstrip("\n") if new_lines else ""
            if not prev_stripped.startswith("//"):
                if kind in ("function", "class") and name in func_requirements:
                    new_lines.append(func_requirements[name] + "\n")
                    added_count += 1
                elif kind == "class" and name in class_requirements:
                    new_lines.append(class_requirements[name] + "\n")
                    added_count += 1
                new_lines.append(def_summaries[name] + "\n")
                added_count += 1

    new_lines.append(line)
    i += 1

with open(FILE, "w") as f:
    f.writelines(new_lines)

print(f"Added {added_count} comments")
print(f"New file has {len(new_lines)} lines")
