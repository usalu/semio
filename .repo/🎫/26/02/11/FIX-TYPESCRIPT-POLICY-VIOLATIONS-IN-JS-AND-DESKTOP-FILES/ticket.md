---
goal: AI-OPTIMIZED-REPO/SINGLE-FILE-REPO/CONSISTENT-SECTIONS
---

# Ticket

## Summary

## Changes

## Log

## Todos

## Plan

1. compose/js/dev.ts - Wrap orphan code in `Dev` section, add summary+spec to `isWindows`, `npmCmd`, `vite`, `storybook` defs
2. compose/js/i18n.ts - Wrap orphan code in `I18n` section, add summary+spec to `Expertise`, `getExpertiseFunction`, `setExpertiseProvider`, `useLabel`, `useHotkey` defs
3. compose/js/index.ts - Wrap orphan code in `Exports` section (only re-exports, no defs needing comments)
4. compose/js/playwright.config.ts - Wrap orphan code in `Playwright Configuration` section (default export, no named defs)
5. compose/desktop/main.ts - Wrap orphan code in `Main Process` section, add summary+spec to `createWindow` def
6. compose/desktop/renderer.tsx - Wrap orphan code in `Renderer` section, add summary+spec to `invokeWindowControl`, `windowEvents`, `os`, `App` defs
7. compose/js/compose.benchmark.ts - Wrap orphan code in `Benchmarks` section, add summary+spec to `ITERATIONS`, `bench`, `findDesign`, `kitMetabolism`, `kitInvalid`, `diffForward`, `diffInverse` defs
8. compose/js/sketchpad/portColor.ts - Wrap orphan code in `Port Color` section, add summary+spec to all type/const/function defs
