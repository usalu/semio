# Fix Solar Beat7 Layout

## Problem
Three-line defs sat too high / off-center; equation sat too low (fixed bottom panel).

## Fix
- Stack defs + equation as one centered block under the subtitle.
- Nudge toward the vertical mid-band; keep clear of the caption.
- Why-copy uses three centered lines (same slot as the defs).
