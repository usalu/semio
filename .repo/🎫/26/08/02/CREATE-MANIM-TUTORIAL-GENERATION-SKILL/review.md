# Now I Get It → tutorial skill notes

## Product (NowIGetIt)

- Prompt → plan → TTS → Manim CE codegen → render → mux → compose
- Hard rules: `Text` only, plain `Scene`, timing ≈ narration, dark palette
- TTS: OpenRouter `/audio/speech` (Gemini → WAV), not gTTS in the product path
- Handcrafted twin: `heating_demands/scene_3_transmission_humidity/`

## semio tutorial

- Cooling beat pipeline is the target style for new work
- Skill: `tutorial/.agents/skills/generate-manim-tutorial/`
- Root discoverability pointer: `.agents/skills/generate-manim-tutorial/`
