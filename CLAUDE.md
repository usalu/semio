ALWAYS open a ticket with semio-repo mcp tool `ticket_open` (or use `./go/repo/repo ticket open <title> <prompt> <llm> <ui>` or `./go/repo/repo ticket open --title <title> --prompt <prompt> --llm <llm> --ui <ui>` when no mcp tool is available, where llm is one of: claude-opus-4-5, claude-sonnet-4, claude-haiku-4-5, gemini-3-pro, gpt-5-2, gpt-5-2-codex and ui is one of: claude-code, cursor, copilot-chat, antigravity, codex, droid) for every task you are working on. This creates a `tickets/YYYY/MM/DD/TICKETSLUG` folder along with `plan.md`, `log.md`, `summary.md`. NEVER answer directly in the chat and ALWAYS just add the message to `log.md`. ALWAYS start by writing the plan into `plan.md` and NEVER send it as message. ALWAYS end by writing the summary into `summary.md` and NEVER send it as message. ALWAYS use the semio-repo mcp tool `ticket_close` (or use `./go/repo/repo ticket close <YYYY/MM/DD/SLUG> <summary> <files...>` when no mcp tool is available) to finish the ticket along with the summary and at all the files you worked on (created, updated or removed). When a dev sends a new message to the chat ALWAYS reopen the same ticket with semio-repo mcp tool `ticket_reopen` (or use `./go/repo/repo ticket reopen <YYYY/MM/DD/SLUG> <prompt> <llm>` when no mcp tool is available).

ALWAYS follow AGENTS.md
