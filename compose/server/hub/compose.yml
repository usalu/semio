# #region 🔖Header
# 2025 Ueli Saluz <ueli@semio-tech.com>
# AGPL-3.0
# Docker Compose for compose-hub only: PostgreSQL plus the Rust compose-hub binary. Schema is created by compose-hub at startup (run_migrations); no external init SQL.
# #endregion 🔖Header

services:
  # #region 🔖Postgres
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-compose_hub}
      POSTGRES_USER: ${POSTGRES_USER:-compose}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-compose} -d ${POSTGRES_DB:-compose_hub}"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "127.0.0.1:5432:5432"
  # #endregion 🔖Postgres

  # #region 🔖Hub
  # Specs: compose-hub Rust backend with embedded /admin dashboard; protect JSON admin APIs with COMPOSE_ADMIN_TOKEN.
  hub:
    image: rust:1.89-bookworm
    restart: unless-stopped
    working_dir: /workspace/compose/hub
    command: ["cargo", "run", "--bin", "compose-hub"]
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-compose}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-compose_hub}
      LISTEN_ADDR: 0.0.0.0:8788
      COMPOSE_ADMIN_TOKEN: ${COMPOSE_ADMIN_TOKEN:?COMPOSE_ADMIN_TOKEN required for /admin JSON APIs}
      RUST_LOG: ${RUST_LOG:-compose_hub=info,tower_http=info}
    volumes:
      - ../..:/workspace
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:8788:8788"
    healthcheck:
      test: ["CMD", "bash", "-lc", "exec 3<>/dev/tcp/127.0.0.1/8788"]
      interval: 10s
      timeout: 3s
      retries: 5
  # #endregion 🔖Hub

volumes:
  postgres_data:
