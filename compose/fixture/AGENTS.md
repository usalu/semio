---
technology: compose
bundle:
  name: fixture
  emoji: 🧪
  description: Test and story JSON for compose algorithms and UI; not shipped in product bundles.
  kind: fixture
---

# 🧾 Specification

Import via `@compose/fixture` from tests, storybook, and dev harnesses only. Builtin kits, meshes, and icons live under `@compose/asset`.
