{
  "name": "compose/graphql",
  "bundleKind": "schema",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["@compose/rs:build"],
      "cache": false,
      "options": {
        "cwd": "compose/client/schema/graphql",
        "command": "bun ./script.ts build"
      }
    }
  }
}
