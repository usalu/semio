#!/usr/bin/env bun
/** 🧩 Placeholder Neo4j migration hook (`generate` + `neo4j` + `migrate`); extend when migrations are scripted. */
console.log("[generate.neo4j.migrate] no-op (no scripted migrations yet).");
process.exit(0);
