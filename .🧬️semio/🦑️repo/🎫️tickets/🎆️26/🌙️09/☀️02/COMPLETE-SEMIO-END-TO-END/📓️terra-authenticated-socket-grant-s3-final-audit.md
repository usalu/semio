# SocketGrant S3 Client Migration — Live Final Audit

Status: **REJECT — S3 is not a release boundary yet.** This is a read-only, live-source audit. It does not claim any build, test, or runtime result. The reported OS-kernel compile seam is not evidence of a real native launch or of an authenticated document session.

## Decision

S1/S2 may be the server foundation, but S3 requires one atomic greenfield cutover: every protected dial obtains a fresh protected grant, offers the ordered `semio.socket.v1, <grant>` subprotocol pair, sends credential-free `SocketHelloV1`, and retains the server receipt actor only in the live owner which constructs outgoing envelopes. There must be no token field, query credential, tag-0 `Hello`, legacy route, bridge URL secret, or compatibility carrier anywhere in the released client/server surface.

The React document/directory worker is materially closer to that target. Native document sync, MCP workspace/upstream, the legacy hub routes and fixtures, and the AgentBridge remain outside it. Therefore a mixed release would either retain forbidden carriers or leave callers unauthenticated.

## Live Source Findings

### Browser broker and React worker — provisional, not yet accepted

The current secure-local topology has a credible narrow authority boundary:

- `🌎️hub/📦️packages/🦀️rust/📜️script.ts` keeps the relay bearer in the relay, requires loopback peer plus exact host/origin/referer/fetch-site and relay secret, admits a small method/path matrix, bounds body/in-flight/deadline, and stores only the digest of the one-use browser proof.
- The initial proof is fragment-only and immediately removed by `ShellHost/🟦️.tsx`; its `MessagePort` is transferred only to `🟦️backbone-worker.ts`. Plugin shards receive typed mutations/snapshots, not `window`, the port, or a raw proof.
- `🟦️backbone-worker.ts:248-281` serializes proof use, uses a fresh next proof digest, and clears local proof on a missing advancement acknowledgement, `401`, or transport failure. The relay now returns `x-semio-browser-broker-advanced: 1` only after consuming the current proof. This closes the earlier pre-advance desynchronization.
- The same worker obtains directory/document grants, uses `SocketHelloV1`, and its directory client is restricted to the BFF allow-list.

This is **not accepted yet**. Required evidence is still a runtime/neutral adversarial oracle: a raw local HTTP client which spoofs browser headers but lacks the proof must have no hub effect; a stale proof replay must fail; a malicious plugin shard must be unable to obtain proof/port or issue a broker request; and startup must prove Vite readiness precedes one-use fragment navigation. No such run is claimed here.

The fragment/worker approach is viable only for the stated threat model (raw local HTTP caller and ordinary plugin shard). It cannot defend against arbitrary trusted main-thread code, browser extensions, or an OS-level process inspector. It must remain an explicitly private worker capability, never a generic same-origin header supplied by Vite.

### Native identity and directory client — partial structural closure

`🧰️framework/🛍️products/💻️os/🔨️modules/📇️directory/🔌️client/🦀️.rs:315-383` defines `LocalHubCredential` as a private byte buffer: `Debug` redacts it and `Drop` zeroes it. Its inherited reader consumes exactly one bounded (16 KiB) fd-3 frame, requires EOF immediately after the frame, validates the native class, loopback origin, expiry, and session-capability grammar. `🪪️identity/🦀️.rs:54-77` gates bootstrap on the non-secret sentinel `S_LOCAL_CREDENTIAL_FD=3`, calls `/auth/sessions/me`, and projects a non-secret `Identity`.

This is a good consumer seam, and the old persistent OS identity `session_token` field is **superseded/closed** in the current tree: `🎚️config/🧬️schema/🧬️mutations/🪪️sign-in/🦀️.rs:14-20,61-67` contains no token.

For a final zeroization claim, also remove the transient duplicate made at `📇️directory/🔌️client/🦀️.rs:350-373`: the raw frame is zeroed, but it is parsed into a `DslValue` string and then copied into the credential buffer. Take ownership of the capability string/allocation from the decoded object before converting it to the sealed byte buffer, so the temporary secret allocation is not merely dropped.

It is nevertheless not a usable native S3 path:

1. `🌎️hub/📦️packages/🦀️rust/📜️script.ts:497-524` can create a direct child pipe at fd 3, serializes the bounded `semio.local.consumer-credential/v1` envelope, and clears the source envelope capability. But its exported native delivery function is reached only by the Node delivery proof helper, not by a real WGPU launcher.
2. The actual WGPU dev runner at `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🎯️targets/🧊️wgpu/📦️packages/🦀️rust/📜️script.ts:260-281` invokes `cargo run` with `{ ...process.env }`. It neither calls the fd-3 delivery helper nor starts the final native executable as the direct child. Cargo is an intermediate process, so fd-3 delivery to it does not prove inheritance by the executable. The copied process environment is also not a secret-safe allow-list.
3. The delivery helper itself uses `{ ...process.env, S_LOCAL_CREDENTIAL_FD: "3" }` at `🌎️hub/📦️packages/🦀️rust/📜️script.ts:500`. A launch boundary must use an allow-list of non-secret configuration, rather than forwarding arbitrary parent environment values.

Required repair: build first, resolve the final native executable, then have the local supervisor spawn *that executable* through one cross-platform fd/handle delivery owner. Pass only an explicit non-secret environment allow-list (`S_LOCAL_CREDENTIAL_FD`, modules root, asset origin, locale/config needed by the program); do not place the capability in argv, cwd, logs, readiness, disk, a config mutation, or any environment variable. Parent must write one bounded frame, close its write half, zero all mutable buffers, and terminate/revoke on child startup/EOF/deadline failure. Unix and Windows need equivalent direct-child handle tests, not only a Node consumer proof.

There is a second ownership condition for that repair: fd 3 must be claimed and made non-inheritable *before* plugin activation. `Shell/…/wgpu/🦀️.rs:2269-2319` creates an app/plugin instance before it starts `bootstrap_identity`; the actual fd read is deferred onto an I/O future at `:4018-4034`. `LocalHubCredential::read_inherited` adopts fd 3 at `directory/client/🦀️.rs:337-343` but does not first set close-on-exec/clear the inherited flag. The native plugin host can spawn process shards (`🔌️plugin/🖥️host/🧵️shard/🚚️process-transport/🦀️.rs:214-215`). A descendant must never race to consume the one-shot endpoint. Earliest native entry must seal and consume the descriptor before any untrusted/plugin child exists; Unix requires close-on-exec ownership and Windows requires a non-inheritable duplicate plus cleared original inheritance. Add a malicious-child law that observes no usable fd-3 credential after this earliest step.

### Native document sync — hard S3 blocker

Native document sync is actively mid-migration and is not a compilable/acceptable S3 boundary:

- The current `PersistenceBinding::Hub` declaration at `🧰️framework/🛍️products/💻️os/🔨️modules/🏪️store/🔄️sync/🦀️.rs:80-96` has removed `token`, which is the right public-schema direction. Its remaining fan-out is inconsistent: the wasm actor still destructures/copies `token` at `:3581-3607`; WGPU Shell still constructs/matches it at `Shell/…/wgpu/🦀️.rs:364,659,3032`; and the OS host helper still accepts it at `🖥️host/🦀️.rs:2459-2460`. This audit did not compile the intermediate tree.
- `ArtifactHost` now has a private credential slot at `🏪️store/🔄️sync/🦀️.rs:1046-1072`, but `set_local_hub_credential` has no live caller. `Shell::poll_identity_bootstrap` only constructs its directory client at `Shell/…/wgpu/🦀️.rs:4043-4051`; it does not hand `outcome.credential` to `document_host`. An actor opened before this handoff must fail closed and rebootstrap once the host has a credential; it must not dial anonymous or retain a stale credential.
- `:756-768` still constructs `/spaces/{space}/documents/{document}/ws`.
- `:1809-1837` dials that route and emits tag-0 `ClientFrame::Hello` containing a client-selected actor and the optional token. The wasm twin does the same at `:3198-3229`.
- `:4797-4804` still generates the token-bearing `client-hello` fixture.
- The WGPU Shell currently supplies `token: None` for the default Hub binding (`Shell/…/wgpu/🦀️.rs:350-367`) and derives `user:{id}#wgpu-*` locally, so it is neither a functioning protected document dial nor a receipt-actor owner.

Repair as one breaking structural packet: delete token from `PersistenceBinding`, `ArtifactActorConfig` serialization, sync actor state, fixtures, TS twins, and all tag-0 framing. Introduce a host-only, non-serializable document-socket issuer injected after actor construction. On each initial/reconnect/rebootstrap dial it must (a) issue the exact scoped HTTP grant with the private inherited/broker credential, (b) validate receipt grammar/audience/expiry, (c) install its `actorId` before any command/presence envelope construction, (d) offer only the two ordered protocols, and (e) send credential-free `SocketHelloV1`. Close/clear the grant immediately after WebSocket construction; cancellation before/while issuance must produce no dial. The same structural API must cover native and browser twins.

### MCP workspace/upstream — hard S3 blocker

MCP still takes and retains a raw hub bearer:

- `🧰️framework/🛍️products/💻️os/🔨️modules/🌉️mcp/🏠️workspace/🔗️remote/🦀️.rs:440-463` accepts `token: &str` and injects it into a directory client; the fixture path at `:627-636` retains the old API.
- `🧰️framework/🛍️products/💻️os/🔨️modules/🌉️mcp/🏠️workspace/🦀️.rs:433-449,1216` owns `WorkspaceOrigin::Hub { token }` and maps it to `PersistenceBinding::Hub { token: Some(...) }`.
- The MCP bridge/transport still has bearer/bridge token configuration and a token-file writer. It needs an independent direct-child fd-3 consumer, not reuse of the browser BFF and not persistence in an MCP descriptor/workspace object.

There is also a live structural mismatch: those MCP lines call `DirectoryClient::new(...); client.set_token(...)`, while the current client exposes `DirectoryClient::authenticated(..., Arc<LocalHubCredential>)` and no `set_token` method (`📇️directory/🔌️client/🦀️.rs:419-429`). This audit did not run a build, so it records the source incompatibility rather than claiming a diagnostic; it is nevertheless a first implementation blocker for the MCP packet.

The packet needs an MCP `InheritedLocalCredential` owner analogous to native (class must be `mcp`), a private protected HTTP/grant issuer, and non-serializable upstream connection authority. Stdio transport remains an upstream delivery channel; it is not a socket grant and must not be copied into a URL, request log, audit record, workspace snapshot, or plugin-visible config.

### Server and global legacy removal — hard S3 blocker

The hub still serves both generations:

- `🌎️hub/📦️packages/🦀️rust/📦️bin.rs:2872-2885` retains query `token` and `/directory/ws`.
- `:3588-3623` mounts `/directory/ws` and `/spaces/{space_id}/documents/{id}/ws` alongside the v1 grant routes.
- The server and package tests still construct tag-0 `ClientFrame::Hello` and query credentials, including `🌎️hub/📦️packages/🟦️typescript/🧪️index.test.ts:793-795` and the Rust tests around `📦️bin.rs:4441` onward.

S3 must remove these routes, handlers, query types, carrier fields, tag-0 parser/encoder/fixtures, and token-bearing test helpers in the same change. A server that merely rejects old frames after accepting an old route is not an S3 release boundary.

### Agent bridge and admin

`AgentBridge/🟦️.tsx:103-138,297-433` still discovers a Vite token and appends it as `?token=`. Its tests assert that behavior. This violates the no-secret-in-URL condition and must be removed, not adapted. The safe admin choice remains authenticated bounded REST snapshot/poll; no admin WebSocket audience is needed or audited for S3.

## Required Atomic Order

1. Land the shared schema/wire deletion: no tag-0 `Hello`, no `token` field in public binding/config/wire types, strict v1 socket header + credential-free hello only. Update Rust/TS fixtures and a neutral codec oracle together.
2. Land a host-private `ProtectedSocketIssuer` interface and receipt-actor installation for document and directory clients. It cannot be `ToValue`/`FromValue`, clone into a plugin, or expose a bearer string.
3. Implement direct supervisor-to-real-native and supervisor-to-real-MCP launches with bounded fd/handle frames, strict class/origin/expiry validation, environment allow-lists, zeroization, cancellation and teardown laws.
4. Migrate React through its worker broker; retain the BFF only if the raw-local/replay/malicious-plugin/readiness/redaction oracle passes. Otherwise fail closed and do not launch the browser.
5. Move admin to bounded authenticated REST polling and delete AgentBridge URL credentials.
6. Delete the old hub routes/handlers, all client old dials, bridge/token config, fixtures, test helpers, and launch paths in the same commit. No compatibility interval.

## Acceptance Laws and Gates

- A protected document and directory dial succeeds only with a newly issued exact-scope receipt, exact protocol order, and `SocketHelloV1`; a second use/restart/expiry/revoke fails before exposure.
- A forged actor, tag-0 hello, bearer header/query token, protocol reordering, extra protocol, and receipt scope/generation/selector mismatch all fail closed with no secret in URL/error/audit/readiness.
- Reconnect and rebootstrap reissue grants and replace the actor before any queued outbound envelope; cancellation/progress are bounded and no cancellable action reaches the transport after cancellation wins.
- Two native children and an MCP child prove fd/handle class isolation, bounded EOF framing, expiry, malformed/oversize rejection, no env/argv/disk/log leak, and cleanup on child/supervisor death. Cover Unix and Windows.
- The BFF oracle independently probes raw local HTTP spoofing, proof replay/rotation acknowledgement, malformed/over-limit routes, `401`, teardown, no bundle secret, and malicious plugin-shard isolation.
- A whole-repository caller census gate returns zero for `/directory/ws`, document `/ws`, `?token=`, `ClientFrame::Hello`, serialized hub token fields, and legacy bridge URL token behavior, except historical audit text outside the shipped source/test surface.

## Evidence Qualification

An implementation message reported an OS-kernel compile seam as green. That can establish only compilation of the cited Rust seam; it does not exercise a final native executable spawned as an fd-3 child, a real session, a document socket, cross-platform inheritance, or redaction. No run is attributed as acceptance evidence in this report.

## Successor Re-read — D0 Gate Is Non-Vacuous in Source, but Integration Remains RED

This supersedes the earlier source-only assertion that the registered D0 gate selected no Rust law. The current `open-plan-check` target is non-cached (`🌎️hub/📦️packages/🦀️rust/📋️project.json:95-100`), invokes `proveDocumentOpenPlanFixture`, lists the Cargo selection, rejects the absence of a `tests::document_open_plan_ledger…: test` entry, and then invokes both the exact schema law and the bin-law selector (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1718-1726`). The selected bin law exists as `document_open_plan_ledger_is_digest_only_bounded_single_use_revalidated_and_restart_scoped` (`📦️bin.rs:5469-5558`), including a concurrent exchange race, restart denial, bounded global/binding ledgers, one-use/replacement/expiry behavior, and an index-cleanup regression. This is a non-vacuous *source registration* only: no terminal output from this audit establishes that Cargo listed or ran it.

The Node portion is qualified rather than fully independent. It independently recomputes descriptor/catalog/receipt encodings with Node crypto and independently decodes the public-plan structure and all fixture negative outcomes (`📜️script.ts:1514-1714`). It additionally calls the shipped TypeScript intent/plan/exchange parsers merely to assert self-parity (`:1707-1709`). The latter three assertions cannot independently detect a common shipped-code parser defect; the neutral structural decoder and negative outcomes can. A valid execution must report both layers distinctly, rather than labelling all six assertions an independent oracle.

The ledger is now constructed in production and test `HubState`s (`📦️bin.rs:1169,4558,4840,4889`), but the router has no issue or exchange route (`:4315-4345`) and the only current ledger uses outside construction are its local laws. It is therefore still fail-closed/non-delivered, not an end-to-end D0 feature. More importantly, the new real admin revoke paths invalidate only `socket_grants`: `RevokeDocumentShare` holds the share gate but omits `document_open_plans.invalidate_binding(binding)` (`:3820-3846`), and `RevokeUserSessions` similarly omits that call for every revoked session binding (`:3847-3886`). Once issue/exchange is exposed, an outstanding D0 receipt would remain exchangeable after its share/session revoke until expiry. Invoke D0 invalidation while the corresponding binding admission is still held and prove the revoke-versus-exchange ordering under the real handler.

## Successor Re-read — Admin Rebuild Capacity/Cleanup and Relay Coverage

The earlier capacity TOCTOU/orphan observation is superseded in current source. Rebuild admission acquires one of 64 owned semaphore permits *before* inserting the runtime (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:3960-3987`); `AdminOperationCleanup::Drop` removes the runtime, releases that owned permit, and schedules the pre-armed interrupted terminal fact on abort/panic (`:3783-3813`). Normal completion appends its terminal fact before disarming the fallback (`:3994-4002`), and stale accepted facts reconcile to the same interrupted terminal after 15 seconds (`:3678-3705`). The focused Rust law `admin_rebuild_slots_are_atomic_and_abort_closes_once` (`:5866-5960`) exercises 128 simultaneous permit contenders, an aborted cleanup task, operation-map removal, slot recovery, and first-terminal-wins. It is valuable source evidence, but it constructs the cleanup seam directly; it does not execute the full HTTP `admin_intents → spawn → controlled rebuild → operation status` path. No run is attributed here.

The current registered `admin-relay-check` is also non-vacuous at source: it executes the local relay adversary and then the uncached `os-hub-admin:test` selection for `🧪️admin.test.tsx` (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1849-1853`). That test includes a genuine pending-fetch unmount law for `ConnectionsPage`: it observes the supplied `AbortSignal`, unmounts, asserts the signal aborted, waits, and asserts no successor poll (`🌎️hub/🔨️modules/🛡️admin/📦️packages/🟦️typescript/🧪️admin.test.tsx:250-284`). The actual component uses a single recursive timeout, a two-second abort deadline, stale-on-failure state, and cleanup of both timeout and in-flight request (`🧱️elements/🔴️ConnectionsPage/🟦️.tsx:42-68`); the same test suite verifies only the active tab mounts REST polling and that tab switches clean it up (`🧪️admin.test.tsx:310-366`). This supersedes the old claim that the relay admitted obsolete mutations: the present adversary admits typed `POST /admin/api/intents`, rejects the legacy route, and checks a bounded connections query (`📜️script.ts:1405-1468`). It remains source-only until this exact current command has a terminal result.

## Successor Re-read — Current Admin Cutover Is RED

This section supersedes the older broad observation above that admin was merely an unaudited REST choice. The current hub mounts only `POST /admin/api/intents` for mutation (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:3882-3889`), but the shipped SPA still calls the removed command endpoints: `AdminSession/🟦️.tsx:93-106` posts `/admin/api/commands`, `/admin/api/directory/rebuild`, connection-close, and session-revoke routes. The live page actions invoke those helpers. The browser relay allow-list and its positive test still permit old mutation paths (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:47-52,1372-1375`) and do not permit `/admin/api/intents`; the MCP process probe likewise posts the removed connection-close route (`:897-899`). Thus every mutation caller is deterministically a `404` boundary today. This is a hard atomic-cutover blocker, independent of the previously green bounded browser-relay component gate.

The new backend intent entry has useful source properties but does not repair that end-to-end RED: it parses one bounded strict body, authenticates an `AdminPrincipalV1`, and records accepted/terminal operation facts before/after execution (`📦️bin.rs:3273-3619`). The event actor includes subject and session (`:1080-1094`), and credential share/revoke calls carry that subject and correlation. The directory branch remains a generic `Directory { command }` dispatch, however, while live overview/docs/spaces reads retain unbounded enumeration and per-space fan-out. The declared page/response bounds and recorded connection/audit projection types have no query route consumer. No operation status/reconciliation route covers an accepted-only crash, and no focused concurrent cross-backend law was found. These are source observations only; no admin runtime evidence is claimed.

## Successor Re-read — D0 Document Open Plan Is Schema-Only and Fail-Closed

As read on 2026-09-03, `DocumentOpenIntentV1`, `DocumentOpenPlanV1`, and `DocumentPlanSocketGrantIntentV1` occur only in the directory Rust/TypeScript schema modules. There is no hub route, plan issuer, receipt ledger, exchange handler, MCP caller, or catalog-selection path; hub readiness explicitly advertises `openPlan: false` (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:1163-1196`). An empty or unavailable catalog therefore cannot currently mint a plan: this is fail-closed by non-implementation, not evidence of a working catalog denial path or a one-use exchange.

The structural packet is mostly explicit: top-level and purpose-built nested values deny unknown fields, receipt grammar is bounded, hash fields reject zero/non-lowercase values, and the TypeScript reader recursively rejects unknown keys. It is not yet a strict cross-language contract. `DocumentScope` and `ArtifactFrontier`, which are nested by the open intent/plan, retain `FromValue` without `deny_unknown_fields` (`📇️directory/🧬️schema/🦀️.rs:61-66,825-833`). The repository derive makes that check opt-in (`🌱️value/✨️derive/🦀️.rs:72-84,455-464`), so Rust accepts surplus nested wire keys that the TypeScript reader rejects. The Rust structure also declares `DOCUMENT_OPEN_PLAN_MAX_TTL_MS = 30_000` (`📇️directory/🧬️schema/🦀️.rs:473-477`) but `DocumentOpenPlanV1::validate` accepts every future expiry (`:658-707`); the TypeScript parser has the same omission (`🟦️.ts:464-514`). A future issuer must cap `expiresAt` against its issuance clock, not merely check expiry at read time.

Finally, the exchange shape contains only an opaque `planReceipt`; no current authority record binds a receipt digest to the authenticated subject/session-or-share generation, exact scope, selected descriptor/catalog/package/surface projection, expiry, and atomically consumed state. Until such a record is created and consumed before grant minting under a concurrency law, the field documentation alone cannot prove one-use, replay resistance, or no catalog disclosure. No compile or runtime result is attributed to this D0 reread.

## Successor Re-read — Admin Connections Is Now an In-Flight Contract Mismatch

The immediately preceding Admin section remains RED, with one backend change since its first reread. `admin_connections` now has a bounded signed-cursor path and emits `AdminConnectionSnapshotV1` built from recorded sync sessions (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:3823-3862`); this supersedes the statement that the new connection snapshot type had no route consumer. It does not make the surface usable. `AdminClient.connections()` still asks for `ConnectionView[]` (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/🔑️AdminSession/🟦️.tsx:84-86`), while the backend now returns an object containing `rows` and omits the legacy actor/surface/presence fields. The MCP process probe likewise reads legacy `firstConnection.actor` before its newly migrated intent post (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:895-903`). The relay admits no cursor-bearing connection request and continues to admit only old mutation paths (`📜️script.ts:47-52`).

The other live reads are still old handlers: overview/users enumerate with `i64::MAX`, spaces/documents fan out per space, and events take a caller-provided limit without the newly introduced page gate (`📦️bin.rs:3770-3893`). `AdminPageQuery` itself has no `serde(deny_unknown_fields)`. These source facts preserve the bounded-read, single client contract, and REST cutover hold. No test/build/runtime result is implied while this packet is changing.

## Successor Re-read — D0 Structural Parser Repairs, but Ledger Gate Is Vacuous

The D0 strict-parser and lifetime findings above are source-superseded: `DocumentScope` and `ArtifactFrontier` now both opt into `deny_unknown_fields` (`📇️directory/🧬️schema/🦀️.rs:61-66,825-833`), and both Rust/TypeScript plan readers reject a future expiry farther than `DOCUMENT_OPEN_PLAN_MAX_TTL_MS` (`🦀️.rs:664-715`; `🟦️.ts:464-514`). The Rust fixture law now explicitly covers the excess TTL and unknown top-level, scope, and frontier keys (`🦀️.rs:1072-1089`). These repair the bounded structural decoding contract; no runtime claim follows.

The newly registered `os-hub:open-plan-check` does not yet prove the named ledger. Its Node phase independently recalculates descriptor/catalog encodings and receipt digest grammar from the committed fixture, but imports the shipped TypeScript parsers for each codec assertion (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:10,1577-1648`). More seriously, its second Cargo invocation filters `--bin os-hub document_open_plan_ledger`, while the current hub binary has neither a matching test nor an authority handler/route; current-tree searches locate only schema, fixture/oracle, and `openPlan: false` readiness. Cargo permits a filtered zero-test success, so this registration is vacuous for issuance, digest binding, atomic consumption, replay, catalog selection, and socket-grant minting. Treat D0 as intentionally fail-closed/schema-oracle-only until a real handler and an asserted nonzero ledger law land.

## Successor Re-read — Admin Long-Operation Recovery Is Still Not Bounded Under Races

The backend now has useful source-only recovery wiring: status, cancel, and audit endpoints; a ten-second rebuild runtime; a stale accepted-record reconciliation path; and an asynchronous rebuild task that removes its runtime after terminal recording (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:3601-3975,4295-4301`). This supersedes the earlier statement that no operation status/cancel route exists. It does not yet establish a bounded operation owner.

Capacity admission is a non-atomic `admin_operations.len() >= 64` check followed by a separate insert. Simultaneous distinct rebuild requests can all pass the check and over-admit. In addition, the spawned task discards a terminal-record append error and has no panic/abort cleanup guard around its later `remove`. A panic before removal leaves the runtime entry resident; stale-acceptance reconciliation treats that resident entry as live forever, so the accepted record cannot converge and global capacity leaks. Require an atomic permit/reservation release path and laws for concurrent capacity edge, terminal-write failure, task panic/abort, deadline, explicit cancel, and a retry that observes the terminal state. The browser client/relay remains on the old REST shapes, so no full admin acceptance is implied.

## Successor Re-read — SPA Began the Typed Cutover; Relay and One Consumer Still Block It

`AdminSession` has now migrated to typed intents, page/snapshot reads, and operation status/cancel (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/🔑️AdminSession/🟦️.tsx:80-166`), superseding the claim that every SPA mutation still posts removed routes. The relay is still its old policy, however: it accepts only queryless legacy reads and old POST paths, with neither `limit`/`cursor`, `/admin/api/intents`, nor operation status/cancel admitted (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:44-53`). The current browser SPA consequently receives relay denial for its first paged read and every mutation.

There is also a live client type/use mismatch: `DocumentsPage` still iterates `client.connections()` as though it returned an array (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/📄️DocumentsPage/🟦️.tsx:35-41`), while the current method returns `AdminConnectionSnapshotV1`. The MCP direct-hub probe has separately migrated to bounded snapshot pagination (`📜️script.ts:811-839`), so it does not prove the browser relay/SPA path. This retains the full admin acceptance hold.

## Live RED — D0 Replacement-Expiry Sweep Can Restore a Superseded Receipt

The D0 hub ledger is now present (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:786-1145`) and has the right overall shape: a domain-separated 32-byte receipt digest, secret-wiping capability owner, 30-second/1,024-record/64-issued-per-binding limits, authority snapshot, and serialized exchange state. It has a decisive index-corruption bug. `remove_issued_indexes` unconditionally removes the binding-and-scope index for every record it deletes (`:995-1009`).

If receipt A is issued, then B replaces it for the same binding/scope, A is marked invalidated and B becomes the current index. When expiry sweeping later deletes A, the unconditional removal deletes B's current scope index. A subsequent C issuance does not find or invalidate B, leaving B and C concurrently `Issued` and exchangeable. The repair is a compare-and-remove: delete `issued_by_binding_scope[binding_scope]` only when its stored digest equals the record being removed. Require an A→B→A-expiry-sweep→C law that proves B rejects stale and only C exchanges. This blocks one-outstanding receipt authority acceptance; no D0 gate/runtime result is credited.

## Successor Re-read — D0 Index Repair and Non-Vacuous Ledger Law Landed

The preceding replacement-expiry RED is source-superseded. `remove_issued_indexes` now compare-and-removes the scope index only when it still contains the removed record digest (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:995-1011`). The registered `document_open_plan_ledger_is_digest_only_bounded_single_use_revalidated_and_restart_scoped` test is now real, not a zero-test Cargo filter: it covers receipt-digest-only storage, parallel one-winner exchange, restart denial, exact authority mismatch, replacement/invalidation, the A→B→A-expiry-sweep→C regression (B stale/C accepted), binding/global caps, and share read-only law (`📦️bin.rs:5893-6005`). This is source-test coverage only; it has not been run by this audit.

The D0 capability remains internal at this reread. There is still no `HubState` ledger owner, protected route, catalog/descriptor authority builder, receipt-exchange-to-socket-grant bridge, or `openPlan: true` readiness declaration. The Node phase remains partially coupled to the shipped TypeScript codec. Therefore the repair closes the ledger data-structure finding but does not provide protected endpoint, empty-catalog, current-authority, or runtime acceptance evidence.

## Successor Re-read — Admin Browser Shape/Relay Sources Are Now Aligned

The immediately prior browser cutover findings are source-superseded: `DocumentsPage` consumes `snapshot.rows`, and the relay now admits exact `limit`/signed-cursor pages, typed intents, and operation status/cancel while rejecting malformed query shapes (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/📄️DocumentsPage/🟦️.tsx:35-50`; `🌎️hub/📦️packages/🦀️rust/📜️script.ts:44-62`). This repairs the cited deterministic client/relay incompatibilities. It does not discharge the pending admin operation atomic-capacity/panic-cleanup law, remaining unbounded document/space overview paths, or any runtime test requirement.

## Most Recent Re-read — D0 and Admin Source Precedence (2026-09-03)

This is the controlling successor for the earlier D0 and admin paragraphs in this report. The D0 ledger **does** now have a production `HubState` owner (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:1169,4558`), and its Cargo target has a nonzero-test preflight (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1718-1726`). It still has no router issue/exchange endpoint (`📦️bin.rs:4315-4345`), so it remains fail-closed and not an end-to-end D0 delivery. The gate's hand-written Node structural/negative oracle is independent of Rust, but its three TypeScript codec checks import the shipped parsers and are self-parity only (`📜️script.ts:1707-1709`). A current terminal gate must show the preflight/list and real test execution; none is credited here.

The current D0 completion blocker is revocation integration. Although `DocumentOpenPlanLedgerV1::invalidate_binding` exists, the real share and session revoke branches call only `state.socket_grants.invalidate_binding` (`📦️bin.rs:3820-3886`). Before a D0 route is released, each must invalidate the D0 binding while holding the same admission gate and a real revoke-versus-exchange law must establish that no receipt survives a successful revoke.

The earlier admin capacity/panic-cleanup hold is source-superseded. A 64-owned-permit semaphore admits rebuilds before runtime insertion and `AdminOperationCleanup` removes the runtime/releases the permit/records the pre-armed interrupted terminal on abort (`📦️bin.rs:3783-3813,3960-4002`); the focused 128-contender plus abort law is at `:5866-5960`. It directly validates the permit and cleanup seam, not an actual HTTP rebuild lifecycle, so current terminal gate evidence remains required. The current registered relay gate runs a local adversary followed by uncached `os-hub-admin:test` (`📜️script.ts:1849-1853`); its component law proves connections-poll abort plus no successor after unmount, and active-tab cleanup (`🧪️admin.test.tsx:250-366`). No current execution result is asserted.

## Most Recent Re-read — D0 Revocation Closure (2026-09-03)

The immediately preceding D0 revocation-integration finding is source-superseded. Current share revocation invalidates the share-bound open-plan ledger before releasing its admission (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:3855-3860`); user-session revocation does the same for every revoked session binding (`:3871-3876`). Session deletion and ordinary socket binding revalidation also invalidate D0 records (`:1664-1668,3384-3388`). The D0 ledger is still unexposed and readiness remains `openPlan: false`; consequently this closes the source fan-out only and does not establish a process/route-level revoke-versus-exchange result. The current unbounded-admin-read finding remains: overview uses `list_users(i64::MAX, 0)` and documents fans out `list_spaces(i64::MAX, 0)` (`:4043-4060,4148-4164`).

## Most Recent Re-read — Exact D0 Gate Selection (2026-09-03)

The D0 target was strengthened after the previous entry. It now lists both the lib schema selector and the hub-bin ledger selector, requires **exactly one** `: test` name ending in each expected suffix, logs those returned fully-qualified names, then calls Cargo with each name and `--exact` (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1718-1736`). This closes source-level zero-test and substring-ambiguity concerns for the registered D0 check. It does not make the Node shipped-code parser comparisons independent, add a D0 route, or turn a source registration into execution evidence.

The newly registered, non-cached `admin-backend-check` is launch-seeded and combines its fixture oracle, Rust admin filters, local relay adversary, uncached SPA test, and hub check (`📦️rust/📋️project.json:87-93`; `📜️script.ts:1869-1880`; `.vscode/🧩️launch.seed.jsonc:3063-3069`). Its broad `admin_` Cargo bin filter has no asserted exact count, unlike D0. More importantly its success wording “bounded snapshots” exceeds current handler behavior: `admin_spaces` returns every projection-space, `admin_documents` returns every requested-space document or fans out every directory space, and overview enumerates all users (`📦️bin.rs:4043-4164`). A passing command cannot by itself negate those current-byte semantic gaps.

## Most Recent Re-read — Admin Principal and Audit Parity (2026-09-03)

The old mutation-side static-admin/generic-command observations are source-superseded. The strict `AdminIntentV1` is a closed operation taxonomy (`🧰️framework/🛍️products/💻️os/🔨️modules/📇️directory/🧬️schema/🦀️.rs:217-234`), its command projection is exhaustive (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:3507-3519`), and command events use the verified request user/session actor (`:1385-1388,3817-3829`). `is_admin()` still reduces successful principal resolution to a boolean, but current callers are read-only admin routes (`:1586-1588,4043-4164`), not a mutation actor.

Audit target parity is still incomplete for create-space. Accepted and terminal operation records describe `targetKind: "space"` but set `targetId` to the request id (`📦️bin.rs:3522-3525`), while `DirectoryCommand::CreateSpace` mints an independent space id inside `decide` (`🌎️hub/📇️directory/🦀️.rs:1324-1332`). The stored event sequence range permits later forensic correlation but the operation audit record itself does not identify the resource it claims as its target. Because accepted and terminal records must agree on `targetId` for idempotency (`🌎️hub/📇️directory/🦀️.rs:929-938`), repairing this requires an idempotently preassigned space id (or an explicitly distinct request-target taxonomy), plus a cross-backend law—not merely changing the terminal fact. No runtime result is claimed.

## Live RED — D0 Revocation Law Has a Missing Fixture Include

The new real-handler revocation law is the right shape: it issues session and share plans, executes `execute_admin_intent` for each corresponding admin revoke, and proves subsequent ledger exchange is stale (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:5566-5635`). The registered exact D0 gate selects it as a third named law (`📜️script.ts:1732-1738`). It currently cannot compile because its `include_str!` spells a nonexistent emoji path, `🧰️framework/🛐️products/💻️os/🪛️fixtures/.../document-open-plan-v1.json` (`📦️bin.rs:5569`); the only current fixture is `🧰️framework/🛍️products/💻️os/🧫️fixtures/📇️directory/📄️document-open-plan-v1.json`. This is a direct source/compile hold, not an unrun-test qualification.

## Most Recent Re-read — D0 Include Fixed; Neutral Oracle Parity Is RED

The missing-fixture include immediately above is source-superseded: the revocation law now references the actual `🛍️products/…/🧫️fixtures` file (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:5566-5570`). The freshly enlarged neutral fixture adds `U+0085` C1-control values for `surface.appId` and checkpoint `headEditId`, both with expected outcome `denied` (`🧰️framework/🛍️products/💻️os/🧫️fixtures/📇️directory/📄️document-open-plan-v1.json`). Rust explicitly rejects those values (`🧰️framework/🛍️products/💻️os/🔨️modules/📇️directory/🧬️schema/🦀️.rs:1118-1124`).

The gate's claimed independent Node oracle does not: its `text()` filter excludes only ASCII C0 plus `U+007F` (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1625-1629`), so `U+0085` passes its structural decoder. `documentOpenNeutralOutcome` then classifies each changed public field as `stale`, not the fixture's required `denied` (`:1660-1684`). The new vectors make `open-plan-check` fail in its Node phase before Cargo. Extend the neutral text grammar to the same Unicode-control set as production and retain both vectors; do not weaken their expected denial.

## Most Recent Re-read — D0 Unicode Oracle Repair (2026-09-03)

The Unicode-oracle RED immediately above is source-superseded. Current `documentOpenNeutralStructure.text()` rejects the full Unicode control category with `\p{Cc}` (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1621-1626`), so the retained `U+0085` fixture vectors classify `denied`, as production does. The current fixture has fifteen negatives and the registered gate selects the exact schema, ledger, and real-handler revocation laws. This is still source review only: no Node-oracle, Cargo-list, or Cargo-test terminal has been run or credited by this audit.

## Most Recent Re-read — Admin Gate Oracle Is Not Handler Parity (2026-09-03)

`admin-backend-check`'s AJV program is independently executable, but it validates only its self-contained fixture and hand-derived actor/projection (`🌎️hub/📇️directory/🧪️fixtures/🧬️admin-intent-v1/🧪️oracle/🟦️.ts:1-33`). It neither invokes a shipped TypeScript admin client nor exercises the Rust handler, so it cannot prove handler/fixture parity. In particular, the fixture declares a 65,536-byte response limit (`🔣️.json:3`) while `ADMIN_RESPONSE_MAX_BYTES` is merely imported and current overview/spaces/documents handlers do not impose it (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:54-57,4043-4164`). The similarly named `admin_document_cursor_is_principal_route_and_exact_page_bound` law checks generic cursor encode/decode and page-limit helpers, not `admin_documents` (`📦️bin.rs:5961-5983`). This makes the gate's “bounded snapshots” success text non-evidentiary for those handlers, even if it later exits green.

## Most Recent Re-read — Admin Audit Backend Coverage Is Not Yet Cross-Backend

The core first-writer/first-terminal law uses `fresh_dir()`, which hard-codes in-memory SQLite (`🌎️hub/📇️directory/🦀️.rs:3286-3290,3317-3347`). There is a separate real-Postgres concurrent-absent-request test (`🌎️hub/📇️directory/🐘️postgres/🦀️.rs:2140-2164`), but no corresponding Neo4j concurrency law was found in the current tree. The broad `admin_operation_audit_` Cargo filter is not preceded by a list/count assertion, so it does not itself state which backend laws execute; a terminal all-feature run cannot be represented as cross-backend proof without its exact discovered outcomes and the requisite external backend availability. This is a source-coverage qualification, not a claim that any backend is currently failing.

## Most Recent Re-read — Exact D0 and Admin Gates; Residual Admin Space Read (2026-09-03)

The prior broad-filter qualification for `admin-backend-check` is source-superseded. Both it and `open-plan-check` now enumerate each selected Cargo subject with `--list`, require exactly one fully-qualified `: test` name ending in the expected suffix, log the resolved names, then run each with `--exact --test-threads=1` (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1749-1769,1901-1927`). D0 selects the schema, digest-only/bounded/single-use ledger, and handler-level revoke law. The admin target selects six explicitly named subjects: both audit-race laws, bounded projection, strict intent taxonomy, cursor boundary, and permit/abort cleanup. Both targets are non-cached Nx targets and launch-seeded (`📦️rust/📋️project.json:87-108`; `.vscode/🧩️launch.seed.jsonc:3063-3081`). This closes source-level zero-test and substring-selection concerns; it is not a terminal execution result.

D0's Node program is only partly independent. It derives descriptor and catalog encodings plus the receipt SHA-256 digest without using the shipped parser, and hand-validates a plan with fifteen mutations. Its intent, plan, and exchange codec assertions import the shipped TypeScript parsers, and it uses the shipped plan reader for denied/expired vectors (`📜️script.ts:1714-1746`). Thus it cannot be called an independent reader/oracle for the intent or exchange contracts. The three selected Rust laws are genuine internal laws—the ledger covers parallel one-winner exchange, restart denial, expiry replacement, binding/global limits, and digest-only storage (`📦️bin.rs:5518-5610`), while the revoke law calls `execute_admin_intent` (`:5611-5657`)—but no router endpoint issues or exchanges a plan. The live router has no D0 path (`:4368-4400`), so the feature stays fail-closed. An eventual issuer must independently prove nonempty verified-catalog selection: current internal `DocumentOpenPlanAuthorityV1::validate` compares the already supplied `catalog.generation_id` but does not resolve it to a live selected catalog (`:859-905`).

The former unbounded overview/documents observation is source-superseded. Overview now calls backend `admin_overview_counts`, and documents uses signed page cursors plus `limit + 1` against `list_document_descriptors_page` (`📦️bin.rs:4080-4095,4186-4219`; `📇️directory/🦀️.rs:1956,3346-3354`). The MCP process probe, SPA, and relay have concurrently moved to the `{ rows, nextCursor }` page with a mandatory bounded `limit`; the relay's local adversary accepts that exact form and rejects an unbounded documents request (`📦️rust/📜️script.ts:44-70,883-890,1431-1437`).

`admin_spaces` and `admin_space` remain an exception, so the admin gate's blanket “bounded snapshots” success text is still too broad. The collection route folds every read-model space and calls `space_view` for each; that helper reads all documents for the space and active sessions (`📦️bin.rs:4098-4120,2889-2917`). The detail route likewise returns every document and invite for its space (`:4111-4120`). Neither route has a page/cursor contract and neither is among the six selected backend laws. No test/build/runtime result is claimed in this source re-read.

## Live RED — Admin Relay Has No Aggregate Admission or Caller-Cancellation Fence

`startLocalAdminRelay` caps each body/response at 1 MiB and applies a two-second upstream deadline, but carries neither an in-flight counter nor a collection of upstream abort controllers (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:83-177`). A cookie-authorized loopback client can start an unbounded number of concurrent requests, each retaining a body and an upstream fetch for up to the deadline. In addition, it does not attach `request.signal` to the upstream request, so browser unmount/abort does not release an in-flight relay fetch early. The adjacent browser relay does both: it limits `inFlight` to 64 and joins a request-owned controller with the timeout (`📜️script.ts:239-311`).

The registered relay oracle does substantively cover one-use proof bootstrap, host-only HttpOnly/Strict cookie issuance, raw-local rejection, same-origin unsafe requests, bounded document admission, rejection of an unbounded/legacy query, and cookie expiry (`📜️script.ts:1385-1458`). The SPA test separately proves that the Connections page aborts its pending poll and creates no successor after unmount, and that only the active panel mounts its poller (`🌎️hub/🔨️modules/🛡️admin/📦️packages/🟦️typescript/🧪️admin.test.tsx:267-375`). Neither test observes the relay's upstream cancellation, concurrent-capacity refusal, or aggregate retained-body bound. Therefore the relay/browser source cannot yet support a bounded-memory or prompt-cleanup acceptance claim; no runtime result is attributed.

## Successor Re-read — Admin Space Collection and Create-Space Audit Target Repaired; SPA Is Now Stale (2026-09-03)

The immediately preceding collection-space N+1 observation and the earlier create-space target-parity observation are source-superseded. `admin_spaces` now uses a storage-folded, bounded `list_admin_space_summaries_page`, a principal-bound route-6 cursor, and `admin_fit_page` to trim serialized bytes below `ADMIN_RESPONSE_MAX_BYTES` (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:4152-4193,3509-3550`; `🌎️hub/📇️directory/🦀️.rs:1976,2338-2348`). The three directory backends expose that summary aggregate. `CreateSpace` now deterministically derives `admin-space:<SHA-256>` from its request id for both the accepted/terminal audit target and `execute_create_space_with_id` (`📦️bin.rs:3589-3600,3892-3897`), so audit target identity no longer depends on indirect event-range correlation.

That correct backend cutover has created a live browser contract break. The route now returns `AdminPageV1<SpaceView>`, while `AdminClient.spaces()` still declares `Promise<readonly SpaceView[]>` and `SpacesPage` spreads its result as an iterable array (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/🔑️AdminSession/🟦️.tsx:108-109`; `🏛️SpacesPage/🟦️.tsx:193-203`). A real `{ rows, nextCursor, observedAtMs }` response therefore rejects at the spread and is caught as an empty space list. The relay permits only queryless `/admin/api/spaces`, the client cannot follow the returned cursor, and the current SPA tests still mock the old array (`📦️rust/📜️script.ts:47-55`; `🧪️admin.test.tsx:219,330`). The individual `admin_space` detail route also still returns all documents and invites without a response-size/page bound (`📦️bin.rs:4195-4206`). This is a current source integration hold, not a runtime claim.

## Successor Re-read — Space Page Consumer Caught Up; Relay Allowlist Now Blocks It (2026-09-03)

The page-shape mismatch in the paragraph immediately above was repaired while this audit was reading. `AdminClient.spaces(cursor?)` now requests `?limit=100` and returns `AdminPageV1<SpaceView>`; `SpacesPage` consumes `page.rows`; the SPA fixtures now use page objects (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/🔑️AdminSession/🟦️.tsx:108-110`; `🏛️SpacesPage/🟦️.tsx:193-203`; `📦️packages/🟦️typescript/🧪️admin.test.tsx:215-220,326-332`). This closes that direct client decode defect.

It exposes a deterministic secure-admin relay mismatch instead. `adminRelayApiPath` admits `/admin/api/spaces` only when `url.search === ""` (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:44-55`), whereas the current client always sends `?limit=100`. The relay returns 404 before the hub handler sees the page request. The SPA tests point at a direct mock hub and the local relay oracle covers bounded connections and documents but not a bounded spaces request (`📜️script.ts:1385-1458`), so neither detects the real relay route failure. The `admin_space` detail endpoint's unpaged documents/invites response remains a separate source qualification. No execution result is attributed.

The Connections poller does correctly preserve its prior rows and mark freshness false on an ordinary failed poll (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/🔴️ConnectionsPage/🟦️.tsx:46-60`). The current test suite proves fresh success plus pending-poll abort/no successor after unmount, but has no success-then-failed-poll assertion for stale status and retained rows (`📦️packages/🟦️typescript/🧪️admin.test.tsx:245-304`). That is a source-test coverage gap only; no terminal test result is claimed.

## Most Recent Re-read — Admin Relay Bounds Closed; Space Detail Contract Is RED (2026-09-03)

The preceding relay aggregate-admission/caller-cancellation RED is source-superseded. The relay now has a 64-request admission cap, a request-owned `AbortController` connected to `request.signal`, a tracked controller set, and stop-time cancellation/idle wait (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:97-205`). Its local adversary now observes downstream abort propagation, fills the exact 64 slot boundary and rejects the 65th request, proves stop cancels an active upstream, retains the request/response max-plus-one checks, and validates the bounded spaces page admission (`:1410-1551`). The stale-on-failure browser coverage gap is also source-superseded: the SPA suite now succeeds once, makes the next connection poll fail, and asserts both the retained row and `Stale snapshot` (`🌎️hub/🔨️modules/🛡️admin/📦️packages/🟦️typescript/🧪️admin.test.tsx:307-349`). These are source laws only; no terminal execution is credited.

The newly paginated backend space-detail response has instead exposed a deterministic current client break. `admin_space` returns a `SpaceView` with **only** `members: AdminPageV1<MemberView>` after principal-bound cursor/limit and byte trimming (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:4206-4258`). `AdminSession` still declares `AdminSpaceDetail` as `members: MemberView[]; documents: DocumentView[]; invites: InviteView[]` and the page spreads `detail.members` as an array (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/🔑️AdminSession/🟦️.tsx:49,112-114`; `🏛️SpacesPage/🟦️.tsx:121,204-206`). A real detail response therefore makes expansion fail because the members value is an object, not iterable; the old documents/invites declaration is likewise false. The strict no-query relay admission matches the current client, so this is a backend/client response-frame mismatch, not an allow-list failure. No SPA law opens a row against that live response shape. This remains a source RED until the typed client/page consume the member page (and expose a bounded continuation policy) with a real response-shaped test.

The registered `admin-backend-check` itself is now source-non-vacuous for seven named Cargo laws: it lists, exact-one validates, and runs two audit races, bounded overview/space/document projections, strict intent taxonomy, cursor boundary, response byte boundary, and rebuild cleanup, then invokes the local relay adversary, uncached SPA suite, and all-feature hub check (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:2021-2048`). The current exact selection does **not** include an `admin_space` detail law, so it cannot observe this contract break. The D0 oracle remains genuinely independent for its manual intent/plan/exchange structural readers and cryptographic encodings, while the following imported shipped TypeScript parser comparisons are correctly only production parity (`📜️script.ts:1721-1865`); neither layer proves a D0 HTTP issue/exchange route, which remains absent. No command terminal is asserted in this audit.

## Most Recent Re-read — MCP Process Entry Has a Poisoned Marker Escape (2026-09-03)

The normal current MCP hub path is source-aligned: the entry point attempts class-bound `mcp` claim before argv parsing (`🧰️framework/🛍️products/💻️os/🔨️modules/🌉️mcp/📦️bin.rs:144-183`), `--token`/bridge-token argv carriers are rejected (`:199-209`), and the library retrieves the process-owned credential only through `claimed_local_hub_credential("mcp")` to construct the private hub driver/HTTP transport (`🌉️mcp/🦀️.rs:649-735`). `HeadlessWorkspace::open_hub` injects the credential and typed socket-grant source into `ArtifactHost`, not into `WorkspaceOrigin` or a serializable workspace descriptor (`🏠️workspace/🦀️.rs:441-474,1226-1243`). This closes the earlier raw bearer/`set_token` source fan-out; it is not process evidence.

There is one current marker-sealing escape. `protected_credential_environment_is_absent()` permits every environment entry named `S_LOCAL_CREDENTIAL_FD` regardless of its value (`📦️bin.rs:107-121`), whereas the actual protected-entry branch recognizes only the literal value `"3"` (`:149-159`). A parent can therefore inject `S_LOCAL_CREDENTIAL_FD=<raw secret or arbitrary value>`; the seal accepts it, no fd claim occurs, and folder-mode/plugin activation retains an untrusted raw carrier in the process environment. Restrict the sole exemption to the exact fd marker `S_LOCAL_CREDENTIAL_FD=3` on the immediately claiming path and reject every other value before argv/plugin handling. Add a real poisoned non-`3` child law in addition to the current normal direct-child/descendant probe. This is a source RED; no compile or runtime result is attributed.

## Successor Re-read — Admin Detail Contract Is Source-Aligned; UI Continuation Is Absent (2026-09-03)

The immediately preceding Admin detail-frame RED is source-superseded. The client now models `SpaceView & { members: AdminPageV1<MemberView> }`, requests a bounded `?limit=100`, and the rendered member table reads `detail.members.rows` (`🌎️hub/🔨️modules/🛡️admin/🧱️elements/🔑️AdminSession/🟦️.tsx:48,107-113`; `🏛️SpacesPage/🟦️.tsx:121,204-220,298`). The backend authenticates the administrator before bounded input handling, scopes cursor route 7 to that principal *and* space, fetches only `limit + 1` members, and repeatedly reduces the member page until its serialized detail is at or below `ADMIN_RESPONSE_MAX_BYTES` (`🌎️hub/📦️packages/🦀️rust/📦️bin.rs:4212-4248`). The local relay permits that detail endpoint only with a valid bounded page query. The byte-bound Rust law does construct a large member detail and asserts both a shortened row set and continuation cursor (`📦️bin.rs:6175-6179`). No current backend response-size, N+1, or route-admission defect was found on this specific detail path.

There remains a bounded-product pagination gap, not an unbounded-read defect: `SpacesPage.loadSpaces()` discards `page.nextCursor`, and `loadDetail()` always calls `client.space(spaceId)` without the supported cursor (`🏛️SpacesPage/🟦️.tsx:198-227`). Consequently the UI displays only the first page of spaces and, for an expanded space, only its first member page; it has no continuation control or accumulating page state. The API never asks the hub for an unbounded page, but an administrator cannot inspect a collection beyond the initial bounded response. The SPA test verifies direct client URL shape but does not mount an expanded detail using a real `{ members: { rows, nextCursor } }` response and exercise a continuation. These are current source coverage/product gaps.

The coordinator-reported `admin-relay-check` terminal (14 SPA tests, including cap/abort/stop/size laws) is recorded only as external evidence. This audit did not rerun it and therefore does not upgrade the report to an independent runtime verdict.

## Residual Qualifier — Static Relay Requests Bypass the API Ownership Fence (2026-09-03)

The admission/cancellation closure above applies to authenticated `/admin/api/*`. The separate public static-shell branch for `GET /admin/*` performs a direct upstream fetch with only `AbortSignal.timeout(LOCAL_RELAY_DEADLINE_MS)`, then streams its body; it does not join `request.signal`, enter the 64-slot `inFlight` counter, or join the stop-owned controller set (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:131-137,151-205`). It intentionally sends no `Authorization` header and has no capability/cookie forwarding, as the local oracle confirms (`:1417-1423,1450-1453`), so this is **not** an administrator-authority or raw-bearer exposure.

If the capacity and prompt-cleanup law is intended to govern every relay path rather than only authenticated API work, static assets still lack that common ownership fence. Otherwise the current API boundary is source-closed. No independent runtime result is claimed.

## Most Recent Re-read — Native Receipt Epoch Source Fence Is Closed (2026-09-03)

The native actor now has one `clear_socket_epoch()` clearing the receipt actor, its explicit Session confirmation bit, and color (`🧰️framework/🛍️products/💻️os/🔨️modules/🏪️store/🔄️sync/🦀️.rs:1803-1807`). Every inspected terminal native connection path invokes it: artifact-bootstrap failure (`:1809-1821`), EOF/transport error (`:1918-1936`), failed local replay (`:2023-2029`), failed send (`:2302-2315`), and unsuccessful connect (`:1885-1907`).

The actor queues, rather than stamps or sends, whenever Session confirmation is false; only a matching `ServerFrame::Session` flips that bit and causes a flush (`:2273-2294,2214-2222`). The focused direct-socket law drives an actual socket through bootstrap failure, EOF, a queued mutation before fresh Session with no received command frame, exactly one fresh-actor command batch after Session, and the same barrier after reconnect (`:4570-4665`). This closes the source-level stale-epoch and pre-Session-delivery concern. It remains source/law review only pending an independent terminal.

## Live RED — Current WGPU Frame-Worker Freshness Gate Cannot Load (2026-09-03)

I independently ran `bun nx run @semio-tech/framework-renderer-wgpu:check-frame-worker --skip-nx-cache`. It exited **1** before the carrier census or deterministic render comparison: Bun cannot resolve the styling import `../../../../../../../../../🔨️modules/🖱️ui/🎨️🟠️styling/🟦️.ts` from `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🎯️targets/🧊️wgpu/📦️packages/🦀️rust/📜️script.ts:25`. Thus prior fresh-artifact reports do not establish the current-tree generator boundary.

Manual current-byte inspection finds no `S_USER`, `VITE_S_USER`, `Authorization`, `Bearer`, `set_token`, `hub_token`, or legacy Hello residue in the shipped `🟨️frame-worker.js`. That is not an execution substitute. Moreover, the owner census currently rejects only three exact legacy strings (`/directory/ws?token=`, one historic Bearer template, and `this.token = token`) (`📜️script.ts:216-225`), rather than the broader protected-carrier taxonomy it is meant to police. Repair the moved import, make the census cover every disallowed carrier representation, then rerun the uncached target before accepting the shipped browser artifact.

### Successor attempt

The styling path and carrier list were repaired immediately after the first attempt: the latter now includes query token, authorization/Bearer, token setter, hub token, raw environment, browser storage, and cookie markers (`📜️script.ts:220-228`). I reran the same uncached target. It again exited **1** before the comparison, now because `🧵️frame-worker/🟦️.ts:6` imports nonexistent `../🧵️📇️interactive-job-registry/🟦️.ts`; the live module is `../🧵️interactive-job-registry/🟦️.ts` without `📇️`. This successor supersedes the first missing-import *cause*, not the live freshness hold: no successful census/render terminal exists yet.

### Terminal successor

After the frame-worker import chain was repaired, I independently reran `bun nx run @semio-tech/framework-renderer-wgpu:check-frame-worker --skip-nx-cache`; it exited **0** and printed `framework-renderer-wgpu: 🟨️frame-worker.js is fresh`. This is a successful current deterministic bundle comparison and its broadened current carrier scan. Direct case-insensitive inspection of the shipped bytes found no authorization, bearer, credential, `S_USER`/`VITE_S_USER`/`S_HUB_URL`, storage/cookie, query-token, hub-token, token-setter, or legacy Hello residue; visible generic `token` occurrences are internal UI-operation tokens, not raw authority. This supersedes the live generator-load hold.

The future-regression guard has one source qualifier: its `includes` policy is case-sensitive and therefore does not catch a future object-literal lower-case `authorization`, nor `access_token`/`?access_token=` variants (`📜️script.ts:220-228`). Normalize the inspected artifact string to lower case and express the bearer/query forbidden terms in that normalized vocabulary. The current shipped artifact is fresh and clean; this qualifier concerns durable census completeness, not an observed carrier.

### Census hardening successor

The case-sensitivity qualifier is source-superseded. The scanner now lower-cases deployed bytes and rejects lower-case authorization, bearer, credential, `?token=`, `?access_token=`, `access_token`, token setter, hub-token, raw environment, storage, and cookie forms (`📜️script.ts:220-229`). I independently reran the same uncached `check-frame-worker` target after that change; it exited **0** and again confirmed the deterministic shipped worker is fresh. This accepts the current browser artifact/freshness boundary. It does not provide native/MCP process proof, which remains a separate compound-smoke requirement.

## Successor Re-read — MCP Non-fd3 Marker Escape Is Source-Closed (2026-09-03)

The poisoned-marker RED above is source-superseded. The protected-environment predicate now receives both key and value and permits `S_LOCAL_CREDENTIAL_FD` only when that value is the literal `"3"` (`🧰️framework/🛍️products/💻️os/🔨️modules/🌉️mcp/📦️bin.rs:107-125`). The class-bound claim remains before argument parsing and activation (`:146-160`). The MCP package probe creates a capability-shaped, non-fd3 marker, requires a nonzero result, and verifies neither stdout nor stderr repeats it (`📦️packages/🦀️rust/📜️script.ts:29-57`). This closes the source-level raw-environment carrier and gives the expected process-probe shape; no independent compiled-binary probe has been run by this audit.

The native entry point now has the same exact-value predicate before inherited claim, probe, plugin selection, and renderer activation (`🧊️wgpu/⌨️native-entrypoint/🦀️.rs:35-89`). The compound secure-local smoke invokes actual native envelope/descendant delivery, then an actual native non-fd3 child; it likewise performs MCP delivery then an actual MCP non-fd3 child (`🌎️hub/📦️packages/🦀️rust/📜️script.ts:1115-1159,1356-1363`). Each child captures bounded stdout/stderr and rejects success or poison reflection. This is a sound registered runtime-probe path, but it must complete its compound terminal before native/MCP process behavior is credited.
