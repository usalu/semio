[package]
name = "semio-framework-os-kernel-protocol-wire"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Protocol hub wire frames — lane-tagged client/server frame types shared by native and wasm sync clients"


[lints]
workspace = true
[lib]
name = "protocol_wire"
path = "📦.rs"

[dependencies]
protocol_core = { path = "../core", package = "semio-framework-os-kernel-protocol-core" }
protocol_causal = { path = "../causal", package = "semio-framework-os-kernel-protocol-causal" }
protocol_command = { path = "../command", package = "semio-framework-os-kernel-protocol-command" }
