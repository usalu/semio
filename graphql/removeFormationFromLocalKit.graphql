mutation RemoveDesignFromLocalKit(
  $directory: String!
  $design: DesignIdInput!
) {
  removeDesignFromLocalKit(directory: $directory, designId: $design) {
    error {
      code
      message
    }
  }
}
