mutation UpdateLocalKitMetadata($directory: String!, $kit: KitMetadataInput!) {
  updateLocalKitMetadata(directory: $directory, kitMetadataInput: $kit) {
    kit {
      name
      description
      icon
      url
      homepage
    }
    error {
      code
      message
    }
  }
}