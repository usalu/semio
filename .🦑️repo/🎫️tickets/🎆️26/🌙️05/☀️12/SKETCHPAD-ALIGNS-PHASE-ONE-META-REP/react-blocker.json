{
  "blocked": true,
  "reason": "@semio-tech/compose-js public surface no longer matches compose/react/index.tsx imports; compose/js/index.ts still has TypeScript errors (e.g. gqlRun undefined, strict JsonObject indexing). Sketchpad imports legacy symbols (KitScope, asKitInstance, InMemoryKitStore, kitStoreFromKitStoreClient, …) that are not exported from the refactored compose/js bundle.",
  "evidence": {
    "compose_js_tsc": "npx tsc -p compose/js --noEmit fails (Piece/Design gqlRun and JsonObject index errors on current tree)",
    "compose_react_tsc": "npx tsc -p compose/react --noEmit reports TS2305 missing exports from @semio-tech/compose-js starting at applyKitClientSnapshotToLocalStore",
    "piece_reads": "Piece class in compose/js exposes mutations but no readName/readPlane helpers; schema-1:1 read hooks need either JS read methods or shared GraphQL selection helpers on Piece"
  },
  "recommended_unblock": [
    "Repair compose/js/index.ts until tsc is clean and restore or replace exports sketchpad and react rely on",
    "Either split compose/react into a legacy barrel plus schema-hooks module, or update sketchpad imports in a follow-up ticket",
    "Add Piece (and other artifact) read* methods in JS aligned with target.schema.graphql, then bindFieldToReact factories in react can subscribe via Kit.subscribe"
  ],
  "ticket_mcp": "repo://ticket/🎫️reactfieldkitreadscqrsclasses"
}
