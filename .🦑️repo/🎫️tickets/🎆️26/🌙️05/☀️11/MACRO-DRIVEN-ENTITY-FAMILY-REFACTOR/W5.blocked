W5 blocked per gate (2026-05-11).

1) Missing W0.complete — expected at:
   .repo/🎫️/26/05/11/MACRO-DRIVEN-ENTITY-FAMILY-REFACTOR/W0.complete
   (only W0.check.log + ticket.json present when checked.)

2) cargo check (compose/rs) not green — exit 101, 76 errors. See W5.check.log in this folder.
   Dominant themes: vcs::Graph no longer has drafts/parent_root_for_active_draft/op_history;
   materialized_kit_for_draft vs materialized_kit_at_point; missing KitReadPointInput; frozen
   binding; iface imports ReadVersion/WriteVersion from vcs (types absent there).

3) entity_family! / entity_input! are not defined in lib.rs yet (plan §2 only); W5 Kit macro
   conversion depends on W0 foundation + a compiling tree.

Unblock: add W0.complete after W0 finishes, fix compose/rs until cargo check passes, then rerun W5.
