CLEANUP LANE — FOLLOW-UPS FOR OTHER FILE OWNERS
================================================
Written by the CORE cleanup pass. Repo-wide grep (excluding node_modules/dist/.git/target/build/.nx,
.claude/worktrees/* [other agents' worktrees], .repo/⚡️/🤖️/*/session.json [session logs], and
.repo/🎫️/*/backup|before/* [archived snapshots from unrelated past tickets]) for every identifier in
the contract's DELETED section turned up 3 real remaining hits, all of them comment-only (no live
class names, props, or behavior depend on them) and all in files owned by other lanes. Per
instructions I did NOT touch these — they are not mine to fix.

1. WGPU-owned: framework/renderer/wgpu/rs/lib.rs
   - Line ~22828: doc comment "the React shell now renders one fullscreen `ui-glass-veil` div..."
   - Line ~22855: doc comment "A *real* glass veil (matching React's `ui-glass-veil`) is infeasible here..."
   - Line ~25654 (inside push_solid overlay call comment): "...`ui-glass-veil`'s own theme-agnostic
     dimming in `ui/js/react/index.tsx`."
   All three just mention the deleted CSS class name `ui-glass-veil` for cross-reference/rationale
   purposes explaining why this renderer uses a solid-fill veil instead of a real glass veil. The
   current React veil class is `ui-veil` (see `veilClass` export, ui/js/react/index.tsx). Suggested
   fix: s/`ui-glass-veil`/`ui-veil`/ in these 3 comments only — no functional change.

2. WGPU-owned: ui/wgpu/rs/lib.rs
   - Line ~12304 (doc comment on the `Level` enum, `//#region 🔖️Level`): "Replaces the old unlinked
     `Level` (canvas/window/panel/overlay/temporary) + `GlassTier` (panel/ribbon/menu/windowOptions)
     axes with one formula-derived enum." This literally contains the string `GlassTier`, which is on
     the contract's DELETED list ("must not exist anywhere after this ticket"). This is a
     documentation-of-migration-rationale comment (mirrors the contract's own "OLD -> NEW MAPPING"
     section), not a live reference, but strictly it's a remaining hit. Suggested fix: reword to
     avoid the literal old identifier, e.g. "Replaces the old unlinked level-name axis
     (canvas/window/panel/overlay/temporary) plus a separate glass-tier axis (panel/ribbon/menu/
     windowOptions) with one formula-derived enum."

3. CODA-owned: coda/client/ui/desktop/js/renderer.tsx
   - Line ~2466 and ~3114 (two near-identical title-bar comments): '{/* Title Bar -- window-level
     chrome ribbon (GlassTier "ribbon" -> ui-glass-chrome @ window) */}' and the #region variant.
     Same pattern as above — a historical-mapping comment containing the deleted `GlassTier`
     identifier. Suggested fix: drop the parenthetical or reword to "(window-level chrome ribbon,
     ui-glass-chrome @ window)".

Everything else the repo-wide grep matched was noise, not real hits:
- `.repo/🎫️/*/backup/**`, `.repo/🎫️/*/before/**`: archived pre-refactor file snapshots belonging to
  OTHER, already-closed tickets (e.g. GENERALIZE-*, DEGENERALIZE-*) — not live source, not consumers.
- `.repo/⚡️/🤖️/**/session.json`: historical agent session transcripts, not code.
- `.repo/🎫️/*/ticket.json` for unrelated past tickets: historical ticket descriptions that happen to
  narrate the old system by name — not code.
- `.repo/🎫️/26/07/20/INTRODUCE-GENERAL-SHELL-BRANDING-WITH-ENTWERFEN-MIT-BESTAND-AGGREGATOR/
  aggregator-analysis.js`: a bundled/minified snapshot of the OLD pre-refactor build captured by a
  different, unrelated ticket's scratch analysis script — not part of the live build graph.
- `.repo/🎫️/26/05/29/ELEMENTS-UI-APP-MODE-WINDOW-ENGAGEMENT/golden-layout-css-recovery.css`: an old
  ticket's CSS recovery/backup scratch file, not the live `ui/styling/js/ui.css`.
- `.claude/worktrees/**`: other agents' worktree checkouts (out of scope, not the live shared tree).
- All `ui/js/react/index.tsx` hits (`.not.toContain("ui-glass-panel")` / `"ui-glass-menu"`) are
  INTENTIONAL negative test assertions enforcing the TEST-ASSERTION CONTRACT (asserting the dead
  class names are absent) — not remaining references, left as-is.
- "canvas" hits inside `ui/styling/tokens.json`'s `appearances.light/dark.canvas` block are an
  unrelated appearance group for actual HTML `<canvas>` 2D-rendering colors (rasterClear/iconFg/
  iconBg/labelFill/labelHalo) — nothing to do with the Level system, left as-is.

No other real remaining hits were found outside the CORE-owned files (ui/styling/tokens.json,
ui/styling/script.ts + regenerated outputs, ui/styling/js/ui.css, ui/js/react/index.tsx).
