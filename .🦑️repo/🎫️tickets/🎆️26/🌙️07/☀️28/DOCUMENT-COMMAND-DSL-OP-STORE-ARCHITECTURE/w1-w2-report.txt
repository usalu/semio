W1 + W2 report — Op binary codec mechanism (M-A)

Landed (all additive, no consumer source changes needed):

1. pack_value (pack/value/rs/lib.rs)
   - DecCtx.pack_file replaced by enum DecSource { File(&PackFile), Inline { symbols } };
     resolve_symref + read_chunked_bytes dispatch on it (Inline chunk refs are Malformed by
     construction).
   - New pub fns in 🔖️Document region:
     encode_record_body(spec, record, options) -> Vec<u8>
       layout: symbol_count varint, (len varint, utf8)*, record fields
       (container-less: no header/segments/manifest/footer; chunking disabled by forcing
       chunk_threshold = u64::MAX so oversized Bytes64 stays inline via TAG_BYTES)
     decode_record_body(bytes, spec, options) -> (RecordValue, DecodeReport)
   - 4 new tests in 🔖️RecordBody region (every-shape round trip incl. forced symrefs for
     statements/table, determinism, oversized-bytes-inline, unknown-field preserve+report).
     pack_value: 17/17 green.

2. pack facade (pack/rs/lib.rs): thin forwards encode_record_body/decode_record_body.

3. protocol_command (protocol/command/rs/lib.rs): new 🔖️OpBinary region beside 🔖️OpText:
     pub trait OpBinary { encode_op(&self) -> Result<Vec<u8>, ProtocolError>;
                          decode_op(&[u8]) -> Result<Self, ProtocolError>; }
   protocol facade re-exports OpBinary (protocol/rs/lib.rs:20).

4. dsl facade (dsl/rs/lib.rs): new 🔖️OpRt region — pub mod op_rt with
     OP_BINARY_FORMAT: u8 = 1
     encode_op<T: DslVariants> / decode_op<T: DslVariants>
   byte layout: format u8 (=1) | variant ordinal varint (indexes DslVariants::variants()
   declaration order — reordering variants is a format break) | record body.
   dsl/rs/Cargo.toml: pack + protocol promoted from (absent/dev) to regular deps — no cycle
   (protocol/pack dep only dsl_core/dsl_schema, never the dsl facade).
   NOTE: op_rt MUST live in dsl, not store — a store-hosted twin binds store's build of
   dsl::DslVariants, which inside dsl's own test build is a distinct trait instance
   (dev-dependency cycle); this was hit in practice and is documented on the module.

5. store (store/rs/lib.rs): 🔖️OpRt region = `pub use dsl::op_rt;` (facade law: apps may spell
   it store::op_rt). test_support gains assert_op_text_binary_equivalence (op-level mirror of
   assert_dsl_pack_equivalence: decode_op(encode_op(op)) == parse_op(print_op(op)) == op +
   determinism). 2 new tests on DemoOperation (round trip + reject wrong format/ordinal).
   store: 72/72 green.

6. dsl_derive (dsl/derive/rs/lib.rs): #[derive(DslOps)] now ALSO emits
     impl ::protocol::OpBinary for T { ... ::dsl::op_rt::{encode_op,decode_op} ... }
   → every op enum fleet-wide gains the binary codec on rebuild, zero app-source changes.
   dsl facade test derived_op_binary_round_trips_every_variant_and_matches_text covers the
   full law through the emitted impl. dsl: 16/16 green.

Verification: cargo test -p pack_value (17) -p pack (14+2) -p protocol_command (8) -p protocol
-p dsl (16) -p store (72) all green; cargo check -p store_sync -p vcs -p protocol_testkit
-p db_wal green; `cargo check --workspace --all-targets` exit 0 (fleet rebuilt under the new
derive emission, zero errors); `bun nx run @semio-tech/store-rs:wasm` green (dsl's new pack +
protocol deps are wasm-clean); store_sync 19/19 via the NEW nx target (below).

Also landed (M-F slice, G14 + stale-launch fix):
- store/sync/rs/project.json + script.ts (nx @semio-tech/store-sync-rs, test/-quick/-long/
  -exhaustive tiers, template pack/value/rs) — store_sync tests reachable via nx for the first
  time; verified `bun nx run @semio-tech/store-sync-rs:test` = 19 passed.
- .vscode/launch.json: fixed broken `🧪️verify🔁️os-sync` (still ran `cargo test -p
  semio-framework-sync`, a crate renamed to store_sync in the extraction); added
  `🧪️test🗃️store` (391.95) and `🧪️test🗃️store-sync` (391.951) after the db family.

Deviation from contract.md W1/W2 split: none in substance; op_rt hosting moved store→dsl for
the trait-identity reason above (contract updated? no — recorded here; store::op_rt path is
preserved as a re-export so downstream spelling is unchanged).

Next waves (not this session unless noted): W3 DocumentCommand text+binary grammar,
W4 history backwards+cursor persistence + storage flip, W5 wire binary flip + TS twin,
W6 kernel unification, W7 backbone factories, W8 fleet conformance (use
assert_op_text_binary_equivalence per app), W9 hygiene, W10 verification.
