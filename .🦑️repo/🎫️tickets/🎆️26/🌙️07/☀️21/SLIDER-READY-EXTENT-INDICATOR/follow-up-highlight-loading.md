# Follow-up

- Ready segment uses `--accent-secondary` (highlighted), not `bg-emphasized`.
- Fill measure label is fixed (`labels.fill`); no `X of Y (planning)` updates.
- `WindowMeasure::Slider.loading` drives the measure tree leaf loading ring while preload is incomplete.
