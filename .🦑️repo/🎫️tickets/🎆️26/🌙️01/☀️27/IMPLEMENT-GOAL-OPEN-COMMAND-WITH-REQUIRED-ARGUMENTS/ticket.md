# Ticket

## Todos

- [ ] Analyze `./repo/cli/main.go` and `graphql/repo/schema.graphql` for `goal` command structure.
- [ ] Implement `goal open` in `./repo/cli/main.go` with positional and flag arguments.
- [ ] Throw error if arguments are missing.
- [ ] Update GraphQL schema and resolvers for `goal open`.
- [ ] Update VS Code extension if necessary.
- [ ] Add unit tests for `goal open` command.
- [ ] Verify functionality.

## Changes

## Log

## Summary

Bulk close
