# Status

- W0–W8: done
- All seven stdio policy rules at 0 after post-close mutation schemaFormats leaf fill (reconfirmed live 2026-08-10: policyStdioCatalogBreaches/ArtifactBuilder/ArtifactDecomposer/SchemaRepresentation/IoSerializerMatrix/IoTerminality/CodecFidelity all 0)
- Reopened 2026-08-10 for Phase 2: standards/subsets + composable builder/analyzer/composer restructure, full plan at /Users/ueli/.claude/plans/the-current-import-export-snappy-thompson.md, waves continue at W9
- Note: repo-wide `bun ./📜️script.ts policy` currently reports 8758 breaches, but only 60 (`app-plugin/app-coupling`, medium priority, pre-existing -- domain plugins depending directly on the stdio crate) are related to this ticket's area; the other ~8700 (dominated by `handcrafted-grammar/spec-distinctness`, 6787) are unrelated backlog from other tickets/goals and are NOT this ticket's baseline. This ticket's zero-breach gate is scoped to the seven stdio-artifacts rules + new W9+ rules only, not the repo-wide sweep.
- W9 (SSOT + tooling): done -- generators/w9_tokens2.json (VS16-verified, collision-checked), w9_standards_table.json (29 codec-verified stdio standard slugs + domain default), w9_build_owner_table_v2.py (+ generated w9_owner_table_v2.json, 29/54/285 counts verified against W0-W8 baseline, idempotent), w9_claims.json
- W10 (vocabulary + dual-shape policy): done -- 🔣️taxonomy.json additive keys (standardsDirName/subsetsDirName/standardDirPrefix/standardSlugPattern/subsetDirs/new*ComponentDirs/new*ChildDirs/standard*Dirs/subset*Dirs + 4 new taxonomyLeafParentDirs entries); 📜️script.ts new region //#region 🏅️PolicyRuleStandardsSubsets (policyListArtifactDialectDirs, policyStandardsCoverageBreaches, policyArtifactAnalyzerBreaches, policyArtifactComposerBreaches) -- all three vacuous (0 breaches) since no artifact has migrated yet, confirmed by direct call; the seven original stdio policy rules reconfirmed still 0 after the taxonomy edit and again after the policy region insert; 3 new gates added to .vscode/🧩️launch.seed.jsonc + mirrored into .vscode/launch.json (orders 410.7-410.9)
- Registry `:check` currently fails with 6513 pre-existing findings (glue/path drift, e.g. 📜️imperative, 📸️remodel) -- confirmed via git diff that this predates and is unrelated to the W9/W10 edits above (documented as historical/W6-domain in 🧪w8-report.md, out of this ticket's scope); did not run `:generate` to avoid a large unrelated repo-wide diff on top of that known-broken state
- W11 (SDK + io kernel, catalog deferred): core groundwork done and verified --
  - New module 🧰️framework/🔨️modules/🚪️io/🦀️component.rs (crate semio-framework, mounted as `pub mod io` in 🧰️framework/📦️packages/🦀️rust/📦️glue.rs): StandardId/SubsetId/Dialect, AnalyzeSource/Confidence/Analysis, ComposeSource/Composition/ComposeError, IoPayload/ErasedComposeSource/ComposedArtifact/ComposerEntry, IoDirection/IoKey/IoResolveError, register_composer_entries/resolve/dialects_for (typed HashMap<IoKey,&ComposerEntry> registry, replaces the design for the stringly os/os-host media registry -- NOT yet wired into os/os-host, that's W15).
  - Plugin SDK (🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🦀️component.rs): deleted the confirmed-dead `//#region 🔖️ArtifactIo` (IoFormatSpec/ArtifactImport/ArtifactExport/ArtifactIo traits, zero impls repo-wide, verified before deletion) and replaced with `//#region 🔖️Dialect` re-exporting the new framework types + three NEW traits: ArtifactSerializer, ArtifactDeserializer, ArtifactComposer. Added ArtifactAnalyzer as a new, separate trait alongside the EXISTING ArtifactDecomposer (deliberately NOT renamed/removed -- adding a required assoc const to an existing trait, or renaming it, would break all 83 current implementors instantly; that rename is deferred to W16's global strict flip once every artifact has migrated off it, per the plan). Crate-root re-export list fixed to match (removed the 4 deleted names, added the 4 new trait names).
  - Verified: `cargo check` clean on semio-framework, semio-framework-os-kernel, semio-framework-os (host), semio-s-plugin-stdio, semio-s-plugin-note (only pre-existing unrelated warnings). Ran `cargo check --workspace`: 3 pre-existing failures (semio-framework-plugin-host: stale `os_dsl` vs `dsl_core` crate alias in its own untouched glue.rs; semio-framework-os-kernel-db + semio-compose-rs: unresolved `db_engine`/`db_wal`/`rusqlite` imports in the exempt/legacy db+compose areas) -- confirmed via `git diff`/`git status` that none of the three failures touch any file or line this session edited; all pre-date this ticket's reopen.
  - Deferred to a later pass: catalog.json SSOT consolidation (owner-table migration out of the ticket folder into the framework, STDIO_FORMAT_CATALOG merge, MediaFormat deletion) -- these are DESTRUCTIVE/deletion-heavy per the plan's own W16 sequencing, correctly done only after every artifact has migrated off the old shape, not now.
- W10.5 (policy shape-awareness fix, done): discovered while about to migrate the first real artifact that the seven original rules were NOT shape-aware -- they'd have regressed the instant one artifact migrated (e.g. policyArtifactDecomposerBreaches would flag a migrated artifact as "missing 🪓️decomposer"). Fixed: policyStdioCatalogBreaches/policyArtifactBuilderBreaches/policyArtifactDecomposerBreaches/policySchemaRepresentationBreaches/policyIoSerializerMatrixBreaches now skip migrated artifacts via new `policyArtifactIsMigrated` helper; added `policyArtifactBuilderMigratedBreaches` (mirrors Analyzer/Composer) since the old builder rule no longer covers migrated artifacts. Registered in policyStdioArtifactsBreaches aggregate + new launch gate `⚖️gate🏗️artifact-builder-migrated`.
- **W12 pilot (💾️binary, DAG root) -- DONE, fully verified end-to-end.** This is the reference migration every subsequent artifact should follow:
  - Moved 🧬️schema, ⚙️engine, 🚪️io verbatim into 🏅️standards/🔖️raw/{⚙️engine, 🪆️subsets/✳️any/{🧬️schema,🚪️io}}; inserted 🔖️raw/✳️any into the (self-referential) io leaf target paths.
  - Moved 🏗️builder content to the subset level (unchanged internals -- `crate::artifacts::binary::...` paths still resolve because nothing outside this artifact needed to change, thanks to glue shims); created thin delegating facades at standard and artifact-root levels (delegation chain: artifact -> standard -> subset).
  - Converted 🪓️decomposer -> 🧐️analyzer (real impl at subset level: renamed decompose/DecomposeSource/Decomposition -> analyze/AnalyzeSource/Analysis, added `sniff`, added `const DIALECT`); deleted the old 🪓️decomposer dir (verified zero external references to BinaryDecomposer first). Old ArtifactDecomposer trait itself untouched, still used by all 82 unmigrated artifacts.
  - Added 🎹️composer at all 3 levels: subset level is the real `ArtifactComposer` impl (analyzer+builder glued); standard/artifact levels aggregate value-level via `composer_entry_of::<C>()` (new helper added to the plugin SDK) into `Vec<&'static ComposerEntry>`, exposing `entries()`/`compose(target, sources)`/`register()`.
  - Converted the self-referential io leaves to typed `ArtifactSerializer`/`ArtifactDeserializer` impls.
  - Rewrote engine's `register()` to call `composer::register()` (which calls the new `register_composer_entries`) instead of the old `io::register()` no-op chain.
  - Rewrote the stdio plugin's `📦️glue.rs` `pub mod binary { ... }` block (brace-matched substitution) to the new nested tree, with `pub use super::standards::v_raw::...::*;` shims at the old `schema`/`engine`/`io` mount points so every OTHER stdio artifact's existing `crate::artifacts::binary::...` references (verified: many, e.g. deflate/zip/bmp/step/glb) keep resolving unchanged.
  - **Verified**: `cargo check -p semio-s-plugin-stdio` clean (0 errors, only pre-existing unrelated warnings); `cargo test -p semio-s-plugin-stdio` 71/71 pre-existing tests still pass + 2 new composer tests added and passing (one proves a direct native round-trip through `compose()`, one proves `register()` -> `io_resolve()` finds the entry through the real typed registry -- not just compiles, actually dispatches correctly at runtime); full 11-rule policy sweep (7 original + 4 new standards/subsets rules) all 0, including catching and fixing a real design/policy mismatch (composer aggregation levels needed a different content marker than the subset's trait-impl level -- fixed policyArtifactComposerBreaches to check for `ComposerEntry` at artifact/standard level vs `ArtifactComposer` at subset level, matching the intentional value-level-aggregation design).
- **ALL 29 STDIO ARTIFACTS MIGRATED -- DONE, fully verified.** Rather than a Workflow fan-out, the proven binary recipe was packaged into two reusable, data-driven, idempotent generator scripts (generators/w12_migrate_stdio_artifact.py, generators/w12_rewrite_glue.py) and run directly (script > 28 separate agent calls for this class of mechanical, well-specified work -- and critically avoids the real concurrency hazard of multiple parallel agents racing to edit the single shared glue.rs file). Processed in DAG-topological batches: L1 (8: txt deflate las dwg jpg gif bmp tiff, single dep on binary) -> L2 (13: xml zip json csv md obj stl ply step ifc dxf png pdf, up to 2 deps) -> L3 (6: gltf svg docx pptx xlsx bcf) -> L4 (1: glb, deepest chain). Verified after every batch: `cargo check -p semio-s-plugin-stdio` (0 errors throughout), `cargo test -p semio-s-plugin-stdio` (73/73 passing throughout, stable), `policyStdioArtifactsBreaches` (0 throughout). Final sweep confirms: all 29 artifacts have 🏅️standards/, zero stray old-shape facets (🧬️schema/⚙️engine/🚪️io/🪓️decomposer) remain at any artifact root.
  - Real bugs found and fixed along the way (not hypothetical -- each concretely would have broken the shared tree if shipped): (1) missing mkdir for artifact-root analyzer facet in the first script version; (2) decomposer->analyzer string-surgery was fragile across artifacts with slightly different formatting -- replaced with clean template generation (matches the plan's own risk assessment: "1150 #[path] lines... single largest mechanical surface"); (3) a real idempotency bug where re-running the script after a partial failure would silently clobber the subset level's REAL content with a facade copy (root cause: art_root/🏗️builder gets repopulated with facade content by write_facades(), so a naive second-run move would relocate that facade instead of true original content) -- fixed with an explicit "does the subset already have real content" guard, verified stable across repeated runs; (4) a genuinely pre-existing, never-before-compiled bug in ifc's and step's binary-deserializer leaves (referenced a `text` field that doesn't exist on IfcSnapshot/StepSnapshot, which only have `brep`) -- surfaced ONLY because the new glue generator mounts every io leaf actually found on disk, unlike the old hand-maintained glue which apparently never wired these two leaves in; fixed by delegating to the same `DocumentDsl::parse_dsl` codec the builder/analyzer already use.
  - Known, accepted simplification for the mass-fan-out (documented, not hidden): individual io leaves for non-terminal artifacts were moved into their target-qualified paths but NOT individually converted to typed `ArtifactSerializer`/`ArtifactDeserializer` impls (only binary's leaves were, by hand, as the reference). This doesn't affect correctness -- every composer reuses the artifact's own analyzer (which already round-trips both native AND single-hop-dependency payloads through `store::Document{Dsl,Pack}`) rather than calling the old free-fn leaves, which are now vestigial. A follow-up pass could convert them for full design fidelity; not required for this ticket's structural/policy goals. Also accepted: a handful of cosmetic "hidden lifetime parameter" warnings on `&[ComposeSource]` (matches the trait's own elided-lifetime style; zero functional impact).
- **Domain recipe proven on 🗒️note (W14 pilot) -- DONE, fully verified.** Domain artifacts turned out substantially harder than stdio's uniform case: two new scripts (generators/w14_migrate_domain_artifact.py, generators/w14_rewrite_glue.py), and genuinely more per-plugin surface area to get right. Real, concrete bugs found and fixed (each would have broken the note plugin's build if shipped):
  1. **Legacy shim modules destroyed.** The pre-existing glue.rs had SIX single-line pass-through shims left over from an even earlier migration (`op`, `dsl`, `spr`, `diff`, `mutations`, `snapshot` -- residue from "absorb dsl/pack/op/spr/diff/mutations into schema/{snapshot,diff,mutations}") that the app-layer command files (`🎛️apps/🗒️note/🎮️commands/*`) still reference directly (`crate::artifacts::note::op::NoteMutation`, `crate::artifacts::note::dsl::parse_dsl`, etc). A naive whole-block regenerate silently deleted them. Fixed generically: `extract_legacy_shims()` reads the artifact's block as last-committed (`git show HEAD:<path>`, read-only), finds any single-line `pub mod X { pub use ...; }` shim not in a small structural-facet exclude list, and re-emits it with its `schema::` target redirected to the new subset-relative path. This is NOT note-specific -- every domain artifact that went through the same earlier dsl/pack/op/spr/diff/mutations absorption may carry the same residue, so this check runs for every plugin.
  2. **Mutations are NOT uniformly single (`set-snapshot`) like stdio.** Note has 12 named mutations (set-pencil-width, set-grid-opacity, set-grid-visible, set-snapshot, set-grid-spacing, set-snap-grid-spacing, put-asset, set-grid-subdivisions, remove-asset, set-blocks, set-snap-enabled, set-eraser-radius). `build_schema_tree` now discovers mutation subdirs from disk instead of assuming exactly one.
  3. **`diff::text::*` convenience re-export.** The original glue had `pub mod diff { ... pub mod text; pub use text::*; ... }` -- an extra flattening some call sites depend on (`schema::diff::diff_set_grid_opacity` instead of `schema::diff::text::diff_set_grid_opacity`). Now emitted unconditionally for every `diff` child.
  4. **`import_stdio_kinds()`/`export_stdio_kinds()` destroyed.** Unlike stdio's io facet root (register-chain only), domain io facet roots also define these two functions, read by the artifact's own `artifact_kind()`. The placeholder overwrite (copied verbatim from the stdio script) silently deleted them. Fixed: regenerated from the actual scanned import/export target lists (kept separate, not unioned, since they can differ).
  5. **`include_str!`/`include_bytes!` relative-path breakage.** Files moved 4 levels deeper (artifact root -> `🏅️standards/🔖️1/🪆️subsets/✳️any/...`); any `../../../📚️examples/...`-style include now resolves to nothing. Fixed with a self-correcting pass (`fix_include_depth`) that checks whether the referenced path actually resolves on disk and only patches ones that don't -- safe to re-run, not a blind depth-delta assumption.
  6. **Composer dispatch path depth.** The generated deserializer-leaf call omitted the inserted target-standard/subset module segments (`dwg::deserialize` instead of `dwg::v_ac1018::any::deserialize`); fixed.
  7. **Missing `ArtifactBuilder` trait import** in the composer (needed for `<StdioKind>Builder::from_text/from_binary` UFCS calls) -- added.
  8. **A real idempotency bug**, same class as stdio's: checking `mv()`'s return value (False on re-run once content has already moved) to decide whether to scan for stdio io targets, silently producing an EMPTY target list on any re-run after a partial failure. Fixed to check actual on-disk state instead of the move's transient return value.
  Verified: `cargo check -p semio-s-plugin-note` clean, `cargo test -p semio-s-plugin-note` 65/65 passing, full policy sweep 0, and `semio-s-plugin-stdio` reconfirmed unaffected.
- Next: validate the now-generalized (not note-specific) domain scripts against 2-3 more single-artifact plugins to build confidence before deciding how far to push the remaining 53 domain artifacts across 31 more plugins -- each is its own separate Cargo crate with its own 📦️glue.rs (no shared-file race like stdio had), but may carry its own plugin-specific legacy-shim or multi-mutation surprises that need the same care note required.
- **🖍️draw migrated -- DONE, fully verified.** 83/83 tests pass, policy 0. Second domain pilot after note; confirmed the generalized scripts hold up on a structurally different artifact.
- **16-plugin domain batch (writer, mathematical, flow, vcs, shooting, sequence, architect/program, lowpoly, forms, layout, playbook, imperative, remodel, dag, raster, cad) -- DONE, fully verified.** All 16 now `cargo check` clean AND `cargo test` clean (modulo one confirmed-pre-existing, migration-unrelated dormant bug class, see below) AND policy-consistent with the note/draw baseline (same `mutation-migration/*` "dual-shape window" breach kinds note/draw already carry, nothing new/worse). Two real migration-script bugs found and fixed while closing this batch out:
  1. **cad: duplicate `#[path]` mount of the same physical file.** The generic W14 `extract_legacy_shims()` legacy-shim logic emitted `mutations`/`diff`/`snapshot` compat shims as fresh PHYSICAL `#[path]` remounts of files the structural `standards::v1::subsets::any::schema::*` tree ALSO mounts (confirmed via `grep -n` on glue.rs: `🔺️diff/📝️text/component.rs` mounted at both line 95 and line 633) -- two non-unified module instances of the same file produced `E0119` conflicting trait impls, `E0592` duplicate `apply_to_artifact`, and cascading `E0308`/`E0432` errors. Root-caused by comparing against the pinned pre-session commit (which had exactly one mount) and against note/draw's ALREADY-CORRECT pattern (`pub mod diff { pub use crate::artifacts::note::standards::v1::subsets::any::schema::diff::*; ... }` -- a pure re-export, no `#[path]`). Fixed by deleting the physical-mount shim block and re-adding `mutations`/`diff`/`snapshot` as pure `pub use` aliases (matching note/draw exactly); also had to add the missing `CadDiff`/`CadMutation` re-exports at the artifact root (only `CadSnapshot` was there) since the subset-level and artifact-level `🏗️builder/component.rs` facades expect all three at `crate::artifacts::cad::{Snapshot,Diff,Mutation}`. `cargo test -p semio-s-plugin-cad`: 124/124 passing after the fix.
  2. **program (🏛️architect): two extra schema-level facets (`🧱️kernel`, `🗄️registers`) never mounted at all.** Unlike note/draw/cad's uniform snapshot+diff+mutations triad, program's schema also has `EntityId`/`EntityHeader`/`TextField`/`TraceKind`/`TraceLink` (kernel) and ~20 domain enums (registers) as SIBLING schema facets -- `build_schema_tree()` only knew about the fixed triad, so these two facets (correctly moved to disk by the mover step) were simply never wired into glue.rs, breaking every leaf file across the plugin that references them (59 `E0432` errors). Fixed by hand-adding both as structural `#[path]` mounts under `schema::` plus `kernel`/`registers` pure-alias shims at the artifact root (program's artifact-root `component.rs` already expected `crate::artifacts::program::{kernel,registers}` as bare siblings, matching its pre-migration shape). `cargo test -p semio-s-plugin-architect`: no new failures from this fix.
  3. **vcs: migration-script builder-facade template hardcoded the wrong Mutation type name.** vcs's real mutation enum is `VcsDemoMutation` (not `VcsMutation` -- confirmed against the pre-session commit, which correctly used `VcsDemoMutation` throughout). The W14 script's generated 3-tier builder facades (artifact-root + standard-level) assumed a uniform `{Artifact}Mutation` naming convention and wrote `VcsMutation`, which doesn't exist. Fixed by renaming the two generated facade files + the artifact-root re-export back to `VcsDemoMutation`. **Flagged for whoever runs the remaining ~38-artifact migration**: this template assumption should be checked (or the script fixed to read the real enum name off disk) for every future plugin, not just patched reactively per-plugin as found.
- **Pre-existing, migration-unrelated bug discovered and spun off, not fixed here:** `cargo test --no-fail-fast` on the batch surfaced dormant failures in writer (7), dag (6), flow (1), lowpoly (2) -- all `semio_grammar_conformance`/`semio_protocol_conformance`/handcrafted-assertion tests failing because their `📖️component.grammar.semio`/`📡️component.protocol.semio` files are a generic "stdio.json.diff"-style placeholder (single-quoted literals) that the real `::dsl::parse_grammar` parser rejects outright, or that doesn't contain the text the artifact's own handcrafted test asserts on (e.g. `.contains("halfedge")`). Confirmed via `git show <pre-session-commit>:<path>` byte-identical to current content AND the failing test module already present pre-session -- this is NOT a migration regression, it's dormant breakage nobody had run `cargo test` against recently. The repo's own policy sweep already tracks this at scale as `handcrafted-grammar/spec-distinctness` (6787 breaches repo-wide, ~30 plugins share this exact template incl. all 15 norm sub-artifacts, block×3, puzzle×3, fem×2, trinity×2, and more). Spun off as a separate background task suggestion (task_624c72b0) rather than folded into this ticket -- wrong scope (content/grammar-authoring, not standards/subsets architecture) and far too large (~30 plugins) to absorb here.
- Next: continue the remaining ~38 unmigrated domain artifacts (procedural×2, gis×2, animate, demonstrator, fem×2, puzzle×3, block×3, norm×15, energy, trinity×2, space, sourcing), watching for the same three bug classes above (physical-mount shim duplication, non-triad schema facets, hardcoded facade type names) in addition to note's original six. Then W15 (os/os-host registry collapse) through W18 (final gate ladder + report).
