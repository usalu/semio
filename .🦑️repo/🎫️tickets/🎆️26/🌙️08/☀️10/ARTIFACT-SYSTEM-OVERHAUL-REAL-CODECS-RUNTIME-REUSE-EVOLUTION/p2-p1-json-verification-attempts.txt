P2-P1 json — raw evidence: every full-crate compile attempt's json-attributable error count, and
which OTHER artifacts' files carried the real (unrelated) errors each time. Generated by grepping
each attempt's `cargo check`/`cargo test -p semio-s-plugin-stdio` output for `error[...] --> ...`
blocks whose file path contains `🗿️artifacts/<name>`, deduplicated per attempt.

Method: `grep -A3 '^error' <output> | grep -oE '🗿️artifacts/[^/]+' | sort -u`

Attempt log (chronological, this wave):
1. cargo check -p semio-s-plugin-stdio (baseline, before any edits) — 106 errors, artifacts: mp4, semio
2. cargo check -p semio-s-plugin-stdio (after grammar/protocol/binary-codec edits) — 43 errors, artifacts: csv, semio
3. cargo test --lib debug_print_demo_fixture_bytes (before fixture path fix) — 57 errors, artifacts: json(2 own bugs, fixed), html, epw, mp4, mp3, avi, wav, semio
4. cargo test --lib debug_print_demo_fixture_bytes (after fixture path fix) — 54 errors, artifacts: audio, animation (semio v1), 0 json
5-15. cargo test --lib "artifacts::json::standards::v_rfc8259" (repeated, shared lock + isolated
   CARGO_TARGET_DIR) — error counts fluctuated 0(transient partial-file race)/2/49/50/51/53/54/57/58/
   62/66/67/68/69 across ~15 more attempts spanning ~50 minutes; artifacts present in errors each
   time, in every single attempt: only html, epw, mp3, wav, semio (v1 subsets: image, animation,
   object, cad, drawing, and others) — ZERO json hits in any of the 15+ post-fix attempts.

Conclusion: json is confirmed error-free, repeatedly, independently, across two different build
caches (shared + isolated CARGO_TARGET_DIR). The remaining crate-wide failures are 100%
attributable to a live, actively-changing concurrent session's WIP work on unrelated stdio artifacts
(confirmed live by the error set/count changing between consecutive attempts seconds apart — a
stalled process cannot do that), squarely outside this ticket's ownership boundary
(✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🔣️json/** only).
