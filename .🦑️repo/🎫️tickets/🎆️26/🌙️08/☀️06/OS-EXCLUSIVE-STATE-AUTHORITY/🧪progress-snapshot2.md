# Progress snapshot

## Green
- semio-framework-os-kernel
- semio-framework-os (default features; os-host-full gated)
- semio-framework-plugin / plugin-host (baseline)
- semio-s-plugin-draw

## CollectionOperation
- Single shape via VCS: `Add { index, item }`, `Move { id, to_index }`
- `spr/command` reexports VCS collection ops (no duplicate enum)

## Remaining
- infinite crate blocks layout/flow/space plugins
- DocumentApp leftovers / LazyLock sessions (subagents)
- m3 host store ownership (subagent)
- Wave 3 policy ungating after breach=0
