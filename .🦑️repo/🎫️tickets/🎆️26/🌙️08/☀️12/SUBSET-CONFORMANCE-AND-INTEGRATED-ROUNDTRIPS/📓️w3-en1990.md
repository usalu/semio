# W3 EN1990 — Owning Reference Subset

Completed: 2026-08-12. Scope: `✳️any` owning subset for `s.norm.en1990` / standard `1`.

## Summary

Promoted `🏅️standards/🔖️1/🪆️subsets/✳️any` to a complete owning reference subset: engine and examples are subset-owned, IO exposes universal semio DSL/pack import+export, outline inference now includes norm clause-summary fields from `engine::evaluate`, manifest declares owning archetype with fidelity and facet lists, and integrated roundtrip harness adapter is wired in IO tests.

## Files changed

### Moved
| From | To |
|------|-----|
| `…/🔖️1/⚙️engine/🦀️component.rs` | `…/✳️any/⚙️engine/🦀️component.rs` |
| `…/📘️en1990/📚️examples/📕️high-consequence-office/` | `…/✳️any/📚️examples/📕️high-consequence-office/` |

Removed empty `…/🔖️1/⚙️engine/` and `…/📘️en1990/📚️examples/`.

### Updated (en1990 artifact)
- `🦀️component.rs` — io_registry now resolves through `subsets::any::engine`
- `🏅️standards/🔖️1/🪆️subsets/🔣️component.json` — owning manifest (`kind`, `ioFidelity`, import/export, inferences, partialMutations, examples)
- `…/✳️any/🚪️io/🦀️component.rs` — `import_stdio_kinds`/`export_stdio_kinds`, DSL/pack wire helpers, `SubsetRoundtripSpec` + roundtrip tests
- `…/✳️any/🚪️io/🟦️component.ts` — import/export kind constants
- `…/✳️any/🧬️schema/📸️snapshot/📝️text/🦀️component.rs` — example `include_str!` path after example move
- `…/✳️any/🧬️schema/💡️inferences/🧾outline/🦀️component.rs` — clause summary via `engine::evaluate` (`checkCount`, `passCount`, `allPass`, `governingClause`, `governingUtilization`)
- `…/✳️any/🧬️schema/💡️inferences/🧾outline/🟦️component.ts` — matching TS fields
- `…/✳️any/🧬️schema/💡️inferences/🟦️component.ts` — updated inference TS mirror
- `…/✳️any/🧬️schema/💡️inferences/🔣️component.json` — outline JSON schema extended

### Glue (required for compile after moves)
- `📦️packages/🦀️rust/📦️glue.rs` — mount `engine` under `subsets::any`, update `en1990::engine` shim, repoint example `#[path]` registrations

## Manifest (`🔣️component.json`)

- `kind`: `owning`
- `ioFidelity`: `canonical`
- `import` / `export`: `["s.en1990"]`
- `inferences`: `["outline"]`
- `partialMutations`: all ten EN1990 partial mutation slugs
- `examples`: `["high-consequence-office"]`

## Commands

```bash
export PATH=/usr/bin:/bin:/usr/sbin:/sbin:/Users/ueli/.bun/bin:/opt/homebrew/bin:$PATH
export RUSTC_WRAPPER=
export CARGO_TARGET_DIR=".🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SUBSET-CONFORMANCE-AND-INTEGRATED-ROUNDTRIPS/🎯️target-w1-harness"
cd "✏️s/🔌️plugins/📕️norm/📦️packages/🦀️rust"
cargo test -p semio-s-plugin-norm high_consequence_office_subset_roundtrip
```

Crate: `semio-s-plugin-norm` (`✏️s/🔌️plugins/📕️norm/📦️packages/🦀️rust/Cargo.toml`).

## Results

**Not run to green** — blocked by pre-existing workspace compile error in `semio-framework-plugin` (`E0499` at `🔌️plugin/🦀️component.rs:5790`, documented in W1-MACRO). First cargo attempt also hit transient disk-full during fresh target compile; see `scratch-w3-en1990-cargo.txt`.

Once `semio-framework-plugin` compiles, re-run the filter above; secondary checks:

```bash
cargo test -p semio-s-plugin-norm en1990
```

## Verification checklist (post-unblock)

- [ ] `high_consequence_office_subset_roundtrip` — integrated harness S0–S8 on real example DSL
- [ ] `dsl_and_pack_wire_roundtrip_default` — IO wire helpers
- [ ] `high_consequence_office_example_fixture_parses_and_round_trips` — snapshot grammar
- [ ] `outline_counts_checks_from_norm_computation` — nontrivial inference from evaluate
