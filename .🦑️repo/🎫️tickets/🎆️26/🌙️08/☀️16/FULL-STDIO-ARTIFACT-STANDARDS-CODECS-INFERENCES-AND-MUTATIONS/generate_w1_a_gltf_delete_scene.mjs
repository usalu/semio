import { mkdir, writeFile } from 'node:fs/promises';
import { join } from 'node:path';

const root = join(process.cwd(), '✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🧊️gltf/🏅️standards/🔖️2.0/🪆️subsets/✳️any/🧬️schema/🧬️mutations', 'delete-scene');
const id = 's.stdio.gltf.mutation.delete-scene.v1';
const json = value => `${JSON.stringify(value, null, 2)}\n`;
const mutationTs = `/** 🦠️ Deletes one top-level glTF scene with typed default-scene reference repair. */
import type { GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { clone, position, reject, remove, type GltfMutationRejection, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteSceneDescriptor = { id: '${id}', version: 1, touchedPathPatterns: ['document/scenes/{index}', 'document/scene'] } as const;
export interface GltfDeleteScenePayload { index: number }
export const validateGltfDeleteScene = (payload: GltfDeleteScenePayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const range = position(payload.index, base.document.scenes.length, 'document/scenes'); if (range) return range; return base.document.scene !== undefined && base.document.scene >= base.document.scenes.length ? reject('gltf.reference.invalid-default-scene', 'document/scene', 'default scene must address an existing scene') : undefined; };
export const applyGltfDeleteScene = (base: GltfSnapshot, payload: GltfDeleteScenePayload): GltfStructuralResult => { const rejection = validateGltfDeleteScene(payload, base); if (rejection) return { accepted: false, rejection }; const snapshot = clone(base); remove(snapshot, 'scenes', payload.index); return { accepted: true, snapshot: clone(snapshot) }; };
`;
const diffTs = `/** 🔺️ Typed sparse deletion delta owned exclusively by delete-scene. */
import type { GltfScene, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { clone, position, reject, remove, same, type GltfMutationRejection, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteSceneDiffDescriptor = { id: '${id}', version: 1, phase: 'diff', touchedPathPatterns: ['document/scenes/{index}', 'document/scene'] } as const;
export interface GltfDeleteSceneDiff { readonly id: '${id}'; readonly version: 1; readonly phase: 'diff'; readonly touchedPaths: readonly string[]; readonly index: number; readonly deletedScene: GltfScene; readonly defaultSceneAfter: number | null; }
const remapDefault = (scene: number | undefined, index: number): number | null => scene === undefined || scene === index ? null : scene > index ? scene - 1 : scene;
const paths = (base: GltfSnapshot, index: number): readonly string[] => { const after = remapDefault(base.document.scene, index); return base.document.scene === (after ?? undefined) ? [\`document/scenes/\${index}\`] : [\`document/scenes/\${index}\`, 'document/scene']; };
export const validateGltfDeleteSceneDiff = (diff: GltfDeleteSceneDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfDeleteSceneDiffDescriptor.id || diff.version !== 1 || diff.phase !== 'diff') return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'canonical identity or phase does not match'); const range = position(diff.index, base.document.scenes.length, 'document/scenes'); if (range) return range; if (base.document.scene !== undefined && base.document.scene >= base.document.scenes.length) return reject('gltf.reference.invalid-default-scene', 'document/scene', 'default scene must address an existing scene'); if (!same(diff.touchedPaths, paths(base, diff.index))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete deletion effects'); if (!same(diff.deletedScene, base.document.scenes[diff.index])) return reject('gltf.mutation.stale-diff', \`document/scenes/\${diff.index}\`, 'current scene does not match the planned deletion target'); return diff.defaultSceneAfter === remapDefault(base.document.scene, diff.index) ? undefined : reject('gltf.mutation.invalid-reference-repair', 'diff/defaultSceneAfter', 'default scene remap must match the deletion index'); };
export const applyGltfDeleteSceneDiff = (base: GltfSnapshot, diff: GltfDeleteSceneDiff): GltfStructuralResult => { const rejection = validateGltfDeleteSceneDiff(diff, base); if (rejection) return { accepted: false, rejection }; const snapshot = clone(base); remove(snapshot, 'scenes', diff.index); return snapshot.document.scene === (diff.defaultSceneAfter ?? undefined) ? { accepted: true, snapshot: clone(snapshot) } : { accepted: false, rejection: reject('gltf.mutation.reference-repair-failed', 'document/scene', 'default scene did not remap as planned') }; };
export const encodeGltfDeleteSceneDiff = (diff: GltfDeleteSceneDiff): string => JSON.stringify(diff);
export const deriveGltfDeleteSceneDiff = (base: GltfSnapshot, payload: { index: number }): { accepted: true; diff: GltfDeleteSceneDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const range = position(payload.index, base.document.scenes.length, 'document/scenes'); if (range) return { accepted: false, rejection: range }; if (base.document.scene !== undefined && base.document.scene >= base.document.scenes.length) return { accepted: false, rejection: reject('gltf.reference.invalid-default-scene', 'document/scene', 'default scene must address an existing scene') }; const touchedPaths = paths(base, payload.index); return { accepted: true, diff: { id: '${id}', version: 1, phase: 'diff', touchedPaths, index: payload.index, deletedScene: structuredClone(base.document.scenes[payload.index]!), defaultSceneAfter: remapDefault(base.document.scene, payload.index) }, touchedPaths }; };
`;
const inverseTs = `/** ↩️ Exact delete-scene inverse restores the deleted scene and default-scene repair state. */
import type { GltfScene, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { clone, position, reject, repair, same, type GltfMutationRejection, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteSceneInverseDescriptor = { id: '${id}', version: 1, phase: 'inverse', touchedPathPatterns: ['document/scenes/{index}', 'document/scene'] } as const;
export interface GltfDeleteSceneInverse { readonly id: '${id}'; readonly version: 1; readonly phase: 'inverse'; readonly touchedPaths: readonly string[]; readonly index: number; readonly deletedScene: GltfScene; readonly defaultSceneBefore: number | null; readonly expectedDefaultSceneAfter: number | null; }
const paths = (before: number | null, after: number | null, index: number): readonly string[] => before === after ? [\`document/scenes/\${index}\`] : [\`document/scenes/\${index}\`, 'document/scene'];
const remapDefault = (scene: number | undefined, index: number): number | null => scene === undefined || scene === index ? null : scene > index ? scene - 1 : scene;
const asOptional = (value: number | null): number | undefined => value === null ? undefined : value;
export const validateGltfDeleteSceneInverse = (inverse: GltfDeleteSceneInverse, after: GltfSnapshot): GltfMutationRejection | undefined => { if (inverse.id !== GltfDeleteSceneInverseDescriptor.id || inverse.version !== 1 || inverse.phase !== 'inverse') return reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'canonical identity or phase does not match'); const range = position(inverse.index, after.document.scenes.length, 'document/scenes', true); if (range) return range; if (inverse.defaultSceneBefore !== null && inverse.defaultSceneBefore >= after.document.scenes.length + 1) return reject('gltf.reference.invalid-default-scene', 'inverse/defaultSceneBefore', 'restored default scene must address the restored collection'); if (inverse.expectedDefaultSceneAfter !== null && inverse.expectedDefaultSceneAfter >= after.document.scenes.length) return reject('gltf.reference.invalid-default-scene', 'inverse/expectedDefaultSceneAfter', 'expected repaired default scene must address the current collection'); if (!same(inverse.touchedPaths, paths(inverse.defaultSceneBefore, inverse.expectedDefaultSceneAfter, inverse.index))) return reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete deletion effects'); return after.document.scene === asOptional(inverse.expectedDefaultSceneAfter) ? undefined : reject('gltf.mutation.stale-inverse', 'document/scene', 'current default scene does not match the deletion repair state'); };
export const applyGltfDeleteSceneInverse = (after: GltfSnapshot, inverse: GltfDeleteSceneInverse): GltfStructuralResult => { const rejection = validateGltfDeleteSceneInverse(inverse, after); if (rejection) return { accepted: false, rejection }; const snapshot = clone(after); repair(snapshot.document, 'scenes', { kind: 'insert', at: inverse.index }); snapshot.document.scenes.splice(inverse.index, 0, structuredClone(inverse.deletedScene)); snapshot.document.scene = asOptional(inverse.defaultSceneBefore); return { accepted: true, snapshot: clone(snapshot) }; };
export const encodeGltfDeleteSceneInverse = (inverse: GltfDeleteSceneInverse): string => JSON.stringify(inverse);
export const deriveGltfDeleteSceneInverse = (base: GltfSnapshot, payload: { index: number }): { accepted: true; inverse: GltfDeleteSceneInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const range = position(payload.index, base.document.scenes.length, 'document/scenes'); if (range) return { accepted: false, rejection: range }; if (base.document.scene !== undefined && base.document.scene >= base.document.scenes.length) return { accepted: false, rejection: reject('gltf.reference.invalid-default-scene', 'document/scene', 'default scene must address an existing scene') }; const before = base.document.scene ?? null; const repaired = remapDefault(base.document.scene, payload.index); const touchedPaths = paths(before, repaired, payload.index); return { accepted: true, inverse: { id: '${id}', version: 1, phase: 'inverse', touchedPaths, index: payload.index, deletedScene: structuredClone(base.document.scenes[payload.index]!), defaultSceneBefore: before, expectedDefaultSceneAfter: repaired }, touchedPaths }; };
`;
const mutationRust = `//! 🦠️ Deletes one top-level glTF scene with typed default-scene reference repair.
use serde::{Deserialize, Serialize};
use crate::artifacts::gltf::GltfSnapshot;
use crate::artifacts::gltf::schema::mutations::top_level_collections_private::{reject, scenes_op, GltfTopLevelFamily, GltfTopLevelMutationRejection};
pub const ID: &str = "${id}";
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)] #[serde(rename_all = "camelCase")]
pub struct GltfDeleteScenePayload { pub index: usize }
pub fn validate(payload: &GltfDeleteScenePayload, base: &GltfSnapshot) -> Result<(), GltfTopLevelMutationRejection> { if payload.index >= base.document.scenes.len() { return Err(reject("gltf.mutation.index-out-of-range", "document/scenes", "index must address a scene")); } if base.document.scene.is_some_and(|scene| scene >= base.document.scenes.len()) { return Err(reject("gltf.reference.invalid-default-scene", "document/scene", "default scene must address an existing scene")); } Ok(()) }
pub fn apply(payload: &GltfDeleteScenePayload, base: &GltfSnapshot) -> Result<GltfSnapshot, GltfTopLevelMutationRejection> { validate(payload, base)?; let mut next = base.clone(); scenes_op(&mut next, GltfTopLevelFamily::Scenes, payload.index, None, None)?; Ok(next) }
`;
const diffRust = `//! 🔺️ Typed sparse deletion delta owned exclusively by delete-scene.
use serde::{Deserialize, Serialize};
use crate::artifacts::gltf::schema::snapshot::GltfScene;
use crate::artifacts::gltf::GltfSnapshot;
use crate::artifacts::gltf::schema::mutations::top_level_collections_private::{reject, scenes_op, GltfTopLevelFamily, GltfTopLevelMutationRejection};
pub const ID: &str = "${id}";
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)] #[serde(rename_all = "camelCase")]
pub enum GltfDeleteSceneDiffPhase { Diff }
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)] #[serde(rename_all = "camelCase")]
pub struct GltfDeleteSceneDiff { pub id: String, pub version: u32, pub phase: GltfDeleteSceneDiffPhase, pub touched_paths: Vec<String>, pub index: usize, pub deleted_scene: GltfScene, pub default_scene_after: Option<usize> }
fn remap_default(scene: Option<usize>, index: usize) -> Option<usize> { match scene { None => None, Some(scene) if scene == index => None, Some(scene) if scene > index => Some(scene - 1), Some(scene) => Some(scene) } }
fn paths(before: Option<usize>, after: Option<usize>, index: usize) -> Vec<String> { let mut paths = vec![format!("document/scenes/{}", index)]; if before != after { paths.push("document/scene".into()); } paths }
pub fn validate(diff: &GltfDeleteSceneDiff, base: &GltfSnapshot) -> Result<(), GltfTopLevelMutationRejection> { if diff.id != ID || diff.version != 1 || diff.phase != GltfDeleteSceneDiffPhase::Diff { return Err(reject("gltf.mutation.invalid-diff-envelope", "diff", "canonical identity or phase does not match")); } if diff.index >= base.document.scenes.len() { return Err(reject("gltf.mutation.index-out-of-range", "document/scenes", "index must address a scene")); } if base.document.scene.is_some_and(|scene| scene >= base.document.scenes.len()) { return Err(reject("gltf.reference.invalid-default-scene", "document/scene", "default scene must address an existing scene")); } if diff.touched_paths != paths(base.document.scene, remap_default(base.document.scene, diff.index), diff.index) { return Err(reject("gltf.mutation.invalid-touched-paths", "diff/touchedPaths", "paths must be concrete deletion effects")); } if diff.deleted_scene != base.document.scenes[diff.index] { return Err(reject("gltf.mutation.stale-diff", format!("document/scenes/{}", diff.index), "current scene does not match the planned deletion target")); } if diff.default_scene_after != remap_default(base.document.scene, diff.index) { return Err(reject("gltf.mutation.invalid-reference-repair", "diff/defaultSceneAfter", "default scene remap must match the deletion index")); } Ok(()) }
pub fn apply(diff: &GltfDeleteSceneDiff, base: &GltfSnapshot) -> Result<GltfSnapshot, GltfTopLevelMutationRejection> { validate(diff, base)?; let mut next = base.clone(); scenes_op(&mut next, GltfTopLevelFamily::Scenes, diff.index, None, None)?; if next.document.scene != diff.default_scene_after { return Err(reject("gltf.mutation.reference-repair-failed", "document/scene", "default scene did not remap as planned")); } Ok(next) }
pub fn encode(diff: &GltfDeleteSceneDiff) -> Result<Vec<u8>, GltfTopLevelMutationRejection> { serde_json::to_vec(diff).map_err(|error| reject("gltf.mutation.encode-failed", "diff", error.to_string())) }
pub fn derive(base: &GltfSnapshot, index: usize) -> Result<GltfDeleteSceneDiff, GltfTopLevelMutationRejection> { if index >= base.document.scenes.len() { return Err(reject("gltf.mutation.index-out-of-range", "document/scenes", "index must address a scene")); } if base.document.scene.is_some_and(|scene| scene >= base.document.scenes.len()) { return Err(reject("gltf.reference.invalid-default-scene", "document/scene", "default scene must address an existing scene")); } let after = remap_default(base.document.scene, index); Ok(GltfDeleteSceneDiff { id: ID.into(), version: 1, phase: GltfDeleteSceneDiffPhase::Diff, touched_paths: paths(base.document.scene, after, index), index, deleted_scene: base.document.scenes[index].clone(), default_scene_after: after }) }
`;
const inverseRust = `//! ↩️ Exact delete-scene inverse restores the deleted scene and default-scene repair state.
use serde::{Deserialize, Serialize};
use crate::artifacts::gltf::schema::snapshot::GltfScene;
use crate::artifacts::gltf::GltfSnapshot;
use crate::artifacts::gltf::schema::mutations::top_level_collections_private::{reject, repair, Change, GltfTopLevelFamily, GltfTopLevelMutationRejection};
pub const ID: &str = "${id}";
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)] #[serde(rename_all = "camelCase")]
pub enum GltfDeleteSceneInversePhase { Inverse }
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)] #[serde(rename_all = "camelCase")]
pub struct GltfDeleteSceneInverse { pub id: String, pub version: u32, pub phase: GltfDeleteSceneInversePhase, pub touched_paths: Vec<String>, pub index: usize, pub deleted_scene: GltfScene, pub default_scene_before: Option<usize>, pub expected_default_scene_after: Option<usize> }
fn paths(before: Option<usize>, after: Option<usize>, index: usize) -> Vec<String> { let mut paths = vec![format!("document/scenes/{}", index)]; if before != after { paths.push("document/scene".into()); } paths }
pub fn validate(inverse: &GltfDeleteSceneInverse, after: &GltfSnapshot) -> Result<(), GltfTopLevelMutationRejection> { if inverse.id != ID || inverse.version != 1 || inverse.phase != GltfDeleteSceneInversePhase::Inverse { return Err(reject("gltf.mutation.invalid-inverse-envelope", "inverse", "canonical identity or phase does not match")); } if inverse.index > after.document.scenes.len() { return Err(reject("gltf.mutation.index-out-of-range", "document/scenes", "index must address an insertion location")); } if inverse.default_scene_before.is_some_and(|scene| scene >= after.document.scenes.len() + 1) { return Err(reject("gltf.reference.invalid-default-scene", "inverse/defaultSceneBefore", "restored default scene must address the restored collection")); } if inverse.expected_default_scene_after.is_some_and(|scene| scene >= after.document.scenes.len()) { return Err(reject("gltf.reference.invalid-default-scene", "inverse/expectedDefaultSceneAfter", "expected repaired default scene must address the current collection")); } if inverse.touched_paths != paths(inverse.default_scene_before, inverse.expected_default_scene_after, inverse.index) { return Err(reject("gltf.mutation.invalid-touched-paths", "inverse/touchedPaths", "paths must be concrete deletion effects")); } if after.document.scene != inverse.expected_default_scene_after { return Err(reject("gltf.mutation.stale-inverse", "document/scene", "current default scene does not match the deletion repair state")); } Ok(()) }
pub fn apply(inverse: &GltfDeleteSceneInverse, after: &GltfSnapshot) -> Result<GltfSnapshot, GltfTopLevelMutationRejection> { validate(inverse, after)?; let mut next = after.clone(); repair(&mut next.document, GltfTopLevelFamily::Scenes, &Change::Insert(inverse.index))?; next.document.scenes.insert(inverse.index, inverse.deleted_scene.clone()); next.document.scene = inverse.default_scene_before; Ok(next) }
pub fn encode(inverse: &GltfDeleteSceneInverse) -> Result<Vec<u8>, GltfTopLevelMutationRejection> { serde_json::to_vec(inverse).map_err(|error| reject("gltf.mutation.encode-failed", "inverse", error.to_string())) }
pub fn derive(base: &GltfSnapshot, index: usize) -> Result<GltfDeleteSceneInverse, GltfTopLevelMutationRejection> { if index >= base.document.scenes.len() { return Err(reject("gltf.mutation.index-out-of-range", "document/scenes", "index must address a scene")); } if base.document.scene.is_some_and(|scene| scene >= base.document.scenes.len()) { return Err(reject("gltf.reference.invalid-default-scene", "document/scene", "default scene must address an existing scene")); } let before = base.document.scene; let after = match before { None => None, Some(scene) if scene == index => None, Some(scene) if scene > index => Some(scene - 1), Some(scene) => Some(scene) }; Ok(GltfDeleteSceneInverse { id: ID.into(), version: 1, phase: GltfDeleteSceneInversePhase::Inverse, touched_paths: paths(before, after, index), index, deleted_scene: base.document.scenes[index].clone(), default_scene_before: before, expected_default_scene_after: after }) }
`;
const payloadSchema = { type: 'object', additionalProperties: false, properties: { index: { type: 'integer', minimum: 0 } }, required: ['index'] };
const sceneSchema = { type: 'object', additionalProperties: true, properties: { nodes: { type: 'array', items: { type: 'integer', minimum: 0 } }, name: { type: 'string' }, extensions: {}, extras: {} }, required: ['nodes'] };
const nullableIndex = { type: ['integer', 'null'], minimum: 0 };
const envelope = phase => ({ $schema: 'https://json-schema.org/draft/2020-12/schema', $id: id, title: `GltfDeleteScene${phase[0].toUpperCase()}${phase.slice(1)}V1`, type: 'object', additionalProperties: false, properties: { id: { const: id }, version: { const: 1 }, phase: { const: phase }, touchedPaths: { type: 'array', minItems: 1, maxItems: 2, items: { anyOf: [{ const: 'document/scene' }, { type: 'string', pattern: '^document/scenes/[0-9]+$' }] } }, index: { type: 'integer', minimum: 0 }, deletedScene: sceneSchema, ...(phase === 'diff' ? { defaultSceneAfter: nullableIndex } : { defaultSceneBefore: nullableIndex, expectedDefaultSceneAfter: nullableIndex }) }, required: ['id','version','phase','touchedPaths','index','deletedScene', ...(phase === 'diff' ? ['defaultSceneAfter'] : ['defaultSceneBefore','expectedDefaultSceneAfter'])], 'x-semio': { id, version: 1, phase, touchedPathPatterns: ['document/scenes/{index}', 'document/scene'] } });
const graphql = phase => phase === 'mutation' ? `input GltfDeleteScenePayloadV1 { index: Int! }\n` : phase === 'diff' ? `type GltfDeleteSceneValueV1 { nodes: [Int!]!, name: String, extensions: String, extras: String }\ntype GltfDeleteSceneDiffV1 { id: ID!, version: Int!, phase: String!, touchedPaths: [String!]!, index: Int!, deletedScene: GltfDeleteSceneValueV1!, defaultSceneAfter: Int }\n` : `type GltfDeleteSceneValueV1 { nodes: [Int!]!, name: String, extensions: String, extras: String }\ntype GltfDeleteSceneInverseV1 { id: ID!, version: Int!, phase: String!, touchedPaths: [String!]!, index: Int!, deletedScene: GltfDeleteSceneValueV1!, defaultSceneBefore: Int, expectedDefaultSceneAfter: Int }\n`;
const proto = phase => phase === 'mutation' ? `syntax = "proto3";\npackage stdio.gltf.mutation;\nmessage GltfDeleteScenePayloadV1 { uint32 index = 1; }\n` : phase === 'diff' ? `syntax = "proto3";\npackage stdio.gltf.mutation;\nmessage GltfDeleteSceneValueV1 { repeated uint32 nodes = 1; optional string name = 2; bytes extensions = 3; bytes extras = 4; }\nmessage GltfDeleteSceneDiffV1 { string id = 1; uint32 version = 2; string phase = 3; repeated string touched_paths = 4; uint32 index = 5; GltfDeleteSceneValueV1 deleted_scene = 6; optional uint32 default_scene_after = 7; }\n` : `syntax = "proto3";\npackage stdio.gltf.mutation;\nmessage GltfDeleteSceneValueV1 { repeated uint32 nodes = 1; optional string name = 2; bytes extensions = 3; bytes extras = 4; }\nmessage GltfDeleteSceneInverseV1 { string id = 1; uint32 version = 2; string phase = 3; repeated string touched_paths = 4; uint32 index = 5; GltfDeleteSceneValueV1 deleted_scene = 6; optional uint32 default_scene_before = 7; optional uint32 expected_default_scene_after = 8; }\n`;
const contract = { id, vectors: [{ name: 'deletesFirstSceneAndRemapsDefaultScene', base: { scene: 1, scenes: [{ nodes: [0] }, { nodes: [] }] }, payload: { index: 0 }, after: { scene: 0, scenes: [{ nodes: [] }] }, undo: { scene: 1, scenes: [{ nodes: [0] }, { nodes: [] }] }, diff: { id, version: 1, phase: 'diff', touchedPaths: ['document/scenes/0','document/scene'], index: 0, deletedScene: { nodes: [0] }, defaultSceneAfter: 0 }, inverse: { id, version: 1, phase: 'inverse', touchedPaths: ['document/scenes/0','document/scene'], index: 0, deletedScene: { nodes: [0] }, defaultSceneBefore: 1, expectedDefaultSceneAfter: 0 }, rejections: [{ name: 'outOfRangeIndex', payload: { index: 2 }, code: 'gltf.mutation.index-out-of-range' }, { name: 'danglingDefaultScene', base: { scene: 2, scenes: [{ nodes: [0] }, { nodes: [] }] }, payload: { index: 0 }, code: 'gltf.reference.invalid-default-scene' }, { name: 'staleInverseDefaultScene', defaultSceneAfter: null, code: 'gltf.mutation.stale-inverse' }] }], laws: ['acceptance','typed-range-rejection','default-scene-integrity-rejection','concrete-touched-paths','diff-plan-apply','inverse-base-reconstruction','stale-inverse-rejection','json-serialization','canonical-id-across-phases'] };
const contractTs = `/** 🧪️ Executes delete-scene laws from the canonical JSON vector. */
import assert from 'node:assert/strict';
import contractJson from './🔣️component.json' with { type: 'json' };
import { applyGltfDeleteScene } from '../🦠️mutation/🟦️component.ts';
import { applyGltfDeleteSceneDiff, deriveGltfDeleteSceneDiff, encodeGltfDeleteSceneDiff } from '../🔺️diff/🟦️component.ts';
import { applyGltfDeleteSceneInverse, deriveGltfDeleteSceneInverse, encodeGltfDeleteSceneInverse } from '../↩️inverse/🟦️component.ts';
import type { GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
export const gltfDeleteSceneContract = contractJson;
const snapshot = (state: { scene: number; scenes: readonly { nodes: readonly number[] }[] }): GltfSnapshot => ({ schema: 'gltf/2.0', sourceForm: 'json', buffers: [], document: { asset: { version: '2.0' }, scene: state.scene, scenes: structuredClone(state.scenes), nodes: [], meshes: [], accessors: [], bufferViews: [], buffers: [], materials: [], textures: [], images: [], samplers: [], skins: [], animations: [], cameras: [], extensionsUsed: [], extensionsRequired: [] } });
export const runGltfDeleteSceneContract = (): void => { const vector = gltfDeleteSceneContract.vectors[0]!; const base = snapshot(vector.base); const applied = applyGltfDeleteScene(base, vector.payload); assert.equal(applied.accepted, true, 'accepts shared deletion vector'); assert.deepEqual({ scene: applied.snapshot.document.scene, scenes: applied.snapshot.document.scenes }, vector.after, 'mutation deletes scene and remaps default'); const range = applyGltfDeleteScene(base, vector.rejections[0]!.payload!); assert.equal(range.accepted, false); assert.equal(range.rejection.code, vector.rejections[0]!.code); const dangling = applyGltfDeleteScene(snapshot(vector.rejections[1]!.base!), vector.rejections[1]!.payload!); assert.equal(dangling.accepted, false); assert.equal(dangling.rejection.code, vector.rejections[1]!.code); const planned = deriveGltfDeleteSceneDiff(base, vector.payload); assert.equal(planned.accepted, true); assert.deepEqual(planned.diff, vector.diff); const replay = applyGltfDeleteSceneDiff(base, planned.diff); assert.equal(replay.accepted, true); assert.deepEqual(replay.snapshot, applied.snapshot); assert.deepEqual(JSON.parse(encodeGltfDeleteSceneDiff(planned.diff)), vector.diff); const inverted = deriveGltfDeleteSceneInverse(base, vector.payload); assert.equal(inverted.accepted, true); assert.deepEqual(inverted.inverse, vector.inverse); const restored = applyGltfDeleteSceneInverse(applied.snapshot, inverted.inverse); assert.equal(restored.accepted, true); assert.deepEqual({ scene: restored.snapshot.document.scene, scenes: restored.snapshot.document.scenes }, vector.undo); assert.deepEqual(JSON.parse(encodeGltfDeleteSceneInverse(inverted.inverse)), vector.inverse); const staleAfter = { ...applied.snapshot, document: { ...applied.snapshot.document, scene: vector.rejections[2]!.defaultSceneAfter } }; const stale = applyGltfDeleteSceneInverse(staleAfter, inverted.inverse); assert.equal(stale.accepted, false); assert.equal(stale.rejection.code, vector.rejections[2]!.code); };
`;
const contractRust = `//! 🧪️ Executes delete-scene laws from the canonical JSON vector.
#[cfg(test)]
mod tests {
    use serde::Deserialize;
    use serde_json::Value;
    use crate::artifacts::gltf::schema::mutations::delete_scene::{diff, inverse, mutation};
    use crate::artifacts::gltf::schema::snapshot::GltfScene;
    use crate::artifacts::gltf::GltfSnapshot;
    #[derive(Deserialize)] #[serde(rename_all = "camelCase")]
    struct Contract { vectors: Vec<Vector> }
    #[derive(Deserialize)] #[serde(rename_all = "camelCase")]
    struct Vector { base: SceneState, payload: mutation::GltfDeleteScenePayload, after: SceneState, undo: SceneState, diff: Value, inverse: Value, rejections: Vec<Rejection> }
    #[derive(Deserialize)] #[serde(rename_all = "camelCase")]
    struct SceneState { scene: usize, scenes: Vec<GltfScene> }
    #[derive(Deserialize)] #[serde(rename_all = "camelCase")]
    struct Rejection { payload: Option<mutation::GltfDeleteScenePayload>, base: Option<SceneState>, default_scene_after: Option<usize>, code: String }
    fn snapshot(state: &SceneState) -> GltfSnapshot { let mut snapshot = GltfSnapshot::default(); snapshot.schema = "gltf/2.0".into(); snapshot.document.scene = Some(state.scene); snapshot.document.scenes = state.scenes.clone(); snapshot }
    #[test]
    fn delete_scene_shared_vector_executes_all_laws() {
        let contract: Contract = serde_json::from_str(include_str!("🔣️component.json")).expect("canonical vector decodes");
        let vector = &contract.vectors[0]; let base = snapshot(&vector.base);
        let after = mutation::apply(&vector.payload, &base).expect("mutation accepts vector"); assert_eq!(after.document.scene, Some(vector.after.scene)); assert_eq!(after.document.scenes, vector.after.scenes);
        let range = mutation::apply(vector.rejections[0].payload.as_ref().expect("range payload"), &base).expect_err("range rejects"); assert_eq!(range.code, vector.rejections[0].code);
        let dangling = mutation::apply(vector.rejections[1].payload.as_ref().expect("dangling payload"), &snapshot(vector.rejections[1].base.as_ref().expect("dangling base"))).expect_err("dangling default rejects"); assert_eq!(dangling.code, vector.rejections[1].code);
        let forward: diff::GltfDeleteSceneDiff = serde_json::from_value(vector.diff.clone()).expect("diff decodes"); let planned = diff::derive(&base, vector.payload.index).expect("diff derives"); assert_eq!(planned, forward); assert_eq!(planned.touched_paths, vec!["document/scenes/0", "document/scene"]); assert_eq!(diff::apply(&planned, &base).expect("diff applies"), after); assert_eq!(serde_json::from_slice::<Value>(&diff::encode(&planned).expect("diff encodes")).expect("encoded diff decodes"), vector.diff);
        let undo: inverse::GltfDeleteSceneInverse = serde_json::from_value(vector.inverse.clone()).expect("inverse decodes"); let inverted = inverse::derive(&base, vector.payload.index).expect("inverse derives"); assert_eq!(inverted, undo); let restored = inverse::apply(&inverted, &after).expect("inverse applies"); assert_eq!(restored.document.scene, Some(vector.undo.scene)); assert_eq!(restored.document.scenes, vector.undo.scenes); assert_eq!(serde_json::from_slice::<Value>(&inverse::encode(&inverted).expect("inverse encodes")).expect("encoded inverse decodes"), vector.inverse);
        let mut stale_after = after.clone(); stale_after.document.scene = vector.rejections[2].default_scene_after; let stale = inverse::apply(&inverted, &stale_after).expect_err("stale inverse rejects"); assert_eq!(stale.code, vector.rejections[2].code);
    }
}
`;
for (const [phase, ts, rust] of [['🦠️mutation', mutationTs, mutationRust], ['🔺️diff', diffTs, diffRust], ['↩️inverse', inverseTs, inverseRust]]) { const logical = phase === '🦠️mutation' ? 'mutation' : phase === '🔺️diff' ? 'diff' : 'inverse'; await mkdir(join(root, phase), { recursive: true }); await writeFile(join(root, phase, '🟦️component.ts'), ts); await writeFile(join(root, phase, '🦀️component.rs'), rust); await writeFile(join(root, phase, '🔣️component.json'), json(logical === 'mutation' ? { $schema: 'https://json-schema.org/draft/2020-12/schema', $id: id, title: 'GltfDeleteScenePayloadV1', ...payloadSchema, 'x-semio': { id, version: 1, phase: 'mutation', touchedPathPatterns: ['document/scenes/{index}', 'document/scene'] } } : envelope(logical))); await writeFile(join(root, phase, '🔗️component.graphql'), graphql(logical)); await writeFile(join(root, phase, '🛰️component.proto'), proto(logical)); }
await mkdir(join(root, '🧪️contract'), { recursive: true }); await writeFile(join(root, '🧪️contract', '🔣️component.json'), json(contract)); await writeFile(join(root, '🧪️contract', '🟦️component.ts'), contractTs); await writeFile(join(root, '🧪️contract', '🦀️component.rs'), contractRust);
console.log(JSON.stringify({ leaf: 'delete-scene', physicalFolders: 3, typedFacets: 15, vectors: 1 }, null, 2));
