# Master tracking doc — Crate Consolidation & Plugin Taxonomy Restructure

Full plan: `/Users/ueli/.claude/plans/the-codebase-has-currently-declarative-ritchie.md` (approved).
This ticket path: `26/08/05/CRATE-CONSOLIDATION-AND-PLUGIN-TAXONOMY-RESTRUCTURE`.

**Scope extension (approved plan `/Users/ueli/.claude/plans/the-codebase-still-has-spicy-umbrella.md`):** the user has extended this initiative's target to zero `⚡️implementations`/`⚡️implementation` dirs *anywhere* in the repo — plugins, framework, hub, ✏️s kernels, the repo product (incl. a full Go domain-tree split), print, and ♻️mit-bestand — with aggressive package-count reduction (~61 rust crates, ~26 npm packages). That plan's wave numbering (W3 verify+hygiene, W4 mechanisms M1–M12, W5 plugin completion, W6 residual mop-up, W7 s-modules, W8 framework/os/repo/print/mit-bestand, W9 periphery, W10 finalization, W11 verification) supersedes the "W2 batch N / W3 framework / W4 finalization / W5 verification" numbering used in the Wave status section below for anything not yet dispatched. Read the spicy-umbrella plan for the full architecture spec, mechanism design, and risk register before dispatching new waves.

**⚠️→✅ "Concurrent orchestration" false alarm resolved (2026-08-05, ~19:15):** the note below (as originally written by the session that authored the spicy-umbrella plan extension) misattributed these 8 open tickets to an unknown third session. **They are not.** They are the ORIGINAL orchestrating session's own W2 batch 2, dispatched in the normal course of this session's plan (the same session that ran W0/W1/batch-1). The originating session self-audited all 8 on report of the alarm: for `🏭️process`, `📏️layout`, `💠️lowpoly`, `📋️forms` — each has exactly ONE ticket at its path, `🎫️ticket.json.sessions` shows exactly one session id (the originating session's), no old crate manifests were deleted, root Cargo.toml was never touched, and no foreign files exist in any of the four plugin trees. `📋️forms`'s agent additionally traced the "partial `📦️packages` disk state" appearance to the repo's auto-commit daemon sweeping up its own in-flight, not-yet-fully-written taxonomy files mid-migration (commit `dc7ac4ce1d`, message *"Consolidate plugin crates across GIS, FEM, Reasoning, Forms, Layout, and Puzzle"* — literally the batch-2 plugin list) — an ordinary auto-commit of one session's own WIP, not evidence of a second writer. `🧩️puzzle`, `🌍️gis`, `🏗️fem`, `💡️reasoning` were still pending re-verification as of this edit (all 8 were halted mid-write as a safety precaution the moment the alarm was read; resume in progress plugin-by-plugin as each confirms clean). **If you are the spicy-umbrella session reading this: there is no third orchestrator — you and the original session are the only two active on this repo.** Please do not dispatch fresh agents against any of these 8 plugin names; the originating session is completing them under its own already-approved plan and will update this doc + hand off cleanly (including the demonstrator interim repoint for puzzle/process/gis) once done. `🌍️gis`'s duplicate-playground-registration finding stands as a real, valid mechanism gap for whichever session's registry-refactor wave lands next (tolerant-mode discovery should dedupe by plugin id, preferring the new-contract crate, instead of erroring on transient dual-registration during an in-flight migration) — it was just misdiagnosed as third-party evidence, not as a bug in itself.

**🚨🌳️ SHAPE V2 — user directive, effective immediately (2026-08-05 late, ticket `26/08/05/SHAPE-V2-TREE-PURITY-BROADCAST`), read before dispatching or continuing ANY plugin/framework migration work:**

The target shape inside every owner's domain tree tightens to exactly THREE allowed things: `component.<ext>`
files, the `📦️packages` folder, and plain `<component>` folders. Nothing else. Concretely, changed from
what's been built so far (31/32 plugins already migrated under the OLD shape as of this notice):
1. **Entry file relocates INTO `📦️packages/<lang>/`** — `📦️lib.rs`/`📦️index.tsx` no longer sits at the
   owner root; `[lib] path = "lib.rs"` (same-dir) instead of `"../../📦️lib.rs"`; every `#[path]` inside
   gains a `../../` prefix.
2. **No sibling variant files** — `🦀️config.rs`, `🦀️terminology.rs`, `🦀️wasm.rs`, any `🦀️<topic>.rs`
   beside a `component.rs` each fold into their own `<folder>/🦀️component.rs`. Module content unchanged,
   only `#[path]`/location moves.
3. **Data (📚️examples/🧫️fixtures/🤖️generated/🛂️manifest.json) and docs (AGENTS.md/README.md) relocate to
   the owner root** — `📦️packages` holds packaging code ONLY (manifests + the entry file), never data.
   AGENTS.md content is moved verbatim, never edited (CLAUDE.md's prohibition is about editing content, not
   relocating the file) — on any root/nested collision, flag and leave both, don't merge.

Full spec + retrofit recipe: `📋️TEMPLATE.md`'s new lead section + new §14. Machine-readable contract:
`🔣️taxonomy.json`'s new `entryLocation`/`rootDataDirNames`/`rootDataFileNames`/`rootDocFileNames` fields
(additive — the existing `🎯️targets`/`🧱️elements` axis from the concurrent `UI-ELEMENT-CO-LOCATION-RESTRUCTURE`
ticket is untouched). **Every plugin migration from this point on (any not-yet-migrated plugin, 📕️norm
currently in-flight, 🪐️space's pending completion, 🎪️demonstrator, and all of W6/W7/W8) builds V2 shape
directly — do not build V1 shape and expect a later retrofit to be someone else's problem.** The ~31
already-migrated plugins need a retrofit pass (mechanical, low-risk, TEMPLATE §14) — the spicy-umbrella
session is fanning that out now; if you dispatch a retrofit agent for a plugin, check this doc first in
case it's already been claimed/done, same collision-avoidance discipline as regular migrations.

**🚨 SHAPE V2 RETROFIT CORRECTION (2026-08-06, found by 🪵️sourcing's retrofit via a real compiler error) — re-verify any already-closed retrofit that only had MANUAL verification:** TEMPLATE.md §14 step 3 originally said to prefix EVERY `#[path = "…"]` string in the relocated `📦️lib.rs` with `../../`. This is wrong for grouping-module `#[path = "."]` resets — only LEAF paths (real on-disk segments) need the prefix; `"."` resets compose against the parent's already-resolved base and get double/triple-corrected if also prefixed, which fails to compile past one nesting level. Sourcing caught this because it happened to get a real `cargo check` early; several other retrofits landed during the window when root-workspace cargo was blocked by 📕️norm's in-flight state and could only verify manually (byte-diffs + `#[path]`-target-existence checks) — **manual verification cannot catch this bug, only a real compile can.** TEMPLATE.md §14 is now corrected (both the lead-section summary and step 3 itself). **If you closed a retrofit ticket using manual-only verification, re-run a real `cargo check -p <crate>` (or isolated `--manifest-path` check) on it now that norm is no longer the active blocker** — if it fails, the fix is mechanical (un-prefix the `"."` resets, leave leaf paths prefixed) and low-risk, just needs doing.

**🚨✅ REPO-WIDE FIX (originating session, 2026-08-06 ~00:45) — `📜️script.ts` import path was wrong in 30 of 32 migrated plugins, breaking their nx test targets:** found via 📸️remodel's agent report. TEMPLATE.md §1's script.ts snippet used 6 levels of `../` from `📦️packages/🦀️rust/📜️script.ts` to reach `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️lib/⚡️implementations/🟦️typescript/📦️index.ts` — the correct depth is 5 (only 🕸️dag and 📸️remodel happened to get it right independently). Verified the count manually (5 climbs from `📦️packages/🦀️rust/` reaches repo root) and confirmed via `bun <script.ts>` resolving cleanly after the fix (previously threw a module-not-found error, now prints normal usage). **Bulk-fixed all 30 affected files in one pass** (flow, procedural, cad, sourcing, animate, shooting, sequence, vcs, writer, forms, reasoning, gis, process, lowpoly, layout, puzzle, note, dag was already correct, mathematical, playbook, block, raster, space, energy, norm, imperative, trinity, draw, architect, fem — the last 4 mid-flight, fixed in their new-location script.ts too). TEMPLATE.md §1 itself should also be corrected by whoever next edits it (the snippet's literal `../../../../../../` needs to become `../../../../../`) — not yet done, flagging for the next TEMPLATE.md edit.

**Registrar passes done by originating session, 2026-08-06 ~00:00–01:15**: 📕️norm (107→1 member swap, 17 workspace-deps removed, own agent's full verification — 894/894 tests — already covers correctness), 📸️remodel (18→1 member swap, 3 workspace-deps removed, agent's own verification — 375/376 tests, the 1 failure a genuine pre-existing photogrammetry bug correctly left alone), 🖍️draw (10 old lines → 3 new crate members — a genuine architectural exception, `semio-s-plugin-draw` + `-draw-fsm` + `-draw-fsm-macros`, since the fsm macros crate is `proc-macro = true` and Cargo forbids merging that with normal lib code; 2 workspace-deps removed; agent's own verification — 85/26/10 tests across the 3 crates, wire-identical with pinned hex for edge-case rows). 📜️imperative's registrar step was already done by the concurrent session before this session got to it (confirmed via real `cargo check` — clean, 10m13s build). **This session's own follow-up isolated re-checks of norm/remodel/draw are still blocked by 🏛️architect's unrelated concurrent in-flight state (old crate transiently missing mid-relocation)** — expected, not a problem with any of the three; will re-verify once architect/trinity/fem settle.

**✅ FIXED (originating session, 2026-08-06 ~01:30) — registry's `validateTaxonomyTree` was V1-stale under Shape V2, flagged independently by 📸️remodel's and 🏛️architect's agents.** Root cause was deeper than "wrong directory": `#[path]` resolution in Rust is genuinely cumulative (each nested mod's base resolves relative to its immediately enclosing scope's *already-resolved* directory, not always the raw entry file), and two valid conventions coexist in this repo — (A) prefix every LEAF path with the full climb, leave every `"."` reset unprefixed (🏛️architect/📸️remodel/🖍️draw's style), and (B) reset the base ONCE via an outer `#[path = "../../."]` then let nested `"."` inherit it (🖨️raster's style). My first fix used flat `join(dirname(libRsPath), p)` resolution for every leaf, which produced ~60 false-positive findings against raster (every component flagged twice). Confirmed the flat model was wrong via a REAL `cargo check --manifest-path` on raster (backgrounded past the 300s timeout) — it compiles cleanly. Rewrote the validator as a proper stack-based recursive-base tracker (walks `📦️lib.rs` line-by-line, maintains a `baseStack` seeded with the entry file's dir, pushes a new resolved base on every `pub mod X {`, pops on `}`, resolves each `#[path]`/bare-mod-name relative to the current top of stack) — this models the real semantics instead of a flat regex scan. Also made the V1-vs-V2 entry-file location check itself (`📦️lib.rs` at plugin root vs `📦️packages/🦀️rust/📦️lib.rs`). Verified: `bun 🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/⚡️implementations/🟦️typescript/📇️registry/📜️script.ts check` now returns **zero false positives** — only finding left is 🏗️fem's genuine, expected in-flight `📡️protocol` residual (fem hasn't finished its V2 retrofit yet). Safe for any future retrofit/migration agent to rely on this validator's output as ground truth.

**✅ FIXED (originating session, 2026-08-06 ~01:45) — ✒️writer's cross-plugin dependency on 🔱️trinity's deleted `trinity-jack` crate + a repo-wide leftover-verification-overlay bug that was blocking root `cargo metadata` entirely.** Two separate things:
1. **Writer→trinity repoint**: writer's `📦️packages/🦀️rust/Cargo.toml` still depended on trinity's old, now-deleted `🔨️modules/🔌️jack/⚡️implementations/🦀️rust` crate (`package = "semio-s-plugin-trinity-jack"`). Repointed to `trinity = { path = "…/🔱️trinity/📦️packages/🦀️rust", package = "semio-s-plugin-trinity" }` and repointed all `trinity_jack::` usages (3 files: writer's writer-artifact `⚙️engine/🦀️component.rs`, `📌️panels/🔍️inspection/🦀️component.rs`, `🎭️modes/✏️edit/🪟️windows/✒️main/🦀️component.rs`) to `trinity::core::` — confirmed the public path by reading trinity's own `🫀️core/🦀️component.rs`, which re-exports `Token`/`SpannedToken`/`SpannedNode`/`lex_spanned`/`parse_spanned`/`complete`/`lint`/`format`/`semantic_tokens`/`example_graph`/`Diagnostic` verbatim via `pub use component::*;` under `pub mod core`.
2. **Trinity registrar step settled**: root Cargo.toml's member/dep swap (17→1, applied earlier) is correct as-is; no further edit needed.
3. **Repo-wide leftover verification-overlay bug (real, blocking)**: root `cargo metadata` failed outright with "multiple workspace roots found" naming 🌍️gis, 🎞️animate, 🧩️puzzle, 💡️reasoning, 🎥️shooting, ✒️writer — each still had a **temporary `[workspace]`-table verification overlay** (TEMPLATE.md §3's isolated-verification trick) left in its `📦️packages/🦀️rust/Cargo.toml` instead of being deleted before handoff, exactly as each overlay's own comment says to do. Also found (via `grep -rl "TEMPORARY VERIFICATION OVERLAY"`) the same leftover in 🏭️process and 🎬️sequence (both long-closed, batch 1) — deleted those too. **Left 🏗️fem's overlay untouched** since fem is still actively in-flight (its old `⚡️implementations` crates still exist and fail to compile with the known systemic `Ok(Emit::…)`-missing-paren bug — pre-existing, not caused by anything here — touching fem's manifest mid-edit by another agent/session would risk a collision). Verified: `cargo metadata` now succeeds cleanly; `cargo check -p semio-s-plugin-trinity -p semio-s-plugin-writer` compiles clean (only pre-existing unrelated warnings); `cargo check --workspace` now only fails on fem's own known in-flight old `🖱️ui` crates (`semio-s-app-fem-2d-ui`, `semio-s-app-fem-3d-ui` — the documented systemic missing-paren bug), nothing else. **Lesson for TEMPLATE.md/future agents: the verification-overlay deletion step is easy to forget under time pressure — worth a follow-up grep sweep (`grep -rl "TEMPORARY VERIFICATION OVERLAY" ✏️s/🔌️plugins --include=Cargo.toml`) at the start of any future finalization/verification wave, not just relying on each agent's own diligence.**

**✅ CONFIRMED (originating session, 2026-08-06 ~02:00) — 📕️norm, 📸️remodel, 🖍️draw (+ its `-draw-fsm`/`-draw-fsm-macros` sibling crates) all isolated-recheck clean now that 🏛️architect settled.** `cargo check -p semio-s-plugin-norm -p semio-s-plugin-remodel -p semio-s-plugin-draw -p semio-s-plugin-draw-fsm -p semio-s-plugin-draw-fsm-macros` finishes with zero errors (only pre-existing unrelated warnings in framework store-sync/plugin crates). This also required one more overlay cleanup: 🧱️block's `📦️packages/🦀️rust/Cargo.toml` had the same `SHAPE-V2-RETROFIT-PATH-PREFIX-BUG-AUDIT` leftover `[workspace]` overlay as gis/reasoning/etc (block's own migration ticket is long CLOSED) — removed it the same way. **Repo-wide `grep -rl "TEMPORARY VERIFICATION OVERLAY" ✏️s/🔌️plugins --include=Cargo.toml` now shows only 🏗️fem** (untouched, genuinely in-flight under its own session). All of batch 3 (📕️norm, 🏛️architect, 📸️remodel, 🔱️trinity, 📜️imperative, 🖍️draw) is now fully registrar-settled and independently verified. **🎪️demonstrator is now fully unblocked** — all 6 of its dependency plugins (procedural/cad/puzzle/sourcing/process/gis) are done; only demonstrator's own full taxonomy migration remains, exactly as planned (saved for last).

**✅ FIXED (originating session, 2026-08-06 ~02:20) — round 2 of the leftover-verification-overlay sweep: 🖨️raster, 🕸️dag, 📜️imperative, 📖️playbook.** After norm's retrofit agent reported success, root `cargo metadata` still failed — this time with a *different*, more subtle symptom: not "multiple workspace roots" but `error inheriting wasm-bindgen from workspace root manifest's workspace.dependencies.wasm-bindgen … was not found in workspace.dependencies`. Root cause: 🖨️raster's `📦️packages/🦀️rust/Cargo.toml` still had its `SHAPE-V2-RETROFIT-PATH-PREFIX-BUG-AUDIT` overlay, whose LOCAL `[workspace.dependencies]` only lists `serde`/`serde_json` (not `wasm-bindgen`) — because raster declares its own `[workspace]`, Cargo resolves raster's `wasm-bindgen.workspace = true` against *raster's own* embedded workspace.dependencies instead of root's, and fails there. Removed raster's overlay (its migration ticket is long closed). While sweeping, also found and removed the same leftover in 🕸️dag, 📜️imperative, 📖️playbook (all closed tickets, confirmed via ticket status before touching) — **left 🎪️demonstrator's and 🏗️fem's overlays untouched** since both are genuinely in-flight (demonstrator under this session's own just-dispatched agent, fem under another session). `cargo metadata` now succeeds cleanly (exit 0, zero errors). **Lesson reinforced**: the verification-overlay class of bug can manifest in more than one way (dual workspace roots OR incomplete embedded `workspace.dependencies` causing a dependency-inheritance failure) — a full repo sweep (`grep -rl "TEMPORARY VERIFICATION OVERLAY" ✏️s/🔌️plugins --include=Cargo.toml`, cross-checked against each hit's own ticket status before removing) should be a standard step before trusting any `cargo metadata`/`cargo check --workspace` result, not just something to fix reactively when it happens to block you.

**🎉✅ MILESTONE (originating session, 2026-08-06 ~03:00) — 🎪️demonstrator done. ALL 32 plugins (+ demonstrator, the deliberately-last 33rd) are now migrated to one-crate-per-plugin taxonomy.** Demonstrator (ticket `26/08/06/DEMONSTRATOR-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION`) was already one crate, owns **zero artifacts/apps of its own** (confirmed by reading the code — every app it registers belongs to one of its 6 source plugins), so its taxonomy is `🎪️panes/{🌱️generator,📐️koordinator,🧩️aggregator,🗂️aussuchen,🏭️bearbeiten,🗺️verfolgen}` (one folder per playground variant, its own genuine vocabulary) + a pure-wiring `📦️lib.rs`. Fresh-verified all 6 dependency module paths against their real current code rather than trusting stale recorded mappings; found and dropped 2 dead deps (`semio-framework-core`, `infinite_canvas` — the function that justified the latter no longer exists anywhere in the repo). Equivalence proof: dumped the full serialized `PluginBundle` manifest old vs. new, diffed 10,949 lines byte-for-byte identical (demonstrator has no wire format of its own, so a hex diff would have been fabricated — this was the right proof to reach for instead). Registrar step applied: root Cargo.toml member line swapped to `✏️s/🔌️plugins/🎪️demonstrator/📦️packages/🦀️rust` (nothing to remove from `[workspace.dependencies]` — demonstrator was never aliased, nothing depends on it, it's a pure leaf). `cargo check -p semio-s-plugin-demonstrator` (+ all 6 deps) clean. Registry regenerated (38 plugin crates, 58 playgrounds, 6 framework packages) — fresh. `.vscode/launch.json` regenerate --check — fresh. **Root `cargo metadata` and `cargo check --workspace` now succeed with exactly ONE failure repo-wide: `semio-framework-ui-wgpu` failing on a missing font asset file (`…/🖼️assets/…/🔤️fonts/😀️noto-emoji/🔤️10-400.ttf` and `11-400.ttf` don't exist on disk)** — this is a pre-existing, already-documented, out-of-scope bug (flagged since early in this initiative under "dangling Anta font path" in the W5-sweep backlog), unrelated to any plugin migration, not touched. **This effectively closes out the entire plugin-fan-out wave (W1 pilot + W2 batches 1-3 + demonstrator) — every single plugin in the repo is now one crate in the new taxonomy.**

**⚠️ REAL ARCHITECTURAL FINDING from demonstrator's migration, needs a framework-wave fix (NOT fixed here, out of scope for a single-plugin ticket):** `cargo build --target wasm32-wasip2 --profile wasm-release -p semio-s-plugin-demonstrator` fails with `duplicate symbol: semio_plugin_install_bundle` / `semio_plugin_bundle_installer_link_shim`. Root cause: demonstrator now depends on 6 full plugin crates (procedural/cad/puzzle/sourcing/process/gis), each of which independently expands `plugin_exports!` (or equivalent) into its OWN copies of these two `#[no_mangle]` extern symbols — fine when each plugin ships as its own separate wasm component, but demonstrator links all 6 crates PLUS its own expansion into ONE component, so the linker sees 7 copies of the same symbol. This is structural to consolidating bundle+module crates into one, not a demonstrator-specific bug — it will resurface for ANY future plugin that depends on another plugin as a real (non-dev) dependency and also exports a component. Demonstrator's own agent's recommended fix (in its `📋️registrar-handoff.md`): gate the two `#[no_mangle]` fns behind `#[cfg(not(feature = "semio-plugin-embedded"))]` in the shared `plugin_exports!`-style macro (macro_rules cfg resolves against the *expanding* crate, so it's inert for the other ~44 plugins that don't set this feature), plus a non-default feature added to the 6 source plugins, enabled from demonstrator's own dependency lines. **Do NOT reach for `--allow-multiple-definition`** (the agent explicitly flagged this as the wrong fix — it would silently pick one arbitrary copy's behavior instead of correctly suppressing the redundant ones). This affects the shared plugin SDK (`semio-framework-plugin`), which is squarely W3/W8 framework-wave territory — flagging for whoever picks up that wave next, not fixing here since it requires editing ~45 plugins' shared macro expansion, off-limits for a single-plugin-scoped ticket.

**🚀 ENTERING W4 (spicy-umbrella numbering) — mechanism wave (originating session, 2026-08-06 ~03:15), per explicit user instruction to continue autonomously through the full remaining scope.** W5/W5d (all plugins + demonstrator) are done — see milestone above. Per the spicy-umbrella plan's dependency graph, W4 mechanisms gate W8 (framework wave); W6 residual mop-up and W7 s-modules can start once W5d is done (✓) and don't need to wait on all of W4, except where a step explicitly says otherwise. Checked `.🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️06/` for existing claims before dispatching: only `DEMONSTRATOR-…` (mine, closed) and `SHAPE-V2-RETROFIT-PATH-PREFIX-BUG-AUDIT` (concurrent session, still open) exist — **nothing claimed yet for W4/W6/W7/W8**, so dispatching now. Starting with: (1) **M1 — `🔣️taxonomy.json` + `🟦️discovery.ts` vocabulary/discovery lib**, solo strong model, since M2–M9 and effectively all of W6/W7/W8 consume it (barrier — will not dispatch M2-M9 until M1 lands green); (2) **W6 pilot — 🌊️flow's TS/extension residual mop-up**, dispatched in parallel per the plan's explicit allowance ("can start in parallel with W4/W5a"), writes `TEMPLATE-TS.md`/`TEMPLATE-EXT.md` for the rest of W6. Will report back here as each lands. If you are the concurrent session reading this: I'm claiming M1 + the flow W6 pilot right now — please claim other work in this doc before dispatching to avoid collision, same discipline as the plugin wave.

**🚀 Moving into W6/W7/W8 (2026-08-06, spicy-umbrella session) — user authorized full end-to-end autonomous completion, no further check-ins expected.** Plugin migrations are essentially done: 31/32 plugins V2-shaped and registry-green (only 🏗️fem still in-flight, not touched — that's the other session's). 🎪️demonstrator is ALSO already fully migrated (found already done on disk, presumably by the other session). Dispatched now, all in parallel, dir-disjoint:
- **W6 residual mop-up**: 🌊️flow (pilot — TS module + bim extension, writing `📋️TEMPLATE-TS.md`/`📋️TEMPLATE-EXT.md` into this folder), 📐️cad (6 TS modules + 4 TS extensions), 🎞️animate + 🧩️puzzle (TS residuals), 🔱️trinity + 🪵️sourcing (jack tools + rust extensions).
- **W7**: `✏️s/🔨️modules` 9→4 crates (`semio-s-2d/3d/mindmap/imperative`), fixes the wrong `semio-framework-os-kernel-3d-*` naming.
- **W8a**: `🌎️hub` 5→1 dress rehearsal (writing `📋️TEMPLATE-FAMILY.md` for the framework wave to follow).
**If you're the other session picking this up**: check this section + the ticket folder for `📋️TEMPLATE-TS.md`/`📋️TEMPLATE-EXT.md`/`📋️TEMPLATE-FAMILY.md` before starting any residual/framework-family work yourself — those recipes should exist soon and are worth reusing. Root `package.json` workspaces-array edits are being done directly by these agents (lower risk than Cargo, just touch that one array) — Cargo member-line changes still go through registrar handoff as usual.

**✅ M1 DONE (2026-08-06 ~03:35) — `🔣️taxonomy.json` (schemaVersion 2) + `🟦️discovery.ts` at `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️lib/⚡️implementations/🟦️typescript/`, ticket `26/08/06/MECHANISM-VOCABULARY-AND-DISCOVERY-LIBRARY` closed.** Extended (not rewritten) pre-existing partial seeds from 3 earlier tickets. Resolves the ⚙️engine-list ambiguity explicitly: `artifactComponentDirs` (5, completeness) vs `artifactChildDirs` (6 incl. ⚙️engine, structural) as two named lists with a superset invariant enforced by a new `validateTaxonomy()`. `ecosystems` map covers all 4 role-marker sites (rust/TS/Go/python); Go's manifest is `📋️project.json` (NOT go.mod — a Go module root must contain its own sources, captured as `moduleRootFilename`). `rustEntryPathRules` machine-encodes BOTH valid `#[path]` conventions (leaf-prefixed vs once-reset) from the earlier cumulative-resolution bug. `areaStates` = legacy|mixed|clean|exempt, derived from disk (`packages-dir-exists`), no hand lists. **Verified against the REAL repo, not fixtures**: `discoverPackages()` finds 43 packages/41 owners/34 clean/7 mixed, zero discovery problems; plugin-owner set exactly matches disk; 🏗️fem correctly reports `mixed` with zero false alarms; burn-down counts match an independent `find` exactly. Tests 121/130 pass (fixed the only 2 taxonomy-related failures vs. baseline; 9 remaining pre-existing/unrelated). **Registry/root policy scripts deliberately NOT refactored yet** (that's M3/M4) — this ticket only builds the shared source of truth for them to consume next.

**⚠️ Five findings from M1, not fixed (out of scope for that ticket), need action:**
1. Registry generated output went stale mid-M1 because the concurrent flow W6 pilot deleted flow's legacy bim crate — **registrar duty, needs a `registry:generate` re-run** (will do once flow's pilot fully lands, to avoid double-touching a file it may still be writing).
2. **Live instance of the plan's own predicted "Step 3 silently drops plugins" risk**: flow's NEW `🧩️extensions/🏗️bim/📦️packages/🦀️rust/Cargo.toml` has a `[package.metadata.semio]` table with no `role` key, and its nested path doesn't match the registry's plugin regex either — `flow-extension-bim` has silently vanished from the plugin registry catalog. Needs `role = "extension"` added to its metadata table (mechanical, one line) plus the real fix (M3, registry switching its discovery to `discoverPackages()`) — flagging for whoever does M3, and I'll add the one-line `role` key myself once flow's pilot agent is confirmed done with that file.
3. Root `📜️script.ts`'s `policyDiscoverPluginCrateDirs` still requires a Shape V1 owner-root `📦️lib.rs` to match a plugin — under V2 (which is now ALL plugins) it only matches 🏗️fem. **All 4 root taxonomy policy rules are effectively dark (matching nothing) until M4** — known, expected, not an emergency (the registry's own `validateTaxonomyTree` is still doing the real enforcement), but don't mistake "root policy check passes" for "taxonomy is actually being checked" until M4 lands.
4. Root's `policyTaxonomyBarrelShapeBreaches` compares against a value `"taxonomy"` that isn't in the area enum (`validateTaxonomy()` now rejects it) — the correct value is `"clean"`. **This lives in the concurrent `UI-ELEMENT-CO-LOCATION-RESTRUCTURE` ticket's territory, not this initiative's** — flagging here since M1's agent found it, but not fixing (different ticket's ownership); mention it if that session/ticket is ever coordinated with directly.
5. Burn-down lists 5 markerless package manifests + 23 packaging violations, including **3 MORE leftover verification-overlay `Cargo.lock` files** (flow, reasoning, writer) beyond the ones already cleaned up above — same class of bug as the `[workspace]`-table overlays, but this time it's a stray nested `Cargo.lock` (not a `[workspace]` table) left over from an isolated verification run. Will sweep these next.

**✅ FIXED (2026-08-06 ~03:50) — 🔱️trinity's two residual crates (🐚️shell, 🧠️lsp) were left with a stale registrar handoff from trinity's ORIGINAL migration ticket.** Trinity's own migrating agent (batch 3) had already relocated these two kept-separate crates to their new `📦️packages/🦀️rust` locations and repointed their `trinity_jack`/`trinity_ram` deps to the new merged `trinity` crate, verified standalone via an isolated overlay, and left an explicit registrar-handoff comment — but the registrar swap for these two specific lines was never applied (only the main `semio-s-plugin-trinity` line was, earlier). Found this because it was blocking root `cargo metadata` (dangling member paths pointing at deleted old-sandwich dirs). Verified `🐚️shell`'s new path compiles clean via `cargo check --manifest-path` before touching root; applied the 2-line member-path swap in root Cargo.toml, removed shell's leftover verification overlay (with its own embedded `[workspace]` table + `cargo-features` line — same class of bug as before) and its nested `target/`/`Cargo.lock`; `🧠️lsp` had no overlay/nested-artifact leftovers. **`cargo metadata` now fails ONLY on 🌊️flow's bim extension** (its old sandwich was deleted by the in-flight W6 flow pilot agent, expected transient state — not touched, waiting for that agent's own registrar handoff).

**🚀 M2-M9 dispatched (originating session, 2026-08-06 ~04:05) — 7 parallel agents, dir-disjoint per the plan.** M2 (nx plugin v2 + atomic .mjs relocation — flagged riskiest atom, solo, opus), M3 (registry script refactor to M1's discovery lib), M4 (root policy script refactor to M1's discovery lib — will surface real new findings across all 32 plugins for the first time), M6 (generated bun workspaces), M7 (dependency-cruiser modernization), M8 (ts-rs driver retarget), M9 (generated storybook config). Tickets: `26/08/06/NX-PLUGIN-V2-AND-ATOMIC-CUSTOM-PLUGIN-RELOCATION`, `REGISTRY-SCRIPT-REFACTOR-TO-VOCABULARY-DISCOVERY-LIBRARY`, `ROOT-POLICY-SCRIPT-REFACTOR-TO-VOCABULARY-DISCOVERY-LIBRARY`, `GENERATED-BUN-WORKSPACES-FROM-PACKAGE-CATALOG`, `DEPENDENCY-CRUISER-CONFIG-MODERNIZATION-FOR-TAXONOMY-SHAPE`, `TS-RS-TYPEGEN-DRIVER-RETARGET-TO-OWNER-ROOT-GENERATED`, `GENERATED-STORYBOOK-SCOPES-AND-STORIES-FROM-PACKAGE-CATALOG`. Registrar will do an end-of-wave sync pass once these land (member/workspace edits from M6, .storybook regen from M9, registry regen from M3, dep-cruiser from M7) + full verify gate.

**✅ FIXED (2026-08-06 ~04:00) — 🔱️trinity's shell residual crate's leftover verification overlay reappeared after I first removed it** (its content changed slightly between my two edits — likely a concurrent agent re-running an isolated verification pass on the same file, unaware the registrar swap had just landed). Removed it a second time + cleared the nested target/Cargo.lock again. **If you are another session/agent and find yourself re-verifying 🔱️trinity's `🐚️shell` or `🧠️lsp` residual crates: they are ALREADY registrar-processed (root Cargo.toml member lines fixed, overlay removed) as of this note — no further action needed on them, just don't reintroduce a `[workspace]` overlay into an already-registered crate.**

**⚠️ LIVE BLOCKER (not touched, not this session's to fix) — nx project graph currently hard-fails repo-wide on a duplicate `"os-hub"` project name (found by M8's agent, 2026-08-06 ~04:20).** Both `🌎️hub/⚡️implementations/🦀️rust/📋️project.json` (old, still the registered root Cargo.toml member) and `🌎️hub/📦️packages/🦀️rust/📋️project.json` (new, not yet registered) declare the identical nx project name `"os-hub"` — nx (unlike the registry script's `LEGACY_LAYOUT_TOLERANT` dual-registration handling) has no tolerance for two projects sharing a name and hard-fails its entire graph resolution, breaking `bun nx show projects`/`bun nx graph`/anything nx-driven repo-wide until resolved. Root cause: the concurrent session's `26/08/06/HUB-PRODUCT-CRATE-CONSOLIDATION-DRESS-REHEARSAL` ticket (W8a, hub 5→1) is **open/in-flight right now** — this is completely normal "both-layouts-present mid-migration" tolerant state everywhere else in this initiative, but nx's duplicate-name intolerance makes it a hard blocker specifically for nx. **Not touched** — it's squarely that ticket's own registrar step to resolve (delete old member line + register new one), and I have no visibility into whether it's mid-write right now. **Flagging clearly because this session's own M2 agent (nx plugin v2 + atomic .mjs relocation) needs a working `bun nx show projects | wc -l` as its own before/after safety check** — if M2 reports being blocked by this, that is NOT M2's own bug, it's this pre-existing concurrent collision; the fix is on the hub ticket's side, not M2's.

**✅ M7 done (2026-08-06 ~04:35) — dependency-cruiser modernized.** `no-impl-segment` (WARN, both spellings, sourced from M1's `🔣️taxonomy.json` directly so it can't drift) — 252 hits, all inside the already-flagged-for-deletion 399MB `🔌️plugin-modules` generated dir, nothing else. `no-cross-package-relative` (WARN, "$1-capture" family-prefix heuristic, deliberately skips `✏️s/🔌️plugins` since that's already an ERROR-level existing rule, and skips `📜️script.ts` files since every plugin/module bootstrap script legitimately cross-imports repo-lib by established convention) — 0 hits. Matching ESLint mirror added to `eslint.config.mjs` (4 real, genuine hits after the script.ts exclusion — in ui/math/assets/repo-lib, not fixed, just surfaced). **New finding, not fixed, flagging for a future ticket**: the real dependency-cruiser invocation cruises **zero `.ts`/`.tsx` files repo-wide** (only `.js`/`.mjs`) — a pre-existing config gap unrelated to this ticket's scope, discovered as a side effect.

**✅ M3 + M4 done (2026-08-06 ~04:55) — registry and root policy scripts both refactored onto M1's `discoverPackages()`/vocabulary.** M3: `LEGACY_LAYOUT_TOLERANT` → `PLUGINS_AREA_STATE` from `areaOf()`; both new-contract regexes → role-filtered `discoverPackages()`; 3 legacy regexes → one vocabulary-driven walk (probe-verified zero over-match); new framework catalog section (`🤖️generated/🔣️framework.json`+`🟦️framework.ts`, 7 rows); `🖥️launch.ts` is now a pure module (no CLI/import-cycle risk), folded into the main `check`. Severity is now DERIVED from area state, not hand-flipped — **W10's finalization flip is now a one-line edit in `🔣️taxonomy.json`**. Before/after catalog diff: 0 field mismatches. Also independently re-caught the exact "silently drops plugins" risk a second time (sourcing's 3 extension crates vanished mid-refactor when a concurrent agent moved them to `role="extension"` — recovered). M4: `policyDiscoverPluginCrateDirs` → discovery-driven, now finds **45 rust packages** (was 1, fem only) across plugins+extensions+tools+s-modules+framework+hub; `POLICY_PLUGINS_ROOT` deleted; framework/hub/s-module findings land at "low" priority (tracked, never error, as instructed); two real correctness fixes along the way (skip `target/` when walking for spr-naming, drop the stray-artifact rule's `target/` half since it's gitignored per-dev build output). **Verification**: existing plugin-rule breach counts identical before/after (zero regression); exit code unchanged.

**Applied myself (registrar sync, 2026-08-06 ~04:55)**: added the missing `role = "extension"` to 🌊️flow's `🏗️bim` extension Cargo.toml (M1/M3's repeatedly-flagged discovery gap — safe now that the flow W6 pilot ticket is confirmed closed and its overlay is gone). Re-ran `registry generate`+`check`: **38 plugin crates, 58 playgrounds, 7 framework packages, fresh**; `.vscode/launch.json` regenerated fresh too. Only remaining finding is fem's expected in-flight `📡️protocol` residual.

**⚠️ New findings from M4, not fixed (flagging for later, in scope-order): ** (1) 4 real `taxonomy/dirs` violations in already-closed plugins (architect's `🧱️kernel`+`🗄️registers`, cad's `🎬️interaction-spec`, process's artifact-level `📚️examples` — all "data at owner root under V2" violations); (2) 4 over-budget `taxonomy/lib-shape` entry files (trinity jack-lsp ~529L; framework ui-tui ~2217L, ui-wgpu ~10707L, renderer-engine-wgpu ~818L — framework ones are the plan's already-known "split later" godfiles, now precisely measured); (3) 5 git-tracked nested `Cargo.lock` files still need cleanup (writer, reasoning — high priority; mindmap, ◻2d, hub — low priority) — same class of leftover-verification-artifact bug as before, will sweep; (4) 15 cross-plugin path deps, most already-understood/by-design (demonstrator's 6), but **norm→fem's OLD `🫀️core` crate is a real un-applied registrar repoint** (norm's own `📋️registrar-handoff.md` spelled out the 3-line fix, apply once fem's registrar pass lands — still pending, fem still in-flight) and a new one: **framework renderer-engine→puzzle** (needs investigation, not yet understood).

**✅ FIXED (2026-08-06 ~05:00) — cleaned 2 of M4's 5 flagged git-tracked nested `Cargo.lock`/`target` leftovers (writer, reasoning — both long-closed, low-risk).** **Left ◻2d and 💭️mindmap's alone** — both are `✏️s/🔨️modules` s-module kernels, and a concurrent session appears actively mid-flight on that exact area right now (confirmed separately: `✏️s/🔨️modules/📜️imperative`'s old sandwich path was deleted out from under a dependent's Cargo.toml a short while ago, indicating live W7 s-module work) — safer to let that area's own registrar sync handle its leftovers rather than risk touching files mid-edit. 🌎️hub's nested Cargo.lock also deliberately left alone (W8a hub rehearsal is open/in-flight, see the nx duplicate-name note above).

**✅ M9 done (2026-08-06 ~05:15) — storybook config generator.** `HAND_CURATED_SCOPES` (13 entries needing custom aliases/assets/vitePlugins or living under not-yet-migrated/exempt areas) + `GENERATED_SCOPES` derived live from M1's `discoverPackages()`, via a new generic `readSemioMarkerSubTable()` added to shared `🟦️discovery.ts` (reads a `"storybook"` sub-key from either Rust `[package.metadata.semio.storybook]` or TS `package.json`'s `"semio":{"storybook":{...}}`). Opted in 4 packages (cad, block, animate, ui-styling) as a proof — byte-identical output to the old hand-literal entries. Fixed one real stale entry (`globals.css`'s cad `@source` path still pointed at a deleted TS sandwich). Added `checkStorybookFreshness()` to root `verify gate`. Verification: direct-import confirms scopes match old set exactly; couldn't run a full storybook build (pre-existing font-asset bug, out of scope) or `verify gate` (blocked by the SAME nx `os-hub` duplicate-name issue flagged above, independently re-confirmed) — verified statically instead per the ticket's own fallback instructions.

**✅ M2 done (2026-08-06 ~05:20) — the riskiest atom in the whole plan, landed clean.** Both custom nx plugin `.mjs` files moved to `📚️lib/` (W8d's final Go-split home) + nx.json's two literal paths repointed in one atomic edit (copy → byte-verify → re-run harness → delete old, so nx.json never pointed at a missing file even transiently). **Found the "before" baseline was ALREADY broken** (not by this ticket): (1) our own `@repo/policy-scripts-file` nx plugin was aborting the ENTIRE graph on `ENOENT` — root-caused to a genuine **nx defect**: nx's native file walker corrupts multi-byte emoji/VS16 sequences with lossy U+FFFD substitutions when handing paths to plugins (verified on-disk names ARE valid UTF-8; reproduces even against nx's own bundled `nx/js` plugin — unfixable from our side, worth an upstream nx bug report). Fixed defensively (skip unreadable files instead of aborting, confirmed behavior-neutral). (2) the SAME `os-hub` nx duplicate-name issue already flagged above (confirmed: exactly one duplicate repo-wide, 253 unique/254 entries — resolves the moment that ticket's own registrar step lands, not this ticket's to fix). Given the broken baseline, substituted a **churn-immune verification**: extracted the original plugin files from session-start commit `0050bd03cb`, ran old-vs-new back to back against the identical filesystem snapshot — **254 vs 254 projects, identical name/root set, zero target-set changes, exactly 45 semantic deltas** (44 are a genuine no-op `cwd` addition equal to nx's own default; 1 is a real latent-bug fix, `compose-rs::setup` was invoking the wrong script). **Caught and self-corrected one regression before shipping**: a blanket `cwd` default broke `repo-mcp` (whose 4 targets deliberately omit `cwd` to invoke root `📜️script.ts`) — refined to only apply when the project root actually owns its own `📜️script.ts`. Also removed a confirmed-dead duplicate function, a stale `cpp` namedInput glob, and hardened JSON.parse against half-written project.json files from concurrent in-flight agents.

**🎉✅ M6 done (2026-08-06 ~05:40) — ALL 7 of M2-M9 now closed. The entire W4 mechanism wave is complete.** New `🗂️workspaces.ts` (sibling to M1's files, deliberately shape-agnostic — walks for ANY dir with its own package.json, since most of the repo is still Shape V1 and M1's `discoverPackages()` only sees Shape V2) + `workspaces --write|--check` subcommand + nx targets + 8 new tests. Root `package.json`'s `workspaces` array: 59 hand-maintained → 125 generated entries, fixing the ~40-missing-math-package drift plus several other real gaps, while correctly excluding wasm-pack `pkg/` output dirs that would create bun duplicate-name collisions. **Verified via a real `bun install` A/B test**: the new array gets 100% of workspace-*path* resolution right (0 "workspace not found" errors, was 3+ before); the remaining install failure is a DIFFERENT, confirmed pre-existing/out-of-scope bug (2 dangling `workspace:*` deps in animate/renderer-react-engine's package.json, referencing npm names deleted by concurrent plugin migrations — proven via the A/B test that the OLD array fails identically). Flagged via `spawn_task` rather than fixed (outside this ticket's ownership, one is actively being edited by a concurrent session).

**W4 (spicy-umbrella mechanism wave) is now FULLY COMPLETE**: M1 (vocabulary+discovery) → M2 (nx v2, riskiest atom, clean) → M3 (registry refactor) → M4 (root policy refactor) → M6 (bun workspaces) → M7 (dep-cruiser) → M8 (ts-rs, no action needed yet) → M9 (storybook). Registry catalog fresh (38 plugin crates/58 playgrounds/7 framework packages), launch.json fresh, root policy now sees 45 rust packages (not just fem), dep-cruiser has real taxonomy-aware rules, storybook has a real generator. Next per the plan's dependency graph: W6 residual mop-up (already substantially progressing via concurrent sessions — cad/animate/sourcing residuals appear done per M9's/M3's agent observations) + W7 s-modules (also already in-flight concurrently — imperative/mindmap/◻2d kernels observed mid-migration) → W8 framework wave (hub W8a already in-flight concurrently) → W9-W11.

**📊 Registrar sync + wave-claim survey (originating session, 2026-08-06 ~05:45), after W4 mechanism wave completed.** Registry `generate`+`check`: 38 plugin crates/58 playgrounds/7 framework packages, fresh. Root `workspaces --check`: 125 packages, fresh. Surveyed `.🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️06/` for W6/W7/W8 claims before deciding what to dispatch next: **the concurrent session has already claimed essentially all of W6 and is actively driving W7/W8a too** — `ANIMATE-PLUGIN-RESIDUAL-MOP-UP-TS-RENDERER`, `CAD-PLUGIN-RESIDUAL-MOP-UP-TS-MODULES-EXTENSIONS`, `FLOW-PLUGIN-RESIDUAL-MOP-UP-TS-MODULE-EXTENSION-DE-SANDWICH`, `PUZZLE-PLUGIN-RESIDUAL-MOP-UP-TS-ASSETS` are all **closed**; `SOURCING-PLUGIN-EXTENSIONS-DE-SANDWICH`, `TRINITY-PLUGIN-RESIDUAL-MOP-UP-JACK-TOOLS`, `S-MODULES-CRATE-CONSOLIDATION-AND-NAMING-FIX` (W7), and `HUB-PRODUCT-CRATE-CONSOLIDATION-DRESS-REHEARSAL` (W8a) are all **open/in-flight right now**. Per the plan's hard ordering constraint C6 ("hub before W8b/W8c"), W8b framework families shouldn't start until W8a lands anyway — and it hasn't. **Given essentially the entire immediately-dispatchable frontier is already claimed, this session is holding off on opening new fronts to avoid collision** — will keep doing registrar-sync passes (regenerate registry/workspaces, clean leftover verification-overlay artifacts, fix cross-cutting glue bugs) as the open tickets above land, and pick up W8b (or whatever's next-unclaimed) once hub settles.

**🚀 W8b dispatched (originating session, 2026-08-06 ~06:00), per explicit user instruction to proceed even against apparent collision risk and adapt.** Given W8a (hub) is still open/in-flight under the concurrent session, and the plan's C6 ("hub before W8b/W8c") exists primarily to prove the `role="framework"` discovery mechanism works before touching bigger families — but M1/M3/M4 (already landed) already prove that mechanism works end-to-end independent of hub's own crate consolidation — dispatched 5 dir-disjoint W8b family agents now rather than waiting: 🧮️math (biggest/most varied, 52 dirs, opus), 🖱️ui (feature-flag structure + storybook C9 coordination, opus), 🗺️surface (wasm wrapper unification + renderer-react import-site lockstep, opus), 🧬️singletons+framework-core (58-dependent blast radius, opus), 🖼️assets (lighter, also asked to investigate the long-standing dangling-font-path bug, sonnet). **Skipped 📚️compiler** — genuinely blocked by an open HUMAN ticket (`26/08/05/COMPILER-MODULE-CALL-SITE-SWAP-AND-TYPST-EVICTION`, confirmed still open), a real collision risk distinct from AI-session coordination, per hard constraint C7. Tickets: `26/08/06/FRAMEWORK-MATH-FAMILY-CRATE-CONSOLIDATION`, `FRAMEWORK-UI-FAMILY-CRATE-CONSOLIDATION`, `FRAMEWORK-SURFACE-FAMILY-CRATE-CONSOLIDATION`, `FRAMEWORK-SINGLETONS-AND-CORE-DE-SANDWICH`, `FRAMEWORK-ASSETS-FAMILY-DE-SANDWICH`. Each has exclusive file ownership of its own family dir only, never touches root Cargo.toml (registrar handoff blocks required), and is instructed to expect + tolerate heavy concurrent activity elsewhere in the repo. **If you are the concurrent/hub session reading this: I am now actively working W8b in parallel with your W8a/W7 work — these are dir-disjoint from hub and from each other, so no file collision is expected, but flag here immediately if you see otherwise.**

**Rule for every subagent spawned under this initiative: never call `ticket_close` on this ticket path or on any other agent's ticket. Open and close only your own ticket, with an explicit path.**

**Batch 3 status + SHAPE V2 mid-flight redirect (originating session, 2026-08-06 ~00:15):** Before SHAPE V2 landed, this session dispatched 6 agents for the remaining unclaimed plugin queue: 📕️norm (opus), 🏛️architect (opus), 📸️remodel (opus), 🔱️trinity, 📜️imperative, 🖍️draw — plus a 7th agent that fixed the flow_dag manifest bug (see above). **📕️norm and 📜️imperative finished and closed under the OLD V1 shape** before this session read the SHAPE V2 broadcast — norm: 107→1 crate, 894/894 tests, wire byte-identical across all 79 rows/15 apps, real `🫀️core` (domain kernel + shared `NormConfig` + app-surface kernel — none of the 15 artifacts collapse into a framework kernel, verified by grep). imperative: 8→1 crate, 80/80 tests, wire byte-identical. **Both need the V2 retrofit pass** — not yet done by this session, flagging for whichever sweep (this session's or spicy-umbrella's) reaches them next; check disk state first per the usual collision-avoidance discipline before dispatching a retrofit agent. **🏛️architect/📸️remodel/🔱️trinity/🖍️draw were still in-flight when SHAPE V2 landed — redirected mid-task via SendMessage to build V2 directly (relocating any V1 content already written, not discarding it)**; 🏗️fem (batch 2, still finishing integration) was redirected the same way. Known open coupling from norm's agent: `en1992`/`en1993` engines depend on fem's OLD `fem_core` crate (not yet repointed to fem's new crate since fem was mid-migration when norm finished) — 3-line fix spelled out in norm's `📋️registrar-handoff.md`, apply once fem's registrar pass lands.

**W2 batch 2 complete (originating session, 2026-08-05, ~21:00):** all 8 plugins done and closed — 📋️forms, 💡️reasoning, 🌍️gis, 🏭️process, 💠️lowpoly, 📏️layout, 🧩️puzzle (all registrar-processed by this session) — plus 🏗️fem still finishing (its two sub-builder taxonomy trees are done, parent agent integrating + running verification). 🧩️puzzle was the heaviest (3 apps, 377/377 tests, byte-identical wire format, permanent pinned-hex wire guards) and surfaced a new class of cross-cutting dependent: **two FRAMEWORK crates with real (non-dev) `[dependencies]` on a plugin's old crates** — `semio-framework-os-kernel-surface-board-2d` (puzzle_2d + puzzle_2d_engine) and `semio-framework-os-renderer-wgpu` (puzzle_2d_engine, for `BoardHost`/board-sync glue). Repointed both to `puzzle = { package = "semio-s-plugin-puzzle" }`; board_host/layout helpers live in `engine::board_host`/`engine::layout` submodules, not the `engine` module root, so import paths needed the extra segment (fixed). wgpu renderer's check now shows only its 3 pre-existing unrelated errors (`HostEffect`, `dsl_core`, `request_media_frames` signature — flagged since W0-A). Demonstrator's puzzle repoint applied (`puzzle_3d_ui::apps::puzzle3d::{Puzzle3dPlayApp, create_puzzle3d_app, register_puzzle3d_exports}`) — **all 6 of demonstrator's original app dependencies (procedural/cad/puzzle/sourcing/process/gis) are now repointed**; only its own full taxonomy migration remains, saved for last as planned.

**`cargo check --workspace` will not go fully green until every plugin migrates:** a post-batch-2 full-workspace check surfaced two MORE independent instances of the systemic broken-`Ok(` corruption (TEMPLATE.md §12.3) — in `📕️norm`'s and `🖍️draw`'s still-fully-untouched OLD bundle/ui crates (neither has started migrating; not this session's to fix, will resolve naturally when their own migration turn ports the logic). That's 7 and 8 confirmed instances repo-wide (vcs, cad, shooting, sourcing, animate, gis, puzzle-2d-ui, now draw, norm). Every crate this session actually migrated/touched (all 9 batch-1 + batch-2 plugins, board-2d, wgpu renderer's puzzle-related paths, demonstrator, dsl-fixture-sweep) checks clean individually — `cargo check --workspace` failing is solely these two untouched plugins, not a regression.

**Xcode CommandLineTools note (this machine, discovered independently by puzzle's agent and the orchestrator):** `cc`/`cargo`/bare `python3` fail with exit 69 ("Xcode license not accepted") unless prefixed with `DEVELOPER_DIR=/Library/Developer/CommandLineTools`. Do not run `sudo xcodebuild -license` (interactive, requires the human's password) — always use the env-var workaround for any Bash invocation of cargo/cc/python3 in this session going forward.

**Batch-2 update from the originating session (2026-08-05, ~20:20):** 🏭️process is complete (128/128 tests, wire-identical, wasm+jco build succeeded — this is the ORIGINATING session's plugin, not the spicy-umbrella session's, despite the earlier ambiguity in the false-alarm note; the confusion is now resolved — see below) and its registrar step is done: root `Cargo.toml` member swap + `[workspace.dependencies]` rename + demonstrator interim repoint (procedural/cad/sourcing/gis were already repointed; process is now repointed too, using the exact module-path mapping from process's own agent). 💠️lowpoly's registrar step is also done (its old crates were already deleted on disk when the originating session checked — verification overlay cleanly removed, consistent with a just-finished agent whose completion notification simply hadn't arrived yet); root Cargo.toml updated for lowpoly too. `cargo metadata` currently fails, but for an **unrelated** reason: a nested `[workspace]` verification overlay left on disk somewhere under ➗️mathematical/🗒️note (not this session's plugins — presumably the spicy-umbrella session's own in-flight work on its unclaimed-queue plugins). Not touched, not this session's to fix; will retry `cargo metadata` once that overlay clears on its own. 📏️layout and 🏗️fem are confirmed still legitimately in-progress (old crates intact on disk) — no premature registrar action taken on those.

**Registrar pass applied by the spicy-umbrella session for forms/gis/reasoning (2026-08-05, ~20:00–20:15) — root workspace was found broken and is now green again modulo the process blocker noted below:**
- Root workspace was found with `cargo metadata` hard-failing: 📋️forms, 🌍️gis, 💡️reasoning all had their 31 combined old member lines still listed while their old crate dirs were already deleted, and none of their new `📦️packages/🦀️rust` crates were registered. Applied the standard registrar swap for all three (31 old lines removed, 3 new lines added: `semio-s-plugin-forms`, `semio-s-plugin-gis`, `semio-s-plugin-reasoning-mindmap`); also removed the 4 now-dead `[workspace.dependencies]` aliases (`semio-s-app-forms`, `semio-s-app-gis-2d`, `semio-s-app-gis-3d`, `semio-s-app-reasoning-wires` — confirmed none had `workspace = true` consumers before removing).
- Applied 🎪️demonstrator's gis interim repoint per `GIS-PLUGIN-MIGRATION-…/📋️registrar-handoff.md`'s exact mapping table: its `[dependencies]` collapsed `gis2d`/`gis2d_engine`/`gis2d_ui` → one `gis = { path = "…/🌍️gis/📦️packages/🦀️rust", package = "semio-s-plugin-gis" }`, and `📦️lib.rs`'s 4 call sites repointed to `gis::artifacts::gismap::…`/`gis::apps::gis2d::…`. (Confirmed 🏭️process's demonstrator dep is still old-style — not yet migrated, as expected; no action taken there.)
- `cargo check -p semio-s-plugin-forms`, `-p semio-s-plugin-gis`, `-p semio-s-plugin-reasoning-mindmap` — not yet run individually (blocked by the process issue below before I could get to it); `bun nx run @semio-tech/plugin-registry:generate`/`:check` not yet re-run post-fix either. **Next session/agent to touch this: please run both before assuming this pass is fully closed out.**
- **⚠️ `cargo metadata` is STILL red after this pass, for an unrelated reason**: `🏭️process` (claimed by the other session, ticket still open, old crate dir still present on disk) has its shared `dsl 🧪️fixture-sweep` cross-cutting edit already repointed to its NEW crate (`process_3d = { path = "…/🏭️process/📦️packages/🦀️rust", package = "semio-s-plugin-process" }`) while process's OLD crate (`🛂️manifest/🗿️artifact/⚡️implementations/🦀️rust`, also named `semio-s-plugin-process`) is still a registered member — a duplicate-package-name conflict cargo can't resolve. This is normal transient in-flight state (§8.2's cross-cutting edit landing before §8's old-crate deletion), not a bug — **not touched, not mine to fix, belongs to process's owning session's own registrar step once that migration is verified-green and its old crate is deleted.** Flagging clearly so neither session wastes time rediscovering this or attempts a premature fix on someone else's in-progress plugin.

**Ticket-slug collision discovered (2026-08-05, ~20:00) — a process note for future dispatches:** this session's own 🗒️note migration agent and (apparently) an agent from the other session both ended up in the SAME ticket folder (`NOTE-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION`) — ticket paths are deterministic from the titleized plugin name, so two sessions independently choosing the same next-unclaimed plugin collide on ticket identity even with each opening what they believe is "their own" ticket. The other session's agent had already made substantial, high-quality progress (full taxonomy tree, wire baseline capture, correctly identified a fabricated wasm-bindge bridge that doesn't belong) and cleanly self-halted + handed off once it noticed the collision. This session's agent is continuing that work rather than restarting it. **Lesson for both sessions going forward: check a plugin's ticket folder for existing content (not just ticket status) before assuming a plugin from the "unclaimed" list is actually untouched — the published schedule table can lag actual dispatch by minutes under concurrent orchestration.**

**✅ HOST BLOCKER — WORKAROUND FOUND, no need to wait on the human dev (2026-08-05, ~21:30–22:00):** something reset this Mac's Xcode.app license-acceptance state mid-session; `clang`/`xcrun` refuse to run under the default `Xcode.app` developer dir, breaking every `cargo test`/wasm build. **Workaround (found by the 🧱️block migrating agent, confirmed independently by the spicy-umbrella session with a real `cargo test -p semio-s-plugin-block --lib` → 102/102 passed):** prefix every cargo command that needs to link with `DEVELOPER_DIR=/Library/Developer/CommandLineTools` — the separate Command Line Tools install is unaffected by the Xcode.app license gate. Example: `DEVELOPER_DIR=/Library/Developer/CommandLineTools cargo test -p <crate> --lib`. This is a plain env var, safe for any agent to use, no `sudo`/system changes needed. **Use this prefix for every `cargo test`/`cargo build --target wasm32-wasip2`/jco-transpile step from now on** (plain `cargo check`/`clippy`/`metadata` were unaffected the whole time and don't need it). Accepting the real Xcode license (`sudo xcodebuild -license`, still needs Ueli, no rush) is a separate nice-to-have, not a blocker anymore. Anything that hit this wall and left old crates in place with a "could not verify" note (e.g. 🖨️raster, 🪐️space — see below) can now be re-verified for real with this prefix.

**✅ FIXED (2026-08-05, ~23:30) — flow_dag manifest bug, ticket `26/08/05/FIX-FLOW-DAG-MANIFEST-STALENESS-BUG-IN-GRAPH-MANIFEST-GENERATOR`:** root cause was NOT a removed manifest family — it was a fixture-discovery regex bug in the graph-manifest generator's `📜️script.ts` (`findManifestFiles()` required a directory literally named `manifest`/`🛂️manifest` AND a `.manifest.json` filename suffix; the in-flight taxonomy restructure had already moved `🌊️flow`'s manifest source to the new per-artifact layout `🗿️artifacts/<component>/🛂️manifest.json`, which matches neither predicate, so it silently dropped out of codegen). Fixed the predicate to match the real invariant (`name.startsWith("🛂️manifest.json") && name.endsWith(".json")`) instead of the stale directory/suffix assumption. **Bonus**: this same bug had already silently broken 3 OTHER manifests that migrated the same way — `writer-languages`, `note-blocks`, `wires` — all now regenerate correctly too. Verified: graph-manifest regenerates clean (13 families now, was silently missing 4), dag-port compiles + 117/117 tests pass (including the fixture tests that exercise the fixed validation), `semio-s-plugin-space` compiles. `cargo check --workspace` now only fails on 5 unrelated, currently-in-flight plugin crates (`fem-2d-ui`, `fem-3d-ui`, `trinity-rewrite-ui`, `remodel-ui`, `trinity-jack-ui`) hitting the known systemic `Ok(`-corruption pattern — expected, being fixed by their own migrating agents right now. **🪐️space is unblocked — its final re-verify + registrar step is next.**

**🚨 [RESOLVED ABOVE] NEW, UNRELATED, REPO-WIDE BUG FOUND (2026-08-05, ~23:00) — blocks `cargo check/test --workspace` for anything depending on the dag kernel crate; NOT migration-caused, NOT an environment issue, out of this initiative's scope, needs someone with math/graph domain knowledge:**
`🧰️framework/🛍️products/💻️os/🔨️modules/♾️infinite/🎲️board/🔌️ports/➡️directed/🕸️dag/⚡️implementations/🦀️rust/📦️lib.rs` line 11 imports `mathematical_graph_manifest::{flow_dag::flow_dag_manifest, ...}` and line 710's `validate_dag_fixture_node_kinds` calls `flow_dag_manifest()` — but `flow_dag` **does not exist anywhere** in the current graph-manifest generator (`🧰️framework/🔨️modules/🧮️math/🕸️graph/🛂️manifest/⚡️implementations/🦀️rust`): confirmed via a fully clean regenerate (`rm -rf 🤖️generated/` then `cargo check -p semio-framework-os-kernel-math-graph-manifest`, which correctly re-ran `bun ./📜️script.ts generate`) — the regenerated `registry.rs` has zero `flow_dag` references and the generator's own `📜️script.ts` has zero mentions of "flow_dag"/"flowDag" anywhere. This is **not a build-script race** (ruled out by reproducing identically across 3 independent clean-rebuild attempts) — it's a genuine, real staleness bug: something elsewhere removed/renamed the `flow_dag` manifest concept from the generator without updating this consumer, and it's been silently broken since at least 14:46 today (this file's mtime), possibly much longer — nobody happened to trigger a fully clean dependency resolution reaching this exact function until tonight's heavy concurrent activity did.

**Why this matters broadly**: `semio-framework-os-kernel-infinite-board-port-directed-dag` sits deep in the shared os-kernel chain (`cargo tree -p semio-s-plugin-space` confirms it's a real, unavoidable transitive dependency) — this will block `cargo test`/`cargo check --workspace` for **any** plugin or crate reaching that chain, not just 🪐️space. It happened to surface via space's re-verification tonight but will hit the next plugin (or the eventual W8 os-kernel merge) too if left unfixed.

**Why I didn't fix it myself**: `validate_dag_fixture_node_kinds` is real, load-bearing validation logic (not dead/test-only code) — guessing at a replacement manifest source without understanding the intended semantics risks silently weakening or breaking real validation. This is squarely math/graph-family framework code, outside either session's current wave (framework wave hasn't started; math family is W8). Needs an agent (or Ueli) with context on what `flow_dag` was/became to fix properly — either restore its manifest source, repoint to whatever it was renamed to, or deliberately remove the validation with a clear rationale. **Until fixed, expect `cargo check/test --workspace` to fail here specifically — this is a known, external, non-blocking-to-diagnose issue, don't waste time rediscovering it.**

**Two shared-mechanism fixes landed by the spicy-umbrella session (2026-08-05, ~19:20–19:40), safe for both sessions:**
1. `DEPENDENCY-CRUISER-CONFIG-STALENESS-FIXES` — `.dependency-cruiser.cjs`'s `no-generated-edits-upstream` rule was matching a stale filename (`plugins.json` vs the real `🔣️plugins.json`, guarding nothing); fixed. The hardcoded `PLUGINS` array (missing 🎪️demonstrator) is now derived via `fs.readdirSync("✏️s/🔌️plugins")` so it self-corrects as more plugins land — no more manual upkeep needed per batch.
2. `REGISTRY-PLAYGROUND-DEDUPE-FOR-IN-FLIGHT-PLUGIN-MIGRATIONS` — fixed the real bug behind the "🌍️gis duplicate playground" finding noted above: `LEGACY_LAYOUT_TOLERANT` tolerated dual-registration for the constitutional-crate check but not for playground entries, so registry `check`/`generate` was hard-failing for the whole workspace whenever ANY plugin was mid-migration — as of the fix it was actually 5 plugins simultaneously (fem, forms, gis, puzzle, reasoning), not just gis. New `dedupeInFlightPlaygroundEntries` keeps only the new-contract crate's playground entry when both old+new register the same variant under the same plugin id; cross-plugin-id collisions still hard-error as before. `bun nx run @semio-tech/plugin-registry:check` is green again (41 plugin crates, 58 playgrounds) — the remaining printed lines are expected warn-only taxonomy findings for fem/forms/puzzle/reasoning's normal in-flight WIP state (dangling `#[path]` mid-write, residual `📡️protocol` segments, puzzle's `📦️lib.rs` not yet wired), nothing to action.

## Discovery contract (normative — read before touching registry/policy code)

- Plugin crate = `✏️s/🔌️plugins/<plugin>/📦️packages/🦀️rust/Cargo.toml` with `[package.metadata.semio] role = "plugin"`. All other `[package.metadata.*]` keys (component.package, contributes/consumes, playground, assets) move verbatim from the old bundle crate.
- Framework crate = any `🧰️framework/**/📦️packages/🦀️rust/Cargo.toml` with `[package.metadata.semio] role = "framework"`.
- Taxonomy validator requirements per plugin: every `🗿️artifacts/<a>/` has all of `{🔺️diff,🗣️dsl,🎒️pack,🔧️op,📡️spr}` as `🦀️component.rs`; every `🪟️windows/<w>/` contains only `{🍱️panes,🪀️widgets,🪛️utilities,🎬️actions,🎚️options}` children, each leaf `🦀️component.rs`; plugin `📦️lib.rs` declares a `mod`/`#[path]` for every component file on disk and none dangle; no `📡️protocol` path segment remains under `✏️s/🔌️plugins/`.
- Migration tolerance: `LEGACY_LAYOUT_TOLERANT = true` accepts both old (7-crate) and new (taxonomy) shapes per plugin during W0–W3; a plugin present in both is "in-flight", not an error. Flag flipped off in W4.

## Single-File-Repo hazard ruling

**Status: mitigated in ticket `26/08/05/SINGLE-FILE-REPO-GOAL-RESCOPE-AND-HAZARD-DOCUMENTATION` (goal.json amended; AGENTS.md deliberately left untouched — CLAUDE.md forbids editing AGENTS.md files, no exceptions).**

The `.🦑️repo/🎯️goals/AI-OPTIMIZED-REPO/SINGLE-FILE-REPO/🎯️goal.json` goal and any agent following `.cursor/plans/single_file_consolidation_bdb40556.plan.md`'s playbook (inline `#[path]` modules back into lib.rs, delete `src/*.rs`) **must not act on `✏️s/🔌️plugins/**` taxonomy `component.rs` files or on consolidated-framework-crate module files**. "Single file" for this repo means *one file per semantic component*, which the new taxonomy already satisfies — it is not "one file per crate".

Mitigation actually landed (defense in depth, revised from the plan's original "amend goal.json + AGENTS.md" — the AGENTS.md leg is dropped, not replaced, per the absolute CLAUDE.md prohibition):

1. **Goal content amendment (done).** `🎯️goal.json`'s `description` and `prompt` fields (the only free-text fields the schema — `Goal` struct, repo CLI `🐹️main.go` — exposes; there is no separate exclusions/scope field) now carry an explicit, clearly-delimited scope note and exclusion list, quoted here:
   - *description* appends: *"files under ✏️s/🔌️plugins/\*\*/🗿️artifacts, 🎛️apps, 🎭️modes, 🪟️windows, 🍱️panes, 🪀️widgets, 🪛️utilities, 🎬️actions, 🎚️options, 📌️panels, 🎮️commands, 🛠️tools taxonomy directories, and consolidated-framework-crate module files (🦀️\<name\>.rs wired via #\[path\] from a crate-root 📦️lib.rs), are OUT OF SCOPE for single-file consolidation and MUST NOT be inlined back into their parent lib.rs or module file. These are already 'single file' at the correct grain (one file per component); merging them regresses the taxonomy, not simplifies it."*
   - *prompt* appends an explicit STOP instruction: before inlining any `#[path]`-attributed `.rs` file, check whether it sits under a plugin taxonomy directory or is a consolidated-framework-crate module file; if so, stop, do not inline, do not delete the split file; the `single_file_consolidation_bdb40556.plan.md` playbook is called out as *not applicable* to these paths, with a pointer back to this master ticket section for full context.
   - `status`, `dates`, `github`, `client`, `llm`, `parent` were left byte-identical; this was a content-only edit, not a goal lifecycle action (goal was not opened/closed/reopened).
2. **AGENTS.md — explicitly NOT amended.** The original plan's hazard section proposed amending AGENTS.md alongside goal.json. CLAUDE.md's root rule "You MUST NOT edit AGENTS.md files" is absolute and admits no task-specific exception, so this leg of the mitigation is dropped. The goal-content amendment (1) and the structural lints (3) below are the full mitigation; no AGENTS.md compensation was attempted through any other route (no symlink, no included/generated file, no adjacent doc masquerading as AGENTS.md).
3. **Structural backstop (owned by W0-B, tracked separately).** The taxonomy validator + a new `TaxonomyLibShape` policy lint in root `📜️script.ts` make any inlining fail `verify gate` immediately, independent of whether an agent reads the goal text at all. This is the mitigation of record for agents that skip `repo://goals` entirely — the goal-content edit above helps agents that *do* read goals before acting, but the lint is what actually blocks a bad merge.
4. **This breadcrumb.** Any agent that finds the goal via `repo://goals` and then searches tickets (per CLAUDE.md's "work inside a ticket" rule) lands here before starting; that's this section's job.

## Plugin-ownership schedule (for concurrent human devs — avoid editing a plugin while its wave agent owns it)

Template only below — W1's findings and the orchestrator's batch split populate the TBD rows in later waves; this session does not assign the 31 remaining plugins.

| Wave | Batch | Plugin(s) | Agent | Status | Notes |
|---|---|---|---|---|---|
| W1 | pilot (solo) | 🌊️flow | this-session orchestrator | **closed** | end-to-end pilot; wrote `TEMPLATE.md` for W2 |
| W2 | batch 1 (8) | 🌀️procedural, 📐️cad, 🪵️sourcing, 🎞️animate, 🎥️shooting, 🎬️sequence, 🌿️vcs, ✒️writer | this-session orchestrator | **closed** | registrar pass done |
| W2 | batch 2 (8) | 🏗️fem, 📋️forms, 🌍️gis, 📏️layout, 💠️lowpoly, 🏭️process, 🧩️puzzle, 💡️reasoning | **concurrent session (not this one)** | in progress | do not redispatch |
| next (unowned) | batch 3 | 📕️norm, 📸️remodel, 🏛️architect, 🔱️trinity, 🧱️block, 🪐️space, 🖍️draw, 🗒️note, 🖨️raster, 📜️imperative, 📖️playbook, 🕸️dag, ➗️mathematical | spicy-umbrella session | not started | 🎪️demonstrator strictly last, after procedural+puzzle+process+gis are registrar-processed |
| next | batch 3 | 🔋️energy | spicy-umbrella session | **closed, registrar-processed** | single-crate fast path (no union step); see note below |

Heavier plugins flagged for strongest-model assignment: 📕️norm (107 crates/15 apps), 📸️remodel (28k lines), 🏛️architect (spine decomposition, highest judgment), 🏗️fem (claimed by concurrent batch), 🧩️puzzle (claimed, multi-app), 🌍️gis (claimed), ➗️mathematical is actually small (1.3k) despite the name.

(Updated live as waves dispatch — see wave status below. **Before claiming any plugin for a new wave, check its ticket dir under today's date and its `📦️packages` disk state — the registrar-owned `git status` snapshot goes stale fast with concurrent sessions active.**)

## Wave status

- [x] W0 — mechanism preparation (7 parallel agents, all closed their own tickets: REGISTRY-DISCOVERY-CONTRACT-TOLERANCE-AND-RUST-TWIN-COLLAPSE, ROOT-SCRIPT-POLICY-REVIVAL-AND-TAXONOMY-LINT-PREP, LAUNCH-JSON-GENERATOR-FROM-PLAYGROUND-REGISTRY, STALE-CONFIG-FIXES-AND-CAPABILITY-LINT-REVIVAL, ROOT-WORKSPACE-DEPENDENCIES-AND-REGISTRAR-PROTOCOL, SINGLE-FILE-REPO-GOAL-RESCOPE-AND-HAZARD-DOCUMENTATION, plus a framework AppBuilder/app_commands!/widget-extraction ticket). Orchestrator additionally fixed a blocking bug in `🧰️framework/⚡️implementations/🦀️rust/📜️script.ts` (wrong `root/rs`+`root/js` subdirectory assumptions broke `framework-core-rs:check/generate/lint/test` — independently hit by 3 agents; corrected to the crate's real flat layout + sibling `🟦️typescript/generated/manifest.ts` output path). Known pre-existing, unrelated, out-of-scope issues surfaced and deliberately left for W5: `@semio-tech/ui-wgpu-rs:check` (ui-axes generated file stale, confirmed pre-dates this session), a dangling Anta font path breaking all Storybook builds, a circular `ui-react`/`assets` nx dependency + stale `js/` path in mit-bestand's build script, 2 plugins with forbidden wasm-bindgen deps (grandfathered as warnings), a syntax error in 🪵️sourcing's ui lib.rs (likely concurrent human edit), and 3 pre-existing errors in the wgpu renderer crate (also concurrently staged).
- [x] W1 — pilot: 🌊️flow. Ticket `26/08/05/FLOW-PLUGIN-PILOT-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION` (closed). 8 old crates -> `semio-s-plugin-flow` at `✏️s/🔌️plugins/🌊️flow/📦️packages/🦀️rust`. 83/83 tests pass (baseline 44 preserved), wire-format byte-identical (verified via before/after hex dump of all 41 command variants). Fixed 4 real gaps in W0's framework prerequisites (`app_commands!` wire-key/dispatch-context/attribute-passthrough, `semio_plugin!` testkit taxonomy-awareness) plus repointed the dsl fixture-sweep crate off the deleted app facade. `📋️TEMPLATE.md` written into this ticket folder (419 lines) — the recipe for W2. **Registrar step done by orchestrator**: root Cargo.toml member lines swapped (9 old -> 1 new, bim extension line kept), `workspace.dependencies` entry renamed, `cargo metadata`/`cargo check --workspace` clean, registry regenerated (36 plugin crates), launch.json confirmed fresh (no-op). **Also fixed a real gap surfaced by this being the first real migration**: `validateConstitutionalCrates` in the registry script didn't exempt plugins already recognized under the new taxonomy contract, so it hard-failed flow (correctly missing its old 7 crate slots) instead of deferring to `validateTaxonomyTree` — now exempts migrated plugins by id; registry `check` passes clean and taxonomy tree audit is clean for flow. Full `framework-os-dev:plugin -- flow` wasm build + jco transpile succeeded via the real pipeline (not just an isolated manual approximation). Dev boot smoke: no crash after 20s (still in legitimate cold-compile of shared framework surface crates — consistent with known slow-cold-boot behavior of this dev server).
- [x] W2 batch 1 (8 plugins): 🌀️procedural, 📐️cad(opus), 🪵️sourcing, 🎞️animate, 🎥️shooting, 🎬️sequence, 🌿️vcs, ✒️writer — all migrated, tickets closed, registrar step done (root Cargo.toml member+workspace.dependencies swap for all 8, `cargo metadata`/`cargo check --workspace` clean, registry regenerated to 36 plugin crates, launch.json fresh/no-op). Test counts: procedural 196, cad 125, sourcing 65, animate 208, shooting 97, sequence 122, vcs 41, writer 86 — every plugin exceeded its pre-migration baseline (several baselines were artificially low because the old crate didn't even compile, see below). Wire-format byte-identical proof captured and verified for all 8.
  - **Systemic pre-existing bug found**: 5 of 8 old `🖱️ui` crates (cad, vcs, shooting, sourcing, animate) were committed BROKEN at HEAD from what looks like one automated repo-wide edit — `Ok(Emit::operations(...))`/`Ok(Emit::config(...))` match arms missing their closing paren, plus a missing `Fault` import in some. Each migrating agent fixed it as a natural side effect of porting `handle()` into `app_commands!` handlers (forward-fix, not a revert). **Not yet migrated plugins may have the same corruption** — each future W2 agent should check for this pattern per TEMPLATE.md §12.3 before assuming they introduced a bug.
  - **Registrar interim fix (orchestrator, not a plugin agent's job)**: 🎪️demonstrator path-depends on procedural/cad/sourcing (+ later puzzle/process/gis) and would have permanently broken `cargo metadata`/`cargo check --workspace` from this point through the end of W2 otherwise. Applied a **dependency-only** repoint of demonstrator's Cargo.toml + lib.rs to the 3 now-merged crates (`procedural`, `cad`, `sourcing` — new module paths `crate::apps::<app>`/`crate::artifacts::<artifact>::...`), WITHOUT doing demonstrator's own full taxonomy migration (that stays last, per plan). Repeat this same interim-repoint step after every future batch that migrates one of demonstrator's remaining 3 deps (puzzle, process, gis) so the workspace never goes permanently red before demonstrator's own turn.
  - Also fixed: 💠️lowpoly's engine crate had a `[dev-dependencies]` test-only dep on cad's old engine crate (found by cad's agent, not in the original plan) — repointed to `cad::artifacts::cad::engine::geometry_import::*`. sourcing's `🧪️fixture-sweep` repoint (deferred by sourcing's agent since `CurateDocument` only existed in the not-yet-a-member new crate) applied now that the crate is a member.
  - **Agent-infrastructure issue discovered and worked around**: several batch-1 subagents used `run_in_background: true` for long cargo commands and then stalled indefinitely — a subagent does NOT get the same auto-wake-on-background-completion the main session gets. Fixed per-agent via `SendMessage` resume instructing synchronous-only Bash calls; added as TEMPLATE.md §12.1 so future batches don't hit it. One agent (animate) also hit a transient API "connection closed mid-response" error, unrelated — resumed successfully.
  - TEMPLATE.md gained §12 (batch-1 findings: never-background-verification, shared-options-at-mode-level pattern from cad, the systemic broken-ui-crate pattern, cross-plugin test-only dependents) and a refinement to the DocumentHelpers placement rule (app-only-state helpers stay at app level even with 2+ consumers, from writer).
- [~] W2 batch 2 (8 plugins, this session, false alarm resolved — see note above): 📋️forms **done/closed** (88/88 tests, caught+fixed its own wire-ordinal bug pre-ship). 🏭️process, 📏️layout, 💠️lowpoly, 🌍️gis, 💡️reasoning, 🏗️fem in progress (fem's fem2d/fem3d sub-builders both finished, parent integrating). 🧩️puzzle collision-check still pending report (opus, heaviest/3-app plugin — longer runtime expected, not yet concerning on its own).
- [x] 🗒️note — closed, registrar-processed (spicy-umbrella session). 63/63 tests, wire byte-identical (41-variant `NoteCommand` baseline), clippy clean, wasm32-wasip2 clean. **Note on process**: this ticket briefly collided with an agent from the other session (see the ticket-slug-collision note above) — the other agent's substantial WIP (full taxonomy tree, wire baseline, a correctly-identified fabricated `wasm.rs` bridge with no basis in the old plugin) was preserved and built on rather than redone. Registrar pass done: root Cargo.toml member swap (8→1) + `[workspace.dependencies]` alias removed. **Two cross-cutting dependents found that the migrating agent's own grep missed** (both fixed by the spicy-umbrella session's registrar pass): `dsl/🧪️fixture-sweep` (`note_app` dep + a `use note_app::NoteDocument` line) and `dsl/📇️registry` (`note`+`note_op` deps collapsed to one `note` dep, `schemas.insert(...)` call sites repointed to `note::artifacts::note::{NOTE_DOCUMENT_SCHEMA,NoteDocument}`/`note::artifacts::note::diff::NoteDiff`). **Lesson for TEMPLATE**: §0.5's dependent-search grep needs to also cover module-level import lines (`use <old-crate>::Type`), not just `Cargo.toml` path strings — a crate can be a real dependent without literally appearing in a fresh grep if the migrating agent's grep pattern was too narrow (e.g. searched only the exact old package name, missing a `use` statement importing via the crate's `[lib] name` alias).
- [x] 🕸️dag — closed on disk (old crates deleted, new crate `semio-s-plugin-dag` complete), registrar-processed by the spicy-umbrella session; **ticket_close notification hadn't landed yet when the registrar pass ran** — if you see this plugin's ticket still marked "open" alongside a fully-registrar-processed state, that's why, not a bug. Same cross-cutting-dependent gap as note: `dsl/🧪️fixture-sweep`'s `dag_app` dep + `use dag_app::DagDocument` fixed (repointed to `dag_app::artifacts::dag::DagDocument`). Root Cargo.toml member swap (8→1) done, carefully excluding two UNRELATED framework crates that also contain a `🕸️dag` path segment (`♾️infinite/…/port/directed/🕸️dag` and `🧮️math/🕸️graph/🕸️dag` — do not confuse these with the plugin, see TEMPLATE.md if this trips up a future agent).
- [x] ➗️mathematical — closed on disk (old 8 crates deleted, `semio-s-plugin-mathematical` complete), registrar-processed by the spicy-umbrella session (member swap 8→1, `[workspace.dependencies]` alias removed). Its own temporary `[workspace]` verification overlay + nested `target/`/`Cargo.lock` (TEMPLATE §3) had NOT been cleaned up before old-crate deletion — removed by the registrar pass since it was now live-blocking `cargo metadata` for both sessions after being registered as a member. Cross-cutting `dsl/🧪️fixture-sweep` dependent fixed (`mathematical` dep + `use` line repointed to `mathematical::artifacts::mathematical::MathProjection`, confirmed genuinely plugin-owned — no shared kernel crate).
  - **⚠️ Self-correction on 🕸️dag, logged for the record**: my first fixture-sweep fix for dag assumed `DagDocument` was the dag PLUGIN's own type and repointed accordingly — **wrong**. Dag's own migrating agent's final report correctly identified `DagDocument` as genuinely **kernel-owned** (`🧰️framework/…/♾️infinite/🎲️board/🔌️ports/➡️directed/🕸️dag`, crate `semio-framework-os-kernel-infinite-board-port-directed-dag`) — a coincidental name collision with the plugin, not the same type. Caught and corrected before it caused a real compile break; verified by grepping for the struct definition in both crates rather than assuming from the crate/module name. **Lesson for every future registrar action (both sessions): grep-verify a symbol's actual defining crate before repointing a cross-cutting dependent — don't infer ownership from naming similarity alone**, especially since this repo already has precedent for kernel-owned types with plugin-sounding names (flow's `FlowFixture` is the same pattern, correctly handled by the flow pilot).
  - **Full end-to-end verification after all three (note/dag/mathematical) registrar passes**: `cargo metadata` green, AND `cargo check -p semio-framework-os-kernel-dsl-fixture-sweep --tests` (the shared crate all three dependents touch) compiles clean — only pre-existing, unrelated warnings in `flow-core`. This is a real compile proof, not just metadata resolution.
- [x] 📖️playbook — closed, registrar-processed (spicy-umbrella session). 50/50 tests, wire byte-identical, clippy clean, wasm32-wasip2 clean. Old bundle used a manual `PluginBundle` builder (not `semio_plugin!`) — migrated to the macro, preserving the exact bundle id `"playbook-play"`. `🧩️extensions/🌀️procedural` confirmed untouched (depends directly on the framework kernel crate, never on old core crate names) — its member line correctly preserved during the registrar pass. Cross-cutting `dsl/🧪️fixture-sweep` fix (playbook alias → the framework kernel crate directly, `semio-framework-os-kernel-playbook`) was self-applied correctly by the migrating agent this time — no correction needed. **Registrar note**: `python3` is unusable in this environment for scratch scripting (`/usr/bin/python3` requires accepting the Xcode license, which needs interactive `sudo` — not attemptable here); did the member-line swap with plain `Edit` calls instead. Worth knowing if either session's future registrar passes reach for a python one-liner for bulk edits.
- [x] 🧱️block — closed, registrar-processed (spicy-umbrella session). 3-app plugin (◻2d/🧊️3d/🖐️5d), each with its OWN artifact (verified by reading code, not guessed — 3 genuinely separate document schemas) plus a non-constitutional `🫀️core` cross-artifact kernel for shared record types. 102/102 tests **verified for real with the `DEVELOPER_DIR` workaround** (see above), wasm32-wasip2 build produced a real `.wasm`, clippy clean. 2 of 3 old ui crates had the known §12.3 `Ok(`/`Fault` corruption, forward-fixed. Caught and fixed 2 real wire-format regressions during its own diff (`#[dsl(...)]` attributes it had over-applied that didn't exist on the original hand-written enum) — good self-correction. **One cross-cutting dependent the agent's own grep missed** (same class of gap as note's): `dsl/🧪️fixture-sweep`'s `block_2d`/`block_3d`/`block_5d` deps + 3 `use` lines, found and fixed by the registrar pass (collapsed to one `block` dep, repointed to `block::artifacts::block{2d,3d,5d}::Block{2d,3d,5d}Definition`).
- [x] 🖨️raster — closed, registrar-processed (spicy-umbrella session). The migrating agent hit the Xcode-license blocker mid-verification and correctly left old crates in place rather than fabricate a pass. Once the `DEVELOPER_DIR` workaround was found (via block, above), re-verified for real: 57/57 tests, `cargo check --target wasm32-wasip2` clean. Removed its own leftover `[workspace]` overlay + nested target/Cargo.lock, deleted old 8 crates, applied registrar swap + fixed the flagged `dsl/🧪️fixture-sweep` cross-cutting dependent (`RasterProjection`, confirmed plugin-owned, repointed to `raster::artifacts::raster::RasterProjection`).
- [x] 🪐️space — **DONE, registrar-processed (originating session, 2026-08-05, ~23:45)**, now that the flow_dag bug is fixed. Re-ran the full verification for real: 85/85 tests pass, native check clean, wasm32-wasip2 clean, clippy clean. Old 16 crates deleted, root Cargo.toml member+workspace.dependencies swap done, temporary verification overlay + nested target/Cargo.lock removed, `cargo metadata`/`cargo check -p semio-s-plugin-space` clean, registry regenerated (38 plugin crates), launch.json fresh. Cross-cutting dependent fixed: `dsl/🧪️fixture-sweep`'s `home` dep repointed from the deleted `semio-s-app-space-home` facade to `semio-s-plugin-space` (`use home::artifacts::home::SHomeDocument`). Superseding the note below (kept for history).
- [x] ~~🪐️space — real bugs (79 errors) found and fixed by a dispatched fix-forward agent (root cause: missing `#[dsl(keyword=...)]` on command payloads, private label structs, missing trait imports — see prior note, superseded here). Agent reported **85/85 native tests passing** before its final re-verification poll ran into the unrelated repo-wide dag-kernel bug documented above (its own diagnosis of that bug matched mine independently — good sign both agents reasoned correctly). I independently confirmed the same blocker via `cargo tree -p semio-s-plugin-space` (real, unavoidable transitive dependency on the broken crate) and 3 separate clean-rebuild attempts. **Space's own code is very likely correct** (85/85 from the fix agent, consistent with my own earlier partial verification before the blocker), but I cannot get a clean, independently-verified `cargo test` pass through the full dependency graph until the dag-kernel bug above is fixed by someone else. Ticket left open, old 16 crates left in place — this is the CORRECT conservative state, not a stall. **Once the dag-kernel bug is fixed (by anyone), re-run `DEVELOPER_DIR=/Library/Developer/CommandLineTools cargo test --manifest-path ✏️s/🔌️plugins/🪐️space/📦️packages/🦀️rust/Cargo.toml --lib` — if still 85/85, complete the registrar pass immediately, no further investigation needed.**
- [x] 🔋️energy — closed, registrar-processed (spicy-umbrella session). Not a play-app: pure headless BEM compute library, no document/projection/Command/DSL/pack/protocol/UI — `semio_plugin!` doesn't apply. Old crate `semio-s-plugin-energy-engine` (11,663-line single `📦️lib.rs`, 50 top-level `mod` domains) → `semio-s-plugin-energy` at `📦️packages/🦀️rust`; the 50 domains split into flat `⚙️engine/🦀️<domain>.rs` files at crate root (no `🗿️artifacts`/`🎛️apps` — genuinely absent, not skipped). 235/235 tests (exact baseline match), clippy clean, `wasm32-wasip2` clean. Wire-proof n/a (no wire codec exists in this crate). Registrar pass done: root Cargo.toml member line swapped, `cargo check -p semio-s-plugin-energy` clean, registry regenerated (45 plugin crates). Fixed one stale cross-cutting test fixture (`🦑️repo/…/📚️lib/…/🧪️index.test.ts:1101`, was asserting the deleted old path/package name). Open judgment call for a future pass: `role = "plugin"` classification and `[lib]` crate-type (no `cdylib`, since no `plugin_exports!` guest interface) may warrant reconsideration once the framework wave defines non-plugin roles (`s-module`/`tool`) more concretely — flagged, not blocking.
- [x] **ALL plugins done, including 🎪️demonstrator (2026-08-06 ~03:00)** — every plugin in `✏️s/🔌️plugins/` (38 crates per the registry catalog) is now one crate in the new taxonomy: 📕️norm, 📸️remodel, 🏛️architect, 🔱️trinity, 🧱️block, 🪐️space, 🖍️draw, 🗒️note, 🖨️raster, 📜️imperative, 📖️playbook, 🕸️dag, ➗️mathematical, 🎪️demonstrator (all closed by the two sessions above, most recently norm's V2 retrofit + demonstrator itself) plus every earlier batch. 🏗️fem is the sole exception, still legitimately in-flight under its own session as of this update. Root `cargo metadata`/`cargo check --workspace` succeed except for fem (expected) and one pre-existing, out-of-scope `ui-wgpu` font-asset bug (flagged since W0, unrelated to any plugin). Registry + launch.json both regenerate fresh. **This closes out W1 (pilot) + W2 (batches 1-3) + the demonstrator finale — the entire original plugin-fan-out mandate is complete.**
- [ ] W4 (spicy-umbrella numbering) — mechanism wave M1–M12: shared taxonomy vocabulary + discovery contract, nx plugin v2, registry/root-policy refactors extended beyond plugins, generated bun workspaces/storybook/launch freshness, dep-cruiser bans, ts-rs retarget, finalization flip. See the approved plan for the full step list.
- [ ] W6 — residual mop-up for already-migrated plugins' leftover `🔨️modules` TS packages + `🧩️extensions` (writer, vcs, shooting, sequence, animate, cad, sourcing, flow, procedural all have some).
- [ ] W7 — ✏️s/🔨️modules kernels 9→4 (semio-s-2d/3d/mindmap/imperative).
- [ ] W8 — framework/os/hub/repo-product(Go split)/print/mit-bestand consolidation (~61 rust crates, ~26 npm packages end state).
- [ ] W9–W11 — periphery sweeps, finalization flip (delete legacy code paths, promote warn→error, zero-⚡️implementations tripwire), full verification matrix.

## Registrar Protocol

Landed in W0 (ticket `26/08/05/ROOT-WORKSPACE-DEPENDENCIES-AND-REGISTRAR-PROTOCOL`), governs root Cargo.toml/Cargo.lock ownership for W1–W3:

- **Only one serialized "registrar" agent may edit root `Cargo.toml`/`Cargo.lock` during W1–W3**, run once per plugin (W1) or per batch (W2–W3) — never concurrently with another registrar pass, matching the shared-file-ownership table above (root Cargo.toml/lock: registrar only, serialized per batch, W1–W3).
- **Migrating agents never touch root Cargo.toml/Cargo.lock.** A migrating agent creates its plugin's new crate directory (`<owner>/📦️packages/🦀️rust/Cargo.toml` per the discovery contract) and, if the future-shape glob member trick is ever wired up per-batch (see note below — it is NOT wired up yet), the new crate auto-enrolls without any root-file edit. Until then, the new crate simply isn't a workspace member until the registrar adds it.
- **Sequence per plugin/batch:** (1) migrating agent(s) build + self-verify their new crate(s) in isolation (`cargo check -p <new-crate>` against the still-unmodified root members list — the new crate is reachable via its own manifest even before being a workspace member, since `cargo check -p` on a path outside `members` still works as a standalone package check); (2) once green, the registrar deletes that plugin's OLD literal member lines from root `Cargo.toml` and adds the new crate's member line(s); (3) registrar runs `cargo metadata` to settle `Cargo.lock`; (4) registrar regenerates the plugin registry + `.vscode/launch.json` (per the mechanism-updates table) in the same pass.
- **Glob members are NOT enabled yet.** W0 tested `members` globs with emoji path segments (e.g. `✏️s/🔌️plugins/*/📦️packages/🦀️rust`) — the emoji syntax itself parses fine, but Cargo hard-errors (`failed to read .../Cargo.toml: No such file or directory`) on any glob matching zero directories, which every future-shape glob does today since no crate has moved yet. So globs can only be added incrementally, one pattern per batch, once at least one real matching directory exists — this is registrar work, not something a migrating agent enables preemptively. Until a batch's registrar pass adds the matching glob (or literal line), new crate dirs are inert to `cargo metadata`/`cargo check --workspace`.
- **`[workspace.dependencies]` adoption is opt-in per crate**, not automatic: a migrating agent MAY rewrite its new crate's `[dependencies]` entries to `dep = { workspace = true }` for names already listed in root `[workspace.dependencies]` (see the externals + internal-path survey landed in this ticket), but this still requires the registrar to already have the crate registered as a member for `cargo metadata` to resolve `workspace = true` — so in practice this rewrite happens in the SAME registrar pass that adds the member line, or after.
- **Red gate blocks the next batch.** If a registrar pass leaves `cargo metadata`/`cargo check --workspace`/`verify gate` red, no further batches dispatch until a scoped forward-fix agent (never a revert — no git-modifying commands allowed) restores green.
