[package]
name = "db_testkit"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Db testkit — SimRuntime, FaultStorage, CrashHarness, generators, cross-crate laws, benchmarks"

[lints]
workspace = true
[lib]
path = "lib.rs"

[dependencies]
db = { path = "../../rs" }
pack_testkit = { path = "../../../pack/testkit/rs" }

[[bench]]
name = "db"
harness = false

[dev-dependencies]
criterion = { version = "0.5", default-features = false, features = ["html_reports"] }

