---
slug: CODE-REPORT
author: Ueli Saluz <ueli.saluz@iek.uni-hannover.de>
summary: Generate code.json code-quality report
model: gpt-5.2-codex
input:
  - prompt: >-
      The analyze script should be extended to create a report for the codebase
      producing code.json (for typescript, python, c#). It should check for:
      Comments in the code. Missing License headers. Regions that dont close
      (every #region REGIONNAME needs to have a corresponding #endregion
      REGIONNAME).
    date: '2025-12-15T22:44:34.206Z'
commit: 2fb81ef29354981c1b9625769dba4a06360a4aef
files:
  updated:
    - AGENTS.md
    - README.md
    - hooks/typescript.ts
    - log/2025/12/15/CODE-REPORT.md
    - reports/README.md
lines:
  added: 1343
  removed: 317
---
# Previously

# Need a new repo-wide report (`reports/code.json`) generated by the `analyze` pipeline.
# The report targets TypeScript, Python, and C# source files.
# Required checks: inline comments, missing license headers, and malformed/mismatched `#region` blocks.

# Plan

# Extend the existing preflight `hooks/typescript.ts` to also emit a `reports/code.json` report.
# Scan tracked files via `git ls-files` and filter to `.ts`/`.tsx`, `.py`, `.cs` with repo directory exclusions.
# Implement three validators: SPDX header presence, comment detection (excluding SPDX + regions), and region stack validation.
# Update developer documentation to include the new report and where it is generated.

# Changes

# Added `reports/code.json` generation to `hooks/typescript.ts`, alongside the existing `reports/typescript.json` output.
# Implemented file scanning for TypeScript/Python/C# with:
# - SPDX header detection (expects `SPDX-License-Identifier:` in the header block)
# - Inline comment detection with allowances for license headers and region markers
# - Region stack validation requiring named `#region`/`#endregion` pairs and proper nesting
# Added `reports/README.md` and documented `code.json` across `README.md` and `AGENTS.md`.
