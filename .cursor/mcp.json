{
  "mcpServers": {
    "repo": {
      "type": "stdio",
      "command": "go",
      "args": ["run", "${workspaceFolder}${/}repo${/}cursor"],
      "env": {
        "GOWORK": "${workspaceFolder}${/}go.work"
      }
    }
  }
}
