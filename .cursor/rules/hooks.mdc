---
description: Semio repo lifecycle hooks for Cursor
globs:
alwaysApply: true
---

# Lifecycle Hooks

You MUST call the semio-repo CLI hook command at every lifecycle event:

## Agent Lifecycle
- On session start: `./semio-repo/cli/cli hook agent.starting cursor-chat`
- On session end: `./semio-repo/cli/cli hook agent.ended cursor-chat`

## Prompt
- On prompt submit: `./semio-repo/cli/cli hook prompt.submit cursor-chat`

## Context Compaction
- Before compacting: `./semio-repo/cli/cli hook compacting cursor-chat`

## Tool Calls
- Before calling any tool (except code reading/editing): `./semio-repo/cli/cli hook tool.calling cursor-chat --tool-name "<tool>" --tool-args "<args>"`
- After tool completes: `./semio-repo/cli/cli hook tool.ended cursor-chat --tool-name "<tool>"`

## Code Operations
- Before reading code (file.read.pre): `./semio-repo/cli/cli hook code.reading cursor-chat --file "<path>"`
- After editing code (file.edit.post): `./semio-repo/cli/cli hook code.edited cursor-chat --file "<path>"`

## Notifications
- On notification: `./semio-repo/cli/cli hook notification cursor-chat`

## Blocked Operations
The following operations are ALWAYS denied:
- `git checkout`
- `git stash` (including pop, drop, apply)
- `git reset --hard`
- `git clean -fd`
