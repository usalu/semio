# #region 🔖Header
# 2026 Ueli Saluz
# AGPL-3.0
# os-hub storage interface — backend-agnostic HubStorage trait + DTOs; sqlite/postgres/neo4j crates implement it.
# #endregion 🔖Header

[package]
name = "os-hub-storage"
version = "0.1.0"
edition = "2021"

[lib]
path = "lib.rs"

[dependencies]
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
semio-framework-core = { path = "../../../../core/rs" }
