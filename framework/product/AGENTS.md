# Platform

A full-blown product framework for extendable platforms.

# Playground

A lightweight product framework for interactive playground apps. Standalone dev uses the single generic runner at `framework/product/playground/dev` driven by each domain's `PlaygroundAppDefinition` in `*/core/playground.ts`.

# Os

Composable operating system shell (media graph, program registry, app hosting). `s` is an Os instance.
