[package]
name = "semio-framework-kernel-db-observe"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Db observability — metrics, spans, health registries, structured/audit event sinks, determinism verification"

[lints]
workspace = true
[lib]
name = "db_observe"
path = "lib.rs"

[features]
otel = []

[dependencies]
db_core = { path = "../../core/rs", package = "semio-framework-kernel-db-core" }
pack_core = { path = "../../../../../s/kernel/pack/core/rs", package = "semio-s-kernel-pack-core" }
