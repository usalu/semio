# Check ID and URI System

## Todos

- [ ] Analyze existing ID and URI implementation in `./semio-repo/cli`
- [ ] Analyze existing ID and URI implementation in `js/vscode`
- [ ] Update `./semio-repo/cli` to match the new ID and URI specification
- [ ] Update `js/vscode` to match the new ID and URI specification
- [ ] Extend existing tests to verify URI compliance (no whitespace, correct format)
- [ ] Verify all tests pass
