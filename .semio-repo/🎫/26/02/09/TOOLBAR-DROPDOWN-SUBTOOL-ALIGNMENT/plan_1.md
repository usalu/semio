1. Inspect toolbar dropdown positioning and tooltree mapping for Selection/Hand in Sketchpad.
2. Update toolbar dropdown rendering so subtools open as an upward vertical list directly above the active tool trigger.
3. Ensure Selection and Hand are represented as subtools in the Selection dropdown table.
4. Move Additive/Subtractive selection controls into the Tool Setting Bar content for active Selection context.
5. Update Playwright toolbar expectations if needed and run targeted tests.
6. Update `README.md` and `AGENTS.md` with the revised toolbar behavior and setting-bar mapping requirements.
7. Append ticket log/changes and final summary in `ticket.md`, then close the ticket with touched files.
