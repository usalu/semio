# Plan

1. Reopen and continue the same browser attach ticket from the new user report.
2. Apply remote-container browser launch fix by forcing UI-side launch in VS Code browser debug targets.
3. Align launch variable usage with `${workspaceFolder}` for stable path resolution.
4. Update root docs with the remote launch requirement and finalize ticket summary.
