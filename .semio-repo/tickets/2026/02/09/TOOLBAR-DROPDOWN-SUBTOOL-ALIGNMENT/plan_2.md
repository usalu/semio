1. Revert non-essential toolbar behavior changes from prior iteration (selection-path defaults and hand-settings behavior).
2. Keep toolbar id naming and test selectors aligned with the previous `toolbar.group.*` convention.
3. Implement only the Selection dropdown anchor fix so the dropdown positions correctly above the selection tool.
4. Update ticket log and summary, then close the reopened ticket.
