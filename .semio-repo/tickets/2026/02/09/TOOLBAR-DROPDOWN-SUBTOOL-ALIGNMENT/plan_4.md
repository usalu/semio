1. Keep current selection dropdown anchoring above the selection tool.
2. Render dropdown subtool entries as a strict stacked list (table-like rows) with full-width rows and separators.
3. Confirm Selection subtool sources remain `select` and `hand`, and settings content remains additive/subtractive controls.
4. Update README.md and AGENTS.md to document the stacked-row dropdown requirement.
5. Update ticket summary and close with touched files.
