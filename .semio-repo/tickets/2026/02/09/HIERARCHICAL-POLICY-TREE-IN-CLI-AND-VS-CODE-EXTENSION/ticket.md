---
goal: AI-OPTIMIZED-REPO/REPO-CLIENT/REPO-BINARY/REPO-MECHANISMS/REPO-POLICY-MECHANISM
---

# Ticket

## Summary

Added hierarchical `policy tree` command to CLI, MCP tool `policy_tree`, and VS Code extension command `semio.policyTree`. The CLI `policy tree` renders policies with violation kinds as nested children in both markdown and text formats. The VS Code extension sidebar already rendered violation kinds hierarchically under policies. Added `EmojiViolation` constant to fix a pre-existing build error. Added test `TestPolicyTreeCommand`. Updated AGENTS.md SRS and README.md documentation.
## Changes

## Log

## Todos

## Plan
