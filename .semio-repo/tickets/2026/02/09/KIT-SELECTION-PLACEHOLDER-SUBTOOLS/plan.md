# Plan

1. Identify the Kit app toolbar Selection group configuration and current sub-tools.
2. Add five distinct placeholder Selection sub-tools to the Selection dropdown toggle.
3. Update tests or fixtures if they assert the old sub-tool count.
4. Update root `README.md` and `AGENTS.md` with the new Kit Selection dropdown placeholder sub-tool behavior.
5. Record changes and summary in ticket files and close the ticket with touched file list.
