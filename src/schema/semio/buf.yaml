version: v1
name: buf.build/usalu/semio
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway
# build:
#   excludes: [google]
breaking:
  use:
    - FILE
lint:
  use:
    - BASIC