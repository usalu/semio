//! 🧮 Sparse linear algebra: COO/CSR/CSC assembly, direct LDLT and iterative PCG solvers, a
//! subspace-iteration eigensolver (modal/buckling), and RCM bandwidth-reduction ordering.
//! Implemented in a follow-up workstream — module reserved here so `analyses`/`mesh` can depend on
//! its (future) public API without a circular-crate dance.
