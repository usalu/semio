// #region 🧲Header
// 💻 elements/lib/react/topology/play/main.ts — Vite entry: mounts topology play via {@link mountTopologyPlay}.
// #endregion 🧲Header

void import("../index.tsx").then((m) => {
	m.mountTopologyPlay();
});
