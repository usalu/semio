# #region 🔖️Header
# 2026 Ueli Saluz
# AGPL-3.0
# os-hub directory interface — backend-agnostic HubDirectory trait + DTOs (identity/tenancy only:
# users, studios, memberships, auth sessions, share tokens, vfs nodes, sync sessions); sqlite/
# postgres/neo4j crates implement it. Document persistence and blobs live in `db::Database` instead.
# #endregion 🔖️Header

[package]
name = "semio-hub-directory"
version = "0.1.0"
edition = "2021"


[lints]
workspace = true
[lib]
path = "../../../../../🌎️hub/🔨️module/📇️directory/⚡️implementation/🦀️rust/📦️lib.rs"

[dependencies]
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
