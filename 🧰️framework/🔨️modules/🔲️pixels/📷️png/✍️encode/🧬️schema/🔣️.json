{
  "$schema":"http://json-schema.org/draft-07/schema#",
  "title":"Incremental PNG Encoding",
  "description":"An owned RGBA8 image is encoded in fixed 4096-byte scanline blocks. Each advance processes one block and yields. Only a completed job exposes a PNG; cancellation discards the unpublished candidate. PNG uses color type 6, bit depth 8, filter None, fixed-Huffman DEFLATE blocks, and validated CRC/Adler checksums.",
  "type":"object",
  "required":["completed","total","done"],
  "additionalProperties":false,
  "properties":{"completed":{"type":"integer","minimum":0},"total":{"type":"integer","minimum":1},"done":{"type":"boolean"}}
}
