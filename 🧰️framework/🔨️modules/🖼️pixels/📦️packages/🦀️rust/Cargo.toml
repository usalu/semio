[package]
name = "semio-framework-pixels"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Owned raster image codecs (PNG) and pixel-buffer utilities for the framework kernel"

[package.metadata.semio]
role = "framework"
id = "pixels"

[lints]
workspace = true
[lib]
path = "🦀️.rs"

[dependencies]

[dev-dependencies]
png = "0.17.16"
