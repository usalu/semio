{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "grants",
    "nonbroadcastDomains",
    "cases"
  ],
  "properties": {
    "version": {
      "const": 1
    },
    "grants": {
      "const": [
        1,
        64,
        4096
      ]
    },
    "nonbroadcastDomains": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "$ref": "semio:local-interaction#/$defs/id" }
    },
    "cases": {
      "type": "array",
      "minItems": 14,
      "allOf": [
        { "contains": { "type": "object", "required": ["id"], "properties": { "id": { "const": "sparse-explicit-private-replacement" } } } },
        { "contains": { "type": "object", "required": ["id"], "properties": { "id": { "const": "sparse-empty-preserves-three-maps" } } } }
      ],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "before",
          "current",
          "restore"
        ],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1
          },
          "before": {
            "$ref": "semio:local-interaction#/$defs/state"
          },
          "current": {
            "$ref": "semio:local-interaction#/$defs/identity"
          },
          "restore": {
            "$ref": "semio:local-interaction#/$defs/restore"
          },
          "expected": {
            "$ref": "semio:local-interaction#/$defs/state"
          },
          "error": {
            "const": "stale-authority"
          }
        },
        "oneOf": [
          {
            "properties": { "expected": { "$ref": "semio:local-interaction#/$defs/state" } },
            "required": [
              "expected"
            ]
          },
          {
            "properties": { "error": { "const": "stale-authority" } },
            "required": [
              "error"
            ]
          }
        ]
      }
    }
  }
}
