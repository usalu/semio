{
  "$schema": "http://json-schema.org/draft-07/schema#", "type": "object", "additionalProperties": false,
  "required": ["version", "maximum", "cases", "expected", "maximumStepBytes"],
  "properties": {
    "version": { "const": 1 }, "maximum": { "const": 9007199254740991 },
    "cases": { "type": "array", "minItems": 7, "maxItems": 7, "uniqueItems": true, "items": { "enum": ["capture-ids", "capture-order", "capture-shadowed-checker", "allocation-entry", "allocation-left", "allocation-right", "allocation-duplicate-owner"] } },
    "expected": { "type": "object", "additionalProperties": false, "required": ["rejected", "preserved"], "properties": { "rejected": { "const": true }, "preserved": { "const": true } } },
    "maximumStepBytes": { "const": 256 }
  }
}
