[package]
name = "semio-framework-ui-runtime"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Headless UI runtime: typed entities with mutation leasing, actual-read dependency tracking, and keyed reconciliation into transactional UiPatch revisions"

[package.metadata.semio]
role = "framework"
id = "ui-runtime"

[lints]
workspace = true

[lib]
path = "📦️glue.rs"

[dependencies]
# 🧬️ The builder-side tree is plain data; serializing it lets a guest ship a fixture or a whole tree
# without going through reconciliation, which the SDK's describe/testkit paths rely on.
serde = { version = "1.0.219", features = ["derive"] }
ui_contract = { path = "../../../🧬️contract/📦️packages/🦀️rust", package = "semio-framework-ui-contract" }

[dev-dependencies]
serde_json = { version = "1.0.140" }
