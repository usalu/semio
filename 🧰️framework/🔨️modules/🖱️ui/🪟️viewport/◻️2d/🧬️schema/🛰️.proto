syntax = "proto3";
package semio.framework.ui.viewport;

// 🪟️ Exact-window navigation pose; each scalar must be present and finite, and zoom positive.
message Viewport2d {
  optional double x = 1;
  optional double y = 2;
  optional double zoom = 3;
}
