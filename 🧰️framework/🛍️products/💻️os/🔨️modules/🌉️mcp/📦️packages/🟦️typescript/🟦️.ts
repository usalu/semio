/** 🌉️ Package entry — re-exports the module's TS surface from `../../🟦️.ts` (the same
 * `📦️packages/🟦️typescript/🟦️.ts` mount pattern every sibling `🔨️modules/*` TS package uses). */
export * from "../../🟦️.ts";
export * from "./🧬️schema-validation.ts";
