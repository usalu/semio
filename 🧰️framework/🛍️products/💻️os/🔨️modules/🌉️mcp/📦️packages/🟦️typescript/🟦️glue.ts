/** 🌉️ Package entry — re-exports the module's TS surface from `../../🟦️component.ts` (the same
 * `📦️packages/🟦️typescript/🟦️glue.ts` mount pattern every sibling `🔨️modules/*` TS package uses). */
export * from "../../🟦️component.ts";
export * from "./🧬️schema-validation.ts";
