{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://semio.tech/schema/directory/invite-capability-transfer.v1.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "capacity", "states", "transitions", "duplicateResult", "shellRoute", "disclosureAuthority", "focusPhases", "labels"],
  "properties": {
    "schema": { "const": "semio.directory.invite-capability-transfer.v1" },
    "capacity": { "const": 1 },
    "states": {
      "type": "array",
      "minItems": 5,
      "maxItems": 5,
      "prefixItems": [
        { "const": "available" },
        { "const": "copying" },
        { "const": "failed" },
        { "const": "copied" },
        { "const": "closed" }
      ],
      "items": false
    },
    "transitions": {
      "type": "array",
      "minItems": 10,
      "maxItems": 10,
      "items": { "$ref": "#/$defs/transition" }
    },
    "duplicateResult": { "const": "reject-without-disclosure" },
    "shellRoute": {
      "type": "object",
      "additionalProperties": false,
      "required": ["actionId", "spaceId", "operationEpoch", "openingPhase", "requestKind"],
      "properties": {
        "actionId": { "const": "os.directory.open-administration" },
        "spaceId": { "type": "string", "minLength": 1, "maxLength": 256 },
        "operationEpoch": { "type": "integer", "minimum": 0 },
        "openingPhase": { "const": "loading" },
        "requestKind": { "const": "directory-administration-open" }
      }
    },
    "disclosureAuthority": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "prefixItems": [{ "const": "author" }],
      "items": false
    },
    "focusPhases": {
      "type": "array",
      "minItems": 5,
      "maxItems": 5,
      "prefixItems": [
        { "const": "ready" },
        { "const": "denied" },
        { "const": "stale" },
        { "const": "failed" },
        { "const": "cancelled" }
      ],
      "items": false
    },
    "labels": {
      "type": "object",
      "additionalProperties": false,
      "required": ["en", "de"],
      "properties": {
        "en": { "$ref": "#/$defs/labels" },
        "de": { "$ref": "#/$defs/labels" }
      }
    }
  },
  "$defs": {
    "labels": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "items": { "type": "string", "minLength": 1, "maxLength": 160 }
    },
    "transition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "input", "to", "discloses", "erases"],
      "properties": {
        "from": { "enum": ["available", "copying", "failed"] },
        "input": { "enum": ["request", "failed", "copied", "close", "authority-lost"] },
        "to": { "enum": ["copying", "failed", "copied", "closed"] },
        "discloses": { "type": "boolean" },
        "erases": { "type": "boolean" }
      }
    }
  }
}
