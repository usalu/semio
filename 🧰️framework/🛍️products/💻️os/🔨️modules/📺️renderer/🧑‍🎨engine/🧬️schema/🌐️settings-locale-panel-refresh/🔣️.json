{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://semio.dev/schema/settings-locale-panel-refresh.json",
  "title": "Settings Locale Mounted Panel Refresh Contract",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contractVersion",
    "initialLocale",
    "nextLocale",
    "labels",
    "refreshScope",
    "settleRequired",
    "requiresGuestRefresh"
  ],
  "properties": {
    "contractVersion": { "const": 2 },
    "initialLocale": { "const": "en" },
    "nextLocale": { "const": "de" },
    "labels": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["key", "en", "de"],
        "properties": {
          "key": { "type": "string", "minLength": 1 },
          "en": { "type": "string", "minLength": 1 },
          "de": { "type": "string", "minLength": 1 }
        }
      }
    },
    "refreshScope": { "const": "full" },
    "settleRequired": { "const": true },
    "requiresGuestRefresh": { "const": true }
  }
}
