[package]
name = "semio-framework-os-kernel-dsl-fixture-sweep"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Repo-wide fixture-law sweep (parse-print-reparse fixpoint + canonicalize idempotence) over every real shipped .example DSL fixture, across every app that derives store::DocumentDsl — test-only, never a real dependency of anything."

[lints]
workspace = true
[lib]
path = "../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/🧪️fixture-sweep/⚡️implementations/🦀️rust/📦️lib.rs"

[dependencies]
fem = { path = "../../../../../../../../✏️s/🔌️plugins/🏗️fem/📦️packages/🦀️rust", package = "semio-s-plugin-fem" }

[dev-dependencies]
dsl = { path = "../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-dsl" }
store = { path = "../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🏪️store/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-store" }
# ✒️ Repointed to the merged writer plugin crate by the crate-consolidation taxonomy migration
# (ticket 26/08/05/WRITER-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION) — `WriterProjection`
# now lives at `writer::artifacts::writer::WriterProjection` (see this crate's `📦️lib.rs`).
writer = { path = "../../../../../../../../✏️s/🔌️plugins/✒️writer/📦️packages/🦀️rust", package = "semio-s-plugin-writer" }
# 🧮️ `MathProjection` and its `store::DocumentDsl` impl are genuinely plugin-owned (no shared kernel
# crate the way flow has), so they now live at `mathematical::artifacts::mathematical::MathProjection`
# (see this crate's `📦️lib.rs`) — the deleted `semio-s-app-mathematical` facade crate was merged into
# `semio-s-plugin-mathematical` by the crate-consolidation taxonomy migration (ticket
# 26/08/05/MATHEMATICAL-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
mathematical = { path = "../../../../../../../../✏️s/🔌️plugins/➗️mathematical/📦️packages/🦀️rust", package = "semio-s-plugin-mathematical" }
# 🌀️ `Procedural2dDocument`/`Procedural3dDocument` are genuinely plugin-owned (no shared kernel crate
# the way flow has), so they now live at `procedural::artifacts::procedural2d::Procedural2dDocument`/
# `procedural::artifacts::procedural3d::Procedural3dDocument` (see this crate's `📦️lib.rs`) — the
# deleted `semio-s-app-procedural-2d`/`semio-s-app-procedural-3d` facade crates were merged into
# `semio-s-plugin-procedural` by the crate-consolidation taxonomy migration (ticket
# 26/08/05/PROCEDURAL-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
procedural = { path = "../../../../../../../../✏️s/🔌️plugins/🌀️procedural/📦️packages/🦀️rust", package = "semio-s-plugin-procedural" }
# 🌊️ `FlowFixture` and its `store::DocumentDsl` impl have always lived in the flow kernel crate; the
# deleted `semio-s-app-flow` facade only re-exported them (it was merged into `semio-s-plugin-flow` by
# the W1 taxonomy pilot). Pointing at the real owner keeps this sweep independent of plugin packaging.
flow_app = { path = "../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🌊️flow/🫀️core/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-flow-core", default-features = false }
# 🌍️ `GisMapDocument`/`Gis3dTerrainDocument` are genuinely plugin-owned (no shared kernel crate the
# way flow has), so they now live at `gis::artifacts::gismap::GisMapDocument`/
# `gis::artifacts::gisterrain::Gis3dTerrainDocument` (see this crate's `📦️lib.rs`) — the deleted
# `semio-s-app-gis-2d`/`semio-s-app-gis-3d` facade crates were merged into `semio-s-plugin-gis` by the
# crate-consolidation taxonomy migration (ticket
# 26/08/05/GIS-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
gis = { path = "../../../../../../../../✏️s/🔌️plugins/🌍️gis/📦️packages/🦀️rust", package = "semio-s-plugin-gis" }
# 🌿️ `VcsDemoProjection` is genuinely plugin-owned (no shared kernel crate the way flow has), so it
# now lives at `vcs_app::artifacts::vcs::VcsDemoProjection` (see this crate's `📦️lib.rs`) — the deleted
# `semio-s-app-vcs` facade crate was merged into `semio-s-plugin-vcs` by the crate-consolidation
# taxonomy migration (ticket 26/08/05/VCS-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
vcs_app = { path = "../../../../../../../../✏️s/🔌️plugins/🌿️vcs/📦️packages/🦀️rust", package = "semio-s-plugin-vcs" }
# 🎞️ `PresentDeck` and its `store::DocumentDsl` impl were folded into the merged animate plugin crate by
# the crate-consolidation taxonomy migration (ticket
# 26/08/05/ANIMATE-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION) — the deleted `semio-s-app-animate-present`
# facade crate only ever re-exported it (see this crate's `📦️lib.rs`'s `use present::artifacts::present::PresentDeck;`).
present = { path = "../../../../../../../../✏️s/🔌️plugins/🎞️animate/📦️packages/🦀️rust", package = "semio-s-plugin-animate" }
# 🎥️ `ShootingFixture` and its `store::DocumentDsl` impl are genuinely plugin-owned (no shared kernel
# crate the way flow has), so they now live at `shooting::artifacts::shooting::ShootingFixture` (see this
# crate's `📦️lib.rs`) — the deleted `semio-s-app-shooting` facade crate was merged into
# `semio-s-plugin-shooting` by the crate-consolidation taxonomy migration (ticket
# 26/08/05/SHOOTING-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
shooting = { path = "../../../../../../../../✏️s/🔌️plugins/🎥️shooting/📦️packages/🦀️rust", package = "semio-s-plugin-shooting" }
# 🎬️ `SequenceFixture` and its `store::DocumentDsl` impl are genuinely plugin-owned (no shared kernel
# crate the way flow has), so they now live at `sequence::artifacts::sequence::SequenceFixture` (see
# this crate's `📦️lib.rs`) — the deleted `semio-s-app-sequence` facade crate was merged into
# `semio-s-plugin-sequence` by the crate-consolidation taxonomy migration (ticket
# 26/08/05/SEQUENCE-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
sequence = { path = "../../../../../../../../✏️s/🔌️plugins/🎬️sequence/📦️packages/🦀️rust", package = "semio-s-plugin-sequence" }

process_3d = { path = "../../../../../../../../✏️s/🔌️plugins/🏭️process/📦️packages/🦀️rust", package = "semio-s-plugin-process" }
# 🔷️ `LowpolyProjection` merged into `semio-s-plugin-lowpoly` by the crate-consolidation taxonomy
# migration (ticket 26/08/05/LOWPOLY-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION) — real owner
# is now `crate::artifacts::lowpoly::LowpolyProjection` (see this crate's `📦️lib.rs`).
lowpoly = { path = "../../../../../../../../✏️s/🔌️plugins/💠️lowpoly/📦️packages/🦀️rust", package = "semio-s-plugin-lowpoly" }
# 🧠️ `MindmapWiresDocument` merged into `semio-s-plugin-reasoning-mindmap` by the crate-consolidation
# taxonomy migration (ticket 26/08/05/REASONING-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION) —
# real owner is now `crate::artifacts::wires::MindmapWiresDocument` (see this crate's `📦️lib.rs`).
reasoning_mindmap_plugin = { path = "../../../../../../../../✏️s/🔌️plugins/💡️reasoning/📦️packages/🦀️rust", package = "semio-s-plugin-reasoning-mindmap" }
# 🌱️ `forms` dropped (26/08/05/FORMS-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION) — `FormSpec`
# repointed to `playbook::PlaybookSpec` directly in 📦️lib.rs, its real owner; `playbook` below already
# covers it.
# 📖️ `playbook` (the "playbook" plugin's dev-dep alias below) used to point at the now-deleted
# `semio-s-app-playbook` facade crate; `PlaybookSpec` was always a bare re-export of the shared
# `playbook` kernel crate (`semio-framework-os-kernel-playbook` — same domain `forms` builds on, see
# the comment above), so this repoints straight at the real owner — no `📦️lib.rs` ripple beyond this
# path/package swap (26/08/05/PLAYBOOK-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION, TEMPLATE
# §8.2).
# 📏️ `LayoutDocument` and its `store::DocumentDsl` impl are genuinely plugin-owned (no shared kernel
# crate the way flow has), so they now live at `layout::artifacts::layout::LayoutDocument` (see this
# crate's `📦️lib.rs`) — the deleted `semio-s-app-layout` facade crate was merged into
# `semio-s-plugin-layout` by the crate-consolidation taxonomy migration (ticket
# 26/08/05/LAYOUT-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
layout = { path = "../../../../../../../../✏️s/🔌️plugins/📏️layout/📦️packages/🦀️rust", package = "semio-s-plugin-layout" }
# 📐️ `CadScene` is genuinely plugin-owned (no shared kernel crate the way flow has), so it now lives at
# `cad_document::artifacts::cad::CadScene` (see this crate's `📦️lib.rs`) — the deleted `semio-s-app-cad`
# facade crate was merged into `semio-s-plugin-cad` by the crate-consolidation taxonomy migration
# (ticket 26/08/05/CAD-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
cad_document = { path = "../../../../../../../../✏️s/🔌️plugins/📐️cad/📦️packages/🦀️rust", package = "semio-s-plugin-cad" }
# 📏️ Every norm `Document` is genuinely plugin-owned (no shared kernel crate), so all fifteen now
# live at `norm::artifacts::<standard>::Document` (see this crate's `📦️lib.rs`) — the fifteen deleted
# `semio-s-app-norm-*` facade crates were merged into `semio-s-plugin-norm` by the crate-consolidation
# taxonomy migration (ticket 26/08/05/NORM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION).
norm = { path = "../../../../../../../../✏️s/🔌️plugins/📕️norm/📦️packages/🦀️rust", package = "semio-s-plugin-norm" }
playbook = { path = "../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/📖️playbook/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-playbook" }
imperative = { path = "../../../../../../../../✏️s/🔌️plugins/📜️imperative/📦️packages/🦀️rust", package = "semio-s-plugin-imperative" }
remodel = { path = "../../../../../../../../✏️s/🔌️plugins/📸️remodel/📦️packages/🦀️rust", package = "semio-s-plugin-remodel" }
trinity = { path = "../../../../../../../../✏️s/🔌️plugins/🔱️trinity/📦️packages/🦀️rust", package = "semio-s-plugin-trinity" }
dag_app = { path = "../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/♾️infinite/🎲️board/🔌️ports/➡️directed/🕸️dag/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-infinite-board-port-directed-dag" }
draw = { path = "../../../../../../../../✏️s/🔌️plugins/🖍️draw/📦️packages/🦀️rust", package = "semio-s-plugin-draw" }
raster = { path = "../../../../../../../../✏️s/🔌️plugins/🖨️raster/📦️packages/🦀️rust", package = "semio-s-plugin-raster" }
note_app = { path = "../../../../../../../../✏️s/🔌️plugins/🗒️note/📦️packages/🦀️rust", package = "semio-s-plugin-note" }
puzzle = { path = "../../../../../../../../✏️s/🔌️plugins/🧩️puzzle/📦️packages/🦀️rust", package = "semio-s-plugin-puzzle" }
block = { path = "../../../../../../../../✏️s/🔌️plugins/🧱️block/📦️packages/🦀️rust", package = "semio-s-plugin-block" }
home = { path = "../../../../../../../../✏️s/🔌️plugins/🪐️space/📦️packages/🦀️rust", package = "semio-s-plugin-space" }
semio_framework_os = { path = "../../../../../../../../🧰️framework/🛍️products/💻️os/⚡️implementations/🦀️rust", package = "semio-framework-os" }
sourcing = { path = "../../../../../../../../✏️s/🔌️plugins/🪵️sourcing/📦️packages/🦀️rust", package = "semio-s-plugin-sourcing" }
space = { path = "../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🪐️space/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-space" }
