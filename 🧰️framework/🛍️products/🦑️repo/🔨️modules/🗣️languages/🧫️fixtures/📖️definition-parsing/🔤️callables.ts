export const answer = 42;
export const shout = (word: string) => word.toUpperCase();
export const later = async function () {
  return 1;
};
export const Widget = class {
  size = 1;
};
export let counter = 0;
export function plain() {
  return 0;
}
