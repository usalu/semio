[package]
name = "semio-framework-repo-mcp"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "semio repo Model Context Protocol: protocol types, session, stdio transport, server, hash-chained event log and per-client profiles"

[package.metadata.semio]
role = "library"

[lints]
workspace = true

[lib]
path = "🦀️.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
