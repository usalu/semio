# 📡️ Events

The event vocabulary and its payload encoding, the envelope emitted to the coordinator, the
append-only JSONL event store and its sequence rules, and the content-hashed event export.

## 📦️ Packages

- `📦️packages/🦀️rust` — `semio-framework-repo-events`
- `📦️packages/🐹️go` — `github.com/usalu/semio/repo/events`

## 🧪️ Tests

One `🥒️.feature` per case under `🧪️tests/` with an adapter per implementation, run through the
`🧪️test` harness; the third-party references are registered in `🔮️oracles/🔣️.json`.

`📇️event-kind-catalog`, `📦️payload-encoding`, `🗃️store-append-sequence`, `🔏️export-content-hash`.
