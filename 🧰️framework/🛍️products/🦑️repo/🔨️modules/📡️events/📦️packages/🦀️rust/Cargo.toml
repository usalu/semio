[package]
name = "semio-framework-repo-events"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "repo events: kinds, envelope, payloads, coordinator emit and the append-only event store"

[package.metadata.semio]
role = "library"

[lints]
workspace = true

[lib]
path = "🦀️.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
