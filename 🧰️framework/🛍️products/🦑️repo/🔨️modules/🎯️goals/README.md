# 🎯️ Goals

The goal lifecycle — list, read, open, close, reopen, change — the goal document codec, the
owner-scoped goal identifier, and the goal tree the CLI renders.

## 📦️ Packages

- `📦️packages/🦀️rust` — `semio-framework-repo-goals`
- `📦️packages/🐹️go` — `github.com/usalu/semio/repo/goals`

## 🧪️ Tests

One `🥒️.feature` per case under `🧪️tests/` with an adapter per implementation, run through the
`🧪️test` harness; the third-party references are registered in `🔮️oracles/🔣️.json`.

`🪪️goal-id-scheme`, `🎯️goal-document-codec`, `🔓️goal-lifecycle`, `🌳️goal-tree-rendering`.
