---
goal: AI-OPTIMIZED-REPO/REPO-CLIENT/REPO-BINARY/REPO-MECHANISMS/REPO-POLICY-MECHANISM
---

# Ticket

## Summary

Added hierarchical `policy tree` command to CLI, MCP tool `policy_tree`, and VS Code extension command `compose.policyTree`. The CLI `policy tree` renders policies with statutes as nested children in both markdown and text formats. The VS Code extension sidebar already rendered statutes hierarchically under policies. Added `EmojiBreach` constant to fix a pre-existing build error. Added test `TestPolicyTreeCommand`. Updated AGENTS.md SRS and README.md documentation.

## Changes

## Log

## Todos

## Plan
