{
  "title": "Corner Panels and Composable Tab Tree Dock",
  "emoji": "🐦‍⬛",
  "status": "closed",
  "description": "Pivot from the two side panels to four corner panels (top-left/top-right/bottom-left/bottom-right) growing from each display corner. Footer returns to a navbar-symmetric normal-flow bar carrying only bottom-corner toggles; footer's former tool ribbon (sync attach, actions/history tools) moves into the bottom corner panels as default tabs. Introduce a composable tab/tree dock: PanelTabLeaf.tree becomes trees: PanelTreeUnit[]; tabs are draggable/dockable between all four corners with arbitrary re-nesting (dragging a tab carries its subtree); tabs are either branches (child tabs) or leaves (tree units rendered as sections); tree units are draggable between leaf tabs. Rust PanelGroup::side() renamed to corner() with 4-way mapping (Workbench TL, Details TR, Display BL, Settings BR); wgpu renderer gets a local group_side() folding corner back to left/right (only 4 call sites repo-wide use .side()). New DockLayoutStore (framework/core/js) persists only the diff from computed defaults, layered os-level + app-level (more specific wins), backed by a StoragePort (localStorage in browser, ~/.semio for native), with a reset mechanism. Full rename SidePanel-\u003eCornerPanel across ui/js/react (chrome, hotkeys, i18n, data-slots), os-shell.tsx reducer/default-dock-assembly generalization, new PanelDock drag-and-drop region (pointer-capture for tabs mirroring the existing ModeDock pattern, native HTML5 DnD for tree units), and periphery updates (Storybook stories, asset icon exports, coda/compose sketchpad textual renames — those two consumers currently can't typecheck due to a dangling @semio-tech/framework-platform-core workspace symlink, so their renames are best-effort). Full plan at /Users/ueli/.claude/plans/ui-the-design-language-quirky-sifakis.md.",
  "summary": "Fixed a bug reported after the corner-panels/composable-dock rollout: the bottom-left and bottom-right corner panel toggles were invisible because the footer (`[data-slot=\"footer\"]`) kept the `z-navbar` utility's z-index of 100, while `CornerPanel` defaults to z-index 20 — the footer painted over the bottom corner panels' chrome. The navbar had already been given a stacking-context override (`position/z-index forced to var(--z-base) !important`) earlier in this project to fix the analogous top-corner case; the footer never got the same treatment since it predates the corner-panel/footer-symmetry pivot. Extended the existing override selector in ui/styling/js/ui.css to include `[data-slot=\"footer\"]`, so both navbar and footer are pinned to the base stacking context and all four corner panels' toggle chrome renders above them, matching the design intent (\"footer carries only bottom-corner toggles, panels overlay it\"). Verified live in note-react-dev: all four toggles (top-left/top-right/bottom-left/bottom-right) render correctly positioned and unobscured; ui-react vitest suite shows only the pre-existing 11 baseline failures (tree helpers, CanvasPickMenu, Shell*Panel/Dialog — concurrent-dev churn, unrelated).",
  "github": {
    "issue": "https://github.com/usalu/semio/issues/1968"
  },
  "goal": "🎯r2602🎯runningsketchpad",
  "sessions": ["⚪ee1a6033fa1e44758ce5451c89e36496", "⚪58dd30a7c65a49a9b46f5a3c4621d675"]
}
