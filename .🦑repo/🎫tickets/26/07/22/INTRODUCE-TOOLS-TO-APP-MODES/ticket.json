{
  "title": "Introduce Tools to App Modes",
  "emoji": "🛠️",
  "status": "closed",
  "description": "Introduce Tools as a first-class mode-scoped manifest concept, distinct from window-scoped Utilities. A Tool is an activatable, stateful capability of a whole app mode (not a window) — rendered as a Tool panel toggle placed left of Commands in the middle of the footer. Migrate puzzle fill (3d and 2d) from a per-window utility to a mode-level tool, since fill is a document-wide generator (count slider, edit-target mode, voxel dims), not a per-window pointer mode. Active tool and active window utility are mutually exclusive (single interaction owner). wgpu renderer gets compile parity only in this ticket; full wgpu tool UI is a follow-up. Delete the dead ModeDefinition.utilities/.mode_utilities() (zero non-test consumers) in favor of ModeDefinition.tools/.mode_tools(). Full plan at /Users/ueli/.claude/plans/introduce-tools-to-app-atomic-shell.md.",
  "summary": "Implemented the approved plan end to end (reconciled with a concurrent session that had independently done overlapping utilities→tools plumbing and closed this same ticket first — its changes and mine target the same design and are consistent). Tool is now a first-class, mode-scoped manifest concept (windowless, activatable), distinct from the per-window Utility. Migrated puzzle fill (2d and 3d) from a window utility to a mode tool. Rendered a Tool panel toggle to the left of Commands in the footer's bottom-middle anchor.\n\nRust:\n- framework/core/rs/lib.rs: ToolDefinition/ToolRef (#region 🔖Tools), ModeDefinition.tools (replaces dead ModeDefinition.utilities/mode_utilities), AppDefinition.tools, ViewState.active_tool_id, SET_ACTIVE_TOOL_ACTION_ID + set_active_tool_action_definition, HostEffect::SetActiveTool, UiDirtyScope::Partial.tools, resolve_mode_tools + unit tests, typegen exports. Also fixed a pre-existing unrelated bug blocking the typegen build (IntroductionStepDefinition.cutouts had an invalid ts(optional) attribute on a Vec field).\n- framework/plugin/rs/lib.rs: AppBuilder.tool()/.tool_simple()/.mode_tools(), ModeSpec.tools, build_definition validation (unique ids, ref resolution, orphan-tool rejection, auto-injected action + keybindings), DocumentApp/PluginApp/VcsDocumentApp::tool_measures, plugin_refresh_ui \"tools\" section, 5 new tests.\n- framework/renderer/wgpu/rs/lib.rs: compile-parity only (per plan) — ModeDefinition/AppDefinition test literals updated for the tools field; wgpu has no tool UI yet (follow-up ticket).\n- framework/product/os/core/rs/lib.rs, s/plugin/rs/lib.rs: ModeDefinition literals updated (utilities → tools field); no other apps used mode-level utilities.\n- puzzle/plugin/rs/lib.rs: migrated fill in both puzzle2d and puzzle3d from a window utility to a mode tool — removed from .utility()/.window_kind_utilities(), added .tool_simple(\"fill\",...) + .mode_tools(\"edit\", [\"fill\"]); activation now sourced from ViewState.active_tool_id (a puzzle3d_scene_active_utility helper keeps the existing scene-mode/engagement-session/viewport-interaction logic unchanged by feeding it \"fill\" when the tool is active); fill's measures (count slider, edit-target-volumes picker, voxel dims) moved into tool_measures, untagged; updated/added tests in both apps. Fixed one unrelated pre-existing compile bug in a newly-added window-instance test (app.render(...) returns UiNode, needed serde_json::to_value before .pointer()).\n\nTypeScript:\n- framework/core/js/index.ts: ToolDefinition/ToolRef mirror types, SET_ACTIVE_TOOL_ACTION_ID, PluginViewState.activeToolId, refresh request/response \"tools\" section, HostEffect.setActiveTool, UiDirtyScope.tools, resolveModeTools (twin of the Rust fn). Regenerated framework/core/js/generated/manifest.ts (gitignored).\n- framework/renderer/react/index.tsx: ActionPaneState.activeToolId + SET_ACTIVE_TOOL action/reducer, WindowUiState.toolMeasuresByToolId + SET_TOOL_MEASURES_BY_TOOL_ID, UiRefresh \"tools\" section wiring, injectActiveTool/injectActiveUtility composition, onAction interceptors for setActiveTool/setActiveUtility enforcing mutual exclusion, applyHostEffects setActiveTool handling (also mutually exclusive), applyModeChange resets the active tool, Escape clears the tool when no utility is active, new #region 🛠️ToolRegistry (buildToolTree/buildToolTabs reusing renderWindowMeasure), FRAMEWORK_CATEGORY_TOOL_ID branch at order 0 (left of Command at order 1) in defaultDock's bottom-middle anchor. Renamed gumballToolForTransformMode → gumballKindForTransformMode and one \"push/pull tool\" comment → \"utility\".\n- framework/renderer/react/index.test.ts: resolveModeTools/buildToolTabs describe block, SET_ACTIVE_TOOL reducer test, tools-section coverage in the batched-refresh describe block.\n- ui/js/react/index.tsx: ui.panelToggle.tool label (type + German \"Werkzeug\" + English \"Tool\").\n\nVerification:\n- cargo test (isolated CARGO_TARGET_DIR to dodge shared-lock contention from ~16 concurrent unrelated cargo processes this session): semio-framework-core 53/53, semio-framework-plugin 91/91, puzzle-plugin 84/84 — all green.\n- bun nx run @semio-tech/framework-core:generate — regenerated cleanly with ToolDefinition/ToolRef/tools/activeToolId present.\n- framework-renderer-react vitest: 221/221 passing (209 pre-existing + 12 new).\n- Targeted tsc check on touched files: clean (full-workspace tsc has ~1759 pre-existing unrelated errors across other packages, not this repo's real verification path).\n- wgpu compile-parity: confirmed once with 0 errors (isolated target dir); a later recheck hit an unrelated, still-in-progress concurrent breakage in infinite_world/kernel_3d_scene (Vec3 missing Mul/length_squared) — confirmed via git status that infinite/world/rs/lib.rs is separately modified by another session, not touched by this ticket.\n- Live browser verification (puzzle-3d-react-dev) could not complete this session: the dev server sat at \"Blocking waiting for file lock on build directory\" the whole session because of heavy concurrent cargo activity from other sessions sharing this repo's target directory. Left the preview server running on port 6013 for a later check.\n\nDeferred (flagged, not done, low risk):\n- Moving fill_count/fill_edit_target_volumes/voxel_dims off Puzzle3dWindowOptions onto pure mode/runtime-level fields (the plan's \"state altitude\" cleanup) — deliberately deferred because that struct is under active concurrent edit by the PER-WINDOW-LOCAL-OPTIONS ticket.\n- Full wgpu tool UI (activation + options rendering) — explicitly out of scope per the approved plan.",
  "github": {
    "issue": "https://github.com/usalu/semio/issues/2220"
  },
  "goal": "🎯r2602",
  "sessions": [
    "⚪f887d665ebf749f98971586146bff1dd",
    "⚪69814de2319e4d18968bd32980c0cd11"
  ]
}