{
  "title": "Universal Command History Across Every Interaction",
  "emoji": "🕒",
  "status": "open",
  "description": "Extend the framework command-history panel (built in a prior ticket) so it captures every interaction, not just VCS-edit-producing operations. Reported bug: in the aggregator app (puzzle plugin, app puzzle3d-play), zooming/panning/dragging leaves the history empty. Root causes verified: (1) zoom/pan/orbit camera lives in ephemeral React/wgpu state and never reaches the plugin at all (the one escaping signal, noteWorldNavigation, is explicitly swallowed by the shell); (2) View/Shell-kind actions that DO reach the plugin (setCamera on 2D hosts, setActiveUtility, etc.) are dropped from the log because dispatch_emit early-returns before logging when no VCS operations are produced; (3) drag-drop actions already log but can render stale because narrow UiDirtyScope::Partial scopes never include the history panel body key.\n\nApproved design (see full plan at /Users/ueli/.claude/plans/every-app-must-show-mighty-pearl.md): (D1) log every command that reaches the plugin, View and Shell kinds included, with edit_id: None; (D2) unify camera to View-kind, session-only, everywhere — move camera storage out of document projections in puzzle 2d/3d/5d, raster, trinity, etc., so undo never reverts camera and it stops being a VCS edit; (D3) add a new framework-injected noteShellCommand {commandId, label, detail?} action so shell chrome commands (dock drag, window resize/close, theme/locale changes) get logged too; (D4) consecutive same-id View/Shell log rows fold into one entry with a ×count instead of flooding the panel; (D5) trinity's GraphFixture.camera stays as a seed-only DSL field (consumed once on load, never written back).\n\nExecuted via a multi-agent Workflow (four phases): WF-A core mechanism (framework core injected action + plugin crate recording tail, sequential since plugin depends on core's new constants), WF-B app-crate camera-out-of-document sweep (parallel fan-out across ~14 disjoint app crates: puzzle 2d/3d/5d, raster, trinity jack/rewrite/ram, architect, cad, fem 2d/3d, shooting, writer, sequence, mathematical, lowpoly), WF-C renderer routing (React + native wgpu renderer, parallel — camera dispatch wiring, noteShellCommand dispatch seams), WF-D integration verify + aggregator end-to-end browser check. Hard cutover per repo convention — no compatibility shims, no migrations; camera ops are deleted from affected op crates and any fixtures hand-fixed.",
  "github": {
    "issue": "https://github.com/usalu/semio/issues/2386"
  },
  "goal": "🎯r2602🎯runningsketchpad🎯apps",
  "sessions": [
    "⚪49de5c21fcfe4e69b53523efb493cc99"
  ]
}