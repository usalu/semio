#!/usr/bin/env bun
/** 🧩️ Archived ticket modules only — greenfield repo has no `migrate neo4j` runner. Use `bun ./📜️script.ts generate neo4j` from the monorepo root. */
console.error("Neo4j migration runners were removed (greenfield). Historical `.ts` / `.cypher` files remain in this ticket folder for reference only.");
process.exit(1);
