WAVE 2 — family: raster
================================================================================
Touched only: raster/plugin/rs/lib.rs (no other files under raster/** needed changes;
raster/example/semio.raster was already DSL text, left as-is per family note).

1. DOCUMENT KINDS COVERED
--------------------------------------------------------------------------------
- App type: RasterPlayApp (raster/plugin/rs/lib.rs)
  Projection: crate::domain::RasterProjection (aliased RasterDocument)
  Schema string used: RASTER_DOCUMENT_SCHEMA = "raster.document" (== RasterPlayApp::document_schema()'s
    real return value, confirmed by reading the DocumentApp impl).
  Codec registered via:
    semio_framework_plugin::plugin_runtime::register_document_codec_for_app::<RasterPlayApp>(RASTER_DOCUMENT_SCHEMA)
  in register_raster_exports() (raster/plugin/rs/lib.rs, //#region 🔖️Manifest), the crate's existing
  native setup fn, already wired as `setup:` in the `semio_framework_plugin::semio_plugin!` macro
  invocation -- exact same pattern as note/plugin, animate/plugin, imperative/plugin, etc.

2. PACK LAW TEST CALLS ADDED
--------------------------------------------------------------------------------
- tests::raster_dsl_round_trips_representative_document: added
  vcs::test_support::assert_dsl_pack_equivalence(&representative_raster_document()) beside the existing
  assert_dsl_round_trip call.
- tests::raster_dsl_round_trips_semio_example_document: added
  vcs::test_support::assert_dsl_pack_equivalence(&semio_example_document()) beside the existing
  assert_dsl_round_trip call.
- tests::raster_document_text_round_trips_store_with_applied_operation: added
  vcs::test_support::assert_document_pack_round_trip(&store) beside the existing
  assert_document_text_round_trip(&store) call.
All three reuse the real existing fixtures (no new fixtures constructed).

3. FIXTURES CONVERTED
--------------------------------------------------------------------------------
None. raster/example/semio.raster was already handcrafted DSL text (per family note "stays as-is"); no
JSON fixture existed to convert.

4. PACK-COMPLETENESS LINT ALLOWLIST ENTRY NOW SATISFIED
--------------------------------------------------------------------------------
raster/plugin/rs/lib.rs -- now calls both assert_dsl_pack_equivalence(...) and
assert_document_pack_round_trip(...) beside every pre-existing assert_dsl_round_trip(...) /
assert_document_text_round_trip(...) call site. Wave 3 can remove this one line from root script.ts's
POLICY_PACK_COMPLETENESS_ALLOWLIST.

5. CARGO TEST STATUS
--------------------------------------------------------------------------------
`cargo test -p raster-plugin`: 22 passed, 7 failed (29 total). All 3 new/edited pack-law tests pass:
  raster_dsl_round_trips_representative_document ... ok
  raster_dsl_round_trips_semio_example_document ... ok
  raster_document_text_round_trips_store_with_applied_operation ... ok

The 7 failures are PRE-EXISTING and OUT OF SCOPE for this ticket -- unrelated to the pack layer, caused
by a missing icon-catalog entry outside raster/**:
  panicked at ui/wgpu/rs/../../../ui/asset/icon/generated/icon_name.rs:1380:31: invalid catalog icon name
Root cause confirmed: IconName::from(&str) panics because the generated catalog
(ui/asset/icon/generated/icon_name.rs) is missing entries for icon ids raster/plugin/rs/lib.rs already
used before this wave ("scan", "sliders-horizontal", "folder-plus", "brush" -- confirmed absent via grep;
"eraser"/"image"/"folder"/"square-dashed" ARE present). `git status` shows ui/wgpu/rs/lib.rs as modified
(uncommitted, another session's in-flight work per the new UNIFIED-6-LEVEL-UI-SURFACE-SYSTEM ticket seen
in .repo/), i.e. concurrent icon-catalog churn, not something introduced by or fixable within this
raster/** -only ticket. Failing tests (all pre-existing behavior, unrelated to any code I touched):
  __semio_plugin_sanity_declared_apps_appear_in_bundle_manifest
  tests::raster_labels_resolve_native_english_by_default
  tests::raster_labels_resolve_german_locale
  tests::renders_layers_tree
  tests::set_active_utility_switch_emits_no_ops_and_reads_from_view_state
  tests::set_hover_highlights_layer_row_via_runtime
  tests::utility_registry_declares_utilities_scoped_to_the_composite_window
