WAVE 2 FAMILY REPORT — test-only-docs (vcs/plugin/rs, dsl/rs)
================================================================================

FILES TOUCHED
--------------------------------------------------------------------------------
vcs/plugin/rs/lib.rs
dsl/rs/lib.rs

DOCUMENT KINDS COVERED
--------------------------------------------------------------------------------
1. VcsDemoProjection (App: VcsPlayApp, vcs/plugin/rs/lib.rs)
   - Schema string used: VCS_DEMO_SCHEMA = "vcs.demo" (the real value returned by
     VcsPlayApp::document_schema()).
   - Registered via register_document_codec_for_app::<VcsPlayApp>(VCS_DEMO_SCHEMA)
     inside the existing register_vcs_exports() native setup fn (called from
     semio_plugin!'s `setup:` slot).
   - Pack-law test: added vcs::test_support::assert_dsl_pack_equivalence(&empty_vcs_demo_projection())
     next to the existing vcs::test_support::assert_dsl_round_trip(...) call in
     tests::vcs_demo_projection_dsl_round_trips (the `//#region 🔖️DslAndOpText` submodule).

2-8. dsl/rs/lib.rs's in-crate test-only document types (no DocumentApp / no plugin —
   pure facade-exercise fixtures, so no codec registration applies):
   - DerivedDocument   -> added assert_dsl_pack_equivalence next to the existing
     vcs::test_support::assert_dsl_round_trip call in
     tests::derived_document_dsl_satisfies_vcs_test_support_helpers.
   - SceneDocument     -> added assert_dsl_pack_equivalence in
     tests::derived_enum_recursive_block_tree_and_map_and_nested_collections_round_trip.
   - ShapeDocument     -> added assert_dsl_pack_equivalence in
     tests::derived_newtype_tuple_variants_round_trip.
   - PaintDocument     -> added assert_dsl_pack_equivalence for both the Some(..) and
     None fixtures in tests::derived_option_statements_field_round_trips_present_and_absent.
   - BrushDocument     -> added assert_dsl_pack_equivalence for both the Some(..) and
     None fixtures in tests::derived_option_block_record_field_omits_rather_than_printing_empty_braces_when_absent.
   - SelfRefDocument   -> added assert_dsl_pack_equivalence in
     tests::derived_self_referential_record_struct_round_trips_nested_values.
   - TableDocument     -> added assert_dsl_pack_equivalence in
     tests::derived_table_field_prints_compact_soa_and_round_trips.

All fixture values reused verbatim from the tests they were added to; no new fixtures
invented.

FIXTURES CONVERTED
--------------------------------------------------------------------------------
None. Family note confirmed: test-only, in-code fixtures, nothing to convert.

CARGO TEST STATUS
--------------------------------------------------------------------------------
`cargo test -p dsl -p vcs-plugin`: GREEN.
  dsl:        15 passed, 0 failed, 0 ignored.
  vcs-plugin: 15 passed, 0 failed, 0 ignored.
Doc-tests for both crates: 0 tests (expected), ok.

PACK-COMPLETENESS LINT ALLOWLIST — ENTRIES NOW SATISFIED
--------------------------------------------------------------------------------
Both of this family's two allowlisted files now carry a pack-law assertion (assert_dsl_pack_equivalence)
alongside every pre-existing DSL round-trip assertion for every document kind they declare. Wave 3 can
remove these two entries from root script.ts's POLICY_PACK_COMPLETENESS_ALLOWLIST:
  dsl/rs/lib.rs
  vcs/plugin/rs/lib.rs

NOT COMPLETED / DEVIATIONS
--------------------------------------------------------------------------------
None. Scope was fully covered: one codec registration (VcsPlayApp), pack-law calls added for
every #[derive(dsl::DslDocument)] type in both files, no fixtures to convert, both packages green.
Root script.ts (the allowlist itself) was intentionally NOT touched per this family's file-scope
restriction — that edit is reserved for wave 3.
