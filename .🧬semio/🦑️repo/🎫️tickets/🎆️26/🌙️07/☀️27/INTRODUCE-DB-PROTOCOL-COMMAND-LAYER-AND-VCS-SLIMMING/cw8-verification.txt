CW8 verification scratch log
=============================

1) Fresh repo-wide grep for real (non-comment, non-worktree, non-.repo-backup) vcs::<shim-name>
   consumers, AFTER all edits (multi-line "use vcs::{...}" aware, Python-based scan over every
   *.rs file excluding target/, .repo/, .claude/worktrees/):

   TOTAL: 0

2) Extra real shim consumers found beyond the compose file the instructions flagged (all pure
   renames, all verified via `cargo check -p <crate> --tests` afterwards):
   - framework/sync/rs/lib.rs:1518 (test module) — `use vcs::{create_document_vcs_envelope,
     operation_envelope_from_edit, Edit, Operation, OperationDiff};` -> split, Edit/Operation/
     OperationDiff now from protocol::, create_document_vcs_envelope/operation_envelope_from_edit
     stay from vcs:: (operation_envelope_from_edit is vcs's OWN permanent function per the shim's
     own "NOT shimmed" doc comment, unrelated to the deleted shim).
   - framework/plugin/rs/lib.rs:2090 and :4738 (two test submodules) — `use vcs::{Operation,
     OperationDiff};` / `use vcs::{Backbone, BackboneMessage, MemoryBackbone, Operation,
     OperationDiff};` -> split, Operation/OperationDiff now from protocol::.
   - framework/plugin/rs/lib.rs:216-219 (top-level, NOT test-only) — multi-line `use vcs::{
     build_history_columns, create_document_vcs_envelope, DocumentVcsCommand, DocumentVcsEnvelope,
     DocumentVcsStore, HistoryColumn, OpText, StudioConflict, };` -> OpText now imported from
     protocol:: via a new top-level `use protocol::OpText;`. This one was missed by a naive
     single-line grep the first pass around (a "use vcs::{" ... "}" block spanning multiple
     lines) — caught only by a regex/script scan that reads whole-file content instead of
     line-by-line grep. Real production usage at line 3547: `.map(OpText::print_op)`.
   - framework/product/os/core/rs/lib.rs:776,780 — `Vec<vcs::ReconcileReport>` / `vcs::ReconcileReport
     { ... severity: vcs::ReconcileSeverity::Warning }` -> both now protocol::.
   - dsl/rs/lib.rs:441,443,654,656 (test module) — `<DerivedOperation as vcs::OpText>::...` /
     `<PaintOp as vcs::OpText>::...` -> protocol::OpText. This crate's dsl_derive-generated
     `impl ::protocol::OpText for DerivedOperation` (dsl/derive/rs/lib.rs:737 already emits
     `::protocol::OpText`, confirmed pre-existing from an earlier wave) meant dsl/rs ALREADY
     needed a `protocol` dependency to even compile its own #[derive(dsl::DslOps)] test fixtures —
     it was missing one entirely (pre-existing gap, unrelated to the shim itself, but blocking
     `cargo check -p dsl --tests` before this fix: error[E0433] cannot find `protocol` in the crate
     root). Added `protocol = { path = "../../protocol/rs" }` to dsl/rs/Cargo.toml
     [dev-dependencies] (no cycle: protocol_command/protocol_history depend only on dsl_core, not
     on dsl or dsl_derive).

3) compose/client/lib/rs/lib.rs — confirmed the instructions' description (two `impl vcs::OpText`
   sites) undercounted by one related site and two import lines:
   - line 7816: `use vcs::{..., Operation as VcsOperation, OperationDiff};` -> split, Operation/
     OperationDiff now protocol::.
   - line 7908: `impl vcs::OpText for ComposeWireOperation` -> `impl protocol::OpText for ...`.
   - line 7919: `impl VcsOperation<KitSnapshot> for ComposeWireOperation` — VcsOperation is just the
     import-time alias for protocol::Operation now; no textual change needed at this line itself.
   - line 10858-10864: `impl vcs::OpText for Operation` + two `<ComposeOperationDsl as
     vcs::OpText>::...` calls -> protocol::OpText throughout.
   - line 19533 (test module): `use vcs::{..., Operation, OperationDiff};` -> split.
   - `crate::vcs::Edit`/`crate::vcs::Graph` references (lines 1664, 1666, 9064, 13694, 13740,
     14899, 17091) are COMPOSE'S OWN internal `pub mod vcs { ... }` GraphQL entity module (a
     same-named sibling, not the external crate) — left untouched, confirmed by reading the module
     boundary (`pub mod vcs {` at line 7180) and the crate's own Cargo.toml aliasing nothing.

4) vcs/rs/lib.rs shim deletion: the literal `pub use protocol::{merge_concurrent_diffs, Edit,
   Operation, OperationDiff, OperationMeta, OpText, ReconcileReport, ReconcileSeverity};` (with its
   `//#region 🚧️TEMPORARY protocol shim` / `//#endregion` wrapper) is gone. Converted to a PRIVATE
   `use protocol::{Edit, Operation, OperationDiff, OperationMeta, OpText, ReconcileReport};` (no
   `pub`, and `merge_concurrent_diffs`/`ReconcileSeverity` dropped — neither is referenced bare
   anywhere inside vcs/rs/lib.rs itself, confirmed by cargo's unused-import warning going away once
   trimmed). Rationale: vcs/rs/lib.rs's OWN body still spells ~150 bare references to `Operation`/
   `OpText`/`Edit`/`OperationMeta` throughout (generic bounds like `Operation: OpText`,
   `Edit<Operation>` struct literals, `crate::Operation<P>` disambiguating the trait from the
   same-named generic parameter) — a private import keeps that internal ergonomics working without
   re-exposing the names on vcs's own public API, which is the actual "shim" being removed (external
   crates could resolve `vcs::Operation` before; none can now).

5) cargo build --workspace: 0 errors (full log: cw8-full-build.txt). Dead-code sweep: grepped the
   full build log for "never used"/"never read"/"never constructed" warnings whose file path is
   under vcs/ or protocol/ — the only hit is `protocol/cli/rs/lib.rs`'s `main` function, which is
   the SAME pre-existing dual bin/lib-target artifact pack_cli and db_cli both also warn about
   ("file found to be present in multiple build targets") — not migration-artifact dead code, so
   left alone. No other CW3-CW7-migration-surface dead code found.

6) cargo test:
   - vcs: 81 passed, 0 failed
   - compose-hub (--manifest-path compose/server/hub/rs/Cargo.toml, its own detached workspace):
     91 passed, 0 failed
   - compose (root workspace member): 59 passed, 12 failed, 1 ignored. `git diff --stat
     compose/client/lib/rs/lib.rs` shows ONLY this session's 4 rename edits (8 insertions/6
     deletions across 14 lines) uncommitted in that file — everything else is exactly HEAD's
     committed content. The 12 failing tests (kit GraphQL mutation/traversal/virtual-file-system
     tests around lines 19951-21367) are nowhere near the three touched regions (~7813-7920,
     ~10855-10867, ~19531-19538) and exercise unrelated kit-graph/GraphQL domain logic that never
     references OpText/Operation. A pure rename of which module path names an already-identical
     trait cannot change runtime behavior. Conclusion: pre-existing failures from concurrent
     compose kit-graph development, not caused by this ticket.
   - dsl: 15 passed, 0 failed
   - semio-framework-sync: 14 passed, 0 failed
   - semio-framework-plugin: 100 passed, 0 failed (1 ignored doctest, pre-existing)
   - semio-framework-os: 55 passed, 0 failed

7) Policy lints — verified by importing script.ts's exported `policy` linter function directly in
   an ad hoc bun script (same technique a prior ticket in this campaign,
   26/07/26/DSL-MIGRATION-LOCK-STEP-..., used for the same reason: `bun ./script.ts lint` runs a
   heavy repo-wide nx ESLint + dependency-cruiser pipeline unrelated to these Rust-facing policy
   checks; `policy` is a plain exported function, so calling it directly is the exact code path
   `bun ./script.ts policy`/the nx lint target would run):
   - policyProtocolMigrationBreaches: 0 breaches (after fixing a false-positive on compose's own
     `crate::vcs::Edit` internal-module references by excluding "crate::vcs::" matches).
   - policyCommandEnvelopeCompletenessBreaches: 0 breaches (55-file seeded allowlist covers every
     current gap; vcs/rs/lib.rs itself is NOT in the allowlist since it already proves the
     mechanism, matching POLICY_PACK_COMPLETENESS_ALLOWLIST's own precedent).
   - policyDbServerOnlyBreaches: 0 breaches (current db/db_* dependents are exactly db/*,
     framework/product/os/hub/rs, and compose/server/hub/rs — all inside the allowed prefixes).
   - Other pre-existing policy kinds (app-plugin/*, dsl-migration/completeness) unchanged at 43
     total breaches, confirming nothing else regressed.
