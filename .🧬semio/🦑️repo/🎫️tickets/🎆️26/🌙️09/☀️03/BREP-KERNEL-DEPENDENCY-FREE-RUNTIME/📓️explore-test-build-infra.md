# 🔍️ Explore — test & build infrastructure

Read-only audit (Haiku explorer, 2026-09-03).

## Packages
- stdio rust: `✏️s/🔌️plugins/🗄️stdio/📦️packages/🦀️rust/` — `📜️script.ts` (`test [quick|long|exhaustive]` → `runCatalogRootContractTests` then `runCargoTestBudgeted([PACKAGE_NAME], repoRoot, rest)`, lines 366–382; `build-wasm-release`, `describe`, `catalog-root`), `Cargo.toml` (`semio-s-plugin-stdio`, cdylib+rlib, feature `plugin-root`), `🦀️.rs` crate root mounting subsets via `#[path]`, `benches/brep_kernel.rs`.
- flow brep extension: `semio-s-plugin-flow-extension-brep` (`✏️s/🔌️plugins/🌊️flow/🧩️extensions/📐️brep/📦️packages/🦀️rust`), nx `@semio-tech/flow-extension-brep-rust:test`.

## Brep tests
- `✳️brep/🧪️tests/mutate-semio-brep/{🦀️.rs,🥒️.feature,🐍️.py,🧫️fixtures}` — the language-agnostic triplet, consuming `🧫️fixtures/*/operand-a.step, expected.step, expected.mesh.json, expected.metrics.json`.
- per-mutation `🧬️mutations/*/🧪️tests/<case>/🦀️.rs` (13 verbs).
- fixtures generated by `✳️brep/🏭️generator/📜️script.ts` with **brepjs (OCCT) as the oracle**, five shards: topology-build, topology-remove, geometry-replace, move-vertex, booleans; `🦀️json-engine` measures expected values; tessellation tol 3e-5 rel / 1e-6 abs.
- oracle registry `✏️s/🔌️plugins/🗄️stdio/🧪️oracle/🔣️.json` (18 third-party oracles, `productionReachable`, `hostPath`, `comparisonProfiles`), host crate `semio-s-plugin-stdio-test-oracle` (`law::carrier_is_exact`).

## launch.json
- `.vscode/launch.json` is generated from `.vscode/🧩️launch.seed.jsonc`; groups `1_keyboard, 2_mouse, 3_dev, 4_test, 5_build`; names `⚖️gate…`, `🛠️dev…`, `📦️…`; existing stdio entries: `🛠️dev🌉️os-mcp🧵️stdio` (4408), `⚖️gate🗄️stdio-catalog` (4683, `bun nx run workspace:stdio-quick`), `📦️catalog-root🗄️stdio` (5871). Flow gates `⚖️gate🌊️flow🦀️check` (3264) etc. No brep-specific entry exists yet → add `⚖️gate🗄️stdio📐️brep🦀️tests` and `⚖️gate🌊️flow📐️brep🦀️tests` in the seed following the `⚖️gate🌊️flow🦀️check` shape.

## Dependency gate
- `bun ./📜️script.ts verify dependencies [literal-external|summary|list|self-test]` (root script ~10864–10937); baseline `🔒️dependencies.json` entries `{ecosystem,name,version,kinds,users,productionReachable}`; `brepjs`/`brepjs-opencascade` at lines ~459–480 are `production-runtime` + `productionReachable: true` for `@semio-tech/cad-js`. Becoming test-only requires moving them to `devDependencies` of the consuming package(s) and regenerating the baseline.

## Wasm
- `.cargo/config.toml`: `wasm32-wasip2` with `-Z threads=8`, `--max-memory=536870912`; `wasm32-unknown-unknown` in toolchain. Peer note: build stdio components with `SEMIO_PLUGIN_PROFILE=wasm-release` (dev profile exceeds the component parser's function ceiling); `codegen-units = 1` now covers cad/gis/procedural/process/puzzle/sourcing.

## How to run
```bash
cd /Users/ueli/Documents/semio && RUSTC_WRAPPER="" cargo check -p semio-s-plugin-stdio --tests
cd /Users/ueli/Documents/semio && RUSTC_WRAPPER="" cargo test -p semio-s-plugin-stdio --lib -- brep::
cd /Users/ueli/Documents/semio && RUSTC_WRAPPER="" cargo test -p semio-s-plugin-flow-extension-brep
cd "/Users/ueli/Documents/semio/✏️s/🔌️plugins/🗄️stdio/📦️packages/🦀️rust" && bun ./📜️script.ts test quick
cd /Users/ueli/Documents/semio && cargo bench -p semio-s-plugin-stdio
cd /Users/ueli/Documents/semio && bun ./📜️script.ts verify dependencies literal-external
```
