# 📕️ Layout PDF export end to end — status

## 2026-09-18

- Ticket opened on disk (repo MCP timed out). Serve from the 09/16 ticket is gone (no screens, 6079 closed).
- Serve restarted (`screen layout-serve`, scripts copied from 09/16). Probe `🐍️layout-export-pdf-probe.mjs` (Actions pane → `exportPdf` row → playwright download capture → `🐍️validate-pdf.py` PyMuPDF check).
- Probe #1 against the 09/17 wasm: `exportPdf refused: dispatch-failed … typed-operation failed: ` (empty) — `LayoutExportToolJob` validated the retained wire as JSON text (`["exportPdf",{…}]` byte scanner + `from_json_str`), but the wire is the command's `OpBinary` encoding since 09-15 (same trap draw hit 09-17). Replaced with `LayoutCommand::decode_op` + verb/page-target check; fixture test now encodes the real command.
- PDF encoder rewritten (`⚙️engine/📤️export/🦀️.rs`): was one page, rect-only placeholders (text = black box), y unflipped. Now: every page when no `page_id` (reducer no longer substitutes the active page for PDF; file named after the document, `Demo.pdf`), per-page plan from the same `build_display_list_for_page` the Preview paints (parent inheritance, overrides, parley-shaped glyphs), items streamed one per unit into content streams with indirect `/Length` (nothing buffered whole), the embedded `MapLabelSans` TTF as a CIDFontType2 (Identity-H, `/CIDToGIDMap /Identity`, glyphs placed by `Tm` + `<gid> Tj`), rect fill/stroke, image placeholders, y-flipped coordinates. New close stages `PdfItems`/`PdfPages`/`PdfEngine`.
- Native: `pdf_export_carries_every_page_with_the_embedded_font_and_shaped_glyphs` walks the xref like a reader (every object offset, every stream length, startxref) — passes; `SEMIO_LAYOUT_PDF_DUMP` hands the bytes to `🐍️validate-pdf.py`: PyMuPDF opens it, 2 pages 400×500, font `LayoutSans` on both, page-1 ink in the text frame / inherited stroke / image placeholder, none in an empty corner, page-2 blank (`🗑️generated/native-demo-page1.png`).
- Restage #2 → probe #2/#3: the download now fires (`Demo.pdf`) but arrives with 0 bytes; a temporary `[DEBUG] segmented download drained` line in the host drain showed `taken: 0` (first `takeSegmentedDownloadChunk` answered `None`).
- Native reproduction (`export_pdf_publishes_a_segmented_download_the_host_can_drain_into_a_whole_pdf`: registered app, host turns, Download-page ACK, chunk drain — the shell's exact sequence): the handle declares 38 848 bytes, a chunk taken right after the ACK is 4 096 bytes, after quiescence the queue is empty. Cause: `MountedTypedCommandFullOperation` keeps its own clone of `request.output_chunks` and pops it in `retirement_step` — every download built on the request's queue is emptied right after the ACK. (A second, masked drain: the job's own `LayoutExportCloseStage::OutputChunks` popped the same shared queue.) Fixes: `layout_build_export_tool_job` gives the job a queue of its own (`ArtifactOutputChunks::new(MAX_LAYOUT_EXPORT_OUTPUT_BYTES)`, puzzle's `export_fixture` shape) and drops the request's; the tool job and the media-export job hand the sealed queue to the download/media result by `mem::replace` and keep an empty stand-in. Law passes; temporary host log removed.
- Restage #3 → probe #5 (`🗑️generated/layout-export-5/`): Actions pane `exportPdf` → drain `taken: 10` → `Demo.pdf` 29 136 bytes → PyMuPDF: 2 pages 400×500, `LayoutSans` on both, ink in text frame (317 px) / inherited stroke (719) / image placeholder (2 400), empty corner 0, page 2 blank; `Demo-page1.png` identical to the native render.
- Probe #6 (`SEMIO_PROBE_ADD_FRAME=1`, `🗑️generated/layout-export-6/`): `addFrame` executed first → the export carries the live document — the new 120×64 rect is fully inked (7 680 px).
- Regression set (`engine::export`, `engine::scene`, bridge tests, `canvas_pointer_down::tests`, minus the three export tests that hang under the fleet): 39 pass / 12 fail, all three pre-existing families (registry-less harness catalog-authority ×5, DSL sample fixture parse ×5, `RetainedJobPayload` drop witness ×2). Descriptor regenerated (`describe`).

### Left as is
- Boot logs `setActiveExample refused: undeclared-action` since a peer's 09-17 `editor_with_examples` change: the shell now dispatches `setActiveExample` at boot and layout declares no such verb (harmless refusal; not this ticket).
- PDF text uses one embedded font (`MapLabelSans`, the font the Preview paints with); paragraph `font_family` is not mapped to other faces, and `/W` widths are omitted (glyphs are positioned absolutely, so rendering is exact; text selection width in readers is approximate). Real image links (`proxyDataUrl`) are still painted as placeholders — no image decoder in the guest export.
- SVG/PNG exports keep the old rect-only single-page plan.
- The three `engine::export` tests (`production_retained_wire_factory_…`, `exact_layout_out_media_…`, `terminal_candidate_…`) still hang >40 min under the build fleet; not run.
- Serve left on 6079 (`screen layout-serve`).
