/** 🩺️ Slice B3a — `block2d` (`block2d`, react :6024) interaction probe. */
import { runInteractionProbe } from "./🐍️b3a-interaction-probe.mjs";
await runInteractionProbe({ plugin: "block2d", variant: "block2d", port: 6024, action: "addHandleKind" });
