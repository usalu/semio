# 🌊️ Extract WFC plugin — status

Ticket opened 2026-09-18 12:02 by session ⚪d1160693 (Fable 5.1 coordinator). Repo MCP failed to connect; bookkeeping is manual on disk.
Ticket start commit: `🗑️generated/start-commit.txt`.
Goal: `🎯️r2602/🎯️runningsketchpad` (RUNNING-SKETCHPAD-APPS).

## Brief (from the dev)
Extract wave function collapse (WFC) from the 🌀️procedural plugin (its `🧩️assembly` artifact carries the 10 kLOC engine under `🧬️schema/💡️inferences/🧩️wfc-engine`) into its own plugin with five artifacts:
1. **bitmap** — the classic overlapping model: an input bitmap and an output bitmap that looks locally similar.
2. **2d-grid** — regular rectangular grid, fixed left/right/top/bottom neighbours, tiles are vector graphics or bitmaps; two windows: grid canvas with cells + 2d preview.
3. **2d** — arbitrary neighbours, arbitrary non-rectangular shape, rectangular tiles (vector/bitmap); two windows: graph with slots + 2d preview.
4. **3d-grid** — regular non-uniform box grid, six fixed neighbours, mesh tiles; two windows: grid box with cells + 3d preview.
5. **3d** — arbitrary neighbours, arbitrary non-boxed shape, mesh tiles; two windows: the same graph-with-slots window as 2d + 3d preview.

## Decisions (coordinator)
- New plugin folder `✏️s/🔌️plugins/🀄️wfc`, crate `semio-s-plugin-wfc`, package id `semio:wfc`, plugin id `wfc`, label "WFC" (de "WFC").
- Shared engine crate `✏️s/🔌️plugins/🀄️wfc/⚙️engine` → `semio-s-plugin-wfc-engine` (Cargo at `⚙️engine/📦️packages/🦀️rust`, lib path `../../🦀️.rs`). The engine moves VERBATIM from the assembly inference tree; every `#[cfg(test)]`-gated module (grid2d, grid3d, extract, symmetry, tiled, solver-*, search, sample, …) is promoted to a production module because the five artifacts need them at runtime.
- Artifact folders / crates / kind ids (puzzle + fem precedent `◻️2d`/`🧊️3d`, `s.puzzle.puzzle2d`):
  | dev name | folder | crate | artifact kind | app id |
  |---|---|---|---|---|
  | bitmap | `🗿️artifacts/🖼️bitmap` | `semio-s-artifact-wfc-bitmap` | `s.wfc.bitmap` | `s.wfc.bitmap@1/*#editor` |
  | 2d-grid | `🗿️artifacts/🔲️grid2d` | `semio-s-artifact-wfc-grid2d` | `s.wfc.grid2d` | `s.wfc.grid2d@1/*#editor` |
  | 2d | `🗿️artifacts/◻️2d` | `semio-s-artifact-wfc-2d` | `s.wfc.wfc2d` | `s.wfc.wfc2d@1/*#editor` |
  | 3d-grid | `🗿️artifacts/🧱️grid3d` | `semio-s-artifact-wfc-grid3d` | `s.wfc.grid3d` | `s.wfc.grid3d@1/*#editor` |
  | 3d | `🗿️artifacts/🧊️3d` | `semio-s-artifact-wfc-3d` | `s.wfc.wfc3d` | `s.wfc.wfc3d@1/*#editor` |
- The procedural `🧩️assembly` artifact is DISSOLVED: its document model (slots with x/y/z, edges, module child handles, weights, rules, seed) becomes the `3d` artifact (and, without z, the `2d` artifact); the assembly folder, its workspace entries, plugin-root wiring, playground row, and every external referrer are removed (greenfield: no legacy, no compatibility layer).
- Engine ↔ artifact mapping: bitmap → overlapping model (extract + symmetry + grid-2d solver); grid2d → tiled model + grid-2d solver (4-stencil); grid3d → tiled model + grid-3d solver (6-stencil); 2d/3d → graph solver over an explicit topology (assembly's existing route).
- Every solve is an inference (`routed_inference` + inference factory), never persisted state — same law as assembly.

## Waves
- W0 (Sonnet, read-only audits → `📓️explore-*.md`): plugin scaffolding checklist, assembly + engine anatomy, editor window/media patterns, artifact taxonomy template, build/verify pipeline. Status: running.
- W1 (Opus, parallel): engine crate extraction; plugin skeleton + repo registration; five artifact authors (one per artifact); procedural cleanup after the engine copy lands.
- W2 (Opus, parallel): plugin-root wiring of all five artifacts, descriptor regeneration, registry/taxonomy/dependency gates, wasm build, per-artifact playground boot + browser probes.
- W3 (Sonnet audits): spec conformance, tests green, ticket close.

## Log
- 12:02 ticket folder created on disk; five W0 audit agents dispatched.
- 12:10 two W0 agents delegated to background children that died with their turn; resumed with "no sub-agents" instruction.
- 12:55 all five W0 audits landed (`📓️explore-*.md`); execution contract written to `📓️plan.md`.
- 12:58 W1 launched: E (engine crate), P (plugin skeleton + registration), A1 bitmap, A2 grid2d, A3 wfc2d, A4 grid3d, A5 wfc3d — 7 Opus agents in parallel. C (procedural cleanup) waits for E's copy confirmation.

## A1 bitmap
- 12:5x read plan + all five explore reports + puzzle2d/assembly/raster/canvas2d reference code; artifact tree created by hand at `✏️s/🔌️plugins/🀄️wfc/🗿️artifacts/🖼️bitmap`.
- ⚠️ BLOCKER FOR EVERY SLICE: `🔣️taxonomy.json` is currently INVALID — `semanticDirectoryMemberKinds["members-of-artifacts"]` gained `▦️grid2d`, and `▦️` (U+25A6) is not a `canonicalTaxonomyEmoji`, so `loadTaxonomy()` throws "has invalid exact member". That kills `bun ./📜️script.ts new subset|new mutation`, `verify taxonomy`, and every taxonomy-driven gate repo-wide. `🖼️bitmap` itself validates. Whoever owns the taxonomy rows must pick an admitted emoji for grid2d.
- `new artifact` + `new standard` scaffolders ran green for bitmap; `new subset`/`new mutation` refused on the taxonomy fault above, so the subset tree is hand-authored from the assembly/puzzle2d templates.
- 13:20 COORDINATOR: `▦️` (U+25A6) is not Extended_Pictographic, so `▦️grid2d` can never pass `canonicalTaxonomyEmoji` (`📚️library/🔍️discovery/🟦️.ts:706`). Renamed the 2d-grid artifact folder to `🔲️grid2d` (RGI emoji + FE0F, same as the engine's `🔲️grid-2d` module) in `📓️plan.md`; A2 renames the folder, P fixes the taxonomy rows and Cargo paths. All other wfc folder emojis (`🖼️`, `◻️`, `🧱️`, `🧊️`, `🌊️`, `⚙️`) pass the rule.

## P scaffolding
- 13:2x plugin skeleton written: `🀄️wfc/🦀️.rs` (`WfcApps`, 10 variants, 5× declare_artifact/routed_inference/inference factory/mutation roster/activation), `📦️packages/🦀️rust/{Cargo.toml,📋️project.json,📜️script.ts}`, `📦️packages/🟦️typescript/{package.json,📋️project.json,📜️script.ts,🟦️.ts}`, `🎮️commands/📌️.empty.md`, `README.md`, `AGENTS.md`, `🧪️tests/{🔬️surface,🚪️close-ladder,😴️idle-turns,🔬️boot-deadline}`, `🧫️fixtures/🚪️close-ladder/🔣️.json`, `🧪️tests/🎚️config/🟦️.ts` (vitest).
- Ports (re-verified free repo-wide before use): bitmap 6041/6141, grid2d 6042/6142, wfc2d 6043/6143, grid3d 6044/6144, wfc3d 6045/6145.
- 13:3x TAXONOMY FIXED: every `▦️grid2d` replaced by `🔲️grid2d` in `🔣️taxonomy.json`, root `Cargo.toml` (member + alias), the TS barrel, `.vscode/🧩️launch.seed.jsonc` (namePrefix + native entry), `🀄️wfc/AGENTS.md` and the `🔲️grid2d/🦀️.rs` stub docstring. `loadTaxonomy()` now returns cleanly (probed directly) — the "invalid exact member" throw is gone and every taxonomy-driven gate loads again.
- ⚠️ `bun ./📜️script.ts verify taxonomy report` still exits 1, but on an UNRELATED pre-existing fault: the gitignored, stale (Sep 15) `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📤️distribution/🧾️manifest.json` has 10 byte-unsorted `inputs[]` pairs → `Current compiler input manifest compiler inputs are not path-sorted`. It contains zero wfc rows. Regenerating it is a dev-distribution build, owned by whoever runs `framework-os-dev`; not touched here.

## A4 grid3d
- 12:2x–12:4x read `📓️plan.md` + all five explore reports + reference code (puzzle2d artifact root/standard/subset/schema facet, assembly snapshot/diff/mutations/inference/editor/viewer/examples/io, puzzle3d World3d main window + instance residency, generation3d preview window, engine `🧊️grid-3d`/`🀄️tiled`/`🏗️model`/`💼️job`).
- Authored the grid3d document model, the 14 mutation folders (generated by `🐍️grid3d-mutation-scaffold.py`), diff facet, snapshot facet + codecs, schema facet + descriptor, mutations text/binary facets, crate root mount tree, `📦️packages/🦀️rust/{Cargo.toml,📋️project.json}`.
- 🚨️ **12:43 INCIDENT — the whole `✏️s/🔌️plugins/🀄️wfc` tree was WIPED by something outside this slice.** `find ✏️s/🔌️plugins/🀄️wfc -type f` went from hundreds of files to SEVEN: only files written in the ~60 s before the wipe survived (one file each in `🖼️bitmap`, `◻️2d`, `🧊️3d`, `🧱️grid3d`). `⚙️engine` (slice E's entire copy) and the plugin root `🦀️.rs`/`📦️packages` (slice P) are GONE; root `Cargo.toml` still lists all seven members, so the workspace is now broken for every peer. Everything was untracked (`?? ✏️s/🔌️plugins/🀄️wfc/`), so git cannot restore it. Not caused by this slice: the only destructive command A4 ran was `rm -f` on two scaffolder-emitted `🟦️.ts` stubs inside its own folder, minutes earlier. Suspect a `git clean`-style sweep or a taxonomy/`enforce` pass with pruning. COORDINATOR: find and stop it before any slice re-authors, and warn E and P.
- Mitigation adopted by A4: every hand-authored grid3d file is now emitted from `$T/🐍️grid3d-*.py` generators that live in the TICKET folder (which survived), so a repeat wipe costs one command instead of hours.

## A5 wfc3d
- 12:3x read `📓️plan.md` + all four named explore reports + assembly (snapshot/diff/9 mutations/inferences/editor/examples/io), puzzle2d artifact root + standard/subset roots, dag NodeGraph window, puzzle3d World3d instance records, note io. Authored the wfc3d document model (`Wfc3dSnapshot{schema,seed,slots,edges,tiles,rules}`, `Slot3d`/`SlotEdge`/`Tile`/`TileMedia3d`/`GraphRule`/`Color`), `Wfc3dDiff`, and all 15 mutation folders (component+diff+inverse+descriptor+payload schema).
- ⚠️ 12:41 THE WHOLE PLUGIN FOLDER MOVED: everything that was under `✏️s/🔌️plugins/🀄️wfc` (engine, plugin root, all five artifact trees, my wfc3d files) is now under `✏️s/🔌️plugins/🀄️wfc` — which has existed since 12:26. `🀄️wfc` is left as an empty directory chain. This is NOT reflected anywhere else: root `Cargo.toml` members/aliases still say `🀄️wfc`, `🔣️taxonomy.json` `members-of-plugins` still says `🀄️wfc`, and `📓️plan.md` §1 fixes `🀄️wfc`. Either the move is deliberate (🌊️ collides with the existing `🌊️flow` plugin dir) and Cargo/taxonomy/plan must follow, or it is an accident and the tree must move back. **Coordinator: please rule, and log it.** A5 is continuing to author in `🀄️wfc/🗿️artifacts/🧊️3d` because that is where the live files are; a later folder rename is a cheap `mv`.
- Mutation vocabulary (15, kebab / variant / verb): create-slot, delete-slot (cascades edges), move-slot, resize-slot, connect-slots, disconnect-slots, pin-slot (verb `bind` — `pin` is not in `protocol::APPROVED_VERBS`), unpin-slot (verb `unbind`), create-tile, delete-tile (cascades rules + releases pins), change-tile-weight, change-tile-media, create-rule, delete-rule, change-seed.
- 🟢️ 12:45 A5 → EVERY SLICE, re: A4's "WIPE" entry: **nothing was deleted — the tree was MOVED.** `find ✏️s/🔌️plugins/🀄️wfc -type f | wc -l` = 647: slice E's whole `⚙️engine`, slice P's plugin root + `📦️packages` + `README.md`/`AGENTS.md`/`🧪️tests`/`🧫️fixtures`, and all five artifact trees (including A4's grid3d and A5's 102 wfc3d files) are intact under `✏️s/🔌️plugins/🀄️wfc/` (dir mtime 12:26, consolidated 12:41). `✏️s/🔌️plugins/🀄️wfc/` is now only an empty directory chain that later writes recreated. DO NOT RE-AUTHOR — `cd` into `🀄️wfc` and check before rewriting anything. Open question for the coordinator stands: `🀄️wfc` vs `🀄️wfc` (root `Cargo.toml`, `🔣️taxonomy.json` and `📓️plan.md` all still say `🀄️wfc`; `🌊️` also collides with the existing `🌊️flow` plugin dir).
- 13:5x COORDINATOR: an outside process moved `✏️s/🔌️plugins/🌊️wfc` → `✏️s/🔌️plugins/🀄️wfc` at ~12:41 (🌊️ collides with the sibling `🌊️flow` plugin emoji; 🀄️ is admitted and already used by the engine's `🀄️tiled` module). DECISION: `🀄️wfc` is the canonical plugin folder from now on. Root `Cargo.toml`, `package.json`, `🔣️taxonomy.json`, `🗺️catalog.json`, launch seeds/configs, `📓️plan.md` repointed by the coordinator; every slice told to write only under `🀄️wfc`; the 144 straggler files under `🌊️wfc` are merged into `🀄️wfc` (newer wins) and `🌊️wfc` removed by the coordinator.
- 14:0x P → folder rename accounted for. FOR THE RECORD, so nobody hunts a ghost: the ~12:41 move was MINE (slice P), not an outside process. `plugin-registry:generate` refused `{ "pluginId": "wfc", "directoryName": "🌊️wfc" }` with `Duplicate module identity or sibling emoji` (`📇️registry/📦️deployment/🟦️.ts:45` folds `directoryName` to its first grapheme, FE0F stripped — `🌊️flow` and `🌊️wfc` both fold to `🌊`), and OMITTING the catalog row is equally fatal: the `@repo/emoji-project-json` nx plugin then fails the whole project graph with `Component needs an authored deployment directory` for any `role = "plugin"` crate. So no state existed in which `🌊️wfc` could host the plugin crate; `🀄️` (U+1F004, RGI, free among the 59 plugin sibling emojis) was picked and the tree moved. Apologies for the churn it caused A4/A5 — I should have announced it before the `mv`.
- P: the leftover `🌊️wfc` tree was diffed against `🀄️wfc` (235 files, **0 unique, 0 newer**) and removed; that is what let the nx project graph load again.
- ✅️ P GATES: `bun nx run @semio-tech/plugin-registry:generate` GREEN — 60 plugin crates, 65 playgrounds, `.vscode/launch.json` regenerated with all 15 wfc entries, and the five wfc rows in `🤖️generated/🎠️playgrounds.json` carry the right app ids/aliases/ports. `cargo metadata --format-version 1 -q` GREEN (workspace resolves with all six wfc crates). `loadTaxonomy(repoRoot)` GREEN.
- ⚠️ P: `verify taxonomy report --scope "✏️s/🔌️plugins/🀄️wfc"` still exits 1, on TWO pre-existing non-wfc faults, in order: (1) the gitignored Sep-15 `🧑‍💻dev/📤️distribution/🧾️manifest.json` has 10 byte-unsorted `inputs[]` pairs → `compiler inputs are not path-sorted`; (2) `📚️library/🧫️fixtures/📐️cad-draw-path-projection/🔣️.json` → `frozen-coordinate-evidence-invalid: document digest does not match registered bytes`. Neither mentions wfc; both belong to other owners.
- ⚠️ P: `cargo check -p semio-s-plugin-wfc -j 4` RED with exactly ONE error, in a peer's file: `couldn't read …/🧱️grid3d/…/🪆️subsets/✳️any/✏️editor/🦀️.rs` — A4's crate root mounts an editor module that has not landed. engine + bitmap compiled. A1 (`🖼️bitmap/🦀️.rs`) and A3 (`◻️2d/🦀️.rs`) crate roots are still P's 1-line stubs — please overwrite them. No artifact API was stubbed anywhere.
- P report: `📓️scaffolding.md` (every edited file with exact lines, port table, per-gate result, and the 7 open W2 items — incl. the four behaviour-keyed `dev-jco-{filesystem,actor,pure,resident}-interfaces` taxonomy sets, deliberately left for the first real jco materialization to decide).

## E engine
- 13:02 E: engine crate registered and checks green
- 13:20 E: DONE. `cargo check --lib --tests` GREEN (0 warnings), `cargo check --target wasm32-wasip2` GREEN, `cargo clippy --lib` GREEN (0 warnings), `cargo test --lib` 301/301 on an idle moment (300/1 under load — see below). 34 modules un-gated out of `#[cfg(test)]` (only `oracle` + `model_vectors` stay test-only); `Assembly{Model,Topology}{Build,Phase}` → `Graph{Model,Topology}{Build,Phase}`; `protocol::`/`dsl::` aliases replaced by `semio_framework_os_kernel::`; `job::{payload_bytes,retire_outcome,close_job}` are production helpers now (`search` ships and calls them). `🧵️parallel` needed NO wasm gate — it never used threads. 3 new tests drive `WfcJob<Grid2dTopology>`/`WfcJob<Grid3dTopology>` and an `extract_2d` 4×4→8×8 local-similarity solve. Procedural's copy is untouched (slice C still owns the delete).
- ⚠️ E: `job::tests::every_large_domain_unit_including_checkpoint_stays_below_watchdog` is FLAKY under a loaded machine — its `max step < 8 ms` assertion measured 7.2/16.5/18.4/21.2/23.2 ms across runs (p99 < 2 ms always passes). Pre-existing, the outlier is always a `PropagateCompatibilityEdge` unit ~324k steps in. I added the measured value to the assert message; W2 should re-run on an idle machine.
- ⚠️ E: two assertions in the copied `💼️job` unit tests were stale since 2026-09-09 (`599a5d8450`): `CommitBuild::new`/`CheckpointBuild::new` run before any `StepContext` exists so they return an empty placeholder `JobFault`, while the tests asserted the detail bytes `wfc-{commit,checkpoint}-admission-exceeded`. They now assert the admission refusal itself. Detail-carrying faults raised inside `step` still assert on bytes.
- E report: `📓️engine.md` (full public-API list with one-line semantics per item — artifact authors read §7; note `Stencil3d::Face6`, there is no `VonNeumann` variant, and `extract_2d` is 2D-only). Rebuild script: `📜️engine-extract.sh` + `🗂️engine-root.rs.txt` + `🗂️engine-grid-job-drive.rs.txt` — re-runnable from scratch if the folder is wiped again.

## A2 grid2d
- 12:1x–12:4x read `📓️plan.md` + `📓️explore-artifact-taxonomy-template.md` + `📓️explore-editor-window-and-media-patterns.md` §1/§3/§4/§7 + `📓️explore-assembly-and-wfc-engine.md` §1/§2.4/§2.5 + `📓️explore-build-verify-playground-pipeline.md` §1/§6, then the reference code (puzzle2d artifact root/standard/subset/window-config, assembly snapshot/diff/mutations/inference/editor/viewer/examples/io, dag io + `SnapshotBuilder`, note window-config, draw `PathSegment`, framework `Board2dScene`/`Canvas2dScene`/`Canvas2dHost` layer contract).
- Followed the folder moves: `▦️grid2d` → `🔲️grid2d` (25 files rewritten) and `🌊️wfc` → `🀄️wfc`. Root `Cargo.toml`'s two grid2d lines and the 11 grid2d lines of the plugin TS barrel were repointed by A2 when they were still stale.
- Authored: document model (`Grid2dSnapshot` + `WfcTile2d`/`WfcTileMedia2d`(bitmap|vector|image child)/`WfcAdjacencyRule2d`/`WfcPinnedCell2d`/`WfcCell2d`/`WfcColor`/`WfcPoint2`/`WfcPathSegment`/`WfcVectorPath`), `Grid2dDiff` (16 lanes, index-carrying upserts), 14 mutation triads, snapshot/mutations text+binary codec facets + grammar/protocol sidecars, the 20-leaf schema descriptor + the 5-leaf inference descriptor, `🚪️io` (`io()` on the `io_mechanism` channel with txt + json hops, both `IoFidelity::Exact`), the `s.wfc.grid2d.solve` inference (`TiledModelBuilder` + `declare_stencil_relations_tiled(Stencil2d::VonNeumann)` + `Grid2dTopology` + resumable `WfcJob`, commit = `assignments [[x,y,tileId]]` + `contradiction` + `entropy [[x,y,v]]`), editor (Board2d grid pane + Canvas2d preview pane, 21 verbs, 3 utilities, per-window config+transient owners), viewer, two examples (`pipes` 5 vector tiles / `terrain` 3 bitmap tiles), 14 fixture quintets, the `🥒️.feature`/`🐍️.py`/`🦀️.rs` replay triplet, `🔮️oracles/🔣️.json`, mount-contract + store-fixture tests.
- Rule default is DOCUMENTED and enforced: an unauthored `(A, B, direction)` pair is FORBIDDEN, so the rule set is the complete whitelist and an empty rule set is unsatisfiable on any grid with two adjacent unmasked cells.
- Generators live in the ticket folder so a repeat wipe costs one command: `📜️grid2d-mutation-leaves.py` (14 payload/manifest/payload-schema leaves), `🐍️grid2d-oracle.py` (independent second implementation of the whole mutation algebra), `🐍️grid2d-fixtures.py` (quintets + mounted tests + feature + replay + oracle manifest + the crate-root mount block).
- ⚠️ `pin`/`unpin`/`mask`/`unmask` are NOT in `protocol::APPROVED_VERBS` (compile-time assert in `#[derive(dsl::Mutations)]`). The four cell mutations keep their kebab KINDS but carry the approved verbs `fix`/`clear`/`remove`/`restore`.

## C cleanup
Report: `📓️procedural-cleanup.md` (full referrer ledger + every command and result). Logs: `🗑️generated/cleanup/`.

- Procedural is assembly-free except the folder itself: plugin root (enum variants, inference factory, `routed_inference`, `artifact`, 4 editor/viewer rows, activation, docstring, capability reason), the 3 `assembly_*` surface tests, the crate dependency, the `assembly` playground row (**ports 6019/6119 released**), and the root `Cargo.toml` member + alias are gone. No `[profile.wasm-dev.package.…assembly]` override existed. `Cargo.lock` self-refreshed (0 hits).
- Swapped to wfc (ids `s.wfc.wfc3d` / `s.wfc.wfc3d.solve`, folder `🀄️wfc`): `📚️example-picker.json` (2 rows), the MCP `💡️inference/🧪️tests/🔬️quick` fixture strings (owner `wfc`, helper `wfc_only_catalog`, test renamed `…finds_the_real_wfc_roster`), the `🪪️artifact-admission` `firstParty` row (still 39 rows), the `💼️jobs/💡️infer` doc comment, and two stale engine-path doc comments (`✨️derive/🆔️newtype-transparent`, `🧮️math`).
- Removed: the `📜️script.ts` `s.assembly.solve` tool-run row, both `.vscode/launch.json` assembly configs (that file is GENERATED — `plugin-registry:generate` now reproduces the removal from the Cargo metadata), and taxonomy member names `🧩️assembly`, `🧩️mutate-assembly-1`, `🧩️wfc-engine`. Kept `🧩️appends-slot-c-at-index-2` for the A3/A5 port. No assembly rows existed in `🧩️launch.seed.jsonc`, `.claude/launch.json`, the TS barrel, `.storybook`, or the plugin-level close-ladder/idle-turns/boot-deadline tests+fixture.
- 🟢 `cargo check -p semio-s-plugin-procedural --lib --tests`; 🟢 wasm32-wasip2 check; 🟢 `procedural-plugin:describe` (descriptor 299 105 → 279 397 B, `🔣️.json` −3 110 lines, **0** assembly strings); 🟢 `plugin-registry:generate`; 🟢 `semio-framework` example-picker test; 🟢 `semio-framework-plugin` admission-fixture test; 🟢 procedural `close_ladder` (3/3) + `boot_deadline` (1/1).
- 🟡 Pre-existing, NOT caused by this slice (`git diff` vs `7bea15c349` shows zero changes to the implicated files): `surface_tests::generation2d_viewer_never_mutates` panics "ordered-map root must be explicitly retired before drop"; `idle_turns` 2/3 fail on generation2d retention (~29 KB/turn).
- 🔴 `cargo test -p semio-framework-os-mcp --lib inference` — the whole lib test target fails to compile at ticket start (10 errors in `📇️registry/🧪️tests/🔬️quick`, `🏠️workspace/🔗️remote`, `🚚️transport`); none in the file I edited. Not fixed (peer breakage). **My MCP swap is therefore unverified by execution.**
- 🔴 `plugin-registry:check` exit 1 — 2 315 findings repo-wide (long-standing; stdio alone 1 248). Procedural: 25, of which exactly one names assembly (`🧩️mutate-assembly-1` unreachable from the Cargo manifest) and vanishes with the folder. **Separately: 🀄️wfc has 128 findings** (examples missing `🟦️.ts`/`🖼️assets`, `🧪️tests` under window dirs, modes missing `🎮️commands`/`🎚️config`/`👥️presence`/`🫧️transient`) — in-progress plugin, for W2.
- ⚠️ Volume hit 100 % mid-slice (4.8 GiB free; shared build dir 323 GB) and killed the first `describe`. I pruned nothing — every incremental session is <45 min old and `wasm32-unknown-unknown` had 18 936 files written today, so it is all live fleet work. It recovered to 24 GiB on its own. Expect more random build kills.
- ⚠️ `🧩️assembly` is STILL ON DISK. After A3/A5 finish: delete it, refresh `Cargo.lock` with `cargo metadata`, run `bun ./📜️script.ts schema generate` (drops the 10 `s.procedural.assembly.1.any*` catalog keys — they render from the on-disk schema tree, so I could not drop them yet), then re-run `plugin-registry:generate`+`:check`. Until then `verify taxonomy` on the procedural scope will flag `🧩️assembly`/`🧩️mutate-assembly-1` as unregistered names — that is the intended end state, not a regression.

## A3 wfc2d
- 12:2x read `📓️plan.md` §0/§1/§3, all four named explore reports, the whole `🧩️assembly` artifact (root/schema/snapshot/diff/9 mutations/826-line inference/editor/viewer/examples/io/fixtures), puzzle2d's `artifact()`/`standard()`/`subset()` shape, dag + sequence NodeGraph windows, note's `io()`/schema-facet layout, and `Canvas2dHost`'s real layer prop contract.
- 12:3x ran the official scaffolders (`new artifact` / `new standard` / `new subset`) before hand-authoring, so the tree shape is the canonical one. `new mutation` was RED on a peer's taxonomy row (`members-of-artifacts` exact member `▦️grid2d`); the 15 mutation folders were hand-authored to the documented shape instead and match assembly leaf-for-leaf. Two ticket-local generators keep them reproducible: `🐍️wfc2d-mutations.py` (triads + quintets + the crate-root mount block) and `🐍️wfc2d-sidecars.py` (grammars/protocols/ebnf/g4/ksy/spicy/abnf + the 5-language facet leaves).
- 12:45 folder rename: 44 of my files landed under `🌊️wfc` between the `mv` and the announcement; all copied into `🀄️wfc` (newer wins) and both generator scripts repointed. Nothing lost.
- ✅️ **A3 DONE. Everything green.** `cargo check --features component-app-assembly --lib --tests -j 4` GREEN; `RUST_MIN_STACK=33554432 cargo test --lib -- --test-threads=4` **174 passed / 0 failed**; `cargo clippy --lib --tests` GREEN (0 warnings in this crate); `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2` GREEN; `bun nx run @semio-tech/wfc-js:test` **4 files / 91 tests GREEN** (incl. the cross-language oracle replaying all 15 quintets through the TS twin); `python3 …/🧪️tests/🧩️mutate-wfc2d-1/🐍️.py` **15 kinds / 0 divergences**. Logs in `🗑️generated/wfc2d/`.
- 🐛️ **A3 → A4/A5/C, three real bugs found while wiring the inference — the first one is INHERITED FROM ASSEMBLY and will bite you too:**
  1. A child `WfcJob`'s `CommitCandidate` carries TWO retained payloads (`state` AND `output`). Keeping only `state` and letting the candidate drop trips `RetainedJobPayload`'s Drop assertion inside your `step`. Retire the other one (`while !payload.terminal_is_empty() { payload.close_step(1, JOB_PAYLOAD_PAGE_BYTES); }`). The `Restore` branch must retire BOTH. `🧩️assembly/…/💡️inferences/🦀️.rs` has the identical latent bug in both branches — slice C should know before that code is copied further.
  2. Do NOT gate `InteractiveJob::terminal_is_empty` on your own `closing` flag (assembly does). The framework calls `begin_close()` itself at close stage 0 and then returns `Blocked` forever while `terminal_is_empty()` is false (`🧵️job/🦀️.rs:2724`), which a `while !terminal_is_empty()` driver SPINS on — a hang, not a failure. Report ownership only, and put a `guard` assert in any headless close loop.
  3. `wfc-unsatisfiable` is a **Fault**, not a commit-less completion: the engine publishes `PublicationKind::Fault` with that detail (`⚙️engine/💼️job/🦀️.rs:1771`). Intercept exactly that detail, retire `fault.detail`, close the child, and answer your contradiction verdict — otherwise the whole inference reports a failure for a perfectly good "no solution" answer.
- 🕸️ **A3 → A5: the `wfc-graph` window is ready to copy VERBATIM** from `✏️s/🔌️plugins/🀄️wfc/🗿️artifacts/◻️2d/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🎭️modes/✏️edit/🪟️windows/🕸️graph/🦀️.rs` (+ its `🧪️tests/🔬️unit/🦀️.rs`, which also copies unchanged except for the `use` path). It depends on NOTHING from wfc2d: its whole view is the local `SlotGraphView` trait over `GraphSlotView`/`GraphEdgeView` plus a `GraphCamera`. On your side you write ONE newtype — `Wfc3dGraphView<'a>(&'a Wfc3dSnapshot)` implementing `SlotGraphView` by projecting `x`/`y` and dropping `z` — exactly as `Wfc2dGraphView` does in `✏️editor/🦀️.rs`. Keep the constants spelled identically: `WFC_GRAPH_WINDOW = "wfc-graph"`, `WFC_GRAPH_BODY = "wfc.graph"`, ports `adjacent-in`/`adjacent-out`, nine action ids, all `Migrated`.
- 🧭️ A3 → plugin root / W2: `crate::artifact::<PA>()` is `semio_s_artifact_wfc_2d::artifact::<PA>()` bounded by the crate's own `ArtifactApps` trait (puzzle2d shape); the inference roster rows are `schema::inferences::{wfc2d_inference_metadata(), register_wfc2d_inference_factory(&ActionBus::production()), wfc2d_artifact_inference_descriptor()}`; editor/viewer are `editor::wfc2d::{Wfc2dEditor, create_wfc2d_editor}` / `viewer::wfc2d::{Wfc2dViewer, create_wfc2d_viewer}`; OS kind `2d.wfc2d`; three examples (`two-room-corridor`, `wall-roof-facade-strip`, `hex-ring`). Editor `Config`/`Transient` are REAL types (`Wfc2dConfig`/`Wfc2dTransient`), not `No*`, so the plugin root must carry their stores.
- ⚠️ A3: `bun ./📜️script.ts verify taxonomy enforce --scope "…/◻️2d"` still exits 1 on the SAME pre-existing non-wfc fault slice P reported — `frozen-coordinate-evidence-invalid: 🧰️framework/…/🧫️fixtures/📐️cad-draw-path-projection/🔣️.json: document digest does not match registered bytes`. It aborts before reaching any wfc check.
- 🟦️ A3 → P: the plugin TS barrel (`📦️packages/🟦️typescript/🟦️.ts`) names `🧬️schema/{📸️snapshot,🔺️diff,🧬️mutations}/{📝️text,💾️binary}/🟦️.ts` for every artifact. wfc2d follows the SCAFFOLDER's layout (native codecs under `🚪️io/…`, five normative language leaves under `🧬️schema/<facet>/`), which is what `bun ./📜️script.ts new subset` emits and what `🗒️note` ships, so those six paths did not exist. They now do, as thin TS facades naming each channel's language id and carrier type, and every scaffolder `SCAFFOLD` stub in this artifact is replaced with real content — the barrel resolves for wfc2d. Worth checking whether A1/A4 put the Rust codecs under `🧬️schema` (assembly's older layout) instead; the two layouts should not coexist in one plugin.
- A3 report: `📓️wfc2d.md` (document model + 5 declared deviations, the 15-mutation table, the reusable-window copy contract for A5, the inference stage list, every command with its result, the three bugs, and 6 known gaps).
- ✅️ A2 GATES, all foreground, logs in `🗑️generated/grid2d/`: `cargo check --features component-app-assembly --lib --tests -j 4` GREEN (0 errors, 0 warnings from this crate); `RUST_MIN_STACK=33554432 cargo test --features component-app-assembly --lib -j 4 -- --test-threads=4` **GREEN 201/201**; `cargo clippy --features component-app-assembly --lib --tests` GREEN (0 warnings); `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2` GREEN; `python3 …/🧪️tests/🔲️mutate-grid2d-1/🐍️.py` GREEN (14 vectors, 0 failures); `bun nx run @semio-tech/wfc-js:test` GREEN (5 files / 113 tests, 22 of them the grid2d cross-language oracle).
- ⚠️ A2: `verify taxonomy report --scope "…/🔲️grid2d"` exits 1 on the SAME pre-existing non-wfc fault P logged — the stale gitignored `🧑‍💻dev/📤️distribution/🧾️manifest.json` throws `compiler inputs are not path-sorted` before any wfc row is read.
- 🐛️ A2 → EVERY SLICE DRIVING `WfcJob` FROM A PARENT JOB (three bugs, all confirmed here by a wedged test binary):
  1. An unsatisfiable grid is published as `PublicationKind::Fault` with detail `wfc-unsatisfiable` (`⚙️engine/💼️job/🦀️.rs:1771`), NOT as a commit-less `Complete`. Intercept that exact detail, `job::retire_outcome`, `job::close_job(child)`, then answer your contradiction verdict — otherwise a perfectly good "no solution" reports as an inference failure.
  2. A child's `CommitCandidate` owns TWO retained payloads (`state` AND `output`). Keeping only `state` and dropping the candidate panics inside `RetainedJobPayload::drop` (*"requires one-page close to terminal-empty"*) and then WEDGES the test binary (tests sat "running for over 60 seconds"). `job::{retire_outcome, close_job}` are production helpers — use them in BOTH the `Solve` and the `Restore` arms. **Assembly's `AssemblyInferenceJob` has this latent bug in both arms; do not copy it forward.**
  3. Do not gate `InteractiveJob::terminal_is_empty` on your own `closing` flag (assembly does): the framework calls `begin_close()` itself and returns `Blocked` while the predicate is false, so a `while !terminal_is_empty()` driver spins (`🧵️job/🦀️.rs:2724`). A2's job has no `closing` field at all and both headless close ladders carry a bounded guard assert.
- 🐛️ A2 → EVERY SLICE WITH AN UPPERCASE WIRE ENUM: `semio_framework_value_derive` supports ONLY `rename_all = "camelCase" | "kebab-case" | "lowercase" | "snake_case"` and **silently ignores** anything else (`🌱️value/✨️derive/⚙️expansion/🦀️.rs:181`). `rename_all = "SCREAMING_SNAKE_CASE"` left `WfcDirection2d` on the wire as `Left`/`Right` while the JSON Schema and the GraphQL enum declared `LEFT`/`RIGHT` — 87 test failures from one silent no-op. Spell such tokens per variant with `#[value(rename = "LEFT")]`.
- 📓️ A2 report: `📓️grid2d.md` (document model, the 14-mutation table with diff lanes + inverses, the inference stage ladder, both windows' scene contracts, the three state tiers, every test suite, every command run, 7 known gaps). Rebuild scripts, re-runnable from scratch: `📜️grid2d-mutation-leaves.py`, `🐍️grid2d-oracle.py` (the independent second implementation), `🐍️grid2d-fixtures.py` (quintets + mounted tests + feature + replay + oracle manifest + the crate-root mount block).
- 🔭️ A2 → W2: the grid2d playground lane (variant `grid2d`, ports 6042/6142) has NOT been booted from this slice — servers are the coordinator's. Everything the boot needs is in place: 21 verbs all `Migrated`, `command_from_action` typed against `DslValue`, the `s.wfc.grid2d.solve` routed metadata (`grid2d_inference_metadata()`) and factory (`register_grid2d_inference_factory`), two examples with real media, and `artifact::<PA>()`/`definition()`/`io()` complete.
- 13:4x–14:xx **A4 grid3d is DONE and green.** `cargo check --lib --tests` ✅️, `cargo test --lib` ✅️ **201 passed / 0 failed / 2 ignored in 0.14 s**, `cargo clippy` ✅️ **zero warnings in this crate**, `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2` ✅️, the stdlib-only Python second implementation replays **14/14 vectors** ✅️. Full report: `📓️grid3d.md`. Logs under `🗑️generated/grid3d/`.
- Shipped: the document model (non-uniform `cellSizesX/Y/Z`, tiles with inline-mesh OR `s.stdio.semio@v1/mesh` child media, a CLOSED six-direction adjacency allow-list, pins, masks), all 14 mutations with canonical-sorted point-invertible inserts, the 20 + 5 handcrafted schema leaves, text/binary facets + derived `.ebnf`/`.g4`/`.ksy`/`.spicy`/`.abnf` sidecars, the resumable `s.wfc.grid3d.solve` inference, two `World3d` windows (`wfc-grid3d-grid` + `wfc-grid3d-preview`, row 50/50), per-window-instance config, viewer, two examples (`blocks` 4×3×4 non-uniform, `pipes-3d` periodic-x), 14 fixture quintets, the oracle manifest and the feature/py/rs triplet.
- The 12:43 "wipe" was the `🌊️wfc` → `🀄️wfc` MOVE; nothing was lost. Everything above is at `✏️s/🔌️plugins/🀄️wfc/🗿️artifacts/🧱️grid3d`, and every generated file is reproducible from `🐍️grid3d-{mutation-scaffold,schema-leaves,case-tests,oracles,grammar-sidecars}.py` in this folder.
- 🐛️ FOUR engine/framework faults found and fixed (details in `📓️grid3d.md` §5) — **every artifact slice should check for them**: (1) `StepContext` grants exactly ONE payload page per STEP, so a paged encode stage MUST yield after each page; a second `admit_payload_page` is refused AND its own fault detail cannot be admitted either, which surfaces as an EMPTY fault string. (2)+(3) confirm A3's two: both retained payloads of a `CommitCandidate` must be retired, and a child `WfcJob` must go through the close ladder before drop. (4) `wfc-unsatisfiable` arrives as a job FAULT — intercepted and answered as `satisfiable: false`, so the commit payload carries that verdict.
- ⚡️ PERFORMANCE, relevant to every slice: assembly's headless `BatchDriveConfig { fuel_per_step: 1, step_budget_us: 2000 }` makes a 48-cell solve take **over ten minutes**. grid3d uses `65_536` / `250_000`; the whole 201-test suite (which solves both examples several times) then runs in 0.14 s.
- ⚠️ TWO gates BLOCKED by things outside this slice: `bun nx run @semio-tech/wfc-js:test` cannot even load the Nx graph — `Native dependency has no Nx project owner: ✏️s/🔌️plugins/🀄️wfc/🗿️artifacts/◻️2d/📦️packages/🦀️rust` (that crate needs a `📋️project.json`; grid3d's is present). `verify taxonomy` still exits on P's recorded stale `🧾️manifest.json`, so grid3d's taxonomy conformance is untested by tooling.
- 📝️ Deliberate deviations from plan §3, both forced: the solve commit row is a RECORD `{x, y, z, tileId}`, not a 4-tuple (`semio_framework_value_derive` has no `ToValue`/`FromValue` for 4-tuples), and the commit carries `satisfiable: bool` so an unsatisfiable grid is an answer rather than a failure.
- 15:1x COORDINATOR: created the missing nx owners `📋️project.json` + `📜️script.ts` for `◻️2d`, `🖼️bitmap` (artifact router shape) and `⚙️engine` (`@semio-tech/wfc-engine-rs`, check/test targets); `bun nx show projects` now loads and lists all 8 wfc projects (`wfc-plugin`, `wfc-js`, `wfc-engine-rs`, five `wfc-<artifact>-rs`) plus the six discovered mutate/mount-contract test projects. W2 (conformance audits on finished artifacts) starting.
- 14:2x **A5 wfc3d DONE — report `📓️wfc3d.md`.** Green: `cargo check --lib` (with and without `component-app-assembly`), `cargo check --lib --tests`, `cargo clippy --lib --tests` (0 warnings in this crate), `RUST_MIN_STACK=33554432 cargo test --lib -- --test-threads=4` → **227 passed / 0 failed / 2 ignored** (the two ignored are the `#[ignore]` fixture+asset GENERATORS), `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2`, `NX_DAEMON=false bun nx run @semio-tech/wfc-js:test` → **8 files / 125 tests**, and `python3 $T/🐍️wfc3d-oracle-runner.py` → **15/15 mutation vectors + mount contract**. Logs in `🗑️generated/wfc3d/`.
- 🐛️ **Three live-solve bugs fixed here that are LATENT IN ASSEMBLY and would hit every other slice that copied its inference** (A3 independently found the same two of them): (1) the child `WfcJob`/`WfcRestore` must go through `engine::job::close_job` before the option is cleared, or its admitted pages stay in the session's payload ledger and `worker_job_close_step` returns `Blocked` forever — the headless solve then hangs in the CLOSE, not in the search (measured: >10 min on a 3-slot problem → 0.04 s for all fixtures after the fix; this is why assembly's own example-outcome test says the live solver "aborts the process"); (2) `InteractiveJob::terminal_is_empty` must NOT gate on a private `closing` flag; (3) a `CommitCandidate` carries TWO retained payloads — retire the one you do not keep.
- ⚠️ `bun nx run @semio-tech/wfc-js:test` FAILS under the nx daemon: `@repo/emoji-project-json … Source file "✏️s/🔌️plugins/🀄️wfc/📦️packages/🦀️rust/Cargo.toml" does not exist in the workspace`. The file exists — the daemon's project graph is stale after the `🌊️wfc`→`🀄️wfc` move. `NX_DAEMON=false` is green. **W2: `bun nx reset` once before trusting any nx target or any `📜️script.ts verify` gate.**
- 📌️ Convention for the other artifact slices, as ruled: kebab kinds stay `pin-slot`/`unpin-slot`; `SemanticDescriptor` carries `verb "fix" / record "Fixed"` and `verb "clear" / record "Cleared"`. The `dsl::Mutations` derive only const-asserts `kind == kebab(variant)` and `is_approved_verb(verb)` — the verb need NOT be the kind's first segment.
- 📌️ wfc3d rules are a DENY-LIST over an unordered tile pair (`relation: None` = every relation); a pair no rule mentions is admitted. Assembly was inconsistent (engine wiring read an allow-list, its outcome checker a deny-list); wfc3d is deny-list on both sides and its Python second implementation independently agrees.
- 🚧️ A5 gaps for W2/W3, all in `📓️wfc3d.md` §7: no `semio_repo_test_host` Rust adapter for `🧩️mutate-wfc3d-1` (oracle manifest + feature + Python reference ARE committed and green); taxonomy `members-of-*` rows not added (slice P owns `🔣️taxonomy.json` — the exact list is in §7.2); `verify taxonomy` / `artifact-field-parity` / `contract` not run (taxonomy was broken repo-wide for part of this slice, and the nx graph is stale); `TileMedia3d::MeshChild` carries no `#[child(kind)]` because the handle sits inside an enum inside a `Vec` (every bundled example uses inline `Mesh`); the preview solves inline per render instead of caching in an app transient.
- 14:2x A1 DONE and green. `semio-s-artifact-wfc-bitmap`: native check ✅️, `cargo test --lib` ✅️ **170 passed / 0 failed / 1 ignored**, `cargo clippy` ✅️ zero warnings from this crate, `wasm32-wasip2` ✅️, `python3 🐍️bitmap-oracle.py` ✅️ (10 vectors, 10 fixture cases, 25 JSON leaves, 0 problems), `bun nx run @semio-tech/wfc-js:test` ✅️ 9 files / 143 tests. Report: `📓️bitmap.md`.
- A1 delivered: 10 mutations with quintet + mounted test each, `s.wfc.bitmap.solve` inference (extract_2d → Grid2dTopology → resumable WfcJob → pixels/contradiction/entropy), Canvas2d input+output windows with per-instance window configs, read-only viewer, 2 examples (`rooms-16`, `flowers-24`), oracle manifest, mount contract in rust/py/ts, io txt+json.
- A1 found 4 real defects (all fixed, details in `📓️bitmap.md` §4): (a) `wfc-unsatisfiable` arrives as a job FAULT, not a commit-less completion — intercept it or a valid "no solution" reports as an inference failure; (b) a `CommitCandidate` carries TWO retained payloads, retire the one you do not keep in BOTH the Solve and Restore branches; (c) `terminal_is_empty` must report ownership only, never a local `closing` flag; (d) **one payload page per small write exhausts the 256-page operation budget** — the encoder must FILL a 16 KiB page before asking for another, and `fuel_per_step: 1` (assembly's value) must not be used in a headless `BatchJobSession`. (d) alone took the inference suite from ">60 s and failing" to 0.03 s.
- A1 note for peers: every mutation payload struct needs `#[value(rename_all = "camelCase")]` or a multi-word field serializes snake_case while its own JSON Schema says camelCase (found by the python oracle, invisible to the rust tests). `resize-*` diffs should carry the re-laid-out buffer explicitly, not re-derive it in `apply`, or the footprint law fails.
- A1 taxonomy: appended the nine new mutation directory names to `semanticDirectoryMemberKinds["members-of-schema"].memberNames` by hand (the `new mutation` scaffolder refuses a directory that already carries `🧪️tests`); `loadTaxonomy()` validates green afterwards. `verify taxonomy enforce` is RED for two repo-wide reasons outside this slice (unsorted compiler-input manifest; `frozen-coordinate-evidence-invalid` on `🧫️fixtures/📐️cad-draw-path-projection`) — it fails identically when scoped to untouched `🌀️procedural/🧩️assembly`.
- A1 for the plugin root: `BitmapEditor` carries a REAL `Transient` (`BitmapTransient`/`BitmapTransientMutation`); `Config` stays `NoConfig` and the per-pane state is WINDOW config (`BitmapInputWindowConfigOwner`, `BitmapOutputWindowConfigOwner`, registered via `register_window_config_owners`). `semio-framework-os-infinite` was dropped from the crate deps — it does not compile right now (`🌍️world/🦀️.rs:291`, a peer edit) and bitmap does not use Board2d.
- A1 final: record names aligned to the plugin-wide ruling (`pin-pixel` → verb `fix` / record `Fixed`, `unpin-pixel` → verb `clear` / record `Cleared`); re-verified after the rename — `cargo test --lib` 170 passed / 0 failed, `🐍️bitmap-oracle.py` PASSED. Ran every nx target with `NX_DAEMON=false` (the daemon graph is stale after the `🌊️wfc`→`🀄️wfc` move, as A5 reported).
- 15:1x **A5: wfc3d switched from deny-list to ALLOW-LIST rule semantics**, as ruled — rules now compile onto an EMPTY `ModelBuilder` (`allow` for `allowed: true`, `deny` for `allowed: false`, both directed orders, `relation: None` = every relation, deny wins at compile), so a tile pair no rule mentions is FORBIDDEN and an empty rule set is unsatisfiable the moment two slots are adjacent. Changed on BOTH sides: the engine wiring in `💡️inferences/🦀️.rs` (the `Rules` stage now lowers each rule to `(allowed, relation-or-all, a, b)` and the `Model` stage replays ONE rule per fuel unit, instead of enumerating every pair per relation) and the example-outcome checker. The three examples list their admitted pairs explicitly and all three still solve: `two-room-corridor` admits only room↔corridor (so both alternations remain and nothing else is reachable), `wall-roof-facade-strip` admits everything and then DENIES roof-beside-roof (deny-beats-allow is now load-bearing there), `tower-stack` admits cap only `above`, plus `pier-pier` everywhere with an `above` deny. Contradiction test now uses "one tile, no rule admits it beside itself"; a new test pins deny-beats-allow; the relation-scoping test asserts an admit does NOT reach another relation. Fixture cases renamed `🚦️forbids-two-rooms-side-by-side` / `🚫️drops-the-room-corridor-pairing`; all fixtures and example assets regenerated. The allow-list law is now stated in the Rust doc, the JSON Schema (normative), GraphQL, TS and proto mirrors. Re-verified GREEN: `cargo check --lib --tests`, `cargo clippy --lib --tests` (0 warnings), `RUST_MIN_STACK=33554432 cargo test --lib -- --test-threads=4` → **228 passed / 0 failed / 2 ignored**, `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2`, `NX_DAEMON=false bun nx run @semio-tech/wfc-js:test` → **10 files / 148 tests**, `python3 $T/🐍️wfc3d-oracle-runner.py` → 15/15 vectors + mount contract. The Python second implementation needed no change: it models the editing ALGEBRA, not the solve, and now says so.
- ✅️ **A3 AUDIT FIXES DONE, all green.** Both blocking gaps from `📓️audit-wfc2d.md` closed plus both minor ones. (1) `ArtifactEditor::render_with_request_context` is now overridden and threads the framework's own `TransientView<Wfc2dTransient>` into the preview — that is the ONLY render entry point whose signature carries the transient lane (`render` has no such parameter by trait design). It delegates to a free `render_with_transient(body_key, doc, cfg, transient)` so a test can drive the identical projection; `ArtifactInstanceOperationOwnerHandle`/`InteractionView` are unconstructible outside the framework crate (`InteractionView`'s fields are `pub(crate)`) and neither is read here. New test `the_host_render_path_paints_the_solved_assignment` asserts a solved board renders differently from an unsolved one through that path. (2) `Bitmap` tile media now paints REAL PIXELS: `🚪️io/📸️snapshot/💾️binary/🦀️.rs::tile_media_png_data_url` expands palette indices to RGBA and encodes through `s.stdio.png`'s own `encode_png`, emitted as a `kind:"image"` + `dataUrl` layer both previews share (no viewer→editor import). Malformed payloads fall back to the outline rather than failing the surface refresh. (3) New bundled example `🗺️terrain-ring` — the hex ring over four 8×8 palette-indexed raster tiles — plus the `change-tile-media` fixture vector now swaps a vector tile for a raster one, so the Rust, Python AND TypeScript oracles all replay `Bitmap` media. (4) The two bare `//` comments inside `impl Drop for Wfc2dSnapshotRetirement` folded into a `///` doc comment.
- ⚠️ A3 → anyone re-running a mutation generator: `🐍️wfc2d-mutations.py` still carried the UNAPPROVED `pin`/`unpin` verbs, because the `fix`/`clear` fix had only been hand-applied to the emitted files. Re-running it reintroduced `E0080: Mutations requires an approved semantic verb`. Fixed in the generator; if your slice has a generator too, check the same thing.
- ✅️ A3 re-verified after the fixes: `cargo check --lib --tests` GREEN (`check-10.log`), `cargo test --lib` **182 passed / 0 failed** (`test-final2.log`), `cargo clippy --lib --tests` GREEN 0 in-crate warnings (`clippy-3.log`), `cargo check --target wasm32-wasip2` GREEN (`wasm-2.log`), `bun nx run @semio-tech/wfc-js:test` **10 files / 148 tests GREEN** (`ts-4.log`), python oracle **15 kinds / 0 divergences**. New deps: `semio-s-artifact-stdio-png` (workspace) and `semio-framework-io-base64` (path, as `base64_codec` — remodel's precedent); both are framework/stdio, no app-plugin coupling.

- 15:0x A1 audit follow-up DONE, both `📓️audit-bitmap.md` items closed. `cargo check --lib --tests` ✅️, `cargo test --lib` ✅️ **176 passed / 0 failed / 1 ignored**, `cargo clippy` ✅️ zero warnings from this crate, `wasm32-wasip2` ✅️, `🐍️bitmap-oracle.py` ✅️ PASSED, `NX_DAEMON=false bun nx run @semio-tech/wfc-js:test` ✅️ 10 files / 148 tests. Logs: `🗑️generated/bitmap/{check,clippy,wasm,ts}-audit.txt`, `test-stroke-2.txt`.
- A1 (1) BLOCKING closed: `every_output_window_of_a_solved_example_occurs_in_the_symmetry_expanded_input` solves BOTH bundled examples through `BitmapSnapshot` → `s.wfc.bitmap.solve` and asserts every N×N output window occurs in the symmetry-expanded input set — wrap-around for the periodic example (`rooms-16`, 24×24, symmetry 8), in-bounds-only for the non-periodic one (`flowers-24`, 32×24, symmetry 2); plus `local_similarity_holds_under_a_different_seed_and_a_non_periodic_output`, which re-checks the law on a second seed and asserts the two seeds really disagree.
- 🚨️ A1 REAL BUG the new test found, relevant to every artifact that maps a `symmetry` COUNT onto D4: taking the first N of `Transform2d::ALL` (rotations then reflections) makes `symmetry = 2` mean "identity + quarter turn", and under it `flowers-24` could not tile its own declared output — the solve answered a CONTRADICTION. The classic reference order alternates (`e, m, r, rm, r², r²m, r³, r³m`), so `2` is a MIRROR. Bitmap now carries `BITMAP_SYMMETRY_ORDER` in that alternating order and the example tiles. **A2/A4 (grid2d/grid3d) and anyone else exposing a 1..=8 symmetry count should check which order they truncate.**
- A1 (2) closed: `stroke-begin`/`stroke-extend`/`stroke-commit` are now catalogued `Migrated` actions on `wfc-bitmap-input` (they were unreachable dead code). The in-flight box lives in the PANE window config — `handle` gets a `ConfigView` but no `TransientView`, so it is the only lane a command can write on pointer-down and read on release — and every mid-drag write carries one coalesce key (`wfc-bitmap-stroke`), so a 200-sample drag is ONE config edit and ZERO document ops. `BitmapEditor::stroke_mutation` is the pure half; the unit test drives begin→extend→extend→extend and asserts exactly one `set-input-pixels` at (2,3) sized 4×5 — the union region — filled with the armed colour. A box outside the sample or a colour off the palette is refused, never clipped.
- ✅️ **A5 wfc3d — `📓️audit-wfc3d.md` follow-up DONE, all four fixes, re-verified green.** (1) Headless drive raised to `fuel_per_step: 1 << 16` / `step_budget_us: 250_000` (grid3d's constants). (2) The two "box CENTRE" docstrings corrected to "MINIMUM corner" (`Slot3d`, `🚚️move-slot` module header). (3) `wfc-3d-preview`'s `instances_json`/`instances_delta_json` now OMIT unsolved slots instead of falling back to the placeholder — one record per SOLVED slot, matching grid3d; new tests `an_unsatisfiable_document_places_no_instances_and_says_so` (zero instances, `status_json` verdict still visible, surface still assembles) and `a_solved_slot_whose_tile_has_no_geometry_borrows_the_placeholder` (the one case the shared placeholder is actually for). (4) Every bare `//` comment inside a definition folded into `///` docs (the `step()` match arms and `impl Drop for Wfc3dSnapshotRetirement`); only `//#region` markers remain. Re-verified: `cargo check --lib --tests` ✅️, `cargo clippy --lib --tests` ✅️ **0 in-crate warnings** (`clippy-5.log`), `RUST_MIN_STACK=33554432 cargo test --lib -- --test-threads=4` ✅️ **229 passed / 0 failed / 2 ignored in 0.14 s** (`test-10.log`), `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2` ✅️ (`wasm-3.log`), `python3 $T/🐍️wfc3d-oracle-runner.py` ✅️ 15/15 + mount contract (`oracle-3.log`).
- 🚨️ **A5 → A4/A1 and anyone raising `fuel_per_step`: a `StepContext` grants exactly ONE payload page per STEP** (`payload_page_granted`), so EVERY stage that admits a page must END its step. wfc3d's `encode_one` admits one page per call and the encode arm looped over pages inside one step; with assembly's `fuel_per_step: 1` that never showed, because the fuel ended the step anyway. Raising the fuel exposed it: the second admission in a step is refused as `OpportunityExhausted`, and the resulting error's OWN fault detail cannot be admitted either, so the job dies as `StepOutcome::Fault` with an EMPTY payload — a completely MUTE failure. Symptom: every solve silently returns `Unsolved` (10/229 tests red, no message anywhere). Fix: the encode arm returns `StepOutcome::Yield` after each admitted page. **`grid3d` and `bitmap` already ship `65_536` / `250_000` — if either encodes its commit page-by-page inside the step loop rather than yielding per page, it has this bug right now.** Related but distinct from A1's (d) (the 256-page OPERATION budget); this one is the per-STEP grant.
- ⏱️ A5 note on the `250_000 µs` budget: it is ~31× `semio_framework_trace::INTERACTIVE_STEP_CEILING_US` (8 ms), against which every step is measured whatever budget its config asked for, and `SUSTAINED_OVERRUN_QUARANTINE_STEPS` (4) consecutive over-ceiling steps terminate the session with that same mute empty-page fault. wfc3d is safe because its step length is bounded by the preview cadence (16 units) and by the one-page-per-step encode, NOT by this deadline — but the deadline is not what keeps steps short. Any artifact that lets a single step run to it will be quarantined.
- ⚠️ A5 → A3: `NX_DAEMON=false bun nx run @semio-tech/wfc-js:test` is now **147/148** — the one failure is `◻️2d/…/🧬️schema/🧪️tests/🧩️suite/🟦️.ts > wfc2d mutation vectors > ships one fixture directory per declared kind`, which compares fixture DIRECTORY names against declared kinds and reads `↔️move-slot` where it wants `move-slot` (the emoji prefix is not stripped). All 9 wfc3d-owned test files are green; not touched here since it is A3's folder.
- 15:4x COORDINATOR: fixed wfc2d's TS fixture-oracle test (`🧬️schema/🧪️tests/🧩️suite/🟦️.ts`) — it compared raw fixture DIRECTORY names (`↔️move-slot`) with declared kinds; it now strips the emoji prefix (`kindOf`). `NX_DAEMON=false bun nx run @semio-tech/wfc-js:test` re-run below.
- 15:5x COORDINATOR: `@semio-tech/wfc-js:test` → 10 files / **148 passed / 0 failed** after the fixture-oracle fix.

## G gates
Report: `📓️gates.md` (every command with its result, what I fixed, what remains red and whose it is). Logs: `🗑️generated/gates/`. `bun nx reset` ran first, so nothing below was read through the stale post-move graph; every cargo command foreground, `-j 4`, no exit-137 retry needed.
- 🗑️ **`🧩️assembly` is DELETED.** `cargo metadata` refreshed `Cargo.lock`; `schema generate` + `schema docs` dropped all 10 `s.procedural.assembly.1.any*` keys from BOTH `🔣️schema-catalog.json` and `📓️schema-catalog.md` (3 200 scopes, 77 of them `s.wfc.*`); `cargo check -p semio-s-plugin-procedural --lib` 🟢. Six prose stragglers fixed (`🌳️surface-scaffold/🟦️.ts:90`, and the `DIFF_ALIASES` docstring in cad/procedural×2/lowpoly/puzzle×2 — all now name `🀄️wfc`, which I verified is truthful: the wfc graph diffs really do split into `<name>Removed`/`<name>Upserted`). Taxonomy member `🧩️appends-slot-c-at-index-2` removed: no wfc artifact uses that case name. The demonstrator `dist/**` hits are build output and `🧪️demonstratorcompileclosure/🟦️.ts:26` is a NEGATIVE assertion the deletion makes true.
- 🔣️ **Taxonomy: 155 wfc-unique member rows added** — `members-of-tests` +67 (the five `mutate-*-1` ids, `🧩️mount-contract`, every mutation-fixture case name), `members-of-engine` +36, `members-of-schema` +28, `members-of-examples` +10, `members-of-assets` +6, `members-of-fixtures` +4, `members-of-windows` +4. `loadTaxonomy()` probed green after. NOT added, deliberately: the generic names `🔬️unit`/`🧩️example`/`🧩️outcome`/`🗄️store-fixture`/`🚪️close-ladder`/`😴️idle-turns`/`🔬️surface`/`🔬️boot-deadline`/`🧩️suite`/`🪟️window` that `📓️wfc3d.md` §7.2 also lists — puzzle, note, remodel and procedural all carry those unregistered today, so `memberNames` is not where they resolve and adding them would change classification repo-wide. Calibration for anyone reading a raw count: the same scan reports 328 unregistered names for puzzle and 257 for remodel.
- 🚨️ **A THIRD inadmissible path emoji, after `▦️grid2d` and `🌊️wfc`: `⬡️hex-ring`.** `⬡` (U+2B21) is not Extended_Pictographic, so `loadTaxonomy()` threw the moment the row was registered. Renamed `◻️2d/…/📚️examples/⬡️hex-ring` → `🔷️hex-ring` (crate-root `#[path]` + the example's own docstrings repointed). Also removed an EMPTY `🔲️grid2d/…/🪟️windows/▦️grid` that survived A2's rename still carrying `▦️`.
- 🧹️ **`plugin-registry:check`: wfc 78 → 54 findings.** Fixed structurally: 154 missing mode/window facet folders across bitmap/grid2d/wfc2d/grid3d (each with the scaffolder's `📌️.empty.md`, the shape A5 and puzzle already ship); the six missing example `🟦️.ts` metadata twins; and **wfc2d's fifteen emoji-less `🧫️fixtures/🧬️mutations/` directories** (`change-seed`, `create-slot`, …) renamed to their `🧬️schema` siblings' emoji names, with the 16 referring files rewritten — `🐍️wfc2d-mutations.py` fixed at the source (it wrote `FIX / semantic / case`, must be `spec["dirname"]`), and `cargo test -p semio-s-artifact-wfc-2d --features component-app-assembly --lib` re-verified **182 passed / 0 failed**. I did NOT run `new surface --all`: it walks every owned subset repo-wide and even the per-subset form wants to create a spurious `🪟️main` window no wfc artifact has. The remaining 54 are all repo-endemic categories (window `🧪️tests` child 429 repo-wide, `👥️presence/🧬️schema` 251, `🎚️config/🧬️schema` 249, plugin-root `🎮️commands/🦀️.rs` 33) plus the 6 `🖼️assets/` ones that are A3's DECLARED deviation — full table with repo-wide counts in `📓️gates.md` §3.1.
- ✅️ **Plugin crate GREEN.** `cargo check -p semio-s-plugin-wfc --lib --tests` 🟢; `cargo test --lib` **14/14**; `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2` 🟢; `describe` 🟢 (10 apps, 2 window kinds per editor, `defaultLayout` on all 10, 13 top-level manifest examples — the same shape puzzle ships); `plugin-registry:generate` 🟢; `NX_DAEMON=false @semio-tech/wfc-js:test` **10 files / 148 tests** 🟢. Two plugin-root defects fixed, no artifact API touched: (1) `🔲️grid2d` nests its roster one level deeper, so the surface test now calls `examples::grid2d::example_source_slice()`; (2) `every_wfc_inference_route_names_its_own_artifact_kind` asserted `artifact_kind().id` (`2d.wfcbitmap`) where all five artifacts — and cad and puzzle — correctly put the DIALECT kind, which is what `ArtifactInferenceServiceRegistry` keys on; the law now asserts each crate's `*_DOCUMENT_SCHEMA`. A3's `Wfc2dConfig`/`Wfc2dTransient` remark needed no root change — the `VcsArtifactApp<EditorApp<…>>` generic carries them.
- ⏱️ **`boot_deadline` 1/1 🟢 and `idle_turns` 2/2 🟢 — both were RED on a plugin-root test defect I fixed.** Both looped over five editors reusing ONE instance id while leaking each runtime (`mem::forget`, because the lifecycle registry's drop authority aborts on an unwind), so the fixed instance-metadata authority answered `plugin.instance-metadata-capacity: saturated or collided` on the second editor. Each editor now has its own id. bitmap boots at `open_us=10051 describe_us=17150 descriptor_bytes=173386 settle_turns=1`; the idle law measures 409 B over 512 turns (0 B/turn) against a 65 536 B ceiling.
- 🔴️ **BLOCKING, for A1/A2/A3/A4/A5 and W3 — `close_ladder` is 0 of 6: EVERY wfc editor's instance close faults, identically.** `plugin.internal.prior-outcome` — "a prior step outcome faulted or was cancelled" — which `🔌️plugin/🦀️.rs:35009` raises when the APP's own `close_step` returned `StepOutcome::Fault`/`Cancelled` inside the guest; then `⚛️reactor/🚪️lifetime/🦀️.rs:489` "runtime lifetimes require terminal exact ACK before teardown" panics in the destructor and SIGABRTs the binary. Four things it is NOT, each checked rather than assumed: **not the framework** (`cargo test -p semio-s-plugin-procedural --test close_ladder` is **3/3 green right now**, same helper, same `VcsArtifactApp<EditorApp<…>>` wrapper); **not the harness** (reran bitmap as a plain `async_test` without the file's own 256 MiB thread — identical fault); **not the instance-id collision above** (one law alone on a fresh process fails the same way, different fault code); **not the plugin root** (`🀄️wfc/🦀️.rs` matches procedural's and puzzle's line for line in every close-relevant respect). So it is artifact-side and uniform — the fourth member of the family A2/A3/A5 documented, but on the EDITOR INSTANCE close path rather than the inference job, which is why every artifact's own `--lib` suite is green and only the plugin lane sees it. I split the one looping law into five per-artifact `#[test]`s so the abort can no longer hide four of the five; reproduce with `RUST_MIN_STACK=33554432 cargo test -p semio-s-plugin-wfc --test close_ladder -j 4 -- --test-threads=1 <artifact>_editor_instance_close_reaches_retired`. Logs `🗑️generated/gates/close-ladder-<artifact>.log`. The cost law never reaches its assertions, so the close-ladder fixture's ceiling/dilution are still the inherited procedural numbers.
- 🔧️ **`verify taxonomy`: I cleared P's fault (1) but fault (2) is deeper, and the fix belongs to the react-build-tooling owner.** The gitignored `🧑‍💻dev/📤️distribution/🧾️manifest.json` is sorted now (10 of 3 030 rows were out of order; backup in `🗑️generated/gates/`). The gate then hits `compiler input bytes differ: bun.lock`, and I measured why: **1 281 of its 3 030 rows point at paths that no longer exist and 283 more differ in bytes** — only a regeneration fixes that, and `bun nx run @semio-tech/framework-os-dev:generate-distribution` dies in 2 s on `Missing or unsafe distribution static input: 🧰️framework/🔨️modules/🖱️ui/📦️packages/🟦️typescript/🎯️targets/⚛️react/🏗️build-tooling.ts`, a path the TRACKED `🧑‍💻dev/🚚️distribution/🔗️inputs.json:11` still declares although the file moved to `🧰️framework/🔨️modules/🖱️ui/🎯️targets/⚛️react/🛠️build-tooling/🟦️.ts`. **Repointing `🔗️inputs.json` unblocks the taxonomy gate for the whole fleet.** The cad frozen-digest fault was not touched (other owner) and sits behind this one in the same abort chain.
- ⛔️ **`bun ./📜️script.ts policy` cannot run in this tree at all**, so its wfc findings are UNKNOWN and the three comment-inside-definition sites in the brief were left alone rather than rewritten blind. It resolves entities through the repo CLI binary `💻️client/client`, which is absent, and `bun nx run @semio-tech/repo-client:build` fails with `stat …/💻️client/⌨️cli/cmd/repo: directory not found` — the Go `main` package the build script names does not exist on disk. Same missing binary as the failed `repo`/`semio` MCP connections this session. **Owner: the repo-client module.**
- 🕸️ `verify dependencies write-baseline` 🟢 (230 third-party). `literal-external` RED repo-wide (229 vs target 0, 15 oracle conflicts) with **no wfc-specific row**: wfc appears only as 4 of the 131 owners in the long-standing `rust:serde_json` oracle conflict, the same way every peer does.
- 16:3x COORDINATOR: W2 playground wave. All five variants activated (`📜️activate-wfc-react.sh`, component-dev 31 s, logs `🗑️generated/playground/activate-*.log`); dev servers started from this session (`📜️serve-wfc-react.sh`): bitmap 6041, grid2d 6042, wfc2d 6043, grid3d 6044, wfc3d 6045. Bitmap boot probe: shell ready, both windows mount a canvas, no faults; input bitmap renders; OUTPUT is a uniform dark square (no solve reaches the live app) and `setActiveExample` is refused as undeclared. Launched B1–B5 (one Opus agent per variant: setActiveExample, live solve, interact/viewer probes) and L (close-ladder fault). Agents append `RESTART SERVE <variant>` here when a server restart is needed.

## B4 grid3d playground
- Boot probe (`🐍️grid3d-console-dump-probe.mjs`, out `🗑️generated/playground-grid3d/boot/`) against the coordinator's serve on 6044: shell READY (`data-semio-os-ready=grid3d`), both World3d hosts mount, **grid 48 instances / 3 meshes, preview 47 instances / 4 meshes, status `{state:"solved",solved:47,cells:47,seed:7}`** — the inline per-render solve DOES run in the browser and both canvases paint (see `boot/final.png`; the in-page canvas readback is a WebGPU false negative, the screenshot is the oracle). No `interactive-ceiling`/quarantine fault: the solve stays inside the budget, so no tool-run migration is needed.
- Baseline interact probe (`🗑️generated/playground-grid3d/interact-baseline/`) found the editor **completely non-interactive**, four real faults:
  1. `setActiveExample refused: undeclared-action` — no window kind declared it (the coordinator's prediction, confirmed).
  2. `setActiveTile refused: dispatch-failed — action 'setActiveTile' is not a framework-reserved action … dispatched exclusively through the typed command channel` — grid3d had **no `command_from_action`**, so the trait default refused EVERY one of the 17 declared window actions. The whole action vocabulary was dead in the app.
  3. `interaction selection lost reason=validate-state-pruned dispatched=…wfc.grid3d.cells=handle:0:0:1 validated=…cell:0:0:1` — the scene set `domain_id` but not `domain_granularity_id`, so the host picked with the `handle` granularity the domain never declares and every pick was pruned.
  4. After the first dispatch failure the instance poisons: every later action answers `plugin.internal.prior-outcome: runtime live cleanup faulted for instance …` — one bad action kills undo/camera/hover for the rest of the session.
- Fixes landed in `✏️s/🔌️plugins/🀄️wfc/🗿️artifacts/🧱️grid3d/…/✏️editor/`: `Grid3dEditorCommand::SetActiveExample`, a full `command_from_action` bridge + `command_id`, `reset_document_effect`/`example_snapshot` + `build_document_store_initialization_job` (the trait default REFUSES the `LoadDocument` envelope), `setActiveExample`/`worldSelect` declared on the grid window, staged argument forms for the cell verbs, and `grid::render` now picks its pick lane from the ARMED UTILITY (domain for `select`, plugin-private `worldSelect` for `pin`/`mask`, granularity always `cell`).
- BLOCKED on a peer's in-flight breakage: `cargo check` cannot get past `🧰️framework/…/♾️infinite/🌍️world/🦀️.rs` `error[E0502]: cannot borrow *state as mutable … take_reference_underlay_upload(state, url)` (the line number moved 11211 → 11260 → 11284 → 11301 across four runs, so it is actively being edited). Polling; nothing in grid3d can fix it.
- 16:5x COORDINATOR: a peer's uncommitted edit of `🧰️framework/🛍️products/💻️os/🔨️modules/♾️infinite/🌍️world/🦀️.rs` (mtime 15:50, +235/−24 lines) left `semio-framework-os-infinite` failing with E0502 (`for reference in &state.references` while calling `take_reference_underlay_upload(state, url)`), blocking grid2d/wfc2d/grid3d checks and every wfc wasm activation. Applied the minimal fix: the loop now iterates a pre-collected `Vec<(url, origin, width)>` of visible references so the mutable helper call is legal. Re-check result below.
- 16:5x COORDINATOR: `cargo check -p semio-framework-os-infinite --lib` GREEN after the fix (4.9 s). B2/B3/B4 unblocked.
- The `♾️infinite` breakage cleared (thanks). `cargo check -p semio-s-artifact-wfc-grid3d --features component-app-assembly --lib --tests -j 4` ✅️ and `RUST_MIN_STACK=33554432 cargo test … --lib -j 4` ✅️ **206 passed, 0 failed, 2 ignored** (`🗑️generated/playground-grid3d/{check-10,test-3}.log`), five of them new laws for this slice.
- Now blocked on slice A5: `cargo check -p semio-s-plugin-wfc --lib` and `activate-grid3d-react-dev` both die in `semio-s-artifact-wfc-3d` (`WFC_3D_RETAINED_TOOL_IDS` / `WFC_3D_SET_ACTIVE_EXAMPLE` / `WFC_3D_NODE_GRAPH_EDIT` not found, then `ArtifactCommandWorkStep::Complete has no field named emit`). The wasm component is shared by all five variants, so grid3d cannot re-stage until `🧊️3d` compiles. Polling; grid3d's own crate is green.
- **Fault 5, found only after re-staging**: with the action bridge in place every verb then answered `interactive-job.missing-factory` — "typed command 'setActiveExample' has no exact controller/owner/factory/tool/schema proof". `Migrated` is the ONLY live classification, so every dispatch runs the retained route, and grid3d had no `TOOL_JOB_IDS`, no publication contracts, no `bounded_first_step_tool_proofs!` and no factory at all. Ported the sibling `🧊️3d` recipe. Complication `🧊️3d` does not have: `ArtifactCommandInputs` carries no `ViewModel`, and grid3d's pick verbs need the armed utility while `setActiveTile`/`setCamera` address one window INSTANCE's config — so the reducer was split into a free `grid3d_command_emit(command, doc, cfg, view_state)` that BOTH routes call, the retained one reading `view_state`/`window_config` off `ArtifactOwnedToolJobContext` (`🗒️note`'s shape). `worldSelect` became its own command variant, because `dispatch_typed_command_inner` rejects a command whose `command_id` is not the action it was admitted under.

## B2 grid2d playground
- 🚨️ **FLEET-WIDE CARGO DEADLOCK, broken and worth knowing about.** For ~15 minutes every cargo in the machine (12 processes: grid2d/grid3d/bitmap/wfc2d/wfc-3d/remodel/layout/wgpu tests plus `plugin-describe`) sat at `Blocking waiting for file lock on artifact directory` with **zero rustc processes alive** — `sample` showed all of them inside `cargo::core::compiler::prebuild_lock_exclusive` / `util::flock::acquire` on `⚡️cache/cargo/target/debug/.cargo-lock`, i.e. the fine-grain-locking deadlock, not a slow build. **Killing ONE participant (my own queued `cargo test`) released the cycle** and rustc restarted within seconds. If the fleet stalls again: `ps aux | grep "[c]argo "`, confirm `ps aux | grep -c "[r]ustc"` is 0, then kill your own cargo first before touching anyone else's.
- Boot probe #1 (`🐍️wfc-console-dump-probe.mjs`, out `🗑️generated/playground-grid2d/grid2d-boot/`) on the coordinator's serve at 6042 — **three faults**: (1) the `grid` window never painted at all, `Board2dHost` threw *"The current app has no registered board session factory."*, so only the preview host mounted; (2) `setActiveExample refused: undeclared-action`; (3) a 404 on one resource.
- 🖥️ **Board2d is unreachable for any plugin but puzzle, and that is a framework fact, not a wfc bug.** `Board2dHost` resolves its session through `resolveAppSurfaceSessionFactory(registrations, {pluginId, appId})`, and the ONLY `kind: "board-2d"` registrations in the repo are `PUZZLE_BOARD_SESSION_FACTORIES` (puzzle's own wasm-bindgen `BoardSession` cdylib), wired into `🧰️framework/…/🧑‍💻dev/🟦️.ts` as the dev shell's single `surfaceSessionFactories` argument. A foreign `Board2d` pane therefore throws before it paints. `🧱️block`'s three board windows are dead for exactly the same reason. **Fix taken: `wfc-grid2d-grid` is a `Canvas2d` pane now** — the same host the preview already used, no per-plugin wasm session needed — with the click resolved by inverting `Canvas2dHost`'s own `screenToWorldLogical` and dividing by the authored cell size.
- 🎯️ **The real reason NOTHING in grid2d was clickable — the same fault B4 found in grid3d, one layer deeper.** After declaring `setActiveExample` the refusal only changed shape: `dispatch-failed — typed command 'setActiveExample' has no exact controller/owner/factory/tool/schema proof`. `Grid2dEditorCommand`'s hand-written `impl protocol::OpBinary` never overrode `TOOL_JOB_IDS`, so it inherited the trait default `["typed-command"]`; `validate_tool_job_rows` then computes `expected = TOOL_JOB_IDS ∩ migrated = {}`, no bounded proof is registered, and `qualified_tool_proof` refuses EVERY app verb with `interactive-job.missing-factory`. All 27 verbs — the fourteen mutations included — were dispatch-dead in the browser while every unit test passed. Ported the sibling `🖼️bitmap` recipe: `GRID2D_TOOL_IDS` (27) as `TOOL_JOB_IDS`, `grid2d_command_id`, `GRID2D_PUBLICATION_CONTRACTS` (Artifact ×16 / WindowConfig ×7 / HostOnly ×4), a `Grid2dCommandWork` + `Grid2dCommandJobFactory`, `bounded_first_step_tool_proofs!`, `register_tool_job_factories`, `build_tool_job`, `command_id`, and `build_artifact_store_one_item_preparation_factory` (without which every Artifact-lane verb answers `publication-authority-missing`). `handle`'s body moved into a public `Grid2dEditor::dispatch` both routes call.
- ⚠️ **Two action ids may never share one command variant.** `dispatch_typed_command_inner` refuses a command whose `command_id` is not the action it was admitted under, so the host's `setCamera` (nested `{camera:{x,y,zoom}}`) got its own `SyncCamera` variant beside the palette's flat `set-camera`. Same trap B4 hit with `worldSelect`.
- 👁️ **`ArtifactViewer` has NO `build_document_store_initialization_job` and no `build_artifact_store_one_item_preparation_factory`** (heads-up for B5 — `🧊️3d/…/👁️viewer/🦀️.rs` is failing `cargo check -p semio-s-plugin-wfc` on exactly those two `E0407`s right now). So a viewer structurally cannot admit a `LoadDocument` envelope: grid2d's viewer declares `setActiveExample` + the five canvas verbs with its own `ViewerApp` proof catalog so the boot announcement and every hover are ACCEPTED, but the example switch is inert there and validates the id only. Switching examples is an editor gesture.

## L close-ladder
Report: `📓️close-ladder.md` (root cause with file:line, the fix, every command + result). Logs: `🗑️generated/close-ladder/`. Foreground, `-j 4`, one cargo at a time, no git-modifying command.
- ✅️ **`close_ladder` is 11 of 11** (was 0 of 6): `cargo test -p semio-s-plugin-wfc --test close_ladder -j 4 -- --test-threads=1` → `ok. 11 passed; 0 failed`. The cost law now REACHES its assertions: `costs=[("cold",22,0),("warm",20,6),("long",31,32)]`, ceiling 768, measured dilution **344 %** against the required 140 %.
- 🔍️ **`plugin.internal.prior-outcome` was a messenger, not the fault.** The app's own fault detail is copied into `RuntimeCloseWorkerState::last_fault` only under the FRAMEWORK crate's `#[cfg(test)]`, i.e. never when the framework is a dependency — which is why five slices read the code and learned nothing. I temporarily printed `fault.detail.single_page()` in `runtime_close_cleanup_pump_one` (`🔌️plugin/🦀️.rs`), ran the lane, and reverted: `git diff` on that file is empty. The detail was `app owner did not provide the required bounded disposer for document-store` (`🔌️plugin/🦀️.rs:15404`).
- 🎯️ **ROOT CAUSE, artifact-side and uniform, as predicted: the five editors declared their stores and nothing about RELEASING them.** `VcsArtifactApp::close_step` (`🔌️plugin/🦀️.rs:29174-29182`) walks eight owned store lanes — document, config, draft, presence, transient, window-config, window-transient, interaction — and `drive_artifact_owned_disposer` hard-faults the WHOLE close the moment a lane's `ArtifactDisposal<T>` is `None`. `EditorApp<E>` forwards those to `E::build_*_store_disposer()`, whose `ArtifactEditor` defaults (`🔌️plugin/🦀️.rs:31586-31605`) are all `None`. Peeling that exposed two more fail-closed gates behind it: `artifact store has no owner-supplied bounded disposer` (`🏪️store/🦀️.rs:16036` — the disposer is driven THROUGH the store's own owner catalog, so `build_*_store_owners` and `build_*_store_disposer` are one declaration in two halves) and `presence close requires its installed local-root retirement factory`.
- 🧩️ **NOT a framework bug**, proven by procedural's green lane: procedural's `🌀️generation2d/…/✏️editor/🦀️.rs:1333-1414` and puzzle's `◻️2d/…/✏️editor/🦀️.rs:4889-4936` both make the full declaration, and every editor in the repo that has ever been closed does. Failing closed on an absent disposer is the intended behaviour — an unbounded `Drop` on a live store is the exact regression the ladder exists to catch. The only framework shortcoming is DIAGNOSTIC (the `#[cfg(test)]`-gated fault capture above); promoting it touches the shared `🔌️plugin/🦀️.rs` for every plugin, so it is recorded for the framework owner, not changed.
- 🛠️ **The fix, same in all five, framework helpers only — no hand-written disposer.** Per editor: `build_{document,config,draft}_store_owners` (`bounded_document_store_owners` / `bounded_config_store_owners` / `bounded_document_store_owners::<NoDraft, NoDraftMutation>`), `build_{document,config}_store_disposer` (`bounded_*`), `build_draft_store_disposer` (`no_draft_store_disposer`), `build_presence_store_disposer` + `build_presence_{local_root,peer}_retirement_factory` (the `no_presence_*` trio — `Presence` is `NoPresence` on all ten surfaces), and the transient pair adapted per crate: **bounded** for `🖼️bitmap` and `◻️2d` (they carry a real transient root), `no_transient_*` for `🔲️grid2d`/`🧱️grid3d`/`🧊️3d`. Where a B slice had already landed one of these (B1: bitmap's transient disposer + root factory; B3: wfc2d's root factory) I kept theirs and added only the missing hooks — the patch is idempotent and I re-ran it after every peer write to the same files.
- 👁️ **The five VIEWERS had the identical defect and now have a law.** They close through the same eight-lane ladder and declared the same nothing, so fixing only the editors would have left five production surfaces that abort the guest on close with nothing watching. Same nine hooks per viewer (`ViewerApp<V>` supplies the draft lane itself), plus `WFC_VIEWERS` + `viewer_close_reaches_retired` + five `wfc_viewer_close_ladder_law!` rows in `🧪️tests/🚪️close-ladder/🦀️.rs`, in exactly the per-artifact shape G gave the editor laws. All five green. **@B slices: do not re-add these hooks; they are in both `✏️editor/🦀️.rs` and `👁️viewer/🦀️.rs` of all five artifacts already.**
- 🧹️ **grid3d's three flagged comments-inside-definitions are gone**: the two in-body blocks in `💡️inferences/🦀️.rs` are now the docstring of `impl InteractiveJob for Grid3dInferenceJob :: fn step`; the `ManuallyDrop` warning in `📸️snapshot/💾️binary/🦀️.rs` moved onto `fn drop`; the crate root's `// ---- Shims … ----` became a docstring on `pub mod schema`. The same `⚠️ ManuallyDrop` comment exists verbatim in the other four artifacts' `💾️binary/🦀️.rs` — the brief named grid3d only, so they are untouched and flagged here.
- ✅️ Rest of the ladder: bitmap `--lib` **185/0**, grid2d **209/0**, wfc2d **183/0**, wfc3d **237/0**, `--test boot_deadline` **1/1**, `--test idle_turns` **2/2**, `CARGO_PROFILE_WASM_DEV_DEBUG=false cargo check --target wasm32-wasip2` 🟢️ (75 warnings, so expansion really ran), `cargo check -p semio-s-plugin-wfc --lib --tests` 🟢️.
- 🔴️ **Two reds that are NOT mine, with evidence.** (1) `semio-s-artifact-wfc-grid3d --lib` **204 passed / 2 failed**, both B4's in-flight `worldSelect` bridge: the test file (`✏️editor/🧪️tests/🔬️unit/🦀️.rs`, mtime 16:00) still asserts `worldSelect` is an ALIAS of `pickCell`, the editor it tests (`✏️editor/🦀️.rs`, mtime 16:11) now bridges it to a `WorldSelect` command of its own — `command_id mismatch for action worldSelect: left "worldSelect", right "pickCell"`. Nothing in the close path touches action bridging. **Owner: B4.** (2) `semio-s-plugin-wfc --lib` **13/1**, the one red being `descriptor_is_fresh`: the B wave added window actions/commands/labels after G's 15:30 `describe`. Store owners and disposers are runtime-only and appear nowhere in the descriptor, so this slice cannot have staled it; `describe` costs ~5 min and would stale again while B* are live. **Owner: coordinator/G once the B wave settles.**
- ⏳️ Three cargo runs died on a peer's in-flight `🧰️framework/…/♾️infinite/🌍️world/🦀️.rs` breakage (`E0433 tool_run`, then twice `E0502 take_reference_underlay_upload(state, url)`) — the same wall B4 hit. I polled its mtime until quiet for ~2.5 min and the runs went through.

## B5 wfc3d playground
- Report: `📓️playground-wfc3d.md`. Probes `🐍️wfc3d-{console-dump,interact,viewer}-probe.mjs`, outputs + every cargo/nx log under `🗑️generated/playground-wfc3d/`. No server was started or restarted by this slice (the coordinator's serve on 6045 answered throughout).
- **The solve was never the problem**: the very first boot probe showed the preview at 3 meshes / **3 instances** / `Solved 3 of 3 slots.`, inline per render, with no `interactive-ceiling` and no quarantine over hundreds of repaints. Everything the user could *touch* was dead.
- Baseline faults (`🗑️generated/playground-wfc3d/interact-baseline/`), all four confirmed live: (1) `setActiveExample refused: undeclared-action`; (2) `change-seed refused: dispatch-failed — not a framework-reserved action` — **no `command_from_action` at all, so every one of the nine declared verbs was dead**, not just the picker; (3) `interactionHover`/`interactionSelect` refused on every pointer move (no interaction domain declared); (4) drag/wire gestures reached the plugin as nothing. Fault 5 appeared only after fixing 1–2: `interactive-job.missing-factory — typed command 'setActiveExample' has no exact controller/owner/factory/tool/schema proof` (same wall B4 hit).
- Landed in `🧊️3d`: full `command_from_action` (21 verbs, `DslValue`-typed) + `command_id`; the exact-owner retained chain (`TOOL_JOB_IDS`, publication contracts, `Wfc3dCommandWork`, `Wfc3dRetainedCommandJobFactory`, `register_tool_job_factories`, `build_tool_job`, `bounded_first_step_tool_proofs!`) ported from B3's `◻️2d` recipe; artifact + config one-item preparation factories; `SetActiveExample` → `Effect::LoadDocument` + `build_document_store_initialization_job`; `NodeGraphEdit`/`NodeGraphViewport` verbs; one `InteractionDefinition` (`slot`) + `interaction_topology`.
- **`cargo test -p semio-s-artifact-wfc-3d --features component-app-assembly --lib -j 4` ✅️ 239 passed / 0 failed / 2 ignored** (`test-10.txt`), eleven new laws. `cargo check -p semio-s-plugin-wfc --lib` ✅️ (`check-plugin-final.txt`). `activate-wfc3d-react-dev` ✅️ (`activate-7.txt`).
- **Live, measured**: boot probe **0 fault lines**; interact probe **10 steps, 0 failing**; viewer probe pass (1 canvas, 3 meshes, 3 instances, `Solved 3 of 3 slots.`). All three examples load and solve through the picker (3/3, 4/4, 5/5 with real positions). **A node drag lands exactly one `move-slot` in document units with `z` kept** — dragging `room-a` 240 canvas px moved its instance `@0,0,0` → **`@0,2,0`**. A wire gesture reaches the document and **undo reverts both gesture edits** (`room-a` back to `@0,0,0`, `room-b` back to `tile:corridor`).
- 🐛️ Two real GL bugs fixed in the preview's mesh catalogue, found by reading the live console: `normals` was shipped EMPTY (the host binds that attribute at item size 3, so every instanced draw died with `glDrawElements: Vertex buffer is not big enough` — the solved boxes reached the pane as bare edges, hundreds of errors per run) and `colors` was RGBA where the host binds RGB. Also decoupled the preview's world camera from `camera_zoom`: the two panes share one config, so a pan on the GRAPH canvas used to warp the 3d view.
- 📏️ `WFC_3D_GRAPH_UNIT = 120.0` in `✏️editor/🦀️.rs` (+ its exact inverse `slot_coordinate`): a slot is a box in METRES, so the graph pane opened on three one-pixel nodes. Caller-side on purpose.
- 🔭️ **B3 / W2 — three things wfc2d needs**: (a) the same graph scale, or its pane renders the same smudge; (b) `wfc2d_node_graph_edit` handles only `move`/`connect`, which the **wasm** surface never sends — it commits `{operation:"setHostSnapshot", hostSnapshotJson}`, so wfc2d's drags reach no document (see `host_snapshot_mutations` in wfc3d); (c) key edge removals on the **endpoint pair, never the edge id** — the canvas mints its own wire ids, and an id-keyed diff silently disconnected the whole graph. Thanks for `graphEditSignature`/`commitGraphFixtureIfEdited` in `🕸️NodeGraph/🟦️.tsx` — without it no wfc drag committed at all. I re-mirrored `🕸️graph/🦀️.rs` (+ its tests) from wfc2d after your argument-form change; `diff` is empty. wfc3d's `move-slot`/`resize-slot` command now carries `Option<f64>` for `z`/`depth` so your 2d form cannot flatten a 3d stack.
- 🕳️ **Not proven live**: a wire gesture landing a `connect-slots` specifically. The gesture channel is live (the drag is exact, and a wire gesture produces a real, undoable adjacency edit), and the lowering is pinned by unit tests over the exact commit payload — but a headless pointer cannot address the wasm canvas' port hotspot (centre ±11/±14/±19 all grab the node body or the canvas) and the surface exposes no DOM/hover read-back for it. Suggested W2 follow-up: mirror `pickTargetsAtScreenJson` onto the host element (or add `data-hover-json`), which makes every canvas gesture addressable by every probe in the repo.
- 🩹️ Peer note: `activate-wfc3d-react-dev` failed twice mid-slice inside `🧱️grid3d`/`◻️2d`/`🖼️bitmap` while those slices were mid-edit (shared wasm component). Retried until green; nothing in `🧊️3d` was involved.
- 📇️ Descriptor note for G/W2: this slice ADDED window actions (`setActiveExample`, `nodeGraphEdit`, `nodeGraphViewport`, the six tile/rule verbs) and their argument forms to `s.wfc.wfc3d@1/*#editor`, so `🔣️.json` is staler than before — `describe` + `plugin-registry:generate` must run once the B wave settles (pitfall #9). The live playground reads the wasm's own manifest, which is why the picker and the Actions pane are green here regardless.

## B1 bitmap playground
Report: `📓️playground-bitmap.md`. Probes: `🐍️bitmap-{console-dump,interact,viewer}-probe.mjs`. Logs/PNGs: `🗑️generated/playground-bitmap/`. Foreground, `-j 4`, one cargo at a time, no git-modifying command, no server started or killed by me.
- ✅️ **The bitmap editor works end to end in the react shell.** Boot probe **0 fault lines** (the `setActiveExample refused: undeclared-action` warning is gone); the interact probe runs seven steps with an empty `faultLines()` on six of them; the viewer probe renders both windows.
- 🧩️ **`solve` now paints a real collapse.** Output census before: one flat `#171a1f` extent at 5.2 %. After `Solve` on `rooms-16`: walls/floor/doors in the input's own palette (`interact/3-solve.png`); after switching to `flowers-24` and solving again: stems with petal crowns on the ground band under sky (`interact/8-solve-second-example.png`). Locally similar to each input, as the brief asks.
- 🖌️ **Interactivity proven**: `set-active-color 2` → `stroke-begin 6,6` → `stroke-extend 9,9` → `stroke-commit` paints ONE 4 × 4 orange block (`region-after-paint-input.png`), `mod+z` reverts it on the FIRST press (`region-after-undo-1-input.png`), `pin-pixel 3,3` marks the output (`region-after-pin-output.png`), the navbar combobox really replaces the document.
- 🔴️ **Six real defects, each hiding the next** (full anatomy in the report): (1) NO `command_from_action` — the framework resolves every host action through it and the default is a hard `app.command.unsupported`, so the whole classified `Migrated` roster was dispatch-dead; (2) no app-owned retained factory → `interactive-job.missing-owned-reducer`, and inside that, `OpBinary::TOOL_JOB_IDS` still at its `["typed-command"]` DEFAULT makes `validate_tool_job_rows` expect an EMPTY set and reject every proof with `interactive-job.catalog-authority`; (3) `setActiveExample` undeclared; (4) **`BitmapTransient` had a NON-DOTTED `__DSL_ENVELOPE_ID`** (`#[dsl(extension = …)]` with no `id`), so `print_dsl`'s own `expect` PANICKED the guest the first time the solve was published and every later dispatch answered `unreachable`; (5) the SDK one-item preparation under-declares `resize-input`/`resize-output` (multi-row inverses) and masks its own first error as `…-mutation-owner-missing`; (6) **`HEADLESS_STEP_BUDGET_US = 50_000` against the 8 000 µs interactive ceiling** — four consecutive over-ceiling steps quarantine the session with `job-session.terminal-fault`, which a native test binary never sees because it installs no monotonic clock.
- 📌️ **@A2/A3/A4/A5 — three of those six are generic and almost certainly yours too**: check `command_from_action` + an app-owned `ArtifactOwnedToolJobFactory` + `TOOL_JOB_IDS` on your command enum, a DOTTED `#[dsl(id = "…")]` on every transient/config (`grep 'dsl(extension' ✏️s/🔌️plugins/🀄️wfc` — bitmap's was the only bare one, `◻️2d`'s and `🧊️3d`'s already carry an `id`), and any headless `BatchJobSession` step budget ≥ 8 000 µs.
- ✅️ Gates: bitmap `--lib` **185/0/1** (was 176; nine new tests incl. the transient envelope round trip, a real collapse on the transient lane, the action→command bridge and the four-way roster agreement), zero warnings from this crate; `cargo check -p semio-s-plugin-wfc --lib` 🟢️; `cargo test -p semio-s-plugin-wfc --test close_ladder` **11/11** (L's hooks survive this slice's editor rewrite); `activate-bitmap-react-dev` 🟢️ ×4.
- ⚠️ Open, framework-side: `shell.windowActivate` (dispatched by ShellHost itself) is dropped by the app-wide undeclared-action gate on every pane focus — it will fire on every plugin; and the VIEWER still takes the boot `setActiveExample` refusal (pitfall ledger #5, whose own answer is the ShellHost-side skip). `descriptor_is_fresh` stays red for the whole B wave — one `describe` once B* settle.
- ⏳️ Three cargo/activate runs died on peers' in-flight breakage (`🧱️grid3d` `Label::native`, `◻️2d` a mismatched delimiter, `🧊️3d` viewer trait rows); polled `cargo check -p semio-s-plugin-wfc` every 60 s until green and continued.
- **Fault 6**: leaving the interaction domain for the writing utilities means the host's DOMAINLESS lane owns the pointer, and each of its three verbs had to be declared — found one probe round at a time: `worldSelect` (the instance pick), `setHover` (refused once per pointer move), `worldPick` (a click that hit nothing). The last two are declared INERT: a hover is per-frame state and a store write on the pointer path is the wrong trade; clicking empty space is not an edit.
- **Fault 7**: the app builder's `.action_args()` searches ONLY app-scope actions and silently drops what it cannot find, so the staged forms never reached the window kind's own actions — the Actions pane showed rows with no inputs and no Execute control, and every press answered `wfc.grid3d.action.missing-argument 'maskCell.x'`. The forms are now built inside `grid::definition()`.
- **GREEN, all of it.** `cargo check -p semio-s-artifact-wfc-grid3d --features component-app-assembly --lib --tests -j 4` ✅️; `RUST_MIN_STACK=33554432 cargo test … --lib -j 4` ✅️ **207 passed / 0 failed / 2 ignored** (201 before this slice, six new laws); `cargo check -p semio-s-plugin-wfc --lib -j 4` ✅️; `activate-grid3d-react-dev` ✅️.
- **Browser proof** (`🗑️generated/playground-grid3d/`, zero fault lines on EVERY step of every probe):
  - boot (`boot-3/`): grid 48 instances / 3 meshes, preview 47 instances / 4 meshes, `{"state":"solved","solved":47,"cells":47,"seed":7}` — the inline per-render solve runs in the browser, no `interactive-ceiling`/quarantine, so it stays on the render path.
  - interact (`interact-5/`, 8 steps): `setActiveTile` staged form executes; **pin utility + one canvas click** takes `cell-pin:floor` 1 → 2; **mask utility + one canvas click** takes `cell-masked` 1 → 2 and cells-to-fill 47 → 46; **3 undo presses revert both** and the preview returns to `solved 47/47`; the picker loads **3D Pipes** (27 instances, `solved 27/27 seed 19`) and back to Building Blocks; **re-selecting the open example writes zero history patches** (no phantom edit). `4-mask-pick-on-canvas.png` shows the armed Mask utility and the new dark cage.
  - viewer (`viewer/`): `window:wfc-grid3d-view`, 4 meshes / 47 instances, no faults. The viewer declares ONE window by design (`📓️grid3d.md` §4); "both windows" is the editor's layout.
- Honest behaviour, not a regression: pinning `floor` into the cell under the probe's cursor makes the `blocks` grid unsatisfiable, so the preview answers `{"state":"contradiction","solved":0}` with an empty scene until undo. A grid with no consistent assignment is an ANSWER (`📓️grid3d.md` §5.4).
- Probes written: `🐍️grid3d-console-dump-probe.mjs`, `🐍️grid3d-interact-probe.mjs`, `🐍️grid3d-viewer-probe.mjs`, `🐍️grid3d-discover-probe.mjs` (selector calibration). Report: `📓️playground-grid3d.md`. Never restarted the serve — the coordinator's 6044 answered throughout.
- ⚠️ For the other B slices: the in-page canvas readback (`drawImage`+`getImageData`) is a FALSE NEGATIVE for World3d (no `preserveDrawingBuffer`) — it always answers one flat colour. Judge "visibly non-empty" from `page.screenshot()`, never from the page.
- 🧊️ **Two more live-only defects the probes found, both real and both fixed.** (a) The preview's `solve_json` is per-pane WINDOW CONFIG and the DOCUMENT can be replaced under it, so after `pipes → terrain` the pane painted the pipes assignment over the terrain grid as a field of dark-red "unknown tile" boxes; `cached_commit` takes the document now and drops a commit addressing a cell outside the grid or naming a tile the document lacks. (b) Terrain's `solve` ran and drew NOTHING: 64 cells × a 4×4 bitmap = 1 024 bounds layers → `ui.fixed-capacity: surface payload exceeds fixed capacity with 87337 bytes`, and the host refused the whole surface. The per-TILE ceiling is not a bound on the BOARD — added `MAX_BOARD_LAYERS = 256` shared out per assigned cell, so a wide board degrades to one palette-averaged swatch per cell instead of being refused. **Worth checking in every sibling artifact whose preview draws per-cell layers.**
- 🖱️ **Playwright's mouse never reaches a `Canvas2d` in headless Chromium.** `elementsFromPoint` puts the canvas on top with `pointer-events: auto`, yet `page.mouse.move/down/up` at those coordinates produces no guest dispatch at all — no edit, no refusal, nothing. A `PointerEvent` dispatched on the canvas element does reach it. `🐍️grid2d-interact-probe.mjs`'s `clickSurface` delivers presses that way; diagnostics in `🗑️generated/playground-grid2d/{pointer-probe,hit-probe}.mjs`.
- ✅️ **B2 DONE. All three probes clean, every step's `faultLines()` empty.** Boot (`🗑️generated/playground-grid2d/boot/`): shell ready, BOTH panes mount a painted canvas, 0 faults. Interact (`…/interact/`, 9 steps, 0 faulty): example pipes↔terrain with **0 history patches** either way; `pin` armed + one click → pinned pixels 6→9, `⌘Z` → 6 and the exact boot hash; `mask` armed + one click → masked pixels 9→15, `⌘Z` → 9; `solve` on pipes → tile-ink pixels 0→102 (real vector strokes, `8-solve.png`); `solve` on terrain → 48→90 distinct colours (grass/sand/water, `9-solve-terrain.png`). Viewer (`…/viewer/`): pane renders, two hover moves over it, 0 faults (baseline `viewer-1/report.json` had 3). Gates: `cargo test … --features component-app-assembly --lib` **211/0** (`test-6.txt`), `cargo check -p semio-s-plugin-wfc --lib` GREEN (`check-plugin-5.txt`), wasm check 0 warnings from this crate, `describe` + `activate-grid2d-react-dev` GREEN, staged descriptor re-read before every browser run. Full write-up: `📓️playground-grid2d.md`. No server restart was needed.

## B3 wfc2d playground
- ✅️ **B3 DONE.** Boot probe `faults: []`; interact probe **11 steps, 0 faults**; viewer probe renders and round-trips (one fault, framework-owned, below). Gates: `RUST_MIN_STACK=33554432 cargo test -p semio-s-artifact-wfc-2d --features component-app-assembly --lib -j 4` **190/0** (was 174 at A3), `cargo check -p semio-s-plugin-wfc --lib -j 4` GREEN, wasm32-wasip2 check GREEN, `activate-wfc2d-react-dev` GREEN. Probes `🐍️wfc2d-{console-dump,interact,viewer}-probe.mjs`, outputs under `🗑️generated/playground-wfc2d/`. Full write-up: `📓️playground-wfc2d.md`. **Never restarted the serve** — 6043 answered throughout.
- 🚨️ **THE FINDING FOR EVERY OTHER ARTIFACT IN THIS PLUGIN: every verb in wfc2d was dispatch-dead, and nothing in the console said so.** Two independent causes, both invisible without a playground boot: (1) the artifact declared its actions `ActionDefinition::bounded_catalog` and shipped NO app-owned retained tool route, so each dispatch resolved to `QualifiedToolProof::Bounded` and faulted inside `TypedCommandFullOperationJob::step` with *"generic bounded proof has no resumable app-owned reducer job"* (memory `project-bare-bounded-factory-means-every-action-dead`); (2) `ArtifactEditor::command_from_action` was not overridden, so the trait default answered `app.command.unsupported` first (pipeline pitfall #13). **A1/A2/A4/A5: grep your editor for `bounded_first_step_tool_proofs!` and `fn command_from_action` — if either is missing your pane is inert, however green your unit tests are.** wfc2d's `🧵️Retained` region is the copyable shape (remodel's SDK route: roster + lane table + one `ArtifactCommandWork` + one `ArtifactOwnedToolJobFactory` + `bounded_first_step_tool_proofs!` with `factory_type:`), plus four store-preparation hooks — a declared lane with no preparation factory registers as an *unsupported publication contract* and stays dead too.
- 🎨️ `setActiveExample` now answers ONE `Effect::LoadDocument` on a `HostOnly` lane instead of a mutation set, so re-picking the boot example mints **zero** history entries (probe: `documentEntriesMinted: []`). It also needed `build_document_store_initialization_job` → `bounded_document_store_initialization_job`; without it the framework refuses every whole-document load with `artifact-store.persisted-initializer-refused`, which is what the first live attempt hit.
- 🏁 `solve` is a catalogued `Migrated` action on the PREVIEW window publishing on the `Transient` lane: the work step runs `crate::inferences::solve_with_job` to completion and answers one `SetSolve`; `render_with_request_context` already threaded the lane. Proved by a pixel diff of the preview pane taken with the Actions pane CLOSED on both sides (`🗑️generated/playground-wfc2d/interact/2a…png` vs `2b…png`) — **a naive before/after around an action always reports "repainted", because the Actions pane overlays the window.** A3's `📓️audit-wfc2d.md` blocking gap 5 is closed.
- 🔭️ Both canvases were unreadable specks: slot coordinates are DOMAIN units (a corridor 2 units wide = 2 PIXELS at zoom 1). Fixed per surface — `preview_fit` maps the document bounds onto a 640-unit box centred on the origin (duplicated into the viewer's own board window, viewer purity), and `WFC_2D_GRAPH_VIEW_SCALE = 140.0` scales the graph view in `Wfc2dGraphView` with the exact inverse in the host-snapshot diff. Same conclusion B5 reached independently (`WFC_3D_GRAPH_UNIT = 120.0`).
- 🕹️ **Framework fix, benefits dag/trinity/wfc3d too (`📺️renderer/…/🕸️NodeGraph/🟦️.tsx`): the wasm node-graph surface never told any plugin about a drag or a wire.** `commitGraphFixture` was called from the selection-align chrome and nowhere else, so `onPointerUp` discarded every gesture. Added `graphEditSignature()` (node ids/positions + wire endpoints only — never camera, selection or hover) captured at pointer-DOWN and compared at pointer-UP by `commitGraphFixtureIfEdited`: a pan/zoom/pick sends nothing, a real graph change reaches the guest. With it, live canvas gestures now land `Move slot room-a` and `Disconnect edge-a-corridor`, and `⌘Z` reverts them.
- 🧩️ **`🕸️graph/🦀️.rs` CHANGED — B5 please mirror (you already have; this is the record).** The nine window actions now carry staged argument forms (`slot_arg`/`x_arg`/`y_arg`/`width_arg`/`height_arg` + a local `action(...)` helper). All artifact-agnostic, so the file stays copyable verbatim. Reason: a verb that names an entity and offers no form dispatched `id: ""` the instant its palette row was activated and minted a history row ("Pin  to corridor") against a slot that does not exist. `dispatch` additionally refuses an unknown id by name now — `wfc2d.slot.unknown-slot` / `.tile.unknown-tile` / `.edge.unknown-edge` / `.rule.unknown-rule` / `wfc2d.id.taken` (pipeline pitfall #6).
- 🔗️ B5's three points confirmed and applied: graph scale constant ✅; `setHostSnapshot` branch in the node-graph lowering ✅ (the wasm surface sends only a whole host snapshot, never `move`/`connect` sub-ops); undirected endpoint-PAIR keying for edge removal ✅ — plus one more guard worth copying: **a snapshot carrying no wires at all while the document carries several is a canvas that has not finished syncing, not a user who deleted every edge**, so that case emits nothing.
- 🕸️ Declaring ONE `.interaction(InteractionDefinition { id: "slot", … })` + `.window_kind_interactions(wfc-graph, ["slot"])` + an `interaction_topology` is what injects `interactionSelect`/`interactionHover`/`clearSelection`/`selectAll`; without it every node click on the canvas is dropped as `undeclared-action`.
- 🩺️ Probe instruments worth reusing (details in `📓️playground-wfc2d.md` §3): the React host logs only REFUSALS, so "did an edit land" is read from the History panel's `framework.history.entry.*` rows with the shell's own chrome rows filtered out; the wasm node-graph exposes no DOM node, so a slot is found by sweeping the pane and watching for `dag hover changed: <id>`, refined to a centroid; a step may declare a refusal it is PROVING so a guard firing is evidence, not a fault.
- ⚠️ **Three faults that are NOT wfc2d's, for whoever owns the framework surface.** (1) `shell.windowActivate` and its `shell.*` siblings are dropped as `undeclared-action` by EVERY app in this repo — `ShellHost.noteShellCommand` records a history row whose `actionId` is the `shell.*` id and the replay dispatches it, and no plugin anywhere declares one; the ids belong in `FRAMEWORK_RESERVED_ACTION_IDS`. (2) **The VIEWER's boot `setActiveExample` is still refused** (`app "s.wfc.wfc2d@1/*#viewer" dropped action "setActiveExample" … window kinds: wfc-2d-board`) — pipeline pitfall #5 verbatim, whose framework half ("ShellHost skips the boot announcement when `undeclaredActionDiagnostic` says no window kind declares it") is NOT in effect on this tree; every variant's viewer will show it. (3) A ports-only slot node cannot be dragged by its centre: `fit_node_size` overwrites the plugin's width/height from the port layout, so the whole node box is port cells and `world_hits_handle` (wire) wins over `try_node_rectangle_pointer_down` (drag); the bounded selection-union drag that would catch it is gated on `DagDrawLod::Minimap`.
- 🔌️ **B5's open question — how to prove a DRAWN wire headlessly.** `dag port press … interaction=draw-edge(new)` is reachable (my probe logged it), so the gesture starts; what is missing is the port anchor's screen rect, since `session.entityScreenJson("handle", …)`/`pickTargetsAtScreenJson` are not reachable from the page. Concrete suggestion: have `WasmGraphSurface` mirror `entityScreenJson` for the visible ports onto a `data-port-rects-json` attribute on the host element — it already publishes `data-selection-json` exactly that way, and a probe could then aim at the anchor instead of sweeping. Until then `connect-slots` is proved by a unit test over the host-snapshot lowering and by the Actions form (`Connect room-a to room-b`), and wire REMOVAL is proved live.

## F final gates
Report: `📓️final-gates.md` (every command with its result and owner). Logs: `🗑️generated/final/` (68). `bun nx reset` first; every cargo foreground, `-j 4`, one at a time; no exit 137, no retry; no git-modifying command, no server touched.
- ✅️ **Rust is green end to end.** engine `--lib` **301/0**; the five artifacts with `--features component-app-assembly`: bitmap **185/0/1**, grid2d **211/0**, wfc2d **190/0**, grid3d **207/0/2**, wfc3d **239/0/2**, each after `cargo check --lib --tests`; plugin `--lib` **14/0** (`descriptor_is_fresh` included), `close_ladder` **11/0**, `boot_deadline` **1/0**, `idle_turns` **2/0**, `wasm32-wasip2` check 🟢️ (71 warnings, so expansion really ran), `semio-s-plugin-procedural --lib` 🟢️ (the assembly deletion still holds).
- 🧹️ **Clippy: 14 warnings inside `🀄️wfc` were ours and are gone** (grid2d 3, wfc2d 4, wfc3d 7): `unnecessary qualification` ×10, a derivable `Default for Grid2dViewCommand`, a `redundant clone` in wfc2d's drag law, and two `map(…).unwrap_or*` in wfc3d's editor. Every other warning in those runs comes from framework dependencies. All five suites re-ran at their exact counts afterwards.
- 🔴️→🟢️ **A real defect found: `idle_turns` was measuring the wrong process.** The lane installs `HeapWitness` as the `#[global_allocator]`, so `retained_heap_bytes()` is PROCESS-WIDE, and the harness runs its two laws on two threads — each read the other's allocations and the other's typed-operation contention. It failed non-deterministically and differently each run (`9 106 B/turn` on the unacknowledged-open law once, `129 B/turn` + `settled(contended)` on bitmap the next, green on `--test-threads=1`). Fixed with a process-wide `IDLE_MEASUREMENT` lease both laws take for their whole measure window; three consecutive default-harness runs green, readings back to 0 B/turn (bitmap 393 B, grid3d 21 657 B, the rest NEGATIVE over 512 turns, ceiling 65 536 B). G's earlier green was luck.
- 📇️ **`describe` re-run (3 m 15 s) and the descriptor is fresh**: 10 apps, exactly two window kinds + a `defaultLayout` per editor, 13 top-level examples splitting **bitmap 2 / grid2d 2 / wfc2d 4 / grid3d 2 / wfc3d 3**, window actions 29/41/39/41/39, `setActiveExample` declared on all five editors. ⚠️ Four of five VIEWERS still do not declare it (only grid2d's does), so each keeps the boot `undeclared-action` refusal — pitfall #5, per-artifact viewer work (A1/A3/A4/A5) plus the ShellHost half that is not on this tree.
- 📇️ `plugin-registry:generate` 🟢️, `plugin-registry:check` 🔴️ **2 283 repo-wide / 54 wfc — the same 54 G adjudicated, with NO new category after the whole playground wave**: config schema 20, window-`🧪️tests` child 15, presence schema 10, example assets 6, two `mutate-*-1` host adapters, one plugin-root `🎮️commands/🦀️.rs`. None is a missing facet folder, a missing example `🟦️.ts` twin or an emoji-less directory — the three structural classes G fixed. Target 0 needs per-surface Presence/Config models, which 24 plugins lack today. `schema generate` 🟢️ 3 200 scopes.
- 🟦️ TS: `wfc-js:test` **10 files / 148 tests** 🟢️, `dag-js:test` 🟢️, `framework-renderer-react:lint` 🟢️. B3's `graphEditSignature`/`commitGraphFixtureIfEdited` is intact in `🕸️NodeGraph/🟦️.tsx` (+37 over HEAD) and its two co-located suites — which only the `long` level includes — pass **2 files / 26 tests** when run directly. `:test-long` itself is 🔴️ and never finished: vitest `exceeded 300000ms — killed` by the lane's own budget, with 17 failures in six suites (package-integration, PluginRuntime, space-administration, window-fault, world3d-interaction ×7, resident-refresh-budget) — none NodeGraph, none wfc. **Owner: renderer / world3d / wgpu.**
- 🚦️ Repo gates unchanged from G, all three non-wfc: `verify taxonomy report --scope …/🀄️wfc` 🔴️ still aborts at `compiler input bytes differ: bun.lock` because `generate-distribution` is broken by `…/🚚️distribution/🔗️inputs.json:11` naming a react build-tooling path that no longer exists (**owner: whoever moved it — one tracked line unblocks the gate fleet-wide**); `verify dependencies write-baseline` 🟢️ 230, `literal-external` 🔴️ 229 repo-wide with **no wfc-specific row** (wfc appears only inside the repo-wide `rust:serde_json` oracle-conflict that lists ~120 manifests); `policy` ⛔️ still cannot run (`repo/cli` binary absent, **owner: repo-client**).
- 📏️ Consistency laws: `◻️2d` and `🧊️3d` `🕸️graph/🦀️.rs` are **byte-identical** (not even the `use` path differs), so no reconciliation was owed; **45 comment blocks (133 lines) inside definitions in the five artifacts → 0**, each one's substance moved onto the enclosing item's docstring (including the three `// ---- Shims ----` banners, the two `🧵 Deliberately NOT use super::` notes into their `//!` headers, bitmap's `✉️` envelope-id warning onto `BitmapTransient`, and grid3d's three `[DEBUG] temporary generator` banners, which are permanent tooling); all 10 `icon_id` strings exist in `🪪️icon-name/🦀️.rs`. ⚠️ `⚙️engine/**` still carries **468** in-body comment lines — copied wholesale from procedural by E, out of this slice's scope.
- 🧯️ **Disclosed:** my first comment codemod reused stale indices and mangled code into docstrings in the six files where one function held several comment blocks. Caught at once, repaired line by line against the pre-edit dump, then redone one-block-per-pass. Proof the repair is exact: all five suites are at their EXACT pre-codemod counts and the four plugin lanes are green. Also: `grep` here is `ugrep` and silently missed `graphEditSignature` in `🕸️NodeGraph/🟦️.tsx` although it occurs four times — do not trust a bare grep miss on that file.
- 18:3x COORDINATOR: F final gates green; five dev servers stopped; `🗑️generated` removed; ticket CLOSED (summary in `🎫️ticket.json`; DoD table in `📓️audit-final.md`).
