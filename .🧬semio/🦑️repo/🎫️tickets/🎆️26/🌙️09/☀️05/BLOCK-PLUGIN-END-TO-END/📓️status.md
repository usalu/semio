# 🧱️ Block plugin end to end — status

Apps under test: `block2d`, `block3d`, `block5d` of plugin `✏️s/🔌️plugins/🧱️block`
(`bun run dev:block:<2d|3d|5d>` → `bun ./📜️script.ts dev block <nd>` → framework-os-dev playground).
Ticket opened 2026-09-05 by session ⚪2adc84fa (Fable 5.1 coordinator). Repo MCP was down; bookkeeping is manual.
Ticket start commit: see `🗑️generated/start-commit.txt`.

## Definition of done
1. `cargo check -p semio-s-plugin-block` green natively and on `wasm32-wasip2`.
2. `bun nx run @semio-tech/block-plugin:test` green (lib test target compiles and passes).
3. Descriptor `🔣️.json` / `🛂️.descriptor.semio` regenerated via `describe`; registry check accepts block.
4. Each of the three playgrounds boots in the react renderer; every window renders non-empty; examples switch; editor actions dispatch (no `interactive-job.missing-owned-reducer` / bounded-factory faults).

## Log
- 2026-09-05 open: exploration fleet launched (2d/3d/5d editors, build+test infra, dev-boot path, TS side).
- block2d explored (`📓️explore-block2d-editor.md`): 9/9 actions `BatchOnlyPendingRewrite`, NO `bounded_first_step_tool_proofs!`/factory at all (→ `interactive-job.missing-factory`), boot snapshot is `Block2dSnapshot::default()` (empty), examples are `include_str!` DSL, `setActiveExample` handler complete but unreachable. All 604 `#[path]` mounts resolve. block5d has the full retained-factory pattern (`BLOCK5D_RETAINED_TOOL_IDS`, `Block5dRetainedCommandJobFactory`). Implementer W1 (block2d) launched.
- block5d explored (`📓️explore-block5d-editor.md`): dispatch healthy (factory_type set, 7/7 Migrated). Gaps: default boot empty (`Block5dSnapshot::default()`), orphan `app_5d_demo_session` module, io impls unregistered on `io_mechanism`, TS oracle path bug (W1 fixes).
- TS/io explored (`📓️explore-ts-viewer-io-schema.md`): 232 TS files are inert typed twins; io leaves `export {}`; Rust io: only JSON real, STL/OBJ/ZIP/PNG import silently return empty snapshots, TXT is an Err stub, io unregistered on the io mechanism in all subsets. W3 (io registration + txt via DSL + honest formats + TS json/txt twins) launched. Coordinator rewrote block-js `package.json` (was a verbatim cad-js copy; block TS imports no workspace packages).
- block3d explored (`📓️explore-block3d-editor.md`): 20 own actions all `Unclassified` (never `.action_interactive_job`), zero tool-proof/factory wiring, boot empty, `setCamera` unregistered, nakagin 1:500 representation references `/mesh/capsule_J.1to500.glb` which no catalog has (would throw `Unknown mesh asset`), orphan `demo-session` example. All 244 `#[path]` mounts resolve. W4 (block3d) launched.
- Infra explored (`📓️explore-build-test-infra.md`): 917/919 mounts resolve (2 are a generated-test-host quirk shared by cad/puzzle/procedural); block is already in the registry playground catalogs; `🔣️.json`/`🛂️.descriptor.semio` never generated at block's owner root (registry `check` warns); storybook scope `block` resolves but has zero stories; dev plugin-module wasm (2026-09-04 17:20) predates the latest block commit.
- Baseline native check (`🗑️generated/block-check-native-baseline.txt`, exit 101) never reached block: stdio's crate entry mounted `🪆️subsets/✳️base/🧬️schema/🦀️.rs`, which was renamed to `🧱️base` by its owner at 03:59 (after the check started) — a stale, queued-check artifact, not a live break.
- Dev-boot path explored (`📓️explore-dev-boot-path.md`): bare `dev block 2d` defaults to wgpu; react needs `SEMIO_RENDERER=react`; ports react 6024/6025/6026; engines (surface/editor/flow-core) all fresh on disk → `SKIP_ENGINE_BUILD=1` valid; block's load closure is {block, stdio}; neither has an owner-root `🔣️.json` (a full un-narrowed build materialises them); `SEMIO_PLUGIN_ONLY=block` unsafe cold. Registry `generate` gate probe: exit 0 (59 crates, 60 playgrounds). W5 (storybook block stories) launched.
- External gate (reported by peer semio-f4, ~04:10): `semio-framework-os-kernel` fails with E0432 `super::schema::DirectorySpaceDetailV1` in `📇️directory/🔌️client/🦀️.rs:22` — an uncommitted directory-schema rewrite (schema mtime 04:07, owner unknown, semio-89/-4f/-f4 asked). It blocks every s-plugin wasm build, block included. Not ours; not editing it.
- W1 (block2d) code landed (`📓️w1-block2d-factory.md`): retained factory + store preparation + proofs with `factory_type` + 9× Migrated, `default_block2d_snapshot()` boots the forest-left example, publication-authority fixture generalised to `apps[]` (2d:9, 5d:7), TS oracle path bug fixed and passing. Cargo check/test outputs pending (queued in target-block).
- Resolved W1/explore flag: inspector `on_change` payloads carry only `field`; the react Interpreter merges `{ value }` at commit (`🗣️Interpreter/🟦️.tsx:785`) — framework convention, not a defect.
- 04:29 os-kernel gate CLEARED (semio-f4): `📇️directory/🔌️client/🦀️.rs` rewritten by ticket 26/09/02/COMPLETE-SEMIO-END-TO-END (Codex-driven); the one remaining `DirectorySpaceDetailV1` reference is under `#[cfg(test)]`. Next external gate: `🗄️stdio` (block depends on it unconditionally) — semio-4f measured 225 errors at 04:12 from the in-flight serde → `ToValue`/`FromValue` migration.
- W3 (io) landed (`📓️w3-io.md`): io registered via `🗒️note`'s pattern (typed Serializer/Deserializer → `IoDeclaration.entries` → subset root `io: io::io()`); txt = DSL both ways (Exact); zip = real archive of DSL+json; STL/OBJ/PNG registered Lossy but return typed `Err` with reason; TS json codec + DSL reader for 2d/5d with a cross-language fixture oracle (bun 8/8 pass); dead `io_registry` channel deleted. Cargo never reached block (kernel E0432 ×2, then a stdio obj-subset rename mid-flight). NEW block-internal blocker: `async fn print_dsl` impls vs a sync `ArtifactDsl` trait (E0053) in block's `📸️snapshot/🦀️.rs` — investigating direction of that codemod.
- Coordinator fix: block's `ArtifactDsl`/`ArtifactPack` impls (12 files: `📸️snapshot`, `📸️snapshot/📝️text`, `✏️editor/🎚️config`, `✏️editor/👥️presence` × 3 subsets) spelled `async fn` against the sync store traits (`🏪️store/🦀️.rs:4891-4895, 9326-9328`, unchanged since Sep 4; committed in block at 7ad363fd1e) — E0053, no body awaited anything. Stripped `async` on those trait methods. Same codemod fallout exists in 12 other plugins (note, dag, forms, layout, remodel, shooting, …) — not ours, not touched.
- Also made the 6 free `pub async fn parse_dsl/print_dsl` in each subset's `📸️snapshot/📝️text/🦀️.rs` sync — every caller (`default_*_snapshot`, `set-active-example`, io leaves) already calls them synchronously.
- ROOT COMPILE BLOCKER (block-internal): the crate carries 2125 `async fn` (214 `async fn diff`/`inverse` on `protocol::MutationKind`, 47 `handle`, 15 `encode_op/decode_op`, 9 `print_op/parse_op`, even `pub async fn add_attribute()` constructors) while the framework traits are sync (`📡️replication/🎮️mutation/🦀️.rs:145-153, 1252-1270`) and puzzle/procedural/stdio have ZERO async `diff`. A blanket async codemod hit block alone. W6 (de-async block to the trait/peer convention) launched.

## 2026-09-05 13:20 — resumed after fleet loss
- 04:50-05:50 all five Opus agents (W1, W2, W4, W5, W6) were killed by the account's Opus session limit (reset 08:10); the box then rebooted (~08:37). Their reports landed (`📓️w1…w6`), W2's has empty verification placeholders, W1's has PLACEHOLDER_CARGO_* — none compiler-verified. All their edits are present in the tree (retained-factory symbols for 2d/3d, boot snapshots for all three, `pub fn io()` ×3, apps[] fixture, 13 storybook stories, W6's 1127 de-async lines; 997 `async fn` remain = 952 `#[async_test]` tests + 45 async io trait impls).
- 05:37 an external emoji-rename pass renamed 383 block paths on disk (`🎬️hexagonal-cut-concrete-forest-left`→`🌲️…`, `🧩️mutate-block-*-1`→`🧱️…`, `🧪️<example>`→`➡️/🌲️<example>`); git shows them as D + untracked. All 1605 `#[path]`/`include_str!` refs resolve on the new names — no data loss.
- Coordinator: removed block2d's orphan `🎬️demo-session` example + mount (W2/W4 did 5d/3d); fixed story doc strings to `🌲️`; launched `cargo check -p semio-s-plugin-block --lib` on the current tree (`🗑️generated/check-lib-1.txt`). Next: widen the publication-authority oracle to multi-lane and add block3d's entry (W4 §7).
- 13:25 Publication-authority oracle widened to multi-lane (`routes[].lanes: Lane[]`, schema `minItems: 3`), block3d entry added (23 routes, `worldSurfacePlace` = Artifact+Config), freshness hostile now `replaceAll` (block3d legitimately has two preparation factories). `bun ./📜️script.ts test` in block-js: 14/14 io tests pass; oracle validates Block2dPlayApp:9, Block3dPlayApp:23, Block5dPlayApp:7; hostile=12 rejected.
- 13:30 Registry example discovery gap (W2 finding) fixed in `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️.ts::registryExampleCatalog`: it now also scans `🪆️subsets/<s>/📚️examples/` (block and puzzle keep examples there). Regenerated: block rows advertise `➡️hexagonal-cut-concrete-forest-right`, `🌲️hexagonal-cut-concrete-forest-left`, `🏢️nakagin-capsule`; puzzle rows gained their three real examples. Note the shell's example switcher reads `PluginManifest.examples` (ShellHost `:6288`), not the registry — tracing that path next.
- 13:40 `cargo check -p semio-s-plugin-block --lib` (`🗑️generated/check-lib-1.txt`): stdio GREEN, block = 60 errors, all block-internal framework-API drift (13 × E0433 `mutation` missing in `↩️inverse`, 3 × `command_from_action` DslValue/JsonValue, 6 × E0046 Config/Presence missing `DESCRIPTORS`/`descriptor`, 7 × `render` must return `Result<ComponentTree>`, 3 × `with_ports` on an async `AppIo`, 13 × `ActionDescriptor` helper signature, 15 × prelude `Label: From<…>`, 2 × mutation ToValue/E0080). W7a/b/c (per-subset fixers, puzzle as oracle) launched.

## 2026-09-05 18:25 — second fleet loss, resumed
- 13:45-14:10 W7a/b/c (per-subset compile fixers) landed their edits (`📓️w7a/b/c-*.md`, 28 block files touched) but were killed by a second Opus session limit (reset 18:10) while queued behind a peer's stdio recompile; W7a's last check showed 3 errors left in its subset, W7b/W7c verification pending.
- 13:45 stdio wasm pre-warm (`target-block-wasm`, `--profile wasm-dev` = dev + codegen-units=1) STALLED: its rustc had 26 CPU-min in 4h18m, 3% live CPU, 8.6 GB RSS; host swap 45/46 GB used (other sessions' 5-hour rustc processes). Killed; relaunched 18:25 with `CARGO_PROFILE_WASM_DEV_DEBUG=false`, jobs=2 (`🗑️generated/stdio-wasm-build-2.txt`, pid 34252). The dev boot's own plugin build uses the same profile — expect the same memory wall without that override.
- 18:22 fresh native check running on the W7 tree (`🗑️generated/check-lib-2.txt`).
