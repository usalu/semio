# S End To End — Plan of Record

Ticket: `26/09/05/S-END-TO-END` · Goal: `🎯r2603` · Coordinator: Claude Fable 5.1 (this session) · Implementers: Opus 5 (`📓️opus-*.md`) · Explorers: Sonnet 5 (`📓️explore-*.md`)

Sibling ticket run by a separate GPT fleet: `26/09/02/COMPLETE-SEMIO-END-TO-END` (hub, AI, admin, provider authority). This ticket owns only the `s` OS shell surface: every registered plugin/app/artifact opens and renders in the React shell from the registered launch targets, with WGPU wasm and native parity as the second and third tiers.

## Definition of done

1. `🛠️dev🖥️s⚛️react` boots to the readiness beacon with the complete live catalog (59 registry rows) installed, no plugin in `failed`/`crashed` status.
2. A registered catalog-wide smoke (`framework-os-dev verify catalog`) spawns every program inside one `s` session and proves a rendered window with zero page errors for each declared app.
3. Every plugin has a fresh owner descriptor pair and the registry `check` is fail-closed and green.
4. `framework-os-dev test-quick`, `.storybook/os-plugins.spec.ts`, `.storybook/s-end-to-end.spec.ts`, `verify e2e`, and `verify collab` run non-vacuously and pass.
5. `🛠️dev🖥️s🧊️wgpu🌐️wasm` boots and `parity verify s` passes; `🛠️dev🖥️s🧊️wgpu🖥️native` boots and opens Home/Studio.

## Baseline — 2026-09-05 03:50

- Served React shell (`dev s served`, port 6070): booting; evidence pending in `📓️baseline-runtime.md`.
- `framework-os-dev:test-quick`: RED, the vitest run exceeds the 30 s quick budget and is killed (`[budget] … exceeded 30000ms`).
- `plugin-registry:check`: RED, crashes with `ENOENT` scanning a concurrent agent's `target-block/` root before reaching the descriptor gate.
- Catalog cache: 57/59 rows have core wasm + JS; `draw`, `layout` unbuilt; `energy` descriptor without module; 19 rows (block, playbook, stdio, trinity + 15 extensions) have no owner descriptor pair; 8 pairs semantically divergent; 4 CAD extension pairs are placeholders.
- `.storybook/os-plugins.spec.ts` and `OsBootHost` import a non-existent `🤖️generated/🟦️plugins.ts`.
- `semio-s-plugin-stdio` reported non-compiling by peer session semio-f4 (`#[path]` mount drift after the emoji rename); f4 owns the mount repair. Native check census running into `target-s-e2e`.
- Action classification: 427 `Migrated` vs 414 `BatchOnlyPendingRewrite` repo-wide; norm (15 apps) at 0 migrated; puzzle 2d/3d/5d, note, flow, fem, cad, gis, dag, imperative, reasoning viewers carry large dead sets.
- WGPU wasm: no completed trunk build since 2026-09-03; native: `ProgramBridge::attach_backbone` is a hard `Err` stub.

## Waves

### Wave 1 (now, independent of stdio)
| Lane | Model | Scope | Report |
|---|---|---|---|
| A `catalog-smoke-harness` | Opus | fix storybook `🟦️plugins.ts` import; make `test-quick` honest within budget; add `verify catalog` Playwright smoke that spawns every program inside `s`; register in project.json + launch.json; run it against the served shell | `📓️opus-catalog-smoke-harness.md` |
| B `descriptor-producer` | Opus | raw+core receipt `describePluginComponent` contract; shared extension `describe` route + targets for 26 extensions; registry `check` fail-closed on missing/divergent pairs; classification-drift gate; re-emit pairs where raw components exist | `📓️opus-descriptor-producer.md` |
| C stdio census | coordinator | native `cargo check --keep-going` census in `target-s-e2e` | `📓️stdio-check-census.md` |
| D runtime baseline | coordinator | browser evidence for the served shell | `📓️baseline-runtime.md` |

### Wave 2 (after stdio compiles)
Lane F `catalog-rebuild` (Opus, dispatch when `semio-s-plugin-stdio` checks clean on wasm32-wasip2 and load average is below ~60): run the registered full boot `S_OS_PORT=6074 SEMIO_PLUGIN=s SEMIO_RENDERER=react SEMIO_BUILD_BUDGET_MS=7200000 CARGO_TARGET_DIR=/Users/ueli/Documents/semio/target-s-e2e RUSTC_WRAPPER="" bun nx run @semio-tech/framework-os-dev:dev -- s` as lease holder so `buildPluginsStreaming` rebuilds all 59 crates serially, `describeBuiltPlugin` re-emits every owner pair, and `🔌️plugin-modules/` gets fresh cores (draw, layout, energy included); then `bun nx run @semio-tech/plugin-registry:check` must reach 59/59 valid pairs; per-crate failures become one Opus lane each (block is owned by ticket 26/09/05/BLOCK-PLUGIN-END-TO-END, procedural by 26/09/03/PROCEDURAL-3D-END-TO-END, process by 26/09/01/PROCESS-END-TO-END; coordinate, do not duplicate).
- stdio non-mount compile errors + `describe`; then dependency-first rebuild of the catalog into the cache (`draw`, `layout`, `energy`, `block`, `trinity`, `playbook`, animate/remodel/fem mutation modules).
- Shared bounded tool-job factory for the 15 norm apps; `PuzzleCommandWork::step` app-instance parameter; viewer action migrations per plugin (one Opus lane per plugin family).
- Descriptor re-emission for all 59 rows; registry check green.

### Wave 3
- WGPU wasm `s` boot + `parity verify s`; native `attach_backbone` replacement and native Home/Studio boot; `verify collab` green.

## Coordination rules for every lane
- No `git commit/stash/checkout`, no worktrees, no ticket close/reopen, never delete another ticket's `🗑️generated`.
- Write build/boot logs to the scratchpad or this ticket's `🗑️generated/`; reports as `📓️*.md` in this ticket.
- Foreground commands only; `cd /Users/ueli/Documents/semio` explicitly per call; private `CARGO_TARGET_DIR` for any cargo work.
- Re-read a region immediately before editing; attribute failures to current evidence, never revert peer work.

## Coordinator log

- 04:05 Explorer corrections: the per-plugin table's viewer `BatchOnlyPendingRewrite` counts are not reproducible in current source (every sampled viewer has zero; several declare only `Command::Noop`); puzzle3d is at 59 Migrated / 8 BatchOnly, and the `PuzzleCommandWork::step` app-instance idea was retracted by `PUZZLE-3D-END-TO-END/📓️plan-2026-09-05.md`; all 15 norm apps share one byte-identical 3-action shape (`setSnapshot`/`evaluate`/`setSelectedCheckIndex`). Use `📓️explore-action-migration-recipe.md` as the authority for Wave 2 dispatch.
- 04:20 Coordinator fixes (library/registry/dev tooling): discovery skips every `target*` Cargo root and tolerates directories renamed mid-walk (`isDiscoverySkipDirectory`, `readdirVanishing`, test `🧪️tests/🎯️cargo-target-discovery-skip`, Nx `test-cargo-target-discovery-skip`, launch `🧹clean🧩️taxonomy🎯️cargo-target-discovery-skip`); `frameworkOsPlaygroundDevEnv` honours the `served` renderer override so `dev s served` listens on the React port (test in `📚️library/…/🔬️index.test.ts`); `readGeneratedCatalogProjection`/`filterProjectedPluginRegistry`/`projectedHostPluginFilter` in the registry script replace four in-process repo walks in the dev boot and the playground session builder (test `📖️generated-projection.test.ts` + fixture `🧫️fixtures/📖️generated-projection.json`).
- 04:35 Peer coordination: semio-f4 owns nothing in stdio either; the emoji-rename ticket owns stdio; semio-08 (PROCESS-END-TO-END) consumes the orphaned wasm-dev build's outputs; both pids traced (orphan `dev` lease holder, DEMONSTRATOR bash lane). Not killed.
- 04:45 Blockers outside this ticket: `semio-framework-os-kernel` E0432 (`DirectorySpaceDetailV1`, directory module mid-edit by the COMPLETE-SEMIO fleet) blocks every Rust check on both targets; a peer taxonomy edit left `🌍️change-annex` duplicated under owner `schema`, which breaks `loadCatalogTaxonomy` and therefore registry tests and dev boots until it settles. Lane C (`fault-discriminators`, Opus) dispatched on the empty-window fault taxonomy.
- 05:55 Served shell answered HTTP 200 for the first time (boot now ≈ 1 registry walk + Vite config bundle ≈ 7 min under load 100; Vite then restarts for ~7 min whenever a peer edits a file in its config import graph). Lane E (`vite-config-diet`) dispatched to shrink that graph.
- 06:00 Opus session limit hit (resets 08:10 Europe/Berlin): lanes A (`catalog-smoke-harness`), C (`fault-discriminators`), D (`norm-shared-factory`) and E (`vite-config-diet`) were terminated mid-edit. Lane B (`descriptor-producer`) had finished: strict gate now reports 1/59 valid owner pairs (only `cad`), 32 pairs lack `packageId`, 19 missing, 4 placeholders, 2 corrupt packs, `puzzle` over the 4 MiB bound; every re-emission needs a fresh wasm build (Wave 2). On-disk state of the interrupted lanes: A landed the `test quick` level gating (`🧪️tests/🟦️.ts`), the `catalog-smoke` Nx target and the `verify catalog` runner in the dev script (unverified), and the storybook `🟦️plugins.ts` import is gone; C left `RuntimeMaintenanceStatus`/`RuntimeCloseStatus` enums in `🔌️plugin/🦀️.rs` and `semio-framework-plugin` checks clean natively in `target-s-e2e`; D left norm edits across the 15 editors plus `🖥️app-surface` (check running); E landed `🔌️vite-plugins.ts`, rewired the dev script and the Vite config to it and to `isHostPlaygroundFilter` from the generated registry (bundle-checks pass). Coordinator resumes the four agents at 08:10 with their own contexts; until then the coordinator verifies their partial work directly.
- 13:35 Sonnet audit `📓️audit-lane-a-catalog-smoke-state.md`: lane A's harness is functionally complete on disk (`runCatalogSmokeVerify` dev `📜️script.ts:2043-2236`, `VerifyScript` routes `catalog`, Nx `catalog-smoke`, seed + launch entries, fixture `🔬️catalog-smoke.json` + schema `🧬️catalog-smoke.schema.json`, 4/4 aggregation tests, live probe `window.__semioOsCatalogProbe` in ShellHost :7584-7610); storybook `🟦️plugins.ts` import fixed. Open: `ajv` used without a declared dependency; `test quick` still exceeds 30 s under load; a peer's in-flight split of `📚️library` (`🎮️playground/🟦️.ts`, `🗂️workspaces/🟦️.ts`, `🏃️process/🟦️.ts`) intermittently breaks `bun build`/imports and restarts Vite every few minutes. Lanes A, C, D, E resumed at 13:30 with their own contexts.
- 14:10 Lane E done (`📓️opus-vite-config-diet.md`): Vite config graph 131 → 32 modules (3.26 MB → 371 KB), `VITE ready in 10136 ms` (was 415 982 ms), zero restarts on `📇️directory`/`🔍️discovery` edits; library split into `🎮️playground/🟦️.ts` and `🏃️process/🟦️.ts` with barrel re-exports; JSON deny-list contract `🧫️fixtures/⚙️config-graph.json` + `🧹️config.test.ts`; framework-os-dev quick 66/66 under bun.
- 14:15 First real runtime evidence recorded in `📓️baseline-runtime.md`: boot is fatal (`shard 0 terminated` by the heartbeat watchdog under load), the router aborts on demonstrator's undeclared dependencies, 23 rows fail descriptor validation, 4 plugins time out loading. Dispatched lane G `shard-liveness` (`📓️opus-shard-liveness.md`) and lane H `router-resilience` (`📓️opus-router-resilience.md`). Lanes A, C, D still running.
- 14:41 `cargo check -p semio-s-plugin-norm --lib` in `target-s-e2e` EXIT 0 with lane D's shared factory on disk (0 errors, 16 warnings). Headless boot probe: 374 requests by 281 s, beacon still unset; per-plugin `timeout loading` for vcs/gis/flow-extension-*/sourcing-module-windows, descriptor 404s for stdio and every extension under `🧩️extension-modules/`, CAD placeholder mismatches.
- 14:50 Lane A done (`📓️opus-catalog-smoke-harness.md`): storybook import fixed; `test quick` honest and green (67 passed, 14 s) via `testLevelAtLeast`/`itLong` in the library and `SEMIO_TEST_LEVEL`; `verify catalog` runner with `commit` navigation, beacon polling, per-program window assertions, JSON+markdown report, Nx `catalog-smoke`, launch `⚖️gate🔬️catalog-smoke`; dev-only `window.__semioOsCatalogProbe`; live runs show the shell never becomes spawnable. Headless boot probe shows all four shard workers raising `worker error Event` at ~120-140 s; forwarded to lane G (record `event.message`, fail fast on missing JSPI). Headless Chromium 151 exposes `WebAssembly.Suspending`/`promising` on the main thread.
- 15:25 Lane C done (`📓️opus-fault-discriminators.md`): `RuntimeCleanupFault` enum with 19 causes and a slug/code table, every store site names its cause (`plugin.internal.abi-mismatch`, `.interactive-ceiling`, `.clock`), fixture `🩺️runtime-fault-vectors.json` + schema with Rust and vitest readers and ajv oracle, ShellHost `🩺️fault/🟦️.ts` classification with `data-semio-window-fault`, EN/DE labels; `cargo check -p semio-framework-plugin --tests` green, focused tests green. Coordinator finding: the shell spawns its shard workers at `/plugin-modules/_shard/🟨️shard-worker.js` (Vite answers HTML) while the file is published at `/🔌️plugin-modules/🧵️shard/🟨️shard-worker.js`; every shard dies at load, which is the fatal boot cause. Source of the ASCII path still being located.
- 15:35 Boot progressed past the shard layer: with lanes G/H live the shell spawns its shard workers at the correct `/🔌️plugin-modules/🧵️shard/` URL, the router excludes demonstrator instead of dying, and the first plugin turn now reports precisely `s#1 … 🪐️s/🌉️bridge.js: unreachable` (stale 09-02 core against today's host). Started the Wave 2 rebuild with the `s` crate first: `SEMIO_PLUGIN_ONLY=s … bun nx run @semio-tech/framework-os-dev:plugin` into `target-s-e2e` (log `plugin-build-s.txt`), stdio wasm32 check still clean and running in the same target.
- 18:25 Opus limit hit a second time (~16:40-18:10); lanes D, G, H cut mid-work and resumed at 18:20. `semio-s-plugin-stdio` checks clean on wasm32-wasip2 (86 min in `target-s-e2e`). The first `SEMIO_PLUGIN_ONLY=s` rebuild queued behind that check and was killed by the 2 h budget while still compiling stdio for codegen; relaunched into a dedicated `target-s-e2e-wasm` with `SEMIO_BUILD_BUDGET_MS=14400000` (log `plugin-build-s-3.txt`); a watcher re-probes the boot as soon as `🪐️s/*.core.wasm` changes.
- 18:35 Coordinator fix: the dev script still called the registry script's `isHostPluginFilter` (two full repo walks per call via `findPluginCargoFiles`) at four sites, stalling `plugin` builds and `dev` boots for 10+ min under load; replaced with the generated pure `isHostPlaygroundFilter` (`📇️registry/🟦️.ts`, equivalence proven by lane E's fixture test). `bun build` bundle-check green; `s` rebuild relaunched (`plugin-build-s-4.txt`).
- 19:05 `📓️explore-wgpu-tier-readiness.md` (Sonnet): the wgpu tier shares the plugin cache, shard worker and JSPI requirement with React; no trunk artifact exists; `check-frame-worker` RED (stale `frame-worker.js`), wgpu `lint` RED (3 raw colour literals `🧊️renderer/🦀️.rs:15240-15242`), ship `BuildScript` points at a non-existent wgpu script path (`📜️script.ts:1373`); `parity verify s` rebuilds all 59 crates then boots React + trunk, and currently aborts in `checkFrameWorker`. Wave 3 lane I (`wgpu-tier`) will follow the report's 8-step checklist once the React tier boots and the catalog rebuild has landed.
- 19:20 Lane G done (`📓️opus-shard-liveness.md`): the fatal boot's root cause was `PluginRuntime/🟦️.tsx:204` declaring an ASCII transliteration of the shard-worker route (Vite SPA HTML → message-less worker error); now imported from `🧵️shard-runtime` and test-pinned; `evaluateShardLiveness` measures silence from the last inbound message, worker beats `module-fetch`/`module-ready`/`actor-ready` plus a ticker while busy, primary boot retried once on a rebuilt shard then typed `plugin.boot.shard-lost`, progress-driven load deadline from `SHARD_LIVENESS_POLICY`, JSPI probed once (`plugin.runtime.jspi-unavailable`), in-worker fault reporting; 205/205 actor + 267/267 os-dev tests. Boot proof on 6076: 0 worker errors, 0 watchdog kills, 0 load timeouts, React chrome renders; `s#1`'s first guest turn traps on `app-definition.interactive-job-classification: unclassified interactive command 's.space.studio@1/*#editor:setAppRegistrations'`. Current source still classifies it `BatchOnlyPendingRewrite` (`🪐️space/⚙️engine/🪐️space/🦀️.rs:975`), so the studio host commands need the owned-factory migration before any rebuild can boot.
- 19:40 The `s` rebuild compiled and transpiled (fresh 64 MB core + component JS in the cache at 19:26) but `describeBuiltPlugin` failed with `Plugin descriptor identity mismatch`: the fresh component's `manifest.pluginId` is `space` because the 03:53 auto-commit changed `✏️s/🔌️plugins/🪐️space/🦀️.rs:603` from `builder("s")` to `builder("space")`, while the Cargo component package (`semio:s`), registry row, host config, deployment catalog and committed descriptor all say `s`. Restored `builder("s")` and relaunched the incremental rebuild (`plugin-build-s-5.txt`); asked lane J to add a native identity test. Lane J (`space-studio-migration`) dispatched 19:20 to migrate the studio host commands (`setAppRegistrations` + 29 more `BatchOnlyPendingRewrite` rows in the space engine).
- 20:58 Lane D done (`📓️opus-norm-shared-factory.md`): all 45 norm actions `Migrated` through one shared `NormBoundedCommandJobFactory<A>` + `NormOneItemPreparationFactory<P,M>` in `📕️norm/🖥️app-surface/🦀️.rs:313-780`, per-app wiring only; found and fixed the LIFO drain of `emit.artifact_mutations` in the retained lane; `cargo check -p semio-s-plugin-norm --lib` EXIT 0 (72 min); TS drift oracle green and registered as `⚖️gate📕️norm🟦️retained-cohort`; fixture schemaVersion 2. Its four Rust tests are unverified: a peer deleted `target-s-e2e` mid-build (5 h 48 min lost) and the retry is queued. `target-s-e2e-wasm` (the `s` rebuild) survived.
- 21:30 Incremental `s` rebuild (build 5, 97 min, log `plugin-build-s-5.txt`) compiled the space crate with lane J's in-progress edits and the fresh component's own `describe()` returns the `assembly-failed` stub, so `describeBuiltPlugin` rejects it and the cache keeps the stale descriptor pair (new core + component JS landed 21:22). Forwarded to lane J: native `plugin()` assembly must pass before the next rebuild. Machine: swap 61 of 62 GB used, load ~100.
- 21:36 The `assembly-failed` stub's label named the cause: `component package identity must exactly match semio:<plugin-id>`; the 03:53 auto-commit had also changed `✏️s/🔌️plugins/🪐️space/🦀️.rs:812` to `.package_id("semio:space")`. Restored to `semio:s` (Cargo component package, registry, host config and deployment catalog all say `s`); rebuild 6 launched (`plugin-build-s-6.txt`). Both literals are now the subject of lane J's identity test.
- 21:45 Lane H done (`📓️opus-router-resilience.md`): `AppRouter.build` total on both sides (kernel TS :573-692 and Rust host `register_manifest` stage-then-commit), typed per-plugin faults with `pluginFaults()`/`faultFor()`, shared fixture `🎠️kernel/🧫️fixtures/🧫️app-router-plugin-faults`, assembly gate `plugin-assembly.surface-dependency-gate` in `try_build`, testkit `assert_surface_dependencies_declared`, ShellHost probe rows carry `routerFault`; demonstrator's seven `.depends_on` kept plus a property test; kernel vitest 50/50, react 2/2, Rust fixture law ok; only the demonstrator native test still compiling. The cached demonstrator core (Aug 27) still needs the Wave 2 rebuild.
- 21:45 Lane J done (`📓️opus-space-studio-migration.md`): the trap was partly a stale-core artifact (current source only rejects `Unclassified`), but studio/home/space-index were unconstructable for three other reasons (proof `controller:` set to the UI id instead of the app id, home proof/factory contract mismatch, space-index with 12 `Migrated` ids and no factory) — all fixed; studio 15/40 migrated (the five shell-dispatched ids `setAppRegistrations`, `openSpace`, `openInstance`, `importSpacePackPayload`, `spawnApp`), home 18/18, space-index 14/14; shared one-item Store preparation authority in `🪐️space/🦀️.rs:575-790`; fixtures + 7 Rust laws + identity test (`manifest_plugin_id_matches_the_cargo_component_package` via `include_str!` of Cargo.toml) + TS three-way pin; native `cargo check -p semio-s-plugin-space --lib` RED only through a peer's in-flight `💻️os/🖥️host/🦀️.rs` async/dyn migration (10 errors, none in the plugin), so the Rust laws are unexecuted. Build 6 (21:35) already covers its production edits.
- 21:55 Rebuild 6 assembles to the stub with label `artifact identity is not owned by the declaring plugin`: lane H's new assembly gate derives the owner of every artifact kind from the canonical `s.<plugin>.<kind>` grammar (`ArtifactKindId::plugin()`), and the space plugin's kinds are `s.space.shome`/`s.space.sspace`/`s.space.studio` while its id is `s`. That is the same inconsistency the 03:53 auto-commit tried to fix from the Rust side alone (`builder("space")`, `package_id("semio:space")`), which broke against the registry/host/deployment authorities that still say `s`. Decision: the plugin identity becomes `space` everywhere (Cargo component `semio:space`, registry projections, deployment catalog row + directory `🪐️space`, host config, shell/harness expectations derived from the catalog, fixtures); the playground/launch variant stays `s` (it names the OS shell variant, not the plugin). Lane K (`space-plugin-identity`) dispatched.
- 23:20 Opus limit hit a third time (~22:00-23:10) during lane K's census; resumed at 23:18 with its context. Lanes D and H each still have one detached native `cargo test` compiling in `target-s-e2e-h`.
- 00:17 (2026-09-06) Lane K's rename has landed on disk (Cargo `semio:space`, Rust `builder("space")` + `.package_id("semio:space")`, deployment row `{space, 🪐️space}`, cache dir moved, registry row `space`/`semio:space`, fixtures); lane K is finishing tests/report. Coordinator launched rebuild 7 (`SEMIO_PLUGIN_ONLY=space`, `target-s-e2e-wasm`, log `plugin-build-space-7.txt`) and relaunched the served shell (`dev-s-served-7.txt`); the descriptor watcher will re-probe the boot when `✏️s/🔌️plugins/🪐️space/🔣️.json` is re-emitted.
- 01:12 Lane K done (`📓️opus-space-plugin-identity.md`): host plugin identity is `space` in every authority (Cargo `semio:space`, root builder/package id, deployment row `{space, 🪐️space}`, cache dir moved, taxonomy path patterns, fixtures/specs, regenerated projections `🧩️plugins.ts`/`🖥️hosts.rs` `("s","space")`); variant stays `s`; new `plugin-identity-check` (11 checks) + lane J's oracle (23 checks) clean; `check-frame-worker` green again as a side effect. Rebuild 7 of the `space` crate running since 00:17 under load average ~200.
- 04:40 (2026-09-06) The machine rebooted again at ~02:49 (session logs in the scratchpad lost; Opus limit also hit a fourth time ~01:20, reset 04:10). Rebuild 7 had produced the renamed component (core + JS at 02:09) and its own `describe()` now decodes cleanly to `pluginId: space`, `packageId: semio:space`, 5 apps — the identity chain is consistent for the first time — but the owner pair/bridge in the cache were not re-emitted before the reboot. Rebuild 8 (`SEMIO_PLUGIN_ONLY=space`, `plugin-build-space-8.txt`) launched at 04:37 under load average 15; shell relaunched on 6070.
- 04:55 Rebuild 8 succeeded (`described space`, owner pair + bridge re-emitted 04:41). Boot probe: the renamed plugin loads, its first guest turn panics in the framework at `🔌️plugin/🦀️.rs:19552` with `interactive-job.catalog-controller: tool proof catalog controller does not match the live registry controller`. Cause: `validate_tool_job_rows` compares `AppActionRegistry.controller_id` (= `definition.controller_id`) with the runtime surface app id; the studio (`⚙️engine/🪐️space/🦀️.rs:1086`) and home (`🏠️home/…/✏️editor/🦀️.rs:675`) apps overrode `definition.controller_id` to `s-play`/`s-home` while the framework default (`🦀️.rs:4871`) and the reference plugin lowpoly keep the surface id. Removed both overrides, updated the two assertions, launched rebuild 9 (`plugin-build-space-9.txt`) with an automatic boot probe.
- 05:20 Rebuild 9 described cleanly; the boot probe passed the catalog-controller gate and the guest ran eight cooperative-maintenance turns, then the shell failed in `PluginRuntime` `wireNatural` with `actor WIT integer is outside the JavaScript safe range: [object Object]`. Cause: the COMPLETE-SEMIO fleet's 03:51 commit (5e03e56997, packet `📓️terra-pack-dynamic-integer-lossless-current-packet.md`) made `decodePackValue` return lossless `PackInteger` carriers `{kind, value: bigint}` and shipped `packValueToExactJson`/`packUIntSafeOrNull` as the projection, but the shell's UI wire decoder still treated pack values as JSON. Fix: `decodeWirePack` projects every op payload through `packValueToExactJson`, `wireNatural` accepts WIT `bigint`s and `PackInteger` carriers via `packUIntSafeOrNull` and names the field/value on failure; new test "projects lossless pack integer carriers onto exact node ids, children and revisions" (react `🔬️index.test.ts`, 5/5 in the suite). Same class of raw decode remains in the wgpu `🐚️plugin-bridge.ts:171-175` (Wave 3) and `ShellHost/🟦️.tsx:1857` identity payload (to verify).
- 05:15 First booted shell census (console dump, 744 lines): 23 plugins fail to install — 19 `plugin.descriptor-unavailable` (block, playbook, playbook-module-procedural, flow-extension-bim/draw, imperative-extension-{control,effect,logic,math,text}, process-extension-{concrete,metal,robotic,wood}, sourcing-module-{beams,slabs,windows}, stdio, trinity) and 4 `plugin.descriptor-identity-mismatch` (the CAD placeholder extensions); demonstrator is excluded by the router (stale core without `.depends_on`). Launched the full catalog rebuild (Wave 2 lane F, coordinator-run: `SEMIO_PLUGIN=s … framework-os-dev:plugin`, all 59 crates serially into `target-s-e2e-wasm`, 4 h budget, log `plugin-build-catalog-1.txt`) and lane A's `verify catalog` smoke against the booted shell (`🗑️generated/coordinator/catalog-smoke-2`). Dispatching lane L (`wgpu-tier`) for Wave 3.
- 05:30 Catalog smoke run 2 against the booted shell: beacon `ready:s` at 15.5 s (commit 105 ms, first module 111 ms), then `studio never opened: Execution context was destroyed` — `openStudioE2e`/`waitForStudioE2eCondition` evaluated `location.pathname` across the studio route navigation. Fixed to read `page.url()` and tolerate a destroyed context during `count()`; smoke run 3 started (`🗑️generated/coordinator/catalog-smoke-3`). Full catalog rebuild is in its first cargo phase in `target-s-e2e-wasm`.
- 05:45 Home renders real content (studio list with "Demo Studio", "Create Space"), but "open" and Meta+n (`mod+n` → `createStudio`) are dropped silently: ShellHost routes an action by `action.controllerId === app.controllerId` (`🟦️.tsx:4397-4401`) and the Home/Studio/space-index action descriptors still used the short controller ids (`s-home`, `s-play`, `s-space-index`) while their definitions now carry the surface app id (the registry gate's requirement). Set `S_HOME_CONTROLLER_ID`/`S_PLAY_CONTROLLER_ID`/`SPACE_INDEX_CONTROLLER_ID` to the surface app ids so descriptors, definitions and the proof catalog agree; rebuild 10 of `space` queued behind the running catalog rebuild (same private wasm target).
- 11:15 The 05:14 catalog rebuild spent its whole 4 h budget under load 60-80 without describing a single plugin (the first crate, animate, was still waiting on stdio's wasm codegen) and was killed by the outer nx wrapper budget (`SEMIO_CMD_BUDGET_MS`). Relaunched at 11:10 on an idle machine (load ~10) with `SEMIO_BUILD_BUDGET_MS=SEMIO_CMD_BUDGET_MS=28800000` (`plugin-build-catalog-2.txt`) and a watcher that prints each real `described <plugin>` line. Opus limit hit a fifth time (~06:20-09:30) and killed lane L mid-work; `SendMessage` is no longer available in this session, so lane L2 was launched fresh with instructions to pick up lane L's on-disk edits.
- 11:20 Catalog rebuild 2 died at 11:13 with `No space left on device` while archiving stdio's rlib; minutes later every `target*` root under the repo (including this ticket's private `target-s-e2e*` dirs) was gone and 70 GB were free again — a peer's disk sweep. Rebuild 3 launched cold into a fresh `target-s-e2e-wasm` (`plugin-build-catalog-3.txt`, 8 h budgets) on an idle machine.
- 11:35 Rebuild 3 stopped at the pre-flight `assertExtensionOutputsFresh`: 13 extension cache dirs (`🧩️extension-modules/*/🧵️plugin-worker.js`, Sep 1) still carried the retired per-plugin worker. Deleted those legacy outputs (the pipeline emits only the shared shard worker now) and launched rebuild 4 (`plugin-build-catalog-4.txt`).
- 12:05 Identity conformance audit of all 59 rows (`📓️explore-plugin-identity-conformance.md`): 57 PASS; `reasoning-mindmap` (builder `reasoning`/`semio:reasoning` vs Cargo/deployment `reasoning-mindmap`) and `imperative-extension-effect` (`EXTENSION_ID` says `imperative-extension-core`) FAIL the same gates that cost `space` four rebuilds; 19 rows without a descriptor pair, 1 true placeholder, 10 extension pairs with empty `packageId` (no extension calls `.package_id`), 25/26 extensions `.extends()` without `.depends_on()`. Lane M (`plugin-identity-conformance`, Opus) dispatched to fix both identities with lane K's recipe before rebuild 4 reaches them. ShellHost routing (`🟦️.tsx:4396-4408`) falls back to the current session for an unmatched controller id, so Home's dropped actions failed inside the guest's controller lookup — consistent with the constant alignment; other plugins' short `*_CONTROLLER_ID` window-chrome descriptors need a live click check once the catalog smoke runs.
- 12:35 Lane L2 done (`📓️opus-wgpu-tier.md`): cheap gates green (lint, browser-worker 32/32, preview-generated 15/15, frame-worker regenerated), puzzle's wasm bridges and the renderer's missing `semio-framework-async`/`wasm_bindgen` declarations fixed, four upstream relocations followed — but `semio-framework-os-renderer-wgpu` still has 49 wasm / 74 native type-level errors at committed HEAD (serde-elimination fallout, `dsl::Fault` without `Display`, actor/kernel/Shell shape drift, `Rc`-vs-`Send` in the frame job, borrowck), so no wgpu boot and no parity run yet; `#semio-wgpu-canvas` mount fixed in the trunk shell, `dumpStructure` introspection needs a frame-transport message pair. Wave 3 re-planned: lane N (`wgpu-crate-repair`, Opus) dispatched.
- 12:55 Lane M done (`📓️opus-plugin-identity-conformance.md`): `reasoning-mindmap` → `reasoning` in every authority (Cargo `semio:reasoning`, deployment `{reasoning, 💡️reasoning}`, cache dir, taxonomy, projections), `imperative-extension-effect`'s `EXTENSION_ID` plus two live `register_native_imperative_module("imperative-extension-core")` call sites fixed, a repo-wide `🪪️plugin-identity.test.ts` gate over all 59 rows (3/3), a lane-K storybook barrel breakage repaired; cosmetic remodel/procedural schema-id drifts confirmed unreachable by the owner gate. Native checks: effect extension green; reasoning/note crates RED under a peer's in-flight async-convention sweep (426/807 errors, not ours).
- 13:20 Rebuild 4 census so far: animate and architect fail with `E0053`/`E0308` storms because their mutation impls still declare `async fn inverse/diff/label/target/…` while the framework traits have been synchronous since 2026-09-01 (peer tickets ENERGY/FEM/BLOCK/… de-async their own plugins; nine plugins nobody owns today still carry 431 async impl files: architect 268, note 35, shooting 33, layout 27, mathematical 18, dag 16, forms 12, reasoning 12, sequence 10). Lane O (`plugin-de-async`, Opus) dispatched to de-async those nine per the ENERGY ticket's `w6-de-async.md` table, smallest crate first; the serial rebuild continues with the crates that compile.
- 14:15 Rebuild 4 per-plugin results so far: animate, architect FAIL (async mutation impls, lane O), block FAIL at describe (`app-definition.invalid: app s.block.block3d@1/*#viewer document must contain non-empty segments`, owned by 26/09/05/BLOCK-PLUGIN-END-TO-END), cad-extension-aec-building FAIL (`resolve_ready` around the now-synchronous `ArtifactContribution::build()`, fixed by the coordinator at `🏢️aec-building/🦀️.rs:220`), cad and the other three CAD extensions compiled and are in the materialize pool. Disk: 58 GB free after removing lane M's target; lanes N/O targets 23/18 GB.
- 14:30 Disk fell to 21 GB free (rebuild 4's debug-info wasm target plus lanes N/O). Killed rebuild 4 (no plugin described yet), removed its target and relaunched rebuild 5 with `CARGO_PROFILE_WASM_DEV_DEBUG=false` (stdio artifact 8.6 GB → 165 MB per memory) in a fresh `target-s-e2e-wasm` (`plugin-build-catalog-5.txt`, 8 h budgets).
- 14:50 Lane N done (`📓️opus-wgpu-crate-repair.md`): `semio-framework-os-renderer-wgpu` 49 wasm / 74 native errors → 0/0; serde fallout moved to `dsl::os_pack::json`; `Fault::describe()` added to the framework with tests; ~30 shape-drift sites, a real `⚙️EngineCanvas` cursor bug, `ActorUiPatchReceipt` threaded through `apply_ui_patch`; `Send` bounds moved to the job-transfer points (`JobThreadTransfer`, no `unsafe impl Send`); 13 borrowck fixes; all four cheap gates green; first real browser wasm built (116 MB, 24 min). Blocked for boot: a peer's live edit in `🌊️flow/🖥️host/🦀️.rs` (27 `retire_cold` calls without `use neural_engine::ColdRetire;`, 18 errors) and the crate's own test suites (56 pre-existing errors). Lane P (`wgpu-boot-parity`) to follow.
- 18:15 Rebuild 5 ended with 0/59: swap exhausted (82.5 of 84 GB, 55 % system CPU) so animate's rustc was SIGKILLed after ~1 h 50 min of stdio codegen, then every remaining crate failed instantly on a peer's in-flight framework edit (`error[E0432]: unresolved import wit_bridge::drain_task_resumes` in `semio-framework-plugin`, 58×). The private wasm and wgpu targets were swept again meanwhile (91 GB free). Lesson recorded: drop `*/incremental` under pressure (freed 36 GB) and run lanes with `CARGO_INCREMENTAL=0`. Rebuild 6 is armed to start as soon as the plugin crate's `drain_task_resumes` resolves.
- 18:25 Lane O done (`📓️opus-plugin-de-async.md`, codemod `🔨️lane-o-de-async.py`): 5383 declarations de-asynced across 1710 files in nine plugins, 62 `.await` removed, async-shaped errors 2418 → 24 (all three framework fns still async: `ActionDefinition::bounded_catalog`, `engagement_token_matches`, `selection_domains_from_surface`); native `--lib --tests` errors 8824 → 876; wasm32: forms 0, the other eight 23-211 residual errors from the mutation-descriptor, UiNode/`Label` and interactive-job waves owned by other tickets. Framework `semio-framework-plugin` RED since ~16:47 (`crate::host::outcome_to_result` missing at `⚛️reactor/🔄️turn/🦀️.rs:199,240`, a peer's reactor rewrite) — every `s` plugin is unbuildable until it lands.
- 20:30 Rebuild 6 (auto-started by a too-loose import heuristic) failed 59× on the peer's reactor split (`wit_bridge::drain_task_resumes`, now defined in the new `⚛️reactor/🔄️turn/🦀️.rs:821`); the peer is still editing (`⚛️reactor/🦀️.rs` mtime 20:22). My compile gate was wrong too: `outcome_to_result` is `cfg(component-guest, wasm32, p2)`-gated, so a bare `cargo check --target wasm32-wasip2` of the framework plugin crate cannot pass; re-arming the gate with the guest feature the plugins enable.
- 20:30 React shell re-verified after the day's changes: headless probe reaches `ready:s` again (34 descriptor-unavailable + 3 identity-mismatch plugin installs remain, all pending the catalog re-emission). The framework plugin crate compiles for wasm32-wasip2 with `component-guest` as of 20:23 (the peer's reactor split landed), and rebuild 7 auto-started in `target-s-e2e-wasm` with `CARGO_INCREMENTAL=0` and debug info off (`plugin-build-catalog-7.txt`).
- 20:40 Rebuild 7's nx wrapper exited at 20:32 but the underlying `bun ./📜️script.ts plugin` (pid 50088) kept running as an orphan with its cargo child on animate; the watcher now follows that pid. Disk 69 GB free, guard armed.
- 21:50 Machine census while rebuild 7 sits on animate (1 h 20 min): 12 concurrent `rustc semio_s_plugin_stdio` from ~15 cargo invocations in peer tickets (energy ×10 in `target-energy-e2e`, puzzle ×3, procedural, flow, gis, raster, remodel, lowpoly, p3d), swap grew from 8 to 66 GB and took the root volume down to 4 GB free; freed 29 GB by deleting lane O's finished target and pruning lane P's incremental caches. Every session is paying stdio's wasm codegen separately; the catalog rebuild cannot finish while that lasts, so the plan waits rather than adding load.
- 22:30 Rebuild 7 died with the root volume full (283 MB free; swap files at 73 GB, twelve peer stdio compiles); its animate step never finished after ~2 h. Deleted the dead `target-s-e2e-wasm` (10 GB) to keep the machine alive. The catalog rebuild must be restarted from scratch when the box is calm; the React shell itself still boots to its beacon on the existing cache.
