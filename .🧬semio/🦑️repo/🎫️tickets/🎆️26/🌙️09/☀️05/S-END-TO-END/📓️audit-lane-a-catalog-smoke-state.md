# Audit — Lane A (`catalog-smoke-harness`) on-disk state

Auditor: Sonnet 5 (read-only). Scope: verify the 4-item brief for lane A, terminated mid-work by an API limit at 06:00 per `📋️plan.md:57`. All paths below are absolute-relative to `/Users/ueli/Documents/semio`, all findings validated against on-disk content, not the plan's own claims.

## Verdict summary

| # | Item | Status |
|---|---|---|
| 1 | Storybook `🟦️plugins.ts` import fix | **DONE** |
| 2 | `test quick` honest within 30 s | **PARTIALLY DONE, currently unverifiable** — see blocker |
| 3 | `verify catalog` (`runCatalogSmokeVerify`) harness | **DONE, functionally complete** |
| 4 | Run against a live shell | **NOT lane A's job** (per brief) — not attempted, correctly out of scope |

Additionally found but not in the original 4-point brief and already landed: Nx `catalog-smoke` target, launch.json/seed registration, unit tests, JSON fixture + JSON-Schema fixture, real (non-hardcoded) DOM plugin-status probe.

## 1. Storybook import fix — DONE

- `.storybook/framework/os/index.tsx:10` imports `PLUGIN_BUILD_TARGETS`, `pluginModuleUrl`, `PluginBuildTarget` from `../../../🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📇️registry/🤖️generated/🧩️plugins.ts` — the real generated file (37,479 bytes, mtime 2026-09-05 13:21, exports `PluginHostMetadata`/`PluginBuildTarget`/etc., header `/** @generated by framework/plugin/registry/script.ts — do not edit. */`).
- `.storybook/os-plugins.spec.ts:10` imports from the same real path.
- `.storybook/stories/framework/os/plugins.stories.tsx:9` imports `OsBootHost`/`PLUGIN_BUILD_TARGETS` from `../../../framework/os/index.tsx` (the fixed file above) — story title `"🛠️framework🖥️os/Plugins"`, matching the spec's `PLUGINS_STORY_TITLE_ID = "🛠️framework🖥️os-plugins"` (`.storybook/os-plugins.spec.ts:12`).
- Repo-wide filename scan (`find`, no content search) for any remaining `🟦️plugins.ts` turned up zero hits — the broken import path is gone everywhere.
- Both edited storybook files' mtimes (2026-09-05 04:08) fall inside lane A's working window before the 06:00 termination.

**Conclusion: item 1 is complete and consistent end to end.**

## 2. `test quick` honesty — landed change is real, but currently unverifiable due to unrelated live churn

- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/🧪️tests/🟦️.ts` (mtime 2026-09-05 05:51) gates the two expensive WIT-mapping in-source suites (`../../../🔌️plugin/📤️return/🟦️.ts`, `../../../🔌️plugin/📥️poll/🏘️composition/🟦️.ts`, "~60 s each, measured 2026-09-05") behind `testLevelAtLeast("long")` (line 18), keeps `environment: "node"` instead of `"jsdom"` under `quick` (line 37, "jsdom's ~7 s environment setup ... costs a quarter of the level's whole wall-clock budget"), and fixed a double-collection bug ("listing them in BOTH keys made Vitest collect them twice", lines 38-39). This is a real, targeted fix matching the brief.
- The quick budget itself (30,000 ms hard SIGKILL) lives in shared library `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🟦️.ts:1131-1136` (`TEST_LEVEL_BUDGET_MS.quick = 30_000`) and is enforced by `runTestBudgeted` (lines 1307-1332), which SIGKILLs the whole process tree and logs `[budget] ... exceeded <ms>ms — killed.` — this is the exact message quoted in `📋️plan.md:18`. Not lane A's code; pre-existing infra lane A's config change targets.
- **Verification attempt, isolated**: `bunx vitest run --config "🧰️framework/…/🧪️tests/🟦️.ts" -t "catalog smoke aggregation"` (run at 13:27:35) → **4 passed, 84 skipped, in 53.02 s wall-clock** (transform 35.11 s + import 49.67 s + tests 1.98 s — the actual assertions are near-instant, the cost is entirely bundling/importing the 430,971-byte `📜️script.ts`). This ran and passed cleanly at that moment.
- **Verification attempt, real command** (`cd .../🟦️typescript && bun ./📜️script.ts test quick`, run at ~13:30) and the requested bundle check (`bun build '…/📜️script.ts' --target bun --outdir <scratchpad>/audit-bundle`) **both fail identically**, before reaching any test:
  ```
  ✘ [ERROR] No matching export in "…/📚️library/🗂️workspaces/🟦️.ts" for import "getWorkspaceRoot"
      …/📚️library/🎮️playground/🟦️.ts:9:9
  ```
- **Root cause traced, and it is NOT lane A's or this ticket's regression**: `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🎮️playground/🟦️.ts` is an **untracked** file (`git status` → `??`), mtime 2026-09-05 **13:30** — i.e. it appeared/changed *during this audit*, after my first (successful) isolated vitest run at 13:27:35 and before my second (failed) run at 13:31. It imports `getWorkspaceRoot` from `📚️library/🗂️workspaces/🟦️.ts` (mtime 2026-09-03 19:55, exports only `computeWorkspaces` and `diffWorkspaces` — no `getWorkspaceRoot`). The shared library entry point `📚️library/📦️packages/🟦️typescript/🟦️.ts:19,6581` both imports from and re-exports `🎮️playground/🟦️.ts`, so this break is transitively pulled into essentially every TS build/test in the repo, including `📜️script.ts` itself and its vitest config — a concurrent peer session is mid-edit on this shared library file right now. This matches the repo's known pattern of concurrent workspace churn (see `📋️plan.md:54` "Peer coordination" note for a same-shaped incident).
- **Consequence for this audit**: I cannot currently re-confirm `test quick`'s wall-clock behaviour end-to-end (whether the WIT-mapping gate alone is sufficient, or whether the 35 s transform + 50 s import overhead of the giant `📜️script.ts` module — independent of WIT mapping — itself risks the 30 s hard kill once concurrent-edit noise settles). The one clean measurement I got (53 s for a *filtered* vitest invocation, not the real budgeted command) is concerning on its own: even with WIT mapping correctly excluded, transform+import alone already exceeds the 30 s budget by ~1.7x. That number came from a single cold run under audit load and is not authoritative, but it is not reassuring either.

**Conclusion: the config-level fix (item 2's actual scope) is real and targeted; whether `test quick` is now honest under the 30 s ceiling is unresolved — partly because of unrelated live repo churn blocking verification right now, and partly because even the clean run's transform/import cost alone is close to or over budget.**

## 3. `verify catalog` / `runCatalogSmokeVerify` — DONE, functionally complete

All in `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/📜️script.ts` (mtime 2026-09-05 06:13, region `#region 🔖️CatalogSmokeVerify` lines 2043-2236):

- **CLI routing**: `VerifyScript.run` (class declared line 2918, registered at line 5022 via `.register("verify", VerifyScript)`) branches on `segments[0] === "catalog"` at **lines 2932-2939**: parses `--out` (falls back to `CATALOG_SMOKE_DEFAULT_OUT_REL`, line 2079: `🧰️framework/…/🧑‍💻dev/🤖️generated/🔬️catalog-smoke`), calls `runCatalogSmokeVerify(studioUrl, { outDir, timeoutMs, perProgramMs })`, computes `catalogSmokeExitCode(report)`, and `throw`s (BundleScript's convention for non-zero exit, matching the sibling `collab`/`e2e` branches) when the report failed.
- **Spawns every program via the palette**: `spawnCatalogSmokeProgram` (lines 2143-2164) opens the command palette (`openStudioE2eCommandPalette`), fills the plugin id, clicks `[data-command-item-id="spawn.<pluginId>"]` — exactly the `spawn.<pluginId>` palette-item mechanism the brief specifies.
- **Non-zero box + descendants assertion**: lines 2213-2221 read `getBoundingClientRect()` and `querySelectorAll("*").length` for the spawned window element; `rendered = width>0 && height>0 && descendants>0`; a row is `"pass"` only if `rendered && firstError === null`.
- **Console/page-error collection**: lines 2178-2181 (`page.on("pageerror"/"console")`) feed a shared `errors[]` array; each program's `firstError` is sliced from an `errorCursor` taken before the spawn.
- **Plugin install-status reporting**: `probe.plugins` (from the shell's own DOM probe, see §4) feeds `summarizeCatalogSmoke`'s `failedPlugins` (lines 2099, 2228) — pluginIds whose status is `"failed"` or `"crashed"`.
- **Report output**: `writeFileSync(…, 🔬️catalog-smoke.json)` and `…🔬️catalog-smoke.md` (lines 2229-2233), both under `opts.outDir`.
- **Exit code**: `catalogSmokeExitCode` (lines 2104-2107) — non-zero if `totals.fail > 0 || failedPlugins.length > 0 || totals.pass === 0` (the last clause also catches "probe never populated / zero programs spawned").
- **Nx target**: `📋️project.json:186-198` — `"catalog-smoke"`, `cache: false`, command `bun ./📜️script.ts verify catalog`, matching the `collab-e2e` target's shape exactly (`📋️project.json:173-185`).
- **launch.json / seed registration**: `.vscode/🧩️launch.seed.jsonc:5640-5650` — `"⚖️gate🔬️catalog-smoke"`, `type: "node-terminal"`, `command: "bun nx run @semio-tech/framework-os-dev:catalog-smoke"`, group `"4_gate"`, order `410.99` — placed immediately after and mirroring `"⚖️gate🌎️collab-e2e"` (`5629-5639`, order `410.98`). The **generated** `.vscode/launch.json:7143-7146` already carries the same entry, so the launch generator has already been re-run since this was added. Both files show as modified in `git status --porcelain`.

### Unit test for aggregation — exists and passes

- `📜️script.ts:6803-6837`, `describe("catalog smoke aggregation", …)` (in-source `import.meta.vitest` block spanning lines 5114-6838) — 4 cases:
  - folds `input` → `report`/`exitCode`/`markdown` against a committed fixture, byte-for-byte (`toEqual`/`toBe`).
  - validates the fixture `report` against the JSON-Schema fixture with an **independent validator** (`ajv`), including two deliberately-invalid mutations that must fail validation — this satisfies CLAUDE.md's "at least one test with a third-party library to validate our own implementation" rule.
  - exit-code matrix: failed program / failed plugin / empty catalog all non-zero, all-pass → zero.
  - markdown-cell escaping (pipe/newline) doesn't corrupt the table.
- **Ran it** (isolated, before the concurrent-edit break landed): `bunx vitest run --config "…/🧪️tests/🟦️.ts" -t "catalog smoke aggregation"` → **all 4 passed** (see §2 for the full run log). This is the closest I could get to "`bun test`" for this suite — the package has no native `bun test` file; its tests are in-source vitest blocks routed through `bun ./📜️script.ts test [level]`, which is currently blocked by the unrelated library break (§2).
- **Wired into vitest config**: yes — `🧪️tests/🟦️.ts:18` always includes `"📜️script.ts"` in `includeSource` regardless of level (only the WIT-mapping suites are level-gated), so this test runs even under `test quick`.

### Fixtures

- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/🧫️fixtures/🔬️catalog-smoke.json` (2,840 bytes, mtime 05:15) — `input`/`report`/`exitCode`/`markdown`, internally consistent (3 programs: pass/fail/skipped; 2 failed plugins; markdown table matches).
- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/🧫️fixtures/🧬️catalog-smoke.schema.json` (2,006 bytes, mtime 05:16) — draft-07 schema, `additionalProperties: false` throughout, matches `CatalogSmokeReport`'s shape exactly field-for-field (including nested `programs[]`/`failedPlugins[]`/`totals`).
- **Note**: both fixtures live directly under `🧑‍💻dev/🧫️fixtures/`, *not* under `🧑‍💻dev/📦️packages/🟦️typescript/🧫️fixtures/` (which only has an unrelated 175-byte `🔣️.json` from Aug 27) — this matches the path the test itself reads (`join(repoRoot, "🧰️framework/…/🧑‍💻dev/🧫️fixtures/🔬️catalog-smoke.json")`, `📜️script.ts:6805`), so it is correct, just worth knowing for anyone searching by directory instinct.

### Dependency note (minor, not blocking)

- `ajv` is imported dynamically (`await import("ajv")`, `📜️script.ts:6815`) but is **not declared** in `🧑‍💻dev/📦️packages/🟦️typescript/package.json`'s `dependencies` or `devDependencies`, nor in the root `package.json`. It resolves today only because some other workspace package hoists `ajv@8.20.0` into the root `node_modules`. This is dependency-hygiene debt (an undeclared transitive import) rather than a functional break — the test passed — but should be added explicitly as a devDependency before someone reruns `bun install` in a state where the hoist disappears.

## 4. DOM-exposed plugin status — real, not hardcoded

`🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🧱️elements/🏛️ShellHost/🟦️.tsx`, region `#region 🔖️CatalogSmokeProbe` (**lines 7584-7610**):

- Exposes `window.__semioOsCatalogProbe` (not `data-semio-plugin-status` / `__semioOsPluginStatus` as the audit brief's grep terms guessed — those exact identifiers do not exist anywhere in this file; the real name is `__semioOsCatalogProbe`, and the harness in `📜️script.ts` already reads that name correctly at line 2136).
- Dev-only guard: `import.meta.env.DEV` (line 7593) — "Never defined in a production build" per its own docstring (line 7589).
- `plugins: registry.map((entry) => ({ pluginId: entry.pluginId, status: pluginStatusById[entry.pluginId] ?? "available" }))` (line 7602) — `pluginStatusById` is destructured from `shellState.pluginRuntime` (line 1398), the shell's real live plugin-runtime state (also feeds the actual rendered plugin-status UI elsewhere in this same file, lines 5737/5753/5765) — confirmed not a stub/hardcoded map.
- `programs`/`spawned` are likewise read from live `panel?.programs`/`panel?.spawnedApps`, not a static list.
- The existing readiness beacon (`data-semio-os-ready`/`-error`/`-not-found` on `document.documentElement`, region `#region 🔖️ReadinessBeacon`, lines 7532-7582) is what `runCatalogSmokeVerify` waits on before reading the probe (`📜️script.ts:2190-2193`) — correctly sequenced.

**Conclusion: item 3's DOM-probe half is real and correctly wired; only the specific global names guessed in the brief were wrong, the underlying mechanism is present and functioning.**

## Remaining steps for the next implementer

1. **Do not touch** `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🎮️playground/🟦️.ts` or `…/🗂️workspaces/🟦️.ts` yourself — that break is a concurrent peer's in-progress, untracked edit (confirmed via `git status` showing `??` and a mtime newer than this audit's own probes). Poll `git status`/mtime before assuming it's still broken; once the peer lands (or is asked to add the missing `getWorkspaceRoot` export, or finish removing the dependency), re-run:
   - `cd /Users/ueli/Documents/semio && bun build '🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/📜️script.ts' --target bun --outdir <scratch>` — must succeed with zero errors.
   - `cd .../🧑‍💻dev/📦️packages/🟦️typescript && bun ./📜️script.ts test quick` — must complete **and** stay under the 30 s hard kill (watch for `[budget] … exceeded 30000ms — killed.` on stderr). If it still blows the budget once the library is fixed, the transform/import cost of the 430 KB `📜️script.ts` itself (35 s transform + 50 s import measured in isolation, §2) is the next thing to trim — e.g. splitting `📜️script.ts`'s in-source tests into a real `*.test.ts` file so vitest doesn't have to import the entire task router to run four aggregation assertions, or moving `runCatalogSmokeVerify`'s Playwright-dependent code path behind a lazier import so `quick`-level collection doesn't pull `playwright` in at all.
2. **Add `ajv` as an explicit `devDependency`** in `🧑‍💻dev/📦️packages/🟦️typescript/package.json` — it is used (`📜️script.ts:6815`) but currently relies on hoisting from an unrelated package.
3. **Run `verify catalog` against a live served shell** (explicitly out of lane A's/this audit's scope per the brief, but the next real gate before this item can be called done): boot `dev s served`, then `bun nx run @semio-tech/framework-os-dev:catalog-smoke` (or the VS Code launch entry `⚖️gate🔬️catalog-smoke`), inspect the written `🔬️catalog-smoke.json`/`.md` under `🧰️framework/…/🧑‍💻dev/🤖️generated/🔬️catalog-smoke/`, and confirm it reflects the real current catalog state (57/59 rows built per `📋️plan.md:20` — expect several `fail`/`skipped` rows for `draw`/`layout`/`energy` and the 19 rows without owner descriptors, not a clean all-pass; a suspiciously all-green report would itself be a red flag worth re-checking).
4. **Re-verify the `.storybook/os-plugins.spec.ts` Playwright spec** once a served/story-served shell is available — it was not run as part of this audit (out of scope: no dev servers), only statically confirmed to import the fixed path.
5. No action needed on: the storybook import fix (§1, complete), the `verify catalog` CLI/harness/report code (§3, complete), the Nx target (§3, complete), the launch.json/seed registration (§3, complete), the fixtures (§3, complete), or the DOM probe (§4, complete).

## Files inspected (for traceability)

- `.storybook/framework/os/index.tsx`
- `.storybook/os-plugins.spec.ts`
- `.storybook/stories/framework/os/plugins.stories.tsx`
- `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📇️registry/🤖️generated/🧩️plugins.ts`
- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/🧪️tests/🟦️.ts`
- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/📋️project.json`
- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/package.json`
- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/📜️script.ts` (lines 1-45, 1861-2107, 2135-2951, 6770-6838)
- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/🧫️fixtures/🔬️catalog-smoke.json`
- `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/🧫️fixtures/🧬️catalog-smoke.schema.json`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🧱️elements/🏛️ShellHost/🟦️.tsx` (lines 1398, 5730-5790, 7530-7610)
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🟦️.ts` (lines 1-45, 1120-1340)
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🎮️playground/🟦️.ts`, `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🗂️workspaces/🟦️.ts`
- `.vscode/🧩️launch.seed.jsonc` (lines 5605-5650), `.vscode/launch.json` (lines ~7132-7146)
- `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️05/S-END-TO-END/📋️plan.md`, `🎫️ticket.json`

## Commands run

- `bunx vitest run --config "🧰️framework/…/🧪️tests/🟦️.ts" -t "catalog smoke aggregation"` — **passed** (4/4) at 13:27:35, wall-clock 53.02 s.
- `bunx vitest run --config "…"  -t "catalog smoke aggregation"` (retry) — **failed** at 13:31 with the `getWorkspaceRoot` build error (unrelated live churn, see §2).
- `cd .../🟦️typescript && bun ./📜️script.ts test quick` — **failed** with the same build error.
- `bun build '🧰️framework/…/📜️script.ts' --target bun --outdir <scratchpad>/audit-bundle` — **failed** with the same build error.
