# 🖨️ Raster plugin end to end — status

App under test: 🖨️raster of plugin `✏️s/🔌️plugins/🖨️raster` (`bun run dev:raster` → `bun ./📜️script.ts dev raster` → framework-os-dev playground; launch.json entries `🛠️dev🖼️raster⚛️react` :6060, `🛠️dev🖼️raster🧊️wgpu🌐️wasm` :6160, `🛠️dev🖼️raster🧊️wgpu🖥️native`).
Ticket opened 2026-09-06 00:00 by session ⚪feae8329 (Fable 5.1 coordinator). Repo MCP timed out at start; bookkeeping is manual on disk.
Ticket start commit: `🗑️generated/start-commit.txt` (3a6a9d6bfc).

## Definition of done
1. Raster crate `cargo check` green natively and on `wasm32-wasip2`.
2. Raster tests (Rust lib tests + oracle/fixture tests + TS oracle) green.
3. Owner-root `🔣️.json` / `🛂️.descriptor.semio` regenerated via `describe`; registry check accepts raster.
4. `bun run dev:raster` boots in the react renderer (and wgpu wasm): every window renders non-empty, examples load and switch, editor actions dispatch at runtime (no `interactive-job.missing-owned-reducer` / bounded-factory faults), confirmed with console logs.

## Log
- 2026-09-06 00:00 open. Host state at open: 32 GB RAM, swap 60.9/61.4 GB used, load avg 104, ~25 peer rustc processes (some 4-5 h old). No builds launched by this ticket until memory recovers; exploration is file-reading only.
- 00:05 exploration fleet (5 × Sonnet, read-only) launched: crate audit, dev-boot+TS, tests/oracles/fixtures, framework module+engines, history+prior tickets. Reports land as `📓️explore-raster-*.md` in this folder.
- 00:40 five explore reports landed (`📓️explore-raster-{crate,dev-boot-ts,tests-oracles,history-and-prior-tickets}.md`; framework-module report pending). Key findings: crate id/component metadata consistent; no `.action_interactive_job` and no `bounded_first_step_tool_proofs!`/`factory_type` anywhere → whole editor action surface dispatch-dead; 12 `#[cfg(test)]` mounts point at renamed fixture dirs (cargo test won't compile); examples (`demo`, `demo-session`) never registered, `setActiveExample` deleted; 8/9 exports + 7/9 imports are stubs; served plugin wasm dated 2026-08-17 with sha256 ≠ registry hash; owner descriptor pair from 2026-09-02; TS `package.json` is a verbatim cad-js copy; `RASTER_PLAY_PORT` in launch.json is dead (only `S_OS_PORT` matters); react `Paint2dHost`/`Canvas2dHost` and wgpu `render_paint_2d` are real.
- 00:45 implementers launched (Opus, no cargo allowed, coordinator compiles): W1 classification+factory+publication-authority, W2 test mounts+`t040` rename+examples+boot snapshot, W3 io honesty+`artifact_kind` reconcile+TS package.json.
- 00:45 baseline `cargo check -p semio-s-plugin-raster --lib` started in private `target-raster` (nice 10, -j 2, sccache bypassed) → scratchpad `check-native-baseline.txt`, copied into `🗑️generated/` at close.
- 01:30 all three Opus implementers killed by the account's Opus session limit (reset 04:10); their partial edits were auto-committed in `5e03e56997` (03:51): W1 editor `create_raster_app` +564 lines + `🔏️publication-authority/{🔣️.json,🧬️.schema.json}`; W2 12 test mounts fixed, `t040`→`📈️switches-the-tone-d5816a`, `🔮️oracle/🔣️.json` + `.feature` updated, new `🎬️set-active-example` command, `🧬️schema/🦀️.rs` +41; W3 `🚪️io/🦀️.rs` +498 and every per-format leaf (compositor region mid-insert). None compiled.
- ~02:50 host rebooted (swap exhaustion); scratchpad and the baseline check log were lost.
- 04:36 resumed. Host healthy (swap 1 GB, load 20). Fresh `cargo check -p semio-s-plugin-raster --lib` started on the warm `target-s-e2e` dir (scratchpad `check-native-1.txt`). W1/W2/W3 relaunched (Opus) to resume from the committed state; they now may run foreground incremental checks on `target-s-e2e`.
- 05:01 baseline `cargo check -p semio-s-plugin-raster --lib` (on the 03:51 tree + in-flight edits): stdio GREEN, raster **29 errors** (`🗑️generated/check-native-1-errors.txt`). 26 are the shared framework-API drift signature block already fixed on its side (15× `Label: From<LabelText|&str|plugin::Label>` in panels, 2× E0046 `DESCRIPTORS`/`descriptor` missing on `RasterConfig`/`RasterPresence`, 2× `render` must return `Result<ComponentTree>`, 4× `ActionDescriptor` helper now returns `Result<(ActionId, Option<UiValue>)>` in brush/eraser options, 4× `BuiltNode` vs `Result<BuiltNode, PluginAssemblyError>` in viewer windows/panels, 1× no field `base` on `BuiltNode`); 2 in `🧬️mutations/💾️binary/🦀️.rs` (E0061 arity, `?` Fault→String); 1 is W2's in-flight duplicate `set_active_example` mount. W4 (Opus) launched on the drift slice with puzzle + block's `📓️w7a/b/c` as oracles.
- 04:46 stdio wasm32-wasip2 pre-warm running in private `target-raster` (`cargo rustc … --profile wasm-dev`, same args as the dev script) so the later `dev raster` boot only builds raster.
- 05:02 W3 report landed (`📓️w3-raster-io.md`): all 9 formats reconciled — bmp/png/gif/jpg/tiff/svg export+import REAL via composite → stdio's own codecs (svg import host-tiered via the framework's usvg interface, propagates the wasm-guest error verbatim), pdf both ways and dwg export honestly declined with typed `Err` constants, json + dwg import unchanged real; `artifact_kind()` now reads `export_stdio_kinds`/`import_stdio_kinds` from the io module (note convention) advertising only working hops; TS `package.json` rewritten (was cad-js copy), `[DEBUG]` log removed; BMP TS twin + 2 shared fixtures, bun 7/7 pass; Rust parity tests added. Cargo section still a placeholder (W3 was queued on the shared lock). Flags: (1) root `📜️script.ts` gate `verify interactivity tool-jobs` (~4003-4011, 10240-10339) enforces the OLD "print DSL under foreign extension" law and had 3 stale paths → W5 (Opus) launched to rewrite the law; (2) raster still declares io via the old `.artifact(declaration).composers(io_registry::entries())` channel (live, consumed) rather than `IoDeclaration`/`declare_artifact` — deferred to a later wave (W6) after the crate compiles, to avoid colliding with W1/W2 in the plugin root file.
- 05:12 W2 report landed (`📓️w2-raster-tests-examples-boot.md`): mounts 232/232 + 111 includes, 0 dangling; `t040` rename complete (0 hits); examples registered via the old-channel twin `editor_with_examples::<RasterPlayApp>(create_raster_app(), examples())` (procedural precedent) — `demo` in the switcher, `demo-session` (cmd replay) mounted+tested but not in the switcher (puzzle precedent), its `.cmd.semio` rewritten with 5 real action ids; duplicate `demo` mount collapsed to one + alias; both example Rust tests mounted; `default_raster_document()` boots the Semio-logo carrier for editor AND viewer; `setActiveExample` chain complete (W1 landed row/import/retained id/contract/mutation/classification concurrently, W2 removed its duplicate mount). bun 2/2. Check: 28 errors, all in W4's drift slice (none from W1/W2/W3).
- 05:31 W5 report landed (`📓️w5-raster-export-gate.md`): root `📜️script.ts` gate `toolJobRasterEnvelopeCallerRetainedExact` rewritten — export clauses now demand real composite→stdio encoders or typed `RASTER_*_EXPORT_UNSUPPORTED` declines and zero `print_dsl(...).into_bytes()` under foreign extensions; the 3 stale leaf paths fixed and made loud; self-tests updated, positive + 2 hostile mutations verified. Residual: 16 other clauses in the same gate still describe raster's pre-serde-elimination schema (`serialize_empty_owned_map`, `rasterOwnedMapSerdeGuards`) and an old wasm bridge (`RasterEnvelopeLoadHandle`, envelope ingress fns) — W5b (Opus) launched to bring them to the current raster code. The full `verify interactivity tool-jobs` run also dies on a foreign 📕️norm factory-proof failure (not ours).
- 05:54 W1 report landed (`📓️w1-raster-classification-factory.md`): 17 routes (16 + `setActiveExample`) each `.action_interactive_job(.., Migrated)`, `bounded_first_step_tool_proofs!` with `factory_type`, `RasterRetainedCommandJobFactory` + 2 store preparations, `RASTER_PUBLICATION_CONTRACTS` (view actions stay `ActionKind::View`/Config lane yet `Migrated`; `setActiveUtility` framework-injected, already Migrated), law test `retained_route_dispositions_are_exact_and_exhaustive` now also asserts `TOOL_JOB_IDS ∩ migrated == proof set`; `🔏️publication-authority/🔣️.json` 17 routes validated with Ajv (puzzle's sibling schema pattern, no central schema exists); taxonomy gained `plugin-publication-authority` owned by `members-of-plugins`. W1's check on its tree: **4 errors left, all W4's** (`raster_measure_action` returns `Option<ActionDescriptor>`, `WindowMeasure::Slider.on_change` wants bare `ActionDescriptor`) — W4's own check is the current lock holder and should catch them.
- 05:32 W4 report landed (`📓️w4-raster-framework-drift.md`, compile verdict pending): `ui_label` admitting helper (15 sites), hand-written `DESCRIPTORS`/`descriptor` on `RasterConfig`(7)/`RasterPresence`(1), both `render`s reshaped to `UiAssemblyResult<ComponentTree>` via `built_to_component_tree`, editor composite/navigator windows moved off the legacy `build_paint_2d_scene` UiNode path onto `scene_surface(.., ContractSurfaceKind::Paint2d, ..)`, viewer windows return `UiAssemblyResult<BuiltNode>`, `BuiltChildren::try_push` replaces `node.base.children`, binary pumps take `Option<&mut StepContext>` with `Fault` mapped verbatim into the `String` error.
- 06:03-06:09 W3 final (bun 9/9, zero io diagnostics), W5b landed (`📓️w5b-raster-gate-residuals.md`): 16 residual gate clauses retargeted from the deleted wasm bridge to the editor-hosted paged-ingress cohort (gis-map shape), `wasm` param removed, loud source reader, 199 clauses, 182 pass; the 17 failing are REAL raster gaps: (1) `🧬️schema/🦀️.rs:25` test-only `serde(serialize_with = …serialize_empty_owned_map)` names a deleted fn (breaks `cargo test`), (2) raster has no retained envelope-ingress caller/test cohort since the 09/01 bridge deletion. W4 and W5b were then killed by the Opus limit (reset 09:30); W4's measure-action fix appears in the 12:08 auto-commit `2d2b39eb7f`, unverified.
- 20:44 resumed (14 h gap). Host: swap 30.9/31.7 GB again, load 40, 22 peer cargo/rustc. `cargo check` restarted on `target-s-e2e` (`check-native-2.txt`, rebuilding deps — a peer moved the lockfile/profile). stdio wasm pre-warm had FAILED at link: `linking with wasm-component-ld failed` (no detail under `--message-format short`) — relaunched without short format (`stdio-wasm-prewarm-2.txt`) to read the linker error. W6 (Opus) launched: finish compile-green, drop the dangling serde attr, port the gis-map ingress cohort, run `cargo test --lib`.
