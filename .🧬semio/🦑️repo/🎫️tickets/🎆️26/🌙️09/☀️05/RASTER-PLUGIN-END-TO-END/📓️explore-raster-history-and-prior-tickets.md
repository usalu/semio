# Raster Plugin — History and Prior-Ticket Reconstruction

Read-only research for a fresh 🖨️raster implementer. Repo and semio MCP both failed to connect (`CONNECT_TIMEOUT`) for this whole session; everything below comes from `git log`/`git show`/`git status` and reading ticket files directly off disk. Sibling explorer report in this same ticket folder, `📓️explore-raster-tests-oracles.md`, covers the current test/oracle/fixture surface in depth — this report does not repeat that content, only cross-references it.

## 1. Git history of raster paths (real dates via `--date=iso`)

25 most recent commits touching `✏️s/🔌️plugins/🖨️raster` or `🧰️framework/🔨️modules/🖌️raster` span **2026-08-21 → 2026-09-05**, all authored by Ueli Saluz (auto-commit; commit-message dates are a frozen template string per house convention, ignored here — used `git log --date=iso`). Full hash/date/subject list captured in scratch; the shape of the work by period:

- **2026-08-21 → 08-24** (`551`…`562`): steady mechanical rename pass across the plugin's `component.rs` files — this era is when files were still suffixed `🦀️component.rs` (pre single-crate migration). Touched editor windows/panels/commands, wasm bridge, snapshot/diff, schema mutations, and — notably — **all eight IO export serializers in one commit** (`e7bd5ecdf70` 2026-08-24 02:34): gif, svg, pdf, jpg, png, dwg, bmp, tiff native-renderer document-parsing arms all touched together.
- **2026-08-23 10:55** (`9d7cabfd9`): isolated wasm-bridge-only touch.
- **2026-08-26 → 08-27** (`574`…`577`): oracle/schema mutation reshuffling (`🔣️component.json` → `🔣️.json` renames begin here), plus a `child-owner-isolation` fixture added.
- **2026-08-27 → 08-28** (`578`): the emoji-normalization sweep hits raster mutation schemas — 10 new per-mutation `payload.schema.json` files added under `🧬️mutations/*`, old `component.feature`/`component.py`/`component.rs` triad under `mutate-raster-1` replaced by `.feature`/`.py`/`.rs`.
- **2026-09-01 → 09-03** (`579`…`586`): further composite-window emoji renames (`🎚️options` → `☑️options`, command folder renames like `🖼️add-layer` → `➕️add-layer`, `🖼️delete-layer` → `🗑️delete-layer`, `🎥️set-camera` → `📷️set-camera`, etc.) — this is the repo-wide "Normalize semantic emoji path identities" sweep (see commit `b0dfa0f09b`, 2026-09-05 19:04, subject line literally says this), landing in raster along with everything else.
- **2026-09-05** (`589`, `590`, `591`): commit `589` (03:53) is the huge end-to-end-audit commit that also touches raster's `Cargo.toml`/`script.ts`/`.rs` (2-line tweaks) plus PDF/PNG artifact directory renames (`🌳️pdf`→`📖️pdf`) and an `io` module addition under `🏅️standards/🔖️1/🪆️subsets/✳️any/🚪️io/🦀️.rs` (+45 lines). Commit `590` (19:04) renames `🟠️mutate-raster-1` → `🖨️mutate-raster-1` (emoji-identity fix on the test dir itself) and updates the `.feature` file (14 lines) plus 6 mutation-test `.rs` files (2-line touches each) and drops a duplicate `🧬️schema/🦀️.rs` (-43 lines, dead file removed). Commit `591` (22:02) is a WAL-compaction/CAS-retention infra commit with zero raster content (only 66 no-op rename/mode-bit entries under a different plugin's fixture tree — a false-positive path match, not real raster work).

**Current uncommitted state**: `git status --porcelain -- '✏️s/🔌️plugins/🖨️raster' '🧰️framework/🔨️modules/🖌️raster'` returns **nothing** — the tree is clean at raster paths right now (only untracked item repo-wide under those globs is this ticket's own folder, which doesn't match the path filter and is irrelevant). **No emoji-rename sweep is currently in flight inside raster.** Every rename observed above (option folders, command folders, `mutate-raster-1` prefix, schema files) is already committed as of `3a6a9d6bfc12` (HEAD). This directly answers the risk question in §5.

## 2. Prior raster tickets — what each achieved, what it left open

All read from `🎫️ticket.json` `summary` fields plus any extra notes files; times below are ticket-open dates, not commit dates.

| Ticket (26/…) | Status | What it achieved | What's still open / binding decision |
|---|---|---|---|
| `06/30/RASTER-DOCUMENT-INTEGRATION` | closed | Full layer management wired into workbench/details panels: nested layer tree, add-layer toolbar, drag-drop reorder, catalogue drops, visibility toggles, context-menu duplicate/delete/add-mask. Catalogue tab (layer types, blend modes, filters). Declarative inspector fields. | None noted; this is foundational UI work, superseded/extended by later react-parity and framework-primitive tickets. |
| `06/30/SEMIO-LOGO-RASTER-FIXTURE` | closed | Unrelated to the raster **plugin** — this is `SINGLE-SIDE-PANEL-TREE-MECHANISM`, a same-day differently-named ticket about the platform React renderer's side-panel registry. Only tangentially relevant (raster inherited whatever declarative-tree side-panel mechanism this produced). Do not confuse with the next row. |
| `06/30/SEMIO-LOGO-RASTER-FIXTURE-AND-IMPORT-EXPORT` | closed | Introduced the raster technology itself (infinite-canvas Rust/WASM/Vello, layers/masks/blend/adjustment/painting/filters). Consolidated to **one Semio-logo fixture** (`semio.raster.json`) with full layer stack (groups, masks, blend modes, clip-to-below, all 4 adjustment kinds, all filter kinds); removed default/paint/photo-edit fixtures. Extended `raster.document/v1` with `assets` (base64 images) + per-layer `filters`. Wired Import/Export Raster toolbar commands (file-bridge download, file-picker load, JSON round-trip). | **Binding decision**: the Semio-logo fixture is *the* canonical example going forward — later tickets (RASTER-SEMIO-EXAMPLE-NOT-SHOWING) build directly on this fixture, don't reintroduce the removed default/paint/photo-edit fixtures. |
| `07/01/RASTER-FRAMEWORK-INTEGRATION` | closed | Aligned raster with draw/play framework integration patterns: tree-row↔layer-id mapping in raster/core, selectionChange on document/masks trees, `uiInspectorGroupsToTree` inspector refactor, `CANVAS_HOVER_SOURCE_CATALOG` in catalogue, `onCommit→commitDocument` wiring. All raster core/play/react tests passing (10+8+2) at close time. | Superseded structurally by the later crate migration (08/05); the *pattern* (draw/play alignment) still binds. |
| `07/01/RASTER-ZOOM-CAMERA-FIX` | closed | Root-caused: camera was included in the WASM compositor sync JSON, so every zoom tick re-parsed layers and re-uploaded embedded assets (visible as "logo changes on zoom"). Fix: compositor sync **omits camera/session fields**; RasterCanvas keeps local `sessionCamera` (draw pattern); camera persisted only on real change; wheel/pan use `canvas::camera::wheel_screen`/`pan_last`; pointer-move no longer commits document every frame. | **Binding decision**: camera must never round-trip through document sync JSON — this is exactly what `07/31/RASTER-CAMERA-AS-SESSION-ONLY-VIEW-ACTION` later formalizes at the schema/action-kind level. Do not reintroduce camera into sync JSON. |
| `07/01/RASTER-ZOOM-COMPOSITE-FIX` | closed | Composite keeps session camera in React/WASM without doc re-sync; navigator uses **fit-to-bounds overview camera** with a React viewport-overlay rectangle driven by composite camera+viewport; WASM selection chrome disabled in navigator view; host wires `contentViewport`/`onViewportChange`. | **Binding decision**: navigator and composite have architecturally different cameras (fit-to-bounds vs session) — don't unify them naively. |
| `07/11/RASTER-REACT-PARITY` | closed | Restored premigration-level (872-line pre-plugin-migration renderer) React interactivity end to end: real WebGPU `RasterSession`, composite+navigator windows, wheel zoom-to-cursor, middle-button pan, marquee/lasso/wand selection w/ modifier merge, stacked-pick disambiguation, hover sync, brush/eraser painting, theme-synced checkerboard, navigator viewport overlay. Replaced flat-PNG `<img>` stub. Ported hit-testing/navigator-camera math from deleted premigration raster-core into `raster/rs/lib.rs`. Replaced flat `pixelsBase64` wire type with document-sync/assets/camera/selection/tool/brush channels. Found+fixed 5 real bugs (see below). 49/49 Rust + 64/64 vitest passing; TS compiles; live-browser confirmed gpuReady. | **5 concrete landed bug-fixes, don't re-break**: (1) `SurfaceKind::Table`/`Raster` needed explicit `#[serde(rename)]` (lowercase wire) or the host could never mount via the real plugin pipeline. (2) `infinite_canvas::Affine` needed a `Mul<Point>` impl for hit-testing. (3) `@semio-tech/raster-rs` needed adding to Vite's `optimizeDeps.exclude` (dev-boot reload-loop otherwise). (4) `raster-host.tsx`'s `syncAll` useCallback must depend on scalar scene fields, not the whole `scene` object (double-attachCanvas race, `gpuReady()` false). (5) `RasterTransform`'s derived `Default` gave `scaleX/scaleY: 0.0` (Rust derive ignores serde deserialize-only defaults) — needs a **manual** `Default` impl; regression test `empty_document_background_layer_has_identity_scale` exists, don't remove it. |
| `07/16/WIRE-RASTER-WGPU-DEV-PREVIEW-INTO-LAUNCH-CONFIG-AND-VERIFY-END-TO-END` | closed | Registered `raster-wgpu-dev` (port 6160, `SEMIO_RENDERER=wgpu`) in `.claude/launch.json`. Fixed a **stale local wasm-bindgen build artifact** crash (`session.setActiveUtility is not a function`) by rebuilding `raster/rs/pkg` — root cause was commit renaming `set_active_tool`→`set_active_utility` in `raster/rs/lib.rs` without rebuilding the gitignored wasm-bindgen output. | **Known-open, filed as follow-up, not fixed**: after that crash fix, the Semio example still renders only a flat background on both Composite and Navigator — none of the 8 fixture layers visibly paint, despite document-sync/asset-upload/WebGPU-init all succeeding without error. This is exactly the bug `07/23/RASTER-SEMIO-EXAMPLE-NOT-SHOWING` (next row) was opened to fix — confirm that fix is still in place before assuming this class of bug is gone. `raster-wgpu-dev`'s own boot was never verified end-to-end in this ticket (blocked by concurrent build-lock contention) — wgpu variant may still need a fresh verification pass. |
| `07/17/GENERALIZE-RASTER-SURFACE-KIND-TO-PAINT2D` | **status: "open" in ticket.json, but the work is DONE on disk** | Rename `SurfaceKind::Raster`(wire "raster")→`SurfaceKind::Paint2d`("paint-2d"), `RasterScene`→`Paint2dScene`, field `raster`→`paint_2d` (JSON `paint2d`), `build_raster_scene`→`build_paint_2d_scene`, `render_raster`→`render_paint_2d`, native-renderer types `Raster*`→`Paint2d*`. **Verified present in current tree**: `SurfaceKind::Paint2d` is live throughout `🧰️framework/🔨️modules/🖱️ui/…` and `🧰️framework/🛍️products/💻️os/…/🎯️targets/🧊️wgpu/🦀️.rs` (grep confirms `render_paint_2d`, `Paint2dScene`, match arms, etc.); a dedicated `Paint2dHost` element directory exists at `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🧱️elements/🖌️Paint2dHost` (no `raster-host.tsx`/`paint-2d-host.tsx` file found anywhere by filename search — the rename evidently went further into the taxonomy-normalized element-folder naming, i.e. `Paint2dHost/` is the current name, not a loose `.tsx` file). | **Ticket bookkeeping is stale — `ticket.json.status` still says `"open"` even though the described rename is fully landed in source.** Don't redo this rename; if you need to touch `SurfaceKind`, it's already `Paint2d` everywhere. Flagged-for-later in the ticket description but unconfirmed by me: whether `os-shell.tsx`'s `createRasterSession` (an app-specific WASM import reaching into framework/renderer/react) was ever architecturally addressed — worth a targeted grep before assuming it's clean. |
| `07/17/INFRA-HYGIENE-RASTER-PROJECT-JSON-REMODEL-LAUNCH-ENTRIES-…` | closed | Added `raster/rs/project.json` `"test"` nx target (mirrors `draw/rs`) + `TestScript` class in `raster/rs/script.ts`; verified `bun nx run @semio-tech/raster-rs:test` → 12/12 passing. `.vscode/launch.json` remodel trio and `note/plugin/rs` Cargo.lock cleanup were unrelated bundled items, already fine. | None specific to raster left open. |
| `07/18/NORMALIZE-RASTER-PLUGIN-ONTO-FRAMEWORK-PRIMITIVES` | closed | Renamed `RasterApp`→`RasterPlayApp` (app id `"raster-play"`) zero-residue. Adopted `PanelKit` (`tree_item_with_action`/`PanelTreeBuilder`) replacing hand-rolled UI-tree construction. Adopted `Terminology` (`app_labels!`/`resolve_labels`/`AppLabelsOverlayExt`) replacing hand-rolled `RasterLabels`. Adopted `Testkit` (`testkit::meta`/`new_app`/`new_app_with_registry`/`assert_ingest_idempotent`) replacing crate-local scaffolding — **forbidden to keep local `meta()`/`new_app()`/`new_app_with_registry()` per contract C5**. Region-marker/order cleanup (`🔖️Scenes`→`🔖️Render`, `🔖️RasterApp`→`🔖️RasterPlayApp`). | **Binding naming decision**: the app struct is `RasterPlayApp`, not `RasterApp`, everywhere — grep for stray `RasterApp` before assuming a clean slate. One test left deliberately untouched/app-unique: `two_instances_converge_disjoint_layer_edits_via_backbone` (needs `load_document`-seeded identical base docs with a fixed layer id — testkit's generic paired-apps helper can't express this). |
| `07/23/RASTER-SEMIO-EXAMPLE-NOT-SHOWING` | closed | Root cause: plugin `Adjustment` layers **dropped `params` on deserialize**, then the paint host required `params`, so `syncDocumentJson` threw and the Semio fixture never composited (blank canvas). Fixed: preserved adjustment params in the plugin domain, defaulted paint-host params, fixed group/parent transform propagation (compositor + picking), taught **wgpu paint2d** to use `cameraJson`, center quads, and draw layers without an `imageKey`. Verified: `framework_surface_paint` (9) + `raster-plugin` (25) tests + wgpu cargo check. | This is the fix for the exact symptom flagged as a follow-up in `07/16/WIRE-RASTER-…`. If a fresh implementer sees a blank/flat-background canvas again, re-check these specific fix points (adjustment-layer `params`, group transform propagation, wgpu `imageKey`-less quad draw) before re-diagnosing from scratch — this bug class has recurred once already. |
| `07/31/RASTER-CAMERA-AS-SESSION-ONLY-VIEW-ACTION` | closed | Moved camera pose **out of the VCS-tracked document entirely** into session-only runtime state (`RasterCamera` now lives on `RasterPlayRuntime` in the ui crate, not `RasterProjection`). Reclassified `setCamera`/`setCameraZoom` from `ActionKind::Operation`→`ActionKind::View` (no longer undoable/replicated). **Deleted `RasterOperation::SetCamera` entirely** — enum variant, diff/absorb/apply/backwards arms, DSL round-trip case — no compat shim, across raster/raster-op/raster-ui/raster-protocol/raster-pack/raster-dsl/raster-engine. Fixed every `RasterProjection` struct literal and the `semio.raster` DSL fixture that referenced the removed field. | **Strongly binding architectural decision — do not reintroduce a camera field on `RasterProjection`/the document/DSL.** Camera lives only on `RasterPlayRuntime` now. **Known-open, explicitly flagged and NOT fixed here**: 2 pre-existing test failures in `semio-s-app-raster-ui` (18/20 passing) — `set_active_utility_switch_emits_no_ops_and_reads_from_view_state` and `utility_registry_declares_utilities_scoped_to_the_composite_window` both panic on `IconName::from("brush")` because the icon catalog is missing a `"brush"` entry. This is an **icon-catalog gap, not a raster bug** — confirmed present at HEAD *before* this ticket's change. Check whether it's still there; if so it's still not raster's to fix but blocks a fully-green raster-ui test run. |
| `08/05/RASTER-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION` | closed 2026-08-06 | Migrated raster from the old 8-crate constitutional layout (manifest/artifact + apps/raster + 6 op/dsl/pack/protocol/ui/engine modules) to the **current single-crate layout**: `✏️s/🔌️plugins/🖨️raster/📦️packages/🦀️rust` = crate `semio-s-plugin-raster`. Confirmed **0** `⚡️implementations` directories left under the plugin tree; root workspace member updated; downstream fixture-sweep repointed. | `cargo check -p semio-s-plugin-raster` **could not be freshly re-verified** at close time — blocked repo-wide by an unrelated missing `3d/🎬️scene` impl manifest, not a raster defect. Prior evidence (`🧪️check-log.txt` in that ticket folder) showed it compiling clean (8 warnings, 0 errors) before the workspace-wide load broke. **If a fresh `cargo check -p semio-s-plugin-raster` won't even load the workspace, check whether that same `3d/🎬️scene` manifest gap is still the blocker before assuming raster itself regressed.** |
| `08/05/RASTER-SHAPE-V2-TREE-PURITY-RETROFIT` | closed | Retrofitted raster to "Shape V2" tree-purity: moved owner-root `📦️lib.rs`→`📦️packages/🦀️rust/📦️lib.rs` (Cargo.toml `[lib]` path updated); folded `🦀️config.rs`/`🦀️terminology.rs`/`🦀️wasm.rs` siblings into their own component folders (`🎚️config/🦀️component.rs`, `🗣️terminology/🦀️component.rs`, `🌉️wasm/🦀️component.rs`). Verified via isolated-workspace overlay (root blocked by other sessions): `cargo check` clean, `clippy --all-targets -D warnings` clean, `cargo test --lib` **57/57** passed (matches pre-retrofit baseline exactly). | Tree confirmed pure at close; no registrar handoff needed. This is the most recent structural-layout ticket — the crate shape it left behind (`📦️packages/🦀️rust/📦️lib.rs` + per-component folders) is the current ground truth layout; don't re-flatten it. |

## 3. Repo-wide end-to-end tickets — what they say about raster

- **`09/02/COMPLETE-SEMIO-END-TO-END/📋️master-plan.md`**: no raster-specific content found; one incidental hit (line 358) mentions "retained render/raster grant laws" in a framework/UI-generic sense (WGPU render-grant law naming), not the raster plugin. `✅️acceptance-matrix.md`: **zero** hits for "raster" — this GPT-fleet ticket owns hub/AI/admin/provider authority, not app-plugin boot, and doesn't track raster at all.
- **`09/05/S-END-TO-END/📋️plan.md`** — this is the ticket that actually owns "every registered plugin/app/artifact opens and renders in the React shell." Its **Definition of Done** (generic, applies to every plugin including raster):
  1. `dev s` boots to the readiness beacon with the complete live catalog (59 registry rows) installed, **no plugin in `failed`/`crashed` status**.
  2. A registered catalog-wide smoke (`framework-os-dev verify catalog`) spawns every program inside one session and proves **a rendered window with zero page errors** for each declared app.
  3. Every plugin has a **fresh owner descriptor pair**; registry `check` is fail-closed and green.
  4. `framework-os-dev test-quick`, `.storybook/os-plugins.spec.ts`, `.storybook/s-end-to-end.spec.ts`, `verify e2e`, `verify collab` run non-vacuously and pass.
  5. WGPU wasm boots + `parity verify s` passes; WGPU native boots + opens Home/Studio.
  - This matches almost verbatim what this ticket's own `📓️status.md` Definition of Done already states for raster specifically — good, the two tickets are aligned.
  - Raster-specific findings inside this sibling ticket's own explorer reports (all dated **2026-09-05 ~04:00-05:15, i.e. ~20h stale relative to now**):
    - `📓️explore-catalog-build-state.md` line 49: raster's **core wasm cache is Y (built), last mtime 2026-08-17 18:40** — nearly 3 weeks stale relative to the ticket's own baseline date; component JS Y, staged descriptor Y, owner descriptor pair Y, playground variant `raster`, no explicit app id.
    - `📓️explore-per-plugin-blockers.md` line 53 (table columns: Mig E/V | Batch E/V | Fac E/V | Proof | Ex | Stub | Descriptor | Ticket status | Concrete blocker): raster shows **0/0 migrated, 0/0 batch, n/n factory, n/n proof, no examples, 2+0 stub, Descriptor "OK", no ticket, blocker "-"**, annotated **"No actions yet."** — i.e., as of that read, raster's plugin **compiled fine** and simply hadn't been touched by the action-migration wave (not blocked, just unstarted).
    - `📓️opus-descriptor-producer.md` line 175: raster is listed among the **32 "stale-schema" owner descriptor pairs** left for Wave 2 (needs a fresh wasm build + `describe` re-emission before registry `check` will accept it) — consistent with the 08-17 stale-wasm finding above.
    - `📓️explore-action-migration-recipe.md` line 413 (dispatch order #10): lumps raster into "remaining plugins with 0 actions or non-compiling crates (block, playbook, trinity, animate, remodel, architect, energy, raster)" and attributes the blocker to **"plugin must compile first… mostly `#[path]`/mutation-module E0433/E0599 errors from the `✳️base`→`🧱️base` rename in flight."** — **this directly contradicts the per-plugin-blockers table's "OK"/"-" verdict for raster in the same ticket, from the same exploration pass.** I could not resolve this discrepancy without running `cargo check` (forbidden for this read-only ticket) — flag it explicitly to whoever runs the first raster build: check whether raster actually fails to compile due to this repo-wide `✳️any`→`🧱️base`-style mutation-module rename, or whether the "OK" row is the accurate one and row #10 over-generalized by lumping raster in with genuinely non-compiling plugins.
- **`08/29/LOWPOLY-END-TO-END-COMMANDS-IO-AND-MUTATIONS/📓️e2e-status.md`**: explicitly clears a suspected raster gap — "every plugin's per-format TS serializers are `export {};` stubs (lowpoly/raster/remodel/gis/puzzle byte-identical…)" is **the repo-wide convention, not a defect**; real serialization lives only in the Rust siblings repo-wide. **Do not "fix" raster's TS IO serializer stubs** — they're supposed to be empty re-export stubs.

## 4. In-flight peer sessions right now (as of 2026-09-06 ~00:15, i.e. "last 6 hours" of 2026-09-05 late evening)

Open tickets under `26/09/05/`: `BLOCK-PLUGIN-END-TO-END`, `DRAW-PLUGIN-END-TO-END`, `S-END-TO-END`, and this ticket (`RASTER-PLUGIN-END-TO-END`). (`CONFINE-DWG-CODECS-TO-ARTIFACT-I-O`, `MERGE-PRINT-AND-MIT-BESTAND-INTO-CURRENT-BRANCH`, `WINDOWS-CHECKOUT-ILLEGAL-FILENAMES` are closed.) Both `BLOCK-PLUGIN-END-TO-END/📌️important.md` and `DRAW-PLUGIN-END-TO-END/📌️important.md` are **empty** (no urgent cross-ticket blocker flagged).

- **`DRAW-PLUGIN-END-TO-END`** opened **2026-09-05 23:55** (~20 min before this exploration started) — actively fresh, same shape as this ticket ("app compiles natively+wasm, owner descriptor regenerated, `dev` boots and renders"). Its baseline notes: host load 109, swap 63/64 GB, **~10 concurrent stdio rustc compiles from peer sessions (5-10h old)** — i.e. the machine is currently extremely contended; expect any cargo command from this raster ticket to queue behind that. Draw's own blocker (unrelated to raster): missing names imported at a drawing-snapshot IO path, from stdio API drift.
- **`BLOCK-PLUGIN-END-TO-END`** — long-running same-day session (files from 03:35 through 19:59). Its `📓️status.md` (tail read) shows the **shared framework/stdio drift signature** relevant to every plugin right now: a native `cargo check -p semio-s-plugin-block --lib` at 13:40 hit **60 errors, all framework-API drift, not block-specific bugs**: 13× E0433 `mutation` missing in `↩️inverse`, 3× `command_from_action` DslValue/JsonValue mismatch, 6× E0046 Config/Presence missing `DESCRIPTORS`/`descriptor`, 7× `render` must return `Result<ComponentTree>`, 3× `with_ports` on an async `AppIo`, 13× `ActionDescriptor` helper signature drift, 15× prelude `Label: From<…>`, 2× mutation `ToValue`/E0080. **This is the signature to recognize as "shared framework churn, not our bug" if raster's own `cargo check` produces the same error shapes.** Host state at 19:35: swap 66/67 GB, a six-hour rustc still on the shared lock at 7.5% CPU — matches this repo's documented "Concurrent Cargo Workspace Churn" pattern.
- **`S-END-TO-END`** — the coordinator ticket for the whole `s` shell wave (see §3); its own explorer docs are ~20h stale now but its coordinator log (as of its last entry ~14:10) shows an in-flight Vite-config-graph diet and a stdio compile census as the two live cross-cutting threads that would affect any plugin's dev-boot, raster included.
- No peer ticket's notes mention `🏪️store`, `serde`, or `ToValue` in a way that names raster specifically — the only `ToValue`/ mutation-leaf drift signature found is the generic one above (shared by block, and by the `✳️base`→`🧱️base` mutation-module rename referenced in §3's action-migration-recipe row).
- **Shared crates under active peer migration right now, with their failure signature**: (1) the **mutation-leaf taxonomy / `✳️base`→`🧱️base` rename** (owns `#[path]`/E0433/E0599 errors in mutation modules across several plugins); (2) the **framework action/render/config-descriptor API** (owns the 60-error signature listed above: `inverse`/`command_from_action`/`DESCRIPTORS`/`Result<ComponentTree>`/async `AppIo`/`ActionDescriptor`/`Label: From<>` drift) — this is very likely a repo-wide framework crate mid-migration, not anything raster-specific; if raster's own check shows these exact shapes, it's "their bug", coordinate rather than hand-fix in the raster crate.

## 5. Summary

**Do-not-undo list (binding architectural/naming decisions from closed tickets):**
- Camera is session-only runtime state on `RasterPlayRuntime` (ui crate) — **never** re-add a camera field to `RasterProjection`, the document schema, or the `semio.raster` DSL grammar. `setCamera`/`setCameraZoom` are `ActionKind::View`, not `Operation`; there is no `RasterOperation::SetCamera` and no compat shim for it.
- Compositor sync JSON must **not** include camera/session fields (separate from the point above — this is the original zoom-flicker root cause fix).
- App struct is `RasterPlayApp` (app id `"raster-play"`), not `RasterApp`.
- Crate layout is single-crate `semio-s-plugin-raster` at `✏️s/🔌️plugins/🖨️raster/📦️packages/🦀️rust/📦️lib.rs`, Shape-V2 tree-pure (config/terminology/wasm each in their own component folder). No `⚡️implementations` directories should exist under the plugin tree.
- `SurfaceKind::Raster`/`RasterScene` are gone from the framework — it's `SurfaceKind::Paint2d`/`Paint2dScene`/`Paint2dHost` now, confirmed live in current source, even though the originating ticket's `status` field is stuck at `"open"`.
- Raster's TS IO serializer stubs (`export {};`) are the repo-wide convention for every plugin — not a gap to fix.
- Raster uses framework primitives: `PanelKit` (tree_item_with_action/PanelTreeBuilder), `Terminology` (app_labels!/resolve_labels), `Testkit` (testkit::meta/new_app/new_app_with_registry/assert_ingest_idempotent) — don't reintroduce hand-rolled equivalents.
- The Semio-logo fixture (`semio.raster.json`) is the one canonical example (groups, masks, all 4 blend/adjustment kinds, all filter kinds) — the old default/paint/photo-edit fixtures were deliberately removed, don't resurrect them.

**Known-open list:**
- `07/17/GENERALIZE-RASTER-SURFACE-KIND-TO-PAINT2D` ticket bookkeeping never got closed even though its work is done in source — harmless but confirm before assuming anything else in that ticket description is still pending (its "flag for later architecture audit" item, `os-shell.tsx`'s `createRasterSession` reaching into raster's own WASM package from framework/renderer/react, was never confirmed resolved).
- Raster's owner descriptor pair is "stale-schema" per the `S-END-TO-END` Wave-2 producer audit — core wasm last built 2026-08-17, needs a fresh build + `describe` re-emission before registry `check` will accept it (this is very likely exactly what this ticket needs to do).
- Two pre-existing `semio-s-app-raster-ui` test failures from an **icon-catalog gap** (`IconName::from("brush")` panics, missing "brush" entry) — confirmed present before the 07/31 camera ticket's own change, not raster's to fix, but blocks a fully-green raster-ui test run; check if it's still there.
- Unresolved discrepancy between two same-session S-END-TO-END explorer docs on whether raster currently compiles clean ("OK"/"-" in the blockers table) or is blocked by the repo-wide `✳️base`→`🧱️base` mutation-module rename (dispatch-order row #10) — resolve this with your own fresh `cargo check` first, and if it fails with the `#[path]`/E0433/E0599 signature, it's shared framework/taxonomy churn, not a raster regression.
- `raster-wgpu-dev` (port 6160) was registered in `.claude/launch.json` but never fully verified end-to-end (blocked by build-lock contention in the ticket that added it) — needs its own fresh verification pass, separate from the react variant.
- The blank-canvas symptom (Semio example renders flat background, no layers paint) has recurred once (fixed in 07/23) after a stale-wasm-artifact incident (07/16) — if you see it again, check adjustment-layer `params` preservation, group/parent transform propagation, and wgpu's `imageKey`-less quad draw path before re-diagnosing from scratch.

**Emoji-rename-sweep risk to raster specifically: currently low.** `git status --porcelain` on raster paths is clean — no in-flight rename touching raster right now. The repo-wide "Normalize semantic emoji path identities" sweep (commit `b0dfa0f09b`, 2026-09-05 19:04) already swept through raster's option/command folders and the `mutate-raster-1` test dir and is committed. The one live repo-wide rename that *could* still reach raster is the **`✳️base`→`🧱️base` mutation-module taxonomy rename** referenced in `S-END-TO-END/📓️explore-action-migration-recipe.md` — its blast radius on raster is exactly the unresolved discrepancy flagged above; watch for it if a fresh `cargo check` on raster fails with `#[path]`/E0433/E0599 in a mutation module.

**Host contention note (not raster-specific but affects any verification you attempt):** at ticket-open time (2026-09-06 00:00), swap was 60.9/61.4 GB used, load average 104, ~25 peer rustc processes running (some 4-5h old) per this ticket's own `📓️status.md`. Expect any cargo command to queue for a long time; this matches the repo's documented "Concurrent Cargo Workspace Churn" and "sccache serializes concurrent builds" patterns.
