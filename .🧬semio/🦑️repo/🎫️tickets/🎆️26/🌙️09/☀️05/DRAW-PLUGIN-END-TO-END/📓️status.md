# 🖍️ Draw plugin end to end — status

App under test: `draw` (editor `DrawPlayApp` + viewer `DrawViewer`) of plugin `✏️s/🔌️plugins/🖍️draw`, crate `semio-s-plugin-draw`
(`🛠️dev✏️draw⚛️react` → `bun nx run @semio-tech/framework-os-dev:dev` with `SEMIO_PLUGIN=draw SEMIO_RENDERER=react S_OS_PORT=6064`).
Ticket opened 2026-09-05 23:55 by session ⚪269a4680 (Fable 5.1 coordinator). Repo MCP was down; bookkeeping is manual.
Ticket start commit: `🗑️generated/start-commit.txt`. Sibling tickets with the same shape: `26/09/05/BLOCK-PLUGIN-END-TO-END`, `26/09/05/S-END-TO-END`.

## Definition of done
1. `cargo check -p semio-s-plugin-draw --lib` green natively and on `wasm32-wasip2`; `cargo test -p semio-s-plugin-draw --lib` green.
2. `bun nx run @semio-tech/draw-js:test` green as a real draw TS package (today `package.json` is a verbatim cad-js copy).
3. Owner descriptor pair `🔣️.json` / `🛂️.descriptor.semio` regenerated via `describe` from a fresh component; registry `check` accepts draw; `🔌️plugin-modules/🖍️draw` exists in the dev cache.
4. `🛠️dev✏️draw⚛️react` boots on 6064 in the react renderer: the canvas window and the panels render non-empty content, examples load and switch, editor actions dispatch (no `interactive-job.*` / bounded-factory faults), zero page errors.

## Baseline — 2026-09-05 23:55
- Catalog cache (`S-END-TO-END/📓️explore-catalog-build-state.md`): draw is one of two rows with zero build output (no core wasm, no component JS, no cache dir); owner descriptor pair exists in source.
- `S-END-TO-END/📓️explore-per-plugin-blockers.md:40`: draw does not build against stdio's drawing snapshot API (absent names imported at `🖍️drawing/…/✳️any/🚪️io/🦀️.rs:16-19`).
- Machine: load 109, swap 63/64 GB, ~10 concurrent stdio rustc compiles from peer sessions (5-10 h old). Every cargo step here is queued behind that.

## Log
- 23:58 Ticket opened. Native `cargo check -p semio-s-plugin-draw --lib` launched into private `target-draw-e2e` (log `🗑️generated/check-lib-1.txt`, jobs=3, `RUSTC_WRAPPER=""`). Exploration fleet launched (compile drift, editor dispatch, build/boot infra, artifact/io).
- 00:16 The fresh `target-draw-e2e` would have recompiled stdio from scratch (peers' stdio compiles take 5-10 h under this load). `target-s-e2e/debug` holds a check-mode stdio rmeta from 21:29 with zero stdio sources newer than it, so the check was relaunched on a copy of that target (`🗑️generated/seed.txt`); the copy itself runs at ~1 GB per 10 min under the swap thrash, cargo starts when it lands. No `CARGO_PROFILE_DEV_DEBUG` override on this lane (it would invalidate the seeded fingerprints).
- 00:45 `📓️explore-editor-dispatch.md` (Sonnet): plugin root `✏️s/🔌️plugins/🖍️draw/🦀️.rs` referenced a non-existent `crate::{editor,viewer,artifacts}::draw` tree and `DrawPlayApp`/`DrawViewer` (the crate mounts `…::drawing`, `DrawingPlayApp`, `DrawingViewer`) → hard compile break; `DrawingGestureOperationJobFactory` omits `PUBLICATION_CONTRACTS` so `register_tool_job_factories` faults `interactive-job.publication-contract` at construction; 20 of 26 commands `Unclassified` (only the 6 canvas gestures `Migrated`); `setActiveExample` id space (`"semio"`) never intersects the registered examples (`"demo"`, `"demo-session"`); boot document, windows, panels, viewer and mutation-subset wiring are sound. Activation kind `"2d.drawing"` matches the repo convention (lowpoly `3d.lowpoly`) — not a defect.
- 00:47 Coordinator fixes: plugin root renamed to the real tree (`editor::drawing::DrawingPlayApp`, `viewer::drawing::DrawingViewer`, `artifacts::drawing::*`); editor proof `owner_file` plugin segment `🖍️drawing` → `🖍️draw`.
- 01:05 `📓️explore-artifact-io.md` (Sonnet): `✳️any/🚪️io/🦀️.rs:15-19` imported four stdio drawing names that lost their `Drawing` prefix (`DrawCanvas`/`DrawLayer`/`DrawNode`/`DrawStyle`) and a non-existent svg `subsets::any` (`write_svg_xml` lives in `subsets::base`); 72 `include_str!` fixture literals in the 8 `mutate-drawing-1*` test hosts pointed at pre-rename slug directories (hash-suffixed on disk); schema derives clean, no `to_dsl_value` self-call, io registered via typed leaves (json + svg export real, the rest honest stubs), native txt/pack codecs deliberately not in `NativeCodecs`; TS io twin's `fidelity` enum cannot express stubs.
- 01:08 Coordinator fixes: io imports repointed (raster's bridge line as oracle); `🐍️fix-fixture-include-paths.py` (dry run → unique prefix match → `--apply`) repointed all 72 literals, 0 unresolved, 130 already valid.
- 01:40 `📓️explore-compile-drift.md` (Sonnet): beyond the fixes already on disk, still live: 2× json rfc8259 `subsets::any` → `subsets::base` in the io leaves, 4× `async fn handle/render` against the sync `ArtifactEditor`/`ArtifactViewer` traits (`🔌️plugin/🦀️.rs:26623`, `:26972`), 2× E0046 `DrawingConfigMutation`/`DrawingPresenceMutation` lacking `DESCRIPTORS`/`descriptor`, 12 stale `#[cfg(test)]` `#[path]` mounts in the crate entry; `Label` import is a latent trap only; no wasm32-gated code; identity triple consistent.
- 01:42 `📓️explore-build-boot-infra.md` (Sonnet): committed owner descriptor pair is schema-stale (no `packageId`) and content-divergent (apps `s.draw.draw@1/*` vs source `s.draw.drawing`) → needs `describe` from a fresh component; identity/registry/deployment rows (`🖍️draw`) consistent; TS `package.json` a cad-js copy (block's is the only fixed oracle, note's is also a copy); no storybook metadata table, `os-plugins.spec.ts` passes vacuously on the missing-artifact panel; boot recipe recorded in the report.
- 01:45 Coordinator fixes: json io leaves `subsets::any` → `subsets::base`; 12 crate-entry test mounts repointed (same script, `#[path]` variant); `async` dropped from editor `handle`/`render`/`render_with_instance_operation_owner` and viewer `handle`/`render` (no body awaited anything); `draw-js/package.json` rewritten after block's shape (`dependencies: {}` — the TS tree imports only node/bun builtins). W2 (compile) dispatched for the mutation `DESCRIPTORS` gap and whatever the compiler surfaces next.
- 00:55 Seed rsync into `target-draw-e2e/debug` finished 00:52 (465 rmeta incl. stdio 21:29). W2 had started its check at 00:46 against the half-copied dir and cargo had planned a near-full rebuild (syn/serde/framework crates recompiling), so that cargo was killed and W2 asked to rerun; the coordinator's own duplicate check was stopped — W2 owns compile verification on this target. W1 (`📓️w1-dispatch.md`) has settled the framework contract and lane table (12 mutation commands `Artifact`, 5 view commands `Config`), W2 (`📓️w2-compile.md`) has added the config/presence `DESCRIPTORS` + `descriptor` and de-asynced six more `ArtifactEditor` methods and `ArtifactViewer::initial_snapshot`. Load 209.

## Session 2 — 2026-09-16 21:55 (session ⚪d58b098f, Opus 5; `/goal Get draw plugin working end to end`)
Repo MCP down again; bookkeeping manual. Ten days of peer commits landed on the crate since session 1 (window config/transient lanes 09-12…09-16), so the definition of done is re-verified from scratch against today's tree. Scripts added after the forms/energy pattern: `📜️activate-draw-react.sh`, `📜️serve-draw-react.sh`, `🐍️draw-console-dump-probe.mjs`.
- 21:57 Native `cargo check -p semio-s-plugin-draw --lib --tests` (shared build-dir, `🗑️generated/check-native-2026-09-16.txt`): `semio-s-artifact-draw-drawing` failed with 8× E0433 — `🖼️canvas/🫧️transient/🦀️.rs` (peer commit 8add1df147, 09-12) used `serde_json` directly, which is only a dev-dependency of the artifact crate. Rewritten after the forms Try-window transient oracle (`dsl::json::to_json_string`/`from_json_str`, utf8 body); no new dependency. Re-check green in 37 s (one pre-existing unused-import warning in the plugin's wgpu layout module).
- 22:05 `bun ./📜️script.ts test` (draw-js) failed: `Draw publication-authority oracle rejected production`. Diagnosed with a scratch script: the fixture/oracle were frozen at 09-09 while the source moved to the window lanes — `canvasPointerDown`/`canvasEscape` are `Artifact+WindowTransient`, `setCamera`/`setCameraZoom` `WindowConfig`, `engagementInput` `WindowTransient`; the framework-injected `setActiveUtility` route no longer exists (the utility swap is a window-transient edit); `build_config_store_one_item_preparation_factory` gave way to `register_window_config_owners`/`register_window_transient_owners`. Fixture rows, the plugin schema lane enum (`window-config`, `window-transient`, `interaction`), the oracle's `Lane` type, ANCHORS and hostile mutation #4 updated to the current contract → `2 pass`, `apps=DrawingPlayApp:24; hostile=5`.
- 22:12 `cargo check --lib --target wasm32-wasip2` EXIT=0 (`check-wasm-2026-09-16.txt`); `cargo test -p semio-s-plugin-draw --lib` 3 passed (`test-native-2026-09-16.txt`). Playground row pinned `app = "s.draw.draw@1/*#editor"` (forms precedent; descriptor app id verified in `🔣️.json`).
- 22:14 `screen -dmS draw-activate` restage launched; `cargo test -p semio-s-artifact-draw-drawing --lib` running detached (`test-artifact-2026-09-16.txt`).
- 22:20 Restage 1 EXIT=0 in 6m32 (`activate-draw-react.txt`, staged core wasm 58.6 MB). `cargo test -p semio-s-artifact-draw-drawing --lib` aborts the whole binary (non-unwinding panic in a Drop witness) three times over before the suite can report: (a) `marquee_select_covers_contained_layer_only` / `canvas_pointer_up_direct_pick_selects_inline` — the INPUT-CAUSALITY-LEDGER peer's staged, in-flight test flips (`settle_registered_typed_operation` faults `registered fixture typed operation fault`), NOT touched here; (b) the whole `retained_mutation_authority_tests` family — `DrawingMutationCandidateAuthority` is 157 312 B (`DrawingSnapshotBoundsAuthority` 71 768 + `DrawingMutationDigestAuthority` 67 864, inline `DRAWING_MAXIMUM_LAYER_DEPTH` frame arrays) so any nested fixture's census (`authority_bytes 157312 + page_bytes 70016 + container 63488 + source 60776 = 351 878`) exceeds `DRAWING_MUTATION_AGGREGATE_BYTES = ARTIFACT_ENVELOPE_DECODE_MAXIMUM_BYTES = 262 144` → `drawing-store.mutation-aggregate-byte-capacity`, then the candidate's Drop asserts. Constants unchanged since 08-23; the authority grew with the 09-12/13 owned-store rewrite (peer). Pre-existing, deterministic, unit-only. With those three groups skipped (`test-artifact-skip4-2026-09-16.txt`): 179 passed / 56 failed, every failure one of the known harness-debt families forms/energy also carry — `interactive-job.live-instance` (bare `drawing_app()` without a bound live instance), `artifact store reached Drop without its exact terminal-empty shallow-shell witness`, `edit history insertion requires its exact mutation retirement factory`, `Drawing retained envelope load did not reach terminal`. None is a runtime fault; see `project-registryless-testkit-new-app-unusable` / `project-store-drop-witness-and-mounted-test-harness`.
- 22:35 Serve 1 (`📜️serve-draw-react.sh`, vite 6064) → boot probe 1: `Framework OS boot failed: primary plugin draw does not declare pinned app s.draw.draw@1/*#editor` — the committed descriptor pair was stale (session 1 already flagged it): the fresh `describe` names the apps `s.draw.drawing@1/*#editor|viewer`. Pin corrected, `bun nx run @semio-tech/draw-plugin:describe` regenerated `🔣️.json`/`🛂️.descriptor.semio` (+764/−693), serve restarted (the registry pin is read at serve start).
- 22:40 Boot probe 2 (`draw-boot-2/`): `ready=draw`, title `semio · drawing`, one window host `window:drawing-composite` (1427×807, 1 canvas), navbar fixture `Demo`, panels Artifact/Catalogue/Inspection, zero pageerrors, zero guest traps. One runtime fault: `action failed setActiveExample {exampleId: demo}: action 'setActiveExample' is not a framework-reserved action … app actions are dispatched exclusively through the typed command channel` — the forms fault (3) verbatim: `DrawingPlayApp` had no `command_from_action`, so every ribbon/palette/actions-pane verb was refused and the Demo example never loaded (`0 layers · 0 selected`). Added `args_bridge` (camel/snake fold, integral floats, JSON-text `value`/`json` fields, `id`→`layer_id` aliases) + the trait override, unit test green. DOM survey (`🐍️draw-dom-survey.mjs`, `draw-survey-1/`): Actions pane rows `action.addLayer|combineBoolean|setActiveExample|copy|cut|paste|clearSelection|selectAll|setSelectionMode|setInteractionGranularity|undo|redo|…`, engagement input `#drawing-canvas-engagement` ("Layer name"), utility bar `framework.window.drawingComposite.utilityBar.unfold`.
- 22:48 Peer mid-refactor broke `semio-framework-plugin` (`🪟️window/🎚️config/🦀️.rs:597` vs `🦀️.rs:26983` arity) for ~10 min; settled by itself. Restage 2 launched with the bridge; `🐍️draw-interact-probe.mjs` written (addLayer via Actions pane → undo → shapeRect canvas drag → example switch).
- 23:05 Interact probe 1 (`draw-interact-1/`): the bridge reaches the guest, but `setActiveExample` TRAPS the actor — `🏪️store/🦀️.rs:2715 artifact envelope terminal shell reached Drop before its app-owned bounded retirement authority detached every nested owner` — process3d's fault (1) verbatim (`project-process3d-load-document-path`): `drawing_reset_document_effect` minted `store::create_document_envelope` just to `print_document_spr`. Now `store::empty_document_spr(&scene.id, DRAWING_DOCUMENT_SCHEMA)` (fem/process3d shape). Restage 3.
- 23:20 Interact probe 2 (`draw-interact-2/`): no trap, `setActiveExample` settles with 1 effect → host `loadDocument pack 28148 spr 261` → 28 417-byte archive ingress → 116 three-byte polls → silence: window stays `0 layers · 0 selected`, no fault, no refresh. Worker console captured (`🐍️draw-console-dump-probe.mjs` now hooks `page.on("worker")`; `draw-boot-3/` with `SEMIO_PROBE_GUEST_DIAGNOSTICS=1`): nothing from the guest either. Reproduced natively with a new law `✏️editor/🧪️tests/🔬️archive-load/🦀️.rs` (sourcing's `begin/poll/acknowledge_document_archive_load` recipe): status `Running 0/1` for 20 000 polls. Driving `store::RetainedPersistedDocumentHydration::from_pack` directly with a TEMPORARY phase eprintln in the framework (reverted, file clean): every phase passes until `RetireValidation`, whose `drive_active` — draw's own `DrawingSnapshotRetirementFactory` cursor — returns `Pending { 0, 0 }` forever. Root cause in `🧰️owned/🦀️.rs` `release_string`: `value.len() > maximum_bytes → Pending { 0, 0 }`, and the demo's `emblem-trace-source` asset is a 29 148-byte base64 PNG while every hydration/retirement grant is `OWNED_SCHEMA_DECODE_PAGE_BYTES` = 4 096. gis/raster carry the same helper but no >4 KiB strings. Fix: page the release from the tail (`truncate` + `shrink_to_fit`, char-boundary safe, `released_items: 0, released_bytes ≤ grant`). Hydration now settles in 315 rounds; the law reaches `Ready 1/1` in 5 polls, `id=semio layers=1 assets=1`, and closes its stores before drop → green. Restage 4.
- 23:0x Disk hit 0 B free twice (peers' wasm-dev incremental sessions, 56 GB); pruned `-mindepth 2 -mmin +45` under `debug/incremental`, `wasm32-wasip2/wasm-dev/incremental`, `wasm32-unknown-unknown/*/incremental` → 21 GB free.
- 2026-09-17 00:10 Restage 4/5 + boot probe with TEMPORARY guest `[DBGARCH]` eprintlns in `🔌️plugin/🦀️.rs` (poll status, replacement state, disposer Blocked reason; all removed again, `git diff` shows only their removal since the auto-commit had caught them): the archive load now passes hydration, sits in `AwaitReplacement` with the replacement in `RetiringCommittedStore` → `Blocked { "document store close awaits a retained reader or owner" }` for ~270 polls, then completes → `Ready 1/1` (`draw-boot-5/`). The transient block is the host's snapshot read leases returning; while the peer's ARTIFACT-TREE-VIRTUALISED-STREAMING wave had the React host rejecting every UI node (`plugin-ui.intake-rejected:typed-normalize:Unknown UI field: granularity|window`, `draw-boot-4/`, TS typed wire at Sep 13 vs Rust contract) the leases never returned at all — that peer landed the TS side ~00:40 and boots are clean again.
- 00:50 Interact probe 4 (`draw-interact-4/`): load `Ready`, yet the host's `contributions document sources` read `packBytes: 250` (the boot doc was 383, the demo pack 28 148) and the window stayed `0 layers` — while `addLayer`'s history label said `create-layer index=1`, i.e. the live fold DID hold the demo layer. Reproduced natively (`🔬️archive-load/🦀️.rs` now also reads `document_pack()` back): `current` = 28 148 B, `envelope.vcs.initial_snapshot` = 250 B = a fully EMPTY snapshot. Traced with temporary step prints to draw's own `DrawingStoreInitializationAuthority` (`🧰️owned/🦀️.rs`): phase `MoveInitialOwner` `mem::replace`d the envelope's initial snapshot with an empty one to seed the runtime — every loaded/replaced document read back (`print_document_pack`, the cold from-scratch fold) as empty. Only draw does this (raster/gis/process3d page-CLONE their initial). Fix: `CloneInitialHeader` → `CloneInitialLayer { index }` (reuses `DrawingLayerCloneAuthority`, one layer per cursor) → `CloneInitialAsset { index, cursor }` (base64 `data` copied one 4 KiB page per step) → `MoveInitialOwner` now adopts the clone; partial clones retire through `pump_terminal_retirement`. Law: `demo_example_load_settles_through_the_host_document_archive_door` asserts `initial == current` and the 29 104-byte emblem survives. Green.
- 01:20 `cargo test -p semio-s-artifact-draw-drawing --lib` aborted on `canvas_pointer_up_direct_pick_selects_inline` (status 22:20 filed it as the INPUT-CAUSALITY-LEDGER peer's flip — it is two DRAW bugs, both also the reason every browser gesture failed `typed-operation failed:` with an empty message in probes 2–4): (1) `DrawingRetainedCommandDecoder` validated the retained wire pages as `["verb", {...}]` JSON text; since 09-15 the host crosses the command's OWN binary op encoding (`app_commands!` `OpBinary`, what `ArtifactRetainedCommandJob` decodes for the bounded lane). Rewritten: pages accumulate within `DRAWING_GESTURE_RAW_BYTES`, `DrawingCommand::decode_op` + verb check + byte-equal re-encode (truncation, trailing garbage, variant swap, oversize all reject; law rewritten to encode real commands). (2) `DrawingGestureOperationOwner::close_step` demanded the whole `DRAWING_GESTURE_RETAINED_BYTES` (32 KiB) grant in one step while every framework close/maintenance pump grants 4 KiB → no gesture owner ever closed (`registered fixture did not reach its exact terminal-empty witness, last pending close authority: Drawing gesture close owner awaits its exact grant`). Now the session drops on the first granted page and the declared budget is handed back page by page. (3) `marquee_select_covers_contained_layer_only` then exposed a third: `FixedOperationRegistry::begin_close_step` and `close_step` share one cursor and each advance it by one, so `DrawingInstanceOperationOwner::close_step` (both per step) only ever marked every OTHER slot closing — an owner on the wrong parity spun `Pending { 0, 0 }` to the Drop witness. The first close step now sweeps all 64 slots. Laws: `drawing_gesture_owner_closes_under_the_framework_page_grant`, `drawing_instance_owner_close_reaches_every_slot_parity`. (4) `drawing_completion_rejection_retires_child_before_decoder_without_redispatch` expected `close_step(0, 1) == Pending{0,1}`; the framework's `ChildEmit::close_one` now short-circuits a zero-item grant → expectation updated. Suite (`--skip retained_mutation_authority_tests`, the known 157 KiB-authority abort family): 191 passed / 54 failed, every failure one of the documented harness-debt families (`interactive-job.live-instance`, shallow-shell Drop witness, missing mutation retirement factory, retained envelope load) — no aborts. Also: arg-less palette `addLayer` now defaults `kind = "path"` in the bridge (the Actions pane dispatches verbs without args). Restage 9 launched.
- Peer churn absorbed on the way: `PanelTreeBuilder::interaction_domain` grew a `controller_id` arg (A8 swept draw's layers panel to windowed sections); a `TreeWindows`-based `render_drawing_body`; `granularity`/`window` UI fields. Disk hit 0 B again (peers' incremental sessions) → pruned to 87 GB free; swap at 20/21.5 GB made the draw test link SIGKILL (137) twice — `-j 1` and retries.
- 01:50 Restage 10/11 with temporary guest `[DBGGEST]` prints (removed again): every browser gesture after the first faulted `drawing.gesture.saturated`. Cause: `FixedOperationRegistry` is direct-mapped by `operation_id % 64` and the framework mints every tool operation id INTO the first vacant residue class of its own 64-slot table (`allocate_operation_id_in_slot`) — ids 256, 320, 384, … all `≡ 0`, so the next gesture always landed on the slot still held by the previous gesture's cancelled-but-unretired owner; and the maintenance pump gave that owner one page per 64-slot cursor lap (576 steps to retire one). Fix (`✏️editor/🦀️.rs`): admission retires the retiring residue-class owner in one bounded sweep before admitting; `maintenance_step` skips idle slots within one sweep (9 steps per retirement). Laws: `drawing_gesture_admission_retires_the_retiring_owner_in_its_residue_class`, `drawing_gesture_maintenance_retires_a_cancelled_owner_promptly`. Also the window status row now reports the LIVE layer count (was a hard-coded `0 layers · 0 selected`, which is what the probe reads).
- 02:00 NEAR MISS: a scripted anchor cut in `🔌️plugin/🦀️.rs` matched a trait declaration and deleted 17 000 lines; caught by `git diff --stat`, restored from the INDEX (`git show :path`, newer than HEAD because the auto-commit stages peers' edits) minus my diagnostics; `cargo check` green. Memory `feedback-anchor-cuts-need-unique-anchors-and-diffstat`.
- 02:10 Restage 12 + interact probe 8/9 (`draw-interact-8/`, `-9/`): boot `1 layer` (demo loaded and visible), Actions-pane `addLayer` → `2 layers`, ⌘Z → `1 layer` (`undone: true`), zero `typed-operation failed`, zero gesture faults; pointer moves/ups now publish `Hover`/`Select`. The rectangle drag still did not commit: `setActiveUtility next=shapeRect` was written into the host's PER-WINDOW register (`active_utility_by_window_id`, keyed by window instance) and only the shell's active window is mirrored into the flat `active_utility_id` draw read, so the gesture ran as a marquee select. `drawing_active_utility(view)` now resolves the addressed window's entry → focused window's → flat field → default, at every site (bounded/gesture payloads, render preview, window engagements). Law `shape_rect_drag_commits_with_the_per_window_utility_map_alone` (on the mounted `inline_selection_app` harness; the retained lane publishes through the receipt, not `result.mutations`). Probe: the utility bar renders GROUPS first (`ui.utilities.drawing-composite.group.group:Drawing`), the utilities (`#shapeRect`) appear once the group is toggled — `🐍️draw-interact-probe.mjs` now toggles the group and hooks the worker console.
- 07:15 Restage 13 + interact probe 10 (`draw-interact-10/`): ALL FIVE journeys pass in the browser — boot `1 layer`, addLayer `2`, undo `1`, shapeRect drag commits `2` (`committed: true`), example switch to `No example` loads (`packBytes 383` ~18 s after the load began; the probe's 12 s settle read the count before completion). Native suite (`--skip retained_mutation_authority_tests`): 194 passed / 54 failed, every failure a documented harness-debt family, no aborts. Open: archive loads are slow in the browser (demo ≈130 s, empty doc ≈18 s — the replacement's `RetiringCommittedStore` waits on the host's snapshot read leases and each poll spends one `BACKGROUND_LANE_WALL_US`); the arg-less Actions-pane row for `addLayer` has no execute control (the probe presses Enter); the descriptor's static engagement text stays `0 layers · 0 selected` until the first render.

## Session 3 — 2026-09-18 (session ⚪d58b098f, Opus 5; `/goal Get the export of the draw artifact to pdf working fully end to end`)
- Found: `DrawingIntoPdf` (`🚪️io/📤️export/…/📖️pdf/🔖️1.4/✳️any/🦀️.rs`) was an honest "not yet implemented" refusal; no draw command exported anything to the host; `s.stdio.pdf`'s 1.4 and 1.7 snapshots are TEXT-ONLY page models (`encode_pdf` regenerates a content stream from `PageDoc.text`, no path-painting operators), and the stdio semio/drawing→pdf 1.7 serializer likewise drops every `Path`/`Image` node — so no existing route could carry vector content.
- Built `drawing_document_to_pdf` in that leaf: one PDF 1.4 page = the artboard, base CTM `1 0 0 -1 0 H cm` (drawing y-down → PDF y-up), painting the canvas's own `flatten_drawing_document_to_scene_nodes` projection — path/shape/boolean/trace outlines (quads → cubics, SVG endpoint arcs → quarter-turn cubics), solid fills (`f*`/`B*`, even-odd like the canvas), axial/radial shadings (`/ShadingType 2|3`, Type 2/3 stop functions, clip-through-outline), strokes (width/cap/join/dash), opacity + blend mode as deduplicated ExtGStates, Helvetica/WinAnsi text (flip-back `Tm`), PNG image layers as Flate RGB XObjects with an alpha SMask. Streams Flate-compressed through `semio-s-artifact-stdio-deflate` (new dep); `semio-s-artifact-stdio-pdf` added as a dev-dependency so the laws open every export with stdio's own 1.4 reader. Five painter laws + one io-row law (`declared_pdf_serializer_entry_answers_a_pdf_for_the_native_pack`).
- New command `exportDocument { format }` (`🎮️commands/📤️export-document/🦀️.rs`): `pdf` (default when the palette row dispatches arg-less) → `Effect::DownloadMediaExport { <id>.pdf, application/pdf, base64 }`; `svg` → the stdio drawing→svg bridge as `image/svg+xml`. Wired: `app_commands!` row (appended — binary ordinal), bridge default `format=pdf`, `DRAWING_BOUNDED_TOOL_IDS` (19) + `HostOnly` contract, proof catalog, palette action "Export PDF" (`ActionKind::View`, icon `download`), `🧫️fixtures/🧪️publication-authority/🔣️.json` route, `every_command` fixture + wire-keyword list, descriptor regenerated (`bun nx run @semio-tech/draw-plugin:describe`). Law `export_document_downloads_a_real_pdf_of_the_document` (mounted harness; both formats). Suite: 201 passed / 54 failed (the same documented harness-debt families, no aborts); draw-js `bun ./📜️script.ts test` 2 pass (publication authority validated, 25 routes).
- End to end (`🐍️draw-export-probe.mjs`, `🗑️generated/draw-export-1/`): restage 14 + serve restarted (it had died overnight) → boot Demo → Actions pane → `Export PDF` row → the host's `DownloadMediaExport` produced a browser download `semio.pdf` (704 B, `%PDF-1.4`, one page, `/MediaBox [0 0 200 200]`, classic xref). Rendered independently by macOS (`sips -s format png semio.pdf`) → `semio-rendered.png` shows the Semio emblem exactly as the canvas draws it. Content stream = three `B*` paths with the demo's fill colours and the 2.5-unit miter stroke.
- Known limits (documented in the leaf's module doc): Helvetica only (no font embedding), gradient-stop alpha folded into layer opacity, group transforms not composed onto children (mirrors the canvas); icon restage 15 pending.
- 14:15 Restage 15 (download icon) + describe regenerated (`iconId: download`) → export probe 2 (`draw-export-2/`): Actions pane row **Export PDF** (download icon) → browser download `semio.pdf` 704 B, PDF 1.4, `/MediaBox [0 0 200 200]`, zero faults; `sips` renders it as the emblem (`semio-rendered.png`). Goal met end to end.
