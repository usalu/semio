- B2→B1/main: `brushFillSessionAdopt|Cancel|Discard|Clear` emit ONLY the `Config` lane (not `Artifact,Config` as registered — over-declared is accepted by the gate, so non-blocking); B2 deleted `is_fill_session_action`/`dispatch_fill_session_action`/`mounted_job_prepare_snapshot_read`/`pending_effects` from the editor and made `handle()` refuse the 8 fill ids — do not re-add (see `📓️wave-B2-report.md` §6). Nothing in B2 is compile-verified.
