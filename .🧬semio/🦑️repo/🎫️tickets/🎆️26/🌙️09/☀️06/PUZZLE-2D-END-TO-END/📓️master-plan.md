# ◻️ Puzzle 2d end to end — master plan (2026-09-06 05:00)

Ground truth from the eight `📓️explore-*.md` reports (all measured today, not inherited):

| Area | State |
|---|---|
| Editor actions | **4 Migrated / 34 BatchOnlyPendingRewrite / 2 Unclassified** (`setLocale`, `setTerminology` have no ActionDefinition at all). Retained-job factory, proofs macro, `build_tool_job`, two bespoke Works (`Puzzle2dActiveExampleWork`, `Puzzle2dForceLayoutWork`) exist. **Missing**: Config/Artifact store-preparation factories, a generic `puzzle2d_retained_reduce` fallback (only `addNode` handled), extents for selection-iterating actions. `forceLayout` hard-gated at 64 nodes → dead on Nakagin (180 nodes). Fill-session family (8 actions, `🧮️set-fill-count/🦀️.rs` 2,401 lines, WorkerPool + global slot registry) needs a bespoke job design. `redrawHandles` is an unbounded whole-fixture call. `selectSameKind`/`lodScaleJson` are genuine no-ops. |
| Publication authority | `🔏️publication-authority/🔣️.json` stale (3 routes, all BatchOnly). TS oracle `📦️packages/🟦️typescript/📜️script.ts:85-89` asserts puzzle2d has NO proofs macro — already false; needs a real branch like 3d/5d. |
| Mutations & fixtures | 26 kinds, exactly 1 `applied` vector each, 0 rejected. `🔌replace-node-handle/🔺️diff/🦀️.rs:13-21` no-op guard runs before the replace loop → verb is always a no-op, fixture bakes the bug in. 4 of 26 `🥒️.feature` vector paths are stale (hex-suffixed dirs). Puzzle fixture lint never discovers bare `<leaf>/🦀️.rs` trees. No fixture generator exists. |
| Schema | `🧬️mutations/🔣️.json` is a byte-copy of the snapshot schema; must become a `oneOf` of 26 `$defs` (architect `ProgramMutation` pattern). Artifact-root `◻️2d/🔣️.json` is an orphaned puzzle3d concrete-forest manifest (glb meshUrls, `puzzle3d.attraction.link`); the live default manifest is the oddly named `◻️2d/🛂️manifest.jsondefault.manifest.json` (include_str! at `⚙️engine/🎲️board-host/🦀️.rs:348`). |
| Examples | concrete-forest = 1 node / 0 edges, 3d-borrowed stub. nakagin = 180 nodes / 179 edges / 358 handles, real. Playground catalog advertises 4 examples; only 2 exist (`capsule-dream` absent, `demo-session` is a cmd-replay fixture). Boot default is an EMPTY board; `setActiveExample` is already migrated, chunked (4 mutations/step). |
| Oracle | `verified-native-second-implementation` (Python) mechanically discharges the gate; a third-party oracle is still owed in spirit. Fit: networkx (handle-as-vertex MultiDiGraph, cascade via the library's own `remove_node`), shapely (move/scale/anchor/geometry), jsonschema (draft-07 payloads), jsonpatch/deepdiff (diff reproduction); TS second ecosystem graphology + ajv + fast-json-patch. Honeybee → OpenStudio → EnergyPlus evaluated and declined (no typed-port/compatibility concept; belongs to 🔋️energy). Test schema `QualifyingOracleKind` enum lacks `verified-native-second-implementation` that TS already accepts. |
| Build | Crate builds 2d+3d+5d; `describe` output stale since 09-04; peer mid-edit on shared `🔌️plugin/🖨️describe`. wasm32-wasip2 uses `wasm-dev` + 8 MiB stack via os-dev. No puzzle `test`/`describe` launch.json entries. |
| UI | One mode, triptych `2d-overview`/`2d-detail`/`2d-selection`, all `Board2d` surfaces rendered by React `Board2dHost` + wasm-pack `BoardSession` (WebGPU). Storybook spec `.storybook/puzzle-2d.spec.ts` exists but `Board.stories.tsx:10-11` imports a non-existent ASCII path and `scopes.ts:92` points at `🎛️apps/◻️2d`. Terminology: 30/30 EN+DE complete but `puzzle2d_labels` silently defaults to English (3d is fail-closed). |

## Waves (parallel, disjoint write regions; main session owns every cargo run)

| Wave | Agent | Scope | Write region |
|---|---|---|---|
| **A1 fixtures** | Opus | Fix `replace-node-handle` diff bug; author real-world scenarios for all 26 mutations from Nakagin (+ concrete-forest), incl. rejected vectors (D6 `🚫️.absent`), fix stale/odd scenario names, regenerate `🥒️.feature` Examples, update `🐍️.py` (implement replace-node-handle, null catalogs), bump `fixtureCoverage`; fix puzzle fixture-lint discovery for bare-leaf trees | `🧬️schema/🧬️mutations/**` (tests + the one diff fix), `🧪️tests/◻️mutate-puzzle-2d-1/**`, `🔮️oracle/🔣️.json` `mutationCatalogs`+`fixtureCoverage` only, rust `📜️script.ts` discovery |
| **A2 schema+hygiene** | Opus | Handcraft `🧬️mutations/🔣️.json` oneOf; resolve orphaned `◻️2d/🔣️.json` + odd manifest filename (with include_str!/taxonomy updates); fix playground example catalog source; storybook path fixes; launch.json puzzle test/describe entries | `🧬️schema/🧬️mutations/🔣️.json`, `◻️2d/🔣️.json`, `◻️2d/🛂️manifest*`, `⚙️engine/🎲️board-host/🦀️.rs:348` only, registry example source, `.storybook/**puzzle**`, `.vscode/launch.json` |
| **B1 migration core** | Opus | Store-preparation factories, generic `puzzle2d_retained_reduce`/extent, register 24 mechanical + `reorganize` + `setActiveExampleStep`, `setLocale`/`setTerminology` ActionDefinitions, flip classifications, remove dead legacy `handle()` example branch, rewrite `🔏️publication-authority/🔣️.json`, real puzzle2d branch in TS oracle | editor `🦀️.rs`: factory/reduce region, the 4 registration lists, `.action_interactive_job` block, `handle()`; fixture json; TS `📜️script.ts` |
| **B2 hard Works** | Opus | Fill-session family bespoke job design + Works (8 ids), `redrawHandles` chunked Work, `forceLayout` gate/extent soundness (Nakagin must pass), `PUZZLE2D_FORCE_MAX_HANDLES` in extent | editor `🦀️.rs`: Work impls region + its own `build_tool_job` arms (appended at end of match), `🎮️commands/🧮️set-fill-count/🦀️.rs`, `🔄️redraw-handles`, force-layout Work |
| **B3 terminology + reserved routes** | Opus | Fail-closed `puzzle2d_labels` (Option, like 3d) + `puzzle2d_localized_phrase`, labels for `setLocale`/`setTerminology`, explicit `import-media` reserved factory (5d pattern) + `build_reserved_tool_job` | `🗣️terminology/🦀️.rs`, editor `🦀️.rs`: reserved-route region + `build_reserved_tool_job` + label helper |
| **C1 oracles** | Opus | networkx/shapely/jsonschema/jsonpatch Python oracle + graphology/ajv/fast-json-patch TS oracle, registrations (`third-party-library`), `pyproject.toml` test group, `🔒️dependencies.json`, fix `QualifyingOracleKind` schema drift | `🔮️oracle/🔣️.json` `oracles[]`, new oracle adapter files under `🧪️tests/`, root `pyproject.toml`, `🔒️dependencies.json`, test schema json |
| **D build+boot** | main | native check → wasm32-wasip2 → `cargo test -p semio-s-plugin-puzzle` → `describe` → registry check → `dev 2d` react boot → browser proof (3 canvases, example switch, action dispatch) | cargo/dev runs only |

Sequencing: A/B/C run now in parallel. D starts native+wasm checks as soon as B1 lands, then tests after A1, then boot. Any compile fallout is routed back to the owning wave.

Honeybee → OpenStudio → EnergyPlus: declined for this artifact (see `📓️explore-oracle-libraries.md` §6); the ticket 🔋️ENERGY-PLUGIN-END-TO-END owns that chain.
