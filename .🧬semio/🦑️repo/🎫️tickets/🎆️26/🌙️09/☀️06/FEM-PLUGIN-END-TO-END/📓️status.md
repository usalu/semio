# 🏗️ Fem plugin end to end — status

Apps under test: `fem2d`, `fem3d` of plugin `✏️s/🔌️plugins/🏗️fem` (artifacts `🗿️artifacts/◻️2d`, `🗿️artifacts/🧊️3d`; kernel `✏️s/🔨️modules/🏗️fem/⚙️engine`).
`bun run dev:fem:<2d|3d>` → `bun ./📜️script.ts dev fem <nd>` → framework-os-dev playground (react 6086/6087, wgpu 6186/6187).
Ticket opened 2026-09-06 00:30 by session ⚪b3308b14 (Fable 5.1 coordinator). Repo MCP timed out; bookkeeping is manual on disk.
Ticket start commit: `🗑️generated/start-commit.txt`.

## Definition of done
1. `cargo check -p semio-s-plugin-fem` green natively and on `wasm32-wasip2`.
2. Every mutation of fem2d and fem3d has a real-world fixture test, passing in Rust and validated against a third-party oracle (Python FEM solver / TS); `bun nx run @semio-tech/fem-plugin:test` green.
3. Owner-root `🔣️.json` / `🛂️.descriptor.semio` regenerated via `describe`; registry check accepts fem.
4. Both playgrounds boot (react renderer, then wgpu wasm): every window renders non-empty, examples load and switch, editor actions dispatch at runtime (no `interactive-job.missing-owned-reducer` / bounded-factory faults), confirmed with console logs.

## Log
- 2026-09-06 00:30 open. Host: 32 GB RAM, swap 63.4/64.5 GB used, load avg 148, ~20 peer cargo/rustc processes (some 1-4 h old). No builds launched until memory recovers.
- 00:35 exploration fleet (6 × Sonnet, read-only, no builds) launched: fem2d editor+dispatch, fem3d editor+dispatch, tests+oracles+fixtures, engine kernel+solver oracles, dev-boot+TS+descriptor gates, history+API-drift+peer gates. Reports land as `📓️explore-*.md`.
- 00:50 dev-boot/TS/descriptor explored (`📓️explore-dev-boot-ts-descriptor.md`): boot mechanics identical to block/raster (default wgpu, `SEMIO_RENDERER=react`, closure {fem, stdio}, react 6086/6087, wgpu 6186/6187); served fem wasm is stale (mtime 2026-08-17, sha256 mismatch vs registry) → first boot must be a full un-narrowed build; io on the dead `ComposerEntry` channel with zero `io_mechanism` entries (block W3 gap class); TS `package.json` is a verbatim cad-js copy; no fem storybook scope; descriptor pair stale since 09-02 (`bun nx run @semio-tech/fem-plugin:describe`).
- 00:47 engine kernel explored (`📓️explore-engine-kernel.md`): 16 files / ~19k lines, no stubs, zero external runtime crates (own LDLT/PCG/subspace-iteration eigensolver/RCM, Bowyer-Watson Delaunay with holes, 3d = footprint extrusion → prisms → tets); elements Bar/Beam/Frame, CST/LST/Quad4/Quad8, PlateDkt, Tet4/Hex8, ShellFacet3; analyses linear static/modal/buckling (no nonlinear, PlateDkt has no geometric stiffness). Solver numerics have NO third-party oracle (only mesh export has manifold-3d/three.js); modal/buckling tests at 10% tolerance; three "order-of-magnitude" tests; inference cache covers only `bounds`; results window re-solves synchronously in `render()` alongside a separate `FemJobGraph` session. 10-item benchmark suite proposed (PyNite/anastruct/scikit-fem/scipy).
- 00:52 fem3d explored (`📓️explore-fem3d-editor.md`): all editor/viewer/window modules real; mutation enums match disk 1:1 (25 kinds each artifact); dispatch only 2/18 `Migrated` (`setCamera`/`setResultDisplay` via `Fem3dRetainedCommandJobFactory`), 16 mutating actions on `BatchOnlyPendingRewrite`; Model windows' base scene is `"[]"` until `live_visual` reconciles. ROOT COMPILE HAZARD: 43 `#[cfg(test)]` `#[path]` mounts in the crate entry (22 fem3d + 21 fem2d) point at long fixture-case dir names that commit b0dfa0f09b (2026-09-05 19:04, Windows-path-length truncation, e.g. `🚫️removes-the-unreferenced-timber-material` → `🚫️removes-the-30f7a2`) renamed on disk without updating the mounts. Fix direction: update the mounts to the on-disk names (the truncation is intentional).
- 00:58 fem2d explored (`📓️explore-fem2d-editor.md`): 25 mutations × 5 subsets all with diff+inverse+schema+1 fixture case, enum matches disk 1:1; NO de-async/UiNode/with_ports/render drift (fem never adopted the old API, 154/160 `async fn` are `#[async_test]`); compile blockers = `Fem2dConfigMutation`/`Fem2dPresenceMutation` missing `DESCRIPTORS`/`descriptor` (E0046, block2d fix shape) + the 43 dangling test mounts; dispatch 3/19 `Migrated` (`setCamera`/`setResultDisplay`/`setLocale`), 16 incl. `setActiveExample` on `BatchOnlyPendingRewrite`; editor boots into an empty snapshot; viewer has only a `model` window.
- 01:00 Wave 1 launched (4 × Opus, edits only, no builds while swap is exhausted): W1 crate-entry test mounts (43), W2 fem2d descriptors+dispatch+boot, W3 fem3d descriptors+dispatch+boot, W4 io_mechanism registration for both artifacts. Compile verification deferred to memory recovery.
- 00:55 tests/oracles explored (`📓️explore-tests-oracles.md`): fixture layer exhaustive at 1 case per kind (25 kinds × 2 artifacts, each with mutation/outcome/diff/before/after + feature/rs/py triple per subset); third-party oracle only for the 3 region/solid geometry kinds (three + manifold-3d); 22/25 kinds per artifact carry a self-declared `noOracleDecisions` debt; the python harness is a cross-implementation, not third-party; NO FEM solver installed (uv env has numpy 2.5 / scipy 1.18 only); cache results are plan-only markers; last real run 2026-08-05 was 318 pass / 10 fail (DSL preamble `expected List, found Absent`).
- 01:00 history/drift explored (`📓️explore-history-drift-gates.md`): prior tickets closed cleanly; grep-level drift classes a–j clean (contradicts fem2d report on `DESCRIPTORS` — W2 resolves against the trait); class k = the 42/43 hash-truncated test dirs also referenced by sibling `include_str!` reads; external gates (stdio, os-kernel, plugin) committed/clean except peer edits in plugin's browser-bundle subtree; no fem build running.
- 01:02 DECISION oracle strategy: add third-party FEM solvers as test-only Python deps (`anastruct` for 2D frames, `PyNiteFEA` for 3D frames, `scikit-fem` for continuum plate/solid + scipy eigen for modal/buckling) in root `pyproject.toml` `test` group; solver oracles judge ANALYSIS RESULTS on real-world snapshots (committed `📊️expected.results.json` produced by the oracle now, Rust asserts against it once compilable), closing the `noOracleDecisions` debt honestly. Wave 2 launched: W5 fem2d solver oracles, W6 fem3d solver oracles, W7 kernel benchmark tightening. W8 (extra fixture cases per kind) waits for W1 (crate-entry owner).
- 01:10 Coordinator: rewrote `✏️s/🔌️plugins/🏗️fem/📦️packages/🟦️typescript/package.json` (was a verbatim cad-js copy: cad description, cad `generate`/`fixture` scripts, 9 cad workspace deps) → fem description, `test` only, devDependencies = the oracle libs fem TS actually imports (manifold-3d, three, three-mesh-bvh, vitest, typescript). Added `🧪️test🏗️fem📚️examples` to launch.json after block's entry.

## 2026-09-06 20:45 — resumed after fleet loss (entry times below are approximate wall-clock)
- ~02:30 all seven Opus agents (W1-W7) were killed by the account's Opus session limit (reset 04:10); the host rebooted ~02:49; the previous session process exited (memory watch lost). Surviving work: W1 had already fixed the 43 crate-entry mounts (`🔨️resolve-mounts.py` → 343/343 resolved, auto-committed) and left `🗺️truncated-names.tsv`; W5/W6 had added `anastruct`, `scikit-fem`, `PyNiteFEA` to `pyproject.toml` `test` group. Nothing else landed (git diff since start: crate entry + TS package.json only).
- 20:45 host: swap 31.8/32 GB used, load 50, 43 peer cargo/rustc. Still no builds. Memory watch re-armed (<70 % swap).
- 20:50 stale old-name references remain outside the crate entry: 43 truncated names × 218 files (130 🦀️.rs incl. `include_str!` fixture reads, 45 🥒️.feature, 43 🔮️oracle `🔣️.json`). W1 relaunched with that scope; W2-W7 relaunched from scratch with the same briefs.
- 21:10 Solver deps resolved: uv env (Python 3.14.5) has anastruct 1.7.0, PyNiteFEA 3.0.0, scikit-fem 12.0.2. `import anastruct`/`import Pynite` initially died with `KeyError: '_items'` from matplotlib's `_get_macos_fonts` (`system_profiler -json SPFontsDataType` returns no `_items` on this thrashing host). Built matplotlib's font cache once with a `system_profiler` shim on PATH (scratchpad `shim/system_profiler` printing `{"SPFontsDataType":[{"_items":[]}]}`); `~/.matplotlib/fontlist-v3.11.0.json` now exists and plain imports succeed.
- 21:12 W1 landed (`📓️w1-test-mounts.md`): 1334 stale pre-truncation references rewritten across 89 files (path 516 / identity-key 85 / prose 733; 63 🦀️.rs, 12 🥒️.feature, 12 🔣️.json, 1 generator script, 42 `members-of-tests` rows of `🔣️taxonomy.json`) with `🔨️fix-stale-names.py`; rule: directory name is authoritative, scenario `id` = `leadingEmojiIdentity(dir).rest`. Coordinator resolved W1's blocker: the emoji-less `t008` case dir (2d `update-analysis-settings`) renamed to `🔢️doubles-the-modal-3fbb1a`, six referencing files + TSV updated, script re-run registered it in the taxonomy. Static verification: crate-entry mounts 343/343, tree-wide `include_str!` 855/855, zero `t008`/old-name hits.
- 21:15 W3 landed (`📓️w3-fem3d-dispatch.md`, 15 files): `DESCRIPTORS`/`descriptor()` added to `Fem3dConfigMutation`/`Fem3dPresenceMutation` (trait `📡️replication/🎮️mutation/🦀️.rs:145-152` has no defaults — the drift report's "clean" was wrong); 18/18 fem3d actions `Migrated` under `Fem3dRetainedCommandJobFactory` (reduce = `command.dispatch`), plus an Artifact-lane one-item preparation factory (else `publication-authority-missing`) and a fixed contract mismatch (proofs declared `bounded_first_step`, factory returned `resumable` → would have faulted even the 2 already-migrated actions); editor+viewer boot `fem3d_boot_snapshot()` (16 nodes / 16 elements / 1 solid); 7 `[DEBUG]` lines for boot/reconcile/window lease; rustfmt parse-clean, not compiled; 10 tests moved to `testkit::fem3d_empty_app()`.
- 21:25 W2 landed (`📓️w2-fem2d-dispatch.md`): 4 config + 1 presence descriptors; 19/19 fem2d actions `Migrated`; two blockers the exploration missed closed — no document-lane `ArtifactStoreOneItemPreparationFactory` (→ `publication-authority-missing`) and no `command_from_action` override (trait default rejects every app action, so no shell button could reach dispatch); `setActiveExample` is `[Config]` (config Snapshot + `LoadDocument` effect); editor+viewer boot `default_fem2d_snapshot()` (bundled demo, `[DEBUG]` counts); example id mismatch fixed (`"default"` vs shell's `"demo"`); 75 stale `resolve_ready(…)` test wrappers swept (fem3d likely still has them). Not compiled.
- 21:27 Coordinator check: `resolve_ready<F: Future>` (`🧰️framework/🔨️modules/🚪️io/🦀️.rs:891`) still wraps 55 call sites in 13 fem3d files (editor 19, add-nodal-load 10, add-node 7, remove-selection 4, set-active-example 4, mutations 3+2, io serializers 2, …) and 6 fem2d files; `vcs::apply_mutation` (`🌿️vcs/🦀️.rs:1192`) is sync → those wrappers are E0277 compile breaks. W8 (Opus) launched: per-call-site sweep in fem3d (excluding W4's `🚪️io/`) + audit of fem2d's 6 remaining files.
- 21:30 Wave 3 launched (3 × Opus): W9 fem storybook scope (block W5 pattern), W10 fem2d ≥3 fixture cases per kind (second real-world model + refusal case, taxonomy `members-of-tests` + crate-entry mounts), W11 same for fem3d. In flight now: W4, W5, W6, W7, W8, W9, W10, W11.
- 21:35 W4 landed (`📓️w4-io.md`, 24 Rust files): fem2d+fem3d io off the dead `ComposerEntry`/`io_registry` channel (deleted) onto `io_mechanism` — `pub fn io() -> IoDeclaration` with 12 entries each, subset roots `io: io::io()`; txt/json/csv/md Exact both ways (csv/md now real RFC 4180 / CommonMark envelopes around the DSL, old csv import silently returned `default()`), stl/obj export Lossy via `build_semio_mesh_snapshot`, stl/obj import typed `Err` (`geometry_import` module); TS twins declaration-only (vitest include glob cannot see `🚪️io/`); rustfmt-parsed, not compiled; 8 open risks in §6.
- 21:40 Coordinator closed W4 risks 1+3 in both artifact roots (`🗿️artifacts/◻️2d/🦀️.rs`, `🧊️3d/🦀️.rs`): `export/import_stdio_kinds` now list csv/json/md/obj/stl/txt (what `io()` registers), added the missing `composer.txt` capability row (`s.stdio.txt@utf-8/*`), rewrote the stale `io_registry` comment.
- 21:42 W7 landed (`📓️w7-kernel-benchmarks.md`, 3 kernel files): modal + buckling tolerances 10 % → 2 % (buckling K = 1.0/0.5/0.7/2.0), three order-of-magnitude tests replaced by same-mesh reference assertions at 1e-6, four new closed-form tests (SS-UDL beam, propped cantilever, portal frame, MacNeal-Harder distorted Quad4); every literal from `🔨️w7-kernel-references.py` computed twice (scikit-fem 12.0.2 + independent numpy/scipy assembly, agreeing ≤1e-11); `SUBSPACE_MAXIMUM_ORDER = 40` caps eigen meshes; Hex8 cantilever recast (L/h = 2, sparse `solve_multi_case`, 4.70 % at 702 DOFs). rustfmt-clean, not compiled.
- 21:45 W8 landed (`📓️w8-resolve-ready-sweep.md`, 14 files): 71 `resolve_ready` sites audited against callee definitions — 53 stale wrappers removed (48 in fem3d), 18 kept (genuinely async callees incl. W4's 4 `🚪️io/` `ArtifactSerializer::serialize` sites); two were production code (`apply_fem2d_mutation`/`apply_fem3d_mutation` wrapping sync `vcs::apply_mutation`), i.e. real `cargo check` blockers; zero `block_on`, zero `.await` on sync callees left. rustfmt-clean, not compiled.
- 21:48 W9 landed (`📓️w9-storybook.md`): fem storybook scope opted in (`[package.metadata.semio.storybook]` id `fem`), `.storybook/stories/fem/{dsl.ts,scene.ts}` + 6 story files × 3 stories (2d/3d × Model/Results/Viewer, en+de, example switch, dispatched `addNode`); fem2d windows are `Canvas2d` → `Canvas2dHost`, fem3d `World3d` → `World3dHost`, neither needs a WASM loader; verify with `STORYBOOK_SCOPE=fem bunx storybook build -c .storybook --output-dir <scratch>` (expect 24 index entries). Bugs flagged: fem3d example id `"demo"` vs handler `"default"`; fem2d `setActiveExample` resets `Fem2dConfig` wholesale (drops locale).
- 21:52 Coordinator fixed W9's two bugs: fem3d `set-active-example` handler + editor option/dispatch/tests now use `examples::demo::ID` (was literal `"default"`, so the shell's `"demo"` switch blanked the document); fem2d handler resets camera + result display as two granular rows instead of a whole-config `Snapshot` (locale preserved, new test `set_active_example_keeps_the_session_locale_2d`).
- 21:55 W11 landed (`📓️w11-fem3d-cases.md`): 50 new fem3d fixture bundles (25 kinds × 3 cases) on a new SI glulam workshop hall snapshot (17 nodes / 21 elements / 2 solids / 4 cases); edge cases pinned to the code's actual semantics (3 Fatal duplicate-id, 16 Error target-missing, 6 Warning no-op); mounted + catalogued + taxonomy-registered; resolver 443/443 mounts, 1314/1314 include_str; replay through the Python reference: 50 agree, 6 agree-noop, 14 refused, 5 DIVERGE. Findings: `add-load`/`replace-*` skip foreign-key resolution, `replace-*` can silently rename, no `delete-*` referrer check, `create-solid` has no geometric validation, no physical-plausibility bounds, `update-analysis-settings` unbounded, the 4 enum-payload `🧬️.schema.json` are wrong (PascalCase `kind`, no fields → 12 vectors fail their own schema incl. 3 pre-existing), snapshot schema `$defs` empty, `apply_fem3d_mutation`'s "rejected" assert branch is dead; new cases not yet in `.feature`/`🐍️.py` (duplicate scenario id + no handler). W12 (Opus) launched to harden fem3d mutation semantics, fix schemas, wire feature/py, resolve DIVERGE.
- 21:58 W5 landed (`📓️w5-fem2d-solver-oracles.md`): new `◻️2d/…/📈️analysis/🧪️tests/🧮️solves-fem2d-1-benchmarks/` (feature + anastruct/scipy `🐍️.py` + engine-side `🦀️.rs` + 9 fixtures); 35/35 oracle scenarios PASS through the repo's real Python host; all 22 undischarged `oracleRequirements` now name `anastruct-fem2d-solver`, `noOracleDecisions` narrowed to 3 residues (deformationScale, region continuum stresses → scikit-fem, record names). The oracle exposed a real kernel defect: `BeamEb2::recover` station moment `m1 + v1·x` → fixed to `-m1 + v1·x` (2D only). Unproven at runtime: `fem2d_solve_all` on the corner-pinned slab; `Singular` for a support-less cantilever.
- 22:00 W10 landed (`📓️w10-fem2d-cases.md`): 50 new fem2d case dirs (300 files) on a new two-storey braced steel frame (EN profiles, SI) + per-kind edge case as the code behaves (19 target-missing/duplicate-id, 2 no-ops, 4 dangling-reference refusals); 50 mounts, 50 catalog scenarios, 50 taxonomy rows; resolver 0 unresolved; `🔨️w10-replay-fem2d.py` 75/75 vectors consistent with the Python model, 150 snapshots pass third-party `jsonschema`. Findings mirror fem3d: `update-analysis-settings` never rejects, no delete referential guard, no replace reference validation, per-kind `🧬️.schema.json` rejects all 75 committed payloads (missing `mutation` tag + PascalCase enum consts), feature/py discovery explicit. W13 (Opus) launched as the fem2d mirror of W12.
- 22:20 W6 landed (`📓️w6-fem3d-solver-oracles.md`): three third-party oracles for fem3d (`pynite-fem3d-solver`, `scipy-fem3d-eigen`, `skfem-fem3d-solid`) across three new `📈️analysis/🧪️tests` cases; 25/25 kinds carry a `fem3d-1-mutate` oracle, `noOracleDecisions` empty; 35/35 oracle scenarios green via `uv run python` (27-node/42-member space frame, 4 closed forms, mutate-then-solve × 22 kinds, modal + 4 Euler columns, Tet4 solid), references committed as `📊️expected.results.json`. Kernel defect found: `Frame3::local_mass` y-bending-plane sign (−66.7 % on a one-element cantilever self-weight tip deflection). Rust adapters not compiled.
- 22:30 Coordinator applied W6's kernel finding in `🧱️elements3d/🦀️.rs`: `Frame3::local_mass` AND `local_geometric_stiffness` (same omission) now apply `S·M·S` (`Y_PLANE_SIGN = [1,-1,1,-1]`) to the `[2,4,8,10]` y-bending block, matching `local_stiffness`/`local_udl`'s `θy = −∂w/∂x` convention; regression test added.
- 22:40 Memory watch fired (swap 39/66.5 GB, load 25). First compile launched: `cargo check -p semio-s-plugin-fem --lib --tests -j 4 --keep-going` in a private `CARGO_TARGET_DIR` (scratchpad `fem-check/target-fem-check`, seeded by rsync from `target-s-e2e-wgpu/debug`, os-kernel rmeta 20:23; `RUSTC_WRAPPER=""` to bypass sccache; nohup + taskpolicy -B; log `fem-check/check-1.txt`, copied into `🗑️generated` at close). Note: peers changed `🔌️plugin` sources at 22:19, so plugin/stdio/fem all recompile. W12/W13 still editing — this run surfaces framework-level fallout from W2-W9; a second run follows their landing.
- 22:30 `bun nx run @semio-tech/fem-js:test` (vitest over `📚️examples/**/🧪️tests/🟦️.ts`) PASSED (exit 0; log `fem-ts/log.txt`). Storybook scope build (`STORYBOOK_SCOPE=fem bunx storybook build`) running in the same lane.
- 22:45 W12 landed (`📓️w12-fem3d-semantics.md`): 22 fem3d `🔺️diff` builders rewritten on a shared `🛡️Guards` region — `replace-*` resolve foreign keys and refuse renames (`mutation.id-mismatch`), every `delete-*` except `delete-node` (explicitly specified permissive) refuses live referrers (`mutation.target-referenced`), solid geometry / material-section plausibility / analysis bounds refuse with `mutation.invariant`; dead `"rejected"` branches now assert on `MutationOutcome::messages()`; 4 per-kind schemas + snapshot `$defs` + mutation facet schema fixed (12 → 0 schema failures over 96 vectors; 21 new refusal bundles, ≥3 per kind, mounted/catalogued/registered); Python references hardened, `reject-`/`hall-vector-` handlers wired into the 5 features; replay 50 agree / 7 no-op / 39 refused / 0 DIVERGE; 487+2123 mounts resolved; rustfmt-parsed, not compiled.
- 22:55 W13 landed (`📓️w13-fem2d-semantics.md`): 17/25 fem2d diff builders hardened onto a shared `mutations::guards` module (id-mismatch, foreign keys, six `delete-*` referrer refusals, region geometry, material/section plausibility, load targets, analysis bounds); 3 round-trip tests repaired + 10 guard-law tests; all 25 payload schemas fixed (`mutation` tag, camelCase variants), snapshot `$defs` with bounds, aggregate mutation schema = 25-variant union, 25 leaf descriptors' `outcomeClasses` corrected; 23 new refusal vectors (75 → 98), wired via `frame-vector-<kind>`/`reject-<kind>-<n>`; replay 0 divergences, 392 jsonschema validations pass, mounts 0 unresolved, rustfmt 248/248. All edit lanes are now landed; remaining work is compile → test → wasm → describe → boot.
