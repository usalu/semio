# Common rules for every worker (read first)
- Repo: /Users/ueli/Documents/semio — `cd` there explicitly in every Bash call. Read `/Users/ueli/Documents/semio/CLAUDE.md` and obey it (schema-first, multi-implementation taxonomy tree, test-driven with a language-agnostic test + third-party/second-implementation oracle per feature, CQRS event-sourcing, no CRUD/CRDT, no legacy/compat/deprecation, no migration scripts left behind, docstrings start with a unique emoji, no comments inside definitions, concise code, launch.json registration for anything runnable, `📜️script.ts` is the only script file kind, bun + nx, zero-touch cross-platform).
- NEVER run git commit/stash/checkout/worktree/reset or any state-modifying git command. NEVER delete a `🗑️generated` folder or anything you did not create. NEVER open/close/reopen tickets. NEVER spawn sub-agents.
- Other agents (this ticket's lanes and unrelated peers) edit this repo concurrently: re-read a file right before editing it, ignore unrelated churn, never stop because others are working nearby, never "fix" another lane's in-progress files — record the finding for its owner in `📓️status.md` instead.
- Builds/tests/servers run in the FOREGROUND of your own Bash calls with generous `timeout` (up to 600000 ms), re-invoked on timeout; long-running servers via `nohup … > <scratchpad>/x.txt 2>&1 & disown` and polled on a real HTTP port. Scratchpad: `/private/tmp/claude-501/-Users-ueli-Documents-semio/411af150-09ce-4f7f-a1b9-ac675c7c067d/scratchpad/`. Never log into the ticket folder while running.
- Rust: `RUSTC_WRAPPER= CARGO_TARGET_DIR=/Users/ueli/Documents/semio/target-energy-e2e CARGO_BUILD_JOBS=4 cargo <check|test> -p semio-s-plugin-energy --lib [--tests] --message-format=short`. Shared target dir; cargo serializes on its lock; never use another target dir. If cargo/rustc sit at ~0 % CPU, `taskpolicy -B -p <pid>` them. Attribute every error by crate; only energy-owned errors are yours.
- Never claim a command passed or a feature works without pasting present-tense output (or browser console evidence) in your report. A failing comparison is a finding, not something to hide.
- Session kills happen (Opus usage limit). Work so that a kill loses little: land one complete unit at a time, and append a `<LANE>:` progress line to `📓️status.md` (ticket folder) at least every 30 minutes of work and at every milestone. Your report file in the ticket folder is the deliverable; append to it as you go.
- Ticket folder `T` = `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️06/ENERGY-PLUGIN-END-TO-END/`. Start by reading `T/📓️status.md` (all of it), your own lane's prior report if it exists (a predecessor was killed mid-work; the disk state is the truth — verify it, do not trust the predecessor's claims), and the files your lane brief names.
- When completely done, reply with a 5-line summary.
