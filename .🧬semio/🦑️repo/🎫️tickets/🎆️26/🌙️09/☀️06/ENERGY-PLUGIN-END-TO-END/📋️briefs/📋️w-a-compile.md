# Lane W-A — crate `semio-s-plugin-energy` compile green (native, wasm, tests)
Read: `T/📓️w1-compile.md` (predecessor progress: `semio_framework::` imports fixed, async→sync `ArtifactEditor`/`ArtifactViewer` conversion was IN PROGRESS when killed), `T/📓️explore-sibling-recipes.md` §1.1–1.3 + §7, `T/📓️explore-editor-ui-and-boot.md` §1 + §5, `T/📓️explore-build-gates-and-history.md`.
1. Native check with the common command (`--lib --tests`), log to scratchpad `w1-check-N.txt`. Fix every energy-owned error to the CURRENT framework API (puzzle `✏️s/🔌️plugins/🧩️puzzle` and remodel are compiling oracles for shapes: `render -> UiAssemblyResult<ComponentTree>`, sync trait fns, `DslValue`, `DESCRIPTORS`). The editor report's repo-wide `EditorBuilder`/`ViewerBuilder` sync-wrapper gap: check whether puzzle now compiles (a peer may have fixed the framework); if the framework really lacks the wrappers, add them in the framework crate in the sibling-consistent way and say so in the report. No shims/compat layers.
2. Other lanes are adding code concurrently (mutation kinds under `✳️any/🧬️schema/🧬️mutations/**` — generator-emitted, owner W-D0/G1–G4; examples `📚️examples/🏛️bestest-*` and `🧪️tests/🏛️simulate-bestest-energyplus` — owner W-C; editor/viewer dispatch — owner W-E; `🚪️io` epJSON — owner W-F). Errors in those files: record in `📓️status.md` as `W-A → <owner>: file:line error` and continue; if an owner is clearly absent for >2 hours (no status lines) fix the error yourself minimally.
3. Wasm: read `pluginCargoArgs`/`PLUGIN_WASM_TARGET` in `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/📜️script.ts`; run the equivalent of `CARGO_PROFILE_WASM_DEV_DEBUG=false SEMIO_BUILD_BUDGET_MS=3600000 … cargo build -p semio-s-plugin-energy --target wasm32-wasip2 --profile wasm-dev --features component-guest`; fix wasm-only errors.
4. `cargo test -p semio-s-plugin-energy --lib` (filters from `runCargoTestBudgeted` in `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🟦️.ts` if slow); fix energy-owned failures; report counts.
5. Keep re-running the native check every time another lane logs a milestone, so the crate stays green; log `W-A: native check green @ <time>` / `W-A: wasm green` / `W-A: tests N pass M fail` in `📓️status.md`.
Report: `T/📓️w1-compile.md` (append).
