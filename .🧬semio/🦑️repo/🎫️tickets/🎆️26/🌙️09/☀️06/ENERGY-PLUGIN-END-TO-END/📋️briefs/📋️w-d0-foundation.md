# Lane W-D0 — mutation foundation custodian + generator owner
Read: all `W-D0:` and `G*:` lines in `T/📓️status.md`, `T/📓️mutation-kind-template.md`, `T/📓️mutation-tag-ledger.md`, `T/🐍️generate-mutation-leaves.py` (yours; G3 was extending its pattern vocabulary when killed — inspect for half-finished edits), `T/📓️explore-mutation-vocabulary.md`, `T/📓️explore-mutation-scaffolding.md`, `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-DIRECT-LEAF-OVERHAUL/📓️taxonomy.md` + `📓️derivation-rules.md`.
Binding decisions already taken: per-field granularity (238-kind catalog); `replace-model` removed (whole-model load = `ArtifactStore::reset`); schedules, run_period, weather_link persisted in `Model`; caller-resolved `EntityId` + one editor-layer `next_entity_id(model)` helper; `dsl::DslEnum` aggregate; groups own ledger ranges and only append rows in their own generator region; nobody hand-edits aggregate files.
1. Verify on disk what the predecessor finished: model fields, snapshot/diff link slots ("switch the snapshot link codecs off serde" was in progress — finish it: no serde round-trips, `ToValue`/`FromValue` + `pack::json` only), the 17 generated kinds, the Python second implementation, `🔮️oracle/🔣️.json`. Run the generator (`python3 T/🐍️generate-mutation-leaves.py` from the repo root) and make it idempotent (running twice changes nothing) and robust to four groups appending rows concurrently (rows sorted by ledger number, regions per group, output deterministic).
2. Finish the model-root group (ledger 1–99) completely: every kind with happy + refusal fixtures materialized (`SEMIO_ENERGY_WRITE_FIXTURES=1` pass), law tests, Python oracle, `🥒️.feature` rows; `next_entity_id` helper + test; `ArtifactStore::reset` wiring for open/import/example-load (coordinate with W-E, who owns the editor commands — provide the function, they call it).
3. Be the custodian: when a group logs a generator pattern extension or a problem, review it for generality, keep the generator coherent, re-run it, and keep `📓️mutation-kind-template.md`/ledger in sync. Add the generator-level validations that the scaffolding report says nobody enforces (unique ledger numbers, FE0F emoji, NFC names, `mutationDirectoryPattern`), failing loudly.
4. Taxonomy gates (`verify-taxonomy-report`, `verify-taxonomy-enforce`, `mutation-outcome-law` — exact invocations in `T/📓️explore-mutation-scaffolding.md` §8 / `T/📓️explore-build-gates-and-history.md`) and the crate tests for all mutation kinds once W-A logs native-green.
Report: `T/📓️w4-mutation-foundation.md` (create/append).
