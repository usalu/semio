# Lanes G1–G4 — mutation groups (generator-driven)
Read: all `W-D0:` and `G*:` lines in `T/📓️status.md`, `T/📓️mutation-tag-ledger.md` (ALL: numbers, emoji, insertion rules), `T/📓️mutation-kind-template.md` (ALL), `T/🐍️generate-mutation-leaves.py` (ALL: the single spec table emitting leaves, fixtures, aggregate surfaces, crate mounts, oracle registration, feature tables, Python second implementation), `T/📓️explore-mutation-vocabulary.md` §2 + your group sections + §5, `T/📓️explore-mutation-scaffolding.md` §6–§9, `T/📓️bestest-contract.md` amendments, `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-DIRECT-LEAF-OVERHAUL/📓️taxonomy.md` + `📓️derivation-rules.md`.
Groups: G1 = Zones & spaces (100–199) + Geometry & envelope (200–299) incl. `glazing_construction_id: Option<EntityId>` on `Fenestration` with assign/clear kinds. G2 = Constructions & materials (300–399) + Internal gains (400–499). G3 = HVAC zone-level (500–599) + HVAC loop-level & plant (600–699) (dangling references per vocabulary §5.4: backing collection or refusal, documented). G4 = Electrical/renewables/other (700–799) + Grouping (800–899) + Schedules (900–999).
Method (binding, all groups concurrently): (1) add kinds ONLY as rows inside your own `#region G<n>`/`#endregion` block of the generator spec table (create it if absent; never touch other rows). Where the diff/inverse shape is not expressible with existing patterns, extend the pattern vocabulary generally (a reusable pattern function, documented at the top of your report and logged for W-D0, the generator custodian). (2) Re-run `python3 T/🐍️generate-mutation-leaves.py` from the repo root; it regenerates aggregates from the FULL table — re-read the generator immediately before editing, re-run after your final append; never hand-edit aggregate files, the crate mount region, or `🔮️oracle/🔣️.json` outside your rows. (3) Per kind: ≥1 happy + ≥1 refusal/no-op fixture materialized via `SEMIO_ENERGY_WRITE_FIXTURES=1`; law tests pass; Python second implementation implements the kind (OBSERVABLE, never a stub); `🧬️.schema.json` validated with a third-party JSON Schema validator against the fixture payloads. (4) Verify with the crate tests filtered to your kinds (record errors outside your kinds for their owner in `📓️status.md` and continue), the Python oracle run, and the taxonomy gates (`verify-taxonomy-report`, `verify-taxonomy-enforce`, `mutation-outcome-law`). Log `G<n>:` after every ~10 kinds.
Reports: `T/📓️w7-g1-zones-envelope.md`, `T/📓️w7-g2-constructions-gains.md`, `T/📓️w7-g3-hvac.md`, `T/📓️w7-g4-electrical-grouping-schedules.md` (create/append).
