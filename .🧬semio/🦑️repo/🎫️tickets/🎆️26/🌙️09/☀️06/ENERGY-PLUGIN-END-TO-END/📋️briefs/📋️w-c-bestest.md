# Lane W-C — BESTEST models as examples/fixtures + engine comparison harness
Read: `T/📓️w3-bestest-engine.md` (predecessor; on disk already: `✳️any/📚️examples/🏛️bestest-{600,600FF,610,620,630,640,650,900,900FF,910,920,930,940,950}` and `✳️any/🧫️fixtures/🏛️bestest-*` with W-B's `🔮️energyplus.json` for 10 cases — verify what is real), `T/📓️bestest-contract.md` incl. amendments (u_value semantics 3.0/0.787, cooling tolerance 10 %), `T/📓️w2-oracle-toolchain.md` (oracle runner CLI, traps), `T/📓️explore-engine-api-and-validation.md`, `T/📓️explore-oracle-toolchain.md` §4, `T/📓️explore-test-oracle-infra.md`.
1. Each case builder (`📚️examples/🏛️bestest-<case>/🦀️.rs` + `🟦️.ts`) mounted from the crate entry; `🧫️fixtures/🏛️bestest-<case>/🔋️model.json` generated by a test and asserted byte-identical to the builder output; `Model` now has `run_period`, `schedules: ScheduleSet`, `weather_link` (W-D0) — use them (schedules for setpoints/gains/night setback/night ventilation).
2. `Model::validate` reachable in production and called before `Engine::run`; annual run from `🧫️fixtures/🌦️denver-tmy/🌦️.epw` via the stdio epw codec (hourly zone timestep, ≥3 warmup days, Jan 1–Dec 31).
3. Harness `✳️any/🧪️tests/🏛️simulate-bestest-energyplus/` (`🥒️.feature`, `🦀️.rs`, `🐍️.py` adapter per the infra report): run engine → contract results JSON → compare with `🔮️energyplus.json` per contract tolerances, one diagnostic per metric, `skipped` when an oracle file is missing. Unit tests: builders validate; deterministic annual run; FF cases zero HVAC energy; controlled cases within setpoints ±0.5 K ≥99 % of hours.
4. For cases without a reference (630/930 fins, 650/950 night ventilation): implement the engine physics if bounded (fins shading, scheduled ventilation), regenerate the reference with W-B's runner (`nx run @semio-tech/energy-oracle-py:oracle-run <model.json> <epw> <out.json>` — see W-B's report for the exact target names) and commit `🔮️energyplus.json`; otherwise document the gap and leave the fixture without an oracle.
5. `🎬️demo` example = a real building (BESTEST 600) with regenerated `🖼️assets/🗣️.dsl.semio` if required.
6. Run everything once W-A logs native-green; tabulate semio vs EnergyPlus vs ASHRAE 140 envelope per case with before/after numbers for every engine fix.
Report: `T/📓️w3-bestest-engine.md` (append).
