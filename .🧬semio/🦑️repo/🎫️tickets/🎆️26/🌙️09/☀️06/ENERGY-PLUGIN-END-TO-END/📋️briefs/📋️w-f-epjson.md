# Lane W-F — epJSON import/export + direct-EnergyPlus second oracle route
Read: `T/📓️w6-epjson-io.md` if present (predecessor started `🚪️io/📥️import/🧩️deserializers/🗿️artifacts/⚡️epjson/🔖️25.2/✳️any/` and was writing the export codec when killed — verify on disk), `T/📓️bestest-contract.md` incl. amendments, `T/📓️w2-oracle-toolchain.md` (cached OpenStudio/EnergyPlus 25.2.0 paths, runner CLI, traps: `Sizing:Zone` needed, no ReadVarsESO), `T/📓️explore-engine-api-and-validation.md` §1–2, `T/📓️explore-test-oracle-infra.md`, `T/📓️explore-sibling-recipes.md` §3 (io honesty) + §7, `T/📓️explore-mutation-vocabulary.md` §1.
1. Follow the existing `🚪️io` declaration convention (`🚪️io/🦀️.rs`, `📥️import/🧩️deserializers/…`, `📤️export/…`, stdio `🌦️epw` and lowpoly real codecs as precedents; `AppIo::with_ports`).
2. Rust epJSON EXPORT of `Model` (no external runtime deps): Version, SimulationControl, Building, Site:Location, RunPeriod (from `Model.run_period`), Timestep, Material, Construction, WindowMaterial:SimpleGlazingSystem (contract semantics), Zone, BuildingSurface:Detailed, FenestrationSurface:Detailed (apertures from `area_m2` centred on the host), ZoneInfiltration:DesignFlowRate, People/Lights/ElectricEquipment/OtherEquipment (60/40 radiant split), Schedule:Compact/Constant from `Model.schedules`, ThermostatSetpoint:DualSetpoint + ZoneControl:Thermostat, ZoneHVAC:IdealLoadsAirSystem with equipment list/connections + Sizing:Zone, Output:Variable (contract's four), Output:Meter, Output:SQLite. Validate every export against the bundled `Energy+.schema.epJSON` with a third-party validator (`jsonschema` via `uv run --group test` in the oracle package). IMPORT of the same object subset into `Model` (round trip export→import→export byte-identical; import a converted E+ example file, e.g. `1ZoneUncontrolled` via `energyplus --convert-only`, asserting counts). Unknown objects → structured diagnostics, never dropped silently.
3. Second oracle route: `✳️any/🧪️tests/🏛️export-epjson-runs-in-energyplus/` (`🥒️.feature`, `🦀️.rs`, `🐍️.py`): for each `🧫️fixtures/🏛️bestest-<case>/🔋️model.json` export epJSON, run cached EnergyPlus directly (`energyplus -a -w <denver epw> -d <out> <file.epJSON>`, skipped with a reason if `oracle-status` reports no toolchain), read SQLite → contract results JSON, compare against `🔮️energyplus.json` within 3 % annual. Register in the oracle package's `📜️script.ts`/`📋️project.json`/launch.json beside W-B's entries.
4. Editor `Export epJSON…`/`Import epJSON…` commands added additively (W-E owns the editor; log your command ids under `W-F:`).
5. Verify with the crate check/tests and the Python oracle; run the direct E+ route for at least case 600 and paste outputs.
Report: `T/📓️w6-epjson-io.md` (create/append).
