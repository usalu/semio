# Lane W-E — editor dispatch, windows, viewer, examples, describe, live UI verification
Read: `T/📓️w5-editor-dispatch-ui.md` (predecessor landed the dispatch apparatus: 18 retained tool ids, `EnergyModelCommandJobFactory`; was implementing the example table + `setActiveExample` verb when killed — verify on disk), `T/📓️explore-editor-ui-and-boot.md`, `T/📓️explore-sibling-recipes.md` §1.4–1.9, §5, §6, §7, `T/📓️explore-build-gates-and-history.md` (gate checklist), `T/📓️mutation-tag-ledger.md` (kind names for commands).
Ownership: W-A owns compile errors; W-D0/G1–G4 own `🧬️mutations/**`; W-C owns example CONTENT; W-F owns `🚪️io` and adds import/export commands additively. You own `✳️any/✏️editor/**` (except W-F's additive commands), `✳️any/👁️viewer/**`, example REGISTRATION, descriptor regeneration, live verification.
1. Dispatch: every action classified `Migrated`, bounded proofs with a real `factory_type`, non-empty `PUBLICATION_CONTRACTS`, lane-matching store-preparation factories; end-to-end dispatch test with `testkit::new_app_with_registry` proving each action dispatches without `interactive-job.*` faults; simulation actions through the sibling-consistent path.
2. Editor commands → semantic mutations for structure (`🌳️structure`, tree kit) and zones (`📊️zones`) windows: create/rename/delete zone, create/delete surface, assign construction, material scalars, thermostat setpoints, site, schedules — using ledger kind names (a kind not yet implemented: wire against the ledger name and log it for the group). Typed `DslValue` payloads, i18n labels English + German with no default, keybindings per puzzle/remodel. Whole-model load via `ArtifactStore::reset` (W-D0 provides the function).
3. Simulation window: run with progress (stage, step budget), cancellation, results (annual heating/cooling meters, peaks, zone temperature series); editable config projection (run period, timesteps, warmup); weather via `weather_link`.
4. Viewer: read-only rendering of the same windows.
5. Examples registered table-driven on both manifest builders: `🎬️demo` + every `📚️examples/🏛️bestest-*` present.
6. After W-A logs native+wasm green: `bun ./📜️script.ts describe` in `✏️s/🔌️plugins/🔋️energy/📦️packages/🦀️rust`, then registry `generate` + `check` (`🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📇️registry/📜️script.ts`); fix every energy finding.
7. Live: react boot (`CARGO_TARGET_DIR=/Users/ueli/Documents/semio/target-energy-e2e SEMIO_RENDERER=react SEMIO_BUILD_BUDGET_MS=3600000 bun ./📜️script.ts dev energy` from the dev package; first boot un-narrowed; poll the port), then browser tools (`mcp__Claude_Browser__navigate`, `read_console_messages`, `read_page`, `computer`) to open the editor, load an example, dispatch each command, run a simulation to completion; no `turn failed`/`render failed`/`interactive-job.*`/`[object Object]` in the console. Then `verify catalog` and the wgpu boot (`?plugin=energy`). Screenshots/console excerpts into the report (PNGs into `T/🗑️generated/` at the end only).
Report: `T/📓️w5-editor-dispatch-ui.md` (append).
