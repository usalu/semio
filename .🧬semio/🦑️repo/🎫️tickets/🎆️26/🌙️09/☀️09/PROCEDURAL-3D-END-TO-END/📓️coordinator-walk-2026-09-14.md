# 🚶 Coordinator user-perspective walk — React editor, 2026-09-14 (session 5)

Stage: fresh direct vite serve on :6021 (`📜️serve-generation3d-react-6021.sh`), guest wasm staged 15:31, host TS as of 17:05 (tree `bb961413d4` + peers' live edits). In-app browser pane, viewport 1600×1000.

## Findings (in order seen)

| # | Window / surface | What a user sees | Verdict | Owner |
|---|---|---|---|---|
| 1 | Boot | Editor role boots in ~15 s: Flow (tree + node graph + `Fit graph`), Preview (canvas with grid), Tool runs panel (`NO DATA`), status pill `Running · Evaluating nodes (1/2)` | boots | — |
| 2 | Preview evaluation | Pill stays `Running · Evaluating nodes (1/2)` for > 90 s, no mesh ever appears. Console: every extension dispatch (`flow-extension-math evaluate`, `flow-extension-brep evaluate/tessellate`, req 1…10) ends in `typed-operation completion effects failed … SemioFaultError: <instance>:framework.panel.toolRun: DuplicateSiblingKey at retainedUiRefreshEffects (🛠️ShellHelpers/🟦️.tsx)` + `[DEBUG] invokeExtension dispatch failed`. The Tool runs panel landed in `f2fc008ee2` (15:57), AFTER the last green React battery (15:18). | **BLOCKER** — no example converges | lane react-current-tree-battery |
| 3 | Generate mode | Generations window (`[no generations]`, ACTIONS › `Add Generation`), Form (`Add a generation to edit input values.`), Preview | renders | — |
| 4 | Viewer role | Full-width Preview, Window Options, Utilities; no mesh (item 2) | renders | — |
| 5 | Example picker (navbar select) | Popover opens with 10 rows but the labels are unreadable: label colour `oklab(0.47 …)` (dark grey) on popover bg `color(srgb 0.69 0.70 0.67 / 0.4)` over the dark shell — `text-popover-foreground` resolves to a light-appearance foreground while the shell is dark. Rows ARE in the DOM (`Sphere Cut With Torus`, `Sphere Box Fuse`, …) and clicking switches the example (navbar label updates). | **DEFECT** (readability) | lane popover-contrast |
| 6 | Example switch | `Sphere Cut With Torus` selected → new instance `2:` runs, same DuplicateSiblingKey fault (item 2) | blocked by 2 | — |
| 7 | Artifact panel (navbar left) | ARTIFACT / OPEN WITH… — VIEWER (`Procedural`, `Set as default`) / OPEN WITH… — EDITOR | renders | — |
| 8 | Settings panel (bottom bar) | DRIVER (Reset panels, Merge policy Normal, Terminology Native, Language English, Driver Default, Layout Desktop, Appearance System), GENERAL (plugin, controller, app id, name), APP; tabs General/Theme/Hotkeys/Default | renders | — |
| 9 | Command menu (bottom bar) | Set Driver…, Reset panels, Set Layout…, Fullscreen; strip Layout/App/Appearance/Language | renders | — |
| 10 | Re-check 17:36 (lanes mid-edit) | Hexagonal column mesh now renders in the Preview (the fault is now `retained surface render fault, scoped to that surface: 1:framework.panel.toolRun: DuplicateSiblingKey` — no longer aborting other completions). But: status pill still `Running · Evaluating nodes (1/2)` while every tree node reads `Evaluated`; the Flow node-graph canvas is a narrow empty beige strip (no node boxes) — at 17:05 it painted nodes. | 2 new observations, re-check after lanes land | react-current-tree-battery / react-hop-latency |
| 11 | Re-check 18:02 | Node graph paints nodes + wires again; hex column mesh renders (`data-meshes-json` 3 644 chars, preview `data-status-json` phase `idle`, nodesDone 7/7, ratio 1.0). Click on the column selects: tree row `S` (ExtrudeCurve output) highlighted, `ExtrudeC…` node cyan, INSPECTION panel opens (`Schema: flow.fixture`, `Widgets: 7`). Hover shows no visible highlight in the screenshot and `data-interaction-json` stays `{}` after hover+click. The `Running · Evaluating nodes (1/2)` pill is the Tool runs panel's run pill (the `previewEval` run never finalizes in that panel because its surface faults with DuplicateSiblingKey, 35 faults per boot), not the preview status. | selection works; hover evidence + pill open | react-oracle-hardening (hover), react-current-tree-battery (pill/panel) |
| 12 | Re-check 18:28 (after popover-contrast) | Node-graph canvas now dark; example picker rows readable. Tool runs pill now reads `Finalized · Evaluating nodes (1/2)` with body `7 nodes evaluated, 2 meshes tessellated` — the run finalizes, but the phase label is frozen at the first phase and the mesh count disagrees with the preview (3 meshes for the hex column per oracle). 27 `DuplicateSiblingKey` surface faults per boot remain. | pill label + count stale; fault open | react-current-tree-battery |
| 13 | 19:03 after the 19:00 restage | Editor boots into a fallback `Number` → `Math.add` graph, empty tree, preview `phase: faulted` (`flow.extension-not-contributed`, "Geometry extension unavailable" / "Geometrie-Erweiterung nicht verfügbar"), `data-meshes-json` empty. Console: `setContributions command failed procedural [DEBUG] command ingress exceeds 64 pages` — pack grew to 272 089 chars. | **BLOCKER** (ceiling) | contributions-ingress-ceiling |
| 14 | 20:03 on the 19:47/20:00 guest | Contributions install; hex node graph paints with minimap; world rail shows `Frame visible` and `Computing 5/7 (71%) Cancel` (chrome safe area works); pill `Running · Evaluating nodes (1/2)`; evaluation never passes 5/7 — console `invokeExtension dispatch failed … targeted window transient capture requires an exact ViewModel roster` for math/brep evaluate. Left tree pane of the Flow window shows no rows in this layout. | **BLOCKER** (regression) | flow-inline-continuation |
| 15 | 23:01 on the 20:14 guest (after the outage) | Contributions install, hex example converges: preview `phase: idle`, `nodesDone 7/7`, `data-meshes-json` 3 689 chars; no `exceeds`/`rejected`/roster fault lines in the console. | converges again | — |
| 16 | 23:06 on :6027 (20:14 guest), `?example=sphere-cut-with-torus` | Boots straight into the example: node graph (Torus Major Radius → sphere/torus → bool.cut → measure.volume), preview `idle 6/6`, `data-meshes-json` 88 245 chars, sphere with the torus cut rendered, `Frame visible` rail visible; Tool runs `Finalized · Evaluating nodes (1/2)` / `6 nodes evaluated, 1 meshes tessellated` (phase label still frozen at the first phase). Load average 126 with 10 lanes. | converges; pill label stale | react-current-tree-battery |
| 17 | 23:15 on :6027, sphere-cut-with-torus | A plain click on the sphere in the edit Preview leaves `data-selection-json.selectedIds = []` (no selection, no hover target), while the same gesture selected the extrude output on the hex example at 18:02. | select red on this example | selection-prune-interact |
| 18 | 23:25 on :6027 | Console: endless `toolRunPace` action storm on a quiet, converged shell (instance 2, seq 107…116 in seconds, 2 800 lines dropped). After clicking `Viewer` the navbar stays `Editor`, the Flow window flips to the fallback `Number → Math.add` graph on a LIGHT canvas, instance 2 appears; the sphere preview stays. | **DEFECT** pace storm; role switch suspect | tool-run-pace-storm; re-check role switch on :6028 |
| 19 | 23:32 on :6028 (fresh boot, sphere example converged) | Click on the navbar `Viewer` control does nothing: navbar stays `Editor`, edit layout stays, no viewer preview. Worked at 17:10 today. | **REGRESSION** role switch | role-switch-regression |
| 20 | 00:45 on :6027, `face-sweep-extrude` (idle 7/7, 1 104 mesh chars) and `box-fillet-preview` (idle 5/5, 19 153) | Both converge, but the boot camera is the seed pose: the fillet box is cut off at the top-right edge of the Preview, the swept face is barely visible; only `Frame visible` frames them. | **GAP** boot framing | boot-camera-framing |
| 21 | 01:08 on a freshly recycled :6027, box-fillet converged | `Frame visible` click leaves `data-viewport-camera-json` at the seed pose; the box stays cut off. (chrome-panel-safe-area measured `fit` 8/8 on :6022 at ~23:50.) | fit inert here | boot-camera-framing (told) |
