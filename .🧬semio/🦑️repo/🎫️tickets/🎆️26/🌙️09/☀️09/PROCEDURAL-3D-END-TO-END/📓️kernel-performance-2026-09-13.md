# 🏎️ Kernel Performance — evaluate → tessellate → mesh-encode, 2026-09-13

Lane: **kernel-performance** (Opus execution lane).
Ticket: `26/09/09/PROCEDURAL-3D-END-TO-END`.
Scope owned here: the brep kernel, the evaluate job, the tessellator and the mesh encoding. The
host/UI dispatch code belongs to other lanes and was not touched.

*(draft — numbers filled in as the runs complete; see the sections below)*
