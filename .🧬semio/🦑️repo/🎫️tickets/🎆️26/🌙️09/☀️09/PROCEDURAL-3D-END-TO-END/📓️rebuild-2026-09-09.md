# Rebuild, Restage, Describe — 2026-09-09/10

**Result: served build fresh — YES.**

```
staged sha256 = 5aa6c8a1400c87ba1da47f8aefa7354b791c8f1cd998bb8cede38b8e41ebe189
served sha256 = 5aa6c8a1400c87ba1da47f8aefa7354b791c8f1cd998bb8cede38b8e41ebe189
descriptor    = b4841058df319e6b341edd652750b317c27fc05f000463469026802f8f6c6c5d  (staged == served)
```

Serve pid **87849** (vite), wrapper **87832**, `http://127.0.0.1:6018/?plugin=generation3d` → 200.

Logs copied to `🗑️generated/rebuild-*.txt`. Inputs kept in the scratchpad
(`$S/rebuild.sh`, `$S/rebuild-loop.sh`, `$S/one.sh`, `$S/describe2.sh`, `$S/serve.sh`).

---

## 0. Precondition correction

The boot lane's vite (pid 91694, started 19:35 per `📓️wave1-boot-report-2026-09-09.md` §6) was
**already dead** when this lane started at 22:00 — `lsof -nP -iTCP:6018 -sTCP:LISTEN` returned
nothing and none of pids 66860/66861/66862/91682/91694 existed. The machine had no server on 6018
for the whole rebuild. So step 3's "restart only if needed" became "restart, unconditionally".

Shared cargo target dir was free at start: the only live cargo (`semio-s-plugin-puzzle`,
pid 95830) used a **private** `…/9e1e818a-…/scratchpad/target-p3d`, not
`/Users/ueli/Documents/semio/target/wasm32-wasip2`. No lock holder on the shared lock.

## 1. Commands and timings

Env for every build call: `CARGO_PROFILE_WASM_DEV_DEBUG=false RUSTC_WRAPPER=""
SEMIO_BUILD_BUDGET_MS=14400000 SEMIO_CMD_BUDGET_MS=14400000 NX_DAEMON=false SEMIO_RENDERER=react`,
cwd `/Users/ueli/Documents/semio`, shared default target dir.

| # | start | dur | command | outcome |
|---|---|---|---|---|
| 1 | 22:00:37 | 12m47s | `nx run @semio-tech/framework-os-dev:activate-generation3d-react-dev` | **fail** — `flow-plugin:component-dev` (2×E0599) |
| 2 | 22:15:15 | 1m21s | `nx run @semio-tech/flow-plugin:component-dev --output-style=static --verbose` | **fail** — diagnosis run, see F1 |
| 3 | 22:26:34 | 5m23s | activate (after F1) | **fail** — flow-plugin green; 4 crates failed on transient peer churn |
| 4 | 22:33:46 | 1m48s | `nx run @semio-tech/flow-extension-brep-rust:component-dev` | **pass** — proved #3 was a mid-edit window |
| 5 | 22:36:33 | 4m56s | activate | **fail** — `procedural-plugin:component-dev` (1×E0599, peer-owned, self-healed) |
| 6 | 22:59:56 | 1m38s | `nx run @semio-tech/procedural-plugin:component-dev` | **pass** |
| 7 | 23:03:24 | 7m03s | activate | **fail** — `semio-framework-os-flow-core:wasm`: 10 errors in `semio-framework-plugin` (peer unqualify sweep, self-healed at 23:04:56) |
| 8 | 23:12:12 | **2m57s** | activate (auto-retry loop, attempt 1) | **PASS** — `Activated generation3d react dev: 11 completed components (changed)` at **23:15:09** |
| 9 | 23:21:32 | 5m23s | `nx run @semio-tech/os-plugin-describe-rs:describe -- procedural` | **fail** — wrong target, see §3 |
| 10 | 23:27:38 | 3m58s | `nx run @semio-tech/procedural-plugin:describe` | **fail** — TS size bound, F3 |
| 11 | 23:34:19 | 5m (×2) | same, after F3 | **fail** — Rust size bound, F4 |
| 12 | 23:43:56 | 3m47s | same, after F4 | **PASS** — `described procedural (plugin semio:procedural@0.1.0)` at **23:47:43** |
| 13 | 23:51:14 | 11s | `nx run @semio-tech/plugin-registry:check` | fail — catalog stale (expected after describe) |
| 14 | 23:53 | 25s | `nx run @semio-tech/plugin-registry:generate` | **pass** |
| 15 | 23:54 | 3m07s | `nx run @semio-tech/plugin-registry:check` | fail — **pre-existing repo-wide** taxonomy, see §5 |
| 16 | 23:58:55 | 0.7s | `bun ./📜️script.ts serve generation3d react dev` (cwd os-dev ts pkg) | **PASS** — `VITE v7.3.6 ready in 726 ms`, `http://127.0.0.1:6018/` |

Attempt 8 used an 8×-retry wrapper (`$S/rebuild-loop.sh`, 90 s between attempts) because attempts
3/5/7 each died inside a different peer's mid-edit window and each was already healed by the time
the failure was read. Riding the windows automatically beat re-diagnosing them one at a time.

## 2. Errors fixed (file:line)

### F1 — `ArtifactView::with_children` deleted out from under its only two consumers
Errors (attempt 1, `flow-plugin:component-dev`):

```
error[E0599]: no associated function or constant named `with_children` found for struct `ArtifactView<'a, P>`
  --> ✏️s/🔌️plugins/🌊️flow/…/✳️any/✏️editor/🦀️.rs:1446:72
  --> ✏️s/🔌️plugins/🌊️flow/…/✳️any/✏️editor/🦀️.rs:1626:76
```

The selection-overflow lane (its `📓️selection-overflow-2026-09-09.md` is named verbatim in the same
diff's `framework_reserved_route_job` docstring) rewrote the constructor block of
`🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🦀️.rs` at 22:12:59, replacing
`pub async fn with_children`'s docstring **and body** with an expanded docstring for the private
`with_render_context`. `git show HEAD:` confirms `pub async fn with_children` existed at
`ebbace9b32` (17:57); the working tree no longer had it. Attribution: their file, mtime 22:12:59.
The two broken call sites are in the flow editor, untouched since **18:08** (4 h stale, nobody
mid-editing), and `✏️s/🔌️plugins/📖️playbook/🗿️artifacts/📖️playbook/🦀️.rs:201-203` still documents
`ArtifactView::with_children`/`ChildContentView` as a real seam — so the removal was collateral,
not a deliberate API retirement.

Waited 12 min (22:13 → 22:25) for their file to settle, re-checked, then restored the seam as a
**synchronous** constructor (the `async` had no awaits — it only moved owned fields), inserted
above the peer's new `with_render_context` docstring so their edit is untouched:

- `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🦀️.rs:7272-7277` — new
  `pub fn with_children(snapshot, history, children) -> Self`.
- `✏️s/🔌️plugins/🌊️flow/🗿️artifacts/🌊️flow/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🦀️.rs:1446` and
  `:1626` — dropped the now-pointless `semio_framework_plugin::resolve_ready(…)` wrapper.

Their invariant is preserved: their docstring scopes `render_operation` totality to **host** entry
points, and these two sites are app-side retained *command* work reading
`ArtifactOwnedToolJobContext::children`, never a host render pass.

### F2 — three peer breakages observed, waited out, **not** edited (per the concurrency rule)

| attempt | failing task | error | resolution |
|---|---|---|---|
| 3 (22:32) | `flow-extension-{brep,bim,dictionary}-rust:component-dev` | transient | attempt 4 (22:33) ran brep alone: green, no edit needed |
| 5 (22:41) | `procedural-plugin:component-dev` | `E0599: no method named command_id found for reference &Generation2dViewCommand` at `…🌀️generation2d/…/👁️viewer/🦀️.rs:55:17` (trait `ArtifactViewer::command_id` is an *associated fn*, not a method) | the unit-suite lane fixed its own file at **22:43:59**; attempt 6 (22:59) green |
| 7 (23:10) | `semio-framework-os-flow-core:wasm` | 10× `E0425/E0422/E0433`: `Edit`, `MutationMeta`, `SnapshotRetirementStep` unqualified at `🔌️plugin/🦀️.rs:13212-13344` | classic peer unqualify sweep; qualifiers (`protocol::Edit`, `protocol::MutationMeta`, `store::SnapshotRetirementStep`) were already back at **23:04:56** — the build had simply read the file mid-window |

Between 22:45 and 23:00 the procedural tree never went quiet for 4 min (12 polls, longest idle
165 s), so waiting for a settle window was abandoned in favour of "run the failing target alone and
see" (attempts 4, 6), then the retry loop.

### F3 — descriptor emitter's TS admission bound too small for the dev component
```
describe semio-s-plugin-procedural failed: raw component …/target/wasm32-wasip2/wasm-dev/semio_s_plugin_procedural.wasm
  must be a regular non-symlink file of 1..67108864 bytes
```
`🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🖨️describe/📦️packages/🦀️rust/📜️script.ts:37`
`FRESH_COMPONENT_MAX_BYTES = 64 * 1024 * 1024`, checked by `emissionArtifact` (`:435`) against the
raw component (79,824,684 B) and jco's extracted core (76.1 MiB). `buildPluginComponent` (`:363`)
builds the **unoptimized `wasm-dev`** profile, so this bounds a build-time input, not a shipped
artifact — it is not the same thing as the runtime ceiling
`DOCUMENT_EXECUTION_TARGET_COMPONENT_MAX_BYTES` (`📇️directory/🧬️schema/🦀️.rs:1778` + `🟦️.ts:1437`),
which applies to the optimized component. Raised to `128 * 1024 * 1024` with a docstring saying so.

Pre-existing, not caused by this lane: the staged component was already 78,698,225 B at 19:35, and
the repo-source descriptor pair was last emitted **Sep 8 14:08 / 23:13** — i.e. describe has been
dead since the plugin crossed 64 MiB. No test asserts the literal (the fresh-component test suite
takes it by injection, `🖨️describe/🧪️tests/🆕️fresh-component/🟦️.ts:153-154`).

### F4 — same bound mirrored in the Rust emitter
```
semio-framework-plugin-describe describe: raw component …semio_s_plugin_procedural.wasm exceeds 67108864 bytes
```
`🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🖨️describe/📦️packages/🦀️rust/🦀️.rs:255`
`DESCRIBE_ARTIFACT_MAX_BYTES: u64 = 64 * 1024 * 1024` → `128 * 1024 * 1024`, docstring corrected
(it claimed to "share the strict catalog's per-artifact ceiling", which is exactly the conflation
that made it wrong) and cross-referenced to the TS constant so the two stay in sync.

## 3. Descriptor target correction

`📓️extension-continuation-2026-09-09.md` §6 names
`bun nx run @semio-tech/os-plugin-describe-rs:describe -- procedural`. That target's `describe`
route is the raw emitter CLI and refuses a bare plugin name:

```
usage: semio-framework-plugin-describe describe <component.wasm> --core <core.wasm> --out <dir>
```

The correct owner-scoped target is **`bun nx run @semio-tech/procedural-plugin:describe`**
(`✏️s/🔌️plugins/🌀️procedural/📦️packages/🦀️rust/📋️project.json` → `bun ./📜️script.ts describe` →
`describePluginComponent(repoRoot, "semio-s-plugin-procedural", …)`), which builds the wasip2
component, extracts the core with jco and writes both files into the owner root. Every migrated
plugin crate follows this convention; §6 should be read as naming the emitter crate, not the target.

Emission receipt (23:47:43):

```
described procedural (plugin semio:procedural@0.1.0) -> ✏️s/🔌️plugins/🌀️procedural
  wasm=f6cfbbed850e9973e0be48e53bb12fe1bedd90733fa3c5d65219681de7c5b96f
  core=8ab1df083b482a0c4570a8493772528e559fc3a4b74f72eeaab77d8db4a52aab
  descriptor=889de7742126a3fd8a8bc47396d16f206a915165fef3d9dde538eb49410013bf
```

`✏️s/🔌️plugins/🌀️procedural/🛂️.descriptor.semio` 229,409 B (Sep 8 14:08) → **232,605 B (23:47)**;
`🔣️.json` 1,007,038 B (Sep 8 23:13) → **1,030,338 B (23:47)**.

## 4. Descriptor action diff (before → after)

Before = repo-source `🔣️.json` as of Sep 8 23:13. After = both the 23:15 staged pair **and** the
23:47 repo-source pair (they agree on every list below).

| app | before | after | delta |
|---|---|---|---|
| `s.procedural.generation3d@1/*#editor` | 46 actions, 1 command | **44 actions, 4 commands** | −`worldPointerDown`, −`graphPointerDown`; commands +`flowEvalResolve`, +`flowTessellateResolve`, +`cancelPreviewEval` (was `flowEvalTick` only) |
| `s.procedural.generation3d@1/*#viewer` | 13 actions | **26 actions** | +7 viewer actions `setShowMode, setLodMode, setCamera, toggleSun, setSunAzimuth, setSunElevation, setSunIntensity`; +6 framework selection actions `interactionSelect, interactionHover, clearSelection, selectAll, setSelectionMode, setInteractionGranularity` |
| `s.procedural.generation2d@1/*#editor` | 39 actions, 0 commands | 39 actions, **1 command** | +`flowEvalResolve` |
| `s.procedural.generation2d@1/*#viewer` | 13 actions | 13 actions | — |

All five window kinds of each editor carry the same list; the counts above are per window kind.

String-table grep, repo-source pair after regeneration:

```
flowEvalResolve          🔣️.json=2   🛂️.descriptor.semio=1
flowTessellateResolve    🔣️.json=1   🛂️.descriptor.semio=1
cancelPreviewEval        🔣️.json=1   🛂️.descriptor.semio=1
worldPointerDown         🔣️.json=0   🛂️.descriptor.semio=0
graphPointerDown         🔣️.json=0   🛂️.descriptor.semio=0
```

Note the descriptor pack spells the routes in **camelCase**, not kebab
(`grep -c flow-tessellate-resolve` → 0, `flowTessellateResolve` → 1); the boot report's §5 kebab
probe matched a different string and should not be reused as the freshness oracle.

## 5. Gate status

- `@semio-tech/plugin-registry:generate` — **pass** (23:53). Required: `check` reported
  `plugin registry catalog is stale: generated/🔌️plugins.json, generated/🧩️plugins.ts` immediately
  after describe, which is the expected consequence of the descriptor changing.
- `@semio-tech/plugin-registry:check` — the catalog-staleness phase is **green** after `generate`
  (the stale line is gone). The second phase, `plugin taxonomy tree violations`, is **red
  repo-wide and pre-existing**: 19,175 findings across **all 33 plugins**, e.g. `🗄️stdio` 6,272,
  `📕️norm` 2,646, `🏛️architect` 1,193, `🌀️procedural` 639, `✒️writer` 165. Procedural's are the
  same two generic classes everyone else has — `artifact "X" is missing 🧬️schema/ | 🚪️io/ |
  ⚙️engine/ | 📚️examples/` (all four artifacts) and `… is not reachable from Cargo manifest
  📦️packages/🦀️rust/Cargo.toml` (572 of 639, i.e. every `#[path]`-included leaf). Nothing in the
  procedural set is descriptor- or action-related. This is not a regression from this lane and is
  outside its remit; it needs its own ticket lane.

## 6. Freshness proof of the served page

Vite restarted at 23:58:55 as the **serve step only** (`bun ./📜️script.ts serve generation3d react
dev`, cwd `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript`) — no second
activation, no rebuild. `ServeScript` (`📜️script.ts:1413`) reads the activation receipt, asserts
`variant`/`profile`, then runs vite; the plugin modules are served from the static staging dir and
the receipt is watched live by `semioActivationVitePlugin` (`🔌️vite-plugins.ts:653`), so a fresh
vite always serves the current staging — there is no import-time snapshot to invalidate.

```
200      2626 B  /?plugin=generation3d
200     10238 B  /🔌️plugin-modules/🌀️procedural/🌉️bridge.js
200    232616 B  /🔌️plugin-modules/🌀️procedural/🛂️.descriptor.semio
200  79757961 B  /🔌️plugin-modules/🌀️procedural/semio_s_plugin_procedural_component.core.wasm
```

Byte identity, staged vs wire:

```
core.wasm   staged 5aa6c8a1400c87ba1da47f8aefa7354b791c8f1cd998bb8cede38b8e41ebe189
            served 5aa6c8a1400c87ba1da47f8aefa7354b791c8f1cd998bb8cede38b8e41ebe189
descriptor  staged b4841058df319e6b341edd652750b317c27fc05f000463469026802f8f6c6c5d
            served b4841058df319e6b341edd652750b317c27fc05f000463469026802f8f6c6c5d
```

Symbol proof on the **downloaded** bytes (`curl … | strings | grep -c`):

```
served wasm        cancelPreviewEval=2  flowTessellateResolve=2  flowEvalResolve=3
                   worldPointerDown=0   graphPointerDown=0
served descriptor  cancelPreviewEval=1  flowTessellateResolve=1  flowEvalResolve=1
                   worldPointerDown=0   graphPointerDown=0
```

Both new continuation routes and the new cancel action are on the wire, and both deleted
pointer-down routes are gone from it.

Activation receipt
(`🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/dist/runtime/dev/generation3d/activation/🔣️receipt.json`),
`variant=generation3d profile=dev`, all 11 at `rebuiltAt=1788988508851` (**23:15:08**):
`flow, flow-extension-{bim,brep,dictionary,draw,list,logic,math,primitive,text}, procedural`.
`procedural artifactSha256 = 310bd5ca92f09eaf…` (was `ed802e29ecb3ce80…` at 19:35).

## 7. Serve process tree

```
87832    1  bun ./📜️script.ts serve generation3d react dev      (started 23:58:54)
 87849 87832 bun node_modules/.bin/vite --configLoader bundle --config ⚙️vite.config.ts
             --host 127.0.0.1 --port 6018 --strictPort            (LISTEN 127.0.0.1:6018)
```

Kill by pid 87832 (its ppid is 1 — the nohup wrapper already exited), never by name; an unrelated
peer vite is on 6013.

## 8. Handover notes

1. Runtime behaviour of the page was **not** verified here — the coordinator owns the browser pane.
   This report proves build, staging, descriptor emission and wire freshness only.
2. `📓️extension-continuation-2026-09-09.md` §6's describe command is wrong; §3 above has the right
   one. Worth correcting in that report if it is re-read.
3. `📓️wave1-boot-report-2026-09-09.md` §5's `grep -c flow-tessellate-resolve` freshness probe
   matches nothing in the descriptor pack (routes are camelCase there); use §6's camelCase probes.
4. The 64 MiB → 128 MiB descriptor bounds (F3/F4) unblock `describe` for **every** plugin that has
   outgrown 64 MiB in `wasm-dev`, not just procedural. If a lane wants the emitter to read the
   optimized `wasm-release` component instead — note `FreshComponentRequestV1.componentProfile` is
   declared `"wasm-release"` (`🖨️describe/…/📜️script.ts:53`) while `buildPluginComponent` hardcodes
   `--profile wasm-dev` (`:363`) — that is a separate, repo-wide decision and a much longer build.
5. The repo-wide `plugin taxonomy tree violations` red (§5) blocks `plugin-registry:check` for
   everyone and needs its own lane.
