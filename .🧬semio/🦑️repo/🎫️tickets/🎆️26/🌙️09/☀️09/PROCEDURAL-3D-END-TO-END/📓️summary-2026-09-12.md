# Procedural 3d End To End — session 3 summary (2026-09-12)

Coordinator: Fable 5.1 (claude-code, session ⚪42c503b8). Repo MCP was down (`-32602`); bookkeeping on disk. Fleet: 6 Sonnet audits + 30 Opus execution lanes, run concurrently with puzzle3d/other peers on the same files. Every lane report is `📓️<lane>-2026-09-12.md`; the chronological log is `📓️status.md` (session 3 section).

## Outcome, from a user's perspective (React renderer, `http://127.0.0.1:6018/?plugin=generation3d`)

| goal | state at session start | state now | evidence |
|---|---|---|---|
| boot to a 3d preview | never (contributions never installed; extension invocations never reached any extension; 24 s per hop; 110 s+ then faults) | hexagonal column evaluated + tessellated + rendered in **12–13 s** | `🗑️generated/journey-9/`, `🐍️journey-probe.mjs` |
| all 8 examples | picker changed the label only | all 8 load with live preview meshes in the editor (3–78 s) and in the viewer; `No example` clears | `journey-9/results.json` |
| every window | flow tree only, blank canvas | Flow (node-graph canvas paints, camera fits, captions, `Fit graph`), Preview (status, progress, cancel), Generations / Form / Generate preview (`Add Generation` → mesh in 5 s), Viewer preview (status parity), roles + modes switchable by click and keyboard | `🐍️generate-mode-probe.mjs`, `🐍️role-switch-runtime-probe.mjs` |
| hover / selection / orbit | wired but unprovable (meshes=0) | hover target, deduplicated selection with gumball + tree highlight + inspection panel, multi-select vocabulary unified, orbit → `setCamera` | `🗑️generated/probe-interact-final-*/` |
| performance limits | 110–130 s per example, tick storms, 256-slot queue overflow, worker watchdog kills, 87 s role switch | one tick per window (latch), no spin, 10 s/hop CSS recalc storm removed, evaluate budgeted/resumable with shard recovery, close ladder 2052 → 125 turns, role switch 253 ms | `📓️host-reconcile-silence…`, `📓️tick-arming-latch…`, `📓️extension-evaluate-budget…`, `📓️close-ladder-budget…` |
| wgpu renderer (`:6118`) | did not boot / stale guest | boots in 4 s, contributions install, documents reconcile into the paint arena, Flow window paints (draw calls 2 / 620 quads / 575 glyphs); the guest's tick reply hits a frame-decode defect — chain to meshes pending (`📓️wgpu-command-frames-mesh-chain…`) | `📓️wgpu-*` reports |

## Root causes found and fixed (highlights)

1. **No extension invoke door**: the React host had no `PluginWasmHandle.invoke` and the guest dropped `Event::Request`; the ABI's request/respond seam now exists on both sides, shared by the wgpu bridge.
2. **Contributions push starved** by the `refreshUi` generation race; now an owned publisher unit; a not-contributed tick no longer re-arms.
3. **Tick storms**: `pending_effects` polled a scratch session; the retained session now owns a per-window latch; fault/invocation-in-flight never re-arm.
4. **10 s per hop**: nine infinite CSS animations on `:root` over inherited registered properties forced full-document style recalcs every frame (112 s of 129 s) — moved onto painting elements, `inherits: false`.
5. **Example switch**: `refreshUi` abandoned every pass under the eval chain (livelock) — coalesced; `No example` loaded the hex column.
6. **Long booleans killed the worker**: `evaluate` is now a budgeted resumable job with liveness heartbeats and shard/app recovery.
7. **View-state carriage**: the 248 KB contributions pack rode in the crossing view state against a 64 KiB cap — removed (nobody read it).
8. **Node-graph paint**: `render_frame` discarded its scene; adapter-less pages poisoned the canvas; surface re-keyed per boot; captions were one letter; camera never fitted.
9. **Mesh delivery** cost 5–11 round trips per solid (4 KiB gesture quota) and units-not-time budgets; the preview chain has its own 64 KiB route; `ratio` no longer lies; a kernel scaffold solid broke `Box Shell`.
10. **Dev tooling**: vite wedged on a whole-repo FSEvents stream (source-watch plugin); three plugin-module staging roots drifted (one root + loud staleness); wasm-dev `opt-level` overrides (96 → 58 MB).

## Gates at close

React engine `test long` 1009/1009; generation3d `example-geometry` 17/17; generation3d `--lib` 379/6 (six in a peer's in-flight `🔌️plugin/🦀️.rs` undo/vcs class); `procedural-plugin:test` 16/1 (generation2d); `verify publication-retirement-authority` green. Repo-wide gates red for reasons outside this ticket's files (dependency truth, interactivity apps/tool-jobs, plugin-registry assembly/shooting port collision, rust-warnings ⏱️trace) — see `📓️audit-gates-status-2026-09-12.md` §1.

## Open items handed on

- wgpu: command-reply framing (`decodeAppFrame: unknown tag 115`) → invoke chain → meshes on the wgpu World3d (lane in flight at the time of writing; see its report).
- generation2d shares the pre-latch tick shape (`📓️tick-arming-latch…` §7.2) and one red viewer law.
- Peers' in-flight refactors (`📡️spr/🧵️channel`, `🕸️graph` generators, `📤️SegmentedDownload`, catalog taxonomy) intermittently break the dev script / restage; `📜️serve-generation3d-react-direct.sh` and `📜️restage-retry.sh` route around them.
- `🧑‍💻dev/🔌️plugin-modules/` (2.4 GB) is orphaned by code but not deleted.
