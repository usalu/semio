# Coordinator App Consumer Boundary Review

Root independently read the app ownership verifier and Energy execution owner while Terra completed its bounded acceptance audit. No product source was changed by this review. The completed direct6/176 ownership result and offline11-check Energy selftest belong to Terra's executed evidence, not a new root rerun.

## Shared Registry Consumer Execution

Root executed the actual repository test API: `loadOracleRegistry(repoRoot)`, then `oracleHostPackagesFor(registry, "✏️s/🔌️plugins/🔋️energy/🗿️artifacts/🔋️model", "python")`. The real contribution scan and nearest-owner selection returned exactly the Energy local source entry plus external jsonschema4.26.0. Assertions confirmed the Energy path is `✏️s/🔌️plugins/🔋️energy/🔮️oracles/🏃️execution`, `oracleHostModule` resolves its declared `🐍️` module, and the anonymous Python implementation exists there. The command exited0 and emitted `[DEBUG]` evidence. Raw output is `🗑️generated/coordinator/energy-generic-registry-consumer.json`.

This proves the moved registration reaches the shared registry consumer, beyond the package-specific execution owner's direct JSON reader. No generic Python host was materialized, no dependency was installed and no simulation was executed. Exact timing was not recorded.

## Existing Host Path Semantics

The actual `OracleHostPackage` contract in repository test `🟦️.ts` explicitly admits local source/module trees as path entries. The current `materializePythonHost` in repository test `📜️script.ts` selects those local paths and prepends them to `PYTHONPATH`; local entries are not passed to pip. That generic materializer does not choose a local module. Energy's separate execution owner validates its declared module and selects it through `import_module`. Those are distinct responsibilities and the source relocation preserves both.

Root also checked the cross-platform key assumption against the actual manifest: its Windows keys are `win32-arm64` and `win32-x64`, matching the app fixture's `process.platform` lookup. No Windows native execution is claimed.

## Audit Wording Corrections

Root read Terra's full draft and required it to retain all repaired findings, including fixture/test role placement, Hub imports, Nx inputs, source-as-data coverage and the new verifier's relative import. Its limits now distinguish the mistaken GIS invocation that entered and failed native Cargo from successful source-only oracle execution. The report's Energy dependency/API statement is limited to the inspected package metadata and command boundary; empty production dependencies alone were not treated as proof of every Python API type.

The initial registered Nx app evidence was pending when this review began. The final focused source/data evidence appears below; see `📓️terra-app-verification-audit-2026-09-12.md` and the executor report for bounded native limits and exact product attribution.

## Additional Neutral Toolchain Data Closure

After source acceptance, root found the custom OpenStudio toolchain pin table still under `🔮️oracles/📦️packages/🐍️python/🔣️.json`. It contains cross-platform tool archives, bundle/binary metadata and a Python runtime pin; it is not a native Python package-manager filename or format. The actual neutral toolchain owner still reads it through its caller's package root. Root assigned its coherent move beside the toolchain implementation to the same Energy executor, including both TS/Python readers, tests/docs and declared inputs. Native pyproject/uv metadata stays in its package. No compatibility copy, downloads or unrelated platform implementation are part of that move.

The source and direct/selftest acceptance already recorded remains historical bounded evidence. Final current Energy ownership needs the neutral table placement and focused revalidation. Root retained the canonical semantic pin projection before the move at `🗑️generated/coordinator/energy-toolchain-data-before.json` for comparison; this is toolchain protocol data, not an implementation-body snapshot.

Root independently checked the completed data move: the old Python-package JSON is absent, the actual `oracleManifest(repoRoot)` reads `🔮️oracles/🛠️toolchain/🔣️.json`, and its semantic projection exactly equals the pre-move projection. Canonical digest remains `ece3337552781b9d3277b9fbe2d12f31070df7c4b15e7402782672c99f886a81`, covering one tool, six platform entries and Python3.12. Raw check is `🗑️generated/coordinator/energy-toolchain-data-after.json`. Root found a stale Python `@see ../🔣️.json` link; Terra found another documentary pin-path literal in the model oracle metadata. Both were sent to Sol for exact rebinding. Terra's fresh direct owner gate now passes6/183 with13leaves and the explicit data edge; final registered/energy reruns remain with the executor.

The completed documentary rebinding and old-path absence are covered by the final thirteen-leaf contract: Sol reports direct6/186 and actual isolated registered Nx6/186 in3.8s, cache skipped. Prepared Energy selftest also passes its registered Nx target11/0 in2.9s with `UV_OFFLINE=1` and `--excludeTaskDependencies`; no dependency synchronization, download, toolchain provisioning or simulation is claimed. Root read Terra's focused final delta confirming those distinct provenance/limits. The neutral data closure is accepted; the executor's exact file report and separate native VCS result remain independently tracked.
