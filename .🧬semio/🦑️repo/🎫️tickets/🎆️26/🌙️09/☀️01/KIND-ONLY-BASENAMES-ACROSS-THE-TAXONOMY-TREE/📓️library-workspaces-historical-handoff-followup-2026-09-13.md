# Remaining Library Workspace And Historical Handoff Ownership

The active Cargo/transaction/Go lane moves nine concrete owners. Its current package router still contains two additional implemented responsibilities, independently identified during package discovery and reread in full by the coordinator. Complete that active lane first, then handle this bounded follow-up before claiming the library router is fully thin. This packet changes no product file and executes no generator, historical script, live workspace write or Git command.

## Workspace Publication

`WorkspacesScript` reads root package.json, computes package membership, reports missing/stale/order changes, and writes a new workspaces array. The existing neutral `📚️library/🗂️workspaces/🟦️.ts` already owns `computeWorkspaces`, `diffWorkspaces` and workspace-root selection. Reuse those authorities. Move the command's projection/comparison/publication concern beside workspaces, with anonymous implementation leaves and an imported routing class. Do not duplicate discovery, place the behavior in another package leaf, or add a generic helper.

Preserve exact `workspaces --check`/`--write` argument admission, duplicate-name failures, ordering, all unrelated root package.json fields, and ordinary Bun/Nx command registration. Add language-neutral private-tree vectors before the move. Independent installed fast-glob/JSON parsing or native package resolution should validate the same expected membership and resulting document. Execute check/write only on the ticket-private fixture, never the live workspace for acceptance. Expensive discovery/publication needs progress, cancellation and no-follow ownership. Actual current source contracts include `getWorkspaceRoot` consumers; an extraction must not pull the full library barrel into consumers that currently import only the neutral workspaces owner.

The existing workspace walker has historical Shape V1 commentary and accepts physical manifest directories independently of semantic role markers. That is an observed separate membership-policy concern; do not introduce new compatibility paths while extracting publication, or claim arbitrary existing manifests satisfy the final taxonomy. A later membership-policy change must use current package/producer contracts and actual Bun requirements, rather than guessing from directory spelling.

## Retire Historical Report Regeneration

`ticketImportantFemHandoffBytes` plus generate/preview/check classes are permanent library code that reexecutes two historical ticket inputs. Both old inputs contain literal August 2026 handoff prose and literal test-result strings; they do not discover current FEM source or run its tests. The update input references a malformed old Markdown filename. The library wrapper copies a Markdown file to that parsed spelling and rewrites an important-file token to a path that is currently absent. This is a historical report reconstruction dependency, not an ongoing product compiler.

The appropriate closure is to preserve the old authored ticket inputs and handoff evidence exactly, and remove the permanent historical regeneration route/authority from production tooling. Do not move this reconstruction into a new generic runtime helper, execute it to replace historical evidence, delete the inputs, rewrite recorded test claims as current, or introduce a compatibility facade. Use a portable before/after authority fixture to assert only this retired contract and its routes disappear while ordinary generator contracts remain registered.

A completed current-source/configuration search found the production relationships below. Re-read before editing because registry and library files are shared. The input documents and archived output remain in their original ticket and are outside the mutation set.

| Current authority | Exact relationship |
| --- | --- |
| `📚️library/📦️packages/🟦️typescript/📜️script.ts` | three constants, reconstruction function, three classes and their generate/preview/check route bindings |
| `📚️library/📦️packages/🟦️typescript/📋️project.json` | generate-ticket-important-fem-handoff, preview-generated and check-ticket-important-fem-handoff targets |
| `📚️library/🔣️taxonomy.json` | ticket-important-fem-handoff owned generator; two historical executable inputs and one historical report output |
| `📚️library/🧫️fixtures/🏭️owned-generator-preview-inventory/🔣️.json` | retired generator ID in exact current inventory |
| `.vscode/🧩️launch.seed.jsonc` | historical generator preview registration |
| `.vscode/launch.json` | derived preview registration; regenerate through the actual renderer |
| OS plugin registry `🧪️tests/🚀️launch/🟦️.ts` | historical preview registration at order 206.115 |

The abbreviated `📚️library` root is `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library`. Preserve the root package identity, all other targets, current source-input closures and both active executor changes. Search-derived membership alone is not a test; require installed-TypeScript/JSONC inspection, native retained-byte identity, actual generator catalog validation and focused registered launch/inventory controls. No full migration or historical generator execution belongs in this acceptance.

## Preserved Evidence Snapshot

These are observed provenance hashes, not permanent implementation-body assertions. Recheck immediately before and after the later code change; all five present files must remain byte-identical. Neither absent malformed/rewritten path should be recreated.

| Historical identity | Bytes | Observed SHA-256 or status |
| --- | ---: | --- |
| `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION/🔧️write-handoff.mjs` | 5411 | `e18760f389273a8db1262c04e79a181e41a26adba2bbb753ac98c6a6d5c3c84b` |
| `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION/🔧️update-handoff-tests.mjs` | 2428 | `5dab8fd67e876c41b5ac411cf9b5f632bf301bef841a6f92f8e8eefddb971be5` |
| `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION/📋️registrar-handoff.json` | 1558 | `52e78199d6fc9c6001f70a87f050036adc687aa772182e81852f0a6777b230f0` |
| `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION/📋️registrar-handoff.md` | 4863 | `7262a2e4c601400531da691d1421f36677c42b085001a72dc1ec2ccabad8733f` |
| `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION/📓️important/📝️.md` | — | absent |
| `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION/📌️important.md` | 434 | `ff261e39dfde4ced2b7e258ec380d4f12c6d2cbbed8df84f95c178fe423b44eb` |
| `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION/�クレアregistrar-handoff.md` | — | absent |

Raw intake receipt is ticket-private `🗑️generated/coordinator/library-fem-handoff-intake.json`; its complete evidence is retained above. The five current historical files are read-only input/evidence, not files to attribute as edited. Retain exact authored controls and the final scoped product path map in the eventual executor report.
