# Generated Implementation Source Execution Packet

The enforcement report now lists 25 persistent generated implementation leaves with semantic basenames, including a playground-session output absent from declared generator roots. Their exact current paths and contract IDs are in `📓️sol-taxonomy-enforcement-2026-09-12.md`. The earlier Terra report provides independent evidence that these are source outputs rather than transient compiler caches.

The executor must update each producer, output contract, tracked/ignored disposition, fixtures, direct consumers, and generated bytes together. Semantic identities belong in domain directories; the implementation basename is only its registered kind and meaningful extension chain. Prefer existing semantic owners and schema-backed members. Never make generator-owned source an exemption merely because it is generated, and never rename output without fixing regeneration.

Audit all declared implementation output paths for these same generators, including named styling outputs outside the `🤖️generated` directory. Preserve tracked output status; ignored outputs must still be produced at the canonical location. The Go executor separately owns the CLI entity-kinds Go output, so coordinate without changing that same producer path independently. The UI executor owns runtime/scene/render/backend source moves; communicate generated import path changes that affect those live source hash/reference proofs.

Use language-neutral output topology fixtures and existing generator freshness/check targets through Bun and Nx, with independent parser/native compiler or third-party oracle checks where appropriate. Capture source/consumer identities before edits and record generator freshness after regeneration. All scratch, caches, and logs belong under the ticket generated directory. Keep a complete exact changed-file list and bounded verification report; do not close the ticket or goal.
