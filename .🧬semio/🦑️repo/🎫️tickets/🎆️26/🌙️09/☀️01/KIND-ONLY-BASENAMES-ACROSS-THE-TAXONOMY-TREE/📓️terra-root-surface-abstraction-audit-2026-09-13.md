# Root Surface and Abstraction Acceptance Audit

Date: 2026-09-13  
Status: **accepted within the stated native limit.**

## Acceptance Shape

The portable contract fixes the intended final extraction at 17 anonymous TypeScript leaves and 21 taxonomy contexts:

- discovery: surface roots; plugin artifact roots; artifact dialect rows;
- surface schema: contract, owner discovery, facet leaves, six independent laws, and their aggregate; and
- abstraction ownership: contract, source projection, verification, and law.

The surface aggregate and abstraction verification are the root’s direct entry owners. No implementation owner may import root `📜️script.ts`.

## Preserved Inventory Semantics

`🔍️discovery/🗿️artifact/🏠️roots/🟦️.ts` and `🔍️discovery/🗿️artifact/🗣️dialects/🟦️.ts` are distinct. Roots returns enclosing plugin artifact paths. Dialects consumes those roots then returns standard/subset rows, including `subsetRel`; it is not artifact-schema owner discovery. The portable contract explicitly requires dialect rows to refer to an artifact root while their subset paths are not themselves root entries.

Both owners use `📖️source-access/🟦️.ts`, whose typed directory API distinguishes missing, unreadable, symlink and wrong-kind states with complete ancestor inspection. Current lower-owner code turns only missing into intentional absence and throws other states. The contract has an injected `EACCES` plugin-root control.

## Required Runtime Evidence

Acceptance will require the existing abstraction proof’s 11 ownership, four nested-schema and three Rust-command vectors to agree with Ajv, while preserving the current 104 artifact contracts across five formats. That proof uses first-party Rust token/structure parsing for command ownership; Ajv validates its projected disposition, not a separate Rust parse. The portable test must keep source-data references to the normative ownership schema and fixture, and the field-parity consumer remains separate.

My independent package invocation was interrupted by concurrent duplicate execution and is retained as non-evidence below. The executor subsequently completed both the clean direct and isolated registered routes; no native command-enum ownership result is claimed.

## Current Materialization Check

All 17 declared leaves are now present. Their direct imports preserve the intended lower-owner boundary: surface owner discovery consumes the surface-root owner; every surface law consumes contract/discovery/facet leaves; the surface aggregate consumes the six laws and abstraction law; abstraction law consumes the separate artifact-root and dialect owners, never artifact-schema owner discovery.

Root `📜️script.ts` now imports the extracted owners directly and no longer declares the moved surface or abstraction implementations. The unrelated shared taxonomy ordering defect was repaired before the current runtime routes passed.

## Stable Static Audit

After root rebinding, `📜️script.ts:182-205` imports the surface-root, artifact-root, dialect, surface aggregate, abstraction verification, and abstraction-law owners directly. The prior root declarations are absent.

An independent AST audit of the portable fixture found all 17 declared owners present with exact declaration rosters, 35 internal direct-import edges, zero cycles, and zero root back imports. The current project target includes every discovery/source-access, field-discovery/surface, abstraction, normative-schema/fixture, and portable schema/fixture/test input. Its package script, seed launch and generated launch all register the same target and command.

The earlier 6/3 direct state came solely from unrelated actor-typegen/UI-axes taxonomy input ordering. It was repaired before the current green direct and isolated Nx runs recorded below.

## Current Runtime Evidence

The executor’s post-format package command is green: `bun ./📜️script.ts test root-surface-abstraction-law-source` passed **9/9 tests and 201 assertions in 27.28 s**. Its isolated registered source target, `@semio-tech/repo-lib:test-root-surface-abstraction-law-source --skip-nx-cache`, is also green: **9/9, 201 assertions**, Bun 16.78 s and Nx 17.1 s, cache skipped. Root compiler is green at 6/86 and the runtime-import check is green. The direct verification output confirms all 11 ownership, four nested-schema, and three Rust-command vectors agree with Ajv, while retaining 104 live artifact contracts across five formats.

My independent package invocation overlapped the executor’s same Bun test. It printed seven passing controls and the full 11/4/3/104 verification output, then its 60-second wrapper killed the child before Bun printed its summary. I retain that as contention-tainted non-evidence, not a product timeout defect. The clean direct and registered results above are the current route evidence.

A separate private workspace Nx attempt failed before its target started when unrelated `@nx/js`/`@nxlv/python` plugin workers timed out amid concurrent jobs. It does not weaken the target result and is not attributed to this extraction.

## Verdict

**Accepted within the stated native limit.** The extraction preserves separate artifact-root and dialect inventories, has an exact 17-owner/21-context acyclic source closure, binds all direct and source-data consumers, and has green direct plus isolated registered acceptance routes. There is no native `syn` command-enum ownership oracle: the command projection remains first-party `inspectRustStructure` compared with Ajv vectors. The existing native physical-reference oracle does not establish that missing claim.
