# OS Native Verification And Browser Builder Extraction Packet

The coordinator parsed seven live mandatory scripts with the installed TypeScript AST on 2026-09-12. All parsed with zero syntax diagnostics. This is a bounded source planning map; it does not claim native law success or that every router class is implementation. Exact function/class/member coordinates are in 🗑️generated/coordinator/os-native-proof-script-declarations.json. Re-read live sources after the active WGPU/dependency and descriptor/registry lanes settle.

## Verification Owners

| Current source under OS | Newline segments | Confirmed behavior families |
| --- | ---: | --- |
| 📦️packages/🦀️rust/📜️script.ts | 2143 | database history/catalog/capability completion, WAL writer/commit/compaction/recovery/capacity, shutdown, document single-flight and durable group journal, directory session/event-page/bootstrap and segment-state oracles |
| 🖥️host/📦️packages/🦀️rust/📜️script.ts | 1194 | persistence/media projection, member opening/retirement/history identity/input/record/dictionary/factory and public handoff proofs |
| 📦️packages/🟦️typescript/📜️script.ts | 696 | GIS approval/inference/mounted probe/peer rebootstrap and document opening state-machine oracles |
| renderer engine React target/📦️packages/🟦️typescript/📜️script.ts | 894 | directory home/invite/scoped presence, 3D pointer/selection merging, surface-host retention and source signature laws |
| 🔨️modules/🌉️mcp/📦️packages/🦀️rust/📜️script.ts | 659 | exact native binary/credential probe, inference/live catalog/checkpoint-resource verification, owned schema mirror rendering |
| renderer engine WGPU target/📦️packages/🟦️typescript/📜️script.ts | 616 | sealed native environment, Trunk preparation, artifact/asset-server staging, native launch/scale selection, retained home/presence and color policy proofs |
| 🔨️modules/🔌️plugin/🌐️browser-bundle/📜️script.ts | 669 | sealed actor-codegen input capture, generated module closure, exact component/core packaging, WASI suspension validation and native closed actor factory construction |

The renderer target prefix is 🔨️modules/📺️renderer/🧑‍🎨engine/🎯️targets, not the old framework UI target tree. These seven scripts should be split into manageable Sol lanes, not one relocated monolith. Existing data/schema/fixture/test owners are the strongest evidence for each concern. Database and member test implementations belong alongside the corresponding test/oracle domain. Preserve domain-neutral ownership: a native test executes Rust but its concern is WAL, session, opening or history, not a rust-tests folder. Keep genuinely thin build/test/argument routes at their mandatory paths.

The OS Rust source already resolves owning schema $defs through OS_MODULE_SCHEMAS. Its DatabaseHistoryCompletionCheckScript builds an independent in-memory bun:sqlite relational oracle and executes publication cases before invoking native exact-law checks. Preserve that schema/fixture/third-party comparison rather than replacing it with source-anchor checks. Its five exported directory/WAL oracle functions must move out of the command API. Equivalent native and source-only routes retain their explicit distinction.

The WGPU/JCO projection APIs have just moved in the active root-artifact/dependency lane. Preserve those neutral owners and their current six-artifact/input-byte contract. Remaining WGPU environment, build/serve/staging and proof logic is distinct and still requires ownership. Reuse shared Cargo/process/asset APIs when roles match. Do not restore root implementation exports or conflate a source proof with native runtime execution.

## Browser Builder Domain And Consumers

The browser-bundle script has no command classes; it is predominantly exported implementation with a bottom test-registration/native-command entry. It exports BrowserComponentCore, BrowserComponentFactoryControl, BrowserActorPort, ClosedBrowserActorArtifactV1, BrowserActorBuildControl and buildClosedBrowserActorArtifactV1, closedBrowserActorBundle, closedBrowserComponentFactory. Separate the exact source/policy capture, native codegen build, generated module closure and factory/actor packaging concerns. Reuse the existing browser materialization/descriptor owners only when their semantics match; closed actor-source packaging and ordinary component distribution are distinct protocols.

Confirmed consumers include Hub's Rust-package command source, actor-import tests at browser-bundle/🧪️tests/🌊️actor-import/🟦️.ts, and the large injected test factory at plugin/🧪️tests/🌐️browser-bundle/🟦️.ts. The latter currently receives all helpers from createBrowserBundleTests at the command-file tail. Its actual tests exercise hostile extra imports/exports/URLs, mismatched core inventories, cancellation before reads, occupied build capacity, temporary source snapshots and runtime capture. Preserve the injection seam over moved source owners and every negative case; remove no proof to simplify the move. A whole-source export regex would incorrectly count embedded generated programs; use the native AST.

Preserve the exact pinned codegen policy, source snapshot and executable fingerprints, maximum component bytes, unique scratch retirement, staged rename, WASI import identity and runtime bytes. Do not leak the build-only TypeScript/AJV/native process dependencies into browser runtime API imports. Existing build dependencies may remain behind an owned interface where a replacement is not part of this source move; no new external runtime dependency or externally owned exported type is authorized.

The four scopes above with schema/native oracle functions expose additional direct and source-as-data consumers through named exports and command invocations. The current keyword census found only their local command calls for several exported directory/React functions; that is not a proof of no dynamic consumer. Re-run full source/fixture/project/launch consumer inventory before deleting exports. Preserve exact command names, native Cargo law selectors, schema output and source test roots. The actual observed MCP law failures recorded in earlier reports remain evidence to diagnose, not permission to invent a baseline.

## Acceptance

Use schema/portable ownership fixtures first and independent TypeScript AST plus existing native oracle libraries. Run the relevant direct and registered native/source law targets with private output roots; label compile-only or zero-test runs precisely. Apply exact semantic kind ancestry; audit removed coordinates, ordinary command routes, source inputs and native-generated identities. Keep source SHA values in reports only unless they identify actual consumed protocol/build bytes. No modifying Git, AGENTS edits, ticket/goal lifecycle, shared cleanup or live service publication. Keep all new test outputs in this ticket's generated subtree and retain exact changed-file Markdown reports for each bounded lane.
