# Font and Asset Script Extraction Packet

Execute Terra’s first consumer-closed source slice after the framework source audit settles. Keep the mandatory `📜️script.ts` command entries, but move real APIs, rendering, parsers, verification and data models into anonymous TypeScript leaves at their semantic owners. This is a source ownership change, with no compatibility reexports from executable scripts. Re-read current consumers and preserve concurrent repository library and OS work.

## Exact Source and Consumer Boundary

- `🧰️framework/🛍️products/💻️os/🔨️modules/♾️infinite/📦️packages/🦀️rust/📜️script.ts`: `FONT_ASSET` and `validateFontAsset` define the packed font wire contract. The current OS dev package script imports both at line 9 and uses them for staged guest font validation and support artifacts near lines 1405 and 1468. Move to a neutral font owner. Preserve little-endian count and extent validation, minimum font byte size, count limits and rejection of trailing bytes. Keep native font-tool production and atomic staging semantics.
- `🧰️framework/🔨️modules/🖱️ui/🎨️styling/📦️packages/🦀️rust/📜️script.ts`: actual catalog parsing, Google font CSS/source selection, cancellable download, appearance calculation, TS/Rust/Python/C# rendering, output membership and styling verification remain here. The TypeScript package router imports `fetchElementsFonts`; update that real consumer. Extract by font/catalog, palette/tokens generation and verification concerns, not a single generic utilities owner. Preserve the completed browser/build split and neutral generated Python/.NET output ownership.
- `🧰️framework/🔨️modules/🖼️assets/📦️packages/🟦️typescript/📜️script.ts`: SVG normalization, icon identities, catalog/metabolism projections, logo matrix/animation processing and output membership remain in the command file. Separate catalog, metabolism and logo source concerns; command classes delegate. Existing `jsdom` is already hidden behind owned interfaces—preserve that boundary, introduce no runtime dependency or external API type.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧪️tests/🔏️path-emoji-statutes/🟦️.ts` currently extracts `logoKeyframePaths` via installed TypeScript AST and `catalogSvgSources` via source matching from the assets command file. Rebase those actual test referents to their neutral definitions and keep the third-party compiler oracle. Do not loosen the tested naming/emoji invariants.

These are live source/consumer observations, not a complete move map. Search all exported APIs and call sites before extracting. Rebase import.meta roots by actual physical referent; package output paths may remain explicit package coordinates, while source/data owners should follow their actual domain. Do not create an executable helper script or expose a compatibility facade from the mandated command entry.

## Styling Gate Coordinate Defect

The current `PX_SCAN_ROOTS` has nine old ASCII taxonomy paths. The coordinator verified all nine physical paths are absent (table below). `collectPxViolations` skips each absent root with `existsSync`, so a no-findings result cannot establish coverage of current source. The color gate inherits these roots and adds `.storybook`; its stale allowlist also contains several old source coordinates. This is a concrete path-reference issue to resolve while extracting the verification concern. Use actual current owner authority and coverage fixtures, with a hostile file placed under a scanned neutral owner, rather than suppressing the gate or claiming broad style compliance. If renewed coverage discovers independent style violations, retain their exact runtime limit and do not rewrite unrelated UI styling merely to green this taxonomy task. Preserve the current predicate semantics unless a separate proven ownership/coverage contract requires change.

| Old Scan Root | Exists |
| --- | --- |
| `framework/module/ui/js/react` | false |
| `framework/module/ui/styling/js` | false |
| `framework/product/os` | false |
| `framework/product/os/module/dev` | false |
| `s/plugin/flow` | false |
| `s/plugin/cad/renderer` | false |
| `s/plugin/puzzle` | false |
| `framework/os/kernel/infinite/world` | false |
| `s/plugin/gis/2d` | false |

## Validation and Report

Start with portable schema/fixtures for owner mapping and packed-font/catalog/rendering equivalence. Validate with installed native parsers or existing third-party oracle libraries; match generated byte identity against owned manifest outputs. Use ordinary registered Bun/Nx test, generator preview/check and font routes where prerequisites are available, retaining cancellation/progress behavior. Scope expensive native/network work with actual prerequisites; a missing prerequisite is distinct from an executed behavior failure. Do not mutate selected live runtime outputs from fixtures. Keep the production Storybook import.meta.vitest gate and verify browser closure remains intact after extracting build helpers.

Retain exact old/new/support paths, source/hash equivalence and executed checks in `📓️sol-font-assets-script-extraction-2026-09-12.md`. Own scratch only below ticket `🗑️generated/sol-font-assets-script-extraction`; remove own disposable generated outputs when done. No Git, AGENTS, lifecycle changes, broad path ignores or compatibility aliases.

## Current Executor Source Split

Sol’s live consumer trace selects infinite/canvas/fonts for the packed font contract; styling/fonts for catalog/provider/download; styling projection for token/palette plans; styling verification for source coverage; assets/icons/catalog and metabolism/catalog for icon projections; assets/publication for declared output membership; and the existing logos/animation concern for matrix/animation code. All implementation leaves are anonymous TypeScript. Reuse the canonical projection prefix/kind when the semantic role is the same; root-framework schema projection already uses 📽️projection. Keep runtime icon exports browser-pure while Node/jsdom/catalog/publication remains build-only. The command scripts retain only task registration/delegation and preview envelopes. Every new owner and ancestor belongs in the portable/scoped context checks.
