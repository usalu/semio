# TeX Shim Literal Admission Review

At 20:21 UTC the coordinator read the new TeX shim classifier and ran four pure current-source hostile probes. Three incorrect thin-delegation admissions were observed: a command/definition in the ProvidesPackage optional metadata, a ^^5c encoded command in the input target, and ^^2e^^2e encoded parent traversal in that target. A leading ordinary backslash command target already returns unresolved because the current slash normalization makes it absolute.

The implementation currently accepts arbitrary nonclosing-bracket metadata before checking implementation tokens and normalizes input backslashes to path slashes. Its suffix and literal parent-segment checks do not establish inert native TeX tokens. The required correction is precise literal metadata/path validation with portable hostile vectors, preserving the current legitimate Unicode path and comments. This finding does not assert native execution of the hostile cases; it records unjustified static body/path admission. Sol and Terra were notified before final print acceptance.

Exact source strings, current grammar and classifier decisions are retained in generated/coordinator/tex-shim-literal-hostile-probe.json. No product source or live native state was modified by this probe. Independent repair evidence will be appended after the executor closes the gap.

## Independent Native Caret Control

The coordinator then ran the pinned Tectonic0.16.9 on a benign private fixture with locked bundle2f375cdacdf7f982138c778c3e12d8cd11e4aa3af3d854deb1467d560e8d71b0, --only-cached, deterministic mode, SOURCE_DATE_EPOCH1782864000 and TZUTC. From its private caller directory, an input path containing ^^2e^^2e/_.tex successfully loaded the ticket-owned parent target. Exit0 and the compiler log marker [DEBUG] ENCODED PARENT TARGET REACHED prove actual TeX caret decoding, not merely a static hypothesis.

Handcrafted controls remain at 📋️latex-literal-controls/🧪️parent-target.tex and 📋️latex-literal-controls/🧪️encoded-entry.tex. Native materialization, logs, PDF and result.json are disposable under generated/coordinator/tex-literal-native. The control read only its authored ticket target and published only ticket scratch. It does not natively execute the separate metadata-command case or the actual graph fixture.

## Current Independent Repair Check

After Sol tightened inert metadata and canonical literal target tokens, root reran all four initial controls plus the real Unicode shim, comments/endinput, and seven additional target controls (interior command, parent/dot/empty segments, active tilde, encoded tilde and drive prefix). All13 decisions match expectation: two literal delegation positives are accepted; eleven hostile forms are rejected, including the original three false admissions. The real fixed-source disposition agrees with the pure parser for every case. Exact current results are in generated/coordinator/tex-shim-literal-hostile-repaired.json. Native graph execution and final registered target evidence remain with Sol; this root probe does not duplicate those runs.
