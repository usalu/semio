# Go Native Cancellation Tree Correction

Independent Terra runtime review reproduced a leak on the exact registered Go route after the scoped dispatcher repair. It ran `bun <repo-lib>/📜️script.ts go-test <cli> - -run ^TestCanonicalGoTestDispatcher$`, waited for the native scope's nested detached `go test -overlay`, stopped only that probe-owned child, and let the outer fundamental 15-second budget expire. The outer route exited 1, while inner Go PID 81669 remained stopped with PPID 1 and PGID 81669. The auditor cleaned only that recorded process group. Raw evidence is `🗑️generated/terra-go-body/cancellation-overlay/result.txt`; the retained final Terra report supplies the precise ancestry and commands.

The existing killBudgetTree kills one process group, while nested runTestBudgeted calls create separate detached groups. Upstream SIGKILL bypasses inner Bun signal handlers. There is also a related static concern in the native Go caller: exec.CommandContext currently kills the direct Bun dispatcher, so direct caller cancellation needs independent native-child closure too.

The original Sol Go executor must repair cancellation across the complete owned command tree without killing any unrelated shared worker. Choose a coherent cross-platform process ownership/cancellation model: correctly collect and terminate owned descendant groups before losing ancestry, or arrange managed group/cancellation propagation that keeps nested runners under a cancellable job. Do not rely only on a signal handler that cannot run after SIGKILL, only on killing the direct child, or on broad process-name matching. Use existing system facilities behind the repository's own interfaces, no new runtime dependencies or public external types.

Extend the portable dispatch/process fixture first with language-neutral cases for nested budget expiry and explicit native command-context cancellation after the inner test signals readiness. Verify prompt bounded return, stopped marker writes, disappearance of every recorded probe-owned descendant, no detached orphan, preserved output and selected test behavior, and temporary overlay cleanup. Include a normal success and a child spawn/error path. Use an independent native process-table/process-group oracle on this host and Windows-compatible implementation/compile evidence; state actual platform execution limits.

Keep permanent command changes in existing 📜️script.ts and registered Bun/Nx/launch routes, with source helpers at neutral owners and exact selected-package behavior preserved. Coordinate edits to repo-lib TypeScript with other source extraction workers. Record source changes and runtime evidence in 📓️sol-go-cancellation-2026-09-12.md, with temporary output under ticket 🗑️generated/sol-go-cancellation. No Git/AGENTS/lifecycle mutation or broad unrelated runner rewrite. Close the known leak before the dispatcher lane is accepted.
