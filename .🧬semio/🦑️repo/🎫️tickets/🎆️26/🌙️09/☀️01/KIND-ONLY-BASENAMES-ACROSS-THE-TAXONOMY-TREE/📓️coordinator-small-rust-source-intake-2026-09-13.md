# Remaining Small Rust Source Intake

Root re-read the three current source concerns. Puzzle metabolism lookup has29literal SVG inputs, all resolve; its ownership slice is active in the separate report.

Trinity Jack shell's131-line source remains in its Rust package as 📦️bin.rs. Cargo explicitly selects that path. The source-relative native test is already below the neutral shell/🧪️tests owner. The CLI also carries an old default fixture string; resolve its actual consumer before accepting any move. No native shell execution was performed.

Stdio's BREP benchmark source is under the Rust package's emoji-named 🏃️benches folder. Current Cargo.toml declares no[[bench]] entry, while BenchScript invokes bare cargo bench for semio-s-plugin-stdio. Thus the documented benchmark has no explicit native target in the inspected manifest; native metadata/dispatch proof is still required before claiming it executes. Its correct neutral BREP concern and real artifact package should be selected before rebinding a benchmark target. No benchmark was run.

These findings are current intake, not accepted fixes. Norm's Python source is being audited separately by Terra.


Root reread the current131-line Trinity Jack shell, its Cargo manifest, native unit and Bun/Nx routers while Norm awaits the generic host correction. The configured [[bin]] still points at package/📦️bin.rs; the main code defaults to trinity/example/🔱️nakagin-capsule-tower.trinity, which is absent in the current workspace (false). The sole native unit constructs a JSON graph and queries one Piece, so it does not validate default DSL fixture selection or public binary launch. Existing RunScript executes staged dist/build binary and passes arguments; read-only source research only, no move/build/run was performed. Read the Trinity AGENTS.md, which supplies domain graph vocabulary and no extra workflow rules. Source ownership must preserve actual binary selection and correct/explicit fixture contract rather than infer launch readiness from the one graph unit.
