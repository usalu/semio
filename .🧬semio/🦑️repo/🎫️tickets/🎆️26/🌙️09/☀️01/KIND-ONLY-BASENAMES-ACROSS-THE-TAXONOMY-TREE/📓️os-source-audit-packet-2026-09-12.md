# OS and Hub Source Boundary Audit Packet

Use a Terra Extra High read-only slot after `📓️sol-os-source-2026-09-12.md` is complete. Bound the audit to 15–20 minutes. Preserve source, generated artifacts, schema, live processes and concurrent repository/framework work. Do not regenerate, edit source, use modifying Git, edit AGENTS or change ticket/goal lifecycle. Own scratch belongs under ticket `🗑️generated/terra-os-source`; retain the final exact acceptance/defect report as `📓️terra-os-source-audit-2026-09-12.md`.

The current portable contract at `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧫️fixtures/🖥️os-source-topology/🔣️.json` records 30 move/extraction identities and the sibling Cargo/Node WGPU package boundary. Use the final executor map and preimage evidence, not an earlier partially written fixture, and validate actual native source/consumer referents with independent compiler/manifest parsing. A hash asserted in the fixture itself is not an independent identity oracle. Distinguish byte-identical moves, required relative-path corrections and concurrent semantic changes using exact evidence.

## Owned Scope

Inspect hub native bootstrap and TypeScript integration harness; admin browser entry; OS effect backbone and backbone worker; infinite icon-value and font dumper; MCP inference/schema validator and native bootstrap; pack/spr/run/semio native entries; renderer artifact creation/packed text; browser-bundle child worker; plugin installation/launch; descriptor Rust domain source and binary adapter; dev browser harness/plugins/executable-source; hostile batch fixture; JCO host fixture; Windows registration; and the anonymous activation declaration. The four originally omitted body overlaps were explicitly included: hub TypeScript body, descriptor Rust body, actor-import guest Rust body and dev executable-source body.

Hub `🏗️bootstrap` must preserve the distinct existing `🧭️local-bootstrap` credential transport owner. Do not conflate or overwrite the two because their names sound similar. The descriptor `📦️packages/🦀️rust/💾️binary/🦀️.rs` currently contains only `main` driving the neutral `run` through framework async and exiting; verify the shared body classifier admits only such actual glue. No package-level implementation exemption is implied by an anonymous basename or an entrypoint label.

## Native and Browser Closure

WGPU Node package metadata, TS adapter, task router and Vitest configuration moved to a sibling `📦️packages/🟦️typescript`, while Cargo/Trunk/native build data remain in the Rust package. Confirm real package.json/Nx/Cargo identities, actual export paths and source graph. Native commands must still resolve their explicit Rust package root; generated browser outputs and package aliases must follow declared artifact authority. Check the final WGPU browser-profile/source-input/output-root lists and their strict ordering against current generated bundle/receipt provenance; earlier in-flight failures from concurrent framework moves have been superseded by owning-lane generation/check evidence. Do not equate a generic compile count or a stale shared graph snapshot with actual target execution.

Windows PowerShell registration was renamed only as a kind leaf; validate parser/reference identity without claiming a native Windows run on macOS. Retain distinctions among executable tool metadata, substantive domain code, source-as-data fixtures and generated JCO companion output. Flow authored JS, native build.rs selectors, persistent binding/distribution artifacts and large dev/Hub `📜️script.ts` bodies are separately queued—report any new causal dependency precisely, not as a silent scope expansion.

## Context and Verification

Verify the exact 14 contextual member registrations and new owner parent relationships; no generic PascalCase, arbitrary emoji/slug or fixture-directory exemption. Root’s earlier scoped OS elements census found 105 ambient directory findings outside the new owners; those are assigned separately to contextual closure. Zero owned findings cannot establish zero repository findings.

Read the final executed-check evidence, use the registered portable topology gate and a bounded set of read-only native source/build checks where safe. Observe any test’s output-root/process behavior before executing it; no fixture may stage or overwrite the canonical session or selected runtime output. The completed isolated session fixture has its own proof and framework audit, so avoid duplicate broad session work. Inspect launch seed/derived registration and generator provenance from the final report. Preserve all meaningful runtime limits and send concrete defects to root promptly; do not claim failures were pre-existing without causal evidence.
