# Root Artifact and Dependency Source Extraction

The expanded script audit identified these independent source domains still implemented in root 📜️script.ts. Root re-read their actual definitions and consumers on 2026-09-12. This is queued work for a Sol executor; preserve concurrent changes to the shared command source.

## WGPU Package Projection

The four exported APIs renderWgpuBrowserBundles, assertWgpuPackageToolchain, renderWgpuPackageArtifacts and runWgpuPackageGenerator, together with loadWgpuPackageTaxonomy, currently occupy approximately root lines 705–850. They implement exact input admission, workspace import resolution, no-follow content capture, Bun browser compilation, package/catalog identity, generator input projections, bounded stdin/cancellation, six-output freshness and publication. Move these coherent concerns to anonymous implementation leaves beneath the actual OS renderer WGPU owner:

🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🎯️targets/🧊️wgpu

An existing ⚙️browser-build/🟦️.ts owns ordinary browser bundling, pinned Bun validation, astral literal decoding, boot rendering and Trunk module routes. Inspect shared semantics before choosing a projection/publication child. Do not confuse its unrestricted entry bundler with the exact catalog-authorized virtual input view: the latter explicitly checks every permitted module and rejects unused inputs. Keep their distinct contracts while colocating truly shared behavior.

Direct current consumers are OS 📦️packages/🟦️typescript/📜️script.ts (generate/check/preview dynamic imports of root) and the WGPU TypeScript package's exported renderFrameWorker API (dynamic root import). Move renderFrameWorker and its freshness/carrier checks into the actual frame-worker/build concern, then update callers; no command-file facade exports. The package command also contains native environment sealing/proof, cache/trunk checks, selected asset staging and dev hosting; the executor should either close these nearby coherent script domains in the same bounded WGPU slice or report the exact remaining APIs for the subsequent source lane.

The root's adjacent runNestedCargoPackageAdapter implements publication of a single schema-owned JCO adapter. It belongs to repository package projection/publication, not to WGPU. Keep this as a separate owner if included. Its root generate-taxonomy command remains routing. Preserve the JCO canonical Cargo manifest and exclusive file creation contract.

A prior nested-cargo projection asset under repo/library/🖼️assets/📽️nested-cargo-package-projection/🔣️.json contains captured replacement source strings referencing root APIs and absent post-create.sh. These are not automatically active consumers. Inspect their producer/reference authority; preserve captured source-as-data evidence and update only active current projections. Do not reintroduce legacy bootstrap paths or rewrite provenance to match live code.

TDD must bind old/new owner and direct consumers, exact six-output membership and deterministic bytes, actual input/profile rows, mode/no-follow checks, cancellation/projection stdin and native compiler output. Preserve the root-framework lane's five new schema/async input rows and exact UTF-8 ordering. Use the existing registered generator/check routes; generated freshness is stronger than a source bundle. Run actual existing native hostile generator fixtures, scoped ancestry and explicit launch route validation. Root command routing may import the neutral owner; neutral source and tests must not import root command APIs.

## Dependency Inventory and Truth

The root DependencyFreeze region is approximately 1,100 lines (13257–14362), with command behavior in VerifyScript.verifyDependencies around 8154–8223 and public names in the root export block. Extract into semantic owners under:

🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🕸️dependencies

Current domain clusters include:

- Manifest declaration parsing and phase classification across Cargo, JS/Bun lock workspaces, Go modules, Python and .NET. Where language labels mean parsed manifest format, they are domain data; avoid confusing input-language ownership with implementation-language containers.
- JS source ownership/import evidence and manifest-lock parity.
- Approved oracle classification, exact mandated-toolchain ownership, first/third-party inventory and literal-external truth.
- Baseline read/write and ratchet comparison, plus text/JSON reporting.

These are domain behavior even when consumed only by a verification command. Prefer coherent concern owners with anonymous leaves and explicit owned interface types. Do not move all source into a generic commands/helper owner. Existing dependencies/🧩️runtime and dependencies/🟦️typescript libraries implement separate dependency infrastructure; inspect rather than overwrite them.

The actual direct test consumers are dependencies/🧪️tests/🔬️dependency-truth/🟦️.ts and dependencies/🧪️tests/🔬️dependency-js-lock-parity/🟦️.ts. Both currently import root 📜️script.ts. Rebase all ordinary and type imports to their real owners. WORKSPACE_ROOT means the repository root; do not keep dirname(import.meta.url) after a move. Use the existing workspace owner or explicit root parameter. The dependency boundary's DEPENDENCY_REPO_POLICY_* contracts also inspect actual router imports and ownership; preserve their meanings and native self-tests.

Keep dependency identity, baseline bytes, approved oracle rules, exact root packageManager/Nx pin, unauthorized owner diagnostics and lock snapshot joins unchanged. A taxonomy extraction is not authorization to rewrite the dependency baseline or widen dependency exemptions. Run the read-only self-test/parity/report paths first; do not run write-baseline merely to make the gate green. Live unmet zero-dependency goals must be recorded as exact current limits.

The registered root verify-dependencies and verify-dependencies-write-baseline targets remain command routes. Discover or add a precise self-test Nx/launch route through the existing root 📜️script.ts command and launch seed authority as needed. Add portable source-ownership vectors and existing installed parser/compiler/native oracles; do not create a runtime TypeScript/compiler/parser dependency.

## Completion Evidence

Retain source/consumer maps and exact changed paths, direct and registered test output, native/third-party equivalence, complete ancestor contexts, current source disposition and generated-byte checks in a ticket Markdown report. Keep temporary artifacts under a lane-specific 🗑️generated directory and delete only that lane's generated output after recording evidence. No modifying Git/worktrees/AGENTS, broad process kills, shared Nx reset or source hash snapshot assertions.
