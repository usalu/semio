# Kind-Only Registered Test Budget Review

The print executor reported that the correct registered kind-only-basename target completed its first six checks and then reached its15-second command-wrapper budget. Root independently reproduced the actual package route with private temp/test outputs: exit1 in15.464seconds, six passing cases, followed by explicit budget termination at15000ms. This is a real route budget mismatch, distinct from the earlier wrongly invoked source-index command.

The final existing filesystem topology case already permits30000ms. runTestBudgeted otherwise selects the fundamental15000ms default. Root changed only the kind-only-basename branch in repository library's TypeScript package 📜️script.ts to an explicit45000ms command budget, preserving the individual case budget and every assertion. The allowance covers the30-second case plus short checks/process startup and remains finite with owned process-tree termination.

No new test mirrors this constant. The existing language-neutral filesystem fixture, independent fast-glob/AJV/TOML oracles, cancellation, native Cargo binding and live semantic topology checks are the meaningful verification. Current repaired direct and registered outcomes will be appended after execution. The dedicated topic route remains at the same Bun/Nx/launch identity, so no new command registration is needed.

Exact modified product file: 🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/📜️script.ts. Preserve concurrent schema-field routing changes in this shared source. Root-owned raw evidence stays in generated/coordinator/kind-only-budget; no Git, lifecycle, AGENTS, live pruning or native compilation change was made by this budget repair.

The repaired direct route passed7/7 with342 assertions: test body14.82s,15.134s including process startup, final topology case13.977s,46 manifests and235 Rust sources. The actual private Nx attempt then failed during project-graph construction before starting this target. Current graph diagnostics name an undeclared @semio-tech/framework import in discovery, frame-worker generated output with no Nx producer, and semio-repo-test-host with no Nx project. These are not target test failures. Root is inspecting the exact live metadata and possible graph-error cascade; no author or prior-state attribution is claimed. Full raw log is generated/coordinator/kind-only-budget/repaired-nx.txt.

## Final Registered Verification

Current source-input resolver probe succeeds for the WGPU browser contract with six source files. The frame-worker producer declares the exact generated output path named in the earlier error. Root changed no graph metadata and did not infer an author or cause for the transient graph state. The next actual isolated Nx invocation passed with cache skipped:7 tests,342 assertions,13.16s testbody and13.5s target run (critical path13.3s). Its final native-source topology case took11.884s and again found46 manifests/235Rust sources. The initial graph diagnostics did not recur; all required focused budget-repair verification is now complete. Full final log: generated/coordinator/kind-only-budget/repaired-nx-current-retry.txt.
