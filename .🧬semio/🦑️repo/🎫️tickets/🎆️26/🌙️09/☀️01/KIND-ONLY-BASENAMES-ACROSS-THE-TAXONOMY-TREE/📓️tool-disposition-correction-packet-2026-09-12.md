# Exact Tool Disposition Precision Correction

Terra's independent Go/body audit confirms that all seven coordinator false-admission variants are rejected after the 45-test policy correction. It also found a concrete false positive: the current OS renderer React package `vitest.config.ts` is referenced by its registered script and is a real fixed Vitest configuration, but its correct whole-file Vitest disposition returns unresolved. The retained Terra report gives the exact disposition identifier, rejected shape and evidence.

The original Sol policy executor must narrow this grammar correction around actual configuration structure and add a portable native TypeScript AST positive/negative pair before editing. Preserve whole-file accounting and reachable-helper validation; do not reintroduce hidden class/computation admission, an import/call-presence exemption, or a blanket fixed-name bypass. Do not extract a valid tool configuration as ordinary domain implementation just to clear the census.

The same semantic review must distinguish mandatory 📜️script.ts orchestration from unrelated domain implementation. The user explicitly requires all permanent scripts in that filename at the respective directory. The current census of 187 script cases is a review list, not authorization to move their executable orchestration into other filenames. Start with representative actual rejected script shapes and their current imported runner/route contracts; retain exact evidence and a closed configuration/orchestration acceptance rule. Keep any ambiguous domain helper visible for a later source owner extraction rather than broadly admitting arbitrary script content.

Also close the seven audited stale coordinates in the existing generic-stem filesystem fixture while preserving its independent native-fs/glob assertions: control-keybinding-context now uses 🕹️; both Select rows use 🔽️Select; Toggle uses 🔀️Toggle; Diagram uses 🕸️Diagram; Tree uses 🌳️Tree; styling uses an actual current test beneath 🧪️tests, such as 🧩️suite/🟦️.ts. Re-read live sibling/target paths before editing because Storybook is moving adjacent documentation sources. The audited source runtime leaves are already present; this is active fixture reference closure, not permission to freeze failed assertions as history. Run the corrected generic-stem suite as well as the body tests.

Run the existing registered portable/native body policy suite, the focused exact live configuration/script regressions, strict taxonomy validation and a fresh categorized body census. Retain exact changed files and tested limits, and preserve the independent audit's native cancellation finding for its separate Go repair lane. No Git/AGENTS changes, new runtime parsers/dependencies, migration scripts, lifecycle mutation or bulk application source extraction. Coordinate shared discovery/normalization/schema edits with the Storybook story-kind worker and generated styling wrapper correction.
