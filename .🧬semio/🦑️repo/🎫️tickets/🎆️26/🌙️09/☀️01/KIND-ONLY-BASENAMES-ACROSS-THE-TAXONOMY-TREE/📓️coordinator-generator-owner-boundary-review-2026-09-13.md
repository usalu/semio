# Generator Owner Boundary Review

Root read the current graph and entity-kind generator implementations while the Sol extraction was still in progress. This is a structural review, not a failed native generator invocation. The existing focused byte/projection results remain bounded evidence; final extraction acceptance requires the semantic owner closure below.

## Current Graph Boundary

The anonymous leaf at `🧰️framework/🔨️modules/🕸️graph/🛂️manifest/🏭️generator/🟦️.ts` currently contains the former command implementation in full: output catalog validation, manifest source models/discovery, naming and Rust/TypeScript emitters, shared registry/type projection, rendered output planning, filesystem membership/publication, and generate/preview/check/test/lint classes. The thin package router is useful, but moving all these concerns behind one generic generator directory does not complete the packet's domain split.

The remaining boundaries are manifest catalog/contract, admitted source discovery, projection renderers, output planning/publication, and command composition. Existing shared ownership should be reused where appropriate. Rendering a target language is an explicit projection concern; it does not make the TypeScript implementation language its owner. Native Cargo test/lint command composition must not be disguised as manifest rendering behavior.

The current source traversal calls `existsSync` before `lstatSync` on each plugin area and checks only that scan root and descended entries. It does not admit the ancestors of the plugin area. That source inspection is a test opportunity, not a claimed native reproduction: a linked ancestor or inaccessible discovery root needs an independent current probe. Terra is executing those controls. No unreadable source may produce a partial or false clean publication plan.

Terra subsequently executed the current native boundary: a linked `✏️s` ancestor was followed and yielded one linked manifest; a native mode000 parent yielded an empty manifest list and rendering reported generic absence. Those are confirmed admission defects, delivered to Sol. Terra also found the graph test target missing its external suite, fixture and manifest schema inputs, and both generator targets missing imported repository runtime/cache owners. Close the actual execution dependency graph as well as the nominal source-owner paths.

## Current Schema Boundary

The anonymous leaf at `🧰️framework/🔨️modules/🧬️schema/🏷️entity-kinds/🏭️generator/🟦️.ts` currently combines catalog reading/provenance, three language emitters, exact target planning and all command classes. Complete source/provenance, projection, plan and command ownership without changing the existing FIRST-WINS data contract or corrected TypeScript type import. The source JSON and actual generated Rust output now have neutral entity-kind locations; preserve their exact real consumers.

Both current moved leaves still retain command shebang/documentation/import residue and comments inside definitions. The final semantic owners must obey the repository's concise implementation and docstring rules. Do not add an alternative permanent executable script or compatibility facade.

## Acceptance Direction

Root sent these remaining requirements directly to Sol and Terra before final registered execution. Finish ownership/schema/fixture/context closure first, then run the final focused routes against that source graph. Preserve already executed graph5/78 and schema6/50 as interim evidence only; the cancelled native graph invocation that waited on Cargo locks provides no native assertion result. Final exact source attribution must include each actual owner and rebased consumer/input.

## Completed Boundary Repairs

Root fully read the final Sol report and Terra acceptance audit. Graph now has five semantic TypeScript owners and schema four, with registration-only package routers. Current graph7/98 and schema7/60 controls, native admission failures with no output, and actual isolated registered freshness targets pass. The exact graph bridge native law passed after a private target setting avoided the earlier shared lock waits: one selected test covering21families, no failures,184filtered; compile1m38s and law0.01s. The actual repository compiler cache wrapper still reports its content-addressed binary under the standard cache, so private Cargo metadata alone is not described as containment of every cache output.

Root checked41 exact product identities in the executor map:36live and five declared removed originals/intermediate monoliths. A final schema-specific native `entity_kind_` selection is dispatched to cover the moved Rust include/code-as-data consumers, all five row fields and FIRST-WINS with the owned validator. The independent native Go prerequisite remains accepted within its narrower ID/emoji scope.

## Final UI, Actor And Native Schema Acceptance

Root fully read the final UI/actor section in `📓️sol-generator-renderer-script-extraction-2026-09-12.md`, Terra's focused UI/actor audit and the actual retained Rust harness. The exact report map contains23 unique product identities:22live and the one intentionally removed UI-axes JSON predecessor. Eight semantic generator owners replace package command bodies; UI's five concerns and actor's three concerns remain distinct. The retained harness directly includes production generated Rust and parses the real neutral source with installed serde/serde_json; it contains no replacement enum or stub implementation.

Final direct9/58 passes; native UI harness1/1 after29.13s compile confirms ordered two-locale/two-terminology values and serde/parse behavior. UI preview2nodes/0stale and actual isolated Nxcheck4/36 in935ms pass. Native actor preview selects the actual exporter1pass/126filtered, produces exactly7934bytes in a2-node protocol, and matches the committed mirror; actual isolated Nxtypegen repeats the selected exporter and publishes the same bytes in5.2s. The full UI-package attempt still has no native application result because its current OS-pack dependency failed compilation; the standalone generated-source result does not disguise this boundary.

The pending schema consumer follow-up is now accepted: actual `cargo test -p semio-framework-schema entity_kind_ -- --nocapture` selects the exact catalog/projection, FIRST-WINS and hostile-declared-violation laws,3pass/25filtered after13.95s compile. This directly exercises moved Rust code/data inclusion and complements the prior Go projection control. No graph retry was needed. Own executor scratch/processes are removed/finished. Root did not duplicate the UI native build. Generator renderer extraction is complete within these explicit runtime boundaries; Vitest configuration ownership is the next separate lane.
