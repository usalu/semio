# Generator Renderer Script Extraction Packet

Execute Terra’s consumer-closed generator-renderer slice with Sol Extra High, after the source/framework metadata batch is coherent. The domain roots are framework graph, schema, UI and actor. Their `📜️script.ts` entries keep required Bun/Nx task orchestration; actual catalog reading, type models, renderers, projection planning and reusable validation move to anonymous source leaves at their semantic owners. Do not introduce a generic helper directory or alternate executable script. Preserve source-first schema/projection authority, output membership and preview/check/write/prune boundaries.

The original review measured graph 454 lines, schema 216, UI 211 and actor 89; re-read live code because several consumer/import paths have moved. Graph and schema have actual projection algorithms; actor’s native typegen preview/Cargo wrapper may be proven task-only after semantic review. Do not force extraction merely because a mandated router is 89 lines. Use installed TypeScript AST to distinguish template-generated text from actual declarations, and keep body admission precise.

## Two Remaining Graph/Schema Filename Cases

Wave-five exact filename census still reports:

- `🧰️framework/🔨️modules/🕸️graph/🛂️manifest/🌉️generated-value-bridge.rs` (31,654 bytes) is explicitly **hand-written** ToValue/FromValue conversion for generated manifest enums. It is included by `🛂️manifest/🦀️.rs:15`. Move this authored conversion to an anonymous Rust leaf under a semantic manifest/value-conversion concern, rebase the include, register exact contextual ownership and retain native enum/wire mapping equality. Do not register it as generated merely because its filename contains that word.
- `🧰️framework/🔨️modules/🧬️schema/🤖️generated.rs` (7,565 bytes) is an actual generated entity-kind Rust catalog. The owning schema Rust package script emits it in the artifact list (current line 160) and messages (line 187); `schema/⚛️component/🦀️.rs` includes both code and source text at lines 69 and 864. `schema/🧪️tests/🏷️entity-kinds/🟦️.ts` names the TS/Rust/Go projection list, and taxonomy has an exact generator output path near line 25345. Move the generated Rust output to the neutral entity-catalog owner beside the corresponding implementation projection, update its real producer and generator authority plus all consumers, and prove generated byte identity through the ordinary preview/check/generate route. Preserve first-emoji-wins ordering and entity IDs rather than merely changing the output basename.

These two cases were not in the completed framework authored source packet and remain explicit work. They are causally different; the new generator renderer ownership map should include each actual role and exact native/projection consumer.

## Validation

Start with portable source/projection fixtures and installed native compiler/parser oracles. Validate actual generation byte identity for every affected language and exact manifest output membership, with output files in private preview roots before any ordinary registered write route. Preserve current root framework schema metadata extraction, generated styling ownership, UI constants, WGPU closure/input list and launch generation. Source moves must rebase package/tool cwd separately from source/data roots, and every introduced directory needs scoped contextual inventory coverage.

Run focused existing graph/schema/UI/actor generator tests and native Cargo/typegen routes with actual assertions; zero-test compilation remains only compiler evidence. No new runtime dependency, compatibility alias, parallel production writer, broad generated-path exemption, Git/AGENTS or lifecycle action. Retain exact source/support/producer changes and native evidence in `📓️sol-generator-renderer-script-extraction-2026-09-12.md`. Own scratch only ticket `🗑️generated/sol-generator-renderer-script-extraction`, removed after evidence is retained.
