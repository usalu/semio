# Framework Root Source Audit

Run as a bounded read-only Terra Extra High audit after `📓️sol-framework-root-source-2026-09-12.md` exists and its executor reports completion. Do not edit source, taxonomy, fixtures, generators, launch files or Git. Store disposable output only in this ticket's generated audit directory; retain a Markdown report with exact acceptance and defects.

Review the completed two-package-body extraction: Rust schema metadata/typegen to `framework/modules/schema/📽️projection/🦀️.rs`; five TS operations to async lease-pool, jittered-backoff, latest-wins and event-wait plus IO fetch-timeout. Verify actual full paths and contextual ancestor kinds, explicit package exports, cfg/path gating, and native typegen registration. The existing async package API should be preserved; the root framework package exports its prior public operators from their new owners.

The portable fixture/schema/test owner is `repo/library/{🧫️fixtures,🧬️schema,🧪️tests}/🧰️framework-root-source-topology`. Run its registered Nx route with isolated workspace data and no cache, plus bounded direct checks needed to independently validate its coverage. Audit the full real ancestry rather than a simplified synthetic path. Verify installed TypeScript parsing/module consumers and Rust metadata/typegen semantics; a zero-test compiler run is compile-only.

The WGPU browser profile now requires exactly the five new reachable TS sources in both `packageGeneration.browserProfile.sourceModulePaths` and input authority, ordered by UTF-8 bytes. Review normal generator/check evidence and run a read-only freshness check only when necessary; do not rewrite canonical playground session inputs or launch a redundant full Storybook build. Confirm browser imports remain pure and do not pull runtime test/build dependencies through package facades.

Also audit the requested removal of permanent live implementation SHA snapshots in the OS source topology test: the prior 30 owner-body hashes should remain report provenance, with stable topology, syntax, native consumer and semantic role checks retained. This is not permission to remove generated-byte freshness, protocol hashes or existing semantic output parity assertions.

The executor observed `@framework-async:test` missing `async/🤖️generated/🟦️async.js`; this lane preserves that package API rather than pretending the binding exists. Retain the precise observed limit without an unproven baseline claim. Distinguish that package from the focused root framework runtime suite and native typegen test.

Report exact owned context failures, broken referents, semantic/test regressions or generator authority defects. Ambient current failures need direct evidence and causal analysis before attribution. The original framework/UI source report, its 24-context correction, the OS 30-source report and Terra OS audit cover separate completed batches; do not count them as new root-framework acceptance.
