# Contextual Directory Acceptance Closure

This later bounded schema/ownership lane closes contextual directory findings retained by completed source moves. An anonymous filename pass does not establish neutral folder ownership, and a semantically neutral folder can still fail contextual admission because its ancestor vocabulary is missing. Resolve these separately with exact source/provenance evidence; do not rename every unresolved folder mechanically or add broad name-pattern exemptions.

The known affected scope reports are:

- 📓️sol-ui-render-2026-09-12.md: 66 ambient runtime/scene/render directory findings. Some render test names encode packages-rust and need actual neutral test owner moves with active fixture/native target closure, not just registration of the old language-specific name.
- 📓️sol-rust-generators-2026-09-12.md: mathematical plugin ancestor; GIF 87a/89a standard spellings; SVG/GIF base subsets; OBJ geometry subset; resulting descendant generator/codec context failures; stdio oracle law/archive/document/tabular/markup/audio/raster/mesh family and unit-test contexts. The old quick-xml-svg-codec-src-unit test was moved and is explicitly resolved. Revalidate the remaining exact paths before edits.
- 📓️sol-package-body-policy-2026-09-12.md: library and normalization ancestor contexts above its newly valid package-body test and oracle nodes.

Inspect existing semantic member catalogs, artifact standard/subset identities and contextual parent relationships before adding schema keys. Reuse global semantic kinds when their existing identity and allowed parent relation fit; declare exact domain members when that is the intended extension point. Preserve version/subset identities and actual native consumer paths. A dynamic contributed member must be recognized through its authoritative manifest rather than a permissive arbitrary-slug acceptance.

Start with a portable fixture proving representative current failures and expected parent/child classification. Validate against an independent existing schema/manifest/native parser oracle. Use scoped inventoryTaxonomy with progress on each affected owner and exact ancestor retention, strict loadTaxonomy and schema validation. Keep any genuinely unresolved semantic decision visible for coordinator judgment instead of suppressing the finding. No historical full-root normalization planner, blind bulk move or automatic compatibility alias.

Use Sol Extra High for bounded corrections after independent Terra audits, or Terra Extra High for a read-only mapping stage. Keep outputs under the ticket generated subtree, retain exact Markdown evidence, and preserve all concurrent source/schema work. No Git or AGENTS mutations, new runtime dependencies, unauthorized script names, ticket or goal lifecycle changes.

## Independent Strict-Statute Follow-Up

Terra's current registered path-emoji-statutes snapshot is 31/37 with 1,138 expectations; no historical baseline is inferred. The Storybook worker received its own stale `🪗️.story.tsx` suffix assertion for immediate closure. The remaining five checks need exact cause review in this context/projection lane: mutation-pair normalization expects create-camera/create-node but returns empty; glTF and TSV fixtures still read singular `🔮️oracle/🔣️.json` where the active semantic owner is plural `🔮️oracles`; the Rust icon include assertion omits one ../ from the actual generated include coordinate; projected single-emoji scenario identity currently classifies `🔌️disconnect` as present. Preserve active assertions and independent native/glob evidence, correct proven obsolete coordinates, and inspect real schema/member projection semantics for the two normalization cases rather than accepting whichever current output clears the test. See 📓️terra-rust-generator-audit-2026-09-12.md and its retained transcript coordinates. None of these assertions identifies a Rust generator manifest/source ownership failure.

## Storybook Native-Package Assertion Follow-Up

The Storybook executor also ran the nested Cargo authority fixture after exact path updates: 24 tests passed and two failed (248 expectations), concerning the diagnostic for an absent registered identity and a missing launch-registration count. There is no verified baseline. Inspect those exact registry/coordinate expectations after the source/metadata slices settle; do not weaken expected ownership or restore old native source roots to make the suite green. See 📓️sol-storybook-2026-09-12.md for exact tests. The Storybook suffix failure itself is now corrected; the last whole path-statutes run was 32/37.

## Fresh Bounded Context Inventory

See 📓️focused-context-census-2026-09-12.md for the coordinator's live inventory: UI elements 337 entries/21 directory findings; OS elements 358/105; normalization 39/13 (12 directories and one lexical source-as-data reference). Two new UI owner findings were assigned to the active framework worker; the remaining repeated component/test/member contexts remain in this lane. The exact normalization `compose` token is an intentional `input.opaquePrefixes` value in a simulated-repository fixture, not a live import. Preserve its hostile test meaning and correct only precise source/reference classification authority. The retained Markdown contains every exact path.

## Controlled Causes of the Two Projection Assertions

The coordinator ran two read-only native probes after strict loading recovered; disposable detail is `🗑️generated/coordinator/context-causal-probes.json`.

1. The path-statutes test `normalization reads explicit catalogs and canonicalizes both mutation pair roots` still creates synthetic entries at `${owner}/🔮️oracle/🔣️.json` (line 86), while the actual `projectionCatalogEntryForSubset` requires `taxonomy.discoverySchema.testOraclesDirName`, currently `🔮️oracles`. Extracting the actual function and compiling it independently with Bun and installed TypeScript produced the same controlled results: singular input selects nothing, plural input selects the entry. This gives a precise stale fixture coordinate cause for empty vectors. Rebase that synthetic coordinate and verify the entire meaningful pair assertions; do not add singular compatibility lookup.
2. The existing projected-scenario fixture `{id: "connect", directoryName: "🔌️disconnect", expected: false}` is rejected by the installed emoji-regex identity oracle but currently returns no problems at all from the actual `semanticProjectionCatalogProblems`. Source line 2247 checks syntax/NFC/generic emoji but does not compare the parsed physical scenario suffix to its declared logical id. This is a concrete disagreement with the existing schema-first fixture, not an environment failure. Confirm intended exact logical/physical mapping using current contract definitions and meaningful positive cases, then correct the validator or authoritative expectation based on that evidence; do not simply weaken the hostile case.

These probes did not rerun or claim the whole path-statutes suite. They isolate two causes before the bounded execution lane.
