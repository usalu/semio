# Manifestless Source Closure Packet

Use a bounded Sol Extra High lane after the current repository/OS batches finish. Read `📓️manifestless-package-body-census-2026-09-12.md` for the causal coverage gap and six additional source identities. The script-policy executor separately corrects discovery so absent metadata cannot suppress body inspection. Do not change that worker’s classifier/gate wholesale. This lane extracts actual bodies and closes their real consumers; no compatibility packages or aliases, metadata added merely to satisfy discovery, modifying Git, AGENTS or lifecycle changes.

## Six Newly Found Source Leaves

1. `🧰️framework/🛍️products/🦑️repo/🔨️modules/🧪️test/📦️packages/🐍️python/🐍️.py` (287 lines): native Python test host with planned scenario execution, adapter import, result/artifact hashes, protocol-v2 content-addressed outputs and production dispatch evidence. Move to the neutral test owner adjacent to the newly extracted Rust/TypeScript/.NET implementations. Resolve coordinator host invocation and test fixture consumers, including any path assembled from catalog/language names. Preserve full content hashes, artifact bundle/projection semantics, cancellation/budget and exact selected-adapter loading. Use real native Python execution and existing cross-language fixtures; do not claim a py_compile-only run validates behavior.
2. `🧰️framework/🛍️products/🦑️repo/🔨️modules/🖥️server/📚️library/📦️packages/🟦️typescript/🟦️.ts` (915 lines): database pool/auth/events/source-parser/server-job implementation. Move to its neutral server library owner, using domain concerns where appropriate; do not introduce a fake Node package. Its concrete adapter lives at the newly extracted coordinator `🔌️ports/🟦️.ts`, and its active coordinator routes currently import `@/lib`. Repair the stale tsconfig referent `../lib/index.ts` by resolving to the actual neutral source or direct owned imports; no old missing path or alias facade should be retained. Preserve the owned external-library interfaces and existing package-root createRequire behavior. Validate the actual alias/relative consumer graph with installed TypeScript/Next resolver without externalizing the internal alias. Database services may be unavailable; report exact native compile/import/route behavior separately from a live database integration claim. Do not rewrite CRUD/event semantics as an unrelated feature project.
3. `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📦️packages/🟦️typescript/🟦️.ts` (1,254 lines): actual JCO transpilation, worker source generation, build/materialization and browser-host/shard support. It has Nx/router metadata but no Node manifest. Select its existing plugin build/browser/materialization owner after inspecting current files, and move the body there. Do not overwrite the distinct current plugin runtime root. OS dev imports this source near its script line 86; actor shard-client tests also read its constants/source as data. Preserve heartbeat/chunk/lifecycle/diagnostics schema equalities, progress/cancellation and worker/native build output contracts. The completed browser audit found production UI inline Vitest imports reaching this Node module; preserve the production sentinel gate and real runtime/build separation.
4. The same OS plugin TypeScript package’s `📦️distribution/🟦️.ts` (67 lines): neutral distribution source owner.
5. Its `📦️distribution/⚡️vite/🟦️.ts` (18 lines): neutral distribution bundler integration, with exact contextual parent authority.
6. Its `🕸️imports/🟦️.ts` (8 lines): Preview2 shim import rewriting. The current source classifier returns unresolved lexical structure; verify with installed TypeScript/Bun syntax before deciding any parser defect. It is actual rewrite behavior and belongs beside its neutral import/materialization concern regardless of that classifier diagnosis. Preserve rewrite exactness with native module/source fixtures, including false-match hostile cases.

The last three are recursively nested under a manifestless package root and were missed by a root-file-only probe. Recursively close source ownership and consumers, not only the three largest root bodies. Existing Norm vocabulary and Flow browser/host/type cases are already assigned in plugin/metadata packets; no duplicate moves.

## Acceptance

Start with portable schema/fixture for exact source-owner/native entry/consumer identities, plus installed parser/native host oracles. Keep the completed repository source fixture, package-root anchors, Next method exports, native CLI/tests and Go cancellation helpers intact. Rebase source-as-data references to actual neutral definitions and retain their semantic tests; do not make them blind substring or existence-only checks. Re-run registered relevant host/projection tests, internal alias native resolution, all-six-owned contextual inventory and source-body check. Derive launch/project metadata from normal authorities if executable routes change. No fixture writes to canonical selected session or other live runtime outputs.

Retain all exact old/new/support paths and executed evidence in `📓️sol-manifestless-source-closure-2026-09-12.md`; own scratch only ticket `🗑️generated/sol-manifestless-source-closure`, removed after evidence is retained. Failures need exact causal scope; no unsupported pre-existing claims or compatibility source.

## Adjacent Plugin Command API Closure

Root re-read the plugin TypeScript command file on 2026-09-12. In addition to the six original source leaves above, include this small, directly adjacent seventh behavior-bearing source in the plugin build extraction: `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📦️packages/🟦️typescript/📜️script.ts` (148 lines at review). It imports the 1,254-line package body being moved and exports `ACTOR_COMPONENT_EXPORTS`, `assertActorComponentExports`, `PLUGIN_DESCRIPTOR_PROBE_SOURCE`, `finalizePluginDescriptor` and `artifactFiles` to OS dev. Its support/materialize methods implement private staging, signal cancellation, native descriptor probing, pack self-hash finalization and publication. Keep the command leaf but move these behaviors into appropriate neutral plugin build/staging/descriptor concerns, alongside the current source extraction. Do not leave command-file API facades.

The descriptor probe embeds `assertActorComponentExports.toString()` and a fixture-derived export table in a native Node JSPI process. Preserve the actual generated probe behavior, imported fixture root and source body serialization through a focused native execution/oracle. `createRequire(import.meta.url)` currently resolves TOML tooling from the package; rebase its native resolution deliberately. Keep signal listener cleanup and the existing publication ownership/receipt semantics. This is not the larger `plugin/🖨️describe` Rust-package command API slice, which remains queued separately; coordinate shared names and consumers without duplicating that implementation.

OS dev imports all five APIs directly at its top and injects them into its test owner. Close those static/test consumers and any additional live references found during the move. Existing input, protocol and generated-byte hash invariants remain valid; do not add a permanent hash snapshot of the new implementation body. Record the seventh source and support paths explicitly in the lane report.
