# Shared Package Body Policy Execution Packet

## Owned Scope

Implement a shared, schema-backed package source-role contract used by discovery and normalization, then expose its justified diagnostics in the registered package-purity gate. Read the package-body audit and Terra enforcement audit first. This lane owns policy, fixtures, tests, command wiring, and precise active authority-fixture expectations. It does not own bulk extraction of repository/Next/application source bodies; report concrete extraction follow-ups after the classifier is trustworthy.

Anonymous filenames alone do not satisfy domain ownership. Language package directories hold manifests and minimal compiler/tool glue; domain computation belongs at a semantic owner with anonymous leaves. Required Next route/page filenames or Cargo entrypoints do not authorize substantive domain bodies. A small function body can compute; a registration call does not exempt unrelated computation elsewhere in the file.

## Shared Contract

Currently discovery and normalization use divergent heuristics. Normalization classifies fixed/configurable filename bodies as configuration without inspection and uses line-count/call-presence shortcuts. Discovery produces both false positives and false negatives. Define one portable positive/negative grammar fixture first. Cover TypeScript/JavaScript export and import wiring, type-only exports, exact tool configuration, Next delegated entries, Rust module/include/re-export mounts and genuine macro/compiler registration, Go bootstrap delegation, misleading comments/strings, nested bodies, and domain computations inside short wrappers. Keep the classification independent of arbitrary file size and keyword occurrence. Use existing third-party parsers or native toolchains only as test oracles; no external runtime dependency or exported external API types.

Both public classification surfaces and package-role collection must consume the same decisions. Preserve precise unresolved diagnostics for unsupported syntax; do not silently admit it or automatically move legitimate glue solely on the old heuristic. Tests must distinguish basename authority, package placement, configuration authority, and body ownership.

## Gate And Authority Closure

The root package-purity policy currently filters out package-implementation and package-role-unresolved. After precision is verified, include the appropriate body diagnostics. Report the actual remaining violations as an extraction backlog; a nonzero gate is expected until source lanes close. Do not weaken the policy or introduce broad exceptions to manufacture a green repository result.

The UI runtime/scene/render lane removed 63 old source paths. Its active-path scan finds old-path strings only in the frozen remaining-package-purity-authority fixture. Determine the fixture’s actual contract: update active expected inventories and pinned reviewed authorities with a precise causal closure, while preserving explicitly historical/nonreference test examples. Other workers may concurrently update shared taxonomy keys and source imports. Work on the live file, make narrow edits, and preserve their changes.

## Verification And Deliverables

Run existing registered Bun/Nx focused package-role and normalization contracts, a new portable fixture with an independent parser/native oracle, and one current package-purity report. If Nx graph discovery is broken, record the exact error and run the same registered package script directly, without claiming an Nx pass. Avoid repeating huge historical normalization planners. New executable routes must be in the seed and derived launch configuration, with project/package/script conventions preserved.

Retain exact results and changed-file attribution in 📓️sol-package-body-policy-2026-09-12.md. Put disposable logs/cache/output under 🗑️generated/sol-package-body-policy and retain handcrafted inputs elsewhere in the ticket. No modifying Git commands, worktrees, AGENTS edits, compatibility shims, migration scripts, ticket lifecycle changes, or goal lifecycle changes. Finish this bounded policy lane independently of the later source extraction work.

## Domain Declarations

The ownership contract also covers domain type/data declarations. A struct, enum, interface or literal domain table can define the domain even without a function body. Importing or re-exporting owner-defined types is package wiring; defining the model in a language package is not universally admitted because a parser calls it a declaration. Include portable positive/negative cases for this distinction, while keeping exact unavoidable compiler/ABI registration declarations narrowly justified.
