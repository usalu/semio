# Small Router Grammar Closure

This is a focused Sol execution lane after the current fixed-script enforcement and large source-owner extractions. Read `📓️small-script-disposition-review-2026-09-12.md` for exact sources, observed decisions and native constant-equivalence controls. Do not treat all unresolved scripts as implementation or broaden arbitrary imported calls into a terminal bypass.

## Current Proven Boundaries

The plugin's imported dirname/fileURLToPath constructor router is now accepted. Four earlier small forms remain unresolved in a fresh pure body probe: print-font-catalog's direct bound router.run, Demonstrator's guarded imported test registration, Stdio's named imported terminal and Puzzle3D's top-level environment default.

The live grammar already models a literal `process.env.UPPERCASE ??=` assignment inside a precise argv guard. The top-level module loop, however, sends every remaining expression directly to terminal validation and never checks that existing environment-assignment predicate. Replacing Puzzle3D's arithmetic expression with the proven equivalent literal still fails the complete module decision. That is observed evidence, not a reason to invent a helper function for a constant.

## Narrow Execution Scope

For direct router.run, require the existing value-bound ScriptRouter receiver and exact safe argument semantics. Reject type-only imports, lexical shadows, reassignment/unknown objects, hidden argument I/O and duplicate terminals. Do not accept any object merely because its method is named run.

For guarded test registration, distinguish an actual import.meta.vitest guard, its imported registration callback and safe dependency/context injection from arbitrary top-level dynamic loading or implementation. Preserve native binding/TDZ/type-only controls and the command's one-terminal rule. Rebind the real test owner if the current source path moves; no copied implementation in the router.

Stdio's terminal currently comes from its parent command script. Close its actual semantic owner in the Stdio lane, then decide the smallest precise terminal metadata representation. Do not whitelist one path or restore a command-module API facade merely to obtain a green body classification.

For Puzzle's environment default, use the existing native four-case contract (absent, empty-present, override and zero string) and the exact literal value 134217728. Admit only the already-modeled safe environment-default form at the appropriate top-level placement, keeping intrinsic process binding, uppercase member, nullish assignment and literal RHS constraints. The arithmetic source must not be silently accepted as arbitrary computation, and the constant-only fix must not claim full Puzzle native execution.

## Verification

Add portable positive and hostile scope/binding cases before implementation, with the installed TypeScript oracle independently enforcing each intended boundary. Preserve unreadable fixed-source classification and real package/nonpackage inventory integration. Run direct and registered classification suites, native environment controls and exact live body decisions after the change. Update only actual routing configuration where needed; keep substantive receipt aggregation, staging, publication, dependency policy and source hashing in their already-queued semantic owner lanes. Record exact files and independently review the resulting grammar without a blanket script exemption.
