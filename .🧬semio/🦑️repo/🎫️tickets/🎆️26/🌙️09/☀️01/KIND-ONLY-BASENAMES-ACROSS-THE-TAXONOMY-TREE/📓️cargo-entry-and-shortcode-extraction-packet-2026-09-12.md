# Cargo Entry and Shortcode Extraction

This bounded metadata/source lane follows the retained native proof in `📓️cargo-build-entry-constraint-probe-2026-09-12.md`. Two current authored `build.rs` files remain: the OS WGPU package's thin include and the OS Infinite package's substantive shortcode compiler. No product source was changed for this packet.

## Binding Authority

The installed Cargo/rustc control already proves `package.build = "_.rs"` executes, whereas an emoji basename fails Cargo's derived crate identifier. Therefore exact `build.rs` is configurable; preserve the native limitation through an exact manifest-bound anonymous ASCII entry, not a blanket ASCII exemption or false unconfigurable contract.

The current library parser `cargoPackageRootBuildScriptPath` recognizes only absent/default `build.rs`, explicit `build.rs`, and false. Both the filesystem filename scan and normalization's captured inventory hard-code `/build.rs` after calling it. The current taxonomy fixed filename and source disposition also encode the default name. Close actual configured-path admission, filename identity and source-body classification together. Manifest selection is authority only for a valid real package, admitted regular leaf and correctly scoped path; retain disabled, malformed, unowned and unsafe-path refusal. Use portable cases and the installed Cargo/native TOML oracle. Do not admit arbitrary files merely because a manifest references them.

The current anonymous Cargo package projection code also contains historical build-path rewriting to `🏗️builder/🦀️.rs`, which the native compiler rejects as a direct build entry. Review that existing rewrite's actual role and fixtures rather than globally changing every `build.rs` token. Its retained projection assets contain old source topology coordinates intentionally used as source evidence. Keep input and destination roles distinct.

## Two Actual Package Closures

WGPU already owns substantive builder behavior at its target's `🏗️builder/🦀️.rs`. Keep one canonical implementation, rebind the tiny Cargo entry and actual manifest, package catalog, renderer schema, source-authority fixtures and generator inputs. Preserve the accepted six-output WGPU producer and native compile evidence. Recompilation of the entry alone is not full renderer acceptance.

Infinite's 134-line build source combines serde catalog input, exact public identity validation, recursive SVG source discovery, output copies and generated shortcode match arms. Extract this under its neutral icon/shortcode compilation concern with an anonymous implementation leaf. Retain CARGO_MANIFEST_DIR-based input coordinates separately from the source file's location. Preserve public IDs/case, ordered BTreeMap behavior, symlink refusal, duplicate detection, exact catalog cardinality, metabolism nonemptiness and actual SVG referents. Keep serde behind the existing build-time boundary; do not export third-party types.

The generated resolver gives emoji precedence, then metabolism, then catalog, and trims the requested code. Its generated Rust include and copied SVG output use Cargo OUT_DIR. Prove matching bytes and actual lookup behavior using a private output root and installed native compiler; do not write into the live build cache just to manufacture a green comparison. Decide compiler-private output admission separately from persistent repository source ownership.

## Consumer Review

The completed current text search found twenty source/config/fixture files. Exact path/line matches are retained in `🗑️generated/coordinator/cargo-entry-text-consumers.json`. This is a discovery set, not twenty required edits: Hub and Metal/HLSL occurrences are explanatory text, and some projection fixtures represent old input topology.

Additional real consumers needing decisions:

- Repository library `🟨️.mjs` already reads explicit package.build strings and detects build hooks while selecting Cargo source closure. Preserve its configured behavior; coordinate print's concurrent inferred-input edits.
- Root `interactivityIsRuntimeSource` currently excludes only filenames ending `/build.rs`. Renaming the entry or extracting its helper must keep build-only behavior out of runtime interactivity claims through actual Cargo source ownership, not a generic `builder` directory exemption. Review the real include closure and source-role boundary.
- Renderer `🧬️schema/🔣️.json` and the WGPU `🪪️package-catalog.json` fix the current Cargo build value, so manifest-only renaming is incomplete.
- OS dev's capability baseline text names a now-absent Puzzle build source. Preserve honest current findings and inspect that baseline's purpose; do not propagate the stale path as an active source or invent a new Puzzle build hook.

## Completion Evidence

Start with schema/portable first-red controls, then own source moves and all active producer/consumer references. Verify strict taxonomy, exact ancestor contexts, configured Cargo execution, actual Infinite resolver behavior and WGPU producer freshness through relevant registered routes and launch entries. Do not use Git, edit AGENTS, introduce migration/compatibility scripts or broaden filename exceptions. Retain exact owned paths and distinctions between parser, compilation and runtime acceptance in the executor report; use only private ticket generated scratch.
