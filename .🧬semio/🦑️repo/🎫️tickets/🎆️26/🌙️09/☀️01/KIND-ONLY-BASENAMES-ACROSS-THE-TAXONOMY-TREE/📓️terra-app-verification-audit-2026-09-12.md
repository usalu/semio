# Independent App Verification Extraction Audit — 2026-09-12

## Scope and conclusion

This read-only acceptance audit examined the current Mathematical, GIS, VCS, and Energy extraction. The implementation leaves are anonymous and their containing concerns identify their role. The final independent direct app-ownership route and the separately executed isolated registered Nx gate are green after the repaired role, consumer, cache-input, source-as-data, and test-registration findings recorded below. A direct package route is not described as an Nx run.

## Current ownership projection

The app-ownership fixture and strict schema declare 12 owners, four routers, 12 semantic directory contexts, and 11 consumer rows. Its direct structural audit confirms every declared path exists, every declared export exists, and the only executable leaf basenames are `🟦️.ts` and `🐍️.py`.

| Concern | Anonymous implementation owners | Command/router boundary |
| --- | --- | --- |
| Mathematical | `✏️s/🔌️plugins/➗️mathematical/🧪️tests/📣️publication-authority/🟦️.ts` | `.../📦️packages/🟦️typescript/📜️script.ts` is a 12-line router. The data fixture remains under `🧫️fixtures`, while executable verification is correctly under `🧪️tests`. |
| GIS | `🧬️schema/🟦️.ts`; five `🧪️tests/<concern>/🟦️.ts` owners for native codecs, inference control, map creation, durable three-store assembly, and cold-map component proof | `.../📦️packages/🦀️rust/📜️script.ts` imports the four executable command owners. The native-codec owner imports the schema compiler and inference control. |
| VCS | `🧪️tests/📇️native-codecs/🟦️.ts`; `🧪️tests/🪪️native-openable-identity/🟦️.ts` | `.../📦️packages/🦀️rust/📜️script.ts` imports both exact proof owners. |
| Energy | `🔮️oracles/🛠️toolchain/🟦️.ts`; `🔮️oracles/🏃️execution/🟦️.ts`; `🔮️oracles/🏃️execution/🐍️.py` | The 17-line Python-package router imports only the TypeScript execution owner and registers the eight established actions. |

The audit fixture also records the Hub's static GIS component import and dynamic GIS/VCS proof imports. The app target's Nx inputs cover the fixture/schema/test, the owners' relevant trees, all Energy oracle files, every package router/project file, and the Hub consumer. Both launch projections contain exactly one command for the app ownership gate.

## Semantic and source-as-data checks

Mathematical’s strict fixture has exactly six routes, including `nodeGraphViewport` with `WindowConfig`. Its verifier reads the production Rust source, rejects three meaningful source mutations (missing WindowConfig contract, generation guard, or `setPoints` registration), and runs two example tests. The original extraction briefly placed this executable verifier under `🧫️fixtures`; inspection of the taxonomy distinguished fixture data from test behavior, and the verifier now lives under `🧪️tests`.

GIS's schema compiler registers strict `x-semio-child-kind`, `x-semio-state`, and `double` support. Its map proof reads the whole map module with explicit schema dependencies plus a separate fixture control schema; it does not use a verifier-body snapshot. The five proof seams remain source proofs unless their separate native target is invoked. The Hub now consumes the extracted component, codec, and inference owners at their current paths.

VCS’s codec owner validates one receipt and nine hostile rows with strict AJV plus matching Node/WebCrypto digests and three dependency-coherence checks. Its identity owner validates one positive record, denies 11 hostile records, and compares the protocol digest through WebCrypto. Both routes explicitly honor `--oracle-only`, so these checks make no native-law, catalog-activation, provider-link, or VCS-execution claim.

Energy's execution owner reads the oracle contribution registry, requires the literal Python owner path and `🐍️` module, prepends that owner to `PYTHONPATH`, and invokes it through `importlib.import_module`. The app gate asserts this source-as-data chain, rejects the former package-contained Python path, and checks the model artifact's current Python owner reference. Root's independent generic-registry execution corroborates that `loadOracleRegistry` and `oracleHostPackagesFor` resolve exactly the Energy execution-directory path/module plus `jsonschema` 4.26.0 for the model owner; it also asserted `oracleHostModule` and source-file existence. That is registry execution only. Independent read-only corroboration of the generic repository Python-host materializer shows that it maps the declared local path into `PYTHONPATH` without a pip installation. It does not select the module; that selection remains Energy execution's separately validated `🐍️` record and `import_module` call. The toolchain owner requires all six platform keys, including matching `win32-*` keys, and nonzero archive byte/SHA-256 pins, rejects an all-zero hostile digest, and streams SHA-256. The inspected `pyproject.toml` has an empty production dependency list and `package = false`; Honeybee/OpenStudio dependencies are in the test group. The inspected router exposes no exported declarations and imports only the internal execution owner. That establishes the inspected package boundary, rather than proving the absence of external types across every repository API surface.

## Independent executions

| Invocation and result | Evidence class and limit |
| --- | --- |
| `bun ./📜️script.ts test` in Mathematical's TypeScript package: green; six routes, WindowConfig, strict AJV, three hostile source mutations, and two example tests | Direct owner/source oracle and examples. It is not an Nx result. |
| GIS direct source commands: map 26 checks; durable roles=3/cancellation=4/rejection=3; component hostile=5/markers=9 — all green | Source proof only. They explicitly make no durable-publication, native-component-build, or browser-acceptance claim. |
| Direct imports of GIS codec and inference proof owners: receipts=2, hostile=8, AJV+Node+WebCrypto; literal/bounds plus interruption=3/rejection=7 — green | Bounded source oracle utilities. |
| VCS `native-codec-check --oracle-only` and `native-openable-identity-check --oracle-only`: green | Oracle-only source proofs, not Cargo laws. |
| `UV_OFFLINE=1 ... bun ./📜️script.ts test` in Energy's Python package: 11 checks, 0 failures | Prepared offline selftest of model translation fixtures. It does not download, set up a toolchain, run EnergyPlus/OpenStudio, or make a simulation claim. |
| `bun ./📜️script.ts test app-verification-source-ownership`: green, 6 tests / 176 assertions / 799 ms | Direct repo-lib package route. It validates fixture/schema, taxonomy contexts, anonymous leaves, router boundaries, all direct and source-as-data consumers, portable authority controls, and package/Nx/seed/generated-launch registration. |
| `bun nx run @semio-tech/repo-lib:test-app-verification-source-ownership --skip-nx-cache --outputStyle=static`: exit 0, 6 pass / 0 fail / 176 assertions, target 5.7 s | Actual registered Nx evidence, executed by the extraction worker with `NX_DAEMON=false`, `NX_ISOLATE_PLUGINS=false`, and ticket-private workspace/cache/TMP/artifact roots. Cache was skipped. |

The native GIS route was intentionally not used as source-only evidence. `native-codec-check --oracle-only` is not a supported GIS command shape: `NativeCodecCheckScript` accepts no segments and correctly proceeds into exact Cargo laws. That accidental attempt printed the two source-oracle successes, then failed an unrelated live OS store compilation (`maximum_close_byte_demand` and `maximum_retained_close_bytes` trait items missing). It is neither an app-extraction failure nor a GIS oracle-only result.

## Repaired audit findings

The audit first found the Mathematical executable verifier under `🧫️fixtures`, although it runs AJV, production-source mutations, and example tests. The executor moved it to the semantic `🧪️tests/📣️publication-authority` owner while retaining the JSON fixture under `🧫️fixtures`.

It also found stale app-consumer closure during the split: Hub imports still pointed at predecessor GIS/VCS proof locations, and GIS/VCS target inputs did not cover the moved TypeScript/JSON owners. The current Hub has the component owner as a static GIS import and resolves the GIS codec/inference and VCS codec owners from their extracted locations. The GIS and VCS project inputs now include the whole plugin Rust, TypeScript, and JSON trees. The Energy source-as-data selector now reads the contribution record, requires the current execution-owner path and `🐍️` module, and the app gate records that chain explicitly.

The first direct app-ownership command reached no assertions because `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧪️tests/📱️app-verification-source-ownership/🟦️.ts:7` climbed one directory too far when importing Energy's toolchain owner. The import was reduced to seven parent components. The independent post-repair command above completed all six tests and 176 assertions.

## Limits

I obtained no successful native Cargo-law acceptance evidence. One accidental GIS invocation did enter Cargo and failed before a law because of the unrelated live OS store compilation errors recorded above. The first registered app gate stalled in shared Nx daemon/workspace coordination without target output and was interrupted; it is environment non-evidence. The isolated registered result above is the acceptance evidence. No GIS component build/browser run, Hub process, Energy toolchain download/setup, EnergyPlus/OpenStudio simulation, publication, installation, or global reset was executed for this audit.

## Focused Energy Toolchain-Data Delta

The pinned OpenStudio tool/archive table moved from the Python package residue to the semantic data owner `✏️s/🔌️plugins/🔋️energy/🔮️oracles/🛠️toolchain/🔣️.json`, beside its TypeScript toolchain owner. The toolchain code reads that exact path, and the app ownership fixture records it as a source-as-data consumer. The obsolete Python-package JSON is absent. A stale Python `@see` link and an Energy artifact rationale path were rebased to the same owner.

I independently reran the focused direct ownership control after the move: **6/6, 183 assertions, 2.19 s**. Sol's final current reruns increased the contract to thirteen leaves and report **6/6, 186 assertions** both directly and through the actual isolated registered Nx ownership target (exit 0, target 3.8 s, cache skipped). Root independently loaded the new data through `oracleManifest`, confirmed one tool, all six platform pins and Python 3.12, and matched the pre-move semantic projection digest `ece3337552781b9d3277b9fbe2d12f31070df7c4b15e7402782672c99f886a81`.

Sol also ran the prepared Energy selftest through its registered Nx target: exit 0, **11 checks with zero failures**, target 2.9 s, `UV_OFFLINE=1`, `--excludeTaskDependencies`, and ticket-private temporary/cache/artifact roots. It did not sync, download, provision, or simulate. This remains a prepared offline execution control, not a native OpenStudio/EnergyPlus simulation result.
## Final Executor Provenance Delta

Sol's final current evidence raises the portable ownership result to 6/6, 188 assertions in 8.27 seconds. The actual isolated registered target @semio-tech/repo-lib:test-app-verification-source-ownership also passed 6/6, 188 assertions in 5.2 seconds with cache skipped. This supersedes the earlier 186-assertion checkpoint; the added assertions cover the Python @see and Energy artifact-registration pin-table paths.

The final registered Mathematical target confirmed six literal routes, WindowConfig, strict Ajv admission, three hostile mutations, and both examples. GIS evidence remains three registered source targets and direct source proofs; no successful GIS native Cargo-law claim is made.

VCS native evidence is limited to three laws: native_codecs passed vcs_native_receipts_bind_literal_one_codec_closure_without_identity_or_factory_substitution and vcs_native_receipt_closure_denies_every_hostile_row_including_the_retired_document_kind; native_openable_identity passed vcs_guest_descriptor_has_one_canonical_native_openable_identity. They completed after Cargo contention in 11m54s with receipt hashes 018d1a4de5e4ddb118704c98f52767320dd8de33f4079c79fe5ad43970aaeb34 and c79ec3e74fd8ee295bb415fbc0f3644b97f05c5a6369e05dddc7330a61f55540. This proves those laws only.

The Energy registered prepared selftest remains 11 checks, zero failures, 2.9 seconds, with UV_OFFLINE=1, no dependency sync/download, and ticket-private temporary state. It exercises translation/case construction; it does not execute an EnergyPlus simulation or freshly provision a toolchain. These are executor-reported current results reviewed here; I did not rerun native or Nx work.
