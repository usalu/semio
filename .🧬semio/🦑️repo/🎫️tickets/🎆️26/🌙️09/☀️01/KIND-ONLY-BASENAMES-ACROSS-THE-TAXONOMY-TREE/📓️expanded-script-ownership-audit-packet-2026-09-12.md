# Expanded Script Ownership Audit

Use a read-only Terra Extra High worker after a current execution lane frees a slot. Read `📓️script-ownership-coverage-expansion-2026-09-12.md` and the exact 455-row installed-TypeScript AST snapshot in generated/coordinator/script-owner-scope-native-ast.json. The 318 paths outside the old 138-entry census are a missing review scope, not 318 proven implementation defects. No source, policy, fixture, Git, generated authority or lifecycle edits.

Produce a precise domain-based execution map and retained Markdown report. Start with the extra command sources: root repository orchestration (31,361 lines at snapshot), repository test and caching APIs, plugin browser bundle, artifact generators/probes and Mit actor-network rendering. Check actual methods, exports and consumers, including dynamic imports and tests that compile or slice source. A small file or BundleScript subclass does not establish thin delegation. Distinguish existing completed relocations (especially the WGPU Node command package) from new ownership findings.

For root orchestration, identify bounded closures beside existing repo-library neutral owners: WGPU packaging/generation, taxonomy/normalization and artifact source authority, interactivity/job/liveness proof parsing, test/launch generation and native command infrastructure. Trace root exports consumed by domain scripts. Do not recommend moving the entire 31k body to a new generic utility owner. Preserve the user-mandated root executable while separating reusable/domain behavior.

For artifact generators, separate already neutral Rust implementation modules from remaining TS catalog/model/render/probe behavior in scripts. Verify actual producer manifests and output consumers. Existing generator ownership acceptance does not automatically exempt command source behavior, but do not redo native-language relocation work already recorded.

Check shared discovery/normalization entry points for enforcement of `root-script` dispositions outside package containers. The current policy executor is fixing recursive router grammar and the missing-manifest package gate; inspect settled code when available. If non-package scripts never reach the classifier, retain a concrete minimal temporary-tree probe showing the gap, with a positive routing sibling and a hidden-domain source sibling. Do not add a runtime compiler dependency or edit the gate yourself.

Root has now executed the real-root counterpart in 📓️nonpackage-script-enforcement-probe-2026-09-12.md: direct classifier unresolved, filename check null, scoped inventory not-package with no body ownership diagnostic. Five comment-only opaque-reference warnings are a separate retained concern. Use that evidence and add only the bounded positive/hostile control needed to establish a precise corrected gate contract; do not repeat broad root scanning.

Prioritize a small number of consumer-closed Sol slices with exact source paths, public API names, current native/third-party tests, ancestor contexts and native configuration exceptions. Retain original nine router adversaries and the seven earlier false-admission regressions as separate policy evidence. Report limits and current failures without unproven baseline attribution. Keep all disposable artifacts inside the audit's generated directory and clean those outputs after retaining exact evidence in Markdown.
