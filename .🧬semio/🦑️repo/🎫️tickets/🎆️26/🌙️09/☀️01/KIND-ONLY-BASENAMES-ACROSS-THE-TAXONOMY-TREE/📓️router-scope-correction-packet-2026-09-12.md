# Router Lexical Scope And Value Binding Correction

The initial correction passes its 91-test corpus and root's exact ten before/after controls. Independent Terra audit then demonstrated that type-only imports are incorrectly trusted as runtime owners. Wait for its retained `📓️terra-script-policy-audit-2026-09-12.md` and use the exact sources and installed TypeScript diagnostic evidence. All four reported type-only cases parse cleanly but produce TS1361 while the owned classifier returns tool-metadata: imported type-only delegate, type-only terminal, type-only BundleScript/ScriptRouter/terminal, and a named type import renamed to a runtime delegate. These are value-binding defects, separate from grammar preferences.

Root reread the current implementation and confirmed the architectural cause is broader than one missing word filter. `ecmaCommandRouterModule` still collects all local and parameter names across the entire source with regular expressions, combines them into one global trusted-member set, then runs the earlier delegated-expression/initializer recognizers behind global effect/member guards. It does not recursively construct lexical scopes or close call argument expressions through a syntax tree. The report's claim of recursive binding closure is therefore stronger than the implemented evidence. Correct the report and implementation; retain past test outcomes as historical results, not final proof.

Replace the routing recognizer with a closed owned token/statement/expression parser or reuse the existing owned TypeScript lexical machinery where suitable. Distinguish runtime imports from type-only import declarations/specifiers and represent aliases, class scopes, method parameters, block locals and closure captures in their actual scopes. Resolve intrinsic console/process behavior only when those names are not shadowed. Do not add another effect-name blacklist or treat a name observed elsewhere in the source as a value binding here. Keep all external parsing/compiler libraries in the independent test oracle, not the runtime policy.

Preserve legitimate task routing: opaque results may flow from an imported owner into another imported owner, a local immutable binding, an imported predicate or a delegation-only closure. Calls such as `execute(prepare(args))` do not themselves implement the delegated domain and are not defects solely for being composed. Likewise, `args[args.length]` has no hidden side effect; a restriction to literal indices needs an explicit accepted contract, not an audit assumption. The audit retracted its initial four opaque-forwarding allegations after root review.

Validate finite literal map/flatMap and callback cases against the actual portable grammar. Local transforms of runtime receipts and import-name shadowing need isolated controls, separated from otherwise legitimate imported forwarding. Retain the original seven false-admission families, root's one positive/nine exact hostile controls, all existing valid router shapes and appropriate fresh intrinsic/type-only/scope vectors. The installed TypeScript oracle must independently use real AST scopes and `ImportClause.isTypeOnly`/`ImportSpecifier.isTypeOnly`, rather than copy the owned parser's shortcuts.

Keep the missing-manifest disk gate, catalog read-only contract, DSL report-owner extraction and repository owner registrations intact. The non-package mandatory-script enforcement gap remains a separate bounded lane. Record exact source/fixture/schema/test changes and independent/native/registered results in the original policy report with a clear corrective addendum or a dedicated retained correction report. Do not claim global 455-script acceptance from the 89-source candidate census.
