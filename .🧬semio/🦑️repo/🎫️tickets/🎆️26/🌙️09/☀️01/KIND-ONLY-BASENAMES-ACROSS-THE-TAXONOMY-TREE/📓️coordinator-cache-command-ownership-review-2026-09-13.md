# Cache Command Ownership Review

Accepted for the bounded cache command extraction. The repository-wide goal remains active.

Root read the full executor report and independent audit. Thirteen anonymous semantic owners now hold the cache command behavior across 22 registered contexts. The 81-line mandatory router delegates through those owners; the 14-edge owner graph is acyclic and has no owner import back into the command. Artifact package behavior is directly under the artifact concern, outside language-package folders.

The exact attribution in `📓️sol-root-cache-command-extraction-2026-09-13.md` contains 24 unique paths, all verified present by root: 23 product files and the retained executor report. Shared-file attribution covers only that report's cache routing/source-input/context/launch changes and the shared-library source-data note.

Current evidence is direct 11/169 in 20.77 seconds, ordinary Bun routing 11/169 in 16.32 seconds, and actual isolated registered `repo:test-cache-command-source` 11/169 in 20.90 seconds (Nx 21.9 seconds), cache skipped. The private lower pruning fixture reports both independent Python and actual private-tree controls passing. The direct doctor, router import and catalog validation pass. No live pruning, transaction, shared cache reset, installation or service operation was used for acceptance.

Terra identified and reviewed the repaired Windows process-tree termination branch. Actual POSIX parent/descendant cancellation is checked with an observed child PID, while Windows `taskkill /pid <pid> /t /f` is verified at source level; no Windows runtime pass is claimed. The first 100 ms child-PID setup control failed under load and is retained as failed evidence. Its bounded two-second setup window passes the final direct and registered routes. Output bounds and cancellation assertions remain enforced.

The current external workspace-cleanup executable consumer remains registered. Cargo's earlier scheduled-prune path was concurrently removed by another lane; neither this extraction nor this review restores or attributes it. Current Cargo API and library transaction/Go behavior form the next separate slice, with a fresh Terra preaudit. The executor removed only its completed private generated scratch and retained authored controls and reports.
