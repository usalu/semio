# Demonstrator Guarded Registration Grammar

## Boundary

Admit one exact import.meta.vitest block with one awaited literal relative test-module import, one simple object callback binding and one awaited call to that same binding. Dependencies must be direct runtime imports or plain literal/import.meta.dir/url data. Preserve the sole command terminal. Reject unguarded imports, unrelated or shadowed callbacks, effects, extra statements, duplicate blocks, wrong guards and else branches.

The actual test owner was read: it exports registerTests1 and registers two Vitest cases. Classification does not execute this callback or the application. The existing JSON schema validates the portable vectors, with independent installed TypeScript AST and compiler evidence.

## Product Attribution

- library/🔍️discovery/🟦️.ts
- library/🧹️normalization/🧫️fixtures/📦️package-boundary-classification/🔣️.json
- library/🧹️normalization/🧪️tests/📦️package-boundary-classification/🟦️.ts

## Evidence

The first setup attempt failed at mkdir with ENOSPC before any source, fixture or report write; no red test ran. Completed coordinator Nx cache was subsequently reclaimed as recorded in the coordinator recovery ledger. Red and implementation evidence follows.
