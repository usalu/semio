# Cursor Playhost Input Preservation

The two former Cursor helpers are inactive one-time repair inputs. They are preserved byte-for-byte as authored ticket inputs under semantic concerns, with anonymous TypeScript leaves. They are not active repository commands and were never executed. No Cursor-wide source exemption, compatibility route or migration runner was added.

## Reachability And Source Evidence

The completed repository text probe used ripgrep with hidden paths enabled, excluding Git metadata, node_modules, repository cache and historical ticket trees. Neither original basename had a current text reference (exit1, no matches). The actual Cursor hook configuration has an empty hooks object; its commented examples call the ordinary root script, neither helper. There is no Cursor-local AGENTS file. Installed TypeScript parsed both actual inputs with zero syntax diagnostics. Each contains23 old target coordinates; none of the46 target coordinates exists under its explicitly declared repository path. The first source has5 top-level statements and the second25. These observations establish the bounded inactive-input disposition; they do not authorize moving arbitrary Cursor files or unrelated intake.

## Executed Preservation Controls

The retained language-neutral fixture and strict JSON Schema precede the move. Installed Ajv validated the fixture; the initial closure check was red0/2 because both old sources remained and neither destination existed. Before each atomic filesystem rename, the coordinator checked regular non-symlink source, absent destination and unchanged observed bytes to avoid overwriting a concurrent edit. The final control passes2/2: original path absent, regular destination, complete byte equality to the captured source, installed TypeScript parse unchanged, and no anonymous-basename finding from the actual repository classifier. Native command output carried the required DEBUG prefix.

| Original Source | Preserved Input Relative To Ticket | Bytes | Observed Source SHA-256 |
| --- | --- | ---: | --- |
| `.cursor/add-playhost-deps.ts` | `📋️cursor-intake/📦️playhost-dependencies/🟦️.ts` | 1946 | `4ec0b193b059ef71f56e9c0bde7d7c3272775e5c9bc5809a171122b047e5488e` |
| `.cursor/fix-playhost-extraction.ts` | `📋️cursor-intake/🧩️playhost-extraction/🟦️.ts` | 8539 | `2643f3027ef8bd953e85b20068756168c345dd974b71dc7cd830201309dfe2e6` |

Hashes record observed preserved-source provenance; they are not expected implementation-body assertions. Full byte equality was checked directly before disposing of private duplicated source snapshots. File rename preserves the original file contents and metadata; no source formatting or code rewriting was performed. The authored sources, schema, fixture and this report remain outside generated output.

No application feature, installed hook, registered command or runtime behavior changed, so no Nx/launch route was introduced and no mutation script was run. The last exhaustive filename census remains wave seven at199findings; this subsequent two-input move has local classifier acceptance but is not a freshly executed global197finding census.

## Exact Owned Paths

Removed repository paths:

- `.cursor/add-playhost-deps.ts`
- `.cursor/fix-playhost-extraction.ts`

Retained ticket-relative authored paths:

- `📋️cursor-intake/📦️playhost-dependencies/🟦️.ts`
- `📋️cursor-intake/🧩️playhost-extraction/🟦️.ts`
- `📋️cursor-intake/🔣️.json`
- `📋️cursor-intake/🧬️schema/🔣️.json`
- `📓️coordinator-cursor-input-preservation-2026-09-13.md`

Only the ticket-generated coordinator/cursor-intake scratch directory is disposable after these exact observations are retained. Other ticket, product and Cursor files remain outside this disposition.

Final scoped whitespace check passes. The exact private generated/coordinator/cursor-intake scratch directory has been removed after retaining this evidence; both authored source inputs and their fixture/schema remain.
