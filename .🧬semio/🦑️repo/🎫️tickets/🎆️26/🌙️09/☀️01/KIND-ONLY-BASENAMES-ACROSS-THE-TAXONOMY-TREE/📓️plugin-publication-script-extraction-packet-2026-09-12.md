# Plugin Publication Script Extraction Packet

Execute the descriptor and registry consumer-closed slice from the Terra script review with Sol Extra High. The descriptor source is TypeScript **inside the Rust package command file**, `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🖨️describe/📦️packages/🦀️rust/📜️script.ts`; there is no corresponding TypeScript package script at the coordinator probe. Registry source is `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📇️registry/📜️script.ts`. Re-read live source before editing. No Git, AGENTS or ticket/goal lifecycle changes; retain shared work.

## Descriptor Source and Callers

The command file defines fresh-source epoch records/capture, source-byte hashing, component build/extraction, owner descriptor pairs and budgeted production/staging. Public families include `FreshSourceEpoch*`, `freshSourceEpochBytesV1`, `captureFreshSourceEpochV1`, `pluginWasmArtifactPath`, `buildPluginComponent`, `extractPluginCore`, `DescriptorEmission*`, `remainingDescriptorEmissionBudgetMs`, `emitOwnerDescriptorPairV1`, `produceFreshComponentV1`, `describePluginComponent` and `describeExtensionComponent`. Move these definitions to anonymous leaves under existing descriptor/production/source-epoch concerns. The executable script retains command routing and imports the owned APIs. Do not leave public compatibility exports in the script.

Many plugin Rust package scripts directly import `describePluginComponent`: the current broad search finds callers in energy, raster, remodel, trinity, imperative, writer, GIS, sequence, process, demonstrator, CAD, DAG, reasoning, VCS, block, flow, lowpoly, stdio, procedural, norm, playbook, draw, FEM, space, animate, puzzle, layout, mathematical, shooting, architect, note, forms and sourcing. Resolve the full live list, including extension callers. Hub imports `produceFreshComponentV1`, GIS imports both source/build APIs, and fresh-component native tests are composed via `createFreshComponentTests` at `describe/🧪️tests/🆕️fresh-component/🟦️.ts`. Preserve their dependency injection and cancellation/budget/process checks.

The descriptor currently imports `verifyDescriptorPairBytesV1` and `verifyFreshCatalogPackageV1` from the registry command source. Close this dependency by moving registry verification to its semantic owner at the same time. Preserve package identity, paired JSON/packed descriptor hashes, leased source epochs, containment, stable reads, fsync/rename publication and the exact process/descendant cleanup behavior; do not substitute a generic helper module or simplify the protocol during the move.

## Registry Source and Callers

The registry script has actual catalog discovery/projection/rendering, playground session construction/staging, scaffold planning, descriptor/source verification, graph ordering/execution, artifact hashing/commit markers, native catalog selection and trusted stdio publication. Separate these by domain concern and import them from command routers. Existing neutral registry runtime, deployment, trusted-stdio-catalog and launch owners already exist; inspect their role before choosing new ownership to avoid duplicate APIs or a generic source bucket. Template strings contain apparent export declarations—use the installed TypeScript AST when measuring actual script body or public declarations.

The completed playground session correction must remain intact: shared activation owns `PLAYGROUND_SESSION_ARTIFACT_KEY`, output/staged path helpers and the pure Vite alias; only the default variant writes the canonical session; fixture producers use private roots and unique scratch children. Rebase direct registry/dev tests that inspect command-file text to the real neutral definition; do not restore live writes or a fixed shared fixture scratch directory. The recorded isolation proof is `📓️sol-session-fixture-isolation-2026-09-12.md`, and its native target checks all 25 live session/receipt files for unchanged hashes and nanosecond mtimes.

OS dev imports registry generation/projection/session APIs; Hub imports registry publication/verification. Rebase their exact imports without pulling all 5,685-line dev or 17,245-line Hub implementations into this slice. Those large compositions are separate later lanes. Their source-substring assertions must follow actual ownership and still test the intended ordering/invariant, rather than requiring the old command file to retain domain bodies.

## Validation and Report

Start with a portable source-owner/consumer fixture plus independent native TypeScript AST, preserving existing behavior fixtures. Run focused registered descriptor fresh-component and registry/session/projection/catalog tests, the ordinary generator preview/check route, and launch seed derivation where inputs changed. Native build prerequisites and fresh source hashes must match the source actually used. If a causal runtime failure is independent of the ownership move, record its exact evidence and keep acceptance scope precise; do not add unrelated plugin mutation semantics to satisfy the known stdio DWG catalog mismatch.

Keep progress/cancellation, output-root containment and atomic publishing. No new runtime dependencies, compatibility facades, broad generated-output exemptions or alternate permanent script names. Retain the exact moved/source/support paths and executed native checks in `📓️sol-plugin-publication-script-extraction-2026-09-12.md`; own scratch belongs only below ticket `🗑️generated/sol-plugin-publication-script-extraction` and must be removed on completion. Coordinate with the repository source worker before changing shared package imports; preserve its neutral library and Go cancellation work.

## In-Progress Discovery Boundary Review

The executor's first neutral split exposed a registry-discovery ↔ playground-discovery function-level cycle. Root read the new files: playground discovery naturally consumes plugin discovery, while the reverse edge serves resolveDefaultHostVariant, resolveRegistryPluginIdsForFilter and defaultHostVariant selection/composition. The executor is reviewing a distinct selection owner above both discoveries, instead of retaining bidirectional ownership merely because module initialization currently succeeds. Root also flagged playground's CATALOG_ID import from catalog-verification as a possible upward dependency to inspect, and requested pruning copied unused imports once boundaries settle. These are current implementation-review findings, not a final defect claim or permission to add a generic helper/compatibility layer. Final report and native graph/consumer proofs must state the resolved design.

### Resolved Direction Checkpoint

The executor removed registry discovery's reverse playground edge. Plugin discovery now owns the canonical catalog ID and Cargo/descriptor primitives; playground discovery derives rows and its default-host selection from those primitives; projection composes both discoveries for projected filters and host selection. Catalog verification imports the catalog ID from discovery, so playground no longer reaches upward into verification. Direct runtime discovery now returns 59 plugins, 61 playgrounds and host s; all three inspected entry bundles pass. The final portable/consumer/native closure remains in progress and must retain this direction explicitly.

### Second Independent Module-Graph Finding

Root followed runtime relative imports, re-exports and literal dynamic imports from all fourteen fixture owners inside the descriptor/registry roots using the installed TypeScript AST. Seventeen current modules were reachable, with zero syntax diagnostics and zero missing module coordinates. The graph still has one two-module cycle: playground/session ↔ projection. Session consumes projected-row reading/selection and its type; projection calls buildPlaygroundSession from validatePlaygroundSessions and consumes native host/artifact emitters from session. Root read those real call sites. No runtime initialization failure is inferred, but the current split retains coupled responsibilities.

The executor should place catalog projected-row reading/selection below both consumers, close direct imports, and consider moving the native catalog emitters to projection where they are actually used. A compatibility facade or arbitrary dependency-injection indirection does not resolve ownership. The present portable test called acyclic checks selected required/forbidden edges only and misses this pair; extend the meaningful contract to cover the real local runtime graph or the exact corrected direction. Keep generated bytes and canonical session isolation intact. Raw root evidence is generated/coordinator/publication-local-module-graph.json; source correction and final validation remain active.

### Native Descriptor Validation Follow-Up

The executor's native descriptor test reached a missing actor_bindings::semio::framework::byte_page module. Root traced the only current reference to the empty test Host implementation at describe/descriptor-emission/🦀️.rs:89. This is a bindgen world module, not the framework crate's actor byte_page API. The test bindgen selects world actor from plugin/schema/📜️.wit; that world imports pure/host-async and exports reactor/jobs/checkpoint/describe. The WIT still defines byte-page for return-page, but the current reactor uses bytes instead. Verify the actual generated world authority and remove an obsolete empty implementation if it is no longer an imported interface; do not invent a compatibility binding. Native rerun remains the executor's obligation, and no author/baseline attribution is inferred.

Root also executed a native path control against the actual descriptor Cargo.toml: URL.pathname is percent-encoded and does not exist as a filesystem path, while fileURLToPath and join(import.meta.dir, Cargo.toml) agree and find the real file. The thin descriptor router currently uses URL.pathname for build and staged binary paths. Use native path conversion/joins in these routing arguments and verify the ordinary build/describe route. Raw evidence is generated/coordinator/descriptor-native-url-path-probe.json. This is a concrete routing path defect; the fixed-script classifier's rejection of an unfamiliar constructor alone would not establish it.
# Coordinator Final Local Dependency Recheck

After the lower catalog-view owner and final command-class extraction, root repeated the native TypeScript local dependency walk from both actual publication command entries. It follows static imports, reexports and literal dynamic imports throughout the plugin subtree. The current graph has 18 reachable TypeScript modules and 42 local edges, no missing source or parser diagnostics, no computed dynamic imports and no cycles. This closes the two concrete local-cycle findings recorded earlier; it does not claim the entire repository graph is acyclic. Raw evidence is 🗑️generated/coordinator/publication-final-local-module-graph.json. No product source changed during the recheck.
