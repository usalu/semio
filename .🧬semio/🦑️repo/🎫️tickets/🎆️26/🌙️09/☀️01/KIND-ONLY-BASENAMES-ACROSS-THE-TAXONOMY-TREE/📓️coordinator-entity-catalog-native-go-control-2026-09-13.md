# Entity Catalog Native Go Consumer Control

Root independently ran the existing native Go catalog consumer after the schema source JSON and generated Rust relocation. The actual repo-client package route passed `TestAllEntityEmojisProjectsTheFrameworkCatalog` with `-count=1`. Native root-package test output reports0.483seconds and the command exited0. The route also ran its existing nine-vector glob oracle. No product source was changed by this control.

## Executed Boundary

Working directory: `🧰️framework/🛍️products/🦑️repo/🔨️modules/💻️client/⌨️cli/📦️packages/🟦️typescript`.

    bun ./📜️script.ts test long -run '^TestAllEntityEmojisProjectsTheFrameworkCatalog$' -count=1 -v

Both `SEMIO_TEST_ARTIFACT_DIR` and `TMPDIR` were set beneath this ticket's `🗑️generated/coordinator/entity-catalog-native-go`. The normal canonical Go input overlay runner compiled the actual domain-owned source graph; its private overlay was cleaned by the runner. This is a direct Bun package result, not an Nx invocation. Other emitted package rows had no selected tests or no test files and establish compilation only.

The existing selected test reads `🧰️framework/🔨️modules/🧬️schema/🏷️entity-kinds/🔣️.json` with native Go JSON decoding. It compares the generated Go catalog's58 ordered IDs and emojis to that source, compares the normalized and deduplicated `AllEntityEmojis` projection in order, checks required entries, and checks the shared seedling emoji resolves to `technology-mono` through FIRST-WINS lookup. It does not compare every label, icon or filterable field; the other schema projection controls retain that responsibility.

## Provenance And Limits

The source JSON SHA-256 at execution was `f237cdd640bd1ac2b4546726f78f33c90aaf0c2ed11e1f6115294c8e9c3be242`; the generated Go source SHA-256 was `16f2e8cab7bc7629cd73bf028f39cb87188f96d883098b7e0565ad1737a091ee`. These hashes identify observed inputs only and are not expected source-body fixtures. A later change to those bytes may require focused revalidation.

An initial quick-level command, following the router's documented literal `--` example, exceeded the30-second process budget before any case output. Its logged command forwarded the literal separator to Go. That attempt supplies no passing native result and does not establish the cause of the timeout. The successful retry used the supported long budget and passed the native selector directly, without the literal separator. No permanent budget, command or documentation was changed.

Raw logs and the asserted `[DEBUG]` result are under the private output directory as `package.log`, `package-long.log`, and `result.json`. No Git mutation, dependency installation, compiler cache reset, application launch or native Rust assertion was involved. Sol and Terra received the result for the generator lane; final generator ownership/registered acceptance is still pending its separate repairs.
