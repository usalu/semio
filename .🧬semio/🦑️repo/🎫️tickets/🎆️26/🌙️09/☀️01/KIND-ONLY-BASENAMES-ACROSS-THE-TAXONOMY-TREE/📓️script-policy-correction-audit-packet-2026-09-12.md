# Script Policy Correction Audit

Use Terra Extra High read-only after `📓️sol-script-router-policy-2026-09-12.md` is complete. Bound to 15–20 minutes; keep source/schema/config/output unchanged. Root’s original packet, Terra semantic review and native AST observations remain retained. Own scratch only ticket `🗑️generated/terra-script-policy-correction`; retain exact findings in `📓️terra-script-policy-correction-audit-2026-09-12.md`.

The executor’s early review classified 83 of the 89 small source candidates as provisionally admissible after extracting DSL report parsing. Six whole-source cases stay rejected: UI runtime status reduce arithmetic, UI render dependency-data/accumulator logic, scale fixture callback parsing Wasm magic, .NET test-host filesystem staging, Trinity jack shell raw subprocess invocation and puzzle-3d artifact arithmetic environment setup. These are preliminary decisions; audit the final fresh census and exact live grammar rather than asserting this bucket is automatically complete.

## Closed Grammar Adversaries

Inspect structural grammar and actual method bodies, using installed TypeScript AST as an independent oracle. Keep all seven original hidden-code counterexamples rejected. For each newly accepted form, verify a paired hostile case in the same syntactic shape: literal-target loops, local arrow delegates, path map/flatMap command arguments, argument guards, imported-result bindings, scalar process.env setup, status logging, process.exit delegation and literal test-owner imports. Default parameter initializers, computed properties, class field/static initializers, decorators, extra constructor/accessor methods and captured closure effects must not smuggle behavior through a thin-router judgment. Only test shapes the final grammar actually permits; do not invent compatibility requirements or over-expand into full AST parser replacement.

Check binding provenance and later use, not only familiar callee names: imported delegate results must not permit later calculations; pure path mapping must not capture a mutable side effect; scalar task settings must reject computed keys/arithmetic; test imports must have literal valid owned referents; shadowed delegate or BundleScript names must not inherit trust merely from an import elsewhere. A source’s 80-line size, no top-level export or familiar filename is never sufficient. Preserve legitimate native task routing and necessary platform/argument choices.

The DSL fixture-sweep report regex/count/semantic assertion body must live under its existing neutral verification owner. Validate actual native stdout/stderr result parsing and negative coverage semantics after extraction; a new neutral filename alone is insufficient. No compatibility re-export of domain functions should remain in the executable router.

## Missing-Manifest Gate

Root independently proved `scanPackagesDir` skipped `collectPackageRoles` for a missing selected manifest at a non-target owner. The correction must inspect source body ownership independently of whether metadata exists. Verify paired manifest-present/absent fixtures with the same implementation and same thin glue, preserve legitimate no-manifest language/tool contracts, and retain catalog projection read-only behavior. Do not require package.json for every language directory or fabricate an ecosystem marker to make a test pass. The newly found six manifestless bodies are assigned separately; if they are still in flight they should be explicit current findings, not exempted to make the policy census zero.

Run the registered focused package-body policy route and the portable new native-grammar/gate fixture, preserving scratch and cancellation boundaries. Recompute only a bounded fresh classification sample if the whole census is expensive. Distinguish compiled syntax, structural role decision, actual command execution and current metadata failures. Return exact source/function/evidence references for any actionable false admission or false rejection, with no unsupported baseline attribution. Do not mutate code or introduce a broad workaround in this audit.


## Coordinator Argument Adversaries

See 📓️router-argument-adversaries-2026-09-12.md: the current implementation admitted all nine hostile controls, each native TypeScript syntax-valid, in addition to the valid positive. Require all nine to be rejected after correction and audit fresh variants involving member receivers, nested arguments, registration/terminal arguments, finite iterable provenance and closure captures. This evidence supersedes any acceptance inferred solely from the existing 65/65 suite.
