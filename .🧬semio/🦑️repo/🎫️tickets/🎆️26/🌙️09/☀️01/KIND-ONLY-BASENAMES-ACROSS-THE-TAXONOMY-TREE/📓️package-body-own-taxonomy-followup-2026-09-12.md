# New Package-Body Oracle Taxonomy Follow-Up

A coordinator read-only `inventoryTaxonomy` probe scoped to the new normalization/tests/package-boundary-classification/oracles directory completed with 13 entries and five findings. Three are unresolved ancestor/test directory kinds (repo library, normalization, and package-boundary-classification). Two are newly authored oracle metadata files: Cargo.toml and Cargo.lock have no admitted scope at the semantic oracle owner and are reported as semantic-stem-unresolved.

The native Rust oracle source is already the correct anonymous leaf. Move its manifest/lock into the ordinary oracles/packages/rust metadata boundary, retain Rust/Go implementation leaves at the semantic owner, update native oracle invocation/source target paths, and register the new test case’s exact semantic context without overlapping existing global kinds. Existing unresolved ancestors should be distinguished from new owned scope. Do not create a fixed-name exception admitting arbitrary Cargo metadata at every source owner to silence these findings.

Run the 30-test portable/native package-body route again, plus scoped inventory on the moved metadata and new test domain. Preserve the active Go dispatcher worker and Rust generator worker’s shared changes. Retain exact changes and results in the package-body report or a separate correction report. This narrow correction is assigned to the original Sol policy executor when the next source slot is free, before the combined independent audit. It does not reopen bulk extraction scope.

## Report Attribution And Command Scope

Correct the execution report’s supporting file list: the new package-body gate diagnostics are in the repository-root 📜️script.ts (observed around line 31039), not the 595-byte 🧰️framework/🛍️products/🦑️repo/📜️script.ts wrapper, which currently has its September 3 timestamp. The registered direct test command bun ./📜️script.ts test package-body-policy is relative to the repo-lib TypeScript package cwd configured by its project, not necessarily the repository root. Use an exact absolute file reference or state that cwd so the verification command is reviewable. The root package script correctly delegates through Nx.

## Confirmed Classifier Corrections

The active follow-up also owns the three reproduced false admissions in 📓️package-body-counterexample-review-2026-09-12.md: Python import-line partial matching, private domain code hidden beside Vitest configuration, and private domain code hidden beside a command-router call. Correct whole-source/disposition validation with portable/native negative cases before independent audit.
