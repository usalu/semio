# Root Taxonomy Inventory And Mutation Extraction Packet

This is the next bounded root-script implementation slice after the active WGPU/JCO/dependency extraction. The source was reread on 2026-09-12 after that extraction reduced root offsets; current coordinates are planning aids only. Sol Extra High should reparse the live AST, preserve concurrent edits and retain semantic concerns separately. No Git, AGENTS or lifecycle operations.

## Concern Boundaries

Root source around 14060–14176 defines operation option contracts, guarded ticket-relative paths and generated artifact naming. The following TaxonomyInventoryShards region owns roughly 490 lines of canonical streaming, 5 MiB shard partitioning, violation/source identity closure, digest verification, publication and rendering/counts. Move these into specific normalization command-contract, inventory serialization/sharding and publication owners. Reuse existing canonical JSON/hash primitives when semantically identical, but preserve normative byte ordering and protocol output. There is no need to retain exported domain APIs in the command file.

MutationTaxonomyWorkflow and SourceFileFacts begin around 14667 and 14863. They contain schema capture, source admission, a captured source index, immutable source facts and assignment ledger resolution, mutation owner/consumer graph evidence, inventory planning and CLI execution. These belong under explicit normalization/mutation concerns, with source capture reusable through the actual repository discovery owner. Do not turn a source-language path or the existing region name into the domain. Preserve injected capture seams, stable source-tree digests, mount/re-export origins, canceled/unreadable/symlink behavior, guarded plan authority and unresolved assignment evidence. The SourceFileFacts region also contains CleanScript and unrelated cleanup functions; inspect lexical dependencies and leave cleanup for its own lifecycle lane.

The root JsonSchemaSubset region around 23015–23077 contains five actual validator functions, not the 444-line editorial range in the broader region map. Its public validateJsonSchemaSubset depends on canonicalJson and owns the repository Draft-07 subset. The subsequent mutation structural descriptor, aggregate union, native Rust tag and reachability code is a different domain. Move the validator to a schema validation concern, reuse it from mutation descriptor validation, and do not replace it with an external runtime validator. Preserve existing subset semantics and independent AJV comparisons; this source extraction is not authorization to silently widen the supported JSON Schema dialect.

Mutation structural source view and root reachability around 23173 onward depend on the same captured source index, Rust module/structure/metadata facts and exact current taxonomy. Close that dependency graph when separating inventory and structural policy owners. The contract proves an unconditional direct leaf mount and wrapped payload origin; a path substring or a new graph reread is not equivalent evidence. Other repository law families remain later slices.

## Actual Consumers And Existing Oracles

The repository library test at 🧪️tests/💠️inventory-artifact-shards/🟦️.ts dynamically imports root APIs in eight test blocks. Rebase each actual import to the canonical ownership split. It already compares stable serialized bytes with fast-json-stable-stringify and native WebCrypto SHA-256, validates fixture shapes with AJV, and exercises closure, bad digest, duplicate/source identity, metadata retention and private publication. Its current default ticketRoot points to an older normalization ticket; run with the existing supported artifact-root seam or correct test-output ownership to this task's generated root without removing retained older inputs.

The actual workspace contract at 🧪️tests/🔬️workspace-contract/🟦️.ts imports inventoryMutationTaxonomy, planMutationTaxonomy, runMutationTaxonomyCli, inspectMutationRootReachability, policyMutationStructuralBreaches, MUTATION_STRUCTURAL_POLICY_KINDS, newScaffoldMutationTree and validateJsonSchemaSubset from root. The first seven relevant contracts must follow their real owners; scaffold extraction is distinct unless required by actual dependency closure. Existing cases around 7285–7662 cover AJV parity, assignment/source epochs, captured-source races, symlinks, stale plans, terminal inventory and native Rust payload origins. The separate 🧪️tests/📸️source-index-capture/🟦️.ts plus schema/fixture at 🧬️schema/📋️mutation-inventory/📸️source-index-capture and 🧫️fixtures/📋️mutation-inventory/📸️source-index-capture preserve exact one-capture authority.

Search the current full source population for static, dynamic and source-as-data consumers before cutting declarations; the references above are confirmed consumers, not a closed global count. Root CleanScript must delegate to the owned implementation. Its exact mandatory filename stays a router path. Register all new semantic ancestry in current taxonomy and keep portable owner/consumer fixtures ahead of moves.

## Verification

The later 📓️root-native-declaration-dependency-review-2026-09-12.md gives actual installed-TypeScript symbol closures from the current root snapshot: shard protocol24 local declarations/382 declaration lines, mutation workflow and structural evidence101/1,088. These overlap and are not intended as single files. There are no multi-declaration SCCs in that local graph, but imported/dynamic/native/source-as-data consumers remain separate review obligations. Use its exact dependency list to move common primitives below both workflow and policies; do not create reverse imports to the root command file. The report also bounds distinct cleanup and scaffold follow-ups.

Start with schema/portable ownership and native TypeScript AST controls, retain existing behavior vectors and run focused registered inventory/source-capture/mutation contract checks. Preserve protocol and generated hashes; source-body SHA values may be move evidence only. Use ticket-private output roots, never execute live cleanup, mutation apply or shared transaction replay merely to validate the move. Audit root command imports, actual file references and strict/scoped taxonomy afterward. Record concrete results, failures, exact owned paths and native limits in 📓️sol-root-taxonomy-workflow-extraction-2026-09-12.md. Keep scratch below ticket 🗑️generated/sol-root-taxonomy-workflow-extraction and remove disposable output when finished.
