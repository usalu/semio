# Sol Print Macro Staging Execution — 2026-09-12

## Disposition

The first bounded print macro lane is complete. The graph implementation now lives at the semantic owner `🧰️framework/🛍️products/📓️print/🔨️modules/🕸️graph/📐️.tex`. The repository's fixed `semio-graph.sty` identity is a three-line native package shim. Each production publication stages the caller document and the explicit print library into separate invocation-local roots, maps the exact canonical graph leaf to compiler-only `_.tex`, and passes the staged library through the light, dark and panel compiler paths.

The graph implementation bytes, visual dimensions, styles, opacity, registry vocabulary and token authority were preserved. No compiler-generated `_` leaf was added to the repository. The remaining 96 named LaTeX contracts remain separate reviewed debt.

## Authored File Attribution

These paths contain this lane's authored additions or localized edits. Shared files may also contain concurrent work outside the cited concern.

- `.vscode/🧩️launch.seed.jsonc` — registered `🧪️test print macro staging` in the source launch registry.
- `.vscode/launch.json` — derived launch output regenerated through the repository generator; this lane contributes the matching macro-staging entry.
- `♻️mit-bestand/📋️bericht/📦️packages/🟦️typescript/📋️project.json` — declared the graph implementation leaf in all three report builds and the three report test targets.
- `♻️mit-bestand/📋️bericht/📦️packages/🟦️typescript/📜️script.ts` — added quick producer-input assertions for the graph implementation leaf.
- `🧰️framework/🛍️products/📓️print/🎮️commands/🧪️print-pipeline-verification/🟦️.ts` — added the permanent `test macro` command route.
- `🧰️framework/🛍️products/📓️print/🎮️commands/🧪️print-pipeline-verification/🧪️tests/🖨️pipeline/🟦️.ts` — added portable staging, source-kind, shim-gate and native production-seam controls; corrected the gallery fixture reference to its current semantic owner.
- `🧰️framework/🛍️products/📓️print/🎮️commands/🧪️print-pipeline-verification/🧫️fixtures/🖨️macro-staging/🧬️schema/🔣️.json` — new portable fixture schema.
- `🧰️framework/🛍️products/📓️print/🎮️commands/🧪️print-pipeline-verification/🧫️fixtures/🖨️macro-staging/🔣️.json` — new portable staging, collision, ownership and hostile-shim cases.
- `🧰️framework/🛍️products/📓️print/🎮️commands/🧪️print-pipeline-verification/🧫️fixtures/🖨️macro-staging/🧪️control.tex` — new native graph registry control document.
- `🧰️framework/🛍️products/📓️print/📦️packages/🟦️typescript/📋️project.json` — registered `test-macro-staging` with its exact source, schema, taxonomy, compiler and tool inputs.
- `🧰️framework/🛍️products/📓️print/🔨️modules/📥️source-staging/🟦️.ts` — implemented exact `📐️.tex` to `_.tex` projection, token-aware reference rewriting in `.tex`, `.bib`, `.sty` and `.cls`, and pre-mutation collision rejection.
- `🧰️framework/🛍️products/📓️print/🔨️modules/🔣️.json` — registered the semantic graph module.
- `🧰️framework/🛍️products/📓️print/🔨️modules/🕸️graph/📐️.tex` — new canonical graph macro implementation owner.
- `🧰️framework/🛍️products/📓️print/🔨️modules/🖨️tectonic-template-compilation/📇️catalog/🔣️.json` — declared the exact shared library sources, `🖋️latex` and `🔨️modules/🕸️graph/📐️.tex`.
- `🧰️framework/🛍️products/📓️print/🔨️modules/🖨️tectonic-template-compilation/📇️catalog/🧬️schema/🔣️.json` — required a non-empty unique portable `librarySources` catalog.
- `🧰️framework/🛍️products/📓️print/🔨️modules/🖨️tectonic-template-compilation/📇️catalog/🟦️.ts` — validated and exposed `printLibrarySources()`.
- `🧰️framework/🛍️products/📓️print/🔨️modules/🖨️tectonic-template-compilation/🟦️.ts` — stages the explicit library independently of each caller source root and supplies only staged library roots to compiler search paths and `TEXINPUTS`.
- `🧰️framework/🛍️products/📓️print/🖋️latex/semio-graph.sty` — reduced the fixed native identity to declaration plus delegation to the canonical semantic leaf.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔣️taxonomy.json` — registered the exact graph-owner TeX source context and constrained fixed TeX package-glue disposition.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️.ts` — recognizes scoped implementation kinds for basename enforcement and classifies only literal, inert TeX package delegation as the fixed shim contract.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts` — admits registered scoped roles and resolves their optional exact parent constraint while preserving the canonical basename decision.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🟨️.mjs` — projects the print catalog's validated `librarySources` into ordinary producer inputs.

## Test-Driven First Reds

The first portable staging control expected `graph/_.tex` and observed `graph/.tex`, proving the generic grapheme stripping was not a valid compiler projection. The initial catalog control could not import `printLibrarySources()`, proving there was no explicit shared-library producer authority. The initial source-kind control returned no scoped implementation kind for the graph owner and no fixed-body decision for the TeX shim.

A later reference control observed bare `📐️` prose rewritten to `_`; the replacement was narrowed to the exact source-relative canonical token. Hostile owner basenames then showed `📐️reconcile.tex`, `reconcile.tex` and `_.tex` falling back to generic TeX documentation with no basename finding; scoped role recognition now covers direct TeX leaves under the exact graph owner while canonicality independently requires `📐️.tex`.

The first literal-shim control accepted a `\def` hidden in `\ProvidesPackage` metadata. Coordinator probes also showed that `^^5c` and `^^2e^^2e` TeX encodings crossed the intended literal command and traversal boundaries. The repaired classifier requires NFC literal paths, inert metadata, non-empty path segments, and rejects control syntax, backslashes, carets, dot segments and TeX-active path syntax before assigning the thin-delegation disposition.

All raw red logs were disposable lane output under `🗑️generated/sol-print-macro-staging`; their exact failure conditions are retained above and in the permanent portable fixture.

## Portable Contract Coverage

The schema-validated fixture covers a root document delegating through the named class/package identity to `🔨️modules/🕸️graph/📐️.tex`, with the expected staged graph leaf `modules/graph/_.tex`. It verifies full and extensionless references in `.tex`, `.bib`, `.sty` and `.cls`, unchanged unrelated `.txt` bytes, unchanged bare `📐️` prose, and atomic rejection of canonical/existing-output, repeated-entry, and directory/file collisions.

Ownership controls classify only direct TeX leaves under the semantic graph owner as `scoped:print-graph-macro-source`. The canonical leaf passes; `📐️reconcile.tex`, `reconcile.tex` and `_.tex` produce the required `📐️.tex` basename finding; neighboring TeX documents remain documentation. Shim controls accept the current Unicode literal delegation with comments and optional `\endinput`, and reject runtime definitions, composition with a second input, metadata `\def`, literal-backslash targets, `^^5c` command encoding, `^^2e^^2e` traversal encoding and expanded `\csname` targets.

## Executed Verification

| Invocation | Result | Evidence |
| --- | --- | --- |
| Direct `verifyPrintMacroStaging()` after the final literal repair | Pass | `[DEBUG] Print macro staging: schema, references and collision atomicity PASS` |
| Exact graph-owner taxonomy inventory | Pass | `📐️.tex` classified `scoped:print-graph-macro-source`; canonical path unchanged; zero findings. |
| `bun nx run @semio-tech/print:test-quick --skip-nx-cache` | Pass, 3.1s | Command boundaries, 87-target catalog, bundle, toolchain, fonts, macro staging, visualization coverage and unit controls passed. |
| `bun nx run @semio-tech/repo-lib:test-package-body-policy --skip-nx-cache` after the literal repair | Pass, 58.0s | 121 tests, 530 expectations, zero failures. |
| `bun nx run @semio-tech/mit-bestand-bericht:test --skip-nx-cache` | Pass | Report source routing and producer/Nx graph-source input contracts passed. |
| `bun nx run @semio-tech/plugin-registry:generate` | Pass | Derived launch configuration contains the registered macro-staging target. |
| Final `bun nx run @semio-tech/print:test-macro-staging --skip-nx-cache` | Pass, 2m52s | Fonts, locked Tectonic and TeX preparation, generation, two native publications, byte identity and PDF.js marker passed. |

The broader `@semio-tech/repo-lib:test-kind-only-basename` target reached its existing 15,000 ms command-router budget after its first six tests. A direct diagnostic reproduced the same budget termination rather than an assertion failure. The exact graph inventory, hostile basename fixture, package-body suite, portable print target, and independent coordinator probes cover this lane's focused contracts.

## Native Runtime Evidence

The registered native control used prepared Tectonic 0.16.9 and the locked offline bundle. It invoked the real `publishPrintArtifact` seam twice with separate temporary roots and `dark: false`. Both builds loaded the staged package shim and semantic graph owner, exercised `\SemioGraphState` and `\SemioGraphEdgeKind`, and emitted byte-identical PDFs:

`3226b206b9f90b1a3336df7c8238265d7a97f79ae38077bfd30b6da4095ed64a`

PDF.js independently extracted the expected `Graph macro staging reached` marker. This proves the graph registry macro path through native compilation; the fixture does not draw a graph.

Root independently reproduced the portable verifier and the hostile owner-name decisions. Root's separate pinned Tectonic control established that `\input{^^2e^^2e/_.tex}` really loads a private parent target and logged `[DEBUG] ENCODED PARENT TARGET REACHED`; its authored input remains under `📋️latex-literal-controls`, with the review summarized in `📓️coordinator-tex-shim-literal-review-2026-09-12.md`. That oracle supports the repaired caret rejection and was not rerun in this lane. Terra's read-only acceptance in `📓️terra-print-macro-staging-audit-2026-09-12.md` independently checked portable staging, actual-catalog materialization, registered producer inputs, the literal boundary, and the native result.

## Production and Evidence Limits

The native fixture ran only with `dark: false`. Current code carries the same staged library root through the dark branch and panel recompilation, but this lane did not execute a native dark or panel control. The fixture uses its own document `sourceRoot`; Mit Bericht uses a separate caller root through the same publication seam, and its producer inputs and router contract were verified without a native Mit PDF build. No live PDF output or all-document/all-97-macro compilation was performed.

Disposable PDFs, compiler logs, Nx caches and direct-probe output lived only under this lane's ticket-local `🗑️generated/sol-print-macro-staging` root and are removed after retaining this report. Authored fixtures, schemas and native control source remain.
