# Language-Named Directory Spot Audit

The coordinator performed a no-follow filesystem sweep for exact language-named directory segments outside `📦️packages`, excluding dependency/build caches and ticket state. It visited 162,034 entries in 5.738 seconds and found five candidates below. This is a lexical screen followed by source inspection; it does not prove every contextual directory valid, inspect compiled outputs, or classify arbitrary compound directory names.

- `temp/compose/client/lib/go`: preserved intake/source provenance is already assigned to the tooling/Mit-Bestand packets. Do not delete or restore this input as an active product based only on the language segment.
- `✏️s/🔌️plugins/🎞️animate/🎛️apps/🎬️presentation/⚡️implementations/🟦️typescript`: existing obsolete implementation container is already assigned to package metadata/source ownership. It requires a real neutral owner and consumer closure.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/⚡️caching/📦️artifacts/🟦️typescript`: contains `📜️script.ts` that builds, checks and tests TypeScript declaration artifacts through tsc/Bun and a declared artifact source. TypeScript here identifies the artifact/toolchain subject.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/⚡️caching/📦️artifacts/🦀️rust`: contains a TypeScript `📜️script.ts` routing native Cargo artifact builds/checks/tests. The source language already differs from the Rust domain selector, directly demonstrating that this directory names the toolchain subject rather than enclosing Rust implementation leaves.
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🕸️dependencies/🟦️typescript`: contains anonymous JavaScript `🟨️.mjs`, schema and fixtures implementing declared JS/TS module input-graph resolution via the TypeScript parser. Its typed-language subject does not prevent another implementation language. Preserve native input-graph semantics and assess exact domain/context authority; do not mechanically rename it merely because a language appears in the subject name.

The three repository tooling concerns are not the inverted package/implementation container demonstrated in the user's WGPU example. Their task bodies still require the separately queued mandated-script/source review where applicable. Their neutral domain roles can remain explicit while implementations change language. The global contextual audit must validate their exact registered parent/member contracts; this spot audit does not grant a broad language-folder exemption.

Ignored directory basenames for this sweep: `.git`, `.pytest_cache`, `.venv`, `.🧬semio`, `__pycache__`, `coverage`, `dist`, `node_modules`, `storybook-static`, `target`, `📤️dist`, `🔌️plugin-modules`, `🗑️generated`. Native package `pkg`, TypeScript `out` and .NET `obj` were also skipped as exact build-state paths already covered by the persistent-output/metadata lane.
