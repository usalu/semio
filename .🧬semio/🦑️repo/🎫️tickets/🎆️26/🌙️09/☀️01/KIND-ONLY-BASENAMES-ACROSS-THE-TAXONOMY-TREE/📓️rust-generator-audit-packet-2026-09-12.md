# Rust Generator And Oracle Independent Audit

Use Terra Extra High for a bounded read-only review after 📓️sol-rust-generators-2026-09-12.md is complete. Read that report, the original execution packet, the source identity map and exact consumers. Keep probe output under ticket 🗑️generated/terra-rust-generators and retain 📓️terra-rust-generators-2026-09-12.md. Do not mutate source, schema, configuration, Git, ticket or goal lifecycle, and do not interrupt other workers.

Verify semantic concern ownership of the 45 relocated generator/probe crates, plus the stdio oracle package body's extraction. Every native package identity and explicit lib/bin target must be retained; duplicate generate-bin names were the reason not to merge sibling packages. Cargo metadata belongs under each concern's ordinary packages/rust boundary, with domain source outside the package. The older generator emoji spelling must have one canonical replacement and no compatibility alias. Inspect newly registered codec/generate kinds and removal of generator-crate/cargo src/build/probe shortcuts against their actual remaining consumers.

Independently verify the reported move map and pre-move hashes with path-only source normalization where consumer mounts changed. Re-resolve literal source mounts and Cargo dependency/target paths, including semantic code/fixtures/readers and the stdio oracle. Use an installed Cargo/TOML parser as the independent metadata oracle. Run the portable kind-only fixture and a bounded native nontrivial generator test or actual generator invocation from the current registered route; avoid repeating all native suites absent a defect.

The executor reports native tests passing for GIF, BMP, OBJ and PNG and compile-only/no-test results for equation and note. Stdio ran 32/33 with a DWG catalog no-mutation assertion failure; path-statutes ran 32/37 with five oracle/mutation/generated-asset failures. Verify exact current evidence and identify any causal relation to this lane without assuming baseline provenance. A native compile or zero-test run does not prove fixture output semantics.

Use scoped inventoryTaxonomy on representative relocated concern owners to verify contextual semantic directory admission, package metadata contracts and anonymous implementation leaves together. Distinguish newly introduced findings from ambient ancestor registrations, and retain exact remaining source ownership gaps rather than weakening the gate. Verify strict taxonomy authority loading and that no active producer/consumer still targets obsolete generator/crate/src paths. Historical reports and explicitly frozen evidence are not active consumers.

Finish with actionable findings, bounded passed checks and unresolved limits. Do not wait for global source enforcement or run the historical full-root planner. If a shared live schema edit temporarily prevents one check, complete independent checks and retry that one dependency once after coordination rather than treating a transient file as a proven lane regression.
