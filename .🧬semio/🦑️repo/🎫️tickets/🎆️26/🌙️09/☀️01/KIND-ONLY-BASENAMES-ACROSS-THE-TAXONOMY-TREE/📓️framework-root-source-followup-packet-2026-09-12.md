# Framework Root Package Source Ownership

This is a separate bounded source lane after the current framework/UI move batch. The fresh body census also retains two root framework package bodies that are not included in that lane's 23-path overlap list. Do not silently leave them as package glue merely because their basenames are already anonymous.

- `🧰️framework/📦️packages/🦀️rust/🦀️.rs` contains 2,176 lines. The first 1,979 lines after its imports are the `typegen`-gated inline `schema_metadata` module: real SchemaMetadata types, a large generated-language declaration catalog and collection logic. The remaining native root primarily mounts semantic owners and reexports APIs; inspect the inline interaction mount before deciding its actual role. Keep the Cargo package identity `semio-framework` and feature gating, and move authored metadata/catalog behavior to a neutral schema/projection owner with an explicit path mount. Trace its real producer consumers and ordered generation authority before moving. There is no need to rewrite the catalog's semantic content to correct source ownership.
- `🧰️framework/📦️packages/🟦️typescript/🟦️.ts` contains 417 lines at this checkpoint. Its package reexports wrap real implementations: `Lease`/`LeasePool` and createLeasePool (lines 89–215), jittered retry/backoff and abortable delay (217–282), latest-wins serialization (284–321), fetch-with-timeout (323–367), and wait-for-event (369–410). The file ends with an existing inline Vitest registration. Identify the existing async/resource/event owners and extract these coherent operations into adjacent anonymous TypeScript leaves there. Reuse semantic kinds/owners and tests; avoid inventing one catch-all framework implementation folder. Preserve the package API by explicit current reexports and keep required test registration thin, or move the test entry to its exact test owner when native tooling permits.

Start with the existing portable fixtures and a language-agnostic ownership case validated by installed TypeScript/Rust parser or compiler tooling. Capture pre-move content hashes and active referents, including import-meta roots and dynamic test imports. Verify actual behavior using the focused lease/backoff/timeout/event tests and the native typegen metadata consumer/check route. No new runtime dependencies, migration or compatibility files, AGENTS/Git/lifecycle mutations. Use Bun/Nx and existing launch seed/derivation for any command changes. Preserve all concurrent framework and OS work. Retain `📓️sol-framework-root-source-2026-09-12.md` with exact moves/support paths and scoped taxonomy/native outcomes; disposable output only under ticket generated.

## Current Executor Map and Browser Closure

Sol’s concrete map moves the Rust schema metadata/typegen body to schema/📽️projection/🦀️.rs; TS concerns to async/🎟️lease-pool, async/🔁️jittered-backoff, async/🥇️latest-wins, async/🔔️event-wait and io/🌐️fetch-timeout, each with 🟦️.ts. The root package retains explicit exports/native test registration and Rust cfg/path wiring. Validate all new contextual owners and actual ancestor chains. Root framework TS belongs to the WGPU browser graph: inspect exact declared source-input/profile authority, update current ordered rows when required by actual new imports and regenerate/check the frame worker through its normal owning route. Coordinate those rows with the active Terra OS audit to distinguish in-flight and settled evidence.
