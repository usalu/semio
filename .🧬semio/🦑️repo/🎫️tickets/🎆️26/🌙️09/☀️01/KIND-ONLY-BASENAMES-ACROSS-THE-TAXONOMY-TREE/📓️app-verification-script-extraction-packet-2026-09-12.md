# App Verification Script Extraction

This is a queued Sol execution slice from Terra's mandated-script review. Root re-read the four current command sources and their concrete consumers on 2026-09-12. It is not a completed refactor or runtime acceptance report. Preserve concurrent work and re-read all sources before moving them. Use schema-first portable topology/behavior fixtures, installed third-party or native oracles, anonymous source leaves and exact ancestor registration. Keep body hashes in the report as provenance; do not freeze implementation bytes in permanent tests.

## Mathematical Publication Authority

Source: `✏️s/🔌️plugins/➗️mathematical/📦️packages/🟦️typescript/📜️script.ts` (52 lines at review). This contains `Lane`, `Fixture`, `exact`, `oracle`, schema construction, production Rust parsing, route/authority assertions, three hostile source mutations and strict Ajv checks. Extract to the existing publication-authority verification concern beside its fixture, with an anonymous TypeScript leaf. Separate the package metadata assertions only if their actual domain ownership warrants it; avoid a generic utility owner.

The existing fixture is `➗️mathematical/🧫️fixtures/📣️publication-authority/🔣️.json`; the schema export is `MathematicalPublicationAuthority` in the plugin schema. Preserve its three hostile source controls and the two existing equation example tests. `@semio-tech/mathematical-js:test` is registered in the launch seed; retain the command contract and prove the router invokes the moved oracle. Ajv is already a test dependency; do not expose its types in the owned API.

## GIS Verification

Source: `✏️s/🔌️plugins/🌍️gis/📦️packages/🦀️rust/📜️script.ts` (509 lines at review). The actual plugin directory is `🌍️gis`, not `🗺️gis`. Five public proof functions belong beside their existing native-codec, controlled-proposal, map-region, durable-three-store and component-cold-map fixture/test concerns. The schema compiler is shared verification infrastructure and must not become an exported Ajv API.

The public functions are `proveGisNativeCodecReceipts`, `proveGisControlledProposal`, `proveGisMapCreateRegionGroup`, `proveGisDurableThreeStoreAssembly` and `proveGisComponentColdMapPatch`. The local `freshGisComponentBuildControl` and component production/check orchestration also contain actual cancellation/deadline/staging behavior; move to the appropriate component acceptance owner if the router would otherwise retain that body. Preserve exact Cargo law names, artifact ownership, cleanups, error conditions and optional feature arguments.

Close the Hub static import of `proveGisComponentColdMapPatch` and the actual Hub callers. At review the Hub source also inspected the native gate for the literal `await proveGisComponentColdMapPatch(this.repoRoot)`; after extraction it must inspect the real owner/consumer contract rather than silently retaining a script-body assertion. Check every source-as-data expectation before updating it.

The GIS source imports `describePluginComponent`, `produceFreshComponentV1` and `FreshBuildControlV1` from the descriptor command source. The queued publication slice moves those APIs; coordinate the fresh referents and do not retain script facade exports. The imported descriptor types are first-party control interfaces.

Run the bounded source/oracle routes and at least the affected exact native law set through registered Nx commands. Do not equate a source check with actual fresh component production, browser acceptance or Hub publication. Keep any independent current native failure with concrete diagnostics and no unproven baseline attribution.

## VCS Verification

Source: `✏️s/🔌️plugins/🌿️vcs/📦️packages/🦀️rust/📜️script.ts` (157 lines at review). Move `compileVcsScopeExport`, `proveVcsNativeCodecReceipts`, and the native-openable-identity method's schema/hostile/protocol/source checks to their neutral verification concerns. Preserve both Node/WebCrypto digest parity and the native receipt fixtures; these semantic output hashes are legitimate assertions, unlike a snapshot of the verifier's own body.

Root found a second Hub API consumer not emphasized in the earlier Terra map: the Hub Rust command source dynamically imports the VCS command file and calls `proveVcsNativeCodecReceipts` (around line 5433 at review). Redirect this dynamic import to the actual anonymous owner, in the same batch. Preserve descriptor API handoff with the publication lane.

The existing `--oracle-only` split permits bounded source/fixture checks independently of native compilation. Preserve the split and clearly report which route actually ran. Native checks enumerate two `native_codecs` laws and one `native_openable_identity` law. Update exact consumer contracts and launch/project registration as necessary.

## Energy Oracle Toolchain

Source: `✏️s/🔌️plugins/🔋️energy/🔮️oracles/📦️packages/🐍️python/📜️script.ts` (171 lines at review). The command source owns platform resolution, pinned archive parsing, cache path selection, streaming hash verification, download/extraction, environment assembly and Python dispatch. These are toolchain/provisioning and execution concerns under `🔮️oracles`, implemented in anonymous TypeScript leaves. Retain only their task routing in the required script.

`PACKAGE_DIR = import.meta.dir` currently anchors `🔣️.json`, the uv project and the Python module `🔮️oracles.🐍️`. Rebase these against the real package container after extraction. The separately queued plugin source lane may move the Python implementation out of its package; coordinate that native module selector rather than creating a compatibility module or duplicating implementation. `deps` remains Bun/Nx orchestration over the existing uv locked Python environment, not a replacement package manager for this repo's JS.

Eight existing Nx targets route through this one command source: setup/status/run/native/epjson/emit/test plus deps. The Python selftest intentionally avoids running EnergyPlus. Use it and fixture-based platform/archive/hash/selector checks for bounded validation; installation or simulation should be run only when needed to prove a changed producer contract, with progress/cancellation and ticket-owned outputs. Do not count a missing toolchain as a passing simulation. Preserve actual platform tables and integrity checks; no blanket generated/cache exemption.

## Acceptance And Report

Coordinator current prerequisites are retained in 📓️app-verification-prerequisites-2026-09-12.md. Both VCS oracle routes and four of five GIS source/fixture proofs passed. Mathematical test currently stops on six fixture routes versus schema minItems seven; its production WindowConfig enum also exceeds the verifier's old Artifact/Config pattern. GIS map proof stops on the unregistered x-semio-child-kind schema annotation. Read the exact evidence and align extracted verifiers to real current authority with strict, meaningful hostile controls; no invented actions, compatibility forms or blanket strict-mode disabling. These are source-oracle results, not native law execution.

Record the exact source/consumer/context map, before/after routing semantics, portable positives and hostile negatives, native and registered command results, and remaining limitations. Close all script API consumers; anonymous implementation owners must resolve through their full real ancestor chain. The script classifier must admit only the remaining thin routing and reject the original behavior-bearing bodies. Re-read the live policy after its adversarial correction; do not weaken it to accept retained oracle bodies. Record all exact changed files and clean only this lane's generated output when finished.
