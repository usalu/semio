# Current Vitest Configuration Consumer Baseline

Root evaluated the exact43 current `vitest.config.ts` inputs from the previously parsed configuration population through installed Vite7.3.6 `loadConfigFromFile`, explicitly selecting its `native` loader under Bun. This evaluated each actual export and its imports without Vite's default bundled-loader temporary module writer. The sequential run completed in59.038seconds:42 configurations loaded and one failed. It ran no Vitest cases, browser, package build, default bundled-loader suite, or editor integration. No product source was changed by this investigation.

## Concrete Load Failure

`♻️mit-bestand/🎤️präsentation/📅️33.projektetage/📦️packages/🟦️typescript/vitest.config.ts` cannot import `semioAssetsVitePlugin` from the styling root `🟦️.ts`, which now exports only theme. The actual builder declaration is under styling `🏗️builder/🌐️vite/🟦️.ts`. The sibling `⚙️vite.config.ts` has four builder imports from that same wrong barrel. Root read both sources and the actual export owner, then assigned a direct consumer repair to the app/print Sol executor. No compatibility barrel should be restored. That executor will record post-repair native loader evidence and exact attribution separately.

## File-Selection Projection

For the42 loaded exports, root resolved their configured test root (or config root/cwd when absent), explicit include patterns, setup paths and absolute aliases. Installed `fast-glob` enumerated existing files with symlink following disabled, and `node:fs` checked exact referenced paths. This is filesystem projection, not Vitest collection or evidence that an alias is exercised. It produced59 explicit include patterns and67 unique matched paths;15 patterns matched nothing. Configurations relying on `includeSource` or default patterns are not fully measured by this explicit-include projection.

| Configuration concern | Observed path issue | Required follow-up |
| --- | --- | --- |
| Animate | Literal presentation-spec alias points at the absent presentation package `🟦️.ts`. | Inspect the current authored spec owner and actual consumers; keep this with the presentation configuration closure. |
| 2D | Literal Flow alias points at `framework/🔨️modules/🛍️products/…/flow_core.js`, which is absent. | Rebind through the actual Flow projection producer and consumer authority from its separate packet; do not invent a substitute module. |
| Hub admin | Explicit admin test include and environment setup resolve into `hub/🔨️modules/🧪️tests`, where they do not exist. | Resolve the actual admin test/environment owner during Hub/configuration closure and run its registered route. |
| OS MCP | Explicit test glob resolves into OS `🔨️modules/🧪️tests`, yielding no files. | Inspect current MCP test owners and actual registered routing; preserve real-process limits. |
| WGPU TypeScript package | Twelve explicitly named browser/worker/integration test patterns yield no files at the configured root. | Compare the already moved owners against actual runner/config consumers; a loaded config is not a passing suite. |
| Remodel | The schema-oracle include pattern yields no files; its example pattern does match. | Determine whether that additional schema case was moved, is absent, or is no longer an active declared source. Do not manufacture an empty passing oracle. |

Two additional apparent alias misses are **not findings**: CAD and Infinite R3F use regular-expression replacements ending in `three/examples/jsm/$1`. The `$1` is a substitution placeholder, not a literal file. The raw JSON serializes regular-expression `find` objects as `{}`; it must not be used to claim malformed alias configuration or nonexistent literal dependency paths.

Root subsequently located actual replacement test owners without changing them: Hub admin's test/environment are below `admin/🧪️tests` (the config climbs one parent too far); OS MCP's cases are below `mcp/🧪️tests` (same extra parent); Remodel's schema oracle is under `🧬️schema/🧪️tests/🧩️suite/🟦️.ts`, adding the omitted suite concern. Eleven specifically named WGPU cases now exist below renderer `🧑‍🎨engine/🧪️tests`; the package router's `test-browser-worker` and `test-preview-generated` still forward package-local case arguments into `runVitest`, so this is an active command consumer closure as well as a config literal. The generic `package-integration` twelfth identity needs exact discovery before choosing its owner; do not bind it by basename alone. The presentation has a real `🧪️tests/🧪️projektetage-deck/🟦️.ts` and a named `📦️index.ts`; selecting the true spec API requires inspecting exports, not simply treating either file as a replacement alias.

## Additional Vite and Playwright Sources

The earlier focused50-configuration population excludes four explicitly named Vite inputs and one Playwright input. Root read all five current sources and parsed them with installed TypeScript: zero syntax diagnostics,73 top-level statements,463 newline segments. These are additional to the named styling Tailwind preset, bringing the known configuration-source review population to56. This is a scoped population, not a claim that every possible tool configuration in the repository has been enumerated.

| Source | Current size | Ownership/consumer boundary |
| --- | ---: | --- |
| `♻️mit-bestand/🧺️demonstrator/⚙️vite.config.ts` | 109 lines | Runtime-target/asset composition, registry module roots, activation receipts and build/dev plugin selection; preserve existing semantic imported producers. |
| `♻️mit-bestand/🧺️demonstrator/🎭️playwright.config.ts` | 52 lines | Test-host configuration requires `PLAYWRIGHT_BASE_URL` from the Nx-prepared service. A direct unset-env import is intentionally rejected; preserve the actual managed-service consumer rather than adding an arbitrary fallback server. |
| `♻️mit-bestand/🎤️präsentation/📅️33.projektetage/📦️packages/🟦️typescript/⚙️vite.config.ts` | 46 lines | Presentation builder-import repair is assigned; physical configuration ownership and current aliases remain separately governed. |
| `🌎️hub/🔨️modules/🛡️admin/📦️packages/🟦️typescript/⚙️vite.config.ts` | 35 lines | Admin build root, aliases and static output must move with the actual source coordinate and command selector. |
| `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/📦️packages/🟦️typescript/⚙️vite.config.ts` | 221 lines | Actual runtime/publication/distribution composition includes receipt selection, module roots, source-data package reads and session imports; reuse the extracted domain owners, retain distinct dev/build inputs and avoid hiding this behavior in a required shim. |

The Vite configurations were read and parsed here, not loaded or executed; the sole earlier failing native load was the presentation **Vitest** configuration. The Playwright source was also not executed, and no service or browser was started. Exact source counts are disposable `additional-vite-playwright-population.json` beside the other native configuration records.

## Provenance and Acceptance Limits

Disposable full records are `🗑️generated/coordinator/tool-config-controls/current-vitest-native-evaluation.json` and `current-vitest-config-path-projection.json`. The43 paths were read from the retained prior current-population probe; concurrent future edits require focused revalidation, not immutable counts. This baseline does not establish a reason for any conventional basename exemption. The queued taxonomy move still must close explicit runner/default selectors, exact source-token tests, native roots, editor discovery and registered command inputs. The current root configuration remains deliberately empty and must not become a global aggregator.
