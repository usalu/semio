# Framework Owner Registration Correction

Execute with the original Sol framework source worker after Terra’s bounded audit frees a slot. Read the final `📓️terra-framework-source-audit-2026-09-12.md` first for any additional causal defects, and the exact source/support identities in `📓️sol-framework-source-2026-09-12.md`. Do not repeat the already completed source moves or global suites.

Terra’s independent scoped inventory found all 23 newly renamed render test-owner directories unresolved under `🧰️framework/🔨️modules/🖱️ui/🖌️render/🧪️tests/🔬️{d3d12,metal,vulkan,webgpu}-…`. Their anonymous Rust leaves are present, old owners absent and source/native mounts correct, but the semantic owner registration is incomplete. The render scope has 79 entries and 37 unresolved directory findings: 23 match this move map; 14 are ambient to the map, with no pre-existing baseline claim. Additionally, the extracted `🧰️framework/🔨️modules/🖱️ui/🎨️styling/🏗️builder/🌐️vite` directory is unresolved. These are 24 directly owned failures, independent of source compilation.

Exact current evidence is retained by Terra at ticket `🗑️generated/terra-framework-source/direct-owner-taxonomy-findings.json` and `taxonomy-scope-summary.json`; the final Markdown report must retain every actionable path before disposable output cleanup. Add precise contextual semantic member authority using the live parent kinds and declared domain identities. Do not allow arbitrary test slugs, generic emoji prefixes or all children of a member owner. Extend the portable framework topology check to exercise every newly introduced owner and actual ancestor context; a source basename check or a subset of new owners did not catch this defect.

Re-run strict taxonomy validation and scoped inventories for all 24 owned paths/ancestors, plus the focused registered topology target. Preserve the production Storybook Vitest sentinel gate, Metal backend-testing semantics, current WGPU input/output lists and the live session isolation boundary. A second full Storybook preview is unnecessary for a pure taxonomy registration correction unless another audited code defect changes the browser graph. Any remaining ambient vocabulary goes to `📓️contextual-directory-closure-packet-2026-09-12.md` with exact current counts; do not silently accept it as globally clean.

Retain changes and executed checks in `📓️sol-framework-owner-registration-correction-2026-09-12.md`. Own scratch only ticket `🗑️generated/sol-framework-owner-registration`; no Git, AGENTS, lifecycle changes, broad exemptions or compatibility names. After this correction is complete, root will assign the font/assets script extraction slice separately.
