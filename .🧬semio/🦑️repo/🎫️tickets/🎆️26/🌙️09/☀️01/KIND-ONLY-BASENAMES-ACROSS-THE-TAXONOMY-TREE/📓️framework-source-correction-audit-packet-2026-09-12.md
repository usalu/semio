# Framework Source and Browser Boundary Audit

Use a Terra Extra High read-only slot after `📓️sol-framework-source-2026-09-12.md` is complete. Bound the work to 15–20 minutes and return exact accepted scopes plus causal actionable defects. Preserve all shared source/schema/output state; no source edits, regeneration, Git, AGENTS or lifecycle changes. Own disposable audit transcripts belong under ticket `🗑️generated/terra-framework-source`; retain `📓️terra-framework-source-audit-2026-09-12.md`.

Review the exact move/support map and source hashes from the completed report and portable topology fixture. Verify actual anonymous leaves outside language package roots, semantic ownership/parent contexts, native Rust mounts and proc-macro target paths, JS/TS package glue, CSS import closure and generated styling producer consumers. The separate root framework package bodies and 138 mandatory-script cases are explicitly queued; do not misreport them as omitted from this bounded batch or widen the audit to execute their decomposition.

Use existing native parsers/compiler evidence and focused registered fixtures to verify source/consumer identity, not only basename shape. Check the 23 old render test-owner names containing `packages-rust`: old active mounts and fixture coordinates must be absent, current tests under neutral concerns and same native behavior. Scope inventories to exact new owners and ancestor relationships; report remaining ambient semantic vocabulary separately from invalid source ownership.

Check the browser/build split by actual entry graph: styling browser API/data must not reach repo Node workspaces/discovery or Vite orchestration. Trace the Storybook preview or representative native browser bundle, including the iframe plugin extraction, and preserve build/dev helpers under their actual source owners. No Node polyfill or alias that merely hides a mixed browser/server API is acceptable. The earlier Storybook run indexed stories and failed preview at Node path; that is causal context, not a baseline. Avoid repeating all 333 identity records unless this lane changed story semantics.

Review the Metal `Scene::finish` correction causally: the scene helper and related pipeline were gated with cfg(test) in the dependency; backend tests require the explicit backend-testing feature. Confirm it is enabled only through the intended test feature and the actual Metal suite now compiles/runs. The coordinator observed a transcript with 10 native tests passing and one unnecessary-unsafe warning, including tests allowed to return on absent hardware. State the exact device/runtime limits and do not equate the test count with proof of a physical GPU path.

If the session isolation correction report is also complete, spot-check its production-shared output-root and Vite-resolver boundary and recorded live hash/mtime invariance before executing any session fixture. The prior regression was proven mutating; never run an unverified old body. It must be independent of pre-existing staged variants and write only private fixture paths. Reuse the earlier Storybook/session audit's positive evidence rather than repeating unrelated suites.

OS/WGPU moves are active concurrently. Distinguish coherent completed source from in-flight metadata/import states; registered graph failures are not executed target failures. Keep unsupported assumptions visible and send a concrete corrective item promptly when found.

## Latest Exact Browser Trace

The executor's scoped UI preview run advanced through 1,892 modules in 3m10s, then still reached repo workspaces. Its temporary Rollup importer trace identified a second edge distinct from the styling facade: UI runtime → framework package/kernel → actor shard-client → inline Vitest test modules → OS plugin build package → repo process/workspaces. The trace was removed. The production Storybook configuration now defines `import.meta.vitest` as `undefined`, and the portable topology fixture covers that production gate. A new scoped build is running; read its final retained report before claiming preview success. Audit that ordinary native test routes still activate their intended inline tests and this production setting does not suppress registered test execution. The existence of a pure styling facade alone did not prove the whole preview graph safe.

## Completed Executor Evidence

The retained executor report is now complete. Final topology rerun after launch/catalog/WGPU generation: 4/4, 253 expectations. The successful UI-scoped build reported 1,555 transformed modules, Vite 2m41s and observed wall time 3m26s; earlier failed-run progress counts must not be attributed to this green run. The executor deleted the 2.7 GB preview and its own disposable logs, retaining exact report evidence. Terra UI/generated has been assigned this bounded independent audit.
