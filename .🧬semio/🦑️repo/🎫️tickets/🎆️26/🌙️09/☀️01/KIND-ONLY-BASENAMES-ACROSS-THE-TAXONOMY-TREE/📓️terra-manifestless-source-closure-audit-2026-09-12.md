# Manifestless Source Closure Audit

Date: 2026-09-12

## Verdict

Accepted for the bounded seven-source extraction. The former manifestless implementation leaves are absent, their behavior now has contextual semantic owners with anonymous language leaves, the package command is routing-only, and the active alias, consumer, source-admission, cache, descriptor, distribution, import, and host controls exercised by this audit resolve to those owners.

No implementation, taxonomy, descriptor, or registry source was changed by this audit. `🧪manifestless-source-controls.ts` is retained as its independent native control.

## Topology And Consumer Closure

The portable fixture declares seven former behavior sources: the Python host; the five-way repository-server split; plugin materialization; distribution; Vite integration; Preview2 import rewriting; and behavior removed from the retained plugin command leaf. The direct gate confirms all former source paths are absent where required, every canonical leaf exists with the language-only basename, all five server concerns are present, the adjacent .NET host is mounted from `🧪️test/🖥️host/🔷️.cs`, and the package command is within its 12-line routing limit without the five former exports.

The four coordinator aliases resolve through the installed TypeScript resolver to their exact semantic leaves: persistence, source index, authentication, and event. Live coordinator consumers use those exact aliases. A focused active-source scan found the three former plugin distribution/import coordinates only in the closure fixture's historical paths, not in active code. Current direct consumers include the OS dev command's descriptor, inventory, and materialization imports; actor shard-client source-as-data imports of materialization; demonstrator Vite distribution integration; and demonstrator assets' distribution/import-rewrite references.

The semantic browser source root is an input of both support profiles in the plugin project and of component-materialization cache authority in the repository library. The exact `.gitignore` negations admit `plugin/🌐️browser-bundle/📦️distribution/🟦️.ts`; native `git check-ignore --no-index --quiet` returned its expected non-ignored status.

## Independent Native Controls

The retained control created one ticket-local artifact manifest and component source, then exercised the current owners directly:

| Control | Result |
| --- | --- |
| Preview2 rewrite | Exact `@bytecodealliance/preview2-shim/cli` rewrite produced `../shims/cli.js`; unrelated text stayed unchanged. |
| Distribution and inventory | One manifest-authorized regular file copied, was inventoried, and produced a 64-hex SHA-256 digest. |
| Vite plugin | Actual `configResolved` and `writeBundle` callbacks staged the same rewritten artifact. |
| Native descriptor | Node accepted a complete generated actor module and emitted `AQID`; a module missing `reactor.poll` failed with the exact missing-export diagnostic. |
| Materialization commands closure | Bun bundled the actual commands owner and its moved dependencies: 281 modules, 11.71 MB, 407 ms. |
| Python host | The moved host executed the real parity adapter's digest-and-fixture scenario and emitted a passed protocol-v2 result. |
| .NET host | A ticket-local compile harness linked the moved C# host and real parity adapter; it executed the same scenario with a passed result. Its projection values matched the Python host: vector digest `7087e4e006b72ee5b808b9f622732600`, literal digest `96e272947bd23fe166b7da2ff2269e41`, seed 42, and the expected fixture name. |

The direct host harnesses wrote only under this audit's ticket-local scratch roots. They establish host/adapter execution for the moved sources; they do not claim a live coordinator, database, or service integration.

## Focused Verification

- Direct `bun ./📜️script.ts test manifestless-source-closure`: **9 pass, 148 expectations, 0 fail** in 2.01 seconds.
- Isolated registered `@semio-tech/repo-lib:test-manifestless-source-closure`, cache skipped with ticket-local Nx workspace and artifact paths: **9 pass, 148 expectations, 0 fail**; Bun 1.478 seconds, Nx 1.7 seconds, critical path 1.6 seconds.
- Strict `loadTaxonomy()` plus `validateTaxonomy()` returned zero diagnostics.
- The permanent gate independently validates the draft-07 fixture/schema, contextual directory membership, exact alias targets, language parser acceptance, native descriptor positive/hostile results, cache-source inputs, launch registration, source admission, and command-leaf limit.

## Browser Boundary Observation

`🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/⚡️caching/🔒️leases/🟦️.ts` contains the dynamic `import("node:sqlite")` fallback in `openDatabase` after its Bun SQLite branch. That static Node builtin reference explains the reported browser/Vite collection boundary encountered by the broader descriptor suite. This audit did not treat it as a baseline comparison, did not run a live service, and does not attribute it to the manifestless extraction; the focused descriptor and browser-owner controls above remain green.

## Limits

This audit is restricted to the seven moved sources and their direct closure. It does not decide the broader browser-bundle contextual findings, full Vite service behavior, database availability, fixed-script enforcement, or unrelated descriptor/registry publication work.
