# Script Router Policy and Source Separation

Terra's bounded report `📓️terra-script-ownership-review-2026-09-12.md` gives five source extraction slices and useful explicit small-script decisions. Its 89 small router candidates must remain conditional until method bodies are checked; top-level functions/types/exports alone cannot establish task-only behavior. This packet is a separate bounded Sol policy correction, preserving the user-mandated executable script leaf and all earlier hidden-code rejections.

The coordinator traversed the actual class method bodies with the installed TypeScript compiler. Of the 89 small candidates, 13 contain nested functions and two contain regex literals. Most are finite path mapping/native command closures, but one is concrete verification logic: `🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/🧹️fixture-sweep/📦️packages/🦀️rust/📜️script.ts` reads native law stdout/stderr, parses two domain-specific report formats with regex, converts captured counts and asserts semantic coverage. It has no top-level function/type/export, demonstrating why that AST bucket is not an unconditional admission proof. Move that parser/assertion behavior to its existing fixture-sweep verification owner. Preserve the executable gate and actual native output assertions. The other regex case is a Mit-Bestand command argument guard for --outDir/--config/--root; that is a different task-control role.

Full disposable method observations are `🗑️generated/coordinator/router-candidate-native-ast.json`; the retained 138 exact source inventory is `📓️mandated-script-review-packet-2026-09-12.md`. No source changes or target reruns were made for this probe.

## Execution Contract

Review all candidate method bodies against a closed task-routing grammar. Extend only exact necessary forms with schema-first portable vectors and an independent installed TypeScript AST oracle: argument guards; finite literal target loops that delegate to imported native command runners; local command closures whose sole body is imported delegation; finite path mapping for command arguments; literal status logging and the established router/main guards. Each admitted form needs hostile variations containing domain calculations, hidden declarations or extra effects within the same shape. Preserve the seven original rejected counterexamples and the later valid multiline-arrow/config fixtures. Do not admit arbitrary class bodies, local closures, loop bodies, filenames or imported BundleScript presence. If a form cannot be proved task-only cleanly, move its actual reusable behavior to its neutral owner instead of adding a weak escape.

Do not weaken global package-body ownership, add runtime dependencies or special-case live paths merely to clear findings. Keep all permanent executable routing under 📜️script.ts and Bun/Nx/launch authority. Use the existing registered body-policy target and direct portable native oracle; source extraction checks should validate actual behavior. Finish with a fresh exact body census and retain changed path/source role decisions in `📓️sol-script-router-policy-2026-09-12.md`. Scratch only under ticket `🗑️generated/sol-script-router-policy`. No Git, AGENTS, compatibility or lifecycle changes.

The larger API extraction lanes remain separate; do not pull 17,245-line Hub or 5,685-line OS dev compositions into this policy task. The repository source executor is currently moving source APIs and some task imports; re-read current references, preserve its work and avoid a stale whole-file rewrite.
