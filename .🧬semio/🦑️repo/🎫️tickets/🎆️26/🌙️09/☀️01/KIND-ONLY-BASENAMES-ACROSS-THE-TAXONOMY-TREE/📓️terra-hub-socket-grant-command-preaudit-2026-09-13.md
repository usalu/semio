# Hub Socket Grant Command Preaudit

## Status

Read-only pre-extraction review of `📓️sol-hub-socket-grant-command-owner-plan-2026-09-13.md`. No Hub process, Cargo command, Nx target, service, browser, or native law was started by this audit.

## Current Boundary

`🌎️hub/📦️packages/🦀️rust/📜️script.ts` currently contains both shared local-relay selectors and `SocketGrantCheckScript`. The current command always runs the fixture proof first. It returns early only when its first segment is `oracle`; otherwise it runs seventeen binary selectors, two library selectors, one replication selector, and a final binary check. Two listed binary selectors have no corresponding current Rust declaration:

- `tests::directory_socket_forced_lag_is_scope_authorized_and_closes_1013`
- `tests::document_socket_forced_lag_sends_verified_control_then_closes_1013`

The proposed seven-owner split is appropriate: local-relay routing; pure directory socket-grant decision; fixture verification; native-law plan; command execution; portable source-command adapter; and a Rust test oracle. The package router should directly import those owners. It must not recover a root facade.

## Source And Route Closure

The existing registered route is `os-hub:socket-grant-check`, whose project command remains `bun ./📜️script.ts socket-grant-check`. Its existing seed and generated launch authority is `⚖️gate🔌️socket-grant🛡️server`. The planned portable route and source launch are distinct evidence; they must not be described as running the server route.

The current local-relay selector functions accept and normalize paths for execution targets, artifact creation, GIS inference, and upstream requests. Their proposed routing owner is coherent because it depends on the existing GIS cursor authority rather than reproducing that bound. The pure grant decision must not import the package router, filesystem, process, or root implementation.

The source input closure needs the direct router and project, all seven owners, schema/fixture/source test, directory schema and fixture, integration-harness authority, phase-one staging environment, generic owned-process authorities, Hub Cargo manifest/library, lock file, all admitted Rust-law source files, and both launch representations. Exact project-input equality is required because the registered route is cacheable.

## Required Controls

- The source test needs Ajv, TypeScript AST, TypeScript resolution, direct import-edge checks, all 18 ancestry contexts, four internal edges, and exact named-input plus seed/generated launch bindings.
- The native-law plan must derive declarations syntax-aware from current Rust source. Text search alone admits comments, strings, and duplicate declarations. The independent Rust oracle should parse the relevant test modules, reject non-declarations and duplicate identities, and prove the current 17 binary requests reduce to 15 declared binary laws. Together with two library laws, one replication law, and the final check, that is exactly 19 sequential native stages.
- Command grammar must admit only no segment for native execution and the single `oracle` segment for fixture-only execution. Unknown or additional segments must reject. An injected process boundary must prove `oracle` invokes no native command and that normal execution preserves stage order, cancellation, output handling, and the phase-one Cargo environment projection.
- The portable fixture must retain both absent current selectors as rejected source-admission cases. They are stale selection records, not evidence of a native behavior failure.

## Limits

This preaudit establishes only the intended ownership, source-data, grammar, input, and native-plan boundaries. It does not establish a successful Cargo law, service socket, browser client, live relay, or full Hub compiler result.
