# Flow Browser Ownership Prerequisites

Root reread the live Flow core command source, browser declaration emitter/tests, host runtime tests and open-ownership oracle on 2026-09-12. This is read-only preparation for the source/projection lane in 📓️package-metadata-followup-packet-2026-09-12.md. No Flow product file, native build output or package binding was changed.

## Current Runtime Evidence

The existing direct core `test-browser` route passed. Its retained console evidence confirms two-session isolation and exact retirement, duplicate-export refusal, late open/close ownership, receipt acknowledgement retry, verified versus uncertain open failure, terminal close evidence, event-loop yielding, and the current compiled module's 33-request close. It also exercised four initializer/import ownership cases, fallback 2D presentation on unavailable WebGPU hosts, shared-fixture draw-list primitives, camera/pixel scaling, malformed-list unwind and the actual Bun browser bundle's sibling initializer/host references.

This executes the current compiled module; root did not rebuild Rust/Wasm or assert its source freshness. The browser bundler ran with write:false. The registered semio-framework-os-flow-core:test-browser Nx route subsequently passed with isolated ticket-local workspace/cache/artifact roots and skipped task cache. Nx reports6.2s run duration and5.3s one-task critical path, after the initial graph preparation. The caller exited0.

Calling the actual exported `testFlowBrowserDeclaration` against the current core binding package passed: 105 schema methods, runtime prototype and installed TypeScript parser parity, three package exports, two native TypeScript resolutions and five hostile fixtures. Root then called the actual pure declaration emitter and independently compared both physical declaration files with its returned bytes; both are fresh. The host source and published host copy are byte-identical.

## Current Source And Projection

| Leaf | Bytes | Role |
| --- | ---: | --- |
| wasm/packages/javascript/🌐️flow-browser.js | 6,226 | Authored browser runtime |
| wasm/packages/javascript/🖥️flow-host.js | 41,910 | Authored host runtime |
| wasm/packages/javascript/📝️flow-browser.d.ts | 23,501 | ABI-schema declaration projection |
| core/bindings/🌐️flow-browser.js | 6,301 | Bundled publication |
| core/bindings/🖥️flow-host.js | 41,910 | Published host copy |
| core/bindings/📝️flow-browser.d.ts | 23,501 | Published declaration projection |

These abbreviated coordinates share the exact root `🧰️framework/🛍️products/💻️os/🔨️modules/🌊️flow`; the full paths and SHA-256 snapshot provenance are in disposable generated/coordinator/flow-source-projection-prerequisite.json. Source hashes are not permanent implementation-body assertions.

The actual core command is under `🌊️flow/🫀️core/📦️packages/🦀️rust`, not below wasm/core. Its browser bundling captures one generated initializer import, rewrites the native binding coordinate, externalizes only the exact native wrapper/host sibling, publishes declarations and updates the binding package's files/exports. Keep those exact producer relationships when moving source and generated anonymous leaves.

## Additional Owner Correction

The declaration generator `flowBrowserDeclaration` and writer `writeFlowBrowserDeclaration` currently live in the browser-declaration **test** owner and are imported by the production declarations path. Move schema-to-declaration generation/publication to a neutral ABI/declaration concern; keep AJV, native TypeScript resolution and hostile test fixtures in test/oracle ownership. This prevents a generation path from importing its test implementation as production authority.

The test currently derives coordinates from a synthetic URL ending in `wasm/packages/javascript/📜️script.ts`, a command source that is absent. It is used as a path anchor, not invoked. Rebase all schema/fixture/declaration paths from the actual new owner rather than preserving that obsolete source coordinate. This is a path-authority correction, not a reason to recreate the command or a compatibility source.

The exact existing browser-types fixture and native tests must follow the move. Preserve ABI operation ids and argument ordering, public session/task behavior, module/type exports, published runtime URLs and OS distribution layout input. Keep all runtime dependencies behind owned interfaces, add no production AJV/TypeScript dependency, and do not introduce named implementation leaf exceptions.

## Retained Evidence

- generated/coordinator/flow-browser-current-prerequisite.log
- generated/coordinator/flow-declaration-current-prerequisite.log
- generated/coordinator/flow-source-projection-prerequisite.json
- generated/coordinator/flow-browser-current-nx-prerequisite.log

All raw output/cache paths are disposable ticket-owned generated files; retain this report after cleanup.

Additional concrete consumer from the current configuration baseline: 2D's Vitest `@semio-tech/flow-core` alias resolves under nonexistent `framework/🔨️modules/🛍️products/…/flow_core.js`. Include that alias in the actual Flow projection closure. The filesystem probe is not a test-execution result; rebind through the real producer contract and validate its native consumer rather than manufacturing a replacement module. See `📓️current-vitest-configuration-consumer-baseline-2026-09-12.md`.
