# Mit-Bestand Source And Preserved Input Ownership

The wave-three focused census retains 50 active Mit-Bestand findings and 53 findings under ignored `temp/merge`. This is a queue packet, not an acceptance report. Read current files before editing and preserve simultaneous work. Use the existing ticket, Bun/Nx, schema-first portable fixtures, native or installed third-party oracles, exact source hashes and consumer closure. Keep executable commands in the mandated `📜️script.ts`, edit the launch seed before derivation, never modify Git or AGENTS, and retain an exact Markdown move/support list. No blanket temp, public, fixture, captured-source or generated-directory exemption is acceptable.

## Active Presentation And Demonstrator

The current presentation owner is `♻️mit-bestand/🎤️präsentation/📅️33.projektetage`, with 90 Git-tracked files at the coordinator probe. Its ordinary task metadata is `📦️packages/🟦️typescript/📋️project.json`, not `project.json`. The task router is at that package root. Move authored slide concerns and the large presentation implementation out of the language package to semantic owners with anonymous leaves; maintain distinct slide identities and ordering. Keep the native package as tooling/export glue. Current package exports `./🟦️.ts` although the authored implementation is `📦️index.ts`; verify actual package resolution and close the current route, rather than creating a compatibility copy.

Observed active consumer issues to resolve with the move:

- The package HTML is byte-identical to its intake copy and still loads `./js/index.ts`, which is absent at the current package location.
- The Nx named input still names `.../33.projektetage/slide/**/*.ts`; the live tree is `🎞️slide`.
- The package router's five-ancestor repo-lib import resolves to the existing shared library file in the coordinator filesystem probe. Preserve that live route and rebase source-root assumptions only where the move actually changes them.
- The implementation has `import.meta.glob("../../🎞️slide/**/*.ts")`; its relative coordinate must follow the new semantic owner. Preserve imported default exports, slide IDs and participant/embodiment identities in the portable fixture, and validate actual Vite transformation or native build.
- The Vite config is `⚙️vite.config.ts`, so do not claim an exact conventional filename exemption. Configure an anonymous source leaf beneath an actual configuration concern where possible, preserving the native tool route. Apply the same distinction to demonstrator Vite/Playwright and exact `vitest.config.ts` contracts.
- Demonstrator `🟦️.tsx` directly imports `⚛️footer.tsx` and `🪧️brand.ts`. Move these bodies to footer and brand concerns with anonymous leaves and update test/build consumers. Its active runtime/site scripts and schemas were authored by other work and must remain intact.

The presentation and demonstrator rely on active Animate/OS packages. Re-read current package exports and fix actual moved-source references; do not restore old implementations, old app compatibility or unrelated products. Existing live failures are not proven baselines. Run bounded registered tests/builds and report precisely where execution reaches.

## Captured External Web Page

The presentation publicly serves `🌐️public/🌐️zukunft-bau-entwerfen-mit-bestand.html`, referenced by the implementation's `ASSET_ZUKUNFT_BAU_ENTWERFEN_MIT_BESTAND`. The page identifies TYPO3 and a canonical Zukunft Bau project URL, and loads 12 named JS/CSS files plus media from `🧰️zukunft-bau-entwerfen-mit-bestand_files`. This is evidence of captured external content, but it remains an active iframe asset, not disposable generated output. Its paths were already locally rewritten with emoji names; preserve relative HTML, CSS and JS referents. Prefer a semantic capture owner and anonymous executable asset leaves with exact original-name/provenance mapping, or an exact sealed source-as-data contract if the tool/content identity truly requires literal filenames. Do not broadly exempt everything in public or every minified file. Preserve copyright/provenance and visual/runtime behavior. A portable resource graph checked independently by an installed HTML/CSS parser is an appropriate oracle; do not fetch remote resources just to validate local referents.

## Preserved Merge Inputs

Read-only Git probes found zero tracked files below `temp/merge` and `temp/compose`. `temp/merge` contains print and Mit-Bestand intake; this does not authorize deleting it. The live research intake document at `♻️mit-bestand/🔎️recherche/_neo4j/intake/inbox/2026-09-05-print-merge/INTAKE.md` explicitly records `temp/merge/mit-bestand/recherche` as additive raw input, pending semantic review, received September 5, with source hashes retained in the original merge ticket. The current research owner is a separately tracked Git entry; do not modify its knowledge authority in this taxonomy lane.

Coordinator byte comparisons prove that the intake is not simply interchangeable with the current source:

| Input relative to `temp/merge/mit-bestand/präsentation/33.projektetage` | Current equivalent | Input bytes | Current bytes | Equal |
| --- | --- | ---: | ---: | --- |
| `js/index.ts` | package `📦️index.ts` | 24,155 | 23,428 | No |
| `index.html` | package `🌐️index.html` | 557 | 557 | Yes |
| `globals.css` | `🎨️globals.css` | 687 | 971 | No |
| captured public HTML | current captured public HTML | 148,642 | 148,949 | No |

Before relocating inactive inputs, verify active references beyond the initial root/build/launch/research probe, inspect original intake provenance, and capture complete byte identity. Govern their storage as preserved ticket inputs, never disposable `🗑️generated`, keeping their original metadata and source relationships. Do not merge these stale files back into active implementations or delete authored scripts because they resemble migration files. Respect the instruction not to edit AGENTS. The separate `temp/compose` tree and root architectural/model/media inputs need their own provenance decision; a source-only census does not establish that those inputs are unused. No age-only cleanup and no shared cache reset.

The source index SHA-256 values recorded at this probe were input `11ac93de11d311196b2b7ff76ce575772b214537817c3cc788794b6f90cd8fa8` and current `d3a0d2d4bc94820682178e68eb236038ade79971338355f91fd82b1ba99c42e7`. The common HTML hash was `037d2f3a37ddc62cf7fb81bd100fae3809b77216c9da8e7309792b2d62740ea7`. Recompute immediately before any move because the workspace is shared.
