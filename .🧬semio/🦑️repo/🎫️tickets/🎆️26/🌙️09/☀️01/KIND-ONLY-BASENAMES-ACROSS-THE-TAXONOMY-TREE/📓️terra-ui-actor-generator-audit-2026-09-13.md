# UI Axes and Actor Typegen Acceptance Audit

Date: 2026-09-13  
Status: **accepted for the extracted source, pure protocol, registered routes, and bounded UI/actor native evidence.**

## Ownership and Meaning

The UI concern is split into five anonymous TypeScript leaves beneath `🖱️ui/🎚️axes`: `📥️source`, `📽️projection`, `📋️plan`, `📤️publication`, and `🏃️execution`, with the language-neutral source at `🎚️axes/🔣️.json`. The source parser owns input validation; projection owns Rust and TypeScript text; plan owns the canonical two-target protocol; publication owns byte freshness; and execution owns the package commands. The package router imports only execution.

The source declares exactly two locale rows (`en`/`En`, `de`/`De`) and two terminology rows (`native`/`Native`, `reuse`/`Reuse`). The focused test preserves their order and uniqueness, rather than reducing them to a count. The plan has exactly two files: the WGPU Rust generated leaf and the manifest TypeScript generated leaf. Their live consumers bind the Rust `#[path = "🤖️generated/🦀️.rs"]` module and the manifest `./🤖️generated/🎚️ui-axes/🟦️.ts` import respectively.

Actor typegen has three anonymous leaves: `🧬️typegen/📋️plan`, `📤️publication`, and `🏃️execution`. Plan owns the generated TypeScript mirror and exact sibling pruning list; publication owns admitted-root and no-follow protection before writing or pruning; execution owns the native child, cancellation polling, temporary private Cargo target for preview, and the 600,000 ms preview deadline. Its package router imports execution only.

## Consumer and Registration Closure

Both producer projects include all semantic owners and their focused test in `namedInputs`; their taxonomy generator contracts list the same owners. UI’s two implementation consumers are registered. Actor’s native test reads `SEMIO_TYPEGEN_OUT`, and the mailbox and shard-client TypeScript consumers import the generated actor mirror. The actor contract fixes the preview timeout to 600,000 ms. Launch routes invoke the actual Nx `preview-generated` targets for both owners.

## Independent Focused Check

I ran the two current pure controls directly:

```text
bun test ./🧰️framework/🔨️modules/🖱️ui/🧪️tests/🎚️axes/🟦️.ts ./🧰️framework/🔨️modules/🎭️actor/🧪️tests/🧬️typegen/🟦️.ts
```

It passed **9/9 tests and 58 expectations in 516 ms**. This covers the 2×2 source, Rust and TypeScript projections, exact UI plan/freshness/publication, UI producer/consumer inputs, actor stale sibling behavior, no-follow linked-output rejection before mutation, cancellation and deadline killing, and actor producer/native/TypeScript consumer closure. The disposable log is under `🗑️generated/terra-ui-actor-audit/portable.log` and will be removed with this audit scratch.

## Executor Evidence and Limits

The executor reports isolated Nx UI check success: `@semio-tech/ui-rs:check --skip-nx-cache`, exit 0, **4/36**, 935 ms, cache skipped. Its real actor `preview-generated` invoked the selected native exporter: one `exports_typescript_bindings` law passed with 126 filtered after a 14.31 s compilation; the protocol contained two nodes, zero stale paths, and 7,934 generated bytes equal to the committed output. Its final isolated registered actor route, `@semio-tech/framework-actor-rs:typegen --skip-nx-cache`, also exited 0: one selected native law passed with 126 filtered, native build took 2.88 s, the law 0.03 s, Nx 5.2 s, cache skipped, and the route used private target coordinates.

The executor later completed an offline UI native harness with the production generated Rust and actual neutral JSON: **1 passed / 0 failed**, after a 29.13 s compilation and a 0.01 s law. It exercises the 2×2 order, ALL/COUNT/parse, unknown-value rejection, and serde round trip; only dead-code warnings were emitted. The earlier selected UI native route that reached an unrelated OS-pack compilation failure remains separate non-evidence. The three selected schema `entity_kind_` native laws (catalog/projection, emoji FIRST-WINS, and declared-violation rejection) passed separately; they corroborate the shared generator data boundary but do not execute actor typegen.

No live generation, publication, or mutation was run by this audit. The direct test’s mocked child confirms cancellation and deadline handling; the executor's actor preview is the only cited native actor-export result.
