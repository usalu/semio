# Styling Overlapping Root Probe

Root also independently resolved the extracted module's current nine pixel scan roots, nine color scan roots and ten allowance referents. All 28 paths exist. The allowance paths were read using the installed TypeScript AST; exact rows are saved in generated/coordinator/styling-repaired-referents.json. This confirms repaired referents, not whole-tree style acceptance, and leaves the executed overlapping-root defect below as a separate issue.

Root exercised the freshly extracted `styling/🛡️verification/🟦️.ts` collector in a ticket-generated temporary fixture on 2026-09-12. The fixture contained one regular TypeScript source at `domain/nested/🟦️.ts` with the single line `export const className = "w-[13px]";`. Calling `collectPxViolations(root, ["domain", "domain/nested"])` returned two identical violations for that one file, line and rule. There was one unique violation. The exact result rows are retained under generated/coordinator/overlapping-styling-roots.json.

The repaired current root list has this real shape: OS is a parent of the separately listed dev and infinite-world roots. Both pixel and color collectors walk every root independently. Once the formerly absent roots become live, this duplicates traversal and diagnostic counts. The font/assets executor was asked to add an overlapping/repeated-root portable control and avoid visiting a regular source more than once while preserving the existing style predicates. This is a scan coverage/ownership correction, not authorization to rewrite newly exposed UI styles or widen exemptions.
