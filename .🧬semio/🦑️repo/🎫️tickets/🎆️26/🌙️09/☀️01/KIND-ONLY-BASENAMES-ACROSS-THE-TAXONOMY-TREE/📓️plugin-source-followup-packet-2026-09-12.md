# Remaining Plugin Source And HTML Fixture Owners

Wave three reports 23 implementation-basename findings under ✏️s after generator moves. This is a bounded Sol Extra High execution packet for the remaining authored plugin sources and precise HTML fixture data ownership. It does not own the completed generator/oracle crate move map. Re-read all active paths, metadata, producers and consumers before editing; concurrent semantic/test-taxonomy workers may already be changing adjacent owners.

The current non-HTML findings are:

- Sequence artifact v1/any editor/wasm browser/🟨️.d.ts, wasm/📡️protocol.rs and wasm/🧩️component.rs. Validate generated-versus-authored provenance and correct the declaration file kind; place authored protocol/component source at neutral semantic owners and update native Rust/wasm/browser consumers.
- Norm plugin/oracles/packages/python/🗣️vocabulary.py. Extract the domain source from its language package, keeping only exact native packaging/import glue. Verify a native Python oracle/import through the actual registered route.
- Energy plugin/oracles/packages/python/oracles/🐍️.py is an additional package-body finding from the corrected policy, outside the 23-basename count. Its name is already anonymous, but the authored Python oracle source still needs the neutral oracle owner and exact native packaging/import closure.
- Trinity/jack/shell/packages/rust/📦️bin.rs. Move its actual CLI concern source outside the Rust package and update its explicit Cargo target and references.
- Stdio/packages/rust/benches/⏱️brep-kernel.rs. Put benchmark source beneath the neutral BREP concern and point the package's explicit native bench target to the anonymous source. Do not preserve language-first source ownership merely because this is a benchmark.
- Puzzle/2d v1/any editor/engine/icons/🧩️metabolism.rs. Move the domain name to its semantic directory and keep an anonymous Rust leaf with all consumer mounts closed.

Sixteen further findings are HTML mutation before/after fixtures under stdio/html v5/any fixtures: set-raw-text, set-text, insert-node, remove-node, set-element-name, set-comment, set-doctype and set-attribute, each with ➡️after.html and ⬅️before.html. Inspect their manifest and native test loader. These are source-as-data fixture inputs rather than an excuse to exempt all HTML source. Prefer neutral before/after fixture owners with anonymous HTML leaves and exact active manifest/reference closure, or prove and enforce an existing precise fixture-data contract if it already defines their identity. Do not delete or transform fixture bytes, add a blanket fixture exemption, or freeze active fixture coordinates as history.

Extend an existing portable taxonomy/source-reference fixture with language-neutral expected identities and independently validate source/metadata through installed native or third-party tools. Preserve pre-move source hashes, format-specific output equivalence and real native execution. All durable script behavior belongs in existing 📜️script.ts routes reached by Bun/Nx and launch seeds. Report exact old/new/support paths and tested limits in 📓️sol-plugin-sources-2026-09-12.md, with disposable outputs under ticket 🗑️generated/sol-plugin-sources. No modifying Git or AGENTS, new runtime dependencies, migration/compatibility scripts, ticket or goal lifecycle changes.

## Stdio Package Composition and Contract Source

The additional mandatory Stdio root `✏️s/🔌️plugins/🗄️stdio/📜️script.ts` still defines substantive package-contract derivation, dependency-DAG checks, manifest/source verification, native metadata proof, TypeScript build/declaration publication, captured-process execution and composition commands. Root read the full current source. The four-line TypeScript package command imports `runStdioCompositionPackageMain` from that mandatory root, so its unresolved body decision cannot be closed by merely naming the wrapper an allowed terminal.

Give this source a bounded extraction after the active app/generator slices. Separate the artifact package contract and graph, language package build/publication, source/native proof, and command composition by semantic responsibility; imported test code stays under its existing test owner. Reuse the existing bounded process and compiler execution interfaces rather than copying its local timeout/output-limit machinery into a generic helper. Preserve the actual36-artifact catalog protocol and native Cargo/Bun package/declaration consumers, but re-read their current coordinates before treating hardcoded names or counts as normative.

The existing `createStdioArtifactPackageTests` factory receives many functions plus `{ directory: import.meta.dir, url: import.meta.url }`; close this real dependency/context edge when moving sources. The package router should use direct imported command classes and the existing generic `ScriptRouter`/`runBundleScriptMain` interface where suitable. Do not extend the classifier with a product-specific terminal-name whitelist, import a semantic implementation from another command file, or retain a compatibility wrapper. The separate small-router packet covers genuine generic grammar gaps after ownership is resolved.

Terra is preparing exact source/consumer/input details. Require schema/portable fixtures first, actual source/native package graph and declaration evidence, output/cancellation containment, all producer/cache inputs, Bun/Nx/launch closure and exact attribution. No live destructive publication or broad native artifact suite is necessary merely to prove this extraction. The DWG semantic-catalog finding below remains a separate limit.

Root executed the actual exported `stdioArtifactPackageContract(repoRoot)` and the installed Ajv validation against its actual committed schema/fixture at22:03UTC:36 packages, two accepted values (including the live contract), two rejected vectors,329ms, exit0 with `[DEBUG]` evidence. This is contract-reader and schema-oracle evidence only: no Cargo metadata, Nx graph, build, declaration publication or native artifact suite was invoked. Raw receipt is `🗑️generated/coordinator/stdio-package-contract-prerequisite.json`. The imported test factory merely creates callbacks and does not execute its native graph/build checks at import time.

## Observed DWG Oracle Runtime Limit

The independent Rust audit ran `semio-s-plugin-stdio-test-oracle:test`: 32 of 33 pass. The only failure is `artifacts::dwg::standards::v_ac1024::subsets::any::component::tests::kinds_match_both_catalogs_and_the_vocabulary`, which reads committed catalog JSON and reports missing `no-mutation`. Current DWG source implements and lists that kind. The audit causally separates this semantic catalog mismatch from the taxonomy relocation: all 46 manifests resolve, and the failing assertion reads the existing semantic catalogs. Complete evidence is in 📓️terra-rust-generator-audit-2026-09-12.md.

This observation is a validation limit, not authorization to add mutation kinds or change another logical feature in this source-ownership lane. Preserve the source vocabulary and committed catalogs while making taxonomy changes. Fix only a demonstrated coordinate/ownership defect introduced or exposed by the source move; the auditor found none in this DWG assertion. Do not weaken the test or call it a historical baseline. The affected owners are `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🖊️dwg/🏅️standards/{🔟ac1024,4️⃣ac1018}/🪆️subsets/✳️any/🔮️oracles/🔣️.json`, with source and native unit test below the ac1024 oracle owner. The earlier proposal to reconcile these semantic catalogs in this lane is superseded by this scope decision.
