# Repository Source Extraction Audit

Use Terra Extra High for a bounded 15–20 minute independent read-only audit after the current OS audit slot is available. Read `📓️sol-repo-source-extraction-2026-09-12.md` (including the corrected provenance limits), then inspect the exact 22-move fixture and current consumers. No source/config/output regeneration, modifying Git, AGENTS or lifecycle changes. Own scratch only ticket `🗑️generated/terra-repo-source`; retain `📓️terra-repo-source-audit-2026-09-12.md`.

Audit semantic source ownership and actual relative module references, Cargo library/binary paths, .NET explicit source includes, package roots reduced to actual export glue, VS Code builder entry/sourceRoot and all 12 Next required wrapper exports. Inspect TypeScript declaration-kind correction and server adapter package.json createRequire anchor. A line-count limit or matching fixture text alone cannot prove glue semantics; use the shared classifier and installed parser. Verify each new contextual directory and ancestor, including builder, webhook/detail and authentication/dashboard/shell concerns; the framework audit demonstrated that checking only a subset of introduced owners misses registrations.

The executor retained passing uncached Nx 6/212, package-body policy 49/154, native CLI Cargo 24, test-host Cargo completion, .NET zero-warning build, VS Code Vite 53 modules, and Bun bundles of 12 Next entries and package consumers. Test-host Cargo zero-test completion is compiler evidence, not a native scenario behavior pass. The Bun bundles externalized bare dependencies and aliases, so they do not demonstrate runtime resolution of the internal coordinator alias `@/lib`. That stale tsconfig referent to absent `../lib/index.ts` has HEAD/current evidence; other VS Code type and CLI whitespace diagnostics have only current observations and must not be called pre-existing.

Root found six additional substantive manifestless source leaves after this batch, recorded in `📓️manifestless-source-closure-packet-2026-09-12.md`. They are separate work and not part of the 22 identity fixture. In particular the omitted server library contains the actual API needed by the broken alias; its extraction lane must repair and natively resolve that consumer. The script-policy worker independently fixes discovery’s missing-manifest body bypass. Distinguish current source move defects from these assigned coverage/metadata issues and read their live status before diagnosing a stale path as new breakage.

Preserve completed Go cancellation and source-directory anchors; do not run a cancellation fixture before verifying its private output and descendant cleanup boundaries. Scope any runtime probes to read-only/native build or private scratch, avoid external services or fixture writes to selected runtime output, and report exact limits. Send direct causal defects promptly; do not rewrite metadata or introduce a compatibility module in this audit. After evidence is retained, remove only your disposable output directory.
