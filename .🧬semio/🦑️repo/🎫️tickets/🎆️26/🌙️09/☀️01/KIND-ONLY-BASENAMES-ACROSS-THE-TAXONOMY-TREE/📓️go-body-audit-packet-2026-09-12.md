# Go Dispatch And Package Body Independent Audit

Use a bounded Terra Extra High read-only audit after the Go dispatch report completes. Read 📓️sol-package-body-policy-2026-09-12.md, 📓️package-body-extraction-backlog-2026-09-12.md, 📓️go-test-dispatch-followup-2026-09-12.md, and the completed Go dispatch report. Keep all probe output under 🗑️generated/terra-go-body; retain 📓️terra-go-body-audit-2026-09-12.md.

Independently exercise at least one actual reachable native Go CLI scoped test request against semantic-only anonymous source/test ownership, including selection and cancellation/output behavior. The first Go audit covered registered build/test/dev routes, not native CLI runBundleTests/runFileTests/runSectionTests/runDefinitionTest. Verify that active routes now use the shared canonical plan and no stale direct ./... path remains. Inspect the region marker fix, if any, against the preserved Flat Unicode identity contract.

Cancellation needs runtime proof across the entire native command chain. Coordinator source review observes exec.CommandContext killing the direct Bun child, while runTestBudgeted spawns the Go child detached on POSIX and only kills that child group when its own timeout fires. Its current implementation has no SIGINT/SIGTERM listener. This is a concrete static concern, not yet a reproduced runtime defect: cancel an actual running fixture after its native child starts, then verify prompt return, no continuing test marker writes and no surviving child owned by the probe. Do not infer child-tree cancellation merely from use of CommandContext or kill unrelated shared processes.

Review shared body classification across discovery and normalization: imports/reexports, domain structs/interfaces/enums/tables, short function computation, compiler registration, exact tool configuration with private helpers, and fixed Next/Cargo entries containing domain code. Run the portable/native AST oracle suite and one independently chosen positive/negative counterexample. Confirm no compatibility wrappers, external runtime parser dependencies or broad generated/fixed-name body exemptions remain.

Audit the new test oracle’s own taxonomy and metadata placement as well as its native execution. The coordinator scoped probe now confirms two new Cargo metadata scope findings, recorded in 📓️package-body-own-taxonomy-followup-2026-09-12.md; verify the later correction. Earlier snapshot: standalone Cargo.toml/Cargo.lock currently sit directly under normalization/tests/package-boundary-classification/oracles beside anonymous Rust and Go leaves. Confirm this is justified by the actual taxonomy contract; if ordinary packages/rust metadata ownership is required, identify the exact correction without editing source. Review the 63-extraction/1-facade/6-output triage for obvious configuration/build-hook false positives before it drives broad extraction.

The generic-stem regression logged 38 passes and 7 failures due missing current UI sibling paths. Exact old paths are control-keybinding-context, Select (two cases), Toggle, Diagram, Tree, and the styling tests singular directory. They are not the moved runtime/render named source leaves from our completed lane. Determine actual active fixture authority and ownership, and report a precise path-closure follow-up; do not silently freeze an active filesystem assertion as history. Report TypeScript errors with causal evidence and never infer a baseline from their presence alone.

Finish with concrete actionable findings and tested limits; do not wait for global enforcement, run broad historic planners, mutate source/tickets/goals, or interrupt other workers.

## Root-Reproduced False Admissions

Read 📓️package-body-counterexample-review-2026-09-12.md. The coordinator reproduced five public-API false admissions after the initial 30-test pass: Python import plus function, hidden TypeScript classes in configuration and script content, executable Rust registration macro content, and a C function-like macro discarded as trivia. Verify the original executor’s completed corrections and add at least one independent whole-file/disposition case so a valid import or tool call cannot admit unrelated implementation elsewhere in the file.

During correction, the coordinator also reproduced Rust `semio::register!({ execute(); });` admitted as registration and C `#define COMPUTE(x) domain(x)` admitted as declaration. These bypass the initial arithmetic/control-token blacklists. Verify closed whole-form macro admission, with unknown grammar rejected as unresolved or implementation. Review remaining class-based script findings separately: explicit AGENTS requires permanent executable script bodies to live in 📜️script.ts, so a classifier must not mechanically relabel all authorized script orchestration as domain extraction work.
