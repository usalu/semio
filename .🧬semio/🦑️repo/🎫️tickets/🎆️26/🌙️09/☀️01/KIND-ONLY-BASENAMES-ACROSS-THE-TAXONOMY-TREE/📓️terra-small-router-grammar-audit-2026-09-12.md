# Small Router Grammar Closure — Independent Read-Only Audit

**Scope:** four current `root-script` body dispositions only: Print Font Catalog’s direct router terminal, Demonstrator’s test registration, Stdio’s imported package terminal, and Puzzle3D’s environment default. This audit read the packet, the current grammar, fixture and TypeScript-oracle harness, and the current sources. It did not edit a product file or run a package, Nx, native, or application command.

## Current Disposition and Grammar Boundary

I freshly ran a read-only Bun probe over the four live source bodies on 2026-09-13. All four returned `fixed-source-disposition-unresolved`: Print Font Catalog, Demonstrator, Stdio TypeScript package, and Puzzle3D. The probe imported only the classifier and read source text; it did not execute a package, child process, Nx target, or native command. Its disposable output is `🗑️generated/terra-router-grammar-audit/current-body-decisions.json`. This fresh result agrees with, but is independent from, the coordinator’s earlier `small-routing-current-body-decisions.json`; it is a classifier result, not four behavior failures.

The command-router grammar is in [`library/🔍️discovery/🟦️.ts`](../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️.ts:11204). Its terminal recognizer accepts only five named runtime imports. It requires exactly one terminal. Its parser has lexical prebinding, runtime/type-only import distinction, and `import.meta` recognition; a later local binding therefore invalidates the intrinsic rather than being ignored. The portable fixture and its installed TypeScript semantic oracle are [`package-boundary-classification/🔣️.json`](../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🧫️fixtures/📦️package-boundary-classification/🔣️.json) and [`🟦️.ts`](../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🧪️tests/📦️package-boundary-classification/🟦️.ts:440). Keep both parser and compiler evidence; neither source text nor a body hash is an acceptance oracle.

| Case | Current source and disposition | Exact bounded grammar rule | Required positive and hostile controls |
| --- | --- | --- | --- |
| Print Font Catalog | [`print-font-catalog/📜️script.ts`](../../../../../../../🧰️framework/🛍️products/📓️print/🔨️modules/🔤print-font-catalog/📜️script.ts:12) binds `router` to a `ScriptRouter(...).register(...)` chain and, under `import.meta.main`, calls `router.run(process.argv.slice(2))`. Unresolved because `ecmaRouteTerminal` accepts only named imported terminals. | Add a direct-terminal alternative only for an unshadowed lexical binding whose kind is `router`, receiver property exactly `run`, one argument exactly intrinsic `process.argv.slice(2)`, and an `import.meta.main` guard with no `else`. Count it as the one terminal. | Accept the live bound router. Reject `obj.run`, a type-only `ScriptRouter`, a later/local router shadow, reassignment, `router.run(Bun.file(...))`, any different/member-computed argument, two runs, and a terminal outside or beside the main guard. Retain the existing TypeScript type-only/TDZ controls. |
| Demonstrator registration | [`demonstrator/📜️script.ts`](../../../../../../../♻️mit-bestand/🧺️demonstrator/📜️script.ts:18) has exact `import.meta.vitest`, a one-path dynamic test import, and one registered callback. It is unresolved because the top-level loop recognizes only `import.meta.main` and argv environment guards. | Permit one no-`else` `import.meta.vitest` block only when it declares a destructured callback from one literal dynamic module under `🧪️tests`, then invokes that same dynamically bound callback once. Require the first argument to be exact `import.meta.vitest`; require the remaining dependency/context values to be lexically runtime-imported or literal/import-meta data. | Accept the live registration once the test owner’s callback binding is rechecked. Reject a static/import-type callback, a callback from a non-test or computed dynamic path, a local/shadowed callback, `await import(...).then(...)`, arbitrary top-level dynamic loading, injected I/O, hidden argument calls, extra registrations, wrong guard, and `else`. The TypeScript fixture must declare `ImportMeta.vitest` and the test callback type without `any` suppression so binding and type-only failures remain observable. |
| Stdio package terminal | [`stdio TypeScript package/📜️script.ts`](../../../../../../../✏️s/🔌️plugins/🗄️stdio/📦️packages/🟦️typescript/📜️script.ts:3) calls the parent’s `runStdioCompositionPackageMain(import.meta.dir, import.meta.url)`. It is unresolved because it is a named imported terminal outside the five generic terminal primitives. The parent currently still contains Stdio implementation, so routing classification alone cannot accept that ownership boundary. | Extract the parent’s package contract/build/proof responsibilities into semantic command owners. The package router must then directly import those command classes, bind an ordinary `ScriptRouter`, and call the existing `runBundleScriptMain` primitive. Do not add `runStdioCompositionPackageMain` to a hardcoded terminal-name allowlist and do not retain it as a command API façade. | Accept the resulting standard router only after source ownership closes. Reject type-only command imports, local shadows, command classes with package implementation, arbitrary imported functions, dynamic imports, a second terminal, and statements that add I/O or composition logic. Verify the package contract/build/proof owners and registered route independently. |
| Puzzle3D default | [`puzzle3d/📜️script.ts`](../../../../../../../✏️s/🔌️plugins/🧩️puzzle/🗿️artifacts/🧊️3d/📦️packages/🦀️rust/📜️script.ts:13) has a top-level `process.env.RUST_MIN_STACK ??= String(128 * 1024 * 1024)` before an already-recognized artifact terminal. It is unresolved because `ecmaRouteEnvironment` is only accepted as the sole child of a precise argv guard. The literal-only body probe remained unresolved because top-level placement is the separate gap. | Preserve the existing environment-default predicate—unshadowed intrinsic `process.env`, uppercase noncomputed member, `??=`, and literal RHS—but allow one such declaration before the sole terminal at module top level. Re-express this source with the established literal string `"134217728"`; do not teach the grammar arithmetic or `String(...)`. | Preserve the four control outcomes: absent becomes `134217728`; empty string, user override, and `"0"` remain unchanged. Reject bound/shadowed `process`, lowercase or computed variable names, `=`/`||=`, arithmetic/call/nonliteral RHS, multiple defaults, a default after the terminal, and nested-function/default side effects. |

## Native and Runtime Evidence Limits

The retained [`🧪️task-constant-equivalence/🔣️.json`](🧪️task-constant-equivalence/🔣️.json) and generated receipt establish only Puzzle’s four environment-value equivalence cases. The receipt used the installed TypeScript transpiler and native JavaScript VM; it did not execute Puzzle’s Rust package, Cargo, or an application test.

No native Print, Demonstrator, or Stdio command was executed by this audit. There is no native acceptance claim for any of the four classifier changes. After implementation, run the focused portable grammar + TypeScript-oracle suite, check the actual four live body decisions, then invoke the registered classification target. Run the Puzzle value control separately from any full Puzzle native suite; do not use a green constant simulation as a claim that the native suite ran.

## Acceptance Conditions

1. The accepted cases are lexical bindings and exact terminals, not path exemptions or broad imported-call admission.
2. Existing type-only, shadowing, TDZ, unreadable-source, one-terminal, and package/nonpackage inventory controls remain green.
3. New fixture cases exercise the listed positives and every hostile boundary with the installed TypeScript oracle.
4. The final live probe records tool metadata only for cases whose source ownership is actually closed. Stdio remains unresolved until semantic command classes replace the parent command façade and their source/consumer closure is demonstrated.

**Status:** pre-implementation grammar audit complete. The smallest safe changes are a bound-router terminal, a narrowly shaped Vitest registration block, direct semantic Stdio command classes with the existing generic router primitive, and top-level placement for the existing literal environment-default form. No admission relaxation or product edit is justified beyond those exact forms.
