# Root Artifact, Surface and Inference Law Extraction Packet

Queue these as bounded Sol execution slices after cleanup/scaffold; they are not one generic policy module. The accepted schema-field extraction already owns all language field parsers and comparison primitives. Reuse those owners, and preserve their real native module-resolution behavior. Root inspected relevant discovery, inference, surface/abstraction and aggregate bodies, exact consumer references, and installed TypeScript declaration closures. Large editorial regions contain multiple domains; never cut an entire region mechanically.

## Current Native Declaration Closures

The current root source has 988 top-level statements, 2,324 bound local dependency edges and zero installed TypeScript syntax diagnostics. The checker follows same-file declaration symbols and treats imports as external boundaries; this is neither repository-wide type checking nor a runtime dependency graph. Raw members are in `🗑️generated/coordinator/root-schema-inference-law-closures.json`. Its source SHA is provenance only and must not become a permanent implementation-body snapshot.

| Seed | Local non-import declarations | Declaration lines | Import statements |
| --- | ---: | ---: | ---: |
| `policyInferenceFamilyBreaches` | 18 | 305 | 5 |
| `policyArtifactSchemaBreaches` | 12 | 344 | 9 |
| `policyAppSchemaBreaches` | 33 | 590 | 11 |

These closures overlap. Source counters exclude comments, surrounding region text, imported implementation internals and additional exported proof functions. In particular, `policyArtifactOwnershipFieldParity` and `abstractionOwnershipChecks` are real consumers/behavior outside the aggregate seed closures and must be owned explicitly.

## Executed Current Behavior

Direct root `verify abstraction-ownership test` completed successfully in 2.967 seconds: 11 ownership vectors, four nested schema vectors and three command-source vectors agree with Ajv; 104 artifact contracts expose document state across five schema formats. This is the actual existing command, not a new synthetic replacement and not an Nx run.

Separate read-only calls to the actual exported current discovery/aggregate APIs completed in 10.175 seconds overall. Artifact discovery returned 192 owners and 2,000 findings: facet-completeness 377, normative-leaf 2, field-parity 1,363, state-parity 83, diff-coverage 74, type-name-parity 101. App discovery returned 278 owners and 601 findings: facet-completeness 544, field-parity 25, config-fidelity 14, state-purity 2, type-name-parity 16. These are observed law diagnostics, not green conformance checks, and cannot be attributed to this extraction before any source change. Preserve semantic evidence and do not suppress findings, invent format implementations, or widen exceptions to turn this work green. Inference aggregate is internal and was not executed independently here.

Full current records are disposable `🗑️generated/coordinator/root-schema-law-current-results.json`; console and command outcomes are under `root-schema-law-prerequisites`. These are current-tree observations, not permanent expected counts for all future edits.

## Artifact Schema Concern

Own schema owner discovery, facet loading, declared export identity and the separate completeness/field/state/diff/type laws under the appropriate artifact/schema concerns. Shared low-level schema format parsing remains in the accepted `library/🧬️schema/🔍️field-discovery` owners. Preserve normative JSON Schema semantics, optionality/cardinality exceptions that represent real format limitations, and exact configured facet formats. TypeScript loading must still resolve real imported aliases from each source file's directory.

The current field-parity test under `library/📏️ownership/🧪️tests/🪪️field-parity/🟦️.ts` imports exactly three remaining APIs from root: `policyArtifactSchemaBreaches`, `policyArtifactOwnershipFieldParity`, and `policyDiscoverArtifactSchemaOwners`. Rebind all three to their real owners without root reexports. Root verification and the root lint aggregate are additional consumers; command dispatch keeps registration/orchestration only.

## Surface and Abstraction Concerns

Surface config/presence discovery and schema laws are distinct from the existing abstraction ownership contract. `policyAppSchemaBreaches` calls both. Its closure also reaches standard/subset artifact discovery from a later editorial region, so move/reuse that lower discovery concern instead of reverse-importing root or copying it into a surface-only module.

The existing abstraction schema lives at `library/📏️ownership/🧬️schema/🔣️.json`, with portable vectors under its fixture concern. Own declaration types, schema/source extraction and violation evaluation in that domain. Its Ajv/real-source proof belongs under the test domain; fixture JSON stays a fixture. Keep the actual field-parity audit separate from pure ownership evaluation. Root verify/report/enforce routes and their registered workspace targets must import the actual owners.

Current discovery includes names described as legacy config/wasm directories and separate rules that forbid them. Distinguish detecting forbidden source from implementing compatibility; do not introduce new fallback aliases. Validate actual current reachability before deciding whether an existing fallback can be removed within the taxonomy task. Retain genuine violation reporting while moving the code.

## Inference Concern

Own inference family discovery, root format completeness, implementation presence, per-family emoji uniqueness, assembly coverage and persisted-state exclusion together by their actual subconcerns. The current semantics accept both per-entity `InferredField` implementations and whole-snapshot public-function folds; do not require one implementation strategy universally. Emoji uniqueness is scoped within a family. Verify real registered taxonomy conventions rather than copying stale docstring counts. Current severity and report/enforce routing are independent of physical ownership; do not change them just to hide new findings.

Shared primitives currently reached from root are `POLICY_SKIP_DIRS`, `policyReaddirSafe`, `policyReadFileSafe`, `policyWalkRelFiles`, and `policyLineOfIndex`. Other consumers remain elsewhere in root. Locate the appropriate existing source/discovery owner, move shared behavior once, and rebind those consumers. The current safe-read implementations catch every error despite comments describing vanished entries; build hostile missing/read-error controls and distinguish absent files from unreadable evidence. Do not quietly certify an unreadable source as clean.

## Consumer and Validation Closure

Use existing portable ownership/schema vectors first and add language-independent fixtures for the new source/domain contract before moving behavior. Native installed TypeScript plus existing Ajv and filesystem oracles supply independent controls. Use semantic results, not body hashes. Include empty/missing facets, alias imports, format shape limits, surface/config identity, inference coverage and non-following source discovery as relevant to each bounded slice.

Re-read current root imports/exports, dynamic/source-as-data references, library test entry points, root compiler fixtures, actual project inputs, `.vscode/🧩️launch.seed.jsonc` and normally generated launch output. Existing workspace targets include artifact-field-parity test/report/enforce and abstraction-ownership test/verify. Do not replace registered routes with direct function-only evidence. Add focused ownership tests and appropriate task/launch registration through `📜️script.ts` only. Validate every new ancestor context and prove no owner imports root.

Do not run all root verify behavior or remote graph export just to test an extraction. Execute only the scoped proof/report routes and inspect output before any broader gate. No product policy weakening, new runtime libraries, compatibility facades, Git mutation, AGENTS edit or ticket/goal lifecycle action.

Keep exact ownership maps and executed native/direct/Nx evidence in separate retained reports per bounded slice. Temporary output belongs only under this ticket's generated child; keep authored controls and Markdown. This packet is preparation, not completed extraction.
