# Remaining Print Native Identities And Domain Bodies

The graph staging lane is independently accepted. This coordinator follow-up inventories remaining native identity files so the first eight-body audit is not mistaken for exhaustive macro ownership classification. The current print/🖋️latex directory contains97 .sty/.cls files. The narrow TeX shim parser returns one thin delegation (graph),17 implementation decisions and79 unresolved decisions. This is a pure body screen, not an actual fixed-disposition inventory for every identity; unresolved is not a proven implementation violation.

## Additional Concrete Owners

- semio-components.sty: 544 newline segments.
- semio-core.sty: 656 newline segments.
- semio-fonts.sty: 20 newline segments.
- semio-logo.sty: 124 newline segments.
- semio-table.sty: 1458 newline segments.
- semio-tokens.sty: 92 newline segments.
- semio-tree.sty: 610 newline segments.
- semio-viz-axis.sty: 146 newline segments.
- semio-viz-charts.sty: 82 newline segments.
- semio-viz-data.sty: 169 newline segments.
- semio-viz-layout.sty: 617 newline segments.
- semio-viz-mark.sty: 365 newline segments.
- semio-viz-scale.sty: 325 newline segments.
- semio-viz.sty: 344 newline segments.
- semio-window.sty: 3707 newline segments.
- semio.cls: 85 newline segments.
- zukunftbau.cls: 52 newline segments.

This includes the earlier seven large remaining macro bodies, smaller real domains (fonts, logo, visualization axis/data/scale/dispatch), chart package composition and two native class definitions. Generated semio-tokens remains governed by its actual token producer; source classification does not make it authored. Source hashes are report provenance only.

## Chart Registration Cohort

All79 unresolved files are semio-viz-chart-*.sty. A complete narrow line screen removed only blank/comment rows and the two native declaration rows, then matched every remaining line against the exact three-literal-argument SemioVizChartKind form. All79 files matched, comprising1910 registration rows and zero unmatched body rows. This is a text-shape screen, not a general TeX parser or native execution proof. Selected bodies were inspected directly; an earlier full-cohort printout was truncated and is not claimed as a complete manual read.

The cohort represents domain-specific registration data executed through the visualization registry. Retain each family identity and rows under its semantic owner with anonymous implementation/data leaves and native loader glue where required by active consumers. Literal registrations do not make these files empty identity shims. No blanket generated marker or package exemption is authorized. Decide any neutral data schema from actual registry/generator consumers, not from filename patterns alone.

## Next Bounded Execution

Use the accepted graph staging and strict literal shim controls. Extend exact librarySources and inferred/explicit producer inputs for each macro owner, including Mit producers. Preserve canonical 📐️.tex to compiler _.tex mapping, inert metadata/target validation and hostile caret/control/parent/empty-segment cases.

Extract macro domains separately with portable ownership contracts and focused native controls. Rebase class/package source-as-data and relative loads. Class shims need a precise native ProvidesClass contract: the current grammar recognizes ProvidesPackage only, so do not admit arbitrary TeX commands. Preserve @ catcode behavior supplied by native package/class loading. Generated tokens need their real producer/output closure and freshness. Register exact ancestor contexts and scoped source rules; ordinary TeX documents remain documents.

Retain the79-family map and validate actual registry values with an independent native fixture. Do not move all bodies into a generic anonymous monolith. Repeating all97 native compilations needs a meaningful dependency reason.

Raw current bodies/decisions: generated/coordinator/print-native-identity-bodies-current.json. Corrected complete registration screen: generated/coordinator/print-chart-registration-screen.json. An initial over-escaped exploratory regex was corrected before this report; its empty command screen was not evidence. No product change or native execution occurred in this inventory.

## Independent Native Registry Prerequisite

Root ran the real semio class and chart loader under pinned offline Tectonic0.16.9, staged shared library and exact font search paths. A native control exports the registry sequence and both property maps through linear native iteration. It passed:79 actual package loads,1910 source registration rows,1910 unique native IDs, zero duplicate IDs, and5730 exported typed rows. Native insertion order and every family/option value exactly match the independent source/loader projection. This proves full registered data consumption, not drawing1910 charts or native rendering of all97 packages. Raw result and native outputs are in generated/coordinator/chart-registry-native/result.json and its sibling logs/TSV/PDF.

The first authored probe omitted the class's explicit options/metadata and failed at begin-document; the control was corrected against the established actual graph fixture preamble. A subsequent per-key property lookup instrument exceeded60s and180s budgets, so no native acceptance was claimed from it. Linear sequence/property-map iteration then passed within120s. These were probe construction/performance observations, not attributed product defects. Retained authored inputs are 📋️latex-literal-controls/🧪️chart-registry.tex (accepted linear control) and 🧪️chart-registry-lookup-control.tex (bounded unsuccessful lookup instrument). Generated outputs remain disposable.
