# Playground Session Output Ownership Correction

Terra's independent UI/generated audit confirmed current registry freshness and valid generated topology, but the registered `@semio-tech/framework-os-dev:check-playground-session` failed after both dependencies succeeded. It traced the failure to two different supported input modes writing the same canonical output, rather than an unexplained stale file. Use the retained final Terra report for complete evidence and exact current paths.

The canonical source is `🧰️framework/🛍️products/💻️os/🔨️modules/🧑‍💻dev/🤖️generated/🎮️playground-session/🟦️.ts`. The explicit generator, preview and check in the dev package `📜️script.ts` always render `DEFAULT_HOST_VARIANT`, currently `s`. `ensurePluginRegistry(filterPlugin?)` in the same script separately selects `filterPlugin ?? SEMIO_PLUGIN ?? PLAYGROUND_APP_KIND ?? DEFAULT_HOST_VARIANT`, then writes the selected variant to that same canonical file. The audit found the canonical file holding a valid `flow-extension-brep` session, host mode false, 3,166 bytes; the default rendering was 12,139 bytes, host mode true. This establishes a shared output/input authority conflict. It does not establish which worker invoked the writer and does not justify overwriting a selected live session as an audit action.

The registry already has an ordinary `session <variant>` command staging `dist/sessions/<variant>/🟦️session.ts`, explicitly intended not to overwrite another variant. Terra traced the dev Vite config at lines 36/127: `virtual:semio-playground-session` resolves to that variant-scoped staged source, not the canonical default source. Re-read this and all other consumers before editing. Preserve active variant-specific deployment/runtime behavior. Reserve canonical generated output for its declared default-input contract and remove or restrict the conflicting explicit/dev writer. Do not add a compatibility route, default-only refresh workaround, circular acceptance based on reading whichever variant happens to be on disk, or a blanket generated-source exemption.

The variant-scoped staged source is itself named `🟦️session.ts`; include its anonymous-leaf correction and real stageArtifacts output/consumer keys if it is authored by this producer and remains persistent. Use a semantic session/variant owner and one canonical source kind leaf, preserving artifact identity through an explicit producer/consumer contract. Inspect existing tests before changing the semantic ownership schema.

The coordinator also found stale source interception in renderer `🧑‍🎨engine/🧪️tests/🧊️wgpu-browser-boot-cache-inputs/🟦️.ts`: the session variable points to the anonymous canonical source, but its bundler plugin filter is `/session\\.ts$/`. Terra confirmed this does not match the canonical basename, while the test asserts zero reads to demonstrate WGPU independence. Target the actual canonical path in the predicate so the negative oracle can observe a regression; preserve the independent Bun/esbuild checks. Inspect the kernel comment near lines 2606–10 describing concurrent mutable generated sessions as fallback context, and update it only if this correction makes that explanation obsolete.

Before implementation, add a language-neutral regression describing default and two explicit variants, with expected source/identity separation. Validate with installed native bundler tooling or existing third-party parser and real registered producer/check routes. Prove that preparing explicit variants leaves the canonical default source unchanged, that two variants coexist with correct session identities, that the Vite alias/native import resolves each selected staged source, and that generator preview/check agrees for the default source after explicit preparations. Avoid expensive unrelated full plugin builds if the existing session producer and fixture exercise the same output route. Runtime logs and post-run path/hash evidence must substantiate behavior.

Work in this existing ticket, preserve simultaneous edits to dev/registry scripts, keep permanent executable commands in `📜️script.ts`, update schema/contracts and launch seed/derivation only where needed. No Git/AGENTS/lifecycle changes, new runtime dependencies, migration scripts or broad OS refactor. Retain `📓️sol-session-output-ownership-2026-09-12.md` with exact files, tests, limitations and observed source/input identities. Disposable outputs belong under ticket `🗑️generated/sol-session-output` and must be removed when this lane finishes.
