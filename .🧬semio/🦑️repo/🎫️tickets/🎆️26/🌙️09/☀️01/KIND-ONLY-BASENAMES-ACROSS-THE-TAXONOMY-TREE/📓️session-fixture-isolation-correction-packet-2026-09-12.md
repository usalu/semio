# Session Regression Fixture Isolation

Coordinator source review found a concrete isolation defect in the new session ownership regression after its production correction completed. The production writer separation and native outcomes remain useful evidence, but the fixture is not read-only with respect to the active shared workspace. Terra has been instructed not to execute the mutating fixture during its read-only audit.

In registry `🧪️tests/🎮️playground-session/🟦️.ts`, the second test spawns the real dev script `generate playground-session`, then calls `stagePlaygroundSession(row.variant, absolute(fixture.staging.rootPath), projection)`. `SEMIO_SESSION_OUTPUT_TEST_ROOT` redirects only compiler scratch outputs; `absolute(fixture.staging.rootPath)` is the live registry `dist/sessions` tree. Thus the test regenerates the shared canonical source and republishes real note/puzzle5d sessions. It has no isolated producer root. Restoring earlier bytes afterward would race other workers and is not an acceptable fix.

Correct this bounded test/producer boundary with the original Sol executor after a slot frees. All fixture-created canonical and variant outputs must live under the caller-owned temporary fixture tree. Keep the tested default/variant identity and immutable-default semantics, native Bun/esbuild/import and actual Vite alias behavior, preview/check agreement and positive WGPU interception control meaningful. Reuse actual injectable producer/output-root/context boundaries where present; if a small domain-neutral staging-path resolver or generator-output context is needed, implement it explicitly and share it with real consumers, not as a test-only compatibility path. Avoid mock-only tests that merely repeat a hardcoded string or import a source copy different from the production route. The fixture must run through its existing registered package/Nx test route with a focused selector, not only direct bunx.

Record a before/after content hash and mtime inventory for the live canonical source and existing staged receipts/files around fixture execution. It must remain identical, while private fixture artifacts demonstrate two different variant/plugin identities and the default invariant. The test must be valid on a clean checkout with only its declared prerequisites and must not depend on previously staged note/puzzle5d output. Keep progress, cancellation and scratch cleanup; no shared cache reset or mutation of another worker's active variants.

OS source work is active in dev/Vite/registry consumer paths. Re-read the live paths and coordinate changes with that Sol worker; do not restore a moved configuration or old `🟦️session.ts` leaf. No Git/AGENTS/lifecycle changes or new runtime dependencies. Retain correction evidence in `📓️sol-session-fixture-isolation-2026-09-12.md`, with exact changed files and native limits. Own disposable output belongs under ticket `🗑️generated/sol-session-fixture-isolation`.
