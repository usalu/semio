# Tooling Hooks And Configurations Follow-Up

## Devcontainer Evidence

The current devcontainer image already installs pinned Bun, so its lifecycle hooks can use the normal Bun/Nx script routes. `postCreateCommand` currently is the argv array bun nx run workspace:deps-javascript. `postStartCommand` and `postAttachCommand` still invoke named shell scripts. The six current named shell leaves are compose-entrypoint, gitkraken-launch, neo4j-host-forward, neo4j-mcp-run, post-attach, and post-start. Inspect every active caller before changing them.

The current README explicitly describes compose-entrypoint.sh as a legacy helper kept for existing callers, while post-start owns actual startup. The user forbids legacy support. Prove current reachability and remove unused legacy input or integrate the necessary operation in the real current route; do not preserve a compatibility shell shim merely because it existed. Domain operations belong in the appropriate semantic owner with anonymous implementation leaves. Permanent executable script routing uses the existing 📜️script.ts conventions, registered Nx targets and launch seed/derived entries. Preserve zero-touch native Windows/macOS/Linux and devcontainer behavior without requiring a newly absent bootstrap runtime.

The native Go CLI fixture still tries to read a nonexistent post-create.sh; actual current creation is the explicit dependency-only Nx route above. Update the test to the current declared behavior when that fixture is owned, and preserve the separate explicit build command model. Do not restore the removed hook or change current creation to build every application just to make the old fixture pass.

## Tool Configuration Authority

Review root and package Vitest, Vite, Playwright, Tailwind and related named TypeScript configurations against the actual installed tool’s configurable path/loader contract. Required file discovery, default conventional names and explicitly configured paths are distinct. Move implementation/configuration ownership to the appropriate semantic tool owner with anonymous leaves, updating consumers and declared exact contracts; retain only truly required minimal entry glue. The Storybook-specific configuration and authored stories are covered by its own packet.

## Cursor Migration Inputs

Inspect .cursor/add-playhost-deps.ts and .cursor/fix-playhost-extraction.ts for active consumers. Old one-time migration inputs are not active implementation infrastructure. Preserve authored inputs under a governed ticket if archival is appropriate, with a retained disposition report; do not delete them as generated output or add a blanket .cursor exclusion that hides future active source.

## Execution And Validation

Use a portable argv/lifecycle fixture first and an existing native shell/parser/process oracle to verify equivalent delegation, environment and cancellation behavior. Run bounded registered tests and configuration-load checks; avoid launching unrelated host services or installing software just to inspect routing. Preserve concurrent development/bootstrap task changes and all AGENTS files. Keep outputs beneath this ticket’s generated tree and retain a report with exact source/consumer/contract moves, reachability evidence, actual checks and limits. No modifying Git, legacy aliases, permanent migration scripts, or ticket/goal lifecycle mutations.
# Root Misplaced Input Provenance Review

A coordinator root-directory listing also shows `09/`, `T/`, `http:/`, `🧬semio/` without the hidden prefix, and root `🗑️generated/`, alongside conventional `test-results/` and `storybook-static/` outputs. Their contents and provenance have not yet been inspected. The taxonomy closure must distinguish misplaced ticket/tool inputs from active authored source and exact generated output contracts. Do not delete or hide them merely by name, follow symlinks into another checkout, or assume all are owned by this task. After evidence establishes their role, preserve authored inputs under the proper governed ticket/semantic owner and remove only confirmed disposable outputs or empty obsolete chains. Use the relevant existing repository cleanup skill if it materially supports that bounded correction, honoring the user's no-modifying-Git and input-preservation requirements.

A subsequent no-follow filesystem probe found: `09/` contains one Python input at `09/☀️02/PUZZLE-3D-END-TO-END/🗑️generated/b16-extract.py`; `T/` contains one Nx lock at `T/🗑️generated/nx-renderer-fixture-moves-data/project-graph.lock`; `http:/` has one empty descendant directory; `🧬semio/` has eight empty descendant directories; root `🗑️generated/` has five Nx workspace-oracle cache/graph files beneath `nx/workspace-oracle-2`. No symlinks occurred in these five trees. The Python input must be preserved after provenance/reference review. Empty chains can later be removed using empty-only operations, which safely fail if another worker populates them. Lock/cache cleanup needs current ownership/activity checks rather than an age-only deletion rule.

## Exact Configurability Evidence Still Required

The coordinator inspected the live fixed-filename catalog after the Storybook setup exception was removed. Several executable configuration exceptions assert configurability `unconfigurable` while their own verification is an explicit `--config` route: `vitest-config`, `tailwind-config`, `postcss-config`, `eslint-config` and `dependency-cruiser-config`. `configurableEntryContracts.vitest-config-entry` simultaneously declares an anonymous `🟦️.ts` configuration selected by `📜️script.ts:runVitest --config`. The exact registered root schema-oracle runner still names literal `vitest.config.ts`, and its documentation mentions editor discovery. Inspect the installed runner and actual editor integration constraints before deciding which exact native discovery glue is required. Do not declare a configurable executable body mandatory merely because a tool has a conventional default basename; equally, do not break an actual zero-touch editor consumer by considering only the CLI. An exact literal shim, when truly required by an active tool, must contain only minimal import/export or registration glue, with implementation/configuration computation in the neutral source owner.

A visible-file `rg --files` probe excluding node_modules/Git/ticket/goal history found 43 `vitest.config.ts`, two `tailwind.config.ts`, and one each of `postcss.config.ts`, `eslint.config.ts`, `eslint.config.mjs`, `.dependency-cruiser.cjs`, `.vscode-test.mjs`; two `build.rs` files also remain. This is a 52-path review population, not 52 proven moves, and most admitted fixed names are absent from the focused 259-finding filename census. Validate real consumer contracts and source roles before updating these exceptions. The current root `vitest.config.ts` is a deliberate no-tests local configuration (`include: []`, `passWithNoTests: false`), not an aggregator to rebuild or restore. No blanket configuration or basename exemption is an acceptable closure.

The completed package-body grammar correction is separate evidence: it recognizes reachable multiline Vitest helpers and closed imported BundleScript delegation; it does not prove a fixed physical filename is required. Preserve that policy and its seven implementation-negative cases while fixing physical/native-tool ownership.

## Current Cargo Build-Entry Selector Evidence

Coordinator-native prerequisites are now in `📓️tool-configuration-loader-prerequisites-2026-09-12.md`: Vite loads both emoji-anonymous and underscore controls, ESLint's anonymous override produces the expected lint error, and the VS Code test loader preserves the anonymous config's path semantics. Installed Vitest Explorer consumes `vitest.configSearchPatternInclude`, while the repository writes the unrecognized `vitest.configSearchPattern`. Move editor discovery through the actual setting together with CLI/default/source-token consumers; no new root aggregator. The focused configuration population is 50, with two Cargo entries accounting for the earlier total of 52. The named styling Tailwind preset is an additional semantic owner behind its two package shims. Re-read the report's native/editor limitations before execution.

Both live native build-script manifests already contain an explicit selector, `build = "build.rs"`, at line 7: OS Infinite `📦️packages/🦀️rust/Cargo.toml` and OS renderer WGPU `🎯️targets/🧊️wgpu/📦️packages/🦀️rust/Cargo.toml`. Thus these particular consumers are not relying on default filename discovery. Review the actual Cargo target/producer contract and point the native selector at an anonymous owned build entry where valid; prove it with Cargo metadata and the bounded native build route. WGPU's generated package builder adapter/catalog is actively changing in the OS source lane; preserve its completed source/package split and update the real catalog/digest/producer authority together. Do not retain an unconfigurable-filename claim contradicted by the explicit native selector, and do not duplicate the existing semantic builder implementation merely to remove one shim.

## Current Configuration Consumer Baseline

Use `📓️current-vitest-configuration-consumer-baseline-2026-09-12.md` before moving configuration sources. Installed Vite's native loader under Bun evaluated43 actual exports:42loaded, one presentation styling-export failure. That direct consumer repair is assigned to the app/print executor. A separate native filesystem projection found concrete setup/selection/alias follow-ups in Animate,2D,Hub admin,OS MCP,WGPU andRemodel, with exact limits. These are not suite executions or reasons to broaden exclusions. Regex alias `$1` placeholders are explicitly excluded from the apparent missing-literal-path findings. Preserve current source/producer semantics, and revalidate only affected boundaries after ownership moves.

The same report now explicitly adds four named Vite sources and the Demonstrator Playwright source that were outside the focused50, plus the previously identified named Tailwind preset: known review population56. All five additional sources parse with installed TypeScript, but none was executed by that additional review. OS dev and Demonstrator configurations contain actual domain composition; preserve their receipt/module/publication owners and actual managed-service launch context rather than disguising behavior as mandatory native glue.

## Coordinator Selector Closure — 2026-09-13

Preserve the eight exact coordinator product paths in `📓️coordinator-test-selection-consumer-repair-2026-09-13.md`. Admin current Node command owner/environment setup and test discovery are repaired; actual Nx18pass. MCP includes six native-case owners, keeps the core in-source consumer and excludes only its import-only helper; focused core8pass and inference-process argv points to the current anonymous source. Remodel selects its schema suite and all134 fixture consumers/35 mutation tags at their actual subset paths; restored full suite has1023pass/330 current parity failures, with no missing-path/module errors. Do not redo these selector repairs or weaken domain expectations to hide the exposed parity result. Anonymous configuration filename/editor work and the remaining Animate/2D/WGPU consumers are separate pending tasks.

Cursor intake disposition is complete in `📓️coordinator-cursor-input-preservation-2026-09-13.md`: the two unreferenced old one-time helpers, with46 absent old target coordinates, are preserved byte-for-byte under ticket-owned semantic concerns. Actual classifier and native TS/Ajv closure2/2 pass. Do not repeat their move, execute them, delete retained inputs or extend this disposition to other Cursor/temp/devcontainer source. Wave-seven census predates this local change.

Inactive devcontainer helpers are disposed in `📓️coordinator-devcontainer-input-preservation-2026-09-13.md`: compose-entrypoint, neo4j-host-forward and neo4j-mcp-run are preserved as anonymous ticket inputs after current caller/configuration review. The README's compatibility paragraph is removed. Active post-start, post-attach and GitKraken launcher ownership remains queued; current lifecycle projection is unchanged and native syntax/portable preservation controls pass3/3. Do not restore a compatibility entrypoint or conflate this retirement with lifecycle implementation acceptance.

The stale Go post-create consumer is repaired in `📓️coordinator-devcontainer-bootstrap-consumer-repair-2026-09-13.md`: actual registered bootstrap case9subcasesgreen, existing neutral runtime-bootstrap proof/native shell controls green. Preserve the current dependency-only container creation argv; native bootstrap has a separate setup/build contract. Active lifecycle semantic extraction remains queued with Terra's lifecycle preaudit.

Empty malformed roots http: and non-hidden 🧬semio are corrected in `📓️coordinator-empty-directory-boundaries-2026-09-13.md`: fresh no-follow inventory found no files/symlinks; native empty-only removals removed2+9directories and final filesystem checks confirm both absent. No product file changed. The09Pythoninput,Tlock and root generated-cache provenance remain pending; do not generalize this empty-only result to them.

## Current Full-Hook Fixture Boundary

Root read the entire active post-attach shell after the source-only Go repair. The existing exhaustive Go attach fixture supplies a private WORKSPACE/HOME and skips tool/extension installation, but the script computes REPO_ROOT from its real source location. Its RepoConfigure block can still invoke the real canonical client binary with configure --repo actual checkout if that binary exists. Do not treat this full-hook fixture as isolated solely because workspace/home are private; the executor must own/inject that operation or stage the complete private source context. Root did not execute this hook. The old exhaustive case also expects MCP config files and go run ./repo/client/mcp/go payloads that are absent from the current shell body; these are source-consumer debt, distinct from the now-green bootstrap-source case.

Root09 misplaced input is now preserved in `📓️coordinator-misplaced-puzzle-input-preservation-2026-09-13.md` with unchanged bytes and unchanged native syntax rejection, no execution. Four empty predecessor directories are gone. The active Puzzle ticket and valid .w-g3-ticket marker remain untouched. T lock and root generated-cache provenance/activity are the remaining root-output questions.


## Preserved Router Probe Closure

`📓️coordinator-router-probe-input-preservation-2026-09-13.md` records the completed byte-identical preservation of the inactive authored probe at `📋️router-grammar-inputs/🧭️body-classification/🟦️.ts`, including installed-TypeScript parsing with zero diagnostics. The old root probe and its generated JSON were removed after all four historical result rows were retained in Markdown. The empty root `🗑️generated` is absent. Attribute the exact created and removed identities from that report; the report itself is retained authored evidence. This is provenance and placement work, not a current router classification or runtime result.
