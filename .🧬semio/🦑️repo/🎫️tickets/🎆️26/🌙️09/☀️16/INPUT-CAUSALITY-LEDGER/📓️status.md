# 🎯️ Input Causality Ledger — status

Ticket opened 2026-09-16 by session ⚪37cccb68 (Opus 5). Start commit: `🗑️generated/start-commit.txt`.
Origin: fem2d "swallowed click after marquee" report; investigation refuted the busy-drop hypothesis on the
direct-actor path and found the real gates (see `📓️design-input-causality-ledger.md` §0).

## Definition of done
1. Laws L1–L7 of the design hold, with pure law tests for the ledger, CAS registers, sample batching, refusal surfacing, causal dequeue and the browser-actor queue.
2. The 8 s `setActiveUtility` echo-off gate and the `pointerleave`→`pointerUp` forgery are deleted; no `performance.now()` remains in `onAction` semantics.
3. Interaction verbs emitted by a guest fold in-reactor (one round trip per pick/hover/marquee, no host re-dispatch).
4. Acceptance probe (design §6) green on the fem2d React lane and on one hub-mounted lane.
5. Crates green natively + wasm32-wasip2; existing laws green.

## Log
- 2026-09-16 investigation + design written (phases 1–4). No code changed yet.
- 2026-09-16 evening — implementation wave 1 (8 agents, disjoint files) landed, all foreground-verified:
  - contract module `🏛️ShellHost/🎯️input-ledger/🟦️.ts` (ledger, causal key, CAS register, refusal vocabulary + de/en notices, gesture sample lane) + `🧪️tests/🎯️input-ledger` 35 laws, `input-ledger-check` script;
  - `📬️mailbox`/`🧵️turn-scheduler`: optional `order` key (ordered-only reordering rule), 12 laws;
  - runtime plumbing: `PluginDispatchHintV1 { order }` from `handleAction/handleCommand` → `AppChannelClient.command` → `runQueuedTurn` → `serializeCommandIngressForActor(…, order)`; wgpu bridge twin with a bounded ordered pending list; engine-contract + wgpu laws;
  - browser-actor transport: `BrowserActorActionMailboxV1` is a bounded FIFO (cap 32, never throws synchronously, timer starts at send, `browserActorActionRefusalReasonV1`); worker `dispatchAction` queues instead of `action-busy`, awaits UI-patch/view-refresh quiescence instead of `action-owner-mismatch`, accepts the painted revision; backbone-envelope-io laws;
  - reactor: guest-emitted `ReplayShellCommand{interaction verb}` folds inline in the same turn (`apply_interaction_verb`, `dispatch_emit` wrapper + typed-operation ladder unit), 4 Rust laws; `DispatchAction{interaction verb}` fold + guest test flips in flight (wave 2);
  - guests: fem2d/draw/layout/wires/vcs/generation2d `CanvasPointerMove.samples`, `CanvasPointerUp.cancelled`, laws per crate;
  - ShellHost: `onAction` → `Promise<InputOutcomeV1>`, every former silent gate is a typed refusal (plain console line + throttled notice), CAS utility/tool registers with `expectedGeneration` stamped by UtilityTree/ShellHelpers, 8 s echo-off DELETED, follow-ups carry `causedBy`/`order`; Canvas2dHost drives `createGestureSampleLaneV1` (`samples`, `cancelled`), react-renderer maps pointerleave/pointercancel/lostpointercapture to a cancel; UtilityTree picker deactivates only on an explicit press.
- Battery `🐍️input-battery.mjs` on the live `:6086` lane (old wasm, new host): PASS — marquee-click 6/6 cleared with exactly 2 sends settling after mouseup (drain 129–196 ms steady state; before: 1–3 s), utility-toggle deactivates at 1.5 s (0 echo-off lines), utility-switch 2 hops, refusal-visible: 337 issued / 337 applied / 0 refused. Reports: `🗑️generated/battery-host-only-3/`.
- Blocked → unblocked: first wasm activation failed on a peer's mid-refactor fem3d (`MeshedSolid`); recompiled ~1 h later, activation re-run (`🗑️generated/activate-2.log`).
- 2026-09-16 late — session restart killed the `:6086` serve; every fresh react serve booted BLACK on `The requested module '…/use-sync-external-store/shim/with-selector.js' (then …/scheduler/index.js) does not provide an export named 'default'`: a peer's builder change (`8773331d23`, `playgroundSceneHostOptimizeDeps`) excludes fiber+drei from prebundling, and Vite cannot serve the CommonJS packages that excluded graph imports. Fix: `PLAYGROUND_SCENE_HOST_CJS_INCLUDE = [scheduler, stats.js, use-sync-external-store/shim/index.js, …/with-selector.js]` in `🖱️ui/🎨️styling/🏗️builder/🌐️vite/🟦️.ts` + law. Serve recycler: `📜️recycle-serve-6086.sh` (a peer's registry regen makes Vite "restart" without a listener — recycle by pid).
- Battery on the rebuilt fem wasm (folding code present, served 80.7 MB): PASS 4/4 — marquee-click 6/6 cleared, ≤ 2 sends after mouseup, drain 133–208 ms; utility-toggle/switch pass; 0 refusals. OPEN: the guest's `interactionSelect` still round-trips through the host (18 `replayShellCommand dispatch` lines) — the inline fold is proven for `dispatch_emit` but `canvasPointerUp` runs on the typed-operation ladder, where the fold unit (`publish_mounted_typed_inline_interaction_unit`) does not fire in the browser. Next: reactor lane on the ladder path with this evidence.
- 2026-09-16 night — typed-ladder fold leak found and closed (reactor lane, `🔌️plugin/🦀️.rs` + contract test only):
  - LEAK POINT: `publish_mounted_typed_operation_unit` installs the completion (`mounted.publication = Some(..)`) and, in the SAME call, falls through to `emit.effects.pop()` → `typed_effect_outbox` for an emit with no durable lane (fem2d `canvasPointerUp` = `effects: [interactionSelect], ui_scope: partial`). The fold gate `mounted_typed_inline_interaction_verbs_are_next` had been consulted BEFORE that install (it reads `mounted.publication`, still `None`) and answered false, so the verb was on the outbox one pump before the fold could ever run; `advance_typed_operation_output` handed it to the host that turn. Per-turn law now: no host-visible turn of a typed operation carries one of the six interaction verbs as an effect.
  - FIX: verbs are peeled the instant the ladder takes the completion (`take_inline_interaction_verbs` at install) and parked on the app keyed by operation (`typed_inline_interaction_verbs`, dropped with the operation's exact removal); the install step hands the pump back to the ladder so the `InteractionWrite` lane / inline fold run BEFORE the `Ui` page is minted; the fold unit takes the parked verbs. Leftover: the last folded verb's `InteractionView` (`TypedOperationLeftover { operation, view }`) rides the `Ui` progress frame's `output` AND — because the host reads a typed command's leftover only from its own reply (`applyLeftoverInteractionView(response.output)`) — `plugin_exchange` drives ≤ 16 extra ladder units for the operation the command started (stops on the fold, on a presented page, or when nothing is runnable) and merges `interactionView` into that reply's `{operationId, generation}` witness (`merge_typed_operation_leftover_into_reply`). Host untouched.
  - In-crate: `typed_ladder_never_hands_a_guest_interaction_verb_to_the_host_on_any_turn` (KeyedTestApp `PickItem` on the typed ladder, drains `take_typed_operation_effect` after EVERY `advance_typed_operation_publication` like the browser; fails-before at turn 2 with the `ReplayShellCommand` and an `Effect` lane page) + `typed_operation_leftover_merges_into_exactly_the_carrying_reply`; 8 fold laws green.
  - Browser (fem2d React :6086, served wasm 80715647 → 80770506 B): battery `🗑️generated/battery-ladder-fold/` PASS 4/4 — marquee-click 6/6 cleared (Inspection reads Summary), ≤ 2 sends after mouseup, drain 20–211 ms (first row 1.2 s = cold), 0 refusals (473/473); `replayShellCommand dispatch` lines: 0 (was 18); 18/18 `canvasPointerUp` settle with `effects:0`; 6 `leftover Inspection tab` (leftover reaches the host through the reply). The serve wedged after each materialize (registry regen) — `📜️recycle-serve-6086.sh` each time.
  - Disk: the shared cargo build dir hit "No space left on device" mid-materialize; pruned the idle `debug`, `wasm32-unknown-unknown/debug` and `wasm32-wasip2/debug` incremental caches (~42 GB), left the peer's live `wasm-dev` incremental alone.
  - Pre-existing, NOT this lane (fail identically with the fold disabled): `fixture_contract_is_anchored…` (reactor source grep), `ephemeral_snapshot_carries…`/`interaction_*` (registryless `SetLabel` typed dispatch → `missing-factory`), `retained_latest_wins_reserved_slots…` / `retained_presence_fills…` / `retained_window_input_recursive…` (a peer's in-flight worker-session/store refactor in the working tree).
- 2026-09-16 22:15 — END-TO-END PROVEN on the fem2d React lane with the rebuilt wasm (`🗑️generated/battery-ladder-fold/`): 4/4 lanes pass; guest interaction verbs fold in-reactor on BOTH the emit path and the typed-operation ladder (0 host re-dispatches, was 18; the leak point was `publish_mounted_typed_operation_unit` popping effects before the fold gate — verbs are now peeled at install and the leftover rides the carrying reply); ≤ 2 pointer sends settle after mouseup (drain 20–211 ms; before 1–3 s); utility toggle-off within 8 s works; Marquee→Lasso at 140 ms lands; 473 inputs issued / 473 applied / 0 refused. `DispatchAction{interaction verb}` folds too; guest tests flipped where they run through the reactor (layout focus-preflight, wires pointer-down green; draw/animate/layout pointer-down suites fail on PRE-EXISTING harness gates — `BatchOnlyPendingRewrite` missing-factory, draw wire decoder, animate catalog-authority — documented in the agent log).
- Definition of done: 1 ✅ (laws: ledger 35, mailbox 12, runtime order 2+3, browser-actor FIFO + worker wait, reactor 8, per-app guest laws, optimizer CJS law); 2 ✅ (echo-off + forged pointerUp deleted, no `performance.now()` in onAction semantics — the refusal-notice throttle uses it only for toast spacing); 3 ✅; 4 ◐ fem2d lane ✅, hub-mounted lane NOT browser-run (collab e2e boots the full `s` lane, blocked by the peer's fem3d refactor tonight; the browser-actor FIFO + worker wait-for-ack are law-covered in `🧪️backbone-envelope-io` and `🧪️space-artifact-creation-owner`); 5 ◐ touched crates check green (`semio-framework-plugin`, fem2d/draw(lib)/layout/wires/vcs/generation2d), wasm32 built via `fem-plugin:component-dev`; pre-existing red suites listed above, none introduced.
- Also fixed on the way (blocking verification, peer-caused): fresh react serves booted black after `8773331d23` — `PLAYGROUND_SCENE_HOST_CJS_INCLUDE` in `🖱️ui/🎨️styling/🏗️builder/🌐️vite/🟦️.ts` + law.
- Ticket left OPEN for the hub-lane browser run once the `s` lane builds again; repo MCP was down all session, so closing must happen with an explicit path later.
- 2026-09-17 — user report: "Die Eingabe konnte nicht zugestellt werden" spam while orbiting / rectangle-selecting in process3d. Measured on a fresh `:6022` lane: every `setCamera` is refused `dispatch-failed` ("typed-operation failed: validation failed: batched item candidate failed its exact fixed fold contract") and `worldSelect` is `undeclared-action` on `process-workpiece` — both PRE-EXISTING process3d faults that used to be silent `[DEBUG] action failed`/`console.error` lines; the ledger made them visible and (wrongly) toasted them. Fix: new `InputOriginV1 = "gesture"` stamped once for every scene host at `renderComponentSceneHost` (`🗣️Interpreter/🟦️.tsx`, memoised per funnel so host `useCallback` chains stay stable); notices are reserved for CONTROL-originated inputs (`user`/`tutorial`/`replay`) — gestures are logged only. Law added (36/36). Probe `🗑️generated/world3d-toast-probe.mjs` on process3d: refusals `dispatch-failed/gesture ×7`, `undeclared-action/gesture ×3`, notices seen: none.
- Handed to the process3d owners (not this ticket): `setCamera` fold-contract validation failure on every camera sync; `worldSelect` not declared on the `process-workpiece` window kind.
