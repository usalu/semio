# 🔋️ Energy 3d model, tree and inspector — status

Ticket opened 2026-09-16 14:55 by session ⚪c7d63d40 (Fable 5.1 coordinator). Predecessor `26/09/06/ENERGY-PLUGIN-END-TO-END` left the react playground (6106, `energy-react` launch entry) booting with three windows (structure tree, zones table, simulation), 16 examples, retained actions and a finishing annual tool run. Start commit: `🗑️generated/start-commit.txt`.

## Definition of done
1. A 3d viewport window renders the whole model as geometry: zones as volumes, walls/floors/roofs/ceilings from `Surface.vertices_m`, fenestrations as real polygons (arbitrary shapes, not area/height/sill only), shading surfaces; camera orbit; example switch re-renders.
2. Artifact tree panel lists every entity family and every entity (building/site → zones → spaces/surfaces → fenestrations; shading; materials; constructions; loads; controls; HVAC; schedules) with selection wired to the interaction domain, so tree pick ⇄ 3d pick.
3. Inspector panel shows the selected entity's properties and edits them through retained actions (u-value/g-value/vlt of windows, construction assignment, material conductivity/thickness, zone volume/name, surface class/boundary, site).
4. Results mode: after a simulation run the 3d scene colours surfaces by a result field (transmission/conduction loss, solar gain) with a legend; per-surface results exist in the engine output.
5. Crate green natively + wasm32-wasip2; existing laws stay green; restage + browser probes prove 1–4 with console logs and screenshots.

## Log
- 15:20 Exploration wave (5 Sonnet agents, reports `📓️explore-*.md`): no 3d viewport/panels/interaction exist in energy; surfaces+shading carry z-up CCW polygons, fenestrations only area/height/sill (rectangle derived in engine `precompute::place_window` AND the epJSON exporter); no per-surface results survive a run (kernel discards conduction/solar per timestep; run payload is one scalar); the leaf generator has drifted from the crate layout (new kinds are hand-authored by copying a leaf); `reduce()`'s `model_edit` has no `diff_fenestrations` (window field edits would vanish silently); World3d recipe = fem3d `🧱️model` + `world3d_scene`/`scene_surface`, picking framework-owned via `scene.domain_id`; results colouring = per-vertex colours + hand-rolled ramp; the only run→window channel is `ToolRunView.payload` (tick payload ≤ 256 KiB) + `ToolRunDefinition.windows` for per-tick refresh.
- 15:25 Coordinator created the shared `✏️editor/🕹️interaction/🦀️.rs` (domain `energyModel`, 13 granularities, `EnergyModelInteractionSnapshot`, `energy_entity_kind`, `energy_target_id`), mounted it and wired `.interaction(...)` in the manifest, then dispatched wave 1 (5 Opus lanes): A schema (fenestration polygon + new kinds), B 3d model window, C tree panel + inspector + commands, D per-surface results + tick payload + colour module, E probes/verification harness.
- 17:40 Wave 1 first restage (`🗑️generated/activate-energy-react.txt` exit 0) + probes: **the 3d window renders** (`window:energy.model.3d`, 8 meshes/instances for BESTEST 600 = 6 surfaces + 2 windows, ids 40–45/50–51, canvas mounted), example switch to BESTEST 620 re-renders (600→900 is geometry-identical, probe default changed), a 3d pick selects through the `energyModel` domain (`selectedIds=["44","40"]`) and the inspector shows the picked surface's form (`🗑️generated/energy-3d-w1b/3-pick.png`); **the tree panel publishes 48 rows** (zones/surfaces/windows/materials/glazing/constructions with human labels). Open: host dispatches `setCamera` on orbit which the window does not declare (→ lane B), the legend caption path faults `DuplicateSiblingKey parent=#0 key=#0` once the run ticks (→ lane B), the panels probe looked under the wrong namespace (`panel:energy-model-artifact/`, `panel:energy-model-inspection/` are the real ones → lane E), the results probe's run-state regex misses the new simulation-window text (→ lane E). A peer's live `World3dHost` refactor (`useThree` from `sceneHostPort.fiber`, 17:36) caused one transient `ReferenceError` on the first probe only.
- 17:45 Coordinator wired lane D's results into the 3d window (`render_body`: `surface_energy_from_run` → `surface_colors` → `legend_caption` → `model_window::render(.., overlay, caption)`); lib check green.
- 18:30 Lane E re-probed (`🗑️generated/energy-panels-w1h`, 9/12): tree 48 rows, inspector shows 31 fenestration / 19 surface fields, and the inspector DOES dispatch `set-fenestration-property {uValueWM2K, id 50, 1.5}` — refused by the shell: `window kind energy.model.3d does not own action set-fenestration-property` (panel actions dispatch in the ACTIVE window's context; same root cause as the `setCamera` drop). Also: a selected surface's own window rows collapse into a non-expanding `…windows.more` marker. Both → lane C (ownership of inspector actions on every window kind; paging quota). Results probe parsing fixed, to be re-run after the caption fix restage.
- 18:50 Coordinator killed 16 deadlocked lane cargo checks (0 % CPU, no rustc children, 15–75 min holding unit locks in the shared fine-grain cache — the known wasm/native deadlock shape); lanes re-run their gates.
- 19:35 Lane B complete (caption key fix, persisted setCamera via window config + set-camera leaf, 77 window/scene/viewer tests green, suite 6221/4). Restage #2 started.
- 20:20 All four Rust lanes complete (A schema/10 kinds, B 3d window + setCamera + caption fix, C panels/inspector/commands/ownership, D per-surface accumulator + tick payload + results module + set-result-field); each reports the full crate suite at 6222 passed / 3 failed (the three pre-existing `sim::p7c*` wire laws) and wasm32-wasip2 green. Restage #3 probes: **3d probe 7/7** (`🗑️generated/energy-3d-w2`), **panels probe 12/12** (`energy-panels-w2`: tree pick 50 → inspector fenestration form → u-value 3 → 1.5 → history label `Set window 50 uValueWM2K`); results probe 5/7 (`energy-results-w2`: run finalizes with 0 faults, legend text present, meshes not yet recoloured — the served wasm predated lane D's final tick-payload edits). Restage #4 started.
- 20:45 Restage #4 (all lanes): 3d probe 7/7, panels probe 12/12, results probe 5/7 — the run finalizes with 0 faults but the 3d meshes never recolour and no legend caption ever appears (no 3d re-render observed during the run). Lane D resumed to write the app-level recolour law (toolRunStart → pump → render `energy.model.3d` → caption + colours) and fix what it exposes; lane E resumed to timeline the 3d host's revision/caption during the run and to drive `set-result-field` from the simulation window's Actions pane. Wave 2 dispatched: W2-A (zone volumes, legend strip, hover, viewer camera, authored-polygon test), W2-B (result-field selector in the inspector, app-level ownership of every keybound verb, material name/roughness, construction layer editing, tree selection markers), W2-C (Sonnet review of the wave-1 diff).
- 21:05 Lane E timeline (results probe 6/7, `energy-results-w5`): meshes recolour during the run (histogram [0,0,0,12,0,12,24,0]→[0,0,48,0,0,0,0,0]) and snap back byte-identical at Finalized → finalize drops the payload (→ lane D); all vertices in one ramp bucket during the run (→ lane D); `set-result-field` switch proven from the simulation window's Actions pane (caption flips to 'Solar transmitted').
- 21:20 Usage limit hit mid-wave-2 (W2-A, W2-B, W1-D follow-up terminated; W2-C review landed: blocker site/thermostat args shape, interzone partner select missing, tree row allocation ignores selection → W2-B). Limit reset; all three lanes resumed via SendMessage.
- 22:20 W2-B complete (app-level ownership of set-simulation-settings/set-result-field/set-run-period/create-*/rename-zone, inspector Results selector, set-material-property widened + change-material-roughness kind (287 kinds), set-construction-property + layer editor + U-value, tree ●/○ markers + Site row, all four W2-C review fixes; 106/106 targeted, suite 6289/4 with the 4th a wall-clock test that passes alone).
- 23:10 Wave 2 landed: W2-A (zone hulls as non-pickable translucent instances, 8-swatch SVG legend strip + min/max, hover tint, viewer setCamera, 5-vertex authored window law; 83/83, suite 6290/3), W2-B (see 22:20), W1-D §10 (framework fix: post-finalize `release_provisional(true)` keeps the run payload; publication gated on `has_energy()`, 16-timestep republish, rank-banded `surface_colors`; app-level recolour law; suite 6290/3). Restage #5 + serve restart (6106 had died during the limit window).
- 23:20 **Final probes on restage #5 — all green**: 3d `energy-3d-w3` 7/7 (9 instances = 6 surfaces + 2 windows + 1 zone hull, example switch re-renders, 3d pick selects through `energyModel`, 0 faults); panels `energy-panels-w3` 12/12 (47 tree rows, tree pick → inspector fenestration form 31 fields → u-value 3→1.5 → history `Set window 50 uValueWM2K`, surface form 21 fields); results `energy-results-w6` 7/7 (run finalizes, meshes recolour into 4 ramp bands during the run AND stay coloured after `Finalized`, legend `Conduction loss · 101.1 – 555.4 kWh` inside the 3d host, `set-result-field` → Solar transmitted recolours again, 0 faults). Coordinator's own final suite + wasm check running (`test-final-1.txt`, `check-wasm-final.txt`).
- 23:35 Coordinator's own gates: `cargo test -p semio-s-artifact-energy-model --lib` → **6290 passed / 3 failed** (only `sim::tests::p7c1_weather_owner…`, `p7c2_preview_typed_view…`, `p7c2_restored_commit_bytes…`, pre-existing); `cargo check -p semio-s-plugin-energy --lib --target wasm32-wasip2` green.

## Closing summary (2026-09-16)
DoD 1 ✅ World3d `energy.model.3d` on editor + viewer: walls/floors/roofs/ceilings from `Surface.vertices_m`, fenestrations as real polygons (new optional `Fenestration.vertices_m`, engine `place_aperture` single source of truth, epJSON import keeps real apertures, 5-vertex authored-window law), shading surfaces, zones as convex-hull volumes (non-pickable translucent), orbit with persisted `setCamera`, example switch re-renders. DoD 2 ✅ `energy.model.artifact` tree: 11 sections (site, zones→surfaces→windows, shading, materials, glazing, gas, constructions, loads, controls, HVAC, schedules), raw-id rows bound to the `energyModel` domain, tree pick ⇄ 3d pick, ●/○ markers. DoD 3 ✅ `energy.model.inspection`: forms for surface/fenestration/zone/material/glazing/gas/construction/thermostat/site + Results selector, `set-*-property` commands (Site/Thermostat args-shape blocker fixed, interzone partner select), `diff_fenestrations` + probe-exact projections so no edit vanishes, 11 new mutation kinds (287 total), app-level ownership of every inspector/keybound verb. DoD 4 ✅ per-surface conduction/solar accumulator (`Results::per_surface`, exact vs zone meter), tick payload `ESF1`, `set-result-field`, rank-banded 8-stop ramp, legend strip + caption, framework fix so the payload survives `Finalized`. DoD 5 ✅ 6290/3 + wasm green; probes 7/7, 12/12, 7/7 with screenshots under `🗑️generated/{energy-3d-w3,energy-panels-w3,energy-results-w6}`.
Owed (lane reports §owed): hull opacity 0.45 (host lacks an alpha lane), `solar_absorbed` conflates outside/inside absorption, 8 192-row payload ceiling truncates silently, `add-construction-layer` admits opaque materials only, honeybee oracle ignores `vertices_m`, nx taxonomy gates not run, the 3 engine `p7c*` wire laws (pre-existing), `eprintln!("[DEBUG]` scaffolding in `🧪️sim` fault paths.
