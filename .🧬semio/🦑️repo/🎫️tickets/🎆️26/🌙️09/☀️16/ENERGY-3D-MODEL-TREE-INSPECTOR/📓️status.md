# 🔋️ Energy 3d model, tree and inspector — status

Ticket opened 2026-09-16 14:55 by session ⚪c7d63d40 (Fable 5.1 coordinator). Predecessor `26/09/06/ENERGY-PLUGIN-END-TO-END` left the react playground (6106, `energy-react` launch entry) booting with three windows (structure tree, zones table, simulation), 16 examples, retained actions and a finishing annual tool run. Start commit: `🗑️generated/start-commit.txt`.

## Definition of done
1. A 3d viewport window renders the whole model as geometry: zones as volumes, walls/floors/roofs/ceilings from `Surface.vertices_m`, fenestrations as real polygons (arbitrary shapes, not area/height/sill only), shading surfaces; camera orbit; example switch re-renders.
2. Artifact tree panel lists every entity family and every entity (building/site → zones → spaces/surfaces → fenestrations; shading; materials; constructions; loads; controls; HVAC; schedules) with selection wired to the interaction domain, so tree pick ⇄ 3d pick.
3. Inspector panel shows the selected entity's properties and edits them through retained actions (u-value/g-value/vlt of windows, construction assignment, material conductivity/thickness, zone volume/name, surface class/boundary, site).
4. Results mode: after a simulation run the 3d scene colours surfaces by a result field (transmission/conduction loss, solar gain) with a legend; per-surface results exist in the engine output.
5. Crate green natively + wasm32-wasip2; existing laws stay green; restage + browser probes prove 1–4 with console logs and screenshots.

## Log
- 15:20 Exploration wave (5 Sonnet agents, reports `📓️explore-*.md`): no 3d viewport/panels/interaction exist in energy; surfaces+shading carry z-up CCW polygons, fenestrations only area/height/sill (rectangle derived in engine `precompute::place_window` AND the epJSON exporter); no per-surface results survive a run (kernel discards conduction/solar per timestep; run payload is one scalar); the leaf generator has drifted from the crate layout (new kinds are hand-authored by copying a leaf); `reduce()`'s `model_edit` has no `diff_fenestrations` (window field edits would vanish silently); World3d recipe = fem3d `🧱️model` + `world3d_scene`/`scene_surface`, picking framework-owned via `scene.domain_id`; results colouring = per-vertex colours + hand-rolled ramp; the only run→window channel is `ToolRunView.payload` (tick payload ≤ 256 KiB) + `ToolRunDefinition.windows` for per-tick refresh.
- 15:25 Coordinator created the shared `✏️editor/🕹️interaction/🦀️.rs` (domain `energyModel`, 13 granularities, `EnergyModelInteractionSnapshot`, `energy_entity_kind`, `energy_target_id`), mounted it and wired `.interaction(...)` in the manifest, then dispatched wave 1 (5 Opus lanes): A schema (fenestration polygon + new kinds), B 3d model window, C tree panel + inspector + commands, D per-surface results + tick payload + colour module, E probes/verification harness.
- 17:40 Wave 1 first restage (`🗑️generated/activate-energy-react.txt` exit 0) + probes: **the 3d window renders** (`window:energy.model.3d`, 8 meshes/instances for BESTEST 600 = 6 surfaces + 2 windows, ids 40–45/50–51, canvas mounted), example switch to BESTEST 620 re-renders (600→900 is geometry-identical, probe default changed), a 3d pick selects through the `energyModel` domain (`selectedIds=["44","40"]`) and the inspector shows the picked surface's form (`🗑️generated/energy-3d-w1b/3-pick.png`); **the tree panel publishes 48 rows** (zones/surfaces/windows/materials/glazing/constructions with human labels). Open: host dispatches `setCamera` on orbit which the window does not declare (→ lane B), the legend caption path faults `DuplicateSiblingKey parent=#0 key=#0` once the run ticks (→ lane B), the panels probe looked under the wrong namespace (`panel:energy-model-artifact/`, `panel:energy-model-inspection/` are the real ones → lane E), the results probe's run-state regex misses the new simulation-window text (→ lane E). A peer's live `World3dHost` refactor (`useThree` from `sceneHostPort.fiber`, 17:36) caused one transient `ReferenceError` on the first probe only.
- 17:45 Coordinator wired lane D's results into the 3d window (`render_body`: `surface_energy_from_run` → `surface_colors` → `legend_caption` → `model_window::render(.., overlay, caption)`); lib check green.
