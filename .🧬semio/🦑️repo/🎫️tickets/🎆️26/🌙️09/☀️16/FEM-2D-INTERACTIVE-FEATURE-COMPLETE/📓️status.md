# 🏗️ Fem 2D interactive feature complete — status

App under work: `fem2d` of plugin `✏️s/🔌️plugins/🏗️fem` (artifact `🗿️artifacts/◻️2d`, crate `semio-s-artifact-fem-2d`, plugin crate `semio-s-plugin-fem`). React dev lane: launch entries `fem2d-react` / `fem2d-react-attach` (port 6086). Predecessor ticket: `🎫️tickets/🎆️26/🌙️09/☀️06/FEM-PLUGIN-END-TO-END` (closed 2026-09-16 03:30; its scripts `📜️activate-fem-react.sh`, `📜️serve-fem-react.sh`, `📜️nextest-fem-crate.sh` are the build/serve/test recipe).
Ticket opened 2026-09-16 by session ⚪34c772d7 (Fable 5.1 coordinator). Repo MCP down; bookkeeping manual on disk. Start commit: `🗑️generated/start-commit.txt`.

## Definition of done
1. Artifact tree panel: every document entity kind (nodes, bars, beams, regions/mesh, supports, nodal loads, member loads, area loads, self weight, load cases, combinations, materials, sections, analysis settings, results) listed as a nested, paged tree; clicking a row selects it (shared selection with the viewport); rows show human labels.
2. Selection + inspector: the inspector renders the selected entity's editable fields (numbers/selects/toggles/sliders); editing dispatches a real mutation (new `update-*` mutations where missing) with inverse, diff, schema, fixture and unit tests, and the viewport re-renders.
3. Deformation animation: results window animates the deformed shape (phase 0→1 or sinusoidal) with a timestep slider, play/pause, speed, loop, and step buttons; works for static (scaled) and modal (mode shape) displays; driven by a config/view-state that survives refreshes.
4. Everything compiles natively and on wasm32-wasip2; `fem-2d-rs:test` and `fem-plugin:test` stay green; the React lane boots with the new panels rendering non-empty and dispatching, proven by console logs.

## Log
- 2026-09-16 open. Exploration fleet (4 × Sonnet, read-only): editor architecture, framework UI primitives/tree/inspector/animation precedents, runtime+verification recipe, schema/mutations/solver outputs. Reports land as `📓️explore-*.md`.
- 11:05 coordinator precedents (own reads): fem2d today has NO panels, NO interaction domain, NO pointer actions — `render` serves two Canvas2d bodies (model/results) only; `command_from_action` bridges 18 actions. Reference patterns: cad `📌️panels/🗿️artifact` (PanelTreeBuilder + `interaction_domain` + `INTERACTION_SELECT_ACTION_ID` row picks, paged via `paged_panel_section`/`PanelRowBudget`), cad `📌️panels/🔍️inspection` (read-only rows + action rows), puzzle3d `📌️panels/⚙️settings` (`Component::NumberStepper` with `Trigger::Change` binding — the React interpreter dispatches `change`/`commit` with the new value), draw `🎮️commands/🖱️canvas-pointer-down` (host `Canvas2dHost` emits `canvasPointerDown{x,y,button,shift,ctrl,meta,alt,width,height}`; the React canvas tracks NO layer picking, so hit-testing is app-side; selection is requested through `Effect::ReplayShellCommand{interactionSelect,{domainId,targets,merge,method}}`), draw/cad manifests (`.interaction(InteractionDefinition{..})` + `.window_kind_interactions(window, [InteractionRef])`, `.panel_tab_def(..)`). UI vocabulary: Container/Text/Button/Separator/Input/Select/Toggle/KeyValueList/Slider/NumberStepper/Ring/IconSelect/Progress/Tree/TreeSection/TreeItem/Image/Surface/Extension. Host: 6087 (fem3d) still serving from the predecessor ticket, 6086 (fem2d) is free; load ~36, 10 peer cargo/rustc processes.
- 11:40 exploration reports saved (`📓️explore-2026-09-16-{editor-architecture,ui-primitives-and-precedents,schema-mutations-solver}.md`; the runtime audit is folded into `📓️plan.md`). Finding: the fem2d editor is gated behind `--features component-app-assembly` and the nx `fem-2d-rs:test` target runs WITHOUT it — the predecessor's "900/900" never exercised the editor tests. Baseline `cargo check -p semio-s-artifact-fem-2d --features component-app-assembly --tests` green (`📜️check-fem2d.sh`, run 1).
- 11:45 `📓️plan.md` written (architecture: framework-owned `fem2d` interaction domain, three panels, patch commands → replace mutations, window-config animation with a `DispatchAction` clock). Slice C (Opus: `replace-node`, `replace-load`, `change-load-case-name`, `replace-combination` on every schema surface) launched.
- 12:05 coordinator skeleton landed: 15 stub command modules (`🖱️canvas-pointer-{down,move,up}`, `🩹️patch-*` ×9, `⏯️set-result-animation`, `⏱️result-animation-tick`, `🎯️focus-entity`), `🕹️interaction`, `🗣️terminology`, `📌️panels/{🗿️artifact,🔍️inspection,📊️results}` stubs, `✏️editor/🦀️.rs` (command rows, retained ids, bridge arms with `scalar_text`, window-scoped `handle` arms, `render_with_request_context` + `render_body`, `ui_label`/`ui_node_list`/`fem2d_action`/`results_window_instance_id`, manifest: interaction domain, window bindings, panel tabs, actions, keybindings), crate-root mounts, `every_command()` rows (33). Check run 3 green. Slices A (interaction/picking/highlight), B (artifact tree + labels), D (inspector + patch commands), E (animation + results panel) launched (4 × Opus). `📜️nextest-fem2d-editor.sh` added (feature-enabled nextest).
- 11:35 (real clock — the two entries above were logged as 11:45/12:05 but happened 11:10–11:20) law tests after the skeleton: 15/21, then publication contracts + first-step proofs extended for the 15 new tools and the law test now routes window-scoped commands to `handle_window`. Host load ~200 (28 cargo/rustc from the fleet + peers); compiles are slow. Verification lane prepared: `📜️activate-fem2d-react.sh`, `📜️serve-fem2d-react.sh`, `🐍️fem2d-console-dump-probe.mjs`, `🐍️fem2d-interact-probe.mjs`, `🐍️fem2d-panels-probe.mjs` (tree pick → selection, inspector edit, results transport play → scene hash changes).
- 12:08 slice E report landed (`📓️w-e-animation-2026-09-16.md`): `animation` block on all five results-window-config surfaces (default phase 1 keeps every pre-existing render bit-identical), one-clock-per-window law, inert stopped tick, `{field, value}` convention on `setResultDisplay`/`setResultAnimation`/`setAnalysisSettings` (bridge arms already applied), thread-local results cache keyed by (instance, base revision), results/playback panel. Open: the transport needs a focused results pane (`ConfigView` captures one partition). Slices A/B/C/D still running; load back to ~60.
- 12:18 slice C report (`📓️w-c-mutations-2026-09-16.md`): `replace-node`, `replace-load` (`Box<FemLoad>`), `change-load-case-name`, `replace-combination` on every surface (leaves, 18 scenarios + fixtures, aggregate rs/ts/json, subset feature/rs/py twins, oracles, generator, taxonomy + schema catalog regenerated); 25 → 29 kinds; whole-crate nextest 1030/1032 (2 wall-clock engine budget tests red under fleet load). Open: carrier generator output drifted (compact vs pretty JSON) before this ticket. Slice B report (`📓️w-b-artifact-tree-2026-09-16.md`): nine-section outliner keyed by raw ids, max-min fair paging (demo: nodes +5 / elements +3 truncated within the 31-row page), 82-label `Fem2dLabels` set with a German-differs law; B hit ENOSPC (102 GB incremental cache) and deleted `⚡️cache/cargo/build/debug/incremental`.
- 12:45 slice D report (`📓️w-d-inspector-2026-09-16.md`): nine `patch*` commands → whole-record replace mutations (three refusal codes `fem2d.patch.<noun>-{missing,field,value}`, unchanged value = no-op), inspector with real controls per entity kind, 48 tests green, wasm check green. Audits (2 × Sonnet): C — one blocking gap: `combination_term_references` accepts a self-referencing term on `replace-combination` (fix agent launched, also mirrors the ten `🐍️.py` twins + a scenario); B/E — `HostOnly` lane had been dropped again from the two playback contracts (restored), the results panel's `windowId` tag is dead in the bridge (slice E asked to wire `window_id` through the three commands and `addressed_window_id`), stale "ascending keys" comments in three panels (nit). `results_window_instance_id` now prefers `focused_window_id`. Scripts run with `CARGO_INCREMENTAL=0` (incremental cache regrew to 40 GB in 20 min; 35 GB free).
- 13:15 slice A report (`📓️w-a-interaction-2026-09-16.md`): canvas-pixel hit-testing composed with the Canvas2dHost projection (node 8 px → support 11 px → load 6 px → element 6 px → region), `interactionSelect`/`interactionHover` replay effects, pointer commands, `focusEntity`, `sel-*`/`hov-*` overlay layers via `fem2d_structure_layers_with`, 19/19 slice tests. Combination self-reference guard fixed (`📓️fix-2026-09-16-combination-self-reference.md`: `mutation.invariant` Fatal, ten `🐍️.py` twins, scenario `👻️self-term-0f54d1`, 96 combination tests + 19/19 python reject rows). Integration: framework refuses `HostOnly` beside another lane (`🔌️plugin/🦀️.rs:13262`), so the two playback routes are `[WindowConfig]` and the fem2d lane law now says "a publishing route must not claim HostOnly; a HostOnly route must not publish". A peer's 12:06 framework commit requires `bind_instance_id` before typed dispatch (`interactive-job.live-instance`); the harness gained `fem2d_mounted_app()` + `close()`. Full feature-enabled nextest run 2 launched (`📜️nextest-fem2d-editor.sh 2`).
- 13:35 all five slices reported (A/B/C/D/E) + combination guard fix. Full feature-enabled nextest run 2: 1200/1239 — 27 × `interactive-job.live-instance` (peer commit f66680537c 12:06 requires `bind_instance_id` before typed dispatch), 4 × store `Drop` witness (same commit: every store must be closed before drop), 1 × stale generated tool catalog (`paired_apps` proof join: `generated_migrated=false` for the 15 new tools), 1 × law fixture ids not in the demo (`e1`/`ipe300`/`l1` → `e3`/`chs76`/`l6`), 2 × engine wall-clock tests. Harness: `Fem2dApp` is now a wrapper bound to the test instance that closes itself on drop (`close_registered_fixture_app`, skipped while panicking); paired convergence apps closed explicitly. `📜️describe-and-registry.sh` regenerates the descriptor pair + registry catalog; editor nextest run 3 launched.
- 13:40 harness settled: `Fem2dApp` wrapper (bound + self-closing), `dispatch` settles the retained typed operation and folds the receipt's effects, `fem2d_empty_app()` for the pre-existing tests that counted from an empty boot document (the app boots on the demo; `SetActiveExample("default")` loads the EMPTY document — the demo id is `demo`), three load tests rewritten against demo ids, visual-job and bare-store tests retire their owners. Editor nextest run 6: 192/193 — only `two_instances_converge_on_disjoint_edits` (stale generated tool catalog) left; `📜️describe-and-registry.sh` is executing the component. Removed the `[DEBUG]` print in `set-active-example` and the stale ascending-keys comments.
- 13:50 `fem-plugin:describe` + `plugin-registry:generate` green (descriptor pair regenerated with the 15 new tools). Full feature-enabled nextest run 7: 1237/1239 — `mesh_job_large_boundary…` (wall-clock, green alone) and `two_instances_converge_on_disjoint_edits`: the registry-less `paired_apps` path has no `Migrated` declarations for the proof catalog (`generated_migrated=false`), and the registered `paired_registered_apps` path refuses `attach_backbone` (`module.vcs`: "remote snapshot merge is fail-closed until the app-owned streaming envelope decoder and persistent candidate transaction are terminal-authorized"). Rewritten to the framework's own `assert_two_instances_converge` law (as remodel does); a framework gap either way. Activation (`📜️activate-fem2d-react.sh 1`) running.
- 13:52 canonical nx targets green: `fem-plugin:test` 5/5, `fem-2d-rs:test` 1038/1038, `fem-js:test` 6/6. Activation 1 + serve on 6086: boot clean (`fem2d-boot-1`: ready, both canvases, tabs Artifact/Inspection/Results). Panels probe 2: tree row `n1` picks and marks; BUT the inspector rendered empty and the results panel faulted `ui.value.map … admission failed` on play — the tree's two row actions per row (2 maps + 2 id lists) exhaust the ONE-page `UiValue` argument arena (`UI_VALUE_LIVE_PAGES = 1`), exactly the cad incident. Fix: tree rows author their pick only; Focus/Delete moved to an inspector "Actions" section. Panel tests 35/35; activation 2 running.
- 14:05 built-in browser session on 6086 (activation 2): artifact tree lists the demo, a row pick marks the row and paints the yellow selection halo in BOTH canvases; the results panel renders Display/Playback/Analysis; Play arms the clock (22 tick dispatches / 3 s headless) and the results window animates (caption `phase 0.45 · ▶ 0.5 Hz` → `phase 0.94`, reactions and deformed shape change). Gap: a panel projection captures only the FOCUSED window's config (framework), so the results panel shows defaults (Play, phase 1) until the Results pane is focused — after focusing it reads Pause / phase 0.04. Fix: `results::config::captured(cfg)`; unfocused panel renders a bare play/pause toggle + a focus hint; `setResultAnimation`/`resultAnimationTick`/`setResultDisplay` declare a `Partial` dirty scope naming the results window body and the results panel body so ticks refresh the panel.
- 14:25 activation 3 + browser session: full crate nextest run 12 = 1238/1240 (known: framework convergence gap, load-sensitive mesh timing). Serve on 6086 had died on a peer's framework-file rename (vite watcher ENOENT) — restarted. Verified live: tree pick `n1` → row + both canvases highlighted → inspector shows `Node / ID n1 / Actions Focus, Delete`; results panel unfocused mode shows the hint + `Play / Pause` toggle; toggle arms the clock (118 tick dispatches), focusing the Results pane switches the panel live (`Pause`, phase 0.3 → 0.45 across ticks — the Partial dirty scope refreshes the panel). DEFECT: the inspector's form controls (number inputs, selects, toggles) are dropped by the React tree renderer because they sit inside `PanelTreeBuilder` sections — only TreeItem rows render; the results panel renders controls because it uses `ui::column → ui::section → field > control`. Opus agent rewriting the inspector to that layout.
- 14:50 browser (activation 3): tree pick of `s1` → inspector `Support / ID s1 / Actions`; inspector Delete removes the support (`Supports (4)` → `(3)`, results window reports the under-constrained stiffness matrix instead of faulting); an empty-canvas click clears the selection (pointer path live). Earlier "support/element rows don't select" was a probe artefact: the persisted Results dock panel hid the tree, so the refs pointed at hidden rows. Playback: the tick chain had exhausted the window-config store's fixed applied-edit capacity after minutes ("batched publication requires preinstalled fixed applied and revision capacity") → playback emits now carry `coalesce_key = fem2d.results.playback` so ticks amend one edit (law added; 13/13). `mod+z`/`mod+shift+z` keybindings were missing from the manifest → added. Inspector form-layout rewrite (controls dropped inside tree sections) in progress.
- 15:05 activation 4 (inspector form layout): browser journey — pick `n1` → inspector `Node / ID / X [0] / Y [−4] / Actions` with real number inputs; typing X = 1.5 + Enter dispatches `patchNode` → tree label `n1 · (1.50, −4.00)`, the model canvas moves the node, the results window re-solves (reactions/deformed shape change). Full crate nextest run 14: 1240/1243 (convergence gap + two load-sensitive engine timing tests while activation compiled in parallel). Activation 5 (undo/redo keybindings) building.
- 15:25 activation 5 (undo/redo keybindings, coalesced playback): fresh boot clean, inspector summary renders as a form, pick `n1` → `X`/`Y` inputs, X = 1.5 + Enter → `n1 · (1.50, −4.00)`, `mod+z` → `n1 · (0.00, −4.00)`. Report: `📓️fem2d-interactive-2026-09-16.md`. Closing on disk (repo MCP down); serve left running on 6086 (`fem2d-react-attach`); `🗑️generated` removed.

## Definition of done — 2026-09-16 15:25
1. Artifact tree panel with every entity kind, human labels, paged, shared selection. ✅
2. Selection + inspector with editable controls dispatching real mutations (4 new kinds, tests on every surface). ✅ (viewport pick, tree pick, inspector edit, delete, undo verified live)
3. Deformation animation with phase slider, play/pause, speed, loop, waveform, step buttons; static and modal/buckling. ✅ (live: caption, reactions, deformed shape; panel live while the Results pane is focused)
4. Native + wasm32-wasip2 green; `fem-plugin:test` / `fem-2d-rs:test` / `fem-js:test` green; feature-enabled nextest 1240/1243 (framework convergence law + two wall-clock engine tests under fleet load); React lane boots with the panels rendering and dispatching, proven with console dumps and screenshots. ✅
