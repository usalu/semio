# 🏗️ Fem 3D interactive feature complete — status

App under work: `fem3d` of plugin `✏️s/🔌️plugins/🏗️fem` (artifact `🗿️artifacts/🧊️3d`, crate `semio-s-artifact-fem-3d`, plugin crate `semio-s-plugin-fem`). Blueprint: `🎫️tickets/🎆️26/🌙️09/☀️16/FEM-2D-INTERACTIVE-FEATURE-COMPLETE` (closed 2026-09-16 15:25) and `FEM-2D-TRANSFORM-GUMBALL`. Predecessor: `🎫️tickets/🎆️26/🌙️09/☀️06/FEM-PLUGIN-END-TO-END`.
Ticket opened 2026-09-16 by session ⚪9dc2b27f (Opus 5). Repo MCP down; bookkeeping manual on disk. Start commit: `🗑️generated/start-commit.txt`.

## Definition of done
1. Selection: viewport pick (World3d hit → shared `fem3d` interaction domain), marquee, artifact tree pick, hover/highlight, delete/backspace, focus; every entity kind (nodes, bars, frames, solids, supports, nodal/member/area loads, self weight, load cases, combinations, materials, sections, analysis settings, results) in a paged artifact tree with human labels.
2. Transform: `transform` utility arms the host gumball (`selectionJson`: gumballActive/gumballTarget/gumballConfig/transformMode); translate/rotate/scale dispatch coalesced document mutations during the drag; utility options (move/rotate/scale axes/uniform).
3. Realtime displacement: the results window re-solves and re-renders the deformed shape on every coalesced transform step (not only on drag end), proven with console logs.
4. Inspector: editable controls per entity kind dispatching patch commands onto replace mutations (with inverse, diff, schema, fixture and unit tests).
5. Deformation animation (phase/play/pause/speed/loop/waveform) like fem2d.
6. Real-world house example: foundation, ground/floor slabs, walls with window openings, gable roof; solids + supports + loads + self weight; solves and renders.
7. Native + wasm32-wasip2 green; `fem-3d-rs:test` / `fem-plugin:test` green; React lane boots with the panels rendering and dispatching, proven with console dumps/screenshots.

## Log
- 2026-09-16 open. Reading fem2d blueprint + current fem3d editor/schema/session.
- 2026-09-16 schema + engine landed: `FemAxis` on `FemSolid` (X/Y/Z extrusion), meshing rewritten (`NodeMerger`, cross-solid node sharing, `mesh_solids`/`resolve_geometry`), four new mutation kinds (`replace-node`, `replace-load`, `change-load-case-name`, `replace-combination`), shared `🎬️scene` node (instances keyed by raw entity id + `interactionGranularityId`), editor rewritten on the framework interaction domain (34 commands: patch ×9, result animation, focus, gumball translate/rotate/scale, gumball flags), results/model window configs, panels (artifact tree, inspector, results), transform utility options.
- 2026-09-16 tests: `📜️nextest-fem3d-editor.sh` run 4 → 929/932 (fixtures + wire-law re-pins, `with_caption` positional keys, focus test via `focused_orbit`, empty-doc caption before solving); run 5 → 1107/1109 after the mutation vector wave (below); run 6 → the 173 mutation tests green.
- 2026-09-16 mutation vector wave (`🐍️fem3d-mutation-vectors.py`, `🐍️fem3d-mutation-harnesses.py`): 22 committed scenario bundles + Rust fixture tests for the four kinds (spec/hall/reject/no-op), subset + `✳️any` differential harnesses (Rust/feature/Python), subset + any oracle catalogs/manifests, JSON-carrier generator (26 kinds, seed carries `axis`, corpus regenerated, reader re-measured 26/26 both ways), PyNite benchmark (four after-models + references via the ticket's `🔨️run-fem3d-oracle.py`), JSON-schema/TS union facets, schema catalog regenerated.
- 2026-09-16 framework `World3dHost`: instance pick/hover/marquee resolve `interactionGranularityId` per record (`world3dInstanceInteractionTarget[s]`, `world3dSelectionTargetsActionArgs`, engine-contract test green); `WorldSelectionRecord.gumballLiveDispatch` → incremental translate/rotate/scale dispatch mid-drag (latest pose wins, one in flight, tail delta + `transformEnd` on release), layer skips the local preview so instances draw the document.
- 2026-09-16 house example `📚️examples/🏠️house` (generator `build()`, asset written via `SEMIO_FEM3D_WRITE_HOUSE_ASSET=1`): raft + ground/attic slab + 4 walls (windows/door holes, drawn in elevation) + gable roof (chevron swept along the ridge), 0.5 m module, 63 raft supports; meshes to 6408 tets / 1939 nodes as ONE connected structure (union-find law); static solve balanced but 101 s in a debug build — release timing pending.
- 2026-09-16 solver: the house solve was 101 s (debug) — `sparse::ldlt_factor` rewritten as an envelope (skyline) LDLT bit-identical to `LdltJob` (parity tests `==`), `analyses::DofMap` hash-indexed (`from_order`/`push`), `positions_of` through a node-slot index → 9 s debug (assembly stepping 3.5 s, LDLT 3.8 s). fem-2d sparse/analyses suites green (long suite under `--profile long`). Disk filled mid-build (135 GB debug + 147 GB wasip2 caches); pruned stale incremental sessions.
- 2026-09-16 browser (react lane on 6087, `🐍️fem3d-console-dump-probe.mjs` + `🐍️fem3d-panels-probe.mjs` + built-in pane): boot clean, both World3d windows draw (3 meshes each); artifact tree pick → `selectedIds:["n00_l1"]` in the model window's selection record; the inspector auto-opens on pick (Node / ID / X / Y / Z number inputs + Focus/Delete) and typing Z = 3.4 + Enter dispatches `patchNode` → tree reads `(0.00, 0.00, 3.40)`, the model window moves the joint, `results solve #3` logged; Transform utility arms the gumball (`gumballActive:true`, `transformMode:"transform"`, `gumballTarget:[0,0,2.8]`, options rail Move/Move Planes/Rotate/Scale Axes/Uniform Scale); a live drag dispatched incremental `rotateSelection` steps mid-drag, but `transformBegin`/`transformEnd` were refused as undeclared window actions → declared as empty host brackets (36 commands now), law `transform_brackets_complete_empty`; House example loads (63 nodes / 8 solids, 456 mesh items) and the results window solves it in ~11 s in wasm. Known limit pinned: the mounted live-visual child refuses the demo at the 4 KiB owner page (`fem3d_production_numerical_child_refuses_the_demo_at_the_mounted_owner_page`).
