# 📓️ Status — Event-Sourced Framework Version Control

## 2026-09-19 — session 1 (manual ticket bookkeeping: repo MCP `ticket_open` answered "invalid tool params")

### Done
- 🔀️ `📡️replication/🔗️causal/🔀️transition/🦀️.rs`: `HistoryTransition` (Revert/Reinstate/Commit/Branch/Checkout/Repin),
  `HISTORY_TRANSITION_SCHEMA`, binary codec, content-addressed `history_transition_envelope`, pure
  `fold_history` (HLC-ordered fold → applied/redo/checkpoint/alternative + change/checkpoint/alternative facts).
  12 unit tests green (`cargo test -p semio-framework-replication --lib transition`).
- 🏪️ Store (`🏪️store/🦀️.rs`):
  - `ArtifactEnvelopeOwners.transitions` = authoritative structural log; cursor, active alternative and the
    change/checkpoint/alternative ledgers are its projection (`fold_envelope_history`/`fold_event_log`).
  - Every structural command authors a transition (`commit_transition`) and materializes through
    `reproject` — the same path remote transitions take (`admit_remote_transitions` from `ingest_remote`).
  - Apply/Amend announce their new operation envelopes; every command flushes its events as one
    `Mutations` batch (`flush_outbound`). No command broadcasts a snapshot anymore.
  - `BackboneMessage::Snapshot` → `Genesis { pack }` (identity only); `attach_backbone` announces genesis +
    the whole event log (`event_log`), peers verify the genesis (`verify_genesis`) and fold unknown events.
  - Deleted: `merge_remote_snapshot`, `remap_snapshot_message_ledger`, `snapshot_ledger_targets`,
    `assert_equivalent_remote_edit`, `same_edit_operation_identities_and_payloads`, `replay_suffix`,
    `checkout_checkpoint_internal`.
  - `.ops` text mirror: Change/Checkpoint/Alternative/Active/Cursor lines → semantic
    revert/reinstate/commit/branch/checkout/repin lines (ids re-derived and checked on parse).
  - `.spr` print/parse: only edits + transitions (+conflicts, composition owner/dialect); positions derived.
- 🔄️ Sync actor: persists transitions as `REC_TRANSITION`, adopts/validates `Genesis`, hub bootstrap pushes
  genesis + events, external divergence is a document replacement (no push into the store).
- 🖥️ Host `BackboneDocument`: `cursor` → `transitions`; space plugin derives applied ids by fold.
- 🟦️ TS codec: `BinaryBackboneMessage` `snapshot` → `genesis`; binding fixtures/schema/oracles updated.

- 📜️ `.spr` `HistoryLog` carries `transitions` (`REC_TRANSITION = 0x43`); hydration, CLI, IO, materialize and
  compaction consume the event log (`📓️spr-history-transitions.md`). `HistoryAppender` appends composition + conflicts.
- 🪪️ Batched retained publications stamp `authority.edit_id()` (blake3 over actor/sequence/clock) — replica edit ids
  no longer collide across instances.
- 🧹️ `ingest_remote` rewinds retire every intermediate replay projection, every replay diff and the rewind base
  (`replay_suffix_partitioned`); refusals retire the candidate causal DAG (`refuse_with_candidate_dag`).
- 🧫️ Language-agnostic payload corpus `📡️replication/🔗️causal/🧫️fixtures/🔀️history-transition-v1` + JSON Schema
  `🧬️schema/🔀️history-transition-v1`, generated by an independent Python encoder (`🧪️generate-history-transition-fixture.py`),
  checked byte-for-byte by the Rust codec and by a TS encoder + Ajv (`🧪️tests/🧪️history-transition`).
- ✅️ Verified: replication transition tests 14/14; replication TS 8/8; os TS 79/79; plugin tool-run 36/36; store suite
  366 pass (remaining failures pre-existing debt, see below); lowpoly + remodel + generation2d + gismap convergence green.

- 🧊️ Scratch values of a remote ingest never reach `Drop`: replay projections, replay diffs
  (`MutationDiff::retire_cold`), the rewind base, decoded/cloned edits and displaced inverses
  (`retire_scratch_edits`/`retire_scratch_operations` → `Mutation::retire_cold`), also in `accept_quarantined_events`.
- 🗣️ `#[derive(Mutations)]` gains `#[mutations(retire_cold = path)]` (fixture-first: `🧫️fixtures/🏷️mutation-attributes`);
  imperative wires it (`retire_procedure_mutation`, `ColdRetire` for engine `Step`/`Path`).
- 🧪️ `assert_registered_ingest_idempotent` binds instances, settles the publication, asserts events reach the
  channel and materialize, detaches before close.
- 📜️ Policy gate `toolJobArtifactResolutionCandidateExact` → `toolJobArtifactEventSourcedMergeExact` (+ self-test).

### Plugin convergence (registered harness: two replicas, disjoint edits, exchange, checkpoint commit replicates)
- ✅️ green: lowpoly, remodel, generation2d, gismap, puzzle-2d, puzzle-3d, reasoning-wires, note, fem-2d, forms, dag.
- ✅️ imperative: replication + convergence assertions pass; fails afterwards in CLOSE — the plugin's snapshot
  retirement drops `ProcedureSnapshot` dictionaries (plugin close debt, `BoundedConfigValueRetirement`).
- ⚠️ plugin debt, not VCS: animate (`missing-factory`, 8/20 editor tests red), playbook (no
  `build_presence_local_root_retirement_factory`), raster/sequence/shooting/space (whole test contexts registryless →
  `catalog-authority`), flow (needs window view; composed-child edits are separate documents with their own logs).
- 🧱️ peer compile breaks: cad `CadEdgeCurve`, generation3d `Camera3d`, equation `close_registered_fixture_app(Box<…>)`.
- 📜️ `verify interactivity tool-jobs` stops early on an unrelated wgpu `recordTutorial` check; its older store
  predicates look for test names that moved into split `🧪️tests` files (auto-commits 556/601) — pre-existing drift.

### Store suite: 367 pass / 51 fail (was 366/52) incl. `--features sync`; no new failures (diffed per test).
