# Semio Codec And Fixture Recovery

The retained representative native receipt supplies red tests for BREP diff tolerance loss and Document invalid image-removal law setup. The coordinator is repairing the concrete schema/codec/fixture defects while the other native lanes run.

## Current Scoped Source Changes

Document law fixtures now include img1 only for the RemoveImage case, preserving strict removal and both inverse checks without conflicting with the independent InsertImage case. BREP diff encoding/decoding now imports the six existing vertex/edge/face record codecs from the snapshot module, whose exact grammar already preserves tol. Those helpers are visible only to the schema module; no reverse snapshot-to-diff dependency or duplicate codec implementation is introduced. The existing nonzero-tolerance text/binary roundtrip law remains the acceptance gate.

## Remaining Findings

The required tolerance fixture cohort and inverse construction need schema-first reconciliation. Value named absorption has a deeper transport limitation: removals contain only identity, while additions contain positions. A sound base-free composition cannot infer whether a removed unknown base entry preceded an addition. The audit explicitly rejects clamping or blind index adjustment; no Value implementation has been changed. The separate feature/backend audit establishes that the15 BREP geometry failures have no extraction feature, dependency, registration or algorithm-source-diff cause. Native validation of the scoped repairs remains pending.

## Value Scope Resolution

The coordinator compared the exact Value diff implementation and associativity test files against HEAD: both are byte-identical. Their SHA-256 receipts and the missing-ordering-information reasoning are retained in `📓️value-diff-package-parity.md`. These two native failures are tracked as separate baseline algebra defects; no unsound index repair or transport migration is part of this package extraction. The aggregate remains a failed receipt even where package-specific gates accept.

## Downstream Tolerance Literal Census

A coordinator read-only `rg` census of CreateVertex/CreateEdge/CreateFace and BrepVertexDiff/BrepEdgeDiff/BrepFaceDiff literals outside the BREP subtree found one remaining apparent BREP constructor without tolerance: `✉️base/🧪️tests/✉️mutate-semio-base/🦀️.rs` at line250 (`v-probe`). The other base mutation unit constructor already includes tol. Remaining CreateEdge matches belonged to Graph or Trinity. This finding was sent to the BREP owner; no coordinator caller edit was made.

## Current TypeScript Package Acceptance

The ordinary current `@semio-tech/stdio-semio:check` and `:test` targets both exited 0 after the BREP schema mirrors settled. Check took 9.3 seconds; test rebuilt three package outputs and verified its single public export in 9.8 seconds. These are TypeScript/package receipts, not native BREP law acceptance. The later independent audit requested additional neutral inverse/parser coverage; that follow-up is active and does not invalidate this result unless the TS API changes. Raw receipts: `🗑️generated/stdio-semio-ts-final-check.txt`, `🗑️generated/stdio-semio-ts-final-test.txt` and `🗑️generated/stdio-semio-ts-final.tsv`.
