# Flow Native Fixture Recovery

## Observed Failures

The retained native first-panic stdout identifies actual catalog-authority failures before the secondary strict destructor abort. The child-edit route constructed `VcsArtifactApp::new` with no registered migrated declarations, although concrete factories require an exact manifest join. The registered retained-addWidget route rejected `flowEvalResolve`: a live factory and migration classification existed, but no command declaration had been added to the manifest. Receipts are `flow13-current-4-artifacts/exact-cargo-laws-hOZ9gV/00/law-0.stdout` and `exact-cargo-laws-vK7a7p/00/law-1.stdout`. The standalone exact factory-role law passed1/1; this does not establish the route aggregate.

## Changes

Flow declares the existing runtime-only resolve command alongside the tick command, with both English/German labels and palette visibility disabled. Its default test fixture now constructs and binds the actual registered application, preserving concrete factory authority. The shared fixture completion and close helpers accept `PluginApp`, allowing Flow's Semio child-member composition to use the same protocol as other artifacts; their bodies are unchanged. The close signature was changed concurrently by the Norm executor for the same purpose and preserved.

The two-command child-edit law now requires empty admission results, waits for real bounded publication with exact ACK, asserts one child group and terminal page per command, preserves exact parent coordinates and two expected node identities/positions, then executes two inverses and compares the restored child snapshot. It no longer interprets an immediate admission as a completed mutation. The retained-addWidget fixture consumes all result/effect/event/UI lanes through the same helper instead of spinning with undrained outboxes. Strict terminal-empty close assertions remain.

The existing neutral child and retained-addWidget fixtures remain the expected-data sources; registered routes run their independent AJV/Immer/stable-JSON source oracles before native laws. Scoped whitespace validation passed before the final retained-loop edit. Current ordinary Nx retry5 runs child-identity, child-edit and retained-addWidget sequentially; raw receipts use `flow-recovery-5-*` and `flow-recovery-5.tsv`. Native acceptance is pending.

## Files

- `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🦀️.rs`
- `✏️s/🔌️plugins/🌊️flow/🗿️artifacts/🌊️flow/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🦀️.rs`
- `✏️s/🔌️plugins/🌊️flow/🗿️artifacts/🌊️flow/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🧪️tests/🔬️testkit/🦀️.rs`
- `✏️s/🔌️plugins/🌊️flow/🗿️artifacts/🌊️flow/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🧪️tests/🔬️unit/🦀️.rs`
- `✏️s/🔌️plugins/🌊️flow/🗿️artifacts/🌊️flow/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🎮️commands/➕️add-widget/🧪️tests/🔬️unit/🦀️.rs`

## Read-Only Audit Follow-Up

The coordinator strengthened both publication receipts to require the exact ordered `[Child, Terminal]` vector, rejecting extra lanes. Undo now compares the complete child snapshot after each inverse: the original document plus the first identity/position-checked inserted node, then the original document. This catches restoration of the wrong node or unintended changes to prior nodes, schema or edges. The active ordinary Nx run remains responsible for runtime acceptance. Scoped diff whitespace check passed.

## Current Native Canonical Casing Failure

Retry5 compiled the Flow artifact test binary successfully in17m39s, then the first exact identity law failed0/1 at retained/artifact unit test line97. The neutral canonical fixture requires `inputPorts`/`outputPorts`, but `Widget` declared only enum-tag `rename_all`; current value-derive correctly requires explicit `rename_all_fields` for variant fields. Its decoder therefore discarded the supplied ports into default empty vectors and re-encoded snake_case. Added `rename_all_fields = "camelCase"` to the first-party Widget schema declaration; retained neutral fixture and independent Node crypto/JSON oracle remain unchanged. Raw red: `flow-recovery-5-artifacts/exact-cargo-laws-cXgsJh/00/law-0.stdout`. The remaining retry5 routes continue; exact identity retry remains required.

## Retry 5 Terminal Results

Identity law 0 failed the exact canonical wire fixture before the remaining nine laws ran; the source now explicitly preserves camelCase variant fields. Child-edit compilation failed on shared IO calls that have since been corrected. The retained factory law passed, but its runtime law faulted with `job-session.terminal-fault`, then fixture Drop caused a secondary abort. Inspection shows the generic worker fault can represent a callback watchdog violation, invalid deadline, or worker panic; no prior worker panic appeared in the log. The runtime test now closes the exact fixture before reporting a publication error and includes the existing watchdog diagnostic records. It also asserts the complete ordered Child/Terminal lane sequence. A package-local development dependency exposes trace diagnostics only to tests. Native acceptance remains pending.

## Retained Work Timing Review

Read-only inspection shows add-widget constructs a complete FlowHost from the child snapshot, installs session baseline and neuron-kind metadata, seeds the catalogue, applies canvas options, and calls FlowHost::add_widget, which rebuilds the DAG. The callback ceiling is8,000 microseconds. This identifies a plausible source of callback overrun, but is not runtime proof of the observed terminal fault. The diagnostic test now captures the actual watchdog violations; no budget increase or speculative algorithm replacement was made.

## Recovery 6 Terminal Result

The ordinary retained Flow route compiled its native test binary in 66m14s including shared Cargo contention. The factory law passed 1/1. The runtime law aborted; its first failure was a worker panic `ordered-map root must be explicitly retired before drop`, followed by a poisoned instance-owner close fault and secondary strict-disposer abort. This evidence supersedes the unconfirmed watchdog hypothesis. Identity and child-edit were earlier graph failures in this batch. An exact retained-binary replay with full backtrace was run only for first-cause diagnosis, with binary SHA-256 and exit code recorded under `🗑️generated/flow-recovery-6-worker-first-cause.json`; it is not new package acceptance.

The full-backtrace replay identified the concrete worker stack: `OrderedMap<WidgetLayout>::drop` → `FlowFixture` → `FlowHost` → `add_widget::child_add_widget_mutation` → `FlowChildGroupWork::step`. The temporary host and the original fixture have no explicit retirement at this call site. No watchdog violation is needed to explain this run. The replay returned SIGABRT, binary SHA-256 `a3f161e402bef92879712a0cf1c18e06b9d97828d0e59db3befd552fa20044b6`. Source repair was assigned to the Sol executor with the constraint to preserve bounded ownership and identifier/default-node semantics; simply draining ownership inside the interactive callback or weakening Drop assertions is excluded.

## Final Descriptor Coverage And Diagnostic Cleanup

The executor added full Semio payload assertions for all eight widget categories, including named and variadic neuron ports, plus explicit-ID collision and smallest-free-ID application cases. These native laws are being attached to the existing child-edit route. The coordinator removed the temporary Watchdog failure branch after the exact backtrace established the temporary-host drop cause, and removed only its direct trace dev-dependency and matching Flow package lock edge. The retained runtime law again performs its original exact acknowledged child/terminal assertions and strict terminal close. No native acceptance is inferred from source cleanup.

## Recovery7 Fixture Discovery Repair

Both ordinary identity and child-edit gates first stopped in the source oracle because its old schema/mutations scan found zero snapshots instead of the required20. The current neutral corpus still contains20 snapshots under the subset fixture taxonomy. The coordinator corrected the scan URL, preserved the exact20 assertion and independent AJV child/target validation, and pointed the duplicate-widget payload check at its descriptive fixture path. Directory URLs now carry trailing separators so relative resource resolution stays under the intended owner. An ordinary test-source rerun is active; native identity/child-edit acceptance remains pending.

Recovery7 is terminal with all three routes exiting1. Identity and child-edit failed the stale fixture scan; retained passed its 2 accepted/24 hostile source oracle, then failed compiling the same six removed shared-plugin helper exports as PDF19 and Semio. No native test executed in recovery7. The exact build receipt directory is 🗑️generated/flow-recovery-7-artifacts/exact-cargo-laws-CvAwDS/00. Source fixture repair is under its ordinary Nx test-source check; all three native routes will retry after current owner-bundle integration settles.

The ordinary Flow test-source correction passed with exact exit0 and all5 Nx tasks successful, duration22.1s (17.9s critical path). It checked20 persisted snapshots plus the demo asset, the canonical duplicate-widget payload and all existing independent AJV/digest/ownership/hostile source contracts. The earlier zero-snapshot failure supplies the red observation; this rerun supplies the green observation. Raw receipt: 🗑️generated/flow-source-fixture-path-recovery.txt. This is source-contract acceptance, not native lifecycle acceptance.
## Recovery8 Schema Annotation Registration

All three ordinary Flow routes exited1 before native execution because the current schema includes `x-semio-child-kind`, `x-semio-child-standard` and `x-semio-child-subset`. Strict AJV rejected the first unknown annotation. Both affected validators now register these annotations explicitly with string meta-schemas alongside `x-semio-state`; strict schema validation and every existing fixture assertion remain enabled. The ordinary source target and retained oracle are being rerun. This entry records the observed red result and the scoped source repair, not an unrun green or native result.


The second annotation retry passed ordinary graph construction after the Nx resolver fix and ran all four prerequisites, then exposed the current module’s external Store child schema reference. Both validators now register the authored IO schema and Store child schema before compiling Flow exports, preserving full strict root/module validation. Retry3 is active; no native result is inferred.

The third ordinary retry passed full strict schema registration, then the existing exact command-order assertion observed13 versus14. The current FlowCommand declaration has34 entries after the concurrent window ownership refactor removed an earlier host command. Six retained host-wire vectors now name the canonical ordinals13,21,23,24,32,33 for their unchanged command identities. The repair changes only those six integer literals; payloads, byte counts, symbol counts, grant/cancellation cases and exact source-order assertions remain unchanged. Retry4 will check the independent scalar-record oracle against these vectors.

Retry4 is terminal exit0. Ordinary Flow test-source passed all5 Nx tasks (43.5s), including six corrected host-wire vectors, complete strict shared IO/child schema registration, twenty mutation snapshots plus the demo and all existing independent oracles. The retained oracle also passed its2 accepted and24 hostile cases through ordinary Nx (9.1s).

The audit found two native constructor references to commands no longer emitted by the macro. Concurrent work removed both before the coordinator patch, so that removal is attributed externally. Static preflight now confirms34/34 representative constructors in declaration order. The coordinator strengthened the remaining fixed37 count assertion to exact equality with FlowCommand::TOOL_JOB_IDS and added its existing law plus existing text/binary and wire-keyword laws to the identity gate. This validates the wire-fixture recovery through the same native binary. The separate action-cohort catalog is undergoing concurrent retained-route changes and is not accepted by these focused gates.
