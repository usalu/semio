# Flow Editor Fixture Schema Recovery

Recovery9 reached all three ordinary Nx routes. Identity failed before native execution on a shared plugin error translation, subsequently corrected by concurrent work. Child-edit and retained stopped in AJV because the persisted artifact schema now contains only schema/content, while both validators still requested named fixture declarations from that schema.

The fixture declaration closure is now owned by the editor fixture taxonomy at `✏️s/🔌️plugins/🌊️flow/🗿️artifacts/🌊️flow/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🧫️fixtures/🧬️schema/🔣️.json`. Its independent id is https://semio.tech/schema/s/flow/flow/editor/fixtures.json. The declaration set contains14 requested roots and41 transitive definitions, all local references. Both existing validators load this module with strict AJV settings. The persisted artifact schema is unchanged. The declaration contents came from the last preserved schema; existing fixtures and hostile cases must validate their current meaning.

The preceding recovery9 logs establish the failing baseline. The first ordinary retained oracle rerun passed2 accepted and24 hostile cases (12.5s, cache miss). The source rerun advanced through the child, tree, wire, recipe and parameter oracles before rejecting the stale grant-frontier canonicalVariants requirement. The current native SceneCopy byte law consumes cases; its committed fixture intentionally omits that obsolete artifact variant list. The dedicated fixture schema now omits that field, and the source oracle explicitly rejects reintroducing it plus truncating the six-case corpus. All10 hostile vectors remain. The second ordinary source rerun passed all five Nx tasks (56.8s; one prerequisite cache hit, current source task executed). Its output confirms6 byte fixtures/10 hostile rejections,5 slider fixtures/3 denials,9 widget and10 mutation variants/5 denials,5 identity cases, plus all child/tree/wire/recipe/parameter/owner oracles. Native execution remains pending in recovery10.
