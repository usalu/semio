# Nx Graph Recovery

PDF invocation 11 failed before scheduling tasks because the field-parity test imported its fixture from its previous directory. A later read confirmed another agent already corrected the import to `../../🧫️fixtures/🪪️field-parity/🔣️.json`; no import or fixture edit was made by this task.

The next GIS graph failure reported unsorted `generatorContracts["jco-package-adapter"].inputPatterns`. The coordinator normalized only that existing input list without changing membership. This is required by the existing taxonomy contract; native retries remain pending.

The final narrow Semio run later failed graph construction on the store artifact-addressing link-schema import. A subsequent coordinator read resolved the exact relative path and parsed the existing JSON successfully; no source edit was necessary. This was an observed transient shared source state, not a Semio compile failure.

BREP TypeScript check/test attempts later failed graph construction while actor-import project metadata moved from browser-bundle/🧫️fixtures to browser-bundle/🧪️testkit. Read-only inspection verified the new project file exists and found no stale textual project reference in the relevant taxonomy or scripts. This is consistent with Nx enumerating the old path before a concurrent move; it is not proof of a persistent missing project. The owner will retry sequentially with fresh private workspace data, preserving both failed logs. No old metadata file was recreated.

The Playbook document-contract fixture was subsequently relocated. Its current test had also redirected all five schema imports into nonexistent fixture paths, not just the vector import. The coordinator restored the five canonical schema destinations after verifying each current JSON file exists and parses, and pointed only vectors to `../../🧫️fixtures/🪪️document-contract/🔣️.json`. All six JSON imports now resolve. Other concurrent test edits were preserved. This exact import correction is in the coordinator file ledger; native retries remain pending.

A bounded read-only follow-up scanned all 7 plugin document-contract test modules for JSON import resolution after the Playbook repair. It found 16 missing imports; the exact source/name/destination census is retained in generated output.

The follow-up repaired all16 missing canonical schema imports in Forms, Norm, Trinity Rewriting, and Note while preserving their relocated vector imports. Norm and Note dynamic facet imports also incorrectly targeted the vector fixture; those now load the canonical `🔣️.json` adjacent to each facet implementation. The first scoped repair attempt stopped at the untracked Norm file after correcting Forms; the follow-up completed the remaining files using verified canonical schema destinations. No whole-file reset occurred. All four paths are in the coordinator ledger and exact static import repairs are retained in generated evidence.

The next graph run cleared document-contract imports and reached a stale root router import of the relocated shared-map-delta oracle. Another agent corrected that import to `🧪️tests/🧪️shared-map-delta-source/🟦️.ts` between the coordinator read and its scoped patch attempt, so the patch made no change. BREP native attempts27993 and15058 both terminated before Cargo on graph prerequisites and provide no native result.

After imports cleared, taxonomy validation threw ReferenceError for the undeclared `artifactProjection` flag. The validator loop already restricts contracts to the two artifact projection rationale kinds and validates their sourceArtifactMemberName. The coordinator removed only the stale flag reference, retaining the source-member guard and exact forward-profile validation. The discovery path is in the coordinator ledger; normal graph generation must still verify this scoped correction.

## Fresh Graph Acceptance

The fresh private-state invocation `bun x nx run repo:artifact-package-contract --output-style=stream` cleared current Nx project-graph construction and entered the repository contract. Its full result remains pending. The affected Semio TypeScript package independently cleared the current graph and passed its ordinary Nx `check` target in 9.3 seconds; its package `test` target is still running. Norm's correct ordinary `workspace:norm-document-contract` target also exited 0, including 46 snapshot and 18 diff vectors and independent owner/child rejection. Raw receipts stay under `🗑️generated` until ticket completion.

The fresh repository artifact-package contract subsequently finished with process exit 0. Current ownership, source placement, schema oracle and Cargo/Nx dependency checks are accepted on the repaired graph. Raw receipt: `🗑️generated/artifact-package-contract-final.txt`.

## Document Contract Runtime Verification

All four repaired import consumers executed through their ordinary workspace Nx routes. Playbook accepted 18 snapshots and 5 committed diffs; Forms accepted 20 snapshots and 6 committed diffs; Note accepted 66 snapshots and 33 committed diffs, plus 3 independent link/block delta vectors. All include independent owner/child rejection. Norm separately accepted its 46 snapshots and 18 diffs. These checks establish real fixture loading after the path repairs.

Rewriting's first exit-0 receipt was insufficient: its console reported zero committed diffs. The fixture relocation had also rewritten its recursive suffix matcher and left the scan rooted in the old mutations source directory. Added a nonempty-coverage assertion first and ran the ordinary route; it failed with the expected assertion and exact process exit 1 (`repaired-rewriting-document-contract-nonempty-red.txt`). Then corrected only the scan root to the actual subset fixture taxonomy and its suffix to `/🔺️diff/🔣️.json`. The green retry is in progress; the old zero-input receipt is not counted as committed-diff acceptance. Existing document/value rejection cases were preserved.
