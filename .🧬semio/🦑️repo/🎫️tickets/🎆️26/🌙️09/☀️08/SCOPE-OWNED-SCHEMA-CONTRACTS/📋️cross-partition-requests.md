# Cross-partition request ledger

Coordinator routing of requests raised in worker reports. Status: `open` → `dispatched(<wave>)` → `done`.

| # | From | To (partition) | Request | Status |
|---|------|----------------|---------|--------|
| 1 | wp2-rust-schema-registry | every scope with named exports (plugins, hub, os, framework) | call `register_scope_schema_exports(ScopeSchemaExports{scope, exports})` beside `register_artifact_schema_descriptor`; scope id = module `$id` path form; export ids must not reuse artifact/snapshot/diff/mutations | open |
| 2 | wp2-rust-schema-registry | W2 tooling (root script) | `schema verify` cross-checks `🔣️schema-catalog.json` against `schema_export_catalog_entries()`; ascii format ids, map via `SCHEMA_FORMAT_TAXONOMY_KEYS` | done(W2t) |
| 3 | wp2-rust-schema-registry | W2 tooling (root script/project/launch) | nx target for `🧰️framework/🔨️modules/🧬️schema/✅️draft07-oracle.test.ts`, part of `schema test` | done(W2s) |
| 4 | wp2-rust-schema-registry | W2 tooling (root package.json) | declare `ajv` explicitly in devDependencies | done(W2s) |
| 5 | wp2-rust-schema-registry | W5 os (🌉️mcp) | decide 2020-12 wire label vs draft-07 validator once tree is draft-07 | dispatched(W5b) |
| 6 | wp4-plugins-conventions | W10 framework modules (🖱️ui) | export `RetainedCommandLimits`, `RetainedCommandRoute`, `RetainedCommandRoutes` at `https://semio.tech/schema/framework/ui/schema.json#/$defs/…` | dispatched(W10 brief) |
| 7 | wp1-harness-invariants | W7 mutations (🏛️architect) | 61 leaves under `🏛️architect/🗿️artifacts/🏛️program/…` carry no `🔣️.json` descriptor, so nothing declares their payload contract | dispatched(W7c) |
| 8 | wp1-harness-invariants | coordinator | harness report shows no vitest run of the harness suite; coordinator runs `bunx vitest run` in `🧪️test/📦️packages/🟦️typescript` (scratchpad `wp1-harness-vitest.txt`) | in progress |
| 9 | wp4-mutations | W2 tooling (root script) | `policyMutationStructuralBreachesView` aggregate identity check now relies on `x-semio-mutationKinds` in the `$ref` union; keep or replace with structural check | done(W2s) |
| 10 | coordinator (from wp2 check: scope-id-duplicate=2249) | W7 + W8 mutation workers | rewrite every leaf `$id` to `…/<root scope path>/mutation/<semanticKind>/schema.json` per contract §A `$id` grammar; aggregates keep their `$ref`s (paths unchanged) | dispatched(W7c) |
| 11 | coordinator | W2 tooling | generator must treat `🧬️mutations/<leaf>/🧬️schema/🔣️.json` as its own scope (`<root>.mutation.<kind>`), not a facet of the root; facet documents never deepen scope; add Rust-vs-catalog parity code once exports register; tolerate torn catalog reads in `schema test` | done(W2t) |
| 12 | wp2-mechanism-tooling | everyone with `$id`s | `module-scope-id-inconsistent=135`, `document-id-missing=84`, `document-dialect-unexpected=1020` rows in `🗑️generated/schema-check.jsonl` are partition-owned; each wave-2 worker clears its rows | dispatched(W7c) |
| 13 | wp2-mechanism-tooling | W1 harness | harness fixture cases live at `📚️library/🧪️tests/🧬️schema-scope-catalog/🧫️fixtures/🔣️.json`; reuse rather than re-author | done(W2t) |
| 14 | wp4-repo-product | W2 tooling (taxonomy) | `schemaScopeOwnerLevels` must declare `♻️mit-bestand/<product>/🔨️modules/<m>`; `🗑️generated/schema-check.jsonl` reports `module-level-ineligible` for the two new modules `♻️mit-bestand/📋️bericht/🔨️modules/📄️documents/🧬️schema` and `♻️mit-bestand/🧺️demonstrator/🔨️modules/🧩️runtime/🧬️schema` | done(W2t) |
| 15 | wp4-repo-product | W-tooling (`📚️library/⚡️caching/🧪️tests/📜️script.ts`) | `testDemonstratorRuntime()` reads the deleted `🧩️runtime/🧬️schema.json` and `🧬️pipeline.schema.json`; read `🧩️runtime/🧬️schema/🔣️.json` instead (draft-07 → plain `ajv`, not `ajv/dist/2020`), pipeline at `$ref: "#/$defs/DemonstratorPipelineContract"` with a varied `$id` — exact diff in `📓️wp4-repo-product.md` §7.1 | done(W2t) |
| 16 | wp4-repo-product | W-tooling (`📚️library/📦️packages/🟦️typescript/🧫️fixtures/🧼️remaining-package-purity-authority/🔣️.json`) | drop the rows for the deleted `🎛️coordinator/📦️packages/🟦️typescript/✅️validation.ts` and `🔬️validation.test.ts` (lines ~5102/5110 and ~5196/5204); optionally add `🔬️schema.test.ts` and `🔬️server-persistence.test.ts` | done(W2t) |
| 17 | wp4-repo-product | W-tooling (`📚️library/🐹️.go:345`) | event emitter posts to `/api/v1/events`; the route is `/api/v1/event` (singular) so it has never reached the coordinator. Body already matches `$defs.EventPublishRequest` | done(W2t) |
| 18 | wp4-repo-product | W9b repo product (`💻️client/🪶️sqlite`) | two different unreferenced SQLite schemas outside any module: decision = the sqlite client module owns `🪶️sqlite/🧬️schema/` (`🗄️.sql` + `🔣️.json` row exports); keep only the schema the live client actually creates/uses, delete the other | dispatched(W9b) |
| 19 | wp4-repo-product | W9b repo product (`💻️client/⌨️cli`) | exhaustive Go tests time out at 10 min under load; establish baseline once the machine is idle | dispatched(W9b) |
| 20 | wp4-mutations | W2c tooling (`📚️library/🧪️tests/🔏️path-emoji-statutes/🟦️.ts`) | three live assertions hard-code `payloadSchema === "🧬️.schema.json"`; must assert the taxonomy location `🧬️schema/🔣️.json` | dispatched(W2s) |
| 21 | wp4-mutations | W1b harness (`🧪️test/📜️script.ts payloadSchemaCommand`) | confirm no code path still writes `🔣️.schema.json`; W1 reported `--write` removed — verify by grep | dispatched(W1b) |
| 22 | wp4-mutations | W6 plugins (`✏️editor/*/🦀️.rs`, 22 files) | `payload_schema: "🔣️.schema.json"` literals for owners that name no leaf directory; point at the taxonomy location or drop the field where the owner has no payload schema | done(W6c) |
| 23 | wp4-mutations | W7b/W8 (after W2b lands structural aggregate check) | drop `x-semio-mutationKinds` from aggregates (ajv `strict:true` rejects unknown keywords) | open(unblocked, row 90) |
| 24 | wp4-hub §7.1-2 | W5b os (`📇️directory`, `🌉️mcp`) | approval request/receipt + undo/checkpoint-frontier types: os copies become conformance-checked mirrors `$ref`-ing `https://semio.tech/schema/hub/inference/schema.json#/$defs/InferenceApprovalRequestV1|InferenceApprovalReceiptV1|…`; inline AJV literal in `💡️inference-bridge.ts` removed | dispatched(W5b) |
| 25 | wp4-hub §7.5 | W5b os (`🧑‍💻dev/…/📜️script.ts:5269-5277`) | reads deleted `🛰️lag-rebootstrap/🧪️fixtures/🪢️canonical-pair/🧬️.schema.json`; replacement in `📓️wp4-hub-bootstrap-auth-admin.md` §E.1 | dispatched(W5b) |
| 26 | wp4-hub §7.3-4,6 | W4b hub (after W5/W6/W10 land) | hub `📜️script.ts` readers still point at schemas deleted by stdio/gis/value/os-db/os-directory partitions (list in §7.4); rewire to those scopes' modules; feed catalog `dependsOn` into one shared Ajv so cross-scope `$ref`s replace restatements; replace hand-maintained `HUB_SCHEMA_SCOPES` with a catalog read; re-run `cargo check -p semio-hub --all-targets` once the gis crate split lands | done(W4c; 4 externals pending rows 85-86) |
| 27 | wp4-hub §8.2 | contract | fixture directory name: taxonomy `testFixturesDirName` (`🧫️fixtures`) is canonical; `🧪️fixtures` directories are renamed by their partition owners in wave 2 | decided |
| 28 | wp4-hub §8.3 | contract | dev-tool output graphs that are serialized and consumed are scope contracts; `hub.admin` module stays | decided |
| 29 | wp4-hub §8.5 | contract | deviation accepted: hub fixtures routed by real owner (`hub.directory`, `hub.artifact-authority.native-openable-provider`), not all to `hub.inference` | accepted |
| 30 | wp4-hub | W3b framework schema registry | owned validator lacks `if`/`then`/`else` (four hub modules use them); complete draft-07 keyword coverage + pinned `format` policy + runtime entries dump for `schema verify --rust-entries` | dispatched(W3b) |
| 31 | wp4-hub §6.7 | peer session (not this ticket) | `os-hub` binary has 6 `E0308`s from an os-kernel `DirectoryStreamMessage::Event` boxing change; `🚀️bin.rs` staged by a peer; exact fixes recorded in `📓️wp4-hub.md` | external |
| 32 | coordinator (harness `bun test` full run, `🗑️generated/harness-bun-test-full.txt`) | W1b harness (after W1 finishes) | 144 pass / 28 fail across `🧪️index.test.ts` + `🧬️invariants.test.ts`: oracle registry (2), discovery and contract (3), dependency ratchet, clean safety, non-aggregate metrics, case-above-subset, mutation-without-fixture census, oracle purity (2), cross-language oracle hosts, recorded production debt (3), comparison profiles (4), contribution directory ownership (likely W2's 174 dropped `testContributionDirectoryOverrides`), open/closed (root script names the test module), physical mutation vector registry (3), owner eligibility glob vocabulary, parity with the catalog generator's vector, exempt-area discovery. Triage each: caused by this ticket's migrations vs peer churn vs stale expectations; fix in partition, request the rest | dispatched(W1b) |
| 33 | wp4-framework-modules §7 (`wp4-framework-cross-partition.md`) | W5b os | ~50 os consumer path/`$id` rewrites: `⚛️react/🔬️index.test.ts`, `🧯️router-plugin-faults.test.ts`, `📃️UiDocumentStore/🟦️.tsx` (40 paths), `🔌️PluginRuntime/🟦️.tsx`, `🔌️plugin/📤️return`, `📥️poll/🏘️composition`, `🔌️plugin/📦️packages/🟦️typescript`, `🛠️ShellHelpers`, `⚛️reactor/📨️pending/🧬️authority.schema.json`, `🧑‍🎨engine/💾️resident/🧬️schema.json`, tutorial-interaction fixture, dev distribution manifest regenerate | dispatched(W5b) |
| 34 | wp4-framework-modules §7 | W2c tooling (root script + taxonomy + 📚️library tests) | root `📜️script.ts` `🧵️job` fixture-schema paths + `⏱️trace` contention path (`…/⏱️clock/🧬️contention/🧬️schema/🔣️.json`, export `Contention`); taxonomy literal paths (`🕸️graph/🛂️manifest/🧬️outputs.schema.json`, `🖼️assets/🔤️fonts/🧬️catalog.schema.json`); `🧪️tests/🤝️package-language-kind-handoff/💾️resident-package` value-resident paths; job-budget consolidation readers (§8.3) | done(W2s) |
| 35 | wp4-framework-modules §8.3 | W10b framework | consolidate `🧵️job/⏱️budget/🧬️schema/{⏱️clock,🪢️binding,🪫️budget}.json` into `🔣️.json` `$defs` (W2c rewires the root script readers) | done(W10b) |
| 36 | wp4-framework-modules O-1 | W10b framework + W5b os | decision: rename `🎭️actor/🎠️activation` → `🎠️activation-reservation` so `framework.actor.activation-reservation` is derivable; drop the `SCOPE_OVERRIDE`; fix `#[path]` (W10b) and the three os `📜️script.ts` joins (W5b) | done(W10b) |
| 37 | wp4-framework-modules O-2 | W10b framework | decision: hoist `framework.ui.render.targets.metal.packages.rust` and `…targets.webgpu` modules to `framework.ui.render/🧬️schema/` as named exports (`MetalAbi…`, `Webgpu…`); rewire the metal/webgpu Rust packages' `include_str!` | done(W10b) |
| 38 | wp4-framework-modules O-3 | W10b framework | hand-author `🖱️ui/🖥️host` declaration contract with real bounds, replacing the generated structural schema | done(W10b) |
| 39 | wp4-framework-modules O-4 | W6b plugins | normalize retained-command lane/disposition/execution casing in plugin data to one vocabulary (`artifact|config|host-only`, `migrated`); tighten `framework.ui` shape afterwards (W10b) | done(W6c) |
| 40 | wp4-framework-modules O-5 | W10b framework | register the 117 modules / 211 exports via `register_scope_schema_exports` in each module's `🦀️.rs` (row 1 pattern) | partial(W10b; framework.ui.contract blocked on row 76) |
| 41 | wp4-framework-modules §8.2 | contract | accepted: `🖱️ui/🧬️contract/` (singular) is the `ui-contract` crate root and an eligible nested module scope; only `🧬️contracts/<x>` per-contract directories are forbidden | accepted |
| 42 | wp1 §6.1 | W2c tooling (taxonomy `fixtureOwnerReason`) | decision: a schema document inside a `🧪️*`/`🧫️*` tree IS a finding (`schema-fixture-defines-schema`) unless the enclosing case declares `inertSchemaData` explicitly (parser test inputs); align the taxonomy text and `schema check` with the harness | done(W2t) |
| 43 | wp1 §6.3 | W2c tooling (catalog shape) | catalog must name the file carrying each export: `exports: { "<ExportId>": { "file": "<module-relative path>", "facet": "<facet id>" } }` (1,006 exports live in `🔺️diff/📸️snapshot/💡️inferences/🧬️mutations` children); the 351 exports referenced but declared nowhere are listed per partition in `🗑️generated/` by the harness report — each partition owner declares or removes the reference | done(W2t) |
| 44 | wp1 §6.2 | W2c tooling (`📚️library/🧪️tests/🗺️testing-readme-coordinates/🟦️.ts:127`) + W1b | test-protocol `$id` migration: W1b changes the `$id`, W2c updates line 127 in the same wave | dispatched(W1b) |
| 45 | wp4-stdio-mutations §9 | W8b stdio | decision: contract §A grammar applies to stdio too — leaf `$id` `https://semio.tech/schema/s/stdio/<artifact>/<standard>/<subset>/mutation/<semanticKind>/schema.json` (slash scope path, keyed by `semanticKind`, not directory path), aggregate `$id` `…/<subset>/mutations.json`; rewrite all 1011 ids via `ID_ROOT`/`leaves()` + `audit --write` + `aggregates --write` | done(W8b) |
| 46 | wp4-stdio-mutations §8.1 | W8c stdio (separate wave, needs a stdio build; machine must be idle) | casing conformance: taxonomy declares camelCase for JSON Schema; 440 stdio payload structs + nested snapshot types lack `#[value(rename_all = "camelCase")]`; add it, re-case committed fixtures, regenerate schemas with `audit --write`; verify with a stdio build | open |
| 47 | wp4-stdio-mutations §8.3-4 | W10c framework (`🌱️value/✨️derive/🦀️.rs`) | (a) `field_rename_all()` falls back from `rename_all_fields` to `rename_all` — serde does not; remove the fallback and re-derive the four stdio fixtures' expectations with W8; (b) internally tagged newtype asymmetry: encode emits `{tag, value: scalar}`, decode hands the object to a scalar `FromValue` — fix decode to unwrap `value` (affects `JsonPathSegment`, `XlsxCellValue`) | dispatched(W10d) |
| 48 | wp4-stdio-mutations §9 | W2c tooling (multi-format parity policies `📜️script.ts:30343,30447,30883`) | with `title` = Rust type and real wire field names in JSON Schema, the `🟦️.ts`/`🔗️.graphql`/`🛰️.proto` leaf surfaces may now be the drifted side; run the parity policies on stdio and report | blocked(row 89) |
| 49 | wp4-stdio-mutations §7 | W8c stdio (same build wave as row 46) | decision: schema-first — the committed fixtures (`{"mutation": "setSnapshot", …}`) are the intended wire; add `#[value(tag = "mutation")]` to `BcfMutation`, the mp3 and csv mutation enums; for the four `rename_all` fallback cases the fixtures written against serde semantics are correct and the derive is fixed (row 47a); re-run the 188 fixture replays after the build | open |
| 50 | wp4-plugins §6A | W4b hub (`🌎️hub/📦️packages/🦀️rust/📜️script.ts` lines 4746-4784, 5195, 9079) | six reads of files migrated into `s.stdio.registry`; load `✏️s/🔌️plugins/🗄️stdio/📇️registry/🧬️schema/🔣️.json` once, `addSchema`, compile `$ref` per export table in the report (BREAKING NOW) | done(W4c; 4 externals pending rows 85-86) |
| 51 | wp4-plugins §6B | W10c framework (`🖱️ui/🧬️schema/🔣️.json`) | widen `retainedCommandOracle` (+`ownedInterface`, `expected`), `retainedCommandByteBudget` (+`artifactStoreBytes`, `decodedItems`), `retainedCommandLane` (+`transient`), `RetainedCommandRoutesDocument` (+`maximumRawBytes`, `maximumWorkItems`), add `RetainedCommandCohort` and a public `RetainedCommandRoute` export | dispatched(W10d) |
| 52 | wp4-plugins §6C-D | W5b os | `🌉️mcp/📦️packages/🦀️rust/📜️script.ts` ~144/163 must target `s.gis` exports if a schema read returns; `🔌️plugin/📇️registry/🪪️plugin-identity.test.ts:89` reads `<fixtureDir>/🧬️.schema.json` → read `<pluginRoot>/🧬️schema/🔣️.json` `$defs.<Plugin>PluginIdentity` | dispatched(W5b) |
| 53 | wp4-plugins §6E,F | W2c tooling | taxonomy `members-of-fixtures.memberNames` lists three deleted files; regenerate catalog (12 new plugin-root scopes; `dependsOn` derived from `$ref`s so the 7 `schema-cross-scope-dependency-forbidden` rows resolve); add `plugin-extension` owner level `✏️s/🔌️plugins/*/🧩️extensions/*`; implement `x-semio-formats` restricted-support rule (contract §B) in `schema check` | done(W2t) |
| 54 | wp4-plugins §6G | W2c tooling (📚️library TS package) | hoist `compileScopeExport(modulePath, exportId)` (ajv draft-07, `x-semio-*` keywords, numeric formats) into the repo library package; consumers in gis/stdio/space/flow/block/draw/lowpoly/norm/hub/os scripts switch to it in their next pass | dispatched(W2s) |
| 55 | wp4-plugins §7.2 | W6b plugins | eleven same-pattern files outside fixture trees (`🔏️publication-authority/🧬️.schema.json` ×3, cad retained-jobs + presence, vcs/gis `📇️native-codecs/🧬️.schema.json`, norm registry contract + `🧬️mutation-leaf-taxonomy-v1.schema.json`, stdio `📦️artifact-package/🧬️.schema.json`, `📇️registry/🧬️schema/📜️native-codec-factories.json`) → owner modules; puzzle `cargo check --target wasm32-wasip2` re-run to green; `PlaybookViewCommandLimits` (no consumer) deleted with its data unless a fixture binds it; annotate law exports with `x-semio-formats`; row 22 (`✏️editor/*/🦀️.rs` payload_schema literals) and row 39 (casing) | done(W6c) |
| 56 | wp4-plugins §6F | W1b harness | completeness rule: an export annotated `x-semio-formats` is complete when present in exactly those formats; unannotated exports must exist in every format the scope provides | done(W1c) |
| 57 | wp1b R-1 | W2c tooling (taxonomy) | `taxonomy.areas` declares no `exempt` area, so the harness `exempt area` test cannot pass; declare the exempt areas the discovery library actually skips (tickets, vendor) or drop the concept with the test (coordinator: declare them) | done(W2t) |
| 58 | wp1b R-2 | W2c tooling (taxonomy) | `testContributionDirectoryOverrides` 176 → 2: confirm every owner's contribution dir is really `🔮️oracle` on disk; owners that name another directory must keep their override (drop only true duplicates of the default) | done(W2t) |
| 59 | wp1b R-3 | W2c tooling (root script) | root `📜️script.ts` must not contain the literal test-domain path or phase vocabulary (`🧩️ open/closed` test); read them from the taxonomy | done(W2v) |
| 60 | wp1b R-4 | W2c tooling (`📚️library/🧪️tests/🧬️schema-scope-catalog/🧫️fixtures/🔣️.json`) | remove the `fixture-owned-schema` "silent" case and `fixtureOwnerPathPatterns` exemption; per contract/row 42 a fixture-tree schema is a finding unless `inertSchemaData` is declared | done(W2t) |
| 61 | wp1b R-5 | W2c tooling (`📚️library/🧪️tests/🗺️testing-readme-coordinates/🟦️.ts:127`) | expect `$id` `https://semio.tech/schema/repo/test/schema.json` | done(W2t) |
| 62 | wp1b R-6 | peer session (external) | a peer is refactoring `🧪️test/📦️packages/🟦️typescript/🟦️.ts` (removed `loadMigrationBaseline`, `surveyUnmanagedTests` still imported by `🧪️index.test.ts`) | external |
| 63 | wp1b R-7, R-8 | W2c tooling (catalog generator) | regenerate catalog with `exports: {ExportId: {file, facet}}` (nested facet dirs in `file`); emit a scope for the repository test module `🧪️test/🧬️schema` | done(W2t) |
| 64 | wp3b §7.1-3 | W2c tooling (root script/project/launch/package.json) | oracle spec path moved by the taxonomy sweep to `🧬️schema/🧪️tests/✅️draft07-oracle/🟦️.ts`: point `SCHEMA_DRAFT07_ORACLE_SPEC` (`📜️script.ts:22732`) there and add a vitest include for `🟦️.ts` (`:22868`); register `cargo test -p semio-framework-schema --test schema-module-compile` (gate, `SEMIO_SCHEMA_MODULE_COMPILE_OUT`) and `--test schema-export-entries` (`SEMIO_SCHEMA_EXPORT_ENTRIES_OUT`) as `schema verify` inputs; declare `ajv` + `ajv-formats` in devDependencies | done(W2s) |
| 65 | wp3b §7.4-5 | W5b os | 32 path-style `$ref`s → target `$id` + `#/$defs/<Export>`: `🏪️store/🧫️fixtures/**/🧬️mutations/*` (13), `📡️spr/🎮️command/🧪️tests/🧬️mutation-laws/**` (5), `♾️infinite/**` (4), `🔌️plugin/🧪️tests/🛰️declaration-channels/**` + `⚛️reactor/💼️jobs/🧬️mutation-plan/**` (`https://semio.dev/…` ids), `🔌️plugin/📇️registry/📦️deployment` (`semio:installation-directory-v1`); `📇️directory/🧬️schema/🔣️.json` OpenAPI `discriminator` → `oneOf` + `const` tag; full list `🗑️generated/wp3b-module-compile.json` | dispatched(W5b) |
| 66 | wp3b §7.4 | W7c mutations | `📸️remodel/**/✏️editor/{🎚️config,👥️presence}/🧬️schema/🧬️mutations/*` (3) `$ref` `../../../🔣️.json` → target `$id` form | dispatched(W7c) |
| 67 | wp3b §7.6 | W6b plugins (`🎬️sequence/…/✏️editor/🌉️wasm/🧬️schema/🔣️.json`) + W5b os (`🌊️flow/🕸️wasm/🧬️schema/🔣️.json`) | decision: wasm surface descriptors are interface facets — express as `🧬️schema/📜️.wit` (taxonomy `📜️interface` facet, normative wit) and delete the JSON from the schema slot; if a JSON consumer exists, it reads a generated projection with provenance, never the slot | plugins done(W6c); os W5b running |
| 68 | wp3b §7.7 | W3c framework schema + W9c repo client | `🧰️framework/🔨️modules/🧬️schema/🔣️.json` is a technology-catalog array duplicated in `🦑️repo/🔨️modules/💻️client/🧩️vscode/📦️packages/🟦️typescript/🟦️.ts`: decision — the vscode client owns it: W9c moves the data to `🧩️vscode/🗂️technologies.json` with a `🧩️vscode/🧬️schema/🔣️.json` `TechnologyCatalog` export and the TS reads the JSON; W3c replaces the framework file with the real `framework.schema` facet (`$id https://semio.tech/schema/framework/schema/schema.json`, `$defs` SchemaExport/ScopeSchemaExports/SchemaExportEntry/SchemaResolveError) | done (both halves; client scope id derives to `repo.client.vscode`, `📓️wp4c-repo-product.md` §4 — but the framework generator still reads the replaced file, now row 101) |
| 69 | wp3b §7.8, §8 | W2c tooling | `schema check` enforces `$id` uniqueness statically (1 collision in `🗑️generated/wp3b-module-compile.json`) and compiles each module against exactly its declared `dependsOn` set (undeclared cross-scope edge = error); reject `enum: []` with a hint (`propertyNames: false` / `maxProperties: 0`) | done(W2t) |
| 70 | wp4b-repo-client A | W3c framework schema (`🧬️schema/📦️packages/🦀️rust/📜️script.ts:115`) | entity-catalog emitter Go target path → `💻️client/⌨️cli/🐹️entity_kinds.g.go` (emoji dir is uncompilable by Go); time-sensitive | done(W3d) |
| 71 | wp4b-repo-client B, F | W2c tooling | launch.json entry `🪶️sqlite schema test` (`bun nx run @semio-tech/repo-sqlite:test`, group 3_dev after `🦑️mcp test`); taxonomy `outputRoots` entry `:24661` → same Go path as row 70; regenerate catalog (picks up `repo.client.sqlite`); `memberNames` edit so `🔌️mcp/🔗️graphql/🔗️.graphql` can move into a `🧬️schema/` module | dispatched(W2t,W2s) |
| 72 | wp4b-repo-client C, D | W9c repo product | README link `🔨️modules/💻️client/🪶️sqlite/README.md`; `🔬️server-persistence.test.ts` constraint-keyword `startsWith` → word-boundary regex | done (`📓️wp4c-repo-product.md` §3; the regex fix exposed six columns missing from `repo.server`'s `🔣️.json` and a coordinator vitest config that ran zero tests — both fixed) |
| 73 | coordinator (os residue grep) | W5b os | `🧰️framework/🛍️products/💻️os/🧫️fixtures/🎒️pack-dynamic-integer-v1/🧬️.schema.json` still exists on disk (fixture-owned schema; possibly a peer's new file or an unfinished lane); migrate to the owning module (`framework.value` pack integer carrier or `os.kernel`), delete | dispatched(W5b) |
| 74 | wp4b-framework §7.1, §7.4 | W5b os | `🔌️plugin/🖥️host/📦️packages/🦀️rust/📜️script.ts:187` `🎠️activation` → `🎠️activation-reservation` (lines 165/179 untouched); os crates call `register_scope_schema_exports` for their modules (row 1 pattern) | dispatched(W5b) |
| 75 | wp4b-framework §7.2, §7.5 | W2c tooling | root `📜️script.ts` `toolJobMicrosecondBudgetSelfTests()` :2015-2058 → one module read + `$defs/Budget|Clock|Binding` (exact diff in the report); regenerate catalog + docs (`catalog-stale=1`) | done(W2s) |
| 76 | wp4b-framework §7.3 | W3d framework schema | decision: split the registry/resolver types (`SchemaFormat`, `FacetLeaves`, `SchemaExport`, `ScopeSchemaExports`, registry OnceLock, resolve API) into an os-kernel-free leaf crate `semio-framework-schema-registry` under `🧰️framework/🔨️modules/🧬️schema/📇️registry/📦️packages/🦀️rust`; `semio-framework-schema` re-exports it; dependency-restricted scopes (`framework.ui.contract`, …) register from their own crate | done(W3d) |
| 77 | wp4b-framework O-2b | W2c tooling + W1b harness | decision: module-internal `$ref`s (`#/definitions/<helper>`, `#/$defs/<Export>`) are legal and never findings; `ref-not-export-addressed` / `ref-not-catalog-addressable` apply only to cross-document refs, which must be `<$id>#/$defs/<ExportId>`; helpers stay in `definitions`, exports in `$defs` | done(W1c) |
| 78 | wp4b-stdio §5.2, §7.2 | W2c tooling | decision: row 45 grammar stands (subset-level slash scope path). `mutation-leaf-id-grammar` must expect `<root module $id scope path>/mutation/<semanticKind>/schema.json` derived from the root `🧬️schema/🔣️.json` `$id`, never a dotted artifact-level id (collides 229 stdio leaves onto 80 ids); fix the coverage gap (check reaches 491/915 leaves, 50/88 aggregates, 55/66 facets); stop double-counting `module-scope-id-inconsistent` on aggregates | done(W2t) |
| 79 | wp4b-stdio §7.1 | contract → W7c + W8c | decision: aggregate `oneOf` branches reference leaves by absolute `$id` (`<leaf $id>` or `<leaf $id>#/$defs/Payload`), resolved through the catalog; validators register all leaves in one Ajv by `$id`; `wp4-mutation-aggregates.py`/`wp4-stdio-schemas.py aggregates --write` one-line change + regen | stdio done(W8d); W7c running |
| 80 | wp4b-stdio §7.3 | W2c tooling (taxonomy `schemaScopeOwnerLevels`) | admit two-segment mutation leaves `…/🧬️mutations/<domain>/<verb>` (120 gltf leaves) | done(W2t) |
| 81 | wp4b-stdio §7.4-5 | W8c stdio (JSON pass) | codec facet documents `🧬️mutations/📝️text` + `💾️binary`: keep the six real grammars, delete placeholders/byte-identical restatements/`{"type":"object"}` stubs, unify export-id spelling to `<Aggregate>MutationText` / `…MutationBinary`; gltf `📜️contract/🔣️.json` (120): delete the stray `$schema` key (data, not schema) | done(W8d) |
| 82 | wp4b-stdio §7.6 | W8d stdio build wave | corrections: row 49 attribute is `#[value(tag = "mutation", rename_all = "camelCase")]`; row 46 is 569 structs (128 move the wire), 16 enums excluded; 17 enums need `rename_all_fields` in the same commit as row 47a; work order in `wp4b-stdio-rust-changes.md` | open |
| 83 | wp4b-stdio §7.7 | W6c plugins (`🗄️stdio` outside `🧬️mutations`) | non-mutation stdio `$id`s (`💡️inferences` etc.) use artifact-level paths with standard/subset dropped; 415 `document-dialect-unexpected` + 189 `module-scope-id-inconsistent` under `🗄️stdio` outside mutation trees → contract grammar | done(W6c) |
| 84 | wp4b-hub §6.1 | W2u tooling (taxonomy) | add owner level `plugin-submodule` (`✏️s/🔌️plugins/<p>/<module>`, e.g. `🗄️stdio/📇️registry`) to `schemaScopeOwnerLevels`; hub then drops `UNCATALOGUED_SCHEMA_MODULES` | done(W2t) |
| 85 | wp4b-hub §6.2-3 | W5b os (`📇️directory`, `🔌️plugin/🌐️browser-bundle/{🧵️child,🧾️describe}`) | delete the five `discriminator` annotations (row 65); browser-bundle modules: `$id` `https://semio.tech/schemas/os/…` (plural, wrong) → `https://semio.tech/schema/os/plugin/browser-bundle/child|describe/schema.json` with `$defs` exports `BrowserActorChildV1`, `BrowserActorDescribeV1` | open |
| 86 | wp4b-hub §6.4-5 | W6c plugins (gis, vcs) | `🌍️gis/📇️native-codecs/🧬️.schema.json` → `s.gis` module export `NativeCodecReceipts` (2020-12 → draft-07); `proveGisNativeCodecReceipts` (`gis/📦️packages/🦀️rust/📜️script.ts:54`) and `proveVcsNativeCodecReceipts` (`vcs/…/📜️script.ts:34`) still read the deleted `🌱️artifact-document-id-v1/🧬️.schema.json` and `📇️native-codecs/🧬️.schema.json` with `Ajv2020` → bind the owner exports with draft-07 ajv | open |
| 87 | wp4b-hub §6.6 | W2t tooling | catalog `dependsOn` is empty for 2593/2620 scopes; generator must derive it from cross-document `$ref`s (row 63 already) — hub drops `schemaReferencedScopes` afterwards | done(W2t) |
| 88 | wp4b-hub (hazard) | every partition | source-text laws (`readFileSync(…"🦀️.rs")` grep assertions in `📜️script.ts` oracles) break while the END-TO-END-TESTING-REFACTOR peer moves `#[cfg(test)]` law names into `🧪️tests/🔬️unit/🦀️.rs`; use a `moduleRustSource()`-style helper that reads the module root + its `🧪️tests` unit file; stopped workers' edits are STAGED (auto-commit) — check `git diff --cached`, not only unstaged diffs | note |
| 89 | wp2c-root-script §3.3, §4.5 | external (COMPOSABLE-STDIO-ARTIFACT-PACKAGES ticket owns stdio artifact definitions) | `runtime_capabilities` ids (308/308) do not match `stdioAssertDefinition`'s `<artifactId>.runtime.<category>.<leaf>.vN` grammar since 2026-09-05; the root stdio gate (`schema-parity`, `quick`, `standards-coverage`, `codec`, `mutation-law`, `inference`) is dead until that ticket settles the id scheme; row 48 unmeasurable until then | external |
| 90 | wp2c-root-script §4.6 | W7c + W8d (next pass) | row 23 unblocked: `policyMutationAggregateMembers` is structural; drop `x-semio-mutationKinds` from aggregates; then tooling deletes the relative-path tolerance so §B is enforced | open |
| 91 | wp2c-root-script §6 | external (repo boundary) | `verify taxonomy report` blocked repo-wide: `♻️mit-bestand/🔎️recherche` gitlink needs an explicit repository-boundary decision in the taxonomy; not schema-related | external |
| 92 | wp4c-framework §4.2, §8.1-2 | contract → W10e framework + W6c plugins | decisions: lanes = the runtime `ArtifactToolPublicationLane` seven in kebab case (`artifact|config|host-only|draft|presence|transient|child`); disposition/admission/status = the runtime `InteractiveJobClassification` in kebab case (`unclassified|migrated|batch-only-pending-rewrite|forbidden-from-ui|deleted|fail-closed`), no collapse to `batch-only`; plugin re-casing map (row 39) updated accordingly | decided |
| 93 | wp4c-framework §5.1-5.2 | W6c plugins | apply the 130-value re-casing across 7 fixture files with the row-92 vocabulary (`wp4c-retained-command-fixtures.mjs` prints per-file counts); update the 9 owner schemas' `const` narrowings, the Rust lane-string readers (`🪐️space/🦀️.rs:948,965`, `⚙️engine/🪐️space/🦀️.rs:1445`, shooting/remodel/fem editors, unit tests), `🪐️space/📦️packages/🦀️rust/📜️script.ts:135,431`; norm adopts `RetainedCommandCohort` (`allOf` + const narrowings), drops the instance-less `placeholder` execution member | open |
| 94 | wp4c-framework §5.3, §8.4 | W5c os (build wave) + W8e stdio (build wave) | row 47a coupling: 33 plugin enums + 17 `💻️os` enums need `rename_all_fields = "camelCase"` (list `🗑️generated/wp4c-rename-all-fields-repo.txt`, regenerate with `wp4c-rename-all-fields-scan.py`) | open |
| 95 | wp4c-framework §5.4 | W5c os | one boot-time call site (os plugin host init) for `semio_framework::interaction::schema::register_scope_exports()` and `semio_framework_ui_contract::schema_metadata::register_scope_exports()`; exact duplicates tolerated, conflicts fatal | open |
| 96 | wp4c-framework §8.5 | W10e framework | annotate `ConformanceCatalogFixture`/`ContractFixture` with `x-semio-formats: ["🔣️jsonschema","🦀️rust"]` instead of declaring empty TS/GraphQL/proto leaves | open |
| 97 | wp1c R-1, R-2 | W2u tooling | catalog `facet` labels: always ASCII (`diff/text`), never emoji chains (11 rows); diagnostic-code vocabulary: the harness `schema-*` set is canonical — tooling renames its codes to match (`export-format-missing`→`schema-export-incomplete`, `ref-not-export-addressed`→`schema-ref-unresolved`, `mutation-leaf-id-grammar`→`schema-mutation-leaf-id`, …) and both read one shared code table | harness side done(W1d); tooling adoption W2u/W2w |
| 98 | wp1c Q1-Q4 | contract → W1d harness | decisions: `x-semio-formats` must list the normative format too (keep `schema-export-format-undeclared`); broken internal pointers get their own code `schema-ref-broken-internal`; GraphQL exports may be any of `type|input|enum|interface|union|scalar`; TS completeness requires both the exported type and `parse<Export>()` (activate now; owners fix in wave 3) | done(W1d) |
| 99 | wp1c §5.3 | wave 3, per partition | live-tree findings 3,493: `schema-export-incomplete` 2,841, dialect 173, fixture-defines-schema 157, ref-unresolved 128, owner-ineligible 69, placement-outside-module 55, file-missing 31, export-unknown 21, forbidden-filename 18 — per-owner rows in `🗑️generated/wp1c-test-schema.json`; each partition clears its rows (annotate `x-semio-formats` honestly or add the missing format entities) | open |
| 100 | coordinator (wave 3) | W4d hub, W10f framework (excl. ui), W1d harness — dispatched; W6d plugins (1,977), W8e stdio (691), W5c os (400), W7d mutations (135), W2u library (104), W9e repo/mit-bestand (15) — queued behind running workers | clear each partition's `wp1c-test-schema.json` rows; register Rust exports via `semio-framework-schema-registry`; annotate `x-semio-formats` honestly; drop `x-semio-mutationKinds` (row 90) | dispatched(partial) |
| 101 | wp4c-repo-product §7 (97, 99) | W3e framework schema (after W3d) | `🧬️schema/📦️packages/🦀️rust/📜️script.ts:23` `readEntityKinds()` must read `💻️client/🧩️vscode/🗂️technologies.json` (the framework `🔣️.json` array is gone; `check`/`generate`/`preview-generated` currently throw `kinds.map is not a function`); `🤖️generated.rs` `entity_kind_by_emoji` + emitted TS index must be first-wins per the vscode schema | done(W3d) |
| 102 | wp4c-repo-product §7 (98, 100) | W2u tooling (taxonomy) | `schema-entity-catalog.inputPatterns` (`:24649`) → `💻️client/🧩️vscode/🗂️technologies.json`; drop dead `🔗️graphql` memberName in `members-of-members-of-members-of-modules` (`:8560`) after the mcp GraphQL move | superseded by row 122 |
| 103 | wp4c-repo-product §7 | W2u tooling (`📚️library/📦️packages/🟦️typescript/🧫️fixtures/**`) | `⚖️readme-license-owner-authority/🔣️.json:1021` still carries source/destination rows for the deleted `🔌️mcp/🔗️graphql/README.md`; `🧼️remaining-package-purity-authority/🔣️.json:5007` carries a row for the vanished `🧩️vscode/📦️packages/🟦️typescript/🧪️extension.test.ts` (its own target field spells `🧪️extension` where the tree has `🧩️extension`). New authored files: `🧩️vscode/🧬️schema/🟦️.ts`, `🔌️mcp/📦️packages/🟦️typescript/{📜️script.ts,🧪️tests/🟦️.ts}` | open |
| 104 | wp4c-repo-product §7 | W2u tooling (root `.vscode/launch.json` + root `package.json`) | new nx project `@semio-tech/repo-mcp-schema`: launch entry `🔌️mcp schema test` (`bun nx run @semio-tech/repo-mcp-schema:test`, group `3_dev`) beside row 71's still-open `🪶️sqlite schema test`, and a `workspaces` entry for `…/💻️client/🔌️mcp/📦️packages/🟦️typescript` | open |
| 105 | wp4c-repo-product §7 | W2u catalog owner | `📚️library/🔣️schema-catalog.json` gains `repo.client.vscode` (2 exports, jsonschema+typescript) and `repo.client.mcp` (62 exports, jsonschema+graphql), both `dependsOn: []`; full export lists in `📓️wp4c-repo-product.md` §7 | open |
| 106 | wp4c-repo-product §7 | coordinator / taxonomy-sweep owner | every `*_test.go` under `🦑️repo` is now `🧪️tests/<case>/🐹️.go` in an emoji directory, so a bare `go test|vet ./...` fails at package load with `malformed import path …: invalid char '🧪'`. Only `runCanonicalGoTests`'s overlay works — document it, or CI/workers read it as catastrophic breakage | open |
| 107 | wp4c-stdio §6.1 | W6c/W6d plugins (stdio `🚪️io`) | decision: a `🚪️io/🧬️mutations/📝️text/🔣️.json` is not a schema scope (io trees hold codec grammars in their native formats); delete the orphaned gltf document with its dangling `https://semio.tech/schema/s.stdio.gltf/mutation.json` refs and stray `$id` | decided |
| 108 | wp4c-stdio §6.2 | W6c/W6d plugins (stdio svg `📸️snapshot/🦀️.rs:1394-1419`) | the "no json" substring assert over 20 `include_str!` facets is broken by contract-mandated `$schema`/`$id` in `🧬️mutations/📝️text/🔣️.json`; drop `🔣️.json` from the include lists or assert on `description`/`type` | open |
| 109 | wp4c-stdio §6.4 | W2u tooling | `dependency-undeclared` fires on 1 of 88 equivalent aggregates and hashes 38 deleted facet documents — stale catalog/index; regenerate + fix the anomaly | open |
| 110 | wp4c-stdio §6.5 | W8e stdio (build wave, JSON step) | `🔗️.graphql`/`🛰️.proto` twins of the 38 deleted codec facet documents are scaffolding; delete them with the same classification, keep twins of the 27 kept grammars only if they are real | open |
| 111 | wp2c-library §9.1-2 | contract → wave 3 (plugins 6,278+649, os 240, hub 63, framework 2) | decision: a scope never restates another scope's `$defs`; surface lanes (`🎚️config`/`👥️presence`/`🫧️transient`) and facet documents (`🔺️diff`, `📸️snapshot`) `$ref` the owning export (`<owner $id>#/$defs/<Export>`); Rust re-exports are `pub use` (private `use` is not a declaration). Clears `export-format-missing` + `export-id-duplicate` together | decided |
| 112 | wp2c-library §6 R-C | W6d plugins (78) + W5c os (2) | surface-lane `$id`s: `…/app/<p>/<a>/config/schema.json`, `…/presence/schema.json`, `…/transient/schema.json` (scope ids `app.<p>.<a>.config|presence|transient`); clears `scope-id-duplicate` 80 and `dependency-undeclared` 21 | open |
| 113 | wp2c-library R-A | W2v root script | `policyRepositoryOwnedRoots()` (`📜️script.ts:23511`) must filter areas to `state === "clean"` so the newly declared exempt tickets area is not walked; re-verify row 59 literal removal | done(W2v) |
| 114 | wp2c-library R-D | W1e harness | `isFixtureOwnedPath` reads the carve-out segment from `schemaScopeOwnerLevels.moduleMemberDirName` | open |
| 115 | wp2c-library R-F + §6 own rows | W2u library | row 20: the three synthetic `"🧬️.schema.json"` literals use the taxonomy location; the library's 67 own rows: rename ~25 `🧪️tests/<case>/🧬️schema/` → `🛂️schema/` and ~14 `🧬️schema.json` → `🛂️schema.json` (taxonomy `test-fixture-schema-authority`), rewire `🧪️tests/🔬️index/🟦️.ts` + `⚡️caching/🧪️tests/📜️script.ts` (peers editing concurrently — proceed); rows 97 (code table adoption once the harness exports it), 102, 109; delete the facet-id fallback once `document-id-missing` is 0 | open |
| 116 | wp2c-library R-E | W6d/W5c/W7d | `fixture-defines-schema` plugins 2, os 16, mutations 43 → owner module or `inertSchemaData` | open |
| 117 | wp3c R-3, R-4 | W2w root script (after W2v) | `schema test` `oracleArguments()` (`📜️script.ts:17452-17456`) writes a throwaway vitest config to `tmpdir()`; use the tracked module config in `🧰️framework/🔨️modules/🧬️schema`; register the new crate's targets (`📇️registry schema test` etc.) in `📋️project.json`/launch.json following the `🧬️schema` grouping | open |
| 118 | wp3c R-6 | W2u library (running) / W2w | `schema verify --rust-entries` reference dump committed at `🧬️schema/🧫️fixtures/📤️schema-export-entries-dump.json`; wire it as the default input and add `--rust-entries-complete` reporting `rust-scope-unregistered` per catalogued scope | open |
| 119 | wp3c R-7 | W10e/ui owner | run `cargo check -p semio-framework-ui-contract` + its `cargo tree` invariant against `semio-framework-schema-registry` | open |
| 120 | wp3c R-8 | W5b os | `💻️os/🖥️host/🧪️tests/🕸️media-projection/🧬️schema/🔣️.json` uses `prefixItems` (2020-12); migrate to draft-07 or move the test-local schema per the fixture rule | open |
| 121 | wp3c §8 | contract | `📇️registry` (a crate root with no `🧬️schema/`) is a packaging concern, not a scope; the generator must not demand a scope for every crate root — only directories carrying a `🧬️schema/` module are scopes | decided |
| 122 | wp3c R-1 (row 68 premise wrong) | contract → W3e framework schema + W9f repo client + W2w library | decision: the entity-kind catalog is owned by `framework.schema` (`🧬️schema/🔣️entity-kinds.json` data + `$defs.EntityKindCatalog`/`EntityKind` exports; the entity-catalog generator emits `🟦️entity-kinds.ts`, `🤖️generated.rs`, `🐹️entity_kinds.g.go` with provenance, first-wins emoji index). The vscode client consumes the generated `🟦️entity-kinds.ts`; `💻️client/🧩️vscode/🗂️technologies.json` and the `TechnologyCatalog*` exports are deleted (row 68 client side reverted). Taxonomy `schema-entity-catalog.inputPatterns` → `🧰️framework/🔨️modules/🧬️schema/🔣️entity-kinds.json` (corrects row 102) | decided |
| 123 | wp1d §5.4 | wave 3 owners | live tree now 5,166 findings: `schema-export-incomplete` 2,891, `schema-export-parser-missing` 1,696 (new TS `parse<Export>()` rule), dialect 162, fixture-defines-schema 156, ref-unresolved 115, owner-ineligible 69, placement 51+18, broken-internal 5, file-missing 3 (`🗑️generated/wp1d-test-schema.json`) | open |
| 124 | wp1d (unchanged red) | W2w library | the generator-vector case `fixture-owned-schema` still expects silence (row 60 not actually applied on disk?) — verify and remove; the harness invariants suite must go 102/102 | open |
| 125 | wp1d §7 | contract / W2w library | decisions: `parse<Export>` may be a `function`, `async function`, or an exported arrow constant (permissive, no TS parsing); the shared code table declares every code either side emits, each tagged with its emitter (`harness` / `check`), so `schema-mutation-leaf-id` lives in the table as a `check`-emitted code; generator must catalogue aggregates' `dependsOn` from leaf scopes when leaves are scopes | decided |
| 126 | wp4b-plugins §8 A, F | W4e hub (after W4d) | `📜️script.ts:9134,9166` `📇️registry/🧬️schema/📜️native-codec-factories.json` → `📇️registry/📜️native-codec-factories.json`; `:9170` gis schema path → `s.gis` module `$ref`; two hub fixtures drop the `🧬️schema/` segment; `✏️s/…/🧪️fixtures` → `🧫️fixtures` in hub script paths | open |
| 127 | wp4b-plugins §8 C, D, E | W2w library | taxonomy: `📜️interface.facetPathIdentities` += sequence editor `🌉️wasm/🧬️schema`; drop deleted playbook memberNames; regenerate catalog (507 rewritten ids, surface scope split, 12 plugin-root scopes); hoist `compileScopeExport` into the repo library TS package (row 54) registering `x-semio-*` keywords + numeric formats | open |
| 128 | wp4b-plugins §8 F | W5c os + W2w root | `🧪️fixtures` → `🧫️fixtures` in `✏️s` paths spelled by `🌊️flow/…/🔬️slider-label/🦀️.rs`, `📺️renderer/🧑‍🎨engine/🧪️tests/🔬️engine-contract/🟦️.ts`, root `📜️script.ts` | open |
| 129 | wp2d §4.1 | W2w library | rename the 15 library-emitted codes to the harness vocabulary per the mapping table in `📓️wp2d-root-script.md` §4.1; the five without counterparts become new table codes tagged `check`-emitted (row 125): `schema-document-id-duplicate`, `schema-document-id-unaddressable`, `schema-export-id-duplicate`, `schema-export-id-invalid`, `schema-mutation-leaf-id`; acceptance = root `schema check` `unshared-codes=0` | open |
| 130 | wp2d §4.2-4.3 | W1e harness | add `schema-catalog-stale` + the five `check`-emitted codes (row 129) to `SCHEMA_DIAGNOSTIC_CODE_TABLE` with emitter tags; `🧩️ open/closed › every oracle … arrives as an owner contribution` exceeds its own 30 s budget on the live walk — fix the walk (cache discovery once per test file) rather than raising the budget; row 114 (`moduleMemberDirName` from taxonomy) | open |
