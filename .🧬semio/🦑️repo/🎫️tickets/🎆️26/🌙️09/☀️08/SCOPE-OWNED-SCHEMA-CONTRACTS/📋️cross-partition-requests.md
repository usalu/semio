# Cross-partition request ledger

Coordinator routing of requests raised in worker reports. Status: `open` → `dispatched(<wave>)` → `done`.

| # | From | To (partition) | Request | Status |
|---|------|----------------|---------|--------|
| 1 | wp2-rust-schema-registry | every scope with named exports (plugins, hub, os, framework) | call `register_scope_schema_exports(ScopeSchemaExports{scope, exports})` beside `register_artifact_schema_descriptor`; scope id = module `$id` path form; export ids must not reuse artifact/snapshot/diff/mutations | open |
| 2 | wp2-rust-schema-registry | W2 tooling (root script) | `schema verify` cross-checks `🔣️schema-catalog.json` against `schema_export_catalog_entries()`; ascii format ids, map via `SCHEMA_FORMAT_TAXONOMY_KEYS` | dispatched(W2b) |
| 3 | wp2-rust-schema-registry | W2 tooling (root script/project/launch) | nx target for `🧰️framework/🔨️modules/🧬️schema/✅️draft07-oracle.test.ts`, part of `schema test` | dispatched(W2b) |
| 4 | wp2-rust-schema-registry | W2 tooling (root package.json) | declare `ajv` explicitly in devDependencies | dispatched(W2b) |
| 5 | wp2-rust-schema-registry | W5 os (🌉️mcp) | decide 2020-12 wire label vs draft-07 validator once tree is draft-07 | open |
| 6 | wp4-plugins-conventions | W10 framework modules (🖱️ui) | export `RetainedCommandLimits`, `RetainedCommandRoute`, `RetainedCommandRoutes` at `https://semio.tech/schema/framework/ui/schema.json#/$defs/…` | dispatched(W10 brief) |
| 7 | wp1-harness-invariants | W7 mutations (🏛️architect) | 61 leaves under `🏛️architect/🗿️artifacts/🏛️program/…` carry no `🔣️.json` descriptor, so nothing declares their payload contract | dispatched(W7b) |
| 8 | wp1-harness-invariants | coordinator | harness report shows no vitest run of the harness suite; coordinator runs `bunx vitest run` in `🧪️test/📦️packages/🟦️typescript` (scratchpad `wp1-harness-vitest.txt`) | in progress |
| 9 | wp4-mutations | W2 tooling (root script) | `policyMutationStructuralBreachesView` aggregate identity check now relies on `x-semio-mutationKinds` in the `$ref` union; keep or replace with structural check | open |
| 10 | coordinator (from wp2 check: scope-id-duplicate=2249) | W7 + W8 mutation workers | rewrite every leaf `$id` to `…/<root scope path>/mutation/<semanticKind>/schema.json` per contract §A `$id` grammar; aggregates keep their `$ref`s (paths unchanged) | dispatched(W7b) |
| 11 | coordinator | W2 tooling | generator must treat `🧬️mutations/<leaf>/🧬️schema/🔣️.json` as its own scope (`<root>.mutation.<kind>`), not a facet of the root; facet documents never deepen scope; add Rust-vs-catalog parity code once exports register; tolerate torn catalog reads in `schema test` | dispatched(W2b) |
| 12 | wp2-mechanism-tooling | everyone with `$id`s | `module-scope-id-inconsistent=135`, `document-id-missing=84`, `document-dialect-unexpected=1020` rows in `🗑️generated/schema-check.jsonl` are partition-owned; each wave-2 worker clears its rows | open |
| 13 | wp2-mechanism-tooling | W1 harness | harness fixture cases live at `📚️library/🧪️tests/🧬️schema-scope-catalog/🧫️fixtures/🔣️.json`; reuse rather than re-author | open |
| 14 | wp4-repo-product | W2 tooling (taxonomy) | `schemaScopeOwnerLevels` must declare `♻️mit-bestand/<product>/🔨️modules/<m>`; `🗑️generated/schema-check.jsonl` reports `module-level-ineligible` for the two new modules `♻️mit-bestand/📋️bericht/🔨️modules/📄️documents/🧬️schema` and `♻️mit-bestand/🧺️demonstrator/🔨️modules/🧩️runtime/🧬️schema` | open |
| 15 | wp4-repo-product | W-tooling (`📚️library/⚡️caching/🧪️tests/📜️script.ts`) | `testDemonstratorRuntime()` reads the deleted `🧩️runtime/🧬️schema.json` and `🧬️pipeline.schema.json`; read `🧩️runtime/🧬️schema/🔣️.json` instead (draft-07 → plain `ajv`, not `ajv/dist/2020`), pipeline at `$ref: "#/$defs/DemonstratorPipelineContract"` with a varied `$id` — exact diff in `📓️wp4-repo-product.md` §7.1 | open |
| 16 | wp4-repo-product | W-tooling (`📚️library/📦️packages/🟦️typescript/🧫️fixtures/🧼️remaining-package-purity-authority/🔣️.json`) | drop the rows for the deleted `🎛️coordinator/📦️packages/🟦️typescript/✅️validation.ts` and `🔬️validation.test.ts` (lines ~5102/5110 and ~5196/5204); optionally add `🔬️schema.test.ts` and `🔬️server-persistence.test.ts` | open |
| 17 | wp4-repo-product | W-tooling (`📚️library/🐹️.go:345`) | event emitter posts to `/api/v1/events`; the route is `/api/v1/event` (singular) so it has never reached the coordinator. Body already matches `$defs.EventPublishRequest` | open |
| 18 | wp4-repo-product | W9b repo product (`💻️client/🪶️sqlite`) | two different unreferenced SQLite schemas outside any module: decision = the sqlite client module owns `🪶️sqlite/🧬️schema/` (`🗄️.sql` + `🔣️.json` row exports); keep only the schema the live client actually creates/uses, delete the other | dispatched(W9b) |
| 19 | wp4-repo-product | W9b repo product (`💻️client/⌨️cli`) | exhaustive Go tests time out at 10 min under load; establish baseline once the machine is idle | dispatched(W9b) |
| 20 | wp4-mutations | W2c tooling (`📚️library/🧪️tests/🔏️path-emoji-statutes/🟦️.ts`) | three live assertions hard-code `payloadSchema === "🧬️.schema.json"`; must assert the taxonomy location `🧬️schema/🔣️.json` | open |
| 21 | wp4-mutations | W1b harness (`🧪️test/📜️script.ts payloadSchemaCommand`) | confirm no code path still writes `🔣️.schema.json`; W1 reported `--write` removed — verify by grep | dispatched(W1b) |
| 22 | wp4-mutations | W6 plugins (`✏️editor/*/🦀️.rs`, 22 files) | `payload_schema: "🔣️.schema.json"` literals for owners that name no leaf directory; point at the taxonomy location or drop the field where the owner has no payload schema | open |
| 23 | wp4-mutations | W7b/W8 (after W2b lands structural aggregate check) | drop `x-semio-mutationKinds` from aggregates (ajv `strict:true` rejects unknown keywords) | blocked(W2b) |
| 24 | wp4-hub §7.1-2 | W5b os (`📇️directory`, `🌉️mcp`) | approval request/receipt + undo/checkpoint-frontier types: os copies become conformance-checked mirrors `$ref`-ing `https://semio.tech/schema/hub/inference/schema.json#/$defs/InferenceApprovalRequestV1|InferenceApprovalReceiptV1|…`; inline AJV literal in `💡️inference-bridge.ts` removed | open |
| 25 | wp4-hub §7.5 | W5b os (`🧑‍💻dev/…/📜️script.ts:5269-5277`) | reads deleted `🛰️lag-rebootstrap/🧪️fixtures/🪢️canonical-pair/🧬️.schema.json`; replacement in `📓️wp4-hub-bootstrap-auth-admin.md` §E.1 | open |
| 26 | wp4-hub §7.3-4,6 | W4b hub (after W5/W6/W10 land) | hub `📜️script.ts` readers still point at schemas deleted by stdio/gis/value/os-db/os-directory partitions (list in §7.4); rewire to those scopes' modules; feed catalog `dependsOn` into one shared Ajv so cross-scope `$ref`s replace restatements; replace hand-maintained `HUB_SCHEMA_SCOPES` with a catalog read; re-run `cargo check -p semio-hub --all-targets` once the gis crate split lands | blocked(W5,W6,W10) |
| 27 | wp4-hub §8.2 | contract | fixture directory name: taxonomy `testFixturesDirName` (`🧫️fixtures`) is canonical; `🧪️fixtures` directories are renamed by their partition owners in wave 2 | decided |
| 28 | wp4-hub §8.3 | contract | dev-tool output graphs that are serialized and consumed are scope contracts; `hub.admin` module stays | decided |
| 29 | wp4-hub §8.5 | contract | deviation accepted: hub fixtures routed by real owner (`hub.directory`, `hub.artifact-authority.native-openable-provider`), not all to `hub.inference` | accepted |
| 30 | wp4-hub | W3b framework schema registry | owned validator lacks `if`/`then`/`else` (four hub modules use them); complete draft-07 keyword coverage + pinned `format` policy + runtime entries dump for `schema verify --rust-entries` | dispatched(W3b) |
| 31 | wp4-hub §6.7 | peer session (not this ticket) | `os-hub` binary has 6 `E0308`s from an os-kernel `DirectoryStreamMessage::Event` boxing change; `🚀️bin.rs` staged by a peer; exact fixes recorded in `📓️wp4-hub.md` | external |
| 32 | coordinator (harness `bun test` full run, `🗑️generated/harness-bun-test-full.txt`) | W1b harness (after W1 finishes) | 144 pass / 28 fail across `🧪️index.test.ts` + `🧬️invariants.test.ts`: oracle registry (2), discovery and contract (3), dependency ratchet, clean safety, non-aggregate metrics, case-above-subset, mutation-without-fixture census, oracle purity (2), cross-language oracle hosts, recorded production debt (3), comparison profiles (4), contribution directory ownership (likely W2's 174 dropped `testContributionDirectoryOverrides`), open/closed (root script names the test module), physical mutation vector registry (3), owner eligibility glob vocabulary, parity with the catalog generator's vector, exempt-area discovery. Triage each: caused by this ticket's migrations vs peer churn vs stale expectations; fix in partition, request the rest | dispatched(W1b) |
| 33 | wp4-framework-modules §7 (`wp4-framework-cross-partition.md`) | W5b os | ~50 os consumer path/`$id` rewrites: `⚛️react/🔬️index.test.ts`, `🧯️router-plugin-faults.test.ts`, `📃️UiDocumentStore/🟦️.tsx` (40 paths), `🔌️PluginRuntime/🟦️.tsx`, `🔌️plugin/📤️return`, `📥️poll/🏘️composition`, `🔌️plugin/📦️packages/🟦️typescript`, `🛠️ShellHelpers`, `⚛️reactor/📨️pending/🧬️authority.schema.json`, `🧑‍🎨engine/💾️resident/🧬️schema.json`, tutorial-interaction fixture, dev distribution manifest regenerate | open |
| 34 | wp4-framework-modules §7 | W2c tooling (root script + taxonomy + 📚️library tests) | root `📜️script.ts` `🧵️job` fixture-schema paths + `⏱️trace` contention path (`…/⏱️clock/🧬️contention/🧬️schema/🔣️.json`, export `Contention`); taxonomy literal paths (`🕸️graph/🛂️manifest/🧬️outputs.schema.json`, `🖼️assets/🔤️fonts/🧬️catalog.schema.json`); `🧪️tests/🤝️package-language-kind-handoff/💾️resident-package` value-resident paths; job-budget consolidation readers (§8.3) | open |
| 35 | wp4-framework-modules §8.3 | W10b framework | consolidate `🧵️job/⏱️budget/🧬️schema/{⏱️clock,🪢️binding,🪫️budget}.json` into `🔣️.json` `$defs` (W2c rewires the root script readers) | dispatched(W10b) |
| 36 | wp4-framework-modules O-1 | W10b framework + W5b os | decision: rename `🎭️actor/🎠️activation` → `🎠️activation-reservation` so `framework.actor.activation-reservation` is derivable; drop the `SCOPE_OVERRIDE`; fix `#[path]` (W10b) and the three os `📜️script.ts` joins (W5b) | dispatched(W10b) |
| 37 | wp4-framework-modules O-2 | W10b framework | decision: hoist `framework.ui.render.targets.metal.packages.rust` and `…targets.webgpu` modules to `framework.ui.render/🧬️schema/` as named exports (`MetalAbi…`, `Webgpu…`); rewire the metal/webgpu Rust packages' `include_str!` | dispatched(W10b) |
| 38 | wp4-framework-modules O-3 | W10b framework | hand-author `🖱️ui/🖥️host` declaration contract with real bounds, replacing the generated structural schema | dispatched(W10b) |
| 39 | wp4-framework-modules O-4 | W6b plugins | normalize retained-command lane/disposition/execution casing in plugin data to one vocabulary (`artifact|config|host-only`, `migrated`); tighten `framework.ui` shape afterwards (W10b) | open |
| 40 | wp4-framework-modules O-5 | W10b framework | register the 117 modules / 211 exports via `register_scope_schema_exports` in each module's `🦀️.rs` (row 1 pattern) | dispatched(W10b) |
| 41 | wp4-framework-modules §8.2 | contract | accepted: `🖱️ui/🧬️contract/` (singular) is the `ui-contract` crate root and an eligible nested module scope; only `🧬️contracts/<x>` per-contract directories are forbidden | accepted |
| 42 | wp1 §6.1 | W2c tooling (taxonomy `fixtureOwnerReason`) | decision: a schema document inside a `🧪️*`/`🧫️*` tree IS a finding (`schema-fixture-defines-schema`) unless the enclosing case declares `inertSchemaData` explicitly (parser test inputs); align the taxonomy text and `schema check` with the harness | decided |
| 43 | wp1 §6.3 | W2c tooling (catalog shape) | catalog must name the file carrying each export: `exports: { "<ExportId>": { "file": "<module-relative path>", "facet": "<facet id>" } }` (1,006 exports live in `🔺️diff/📸️snapshot/💡️inferences/🧬️mutations` children); the 351 exports referenced but declared nowhere are listed per partition in `🗑️generated/` by the harness report — each partition owner declares or removes the reference | open |
| 44 | wp1 §6.2 | W2c tooling (`📚️library/🧪️tests/🗺️testing-readme-coordinates/🟦️.ts:127`) + W1b | test-protocol `$id` migration: W1b changes the `$id`, W2c updates line 127 in the same wave | dispatched(W1b) |
| 45 | wp4-stdio-mutations §9 | W8b stdio | decision: contract §A grammar applies to stdio too — leaf `$id` `https://semio.tech/schema/s/stdio/<artifact>/<standard>/<subset>/mutation/<semanticKind>/schema.json` (slash scope path, keyed by `semanticKind`, not directory path), aggregate `$id` `…/<subset>/mutations.json`; rewrite all 1011 ids via `ID_ROOT`/`leaves()` + `audit --write` + `aggregates --write` | dispatched(W8b) |
| 46 | wp4-stdio-mutations §8.1 | W8c stdio (separate wave, needs a stdio build; machine must be idle) | casing conformance: taxonomy declares camelCase for JSON Schema; 440 stdio payload structs + nested snapshot types lack `#[value(rename_all = "camelCase")]`; add it, re-case committed fixtures, regenerate schemas with `audit --write`; verify with a stdio build | open |
| 47 | wp4-stdio-mutations §8.3-4 | W10c framework (`🌱️value/✨️derive/🦀️.rs`) | (a) `field_rename_all()` falls back from `rename_all_fields` to `rename_all` — serde does not; remove the fallback and re-derive the four stdio fixtures' expectations with W8; (b) internally tagged newtype asymmetry: encode emits `{tag, value: scalar}`, decode hands the object to a scalar `FromValue` — fix decode to unwrap `value` (affects `JsonPathSegment`, `XlsxCellValue`) | open |
| 48 | wp4-stdio-mutations §9 | W2c tooling (multi-format parity policies `📜️script.ts:30343,30447,30883`) | with `title` = Rust type and real wire field names in JSON Schema, the `🟦️.ts`/`🔗️.graphql`/`🛰️.proto` leaf surfaces may now be the drifted side; run the parity policies on stdio and report | open |
| 49 | wp4-stdio-mutations §7 | W8c stdio (same build wave as row 46) | decision: schema-first — the committed fixtures (`{"mutation": "setSnapshot", …}`) are the intended wire; add `#[value(tag = "mutation")]` to `BcfMutation`, the mp3 and csv mutation enums; for the four `rename_all` fallback cases the fixtures written against serde semantics are correct and the derive is fixed (row 47a); re-run the 188 fixture replays after the build | open |
