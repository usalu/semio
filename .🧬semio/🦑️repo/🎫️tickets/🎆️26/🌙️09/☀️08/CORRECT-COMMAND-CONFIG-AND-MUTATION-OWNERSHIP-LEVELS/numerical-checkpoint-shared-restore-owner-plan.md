# Shared Numerical Checkpoint Restore Owner Plan

## Current Acceptance — 2026-09-13

The lower package of canonical combined native green2 passed **35/35**, 940 filtered, 118.34s Rust, normal 2 MiB stack. All fifteen numerical/checkpoint/resume/replay laws are included and green. This freshly accepts the shared sequential LDLT and Subspace close owners, sticky public restore faults, delegated modal stiffness cleanup, and the twelve-value solver-only work record. Fields 522/534 have identical bytes for local retirement positions 0/11; the thirteen-value hostile control rejects with Truncated and its retained 32-byte backing closes.

Actual conservation: LDLT close 144/144 bytes; Subspace close 120/120; modal complete owner 64/64. Zero and subexact grants retain backing; exact grants retire one owner. All three matrix shapes, five scalar-page cases, twenty entry-width boundaries and nine hostile pages pass. The maximum admitted LDLT remains 40 rows/820 values; private larger scalar fixtures do not raise that limit.

The combined upper FEM3D package also passed **20/20**, 829 filtered, 1.52s Rust. The entire registered run exited zero in 12m55s, zero cache hits. The full numerical child reaches ready static/modal output and exact logical terminal cleanup. Log: `🗑️generated/fem3d-canonical-physical-green-2.log`. The same run passes both stories, all embedded oracles and strict TypeScript. This does not accept the separate mounted-model physical backing and assembly accounting gaps.

Remaining numerical work includes canonical control conversions/ranges/reserved-header admission and cold PCG checkpoint/output serialization. Mounted model paging, real assembly allocation accounting and parent diagnostics are separate open owners.

## Earlier Evidence and Development Record

Mixed3 completed with12passed/3failed,960filtered,219.74s Rust,13m18s Nx, normal2MiB. Its three failures are exactly the Subspace grant, modal backing and physical work-control regressions recorded below; all four checkpoint/resume/replay filters pass. The three production repairs were applied after their runtime RED and are not present in that compiled binary. The canonical combined native run is next.

Mixed3 actually starts all fifteen native laws on2MiB. Its recorded observations confirm the applied LDLT close repair: zero/subexact grants retain both inner backings, exact grant retires one, all three cases conserve144bytes. Both parents now preserve Admission and Stale across repeated steps with unchanged coordinates and terminal cleanup. The three pre-fix regressions fail as designed: Subspace zero/subexact grants discard120bytes while reporting0; modal completed-state cleanup discards all64bytes under every grant while reporting0; both work fields522/534 persist13controls and change bytes with close_lane, and a supplied lane11 returns terminal while32bytes remain. Fixtures recover and release their retained test backing before assertions.

After these actual RED observations, all three repairs are applied. Subspace job and restore now share checkpoint-level sequential close. Modal cleanup always delegates to the stiffness owner's terminal result before mass. Both work-control writer forms share one twelve-value solver record; both readers enforce that exact count, reject thirteen values, and initialize local retirement at zero. Rustfmt parse and scoped diff checks pass. The still-running mixed3 binary uses pre-fix compiled source; a fresh canonical combined native route is required for acceptance.

At 01:10 UTC the fresh mixed3 native route was started after the Pack caller run was confirmed interrupted with no final verdict. Mixed3 runs the latest expanded oracle/strict TS and all numerical_page plus existing LDLT/subspace checkpoint/replay filters. Expected native count is fifteen: LDLT cleanup and first-fault fixes are applied; Subspace grant, modal backing and local work-control cursor regressions remain deliberately pre-fix. Root has only this single Cargo validation active. Native counts and outcomes must come from its eventual log.

Mixed2 native was deliberately cancelled before tests at 01:04 UTC to consolidate overlapping root FEM package builds. No new native assertions executed. The earlier broader FEM3D contract run remains active; a fresh numerical native attempt must follow it. Expanded-neutral2 is green; all applied repairs and new native regressions remain pending. See process-budget-source-observation.md for the verified owned PID and lock-wait sample.

Fresh expanded-neutral2 passed in 8.1s through registered Nx, zero cache hits, including strict TypeScript. It executes all five scalar cases, twenty width boundaries, nine hostile pages, three matrix shapes, three cleanup grants, two sticky first-fault cases and the two local cleanup positions projected to the same persisted solver record. This is independent expectation validation; Subspace/modal/control native regressions remain unaccepted until the pending native route returns.

Source review found a further level mismatch: both active and retiring `SubspaceWork` control records persist `close_lane`, a local physical-retirement cursor. Restoring a supplied lane can skip backing owners, including newly allocated restore backing that did not exist at the saved physical layout. The numerical record must carry only resumable solver work; each restored physical owner starts its own retirement cursor at zero. The control record should have twelve entries rather than thirteen, with both writer and reader changed together and no legacy branch. Required regression: both serialized work-control forms omit the physical cursor, an old thirteen-entry form rejects before publication, and a restored work owner with actual backing retires every allocation from its fresh cursor. This correction is planned, not yet applied.

The work-control regression is now authored and rustfmt-parse checked. It compares actual checkpoint pages for both fields 522/534 with local cleanup lanes 0/11, requires twelve entries and identical bytes, then feeds a thirteen-entry page with lane 11 into a work owner with real retained RHS backing. Observations are captured before explicitly recovering the test fixture's cleanup lane and draining all backing; assertions occur only after cleanup. The independent JSON Patch oracle removes the physical field from the persisted record. This source was added while mixed2 was waiting; a fresh post-fix route must run the expanded oracle even if the in-flight Cargo build includes the native test.

Another pre-fix regression now covers the modal completed-result owner. Its current parent checks only stiffness vector lengths before discarding the completed pair; empty vectors may still own backing. The test uses the same neutral grant fixture and actual capacities of all three stiffness arrays. The expected fix delegates every opportunity to the stiffness owner's existing close method and proceeds to mass only after its terminal result. It must not infer physical emptiness from logical length. Added while mixed2 is compiling; its actual test count determines inclusion. No modal close repair is applied yet.

The fresh registered restore-close-mixed2 route includes both applied LDLT/first-fault repairs plus a new pre-fix Subspace restore regression using the same three neutral grant cases. Its native Subspace fixture owns real empty-but-capacitated factor columns and diagonal backing; it measures actual released bytes and exact state retention before final close. Planned repair is one checkpoint-level sequential close function shared by the ordinary Subspace job and its restore candidate. Grant rejection must return pending immediately. A parent may remove the checkpoint only after all matrix, vector, active-work and retiring-work owners report terminal. No separate restore-only disposal algorithm should remain.

The neutral fixture now also declares Admission→Truncated and Stale→Truncated first-fault cases; JSON Patch independently preserves the first fault and unchanged input/allocation coordinates. Both native parents consume these same cases. Runtime acceptance is pending the current mixed route.

2026-09-13 restore-close-red1 actually executed 12 tests: 10 passed, 2 failed, 960 filtered, 84.88s Rust, 13m04 Nx, normal 2MiB. All three matrix shape cases passed with exact dimensions, partial-capacity restoration and actual allocation/release conservation. All four LDLT/subspace resume/replay laws passed. The nested-close law failed: zero/subexact grants physically discarded both inner owners and reported zero bytes; an exact grant discarded both but reported only the first. The first-fault law failed for both parents: Admission was overwritten by Truncated. These are actual pre-fix runtime failures. Shared sequential LDLT checkpoint cleanup and public restore first-fault boundaries are now applied; the next run must verify them. Subspace restore grant rejection remains a separate source finding requiring its own regression and repair.

Current executed green1:5/5 native laws,964filtered,.03s execution,9m16s Nx, normal2MiB. Both existing writer tests remain green; the admitted40-row/820-value LDLT checkpoint restores and closes; all five real paged-f64 cases restore, including2044/2048/4092 values. Actual retained/released backing is18304/18304 for the first four nonempty shorter cases and36224/36224 for4092; empty0/0. All nine hostile cases reject with expected faults, unchanged target/cursor and exact closure (continuation cases18304/18304). The three new matrix-shape cases and three actual nested-close grant cases were added after this compilation and are not included. Registered restore-close-red1 now selects those plus the four existing LDLT/subspace resume/replay filters. No nested-close production fix yet.

Executed restore-red2:4 tests,3 pass/1fail,964 filtered,.03s native,6m37s Nx. The full admitted40-row LDLT checkpoint restores and closes. Empty and exact-page paged-f64 reads pass;2044,2048,4092-value reads fail with Truncated before allocation. This is the shared reader RED. A new private NumericalOwnerRestoreCursor and scalar/owner interfaces are now applied to both LDLT/subspace parents and all scalar/pair/matrix readers. The cursor retains shape and page coordinates, validates canonical byte count before admission, reserves one backing or appends one value per call, and prevents advancing the field at an intermediate physical page. u64-to-usize and read-offset arithmetic are checked. Initial green1 is running; hostile continuation and final close/fault hardening remain required before accepting the slice.

The accepted writer preflight uses one canonical page header and retains field/owner/item across physical page boundaries. Native restore-red2 tests the real PagedList<f64> reader against the same five independently reconstructed JSON fixtures and separately verifies the admitted 40-row LDLT matrix. A 64-row LDLT was invalid and is not accepted failure evidence.

Implement one allocation-free retained numerical-owner cursor beside each parent restore cursor. It retains the declared length and optional matrix dimensions, exact next item, current page start and page-admission state. A private typed scalar codec covers u32, usize, f64 and (u32,f64); a private owner interface covers Vec and PagedList without changing their public API. Matrix restoration uses the same item/page cursor, retaining its shape and allocation target.

Validate all page coordinates, integer conversions, declared maxima, byte alignment and canonical page length before any reservation or append. First page carries length/shape; continuation carries neither. A single restore opportunity reserves one backing or appends one scalar, then returns. Page completion and owner completion are separate witnesses: the parent closes the consumed physical input page on the following opportunity, keeps expected_field while the owner continues, and advances the field only after all declared entries exist. Empty owners complete explicitly.

Vec admission validates the requested capacity before allocating, then records actual capacity; PagedList reserves one physical page per opportunity. Allocation failures leave the candidate retained for bounded close. Parent faults must be sticky, zero-fuel/expired/cancelled contexts must preserve all owner cursors, and hostile pages must not cause unbounded Drop or partial publication.

Required verification: five scalar page cases, all scalar/pair/matrix widths, duplicate/skipped/foreign continuation coordinates, length narrowing and offset overflow, truncation, missing final page, zero/cancel during reserve and append, exact output reconstruction, physical retained/released conservation, admitted maximum LDLT and subspace checkpoint resumes. Existing 40-row production limits remain unchanged.

Fresh neutral1 is green in16.5s: strict TypeScript, five scalar page cases, twenty writer-width cases and nine hostile restore-coordinate/length cases rejected by an independent Ajv schema. A later three-case close fixture and native actual-capacity test are now added but not yet executed. Source review found the existing LdltRestoreCursor ignores failed backing grants and drops its state, and both LDLT close paths eagerly evaluate two nested releases before reporting the first. The regression measures real before/after capacities, ungranted preservation and one physical owner per operation; no close production repair is applied yet. The next native route also selects the existing LDLT/subspace resume and cancellation/replay laws; green1 was dispatched before that filter expansion and its actual count will determine which new source tests were included.
