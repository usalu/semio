# Stdio Native JSON Number Oracle

The first registered combined Object/Kit native run compiles and stops at the Object round-trip assertion: transform numbers serialize as1.0/0.0 while serde_json retains the fixture tokens as integer1/0. The actual numeric values agree.

The assertion still independently parses emitted JSON with serde_json, then compares both JSON trees in the repository's schema numeric value domain. This preserves every field, array entry and string while treating equivalent JSON numeric spellings equally. No fixture expectation, domain codec, child identity check or mutation behavior changed. The registered combined Stdio native route is rerunning so Object and Kit can reach their runtime assertions.
