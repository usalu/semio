# Ownership Completion Checklist

The goal and ticket remain active. Passing parent schema checks does not close runtime window ownership or full composed-document persistence.

## Current Checkpoint — 2026-09-13

Latest execution update at 03:53 UTC: the PCG RHS norm correction has actual native RED (15/16) followed by GREEN (16/16) on normal 2 MiB, including eight RHS cases through both production constructors, actual solutions, dense CSR continuation, empty restore, 52 malformed inputs, 173 publication cuts and 125 restore cuts. The composed FEM3D post-norm route is running; earlier composed acceptance predates this numerical correction. Generation2d’s canonical exact-camera route remains accepted 3/3. The exact WindowConfig loader has reached compile coherence through the registered plugin check, but its current hydration scaffold retains whole-clone/decode/replay defects and is explicitly not accepted. The UI retained-hit feature boundary is corrected, with engine native laws 2/2 and default-tier plugin compilation passing. Pack's lower inflater slice remains accepted; shared Flow/ordered/neural physical retirement is at a focused milestone with outstanding nested Arc/BTreeMap/frontier/caller accounting before typed snapshot/mutation and SPR/history ownership. Home and Space now receive live host session identity through ViewModel; registered .236 r8 browser/schema checks pass and Terra is auditing the source milestone. Native identity/Space presence and full Home 90 remain required. Ten consumer native checks remain queued. Existing generic ResidentLedgerRoot and the older UI resident ledger have been reviewed as distinct candidates; no process-wide admission authority has been claimed or newly introduced. These facts supersede older running/held labels below.

The goal and ticket remain active. This checkpoint supersedes all historical run labels below. Passing the ownership source gate does not prove runtime isolation, complete recursive persistence, or every generic schema's module placement.

| Slice | Current executed evidence | Remaining work |
| --- | --- | --- |
| Broad ownership gate | Refresh4b passes in5.3s after Architect and Generation2d window source changes:11 ownership vectors,4 nested-schema vectors,3 command-source vectors and104 document contracts across five formats; live scan reports zero misplaced declarations. The original Jack locale-named source is absent. Refresh4 first stopped on transient taxonomy ordering, already fixed by concurrent work without root edits. | Broader module/type/fixture-schema ownership audit and runtime coverage. |
| Shared paged list | Native3: 5 generic + 8 UI laws pass on 2 MiB, including allocator actual signed-total rejection and exact retained release; independent Ajv/JSONPatch and strict TypeScript pass. | Audit any newly added consumers when they change. |
| Retained Pack source, catalog, value and inflater | Source/catalog coherence remains green. Value native5 passes target-aware PagedList 1/1, symbol table 2/2, catalog 3/3, codec 1 1/1, value 3/3, body 3/3 and both Generation snapshot/mutation callers. The inflater caller route passes deflate 3/3, Pack physical 2/2, compact diagnostic 1/1, cold codec 1 1/1 and Generation2d/Generation3d mounted 2/2; it observes exact 32,768-byte ring allocation/release, distance 32,768 across wrap, repeat16/17/18, identity zero demand and three-segment backing reuse. | These bounded lower slices are accepted. Typed snapshot collections, mutation builders and retained SPR/history remain next; see `retained-typed-snapshot-mutation-and-spr-history-design.md`. Archive/recursive/common hydration and correct Store capability composition remain later work. |
| Shared job physical page close | Native3: all 27 job-crate laws pass, zero filtered, 2 MiB stack; neutral four-case Ajv/JSONPatch and strict TypeScript pass. Committed/staged/rejected page grants and counters now use physical backing. | Recursive49 and FEM23 short-child close now pass; serial job-crate harness is explicit. |
| Shared viewport | Latest canonical viewport route passes 2 framework pose and 2 kernel DSL/Pack laws, with 20 neutral cases, strict Serde/FromValue agreement and strict TypeScript. UI Scene quick route passes 117/117. NodeGraph typed Surface WASM, UI scene 1/1, Surface 44/44 and WGPU attach 4/4 pass. Projection final Nx route passes in 5.1s: 3/3 viewport and 3/3 UI-scene Pack/math laws on 2 MiB, 140 mode/orientation combinations, finite active values distinct from preference slider bounds. Four host laws now pass on2MiB: exact window persistence/context, canonical OS preferences and OS command coverage. | Ten artifact consumer laws remain queued. Projection values remain separate from the later World3dScene/API/framing migration. GraphQL linking/Proto compiler not claimed. |
| Note | Native10: all 3 selected NoConfig, exact camera/reload/close and transient lifecycle laws pass on 2 MiB. | Broader app suite remains outside this selected result. |
| Recursive archive | History51: 2/2. Integrated suite52:5/5,684 filtered,0.43s runtime. Parent/member now share Store-owned retained hydration;87 consumers use DocumentStoreOwners with no alias; actual app initialization preserved. | Root review found counter-only Pack scan before whole decode, false physical-byte retirement from Vec truncation, and unreachable old member typed phases. Correction and real parse/physical-retirement tests required; suite52 proves functional behavior only. Real domain parent adoption remains. |
| FEM viewport, commands and stories | Canonical combined green2 passes both story laws, both contracts and all oracles/strictTS, then lower35/35 plus upper20/20 on2MiB. The upper set includes all seven window laws and fourteen session laws with one overlapping name. | Production-host restore ordering remains a shared open scope; direct runtime reopen is accepted. |
| FEM engine | Combined green2 passes35/35 lower and20/20 upper on2MiB, including ready static/modal output and full child close. Assembly physical native-green1 passes9/9,971filtered,.03s Rust,2m52 Nx:208-byte moved model metadata, all3paged owner kinds × empty/one/maximum,15partial-admission faults/closes, exact grants and29568/29568 matrix retirement. PCG neutral3 passes9publication/4constructor/21wire-page cases and strictTS in4.7s. | Full child post-paging native1 ended before upper tests on the active Pack API cutover; lower3Delement1/1 passed. Fresh native retry and PCG actual RED are held until source coherence. The completed working-memory-admission-owner-audit.md confirms no inspected existing process admission authority is suitable. Shared allocation policy, PCG serialization/control admission/diagnostics and all production retained restore remain required. |
| Exact window Pack identity | Native1 reproduces the missing boundary: the actual registry admits a foreign same-kind inner window identity. Same-window load and explicit cleanup pass before the foreign-case failure. Three neutral Ajv/JSONPatch cases and strict TS pass. | Retained load capability must reject mismatched inner identity and retire its candidate without an unbounded drop; the third colon-id case executes before assertions but its result remains unasserted and unlogged in native1. |
| Production window-config persistence and initialization | The expanded Generation3d lifecycle audit traces all eager capture paths and both production hosts. React and TypeScript WGPU expose read/loadWindowConfigPack methods but do not call them in production; native ProgramBridge lacks the restore API. Current first render therefore creates defaults without a restore barrier. Existing per-app reopen laws are direct runtime API tests, not production-host persistence proof. | Required shared work for every exact window config: real host persistence and restore ordering, explicit owner initialization, non-materializing capture, retained seed publication and exact surface/command readiness gates. The next neutral lifecycle schema/oracle is assigned to the Generation2d agent; no production seam yet. |
| GIS | Native13: 3/3 pass, including two exact Map windows, rendered config, identical reopened payload bytes, six config fields, and bounded candidate transfer. Runtime law uses an explicit 8 MiB thread. | Normal-stack runtime proof and broader GIS app suite remain outside this selected result. |
| Remodel | Native10: 3/3 pass, including six concrete windows, rendering, exact reopen, codec and one-item candidate transfer. Runtime law uses an explicit 8 MiB thread. | Native9's hidden-mesh assertion decoded only the scene spine; the passing law now uses host-equivalent lane reassembly. Normal-stack runtime proof and broader app suite remain unclaimed. |
| Norm | Native9: 2/2 pass with 224 filtered in 0.30s. Neutral Ajv/JSON Patch and strict TypeScript pass for 15 families and two same-kind windows. The runtime isolates focused Inspection selection, persists/reopens both exact Results configs, preserves document/app-config Pack and SPR bytes, rejects invalid identities, and reaches exact bounded close on an 8 MiB stack. | Broader Norm app suites are outside this selected result. |
| Drawing | Native3:2/2 on normal stack; exact two Canvas IDs, shared Viewport2d persistence, transient reset, stable document bytes; oracle/strictTS pass. Authored defaults preserved. | Broader scene/type migration remains separate. |
| Architect | Four exact WindowConfig owners and all five schema facets are implemented. Neutral oracle/strictTS pass. Native r9 passes8/8 on2MiB with two real registered apps, eight exact windows, all four renders, all eight config Pack reloads, stable document/app bytes and terminal-empty close through explicit real app-local disposer registrations. | Three actions have retained WindowConfig-only publication; seventeen remain BatchOnlyPendingRewrite. Report generation still needs a cooperative retained owner while preserving its accepted exact-selection semantics. |
| Home host panel and identity | Panel native r4 passes2/2 on2MiB; post-removal r5 repeats renderer2/2 and the then-current full Home suite runs85/90. Directory schema/catalog, truthful nine-route registry and exact document Store owners are now source-complete. Host `ViewModel.sessionIdentity` replaces Home and Space client config mirrors; registered `.236` r8 passes13 shared vectors,29 source checks, browser10/10 and the related renderer law. | Post-identity native and full Home90 are not yet run. The focused gate must prove native stale/live/missing identity, Space presence direct/retained identity, and all prior failures. Seven Home operations remain `BatchOnlyPendingRewrite`. Studio `SpaceConfig.active_panel_tab` remains open; Home directory projection stays its authenticated read-model owner without identity fields. |

Current Pack state: source, catalog, value/record-body, compact diagnostics and inflater/segment physical owners are coherent. Inflater callers1 exited zero after deflate3/3, Pack2/2, diagnostic1/1, codec1/1 and both mounted Generation suites2/2. The language-neutral planned typed-persistence contract passes Ajv 2020, fast-json-patch and strict TypeScript in4.8s. Typed Generation collections/builders and retained SPR/history remain unimplemented. Recursive suite52 remains functional evidence only until those physical owners, domain factories, retained common hydration and paged ingress are complete.

Current fleet: root coordinates FEM paging and the native queue. The Pack inflater API cutover is complete and released; the next typed/SPR contract is prepared for read-only review before production work. Architect and Generation2d exact-window work remains separately coordinated. No Store capability or lifecycle production switch is part of the Pack slice.

The first Pack source slice is accepted after the real close demand propagated through mounted snapshot/VCS/field/Store ownership and the native outer law proved exact cancellation and physical release. Catalog scalar/span and chunk/observation backing, value/record-body backing, compact hot diagnostics and the retained inflater/segment ring are also accepted without claiming typed collections, retained SPR/history or Store factories. `retained-typed-snapshot-mutation-and-spr-history-design.md` traces the next owner chain. The new projection types are bounded by world-projection-neutral-implementation-plan.md: no scene API switch yet, no implicitPerspective compatibility variant, and no inferred renderer lens preservation. The audit traces parsed/helper45 being overwritten by a Three rig default50; configured helper FOV must become active-spec data during the later scene/framing correction.

Home ownership correction: `home-host-read-model-owner-refresh.md` supersedes the old exact-window tab recommendation. Browser and native `setActivePanelTab` are intercepted at the exact host/session controller and update strict `session.viewState.panelJson`; the native route persists the same state into the owning DirectoryHomeProjection. The existing directory coordinator orchestrates guest actions and receipt ACKs without storing the model. The mirrored Home command/config/action is removed after native r4 acceptance. The Home and Space client identity mirrors are now removed in favor of live host `ViewModel.sessionIdentity`; browser evidence is green and native evidence is pending. Studio's config panel mirror remains open, while Home's authenticated directory read model remains the projection owner without identity fields. Architect exact Register/Adjacency/Graph/Report ownership is accepted; report selection addresses authored document records rather than persist duplicate report JSON, while seventeen batch-only actions still need cooperative retained owners.

Before the persisted-snapshot factory is added, apply the completed store-owner-capability-level-audit.md and root review: separate StoreLifecycleOwners from required document/config/draft/exact-window load capabilities and generic mutation preparation. The87-source rename inventory is not87archive grammars. Preserve each store's ordinary primary history; HistoryLane and application publication targets are different axes. Window Pack load compares schema but not the inner generated partition identity; native1 now reproduces actual foreign-inner-id acceptance. Retained rejection/retirement remains required. See window-config-pack-inner-identity-law.md.
Evidence: `framework-paged-list-ownership.md`, `shared-viewport-implementation.md`, `note-empty-config-owner-preparation.md`, `fem-numerical-owner-follow-up.md`, `recursive-document-archive-protocol.md`, `persisted-archive-decode-owner-review.md`, `norm-empty-presence-and-inspection-ownership.md`, `remodel-window-migration-preparation.md`, the GIS report, `ui-protocol-owner-audit.md`, `ui-protocol-owner-repair.md`, and `job-payload-physical-close-owner.md`. The existing root verify command also has a ticket Nx facade alias; canonical launch entries remain registered.

Completed earlier selected foundations include the 52-law shared empty-state admission suite, 16-law variant-field casing suite, Layout/Sequence 2-law lifecycle slices, exact Semio envelope identity, Curation/Kit identity, Process3D diff laws, canonical child-kind and closure oracles. Their reports preserve exact scope and stack settings; they do not close the remaining full-app and real composed-child work.

## Historical Runtime And Source Checkpoint

- Shared Semio envelope identity: independent eleven-vector Ajv/TypeScript oracle, strict TS and native law passed. All 205 identified Pack decoder predicates now use exact artifact/component/version identity; complete workspace runtime coverage remains open.
- Kernel protocol-laws/record-codec-laws and SDK artifact-app-testing are explicit default-off capabilities requested by downstream dev dependencies. Both full Forms and DAG libraries compile after repairs. Their full suites expose further app/runtime failures and are not passing.
- Forms full native6 selected209tests; app construction rejects the foreign updatePlaybook manifest action. The stale Forms entry is removed. DAG full native6 selected191tests; demo loaded the wrong framework asset, some native mutation vectors compared integer/float representations, command-factory proof and terminal retirement failures remain. Example ownership and typed oracle comparison are corrected. Targeted five-law contract runs7 are queued.
- Retained input native17 reproduces rejected publication retirement failure and recursive fixture stack overflow. Root owns the exact transient diagnostic run18; the Sol agent owns recursive candidate repair and full archive protocol implementation.
- Terrain native5 reaches runtime with only exaggeration3.0-versus3 serde_json representation failure; neutral numeric spellings are corrected and native6 is queued.
- OS host native resume1 has passed its first concrete-window persistence law. Three remaining host/preference laws are still pending.
- Fresh child-slot audit scans114snapshot schemas:37fields have readable fixtures,10have no direct fixture evidence,9fields show declaration/identity issues. Kit agent subsequently corrected its3kind declarations in both native facets. Remaining original families are Assemblymodules, Block3Dcatalog, Sourcingcatalog and Process3Dstock/steps/tools.
- Kit strict TS and independent Ajv/JSONPatch oracle pass14neutral snapshots,10neutral diffs,30committed snapshots,15diffs and all15mutations; native pending. Layout native fixture/codec law passes; actual runtime exposed a contradictory HostOnly/WindowConfig lane declaration, now corrected with rerun pending. Sequence source/oracle and GISMap follow.
- Future ticket native invocations now use per-command uplifted output directories. The configured shared Cargo build-dir and intermediate cache are unchanged. Earlier running processes retain cargo-trinity.

Evidence: shared-envelope-identity-ownership.md, kernel-test-law-capability-audit.md, plugin-artifact-app-law-capability-audit.md, forms-dag-native-regression-findings.md, retained-window-input-replacement-native-audit.md, terrain-native-number-oracle.md, child-slot-kind-and-identity-fixture-audit-refresh.md, native-validation-deliverable-ownership.md. No full-window migration or recursive persistence completion is claimed.

## Historical Fleet (Earlier 2026-09-12)

| Owner | Current bounded work |
| --- | --- |
| Root / Astra Extra High | Composition policy and canonical metadata; Process3D ownership; CAD/FEM native handoff and lifecycle coordination |
| Recursive Replacement / Sol Extra High | Root/branch/leaf archive candidate, bounded member open/retirement, atomic/fault/cancel/stale-owner tests, public maintenance stack boundary |
| Layout/Sequence Windows / Sol Extra High | Layout/Sequence/GIS native laws; Remodel exact window owners |
| Viewport Audit / Terra Extra High | Read-only reusable viewport record, renderer payload and codec dependency decision; Note9 runtime handed to root |

The Stdio/CAD/FEM execution handoff is complete at source/oracle level; root owns native feedback. Earlier Terra audits are complete in new-owner-boundary-audit.md and remaining-window-owner-audit.md. A Terra slot now audits reusable viewport ownership; Layout/Sequence Sol advances Norm inspection-window config while shared source edits hold Cargo.

## Current Validation

| Check | Observed result |
| --- | --- |
| Presentation parent contract | Oracle, strict TS, and one native JSON/text/Pack/child-projection test passed |
| Procedure parent contract | Oracle/strict TS passed; targeted native rerun still needed after shared compile repairs |
| Writer partial Unicode construction oracle | Passed: TypeScript, Ajv, independent JSON Patch, three 12KiB cases and existing window vectors |
| Artifact field parity | Integrated inspector report passed with zero representation disagreements after Layout correction. TS alias/inheritance and GraphQL directive false positives corrected |
| Shared ephemeral transfer/task native | Native3 ran four tests: three passed; one setup failure used a different presence retirement-factory Arc. Exact shared Arc setup corrected; rerun pending |
| SDK window/document replacement native | Attempt 7 failed new composition trait bounds in leaf fixtures/local InteractionState stores and private registry APIs. Agent reports all 22 diagnostics corrected; replacement fault/retirement laws in progress |
| Block3D preview native | First run: three tests passed; runtime expected two artifact receipts, observed one batch receipt and correct two-window generations. Expectation corrected and actual cleared previews added; second run queued |
| Writer native | Second run failed before tests on a missing MEMBER_OPEN_IDENTITY_BYTES Store export. Export corrected; rerun waits for coherent SDK source |
| Store reload→batch native | Passed one targeted native runtime law; retained durable batch accepted after empty-history reload |
| Wires native | Normal-stack green-4 queued; larger-stack diagnostic exposed viewport classification and reload capacity, but did not resolve normal-stack runtime by itself |
| Jack Results native | Feature-enabled green-2 queued; >4KiB result fixture and bounded retirement added |
| Note and Puzzle native | Capacity source/tests handed off. Root full Note native queued; Puzzle sequence remains. Prior Note check4 is orphaned with no result |
| Flow exact window ownership | Independent Ajv/JSON Patch oracle passed; root native run queued after oracle passed |
| Map document contract | Twelve facets plus domain feature schema implemented; oracle/strict TS passed for 24 snapshots and 12 diffs. Native pending |
| Recursive owned-document closure | Ajv/Graphlib and strict TS passed. Native1: one authority law passed, neutral suite failed foreign OwnerRef field admission. Native deny-unknown correction implemented; native2 queued. SDK candidate/archive integration open |
| Shared schema record | Map/Terrain consume framework declared-record parser; Ajv oracle and strict TS passed |
| Terrain document contract | Red reproduced missing mesh field; twelve facets and native conversion/identity fixes implemented. Oracle/strict TS passed, native queued |
| OS-wide preferences and context | Source/oracle previously implemented; four native host laws still need successful runtime completion |

## Required Remaining Work

1. Finish current compiler/runtime feedback and remove temporary Wires work-step diagnostics after a normal-stack proof. Confirm shared fault/cancel/close and generation replacement behavior against real runtime debug logs.
2. Verify the completed Layout contract source through native JSON/text/Pack/child-projection tests. The integrated schema parity report now has zero disagreements. Layout still retains inline drawing payload until actual child stores and views can own it.
3. Refresh child-slot-kind-and-identity-fixture-audit.md after completed domain corrections and finish the remaining families. The original 32 findings are historical, not the current remaining count. Never weaken child_id == target.artifact_id. Verify constructors and all committed fixtures.
4. Implement complete recursive owned-child persistence. Parent codecs remain handle-only; archives and candidate replacement must own the nested closure, full dialect/owner coordinates, exact child completeness, atomic publication and bounded abort/old-child retirement. The 64-reference per-parent limit is distinct from the global 1,024-member registry limit. Stdio Kit→Object→Mesh/Brep/Value proves direct-child-only archives are insufficient.
5. Replace Presentation, Program and Procedure scratch-cache authority with actual owned member stores, exact member factories and child-view reads. See composed-child-full-document-persistence-audit.md; Presentation kind/identity correction is already done, but full payload ownership is not.
6. Finish remaining app/window scope migrations (Layout, Sequence, Remodel, GIS, Terrain, Shooting, Block/Lowpoly/Procedural/Drawing/Raster, Forms/CAD/FEM/Architect; Generation3D preview source has moved to exact window transient and requires native verification). Reuse domain-neutral config/transient lifecycles and keep domain state schemas at their concrete window owner. Home host-panel ownership is corrected; its directory read-model/receipt owner remains Home-specific and its client identity mirror is removed in favor of the host session projection. Studio's panel mirror remains open.
   Architect's four exact WindowConfig owners are source-complete, but the seventeen batch-only actions listed in its current-evidence row still require truthful retained execution owners. `runReport` must retain its exact invoking Report-window selection while moving report construction and record authoring onto a bounded, cancellable route.
7. Run the broader artifact laws only after their owning source changes settle. Record unrelated full-suite failures separately; a passing targeted contract is not a passing full editor suite.
8. Close the ticket only after all required work is complete, with exact changed-file ledger and summary. Then remove ticket generated outputs; preserve reports, inputs and scripts. Mark the goal complete only after that.

## Current Root Native Evidence

All listed outputs are under ticket 🗑️generated. Old September 9 queued runs have no current completion record and are not live.

| Fresh run | Observed result |
| --- | --- |
| forms-identity-native-3 | Passed four compiled contract tests with runtime logs |
| forms-identity-native-4 | Stopped before tests on six SDK artifact_app_laws imports during concurrent rename; new fifth Pack test unverified |
| dag-identity-native-3 | Three passed, foreign text-envelope test failed; corrected |
| dag-identity-native-4 | Stopped before tests on concurrently relocated oracle include; corrected |
| map-document-contract-native-resume-1 | Passed oracle/strict TS and one targeted native JSON/text/Pack/replacement law |
| terrain-document-contract-native-resume-1 | Stopped on transient missing Kit GraphQL/Proto files; agent restored all paths |
| semio-envelope-identity-native-1 | Passed eleven independent Ajv/TypeScript vectors, strict TS and one native eleven-vector law |
| DAG envelope5, Terrain envelope5, Program resume1, CAD resume1 | Oracles passed; Cargo stopped on relocated OS scale manifest referenced at old path |

Root corrected both scale workspace/dependency entries in Cargo.toml to the actual 🧪️tests/⚖️scale/🦀️rust package. A Bun TOML read confirms every explicit workspace manifest currently exists. Recursive SDK execution is aligning the artifact_app_laws module/export with concurrently renamed consumers. Fresh reruns follow these source corrections.

## Refreshed OS/Host Declaration Gate

`ownership-scope-refresh-2.log` passed via registered Bun/Nx report: eleven ownership vectors, four nested-schema vectors, three command-source vectors, and 104 artifact contracts across five schema formats. The scan detected zero misplaced OS/host declarations in its current inventory. This does not prove all window runtimes or full child payload persistence. The first refresh found four missing Map diff JSON state annotations; those were corrected before this passing run.

## Stdio and Program Milestones

Program source/oracle is complete for 532 snapshots, 260 diffs, 64 registers and 1,064 child identities; native queued, with closed-record unknown-field rejection added. Shared Stdio geometry oracle and strict TypeScript pass for 12 neutral vectors. Object exact artifact/snapshot/diff parsers pass 18 neutral vectors, 18 committed snapshots, 9 committed diffs, and four independent JSON Patch edit laws. Object native JSON/text/Pack/typed mutation tests are authored and pending. Kit source migration is delegated to the Flow execution slot. See stdio-object-kit-ownership.md and stdio-object-fixture-ledger.md for exact files.

## Resumed Evidence (2026-09-12)

The previous generated log directory is absent and no previous native task process remained. Historical queued entries above therefore remain unverified; they are not live waits or passes. The repository now shares Cargo intermediates through `.cargo/config.toml` while ticket `CARGO_TARGET_DIR` retains only final deliverables. No cache relocation or deletion was made during this resumption.

Forms and DAG fresh red oracle runs each reproduced missing exact-child rejection. Both updated oracle-only runs passed. Strict TS first exposed old Bun-only `import.meta.dir` test path typing; tests now use the standard file URL conversion. Updated registered oracle plus strict TS runs (`forms-identity-green-3.log`, `dag-identity-green-3.log`) passed. These cover 20 Forms snapshots/6 diffs/6 sparse laws and 28 DAG snapshots/2 sparse laws, independent Ajv/JSON Patch and full invalid child dialect/identity vectors. Rustfmt parsed the modified native modules; both native runs failed before tests (`forms-identity-native-1.log`, `dag-identity-native-1.log`) because the relocated UI WGPU module resolved its generated IconName include one directory too shallow. The canonical generated asset exists. Another concurrent agent corrected the include before root applied a patch; no root UI edit was made. Native runtime completion remains pending.

Native pure apply/absorb/delta behavior has moved from each text codec into its schema diff owner with all 23 mutation imports retargeted. The initial constructors now create equal child/target IDs and Forms uses distinct value/table IDs. Content-derived identities, parent mutation routing into actual child stores and full recursive persistence still require completion. Forms/DAG details and exact touched files are in `forms-dag-child-ownership.md`.

### Latest Native Feedback

Forms native2 passed three compiled contract tests; fourth envelope test is running in native3. DAG native2 failed two of three tests on its default fixture marker; the fixture owner correction is implemented and rerun is next. SDK retained-window-input native8 exited before tests with composition trait bounds, including RecursiveMemberSnapshot missing ArtifactCompositionFields. The recursive execution agent owns these repairs; this run is no longer live.

### Fresh Map And Envelope Evidence

Map native resume1 passed its targeted JSON/text/Pack/replacement law and independent oracle/strict TS. Forms native3 passed all four compiled contract tests. DAG native3 passed three tests and reproduced foreign text-envelope admission; its check is corrected. New fifth Forms/DAG Pack identity tests are pending, with shared envelope equality introduced at the OS container owner. Terrain native resume1 stopped before tests while the Kit agent temporarily replaced eight GraphQL/Proto include files; the agent confirms all paths are present again. Root is preserving concurrent edits and will rerun.
