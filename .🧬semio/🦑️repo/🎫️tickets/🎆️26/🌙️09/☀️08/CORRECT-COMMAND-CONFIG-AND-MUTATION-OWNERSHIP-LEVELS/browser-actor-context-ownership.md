# Browser Actor Host Context

The cold browser actor worker is another real SurfaceVisible producer. It currently sends only a window-kind surface string, conflates that value with the renderer body, and receives no resolved OS language or host controls. The fix is an ephemeral, opening-attempt-scoped `browser-actor-view-state` worker message. ShellHost supplies the resolved target session view immediately after opening and when host preferences/window controls change. The worker retains only the latest host projection, resolves bodyKey from the verified package descriptor, and renders the base concrete window from that target. The view is never part of ArtifactActorConfig, document snapshots, or shared presence.

The shared manifest owns strict resolved-view validation and window/panel projection. The browser-specific envelope owns scope/client-opening identity. Closed/replaced openings reject stale views. Existing bounded render turns and cancellation control apply to updates; rendering waits for activation/initial cold transfer to avoid overlapping guest polls. Validation will cover explicit non-default locale, distinct body/window identity, stale opening refusal, and preference rerender. No validation result yet.
# Host Opening Context Validation

Final focused worker run passed with direct runtime console evidence: `[DEBUG] browser-host-context live-preference-refresh=1 artifact-config-unchanged=1 stale-opening-rejected=1`. The cold pair transferred all 64 pages (4,194,304 bytes), completed lifecycle acknowledgement, applied the initial tiled-map patch, acknowledged and rejected exact patch receipts, and rendered again after preferences changed. Refusal tests exercised turn exhaustion, stale context before/after a turn, unexpected cold ingress, and a faulted turn. One test passed; the other 301 suite tests were intentionally filtered. This validates the mocked worker/guest boundary, not a full browser application launch.

The expanded worker integration passed through Nx on the next bounded run: one test passed, 301 intentionally filtered, with the complete 4 MiB cold pair and live host preference refresh. Its generic deep equality checks on two 2 MiB byte arrays were replaced with Node Buffer byte comparisons to keep the same exact assertion without expensive per-element assertion bookkeeping. A final run enables direct console forwarding for the required runtime debug evidence and includes the refresh-failure status path added during review.

Added a schema-first ephemeral `browser-actor-view-state` request. Its opening UUID and document scope select only the current worker document opening; required locale and terminology come from the canonical host view. The worker stores the context outside artifact config and releases it on close. A latest-view refresh re-renders an activated cold actor. Surface-visible uses the verified descriptor's authored body key and the addressed concrete window projection.

The six-case neutral envelope corpus passes strict Ajv validation and the actual worker wire encode/decode round trip through Bun and Nx. The eight-case shared resolved-view corpus also passed. The expanded cold-actor worker test checks stale opening refusal, live locale/terminology refresh, unchanged artifact config/frontier, packed view delivery, and retirement. Its first run correctly rejected the older fixture descriptor without a body key. The fixture now explicitly declares `gis2d.play.composite`, with canonical packed bytes and byte checksums updated; the rerun is pending. No worker runtime success is claimed yet.

Concurrent infrastructure renamed project metadata to `📋️project.json` and introduced `.vscode/🧩️launch.seed.jsonc`. New ownership validation targets and the canonical OS config crate follow that current structure. Launch registration is present in both the seed and generated launch output.

Files in this wave: the shared manifest resolved-view schema and tests; browser-bundle view-context schema, parser and neutral fixture/tests; OS worker wire and dispatch; cold actor integration test; hub execution-target lease fixture; root Cargo workspace and verification script; root project targets; both launch files; OS config crate script/project registration; ticket validation script/project.
