# Curation Catalog Ownership

The registered red oracle reproduced Curation accepting an empty catalog object. The previous artifact/snapshot/diff TypeScript facets independently redeclared `ArtifactDialect`, `ArtifactRef` and `ArtifactChildHandle`; JSON accepted any object, GraphQL called the catalog an ArtifactLink, and Proto reduced its target to a string.

Curation now imports the OS Store child type and the shared Stdio exact-subset parser. The shared Stdio child JSON schema owns the new Kit policy. All three Curation facets declare artifact kind `s.stdio.semio`; standard `v1`, subset `kit` and equal child/target identities are independently checked. Artifact/snapshot records share a single TypeScript parser. Unused document copies of Filters/TableSort/SortDirection are removed; the editor configuration retains its existing declarations. Full concrete pool-window migration remains separate required work.

Native Curation artifact/snapshot/diff admission checks exact Kit handles. Typed diff application validates replacements before changing a document. Pure apply/absorb helpers and their unit tests moved from the text IO leaf to the schema diff owner. Native text/Pack implementations moved from snapshot schema to their existing IO leaves; JSON has an explicit IO snapshot leaf. The set-artifact-json command and scenario adapter now import these codecs from IO, with no compatibility export at the old schema path.

Validation: `curation-document-contract-red-1.log` failed on the missing exact-child exception. `curation-document-contract-green-2.log` passed independent Ajv admission, six committed snapshots, three committed diffs, neutral sparse/null diffs, child/owner rejection vectors and strict TypeScript. Green3 also passes all four geometry variants and strict TypeScript. Native JSON/text/Pack/typed-replacement law is authored. Native1 was cancelled before package compilation while the fleet released shared Cargo contention; a coordinated rerun is pending, and no native pass is claimed yet. Full recursive Kit payload storage and stable identity across catalog edits are not completed by this parent contract.

Changed owners: the shared Stdio base child JSON policy; Curation artifact, snapshot and diff facets in Rust, TypeScript, JSON Schema, GraphQL and Proto; artifact Rust module mounting; snapshot text/binary/JSON IO; diff text IO; moved diff unit test; new neutral fixture and document-contract tests; set-artifact-json command import; mutate-curation scenario import; root/ticket registered script/project routes and root launch entries 311.184/311.185.

Native2 compiled the current Curation crate in 60 seconds and reached the geometry JSON comparison after the base JSON/text/Pack and Box text/Pack roundtrips. It failed because the original test compared integer-versus-float representation tags (`4`/`4.0`) in `DslValue`, which deliberately preserves those tags. The test now uses the existing shared `store::pack_rt::json_values_equal` contract with independently decoded serde_json values; no fixture values, codec output or runtime validation were weakened. The pending Stdio Object oracle now uses the same shared comparison. Native3 remains required.

Native3 (`curation-document-contract-native-3.log`) passed the independent oracle, strict TypeScript, and the compiled native contract test with `[DEBUG]` evidence. Native coverage includes exact Kit identity across artifact/snapshot admission; base plus four geometry JSON/text/Pack roundtrips; invalid child/diff rejection; sparse and nullable diff admission; typed invalid-child replacement rejection; and foreign text envelope rejection. This completes the bounded parent-contract and codec ownership verification. Complete recursive payload storage and concrete pool-window state ownership remain required.
