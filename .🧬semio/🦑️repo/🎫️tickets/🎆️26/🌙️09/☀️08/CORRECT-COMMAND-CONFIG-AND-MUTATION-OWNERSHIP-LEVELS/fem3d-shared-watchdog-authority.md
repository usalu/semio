# FEM3D Shared Watchdog Authority

## Current Combined Acceptance — 2026-09-13

Canonical combined native green2 completed the FEM2D engine package with **35/35**, 940 filtered, 118.34s Rust, normal 2 MiB stack. Its mounted 3D element law now freshly passes NumPy stiffness parity and exact Bar3/Frame3/Tet4 string-owner retirement (3/3/5). The shared numerical close/control corrections pass in the same binary.

The upper FEM3D window/session package passed **20/20**, 829 filtered, 1.52s Rust. The complete registered Nx run exited zero in 12m55s with zero cache hits. The full numerical child reports terminal/ready/static/modal/closed all true, close_lane22 and both job close bits set. It uses245452 of27689000 finite test opportunities; PCG converges in11 iterations with residual5.243634533307156e-9, and Subspace in8 with residual3.3764473060475575e-7. The shared watchdog records5 isolated overruns, longest run1, without sustained quarantine. Its8ms ceiling,4-consecutive quarantine rule, one-fuel opportunities and2MiB stack are unchanged.

This accepts the concrete string-cleanup repair through the full child and all existing selected window/session laws. Actual model-root admission and assembly physical byte accounting remain separate open gaps. The new assembly physical-owner schema/oracle passes7.9s; two pre-fix native regressions are now running in `fem-assembly-physical-native-red-1.log`. Their production fix is not applied.

Log: `🗑️generated/fem3d-canonical-physical-green-2.log`.

## Earlier Evidence and Development Record

2026-09-13 current queue: broader `fem3d-window-contract-close-green-1.log` passed its two story laws, neutral contracts and strict TypeScript, including the independent mounted string-count expectations 3/3/5. Native Cargo remained in a verified shared prebuild lock wait for 30 minutes without executing tests. Root deliberately stopped only its owned facade/Cargo processes and queued fresh FEM validation behind Pack caller work. This is a pre-test cancellation, not runtime acceptance or failure of the string repair. The last actual full-child runtime remains close-red2: 13/14, with absent-lane cleanup fixed and AssemblyCsrBuild lane3 stalled.

## Current Verified State — Native3

Current string-release source correction is applied after repeated actual full-child close failures: a common borrowed-string owner helper serves Bar3,Frame3,Tet4 and the existing Bar2,BeamEb2,Tri3Cst implementations. Unsupported 3D defaults are replaced with exact next-allocation observation, one backing retirement and terminal emptiness. Close-red3 did not execute tests because root taxonomy initialization rejects currently missing fixed contract ids/dispositions. This is not element test failure evidence. The already registered broader FEM3D window-contract native route remains usable without that root initializer; it now runs after correcting its stale Vitest config path to the verified canonical config. Its story/neutral/strictTS stage passes and it is compiling the selected lower element/assembly/PCG/subspace and upper window/session laws. Canonical taxonomy acceptance remains open independently.

Close-red2 confirms the four absent-lane cases now pass:13/14 session laws,835filtered,6.36s execution. The only remaining runtime failure is still the full child at close_lane3 with valid static/modal output (245579 opportunities,106isolated recorded overruns,longest consecutive run1). This run predates the common 3D string implementation. No full-child cleanup acceptance yet.

Validation-route correction: the ticket facade still called its old duplicated14-session Cargo route, bypassing the expanded root script. Root found and replaced that branch with delegation to root 📜️script.ts. Thus close-red2 remains useful parent/full-session evidence only and does not execute the lower mounted-element law or new neutral stage. The next run will execute canonical neutral/strictTS, one positively counted lower element law, then the14 session laws. No lower element test outcome is claimed until that route actually executes.

Executed close-red1:14 selected session laws,12pass/2fail,835filtered,2.13s runtime. All four absent lanes return complete=true at their unchanged lane, including zero-byte grants; the full numerical computation still succeeds then stalls in lane3. The absent-lane correction is now applied: strict zero-grant no-op, explicit lane advancement, and a final child witness covering every retained optional owner and inline slot admission. No 3D element string implementation is applied yet. The element test is compiled by the FEM2D package rather than FEM3D, so the native target now runs that positive-count lower filter before the14 session laws. Close-red2 is running to capture its actual element failure.

Native3 executed13 laws,12 passed,1 failed,835 filtered,1.16s native and13m15s Nx at2MiB. It confirms the same complete static/modal result at245464 opportunities. Cleanup stalls at parent close_lane=3 (AssemblyCsrBuild), before LDLT/subspace begin-close bits. Source inspection identifies all three production mounted 3D elements (Bar3,Frame3,Tet4) missing the Element string-release interface: they inherit mounted_next_string_bytes=Some(usize::MAX), while MountedAnalysisModel refuses a grant smaller than that sentinel. This is a source-supported cause consistent with the observed retained assembly lane; the next native law must confirm the repaired full close.

The existing mounted element fixture/schema/oracle now declares exact string owner counts3,3,5; independent JSONPatch removes those owners individually. Its native test now checks advertised next bytes against each actual release and the terminal witness. The numerical-child target includes this existing element law alongside the13 session laws. Registered close-red1 is running; no 3D element cleanup production repair is applied yet.

Four later parent lanes14,19,20,21 also return whole-child terminal early when an optional owner is absent. They must advance their lane and retain later owners instead. A final true return is not sufficient proof until every physical owner is empty. AssemblyCsrBuild also reports inline AssemblyJob/Csr struct size as freed allocation; model Arc backing accounting and LDLT eager double-owner close are separately open and must not be hidden by the immediate string fix.

Root traced the recorded RecoverReaction overrun through the numerical child, CSR entry lookup, scalar lookup, and cancellation checks. The success route performs direct lookups and one scalar accumulation; cancellation reads an atomic root token. This source review does not prove why native29 observed8015µs.

The current framework trace module already owns the cross-platform overrun policy: Watchdog records every elapsed sample above INTERACTIVE_STEP_CEILING_US, while a per-session StepOverrunLedger rejects clock faults or SUSTAINED_OVERRUN_QUARANTINE_STEPS consecutive breaches. The existing threshold remains8000µs and the existing sustained count is4. CallbackVerdict explicitly states that one elapsed sample is a measurement, not itself quarantine authority. This policy is also used by the production job scheduler.

The FEM3D end-to-end native test duplicates an unconditional local wall-time rejection instead of using that framework authority. Root will replace that duplicate test policy with the existing Watchdog and StepOverrunLedger, keep the actual8000µs deadline, record every individual overrun with before/after stage and scalar cursors, and fail on the shared terminal verdict. No production limit or framework threshold changes. The200000-opportunity bound remains unchanged. An expired-before-work yield may keep its one fuel only when the observed phase/cursors remain unchanged and no terminal is published.

This aligns test ownership with the running scheduler. It does not excuse the source-proven mesh reservations, insertion retirement, model-root admission, scalar identity allocations or PCG publication gaps; those remain independently required.

## Executed Native1

The registered normal2MiB run completes13 selected tests in3.74s after3m30s compilation:12 pass and the full numerical law fails at its200000-opportunity test bound. The shared ledger records59 isolated overruns, a longest consecutive run of2, and no terminal rejection. The test reaches genuine PCG convergence at iteration11 with residual5.243634533307156e-9, then modal iteration6 of30 with residual7.050211346167176e-5. It does not reach field readiness or the final close assertions, so full numerical acceptance remains open.

The old200000 bound was retained when the harness adopted the framework watchdog, but now counts individual scalar/cell operations throughout the modal algorithm. Source permits30 modal iterations and100 Jacobi sweeps within each, with up to36 pairs for its9-column subspace and multiple scalar operations per pair. The next harness adjustment must bound the entire algorithm from those finite dimensions, preserve the8000µs per-step authority, and explicitly retire the child before reporting any non-terminal failure. It must not claim that these observations prove all numerical source owners are bounded.

Native2 is now running with a finite whole-algorithm harness allowance derived from the30 modal iterations,100 Jacobi sweeps,9 modal columns and maximum40 supported modal equations. The allowance includes200000 non-modal opportunities, scalar pair rotations and conservative non-Jacobi work. Every actual step still receives one fuel and the shared8000µs deadline/watchdog. The harness records step/failure/readiness facts, retires the child with the maximum of its JobPayload and scene-page requirements, and only then asserts success. The close loop remains capped at200000. This changes the test's total completion allowance, not a production execution grant, deadline, iteration limit or convergence requirement.

## Executed Native2

Native2 runs13 laws on2MiB;12 pass and the full numerical law reaches actual successful output in245464 scalar/cell opportunities. PCG converges in11 iterations; modal computation converges in8 of30 at residual3.3764473060475575e-7. The logged flags are terminal=true, ready=true, static=true andmodal=true, with zero recorded watchdog overruns. The remaining failure is exact close:200000 close calls do not reach terminal. The full law therefore remains failing. Native3 adds the retained close lane and child-started bits to identify that runtime boundary before changing production cleanup.

Sources: [trace watchdog and ledger](/Users/ueli/Documents/semio/🧰️framework/🔨️modules/⏱️trace/🦀️.rs:429), [numerical child](/Users/ueli/Documents/semio/✏️s/🔌️plugins/🏗️fem/🗿️artifacts/🧊️3d/🏅️standards/🔖️1/🪆️subsets/🌐️any/✏️editor/🧵️session/🦀️.rs:1081).
