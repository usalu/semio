# Ownership Source Refresh 4

The first registered Nx refresh stopped during module initialization, before any ownership oracle or source scan. The taxonomy loader rejected input-pattern ordering for the actor-typegen and ui-axes generator contracts. Evidence: [ownership-source-refresh4.log](🗑️generated/ownership-source-refresh4.log), exit1 in929ms.

Root inspected both current arrays immediately afterward. They already satisfy the exact JavaScript lexical-order and uniqueness predicates; another simultaneous edit had resolved the transient source state. Root changed no taxonomy source or validator. A fresh registered refresh is warranted against the now-coherent metadata. This initialization failure is not an ownership gate result and does not supersede accepted refresh3 evidence.

The fresh registered run passed in5.3s with exit0:11 ownership vectors,4 nested-schema vectors,3 command-source vectors,104 document contracts across five formats and0 misplaced declarations. Evidence: [ownership-source-refresh4b.log](🗑️generated/ownership-source-refresh4b.log). This now supersedes refresh3 as the latest executed source gate after the Architect and Generation2d camera-owner source changes. It remains a source-contract result, not full native or recursive physical acceptance.
