# Procedure Document Contract Ownership

Native Procedure composes the shared stdio flow and text child identities. The non-Rust artifact, snapshot and diff still declare inline path/seed content. The shared ArtifactChild wire boundary contains childId and the canonical IO ArtifactRef; embedded flow/text content remains in its admitted typed child store. Existing Procedure source and all eight committed snapshots confirm this ownership. This correction aligns all twelve non-Rust facets with that boundary, removes obsolete document delta definitions, and adds a neutral document/delta plus foreign editor/embedded-payload rejections. Native domain mutations remain unchanged.

Validation is pending. The oracle is implemented before facet edits and compares production parsers against Ajv and all eight snapshot/two diff files.

## Red Evidence And Implementation

`procedure-document-contract-red-1.log` failed as expected: Ajv required stale path/seed fields and rejected native flow/text child fields. Updated the twelve non-Rust document facets to schema/flow/text, using the shared ArtifactChild and IO reference contracts. The diff retains native artifact replacement plus nullable sparse schema/flow/text updates; the first-party TypeScript parser normalizes omitted fields to null. Removed document-only Path/Step delta duplicates from those facets. No committed fixture needed modification. GraphQL/Protobuf imports follow the shared schema convention; compiler validation remains pending.

## Oracle Result

`procedure-document-contract-green-2.log` completed with exit 0 through Bun/Nx: the first-party parsers match Ajv on eight committed snapshots, two diffs, a populated replacement delta, an empty sparse delta, and the neutral refusal vectors. Strict TypeScript validation passed. Added native JSON/text/Pack round-trip and refusal tests against the same neutral fixture; native artifact/snapshot/diff now explicitly deny unknown fields to match their closed schemas. Native execution is queued separately and remains unclaimed.

## Full Native Diagnostic

`procedure-document-contract-native-1.log` compiled Procedure successfully and started 137 native tests. The process aborted during existing addStep editor tests: the command has no exact controller/owner/factory/tool/schema proof, then unwinding dropped a non-empty ArtifactStore cursor disposer. No success is claimed for this run or for the new codec test, which had not printed a result before the abort. The contract target now selects its named native contract test. The editor failure remains an open retained-command ownership task; this narrower target does not suppress or resolve that failure.

## Native Attempt Two

The targeted native retry stopped at shared SDK transient helper exports while that interface was being replaced. No Procedure runtime ran. The loaded-child metadata now uses the actual artifactKind s.stdio.semio rather than a subset-qualified pseudo-kind; the named native contract test now also requires the shared loaded-child projection to accept its two child handles.
