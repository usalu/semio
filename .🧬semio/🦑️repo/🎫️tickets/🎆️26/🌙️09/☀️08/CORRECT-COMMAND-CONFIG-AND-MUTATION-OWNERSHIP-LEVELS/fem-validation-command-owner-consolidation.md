# FEM Validation Command Owner Consolidation

The ticket's FEM2D/FEM3D window-contract facade repeated the root verifier's story, oracle, TypeScript, feature and Cargo filter logic. That duplication had already let the numerical-child facade miss newer checks. The canonical root verifier now loads successfully in fresh neutral and numerical-native attempts, so the temporary duplicated window-contract branch is removed. It delegates directly to root `📜️script.ts verify` like the numerical and mesh aliases.

The canonical FEM3D contract verifier now includes the numerical-page oracle and its strict TypeScript source, and its existing serial lower FEM2D Cargo filter set includes the numerical page, LDLT checkpoint/replay and Subspace replay laws. The upper FEM3D package still covers exact-window and full live-visual child laws. Existing task and launch registrations are unchanged; no new executable command is introduced. This combines the pending shared cleanup repairs, concrete element-string cleanup and full parent numerical behavior in one fresh post-fix validation route.

Canonical expanded-neutral8 is dispatched through the registered ticket Nx alias to verify the actual delegated command. Native execution remains separately queued behind current numerical mixed3; no new native result is claimed by this command refactor.

Executed canonical expanded-neutral8 passes in 44.5s, zero cache hits: both story laws, both FEM window contracts and their embedded engine/close oracles, mounted 3D NumPy/string-owner checks, all expanded numerical-owner cases, and strict TypeScript. This confirms the ticket facade now reaches the canonical root implementation. Mixed3 native has advanced through the kernel and into Stdio artifact compilation; no native test result is recorded yet.
