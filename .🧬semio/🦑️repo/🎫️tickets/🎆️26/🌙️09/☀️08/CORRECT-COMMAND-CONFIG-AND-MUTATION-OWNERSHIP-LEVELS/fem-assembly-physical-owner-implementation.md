# FEM Assembly Physical Owner Implementation

## Post-Paging Composed Acceptance

The fresh registered fem3d-numerical-child-native run2 completed successfully after Pack API coherence: lower3Delement1/1 (982 filtered), upper session14/14 (835 filtered),0.48s upper Rust and2m12 Nx, zero cache hits, normal2MiB stack. The real child logged terminal/ready/static/modal/closed all true, close_lane22, all3jobs began close,245456/27689000 turns. PCG converged at11 iterations with residual5.243634533307156e-9; subspace converged at8/30 with residual3.3764473060475575e-7. Recorded watchdog overruns were zero. Log: 🗑️generated/fem3d-paged-model-native-2.log. This accepts the paged model in the composed child before the pending PCG retained publication switch.

## Current State

Post-paging FEM3D native1 ended before upper tests during the separately coordinated Pack API cutover. The lower mounted3D element law passed1/1; upper compilation encountered the changed Deflate constructor/removed close method at Segment callers. Exit1 after4m41 is a pretest dependency mismatch, not a FEM numerical failure. Root holds the fresh upper retry until Pack source is coherent. Focused model/assembly9/9 remains accepted.

Native-green1 is accepted **9/9**,971filtered,.03s Rust,2m52 Nx, normal2MiB, zero cache hits. The expanded neutral oracle and strict TypeScript pass in the same registered route. Inline model metadata is now208bytes; the old41528-byte payload arrays are gone. Actual admitted/released page totals match for node one4848/4848 and maximum7296/7296, element one4864/4864 and maximum36224/36224, support one/maximum2944/2944; all empty owners0/0. Every one of fifteen partial-admission/fault cases closes with exact allocation conservation. Zero/subexact allocation grants preserve metadata/initialized/admitted state; logical admission never accompanies a physical allocation.

Both inline Option laws and all six matrix close cases remain green, including29568/29568 actual capacity. Existing paged triplet construction/CSR behavior also passes. The full FEM3D numerical-child route is now running separately in `fem3d-paged-model-native-1.log`; parent integration is not accepted until it finishes.

Post-fix native-green1 is running (session30084). The paged model/API/direct-move switch and all three production admission kinds are applied. Expanded native coverage selects nine assembly laws; the new model cases consume the same schema's limits/targets. Ajv independently rejects maximum-plus-one admission and JSON Patch derives the unchanged refusal state. Source and native test Rustfmt parsing and scoped diff checks pass; no post-paging native success is claimed yet.

Native-red2 executed seven tests on2MiB in17.2s Nx:6passed/1failed,971filtered,.03s Rust. Both inline accounting repairs pass. Every empty/capacitated matrix case conserves actual allocation (empty0/0; three PagedList roots29568/29568), retains zero/subexact grants and releases exactly one4096-byte backing at the exact grant. The remaining actual RED is the mounted model:41528 inline bytes exceed its4096-byte metadata limit. This is the baseline for replacing arrays with the shared PagedList owner and removing the sole mounted Arc transfer.

Native-red1 executed six tests on2MiB:4passed/2failed,971filtered,.03s Rust,3m53 Nx. Both new laws reproduce false physical releases: the inline AssemblyJob removal reports688bytes and empty Csr removal reports152bytes despite actual0backing, including a zero-byte grant. The fixture drains its test owners before assertions. The model-size baseline was added after this binary compiled and is not included.

After that actual RED, both inline Option removals now report one logical owner and zero bytes. Native-red2 will verify all matrix capacity/grant cases and expose the current mounted-model inline-size baseline. The model paging implementation has not been applied.

The new engine-owned schema, fixture and independent Ajv/JSON Patch oracle cover two inline owner kinds, empty and three-backing matrix states, and zero/subexact/exact byte grants. The registered neutral route passed in 7.9 seconds, zero cache hits, with strict TypeScript. Log: `🗑️generated/fem-assembly-physical-neutral-1.log`.

Two native regressions are authored and the registered native-red1 run is active, session51343. They are included in the existing `assembly_triplet_pages_` family, selecting six tests with the four earlier assembly laws. The assembly-record fixture first drains all nested job owners, explicitly releases its cold test Arc by replacing it with a static borrowed empty model, then isolates inline record retirement. It does not claim the production mounted model Arc is accounted. The matrix fixture retains actual empty-capacitated PagedList backings and compares every before/after allocation total with reported release; zero/subexact grants must preserve them.

Production `AssemblyCsrBuild::close_step` still reports `size_of::<AssemblyJob>()` and `size_of::<Csr>()` on inline Option removal. Those are confirmed source findings; native RED is required before correction. The mounted model PagedList migration and Arc-root decision remain the separate plan in `fem-mounted-model-physical-owner-next-slice.md`.

## Command Ownership

The schema now also fixes the mounted model's logical maxima128/128/64, zero initial backing,4KiB maximum metadata/root size and separately granted4KiB pages, retained allocation faults and direct move ownership. A third native baseline checks the current model's inline footprint against this bound without constructing or allocating it. Added while native-red1 is compiling; actual native count determines whether that binary includes it. Fresh neutral/post-fix execution is required for this extension.

Root `📜️script.ts verify fem-assembly-physical-owners [native]` owns the orchestration. Root and ticket Nx targets call their respective canonical script, and the ticket script forwards unchanged arguments to the root command. Launch and seed registrations use orders 311.242 and 311.243. This keeps an engine physical-memory test available independently of the artifact window-contract orchestration.

## Files

- Engine analyses `🧪️tests/📦️physical-owners/🧬️schema/🔣️.json`
- Engine analyses `🧪️tests/📦️physical-owners/🧫️fixtures/🔣️.json`
- Engine analyses `🧪️tests/📦️physical-owners/🟦️.ts`
- Engine analyses `🧪️tests/🔬️unit/🦀️.rs`
- Root `📜️script.ts` and `📋️project.json`
- Ticket `validation/📜️script.ts` and `validation/project.json`
- `.vscode/launch.json` and `.vscode/🧩️launch.seed.jsonc`

Rustfmt parse and scoped diff whitespace validation pass. No production repair or native acceptance is claimed yet.
