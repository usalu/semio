# Process Budget Source Observation

Observed 2026-09-13: the shared repository process runner currently defines `BUILD_BUDGET_MS`, `CMD_BUDGET_MS`, `ORCHESTRATOR_BUDGET_MS`, and `DAEMON_BUDGET_MS` as zero. Its comments and budget resolvers describe zero as unlimited. Optional environment overrides remain available through the corresponding `SEMIO_*_BUDGET_MS` variables.

Source: `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🏃️process/🟦️.ts`, lines 13–47. This task did not change that file. Earlier build timeout observations do not establish the current defaults. Explicit task/environment budgets still require separate inspection. Runtime step fuel, allocation authority, cancellation, watchdog and cleanup bounds remain independent requirements.

At 00:54 UTC, read-only process inspection found Pack Gen2-mounted Cargo59072 and root FEM Cargo62941/74489 sleeping with no child processes. Each had open per-unit files under the configured shared Cargo build tree; the durable lsof snapshot is `🗑️generated/cargo-owner-open-files-0054.txt`. Other Cargo work still had active rustc processes. This observation does not establish a deadlock or stale lock. No process, cache or lock was modified; the current runs remain pending, with their previously completed oracle results recorded separately.

A one-second read-only sample of root Cargo62941 confirms its active worker is blocked in `prebuild_lock_exclusive` → `LockManager::lock` → `flock`; the main thread waits in the job queue. Evidence: `🗑️generated/fem-cargo-62941-sample.txt`. This establishes a build-lock wait, not solver runtime work, and still does not identify a deadlock or justify touching foreign processes or lock files.

At 01:04 UTC root consolidated its duplicate FEM package runs by sending TERM only to its verified newer Cargo74489, the exact `fem-numerical-page-owners-native` invocation started by root. It had no test execution and was waiting for shared build work. Older root Cargo62941 and Pack Cargo59072 remain active; no foreign process, lock file or cache was touched. Mixed2 is now a deliberately cancelled pre-test attempt, not a compiler/test failure and not acceptance of any new native regression. The expanded neutral-only route already passed independently.

At 01:07 UTC a second one-second sample confirms Pack Cargo59072 is also waiting in the same prebuild exclusive-lock call. Root then queued its broader FEM run behind the Pack caller run: TERM was sent only to verified owned facade62586 and child Cargo62941 after 30 minutes with no native tests. Session25862 ended; its story/oracle/strict-TS evidence remains valid, but native acceptance is absent. The wrapper was stopped with the child so its catch-and-continue loop could not launch a second Cargo package. No foreign process, lock file, cache setting or cache content was modified. Both FEM native routes must be restarted serially after current caller work advances.
