# NodeGraph Consumer Native Queue Review

The ten-consumer route still selected the removed Generation2d node_graph_viewport_sets_camera test. Root changed that entry to the current every_command_round_trips_through_text_and_binary law. The dedicated Generation2d window-owner native route remains responsible for exact instance routing/render/reopen; the consumer route verifies its typed wire ABI.

The route now submits all ten packages in one Cargo test invocation, with each existing feature qualified by its package name, the union of the existing test-name filters and serial test execution. This avoids ten separate Cargo acquisitions and rebuilding dependencies for changing feature sets. No package, feature or declared consumer law was removed. Actual per-package selected test counts must be read before acceptance; a zero-test package is a failure of coverage, not a pass.

The change remains launcher-visible through the existing root/ticket node-graph-viewport-ownership-consumers-native target. Native execution awaits shared API coherence and the active Architect/Generation2d owner changes. No new script or launch entry was needed.
