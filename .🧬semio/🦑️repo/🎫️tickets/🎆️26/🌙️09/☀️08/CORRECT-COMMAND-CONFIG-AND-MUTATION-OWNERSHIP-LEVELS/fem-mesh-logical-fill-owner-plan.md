# FEM Mesh Logical Fill Owner Plan

Current native4 is accepted: 4/4 tests passed, 962 filtered, .03s execution, 18m6s Nx, normal2MiB stack. All three pre-append refusal cases preserve pending input and exact allocations: points504/504/504 admitted/retained/released; closing-edge368/368/368; pending-edge368/368/368. The prior reservation and cancellation laws remain green in the same run. Evidence: generated/fem-mesh-preparation-fill-native-4.log in the ticket generated folder.

The preparation owner must carry the logical maxima it enforces: one shared points/lookup maximum and the checked constraint maximum derived from triangle capacity. Parent-only checks after mutation cannot prevent backing growth. The cold unbounded constructor remains explicitly unbounded; each finite owner ceiling must be enforced independently.

Three strict neutral cases now cover the fourth distinct point, a fourth polygon-closing edge and a fourth pending edge in a five-point polygon. The points/lookup pair must be refused together before either append. A pending index must remain present until its required edge capacity is known to fit; bounds, previous/first indices and segment/edge cursors remain unchanged on refusal. Polygon closure must check before appending or advancing the polygon cursor.

Preparation errors transition the parent to the existing sticky failed stage. Remove the post-append parent length check once the preparation owner enforces admission. Overflow in finite triangle-to-constraint multiplication is rejected before constructing or allocating that owner. Denied opportunities must preserve all pending and backing witnesses. Every observed case closes before final test assertions; retained, admitted and released physical byte totals must agree.

The registered neutral route passes in4.1s. Native1 reproduces all three violations on2MiB: the point case appends a fourth point and grows504 admitted bytes to624 retained bytes; closing-edge and pending-edge cases grow368 admitted bytes to416 retained bytes. All three retire their actual backing before assertion; the overall selection is3/4 passing. The production preparation owner now checks logical capacity before the paired append, pending-index consumption and closing-edge append. Native2 stopped before tests on the concurrent UI reservation API transition; current source has the completed Result/census declarations and native3 is running. Empty mesh fault detail remains a separate publication gap.
