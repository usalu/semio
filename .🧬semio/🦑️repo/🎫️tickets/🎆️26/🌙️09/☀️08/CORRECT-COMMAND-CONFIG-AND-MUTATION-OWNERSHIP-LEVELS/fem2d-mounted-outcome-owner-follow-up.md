# FEM2D Mounted Outcome Owner Follow-Up

Root followed every production PcgJobConstruction call while preparing the RHS-norm correction. FEM3D supplies mounted assembled RHS values. FEM2D's mounted BuildCsr stage still calls the constructor which fabricates a unit RHS. This is independent of the retained constructor's wrong norm for supplied RHS values; correcting that norm will not establish correct FEM2D load assembly.

The current FEM2D mounted session is at `✏️s/🔌️plugins/🏗️fem/🗿️artifacts/◻️2d/🏅️standards/🔖️1/🪆️subsets/🌐️any/✏️editor/🧵️session/🦀️.rs`. Its BuildCsr branch transfers only the matrix into PcgJobConstruction. The source contains no retained RHS assembly owner; the load matches found elsewhere in this file concern snapshot admission and visual loads. Actual document-load numerical assembly therefore needs a separate trace and implementation, using the proper shared assembly/numerical owner instead of defining a second renderer-side load model.

The same source still has synchronous child-outcome bridges: `take_retained_payload` allocates a full Vec, iterates every payload page, then calls `close_retained_payload`, which loops to terminal with usize::MAX. Graph, mesh, assembly and PCG branches call those helpers in the parent step. They do not decode the old PCG wire format; visual output reads the actual job's scalar/progress views. Therefore the PCG codec migration does not require a compatibility decoder here, but it also does not fix these parent's allocation/cancellation/retirement boundaries.

The required correction must retain the complete child StepOutcome, then consume/copy/transfer/retire only caller-granted work. Reuse or extract the accepted FEM3D child-outcome ownership primitive at a domain-neutral shared numerical boundary after checking its actual dependencies. No child state/output/fault owner may be synchronously dropped during an interrupted parent step. Preserve the exact parent/operation generation, pending visual transfer and first fault.

Acceptance must include zero/expired/cancel at every output page and owner transition, exact physical allocation/release, actual assembled RHS numerical comparison for a non-unit load and complete FEM2D terminal close on the normal 2 MiB stack. Prior FEM3D acceptance and focused PCG codec tests do not establish this FEM2D parent behavior. This follow-up made no production edits and ran no FEM2D parent runtime test.
