# Terrain Native Number Oracle

Native contract run coherent5 reaches runtime and reports only a representation mismatch at exaggeration: actual serde_json Number(3.0), expected Number(3). These are the same JSON number under the language-neutral schema and TypeScript oracle. The neutral fixture now spells the declared floating-point exaggeration values with decimal notation so serde_json's representation-sensitive equality tests semantic state consistently. No domain behavior or expected numeric values changed. Native rerun pending.
