# Retained Window Input Replacement Native Audit

SDK recursive native run 15 reached runtime and failed the pre-existing retained_window_input_replacement_rejects_old_authority_and_publication law at pending.terminal_is_empty(). The replacement registry correctly begins close when document generations differ. Current publication fixture owners now use the shared production ephemeral task preparation lifecycle; this differs from earlier fixture implementations. A fresh registered retained-window-input run is queued to establish behavior on the coherent source before editing its lifetime semantics.

## Sparse Registry Diagnosis And Correction

Diagnostic18 proves this is finite empty-capacity scanning: after releasing preparation, turns7 through1023 report one cursor visit but no released bytes. The Store registry scanned every unoccupied entry to wrap its1024slot cursor. It now consults its fixed16word occupancy bitmap and visits one occupied slot per call, preserving fairness and zero-allocation ownership. New neutral/native cases cover a returned entry before the cursor, wraparound and a live entry preceding returned entries. The TypeScript oracle independently applies JSONPatch to an occupied-slot model with Ajv validation. Native/runtime reruns pending; temporary turn diagnostics remain until that proof.
# Sparse Read Retirement Native Checkpoint

SDK `retained-window-replacement-native-20.log` now passes the actual rejected-authority/publication test on the normal configured stack: one test passed, zero failed. Its runtime log confirms both concrete windows reset, old publications are fenced, and every owner retires under one-item grants. The temporary per-turn diagnostic is removed; the law's final `[DEBUG]` evidence remains. This closes the sparse read retirement regression, not the separate recursive archive/candidate task.

The registered `snapshot-read-retirement-native` third run passed the Ajv/JSON Patch oracle, strict TypeScript, and one native test covering all three neutral cases. The initial native test incorrectly dropped raw lease guards; it now uses public `SnapshotRead` owners so actual Drop returns the exact lease. Native run2 stopped during an in-progress archive helper edit, before tests. Run3 logged the successful sparse-slot, wrap, and live-read fairness law.

The original SDK rejected-publication law is still pending. Run19b stopped before tests because the canonical generated IconName input is absent; no passing SDK retirement result is claimed yet.
