# FEM Mesh Preparation Ownership

## Current Acceptance

Native3 passes3/3 with960 filtered in0.03s, exit0 through the registered Nx route in6m06s on a2MiB stack. All four reservation/refusal cases preserve exact stages and backing under denied opportunities and conserve admitted/released bytes:704/704,0/0,3200/3200 and320/320. The four cancellation cuts conserve0/0,128/128,320/320 and704/704; the existing mounted preparation/publication/replay/close law also passes. Evidence: [fem-mesh-preparation-native-3.log](🗑️generated/fem-mesh-preparation-native-3.log). The reservation, handoff and partial-owner cancellation slice is accepted. Logical-fill admission, meaningful fault-detail publication and insertion workspace ownership remain required follow-ups described below. Historical entries preserve the test-first counterexamples.

Root is implementing the bounded preparation lifecycle in the shared FEM mesh engine. A strict schema and four neutral cases precede production changes. The cases cover three separate reservations and refusal at the points, lookup-index, or constraints boundary. The independent JSONPatch oracle evaluates both 32-bit and 64-bit pointer-width layouts.

The native law drives the actual mounted MeshJob through preparation. It records every allocation transition, preserves the exact cursor/backing under zero fuel, expired deadline, and cancellation, checks handle identity during points/constraints handoff, and accounts for the lookup index through exact bounded close. All observed cases close before their final assertions, including a diagnostic exact larger grant needed to safely retire a legacy oversized allocation during the red test.

Production is unchanged at this point. Source review found the current single-step three-reserve burst and the successful handoff dropping preparation.point_indices. Insertion workspace reservation and retirement remain a separate open slice; this change does not claim to fix them or explain the FEM3D native29 wall-time failure.

Registered routes are workspace:fem-mesh-preparation-owners and workspace:fem-mesh-preparation-owners-native, with launch order 311.229 and 311.230. Native validation is pending shared catalog source coherence.

The neutral route passed in 7.0 seconds: all four cases, both pointer widths, strict Ajv and strict TypeScript. Rustfmt parsed the actual native law. Evidence: [fem-mesh-preparation-neutral-1.log](🗑️generated/fem-mesh-preparation-neutral-1.log). Native has not run yet.

The red native route completed in4m52s with1/2 laws passing and960 filtered. The new actual law failed as intended; the existing mounted preparation/replay/publication/close law passed. All four new cases executed and closed before assertions. The success case allocated all three owners in one opportunity, admitted704 bytes and released512: the192-byte lookup index disappeared at handoff. The three refusal cases still allocated all three owners (10664,8384 and14720 bytes); their complete diagnostic cleanup needed exact grants larger than4096. Evidence: [fem-mesh-preparation-native-1.log](🗑️generated/fem-mesh-preparation-native-1.log).

Production now reserves points, lookup index and constraints in separate named stages. Each reservation checks requested bytes before allocation and retains any actual backing on failure; the failure stage is sticky. Handoff transfers only points/constraints and keeps the lookup index in the preparation owner until governed job close. The green rerun is pending.

Native2 executed3 laws: the new four-cut cancellation law and existing mounted lifecycle law pass. The main law logs the correct reservation sequence and exact conserved bytes for all four cases, including704/704 for successful handoff. Its final assertion failed because the added repeat-fault check assumed a text payload, while the existing MeshJob::fail deliberately returns an empty retained Fault payload. The assertion now compares the actual first and repeated payloads and unchanged failure stage/backings. This accepts neither meaningful fault-detail publication nor a new error protocol: retaining/publishing mesh fault details is separately open. Native3 will rerun that corrected assertion.

Follow-up source boundary: preparation.advance_pending_point pushes into points and point_indices before the parent checks maximum_points; accept_pending_index and polygon closure similarly push a constraint before the parent checks its logical ceiling. Thus a later generated point/edge can still grow an admitted Vec before refusal. This is a separate actual logical-fill boundary, not covered by the reservation/handoff cases. Its next law must use a four-point domain with maximumPoints3 and, separately, maximumTriangles1 (three constraint slots), prove the pending input remains unchanged before the fourth append, and retain exact pointers/capacities through sticky rejection and close. Preparation must receive its own logical limits and refuse before either paired point/index append or boundary-edge append. The dynamic cold route remains separate; do not use a truncate-after-growth repair.

The same strict fixture/schema now contains three logical-fill cases: fourth distinct point, fourth closing edge, and fourth pending edge in a five-point polygon. Their independent JSONPatch/strictTS route passes in4.1s, [fem-mesh-preparation-fill-neutral-1.log](🗑️generated/fem-mesh-preparation-fill-neutral-1.log). A fourth native law now drives those actual mounted inputs, checks complete pending-state/backing witnesses under zero/expiry/cancel and sticky refusal, and closes every case before its final assertion. Its red run is pending the Pack value/record-body API cutover; no logical-fill production fix has been applied yet.
