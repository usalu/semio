# Host Ownership Native Review

All four existing host laws passed on RUST_MIN_STACK=2097152 through the registered host-ownership-native-test route, exit0 in17m47s. Concrete-window round-trip, exact concrete-window context-menu resolution, canonical OS preference projection parity and coverage of every wired OS setting each passed1/1 with506 filtered; their runtimes were0.02s,0.01s,0.01s and0.00s. Evidence: [host-ownership-native-resume-1.log](🗑️generated/host-ownership-native-resume-1.log). These selected laws do not prove the separately open Home native panel reducer.

The permanent root/ticket script route now batches all four filters into one serial native executable invocation, avoiding repeated Cargo acquisition and recompilation between laws. The completed invocation retained the previously loaded four-call script; no unrelated process was interrupted. No renderer production file changed for these checks.
