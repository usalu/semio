# Native Compilation Follow-up

The Note full run and Block second run stopped before tests on shared compile errors. Current OwnerRef has one correct derive import and the member-open close path uses the corrected inherent method. These failed runs do not count as runtime evidence.
\n
Map native1 reached test execution and failed its foreign-field rejection assertion, unlike the earlier compile-only failures. Map/Terrain closed-record derives were corrected; Map native2 queued. Terrain2/Flow2/Program1/CAD1 ran their TypeScript/oracle phases successfully and are queued on cargo-trinity. The shared plugin registry compile surface passed in Map's compile before the native decoder assertion failed. Full SDK recursive replacement is still under implementation.
