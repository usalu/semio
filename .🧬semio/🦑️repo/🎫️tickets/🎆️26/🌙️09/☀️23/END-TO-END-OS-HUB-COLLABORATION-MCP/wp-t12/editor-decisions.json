{
 "0": {
  "summary": "it holds the 3d model window's camera pose (position, target, zoom).",
  "rationale": "`EnergyModelWindowConfigMutation` is the config state lane of `s.energy.model`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/🧊️model/🎚️config surface: it holds the 3d model window's camera pose (position, target, zoom). This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-camera`) changes exactly its own part of `EnergyModelWindowConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`EnergyModelWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "1": {
  "summary": "it holds the simulation settings (zone and system timesteps, warm-up days) and the per-surface result field the 3d window colours by.",
  "rationale": "`EnergyModelConfigMutation` is the config state lane of `s.energy.model`'s ✏️editor/🎚️config surface: it holds the simulation settings (zone and system timesteps, warm-up days) and the per-surface result field the 3d window colours by. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`change-simulation-settings`, `change-result-field`) changes exactly its own part of `EnergyModelConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`EnergyModelConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "2": {
  "summary": "it holds the viewer's 3d model window camera pose (position, target, zoom).",
  "rationale": "`EnergyModelViewerWindowConfigMutation` is the config state lane of `s.energy.model`'s 👁️viewer/🎭️modes/👁️view/🪟️windows/🧊️model/🎚️config surface: it holds the viewer's 3d model window camera pose (position, target, zoom). This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-camera`) changes exactly its own part of `EnergyModelViewerWindowConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`EnergyModelViewerWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "3": {
  "summary": "it holds the host-pushed block-kind contribution list (`contributionsJson`) that hot-swaps `playbook.blockKind` installs.",
  "rationale": "`PlaybookConfigMutation` is the config state lane of `s.playbook.playbook`'s ✏️editor/🎚️config surface: it holds the host-pushed block-kind contribution list (`contributionsJson`) that hot-swaps `playbook.blockKind` installs. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-config`, `set-contributions`) changes exactly its own part of `PlaybookConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`PlaybookConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "4": {
  "summary": "it holds the results window's playback clock (phase and direction), or none while playback is stopped.",
  "rationale": "`Fem3dResultsWindowTransientMutation` is the transient state lane of `s.fem.3d`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/📊️results/🫧️transient surface: it holds the results window's playback clock (phase and direction), or none while playback is stopped. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-playback-clock`) changes exactly its own part of `Fem3dResultsWindowTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`Fem3dResultsWindowTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "5": {
  "summary": "it holds the equation graph window's camera (x, y, zoom).",
  "rationale": "`EquationGraphWindowConfigMutation` is the config state lane of `None`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/🕸️graph/🎚️config surface: it holds the equation graph window's camera (x, y, zoom). This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-camera`) changes exactly its own part of `EquationGraphWindowConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`EquationGraphWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "6": {
  "summary": "it holds the shot selection and live camera this collaborator broadcasts to peers.",
  "rationale": "`ShootingPresenceMutation` is the presence state lane of `s.shooting.shooting`'s ✏️editor/👥️presence surface: it holds the shot selection and live camera this collaborator broadcasts to peers. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-presence`) changes exactly its own part of `ShootingPresence`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "y-protocols (awareness)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Broadcasts an opaque per-client awareness object; it has no typed presence record, no replace-presence mutation and no inverse to compare against."
   },
   {
    "package": "liveblocks (presence)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Hosted per-user presence objects merged by key; it neither models this surface's presence record nor defines a mutation vocabulary with inverses, and it is a cloud service this local-first repository does not depend on."
   }
  ],
  "whyNone": "`ShootingPresence` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "7": {
  "summary": "it holds the default shot and asset formats, the shot selection, the centre-model toggle, the viewport fit revision, the camera-label draft and the live camera.",
  "rationale": "`ShootingConfigMutation` is the config state lane of `s.shooting.shooting`'s ✏️editor/🎚️config surface: it holds the default shot and asset formats, the shot selection, the centre-model toggle, the viewport fit revision, the camera-label draft and the live camera. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-shot-selection`, `set-camera`, `set-center-model`, `set-camera-draft-label`, `replace-config`, `set-fit-revision`, `set-defaults`) changes exactly its own part of `ShootingConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`ShootingConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "8": {
  "summary": "it holds the active register, the adjacency-kind filter and the graph camera this collaborator broadcasts to peers.",
  "rationale": "`ArchitectPresenceMutation` is the presence state lane of `s.architect.program`'s ✏️editor/👥️presence surface: it holds the active register, the adjacency-kind filter and the graph camera this collaborator broadcasts to peers. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-presence`) changes exactly its own part of `ArchitectPresence`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "y-protocols (awareness)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Broadcasts an opaque per-client awareness object; it has no typed presence record, no replace-presence mutation and no inverse to compare against."
   },
   {
    "package": "liveblocks (presence)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Hosted per-user presence objects merged by key; it neither models this surface's presence record nor defines a mutation vocabulary with inverses, and it is a cloud service this local-first repository does not depend on."
   }
  ],
  "whyNone": "`ArchitectPresence` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "9": {
  "summary": "it holds the search query and history, the last action result and the last analysis result.",
  "rationale": "`ArchitectConfigMutation` is the config state lane of `s.architect.program`'s ✏️editor/🎚️config surface: it holds the search query and history, the last action result and the last analysis result. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-config`) changes exactly its own part of `ArchitectConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`ArchitectConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "10": {
  "summary": "it holds the host-declared contribution list (`contributionsJson`) projected into the forms app.",
  "rationale": "`FormsConfigMutation` is the config state lane of `s.forms.forms`'s ✏️editor/🎚️config surface: it holds the host-declared contribution list (`contributionsJson`) projected into the forms app. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-config`, `set-contributions`) changes exactly its own part of `FormsConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`FormsConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "11": {
  "summary": "it holds the canvas camera (x, y, zoom) this collaborator broadcasts to peers.",
  "rationale": "`NotePresenceMutation` is the presence state lane of `None`'s ✏️editor/👥️presence surface: it holds the canvas camera (x, y, zoom) this collaborator broadcasts to peers. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-presence`) changes exactly its own part of `NotePresence`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "y-protocols (awareness)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Broadcasts an opaque per-client awareness object; it has no typed presence record, no replace-presence mutation and no inverse to compare against."
   },
   {
    "package": "liveblocks (presence)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Hosted per-user presence objects merged by key; it neither models this surface's presence record nor defines a mutation vocabulary with inverses, and it is a cloud service this local-first repository does not depend on."
   }
  ],
  "whyNone": "`NotePresence` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "12": {
  "summary": "it holds the last run's output scope and the host-pushed module contribution list.",
  "rationale": "`ImperativeConfigMutation` is the config state lane of `s.imperative.procedure`'s ✏️editor/🎚️config surface: it holds the last run's output scope and the host-pushed module contribution list. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-run-output`, `replace-config`, `set-contributions`) changes exactly its own part of `ImperativeConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`ImperativeConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "13": {
  "summary": "it holds the last solve's slot assignments and whether it ended in a contradiction.",
  "rationale": "`Wfc2dTransientMutation` is the transient state lane of `s.wfc.wfc2d`'s ✏️editor/🫧️transient surface: it holds the last solve's slot assignments and whether it ended in a contradiction. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-solve`) changes exactly its own part of `Wfc2dTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`Wfc2dTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "14": {
  "summary": "it holds this pane's camera (x, y, zoom) and the tile a pin gesture assigns.",
  "rationale": "`Wfc2dConfigMutation` is the config state lane of `s.wfc.wfc2d`'s ✏️editor/🎚️config surface: it holds this pane's camera (x, y, zoom) and the tile a pin gesture assigns. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`change-active-tile`, `change-camera`, `replace-config`) changes exactly its own part of `Wfc2dConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`Wfc2dConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "15": {
  "summary": "it holds the last solve's palette-index pixels, its output extent and whether it ended in a contradiction.",
  "rationale": "`BitmapTransientMutation` is the transient state lane of `s.wfc.bitmap`'s ✏️editor/🫧️transient surface: it holds the last solve's palette-index pixels, its output extent and whether it ended in a contradiction. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-solve`) changes exactly its own part of `BitmapTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`BitmapTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "16": {
  "summary": "it holds the last solve's slot assignments and whether it ended in a contradiction.",
  "rationale": "`Wfc3dTransientMutation` is the transient state lane of `s.wfc.wfc3d`'s ✏️editor/🫧️transient surface: it holds the last solve's slot assignments and whether it ended in a contradiction. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-solve`) changes exactly its own part of `Wfc3dTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`Wfc3dTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "17": {
  "summary": "it holds this pane's camera (x, y, zoom) and the tile a pin gesture assigns.",
  "rationale": "`Wfc3dConfigMutation` is the config state lane of `s.wfc.wfc3d`'s ✏️editor/🎚️config surface: it holds this pane's camera (x, y, zoom) and the tile a pin gesture assigns. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`change-active-tile`, `change-camera`, `replace-config`) changes exactly its own part of `Wfc3dConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`Wfc3dConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "18": {
  "summary": "it holds per-layer visibility and stroke scale, the map camera, the render mode, the vector style and the LOD tier.",
  "rationale": "`MapWindowConfigMutation` is the config state lane of `s.gis.gismap`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/🗺️map/🎚️config surface: it holds per-layer visibility and stroke scale, the map camera, the render mode, the vector style and the LOD tier. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-camera`, `set-vector-style`, `set-layer-visibility`, `set-layer-stroke-scale`, `set-lod-mode`, `set-render-mode`) changes exactly its own part of `MapWindowConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`MapWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "19": {
  "summary": "it holds the text of the last generation preview, or none before the first one.",
  "rationale": "`Generation2dTransientMutation` is the transient state lane of `s.procedural.generation2d`'s ✏️editor/🫧️transient surface: it holds the text of the last generation preview, or none before the first one. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-generation-preview`) changes exactly its own part of `Generation2dTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`Generation2dTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "20": {
  "summary": "it holds the text of the last generation preview, or none before the first one.",
  "rationale": "`Generation3dTransientMutation` is the transient state lane of `s.procedural.generation3d`'s ✏️editor/🫧️transient surface: it holds the text of the last generation preview, or none before the first one. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-generation-preview`) changes exactly its own part of `Generation3dTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`Generation3dTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "21": {
  "summary": "it holds the LOD tier, the preview shading mode, the flow-graph canvas camera, the 3d preview camera, the sun and the selected generation.",
  "rationale": "`Generation3dConfigMutation` is the config state lane of `s.procedural.generation3d`'s ✏️editor/🎚️config surface: it holds the LOD tier, the preview shading mode, the flow-graph canvas camera, the 3d preview camera, the sun and the selected generation. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-snapshot`, `set-sun`, `set-show-mode`, `set-preview-camera`, `set-lod-mode`, `set-camera`, `set-selected-generation`) changes exactly its own part of `Generation3dConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`Generation3dConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "22": {
  "summary": "it holds the flow evaluation of the current document the preview window renders, or none before the first evaluation.",
  "rationale": "`Generation3dViewTransientMutation` is the transient state lane of `s.procedural.generation3d`'s 👁️viewer/🫧️transient surface: it holds the flow evaluation of the current document the preview window renders, or none before the first evaluation. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-preview-eval`) changes exactly its own part of `Generation3dViewTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`Generation3dViewTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "23": {
  "summary": "it holds the 3d preview camera and shading mode this viewer broadcasts to peers.",
  "rationale": "`Generation3dViewPresenceMutation` is the presence state lane of `s.procedural.generation3d`'s 👁️viewer/👥️presence surface: it holds the 3d preview camera and shading mode this viewer broadcasts to peers. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-show-mode`, `set-preview-camera`) changes exactly its own part of `Generation3dViewPresence`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "y-protocols (awareness)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Broadcasts an opaque per-client awareness object; it has no typed presence record, no replace-presence mutation and no inverse to compare against."
   },
   {
    "package": "liveblocks (presence)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Hosted per-user presence objects merged by key; it neither models this surface's presence record nor defines a mutation vocabulary with inverses, and it is a cloud service this local-first repository does not depend on."
   }
  ],
  "whyNone": "`Generation3dViewPresence` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "24": {
  "summary": "it holds the LOD tier, the preview shading mode, the 3d preview camera, the sun and which bundled example the read-only surface shows.",
  "rationale": "`Generation3dViewConfigMutation` is the config state lane of `s.procedural.generation3d`'s 👁️viewer/🎚️config surface: it holds the LOD tier, the preview shading mode, the 3d preview camera, the sun and which bundled example the read-only surface shows. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-sun`, `set-active-example`, `set-show-mode`, `set-preview-camera`, `set-lod-mode`) changes exactly its own part of `Generation3dViewConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`Generation3dViewConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "25": {
  "summary": "it holds nothing yet: the presentation editor broadcasts no app-specific presence, so the record is empty.",
  "rationale": "`PresentationPresenceMutation` is the presence state lane of `s.animate.presentation`'s ✏️editor/👥️presence surface: it holds nothing yet: the presentation editor broadcasts no app-specific presence, so the record is empty. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-presence`) changes exactly its own part of `PresentationPresence`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "y-protocols (awareness)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Broadcasts an opaque per-client awareness object; it has no typed presence record, no replace-presence mutation and no inverse to compare against."
   },
   {
    "package": "liveblocks (presence)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Hosted per-user presence objects merged by key; it neither models this surface's presence record nor defines a mutation vocabulary with inverses, and it is a cloud service this local-first repository does not depend on."
   }
  ],
  "whyNone": "`PresentationPresence` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "26": {
  "summary": "it holds the engagement input text of the presentation editor.",
  "rationale": "`PresentationConfigMutation` is the config state lane of `s.animate.presentation`'s ✏️editor/🎚️config surface: it holds the engagement input text of the presentation editor. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-engagement-input`) changes exactly its own part of `PresentationConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`PresentationConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "27": {
  "summary": "it holds the editor selection, the lint generation counter and the engagement input of the main window.",
  "rationale": "`WriterMainWindowTransientMutation` is the transient state lane of `s.writer.writer`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/✒️main/🫧️transient surface: it holds the editor selection, the lint generation counter and the engagement input of the main window. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-engagement-input`, `set-editor-selection`, `set-lint-generation`) changes exactly its own part of `WriterMainWindowTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`WriterMainWindowTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "28": {
  "summary": "it holds the main window's camera and editor settings.",
  "rationale": "`WriterMainWindowConfigMutation` is the config state lane of `s.writer.writer`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/✒️main/🎚️config surface: it holds the main window's camera and editor settings. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-editor-settings`, `set-camera`) changes exactly its own part of `WriterMainWindowConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`WriterMainWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "29": {
  "summary": "it holds the in-progress node drag on the canvas (node, start and last pointer position, zoom).",
  "rationale": "`WiresCanvasTransientMutation` is the transient state lane of `s.reasoning.wires`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/🕸️canvas/🫧️transient surface: it holds the in-progress node drag on the canvas (node, start and last pointer position, zoom). This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-drag`) changes exactly its own part of `WiresCanvasTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`WiresCanvasTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "30": {
  "summary": "it holds the canvas window's camera.",
  "rationale": "`WiresCanvasWindowConfigMutation` is the config state lane of `s.reasoning.wires`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/🕸️canvas/🎚️config surface: it holds the canvas window's camera. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-camera`) changes exactly its own part of `WiresCanvasWindowConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`WiresCanvasWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "31": {
  "summary": "it holds the graph camera (x, y, zoom) this collaborator broadcasts to peers.",
  "rationale": "`DagPresenceMutation` is the presence state lane of `s.dag.dag`'s ✏️editor/👥️presence surface: it holds the graph camera (x, y, zoom) this collaborator broadcasts to peers. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-presence`) changes exactly its own part of `DagPresence`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "y-protocols (awareness)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Broadcasts an opaque per-client awareness object; it has no typed presence record, no replace-presence mutation and no inverse to compare against."
   },
   {
    "package": "liveblocks (presence)",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "Hosted per-user presence objects merged by key; it neither models this surface's presence record nor defines a mutation vocabulary with inverses, and it is a cloud service this local-first repository does not depend on."
   }
  ],
  "whyNone": "`DagPresence` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "32": {
  "summary": "it holds the graph camera (x, y, zoom).",
  "rationale": "`DagConfigMutation` is the config state lane of `s.dag.dag`'s ✏️editor/🎚️config surface: it holds the graph camera (x, y, zoom). This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`change-camera`, `replace-config`) changes exactly its own part of `DagConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`DagConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "33": {
  "summary": "it holds the brush preview under the pointer in the 3d world window, or none.",
  "rationale": "`Block3dWorldWindowTransientMutation` is the transient state lane of `s.block.3d`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/🌐️world/🫧️transient surface: it holds the brush preview under the pointer in the 3d world window, or none. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-brush-preview`) changes exactly its own part of `Block3dWorldWindowTransient`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "zustand",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "An in-memory state container with arbitrary setter functions; it has no typed transient record, no per-kind mutation vocabulary and no computed inverse."
   },
   {
    "package": "im",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Persistent immutable collections; they version values structurally but define no mutation kinds, no-op guards or inverses over this surface's record."
   }
  ],
  "whyNone": "`Block3dWorldWindowTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "39": {
  "summary": "it holds the read-only viewer map window's camera (x, y, zoom).",
  "rationale": "`GisMapViewerWindowConfigMutation` is the config state lane of `s.gis.gismap`'s 👁️viewer/🎭️modes/👁️view/🪟️windows/🗺️map/🎚️config surface: it holds the read-only viewer map window's camera (x, y, zoom). This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-camera`) changes exactly its own part of `GisMapViewerWindowConfig`, that a kind declaring `no-op` leaves a record that already holds its value untouched with a warned `mutation.no-op`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed config record, its per-kind setters, their no-op guard or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed config mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`GisMapViewerWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change, when it is a no-op, or what its inverse is."
 },
 "35": {
  "summary": "it holds one jack graph window's camera and LOD mode.",
  "rationale": "`JackGraphWindowConfigMutation` is the config state lane of `s.trinity.jack`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/🌐️graph/🎚️config surface: it holds one jack graph window's camera and LOD mode. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-camera`, `set-lod-mode`) changes exactly its own part of `JackGraphWindowConfig`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed state record, its per-kind setters or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed state mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`JackGraphWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change or what its inverse is."
 },
 "36": {
  "summary": "it holds one results window's last query execution id, result and error.",
  "rationale": "`JackResultsWindowTransientMutation` is the transient state lane of `s.trinity.jack`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/📊️results/🫧️transient surface: it holds one results window's last query execution id, result and error. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`replace-query-result`) changes exactly its own part of `JackResultsWindowTransient`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed state record, its per-kind setters or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed state mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`JackResultsWindowTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change or what its inverse is."
 },
 "37": {
  "summary": "it holds one query editor window's text selection.",
  "rationale": "`JackEditorWindowTransientMutation` is the transient state lane of `s.trinity.jack`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/📝️editor/🫧️transient surface: it holds one query editor window's text selection. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-editor-selection`) changes exactly its own part of `JackEditorWindowTransient`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed state record, its per-kind setters or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed state mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`JackEditorWindowTransient` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change or what its inverse is."
 },
 "38": {
  "summary": "it holds one query editor window's jack query text.",
  "rationale": "`JackEditorWindowConfigMutation` is the config state lane of `s.trinity.jack`'s ✏️editor/🎭️modes/✏️edit/🪟️windows/📝️editor/🎚️config surface: it holds one query editor window's jack query text. This is this repository's own editor state record, not a published format, so no third party implements it. What is under test is that each kind (`set-query`) changes exactly its own part of `JackEditorWindowConfig`, and that every kind's computed inverse, read off the pre-mutation record, restores it exactly. Confidence comes from one committed, handcrafted (before, mutation, after, diff, outcome) vector per kind and declared outcome class under `../🧫️fixtures/`, exercised end to end through the production dispatch bridge, and from the inverse law as a metamorphic property.",
  "ecosystems": [
   "npm",
   "crates.io"
  ],
  "candidates": [
   {
    "package": "electron-store",
    "ecosystem": "npm",
    "verdict": "cannot-express-the-mutation",
    "reason": "A persisted key-value store for desktop settings; it has no model of this surface's typed state record, its per-kind setters or the inverse each kind computes."
   },
   {
    "package": "confy",
    "ecosystem": "crates.io",
    "verdict": "cannot-express-the-mutation",
    "reason": "Loads and stores a whole configuration struct on disk; it defines no typed state mutations and neither their no-op nor their inverse behaviour."
   }
  ],
  "whyNone": "`JackEditorWindowConfig` is a state record defined by this repository with its own typed mutation vocabulary; generic state containers can hold its values but cannot adjudicate what each kind must change or what its inverse is."
 }
}