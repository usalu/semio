semio-framework-os-kernel — LD item 1: store announces only the ops a coalesced batched gesture appended (guest store re-announce fix; unblocks collab-e2e writer typing). Every guest links it.
semio-framework-replication — LD item 1: new `mutation_envelopes_from_edit_since` (range encoder used by the kernel store). Every guest links it.
