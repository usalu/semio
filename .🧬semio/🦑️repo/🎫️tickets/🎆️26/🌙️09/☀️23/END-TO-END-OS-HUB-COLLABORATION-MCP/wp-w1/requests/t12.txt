T12 (session 11) guest requests — 2026-09-25 03:28
Edits that landed after W2's rebuild started (all compile-checked; native --lib --tests green):
1. crates semio-s-plugin-architect (semio-s-artifact-architect-program) and semio-s-plugin-imperative
   (semio-s-artifact-imperative-procedure), at 02:5x: whole-record state lanes answered an unchanged value with
   `MutationOutcome::empty()`, and because their diff type IS the record, the store applied that default diff and WIPED
   architect's presence/config and imperative's config to defaults on any identical re-apply (measured by the T12
   vector harness). The 5 leaves now return the unchanged record with the same warned `mutation.no-op`.
   Behavioral fix → please rebuild/restage both guests if they were built before 03:00.
2. crate semio-framework-os-kernel (every guest links it), 02:4x: additive `store::os_store::test_support::
   mutation_report_json::<S, M>` (test bridge, no production caller, no behavior change). No rebuild needed for it
   alone; guests built before/after differ only by this unused function.
Upcoming (will be appended here when landed): per-lane `*_report_json` one-line bridges in ~20 artifact crates
(additive, test-only callers), raster standalone control pool refactor (production path unchanged).
3. 03:4x crate semio-s-plugin-raster (semio-s-artifact-raster-raster): standalone control credits now come from an explicit
   pool (production = the same process pool and capacity as before; only the saturation tests own private pools). No
   behavior change for the guest; rebuild only if convenient. Native lib tests 228/228 ×5.
4. S15 guest fixes (07:2x–07:4x), native-checked, wasm32 batch check pending:
   - semio-s-plugin-sourcing AND semio-s-plugin-demonstrator (both link semio-s-artifact-sourcing-curation):
     curationSetCount's delta/value args are now number args (manifest/descriptor change) → describe + release + restage.
   - semio-s-plugin-norm (semio-s-artifact-norm-contract, -din18599, -en1990): setSnapshot decodes the declared
     `snapshot` JSON → release + restage.
   - semio-s-plugin-procedural (generation2d viewer panicked on open) + raster, process, reasoning, writer, trinity
     (their viewers now retire documents through the artifact's own owner catalogue) → release + restage.
     Pending: draw viewer (same one-line fix) after draw's release .ok.
   Note: the writer viewer edit (one function, compile-checked) landed ~07:45, after batch b's 07:22 START line but
   while batch b was still queued at the wasm mutex (WG7 held it) — batch b compiles it; no mid-build edit.
5. S15 stdio ×9 (07:3x–07:5x) — crate semio-s-plugin-stdio (editor code of semio-s-artifact-stdio-{csv,tsv,txt,md,html,json,xml},
   compiled only under the stdio plugin's `component-app-assembly` feature, so no other component's build is touched):
   `set-cell`/`replace-text`/`set-node` are now app-owned retained routes (roster + Artifact-lane publication contract +
   bounded proof + reducer sharing `handle`'s emit) and each editor owns an artifact one-item publication authority
   (without it the lane failed `interactive-job.publication-authority-missing`, found by the new live laws).
   Native `cargo check -p semio-s-plugin-stdio` green; lib tests of the 7 crates green (9 live kit-verb laws + html
   invalid-text refusal). stdio was already released (.ok) before the edit → re-release + restage stdio. No ABI,
   pack-schema or codec-hash change. wasm32 batch check pending.
6. S15 vcs viewer (07:5x) — crate semio-s-plugin-vcs (semio-s-artifact-vcs-vcs viewer; linked by no other component): the
   read-only history tree painted NOTHING for a document without checkpoints (every freshly opened viewer) because
   TreeWindowKit renders no rows for an empty roster; the document is now the tree's root row (its title, `—` when
   untitled) with the checkpoint forest beneath. Law `a_document_without_checkpoints_still_paints_its_own_row`;
   vcs artifact lib 126/126, `cargo check -p semio-s-plugin-vcs` green → release + restage vcs.
7. S15 gis viewer (08:0x–08:2x) — crate semio-s-plugin-gis (semio-s-artifact-gis-gismap viewer; batch-b components do not
   link it; gis was released .ok before the edit): the viewer's map window now declares the host's `setCamera`
   (Migrated view action with its `camera` arg) and retains the camera per window in a viewer-owned window config
   (`gis.mapviewerwindowcfg`, the energy viewer's pattern: retained route on the WindowConfig lane only, ViewEmit
   untouched). FREEZE NOTE: this ADDS one local-only window-config record + its `set-camera` vocabulary; no existing
   artifact pack schema, codec or guest ABI changes. The viewer descriptor changes (one action) → describe + release
   + restage gis. gismap lib 285/285 incl. the live law `a_dispatched_pan_is_retained_by_its_window_and_rendered_back`.
8. S15 trinity ×2 (08:2x, W2 batch b had already built writer, the only component embedding trinity) — crate
   semio-s-plugin-trinity (semio-s-artifact-trinity-jack / -rewriting; writer embeds both → writer too):
   `patchNodes` now patches the selection when `nodeIds` is empty, splits a comma/space id list, and refuses loudly
   (`mutation.target-missing` / `app.command.invalid-args`) instead of an empty emit; the text-host gesture verbs
   (`textEdit`, `textSelect`) and rewriting's canvas verb `nodeGraphEdit` are `in_palette: false` (off the rail, the
   palette and the context menu's transform group). Descriptor changes (in_palette, optional nodeIds) → describe +
   release + restage trinity AND writer. jack lib 216/216, rewriting lib 160/160.
9. draw viewer (10:2x, W2 idle) — crate semio-s-plugin-draw (semio-s-artifact-draw-drawing viewer): declares the
   drawing's own document-store owner catalogue like the six other viewers (preventive: the viewer law is green with
   and without it on the default document) → release + restage draw with the next batch.
   Raster viewer: a docstring the 07:2x viewer codemod had displaced onto `build_document_store_owners` is back on
   `initial_snapshot` (comment-only).
   wasm32-wasip2 check of all 15 touched guest plugins (stdio sourcing demonstrator norm procedural raster process reasoning writer trinity gis vcs draw architect imperative) through the fleet mutex: EXIT 0, 95 s (10:27, wp-t12/generated/check-s15-wasm-1.txt).
10. remodel (10:4x–11:0x): TEST-ONLY — the 29 worker-step wall-clock laws now admit steps through the runtime's own StepOverrunLedger rule (#[cfg(test)] helper). No guest behaviour change; no rebuild needed.
11. trinity jack (11:xx, before W2's next START release) — crate semio-s-plugin-trinity (semio-s-artifact-trinity-jack; writer
   embeds it): `✏️editor/🫧️transient` re-exports the two existing `*_transient_mutation_report_json` test bridges
   (additive, no production caller). No behaviour change; rebuild only with the next trinity/writer batch.
12. TEST DATA / TEST-ONLY, no guest source (13:0x–13:4x): regenerated plugin `🏭️bridge` test bridges; gisterrain view
   window-config manifest; generation2d subset oracles + re-copied fixtures; step cc6 subset bridge removed; layout
   `rotate-frame` vector (quintet, catalog vector, two case rows, both adapters, a #[cfg(test)] mount in the layout crate
   root); os.config ui-preferences catalog + host case `🎨️mutate-os-config-ui-preferences` (Rust + TS adapters, taxonomy
   member name); remodel `mutate-remodeling-1` rows renamed to their registered scenario ids (feature + both adapter
   lists; python oracle 275/275 at exhaustive).
13. pdf 1.7 base (13:1x–13:4x) — crate semio-s-artifact-stdio-pdf (linked by semio-s-plugin-stdio): TEST-ONLY. A
   #[cfg(test)] mount of `🧬️schema/🧬️mutations/🧪️tests/⚖️lopdf-vectors` (44 laws, all green; crate lib 502/502), 44 lopdf
   fixture pairs, the lopdf generator (`🏭️generator/🔁️codec`, standalone workspace) and the subset oracle manifest
   (44 manifest mutations + 44 fixture manifests). No production code, ABI, pack schema or codec hash touched →
   no stdio rebuild needed for it.
14. TEST-ONLY, no rebuild (13:5x–16:4x): literal outcome copies in 5 `🔬️structural-correspondence` laws settled from the
   leaf declarations (curation, writer, gisterrain, imperative, space — fallout of my 00:41 outcome switch); gis plugin
   surface law `gismap_viewer_never_mutates` restated for the retained-route viewer (energy's form); layout rotate-frame
   test include paths; Python case oracles re-pointed from the retired `<leaf>/🧪️tests/<vector>` paths (layout,
   wires, space, procedure, shooting; procedure also reads the wire's `{owner: null}` root path); os.config identity
   case adapters (TS fixture paths, `email` instead of the retired `token`); test platform fixture rule honours
   `deferredKinds` (+ law).
15. TEST-ONLY (17:0x–17:3x): Python case references re-pointed / aligned — process3d, dag, home, vcs (retired or
   emoji-renamed vector paths; vcs diff members), gismap and jack (example read through `asset://`), writer (`text`
   member; `edit-text` no-op branch stated; feature narrative). No guest source touched.
16. SPACE (18:1x, S15 §12 item (3), blocks G10's human-sees-agent proof + the Space journey) — crate semio-s-plugin-space
   (semio-s-artifact-space-space, `✏️editor/🎮️commands/📇fold-directory-events`): the index document now folds the ONE space
   its directory history names when its snapshot `space_id` is empty (the browser's local-genesis index); a non-empty
   `space_id` still selects that space, and an empty id with two or more folded spaces emits nothing (never guesses).
   Laws `an_index_without_a_space_id_folds_the_one_space_its_history_names` / `…_never_guesses_between_two_spaces`
   (fold laws 5/5). Test harness: the editor unit context binds instance 1 (test-only). No descriptor, pack schema,
   codec or ABI change. native `cargo check -p semio-s-plugin-space --lib` EXIT 0; wasm32-wasip2 check through the fleet
   mutex EXIT 0 (18:0x, s11-t12-captures/space-wasm-1.txt). restage3 built space BEFORE this change → release + restage
   `space` again (its committed hash a75b7a234ecd is stale).

T12 (session 12) — 2026-09-25 23:2x
17. stdio: open-target declarations (T12). crate semio-s-plugin-stdio (editor code of semio-s-artifact-stdio-{txt,tsv,html},
   compiled only under stdio's component-app-assembly; no other component links these editors). Edited at 23:18, AFTER
   stdio's b-warm component-release finished (nx had moved to gis) and before b-publish. The txt, tsv and html editors now
   declare the kind they edit (`.artifact_kind(crate::artifact_kind())`, the writer/draw pattern), so the hub's one rule
   (`app_opens_kind`) answers 6 new open targets (stdio.txt / stdio.tsv / stdio.html × editor + viewer). DESCRIPTOR CHANGE
   (3 apps gain `artifactKinds`) → stdio needs describe + release before `--packages all` (b-publish builds it fresh from
   the tree anyway). ABI-safe: no WIT, export, pack schema, codec hash, closed-actor format or native-codec change; the
   26 native-codec kinds and their receipts are untouched. Laws `the_editor_declares_the_artifact_kind_it_edits` ×3 green
   (editor tests 13/13, 12/12, 11/11); native `cargo check` of the 3 crates with component-app-assembly EXIT 0.
   stdio's committed `🛂️.descriptor.semio` is stale until that describe (its `descriptor_is_fresh` law is red until then).
   NOT ABI-safe, prepared as a post-publish patch set (T12 report): csv, json (×2 subsets), xml (×2), md stay unopenable —
   their kinds are plugin-level native-codec kinds `stdio.<x>` whose dialect is `s.stdio.<x>`; the one rule opens a
   plugin-level kind only through the dialect that names it, so the fix is the kind id = `s.stdio.<x>` (gltf/vcs precedent),
   which changes the native-codec receipts.
17-WITHDRAWN (23:5x): the stdio txt/tsv/html declarations are REVERTED (working tree == HEAD 19:34 for those 6 files), stdio
   is back to exactly what b-warm built. Reason, measured in the hub source: the `local-stdio-gis-open-v1` fence
   (`🔏️trusted-catalog/🦀️.rs` ~1580) requires stdio to carry 26 native codecs and ZERO open targets, and every hub gate law
   in `🌎️hub/📦️packages/🦀️rust/📜️script.ts` publishes `stdio,gis`; a stdio with open targets would also make the bootstrap
   build a closed browser actor for stdio (release component 39 MB, gis's actor sat 121 KB under the 64 MiB bound). All of
   stdio (declarations + canonical kind ids + fence + actor) moves to ONE post-publish patch set. Nothing to do for W2 now.
18. wfc (lane 2, before its build) + demonstrator (rest, not started), 23:4x: the wfc bitmap, grid2d, wfc2d, wfc3d editors and
   the demonstrator playground editor declare the kind they edit (`.artifact_kind(crate::artifact_kind())`, like wfc grid3d)
   → +10 open targets (5 kinds × editor + viewer); both packages already open kinds, so no new actor and no profile fence.
   DESCRIPTOR CHANGE → describe + release wfc and demonstrator before `--packages all`. Laws
   `the_editor_declares_the_artifact_kind_it_edits` ×5 green; editor tests wfc2d 45/45, wfc3d 56/56, playground 12/12,
   bitmap 3 solver tests pass alone (red only under parallel load, pre-existing timing) — ABI-safe.
19. trinity (S15 session-12 FAILs, 00:3x) — crate semio-s-plugin-trinity (semio-s-artifact-trinity-rewriting; writer links
   semio-s-plugin-trinity, so writer too if its component carries the rewriting editor): `addRuleClause` now refuses by name
   what it cannot add (a second WHERE clause, an unknown kind, an undecodable rule: `app.command.invalid-args` /
   `trinity.rewriting.rule-undecodable`) instead of an empty emit, and rewrites ONLY the side it changed (kind=create no
   longer publishes a re-printed `edit-lhs`). Behaviour change, no descriptor/ABI/codec change. rewriting lib 162/162.
   Edited 00:2x while writer's lane was compiling stdio crates (before trinity); checked immediately (EXIT 0). → release +
   restage trinity (and writer) with the next batch. The jack/rewriting "no ↶, undo does nothing" does NOT reproduce
   natively (4 new round-trip laws green, incl. selectAll→patch→clearSelection→undo→redo on the loaded curated example);
   root cause still open (T12 report §S12-2).
20. trinity rewriting (00:5x, after writer's lane finished): the rewriting editor declares the kind it edits
   (`text.rewriting`, `.artifact_kind(crate::artifact_kind())`) → +2 open targets (editor + viewer). DESCRIPTOR CHANGE →
   describe + release trinity (and writer if its component carries the rewriting editor) before `--packages all`.
   `cargo check -p semio-s-artifact-trinity-rewriting --features component-app-assembly --lib` EXIT 0.
21. sequence (00:3x, REST, not started): production JSON moved off `serde_json` onto first-party `pack` JSON (contract
   `testing/dependency` ×2); behaviour-preserving, no descriptor/ABI/codec change; lib 210/210; wasm32 check queued
   through the mutex (pid 57241). Rebuild with the rest batch as usual.
22. note (G10 relay, 01:3x, after note's lane finished): every note verb that reads arguments now DECLARES them
   (`.action_args`: patchBlocks blockIds/field/value, moveBlock blockId/targetRowId/dropPosition, deleteBlock and
   duplicateBlock blockId, setGridSpacing/Subdivisions/Opacity, setSnapGridSpacing, setPencilWidth, setEraserRadius,
   setCameraZoom value) so agents get a real MCP input schema; patchBlocks/moveBlock/deleteBlock/duplicateBlock refuse by
   name (`mutation.target-missing`, `app.command.invalid-args`) instead of an empty emit. DESCRIPTOR CHANGE (args) →
   describe + release note before `--packages all`. Laws: census `every_note_verb_that_reads_arguments_declares_them`
   (red with 11 offenders before, green after), declared-args decode, refusals; note lib 400/400; check EXIT 0.
23. DESCRIBE-ALL needed (measured 03:5x, `test quick` of all 60 plugin/extension packages): 28 packages fail ONLY their
   `descriptor_is_fresh` law — committed `🛂️.descriptor.semio` older than the tree (D1's descriptions, T12's kind/arg
   declarations, peers): writer, mathematical, wfc, procedural, flow, animate, shooting, demonstrator, sequence, fem,
   architect, process, lowpoly, reasoning, norm, playbook, playbook-module-procedural, imperative, remodel, energy,
   trinity, dag, stdio, note, puzzle, block, space, sourcing. 32 packages are fresh and green. After the describe-all,
   T12 re-runs the 28 (chain `wp-t12/test-quick-all.sh`).
