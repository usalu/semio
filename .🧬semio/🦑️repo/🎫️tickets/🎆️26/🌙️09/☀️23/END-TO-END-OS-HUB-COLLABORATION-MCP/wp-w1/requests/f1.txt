f1: semio-framework-os-infinite (canvas text) → rebuild the two BROWSER wasm bundles that link it: nx `@semio-tech/framework-editor-rs:wasm` (pkg/framework_editor*, EditorSession/DagSession) and `@semio-tech/framework-surface-rs:wasm` (🕸️bindings/framework_surface*, MapSession/GraphSession/DagSession/RasterSession). Reason: shaped-label cache (2026-09-26 04:0x, F1-2a) — every label paint/measure re-ran usvg (SVG parse + ttf_parser face parse + rustybuzz shaping) on every frame: trinity query editor 85–92 ms per paint (keydown→paint p50 217 ms), gismap 354 ms per paint (299 ms in append_label). No ABI change (no WIT / owned export / pack schema / codec); host/browser-only code (wasip2 arms unchanged; wasm32 checks queued via the mutex, pid 39813). Plugin guests are unaffected (their wasip2 builds see no new item), so no guest restage is needed for this — only the two browser bundles. Report: 📓️wp-f1.md F1-2a.
f1 (05:2x): WITHDRAWN — F1 built both browser bundles itself in the coordinator's `wasmshort` lane (framework-editor-rs 05:07, framework-surface-rs 05:17, both rc 0, `--skip-nx-cache`), now with the new `devOptimizedCrates: BROWSER_CANVAS_HOT_CRATES` (library `runWasmPackWebBuild` option: `--config profile.dev.package.<crate>.opt-level=3` for the usvg/ttf-parser/rustybuzz/fontdb/… label hot path, dev mode only, scoped to these two bundles). Nothing to do for W2. Note for W2's own later builds of these two targets: pass `--skip-nx-cache` or verify the nx inputs cover the path-dependency `semio-framework-os-infinite` (not verified whether the cargo nx plugin adds it).
f1 (09:4x, POST-PUBLISH landing window — prepared, NOT applied; guest freeze): typing coalescing P0 (coordinator 06:0x). Patch set `.tmp-ticket/wp-f1/patches/typing-coalescing/` (`python3 apply.py --dry-run` clean at 09:4x; F1 applies it itself when the coordinator opens the window, then `cargo test` + wasip2 checks). Guest crates whose staged component changes (need the consolidated restage): `semio-s-artifact-trinity-jack` (text-edit gains coalesce_key "jack-query-typing": from the 65th typed character every keystroke is refused today, measured live) and `semio-s-artifact-vcs-vcs` (text-edit `Emit::amend`). Test-only (no component change): `semio-framework-plugin` (`artifact_app_laws::typing_run` + fixture `🔌️plugin/🧫️fixtures/⌨️typing-run/🔣️.json`), `semio-s-artifact-writer-writer` (law only; writer already coalesces). No ABI change (no WIT / owned export / pack schema / codec).
f1 (09:5x): correction to my 05:2x note — verified with `nx show project @semio-tech/framework-editor-rs`: the wasm target's inputs include nativeSources of semio-framework-os-infinite (and 35 other path deps), so a normal cached nx run of these two bundles rebuilds after a canvas change; --skip-nx-cache is not needed.
