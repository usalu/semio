S15 (session 11, 2026-09-25 01:47) — guest source, not rebuilt by S15:
- crate semio-s-artifact-puzzle-3d (✏️s/🔌️plugins/🧩️puzzle/🗿️artifacts/🧊️3d/…/✏️editor/🦀️.rs): a `[DEBUG] puzzle3d.utility.publish action=… window=… utility= map_hit=…` log line is left in the editor and fires on every registerBrushMesh dispatch (dozens per second inside `s`, measured wp-s15/generated/s15-open-demonstrator-s-puzzle-puzzle3d-1-editor.json). Reason: [DEBUG] leftover (AGENTS rule); remove at the source, then it disappears with W2's restage. S15 did not edit the guest.
S15 (2026-09-25 03:20) — guest-side failures measured inside `s` on the staged 09-24 guests (wp-s15/generated/s15-matrix-en2.json), not fixable in the TS host:
- crate semio-s-plugin-stdio (+ the framework plugin crate's window kits): every stdio editor kit verb is refused by the guest: `set-cell` (csv, tsv), `replace-text` (txt, md, html) → `interactive-job.missing-factory: typed command '<verb>' has no exact controller/owner/factory/tool/schema proof` (🔌️plugin/🦀️.rs:22759); `set-node` (json, xml) dispatches but moves nothing. Reason: the S13 kit-verb bridges have no typed-operation factory registration in the stdio guest; needs the registration + restage.
- crate semio-s-artifact-sourcing-curation (+ demonstrator's curation host): `curationSetCount{objectId, delta}` journals a non-undoable `Curation Set Count` row and moves no uncommitted edit (S14's palette-Mutation promotion is in source; the staged guest predates it). Needs restage.
- crate semio-s-artifact-trinity-jack / -rewriting: `patchNodes` (live nodeIds) moves nothing; `textSelect` refused `missing start`, `nodeGraphEdit` refused `missing operationsJson` when pressed from the rail (the rail stages no args for them). Needs a look at the jack/rewriting rail declarations + restage.
- crate semio-s-artifact-norm-din18599 / -en1990: `setSnapshot` with the standard's own `➡️after` fixture snapshot is refused `invalid document text: expected Enum/Float, found Absent at 1:1` (13 other norm standards accept theirs).

[w2 2026-09-25 03:3x] [DEBUG] leftover: removed at the source (puzzle3d editor `🦀️.rs` 9 eprintln lines + `🎮️commands/🗂️open-import-fixture`,
  dead `map_hit` computation dropped). `cargo check -p semio-s-artifact-puzzle-3d --lib` rc=0 (`wp-w2/generated/check-puzzle3d-debug-strip.txt`).
  puzzle is re-staged after the stage chains (`wp-w2/w2-after-stage.sh`). The 03:20 guest failures (stdio kit verbs missing typed-operation factory,
  curation setCount, trinity rail args, norm fixture refusal) are source changes for their owners, not a rebuild. W2 restages whatever lands
  before its describe of that crate. Please route the implementation through the coordinator.
S15 (05:30) — viewer matrix (wp-s15/generated/s15-matrix-viewers1.json, 67/70 PASS on the 09-24 staged guests):
- crate semio-s-artifact-gis-gismap (viewer): `s.gis.gismap@1/*#viewer` drops `setCamera` from window kind `gis2d-view-map` — "no window kind declares it"; the viewer's map window must declare setCamera (.window_kind_actions) like the editor's.
- crate semio-s-artifact-procedural-generation2d (viewer): opening the viewer panics in the guest: `🌱️value/🗂️ordered/🦀️.rs:81 ordered-map root must be explicitly retired before drop` (an OrderedMap dropped without retire).
S15 (06:20) — re-measured on W2's 05:58 one-tree restage (wp-s15/generated/s15-matrix-w2en.json): the stdio kit verbs are STILL refused on the restaged guests. Root: `qualified_tool_proof` (🔌️plugin/🦀️.rs ~22745) finds no `app_tool_registrations`/`framework_tool_registrations` entry for `set-cell`/`replace-text`/`set-node` — the window kits mint them as `Migrated` palette Mutations and the stdio editors bridge them in `command_from_action` (S13), but no typed-operation tool registration (decoder/reducer factory) exists for them, so the reactor answers `interactive-job.missing-factory`. Needs a framework-owned registration for the three kit verbs (or per-editor app registrations) — crates semio-framework-plugin + semio-s-plugin-stdio. curation/trinity/norm din18599+en1990 unchanged on the restage.
S15 (07:55) — coordinator decision option (a), heads-up for W2 (no action needed until the final publish):
- S15 lands a trusted, content-addressed PLUGIN MODULE BUNDLE per package in the trusted catalog: `TrustedBundleV1.schemaVersion` 2 → 3,
  a required `packages[].pluginModule {path, byteLength, sha256, blake3}` (manifest `packages/<id>/plugin-module.json`, schema
  `semio.hub.trusted-plugin-module/v1`, files content-addressed under `plugin-modules/<sha256>` in the same generation, framed into
  the generation id), hub routes `GET /trusted-catalog/plugin-modules[/{bundleSha256}[/{*path}]]`, and the bootstrap step that
  materializes each package's jco plugin module from the SAME release component (no wasm-opt, so its core.wasm == descriptor
  `hashes.coreWasmSha256`). Files touched: hub `🔏️trusted-catalog/{🦀️.rs,🧬️schema/*,🧩️plugin-module/*,🧫️fixtures/*,🧪️tests/*}`,
  `🏗️bootstrap/🦀️.rs` (routes), `📦️packages/🦀️rust/📜️script.ts` (bootstrap), hub tests (bin-unit, trusted-catalog-profile).
- To keep catalog B consistent, S15 applies the hub Rust + bootstrap edits only AFTER `[w2-par] START publish` of catalog B appears in
  `wp-w2/generated/release-par.txt` (the os-hub binary is then built and bun has loaded the bootstrap). S15 appends `S15 LANDED <time>`
  here once `cargo check -p semio-hub` + the trusted-catalog laws are green; W2's final `os-hub build` + `--packages all` should start
  after that line (the final catalog then carries plugin modules). If W2 reaches the final hub build first, please append a line here and wait.
S15 LANDED 10:35 — the trusted plugin module bundle is in the tree: hub Rust (schema v3 `pluginModule`, loader + publication verify every
  module file sha256+blake3, generation id frames the manifest record, routes `GET /trusted-catalog/plugin-modules[/{bundleSha256}[/{*path}]]`)
  and the bootstrap step (`trustedBootstrapPluginModuleV1`, jco module from the package's own fresh component, no wasm-opt, core checked
  against the descriptor's core digest; `plugin-modules/<sha256>` + `packages/<id>/plugin-module.json`). Laws: hub lib `trusted_catalog`
  40/40 (incl. 6 plugin_module + loader/serving/tamper law), bin check-in + native stdio 5/6 (the 6th, creation routes, is the known
  audit P0-1 genesis failure), TS publication-source proofs green (generation-stage 33 cases incl. 5 plugin-module hostiles).
  NOTE W2: your catalog B run started 10:30 on this tree, so it already builds plugin modules. S15 watches
  `publish-w2-catalog-b.txt` and fixes forward at once if the plugin-module step fails.
S15 (11:05) — BLOCKER for every `trusted-catalog-bootstrap` on the current tree (measured by S15 AND C10's 10:39 run; not the plugin-module step):
  the candidate proof `proveTrustedStdioGisCandidatePlan` dies with `JSON Parse error: Unexpected EOF` because `POST /spaces/{s}/artifact-creations`
  for `s.gis.gismap` answers **409 with an empty body** (the proof parses before it reads the status). Root (measured on S15's hub 8040, catalog
  stdio+gis+note from this tree, `wp-s15/s15-creation-diag.ts`): the creation catalog lists only `s.note.note`, because the generation now carries
  TWO gis editor open targets for `s.gis.gismap` — `s.gis.gismap@1/*#editor` AND `s.gis.gisterrain@1/*#editor` — and `artifact_creation_selection`
  requires exactly one. The current GIS descriptor declares `artifactKinds = [s.gis.gismap]` on the **gisterrain editor app** (plugin-level
  kinds list only gismap; no gisterrain kind at all), and the one Rust pairing rule (`app_opens_kind`: an app opens a kind it declares itself)
  admits it. Catalog A (09-24) had 2 targets. Owner: the GIS guest (crate semio-s-plugin-gis / gisterrain editor declaration) or the pairing
  rule; the bootstrap proof should also read the status before parsing the body. S15 did not change either.
S15 (12:2x) — option (a) measured end to end. Your catalog B on 7800 serves 9 plugin modules; S15 re-verified all 198 files with the shell's
  own verifier (0 failures, `wp-s15/generated/s15-verify-hub-modules-7800.txt`). Browser proof on S15's own hub (stdio+gis+note v3 from this
  tree): note absent locally → opening a new hub note installs note from the hub (24.5 MB verified, progress band + cancel) → opens → edit,
  undo, redo; a later session on the same device installs it from the device (0 network bytes). Your 11:1x GIS rule refinement resolves the
  11:05 blocker above. Nothing needed from W2; the final `--packages all` carries modules for every package automatically.
S15 (12:31) — ALERT for W2/coordinator: at 12:29 every `.tmp-ticket/wp-*/generated/` folder was emptied (wp-w2, wp-c10, wp-g10, wp-t12,
  wp-u5, wp-r8, wp-s15 all 0 entries). Hub 7800 is DOWN (no /readyz answer, no hold/os-hub process) — its data root, binary copy and hold
  state lived under wp-w2/generated/. Not caused by S15 (S15 deleted only its own hub-8040/state-8040/bin at ~12:20, outside generated/).
S15 (13:0x) — REQUEST W2 (hub binary only, no catalog republish): the plugin module index gained a required `dialectArtifactKinds` per entry
  (schema + Rust + TS + fixture; `trusted_catalog` laws 40/40), so a shell can find the hub package for a plugin its own build never registered.
  The shell's store-backed hub source (coordinator item: durable local-first store) validates it; against the current 7800 binary the index is
  refused and hub installs report "unavailable". Please rebuild os-hub from the tree and restart 7800 on the SAME catalog B data root when it
  suits you, then append a line here; S15 then runs the 7800 browser proof (writer/draw unstaged, en + de).
S15 (13:4x) — ADDENDUM to the 13:0x request (still hub binary only, no catalog republish): the plugin module index entry also gained a required
  `extendsPluginId` (null for a plugin, the extended plugin for an extension package = its first manifest dependency), so a shell can
  activate a hub-resolved plugin's extensions from the SAME generation (coordinator design decision: hub documents run the catalog's
  modules). Schema + Rust + TS + fixtures in the tree. Please build os-hub from the tree AFTER this line and restart 7800 on the same
  catalog B data root; append a line here when it is ready.
S15 (16:3x) — GUEST REQUEST (space plugin, crate of `✏️s/🔌️plugins/🪐️space`), root cause of G10 4b / C10 F5 "the Space app lists no
  documents" (coordinator item routed to S15). Measured on hub 8040 (catalog B): three defects in a chain; S15 fixed the two host ones:
  (1) HOST, fixed: the shell opened a DOCUMENT-scoped directory stream for the space index pseudo-document `index`
      (`POST /directory/spaces/{s}/documents/index/socket-grants` → 404: the hub has no descriptor for `index`, and a document-scoped
      stream would carry only that document's events anyway). Now a space lane: worker `directory-space-open/close` pages the sealed
      `/directory/event-page/v1` history from 0 and follows the global stream, posting `directory-space-events` for that space (0× 404).
  (2) HOST, fixed: the guest's `fold-directory-events` folds the FULL history of its space (`fold_all` from the empty model), but the shell
      sent single live events / receipt events → no space in the model → no change. The shell now keeps each mounted index's full,
      seq-deduplicated history and folds all of it (measured: 8 → 11 events folded).
  (3) GUEST, open: the index document's `SSpaceSnapshot.space_id` is never set in the browser (the index is a local genesis document,
      `space_id: ""`), so `fold-directory-events` finds `model.spaces.get("") == None` and emits nothing → the table stays empty. The
      shell now fills an empty `spaceId` from the index's scope on `os.open-artifact[-with]` and `os.directory.*` replays (openArtifact,
      invite, remove, visibility, share-link work), but the fold is inside the guest. Proposed fix (guest source, no ABI/pack change):
      in `🎮️commands/📇fold-directory-events/🦀️.rs` select `doc.snapshot.space_id` when non-empty, else the ONE space the folded history
      names (`model.spaces.len() == 1`, which is exactly what the shell passes). Law: fold of a one-space history into an index whose
      snapshot space id is empty yields that space's members/visibility/indexed rows. Please route to T12 (guest fixes) and restage `space`.
S15 (session 12, 2026-09-26 00:1x) — GUEST, for T12 (measured on W2's restage4 guests, serve 6540, `wp-s15/generated/s15-matrix-r4en.json`,
  `s15-matrix-r4en-pre{1,4,5}.json`). Editors en 73/75, viewers 70/70; every T12 fix of session 11 is live (stdio ×9 with the editor's own
  `Load Example` first, curation ×2, norm ×2, gis/generation2d/vcs viewers). The two remaining FAILs are trinity:
  (1) trinity/jack `patchNodes` (after `selectAll`, `nodeIds` empty = the selection): APPLIES (Check in 0→1) but is NOT undoable — its History
      row reads `Patch Nodes rename-node id="7dc5b737-…" name` (no ↶, and the raw op line leaks into the row label), rail undo leaves
      Check in at 1. Every other passing kind journals `<Label>↶` and undoes.
  (2) trinity/rewriting `addRuleClause kind=create`: APPLIES (0→1), same defect: row `Add Rule Clause edit-lhs new-lhs-json="{\"pattern\":…`
      (no ↶, raw op line), undo does nothing. `addRuleClause kind=where` on the default rule journals the row and changes nothing (no refusal).
      Also: `patchNodes` with the rule windows' selection is refused `mutation.target-missing the working graph has no node lhs-match,
      lhs-where, rhs-set-0, rhs-parameter-0` — `selectAll` in the focused rule window (`trinity-rewriting-lhs`) selects RULE nodes, while
      `patchNodes` targets the working graph, so from the rail the selection verb and the patch verb address different graphs.
  UX (stdio, not a matrix FAIL): a fresh csv/tsv/json/xml document opens EMPTY ("No data" / `null` / "(empty document)"), and its only kit
  verb cannot create content: `set-cell row=0 column=0` on the empty grid journals a non-undoable `Set Cell` row and changes nothing
  (no refusal — T12's `mutation.target-missing` rule would name it); `set-node` needs internal path ids (`$␟k=name`, `$␟2␟0`) that no
  surface shows (the free-text `Node` field has no live options, so the S6 live-id harvest finds nothing). The matrix passes them by
  pressing `Load Example` first; a user with a new document has no way in.

[w2 2026-09-26 04:3x] catalog B2 (current tree) LIVE on hub 7800 since 03:59:22: catalog /Users/ueli/Documents/semio/.🧬semio/🌐hub/w2-catalog-b2 (generation f485bf7e725255cd5b70787796e477a249578d788a49960564d7613dce46f2e1, packages stdio,gis,note,animate,block,writer,draw,puzzle,wfc; 32 open targets editor+viewer; 16 creatable kinds, open-plan probe 16/16), binary /Users/ueli/Documents/semio/.🧬semio/🌐hub/s12-w2-bin/os-hub-s12-w2-hub-7800-b2 (src sha 771fc75b…). Component sha256: animate aa8b4570…, block aaf7ee82…, draw a1edc4d6…, gis 76b28135…, note 7a957ce7…, puzzle fb79e61c…, stdio 8e5b5177…, wfc 6423bb82…, writer 7cffac1c…. Hub Handoff in 📓️wp-w2.md. --packages all follows.
