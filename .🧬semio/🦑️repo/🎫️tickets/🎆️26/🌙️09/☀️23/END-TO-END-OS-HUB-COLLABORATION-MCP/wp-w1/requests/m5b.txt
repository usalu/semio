# M5b (G11 G8/G9 — MCP catalog descriptions / classification / destructive), 2026-09-24 17:15
# Request: regenerate the committed descriptors (`describe`, i.e. `bun nx run @semio-tech/<plugin>-plugin:describe`)
# of the plugins below. Source-only changes: chain steps `.action_destructive(id)`, `.action_audience(id, Chrome)`,
# `.action_describe(id, …)` in each plugin's editor builder chain. No guest behaviour change, only the emitted
# `🔣️.json` (+ `🛂️.descriptor.semio`) semantics fields `effects.destructive`, `policy.approval`, `audience`,
# `description`. Wasm compile proof: `.tmp-ticket-0918/wp-m5b/generated/m5b-wasm-check.txt`.
# Reason: the live MCP catalog is compiled from these committed descriptors; until they are regenerated an agent can
# export/save to a user path, reset/replace documents and delete spaces without approval, and sees camera/preview
# chrome as agent verbs. Proven by `cargo test -p semio-framework-os-mcp --lib search::long` (fails until regen).
# Priority 1 (the six authored plugins the law pins):
semio-s-plugin-draw        exportDocument, commitDocument -> destructive
semio-s-plugin-note        saveDownload -> destructive
semio-s-plugin-layout      exportPng/Svg/Pdf/Package -> destructive; translate/rotate/scaleSelection described
semio-s-plugin-forms       exportFixture -> destructive; resetTry/previousStep/nextStep -> Chrome
semio-s-plugin-cad         saveCurrent/saveSelected/saveInPlay -> destructive; setCamera/setProjectionParam -> Chrome; patchCadPlayReference described
# Priority 2 (capability-audit-check findings):
semio-s-plugin-animate     resetGrid, exportVideoFromDeck
semio-s-plugin-architect   exportProgram, exportRegistersCsv
semio-s-plugin-flow        setActiveExample
semio-s-plugin-procedural  generation2d setActiveExample; generation3d editor+viewer exportDocument
semio-s-plugin-process     exportModel
semio-s-plugin-puzzle      exportFixture (2d/3d/5d)
semio-s-plugin-remodel     exportQcReport, resetPlaceholderMesh
semio-s-plugin-shooting    exportActiveShot, exportAllShots, resetFixture, saveDownload
semio-s-plugin-space       deleteSpace, removeMember, requestDeleteArtifact, exportMedia, exportStudioDsl, exportStudioPack
semio-s-plugin-trinity     resetRule
semio-s-plugin-lowpoly     exportMesh, replaceSnapshotJson
semio-s-plugin-demonstrator  playground setActiveExample (+ picks up cad/procedural/process/puzzle upstream marks)
# --- update 17:55 ---
# DONE by m5b inside its own 17:40:45–17:49:38 mutex hold (wasm check of all 17 crates EXIT=0 in 1m35s, then describe):
#   layout, draw, forms, cad, note  (Priority 1 complete — `search::long` 4/4 green on the committed descriptors).
# Already regenerated by someone after the source edits (audit clean for them): animate, architect, flow, procedural,
#   process, puzzle, trinity, demonstrator(setActiveExample).
# STILL NEEDED (capability-audit-check lists exactly these 14): semio-s-plugin-lowpoly (exportMesh, replaceSnapshotJson),
#   semio-s-plugin-remodel (exportQcReport, resetPlaceholderMesh), semio-s-plugin-shooting (exportActiveShot,
#   exportAllShots, resetFixture, saveDownload), semio-s-plugin-space (deleteSpace, removeMember, requestDeleteArtifact,
#   exportMedia, exportStudioDsl, exportStudioPack). m5b will NOT take the wasm lock again.
