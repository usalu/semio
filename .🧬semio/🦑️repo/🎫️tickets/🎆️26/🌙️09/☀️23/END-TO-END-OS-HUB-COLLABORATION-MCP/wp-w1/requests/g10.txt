g10 (2026-09-25 00:53) — G9 commit binding LANDED (see 📓️landing.md row G10).
  1. wfc: describe + materialize + restage. New: bitmap editor action `pin-solution` (destructive), `commit` field on
     every inference payload contract (`WireInferencePayloadContract.commit`; bitmap → pin-solution). Every guest links
     semio-framework-plugin, so every describe moves with the full rebuild anyway.
  2. Catalog: my live proof needs a hub whose catalog carries stdio, gis, note, wfc from the post-landing tree
     (the full all-package catalog is fine). Purpose: generic inference quartet live (wfc guest-executed, gis hub-executed).
g10 (2026-09-25 05:24) — hub binary need (W2 / hub owner): `semio-hub` hub.inference `INPUT_MAX_BYTES` 65_536 → 1_048_576
  (schema const + JSON `InferenceLimitsV1.inputMaxBytes` + TS twin + `gis-map-proposal-approval-v1` fixture + law). Measured on
  hub 8030 (catalog A): every map the hub creates packs to 80,801 B, so `inference_submit` of gis on any fresh map answered 413
  `inference.bounds` — hub-executed GIS inference could never run. Needs an os-hub built after 05:23 for the live quartet (gis half).
  `cargo check -p semio-hub --lib --tests` green 05:23.

[w2 2026-09-25 12:3x] catalog B published from the post-landing tree: /Users/ueli/Documents/semio/.🧬semio/🌐hub/w2-catalog-b (generation e8167ce8ed3e61f0a4252661df9f2e3043aabb258e474b797fc3f0612c9ecc53, 9 packages stdio,gis,note,animate,block,writer,draw,puzzle,wfc; 12 editor open targets, compressed closed actors, S15 plugin modules). Hub 7800 runs it since 11:50 with binary .tmp-ticket/wp-w2/generated/bin/os-hub-hub-7800-b (Hub Handoff in 📓️wp-w2.md). Open-plan probe 12/12 (wp-w2/generated/open-plan-probe-7800-b.txt). The full --packages all catalog follows.
g10 (2026-09-26 00:5x, session 12) — LANDING-WINDOW guest patch, NOT applied (plugin framework = every guest): 
  `wp-g10/g10-preview-effect-refusal.py` (dry run clean): the agent-lane preview refuses a verb whose Emit carries
  `Effect::LoadDocument` (note setFixtureJson/setActiveExample) with `interactive-job.preview-document-load`. MCP side already
  landed (no-change answer instead of INTERNAL). Needs: --apply → cargo check native + wasm32 → full describe/restage.

[w2 2026-09-26 04:3x] catalog B2 (current tree) LIVE on hub 7800 since 03:59:22: catalog /Users/ueli/Documents/semio/.🧬semio/🌐hub/w2-catalog-b2 (generation f485bf7e725255cd5b70787796e477a249578d788a49960564d7613dce46f2e1, packages stdio,gis,note,animate,block,writer,draw,puzzle,wfc; 32 open targets editor+viewer; 16 creatable kinds, open-plan probe 16/16), binary /Users/ueli/Documents/semio/.🧬semio/🌐hub/s12-w2-bin/os-hub-s12-w2-hub-7800-b2 (src sha 771fc75b…). Component sha256: animate aa8b4570…, block aaf7ee82…, draw a1edc4d6…, gis 76b28135…, note 7a957ce7…, puzzle fb79e61c…, stdio 8e5b5177…, wfc 6423bb82…, writer 7cffac1c…. Hub Handoff in 📓️wp-w2.md. --packages all follows.
