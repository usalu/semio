# Work Packages

Each package is executed by one Grok 4.7 High agent and reports into `📓️wp-<id>.md` in this ticket folder.

## Collaboration (from `📓️audit-collaboration.md`)

- **WP-C1** Local-bootstrap schema AJV fix (G3) + language-agnostic two-client document e2e (G1) in `🌎️hub/🧪️tests/🤝️two-client-document/`.
- **WP-C2** (done, PASS; coordinator runs the full kernel `sync` suite as follow-up) Backbone parity contract: Rust `🔄️sync` actor vs TS `👷️worker` produce identical projections from the same frame fixture (G2).
- **WP-C3** (done, PASS on unit level) In-canvas remote presence: peer cursors/selections rendered in writer, draw and puzzle viewports via presence `interaction` domains (G4). `activeTool` bit 12, peer-overlay layer, `CanvasPresenceOverlayV1` in Board2d/World3d/TextEditor. Report `📓️wp-c3.md`.
- **WP-C3b** (done; unit PASS, live proof pending) No `__local__`, `rayOrigin` on the presence wire, World3d projected cursors + selection highlights, collab-e2e STEP 14 peer-cursor asserts in the O3 harness. Live run was blocked by the taxonomy error below (now fixed by the coordinator); the live two-browser cursor proof rides on WP-O3's collab-e2e run. Report `📓️wp-c3b.md`. Original scope: C3 gaps: real local actor threaded into hosts (no `__local__` placeholder), world-space 3D peer cursors + selection highlights (no HUD fallback), browser two-user cursor proof in writer/draw/puzzle 3d via collab-e2e.
- **WP-C4** (done, PASS) Hub framework port: wire `db::Database` behind `ServerInstance::Documents`, move auth and deciders into `HubModules`/`HubDeciders` (A1, A2). `HubDocumentAuthority`, HT16 wire projection, router merge; documents 4/4, stores hub_* 2/2, framework gateway 24/24. Report `📓️wp-c4.md`.
- **WP-C4b** (done, partial) Legacy document socket deleted; clients on `/scopes/{space}%2F{doc}/document/ws` with `semio.session.v1`; hub/server/os `cargo check` green. Suite + live DBs not run (hub-lock inversion, Docker). Report `📓️wp-c4b.md`.
- **WP-C4c** (running) Fresh `os-hub` + full hub suite, framework-ws two-client/resume/late-join proof, all hub-socket harnesses migrated; pg/neo4j only if Docker answers. Original C4b scope — Delete the legacy `/spaces/.../socket/v1` axum surface (framework router only, all clients migrated), full hub suite green, live Postgres + Neo4j two-client document edit.
- **WP-C5** (done, PASS) Durable collaborative redo: server-stamped per-user redo log per document (A3). Built on the existing Revert/Reinstate ledger; default Undo transforms against concurrent edits; redo survives pack + `.spr` reload; hub two-author claim `durable-collaborative-redo` (AppChannel 17). Report `📓️wp-c5.md`.
- **WP-C6** (done, PASS) Data-classification routing: hub spaces always `origin: "hub"` with directory fold active in both shells; ephemeral studios block share/collab UI (D1, D2, D3). Kernel PDC fixture 1/1, space home ephemeral 4/4, share 4/4, promote 1/1, persist 1/1, vitest PDC 3/3, framework-os typecheck green. Report `📓️wp-c6.md`.

## Os frontend (from `📓️audit-os-frontend.md`)

- **WP-O1** Green os gate: `framework-os:typecheck`, `framework-renderer-react:typecheck` (scoped tsconfig, bun types), `plugin-registry:check` (regenerate + make generation deterministic), `web_sys` in `semio-framework-ui` wasm target, vite config module graph budget via refactor.
  - (done, partial) typechecks + renderer-react/os-shell tests + config-graph budget PASS (30 modules / 360840 B). Remaining split into O1b/O1c. Its "TC5 hung at 0% CPU" claim was wrong: TC5's stdio cdylib rustc is alive but CPU-starved (load avg ~91 on 10 cores).
- **WP-O1b** (done, PASS) framework-os 373/373, os-mcp 52/52, plugin-registry 60/61 (1 skip), host-rs vitest 373/373 (same OS config), os-dev 163. Roots: FakeHub state + stale WS path/protocol in presence tests, missing `lease.drop()`, WIT option receipt, host-activation boot-scoped expectation; `resolveTestLevel(…, "quick")` floors use the pre-existing `minimum` API (as wgpu uses `long`) — accepted. **Phase 3 checks:** (a) norm `🔣️.json` + `🛂️.descriptor.semio` were hand-synced (15 navbar examples, self-hash) — must be reproduced byte-identical by `describe norm` / `plugin-registry:generate`; (b) os-dev freshness pass relied on O2b's rejected marker trust — re-verify after O2b round 2. Root-fix suite hangs: framework-os, os-mcp, plugin-registry, os-host-rs vitest; 4 os-dev transform-freshness timeouts.
- **WP-O1c** (done, partial) taxonomy 0 findings (was ~340); renderer-wgpu lib+tests compile (Shell `os_pack`, `window_kinds.first().id`, `handle_shell_hit(…, &InputState)`); wfc descriptor hand-re-encoded; `check` blocked on stale `plugins.json` (generate queued on wasm mutex); renderer-wgpu:test 1282 pass / ~89 fail; wasm32 ui check queued.
- **WP-O1d** (running) generate → check green (generated descriptors must supersede hand-synced wfc/norm), renderer-wgpu:test 0 failures by root cause, wasm32 `semio-framework-ui --features wgpu-engine` check.
- Original O1c scope — plugin-registry:check ~340 structural issues → source fixes + generate; renderer-wgpu `WindowKindDefinition`/`pack` compile after peer window-schema changes; wasm32 `semio-framework-ui --features wgpu-engine` check.
- **WP-O2** (done, partial) Content-hash freshness (mtime `source-newer` removed), publish-then-serve, lazy-activate middleware (SSE progress, AbortSignal, prefetch); staging-root cache contract 49/49; warm serve HTTP 200 in 60–87 s vs ~16 min cold. Nx serve, `test quick` and on-demand materialize were blocked by the taxonomy error. Report `📓️wp-o2.md`.
- **WP-O2b** (round 1 done: warm 5.5–7.7 s, test quick 159 pass, lazy ×3 200 + cancel; REJECTED parts: marker-trust freshness never reads sources → stale modules; `serve-*` `dependsOn: []` unproven on fresh clone) (round 2 done, PASS: schema-first `SourceStatIndexV1` stat-walk freshness, `walkSources`/marker trust deleted, tests a–d green (53), live animate stale→restage proof, `dependsOn: session-*` restored + lazy materialize of an emptied module, warm direct 3.98 s; dev-facing nx: graph prime 334.6 s, first iso serve 241.2 s at 0/3 cache).
- **WP-O2c** (round 1 done: warm nx serve 3/3 cache hits, nx run 1.2–5.4 s; cold graph 334.6 s → 52 s wall / 42.5 s CPU; graph 133 → 83 MB; os-dev quick 163. Accepted: cacheable `generator-inputs`, namedInput interning. REJECTED: entry-point-only `createDependencies` scan may drop cross-project edges → stale cache hits) (round 2 done, PASS: edge loss confirmed — entry-only dropped 260 project edges (`wp-o2c/generated/edge-diff.json`); replaced by full per-file scan made incremental via `filesToProcess` + per-file-hash edge cache + parallel reads, equality with full scan PASS (4010 edges / 830 import); cold graph 31.3 s CPU (98 s wall under load), single-file edit 2.1 s CPU; warm serve 3/3 cache; os-dev quick 163). Dev-facing launch path: truly-warm nx serve cache hits (fix nondeterministic session/generate inputs), profile + root-fix project-graph construction (target cold < 30 s CPU, warm < 2 s), CPU-time evidence. Verify the blocked O2 items, warm `dev s` under 10 s with a per-phase profile, real cold-boot measurement. Original O2 scope — Cold boot: `dev s` served mode skips fresh builds (content-hash freshness), lazy plugin activation via activation events; target seconds, not minutes.
- **WP-O3** (done, partial) `uiPatch.receipt: invalid bytes` cleared live; mount then hit framework document-ws 403 (authenticated template not applied by the live policy engine); left a name-fallback in `applicable_grants` (to be replaced). Collab still 3/10. Handoff `wp-o3/links/c9.md`.
- **WP-O3b** (running) Root-fix the 403 via proper auto-apply of the `authenticated` template at hub compose time (delete the name-fallback), remount, collab-e2e 10/10 + C3b STEP 14, zero-touch `dev s`/`▶️start` with local hub. Original O3 scope — Zero-touch hub by default: `dev s` / `▶️start` brings up local hub + local-bootstrap credentials; browser two-user `collab-e2e` green.

## Continuations of session 8 (canonical ticket 26/09/18)

- **WP-M10b** Agent commits a hub edit over the semio mcp, human sees it; `hub-agent-participant` 17/17 (continues M10).
- **WP-S14** (second run; first run stalled after landing writer `setText` + sourcing `curationSetCount` palette fixes) 35/35 kinds spawn/mutate/undo/redo inside `dev s`, en+de history rows, 60-component guest rebuild (continues S13).
- **WP-TC5** Codec sweep green, native twin, disposer default, full-catalog publish + hub 7700 (continues TC4).
- **WP-GJ2** (done, partial) Zero-touch `ensureMcpBinary` (content-hashed staging); probe: 28 tools, 7 resources, 5 prompts, `inference_list` 74 rows; G19 reach gaps closed; GJ1 host laws green. Not proven: live `inference_run` hangs after first progress, `client-e2e` 30/38. Report `📓️wp-gj2.md`.
- **WP-GJ3** (running) Root-cause the `inference_run` hang (`s.wfc.bitmap.solve` over semio mcp), `client-e2e` 38/38, regression fixture.

## Fleet (12:45)

Audits: plugins-a, plugins-b, hub-backend, semio-mcp. Execution: C1, C2, C3, C4, C5, C6, O1, O2, O3, M10b, S14, TC5.

## Plugins (from `📓️audit-plugins-a.md`, `📓️audit-plugins-b.md`)

- **WP-P1** (running) Plugin test gate batch A: fix the gis `artifact-document-id-v1` fixture vs `ArtifactDocumentIdV1` schema drift (`artifactId` → `documentId`) and `proveGisNativeCodecReceipts`; run `test-quick` for all 17 batch-A plugins with a sufficient `SEMIO_BUILD_BUDGET_MS` to a definitive result and fix every failure; make the default quick budget match the real suite cost so the product verb is deterministic. Coordinates with TC5 (codec sweep) and H1 (hub TS native-codec tests).
- **WP-P3** (running) Plugin test gate batch B: the 13 plugins without a confirmed `test-quick` result (imperative, remodel, energy, trinity already pass), extensions (cad×4, imperative×5, playbook×1, sourcing×3) loading with their parent, real test double for the draw FSM test-only `unimplemented!`.
- **WP-P2** (done, partial) lowpoly gltf/las/dwg/stl io 13/13 + descriptor fresh; forms csv/xlsx/zip DSL carriers round-trip ok; framework `serialize_dsl_txt`/`deserialize_dsl_txt` wires ~28 plugin txt stubs as Lossless (no per-crate evidence); architect upserts code-only; stdio IFC/STEP/PNG oracle not re-run.
- **WP-P2b** (running) Per-crate txt round-trip tests + runs for the ~28 plugins, architect tests, stdio IFC/STEP/PNG oracle proofs, descriptor regeneration via `describe` (incl. hand-synced norm byte-identity).
- Original P2 scope — Plugin stub closure across both batches, priority: stdio SDK Wave 3 builder/decomposer traits (IFC/STEP/PNG round-trip), then architect register upserts (relationship/knowledge), lowpoly mesh-handle resolution for dwg/gltf/las/stl export, forms csv/xlsx/zip interchange, shared txt IO, oracle/second-implementation debt (mathematical, sequence, fem, architect).
- Wasm activation of all plugins inside `dev s` is owned by WP-S14 (35/35 kinds) and WP-O2 (lazy activation); flow-extension chain for procedural + demonstrator is checked there too.

## Coordinator fixes

- 13:55 Repo taxonomy (`📚️library/🔣️taxonomy.json`) was invalid for every dev verb: five new `dev-vendor-*` preview2-shim fixed-filename contracts (common, in-memory-filesystem/http/sockets, opfs-filesystem) lacked `packageSourceDispositions`. Added matching `adapter-source` dispositions; `loadCatalogTaxonomy()` valid.
- 14:10 `test-package-body-policy` was missing from `.vscode/launch.json` and `🧩️launch.seed.jsonc` (AGENTS.md launch rule); registered as `🧹clean🧩️taxonomy📦️package-body-policy` (4_build, 206.175). Boundary-classification test 121/121 with it.
- 14:58 Lock inversion: H1 held the `hub` mutex 35 min while its `trusted-catalog-bootstrap --packages stdio,gis` waited on the repo wasm prebuild flock held by TC5's full-catalog release build; C1/O3/C4b starved. H1 interrupted to release; preamble rule 9 (wasm → hub order, never hold hub around wasm builds, consume TC5's catalog).
- 15:05 Blocked on the user: Docker Desktop engine hung (socket EOF; `open -a Docker` and a graceful quit both failed; not force-killed). No native Postgres/Neo4j. Live pg/neo4j proofs wait until Docker Desktop is restarted by the user.
- 15:24 TC5 is not hung: its fleet-mutex wrapper sits at 0% CPU but the child rustc (stdio cdylib, wasm-release) is alive and progressing, CPU-starved at load avg ~91/10 cores. Three duplicate `serve puzzle5d` vites on 6014 (other team's `🔁️serve-supervisor.sh`, ~60% CPU each, only one bound) contribute; not ours, left alone.
- 15:36 Kernel lib-test compile was red for everyone: uncommitted peer `🚪️io` `deserialize_dsl_txt` returned `S::parse_dsl` as `IoResult<S>` (E0277 inferred `IoOutcome<S>: ArtifactDsl`). Wrapped with `.map(IoOutcome::clean)`; emoji docstrings/region. C2 full sync rerun → `🗑️generated/wp-c2/rust-sync-full-2.txt`: compiles, 49 pass / 11 fail (actor↔mock-hub never reaches Session ×6, ops-text inverse record ×2, missing fixtures path + 1≠42 remote ops, retained-turn owner leak) → **WP-C2b** (done, PASS 60/60; TS parity 7 + neighbours 9 green): mock hub speaks `semio.session.v1`, `print_edit_lines` emits inverse+metadata, fixtures under `store/sync/fixtures`, HLC on append-only external remotes, TurnFault no spurious Pool job.
- 17:36 **Security (impersonation):** framework `document/ws` takes the socket actor (plus presence/session) from the caller's `?actor=` query; gateway only checks `subscribe` on the scope and hub `submit_frame` ignores `_principal` → any subscriber can author as another actor. **WP-C4d** (done, PASS; coordinator-verified in code): `DocumentStreamQuery` now only `surface`/`resume`; `DocumentAuthority::bind_socket` binds actor+session from the resolved credential (hub override requires a hub-issued actor grant, anonymous → Unauthorized); no client builds `document/ws?…actor=`. Kernel sync 60/60, hub `documents::` 9/9 (bind reject, foreign actor, two-user), framework-server lib 88 (`gateway::` 26), TS parity 7 + neighbours 9, server TS 14. Peer note `wp-c4d/protocol-change.md`.
- 18:05 TC5 agent died (repeated resume failures); its `trusted-catalog-bootstrap --packages all` (wasm mutex pid 39217) lives on: stdio 8/8 done, gis cdylib compiling. One run = 34 release cdylibs under a single wasm hold → starves s14/o3b/gj3b/o1c/p1 for hours. **WP-TC5b** (running): terminate after gis finishes, rebuild per package with one wasm-mutex hold each (same target dir, crates reused), final `--packages all` publish + hub `/readyz` + 34-plugin catalog proof.
- Open (repo tooling, outside the four outcomes): `taxonomy transaction dispositions v2 > relocates three complete embedded ticket roots` finds 0 of 3 embedded roots in its own temp fixture — normalization regression, left for a repo-tooling ticket.

## Hub backend (from `📓️audit-hub-backend.md`)

- **WP-H1** (done, mostly PASS) fresh hub binds published catalog by content hash (~2 s, wasm skipped) → `/readyz ready` (7748); publication lock atomic handshake 1/1; presence-lease checks 20; hub TS typecheck exit propagates; `IoFidelity::Lossless`→`Exact` in 32 plugin files after a peer removed `Lossless` (accepted: Exact is the strongest rank); stall-law `div_ceil` fix; `os-hub:test-quick` 229/231.
- **WP-H1b** (running) Replace auto-discover `"openTarget"` string-sniffing with schema validation; fix `admin_removal_revokes_visible_plan_presence_and_target_after_sqlite_reopen` + `an_unauthenticated_document_route_is_refused` (hub side, aligned with C4d); verify root `test quick` exit code propagates cargo failures.
- Original H1 scope — Zero-touch readiness: a fresh hub boot publishes/binds a trusted catalog so `/readyz` is ready without manual steps (coordinate with WP-TC5's full-catalog publish); fix `trusted_publication_owner_process_crash_releases_exact_lock`; `presence-lease-check` fixture path; hub TS typecheck red while the script exits 0 (exit code must reflect failure); `build-dev` first-build latency under lock contention.

## Session 10 (Claude Code fleet, 2026-09-23 22:55, after the 19:48 reboot)

Rules: `📓️session-10-preamble.md`. Every session-9 "running" package was cut by the reboot and is continued below.

- **W1** (wp-w1) Sole owner of all-plugin wasm work: `plugin-registry:generate` → `check` green + `descriptor_is_fresh` everywhere (supersedes hand-synced norm/wfc), all s guests rebuilt+activated (continues S14/O1d), full trusted catalog publish per package + hub 7800 ready (continues TC5b), guest-rebuild request inbox `wp-w1/requests/`.
- **H2** (wp-h2) Hub backend: reconcile the document socket (C4d vs H1b vs O3b) into one route/one owner, full hub suite green (continues H1b/C4c), sqlite live two-client + presence, pg/neo4j if Docker answers. Owns `semio-hub` builds.
- **C7** (wp-c7) Browser collaboration: `action-owner-mismatch` + empty presence root fixes, zero-touch local hub, collab-e2e 10/10 + peer cursors STEP 14 (continues O3b).
- **G4** (wp-g4) Semio mcp: client-e2e, hub-agent-participant 17/17, live-agent-loop, probe transcript, agent edit seen by a human (continues GJ3/M10b).
- **R1** (wp-r1) Native: renderer-wgpu 0 failures (continues O1d), plugin logic failures (space, flow stall, extensions, stdio oracles; continues P1/P2b/P3), os vitest suites green.
- Session 10 follow-ups: **H3** pg/neo4j WAL writer fence (done), **H4** hub ledger head / edit-id collision, **H5** graceful shutdown + db reds (done), **G5** guest step overhead (done, 2.07×, client-e2e 38/38), **G6** cancellable binding + relay hop + idle workers, **R2** kernel edit-text + semio-base (done), **R3** semio-base json oracle (done), **R4** db crate lib suite (~72 pre-existing reds, artifact runner close hang), **R5** test-platform + subset schemas + mesh (done), **P4** restore `#[derive(DslArtifact)]` pack-schema identity for 18 packages (coordinator decision), **C8** checkpoint panic + inspector refresh + collab 10/10 + STEP 14 + zero-touch, **T1** contract rules/oracles/test placement (Go in-package tests admitted by rule, coordinator decision), **T2** case/fixture layout + catalog content, **T3** real plugin import/export serializers.
- 2026-09-24 ~07:30 the account usage limit cut all 11 running agents (reset 08:30). Wave 1 resumed 08:32 (critical path): W1, H4, C8, P5, R6, G7. Wave 2 parked until wave 1 thins out: T1, T3, P6, T4, T5 (each resumes from its own report). H5 and T2 had already finished.
