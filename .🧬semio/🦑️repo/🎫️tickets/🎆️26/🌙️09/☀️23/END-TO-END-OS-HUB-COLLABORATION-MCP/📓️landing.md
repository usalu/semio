# Session 11 Landing Window

Prepared patch sets from the session-10 freeze, landed compile-atomic before W2's full rebuild (preamble rule 1).

| Slice | Set | Crates checked (native / wasm32) | Result | Time |
|---|---|---|---|---|
| G10 | G9 commit binding (`wp-g9/g9-apply-commit-binding.py`, dry run clean, all 11 hunks applied unchanged): `InferenceCommitBinding` on the manifest payload contract + plugin `ArtifactInferenceCommitBinding`/`WireInferenceCommitBinding` + TS projection; wfc bitmap `commit: pin-solution`, other four wfc `None`; MCP `declared_commit_action` real body | native `--lib --tests`: semio-framework, semio-framework-plugin, semio-framework-os-mcp, semio-s-artifact-wfc-{bitmap,2d,grid2d,3d,grid3d} (`--features …/component-app-assembly`) / wasm32-wasip2 `--lib`: semio-framework, semio-framework-plugin, wfc ×5 (same features) | green / green (`wp-g10/generated/landing-check-native-2.txt`, `landing-check-wasm-1.txt`; run 1 without the feature hit the pre-existing wfc-2d feature-gated test module) | 00:42 applied → 00:52 wasm green |
| WG7 | Kernel `ureq` → `[target.'cfg(not(target_arch = "wasm32"))'.dependencies]` (N2 §6.1) + the one wasm32 break it uncovered (`🔄️sync` `use crate::os_spr::ArtifactId` was native-gated but `envelopes_from_history_edit` is target-neutral → un-gated); N2 relay `🐍️n2-relay-edits.py` all 69 hunks (true dry run on a scratch copy clean, applied unchanged) incl. its 2 test mounts (`📂️wgpu-document-relay`, `📡️wgpu-document-backbone-effect`) | native: semio-framework-os-kernel `--features sync,ureq`, semio-framework-os-renderer-wgpu `--lib` / wasm32: kernel `--lib --features sync --target wasm32-unknown-unknown`, kernel `--lib --target wasm32-wasip2`, renderer `--lib --target wasm32-unknown-unknown` | green / green (`wp-wg7/generated/l1-*.txt`, `l2-*.txt`) | 00:37 kernel → 00:41 relay applied → 00:51 renderer wasm32 green |
| H9 | H8 frozen set, re-derived: `codec.replay-envelopes` guest ABI export — WIT `codec` interface; plugin crate `PluginApp::artifact_replay_envelopes`, `artifact_app_replay_envelopes`, `plugin_artifact_replay_envelopes`, component `codec::Guest::replay_envelopes` + owned core export `semio_owned_replay_envelopes_v1` (`OwnedSemioExport::ALL` 13 → 14); plugin-host owned/wasmtime/enum dispatch; kernel `store::replay_envelopes_onto_pair` + `ArtifactCodec::replay_envelopes` (shared `close_codec_reduction_store`); replication `MutationDag::pending_is_empty`; MCP guest-backed codec twin; hub `TrustedArtifactReplayCodec`. **Guest ABI change: every host built from this tree refuses components built before it (owned ABI requires all 14 exports); W2's full rebuild picks it up.** | native: semio-framework-os-kernel `--lib --tests`, semio-framework-plugin + semio-framework-plugin-host `--lib --tests`, semio-hub `--bin os-hub --tests`, semio-framework-os-mcp `--lib --tests` / wasm32-wasip2: kernel `--lib`, semio-framework-plugin `--lib`, guest semio-s-plugin-note `--lib` | green / green (`wp-h9/generated/abi-check-*.txt`, `abi-wasm-check-1.txt`) | 00:56 started → 01:13 wasm green |
| T12 | T10 outcome switch (`wp-t12/switch.py` = `wp-t10/switch.py` with captures redirected; dry run on the current tree 2,544 files / 0 problems, applied unchanged) + `manifest-align.py --write` (1,898 rows aligned, 682 already agreed; 1 unresolved = binary `splice`, T11 follow-up) + one TS fix (`isMutationOutcomeClass` guard; the scaffolder's outcome set is now typed) | native `--lib`, one invocation: replication, dsl-derive, os-kernel, os-config, 97 artifact crates, 36 plugin crates (134 `-p`) / wasm32-wasip2: replication, plugin stdio, gis, cad; TS: test platform + vcs codec test | native green (7 min) / wasm32 green (3.5 min) / tsc 0 errors / kernel+derive+replication `--tests` green; the 37 standalone bridge workspaces (native host tools, not guests) are being swept one by one (`wp-t12/generated/check-switch-bridges-2.txt`, 4/37 green so far) | 00:41 applied → 00:51 native green → 00:56 wasm green |
| T12 | T11 stdio follow-ups: binary one name `replace-byte-range` (wire `replaceByteRange`; rename attribute, `splice` keyword/grammar/proto/TS/catalog/manifest row/fixture dir/feature/adapter/oracle all renamed, `wp-t12/binary-one-name.py`); dwg ac1018 duplicate leaf dirs `📸️set-snapshot`/`🏷️set-version-info` deleted + their dead crate-root mounts, AC1018 fixture case moved to `🧬️mutations/🧪️tests/🔬️fixture` (`dwg-ac1018-leaves.py`); jpg/tiff baseline leaf payload fields `pub(crate)` → `pub` (20 fields / 15 leaves, `leaf-fields-pub.py`); `schema generate` rerun | native `--lib --tests`: stdio-binary, stdio-dwg, stdio-jpg, stdio-tiff; standalone stdio test-oracle `--features oracles --lib --tests` / wasm32-wasip2 `--lib`: plugin stdio + the four artifacts | green / green (`wp-t12/generated/check-followups-1.txt`, `check-followups-oracle-1.txt`, `check-followups-wasm-1.txt`). **T12 landing done** | 01:04 applied → 01:05 native green → 01:08 wasm green |
