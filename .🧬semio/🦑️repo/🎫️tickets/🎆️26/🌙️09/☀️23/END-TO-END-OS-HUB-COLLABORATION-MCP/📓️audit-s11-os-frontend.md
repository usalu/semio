# S11 — os `s` Frontend Audit (Outcome 1: working os `s`, all plugins/artifacts, as a user experiences it)

Read-only, no edits/builds/servers started (one `curl` to already-listening local ports, per the rules). 2026-09-25,
~00:35–01:20. Sources read in full: `📓️peer-audit-g10.md` (session-10 G11 copy), `📓️audit-os-frontend.md`,
`📓️audit-plugins-a.md`, `📓️audit-plugins-b.md` (all dated 2026-09-23), `.tmp-ticket-0918/📓️s3-foreign-kind-open-and-block-inside-s.md`
(2026-09-24, session 10), `.tmp-ticket-0918/📓️s13-thirty-five-on-rebuilt-guests.md` (session 8, 2026-09-22, **incomplete —
see §0**), `.tmp-ticket-0918/📓️g5-ux-completeness-audit.md` (session 7/8, undated header, cross-references G1/K1/AU2),
`.tmp-ticket-0918/📓️s1-plugin-coverage-matrix.md` (**2026-09-18 — 7 days stale, see §0**),
`.tmp-ticket-0918/📓️u4-pinch-diagram-nav-contrast-wired.md` (2026-09-24), `.tmp-ticket-0918/📓️status.md` tail (line 700+,
sessions 8/9-bis/10/11 start), `.tmp-ticket/📓️work-packages.md`, `.tmp-ticket/📓️session-11-preamble.md`,
`.tmp-ticket/📓️landing.md`, `.tmp-ticket/📓️wp-{w2,s15,c10,h9,t10,t12}.md`. Verified directly against the tree: the
activation receipt, every one of the 60 `dist/dev/🔌️plugin-modules/*/…component.core.wasm` files (mtimes), the 34
plugin folders, one live `/readyz` curl.

## 0. Headline — what's stale, what's new, what session 11 has actually done so far

- **Session 11 has not yet produced any Outcome-1 evidence.** `wp-s15.md` (the owning slice) is still the
  unfilled skeleton (`(filling)` in every row); `wp-w2.md` (the sole wasm/catalog owner) is `BOOTING`/`IN
  PROGRESS`/`WAITING for "landing done"`; `📓️landing.md` has zero landed rows. Everything below is session-10
  (2026-09-24) or earlier evidence, re-verified against the current tree where cheap to do so.
- **Tree check (done this pass, 2026-09-25 ~01:00): all 60 registered plugins/extensions have a staged
  `*_component.core.wasm` on disk**, mtimes 2026-09-24 16:xx–22:xx. This **contradicts session-10's S3 finding**
  ("15 guests' `core.wasm` deleted at 12:54", including `raster`, `stdio`, `puzzle`, `norm`, `gis`, `cad`,
  `architect`, `process`, `wfc`, `flow`, `animate`, `demonstrator`, `vcs`, `energy`, `trinity`) — those 15 were
  restaged sometime after S3's 16:5x probe (all plausibly during W1's evening catalog-A publish run, which ended
  2026-09-24 ~20:46). **This is a correction, not a re-proof**: nobody has re-run S3's browser probe against the
  restaged wasm, so "raster opens inside `s` again" is a disk-state inference, not an observed fact. The
  activation receipt itself (`🔣️.../🔣️receipt.json`) is still W1's original 2026-09-24 12:20 write (60/60,
  unchanged) and was **not** regenerated by the restage — receipts are descriptor-level, not wasm-content-level
  (S3 §0.1's own finding, still true).
- **The 0918 ticket's "35/35 kinds" narrative is itself incomplete, not a stale number to trust.** `📓️s13-…md`'s
  title promises 35/35 but its own body has only §1 filled in (root-causing 5 survivor kinds from S12's 30/35);
  §2 "the 60-component guest rebuild", §3 "the full sweep… en+de", §4 "the hub-document journey", §5 "honest
  gaps" are **all literally `(filling)`, empty**. No later 0918 report (S14 in `work-packages.md` claims to
  "continue S13" but no `📓️s14…md` file was found in `.tmp-ticket-0918/`) completed it. **The last real, fully
  evidenced number is S12's 30/35** (2026-09-22), and even that is compromised: S3 (2026-09-24) found and fixed a
  React-shell regression (introduced 2026-09-20 23:20) where any spawned window whose kind id differs from its
  body key (`dag`, `block2d`, others) painted only a loading skeleton forever — **verbs, undo and redo reached
  the guest and the ledger scored a PASS, but the screen showed nothing**. S3's own §7: "the S11–S13 PASS rows
  for kinds with id ≠ body key were scored on the ledger with an unpainted body; not re-swept here." **So the
  30/35 figure is unverified for however many of those 30 have id≠bodyKey — nobody has re-run the sweep since
  the fix.** This is the single biggest correction in this audit.
- **G1's "no mobile layout system" claim (repeated in several early tickets) is independently corrected by G5**:
  a real, dual-implemented (React + wgpu), byte-parity 767px mobile breakpoint exists — see §2 Multi-device.

---

## 1. Per-plugin / per-extension matrix

Legend — **Staged**: wasm on disk now (verified 2026-09-25) + receipt row. **Compile**: `cargo check`
(2026-09-23, `audit-plugins-a/b`). **Boot-in-`s`**: last time this kind was opened *inside the running `s`
host* (not an isolated single-plugin playground) with a date. **Mutate/undo/redo**: last live state-changing
proof, with date and where it was scored (ledger-only vs. rendered). **Faults**: known blockers.

| Plugin (artifacts) | Staged (wasm+receipt) | Compile (09-23) | Boot-in-`s` (date, evidence) | Mutate/undo/redo (date, scope) | Known faults |
|---|---|---|---|---|---|
| ✒️writer | yes/yes | 🔴 red 09-18 (peer stdio-pdf dep, presumed fixed since — not rechecked); 🟢 09-23 `cargo check` exit 0 | not probed inside `s` this ticket | `formatDocument` proven **by design a no-op** on canonical text (S13 §1.4, 09-22); real doc verbs (`textEdit`/`setText`) are `in_palette:false` — no rail row can move the document at all | Real edit verbs unreachable from the Actions rail by design; only direct typing in-window exercises them (unverified live) |
| ➗️mathematical | yes/yes | 🟢 (600s retry) 09-23 | not probed inside `s` | none found | 10 mutation kinds lack 3rd-party oracle; `test-quick` times out at 300–600s (inconclusive, not failing) |
| 🀄️wfc (5 artifacts) | yes/yes | 🟢 09-23 | not probed inside `s` | none found | test-quick timed out in audit window (inconclusive) |
| 🌀️procedural (2) | yes/yes | 🟢 09-23 | not probed inside `s` | none found | runtime dependency on 8 flow extensions for 3d eval chain; historically a recurring vite dev-serve wedge under load (09-18 note) |
| 🌊️flow (+9 extensions) | yes/yes | 🟢 09-23 | not probed inside `s` | none found | txt IO stubs; extension WIT-invoke vs linked-installer split still noted in registry comments |
| 🌍️gis (2) | yes/yes | 🟢 09-23 | **live, hub-connected** 2026-09-22 (0918 C7: `addFeature` dispatched from the Actions rail, guest-applied, hub-persisted, reload shows it) | ✅ **PASS, live rail verb, hub round-trip** (C7, 09-22) | `test-quick` FAILED 09-23 (`artifactId` vs `documentId` fixture/schema drift) — unclear if fixed since |
| 🌿️vcs | yes/yes | 🟡 mixed 09-18 (artifact green, plugin red on stdio-pdf dep) / 🟢 09-23 | not probed inside `s` | none found | 09-18 found a real `DuplicateSiblingKey` render fault independent of the peer dep — unclear if fixed |
| 🎞️animate | yes/yes | 🔴 09-18 (stdio-pdf dep) / 🟢 09-23 | not probed inside `s` | none found | video/raster path returns honest wasm `Adapter` error; TS play-app registration "minimal stub" (09-18) |
| 🎥️shooting | yes/yes | 🟢 09-23 | not probed inside `s` | ✅ proven 09-16/17 in its own playground (`addLayer`-class verbs; not the running `s` host) | none blocking |
| 🎪️demonstrator | yes/yes | 🟢 09-23 | not probed inside `s` | none found | integration hub — needs cad/gis/procedural/process/puzzle/sourcing + 8 flow extensions all loaded; artifact intentionally minimal |
| 🎬️sequence | yes/yes | 🟢 09-23 | not probed inside `s` | none found | 4/8 step kinds uncovered by csv oracle |
| 🏗️fem (2d/3d) | yes/yes | 🟢 09-23 | not probed inside `s` | ✅ proven in its own playground, 09-1x (`addNode`, `setCamera`, `setResultDisplay` round-trip) — not the running `s` host | txt import/export explicitly unimplemented; 22+ mutation kinds invisible to mesh oracles |
| 🏛️architect | yes/yes | 🟢 09-23 (158s cold) | not probed inside `s` | ❌ refused 09-18: `setAdjacencyKind` hit `BatchOnlyPendingRewrite` | **was** the root cause of a global MCP catalog failure (duplicate capability id `setAdjacencyKind`, 09-18) — status of that fix unverified this pass; register-import upsert stubs |
| 🏭️process (+4 material ext.) | yes/yes | 🟢 09-23 | not probed inside `s` | none found | demonstrator/process integration needs the 4 machine extensions loaded; 09-18 catalog drift on extension directory names |
| 💠️lowpoly | yes/yes | 🟢 09-23 (test-quick failed, `ENOENT .tmp-wp-c3`, likely env/transient) | not probed inside `s` | none found | dwg/gltf/las/stl export blocked by content-addressed mesh-handle architecture (documented, not a bug) |
| 💡️reasoning | yes/yes | 🟢 09-23 | not probed inside `s` | ❌ 09-18: `addNode` reaches the typed op then dies on a bespoke retained-publication authority that only admits `MoveNode` — named one-file fix (swap in the generic framework factory, as `dag`/`trinity` do); unclear if landed since | closest of the "furthest-behind" plugins to passing per 09-18 |
| 📋️forms | yes/yes | 🟢 09-23 | not probed inside `s` | ✅ proven 09-16, own playground: `add-step` → tree +1 → undo → `nextStep`, **zero fault lines** | consumed by procedural/demonstrator via `questionKind`; csv/xlsx/zip stubs |
| 📏️layout | yes/yes | 🟢 09-23 | not probed inside `s` | ✅ proven 09-1x, own playground: tree-row click → `interactionSelect`, aria-selected changes; native gesture tests pass | 6 verbs migrated off `BatchOnlyPendingRewrite` per 09-18; not reverified this pass |
| 📐️cad (+4 building ext.) | yes/yes | 🟢 09-23 | not probed inside `s` | 🟡 baseline proven 09-16 (own playground); extensions unverified | extensions need parent loaded at runtime; catalog drift noted 09-18 |
| 📕️norm (15 standards) | yes/yes | 🟢 09-23 (all 17 crates, 67s) | not probed inside `s` | ❌ 09-18: boots **clean, zero fault lines**, 19 evaluated checks, but every verb refused (`command_from_action` missing) — named "cheapest win in the batch," ~20-line bridge; unverified fixed since. **393 hard-coded English `MutationKind::label()` sites** (worst offender in the repo, G5) | component-size gate (0918 fleet 8 NB1) separately fixed a 274 MB wasm-bound overrun; unrelated to interaction |
| 📖️playbook (+1 procedural ext.) | yes/yes | 🟢 09-23 | not probed inside `s` | ❌ 09-18: **could not even activate** before a same-day fix (assembly-failed identity assert); after the fix, boots with a rendered editor, but `addStep` still refused (`BatchOnlyPendingRewrite`) | biggest single day-over-day win in the 09-18 batch; current live state unverified |
| 📜️imperative (+5 module ext.) | yes/yes | 🟢 09-23 (incl. wasm32-wasip2), test-quick ✅ 3/3 | not probed inside `s` | ❌ 09-18: renders its document but **Actions pane has zero entries** — app never registers window-kind actions at all | worst-of-batch on the interaction axis per 09-18; unverified fixed since |
| 📸️remodel | yes/yes | 🟢 09-23, test-quick ✅ 3/3 | not probed inside `s` | ✅ proven per ticket note ("editor actions dispatch at runtime… confirmed with console logs"), date unclear | recent work is on the SfM reconstruction algorithm, not shell boot |
| 🔋️energy | yes/yes | 🟢 09-23, test-quick ✅ 4/4 | not probed inside `s` | ✅ proven 09-16/17 own playground (9-step interact probe); S13 (09-22) separately fixed a probe defect (`create-zone` missing a required `volumeM3` arg) | window-kit bridge fix landed for `set-node` (S13, 09-22) |
| 🔱️trinity (jack/rewriting) | yes/yes | 🟢 09-23, test-quick ✅ 5/5 | not probed inside `s` | 🟡 boot proven, no clean single-verb capture; S13 (09-22) found the S11 sweep had pinned the **wrong verb** (`setParameter` vs. real `patchNodes`) and re-pinned it | verify wasm component size for browser boot (09-18 note) |
| 🕸️dag | yes/yes | 🟢 09-23 (incl. wasm32) | ✅ **live, 2026-09-24** (S3): opened from Home, graph painted, one WebGL/2D canvas render, `addNode`→undo→redo round-trips **with the graph visibly updated** (DSL text gains `n1:math.add`) | ✅ **PASS, rendered, not just ledger** (S3, 09-24) — the strongest, freshest single proof in this matrix | none currently open |
| 🖍️draw | yes/yes | 🟢 09-23 | not probed inside `s` | 🟡 09-18: boots clean, real interaction landed same day but with 3 deep framework-level decode/close-path bugs found+fixed; suite "191 pass/54 fail", failures classified as harness debt | one test-only `unimplemented!()` in an FSM host test (09-23, documented) |
| 🖨️raster | **yes** (restaged after a 09-24 16:5x deletion — see §0) | 🟢 09-23 | ❌ **refused, visibly and localized**, 2026-09-24 16:5x (S3): `core.wasm` 404 at that time → en/de notice, 0 silent faults. **Disk state now shows the wasm restored; not re-probed live** | ✅ proven 09-1x own playground pre-deletion (most thoroughly proven plugin per 09-18: add/undo/hover, zero refusals) | **re-run S3's probe** before trusting raster opens inside `s` again |
| 🗄️stdio (39 codecs) | yes/yes | 🟢 09-23 (2m28s) | N/A — I/O library, no standalone UI app | S13 (09-22) fixed a real product defect: `set-cell` was a dispatchable-looking palette row with no declared arguments and no editor bridge across all 9 editable stdio editors — **fixed at both roots** | Wave-3 builder/decomposer abstraction still partial per stdio's own AGENTS.md |
| 🗒️note | yes/yes | 🟢 09-23 | not probed inside `s` | ✅ proven (native + documented `interactionSelect`→`deleteSelection` chain), date unclear | `InkCanvasHost` doesn't route `setSelection`/`setHover` into the standard interaction lane (framework contract gap) |
| 🧩️puzzle (2d/3d/5d) | yes/yes | 🟢 09-23 | not probed inside `s`; **Board2dHost pinch/pan wiring untestable live** (U4, 09-24) because puzzle2d's `core.wasm` was not in the staged dist at that time — **now present per this pass's tree check**, unconfirmed live | 🟡 most-proven plugin by ticket volume historically; no single fresh dated capture in this pass | active concurrent 5d editor churn noted 09-23 |
| 🧱️block (2d/3d/5d) | yes/yes | 🟡 not rechecked 09-23 (Batch B: cargo lock contention); 09-18 flagged an **open E0053 async/sync trait mismatch**, unclear if ever fixed | ✅ **live, 2026-09-24** (S3): all three variants open from Home, **2d text updates after redo (6→7 Handle Kinds), 3d renders a real WebGL mesh 1427×780, 5d renders board+world**, one rail verb each round-trips | ✅ **PASS, rendered** (S3, 09-24) — second-strongest fresh proof after `dag` | still the only plugin with **zero committed MCP descriptor** as of 09-18 (`NotFound: no committed descriptor`) — status vs. the catalog unverified this pass |
| 🪐️space (home/space, OS host) | yes/yes | 🟢 09-23 | ✅ **is** the running `s` host itself — every other row's boot evidence implies space booted; S3 additionally verified the footer pill, `/hub` route, sign-in and opening a listed space | n/a (host shell, not an editable artifact in this sense) | Home's own space table stays empty post-sign-in even though the hub workspace lists 7 spaces (S3, 09-24) — unexplained discrepancy |
| 🪵️sourcing (+3 module ext.) | yes/yes | 🟢 09-23 | not probed inside `s` | ❌ by design (S13, 09-22): `curationAdd`/`curationSetCount`/etc. are all `hidden_operation`, `in_palette:false` — reachable only via drag/stepper gestures inside the window, never a rail row | extensions require parent loaded; consumed by `demonstrator` |

**Extensions not broken out above** (26 total: cad×4, imperative×5, playbook×1, sourcing×3, process×4, flow×9):
all staged (wasm present, confirmed this pass); compile green per audit-plugins-a/b; every one's runtime
correctness is gated on its **parent plugin's** row above — none has an independent boot-in-`s` or
mutate/undo/redo proof in any source read for this audit.

---

## 2. User journeys

| Journey | Status | Evidence (file:§ or capture, date) |
|---|---|---|
| Zero-touch first run (`dev s` cold boot → receipt) | **WORKS-LIVE** | Receipt on disk, verified this pass (`🔣️receipt.json`, written 2026-09-24 12:20, 60/60); `audit-os-frontend.md` §4 measured a cold `served` boot HTTP 200, "Activated s react dev: 60 completed components", ~16 min cold (2026-09-23) |
| Zero-touch hub bring-up alongside `dev s` | **MISSING by default** | `audit-os-frontend.md` §5 P3: default `🛠️dev🪐️space⚛️react` launch row carries no `S_HUB_URL` — local-only unless a compound launch is used (2026-09-23, unchanged since); C10 (session 11) "zero-touch clean state, timed" is `ANALYSIS IN PROGRESS`, not measured |
| `PLAYGROUND_SESSION.plugins.length === 60` read from a real browser console | **WORKS-LIVE** | S3 §1.1 (2026-09-24): census read every run, 60 rows, 60 `loaded` |
| Home surface, footer pill, `/hub` route | **WORKS-LIVE** | S3 §4 (2026-09-24): sign-in badge, palette, `/hub` overlay, session survives reload |
| Create/open every kind | **PARTIAL, evidence-weighted** | Fresh rendered proof for `dag`, `block2d/3d/5d` (S3, 09-24); `gis` live+hub-persisted (0918 C7, 09-22); most others only proven in isolated single-plugin playgrounds, several dated 09-16/18 and never reproduced inside the actual `s` host; the historical "30/35" aggregate (S12, 09-22) is **compromised by the id≠bodyKey rendering regression** (§0) and has not been re-swept since the fix |
| Windows/panes/panels (dock, tabs, window rearrange) | **WORKS-LIVE (desktop)** | S3's block/dag windows dock and render correctly; G5 §3 confirms the dual React/wgpu dock system is real, not incidental |
| Command palette (spawn from Home) | **WORKS-LIVE** | S3 §1: `spawn.<pluginId>` rows drive `spawnProgram`/`handle.createApp` |
| Undo/redo + history mechanism | **WORKS-LIVE** (mechanism); **PARTIAL** (UX) | S3: dag/block undo→redo round-trips render correctly; G5 §1.3 finding 1: `MutationKind::label()` — **2690 hard-coded English strings** (393 in norm alone) feed the undo/history panel verbatim — history UI is English-only regardless of shell locale, in every plugin, right now |
| Settings/customization: themes, contrast | **PARTIAL** | U4 (2026-09-24): live WCAG contrast warning wired into the theme editor, en+de, exact ratio formatting per locale — **measured live**; but the **default light theme itself fails WCAG AA on 3 measured pairs** (`mutedForeground`/panel 2.34:1, `mutedForeground`/base 3.54:1 large-only, `activeForeground`/activeHover 4.45:1 large-only) — reported by the new warning, not fixed |
| Settings/customization: layout persistence | **UNKNOWN** | not covered by any source read this pass |
| i18n en/de, no default language | **PARTIAL, mostly WORKS-LIVE** | G5 §1: chrome/product bundles (~402 keys) and manifest labels are compile-checked, 0 missing keys, 0 duplicate-stub translations across 2853 call sites; locale switch re-renders live (0918 S11, 09-22, "measured en→de"); U4 (09-24) added live en/de for diagram keyboard nav and contrast warnings. **Gaps that bypass the system**: `MutationKind::label()` (above), `ShellSync`'s connection-status text (`live`/`connecting…`/`offline`/etc., entirely hard-coded English), and the rejected-mutation toast's raw Rust cause message (intentional, documented tradeoff). `fallbackLng: "en"` is a soft implicit default (defensible under the SHOULD-English-first clause, but technically contradicts "no default language" if a free-text gap is ever hit) |
| a11y: keyboard | **WORKS-LIVE for diagram/node-graph** | U4 (09-24): full arrow/Enter/Shift+Enter/Escape nav + localized live-region announcements, measured en+de, on the actual live `dag` window inside `s` |
| a11y: screen reader / AT (wgpu) | **TESTS-ONLY** | G5 §2.3: a real DOM-mirror ARIA bridge exists for the browser wgpu host (locale-aware `aria-label`s, live refresh) — code-verified and test-exercised, not observed with an actual screen reader in any report; **native (non-browser) wgpu winit build has no AT story at all** |
| a11y: contrast (system-generated palette) | **TESTS-ONLY** | G5 §2.4: default palette asserted ≥4.5:1 by an automated test; not a live-user observation |
| a11y: node-graph beyond keyboard | **PARTIAL** | G5 flagged `🕸️Diagram`/node-graph as fully mouse-only (0 role/kb hits); U4 (09-24) closed the keyboard half live; full AT/role parity for the canvas itself remains the documented, unresolved limitation G5 cited |
| Desktop → mobile → tablet | **PARTIAL, code-verified not live** | G5 §3: a real, dual-implemented (React+wgpu), byte-identical 767px mobile breakpoint exists (corrects an earlier "no mobile layout" claim); tablet exists only as a manual settings override, never auto-detected; no live device-emulation run was found in any source for this pass |
| Touch gestures (pinch/pan) | **WORKS-LIVE for dag + one 3D host** | U4 (09-24) measured live: dag pinch-zoom exact (`1.1^11 = 2.853…`), two-finger pan (Y-axis; X-axis needs one unlanded Rust hunk), gesture latch fix; World3d (block3d) pinch-dolly ratio exact (1/3). Board2dHost/Flow/Canvas2d/Paint2d/TiledMap/Ink are wired+typechecked+unit-tested but **not driven live** (no healthy `s` program was available for each at probe time) |
| Progress + cancellation | **MOSTLY MISSING (UI wiring), backend present** | G5 §4: `TaskManager` panel (the one central progress+cancel surface) is built but **never mounted as a real window** anywhere in the shell; plugin install shows status text but has no cancel button; agent chat panel shows running/succeeded/failed but has no cancel button despite `inference_cancel`/`job_cancel`/`action_cancel` existing and working at the MCP layer |
| Local-first / offline | **PARTIAL** | G5 §5: `ShellSync` popover shows real connection state (live/connecting/reconnecting/offline, pending count) and a bounded-retry backoff mechanism keeps short hub hiccups from freezing the UI (corroborated, not re-run this pass); gap: no persistent, always-visible hub-connection indicator outside each document's own popover; the status text itself is unlocalized (see i18n) |
| Hub sign-in + spaces inside `s` | **WORKS-LIVE** | S3 §4 (2026-09-24): sign-in on hub 7891 as a real user, `/hub` overlay, 7 spaces listed, opening a listed space mounts its space-index window with no notices; caveat: Home's own space table stays empty post-sign-in (unexplained) |
| Lazy install of foreign kinds | **MISSING / UNVERIFIED** | S3 §1.2 + G11: host mode installs all 60 plugins at boot — the lazy-install code path only runs on the hub creation saga, and **no hub with a published catalog existed** to exercise it as of 2026-09-24; session 11's W2 (canonical hub 7800) is still `BOOTING`/not-ready (`/readyz` confirmed `"not-ready"` this pass) and S15's own "lazy install from hub catalog" task is unstarted (`(filling)`) |

---

## 3. Stale-claim flags (explicit)

1. **"35/35 kinds pass the full interaction bar"** — never actually measured; `s13-thirty-five-on-rebuilt-guests.md`'s
   own §2–§5 (guest rebuild, full sweep, hub-document journey, honest gaps) are empty placeholders. The real
   last number is **30/35** (S12, 2026-09-22), and it predates a rendering regression fix that likely invalidates
   an unknown subset of those 30 passes (§0).
2. **"raster refused, `core.wasm` 404"** (S3, 2026-09-24 16:5x) — **disk state has changed**: this pass found
   `raster`'s `core.wasm` present (mtime 2026-09-24 20:01), along with the other 14 guests S3 found deleted.
   Treat "raster opens inside `s`" as **plausible, not proven** until re-probed.
2b. Same correction applies to `puzzle`, `stdio`, `norm`, `gis`, `cad`, `architect`, `process`, `wfc`, `flow`,
   `animate`, `demonstrator`, `vcs`, `energy`, `trinity` — S3's "staged lane no longer 60/60" finding (§0.1 of
   that report) is **no longer true on disk**, but nobody has re-run the census+open probe against the restaged
   binaries.
3. **G1's "no mobile layout system exists"** — directly contradicted by G5 with file:line evidence (§2, Multi-device
   row above). Treat G1's claim as superseded.
4. **"MCP catalog blocked by architect's duplicate `setAdjacencyKind` capability id"** (2026-09-18) — no source
   read for this pass confirms whether this was fixed; treat as **open until re-verified**, not as resolved.
5. **09-18's plugin-by-plugin compile/interaction numbers (`s1-plugin-coverage-matrix.md`)** are 7 days old and
   partially superseded by 09-23's audit-plugins-a/b (compile) and 09-22–09-24's sweep/S3 reports (interaction).
   Used in the matrix above only where no fresher evidence exists, and labeled accordingly.

---

## 4. Gaps, ranked

### P0

1. **Re-run the full "boot→mutate→undo→redo, 0 faults" sweep inside one running `s` session, on the current
   (restaged) wasm, past the id≠bodyKey rendering fix.** Acceptance: ≥35/35 kinds pass with the **body actually
   rendered** (not ledger-only), en+de rows both captured, one capture file per kind. Owner: **S15** (already
   scoped as its tasks #2 and #6; currently unstarted).
2. **Re-verify `raster` (and the other 14 previously-deleted guests) actually open inside `s`** now that their
   `core.wasm` is back on disk. Acceptance: S3's own probe (`🐍️s3-foreign-kind-in-s.mjs`) re-run, raster
   verdict flips from "refused" to "PASS" with a rendered body. Owner: **S15**.
3. **Lazy install of a foreign kind from a published hub catalog, observed live at least once.** Acceptance:
   inside a running `s` session with `S_HUB_URL` pointed at a hub with a real trusted catalog, open a kind the
   session did NOT boot with; `installPlugin`/`openArtifactWithAppRef` network+console lines fire; the kind
   renders. Blocked on **W2**'s hub 7800 reaching ready + a published all-package catalog. Owner: **S15**
   (consumer), **W2** (blocker).

### P1

4. **Localize `MutationKind::label()`** (2690 call sites, worst offenders norm 393 / architect 268 / puzzle 106 /
   block 105) so the undo/history panel is not permanently English-only. Acceptance: `🕰️HistoryTable` renders a
   localized label for at least one representative mutation per plugin, en+de, live. This is a large-blast-radius
   trait-signature change (`🎮️command/🦀️.rs:231`) — needs its own scoped slice, not a drive-by fix. Owner:
   **T12** (plugin contracts) or **NEW**.
5. **Mount `TaskManager` as a real window** and wire a cancel button into `AgentChatPanel` for in-flight tool
   calls — the backend (`ActivationRegistry` suspend/resume/cancel, `inference_cancel`/`job_cancel`/
   `action_cancel`) already works; only the UI wiring is missing. Acceptance: a user can see and cancel a running
   actor task and an in-flight agent tool call from the shell chrome. Owner: **S15** or **NEW**.
6. **Confirm (or fix) architect's `setAdjacencyKind` duplicate-capability-id MCP catalog blocker** — status
   unconfirmed since 09-18; if still present it silently degrades `capabilities_search` for every plugin, not
   just architect's. Owner: **G10** (MCP) or **T12**.
7. **Re-verify per-plugin `test-quick` status** for the plugins flagged red/timed-out on 09-23 (gis fixture drift,
   lowpoly transient ENOENT, mathematical/wfc/fem/architect budget timeouts) — none of these were re-run this
   session. Owner: **R8**.

### P2

8. **Localize `ShellSync`'s connection-status text** (`live`/`connecting…`/`reconnecting…`/`offline`/`saved`/
   `pending`) — small, contained, high-visibility fix (it is the literal offline/reconnect status a user sees).
   Owner: **S15**.
9. **Add a persistent, always-visible hub-connection indicator** independent of each document's own `ShellSync`
   popover. Owner: **S15**.
10. **Auto-detect "tablet" as a real breakpoint** (currently binary mobile/not-mobile; tablet is a manual
    settings override only) if AGENTS.md's explicit desktop→mobile→tablet priority is to be more than partially
    satisfied. Owner: **S15** or **NEW**.
11. **Land the one-hunk Rust fix for dag's horizontal two-finger pan** (`delta_x` currently discarded in
    `wheel_screen`/`plan_wheel`, `🗺️surface/🕸️node-graph/🦀️.rs`) — written out in full in U4's report, not yet
    applied (no cargo in that slice). Owner: **S15** or **T12**.
12. **Give the native (non-browser) wgpu winit build an accessibility story** — the DOM-mirror ARIA bridge only
    exists for the browser host today. Owner: **WG7** or **NEW**.
13. **Investigate why Home's own space table stays empty after sign-in** even though the hub workspace overlay
    correctly lists spaces (S3, 09-24, unexplained discrepancy). Owner: **S15**.
