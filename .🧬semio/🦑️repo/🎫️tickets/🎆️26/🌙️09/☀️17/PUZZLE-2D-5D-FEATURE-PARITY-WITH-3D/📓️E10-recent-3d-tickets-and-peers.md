# E10 — Puzzle 3d tickets since 2026-09-08 and live peer activity (audit, 2026-09-17 ~10:15)

Read-only audit. No source touched.

## Part 1 — Ticket-by-ticket (files touched, 2d/5d debt)

All notes below are informal `📓️` markdown, not `🎫️ticket.json` (no formal status field) unless noted.

### ☀️13 PUZZLE-3D-WINDOW-ADD-OBJECT-BUTTON
- **What**: removed the duplicated `openAddObjectDialog` quick-action chip from `main::engagement` (it was painted on every split window pane via `WindowEngagementOption`). Add-object now only lives in catalogue drag and the shell palette.
- **Files**: puzzle3d `main` editor engagement wiring + `engagement_exposes_no_utility_switch_options` law.
- **2d/5d debt**: check whether 2d/5d `main::engagement` publish the same `WindowEngagementOption` pattern (duplicate chip risk on split panes). If they never had it, nothing to do; if they do, same fix applies in each artifact's editor engagement module.

### ☀️13 PUZZLE-3D-TRANSFORM-GUMBALL-PREVIEW
- **What**: fixed rotate-handle gumball drags being dispatched as `translateSelection` (leftover overlay stomped guest `transformMode`); added `WorldGumballTransformPreviewStore` so sibling `World3d` panes get live drag preview and vortex/attraction markers track the preview delta.
- **Files**: host-side World3d merge/gumball logic (`🌐️World3dHost/🟦️.tsx` region), engine-contract tests.
- **2d/5d debt**: 5d is also 3d-viewport-based (parts/grips/fasteners in a World3d scene) — same `World3dHost` fix is shared infrastructure, so 5d inherits it automatically once it renders through the same host; verify 5d actually mounts gumball transform via this host and not a bespoke path. 2d uses `Board2dHost`, a different component — no direct analog unless 2d has its own transform-gumball-equivalent (drag-handle) with an equivalent live-preview requirement; check `Board2dHost` for a parallel preview-store gap.

### ☀️13 PUZZLE-3D-CATALOGUE-DROP-LIVE-PREVIEW
- **What**: six-layer fix so a catalogue-drag ghost mesh follows the cursor before drop (React `dragover` capture + invalidate-on-demand, wgpu `World3dState.catalogue_drop_preview` + ground-plane pick/snap, `engine_canvas` payload parser, `TreeView` interpreter wiring `catalogueTreeDragController`, wgpu retained-panel drag sync).
- **Files**: `🌐️World3dHost/🟦️.tsx`, `♾️infinite/🌍️world/🦀️.rs`, wgpu `🐚️Shell` target, `⚙️EngineCanvas` wgpu target, framework `🌳️Tree/🟦️.tsx`.
- **2d/5d debt**: this is framework-owned (`World3dHost` + wgpu world target), so 5d catalogue-drop-in-3d-scene inherits it for free if 5d's catalogue rows are wired through `catalogueTreeDragController`/`TreeView` like 3d's (worth a quick check — A2 report below rewired 5d's catalogue panel, verify drag data shape matches). 2d catalogue-drop is a **2D board** drop, not `World3d` — needs its own equivalent ghost-preview wiring in `Board2dHost`/board wgpu target if 2d catalogue drag currently only drops without a live ghost. Confirm against `PUZZLE-2D-END-TO-END` battery (33 PASS/4 FAIL as of 09-17 02:14, per our own ticket description) whether catalogue-drop-preview is one of the 4 FAILs.

### ☀️13 PUZZLE-3D-FILL-ORBIT-CAMERA-RESET
- **What**: stopped the camera auto-reframing during fill/orbit. Root causes: viewport ownership taken too late in the orbit gesture, and fill's `provisional: true` instances counted toward `worldSceneContentBounds`. Fix wires `WorldOrbitGated.onCameraNavigate` (`start` claims ownership immediately) and drops provisional records from framing bounds; locks continuous projection reframe while fill is armed.
- **Files**: `🌐️World3dHost/🟦️.tsx`, engine-contract tests, react target re-exports.
- **2d/5d debt**: framework-owned host fix — 5d benefits automatically if it shares `World3dHost`. Does 2d/5d have a "fill" tool with provisional placements at all? Puzzle-2d has fill (`🪣️fill` precompute dir exists per the recent-file listing below); if 2d's board camera/viewport has an equivalent auto-fit-during-fill bug, it needs a **board-side** fix (different component), not inherited.

### ☀️14 PUZZLE-3D-HIDE-FIT-LANE-FRAME-OVERLAY
- **What**: `World3dHost` painted the manual "Sichtbares einpassen" (frame-visible) overlay button even when the document already publishes an enabled `fitJson` lane for document-driven auto-fit. Added `world3dFrameVisibleOverlayOffered(fit)` gating the button off when `fit.enabled === true`.
- **Files**: `🌐️World3dHost/🟦️.tsx`, react target re-export, engine-contract test.
- **2d/5d debt**: framework host-level — 5d inherits if it publishes a fit lane the same way (`world3d_fit_json`). 2d has no World3d equivalent; if `Board2dHost` has its own manual-frame overlay + auto-fit lane, check whether the same "don't double-offer" rule needs a board-side counterpart.

### ☀️15 PUZZLE3D-MARQUEE-RECT-OVERLAY
- **What**: rubber-band marquee rectangle wasn't painted (selection worked, box didn't draw) in both wgpu (stale legacy `marquee_active`/`marquee_points` fields instead of the live `WorldInteractionAuthority::marquee` gesture) and React (`world3dMarqueeOverlayShape("pick")` returned null so `SelectionMarquee` never mounted for puzzle3d's `pick`-method selection).
- **Files**: `♾️infinite/🌍️world/🦀️.rs` + its unit tests (wgpu world engine, not host-side — fix was Rust-only per the note).
- **2d/5d debt**: this specific bug is in the **3D world engine's** wgpu marquee paint and the React `world3dMarqueeOverlayShape` helper — 5d shares the World3d marquee path if it uses `pick`-method selection the same way (verify 5d's `selection.method`). 2d's board marquee is a **separate** 2D overlay component; check `Board2dHost`/board engine for the analogous "selection works, rectangle invisible" gap independently — the "pick vs rect method" mapping bug pattern (`world3dMarqueeOverlayShape`) may have a `board2d` sibling function worth auditing.

### ☀️15 PUZZLE3D-REFERENCE-3D-VISIBILITY
- **What**: puzzle3d publishes `referencesJson` for reference-image planes, but the wgpu `sync_world3d_state` path never copied that lane into `World3dState.references`, so wgpu rendered zero reference planes (React path presumably fine — bug was wgpu-only). Also fixed reference-image asset fetch (decode pump only published GLB leases at Ready, so `ReferenceImage` bytes never landed).
- **Files**: `♾️infinite/🌍️world/🦀️.rs` (+ unit tests), wgpu `📺️renderer/…/🧊️renderer/🦀️.rs`.
- **2d/5d debt**: "references" (background/context images) is a puzzle-3d-specific concept tied to `World3dScene`; likely N/A to 2d (board, no 3D references) and to 5d unless 5d documents also carry a `referencesJson` lane through the same `World3dScene`. Check whether puzzle-5d ever publishes `references` — if yes, this wgpu-copy bug class applies there too (same `sync_world3d_state` code path, framework-shared), if no, no parity item.

### ☀️15 PUZZLE3D-OBJECT-TREE-LABELS
- **What**: document outliner showed kind ids / opaque ids while the 3D viewport showed authored labels. Added `puzzle3d_object_display_label` (label → catalog display name → id precedence) and `puzzle3d_next_object_label` (auto-numbering " 2", " 3", … for duplicate kinds), used by outliner `object_row`; fill/brush/add-object/accept-suggestion/duplicate paths now stamp labels at creation/publish time.
- **Files**: puzzle3d editor (label helper functions, `object_row`, fill run-ops, brush/add-object/suggestion/duplicate command paths) + a unit test.
- **2d/5d debt**: this is puzzle3d-artifact-local logic (not framework-shared) — **2d and 5d need their own copies**. Check puzzle-2d's outliner `object_row` (or node/edge row builder) and puzzle-5d's part/grip/fastener row builder for the same "shows raw id/kind instead of authored label" gap, and whether 2d/5d have any multi-instance auto-numbering at all for duplicate kinds dropped from the catalogue.

### ☀️15 PUZZLE-3D-CAMERA-NO-IMPLICIT-JUMP
- **What**: camera jumped on catalogue placement / fill / other edits with no explicit camera action. Two causes: `WorldAutoFit`'s fit key included the scene mesh roster (adding a catalogue kind re-armed auto-fit), and `WorldProjectionContentFrame` kept reframing on growing `contentBounds` before `viewportOwned` was set (esp. orthographic Top pane). Fix: `world3dAutoFitKey` drops the mesh list unless the producer explicitly publishes bounds (generation3d does; puzzle3d doesn't); lock continuous projection reframe + set `viewportOwned` after the first seed.
- **Files**: `🌐️World3dHost/🟦️.tsx`, engine-contract tests. Explicitly related to/supersedes ☀️13 FILL-ORBIT-CAMERA-RESET.
- **2d/5d debt**: framework-owned `World3dHost` fix — 5d inherits automatically if it mounts through the same host and publishes a revision-only `fit_json` the same way puzzle3d does (worth confirming puzzle-5d's fit-lane shape matches `world3dAutoFitKey`'s expectation). 2d's board camera has no `World3dHost`/`WorldAutoFit` — any analogous "camera jumps on catalogue drop" bug in the 2D board is a separate, unrelated fix surface (board pan/zoom autofit, if one exists).

### ☀️16 PUZZLE-3D-ARTIFACT-TREE-PAGING
- **What**: the truncated "ellipsis" row bug — virtualised outliner's continuation row (`+N`) used icon id `ellipsis`, which isn't in the vendored icon catalog, so it rendered as garbled fallback text instead of a `⋯` glyph. Fix: map `ellipsis` → `more-horizontal` in `CATALOG_ICON_ALIASES` + asset icon projection aliases, and emit `more-horizontal` from `panel_continuation_row` and the puzzle3d/cad continuation-row builders.
- **Files**: framework icon alias tables + panel continuation-row builders (shared + puzzle3d/cad).
- **2d/5d debt**: this whole continuation-row/paging mechanism (`panel_continuation_row`, `+N` rows, `setPanelPage`) has since been **deleted entirely** by the ARTIFACT-TREE-VIRTUALISED-STREAMING ticket's A1/A2 packets (see Part 1 note below) — 2d and 5d panels were migrated off paging to the new `TreeWindow` mechanism in the last ~12h, so this specific ellipsis bug class no longer applies to any of the three artifacts once that ticket lands.

### ☀️16 ARTIFACT-TREE-VIRTUALISED-STREAMING (open, `🎫️ticket.json` present, **LIVE right now**)
- **What**: framework-wide replacement of the `+N` continuation-row paging mechanism with a `TreeWindow` streaming mechanism (lazy children on expand, viewport-sized materialisation, scroll-driven streaming) — one mechanism, applied to every app's panel trees. This is not a puzzle-3d-only ticket; it directly did the 2d/5d migration itself.
- **Directly relevant — packet A2 (`📓️a2-puzzle2d-5d.md` in this ticket's folder) already migrated puzzle-2d and puzzle-5d panels**: artifact/outliner, catalogue, and inspection panels in both crates moved from the old `paged_section`/`SECTION_ROWS`/`IDS_ROWS`/`setPanelPage` mechanism to `PanelTreeBuilder::window_section_or_placeholder` + `tree_window_item`/`TreeWindows`, deleted the `set-panel-page` command, `panel_pages` window-transient field, and associated schema (`.ts`/`.json`/`.graphql`/`.proto`, hand-edited since puzzle has no schema-gen nx target). New unit tests added (2d: 11 passed panel laws; 5d: 9 passed), `cargo check --target wasm32-wasip2` green for both. Reported done as of ~02:44 on 2026-09-17.
- Packet A1 (puzzle3d) did the equivalent migration for puzzle-3d's panels in parallel (see `📓️a1-puzzle3d.md`, not read in full here — same mechanism).
- **Status as of this audit (~10:15)**: ticket is still open, in "wave 4". Per `📓️status.md`: wave 3 browser verification found two defects (F1: scroll observer bound to unbounded inner div so streaming never re-requests more rows; F2: fem3d House body exceeded the 128-node SDK ledger cap) — both are framework/SDK-level, not puzzle-specific, but they gate whether puzzle3d/2d/5d's new tree-window panels actually stream correctly in the browser. A6 (procedural/process3d/block) and A8 (remaining plugins) are unfinished/stale. Wave 4 (launched ~10:00, this session's predecessor) relaunched F1, F2, A6a/b, A8a/b/c, plus **R1 = puzzle+cad re-verify** and **R2 = fem/energy/A7 re-verify**, and three Sonnet audits (S1 plugin residue, S2 framework/TS residue, S3 streaming-loop correctness). Coordinator's own status.md (10:00–10:08) confirms this fleet is currently running.
- **Peer-liveness signal**: `git status` shows **uncommitted `M`/`MM` changes right now** in puzzle 2d/3d/5d panel files (artifact, catalogue, inspection panel `.rs` + their unit tests, plus `🎚️config`, main editor `🦀️.rs`, fixtures) — this is that ticket's live fleet (R1 in particular) touching puzzle files concurrently with this audit. **Do not assume today's on-disk puzzle 2d/5d/3d panel code matches what's in ☀️16's A1/A2 reports** — R1/R2 may still be adjusting it.
- **2d/5d debt**: none directly — this ticket already delivers 2d/5d parity for the artifact-tree/outliner/catalogue/inspection panel windowing mechanism. Coordinator should treat "outliner"/"inspector" parity items in our own ticket's scope as **substantially covered by this peer ticket** once it closes; recheck after it lands rather than re-doing the panel-tree migration ourselves.

### ☀️16 INSPECTOR-PANEL-TREE-MIGRATION
- **What**: an audit-only note (not a fix) cataloguing which plugin inspection panels are migrated to the `PanelTreeBuilder`/`tree_item` pattern vs. still using `ui::column`/`ui::field`. States puzzle* (all artifacts) are "already tree-native" — i.e., puzzle 2d/3d/5d inspection panels already used the tree pattern before this audit; the gap list (fem2d/fem3d/energy/shooting/animate/block/dag/architect/lowpoly/space) does not include puzzle.
- **2d/5d debt**: none — puzzle already conformant per this audit.

### ☀️16 INPUT-CAUSALITY-LEDGER (open, `🎫️ticket.json` present, large ongoing ticket)
- **What**: framework-wide input/gesture architecture replacing the 8s `setActiveUtility` echo-off and ad-hoc busy/owner-mismatch gates with a provenance-carrying ledger, CAS interaction registers, batched gesture samples, and a typed refusal vocabulary across every transport (direct actor, browser-actor hub, wgpu). Not puzzle-specific but touches `onAction`, `Canvas2dHost` gesture sampling, and reactor-side interaction-verb folding that puzzle's editors dispatch through.
- **Status**: proven end-to-end on the fem2d React lane (2d canvas gestures); hub-mounted lane not yet browser-run. A 2026-09-17 addendum found and fixed a false-positive toast spam in process3d (refusals now correctly logged-only for gesture-origin inputs, not toasted) — **worth checking puzzle3d/2d/5d for the same false-refusal-toast risk** if their `setCamera`/`worldSelect`-style dispatches hit the same `dispatch-failed`/`undeclared-action` gates, since the fix (`InputOriginV1 = "gesture"` stamped in `renderComponentSceneHost`) is host-wide and should already cover puzzle's scene hosts, but it's worth a smoke check once this ticket closes.
- **2d/5d debt**: none puzzle-specific to add; inherited automatically as framework infra once puzzle's `Canvas2dHost`/`World3dHost`-mounted gestures run through it — no separate puzzle work implied.

### ☀️16 R3F-SCENE-HOST-PORT-WIRING (fix note, not ticket.json)
- **What**: fixed "R3F: Hooks can only be used within the Canvas component!" across every `world-3d` surface including puzzle, caused by two fiber-store copies (app-level `@react-three/fiber` imports vs. Vite-prebundled `.vite/deps` copy used by `@react-three/drei`'s `PerspectiveCamera`). Routed `World3dHost`, tool-run trace, and `Scene` hook usage through `sceneHostPort.fiber` (adds `useLoader`); excludes fiber/drei from Vite prebundle instead (`playgroundSceneHostOptimizeDeps`).
- **Files**: `SceneHostPort.fiber`, `World3dHost`, `threeHostPort.canvas`, os-dev + mit-bestand demonstrator Vite configs.
- **2d/5d debt**: framework/host-level, applies automatically to any World3d-mounted puzzle surface (5d). Note this same commit (`8773331d23`) later broke fresh React dev serves black (CJS prebundle exclusion regression) — fixed in the INPUT-CAUSALITY-LEDGER ticket's `PLAYGROUND_SCENE_HOST_CJS_INCLUDE` addendum above; if your own dev serve boots black on `scheduler`/`use-sync-external-store` export errors, that's this chain, already fixed upstream.

### ☀️17 (today) — puzzle-adjacent but not puzzle-owned
No puzzle-titled tickets opened yet today besides our own PUZZLE-2D-5D-FEATURE-PARITY-WITH-3D. Two other ☀️17 tickets touch shared framework pieces puzzle depends on:
- **ENERGY-MESH-SELECTION-HOVER-PARITY** (`📓️fix.md`) — fixed `World3dHost`'s `PaintTexturedMesh` to only keep vertex colours for `neutral`/`disabled` styles and use solid fill+emissive for `hovered`/`selected` (previously forced white/no-emissive for all vertex-coloured meshes, so host selection/hover tokens never applied). This is the **shared `World3dHost`** paint helper — worth a quick visual check that puzzle3d's vertex-coloured meshes (if any) still look correct, though puzzle3d was cited as the visual reference this fix aligns energy *to*, so it should already match.
- **DEMONSTRATOR-GENERATOR-SHIP-PLUGIN-BOOT** (`📓️root-cause-and-fix.md`) — fixed static/production plugin+extension loading (`createBundledPluginSource`, `ShellHost` PROD branch) so a built `dist/site` bundle loads all plugins, not just the primary. Framework kernel/`ShellHost`-level; affects any deployed demonstrator that includes puzzle as a non-primary plugin, not puzzle-specific.

## Part 2 — Live peers touching puzzle / shared framework right now

**Bottom line: at least one other agent fleet (ARTIFACT-TREE-VIRTUALISED-STREAMING, wave 4, launched ~10:00 today) is editing puzzle 2d/3d/5d panel files concurrently with this audit.** Coordinate before touching the same files.

- **Ticket liveness**: two puzzle-adjacent tickets are still `"status": "open"` with live `🎫️ticket.json`: `☀️16/ARTIFACT-TREE-VIRTUALISED-STREAMING` (goal `AI-OPTIMIZED-REPO`) and `☀️16/INPUT-CAUSALITY-LEDGER` (goal `FRAMEWORK`). Both are large, multi-wave, multi-agent efforts with status.md entries timestamped literally this morning (10:02–10:08 for the tree ticket).
- **Files changed in the last ~12h under the puzzle plugin** (`find … -newermt "2026-09-16 22:00"`): build artifacts (wasm dist/pkg), plus source across all three artifacts — `◻️2d`, `🧊️3d`, `🖐️5d` `✏️editor` trees: `📌️panels/{🗿️artifact,🛍️catalogue,🔍️inspection,⚙️settings}/🦀️.rs` + their unit tests, `🪟️window/🦀️.rs` + schema (`.ts`/`.json`/`.graphql`/`.proto` for 2d), `🎚️config/🦀️.rs`, `🎮️commands/…`, `🧫️fixtures/🗄️retained-jobs/🔣️.json`, `🧫️fixtures/🔏️publication-authority/🔣️.json`, plus 2d's `🪣️fill` precompute and 3d's `🧪️tests/🔬️selection-scale`. This matches exactly the ARTIFACT-TREE-VIRTUALISED-STREAMING migration footprint (panel-tree windowing) described in Part 1.
- **Right now, uncommitted (`git status --porcelain`)**: `M`/`MM` changes in puzzle 2d/3d/5d panel `.rs` files and their unit tests, `⚙️settings`, main editor `🦀️.rs`, `🪣️fill` unit tests, and both fixture JSONs — i.e., the tree-windowing fleet (likely R1 "puzzle+cad re-verify" from wave 4) is actively editing these files as of this audit.
- **`World3dHost/🟦️.tsx` and `Board2dHost/🟦️.tsx`**: both modified within the last 12h (framework-shared React hosts puzzle 2d/3d/5d all depend on) — consistent with the many World3dHost-touching tickets in Part 1 (gumball preview, catalogue-drop-preview, fill/orbit camera reset, hide-fit-overlay, camera-no-implicit-jump) plus today's energy mesh-paint fix and yesterday's R3F port-wiring fix, all landing in the same file.
- **`♾️infinite` (board/world engine, wgpu)**: `🎲️board/🔌️ports/➡️directed/➕️normal/🦀️.rs` + its standalone test modified in the last 12h — board (2D) port/engine churn, worth checking before touching 2D board-side puzzle code.
- **git log --date=iso** on the puzzle dir and on World3dHost/Board2dHost: the real HEAD commit for both paths is `a4cda597ea` at **2026-09-17 01:10:14** (matches repo HEAD) — auto-commit hasn't run since ~01:10 despite the above files having much more recent mtimes and live `git status` diffs, so a large amount of fresh work (the wave-4 fleet) is sitting uncommitted in the working tree right now. Commit messages carry a fake constant date suffix (`🎆️26🌙️06☀️04`) — ignore it, `--date=iso` above is the real author date.

## Recommendation for the coordinator

1. **Panel-tree/outliner/catalogue/inspector windowing parity is being delivered by ARTIFACT-TREE-VIRTUALISED-STREAMING's A2 packet already** — do not re-implement it; wait for that ticket to close (or read its final A2/R1 state) before auditing outliner/catalogue/inspector parity gaps between 2d/5d and 3d, since the underlying panel mechanism is being rewritten under you right now.
2. **True puzzle-3d-only feature debt for 2d/5d to actually build** (not inherited from a shared host/framework fix): object-tree authored-label logic (`puzzle3d_object_display_label`/`puzzle3d_next_object_label`, ☀️15 OBJECT-TREE-LABELS) — puzzle-2d and puzzle-5d need their own label-precedence + duplicate-numbering functions in their respective outliner row builders. Everything else in Part 1 tagged "framework-owned"/"World3dHost" is inherited for free by 5d (and by 3d/5d's shared World3d rendering) and does not need separate 2d/5d implementation work, only verification.
3. Given live concurrent edits to puzzle panel files, coordinate file ownership with the ARTIFACT-TREE-VIRTUALISED-STREAMING fleet before any 2d/5d implementation wave touches `📌️panels/*` files.
