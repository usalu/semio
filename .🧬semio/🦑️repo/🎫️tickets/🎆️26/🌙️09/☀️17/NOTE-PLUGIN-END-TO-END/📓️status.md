# 🗒️ Note plugin end to end — status

## 2026-09-17

- Ticket opened on disk (repo MCP timed out). No prior NOTE-*-END-TO-END ticket.
- Audit of the editor root (`✏️editor/🦀️.rs`) against the layout/forms fault families:
  1. Playground `app` missing in `📦️packages/🦀️rust/Cargo.toml` → added `app = "s.note.note@1/*#editor"` (forms fault 1: `primary plugin note does not declare pinned app ""`).
  2. No `command_from_action` → every shell action refused as "not framework-reserved". Added `args_bridge` (snake fold, integral floats, text/number/bool coercion of control `value`s, panel quick-add `addBlock {kind}` defaults `x/y=0`, `inkApplyEvents` `events`→`events_json` + phase default, flat `{x,y,zoom}`→`camera`, `blockId`/`id`→`block_ids` list, null `engagementSubmit.value` stays `None`) + two unit tests (every row round-trips from camelCase; host control contracts).
  3. 26 of 34 verbs still `BatchOnlyPendingRewrite` (every document verb: `addBlock`, `patchBlocks`, `inkApplyEvents`, `deleteSelection`, nudges, grid/snap/pencil/eraser options, `setActiveExample`, `setFixtureJson`, `saveDownload`) → hard-dead at dispatch. `NoteCommandWork` was already generic over every `NoteCommand`, and the full contract table already existed as the unused `NOTE_AUDITED_*` lists — collapsed them into `NOTE_RETAINED_TOOL_IDS`/`NOTE_RETAINED_PUBLICATION_CONTRACTS`, proofs `tools` now lists all 34 rows, manifest flipped to `Migrated`.
  4. The artifact one-item preparation factory (layout fault 4) already exists in note.
- `cargo check -p semio-s-plugin-note --lib` green (10:26 local, 9m39s); the one new warning (qualified `DslValue`) fixed before restage.
- Restage #1 started 10:27 local: `screen note-activate` → `🗑️generated/activate-note-react.txt`.
- Restage #1 done 11:04 local (32 min under load avg 170, `component-dev` 23 min). Serve: `screen note-serve` (vite 6080, `?plugin=note`).
- Boot probe #1: every module 500 — `🏛️ShellHost/🟦️.tsx` did not parse. Root cause and repair (15 renderer/kernel/actor files damaged by a peer's `[DEBUG]` console strip codemod, including silent behaviour changes in action dispatch): `📓️fix-2026-09-17-fused-debug-strip.md`.
- Boot probe #2 (`🗑️generated/note-boot-2`): `data-semio-os-ready=note`, Canvas + Navigator windows, Artifact/Catalogue/Inspection panels. Boot `setActiveExample` (now dispatchable) trapped: `artifact envelope terminal shell reached Drop before its app-owned bounded retirement authority detached every nested owner` → `reset_document_effect` minted a live envelope just to print the spr (process3d fault 1). Now `store::empty_document_spr("note", NOTE_DOCUMENT_SCHEMA)`; also added `build_document_store_initialization_job` (`bounded_document_store_initialization_job`, fem2d shape) — the trait default refuses every `Effect::LoadDocument`. No `type Members` needed: `NoteSnapshot` declares no `#[child]` field, so `visit_child_refs` never reaches the nested text handles.
- Known gap (host, not note): `InkCanvasHost` dispatches `setSelection`/`setHover` to the app; nothing maps them onto the framework-owned `interactionSelect`/`interactionHover`, so canvas-click selection is refused for ink-canvas apps.
- Restage #2 started 11:21 local.
- Restage #2 done 11:32 local (11 min). Boot probe #3 (`🗑️generated/note-boot-3`): no trap, no fault, `setActiveExample` settles — but both windows render "No scene". Two note↔ink-canvas wire drifts (note is the only `SurfaceKind::InkCanvas` consumer; the framework host defines `ink.document`):
  1. Outbound: `note_canvas_document_json` emitted `schema: "note.document"` and text blocks with the composed `content: {handle, paragraphs}` record; `InkCanvasHost.parseInkScene` requires `schema === "ink.document"` and bare `paragraphs`. Now projected onto the ink wire (`INK_CANVAS_DOCUMENT_SCHEMA`, group children recurse).
  2. Inbound: the host sends `InkCanvasEvent`s tagged `operation`; `NoteCanvasEvent` decoded `tag = "mutation"` (09-02 rename) with `from_json_str(..).unwrap_or_default()`, so every canvas gesture silently decoded to an empty batch. Tag is `operation` again, text blocks' `paragraphs` are rebuilt into `note_text_child_record`, and a malformed batch faults `note.ink-events.invalid`. Tests: `ink_wire_text_block_round_trips_into_the_composed_text_record`, `malformed_ink_event_batches_fault_instead_of_vanishing`; the gesture test now sends host-shaped blocks.
- `cargo check -p semio-s-plugin-note --lib` green (15:00 local). Restage #3 started 15:01 local.
- Restage #3 done 15:02 local (2 min, idle machine). The serve had died with the session; restarted (`screen note-serve`). Boot probe #4 (`note-boot-4`): canvas grid renders in both windows, no faults.
- Interact probe #1 (`🐍️note-interact-probe.mjs`, `note-interact-1`; counter = distinct `data-ink-block-id` in `window:note-composite`): the bridge and dispatch work (`addBlock` reaches the retained tool), but every document verb is refused `typed-operation failed: Note one-item Artifact preparation admits only exact retained root-scalar mutations on the document lane` — note's bespoke `NoteRootScalarPreparationFactory` admitted only `ChangeGridVisible`/`ChangeGridSpacing`. Replaced by `bounded_config_store_one_item_preparation_factory::<NoteSnapshot, NoteMutation>("note-artifact-retained", 16 KiB)` (layout/dag/remodel shape); the ~1 700 lines it alone used (text/block/link/snapshot materialization cursors, `NoteOwnedRetirement`, root-scalar preparation) and their `🧪️tests/🔬️materialization` file are deleted. `cargo check` warning-free.
- Also seen: `setHover`/`setSelection` from `InkCanvasHost` dropped as undeclared app actions — the ink scene carries no interaction `domainId` (Board2d's does), so the host cannot route onto `interactionHover`/`interactionSelect`. Framework contract change, out of scope here.
- Restage #4 started 15:06 local.
- Restage #4 done 15:08 local. Interact probes #2–#3 (`note-interact-2/3`), zero fault lines: Actions-pane `addBlock` +1 · `addBlock` then mod+z (created 2 → 1) · Artifact panel "Add Math" (+1 block, +1 tree row `Math math`) · tree row click → `aria-selected` · Delete key (`deleteSelection`) −1 · mod+z restores. Reload resets to 0 because the playground re-applies the Demo example (`LoadDocument`) on every boot — by design.
- Native `cargo test -p semio-s-artifact-note-note --lib` (targeted): the four new tests pass (`command_from_action_round_trips_every_command_id`, `command_from_action_bridges_host_control_contracts`, `ink_wire_text_block_round_trips_into_the_composed_text_record`, `malformed_ink_event_batches_fault_instead_of_vanishing`). The two gesture tests fail `interactive-job.catalog-authority` — registry-less `note_app()` harness debt (same family as layout's 30). A first run also hit a transient `semio-s-artifact-stdio-pdf` compile failure (`PdfVtMutation: Mutation<PdfSnapshot>`) that did not reproduce (`cargo check -p semio-s-artifact-stdio-pdf` green with and without `artifact-app-testing`).
- Demo example empty: `semio_example_dsl_round_trips` failed `expected RBrace, found Ident 'child_id'` — the demo asset still spelled the text child flat (`content=child_id=… target=…`) from before the composed record gained `handle`/`paragraphs`, and `semio_example_snapshot` silently fell back to an empty document. The asset is now the canonical `print_dsl` output (one `Welcome` text block, "Welcome to semio note"), generated and parse-verified by a temporary test (removed).
- Canvas pencil drag (probe #4/#5) still ran as `selectDirect` (`setSelection` dropped) although the `pencil` utility was armed: note read only the flat `view_state.active_utility_id` (draw's per-window utility bug). Added `note_active_utility` (window-instance entry → focused window entry → flat → `selectDirect`) at all three sites.
- Restage #5 started 16:05 local.
- Restage #6 done 16:18 local. **End-to-end proof** (`🗑️generated/note-interact-8/`, `🐍️note-interact-probe.mjs`, eleven steps, zero fault lines except the known host `setHover` drop):
  1. boot: `data-semio-os-ready=note`, Canvas + Navigator render the Demo document ("Welcome to semio note"), panels Artifact/Catalogue/Inspection, no faults.
  2. Canvas Actions pane `addBlock` (kind=text) → Execute → blocks 1 → 2.
  3. `addBlock` again (→3) then immediate `mod+z` → back to 2 (the mutation is in the document store's undo history).
  4. Artifact panel quick-add "Add Math" → 3 blocks, new tree row `Math math`.
  5. Tree row `Text text` click → `aria-selected=true` (framework-owned `blocks` domain).
  6. `Delete` → `deleteSelection` → 2 blocks; `mod+z` → 3.
  7. Utility bar → Draw → Pencil, pointer drag on the canvas → `inkApplyEvents` persists a stroke (Inspection `Blocks 3` → `Blocks 4`, `Utility pencil`); `mod+z` → 3.
  8. Page reload → Demo example reloads (1 block): the playground re-applies the example on every boot by design.
- `bun nx run @semio-tech/note-plugin:describe` regenerated `🔣️.json` + `🛂️.descriptor.semio` (every command row `interactiveJob: migrated`, no `batch-only` verbs left).
- Launch entry `note-react-attach` (6080) added to `.claude/launch.json`.
- Native (`cargo test -p semio-s-artifact-note-note --lib`, targeted): 8/8 pass — the four new bridge/ink-wire tests, the rewritten `set_active_example_loads_the_registered_demo_and_refuses_unknown_ids`, `set_fixture_json_replaces_document`, and both DSL round trips (`semio_example_dsl_round_trips` was failing before this ticket).
