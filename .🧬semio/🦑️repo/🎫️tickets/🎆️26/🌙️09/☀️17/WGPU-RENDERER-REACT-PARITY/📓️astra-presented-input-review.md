# Presented Input Review

The presentation repair is under review before Native98. It correctly delays the hit/owner/widget/geometry swaps until GPU acknowledgement, but the current candidate is identified only by input generation and requires that value to increase. This is not a unique presentation identity. `RuntimePresentationAuthority` starts input generation at zero, and `mark_scene_changed` advances scene revision independently of input generation. A resource-only presentation or repeated redraw can therefore carry the same input generation as an earlier accepted frame. The strict comparison would reject valid later frames; zero is also a valid initial input generation.

The repair needs one exact candidate witness carried from the completed chrome walk through `AppFramePresentation`, with stale/duplicate checks on that witness. A test must present two different candidates under the same input generation and must admit initial generation zero. Comparing only scene revision and input generation also needs justification for repeated presentations of the same pair.

The added moved-control handler law currently registers direct event-bearing hits. It demonstrates that the published hit vector preserves position and direct binding, but does not exercise `route_retained_pointer_press`, which can consult the candidate retained tree. A retained widget under an unchanged key must keep its displayed geometry and binding until presentation; the stronger test must use that actual path. Both findings were sent to the implementation owner. No fresh native or browser acceptance is claimed.

Sol replaced the input-generation token with an exact Shell-minted candidate witness carried through build/preparation/presentation, and added an actual retained Button two-revision law. Select, NumberStepper, keyboard and accessibility generation qualification remain explicitly open. Native98 full compilation failed before tests with one missing `AppFrameBuild.input_candidate` field in the existing empty-preparation cancellation fixture (`wgpu-frame-job-unit`, line93); Nx3m29s. Root made that deliberately empty fixture explicitly carry `None`. No native behavior was tested in this run. Receipt: `🗑️generated/astra-runtime/renderer-native98-full/run.log`.

The next review found an unsafe Button-only fallback: retained pointer dispatch still runs the live candidate router, then manufactures the presented action on any pointer release when that router emits no action. Release without a press, release after cancellation, and press-A/release-B can therefore activate a displayed button. It also leaves candidate focus side effects possible. Sol added three real retained-path fail-first laws for these sequences without changing the production fallback. Native99 now includes those laws, the two-revision law, and the 65 sequential window lifetime law. The required repair must preserve the full presented retained interaction authority, including Select, NumberStepper, keyboard, and accessibility; copying one action descriptor is insufficient.

Native99 completed1304 tests:1272 passed,32 failed, zero skipped;39.508s test time, Nx4m13s. The four retained Button laws did not reach behavior: the fixture omitted required Button `icon`. Sol corrected it to the current complete Button shape. The remaining failures include direct-render test helpers whose candidate geometry has not gone through the now-required presenter acknowledgement; these are under source review and are not automatically classified as harmless fixture failures. The65-window law separately reproduced a real registry capacity failure. Native100 targets the corrected presented-input laws and the complete window-retirement path. Button production remains unchanged until a valid native fail-first receipt.

## Native101 Valid Failure Receipt

Native100’s positive binding assertion read Shell state before processing admitted actions, so that failure was a test observation error. Native101 fixes all four Button observations to inspect admitted actions and includes five generic presented-input laws. Actual result: 13 run, 3 pass, 10 fail, 1298 outside the filter; 114 ms tests, Nx 3m9s. The ten failures comprise eight real presented-input violations (release without press, cancelled press, cross-button release, Select, NumberStepper, keyboard focus, accessibility, stale interaction epoch) and two queued-scene close races. The positive old binding law now passes. Sol owns a generic retained input revision repair; root owns scene close retirement. Log: `🗑️generated/astra-runtime/renderer-native101-presented-and-scene-close/run.log`.
