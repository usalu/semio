# Checkpoint 13 Visual Inspection

The actual paired General Settings screenshots were inspected side by side at 1600 × 1000, DPR 1. Evidence is under `🗑️generated/astra-runtime/paired-checkpoint-13-confirmed/{react,wgpu}/05-settings-general.png`.

React paints the compact General tree above the footer, approximately y=615–952. WGPU paints its labels near y=29–360 while a second set of inline control faces appears around y=638–802. Its right panel glass occupies almost the full content height. Physical General/Drivers hit checks passing therefore does not establish paint parity. Sol Window owns diagnosis of the initial layout/root extent and the local-to-frame paint authority, ahead of accessibility completion. Checkpoint 13 excludes the newer Select popup translation repair, whose actual mounted Up-flow activation law passes in UI28; that repair alone has not been assumed to fix this separate closed-control/tree displacement.

Reference images differ visibly in contrast/opacity: React is muted and translucent while WGPU displays strong black linework and a gray backing. This is separate from the measured synchronous decode stall. Sol Chrome owns checking React's actual reference opacity, tint and color contract alongside the decode fixture; no acceptance follows merely from completing decode off the frame thread.

The initial model is absent in WGPU. Camera3 and the newly identified instance-position producer fix are queued for World15 and a fresh browser checkpoint. Navbar gaps/drag grips and Dock body spacing in these screenshots predate the geometry packet and require a new activation before comparison.

A short isolated fullscreen journey passes enter and exit in both renderers, despite the longer close/reopen journey failing both transitions. The difference count in the short probe reflects React shell-command bookkeeping, not a fullscreen failure. Root is measuring the close/reopen focus and keyboard event path before changing production code.
