# WGPU Diagnostic Checkpoint 14 Runtime

## Artifact Boundary

Activation14 completed seventeen prerequisites, including renderer WASM and the Puzzle3D component, but failed its original frame-worker schema-owned import registration. The taxonomy registration was repaired and focused frame-worker generation passed in 54.9 seconds. Final prepare/activate did not run. Existing owned React7 and WGPU servers served the sealed diagnostic cohort. All five artifact hashes were unchanged after both the fifteen-step baseline and first General-only journey. WASM SHA-256: `2fcbda8f46240f76a037694ed089f66caeec8621433de2bc76d7e450a0a2f9de` (127,167,466 bytes).

The cohort includes per-instance S2a material scalar shading, camera framing, browser keyboard-root ownership and modifier reset, panel/tab geometry, accessible Select publication and asynchronous browser reference decode. It excludes later pointer modifier snapshots, fullscreen-root ownership, codec orientation/wake/SVG follow-ups and S2b textured/conic materials. Source changes are not silently credited to this runtime.

## Actual Browser Journey

The paired fifteen-step baseline completed with one physical failure: exiting fullscreen after window close/reopen. Entry succeeds; trusted chord events initially target an owned mirror DIV, then the fullscreen transition moves focus to BODY and the exit shortcut is ignored. The root fullscreen owner is repaired separately and has an actual isolated Chromium receipt, but requires the next full application activation. Eleven ledger comparisons differ; ledger counts are not interchangeable with physical success.

Models are now visibly resident in both Top and Perspective, including the GLB slab and legs. `dumpMeshStats` describes producer wire payloads and may report zero vertices for URL-backed assets, so those zeros are not resident-load evidence. Early boot and dismiss-tour snapshots precede final camera convergence. Perspective matches React by settings-open (maximum position component delta 0.00005, target 0.00001). Top's target and zoom nearly match, but position z differs: React 21.3572, WGPU 34.1729736, target z approximately 1.5015. The orthographic projection suppresses a direct scale difference, but camera distance/clipping parity remains open.

The settled General screenshot still paints a full-height glass body (approximately y29–953), with correctly bottom-aligned labels and controls around y620–950. React's body is compact. The missing normal-ingress → accepted retained layout → second Shell render test is assigned to the window lane. Appearance/Language trigger dimensions also differ (React 160×16, WGPU 120×22.4 in this cohort).

References are resident in both panes, but WGPU's gray rectangular plane and near-black walls differ from React's muted cream composition. Material, alpha and color-space parity remains open. The isolated decoder oracle is separate from this visual inference.

## General Interaction Harness Boundary

The first nine-step General-only run reports eight errors across the four selection operations because the probe chose enclosing property/tree rows with duplicate keys instead of the actual Select trigger. It is not accepted as a product failure. The harness now filters actual `select-trigger` / `Select` rows and resolves React's combobox rather than the first duplicate ID; the corrected run is pending. Selected-value assertions remain mandatory.

## Native Gate Boundary

Native24 ran all 1,185 tests: 1,183 passed and two newly added decode tests failed at fixture authority Drop because the test's IO reservation was never terminally closed. The fixtures now close their exact authority; production staging behavior was not changed to hide the failure. The next native gate will sample the next coherent source cohort.

Inputs and complete generated receipts are retained in this ticket. The renderer is not yet feature complete.


## Corrected General Journey14b

The corrected nine-step journey opens both renderers' Appearance Select with physical pointer input. React commitsdarkand subsequentlydewith selected-value confirmation. WGPU physically clicksDarkand dispatches`setAppearance`withvalue`dark`, but the popup remains visibly open, the trigger remainsSystemand the canvas remainslight. The later Language steps are blocked by that retainedpopup; the run reports3physicalfailures. This is product evidence, unlike14a's duplicate-ID targeting.

Opening the Select also makes General's glass shrink to approximatelyy610–953. This supports a missing layout-publication/repaint wake before the next useraction; it does not establish a permanently wrong content extent. SolWindow owns the normal ingress/repaint/selection regression. Appearance options have duplicated text in the actual screenshot and remainopenafterclick.

## Profile14

The separate WGPU-only three-step CPU-profile journey completed without physical errors; all5sealedartifacthashes remainedunchanged. RawframeWorker samples contain no oldimage `load_from_memory`/resample callchain. The stagedreferencehandoff contributes2.276msinclusive sampledtime. The browser-input-batch workerTick lifetime maximum is100.1msinthisprofile, versus approximately2444msinthepriorcheckpoint13profile. The differentprofile durations/overhead mean these numbers are attribution evidence, not a controlled throughput orframebudgetbenchmark.

The remaining dominant frameWorker selfsamples are ShellDocumentRetirementRegistry::close_one1043.523ms accumulated and RasterContentIdentity::mix_bytes444.529ms accumulated. Shellrefreshmax remains1089.699msunder its nestedrendererphaseinstrumentation, and dispatchApplymax279.1ms. Performance is therefore notclosedby movingdecodealone. Reports/rawprofiles:`🗑️generated/astra-runtime/profile-checkpoint-14`; filteredreferenceattribution:`reference-attribution.json`.
