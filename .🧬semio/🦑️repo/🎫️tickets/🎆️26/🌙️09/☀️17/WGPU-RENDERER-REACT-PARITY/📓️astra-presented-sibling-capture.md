# Presented Sibling Capture

The shared scene-pointer fixture now declares the A → A,B → A,C,B insertion sequence. The receiver is protocol record 4 throughout. The native law uses actual accepted Canvas documents and asserts that the receiver keeps its mounted host while its arena NodeId changes, that renderer capture maps to the accepted node, and that one terminal release succeeds. This is a renderer capture registry law; it does not establish World/Graph/Map/Board AppInteractionState routing acceptance.

The independent Chromium oracle inserts C before the existing B DOM node without disconnecting B, moves outside every scene, and releases. It confirms that the same captured element receives pointerdown, pointermove, and exactly one pointerup.

## Run Receipt

`chromium-scene-sibling-rebase/run.log`: 12 tests passed, 0 failed, 78 assertions, 4.41 seconds. The runtime log confirms retained B capture through A,B → A,C,B.

Native109 has not run. The renderer rebase production repair remains held for a valid fail-first result. The UI now exposes a witness-qualified O(1) reverse document binding plus candidate lookup, and UI8 is compiling that boundary.

UI8 subsequently finished: 664 tests run, 662 passed, 2 failed, 4.672 seconds. The old UI7 identity, retirement admission, accessibility, and painted document failures passed. The two current failures are the fixed slot table census (measured 164128 bytes per element, capacity 64, owner 520) and a capture fixture precondition whose two NodeIds now coincide after the accepted baseline copy. Sol is repairing the fixture construction to exercise genuinely different slots and reviewing the measured census; neither is reported as green.

## Other Gate Status

Flow22 ran no tests: the new UI mapper omitted its NodeId import, yielding two E0425 compile errors. Sol corrected the import before UI8. This is not evidence about the Flow runtime or the staged publication progress repair.

## Exact Focus Owners

The read-only audit also found old arena NodeIds in FocusedTextEditor, FocusedInkEditor, and InkClipboardAddress. An accepted unchanged keyed editor can move to a different arena slot; the first Text key then clears focus, Ink cancels the active edit, and a later clipboard callback can discard its result. Sol is authoring fail-first laws and a bounded prepare/commit mapping for the same presenter acknowledgement used by renderer pointer capture. Unaccepted candidates and nonparticipating windows must preserve their current presented owner. A mapper may update the node only after the exact component host, key, kind, document, and mount identity still match.

Reverse protocol identity lookup is O(1); candidate document lookup is binary, so the complete mapping is O(log n). The scene-intent queue retains NodeIds too, but current scheduling drains it before presentation; its potential cross-ACK gap remains a hypothesis requiring a queue-boundary law.

## Browser Worker Gate

After component-retirement continuation integration, the complete browser-worker target passed 144 tests across 11 files, zero failures; Vitest 6.54 seconds, tests 2.10 seconds, Nx 14.3 seconds, cache skipped. Receipt: `🗑️generated/astra-runtime/browser-worker-component-retirement/run.log`. This unit gate does not establish browser presentation or visual parity. Native109 is compiling the matching component close/rebase snapshot.

Native109 subsequently stopped before tests on one E0599: EngineCanvas's staged scene borrow returns Option but the new close bridge used Result::map_err. Nx duration 4m21s. Sol is correcting the Option conversion. The new capture, stale focus close, diagnostic retirement, and two-close admission laws have not produced runtime RED evidence yet.

UI9 finished with 664 tests run, 663 passed, 1 failed; test runtime 3.596 seconds, Nx 30.1 seconds. The exact slot census now passes. The revised slider fixture still constructs equal NodeIds (index 2, generation 0), because it fully rebuilds the accepted baseline before publishing the insertion. Actual renderer paged ingress retires that unneeded baseline instead. Sol is switching the fixture to the production ingress path while retaining the inequality precondition. Native110 repeats the root Shell and retirement laws after the EngineCanvas Option fix; the new editor focus laws remain excluded until their setup is corrected.


## Native Ownership Run 110 and Run 111

Native 110 stopped during compilation: two new focus-fixture calls passed the renderer SurfaceKind instead of the contract SurfaceKind. The fixture now passes explicit contract TextEditor and InkCanvas values. No native assertions ran in run 110.

Native 111 is running with the three same-host focus/clipboard laws, stale-window focus close, renderer pointer capture rebase, camera diagnostic retirement, second component retirement admission, and a new real FrameTransaction law asserting zero decoder progress. The decoder law uses the neutral scheduler fixture's expected.frameDecodeUnits = 0 and a real retained capsule GLB response. Production decoding is still frame-owned at this checkpoint.

The UI capture fixture now uses a legitimate superseded candidate history to force different arena slots before testing rebase; its strict unequal-NodeId assertion remains. This revised fixture has not yet been run.

The scheduler's independent browser test run has 144 passing and 3 intended failing tests across 11 files. These failures establish missing two-owner alternation and the missing private asset decode turn. See [asset scheduler](📓️astra-sol-asset-decode-scheduler.md) and [component close asset deadlock](📓️terra-component-close-asset-deadlock.md).


## Native 111: Actual Behavioral Results

The native suite ran 43 tests: 35 passed, 8 failed, in 1.515 seconds (Nx 3m43s). The frame isolation law observed 128,728 bytes decoded by a FrameTransaction before it returned Superseded, against the expected zero. The renderer capture law exercised the same host scene.1.2 with NodeId index 2 changing to index 3 at ACK, then observed no captured owner. Both are valid behavioral REDs.

Other failures: the exact camera diagnostic remained after component retirement; a stale window close cleared successor Text focus; a second scene retirement detached its owner before panicking on the occupied cursor; and the three existing Canvas removal/deadline/remount camera laws still allowed stale or missing dispatch. The exact Engine staged-host close and the external surface-map reservation law now pass.

The three new editor focus/clipboard laws were accidentally excluded by a filter naming the fixture filename instead of the runtime module. Native 112 now selects test(presented_editor_) directly; no passing or failing claim is made before its result.

The independent browser scheduler repair passed all 147 tests across 11 files in 2.28 seconds (Vitest 7.62s, Nx 14.5s). Rust assetDecodeStep integration and real browser acceptance remain outstanding.


## Flow 24 and UI 10

UI 10 passed all 664 tests in 4.249 seconds (Nx 30.3s), including the strict capture fixture with divergent candidate history.

Native 112 did run the three focus laws, but each stopped in a fixture precondition: the first accepted A document has no B editor, so a helper cannot map from a presented B before B is introduced. The helper now stages initial B directly from the candidate and reserves mapping for the later same-host ACB transition. Native 113 is running the corrected 46-test ownership selection, including root capture and diagnostic repairs and the stale Text-close and second-retirement admission repairs.

Flow 24 ran 253 tests: 246 passed, 7 failed, in 5.575s. The ColdRetire fixture repair eliminates the previous Once poison cascade. The retained publication progress law now passes its progress assertions and fails at terminal close, confirming the separately audited one-byte authority-close deadlock. The publication now transfers authority disposal into the existing bounded retirement owner; this production change is not yet rerun. An extended canonical authority law accounts for stamped edit ID bytes and still awaits its fail-first run before the three-string disposer repair.


## Native 113 and Decoder Session Work

Native 113 ran 46 tests: 39 passed and 7 failed in 1.297s (Nx 3m52s). The renderer pointer capture rebase, exact camera diagnostic transfer, stale-window Text focus close, and second scene retirement admission now pass. All three corrected editor focus laws are valid REDs: the accepted editor remains mounted but retains NodeId index 3 where the accepted tree requires index 2. Their production prepare/commit repair is now underway. The other remaining failures are the unchanged frame-decoder isolation law and three delayed Canvas camera retirement laws.

The private portable RendererAssetDecodeJob and native retained session are now written but not wired into the runtime. The job owns one probe, advances one quantum per StepContext, carries typed Ready/Rejected/Cancelled/Fault boundaries, and returns a probe from exact rejected/terminal guards. A bounded handback slot retains the response if a session closes before normal outcome extraction. The native session submits to Maintenance, retains the exact ticket, resumes Yield and pool backpressure, and registers the host wake. No new runtime behavior is claimed yet.

Native 114 is testing decoder cancellation/ownership and saturated-session exact recovery, plus the initial Asset-before-World component close law. The first-party host close phase is still World, so that phase law is expected RED. Native/browser runtime integration, target-specific asset shutdown, full close/handback coverage, and fresh paired browser acceptance remain outstanding.


## Asset Decoder Integration and Current Gate

Native 114 ran three tests: both independent decoder ownership laws passed; the component initial phase law failed with World instead of Asset. The decoder laws confirm one bounded page followed by cancellation returns the same response token, and saturation of the real WorkerJobSession registry returns the unchanged response from the exact rejected guard. They do not yet prove complete native asynchronous scheduling.

The component close bridge now begins with an Asset phase before detaching the World state. The extended law retains A's checked-out request through the first phase, verifies A remains reachable and B remains untouched, returns A, then drives finite Asset/World close. Runtime completion handback now receives its existing one bounded grant before the component-close early-return gate. The target-specific runtime close keeps the World authority live for response handback. Exact native HTTP cancellation and browser transport cancellation remain outstanding.

The decoder now uses the same retained RendererAssetDecodeSession on both targets: native submits one worker step to Maintenance with an exact ticket and host wake, while the browser steps that same retained session only from its private worker turn. The frame-owned parse loop and obsolete half-frame decode budget law were removed. The browser now exports assetDecodeStep and no longer requests frames merely for decoder progress; native pumping occurs outside FrameTransaction before the close gate. Brush mesh announcements retain their own frame continuation. Full-host close also drives the decoder session before releasing its probe.

Native 115 was compile-only: it sampled an incomplete typed decoder edit (four errors: missing pending query, reference decoder bool result, and the two obsolete frame bool uses). These source mismatches are corrected. Native 116 is now running the corrected complete ownership selection plus two new camera laws, two decoder ownership laws, and the finite component asset close law. The root renderer source parsed successfully with rustfmt before this gate completed; no native or browser integration pass is claimed yet.

The Store stamped-authority law produced a valid RED: it released 19 bytes instead of 31 under positive single-byte grants, omitting the stamped edit ID. The retirement owner now owns all three strings; its rerun and Flow publication-close rerun remain pending.

Known decoder audit item before acceptance: cancellation that arrives after the decode job has already reached Ready must override a pending Ready handback before publication. The private session also needs direct real-worker scheduling and close-handback tests. Reference decoder disposal and per-request transport cancellation must be checked with exact sibling isolation.


## Native 116 Compile Stop and Next Regression Gate

Native 116 did not run renderer tests: shared Plugin job-factory edits failed compilation with six missing/changed JobFn, JobCtx, run_two_phase and factory-signature errors (Nx 33.7s). These belong to concurrent logical work and are preserved. Browser runtime wake scheduling separately passed all 148 tests in 11 files (Vitest 8.10s, Nx 22.2s).

The next renderer selection adds a real native worker completion law: decode an SVG to Ready, request cancellation before the host handback, and require the same response token to return Cancelled. The current implementation is expected to fail at that late boundary. Root also synchronized three Flow fixtures from the audit: distinct replica ids and matching action metadata, explicit math extension installation for the direct host edge law, and two coalesced persistent window-config pages plus one transient page. All Rust repairs await rerun.


The Chromium ready-cancellation reference now passes: Chromium decodes the SVG, its AbortSignal rejects the retired request before publication, and the successor signal remains live. The complete browser worker suite passed 149/149 tests in 11 files (test time 2.93s, Vitest 5.40s, Nx 12.3s). This validates the neutral cancellation expectation, not the Rust implementation; Native 117 is still running. The decoder audit also distinguishes runtime checkout (unknown liveness) from cancellation, which native fetch/reference paths currently conflate.


## Native 117 Actual Ownership Receipt

Native 117 ran 52 tests: 46 passed and 6 failed in 1.322s (Nx 3m15s). The frame candidate now advances zero decoder bytes; all three editor focus/clipboard same-host rebase laws pass. The new sibling camera preservation law passes. Four camera removal/remount laws remain RED, including multiple accepted removals becoming stale atomically; Sol is now authorized to implement protocol-id component liveness after this actual RED.

The new real worker law fails exactly at late cancellation: the 67-byte SVG returned Ready instead of Cancelled after 14 host wakes. The exact response token and byte count, worker completion and terminal session cleanup all passed before the final comparison. The repaired boundary now applies explicit cancellation when the host takes the result; ordinary job metadata close no longer sets the cancellation flag. A companion real-worker control requires Ready to survive ordinary session retirement. These repairs await rerun.

The sixth failure is in the extended component Asset-before-World law: its sibling B request lookup fails at fixture line145. The initial phase failure is resolved; the fixture owner is investigating this distinct sibling assertion. No end-to-end component-close pass is claimed.


## Native 118 and Browser Source Gates

Native 118 is running the full ownership selection plus the Map same-host retirement law, a real interaction-checkout cancellation law, and the native response-page handoff law. The camera protocol-id repair is now source-complete; the two late decoder result controls, portable component-session cancellation, immediate publication liveness checks, and nonblocking close handback are included. Native response-page production still intentionally refuses data for the fail-first receipt.

Browser worker tests now pass 150/150 across 11 files (tests 4.38s, Vitest 6.53s, Nx 13.6s), including Chromium byte preservation for one byte and an exact 16384-byte response page, validated against the new language-neutral fixture/schema. The browser source check initially found stale generated boot and frame-worker bundles. Both were regenerated through their Nx targets; check-browser-worker then passed (Nx 5.2s). This is a source/bundle consistency check, not wasm compilation or browser activation.


Native118 stopped at compilation (Nx 3m2s), before any tests: the new Map law spelled the fixture directory with the test-tube emoji instead of the petri-dish emoji. The resolved include path is corrected and verified on disk. Native119 is rerunning the same full selection. React6313 and WGPU6213 both returned HTTP200 when checked; this only verifies server availability, not new renderer activation.


## Native119 Receipt

Native119 ran 56 tests: 49 passed, 7 failed in 1.183s (Nx 53.8s). All four previously failing Canvas camera removal/remount laws now pass, including atomic invalidation after multiple accepted removals; same-host sibling camera preservation also passes. Both real-worker Ready controls pass: explicit late cancellation returns Cancelled and ordinary session cleanup retains Ready. The finite Asset-before-World close law passes with exact sibling B preserved.

The interaction-checkout law is valid RED: temporary unavailability incorrectly returned cancelled=true; the predicate now rejects only explicit Some(false), with rerun pending. The native page law reached cleanup before its intended transfer assertion, but its fixture incorrectly called finish on a still-fetching claim; it now returns the cancelled fetch to the authority before bounded close. Native120 reruns that corrected RED alongside the Busy repair.

Map's new law still fails in setup because the presented receiver is not found; it is not a valid Map production RED. Four Interpreter close laws also regress under required protocol identity and need their exact document/presentation setup investigated. Sol owns these and the browser component-fetch cancellation work.

## Native120 and UI11

Native120 ran two tests: the runtime-checkout cancellation repair passed and the corrected native page law produced a valid RED, observing zero of one byte (37ms, Nx 1m11s). UI11 passed all 664 engine tests in 2.051s (Nx 31.1s), including the protocol witness and existing geometry/retirement coverage.

Native121 is running the 58-law renderer ownership selection. It includes the implemented native page/handoff repair, two new native reference cancellation/disposal fail-first laws, and revised real-document setup for Map and four Interpreter close regressions. No native-reference or Map production repair is claimed before their valid RED receipts.

Native121 completed: 58 run, 55 passed, three failed, 1.289s (Nx 3m21s). Native page handoff and all four Interpreter close laws pass. Both native reference cancellation/disposal laws fail at their intended bounded-ownership assertions; their production repair is now underway. Map still fails its receiver-mounted setup and has no valid production RED. Flow26 is running the complete Flow suite while these renderer edits proceed.

Native122 completed: 60 run, 59 passed, one failed, 2.560s (Nx 3m2s). The two reference disposal laws and both new native handoff contention/cancellation laws pass, as does the source-retained maximum-plus-one page case. Map still fails before its behavioral assertion. Root found that its direct paint fixture never registered the retained ComponentScene hit projections or promoted staged hits; the working Shell helper does both before resolving its receiver. The Map fixture now invokes those same registration/publication seams, with retirement observations retained until after cleanup. Map production remains unchanged pending its first valid behavioral RED. The WGPU19 wasm producer is building while the browser continuation cancellation test is completed.
# Native125 And Map Registration Follow-Up

## Native128 Behavioral RED And Repair

The corrected receiver fixture now completes bounded cleanup through its CPU-only Map fixture bridge. Native128 measured presented arena node index2 and accepted node index3 for the same component host, `engineRetired=false`, and `gestureRetired=true`; it failed at the intended EngineCanvas ownership assertion. All 67 earlier selected ownership laws passed alongside it.

EngineCanvas retirement now indexes the surface registry with `owner.host_id`, matching registration, and compares `same_component_host` rather than the temporary arena-node address. This preserves exact mount identity through a sibling insertion while keeping replacement components isolated. The production repair is source-applied after the RED; GREEN is pending the next native gate.

The Map setup failure is now identified precisely in source: TiledMap registers `HitKind::ScrollRegion`, while the fixture filtered `HitKind::ComponentScene`. The corrected registration-based receiver lookup removes that wrong filter; no production point-lookup defect is inferred. Terra independently confirmed the correction and the real arena-rebase sequence. Behavioral validation remains pending.

Native125 executed 64 selected renderer laws: 59 passed, five failed. Both queued Ink laws reached their intended presentation-barrier failures; the running native image worker lost its retained encoded owner; the stalled TCP body prevented the sibling fetch from entering. These are valid behavioral RED receipts. The Map regression still failed before its substantive assertion because the point-based receiver lookup returned no target.

The Map fixture now consumes the exact scene target already returned by production `register_clipped_retained_hit_targets`, selects its protocol document-node identity, and requires that target to be live after the presentation acknowledgement. It therefore tests the same ownership registration used by the Shell rather than inferring ownership through an additional point lookup. It still requires distinct arena addresses for the preserved component host, exact engine and local gesture retirement, no live gesture afterward, and no duplicate retirement. This setup change remains unrun. The point-lookup discrepancy is not classified as repaired by this fixture adjustment.
