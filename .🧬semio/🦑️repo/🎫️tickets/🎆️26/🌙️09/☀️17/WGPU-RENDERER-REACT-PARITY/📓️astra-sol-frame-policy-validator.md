# Mounted Frame Policy Validator Scope Repair

## Source-coherent pause

The validator now extracts the affected Rust declarations as unique, brace-balanced blocks through the existing `toolJobRustBlock` scanner. Missing, duplicate, declaration-only, and unclosed markers emit one named `P5a policy boundary is stale: …` result, and dependent checks skip an unavailable scope. A fail-first self-test proved the previous validator emitted no named stale-boundary result for a missing navbar scope; the repaired test covers both missing and duplicate declarations.

The invalid Shell child/chrome-group/tour, retained paint, interactive-sync, and scene-node tail slices now bind their current production declarations. Comment and literal text is excluded inside those checked blocks so prose containing `while` is not policy evidence. The eight assigned mutation contracts bind the current source for Select materialization, maintenance release, deferred close, non-text paint, Shell glyph paint, fixed-field read/write, and paint output credit. Every assigned mutation now changes its source and emits its own named policy failure; no mutation-binding failure remains.

## Exact command and current result

```sh
NX_DAEMON=false NX_FORCE_REUSE_CACHED_GRAPH=true bun nx exec --projects=workspace -- bun '.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️17/WGPU-RENDERER-REACT-PARITY/🔬️frame-transaction-policy/📜️script.ts'
```

The focused command remains RED by design. Its latest run took 30.82 seconds and reports only the clean-baseline rejection; the missing/duplicate scope tests and all mutation checks passed. Rebinding the native maintenance close, current preference calls, footer child, glyph cursor, production close surfaces, tree arm, and typed scene cursor removed every stale contract label. Eleven live failures remain: transaction `saturating_add`, `.expect`, and `while`; native preference `.lock`, whole-document key reachability, and `loop`; Shell chrome `.clear`; interactive synchronization `.clone`; incomplete take/resume protocol counting; caller-side worker drive; and a worker loop. This packet does not weaken those rules and does not claim baseline green.

## Files

- `📜️script.ts`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🧪️tests/🔬️interactivity-mounted-frame-transaction/🟦️.ts`
