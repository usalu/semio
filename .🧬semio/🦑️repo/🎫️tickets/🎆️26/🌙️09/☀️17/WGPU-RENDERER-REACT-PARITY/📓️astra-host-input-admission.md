# Host Input Admission Integration

HostCancel2 executed79 tests,70 passed and9 failed. Terra isolated constructor/terminal backing ownership and generation advancement on refusal, independent of the earlier pointer-cancel route. Root is repairing these host dependencies because their behavior directly governs renderer event correctness.

The queue now begins with zero heap backing. Its first accepted discrete event reserves the existing256-slot bound fallibly; oversized/full/refused inputs do not allocate or advance the generation. Replaceable pointer/scroll/metrics continue to use fixed Copy slots. InputGeneration uses checked advancement, so exhaustion preserves all queued content and emits the existing Overflow outcome. Metrics now returns that same explicit outcome. The overflow counter saturates rather than overflowing.

Explicit close retires one pending event per step, releases the empty queue backing in a separate step, then permits terminal completion. The physical terminal predicate now includes zero retained capacity. No capacity or neutral fixture was raised or weakened.

The renderer callback currently advances its frame generation before calling queue admission. Root added a new law driven by the existing neutral event-full vector to expose that remaining boundary before repairing it. The next native census will capture this expected red witness as well as the kernel/General/palette/raster repairs. Full production behavior is not yet accepted.

HostCancel3 passed the full native host suite after these production changes; the exact test census is recorded in native-host-cancel-3.log. The renderer callback law remains pending the coherent full renderer build.

## Mounted Callback RED and Repair

Native37 executed all1220renderer laws and reproduced the new event-full refusal defect: frame generation advanced by1, expected0. Root repaired both event and metrics helpers to preflight a local candidate, admit into EventQueue, then commit generation/scheduler invalidation only on Accepted. Metrics now returns the outcome and handle_metrics publishes surface/mailbox resize only after acceptance. The permanent frame-exhaustion metrics law now checks the explicit Overflow result. Removed the temporary per-event Debug formatting of whole input payloads from the UI callback. Full verification remains due.

Native39 passes all17selected General/Winit laws (1,205outside selection). This includes accepted-only generation, permanent exhaustion and the mounted General popup retirement/appearance commit. Native38 had9passes/3Ink failures before fail-fast cancelled8remaining selected tests; it is not counted as a passing gate. No fresh browser activation is claimed.
