# Checkpoint 16 Runtime Acceptance

WGPU activation16 and matching React activation9 completed. Five browser artifacts were sealed before the physical journey and remain unchanged after the completed 57-step full run.

## Palette A

Both renderers booted and dismissed their tour. Palette activation was not accepted: the React probe reused an ephemeral option DOM id after closing/reopening the dialog; the WGPU probe accepted an unfiltered accessibility list merely because the target command was present. Its final screenshot shows the correctly filtered Set Theme row after the early probe refusal. These are concrete harness ownership/wait defects, not proven product failures.

The ticket probe now requires the single filtered row, canonical command id, and a physical hit rectangle stable across two observations. It resolves the current row after every dialog reopen and immediately before physical activation. Paired replay B is running; no application source was changed.

## Initial Visual Inspection

Root viewed paired 02-dismiss-tour captures. Both publish the GLB model and a portrait reference image. Remaining visible differences include reference image proportions, grid coverage and opacity, scene background, model edge outlines, several chrome glyphs, central navbar widths and palette backdrop/selection styling. Reference origin is not established as defective by these screenshots. Paired cameras agree within0.00005 and viewport bounds within0.066CSSpx. Terra independently verified that React derives GLB edge outlines while WGPU omits zero-edge meshes, and measured chrome offsets up to124CSSpx. See `📓️terra-checkpoint16-harness-visual-audit.md`.

Evidence: 🗑️generated/astra-runtime/checkpoint-16-palette/{steps.json,react,wgpu} and checkpoint-16-palette-b. Full settings/window and dedicated surface acceptance remain pending.


## Palette B: Concrete WGPU Runtime Fault

React passes the complete hardened pointer journey: interior absorption, row drag/release cancellation, physical Close, outside dismissal, query-preserving reopen, canonical Set Theme activation, dialog retirement and the actual staged Appearance form with Execute and Reset.

WGPU reaches Close/reopen and outside dismissal but faults on the subsequent reopen. Its console ends at58,549ms with `wgpu renderer fault: worker-input-failed: text edit step failed: Protocol`; the final screenshot displays the quarantined surface. This is an actual production failure, assigned to Sol for a neutral owner-lifecycle regression and narrow Rust repair. The harness now classifies this fatal fault so later steps are unmeasured rather than misleading cascades.

Receipt: checkpoint-16-palette-b/steps.json, wgpu/console.txt, wgpu/03-command-palette-activate.png. No palette parity is claimed.


The full journey now verifies normalized applied settings: React mounted ShellScope appearance and scoped/page locale, WGPU retained General preference values projected through the accessibility mirror. It compares both receipts across renderers and requires dark/de before later steps. Full run is active.

The additional frame-policy self-test now aggregates all diagnostics. It exposes eight further stale mutation needles and broad source-boundary guard mismatches after the shared source refactors (frame-transaction-policy-9.log). This auxiliary policy gate remains RED; its errors have not been suppressed, and it is distinct from the executed native1,231 and React1,838 law results.


## Verified Text-Owner Repair

UI57 executes the new shared owner-lifecycle law and fails exactly with Protocol after a checked-out projection is blurred/refocused. The narrow InputState repair preserves the live text_projection_pending obligation through reset_text_view so the existing bounded projection drains before replacement. UI58 runs all646 native UI laws:646passed,0skipped,4.016seconds. The law also retains the true concurrent-start Protocol refusal. Fresh browser acceptance requires activation17; checkpoint16 artifacts still contain the pre-fix code.

## Completed Full Journey

At14, WGPU dispatches setAppearance dark and visibly paints dark, but its retained Appearance select and accessibility projection still say system. At16, the locale mutation similarly leaves the retained select at en. React applied receipts are dark/en then dark/de. The select-publication defect is assigned to Sol; the test has not been weakened to accept only the changed canvas colors. Drivers closure18 also leaves child hits visible.

At21, the actual revealed Abort row does not dispatch engagementAbort on WGPU; React succeeds. Subsequent pane-close/search steps also fail their required child-control consequences. The ownership/interaction packet is assigned separately. These are real runtime failures beyond the green native cohort.

Both renderers pass the actual120CSSpx split drag, focus/unfocus/refocus body bounds, individual close, close-last, and empty-dock Display-handle drag producing a new live World body. New window identities are renderer-assigned (-3 versus-2); the created count and live body are the behavior. WGPU faults at palette step44 with the pre-repair text-owner Protocol; steps45–57are unmeasured. React step44 uses a wrong hardcoded German query from WGPU: React's canonical label is `Thema festlegen…`, while WGPU says `Design festlegen…`. The probe now reads the canonical command row's localized label and compares command identity/label across renderers after the physical operation. A WGPU locale law/repair is assigned.

## Fail-First Retained Shell Repairs

Native49 compiled the Actions ownership repair and passed1focused law; it is not a RED receipt. After narrowly restoring the production hunk, Native50 actually runs both strengthened laws and fails2/2: General `setAppearance` retains the same UiRevision4642242816561439936; Actions PointerDown is classified Surface instead of Chrome. The independent source repairs can now be restored against those executed failures. Native50 excludes1230unrelated laws by explicit filter. The next full census and activation17remain required.

The ticket probe now emits physical.md and gates boot/dismiss-tour named chrome geometry at1CSSpx, seeded window target identity and before/after body bounds, pane child sets, and creation/close counts. This makes geometry a real acceptance gate. Replaying sealed evidence is explicitly reported as offline analysis, not a new runtime run.

Offline physical replay completed: all4injected wrong-window/body/missing-body/missing-outcome mutations were rejected. The unchanged checkpoint16receipts pass the seeded window operations and expose18interactive chrome geometry differences at boot. Root's second image inspection confirms the static presence text is painted in WGPU; its absence from the hit registry is expected for noninteractive text and does not prove missing paint. Its text bounds remain unmeasured pending a text/pixel receipt. React-only fresh11-step dark/de settings and both pointer/keyboard palette journeys then complete with0errors using the actual canonical localized row. Native51passes the repaired General and Actions laws, and fails the new locale law exactly at `Design festlegen` versus `Thema festlegen` (2passed,1failed). The canonical WGPU label repair is now authorized by that executed witness.

The auxiliary policy boundary findings are separately recorded in `📓️astra-frame-policy-boundaries.md`; missing source end markers explain some false scope, while actual transaction constructs still need deliberate reconciliation.

## Expanded Settings and Disclosure Boundary

Native52actually exposes four further missing General publications: setLayout, setTerminology, setMergePolicy and setDriver. Its driver workflow initially fails a test reader that stops on a same-key TreeItem wrapper, and the first Drivers disclosure test uses a whole repaint between mouse down/up and fails at opening; neither is treated as the intended product witness.

Native53uses the corrected retained reader and fails the real driver mutation roster: setDriverField, setDriverSaveLabel, saveDriver and deleteDriver. Its exact-owner refusal law also fails: retirement refusal has already replaced generation1withgeneration2, proving owner/publication ordering is wrong. Both repairs are now source-ready with retirement admission reserved before publication; Native54runs these laws plus expanded preferences/locale and the strengthened Up-flow disclosure law.

Full16step17's WGPU Drivers header is registered at396.79993after expansion, exactly over the Save row/button, despite its closed location at612.8and React's fixed header at615. The close probe resolves the exact section.chevron ID, then its center physically hits Save. This is a retained header geometry defect, not a reason to bypass the header with a different probe target. The focused law must require a header below its children and the top hit at its center to be that same header before testing close retirement.

Native54confirms that exact product witness: expanded Up-flow header y516.8/h24is above its child y544.8/h16. Five other laws pass (expanded General preferences, driver edit/save/delete workflow, exact-owner refusal/retry, MAX+1registry admission and canonical locale). The generic header hit-translation repair is assigned; full Native55/UI59and fresh activation17remain required.
