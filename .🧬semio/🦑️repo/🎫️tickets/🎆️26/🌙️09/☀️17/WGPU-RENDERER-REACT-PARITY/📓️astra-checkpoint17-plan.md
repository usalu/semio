# Checkpoint 17 Acceptance Boundary

The next browser artifact includes the General and driver retained-document publication repair, exact retirement admission, command-palette text projection ownership, canonical German Set Theme label, Actions pointer ownership, and the generic Up-flow section-header hit band repair. Native54 proves five settings/retirement/locale laws and fails the intended old header geometry. The corrected source awaits full Native55 and UI59 before WGPU activation17 and matching React activation10.

GLB outline production is held outside this capture. Its schema and actual Three reference tests pass three cases; the native RED and bounded implementation remain drafted until activation17 completes. Chrome width and label differences have independent source evidence but no implementation in this capture. Display projection branch disclosure remains open and must not be counted as fixed merely from the section-header change.

Five checkpoint16 artifacts were rehashed after Dock C/D and remain unchanged; receipt: `🗑️generated/astra-runtime/checkpoint-16-artifacts-after-dock.json`. Dock openTemplateSource already preserves the qualified Tree scope in its derived row id, so its exact transfer-handle selector does not require the speculative correction noted earlier.

The dedicated Draw React activation1 is running in the root compile lane. Existing listeners occupy canonical Draw/React6064, Layout/React6079 and Note6080/6180; their processes are preserved. Fresh owned listeners may use explicit alternate ports to prevent HMR and configuration differences from invalidating measurements.

The canvas physical adapter now accepts an output subdirectory and uses the same Chromium WebGPU/Metal launch flags as the successful main and Dock probes. Its unchanged schema/trusted pointer-cancel/trusted HTML drag contract passes again in `canvas-interactions-contract-5.log`. This is adapter validation; dedicated app results remain unmeasured.

## Executed UI59 and Native55

UI59 passes all 647 tests with zero skips (33.1 seconds Nx), including the new direct Up-flow header-band law. Native55 executes all 1,238 tests: 1,235 pass, three fail, zero skip (3m38s Nx).

The real Shell Drivers law now passes header placement and topmost hit identity, then fails child retirement after the physical close. Source diagnosis identifies the second hit path: EventRouter still tests the section top, even though the flat registry now points to its painted bottom. Sol is repairing that generic router geometry.

The Display law initially stops during setup because its test viewport is missing; this is not yet the desired missing-chevron RED. The Theme law initially observes no Primary field because that field is beyond the virtualized initial page; the test now requests its actual scroll window. Native56 runs the corrected Theme law. Both initial failures remain recorded; neither is relabeled as a renderer defect or a passing acceptance case.

Checkpoint17 scope now includes the Theme leaf publication and generic TreeItem disclosure repairs after actual fail-first evidence. GLB production remains outside this capture. Full native/UI reruns and activation remain pending.

## Native57 Focused Receipt

`🗑️generated/astra-runtime/native-renderer-57.log` executes five selected laws: three pass, two fail; 1,234 tests are outside the explicit filter. Theme's full eight-field mutation workflow and invalid-input no-successor law pass, as does the existing editor-coordinate law. The Drivers law now proves header geometry, topmost hit identity, physical closure and child disappearance from current host hits, then fails the separate retained owner-map retirement assertion. The corrected Display viewport reaches the intended missing childful `tree.chevron` failure with actual label and transfer hits present. Sol's generic disclosure and owner-map repair remains in progress; no full renderer acceptance is claimed.

## Physical Adapter Calibration

`canvas-interactions-contract-6.log` passes schema, Chromium-trusted pointer cancellation and native HTML drag/drop witnesses. `canvas-interactions-types-7.log` completes without diagnostics. React's public input ledger intentionally contains identity and outcome, with no argument payload; the adapter now records those real fields and uses passive trusted DOM drag witnesses for Layout MIME/payload validation.

Draw8 still fails the second rectangle's exact layer-count increase after re-arming the one-shot utility. Terra is auditing the actual outcome receipt. Layout1 stops at a nonexistent fixture picker: the current app declares no `setActiveExample` action, so the shell correctly omits the picker. Its `initial_snapshot()` directly creates the Demo document. Layout2 resets through browser reload and retains exact seed-count assertions before each physical catalogue case. These are calibration runs, not renderer acceptance.

## Full UI61 and Native58 Boundary

UI60 executes 649 tests: 648 pass and one collapsed-row geometry law fails. The repair preserves the existing full-width/zero-height geometry for collapsed descendants while live disclosure still excludes their paint and input. UI61 then passes all 649 tests with zero skips. Both runs cover the full input/tree/layout/paint census.

The locale refresh cursor is source-coherent, with a neutral fixture/schema and 2/2 mounted React oracle cases. It refreshes exactly one currently mounted shell-owned panel per maintenance step, retries the same owner on retirement refusal and supersedes an older locale generation without guest refresh or cap changes. Full Native58 now captures this and the coherent tree packet. Activation17 remains pending.

Native58 finishes after 5m32s with three compile errors in the newly added locale test: a fixture include path, a stale `Component::Section` variant and a stale `Label.as_str()` use. It executes no tests. Sol is correcting those declarations against the current contract before Native59; the previous source-ready marker is not a passing build result.

Native59 completes the full 1,241-test census: 1,237 pass, four fail, zero skip (5m34s Nx). The Drivers closure/retirement law now passes. Remaining failures are the Display childful template law, General preference publication timing, and the two locale retained-label laws. On continuation, the old process handle is absent and the final log confirms completion; it is not treated as a running wait. Two execution agents are assigned the Display and locale failures. The previous goal turn made concrete progress through runtime receipts, source repairs and the full census; this is not a no-progress restart.

Draw9's actual dispatch diagnostics confirm both rectangle gestures reach Down and Up with `shapeRect` current at the host; the public ledger admits both action sequences but only the first creates a layer. Layout4 independently establishes the React pointer-catalogue transport gap, confirmed by source audit. Note2 exposes refused selection/hover verbs and missing domain presence projection. These shared reference gaps are assigned for narrow repair and are not counted as WGPU acceptance. Dedicated reports hold the evidence.

## UI 62–63 and Layout 8 Continuation

The new nested Up-flow disclosure regression fails first in UI62: closing leaves the mounted child at height 24 rather than 0. UI63 adds causal assertions and remains red at the same layout check: the child does not overlap the stable header, and the second physical click does set the disclosure state to closed. The failure is therefore downstream of accepted input in mounted layout/publication. Each focused run executes one test; 649 tests are outside its filter. The generic repair is active before the next full native gate.

The three Native59 locale/settings failures are corrected as contract expectations: locale changes publish through one-owner bounded maintenance, Theme's dock and body use distinct canonical localized labels, and Hotkeys is a Section-role Container. Exact owner, generation, refusal/retry and absent-panel assertions remain; focused React/schema validation passes 12 tests across two files. Native60 has not run yet.

Root's Canvas2d pointer MIME repair has fail-first evidence (18 pass/4 fail), then 23/23 mounted React tests including independent MIME roster ownership and cancellation. Physical Layout7 and Layout8 each pass all four authored catalogue cases. Layout8 records settled applied outcomes for every transfer preview, leave and drop, exact seed-to-created counts, and exact bitmap restoration after leave. It is React reference evidence only. The browser's optional bridge connection errors remain disclosed in the Layout report.

Sol's next bounded packet implements Note's canonical Ink domain interactions; Terra is tracing Draw's repeated-shape retained owner. Root remains the sole Cargo/activation owner. Existing main listeners at ports 6213 and 6313 remain live and unchanged; fresh activation17/React10 and artifact sealing still precede their next scored comparison.

UI64 passes the focused nested Up-flow law after the root scheduling repair. The accepted layout had cleared root obligations while intermediate ancestors remained marked; a later nested mutation stopped bubbling at an old marked ancestor, leaving the root unscheduled. The repair bubbles to the root when it lacks a current layout obligation and marks committed layout for paint/hit publication. UI65 then passes the complete 651-test corpus with zero skips (39.9 seconds Nx), including physical open/re-close under nested panel Stacks in both Up and Down flows. Native60 awaits the Ink source-coherent boundary before compiling the integrated renderer.

Native60 finishes after 4m55s with one compile error and executes no tests: the new locale helper reads `label` directly from `UiNodeRecord`, which has no such field. Sol is correcting the helper against the retained representation. Root also found that Ink's stated closed domain envelope was actually two independently optional fields; both renderers refuse half-populated values at runtime, but the schema admits invalid combinations. A single typed optional envelope and strict validation are being prepared before Native61. DrawNative1 is now compiling the actual two-gesture application law through the existing artifact test router.

Native61 finishes in 4m40s: 1,241 of 1,244 tests pass, three fail, zero skip. Two locale fixtures omitted the separate dock-tab catalog; the setup now uses production `sync_dock_tabs()` before mounting its four retained documents. Ink's local edit activation had been incorrectly made conditional on an interaction domain; that production regression is repaired while preserving strict scoped publication. Root review additionally found DSL `FromValue` ignored unknown members although serde rejected them; explicit unknown/duplicate refusal and actual negative laws are now present. Native62 is running this source-coherent packet.

React census4 executes 1,905 tests across 95 files: 1,904 pass, one app-scoping assertion fails because it expects the procedural `setActiveExample` action on five windows while the current descriptor contains none. Current command ownership is being audited before altering the law. DrawNative2 reaches the intended repeated-creation failure, and DrawNative3 passes the focused law with 276 tests outside the filter. The source was further strengthened to include admitted operation and base-revision identity plus an identical-geometry third creation; a fresh complete Draw census is still required before attributing acceptance to that final form.

Native62 completes in 4m18s with 1,242 of 1,244 tests passing and two locale failures, zero skips. Ink editing now passes. The locale fixture's dock was still empty because production `default_dock()` correctly requires an active app session. The fixture now creates the actual test app/session, requires the expected dock roster before publication, and exposes that roster in any failure. Native63 is a focused rerun of the two locale laws. The separate UI-scene Ink law passes with 140 tests outside its filter, exercising both serde and DSL envelope validation; the first invocation's unsupported `--nocapture` flag executed no tests.

The procedural descriptor audit confirms every relevant verb has one app-level declaration and zero window copies. Root replaces the stale copy-count assertion with the current production dispatch gate, checks acceptance from every window and from the app, and proves removing each non-reserved app declaration yields a refusal. Both window-action scoping laws pass (667 tests outside the focused filter). Full React census5 is running. See `📓️terra-generation3d-app-action-census.md`.

Native63 passes both locale laws with 1,242 tests outside the filter (4m45s Nx including compilation). Native62's remaining 1,242 tests were green. React census5 fixes the action-scoping failure, but one unchanged brush-hover assertion times out while dynamically importing the full World3dHost module inside its five-second test body. Root moves that exact three-assertion law into the existing World3d interaction corpus, which already imports the module during collection; no timeout or production behavior changes. Its focused corpus is green, and React census6 is running. FlowNative1 is the separate fail-first app test packet; it is not part of main renderer acceptance.

React census6 passes all 1,905 tests across 95 files in 68.99 seconds; the focused World3d corpus passed 15/15 before that full run. No skipped or weakened test was needed. Fresh artifact activation is still pending the root-owned native queue.

DrawNative4 completes the full 278-test corpus in 2m54s: 215 pass, 63 fail, zero skip. The new repeated-shape and durable-scope identity laws are absent from the failure list. The failures include old app fixtures dispatching without a mounted live instance, stores dropped without terminal-empty retirement, edit history created without its retirement factory, stale canonical diffs with removed camera/locale/selection fields, and a mutation catalog path assertion. These failures remain open; they do not justify changing production authority to accept invalid fixture ownership. Final source identity uses the complete existing durable operation context, document ordinal and persisted geometry. A separate FlowNative2 four-law run now follows the corrected explicit fixture dereference; FlowNative1 failed to compile and executed no tests. The next main artifact activation remains the priority after this bounded queue.

FlowNative2 executes all four selected tests: zero pass, four fail, with 246 outside the filter (2m58s Nx). The unknown-operation law reaches the intended missing atomic refusal. Two scene laws still stop at fixture scene extraction, and the move law observes no initial widget; these require setup correction before attributing their failures to production. Sol is making those preconditions explicit. Main WGPU activation17 then starts and completes renderer wasm but fails its browser-support dependency on the outdated Preview2 guest-log vendoring pattern. Root reproduces that exact failure with the installed third-party shim, preserves its existing streaming decoder, and updates only default stderr severity classification. The final registered vendoring law and all 17 browser WASI laws pass; see `📓️astra-preview2-vendor-activation.md`. The failed activation attempt is still draining its component work before a canonical retry.

The first activation attempt finishes after 7m42s with both renderer wasm and the app component built, but browser support failed and prepare/activate were not run. WGPU17b then succeeds in 56.5 seconds after the support repair; React10 succeeds in 37.9 seconds. Both activate their one selected puzzle component with no excluded components. Root restarts only the verified owned main listener processes on 6213 and 6313, so cached transformed modules cannot carry earlier sources into the comparison. Both fresh listeners report ready. The React freshness reporter additionally lists unrelated staged catalog entries absent from this single-plugin receipt; those messages are retained in its server log and are not treated as active puzzle component failures.

`checkpoint-17-artifacts-before.json` seals 43 files: the five renderer artifacts, both activation receipts, all current puzzle component files and the generated puzzle3d session. The renderer wasm SHA-256 is `dc7ec90ef87908b877e964c3b9e3f51995049d52fe2bc3b2594c6a91cedda951`. The full paired physical journey is running in `checkpoint-17-full` with the standard 1,600×1,000 viewport and 2,400 ms settling interval. No further activation is allowed during this measurement.


Fresh WGPU17b and React10 activations both succeed with identical selected puzzle component digests. The full 57-step browser journey completes: 29 of 56 measured comparisons differ, one is unmeasured, and WGPU palette steps 44–45 fail their action postconditions. The earlier palette text-projection crash does not recur. Physical geometry checks fail only boot/dismiss-tour chrome; measured pane controls and split/focus/close/reopen cases pass. The dedicated Dock17 suite then passes 8/8 React and 7/8 WGPU. WGPU's remaining template case is physically covered by `panel.resize.bottom-left.outer`; this differs from the previous absent disclosure defect. All 43 tracked artifacts remain byte-identical after both journeys. Results do not establish visual, gesture-journal or representative-app parity. Source repairs resume only after this seal.

FlowNative3 executes five selected laws in 2m1s: one passes (closed operation vocabulary), four fail, and 246 are outside the filter. The scoped-domain law reaches its intended missing-envelope assertion. Selection still observes Null; movement reaches an ordered-map retirement panic; unknown-operation refusal is followed by a fixture terminal-close failure. These are being diagnosed before claiming app-local runtime acceptance. Native64 separately runs two new fail-first panel reservation/activation laws.

Native64 reaches both intended panel failures before the root repair: a 300 px right panel does not reserve its 306.4 px band, and a visible middle panel activates the covered window. Native65 now checks their repair together with the independently authored resize-hit and actual retained World3d ownership regressions. The shared React/Chromium Layout gate passes all five tests twice, including seven width cases, five overlay cases and actual canvas pointer/wheel routing in its final form. Sol's palette repair separately passes 37 keyboard-scope and 10 accessibility tests; its native owner/refusal law remains queued.

At approximately 02:36 local time, concurrent workspace activity removes the ticket generated-output tree and build caches. Source fixtures and retained Markdown reports survive. The initial Native66 launch fails at shell redirection before executing any test. Root recreates its output directories and restarts the complete native renderer census. Native64/65 and checkpoint17 results above remain historical observations recorded in the tool outputs and reports; their raw logs/screenshots/hash receipts are no longer available for rereading. Future acceptance requires regenerated outputs and fresh activation.
