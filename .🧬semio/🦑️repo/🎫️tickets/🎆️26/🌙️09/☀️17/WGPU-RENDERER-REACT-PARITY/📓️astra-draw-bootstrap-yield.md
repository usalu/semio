# Drawing Bootstrap Deadline Yield

Draw16 GREEN completed the entire suite: 282/282 passed, zero skipped; nextest 2.344s, Nx 30.2s, run `08c84c56-08f5-4421-8b7b-d1a67477e460`. This includes the new deterministic deadline regression and both complete-envelope initialization failures from Draw14. The repair is the single inner deadline return described below. The browser's broader renderer parity remains open.

Draw15 RED completed: zero passed, one failed, 281 outside the filter; nextest 13ms, Nx 1m58s. The actual retained state was absent (`None`) where the fixture required `Some((0, None))`: the partially constructed owner had entered retirement solely because its deadline expired. Root then changed the pool bootstrap's deadline result from `Err` to `Ok(false)`, preserving the building owner for a later grant. No capacities, scheduling budgets or domain-fault handling were changed. Draw16 now runs the complete 282-law suite, including the two full-envelope initialization regressions from Draw14.

Draw14 failed two existing complete-envelope initialization laws with `drawing-store.mutation-arena-bootstrap-fault`. The source has a specific deadline race: the job checks `StepContext::should_yield` before entering the retained process state; the pool bootstrap checks again after the stage transition. That second check returns `Err` on deadline exhaustion without storing a fault, so the outer owner enters retirement and ultimately reports the generic fault. A cooperative budget expiry must preserve that owner for the next turn.

The neutral mutation-admission fixture now specifies a clock reading zero at admission and one at its deadline, with no new allocation and the lifecycle `yielded → resumed`. The strict schema was extended before the Rust regression. The new law retains one partially constructed arena, crosses the deadline between the two real guards, records whether the same owner remains, then drives either success or fault to terminal before asserting. The existing Draw TypeScript target runs the same pause/resume expectation through third-party React Scheduler's actual continuation queue and validates the fixture with Ajv.

Draw15 RED is running only `retained_drawing_arena_bootstrap_deadline_between_guards_preserves_and_resumes_its_owner`. Production has not changed yet. The full Draw suite remains required after a verified repair.

The canonical `@semio-tech/draw-js:test` target passed both example tests, strict Ajv fixture validation, all five publication-authority hostile cases, and the React Scheduler continuation oracle. The actual third-party queue stopped after its first yield with zero allocations and a retained callback, then resumed that same continuation and became empty. Console receipt: `[DEBUG] Drawing bootstrap third-party React Scheduler oracle: yielded→resumed; allocations before resume=0`. Nx completed in 515ms; native RED remains pending.
