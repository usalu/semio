# Checkpoint13 Runtime Receipt

WGPU activation13 passed all20 tasks in7m33s. React activation7 passed all14 tasks in7m30s. Five WGPU artifact hashes match the renderer completion seal after full activation and again after React activation, before navigation. This actual paired15-step Chromium run uses1600×1000 DPR1 and optional worker CPU sampling at1000µs. Sampling and concurrent builds mean its timing is attribution evidence, not an isolated performance baseline.

The profiled and fresh unprofiled runs have both completed. General Settings now publishes its Appearance control without a worker panic. Drivers is initially collapsed, opens allseven control hit targets, and closes them again. The physical Settings footer Toggle then closes its active panel. These postconditions pass in both renderers. UI24 independently passed the actual empty/nested/terminal Up-flow paint law.

Both initial World panes have a new blocking runtime failure: through at least90s their model draws remain zero. The guest mesh and instance payload still names the concrete-forest GLB and seed instance, but the worker records no GLB-ready or mesh-published line. Only built-in reference-plane, box, vortex-marker, and gumball-plane meshes are resident. Reference images load and render. Neither camera auto-fit completes; Top's live target is[7,0,.01] after a delivered seed[3.5,0,.005], while Perspective adopts the initial template direction but retains its seed target. Sol Settings is tracing the camera2/asset-admission interaction before S2 production. No unit-only camera success is promoted to runtime acceptance.

The first window-reopen assertion is a probe namespace bug, not yet a publication failure. The actual Display header is `section.chevron.framework.display.windows/framework.display.windows.puzzle3d-main`; the old section regex allowed only start/dot delimiters. The screenshot shows the correctly collapsed Puzzle3D section. The probe now accepts the observed slash namespace and a fresh sealed13 rerun will be required. Close-all already passed its physical tab-removal postcondition.

The recorded2.4443s worker maximum coincides with actual reference-image decode logs: decode starts at8276ms and completes at10716ms. A second reference-image decode runs10744–12571ms. The CPU profile will independently identify the sampled stack when the journey ends. The raw logs already distinguish this decode interval from the separate General paint crash fixed in checkpoint13.

Evidence is under `🗑️generated/astra-runtime/paired-checkpoint-13/`. This report will be updated after completion/profile collection and artifact verification. Goal and ticket remain active.

## Completed Profiled Run

The15-step run completed with6 explicit failed postconditions: the namespace-sensitive reopen step, three subsequent gestures with no live body to target, and both fullscreen shortcut transitions while the dock remained empty. Eleven ledger/structure comparisons differ; that count is not eleven independent user-visible defects. No worker panic occurred. Allfive artifact hashes remained unchanged after the browser run. A fresh unprofiled run with the corrected Display namespace is underway in `paired-checkpoint-13-confirmed` to separate the probe failure and confirm the missing-model behavior without profiling.

Actual Chrome CPU profiles were collected successfully for the frame worker and four plugin shard workers. The frame profile independently records3854.017ms of sampled inclusive time under `apply_reference_image_bytes`, with3260.351ms under image resizing across the run. The largest sampled leaf costs inside reference decoding are vertical resampling1209.684ms and horizontal resampling586.896ms. Combined with the exact decode begin/end console intervals, this confirms reference-image decode/resizing as a cause of multi-second synchronous worker stalls in this run. It does not attribute every other overrun to that path. Raw profiles, per-worker tables, and `reference-decode-profile.json` are retained with the generated runtime evidence.

## Fresh Unprofiled Confirmation

The corrected Display namespace probe completed all15 steps with2 explicit failed postconditions: entering and exiting browser fullscreen through the macOS Control+Meta+f shortcut. Both renderers passed actual General Appearance publication, allseven Drivers controls opening/closing, the Settings footer close, closing both windows, and Display drag reopening a new live World body. Orbit dispatches three actions with different ledgers; pan and zoom ledgers match. Nine ledger/structure comparisons differ and are not promoted individually to user-visible defects. No worker panic occurred. Allfive checkpoint13 artifact hashes remain unchanged after this run. Evidence: `🗑️generated/astra-runtime/paired-checkpoint-13-confirmed/` and `checkpoint-13-confirmed-post-browser-hashes.json`.

The missing model cause is now proven in source and the runtime log: the URL snapshot delivers successfully and seals a draw rebuild, then the initial projection seed advances interaction ownership. The old rebuild becomes Stale before it publishes a URL draw, so no GLB request can be admitted. The Top content-frame synchronization advances camera ownership once more. The repair centralizes camera-only view revision changes and carries a pending sealed draw rebuild into the same revision without changing snapshot/document freshness. Its actual URL-bridge law covers snapshot completion, Top seed plus content-frame sync, rebuild publication, exactlyone GLB request, decoded geometry publication, and loaded-bounds fit. World14 is running; no runtime acceptance is claimed before a new wasm/browser checkpoint.
