# Open Panel Reservation and Window Activation

Fresh checkpoint 17 supplies two connected parity findings. At `checkpoint-17-full/*/16-settings-language-activate.png`, React reserves the right-hand Settings band outside both window panes, while WGPU retains the full-width pane layout and overlays its Settings panel. The actual action ledger at step 13 additionally records WGPU `shell.windowActivate` for the underlying Perspective window while the user opens the Appearance control; React publishes no window activation for that control.

The current React authority is `🧰️framework/🔨️modules/🖱️ui/🧱️elements/📐️Layout/🟦️.tsx`, `layoutPanelReserveStyle`. For each side it takes the widest visible anchor with nonempty tabs, using the panel's own default size when unset, and adds twice `--spacing-single`. Left anchors are top-left, left-middle and bottom-left. Right anchors are top-right, right-middle and bottom-right. Top-middle and bottom-middle reserve no band. The padding belongs to `layout-canvas-column`; navbar, footer and absolute panel anchors keep the full shell region. Mobile uses its distinct existing full-height panel behavior.

WGPU `ShellState::body_rect` currently subtracts only navbar/tutorial/footer height. Its chrome pass plans the dock using that full body. The repair must introduce a separate canvas region, rather than shrinking the common body used to position panels. The same reduced canvas region must own painted windows, dock drop targets, split drags, active bodies and scene viewport dimensions. Closing the last open anchor on a side must restore the exact original region.

WGPU `activate_window_under_pointer` rejects modal layers but searches window rectangles underneath nonmodal shell panels. `pointer_owner_at` already knows about open panel geometry, yet the activation helper does not use that ownership. Reservation removes overlap at edge panels, but centered panels still require explicit no-underlying-window activation. The real production press route must preserve normal scene and window-control activation while respecting shell panel ownership.

Planned fail-first coverage will consume a neutral anchor/width fixture from both the actual React layout and native Shell. Cases include no open panel, each edge, widest-of-multiple anchors, folded/empty anchors, centered anchors, resize and close restoration. A separate physical activation law must press panel content and padding over a different window and prove that the active window and activation history stay unchanged, then prove an unobscured window press still activates normally. No production change has been made in this packet yet; checkpoint 17 capture is still running.

The new shared fixture contains seven reservation layouts (both sides, middle, folded/empty, widest-per-side and mobile) plus five overlay ownership cases. The registered React law validates its JSON Schema using AJV, renders actual production reserve styles through React, and measures their canvas rectangles plus trusted pointer dispatch in Chromium. All five Layout tests pass in `panel-reservation-react-1.log` (10.03 seconds); the new native laws exercise the actual first two main-window chrome phases and are awaiting their fail-first result in Native64.

A separate routing gap is confirmed by fresh pointer logs: the orbit/pan/pick presses resolve `World3d` at the new window, but `pointer_owner_at` upgrades every retained hit to Chrome. `retained_router_owns_pointer` excludes World3d, so the chosen shell route dispatches no scene event. The actual new-window lifecycle law now promotes the retained owner registry as production does and requires the shared surface-owner/wheel outcomes. Chromium independently verifies those outcomes on a real canvas. This is distinct from the panel activation guard. Native fail-first execution is queued.

Native64 finishes in 4m44s with both new tests failing at their intended production assertions (1,245 tests outside the filter): the right-panel case leaves canvas width 1,593.6 instead of 1,287.2, and a visible bottom-middle panel permits activating the covered window. The repair now derives a separate window-canvas rectangle from the widest open edge panel plus two insets, preserves the full anchor body and mobile branch, and guards window activation with the same live panel geometry used for pointer ownership. Native65 checks those repairs and the independent resize/World3d routing regressions.

Native65 runs four laws: the panel activation guard passes; the resize rail reaches its intended compact-width failure; the real retained new-window World3d law reaches Chrome instead of Surface. The reservation case now reaches all geometry checks through widest-both, then encounters a test ordering error: closing each anchor immediately after checking it relocates a later anchor before its own comparison. All positions are now checked before any anchor is closed; no product expectation changes. The World3d repair keeps modal/panel precedence but excludes the dedicated World3d hit kind from retained-control routing, restoring the existing engine pointer/wheel path. Its runtime acceptance remains pending.
