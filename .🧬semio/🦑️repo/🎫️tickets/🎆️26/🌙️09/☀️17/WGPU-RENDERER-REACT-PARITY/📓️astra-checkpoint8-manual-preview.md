# Checkpoint8 Manual Preview

## Boundary

The existing IAB tab7 was explicitly reloaded after the new renderer wasm producer completed, while canonical activation8 was still materializing its plugin component. These are diagnostic observations, not the fresh paired acceptance journey. The final canonical activation later passed20tasks in17m40seconds. Five completed renderer/browser artifact hashes are recorded separately.

## Observations

At1280×720 the two puzzle windows paint Top and Perspective with their reference image and compact bottom-right gizmos. The previous giant colored gizmo rectangles are absent. No new warning/error was returned by the tab console check.

Activating the semantic Settings switch through CUA accessibility opens the Settings dock after delayed publication and transfers focus to the switch. The visual switch is active but AX still reports Value0. All displayed panel tabs also report Value0. The retained app settings body exposes four unnamed spinbuttons and empty paragraph nodes. Clicking the semantic General tab updates accessibility focus to General but did not change the visible selected tab or body after a later observation more than30seconds afterwards. Terra accessibility audit is investigating the real activation and projection paths; no source cause is assumed from this evidence alone.

The painted settings body appears blank/translucent over the reference image, and an on-canvas red diagnostic begins with `validation: puzzle3d.panel.settings`. Its full text is not captured in this manual view. The native console warning/error check alone does not cover this on-canvas fault. Capture exact fault/current scene state in the forthcoming canonical headless journey before attributing it to compositing or the component.

The current tab remains available for further UI verification. No saved preferences or user documents were changed.

## Accessibility Closure

The browser mirror is now an importable production module. A described input owns `aria-describedby`, while the description span is appended beside the void input in its retained hierarchy. Paragraph nodes materialize their label as text content. Repainting preserves renderer focus by the full window id, window generation, node id, and node key address, and a failed parse or paint is not recorded as published.

The retained Shell projection now derives the Settings switch's `checked` value and each `PanelTab` node's `selected` value from the same dock anchor visibility and path read by chrome. A Field copies its label and description into unnamed focusable descendants without replacing a child-authored name. Engagement controls use this Field path, so an unnamed stepper, slider, select, ring, or toggle group carries the visible engagement label in its semantic projection.

The neutral accessibility fixture adds a described spinbutton, paragraph, active Settings switch, and selected/focused General tab. Its mounted law uses the production mirror and `dom-accessibility-api` as the independent accessible-name and accessible-description oracle. Native laws exercise Field and engagement projection through `panel_ui_records`, and Settings/General activation through the real node-addressed `handle_accessibility_event` route followed by a fresh chrome projection. The handler itself was not changed: the earlier manual General result did not establish a routing defect.

### Verification

Passed:

```text
NX_DAEMON=false NX_FORCE_REUSE_CACHED_GRAPH=true bun nx exec --projects=workspace -- bun x vitest run '🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🧪️tests/♿️wgpu-accessibility-interaction/🟦️.tsx' '🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🧪️tests/📨️browser-frame-transport/🟦️.ts' --config '🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑‍🎨engine/🎯️targets/🧊️wgpu/🧪️tests/🎚️config/🟦️.ts'
2 test files passed; 44 tests passed.
```

Read-only parsing passed for the edited Shell production and focused Rust law files:

```text
rustfmt --edition 2021 --emit stdout <file> >/dev/null
```

No Cargo, native, wasm, shader, generated-browser publication, or fresh paired runtime acceptance was launched in this packet. The newly added native laws are source-stable and await the root-owned Rust run. The root paired probe owns confirmation of the real Settings → General → Appearance publication journey and any independent chrome hit-geometry or occlusion finding.
