# Checkpoint 17 Visual Review

Fresh checkpoint 17 images are not available yet. This file records the explicit comparison to repeat after successful activation; it does not promote checkpoint 16 screenshots into current-source acceptance.

Root re-opened `🗑️generated/astra-runtime/checkpoint-16-full/react/02-dismiss-tour.png` and its WGPU counterpart. The two pane bounds and slab geometry are already close, but several substantial visual differences remain in that recorded build:

- The Top reference floor plan is offset relative to the slab. A camera-only explanation is insufficient because the slab remains near the same location in both images; the reference transform and publication authority need reconciliation.
- WGPU's world grid is darker and more visibly aliased, particularly near the perspective horizon. The actual React line/fade/material policy must be used when implementing the fix.
- WGPU lacks the slab's thin visible edge outlines. The existing GLB outline packet already covers that scope and should be used rather than audited again.
- Navbar fixture width, role button widths, glyph choices, active Top tab styling, pane-control handle glyphs, and footer Task Manager label/order differ. The chrome packet has actual mounted React width evidence and drafted native geometry laws.

These are visible historical findings to re-measure on checkpoint 17, whose principal changes concern retained settings, locale publication, command-palette text ownership and Tree disclosure. The full paired journey must retain its equal viewport and capture camera, action, geometry and console evidence alongside the images. Artifact hashes before and after the run must include the renderer, bootstrap/worker, both activation receipts, current app component files, and generated app session.


Fresh WGPU17b and React10 activations both succeed with identical selected puzzle component digests. The full 57-step browser journey completes: 29 of 56 measured comparisons differ, one is unmeasured, and WGPU palette steps 44–45 fail their action postconditions. The earlier palette text-projection crash does not recur. Physical geometry checks fail only boot/dismiss-tour chrome; measured pane controls and split/focus/close/reopen cases pass. The dedicated Dock17 suite then passes 8/8 React and 7/8 WGPU. WGPU's remaining template case is physically covered by `panel.resize.bottom-left.outer`; this differs from the previous absent disclosure defect. All 43 tracked artifacts remain byte-identical after both journeys. Results do not establish visual, gesture-journal or representative-app parity. Source repairs resume only after this seal.
