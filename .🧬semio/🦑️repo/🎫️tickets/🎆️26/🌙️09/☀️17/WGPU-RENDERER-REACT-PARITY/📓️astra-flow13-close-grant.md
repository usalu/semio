# Flow13 Close Grant

Flow13 ran four selected laws, with one pass and three failures (249 outside the filter), nextest1.427s/Nx2m1s; run `b57d6684-61a2-4308-a11e-6e3496f95122`. The retained render projection law passes. The cohort law still fails because its retained-route vector and migrated-group projection contain the same 34 Flow rows in different orders; the fixture owner is correcting the ordering and independently auditing the Note census.

Both `context_menu_at_is_a_no_operation` and `evaluate_updates_preview_state_without_operations` fail in the real registered fixture close state machine at turn1095: one item and16,384 bytes are reported released under a grant of one item and4,096 bytes. The assertion remains strict. Root is tracing the production owner responsible for the oversized release; a higher test budget is not a repair. Receipt: `🗑️generated/astra-runtime/flow-native13-four/run.log`.

Source identifies a concrete lost-grant boundary: `close_typed_operation_step` forwards a Worker-stage operation to `MountedTypedCommandFullOperation::drive_worker_step`, whose rejected-session, retained-outcome and terminal-session close branches hardcode `JOB_PAYLOAD_PAGE_BYTES` (16KiB). This discards the caller's 4KiB grant. The worker method now accepts the exact byte grant and caps those three close calls to the smaller caller/page limit. Ordinary worker calls retain the existing page grant, while close and maintenance calls pass their caller's grant. The latest-wins hostile source mutations were updated to match the new explicit argument without removing any mutant. Flow14 native verification remains required; no GREEN is claimed from source inspection.

The registered `workspace:verify note-empty-config-ownership` gate passed after updating the explicit worker-call arguments in the source validator and its remove/duplicate hostile mutations; Nx3.6s (`🗑️generated/astra-runtime/typed-worker-close-grant-source.log`). This checks the exact initial worker dispatch and the existing Note ownership oracle/typecheck. The two actual Flow close failures still require the next native execution.
# Flow14 Compile Receipt

Flow14 stopped before native tests: E0433 at the composed archive test driver, `🧬️mutations/💾️binary/🧪️tests/🔬️unit/🦀️.rs:42`, where `semio_framework_async::yield_once()` had no declared crate dependency. Nx took 1m54s. Sol added the repository's internal async crate under Flow's dev-dependencies; no runtime dependency was introduced. The close-grant repair therefore remains behaviorally unverified until Flow15. Receipt: `🗑️generated/astra-runtime/flow-native14-archive-grant/run.log`.
