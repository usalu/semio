# Renderer Parity Continuation — 26 September

## Latest Verification — Coherent WASM Check and Native 157

The second direct locked/offline WASM check passed in 1 minute 45 seconds. Canonical browser build 4 is now running; the type-check is not yet a browser artifact or runtime parity claim. The native production library also compiled, but test compilation found an unbound Ink test node and an unqualified VFS InputState. SolSelect corrected both. Native157 is running the combined pending laws through the registered `test-wgpu-unit` target; an initial invocation accidentally named nonexistent `test-unit` and ran no tests.

UI40 passed all four selected native tests (681 skipped): receipt identity for duplicate payloads, poisoned duplicate receipt publication, exact input cancellation reporting, and mounted toggle semantics. UI41 passed the rounded avatar raster law (1 passed, 684 skipped). The old avatar include failure is resolved.

The full WGPU TypeScript run 2 executed 415 tests: 414 passed and one test assertion failed. Its new direct helper invocation omitted the fixture's select payload and authored arguments. Root corrected the oracle to execute `uiIntentToActionDescriptor` with the actual neutral select intent; full run 3 is pending. The original missing Chromium cache path is fixed for this invocation.

SolSelect's editor receipt/coalescing packet is source-coherent and its neutral native laws are queued. The same execution agent now owns the remaining whole-future cancellation guard. SolTree owns platform temporal formatting and shared ISO grammar, with Terra independently auditing native FFI and platform loading. No new native or browser behavior is declared passing until those commands execute.

## Latest Verification — Native 156 and WASM Diagnostics

The exact native contract run 4 passed all four selected tests (210 skipped): inline Tree toolbar ownership, Toggle state channels, generic temporal reply validation, and the direct Tree/DiffView detail relation. Native156 failed before renderer tests with pending editor receipt interfaces and two test-scope errors still being located; it is not a behavioral result.

Renderer receipt retirement now drains completed async handbacks, resident deferred cursors, staged action owners and source input in bounded steps. A single-receipt guard handles cancellation while dispatch is suspended. The audit still tracks the remaining cursor owned by an in-flight future during full host shutdown; the completed-handback case and current-action guard do not by themselves prove that final lifetime path.

The WGPU TypeScript frame-action-ledger suite passed 4 tests (694 ms), including the neutral receipt fixture and Ajv oracle. An initial invocation used the React test configuration, which does not include this WGPU-owned file; it found no tests and is not a pass. The correct WGPU configuration owns the passing run.

UI39 stopped before tests on a root-authored avatar fixture include with one excess parent directory. The include is corrected. A targeted filesystem check now resolves all 15 literal includes in the changed UI action/input/draw, frame ledger, and contract component tests. UI40 remains pending; a passing native avatar result is still required.

SolSelect completed the VFS English/German labels and accepted-frame accessibility packet. Its actual mounted React oracle passed 2 tests (699 skipped); native controls, retirement and localized label laws are queued. SolSelect now owns the EngineCanvas coalescing/echo implementation behind the receipt API.

The new actual mounted React delivery suite passed 4 tests (697 unrelated skipped, 45.79 seconds). Its neutral schema/fixture verifies first/latest typing coalescing, edit refusal suppressing the matching selection, and viewer refusal restoring saved text and making the host read-only. Root is preserving opaque receipt identity through the WGPU bounded queue, frame owner and dispatch/cancellation path. EngineCanvas coalescing/echo integration remains in progress; no WGPU pass is claimed for it.

The direct WASM check exposed 23 errors, all for the newly introduced receipt transport and pending EngineCanvas receipt functions while those interfaces were being added. It did not reproduce the earlier unrelated 41-error cohort. It remains a failed compile, with another coherent check required after editor integration.

The exact native contract run failed before tests on one remaining DiffView fixture emoji typo in the contract component test. Root corrected that include path and started run 4, now including the direct Tree/DiffView relation law.

The combined actual React descriptor/editor regression run passed 21 tests (676 unrelated skipped, 57.23 seconds). It includes VFS tagged descriptors and rounded avatar classes, editor keys, clipboard/composition, and closing/remounting one of two sibling editor hosts.

Native155 stopped before executing tests. Fixture emoji paths, native IME helper scope/imports, and the DirectoryDoorRequest initializer were corrected. Other errors referred to dependency members present in the current source after that run compiled its dependencies. Native156 is now running the combined pending behavioral laws against current sources.

The third canonical WASM producer failed after 32 minutes. Trunk reported 41 renderer errors but omitted their diagnostic messages; the successful first Cargo stage is not a successful WASM build. A direct locked/offline WASM cargo check through Nx is running to expose actionable compiler diagnostics before another canonical browser build.

The existing full parity goal remains active. Prior reports and source changes are present, but the ticket generated output directory and prior browser sessions are absent. Historical pass receipts are historical evidence only; this continuation starts fresh runtime and test verification.

The fleet consists of Astra coordination/integration, Sol Select behavior, Sol Tree inline controls/Dock follow-through, and Terra read-only accessibility and remaining-scope audit, all at the requested extra-high effort.

Repository ticket MCP and `repo://goals` are unavailable in this session after tool/resource discovery. Work continues in the existing ticket; no ticket or repository goal lifecycle change is claimed.

The current repository routes through emoji-prefixed `📋️project.json` files and shared fine-grained Cargo caches. The old private Cargo directories are not reused. Puzzle browser launch ports are now 6013 for React and 6113 for WGPU. Canonical Bun/Nx native tests and both inferred serve targets are running; the latter invoke their current producer dependencies.

The first React serve attempt failed before server startup because `.vscode/🧩️launch.seed.jsonc` had an extra opening brace before the EN1996 asset entry and a missing opening brace before the surface schema entry. The WGPU dependency graph reported the same failure while its renderer compilation continued. The two-brace repair preserves both commands; Bun JSONC parsing now confirms all 386 entries and each repaired entry exactly once. React startup is retried through the same canonical target.

The owned Tree window decoder law now has a fresh executed receipt: `@semio-tech/framework-renderer-react:test-long`, filtered to `requires and owns every closed Tree window row extent`, passed 1 test with 1 unrelated test skipped (751 ms Vitest duration). Its owning suite is the renderer React long suite, not UI React.

Native 152 and the first WGPU browser producer did not reach runtime checks: both compiled renderer source against a Tree contract artifact without the concurrently added `inline_toolbar` field. This is a build failure, not a passed or failed behavioral law. The Tree owner is finishing the full contract/copy/reconcile path before a coherent rebuild. Puzzle component materialization completed in the same graph.

New Dock accessible-name neutral fixture/schema cover authored instance titles, English/German focus/unfocus/close/drag, maximization, and accepted-frame publication. The independent React Testing Library oracle passed both locale tests (2 passed, 5 unrelated skipped; 19.44 s suite duration). The WGPU fail-first law is running. The current React server is waiting for its canonical local hub startup; no browser result is claimed yet.

## Current Browser Reference, 26 September

The canonical React preview on port 6013 became ready after its normal hub/bootstrap work. The initial module load needed several minutes under concurrent Rust builds; the page then rendered the actual Concrete Forest fixture with both Top and Perspective windows at 1280×720. After dismissing the first-run tour, the browser console returned no warning or error entries. Captured current baseline and General settings screenshots under `🗑️generated/astra-runtime/browser26`.

Observed DOM geometry: Top tab x=6.375, y=38.359375, w=38.078125, h=16; Top panel x=6.375, y=60.75, w=417.09375, h=627.265625; Perspective tab x=433.03125, y=38.359375, w=82.015625, h=16; Perspective panel x=433.03125, y=60.75, w=840.59375, h=627.265625. Both tab roles report selected=true. The language dropdown outer box is x=975.8125, y=459.015625, w=162, h=104.25; its two option rows are 25.5625 high. Selecting Deutsch updated visible chrome, focus/close/drag labels and settings. In this current fixture both German window titles resolve to Puzzle 3D; preserve that observation separately from the authored-title unit fixture.

UI35 executed five native Select cases: four passed and `retained_select_popup_is_viewport_clamped_scrolled_and_glass_foreground` failed. Sol isolated an oversized option in a 3.2px viewport being incorrectly scrolled to its trailing edge; the fix preserves a visible leading edge. UI36 is running the fix and new regression. No passing result claimed yet.

Sol Tree reports the inline toolbar contract/schema/owned decoder/reconcile/layout/AX sources coherent; the canonical WGPU preview build has restarted. Native Dock AX verification remains compiling. No fresh WGPU browser verification has occurred yet.

## Dock Semantic and Candidate Lifecycle Follow-Up

The shared Dock schema/fixture now specifies tab and tabpanel roles, selected state, and a non-actionable panel body. The React DOM oracle also verifies `aria-controls` points at the named panel. Re-execution passed both English/German cases (2 passed, 5 skipped, Nx 35.4s).

WGPU stages Dock roles, selection and panel references beside painted names in the existing candidate label map. Accepted projection copies them with its hit registry. Each new FrameSetup clears candidate semantic records; the previously accepted projection remains untouched. Added a native regression that fills discarded names to capacity, starts a successor walk, and verifies the new name can be published without old discarded entries. These native changes are not yet verified.

Native153 did not execute tests: compilation failed with three `UiTreeItemNode.inline_toolbar` missing initializers in `semio-framework-artifact-playbook-playbook`. Sol Tree has the diagnostics and is updating the remaining literal sites. WGPU serve2 also stopped before runtime: the new dynamic extension module was not yet registered in the browser source schema, and the shared Cargo.lock was stale under locked builds. Sol Marketplace owns the browser module registration; root is resolving the lock against the current workspace without upgrading dependencies.

## Verification Queue Update

UI36 executed four tests (2 passed, 2 failed). The remaining Select defect was a fractional viewport edge: row_top=3.2 versus a viewport height fractionally below 3.2 caused a strict comparison to scroll an already-visible edge. Sol added a 0.001 geometry tolerance; UI37 is running the four cases with `--no-fail-fast`.

The scoped workspace `cargo metadata --offline` completed, and a following locked metadata check completed. Cargo.lock is byte-identical to the captured pre-check copy, so root did not change it; concurrent workspace activity had resolved the earlier drift. WGPU serve3 now passed `deps-cargo`, browser boot and frame-worker generation. Its wasm and fixture build remain pending.

Native154 groups accepted Dock labels/roles, discarded-candidate cache capacity, both inline conflict-button laws and Dock silhouette clipping. Source-level Playbook literal omissions are repaired; this run remains compiling.

Current React browser focus/restore/close journey succeeded: focusing Top left one Top tabpanel and an Unfocus button; restoring brought back both windows; closing Top left Perspective and removed the unnecessary Focus control. Browser console warnings/errors remained empty. Concurrent source generation then triggered a normal plugin reload, so restore the two-window baseline before the paired WGPU comparison.


## Select, Dock, and Accessibility Verification

- UI37: four focused native Select laws passed, including oversized-row nearest reveal and the retained popup geometry; 676 laws were not selected.
- React Dock: all four locale/layout cases passed (English/German, split/stacked); five other tests were not selected.
- The canonical UI contract target stopped in its fixed-list fixture precheck before Rust execution. The exact inline Tree toolbar law is queued separately through scoped Nx exec; no native result yet.
- Live React reference: focus Top, restore the split, and close Top succeeded. Reopening has not been verified. Source HMR can reset this reference page, and local hub catalogue bootstrap remains in progress.
- Root is correcting PanelTabBar/pane-chip accessibility to pressed buttons using the actual React PanelTabBar oracle and the production WGPU mirror. The shared fixture now explicitly names button/aria-pressed semantics; its red run is in progress.
- Native154, UI38 fractional clipping, the exact Tree contract law, and canonical WGPU startup remain in the shared Cargo queue. No result is inferred from a running build.


## Pressed Semantics Regression

The focused red test run completed: 2 passed and 1 failed, because the production mirror omitted `aria-pressed` from the shared fixture's Settings buttons. After adding optional pressed to the shared projection and browser mirror, the entire accessibility interaction file passed: 13 tests, 47.91 seconds. React's actual PanelTabBar oracle verifies both active and inactive states with dom-accessibility-api.

Root staged labels and pressed state for manually registered anchor/mobile panel controls, made clipped Settings catalogue entries pressed buttons, and captured plain pane-chip labels as buttons without checked/selected/pressed. Native assertions cover the constrained strip and pane labels but are not yet executed. Retained Toggle is a separate real mismatch: the React Interpreter renders Checkbox as TreeCheckbox/checked and default appearance as Toggle/pressed; shared projection still modeled default as switch/checked. SolTree owns that correction. SolSelect owns explicit fullscreen and utility toggle pressed semantics.


## Live Marketplace Reference

Opened the actual React Marketplace on port 6013. The visible tree contains disabled extension records grouped by host (sourcing, process, playbook, imperative, flow, cad), each with Enable and Uninstall actions. Puzzle 0.1.0 is Loaded, with Reload and disabled Uninstall. The source reads dev+extensions+hub, and installation offers From file and From URL. The browser returned no warning/error log entries for this inspection. No pre-existing package was enabled, removed, or reloaded. Screenshot and DOM snapshot are under the ticket's generated browser26 directory. The fresh WGPU server is still compiling, so this is only the React reference.


## Mobile and Cancelled Chrome Publication

Added the native mobile panel law using the real paint routine at width 390. It checks actual prefixed control IDs, authored labels, pressed true/false, and absence of desktop catalogue duplicates. The projection omits desktop-only catalogue additions on mobile. Terra reviewed the implementation and requested no semantic correction; native execution is still pending.

Extended the native discarded-chrome capacity law through the real seal/discard/acknowledge boundary: A remains the exact published projection and generation while B is sealed then discarded; B's late ACK is rejected and A's hit remains live. A real FrameSetup resets candidate names, C is accepted, and a repeated late B ACK cannot replace C. This law is selected in native154 but has not yet executed.

Terra traced the production presenter guard: admit_next_frame does not invoke another build producer while has_pending_presentation is true, including presentation, retirement, and gate acknowledgement. Therefore a successor FrameSetup cannot overwrite candidate chrome names while a valid sealed B remains pending in the current pipeline. No candidate map copy or pipeline redesign was introduced.


## WGPU Server 3 Compilation Receipt

The canonical browser build reached the renderer and failed with 25 diagnostics. Current source checks identify a mixed build cohort: the UI contract now exports all EventFeed formatting types/constants and contains AccessibilityProjectionNode.pressed; os_directory now reexports CanonicalCheckpointPairV1; ArtifactHost now implements set_document_seed; and the current ComponentDocumentCodec trait no longer contains genesis. The compiler used older dependency metadata while compiling newer renderer source. No compatibility shim or speculative API repair was added. A new invocation is required to rebuild those dependencies; successful compilation remains unverified.


## Folded Panel Selection

The live React Marketplace reference exposed another distinction: after folding the panel, framework.marketplace retains aria-pressed=true and data-active=true while data-state disappears. Terra confirmed this in PanelTabBar: isActive controls pressed, while isActive && showActiveColor controls fill. The shared fixture now states pressedFollowsSelectionWhenFolded, the actual React oracle runs with showActiveColor both true and false, and the native activation law checks the remembered root selection after folding. WGPU's fallback pressed projection now reads the remembered path without requiring an open anchor. The painted highlight condition is unchanged. These latest test additions await execution.

## Fresh Receipts and Native154 Failure

SolSelect's final accessibility interaction run passed all 15 tests, including folded panels, fullscreen, and utility controls. It supersedes the intermediate 14-pass/1-fail run whose CSS ID selector was corrected. SolTree reports the retained Toggle React oracle passed 2 tests, TypeScript projection passed 263 checks, and EventFeed Intl formatting passed 4 tests.

The canonical workspace browser dependency task passed in 4.2 seconds. Root's fresh worker-step-budget run then passed all 17 tests in 10.39 seconds (exit 0), including the three Chromium cases previously unable to launch.

Native154 exited 1 after 48m11s before running any test: the compiler required `ComponentDocumentCodec::genesis` in plugin-host. The current source trait at os/store contains only schema, pack_schema_hash, and print_mirror, and the current plugin-host implementation supplies those three methods. This is another stale dependency cohort; no source shim was added. A fresh build is needed. UI38 clipping and the exact Tree toolbar contract law still have no execution receipt.

## TextEditor Keyboard and Action Wire Correction

The actual React TextEditor and the authored Trinity Jack consumer use `textEdit {surfaceId, text}` followed by `textSelect {surfaceId, start, end}`. WGPU previously emitted the reverse order with `document` and serialized `selectionJson`; Trinity interpreted the missing text field as an empty edit. Root corrected the production bounded writer, its byte accounting, and the stale native assertions. Navigation and pointer selection now emit selection alone. Reservations still precede host mutation. The unused standalone test-only editor wrappers carrying the old wire were removed after a caller census found none.

WGPU now routes ArrowLeft/Right/Up/Down, shift selection, Home/End, configured Tab insertion, Enter with scene newline gates, and physical Space into EditorHost. Alt character chords are declined. The AppInteraction Space branch preserves menu/retained-Select priority and then offers the focused editor before using the global pan modifier. A primary pointer press outside the exact mounted editor clears its keyboard ownership; secondary/middle presses and releases preserve it.

The existing language-neutral editor text-input fixture now contains nine renderer key cases. All nine pass against the actual React component (86.68 seconds, 674 unrelated tests skipped). Native tests cover those same cases, all 18 existing Chromium typing sequences through the production WGPU key route, primary focus transfer, and actual AppInteraction Space precedence with and without editor focus. Native execution remains pending. Clipboard, IME, accessibility, asynchronous refusal/coalescing, and local echo remain separate work assigned to SolSelect.

## Fresh Build and Fractional Clipping Receipts

UI38 fractional adjacent silhouette ownership passed: 1 test, 680 skipped, 0.032 seconds. The first exact Tree contract run failed before tests because four test references lacked a UiNodeId import; SolTree qualified those references. The first fresh canonical WASM run failed on an accidental `detail: None` inserted into a TreeItem struct declaration. SolTree removed it. These were real source defects, distinct from the earlier stale dependency failures. Canonical WASM retry 2, the exact contract retry 3, and native155 are running; no success is inferred.

The live React reference was affected by development refresh: its brush registration logged `instance-retired`, and scene content temporarily disappeared. Root restarted only its own Vite process with the existing SEMIO_VITE_HMR=0 option. The canonical replacement server is currently waiting for the already-owned local hub; paired live WGPU/React verification is still outstanding.

The text-input oracle completed successfully: all 29 checks passed in 58.10 seconds, including Chromium's 18 neutral typing sequences and the local-echo/refusal cases. The second WASM attempt again found the corrupt TreeItem declaration after it was reintroduced; root repaired the exact declaration, and SolTree confirmed a repository census contains no remaining bad declaration pattern. WASM attempt 3 is running.

The stable React server subsequently became ready. Its fresh Concrete Forest scene loaded, and the two pane bodies retained their earlier geometry (Top 417.09375 × 627.265625 at 6.375,60.75; Perspective 840.59375 × 627.265625 at 433.03125,60.75). Opened Top Window Options and its Vortex Show Select, selected Always, then restored Selected. The visible label followed both actions, and the browser returned no new warning/error entries after this fresh load. The popup measured 128 × 104.25 at 105.875,228.671875; option rows measured 119.625 × 25.5625. Screenshot and DOM/geometry receipt are in generated/browser26/react-pane-select.*. This remains a React reference, not a paired WGPU pass.

Root also opened Top Utilities and activated Transform. The actual utility buttons publish aria-pressed true for Transform and false for Brush, Volume Brush, and Relocate; their Rotate/Move children are native checked checkboxes. Deactivated Transform afterward; no new browser warning/error entries. The DOM receipt is generated/browser26/react-utility-toggle.md.

SolSelect's actual React TableHost keyboard oracle passed 1 test (673 skipped). The three native Table laws are prepared but not yet executed. SolTree's actual DiffView host/third-party oracle passed 2 tests, and the retained TypeScript detail relation test passed 1 (2 skipped). The shared TreeItem detail relation, retained reconcile/layout, selected-conflict projection, and authored Playbook DiffView producer are implemented. Their five native laws remain pending. Root added an editor queue-credit regression ensuring a refused two-action edit leaves text/caret unchanged while a one-action navigation still fits the remaining queue slot; it also remains pending native execution.


## Editor Lifecycle and VFS Descriptor Continuation

- Added shared `hostLifecycle` input to the text-input fixture, a retained phase-4 EditorHost binding/close/reopen law, and an actual React sibling-preservation oracle. React oracle passed 1/1 (685 skipped), 53.24 seconds. The native law is pending; it verifies the CPU component-close bridge and exact bound engine token, not a live GPU presenter close.
- Expanded the existing test-only CPU component-close helper to accept TextEditor and check an available engine token against the scene close request before retirement. Existing map/node/board cleanup remains supported.
- Source audit found WGPU VFS descriptor values serialized tagged objects instead of consuming React's text/time/avatar union. Added a neutral descriptor fixture and native decoder law, plus actual React descriptor checks: 9/9 passed (686 skipped), 42.68 seconds.
- VFS text/avatar decoding now respects declared row columns and descriptor presentation. Column labels fall back to descriptor-kind names. Avatar initials use React's exact trim/split/take ordering, including consecutive spaces. Images use exact-source loading state and rounded raster quads; native shader/draw/runtime receipts remain pending. Sol Tree owns generic host temporal date formatting; Sol Select owns raw-tree expansion and navigation consistency.
- The first WASM compilation reached `Finished dev profile` after 18m38s. Trunk packaging remains active, so this is not yet a successful final WASM build or a paired browser receipt.
- Editor asynchronous dispatch acknowledgement/coalescing remains a substantive open gap: React awaits edit acceptance before selection and resynchronizes rejected edits; WGPU currently has only generic deferred dispatch fault logging. This requires correlated renderer-owned input envelopes, not guessed action-descriptor matching.
