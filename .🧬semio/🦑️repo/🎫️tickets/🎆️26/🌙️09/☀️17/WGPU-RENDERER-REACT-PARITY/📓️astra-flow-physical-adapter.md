# Flow Physical Adapter

Root is calibrating the existing physical Flow adapter while the native Tree and Ink source packets reach a coherent build boundary. The existing React listener at port 6016 is observed without restarting it. This discovery is not a fresh activation or a renderer acceptance result.

The adapter now accepts a ticket-generated output subdirectory and uses the same WebGPU/Metal launch flags as the main, Dock and Canvas probes. Its schema/source/Chromium trusted-input oracle passes in `🗑️generated/astra-runtime/flow-contract-1.log`.

Source review identifies the same stale receipt assumptions already corrected in Canvas: the React public input ledger contains identity and outcome, not action arguments; the mounted host controller may be its retained record id rather than the producer's authored controller string. The Flow adapter currently assumes both. Before scoring a physical run, its assertions must use the actual mounted identity and semantic geometry/selection witnesses, while preserving direct argument checks for WGPU. No fabricated React arguments or weakened selection/move/cancel consequence is acceptable.

Discovery1 and Discovery2 reject the authored surface key as a mounted probe key. Discovery2's inventory establishes the actual mounted surface `window:flow-main`, wrapper key `flow.play.main`, and three real nodes `slider`, `add`, `preview`. Discovery3 and contract2 reject the first selector correction because its schema constant had not yet been updated. The schema and fixture now both require the exact authored wrapper plus current window surface; contract3 passes and Discovery4 reads all three mounted node geometries successfully. Its inventory also exposes record id `1`, the real published selection, and the producer's `flow.host_snapshot` camera/layout data.

The adapter now reads those actual fields: settled ledger outcomes under the exact mounted record/window, selected ids from the published host selection, moved-node layout with unchanged peer layouts, and viewport from the host snapshot camera. WGPU retains direct argument assertions. Adapter typecheck2 completes successfully. FlowReact1 is running against the existing listener as calibration; fresh paired activation remains required, and the current five-case fixture still needs the real port-to-port connection gesture before full NodeGraph acceptance.

FlowReact1 fails all five cases and is preserved. Its first physical click hits the Number slider's inline value control, producing two applied graph edits rather than node selection; the screenshot confirms the visible numeric change. Its wheel target is also the slider's center. The fixture now explicitly targets the plain `add` neuron for node selection/drag and aims wheel input at empty canvas. The ledger waiter now settles every matching action in the captured batch instead of asserting a later still-pending row. FlowReact2 tests these calibration corrections. The pan case's unchanged producer camera remains an unresolved observation; it is not dismissed as an adapter defect or accepted solely from screen movement.

FlowReact2 passes Escape cancellation and fails four other cases. The exact `interactionSelect` action is applied but the published selection remains empty. A committed node visually moves while its published scene layout remains unchanged. Source confirms a known selection gap in the Flow main-window producer, which still deliberately initializes an empty selection rather than reading InteractionView. Pan's later failure is a journey cascade after zoom clips a seed node outside the viewport, so it is not an independent pan diagnosis.

The camera assertion also needs a more precise authority: `hostSnapshotJson` is the Flow host bootstrap snapshot; the NodeGraph scene's `viewport` is the separately published window camera. The existing read-only FlowGraph probe now exposes that actual scene viewport alongside its node layout, and the adapter uses it directly. This adds no action injection or synthesized ledger payload. FlowReact3 is checking the corrected camera witness against the same pre-existing listener; full fresh acceptance remains pending.

FlowReact3 confirms the published camera changes after wheel input. Its next geometry assertion fails because zoom legitimately clips the third seed node; the following pan case is blocked by that same mistaken visible-count precondition. FlowReact4 distinguishes the total node roster from visible geometry, still requiring the complete specimen and a live visible node. Pan records clipped ids and measures common visible nodes. Exact published selection and committed layout checks remain unchanged. The two existing NodeGraph interaction/scroll suites pass 26/26 after the viewport read-back addition; adapter typecheck3 is clean.

FlowReact4 completes with 3/5 passing: Escape cancels the node drag without a graph edit; wheel publishes zoom 1.1 and changed live geometry; middle pan publishes a translated camera and matching node movement. Selection remains published as empty, and the moved `add` node still has scene coordinates `{x:200,y:0}` despite its visible 84×48 displacement. Terra is verifying whether the persisted layout authority is the separate host snapshot layout before attributing the second failure to production. This run remains calibration against the existing listener, not fresh paired acceptance.
