# Checkpoint 16 Integration Plan

The goal and ticket remain active. Checkpoint 15 is sealed diagnostic evidence; current source changes are not included in it. No native or browser gate is running at this planning boundary.

## Source Packets

| Packet | Required completion boundary | Acceptance still needed |
| --- | --- | --- |
| General and dock | Scope and overlay classification, captured Select owner, compact first paint, 160/16/22.4 control geometry, authored split-weight scaling | Actual General commit law, split physical law, fresh Appearance/Language and 120-pixel drag outcomes |
| Tree rows | Actual Actions-document mounted law establishes the cause of 12.04-pixel rows; fixed 24-pixel pitch with intrinsic content and clipped scrolling | Native row/hit/scroll outcomes, same-theme browser geometry and offscreen-command reachability |
| Search and Find | Typed bounded view, exact React item producer, input/focus/AX, grouping and activation | Native normal chrome walk; live query, canonical result activation, dialog retirement and expanded command form |
| Full-resolution raster | Browser begin/push/seal strips, native moved decode, exact CPU/GPU credits and ownership, recovery after eviction | Updated ownership stress laws, World recovery, format/shader consistency, actual natural dimensions and profile comparisons |

## Sequential Native Gates

Use the existing isolated native intermediate directory and run only one native build at a time.

1. UI suite after all new ownership and Tree tests are coherent. UI 39 previously passed 632 tests; new source requires a new run.
2. Shader suite if the final texture storage/shader policy changes. Shader 10 previously passed 136 tests; that result does not validate a changed texture format.
3. World/terrain suite, including S3 authored reference identity/opacity/geometry and the new CPU/GPU eviction recovery laws. The queued run is World 22.
4. Full WGPU renderer unit census after palette and producer/test migration are coherent. Native 31 failed compilation before executing any test. General, dock, palette and existing async-boundary laws must all execute in the next complete census.

Do not add compatibility methods to satisfy old staging tests. Update the laws to the final ownership contract and preserve meaningful refusal/cancellation assertions.

## Pixel and Decode Checks

The actual Three and production WebGPU S2/S3 comparisons must use the final raster storage format and shader sample policy together. Existing sRGB-storage NoColorSpace recovery cannot be combined silently with Unorm storage. Natural reference decoding must preserve the source 2275 × 2560 dimensions and correct plane aspect, with bounded row transfers, retry and cancellation.

## Canonical Activation and Browser Acceptance

After coherent source and relevant gates, complete WGPU activation 16 and the matching React activation. Seal the generated browser artifact hashes before measuring; avoid activation or browser generation during a journey.

- General journey: actual selected Appearance and Language, compact initial placement, Drivers disclosure, close and reopened live body, scene gestures, fullscreen entry and exit.
- Full journey: use the stronger pane child-control, focus/unfocus/refocus, individual-close and divider geometry assertions added after checkpoint 15. The palette step also requires a real input/query/result and command-form consequence.
- Compare screenshots only at equal explicit locale, appearance and density. Record geometry separately from event-ledger differences.
- Profile the same boot/dismiss/Settings sequence to check whether image hashing/repeated offers have left the frame path. Timing from different runs remains diagnostic attribution unless conditions are controlled.
- Recheck all sealed artifact hashes after measurement.

Passing this checkpoint does not complete the broad renderer goal. Representative Canvas, Ink and the remaining surface families, clipboard, additional docking/drop zones, cancellation and configuration variants still need their recorded implementation and runtime acceptance work.

## Executed Source Gates

- UI 41 passed all 635 native UI tests, including bounded raster ownership, GPU residency, and all three raster profiles using the verified sRGB storage contract.
- Checkpoint 16 isolated production-WGSL pixel checks passed: reference visual 13/13 and painted texture 3/3, all RGBA8 byte exact against the recorded actual Three samples. These tests do not replace full renderer integration or natural-resolution browser acceptance.
- Palette source is coherent with its actual React component oracle passing 3/3; full native renderer and live browser query/activation remain queued.

World 22 stopped during compilation: eight older/new ownership test calls still used removed tuple publication or required unavailable Debug output. Root migrated those laws to the final lease/identity contract, preserving exact rejected-owner pixel checks and active-source cancellation. World 23 is running; no World runtime pass is claimed yet.

The shared browser probe now adds Actions Tree acceptance: first two rows must retain 24-pixel height and pitch, Abort must start offscreen, a physical wheel must expose its hit rectangle, and reverse scroll must restore the initial geometry and clipping. A React-only replay validates the oracle before using it against the next WGPU activation.

World 26 passed all 236 selected World/terrain tests (243 tests outside this target were skipped). The pass includes repeated reference replacement, six pooled GPU identities through two CPU slots with same-source recovery, stale-aim/middle-click completion, and all S3 reference-visual laws. The interaction registry bitmap adds exactly 128 inline bytes (owner 40 → 168), now recorded in the fixed-slot memory fixture; the bounded-thread-stack law passed. Full renderer/browser acceptance is still pending.

Native 32 stopped at compile time with five source errors: a palette breadcrumb passed the parsed Terminology enum where the helper takes its id, and the terrain asset branch had two stale restore_output calls referencing nonexistent job/output variables. Root corrected these within the existing ownership behavior. No renderer test executed in Native 32. UI 42 is running next with Tree changes and the new actual six-texture WGPU residency/recovery law.

React-only browser oracle calibrations are complete: Tree row/scroll five-step replay passed; palette pointer activation three-step replay passed after correcting the staged-form predicate; separate keyboard three-step replay passed ArrowDown/ArrowUp, proper combobox/listbox active-descendant, query-preserving Escape/reopen, and Enter activation. These are React oracle passes, not WGPU acceptance.

UI 43 passed all 637 tests after root corrected the scene-raster uploader to respect each lease's transfer-byte limit. UI 46 reran the actual GPU law with libtest output enabled and recorded `[DEBUG] raster-table-scene-integration executed: textures=6 cpu_slots=2 resident_bytes=48 recovered=2x1 closed_bytes=0`. This was hardware execution, not the no-adapter skip path.

The same UI 46 run established two new red scroll laws: absolute coordinates retained the pre-scroll y=72 instead of painted y=24, and a Select trigger at its scrolled screen position did not open. Root unified the child-origin calculation for paint, clipping, event descent and absolute bounds, and changed subtree hit testing to retain window coordinates through placed overlays. UI 47 is checking all UI tests against that fix. The dense Shell Actions law now physically presses/releases the revealed terminal row and requires its action; it still awaits the renderer census.

The stronger React Tree calibration passed all five steps, including a physical click on the revealed Abort row and the observed `engagementAbort` dispatch. Its result is the baseline for WGPU runtime acceptance after activation 16.

The bounded browser raster strip packet is source coherent. Its Chromium/Three oracle passed all eight codec/orientation rows, measured 23 immediate readback/push/yield strips for the natural 2275×2560 plan, confirmed cancellation after one strip, the shared page fallback, and zero decode calls for exact reuse. See `📓️astra-sol-raster-strips.md`; the new Rust exclusive-writer law still awaits the renderer census.

UI 48 passed all 639 native UI tests with zero skipped. UI 47 first isolated the final popup defect: primary release used the ordinary root hit test even after primary press correctly selected the escaped overlay row. Release now resolves the same topmost overlay subtree after releasing capture. Both scroll regression laws and the existing overlay, focus, paint and GPU residency laws pass. No blanket ancestor-clip bypass was introduced.

The final controls audit exposed four keyboard/accessibility boundary gaps and a Stepper press/cancel gap. Sol is implementing AX focus ownership, Select Space/typeahead and blur-commit preservation; root owns explicit pointer cancellation and Stepper timing. Palette2 is source coherent, with its actual React oracle 6/6 and keyboard/mirror tests 34/34 passing. Native renderer acceptance remains pending both controls boundaries.

Root's Stepper law reproduced zero actions at primary press where React emits one. The repaired retained control commits its first increment/decrement on press; release and cancel only retire capture. UI 51 passed 640/641, with the sole older law looking only at release; that law now checks press and asserts release silence. UI 52 passed all 641 tests with zero skipped. The independent mounted React interpreter answered all six shared Settings/Stepper tests. Explicit pointer-cancel now crosses the browser lossless wire, normalized event, host, Shell and retained router. Browser wire/transport passed 67/67 and UI render/dispatch passed 137/137, including independent pointer capture cancellation. The UI host and full renderer integration gates remain pending.

Host cancel gate 1 rejected a duplicate `--no-fail-fast` argument before executing native tests. Corrected gate 2 ran all 79 tests: 70 passed, nine input-admission/root ownership laws failed, zero skipped. The failures concern retained constructor/terminal queue backing and generation exhaustion; they are not a passing host gate. Their full output is in `🗑️generated/astra-runtime/native-host-cancel-2.log`. Native 33 is now executing the full renderer census with coherent palette, focus, scroll, Stepper and pointer-cancel source.

The browser palette probe now resolves the canonical accessibility option to its published physical hit rectangle, asserts the canonical command key on both renderers, and tests dialog-interior absorption, a row press released over the input, physical Close, outside dismissal and preserved query before actual selection. The strengthened probe still needs a React calibration and fresh WGPU acceptance.

Native 33 stopped before test execution at one test-only lifetime error: the dense Actions helper closure returned a borrowed hit from its argument without a named lifetime. The helper now clones the published hit, preserving the measured rectangle and action. Native 34 reruns the full census.

The strengthened React palette calibration is not yet accepted. Its first run sampled a moving dialog corner; after waiting for the filter layout and verifying an interior point, the second run passed interior absorption and cancelled row release but found the physical Close control outside the viewport. The captured screenshot shows the filtered result at the top edge and the input/Close above it. Sol is investigating this reference geometry/focus defect before the next matching React activation; the probe has not been weakened to accept the offscreen control.

Ink clipboard phase one passed all six mounted React oracle tests and added the strict shared contract. The semantic WGPU producer remains pending; see `📓️astra-sol-ink-clipboard.md`. Terra corrected the earlier Ink cancellation assumption: generic Canvas cancellation does not retire Ink's separate queued interaction job. Exact NodeGraph, Board and Ink ownership is being audited before the next production cohort.

Native 34 compiled and admitted 1,212 tests. Six failures were reported before the 300-second runner budget terminated the remaining host-panel test. The source-mutation raster law still named the former 16 MiB item limit, fixed 16 KiB transfer page and pre-lease return tuple. It now checks the final 64 MiB admitted GPU item contract, each lease's bounded row transfer and the retained CPU-release owner; four new negative mutations cover transfer-credit bypass. This updates the guard to the already implemented full-resolution design without changing production capacities. The measured scene-map record now includes the World interaction registry bitmap: slot bytes 23,336 → 23,464 (+128); owner bytes 49,784 → 50,040 (+256 from its inline rejected and retired owners). Table capacity and the bounded-stack limit are unchanged.

The old host-panel test's remaining native hang is a production deadlock: `KernelPoolFuture::run_turn` took its future from a mutex in an `if let` scrutinee, retaining the temporary guard across the body, then attempted to acquire that same mutex when polling returned Pending. Root now ends the guard's scope before entering the polling body. A new neutral immediate/self-wake trace is executed through both the repository kernel task and the existing Tokio executor. The original host-panel test remains intact; no skip or fake guest fixture was added. Both laws await Native 35, and Terra is independently auditing the wake/lock boundary.

## UI53 Accounting and Kernel Response Race

UI53 ran all 641 laws: 640 passed; the exact UiSurfaceSlot size witness failed (161912 measured vs 161896 recorded). The new OverlayRow owns four f32 coordinates, growing LayoutNodeKind from a 12-byte maximum payload to 16 bytes. LayoutInputNode rounds that growth to its eight-byte alignment. MountedLayoutJob retains one inline rejected_node; UiWindow owns two inline jobs through layout_job and layout_rejected, while layout_session keeps jobs behind Arc owners. Thus the slot grows by 2 × 8 = 16 bytes. Updated only the measured fixture; capacity remains 64 and registry owner 520 bytes. UI54 will verify the complete census.

Terra independently validated the kernel pool guard-scope deadlock repair, and found a distinct response delivery race: a producer can publish after the consumer checks result but before waker registration. Root is adding deterministic before/during/after delivery traces and a Tokio oneshot oracle before replacing the two locks with a single state owner.

UI54 passed all 641 tests, zero skipped. The exact +16 byte fixture update is verified. Native35 is deliberately a focused red run for the newly added response-delivery race, before its production repair; the subsequent renderer run will be the full census. Strict Ajv 2020 schema validation accepted the two future traces and three response boundaries (`kernel-pool-fixture-schema-4.log`); attempt 3 failed only at inline shell quoting before executing JavaScript.

Scene cancellation phase one passed 33 mounted/neutral tests and truthfully records the four missing React cancellation producers. Sol is implementing the shared contract in both renderers, while the second Sol owns Ink clipboard and its gesture retirement. Terra is independently classifying the 17 React-census and nine UI-host failures before assigning repairs.

Native35 compiled the new response law and failed exactly at `register-waker: a delivered response must wake its pending consumer` (1 test failed, 1,213 outside the focused selection). The same forced interleaving first passed through Tokio oneshot. Root then replaced ResponseSlot’s two separate locks with one ResponseState mutex: poll atomically takes a result or registers its waker; deliver publishes a result and extracts the waker atomically, then wakes after unlocking. Waker clone/replacement/drop also occur outside the state lock. Native36/full is waiting for the Ink cross-module structural batch to reach a coherent source boundary.

The class-name `test-quick` wrapper obstruction is confirmed upstream of subject execution: `nativeSecondImplementationBreaches` calls `isSemioNativeArtifact(evidence.format)` with an undefined format; the latter dereferences `.length`. This is a global contract-validation failure, not a failing class-name output comparison. The two class-name test-subject projects passed, but the wrapper is still red and has not been waived. No shared test-platform source was changed during this inspection.

## Host and Reference Census Reconciled

HostCancel3 executed all 79 host laws with zero failures/skips after accepted-only input generation and staged physical backing retirement. React Census4 executed all 804 UI laws across 28 files with zero failures after test harness path, live-node and prototype-restoration corrections; the actual body-glass law remains unchanged. Full native renderer and fresh browser acceptance remain open.

Both Sol execution packets now report source-coherent scene cancellation and Ink clipboard interfaces. Native36 will compile the integrated source and intentionally includes the new mounted event-full refusal law before the Winit callback ordering repair. LayoutCatalogue1 stopped on a test-only Label conversion, corrected to LocalizedLabel; LayoutCatalogue2 is the fail-first behavior rerun. The actual mounted React catalogue oracle passed12/12 including strict fixture-schema validation. No Layout native or browser parity claim is made yet.

Native36 stopped before executing tests with39 compile errors in the integrated pointer ownership and Ink clipboard packet. Diagnostics are preserved in 📓️astra-native36-compile.md. Source owners are reconciling unpublished PointerId paths, stale explicit pointer arguments, a duplicate Ink helper, a missing JSON macro and two clipboard error-string conversions. Root started LayoutCatalogue7 focused green verification meanwhile. The mounted Winit refusal RED remains unexecuted, so the callback production repair has not been applied.

LayoutCatalogue7 passes5 focused laws including the real retained app preview/drop in an addressed window for all4 catalogue kinds. Catalogue9 replay records all4 DEBUG receipts; Catalogue8 rejected an unsupported output flag before running. The decoder repair now has fail-first, actual React producer, native producer and native app-runtime evidence. Dedicated physical Layout and malformed drop cleanup still remain.

## Current Activation16 Gate

Full React engine census3 is green1,838/1,838 across90files; native UI56 is green645/645. Native48 full renderer is running after the verified source-tail catalogue cleanup and document-page interruption repair, plus the source-coherent missing-window-body pane-chrome repair. Do not activate before its actual result is reviewed.

On green: run canonical activate-puzzle3d-wgpu-dev with the existing ticket-local checkpoint8 build directory, then matching activate-puzzle3d-react-dev. Seal the five known WGPU glue/wasm/artifact/boot/worker files before physical tests and verify their identities afterward. Existing root-owned servers6313/6213 and Storybook6327 answer200.

First run boot,dismiss-tour,command-palette-activate on both renderers with the hardened physical palette close/inside/drag-cancel/outside/query and keyboard postconditions. Then run the full paired journey, including actual General Appearance/Language values, Drivers collapse, five panes, Actions terminal Abort scroll/click,120px divider movement, focus/unfocus/refocus, close-all/new-window/liveWorld gestures, fullscreen and example/role changes. Record actual actions, runtime console and pixel/geometry evidence; do not infer parity from unit tests.

Dedicated Draw/Layout/Note physical adapters are being completed in the ticket canvas-interactions script. Draw selection must use a real selection-consuming command; the app does not publish selection highlight in either renderer. Layout fixture/schema/producer roster is strict and runtime runner currently refuses until its adapter is complete. Note now records visible publication evidence in addition to its action receipts. None of these app journeys has been activated or accepted yet.


The exported mounted-frame transaction source-policy self-test was invoked with the ticket's dedicated 🔬️frame-transaction-policy/📜️script.ts. It executes as a command but returns RED because the dormant-production-authority mutation's source needle no longer matches the canonical UI runtime file. This does not invalidate the 1,231 executed native laws; it is an additional stale-policy-source issue under read-only audit. Earlier Vitest and inline-eval attempts did not execute the self-test and are not test results. Receipt: 🗑️generated/astra-runtime/frame-transaction-policy-6.log.


Canonical WGPU activation16 completed successfully in8m00s, including renderer wasm and Puzzle3D component/materialization. Matching React activation9 is running. No fresh physical16 acceptance yet.

Matching React activation9 completed successfully. All five WGPU artifact hashes are sealed in checkpoint-16-artifacts-before.json. The paired physical palette journey is now running against React6313/WGPU6213.
