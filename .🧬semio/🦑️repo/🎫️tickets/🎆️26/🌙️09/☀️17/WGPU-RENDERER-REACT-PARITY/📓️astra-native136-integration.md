## Native136 Completed

The third attempt executed 1,362 tests in 47.768 seconds: 1,353 passed, nine failed, none skipped. Nx finished in 6m16s. This improves the previous completed census by ten passes and includes the new Actions/Search physical independent-fold regression as an actual failure: clicking Actions publishes both bodies rather than Actions alone.

Remaining failures: Map retirement; Canvas generation replacement publication; Display branch gutter; procedural engine retained projection; General intrinsic labels; Display-created World hit publication; Find fixture mounting; keyboard close journal; independent Actions/Search folds. These are assigned across root and the two execution agents.

Two earlier attempts failed compilation and did not execute tests. Their logs are preserved under generated output. The final build corrected the three-value GPU watchdog progress fixture and typed NodeGraph viewport construction.

UI18 passed all 665 tests; browser-worker passed all 156 tests across 11 files. The new wasm build and physical browser acceptance are still pending.

# Native136 Integration

This checkpoint integrates the authored compositor, the deferred window-close journal, retained Select option remapping, Find publication, TreeItem accessibility labels, and the pre-reconcile scene-retirement handoff. UI16 has passed all 664 tests; the isolated P5d source and hostile-mutation gate passes.

The first Native136 attempt failed compilation before running tests. The new Find fixture used an unqualified `UiComponentSceneNode` and the kernel's `Viewport2d` instead of the UI-owned type. Sol corrected both. The original compiler output is retained in `🗑️generated/astra-runtime/renderer-native136-full/compile-failure.log`; the rerun is pending.

Root also repaired fixture publication and identity assumptions identified by the read-only audits:

- Navbar/projection fixtures now register the World host-to-window mapping alongside their explicit World state.
- The closed-World owner fixture carries the same mapping used by production retirement.
- The Display transfer fixture acknowledges its painted retained document, mirrors registered hosts, addresses the World state by its actual host, and tests its physical ComponentScene hit.
- The isolated Display Tree fixture seals and acknowledges every painted generation before its physical gutter click.

These fixture changes are pending Native136 verification. The retained Graph/Map/Board fixture is being rewritten to paint real component documents, replacing fabricated scene targets rejected by production liveness checks.

Browser parity remains unaccepted. The last browser window-close journey faulted with `InteractionCheckout`. The new bundle has not yet been published or exercised.

## Display Fixture Corrections After Native136

The direct Display gutter fixture used the test-only `apply_tree` adapter and then acknowledged a presenter candidate. Acknowledgement moves the painted tree into the accepted revision; reconstruction requires the retained document baseline. The fixture now publishes the actual Display panel records as a document and drives document reconciliation before each paint and acknowledgement, including after the physical gutter press. Generated pointer IDs now use their real surface-qualified keys.

The Display-created World fixture also expected the generic ComponentScene hit kind. The production retained scene classifier explicitly assigns World3d its dedicated `HitKind::World3d`; the assertions now follow that exact route. Runtime validation is pending a targeted native run.

## Browser Build Completed

The authored-compositor wasm20 build completed successfully in14m44s and published both renderer artifacts. Canonical browser activation is in progress; no browser parity result is claimed yet.
# Native139 and Window Options Oracle

Native137 and Native138 stopped at compilation: the Display disclosure test first used an absent public enum path, then a private engine import. Both were root fixture mistakes; neither was a behavioral RED result. The fixture now uses `ui_wgpu::wgpu::reconcile::UiDocumentReconcileStep`.

Native139 executed 11 selected tests: **4 passed, 7 failed, 1,353 filtered out** (3.189 seconds; Nx 5m21s). The General label/height, close shortcut, independent Actions/Search folds, and Display disclosure tests passed. Existing failures remain in Map retirement, Canvas generation cancellation, Find activation (missing fixture action program), scene hit provenance, and the second refreshed-body leg of Display-created World. The two new Window Options tests failed on the old non-Tree root and missing chip clearance, establishing their intended RED baseline.

The matching mounted React Window Options oracle passed **8/8** tests (313 ms; Nx 12.4s), using the neutral tree fixture and Ajv schema validation. Three prior harness attempts found no tests, used an unsupported fixture URL, or called unavailable query helpers; none counted as feature evidence.

UI21 compiled the clipping packet and started 667 tests, but the task's 15-second fundamental budget terminated the run before a summary. It is not a passing run. Targeted clipping verification follows while the full-run latency is investigated.
