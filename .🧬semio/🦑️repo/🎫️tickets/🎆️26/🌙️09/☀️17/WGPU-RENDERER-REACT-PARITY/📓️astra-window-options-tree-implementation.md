# Window Options Tree Implementation

Native143 also stopped at compilation: the Playbook artifact had a Toggle initializer missing its new appearance field. Root fixed that initializer and scanned all current Rust source constructors with binary-safe search; no remaining field omissions were found. Native144 includes the same integration cases plus the full guest locale/terminology refresh RED law.

React flow RED reproduced `dir=auto` instead of the owning RTL flow. Tree now consumes FlowProvider inline direction; all nine mounted Window Measures/Interpreter tests passed again (12.12 seconds, Nx 13.9 seconds). The existing Tree component regression suite is running separately. The strict TypeScript presentation wire tests passed 2/2 in Sol’s gate.

Acceptance still outstanding: Native143 selected integration, UI23 full retained-engine suite, Settings presented accessibility and constrained tab overflow, guest locale refresh, a fresh WGPU browser build, then the paired 57-step journey, eight docking cases, and fifteen surface families. The prior browser20 final-window-close observation remains valid for its own build only. The parity goal and ticket remain active.

Current integration: Native141 and Native142 stopped at compilation, so neither provides behavioral results. Native141 encountered mutable borrowing errors in the shared plugin runtime; these have been fixed in source. Native142 found one missing argument after carrying inline flow into retained painting. Sol is correcting that call site before Native143.

The schema now carries `TreePresentation` (`standard`, `compact`) through Rust builders, typed ownership, TypeScript wire decoding, and retained nodes. Sol has integrated per-item metrics, nearest-owner scope, mirrored control/hit placement, and layout flow. Root has integrated bounded right-aligned labels and the compact Window Measures projection. The painter census includes measurement-byte progress.

The new mounted React Interpreter law consumes the same neutral expected overlay as the native projection test. It failed at the missing compact presentation attribute, with the other eight tests passing. The React Tree consumer now receives presentation and establishes local row, font, gutter, and value-column variables; a nested standard Tree resets those variables. Checkbox disabled state is forwarded to its native input. The green rerun passed all 9 tests in 11.44 seconds (Nx 14.1 seconds). This mounted DOM test does not replace physical browser geometry acceptance.

The neutral fixture now has a mounted React oracle: 8/8 tests passed. Native139 reproduced both targeted visual defects: the old root was a generic Container and the body began at the chip rather than beneath it. The replacement projects a Tree, an unlabelled section, and recursive TreeItem rows with distinct `.row` keys. Existing control keys and group header-slider bindings are preserved. Group disclosure defaults match React.

The shared binary control contract now has `ToggleAppearance` (`button`, `checkbox`), typed ownership operations, builder selection, TypeScript wire decoding, and role projection. Window Options selects checkbox; the shared React Interpreter renders TreeCheckbox, and WGPU paints a fixed-size outlined check. These changes still require compilation and accepted-pointer validation.

Native140 stopped at compilation because root's new production checkbox helper referenced a test-only imported type. The helper now names the WGPU component type explicitly. Native141 reruns Map/Canvas/Display/Find/provenance repairs and the new Settings accessibility/overflow RED laws. Native140 is not behavioral evidence.

The next implementation step is a neutral `TreePresentation::Compact` resolved per owning Tree, including nested scope boundaries. Physical value-column placement must consume the document's existing inline flow. A document-wide theme switch is inappropriate because the UI engine concurrently owns unrelated trees.

React uses varying compact control heights (`min-h-tiny` rows, `h-small` Select, and a medium checkbox wrapper). The fixture's 20px bound currently measures native control hits, not every outer row; final dimensions must be validated from the actual React DOM before treating one fixed row pitch as parity.

Actual fresh React browser measurement at 1600×1000 now confirms: group-only row 14.390625px, Select row 16px, Checkbox and Slider rows 22.390625px; font size 9.6px. The RTL value column is exactly 104px and physically left of labels. Its checkbox square is 9.59375px at the column's right edge, not centered. Evidence: `🗑️generated/astra-runtime/checkpoint20-iab/react-window-options-metrics.json`. Reload restored the two default windows; the prior empty dock was not a persistent reload state. The compact layout must derive per-control row heights from existing typography and control tokens. A fixed 20px outer row or new arbitrary token would not match React.

UI22 passed the production clipping-piece sequence oracle, but overlap refusal failed because counter overflow bypassed grant faulting. Sol repaired that path; another UI run is required. UI21's full 667-test run hit its 15-second fundamental budget without a summary.

The compact generic Tree now scopes Input and Select height with an inherited variable reset by nested standard Trees. Terra identified that the existing virtual Tree window protocol assumes a uniform standard row pitch; the correct contract treatment for variable compact row heights remains under audit. No virtual compact parity claim is made.

## Native144 and UI23 Integration

Native144 executed 17 targeted laws: 7 passed and 10 failed. Compact projection, exact neutral overlay outline and existing Measures gesture routing passed; content-hug height still fails before the new checkbox geometry assertions. Map/Canvas retirement, World reuse, stale AX epoch, clipped semantic tabs, Chat accessible name, built-in driver localization, full locale refresh and engine provenance remain under repair.

UI23 full long-level suite executed 667 laws in 4.937 seconds: 666 passed, one failed. Both prepared GPU clip laws pass. The only failure is exact heap accounting for UiSurfaceSlot: observed 164224 bytes versus committed 164128 (+96). This is not a passing full gate. React compact/flow 9/9 and Tree regression 32/32 passed before a subsequent label-style scoping correction; reruns are pending.

## Presentation Handoff and GPU Timing Repairs

The content-hug accessor read only the candidate tree, which becomes the prior tree after the presentation ACK swap. It now selects the presented tree when no candidate is ready. A new compact-tree fixture validates the 89.6px content extent at 720px and 1400px viewports both before and after ACK, with Taffy as the independent layout oracle. Measures now re-solves its accepted layout against the compact host rect before first paint. UI24 is running; these source changes are not yet validated.

Prepared GPU advancement now runs inside one timing wrapper, so metadata, exhausted command/mip transitions, and empty/exhausted glass clips cannot bypass timing admission. The 2000us ceiling and four consecutive overruns remain unchanged, including advancing work. New terminal diagnostics identify command kind, draw scalar and before/after progress. Fake-clock laws cover successful transitions and advancing overruns. This repairs the audited streak bug; Browser20’s exact slow command still requires fresh browser evidence.

React final reruns after label-style scoping passed: compact/flow 9/9 and general Tree 32/32.

## UI25 Full Green and Live React Reference

UI25 full long-level suite passed all 669 laws, zero skipped, in 6.609s. This includes the compact Tree intrinsic/presentation-ACK law and its Taffy oracle, the common GPU timing wrapper, both clip laws, and updated exact heap census. UI24’s only failure was test-fixture Stack layout fields missing from the neutral JSON; the fixture now spells every required field explicitly.

The live React tab was measured again through the in-app Browser after the scoped label-style correction. Projection, Sichtbar, Abstand, Sonne and Intensität all still render at 9.6px with 14.4px line height (Chromium measured 14.390625px boxes). Evidence: generated/astra-runtime/checkpoint20-iab/react-compact-final-reference.json. WGPU remains on quarantined wasm20 until the next activation; these React measurements do not count as WGPU browser acceptance.

## Discarded Candidate Height Regression

UI27 reproduced the retained-height audit: A compact document was presented at89.6px, B standard document was laid out at120px then its presentation discarded, and C was published before layout. The accessor incorrectly returned B’s120px against A’s presented tree. UI26 had first stopped at an incomplete test baseline handoff; that harness defect was corrected before the actual UI27 behavior failure.

Candidate and presented intrinsic heights are now separate witnesses. ACK moves each witness with its tree; new document publication clears only the candidate witness. The language-neutral compact fixture records A/B/C and expected extents. UI28 full is checking this repair, prior compact/ACK/reflow behavior, and exact fixed-slot memory accounting.

UI28 executed all670 UI laws:669 passed, one failed (6.322s, zero skipped). The actual discarded-candidate height regression now passes, normal compact ACK/viewports still pass, and the exact heap census remains164224 bytes per slot. The only failure is the in-flight Input accessibility-label wire golden update, assigned to SolLocale. Native145 is now running the full renderer suite against the integrated Compact/ownership/AX/locale packet.
