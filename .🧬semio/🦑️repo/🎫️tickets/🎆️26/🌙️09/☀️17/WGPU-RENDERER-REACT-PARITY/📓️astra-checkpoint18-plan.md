# Checkpoint 18 Readiness

The parity goal remains active. This checkpoint has not been activated or physically accepted.

## Current Native Gates

- Native64: two intended panel reservation/activation failures.
- Native65: activation passed; panel geometry reached a fixture sequencing error (corrected), resize width failed as intended, and real retained World3d ownership failed as intended. Production repairs are now present.
- Native66: compile failure, no tests; six intended chrome API absence errors plus one corrected root fixture type mismatch. 11m50 cold rebuild after generated-output deletion.
- Native67: compile failure, no tests; renderer compiled newer settings callers against the dependency snapshot preceding its new APIs. 4m52. Settings APIs are now complete.
- Native68: full renderer census currently running at `🗑️generated/astra-runtime/native68-full.log`. It includes chrome, palette, resize, window reserve, World3d repair, settings document-lane, new pane-owner regressions and the dedicated Graph/Map/Board routing regression. Pane and three-scene routing production fixes remain held for observed runtime failure.
- FlowNative4: compile failure while shared domain API was being authored.
- FlowNative5: compile failure on two authoritative parser limit constants being private. Their single definitions now have crate visibility. FlowNative6 is required; neither run counts as a test pass.
- Separate OS-services unit still required: `retained_fixed_file_document_preserves_exact_owner_cancellation_supersession_and_atomic_publication`. Its newly added stale post-publication/post-cancellation assertions should expose the retained-generation gap before production repair.
- New actual reference scene-pass law is registered but unrun; it inspects emitted texture/model rather than a manually built matrix.

## Completed Independent Oracles

- React panel resize: all eight anchors through actual Panel, shared schema, pointer drag factors; one selected test passed.
- Chromium example control widths: all five 16px/20px root-font rows passed, including available-width clamping.
- React Window/Panel ownership: two selected tests passed, body/Measures/Actions/Search activation and typing plus panel focus/hide.
- Chromium retained scene pointer/wheel delivery: one selected test passed, eight assertions from the existing neutral retention fixture.
- React/Ajv NodeGraph interaction domain: five tests passed.
- Native preferences neutral/Node filesystem oracle: five tests, 28 assertions, including four bounded writes, prior committed reads, atomic replacement, exact64KiB and plus-one refusal. WGPU TypeScript boundary suite: nine passed.

## Fleet and Next Order

Root owns Cargo serialization, pane and scene hit ownership, activation and fresh paired physical measurements. Sol locale owns chrome and settings; Sol tree owns Flow and the bounded Draw test-fixture repair packet; Terra audits the next procedural-grid rendering packet read-only. Four agents remain the maximum concurrent fleet.

After Native68: fix observed failures, run the separate services stale-generation law, finish owner repairs and re-run relevant/full native gates. Run FlowNative6 with six selected parser/domain laws, then the shared UI domain laws and actual reference scene-pass law. Only coherent verified sources advance to canonical WGPU/React activation and new artifact seals, followed by full main, dock and representative surface journeys.

The entire prior generated-output tree was removed during concurrent workspace activity. Historical Markdown evidence remains, but old checkpoint17 hashes, images and raw logs are unavailable; they must not be presented as live seals or re-read as if still present. New generated outputs are being captured under the recreated ticket directory.
# Native68 Runtime Receipt and Ownership Repair

The full renderer Native68 run compiled and executed 1,257 tests: 1,247 passed, 10 failed, zero skipped, in 31.094 seconds of test runtime. Six newly registered root regressions failed as intended: five pane ownership/focus laws and the retained scene routing law. The remaining failures were three NodeGraph domain fixture/accounting laws and the mounted Command category law. The preference document boundary, navbar clamp, panel reservation, and existing chrome laws were not among the failures.

Root implemented reserved-pane owner resolution, newest-surface keyboard focus with actual retained-node/visibility validation, and exact scene provenance carried from canonical retained registration through atomic host hit publication. Generic retained wheel routing now yields to a live scene hit. These source changes await Native69 and fresh browser verification.

The generated output tree was concurrently removed again after Native68's complete summary was captured. This also interrupted the first services generation run; it is not counted as a test receipt. A new direct-output services run executed the exact stale-generation law and failed at component-unit line 512, “the published generation remains stale for every owner”: zero passed, one failed, 44 outside the selected filter, 0.028 seconds, Nextest run f2a0b9b0-dc26-409c-8579-73776b8a9740. The service repair was released to Sol only after that failure.
# Native69 Compile Receipt

Native69 rebuilt dependencies after concurrent cleanup, then stopped on two unqualified test helper calls in the newly added exact scene capture law. It did not execute tests. Both calls now explicitly address `infinite_world::world::enqueue_world3d_event`; Native70 repeats the full renderer census. No behavior claim is derived from Native69.

The shared overlap/capture fixture passed the actual Chromium oracle: one selected test, two assertions, ten outside filter, 1.151 seconds. The browser delivered down and up only to the topmost captured canvas even though release occurred beyond both canvases.
# Native70 Runtime Receipt

Native70 executed 1,260 renderer tests: 1,258 passed, two failed, zero skipped, in 37.342 seconds. All ten Native68 failures are repaired and passed this run, including the five original pane laws, scene ownership gate, three NodeGraph domain laws, and Command form publication. The additional same-key successor and hidden/closed-panel focus laws also passed.

The new removed-key successor law exposes a real focus identity defect: a replacement key can reuse the old NodeId while the router remains focused. Sol is repairing identity validation after this observed failure. The new exact scene capture law stopped earlier in fixture setup: its paint helper dropped a World3d owner before terminal retirement. Root is repairing the helper before counting any capture failure as runtime evidence.

The full shared UI engine suite is now running, including the two intentionally red procedural grid preparation/encoding laws and the newly extended canonical scene-provenance fixture replay.
# Shared UI Grid RED Receipt

The shared UI engine run executed both new grid laws and they failed at their intended assertions: no prepared PassGrid scalar and no dedicated production grid encoder. Nextest run 10f7544f-76fb-4fd6-a5e4-25770a99688c selected 653 tests, but the command's default fundamental profile killed the broader run at its 15-second ceiling. This is not a complete UI census and no aggregate pass count is claimed. The full rerun must explicitly select the existing long profile. The World producer grid law and actual reference scene-pass law are running next with that explicit profile.
# World Producer and Reference Receipt

The focused World run executed two laws: actual reference scene-pass geometry passed; the procedural grid law failed as intended with 504 finite line vertices instead of zero. Nextest 3f0fe47d-d96d-4bda-b18d-a50b97bd3c7a, 0.439 seconds, 479 outside filter. Sol is now implementing the procedural grid after both native RED receipts and the missing production-WGSL oracle failure.

Native71 is running five focused renderer laws: exact scene capture after the fixture owner repair, three existing focus lifecycle cases, and the new same-key changed-control-kind regression. The kind repair remains held until its native failure is observed.

# Latest Native Receipts

- Native71: aa0cc1da-31cb-4d8f-bcd9-4a110cd921b1, five selected, three pass/two fail, 1256 outside, 247 ms. Actual overlapping World press fanout and same-key changed-control focus are reproduced. Both production repairs are underway.
- Services green-2: 48218461-6823-4fad-a60b-64b8dc16d97e, 45 run, 44 pass/one fail, 426 ms. Retained file generation-floor law passes. Native I/O helper omitted the required outcome checkout before borrowing its job; source fixed.
- Services green-3: 23a251f1-de4e-4b42-8b77-69d73d14762c started 45 then the 30 s quick process budget killed it without per-test attribution. No full passing census. A selected long-profile I/O run follows.
- Draw native5: compile failed at set-active-example String/&str; the shared source was already corrected by another edit when inspected. No tests ran.
- Draw native6: compile failed at six Store mutations whose label methods still returned String after the trait changed to LocalizedLabel. No tests ran. Source consistency is being checked while focused scene repair proceeds.

Native72 did not reach tests: ten Flow artifact mutations referenced absent crate-root LocalizedLabel. Repaired their explicit first-party authority path to the existing os_spr facade. Native72 compile-only failure is distinct from scene/grid runtime results. Services selected long run 9a75ed78-e8d5-482b-b46e-d41743c3b9ec spun in session.close_step waiting for a retained result payload the helper itself still held; root sampled its exact PID then sent SIGTERM after 106.758 s. Sol repaired the test helper to materialize and retire payload observations before owner close. Full services green-5 follows.

Services green-5 completed: 4df2f746-2248-48d3-a42c-64163cfe6bc3, all45 passed/0 skipped,261ms,Nx17.8s. Both generation-floor and test payload close repair are verified. UI Grid green-2 full census bbfa130b-12d7-4c94-980e-bfeeca540123 completed646/644pass/2fail/0skip in1.934s: both actual Grid laws pass. Outstanding failures were old nine-pipeline inventory after Grid became tenth, and two locale value-law files unmounted after first-party locale ownership moved. Root remounted seven existing label/axis laws through WGPU public facade, restored explicit trait imports and corrected fixture-relative axes path; Sol owns exact grid inventory update. Native73 full renderer is running.

Native73 also stopped before tests: the Flow artifact's os_spr facade does not export LocalizedLabel. Root corrected this integration by explicitly reexporting LocalizedLabel from its existing semio_framework_os_kernel dependency in the Flow artifact root and binding its ten label leaves to that verified crate-root API. No compatibility alias or new dependency was added.

UI Grid green-4 completed 8c87ea90-ce66-471f-8976-8be479553412: all653 passed,0skipped,1.672s,Nx26.8s. The seven locale facade laws are restored and the new grid's exact ten-pipeline inventory is verified. UI green-3 had compile-only failure due root's mistaken axes relative-path edit; restored the original correct two-parent path before green-4. Native74 remains compile-only: Plugin has12 LocalizedLabel/String carrier inconsistencies from concurrent schema work. No pointer repair runtime pass is claimed. World grid+reference dedicated run follows.

World dedicated producer+reference green-2 completed7e580f1a-f7eb-4f2d-9704-a5b382eed2b7:2/2 pass,479 outside filter,230ms,Nx37.7s. The actual World pass now satisfies the procedural-grid scalar law and actual textured reference geometry law. Full World regression census follows because the grid producer changed its pass contents.

World full target completed 04b955e0-536c-40c1-b891-9e1deb5ad4dd: 238 selected, 236 passed, two failed, 243 outside the target filter, 791 ms, Nx 3.5 s. Both failures are actual pick/marquee selection granularity: native hardcodes object while the shared fixture and React use scene-owned handle. Sol now owns the scene-default and per-instance-override repair, including granularity-plus-target deduplication.

The expanded Chromium scene owner oracle passed all six pointer capture, motion/wheel, chrome, modal, secondary and cancellation cases. The full Bun file ran 11 tests, 10 passed, one failed, 54 assertions, 1.52 s. The only failure depended on a removed React DEBUG string. Root moved that lifetime evidence to the existing mounted React host test: it must retain its actual session through refreshes and release exactly once on unmount. No DEBUG output is restored as production behavior.

Plugin's twelve label carrier compile errors are source coherent; Renderer Native75 is the first full rerun of the exact scene dispatch and control-kind focus repairs after that integration. Map cancellation/hover and exact World/cache ownership remain open and assigned across the full four-agent fleet.

Native75 and Native76 were compile-only failures: Puzzle3D, then Puzzle2D and Puzzle5D play-snapshot label forwarding still treated LocalizedLabel as text. Root now returns the existing semantic label value directly in all three play projections, preserving its locale matrix. Native77 repeats the full renderer gate.

The strengthened mounted React graph lifetime oracle passed: one selected, 668 outside filter, 106 ms test runtime, Nx 5.6 s. It retained one real browser-runtime session across authored refreshes and called its free method once on unmount. The independent scene topology/capture Bun file passed all ten tests with 53 assertions in 1.55 s. These replace the old DEBUG-string claim with observed host behavior.

Native77 compiled production but stopped on two old HistoryEntry test labels; root converted both to explicit LocalizedLabel matrices. Native78 reached all 1267 tests, then ten scene-paint tests stalled. Native samples of owned PIDs 18876 and 18882 pinpointed root's nested UI_ENGINE lock: register_clipped_retained_hit_targets holds the native WorkerCell mutex and calls retained_scene_target, which locks it again. Root terminated only the ten Native78 test children with that exact binary/parent and is extracting target creation under the existing UI borrow. No passing renderer census is claimed from the stalled run.

The lifecycle Chromium oracle now covers ordinary refresh, changed-node replacement, and sixteen simultaneous real CDP touch captures retired by node removal. All eleven Bun tests passed, 60 assertions, 2.02 s. The new native fixed-grant regression remains unrepaired until its actual RED run.

Native78 finalized after targeted termination: 1257 passed, ten deadlocked scene tests failed by SIGTERM, zero skipped, 127.723 s, Nx 2m51s. The frame single-turn and changed-control-kind focus laws were in the passing set. Root changed target construction to reuse the caller's already-borrowed Ui reference; public callers still acquire one lock. Native79 repeats the full suite and adds the fixed-capacity replacement law.

Native79 completed 29e63c3d-8798-4fb5-9a72-171574797200: 1268 run, 1266 passed, two failed, zero skipped, 29.685 s, Nx 1m46s. All ten previously deadlocked scene laws now pass, including actual topmost World press/outside release, captured motion/wheel, chrome and modal exclusion, secondary context menu, cancellation, ordinary refresh retention, changed-key replacement rejection, and Display transfer. Focus and single-frame-turn laws pass as well.

The intended stale capture grant law failed after sixteen old targets became non-live but kept their slots. Root now prunes non-live owners at bounded admission, retaining the same sixteen-slot limit. The second failure measures World3dState element23536/owner50184 versus the previous committed23464/50040 at fixed capacity256; Sol is verifying the new granularity carrier footprint before updating that exact census. Draw Native7 full regression follows while these source repairs settle.

Draw Native7 did not reach tests: duplicate identical derive attributes on DrawingMutationWorksetPlan produced six conflicting trait implementations. Root removed the duplicate; no behavior or capacity changed. The next renderer run includes the intentionally missing Map cancel/leave APIs and two new actual interleaved-pointer laws. The latter share a neutral sequence with Chromium's simultaneous mouse capture and foreign touch release/cancel; all eleven browser tests passed,64 assertions,1.96s.

Native80 reached the intended test-first compile boundary: seven missing-function errors, five for Map cancel and two for Map hover leave, Nx56.6s. It executed no tests. Sol's production Map repair is now released after this native compile RED, its separately observed TypeScript routing RED, and actual React22/22 cancellation oracle. Draw Native8 is running after the duplicate-derive repair.

Draw Native8 also stopped before tests: its new fixture include had one extra parent traversal, and the new app fixture omitted the PluginApp trait import needed for bind/close methods. Root repaired these exact compile boundaries; Native9 repeats the full Draw suite. Map source is now coherent: pan cancellation settles camera through one host-up publication, marquee cancellation clears only local drag, duplicate cancellation is inert, and hover leave publishes one empty target set before committing its null witness. Root integrated the one-time hover-leave helper for Maps other than the selected retained target; native behavior remains pending.

Draw Native9 completed 4785d48a-c0ff-45b6-9a35-3da38aa82520: 280 tests,252 passed,28 failed,zero skipped,2.136s,Nx26.8s. Sol is repairing the remaining 15 editor/live-envelope, nine retained-mutation, and four structure-diff numeric representation failures without weakening capacity bounds or dropping checks. World granularity is source coherent and its canonical React gate passed75 Node oracle checks plus16 mounted tests,5.42s,Nx7s. Native World verification follows Native81.

Native81 completed 7d8fc53c-b2d4-4f2c-902d-086eedfefe42: 1272 run,1268 passed,four failed,zero skipped,28.080s,Nx1m37s. Both actual Map cancel/hover-leave laws pass; stale capture grant reclamation and World fixed-memory census also pass. Two intentional foreign-pointer laws fail because the single global layer capture loses the first pointer after a second pointer releases or cancels. Root is replacing this with the same sixteen-slot per-pointer authority, including independent position deltas. Two EngineCanvas World laws still expected the superseded object granularity; root derives their expected value from the shared scene fixture and retires the native owner before assertions.

World full granularity run completed 239 selected,237 passed,two failed,243 outside filter,713ms,Nx37.0s. The new bounded granularity carrier and pick/hover/marquee fixture laws pass. The remaining scene bridge expectation still uses render-instance id rather than topology id, and the bounded marquee publication expectation still hardcodes object rather than the authored item granularity. Sol is validating those fixture expectations before the rerun.

The World run id is 68091928-16aa-440a-9a97-b75778340916. Sol corrected both fixture expectations against their retained scene carriers. Draw Native9 source repairs are coherent and its publication audit passed two Bun laws plus Ajv, five hostile cases and four admission cases, Nx1.0s; full native rerun remains queued.

Root's independent Chromium pointer oracle now also records per-pointer client-coordinate deltas across foreign touch release/cancel. All11 tests passed,74 assertions,2.04s. The fixed sixteen-slot Rust capture law consumes the same [290,180] neutral motion delta and verifies a freed slot is reusable. Native82 is running the full renderer suite, including two new actual Map document-publication lifetime laws: ordinary refresh must preserve a gesture, but a new scene key or removal must retire it without synthesizing input.

Flow Native6 stopped before tests after2m25s: the duplicate-widget label test still expected a string although production now returns an English/German LocalizedLabel matrix. Root updated that assertion to the two authored translations; the six focused Flow behavior laws remain pending.

Native82 completed31634cd1-a666-4b69-b73d-4667b0c16c12:1275 run,1274 passed,one failed,zero skipped,29.780s,Nx1m11s. All four Native81 failures are repaired and pass, including the two actual foreign-pointer sequences and both World fixture expectations. The new fixed-slot position law passes. The sole failure is the intended actual Map key-replacement lifetime law: old gesture remains active after generation2 document publication. Ordinary same-identity Map refresh passes. Sol's bounded exact-identity retirement repair is now released after this observed runtime RED.

Draw Native10 stopped at a test-only first-party enum path mismatch,21.1s; Sol changed TypedOperationResultLane to the existing plugin::app export. Flow Native7 executed six focused laws: two parser laws passed,four integration laws failed,246 outside filter,30.048s,Nx1m1s; nextest0fcf4359-aa48-4a7e-94d0-be3ff337e5c3. One moved-layout test drops an unretired ordered-map root; three registered fixtures cannot close while a document reader/owner remains retained. These are not accepted passes.

Root found a broader dispatch gap while auditing cancellation: the actual renderer's exact scene branch only handles World,Graph,Map,Board, while other retained kinds such as Canvas2d fall through without their Shell route. Existing Canvas laws called Shell directly and could not catch this. Two new actual AppInteractionState laws now cover Canvas down/outside-up and a foreign-pointer cancellation between them, sharing the existing neutral Canvas input fixture. Production remains held for actual RED. Terra also confirmed Shell's unconditional cancellation tail can clear unrelated Canvas/catalogue/chrome state; fixed layer capture alone does not establish full multipointer parity.

Draw Native11 completed280run,272passed,eightfailed,zero skipped,2.464s,Nx27.6s. All four canonical structure diffs and most migrated editor/retained-mutation laws now pass. Sol owns the remaining three editor expectations, three retained-mutation cases and two live initializer refusals (mutation-overlay-destination-capacity). World full2 completed e9710256-55e5-4fde-8064-28a96c76e80d:239/239selected passed,243 outside filter,776ms,Nx29.2s. All World granularity and grid regressions selected by that target now pass.

Native83 completed6209827b-6e4d-4d7a-9983-ea6eb68ec387:two selected,one passed,one failed,1279 outside filter,63ms,Nx3m24s. **Correction to the source-level generic-dispatch concern above:** actual AppInteractionState Canvas down/outside-up passes and DEBUG logs show its published ComponentScene hit reaches the existing Shell route. No generic routing rewrite is justified by that suspected fallthrough. The second law reproduces the separate foreign-cancel defect: P78 cancellation publishes P77's terminal Canvas action before P77 releases. Sol now owns exact pointer identity through retained UI/Canvas cancellation; the ordinary route is preserved.

The Draw Native11 run id is22575bb4-1d2b-4b3a-8d69-f305047278f9. Four neutral Canvas camera laws are ready for native RED: cursor-anchored React wheel factors/limits, active primary Pan utility, middle Pan, and secondary document gesture. The mounted React oracle passed allfive selected tests,23 outside,Nx10.6s. Native84 full will verify Map lifetime repair and obtain these actual camera failures.

Native84 completed c6f797f8-917e-4dbd-b451-70217df073d9:1281run,1275passed,sixfailed,zero skipped,35.432s,Nx59.6s. Map same-identity and actual keyed replacement/removal both pass. The expected four Canvas camera laws and foreign cancellation fail; the remaining failure is the exact engine surface heap census after adding Map owner stamps. Sol is measuring that representation change before updating the committed census. Root changed only Canvas wheel to React's ×1.1/×0.9,0.05..32,cursor-preserving camera equation after its observed2.24-versus2.2 RED. Three Pan behavior repairs remain assigned to Sol. The full shared UI suite now verifies the two new retirement identity/backpressure laws.

UI scene-retirement full completed655run,652passed,threefailed,zero skipped,1.767s,Nx30.0s. The two new identity/backpressure tests do not yet reach their assertions because their fixture uses world3d instead of the canonical world-3d SurfaceKind wire value. Sol is correcting the authored fixture. The other failure is UiSurfaceRegistry's exact element census161944versus161912,capacity64/owner520unchanged; the new retirement VecDeque contributes32bytes. Fresh React activation for checkpoint18 is now running through the canonical Nx dependency closure.

Source confirmation for the generic-dispatch correction: Shell::retained_scene_hit_is_live explicitly admits only World3d/NodeGraph/Board2d/TiledMap and returns false for generic kinds. Canvas2d therefore intentionally takes the existing Chrome/Shell retained route. Both the branch and actual Native83 runtime agree; no generic dispatcher repair is needed.

UI full retirement run id:7e4ad441-b94f-4231-b689-52e18ac99db5. Draw Native11's remaining eight repairs are source-coherent; the next full Draw census includes a new late-cancel/stale postcommit owner-return law. Sol is now implementing the three native-RED Canvas pan semantics while the other Sol propagates pointer identity through retained UI and scene cancellation.

A further wheel parity candidate remains unaccepted: AppWheel::accumulate merges stationary wheel events by summed delta, whereas React Canvas uses a fixed factor per event. Two equal zoom-in events and an opposite-sign pair therefore need neutral burst cases and an actual AppWheel→Shell runtime law; the single-event cursor-anchor repair alone cannot prove burst parity. Root retains ownership of that queue integration and requested the corresponding mounted React burst oracle before changing it.

Fresh checkpoint18 React activation passed its complete14-task Nx closure in5m33s,0cachehits. It prepared and activated the single selected Puzzle component, browser support, session and17fonts; no component was excluded. The React listener is starting on6313 through the canonical ServeScript. Draw Native12 full is running next while Map/UI cancellation and Canvas pan source repairs continue. No browser parity result is claimed yet.

Fresh React browser boot completed successfully on6313: booted=true, ready=puzzle3d, boot sequence26, quiet after7seconds, one boot step and zero failures. Artifacts are under generated/astra-runtime/checkpoint18-react-boot. This is a one-sided boot receipt; the paired WGPU acceptance remains pending.

Draw Native12 completed281run,277passed,fourfailed,zero skipped,2.451s,Nx36.7s. Remaining failures are the three editor camera/selection laws and the live initializer stale-ACK exact-owner law. All retained mutation duplicate/cancellation cases, including late cancellation after commit, now pass. Sol is repairing the four observed failures. The mounted React wheel burst oracle also passed allthree selected laws (27 outside,Vitest5.08s,Nx5.8s), establishing two same-sign events apply two factors and an opposite-sign pair yields factor0.99; root is adding actual native queue-to-camera RED coverage.

Draw Native12 run id:a961ece7-67b4-4575-b161-e98933819437. Sol's four remaining repairs are source coherent, full Native13 queued. RendererNative85 full is compiling the integrated per-pointer changes and two actual AppWheel-to-Shell-to-settled-camera burst laws. Fresh React Dock browser probe passed all8cases: left/right/top/bottom split, merge, reorder, Escape cancellation, and orthographic template configuration. WGPU Dock and full paired main journey remain pending.

Replaying the exact Flow Native7 binary through Nx with RUST_BACKTRACE=1 and the canonical128MiB test stack reproduced the move failure (1failed,251outside,0.11s). The stack identifies OrderedMap<WidgetLayout>::drop inside the production retained Recipe::advance, then Preparation::advance and typed publication. This establishes a production preparation-owner defect independent of separate test snapshot/fixture retirement issues. Sol owns the repair. The initial direct replay omitted the standard stack setting and aborted before reaching the useful assertion; it is not a new production finding.

RendererNative85 stopped before tests in3m13s with two test initializer type errors: CanvasCatalogueHover and SceneCameraDispatchFault resets had incorrectly received CanvasGestureSlots::default. Root restored these unrelated optional authorities to None; RendererNative86 full is now running. Production per-pointer source reached compilation without type errors in that gate; runtime correctness remains pending.

Repository access recovered on the resumed goal after a macOS filesystem-access interruption. Fresh reads recovered the final Native86 receipt:5258bb55-e9a9-4ae7-a4a6-3f8ab8415da6,1287run,1281passed,sixfailed,zero skipped,31.862s,Nx1m36s. The actual foreign-pointer Canvas cancellation, independent two-document gestures, Map lifetime laws, fixed-memory census, single-wheel law, and three ordinary pan laws pass. Remaining failures are two direct Interpreter fixtures missing pointer identity, middle-pan cancellation emitting a document action, secondary Canvas press intercepted by context menu, and both new wheel-burst laws. Sol's first four repairs were written before the interruption and await fresh verification. The two burst failures remain root-owned; the unexpected initialzoom1 in the actual retained fixture is separately being checked before using absolute camera assertions.

The wheel source audit found an earlier loss layer in the host EventQueue: it coalesces Scroll deltas and overwrites coordinates/modifiers before dispatch. One original Scroll per ordered bounded dispatch turn is required at both host and renderer layers. Sol owns neutral host transport RED coverage and its bounded repair; root owns replacing AppWheel with direct normalized event routing; Terra audits queue consumers and retained camera initialization. Fresh WGPU activation and paired runtime acceptance remain required.


## Fresh Checkpoint 18 Pair

WGPU activation completed successfully: Nx 20-task closure, 3 cache hits, 10m 6s. Fresh React and WGPU activation receipts both select Puzzle SHA `1294ee62ae6e590f4082f65eddf6b9c271d00f2d1d388fb31b37757d2e131853`. WGPU server is live on 6213; React remains live on 6313. Full paired browser interaction run is in progress. Renderer Native87 is in progress, including the independent first-authored-camera RED law and the four verified source repairs from Native86. No acceptance is inferred from build completion.


## Renderer Native87

Actual full native run: 1288 tests run, 1284 passed, 4 failed, 0 skipped; test duration 63.176s; Nx 2m 43s. The first-authored-camera law fails at 1.1 versus required 2.2, independently confirming initialization. Same-direction burst remains 1.1 versus 2.42; opposite burst publishes zero settled actions versus one. Secondary document drag still returns no Down/Move/Up, so its first repair is insufficient and Sol is tracing the earlier route gate. The two interpreter actor fixtures and local-only pan cancellation no longer fail. Production renderer wheel dispatch is now being moved to its existing one-event runtime dispatch owner; native verification is pending.


## Host, Draw And Flow Verification

Host ordered-scroll full suite is green: run `c7f56e54-2a39-4beb-a4fd-42deca116408`, 83/83 passed, 0 skipped, 242ms, Nx15.5s. Five fail-first laws cover same-point modifiers/deltas, opposite signs, pointer/key ordering, capacity refusal, and pointer withholding across pages.

Draw Native13: run `c8ef9a2c-8498-4035-ac48-92c6ad495ddf`, 281 run/280 pass/1 fail/0 skipped, 7.998s. Camera routing and live stale-ACK repairs pass; remaining opacity-selection test reaches 2 terminal units where its helper assumes 1. Sol is checking the real multi-unit semantics.

Flow Native8: 6 selected/5 pass/1 fail/246 outside, 339ms, Nx3m17s. All three close failures are repaired. Exact binary replay still identifies OrderedMap<WidgetLayout> drop in Recipe::advance. Source audit identified phase22 dropping the original root after begin_set/remove borrowed it to construct a cursor; that root now transfers to retained retirement. Flow9 is pending.

Renderer Native88 full is running with direct wheel dispatch, host generation merge, exact initial camera seeding, and new retained camera lifetime laws. The earlier browser checkpoint18 remains a pre-wheel-fix baseline and is not acceptance for these source changes.


## Shared Compile Interruptions

Renderer Native88 executed no tests: shared plugin source referenced two helpers while they were absent during compilation. Current source restored both helpers. Native89 executed no tests: `artifact_app_apply_ops` called now-private `ArtifactStore::dispatch_apply_exact`. The caller now uses the public `dispatch(ArtifactCommand::Apply { mutations, description: None })` command API already used elsewhere in the plugin. No visibility was widened. Renderer Native90 is the next actual validation attempt; lifecycle tests have not yet produced their RED receipt.


## Checkpoint 18 Browser Baseline Completed

Paired actual browser run, matching Puzzle SHA, 1600×1000/DPR1: 57 steps completed from 08:39:15Z to 09:02:32Z on 2026-09-21. **55/57 measured interaction steps differ; 32 operational failures; 3 physical failures; exit1.** The only matched action/surface steps are example-picker-open and example-switch. This is a failed acceptance baseline. Source wheel/camera fixes made after activation are not included in this browser bundle.

Root visually inspected both settings-open screenshots: React shows the settings panel and GLB; WGPU still presents its full-width World view and no visible GLB at that step. Recorded source audit ties the settings gesture to its correct target and journal, then a retained pending frame with no worker opportunity for tens of seconds. Later recorded frames do show a loaded GLB; that does not satisfy initial responsiveness. Sol owns worker-wakeup liveness; Terra traces mesh latency. Fresh acceptance will follow repairs and reactivation, without increasing probe settle time.

Evidence: generated/astra-runtime/checkpoint-18-main/{steps.json,parity.md,geometry.md,physical.md,cameras.md,latency.md}; root log checkpoint18-main.log. Browser Dock WGPU validation is next against this baseline, paired with the already-green eight React dock laws.
# Native91 and Final Dock Receipt

Flow12 completed3/9 selected passes,6 failures,244 outside filter;144ms/Nx44.2s,run `c02b0301-33a3-4d12-ad06-88d6399d3376`. Real Viewer lifecycle, bare evaluation-session retirement and compiled-window render now pass. The remaining current failure categories and independent SHA audit are recorded in `📓️astra-flow-full10-triage.md`. Flow11 had stopped at four test dereference compile errors and ran no tests; those call sites are fixed in Flow12.

React checkpoint19 activation passed its14-task canonical closure in3m36s,one cache hit. Its selected Puzzle artifact is `18088a78a50847a020ce57fd7fcef53acc74bf241c7e3d403f5d2579908d64d4`, rebuiltAt1789985144112. The owned React listener on6313 is restarting against that activation. WGPU must select the same digest before the paired checkpoint; its prior checkpoint18 bundle is not a matching candidate.

UI full4 passed657/657 tests,zero skips,3.722s/Nx46.2s,run `f2195f26-1e39-4be6-969c-170552b0d7d6`. This includes exact close retirement and a full scene-retirement ledger refusing before mutating the mounted node. React checkpoint19 canonical activation is now running while Sol completes the WGPU presentation owner; WGPU activation and paired runtime acceptance remain pending. The order changed to use the available build lane without compiling partially edited WGPU presentation code.

The complete browser-worker target now passes141/141 in11 files, Vitest3.27s/tests2.00s, Nx11.1s, cache skipped. The callback-separation law uses its own controlled clock; production timing thresholds are unchanged. Receipt and rationale: `📓️astra-frame-channel-clock-fixture.md`. React checkpoint19 canonical activation completed14 tasks in3m36s with one cache hit; selected Puzzle3d SHA is `18088a78a50847a020ce57fd7fcef53acc74bf241c7e3d403f5d2579908d64d4`. Fresh owned React server is listening on6313. WGPU activation19 and matching-hash runtime verification remain pending behind presentation owner integration.

Native97 produced the expected presentation RED and two passes: run `990691ce-f96c-48af-9461-56306fc5c427`,3 selected,2 passed,1 failed,1,294 outside filter; nextest63ms/Nx4m15s. The completed candidate hit routed as `fixture.candidate` while `fixture.presented` was required. The initial closed-window camera repair and the updated engine fixed-slot census pass. Sol is now implementing the presented input owner with actual handler/accept/abort/moved-control coverage. The close repair gained stronger admission, same-id ingress fencing and per-work-turn draining after review; those additions await the next gate. UI full4 runs the full shared engine suite with the new retirement-ledger backpressure test.

Flow Native10 full is RED:253 run,186 passed,67 failed,zero skipped;1.476s/Nx2m29s,run `ef151670-3e78-4138-95a2-e4add8c03ca0`. All seven previously selected repair laws pass, while wider fixture/owner/semantic gaps remain. Initial grouping and the independently corrected Viewer dependency fixture are recorded in `📓️astra-flow-full10-triage.md`; no full Flow acceptance is claimed.

UI full3 passed all656 tests, zero skips, nextest2.445s/Nx29.7s, run `50cf9332-6c1f-4e16-8709-d83416678bf8`. This closes the prior exact-census and retirement-window fixture failures and verifies the component generation changes in the full shared UI engine suite. Flow Native10 now runs the complete artifact suite, extending the earlier seven-law repair verification while both Sol agents prepare their independent lifecycle fixes.

Native96 confirmed the closed-window camera defect in the actual Dock→document-retirement path: `20902770-c79c-4535-8ba5-5931f6b4f067`, one selected law failed, 1,294 outside filter; nextest62ms, Nx4m10s. A camera action still published after the mounted Canvas window closed (one action versus zero required). Sol owns the bounded document/scene retirement repair. UI full3 is running the complete shared engine suite next.

Fresh mounted-frame policy execution remains RED: `🗑️generated/astra-runtime/frame-transaction-policy-current.log` reports nine baseline failures, with no stale marker or mutation-binding failures. The current failures are transaction saturation/expect/while, native preference lock/config-key/loop, chrome clear, interactive-sync clone, and exact take/resume protocol counting. The earlier caller-drive and worker-loop labels are absent from this run. No baseline rule was suppressed; Terra is auditing a separate bounded decode scheduling owner so frame progress does not regress while the decode loop is repaired.

Native95 completed `bc8503d0-1e7a-4106-b9de-3e1bbb5c6971`: 1,294 run, 1,293 passed, one failed, zero skipped; nextest 31.305s, Nx 3m54s. The new same-key remount, camera deadline, incomplete text stream and normalized Dock laws all pass. The only failure is the exact `EngineSurfaceRegistry` element census: measured 75,824 versus committed 75,816 bytes, with capacity256/owner16 unchanged. `EngineSurface.map_interaction_owner: Option<ScenePointerTarget>` now carries the added 64-bit component generation, explaining the eight-byte element increase; the committed census is updated to the measured shape. Native96 is running the independent actual-window-close deadline law before any production close repair.

The input publication audit identifies another concrete lifecycle gap: `dumpChrome` serializes the same published `InputState.hits()` used by pointer dispatch, while the corresponding GPU frame has not necessarily been acknowledged. The checkpoint18 invisible Settings controls were therefore live input authorities over old pixels. Sol owns a fail-first candidate/presented/abort law and a presentation-bound registry repair; see `📓️astra-sol-checkpoint18-input-presentation-generation.md`. This remains unverified in a fresh browser.

Draw16 passed the full 282-law suite with zero skips (nextest 2.344s, Nx 30.2s), closing both Draw14 initializer failures after an actual fail-first deterministic deadline law. The retained bootstrap now yields without discarding its partially allocated owner. Detailed schema, independent Scheduler oracle, RED and GREEN receipts are in `📓️astra-draw-bootstrap-yield.md`.

Native93 confirmed the real remount gap: Canvas → Container → Canvas under the same node key kept the original `ScenePointerTarget` identity. The corrected test failed 0/1 (1,291 outside filter), nextest 44ms, Nx 2m28s. Sol is adding mount identity that survives ordinary same-key refreshes but changes on actual component replacement/remount. Native94 then ran the normalized docking and incomplete-text-stream laws: docking passed; the text law failed after reserving an empty stream, because the old wake predicate treated external ingress as runnable work. Result 1/2 passed, 1,291 outside, nextest 34ms, Nx 44.7s. Sol has implemented the narrower text predicate; native GREEN and refreshed browser acceptance remain pending.

UI full2 completed 656 laws: 654 passed, two failed, zero skipped; nextest 1.600s, Nx 28.0s. Its exact fixed-slot census measured 161,960 bytes per `UiSurfaceSlot`, owner still 520 bytes and 64 slots. The remaining 16-byte increase follows the new pointer-id-qualified `CaptureState` tuple and enclosing alignment; the committed exact element size now matches that measured shape, without changing capacity or bounded-stack limits. The second failure was the retirement test helper reconciling its initial document under `procedural-main` while the test expected `scene-window`; that helper now accepts the explicit window identity. Both await the next UI run.

The corrected same-key successor probe now removes Canvas to a Container and then mounts Canvas under the same node key. Native93 is running this case before any further identity change: a component remount must not inherit stale capture/deadline ownership, while an ordinary same-key authored refresh must preserve the camera. Sol is auditing whether retained NodeId and window generation are sufficient for that lifecycle.

Native92 finished: 1,291 run, 1,290 passed, one failed, zero skipped; nextest 32.138s, Nx 4m33s, run `9216b1b0-bc79-4454-a95b-9c93fe6cfdf6`. Both Canvas retirement laws now pass through expiry and close, preserve the replacement camera, and reject delayed old retirement. Secondary Canvas document gestures and the complete wheel fixture pass. The only failure is a newly added same-key successor test whose fixture merely refreshed the same retained component, so the exact identity correctly remained unchanged. Sol is changing that fixture to remove and remount the component before asserting a successor identity. Production mount identity semantics will remain unchanged. The complete UI engine suite is now running with all-scene hit provenance and the latest ownership census.

Next browser checkpoint: rebuild the canonical WGPU and React Puzzle3d development activations and verify matching selected artifact hashes. First run the existing interaction probe with `SEMIO_PROBE_ONLY=boot,dismiss-tour,settings-open,settings-app,settings-general,settings-drivers-open,settings-drivers-close,settings-close`, retaining the existing 240-second boot limit and 2,400ms step settle. This shorter checkpoint tests the repaired Worker wake boundary and actual panel paint before spending another full journey on a frozen renderer. Acceptance still requires the complete journey, dock suite and all surface families after these narrower gates pass. Existing generated output is retained until the ticket is genuinely complete.

Draw14 completed 281 tests: 279 passed, two failed, zero skipped; nextest 16.691s, Nx 3m00s, run `60b7dbdb-5bbf-403e-94a8-728f5e0a4e9e`. The opacity law fixed after Draw13 now passes. Two full-envelope initializer laws fail with `drawing-store.mutation-arena-bootstrap-fault`, surfaced as `plugin.internal.document-archive-replacement.initializer-failed`; Terra is tracing the exact owner failure. These tests remain required. Native92 is running the complete renderer suite with Canvas retirement, secondary hit provenance and Worker tick output changes.

Flow9 passed all seven selected laws (246 outside filter), including MoveWidgets publication and cancellation with both layout-map roots live; nextest 81ms, Nx 2m32s, run `93f491ce-d5eb-483a-8317-6ede926bf5c5`. Draw14 is now running the complete Draw suite. Canvas retirement tests additionally exercise closing the checked-out deadline cursor and a delayed old retirement after the successor has mounted; the live successor must still publish its 3.3 zoom once. These additions await the next native renderer run.

Native91 completed: 1,290 tests run, 1,286 passed, four failed, zero skipped; nextest 33.790s, Nx 4m39s. The actual normalized-dispatch first wheel, same-burst wheel, opposite-wheel and mounted camera identity laws passed. Two new retained camera retirement laws failed with one publication where zero was required. Secondary Canvas document gesture still failed: the exact live retained owner existed but the optional bespoke scene cache was absent (`scene=false owner=true current=true`). Sol is fixing the generic Canvas route. The wheel FIFO oracle compared all samples successfully, then failed its final queue teardown assertion because drain leaves allocated storage; the test now explicitly runs the queue's bounded close steps before asserting terminal-empty.

The WGPU dock journey completed with eight failures: all four splits, merge, reorder, Escape cancellation and template configuration. React's corresponding eight cases passed. WGPU never promoted the tested tab drags and exposed no template tree during the recorded deadline. This is a failed runtime baseline, not acceptance. Receipts remain under `🗑️generated/astra-runtime/checkpoint-18-dock`.

Root changed Canvas deadlines to retain exact mount identity and retire pending publications when that mount is removed. Checked-out old deadlines cannot restore or publish against a replacement, and a newer deadline keeps ownership. These production changes await Native92. Flow9 is running seven focused laws, including cancellation immediately after layout cursor creation. Worker liveness implementation remains in progress; rebuilt browser acceptance is still required.
