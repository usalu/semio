# Fresh React Hub Workspace Reference

CUA inspected freshly completed React activation2 at `http://127.0.0.1:6313/?plugin=puzzle3d`. The booted footer exposes ambient `Hub connection: signed out` plus the distinct button `framework.hub.signIn`. Clicking the button opens an in-page `Sign in to a hub` workspace and preserves the app URL.

The mounted workspace contains Cancel sign-in; Hub selector defaulting to This device — current origin; Add another hub with Hub address and initially disabled Add hub; Email, Password and initially disabled Sign in; a local-only connection notice. The Spaces section contains Refresh spaces, Find a space, Members/People here, and Invitations with invitation link/code and initially disabled Join space.

The local development host returned `The hub did not accept that. Nothing was changed.` and no spaces. This observation establishes the real mounted surface and local failure state only; no credentials were entered, no authentication or membership mutation was attempted.
