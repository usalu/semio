# Sequential Window Lifetime

The existing close path retires the document and component scene, but source inspection shows `UiSurfaceRegistry::try_admit` can only reuse `None` slots and no close path removes its `UiSurfaceSlot`. Sixty-four distinct lifetime window identities can therefore exhaust the retained registry even when only one window is open at a time. The source finding requires an actual runtime law before any production repair.

The closed Canvas camera fixture and schema now include `sequentialWindowLifetime`: 65 distinct mounts, at most one live window, zero live windows after each close, and zero delayed camera actions after retirement. The new native Shell law mounts a real document, closes its Dock window, sends it through `retire_documents_outside` and the bounded `close_ui_document_one` lane, and then mounts the next distinct window. No registry internals are cleared by the fixture. Native execution is pending the presentation owner's source-coherent checkpoint.

The independent React oracle mounts the real `Canvas2dHost` through React/testing utilities and the existing canvas session seam, arms a camera deadline, unmounts each distinct host, and checks DOM lifetime and late actions. Its first run found a fixture API error: the local `fireEvent` wrapper has no `wheel` helper (32 existing tests passed, one new test failed). The fixture now dispatches the same native `WheelEvent` used by the existing wheel laws. The full file is running again; no production lifecycle change was made.

Generated receipts live in `🗑️generated/astra-runtime/canvas-sequential-window-react`. A read-only audit is tracing every layout, paint, event-router, scene and token owner required for bounded full-window retirement. A blind removal of the registry slot would risk dropping live jobs and reusing stale identity and is not the proposed repair.

The corrected full mounted React input suite passed33/33, zero failures: Vitest10.53s, tests405ms, Nx12.0s, cache skipped (`run-2.log`). This includes the 65-window lifecycle and Ajv schema validation. Native execution remains pending.

Native99 reproduced the exact capacity defect: windows0–63 mounted after their predecessors closed, and window64 was refused with `StaleGeneration` because the retained registry had no vacant slot. This is an actual native fail-first result (1.651s for this law). The full run had1304 tests,1272 passes,32 failures, zero skips;39.508s test time and Nx4m13s. Several other failures concern the separately reviewed presentation boundary and test fixture assumptions.

The repair now latches an exact `UiSurfaceToken` before Dock removal and drives the existing interpreter close queue through silent event-router retirement, rejected/active layout jobs, previews, paint cursors and draw lists, hit registrations, document/reconcile data, remaining tree storage, scene retirement handoffs, and scheduler entries. Only terminal owners release their registry slot. Document accessibility epochs now come from a checked monotonic `Ui` counter, so a same-id successor cannot reproduce the retired window's scene/accessibility identity. Ordinary document refresh keeps the current mounted epoch.

Internal node registries use ordered maps for one-entry removal, and retained drag payloads drain incrementally. No application action, blur commit, drop cancellation, or synthetic pointer release is generated by unmount. Direct admission, event dispatch, frame work, and clock callbacks refuse a closing surface. The close watchdog in Shell fixtures now uses the same262144-opportunity ceiling as retained layout/paint probes because it includes those owners; its previous document-only ceiling omitted them. Added UI laws cover same-id stale tokens/epochs,65 queued-layout lifetimes, and silent focused blur-input unmount. Syntax parsing passed for eight touched Rust files; native verification is pending Native100 and the next UI suite. No browser acceptance is claimed.

Native100 executed10 focused laws:8 passed,2 failed,1294 outside the filter;225ms test time, Nx3m8s, run6a0ea30b-a179-4e2c-b997-9d427649451f. The four close laws all passed:65 sequential windows, pending camera retirement on close, same-id reopening waits for the exact old close, and bounded close queue admission. The failures belong to the separate presented-input work: a stale fixture law count and a real moved-button binding mismatch. The new three UI-specific retirement laws still await the full UI run. The global duplicate pending-command queue is also under separate ownership review; its retained clones are not claimed resolved by this per-window close ladder.

The generic retained-window cases now have their own closed schema and shared `🪟️surface-lifetime` fixture in the UI module. A mounted React `UiNodeView` oracle repeatedly focuses and edits a real blur-commit input, unmounts it, then sends blur to the detached predecessor after mounting the same-id successor. It checks zero old actions and an intact successor across65 mounts. This complements the existing Canvas deadline oracle and is currently running with Ajv validation; no result is claimed yet.

The real React interpreter oracle and Ajv contract validation both passed:2 selected tests,116 outside the filter,223ms test time, Vitest4.58s, Nx5.3s, cache skipped. Receipt: `🗑️generated/astra-runtime/react-surface-lifetime/run.log`. The full native UI suite is now running to verify the new same-id, silent-unmount, and scheduler-capacity laws with all retained widget regressions.

UI5 executed660 tests:657 passed,3 failed. Two new fixture helpers incorrectly assumed the accessibility corpus contained no scene; it does contain a real scene. The helper now enumerates the actual mounted scene owners before close and verifies each exact handoff, its epoch, its uniqueness, and that its registry slot remains present until all handoffs have occurred. The third failure measured the new retained slot at162000 bytes rather than161960; the exact committed census was updated, retaining capacity64 and owner520 bytes. The delta is40 bytes per slot for the retirement state and router storage.

UI6 then passed the full660-test suite with zero skips in1.625s, Nx18.8s, run3ba0e9d7-f237-4021-90fc-7d9e4deb7d98. Receipts: `🗑️generated/astra-runtime/ui-native5-full/run.log` and `ui-native6-full/run.log`.

Terra identified a separate interpreter close race: queued generic scene intents check the still-retained tree/revision during Router→Document retirement but do not check the closing window. Two new native laws now exercise an interleaved scene pump after close admission and require the single close owner to retire queued scene/capture owners before slot removal. They are queued in Native101 as fail-first checks; no production race repair or browser acceptance is claimed yet.

## Ui6 And Native101

Ui6 full suite: 660/660 pass, zero skipped, 1.625s tests, Nx18.8s (3ba0e9d7-f237-4021-90fc-7d9e4deb7d98). Native101 reproduces two remaining host close races: queued input can activate while the Ui tree retires, and queued scene/capture ownership survives slot release. New Native102 fail-first checks cover silent active Canvas gesture closure and 257 sequential working Canvas camera mounts, exceeding both the 64 Ui slots and 256 scene-state slots. React runs the same 257-mount neutral fixture. These checks are running, with no result claimed yet.

## Native102 And React Canvas Closure

Native102 ran both new laws and both failed: active Canvas close emitted a synthetic pointer terminal action; mount 257 (index256) faulted in scenes-host because retained scene-state item credits never returned. Nx nextest reporting truncated the second failure after 64KiB of debug output; a second execution of the exact already compiled test through bun Nx exec recovered the complete failure in `renderer-native102-scene-lifetime-red/capacity-receipt.log`. This is a real scene-host admission failure, not a guessed capacity assertion. React mounted oracle: 33/33 passed with257 mounts (11.1sNx), then34/34 including silent active-gesture unmount (12.8sNx).

Root implementation now fences scene queue admission/dispatch, drains exact queued scene and pointer capture owners before Ui retirement, and silently discards Canvas gesture/hover closure. New generic component mount identities and an incremental SceneSurfaceRetirement cursor return a256-slot scene-state reservation only after nested text, JSON, maps, sets, vectors, and exact identities drain. Close and replacement-paint lanes wait for that acknowledgement. These new Rust changes are not yet compiled. EngineSurface CPU/GPU per-component close, TextEditor UI/focus, raster, and Ink clipboard owners remain open work documented by Terra; no full window-lifetime acceptance is claimed.

## Sibling Component Identity And First Integration Compiles

Native103 and104 never executed tests:103 rejected UiDocumentTree Clone because UiNodeTable is not Clone;104 rejected the attempted UiNodeTable Clone because UiNodeRecord deliberately exposes only fallible credited_clone. Sol is preserving those owner credits in the new presented revision copy. Root source parsed; there is no Rust runtime acceptance of the new scene retirement yet.

Terra confirmed that multiple Component::Surface nodes are legal under the same document surface_id, which remains the wire action address. Root’s initial generic scene mount stamp incorrectly assumes one component per document and is not accepted; it needs a separate composite component host identity across retained state/engine/raster/editor registries. Native sibling fail-first fixture is added and awaits a compile. React neutral sibling-camera oracle passed in full35/35,440ms tests,Nx7.0s; sibling cameras remain independent and removing one preserves the other. Log `🗑️generated/astra-runtime/react-canvas-sibling-lifetime/run.log`.


## Native107 Runtime Receipt

32 selected renderer tests ran: 25 passed, 7 failed, 1286 outside the filter; 2.190 seconds, Nx 1m35s. The 257 sequential window lifecycle law, silent active Canvas close, queued scene/capture close fencing, nested scene retirement, and closing Ink-editor commit fence passed. Three clipboard lifetime laws failed as expected before their repair. One presented-keyboard authority law remains red. Two Canvas replacement/deadline laws expose remaining host identity/presentation ownership gaps. The sibling Canvas fixture failed `Invalid(MultipleParents)` before mounting, so this run does not yet assess sibling host independence. Raw receipt: `🗑️generated/astra-runtime/renderer-native107-host-and-retirement/run.log`.

Native106 was compile-only (4 integration errors: missing admitted-map mutable iteration and two Ink test maps using the old container type). Those errors were corrected before Native107. No browser acceptance or new WGPU activation has been performed.
