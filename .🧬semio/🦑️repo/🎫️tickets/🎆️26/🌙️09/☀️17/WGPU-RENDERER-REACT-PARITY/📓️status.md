# 🧊️ Wgpu renderer React parity — status

## 2026-09-17

- Ticket opened on disk (repo MCP failed to connect). Prior 26/07/11/WGPU-RENDERER-FULL-PARITY is superseded (old `framework/renderer/wgpu` paths gone).
- Surface census: ui `🎯️targets/🧊️wgpu` 33 files / 27.6k lines; os renderer `🎯️targets/🧊️wgpu` ~15k rs + ~13k ts; element wgpu targets 30.9k (Shell 16.3k, EngineCanvas 4.9k, Scenes 4.4k, Interpreter 2.4k, Dock 1.7k, ProgramBridge 1.1k). React side: ShellHost 13.8k, ShellHelpers 6.3k, Shell 1.4k, ChromePanels 1.4k, Interpreter 3.7k, ui ⚛️react target 11.5k.
- Wave 0 (audits, Sonnet, read-only) started: shell/window system, interpreter/element coverage, scenes/engine canvas, runtime/boot/input, build/serve/theme health. Reports land as `📓️audit-*.md` here.
- Native `cargo check -p semio-framework-os-renderer-wgpu --lib --keep-going` (🗑️generated/native-check.txt): 11 errors / 22 warnings. Two families: (A) `semio_framework::ViewModel` gained `tree_windows` + `tree_viewport_rows` (peer ticket 26/09/16/ARTIFACT-TREE-VIRTUALISED-STREAMING) — six wgpu Shell initialisers stale; (B) `JobReplayRoute`/`MountedReplayRouteSeed` lost `document` — three renderer sites stale. Opus packet P0 (📓️p0-compile-restore.md) assigned: port to current contracts, then native + wasm32-unknown-unknown checks.
- P0 done (📓️p0-compile-restore.md): native + wasm32 checks green (0 errors).
- Audits landed: 📓️audit-runtime-boot-input.md, 📓️audit-shell-window-system.md, 📓️audit-build-serve-theme.md, 📓️audit-scenes-engine-canvas.md (merging sub-lanes); 📓️audit-interpreter-elements.md pending.
- Root cause found for every "empty scene" family: 2026-09-08 zero-warnings sweep (auto-commit de617a7c17) moved 2 800 + 675 lines of scene renderers into `#[cfg(test)]` 🧊️wgpu-standalone include files after the phased `render_component_scene_step` dropped the old `match SurfaceKind` dispatch. Table/Canvas2d/Paint2d/InkCanvas/DiffView/BlockList/EventFeed/GraphTimeline/IconRender/VFS/TextEditor paint a blank panel in production.
- Shell P0 root causes (audit): physical-vs-logical pixel mismatch (all chrome constants CSS px, screen/pointer physical → wrong on Retina); 8-anchor dock collapsed to left/right; command dock panel test-only; flex layout engine test-only.
- Wave 1 (Opus) dispatched: W1a list-like scene renderers, W1b canvas/text/icon scene renderers, W1c shell shortcuts + palette, W1d boot-axis parity, W1e Space Administration on wasm, W1f World3d/TiledMap wire + terrain fetch, W1g DPI logical units, W1h 8-anchor panels + command dock, W1i window/dock semantics, W1j overlays (approvals, transient notice, agent presence/chat), W1k theme tokens. Reports land as 📓️w1?-*.md.
- 📓️audit-interpreter-elements.md landed (two schemas: ui_contract::Component vs private wgpu UiNode; flex engine test-only; ActionDescriptor instead of UiIntent; Image placeholder-only; CAD spatial-tree editor has no wgpu target). Wave 1 extended: W1l production flex/grid/scroll/overlay layout + SpaceToken ramp, W1m UiIntent dispatch + Input constraints + reconcile latent bugs, W1n Image/Popover/Dialog/tooltips/skeletons, W1o widget metrics/Select keyboard/locale labels.
- Deferred to wave 2: CAD spatial-tree editor wgpu target (plugin-scoped), mounted-layout tree paging (peer ticket ARTIFACT-TREE-VIRTUALISED-STREAMING owns the tree), panel content-projection escape hatch, wgpu test Nx visibility, wasm warning sweep, live boot verification of every playground.
- W1d done (📓️w1d-boot-axis-parity.md): ONE boot-axis vocabulary across the four doors. New `🎯️targets/🧊️wgpu/🧭️boot-descriptor/🟦️.ts` + its Rust twin `WgpuBootDescriptor` (four per-axis wasm hooks collapsed into `semioWgpuSetBootDescriptor`); trunk page reads `?plugin=&app=&role=&mode=&example=&hub=&user=&dataDir=` over `<meta semio-*>` seeds the serve injects, plus the one-shot `#semio-broker=` proof (read + stripped, React's own rule); `FrameworkOsWgpuBootOptions` gained appId/appRole/mode/example/locks/defaults/brand/hub; native gained `--app --role --mode --example --brand --hub --user --data-dir`. Shell honours appId (boot anchor pin), locks (browser was unlockable — `env_lock` now reads the descriptor) and hides the example picker when locked. Standing law `🧪️tests/🧭️boot-axis-parity/🦀️.rs` (3 tests, commented allowlist). launch.json unchanged (all flags additive). Open: brand is an id only (no wgpu brand registry), hub axis still has no wasm reader, `?mode=` has no React reader.
- W1a done (📓️w1a-list-scene-renderers.md): the six LIST-like component scenes (Table, VirtualFileSystem, GraphTimeline, BlockList, DiffView, EventFeed) paint and dispatch in production again. 27 items moved out of the `#[cfg(test)] include!` quarantine into the production Scenes target (`render_vfs` restored from 860e015bf6, deleted outright in the 09-08 sweep); 20 `#[cfg(test)]` wire-struct attributes dropped; dispatched from `render_component_scene_step`'s kind switch via a new `render_list_scene_step` (one bounded `reserve_list_rows` grant sized from the visible band — every renderer culls to it, so rows can never exceed one reservation). Second defect found and worked around: a `ComponentScene` leaf mints exactly ONE retained hit target (`retained_scene_hit`) and the chrome walk stages it AFTER the scene paint stages its rows, so every row hit is shadowed — row/header/button resolution therefore re-derives the paint's own geometry in `passive_scene_pointer_button` (`scene_list_hit`, `block_list_plan` shared by paint+hit, `write_scene_action` through the bounded queue) and hover moved to `SceneSurfaceState::hovered_control_id`. Eight React drifts fixed (EventFeed `{surfaceId,id}` not `{entryId}`; Table `interactionSelect` branch; Table ordinal row-id fallback; `placement:"menu"` buttons; DiffView split-row pairing; VFS double-click geometry + visible-row indexing; hover). 18 pointer/payload tests added across the five existing test modules incl. a BlockList paint-vs-hit drift law; repaired the stale `production_action_ingress_…` guard (asserted on the production source for functions that live in the include since 09-08). VERIFY: this packet's code compiles clean (round A: 2 own errors, both a `#[cfg(test)]`-narrowed `IconName` import, fixed; round B: 16 errors left, ALL W1b's — `canvas_lum`/`canvas_set_lum`/`canvas_sat`/`canvas_set_sat` deleted while `canvas_blend_channel` still calls them, and `InkDocumentJson`'s four `grid_*` fields gone while the ink grid branch still reads them). wasm32 + `cargo test --lib` NOT run: both need the crate to compile. Build-dir deadlocked twice (`prebuild_lock_exclusive`/`flock`, 18 idle cargos) and 4 of 7 runs were SIGKILLed by peer sweeps.

## 2026-09-18

- Wave 1 reports landed: w1a (six list-like scene kinds restored + `render_vfs` recovered from 860e015bf6; ComponentScene hit-target shadowing found and worked around), w1c (SHELL_SHORTCUT_ROWS transcription of React SHELL_KEYBINDINGS, palette executes every command, rankFuzzyItems port), w1d (one WgpuBootDescriptor across trunk/library/native doors + boot-axis law), w1e (directory_door transport, Space Administration/identity/check-in on wasm32, two SystemTime panics fixed), w1f (terrain tiles through the bounded asset pipeline + per-vertex colour ramp; setCamera and map interactionSelect/Hover payloads now React's; map context menu), w1g (logical px everywhere; scale_factor only at swapchain/scissor/atlas; matchMedia density watcher), w1h/w1i (reports present, workers still verifying), w1j (agent approvals modal, transient notice, agent bridge/presence/chat header, tooltip 400 ms), w1k (token codegen extended, 49→25 literals, error/success/gizmo/scrim/icon bugs fixed, parity law), w1m (UiIntent dispatch with staleness/seq, stepper gating, input constraints), w1n (Image decode/draw, Popover/Dialog placement port, tooltips, skeletons), w1o (line-height/icon tokens, Select keyboard/typeahead/flip, slider/toggle/stepper keys, focus ring).
- Machine pressure: load ~35, swap 4.3/5.1 GB, several cargo runs SIGKILLed; shared build-dir flock deadlock broken twice by workers. Integration must run alone.
- New P0s from w1f: `apply_map_tile_bytes` and `apply_ui_image_bytes` have no callers (map tiles + UI images never fetched, same shape as the terrain bug).
- w1l landed (flex engine production, Grid/Scroll/Overlay/Absolute, SpaceToken::px ramp, 504/6 tests with the 6 peer-owned). w1b/w1h/w1i still verifying.
- Wave 2 plan (after one integrator pass gets ui + os-renderer green natively, wasm32 and `--tests`):
  - W2a scene remainder: dead `apply_map_tile_bytes`/`apply_ui_image_bytes` pipelines (P0), relocate drag out of cfg(test), Board2d keyboard + domain_id, InkCanvas snapshotJson/documentJson drift, Paint2d marquee/navigator/lanes, VFS SurfaceKind kebab tag, shader-contract WORLD3D_SHADER twin.
  - W2b surface context menus for every scene kind (`scene.menu` never read) + TextEditor completions/rename/context popups in production.
  - W2c panel content assembler: Settings branch (general/theme/keybindings/default-apps/conflicts), tool leaves, marketplace, content-hug height, drag drop preview, mobile panel flattening, `expanded_command_id` reader.
  - W2d window remainder: mobile flat stack, drag-lane hoist collapse, click-to-deactivate, measures fold chip, close tears down spawned app.
  - W2e browser directory WebSocket door + document sync + Space Administration pane + agent bridge transport + chat transcript.
  - W2f plugin editors with no wgpu target (CAD spatial tree + census).
  - W2g dispatch remainder: 🖱️dispatch placement math, mono theme keys, bold/focus-visible/bg-muted, icon re-raster on density change, UiIntent kernel seam in production, ActionId.scope controller identity.
  - W2h wgpu tests Nx visibility, wasm warning sweep, empty test stub dirs.
  - W2i live boot: activate puzzle3d wgpu dev, boot on 6113, compare with React 6013, fix boot faults, then every playground.
- All 15 Wave 1 packets reported. Read-only diff review (📓️w1-diff-review.md, six sub-reviews): claims structurally supported for 13 packets; w1k's `iconChevronUiSpacing`/`ICON_CHEVRON` claimed but absent (P1, handed to the integrator); w1n's Image decode pipeline has no production caller (`admit_ui_image`/`take_ui_image_upload` test-only) → P0 folded into W2a with the map-tile/ui-image asset lanes; systemic `//`-inside-definitions hygiene debt across packets → W2 hygiene sweep. Integrator (📓️w1-integration.md) running alone.
- 📓️audit-plugin-react-only-surfaces.md: CAD spatial editor (6.8k lines, little Rust engine) is the largest React-only surface; animate deck editor (engine already Rust), puzzle 5D viz target, presentation product viewer (scope question), keyboard-shortcut registry/persistence (~471 lines) and RTL have no wgpu twin; all 15 SurfaceKinds now dispatch after Wave 1.
- 04:00 usage limit reset; integrator resumed via SendMessage at gate 6 (gates 1–4 compile clean per 🗑️generated/w1int-*; ui suite 504/6, os-renderer test filters still red). Wave 2 dispatched while the machine is idle: W2a scene remainder + dead asset lanes (map tiles, UI images, ui Image decode), W2b surface context menus + TextEditor popups + overlay content, W2c panel content assembler (settings/keybinding registry/tool/marketplace/mobile/chat transcript/content-hug), W2d window remainder (mobile flat stack, drag-lane hoist, deactivate, fold chip, teardown), W2f CAD spatial editor wgpu target (XL). Queued: W2j animate deck paint layer, W2k puzzle5d viz + RTL, W2g dispatch remainder + hygiene sweep, W2h test visibility/warnings, W2i live boot verification.
- 📓️w1-integration.md: gates 1–4 (ui wgpu-engine check, os-renderer native, native-bin, wasm32) 0 errors; gate 5 ui tests 506/5 (all 5 pre-existing resident-reservation pricing family); gate 6 os-renderer tests 752/32 (20 pre-existing/other-lane, ~12 in files W2 workers are rewriting); gate 7 infinite 157/7 (outside Wave 1 diff); TS lint/boot/worker bundles regenerated and green, TS unit 79/2 pre-existing. Fixes: Board2d golden, world3d pointer law, async-boundary laws, dock hit registry, cfg(test) import used by production, WORLD3D_SHADER twin across four backends, chord laws re-pointed, ICON_CHEVRON token + tree chevron, silhouette dead call removed, one [DEBUG] demoted. Beware corrupt incremental cache ICEs (cargo clean -p fixed) and mid-write snapshots: re-run a red check once.
- Recommended own tickets: resident-reservation pricing bug (largest test-noise source), drop-witness→SIGABRT hiding renderer tests, retained_raster_contract dead markers, per-frame world3d [DEBUG] dump.
- 05:05 W2i live verification started from the main session: `bun nx run @semio-tech/framework-os-dev:dev -- puzzle3d` (SEMIO_RENDERER=wgpu, S_OS_PORT=6113; logs 🗑️generated/w2i-puzzle3d-wgpu-dev*.txt). A 6.6 h-old vite serve from an earlier activation already listens on 6113 and reloads on the activation receipt. BEFORE screenshot (stale build): two empty window frames, no navbar content, footer chips only — the complaint reproduced. React reference (6013, peer serve, ?plugin=puzzle3d): navbar [Artifact][Catalogue] · app switcher · example "Concrete Forest" · Editor/Viewer · [Inspection][Tool runs][Chat] · Fullscreen; windows "Top" (2D plan) and "Perspective" (3D) each with Actions/Search/Window Options chips and Utilities/Projection chips; footer Display · Remote · Tool · Command · Settings · Marketplace · History; welcome tour 1/9 on first boot.
- Activation #1 failed in `framework-renderer-wgpu:wasm` on a transient W2k edit (⚙️engine `windows.get(SurfaceId)`), already fixed on disk; activation #2 restarted 05:15.
- W2f done (📓️w2f-cad-spatial-editor-wgpu.md): census correction first — the CAD editor the shell renders is the **Rust play app** (four `SurfaceKind::World3d` panes + UiNode panels, painted by BOTH targets through the shared wire); `📺️renderer/🟦️.tsx` is `@semio-tech/cad-js`'s R3F editor, reachable only from storybook + its own vitest (zero `InteractionRepl` consumers repo-wide), so P0 #4's "no wgpu implementation of any kind" is wrong — the real gap was the engine logic the play app never computed and therefore never put on the wire. Ported: `⚙️engine/🧬️typology/🦀️.rs` (609 lines, 9 modelDefinition + 41 typology + 10 attribute assets `include_str!`-embedded; `resolve_typology_style` bit-for-bit with the TS FNV-1a/golden-angle/HSL derivation, 22 laws) and `⚙️engine/🧲️picking/🦀️.rs` (~950 lines, the whole `🧲️GeometryTargets`/`🧲️GeometryInteraction` half — pick targets for all 7 kernel kinds + typology object rows, typology/primitive/entity-flag/active-view filters, toggle groups, hover-key aliases, reveal index, selection merge over `protocol::MergeMode`, `target_style` as theme TOKEN ids not hex; 42 laws each naming the React `it()` it mirrors). Screen-space marquee/ray math deliberately NOT ported (already Rust in `🖱️ui/🎬️scene/📐️math`). NEW wgpu defect found and fixed: `World3dScene.engagement_preview_json` had **zero** wgpu readers (only the Interpreter's size validator), so every plugin construction preview — all 60 CAD interactions, plus generation3d/process3d — was invisible on that target; `♾️infinite/🌍️world` now parses the four React wire kinds and paints them into the existing `LineDraw3d` lane with `theme.row_hover` (= React's `hover-interactive-fill`), 5 laws. Wire payload now carries the engine: instance colour is the typology style (was a `#3b82f6`/`#64748b` premix that double-painted selection on one target and dropped typology on both), a bounded (192-item) geometry pick overlay rides `engagementPreview` while the session accepts a selection — the shell path's first sub-object pick affordance on either target — and the inspection summary gained the per-pane pick census. Also fixed an O(n²) point dedup the first port inherited from React's `join(",")` key. VERIFY: artifact crate check + wasm32-wasip2 plugin check + os-renderer-wgpu `--lib` all 0 errors; `describe` regenerates clean; 100/100 engine+panel tests and 5/5 world tests green. Whole-crate suite 406/18 — all 18 failures panic in `🏪️store`/`🔌️plugin` laws (tool-proof catalog, mutation-retirement factory, envelope-Drop witnesses) with `🔌️plugin/🦀️.rs` `MM` mid-edit by TRINITY-PLUGIN-END-TO-END; the two the packet did cause (inspection summary) were fixed. Open hand-offs: `World3dScene` still has no pick-target LANE (so sub-object picking shows but does not hit-test), curved edges degrade to endpoints (`CadEdgeCurve` carries only `kind`), instance lane has no per-instance opacity for locked dimming.
- CORRECTION: port 6113 is held by a peer's `serve process3d react dev` (vite pid 57185, parent 57171), so the 05:05 "BEFORE" screenshot was the React process3d page, not wgpu. Activation #3 (🗑️generated/w2i-puzzle3d-wgpu-dev-3.txt) succeeded end to end (renderer wasm, puzzle component-dev, materialize, prepare, activate); the serve now runs on **6213** (`WGPU browser ready: http://127.0.0.1:6213/?plugin=puzzle3d`, log w2i-puzzle3d-wgpu-serve-6213.txt). Wave 2 reports all in: w2a (asset lanes fixed: shared lane never decoded; map tiles/UI images/ui Image now live), w2b (context menus for all 15 kinds, TextEditor popups, overlay bodies), w2c (panel records assembler, settings/keybinding registry/tool/marketplace/mobile/chat), w2d (render_view mobile flat stack + committed-tree drag, deactivate, fold chip, teardown; dock 83/0), w2f (CAD: census corrected — play app is the shell surface; typology + picking engines ported, engagement previews now paint), w2j (animate: premise corrected; canvas-2d pointer wire fixed, tool proofs fixed), w2k (one positioner, RTL flow, chrome polish, mono from tokens, icon re-raster, ActionId.scope, Tabs twin).
- Boot fault 1 fixed by the coordinator: the wgpu vite serve mounted `🔌️plugin-modules/🪞️vendor` onto the infinite fonts dir, shadowing the module root's `🪞️vendor/🤝️bytecode-alliance/🪟️preview2-shim/*.js` the component imports (404 → "Failed to fetch dynamically imported module"). Removed the mount + `fontRoot` (🌐️server/🟦️.ts, 🎚️config/🟦️.ts, browser-serving test); the font is checked inside the module root's vendor dir. Serve restarted on 6213; shims and font 200.
- New probe 🐍️wgpu-console-dump-probe.mjs (playwright, WebGPU on, worker console, failed requests, timed screenshots). Boot 2 (🗑️generated/wgpu-boot-2): plugin loads, navbar/windows/footer/grid/gizmos paint, then `worker-frame-failed: asset retained structure decoder rejected malformed input` (🧊️renderer/🦀️.rs ~2631/2648) quarantines the surface. Chip labels wrap inside chips (measure vs paint mismatch), footer shows Tool leaves inline and no Display tab, navbar toggles all on the right. W3a (asset fault + [DEBUG] gating, owns activation) and W3b (chip text metrics, font, footer/navbar composition) dispatched.
- W3a (📓️w3a-asset-decoder-boot-fault.md): three boot faults fixed — reference underlay refusal quarantined the surface (demoted to a per-asset refusal), `collect_world3d_asset_bytes` never advanced its page cursor (capacity overflow), decoded underlay exceeded raster-lane credits; asset throughput no longer one unit per frame; per-frame [DEBUG] dumps gated behind SEMIO_RUNTIME_DIAGNOSTICS; new diagnostics probe 🐍️w3a-wgpu-diagnostics-probe.mjs. Boot 5: no fault/panic for 75 s. Two P0s left: chrome presents nothing after W3a+W3b (drawCalls 0, glyphCount 0, black canvas) and the World3d bridge drops url-declared meshes (no GLB ever fetched; `mesh_source_urls` has no inserter).
- W3b (📓️w3b-chip-text-and-footer-dock.md): chip widths were priced bytes×0.6×font_size while the painter used real advances; chrome text now measured on the painting atlas with nowrap+clip; frame worker booted the 8×16 debug bitmap font → embedded Anta via `FontAtlas::shaped_default()`; footer Display branch + leading/centred/trailing bands; navbar anchor order fixed.
- W3c (black chrome regression, owns activation) and W3d (GLB url mesh lane, activation after W3c) dispatched.
- W3c (📓️w3c-black-chrome-regression.md): black canvas was a 1-in-5 sticky boot — the swapchain texture was acquired in one host pump and blitted in a later one (browser expires it per task) → `Present` now acquires/blits/submits/presents in one opportunity; glass regions blurred away their own chips → `ForegroundCommands` phase; watchdog signature widened. Chrome paints with Anta; 8/8 boots painted.
- W3d (📓️w3d-world3d-glb-url-lane.md): url-declared meshes kept, GLB fetched through the bounded world-asset lane, glTF Y-up → Z-up, transport URL pinned; tests run; live check pending the rebuild.
- W2h: 86 wgpu case dirs; Nx targets `ui-rs:test-wgpu-engine`, `framework-renderer-wgpu:test-wgpu-unit`, `semio-framework-os-infinite:test-wgpu-world-terrain`; launch.json is generated from 🧩️launch.seed.jsonc; case-wiring law added; four empty stub dirs deleted.
- Coordinator: React reference serve started in `served` mode on 6313 (peer's 6013 died). Rebuilding wasm with --skip-nx-cache (the cached wasm task ignored W3d's infinite-crate change). W4a (appearance/system theme into the worker, boot tour, footer pills) and W4b (footer rail removal, per-pane Actions/Search/Window Options/Utilities/Projection rows, pane titles) dispatched; coordinator owns all rebuilds.
- Boot 6 (🗑️generated/wgpu-boot-6, wgpu-diag-6; wasm 08:29): fault-free 60 s; chrome renders in Anta: navbar Artifact · Catalogue · app id · Concrete Forest · Editor · Viewer · Inspection · Tool runs · Chat · Fullscreen, two "Puzzle 3D" panes with grid + orbit gizmo, footer rail + Tool/Command + Settings/Marketplace/History. Still missing vs React: 3D meshes (state-meshes=3 but no GLB reserve fires, instances 0 → W3d resumed with activation ownership), reference underlay visible?, light appearance, boot tour, per-pane chip rows, footer rail removal, "Top"/"Perspective" pane names (W4a/W4b in flight). React reference re-captured from the coordinator's served-mode serve on 6313 (🗑️generated/react-6313/final.png).
- W4a (📓️w4a-boot-appearance-and-tour.md): `prefers_dark_scheme()` asked for `window` in the Worker → always dark; host appearance now published from page/winit; `start_introduction` was `#[cfg(test)]` → tour never armed, now wired with Skip/Done persistence, i/n counter, Back; footer sync/presence pills now target-neutral. P0 found: `WebLocalStorage` needs `window` → no persistent prefs at all on the browser build → W5a (prefs door) dispatched.
- W4b (📓️w4b-footer-rail-and-pane-chips.md): footer utility rail removed; per-pane Actions/Search/Window Options + Utilities/Projection rows at React insets; caps read authored instance titles (Top/Perspective); gizmo margin [32,58].
- W3d final: GLB fetched/decoded/resident (v847/i2874), instance submitted, five defects fixed incl. the SIGABRT hiding ~750 renderer tests (suite 771/33 now); but the world pass paints only line draws → W5c (owns activation) dispatched. W5b (interaction parity probe + chrome hit registry/action log introspection) dispatched.
- Boot 7 (🗑️generated/wgpu-boot-7/shot-35s.png): light theme, `Top`/`Perspective` caps, per-pane Actions/Search/Window Options + Utilities/Projection, footer pills — close to React. 📓️audit-visual-parity-puzzle3d.md (Sonnet, new `dumpChrome` hit-registry probe 🐍️wgpu-chrome-hits-probe.mjs): Projection chip has no hit target (P0), raw dialect id instead of `semio · puzzle · 3d`, black-disc logo, footer order + extra `Check In`, missing ⌘⌥E/⌘⌥V hints, tour hit targets without visible tour, no `data-semio-os-ready`. W5a (📓️w5a-browser-prefs-persistence.md): host storage door + boot snapshot, key census pinned to React; needs rebuild to confirm. React 6313 serve wedged → recycled by pid (served mode restarted). W6a (navbar/footer polish, Projection hit target, ready attributes, tour introduction records) dispatched; W5b/W5c still running.
- W5b (📓️w5b-interaction-parity-probe.md): `semioWgpuIntrospection.dumpChrome()` (hit registry + last 128 actions) with a React twin in the input ledger; `🐍️parity-interact-probe.mjs` 37-step journey on both renderers (React run 37/37 on 6013). W6a (📓️w6a-navbar-footer-polish.md): Projection chip was disabled (no hit) → enabled with React's fold template; title via appBreadcrumb twin; logo was tinted by theme.text → black disc; footer reordered, `Check In` removed; `data-semio-os-ready/-error` stamped by the page; introduction records present — tour rows exist without an overlay (overlay lane). Chord badges show `Ctrl+Alt+E` where React shows `⌘⌥E` (platform glyphs in the worker) → W7.
- W5c (📓️w5c-world-pass-mesh-draws.md): built the world TEXTURED pass; chrome I/O lane pressure-driven again (maintenance livelock: five shell-maintenance starts per frame, deferred checkouts 186→16). Decisive measurement: `encode_prepared_world_instance` is called zero times because the shell presents ONE frame per boot — every later build terminates `completed=false` (Complete(CommitCandidate) then `Cancelled` from generation 6, token cancelled from outside), and the asset decode pump owns the whole transaction slice. Also: world grid ±6000 @100 vs far=1000 (stray diagonals), underlay paint order, flat lighting. W7b (presenter one-frame-per-boot, pump share, grid, underlay order; owns activation) dispatched. W7a (tour overlay/control ids/chord glyphs) and the Wave 2–6 integrator still running.
- W7a (📓️w7a-tour-overlay-and-chord-glyphs.md): confirmed the canvas is frozen after the first presented frame (hit registry grows on clicks while screenshots stay byte-identical) — the missing tour is the presenter defect W7b owns. Source fixes: tour card measured/anchored with React's spotlight cutouts + pulse ring, veil is a real hit target (no more leaks into the scene), keyboard parity, ids aligned to `ui.introduction.*` and React's bare panel-tab ids, panel toggles journal `shell.panelToggle`/`shell.panelTab`, `⌨️HostPlatform` door (cfg!(macos) is false in wasm) with a shared glyph fixture. New probes 🐍️w7a-canvas-liveness-probe.mjs, 🐍️w7a-tour-paint-probe.mjs.
- 📓️w2-w6-integration.md: ui tests 506/5 → 557/0 (resident pricing bug fixed at the root: `reserve` charged the per-surface ceiling, 31/64 slots), renderer 771/33 → 852/7 (permit leak, raster ledger laws, a real completion/witness race, VFS chevron, restored stripped production prints, paint2d fixtures, env-lock laws, overlay-row conflict resolved on React's side), infinite 169/7 (same seven, pre-existing), TS 92/0, 64/65 in-body comment blocks hoisted. Remaining 7 red are in W5c/W7a live files + SpaceAdministration bootstrap + one board gesture.
- Disk: free fell to 6 GB during peer plugin builds; pruned wasip2 debug units >18 h (+19 GB) and old nx/vite caches. `⚡️cache` needs a standing prune policy (own ticket).
- W7b (📓️w7b-presenter-one-frame-per-boot.md) — ROOT CAUSE of the frozen canvas: `World3dBuildContext::append_step` pushed the underlay's `PreparedRasterProducer` without `bind_frame_generation` → "raster producer generation is stale" → empty JobFault → build cancelled its own token, silently, on every build after the plan image decoded. Fixed + refusal made readable; decode pump bounded to half a step; scene pass order textured→opaque→lines→translucent; grid sized by camera fade. LIVE: 🗑️generated/w7b-fix-1/run-1/shot-30s.png shows the concrete-forest model, plan underlay, grid, chrome and the tour card. Remaining: tour card blurred with the scene (overlay not in the foreground pass), ⌘⌥ glyphs render as boxes (Anta lacks them), Top pane needs orthographic projection, lighting/material, adaptive far plane. W8a (tour crispness + glyph fallback) and W8b (ortho camera, far plane, lighting/grid; owns activation) dispatched. Running 🐍️parity-interact-probe.mjs on both renderers.
- parity-run-1 (🗑️generated/parity-run-1): invalid from step 5 — W8b's activation reloaded the page mid-run ("renderer exposes no dumpChrome"). Valid steps: dismiss-tour surface delta differs; panel-artifact React 1 verb vs wgpu 3; panel-catalogue 1 vs 1 but surface delta differs. Re-run after W8b lands, with no rebuild in flight.
