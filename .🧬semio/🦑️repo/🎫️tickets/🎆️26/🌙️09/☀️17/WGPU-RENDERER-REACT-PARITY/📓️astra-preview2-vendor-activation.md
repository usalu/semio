# Preview2 Guest Log Activation Repair

Canonical WGPU activation 17 failed its browser-support dependency because the vendoring patch expected an obsolete `const textDecoder` block. The installed Preview2 shim instead has a streaming `consoleStream` that already joins fragmented writes, preserves partial UTF-8, and flushes pending text. The old whole-block replacement cannot match that source. The renderer wasm dependency still completed; no successful activation is claimed from this attempt.

The new production patch preserves that stream implementation and changes only its two default stderr callbacks: the global stream and isolated `createCli` stream classify complete `[DEBUG]` lines as debug while keeping other stderr lines as errors. Exactly the two current callback sites must match. A second application is idempotent, and an unexpected source shape refuses publication. There is no older-shim fallback or dependency-file mutation.

The retained neutral WASI fixture supplies fragmented input and exact host output. A new registered law executes the actual installed third-party shim as the independent text-output oracle, then vendors the real production files and checks complete lines, severity, unchanged CLI entry points, idempotence and source-drift refusal. `preview2-vendor-2.log` reaches the intended old-patch failure. `preview2-vendor-3.log` passes after the repair. The initial inline invocation in `preview2-vendor-1.log` failed Nx command quoting before executing the law; the retained ticket `📜️script.ts` removes that ambiguity. The complete browser WASI law is running separately before activation retry.

Source: `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🌐️browser-bundle/🏗️materialization/🟦️.ts`; registered law: the adjacent `🧪️tests/🌐️wasi-activation/🟦️.ts`. Test artifacts stay under this ticket's generated directory.

`preview2-vendor-full-2.log` passes the final expanded law for both global and isolated `createCli` streams, followed by the complete browser WASI gate: AJV, strict TypeScript, actual Preview2 oracle, 17 neutral laws, two actors, 256 resource slots and 128 waiter slots. The preceding full run also passed before the isolated-stream case was added. WGPU17b activation succeeds after the repair (56.5 seconds), followed by React10 (37.9 seconds). The paired 57-step browser journey completes without the earlier text-projection crash; its remaining palette and visual defects are tracked separately.
