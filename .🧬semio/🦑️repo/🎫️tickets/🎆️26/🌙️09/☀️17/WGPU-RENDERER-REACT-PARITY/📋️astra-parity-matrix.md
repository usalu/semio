# Renderer Parity Acceptance Matrix

The React renderer is the behavioral and visual reference. A previous source patch or an old green test report does not establish current parity. Each row needs current implementation evidence and the relevant automated/runtime check. Shared schema contradictions must be resolved consistently in both implementations, rather than adding a wgpu compatibility layer.

## Active Checkpoint 17 Gate

UI61 passes all 649 tests after preserving the collapsed-descendant geometry contract exposed by UI60. The latest completed full renderer census, Native55, passes 1,235 of 1,238 tests and fails three; subsequent focused Native57 passes the Theme workflow, invalid-input revision law and editor-coordinate law, while exposing the Drivers owner-map retirement and Display childful-gutter failures. Their generic Tree repair and the bounded mounted-panel locale refresh are now source-coherent; full Native58 is running. Native48 remains a historical green baseline, not the current full-census result.

Fresh WGPU17/React10 activation and paired browser validation have not run. Dedicated Dock reference acceptance is 8/8 React and 7/8 WGPU, with the remaining template case blocked by the missing disclosure gutter. Dedicated Draw and Layout React activations completed; their physical adapters are being calibrated and neither app has full physical acceptance. See `📓️astra-checkpoint17-plan.md` and `📓️astra-layout-physical-adapter.md` for current evidence.

| Area | Required behavior | Current acceptance status | Owner / next evidence |
| --- | --- | --- | --- |
| Build and contracts | Native and browser builds plus shared/renderer contracts pass | Native48 passes1231/1231, UI58 646/646, full React engine census3 1838/1838 across90files. Prior shader137, Host79 and UI React804 remain green. Canonical WGPU16/React9completed and five artifact hashes remain sealed through the57-step run. Native50 executes2focused RED shell regressions; repairs and full rerun pending. Auxiliary frame-policy selftest remains RED. | Astra fresh activation17and physical parity |
| Window lifecycle | Close, reopen, split, tab, focus, maximize, resize and cancel; an empty dock can receive a new window | Checkpoint16physically passes120pxsplit drag, focus/unfocus/refocus body bounds, individual close, close-last, and actual Display-handle new-window/live-body creation on both renderers. Dedicated dock8-case split/merge/reorder/Escape/configuration fixture is authored with contract/typechecks green; physical run pending. | Sol windows; Astra dedicated dock acceptance |
| Display template drag | Drag a window/projection template into each tab/split zone or the empty root; show matching preview and seed its configuration | Checkpoint 16 passes empty-root creation. Dedicated Dock C+D passes all 8 React cases and 7 WGPU cases: four splits, merge, reorder and Escape. React's template creates an orthographic camera with direction +Z and up +Y. The WGPU Parallel branch has no physical disclosure hit, so its template case fails before dragging. | Sol retained Tree; Astra dedicated Dock rerun |
| Retained Select | Popup bounds, scroll chevrons, keyboard selection, hit geometry, clipping and foreground text match React | UI54 passes popup geometry, scrolling and input ownership. Full16 Appearance/Language selection changes host state but leaves the retained Select stale. Native50 RED → 51 GREEN confirms the narrow publication repair; Native52/53 RED → 54 GREEN covers six settings and the driver workflow, including exact-owner refusal/retry. Fresh browser17 remains required. | Sol settings report; Astra full native and browser gates |
| Modal and popup compositing | Images, text, vectors and controls appear above their own surface with correct clipping; outside content remains behind | Overlay raster lane implemented across admission, measurement, encoding, reset and retirement; native UI laws and wasm check pass; runtime ordering pending | Sol Select/overlay report |
| Authored layout | Stack, grid, overlay, absolute and scroll semantics agree with the owned contract and browser layout | Paired Overlay/Absolute flow, intrinsic-height and mounted-worker laws pass in the full native census; React and Taffy oracles pass | Sol layout and UI integration reports; browser placement checks remain |
| Shell geometry and styles | Navbar cluster alignment, cap heights, active/hover/focus styling, panes, all panel anchors, footer and utility controls match at equal viewport/density | The Tree scroll law now uses a shared painted/hit child origin, and the dense Actions law physically requires Abort. Native48 passes the single-publication Actions rows and document-page retry laws; UI56 passes; the actual React five-step Actions oracle passes. Compact General, 160px value columns, 16px small controls and22.4px medium controls await fresh paired pixels. | Astra checkpoint16; no post-fix visual acceptance yet |
| Scene output | Every supported scene paints; camera framing, grid, materials, references, selection and picking agree | Checkpoint14 visibly loads GLB models in both panes. Perspective matches after settling. The Top distance correction uses aspect one and passes the actual Three oracle and 141 scene tests. S2a Standard, S2b painted/conic and S2c ordered translucent pixel cases are byte-exact; World21 exercises the production sort key. Actual Three and production WebGPU S3 reference rows pass13/13 each. Native48 passes the World ownership/publication laws; natural-resolution dimensions/publication and early browser settling remain open. | Sol scene report; Astra fresh checkpoint |
| GPU raster residency | Reference and paint texture keys retire when no active surface/frame owns them; replacement and pane closure stay within bounded GPU capacity | UI54 passes bounded lease/upload/residency laws. Actual GPU UI46 recorded six textures through two CPU slots, same-source recovery and close-to-zero bytes. Chromium/Three strip oracle passed8 codec/orientation cases and natural2275×2560 processing in23 bounded strips. Natural-resolution application publication and frame latency remain due. | Sol raster strips report; Astra live checkpoint16 |
| Scene input | Pointer modifiers, dragging, keyboard, wheel, context menus and cancellation produce matching actions | Lossless pointer-cancel wire/transport passes67 tests; normalized dispatcher passes independent two-pointer ownership. Stepper press/cancel agrees with six actual React oracle tests and UI54. Scene cancellation Rust interfaces are source-coherent; actual React cancellation gates pass3 focused and34 neutral/Board/Tiled laws. Ink clipboard has7 mounted React laws and bounded browser candidate27/27; Ink native production laws pass in Native48 after publishing a real nonzero document identity in the harness. LayoutCatalogue6 reproduces the action-envelope mismatch; the strict MIME/raw-JSON decoder repair passes all5 focused native laws, including real retained preview/drop/window isolation for all4 kinds, against the mounted React12/12 producer oracle. React terminal cleanup passes17/17; WGPU leave/drop source-batch, downstream frame-owner refusal, fault-tail cleanup, and staged-close laws pass in Native48. Physical Layout acceptance remains open. | Two Sol implementation packets; representative Draw/Layout/Note activations |
| Accessibility | Current controls expose names, states, shortcuts and keyboard reachability | Select Space/typeahead, AX blur/focus ownership and blur-commit source are implemented with focused React/TS evidence. Palette source publishes canonical combobox/listbox/options and physical Close; six mounted reference tests pass. Fresh browser calibration found and repaired React cn() dropping the independent top inset. Native48 is green; paired runtime acceptance remains due. | Sol controls/palette reports; Astra activation16 |
| Locale and appearance | Equal explicitly selected locale, terminology, density, appearance and theme yield matching chrome | Fresh React 11-step flow passes dark/de and pointer/keyboard palette activation. Full16 WGPU retained settings are stale; Native54 now passes all General/driver publication laws. The canonical German Set Theme label passes after Native51 RED. Theme leaf publication, locale refresh across mounted panels, density and paired fresh browser acceptance remain open. | Controlled en/de and light/dark comparison |
| Host and shell integrations | File I/O, configured layouts, shell commands, history/conflicts and connection lifecycle match supported React flows | Hub authority/status/workspace + late-operation token retirement + bounded retry/close transport implemented, React66/66 and WGPU TS100/100 pass. Sync detached body and Task Manager projection source implemented (React/neutral5/5); backend sync attach/detach remain retired-channel refusals. Fullscreen OS routing and explicit Ctrl+Meta collision repaired; neutral mod law now requires exactly one of Ctrl/Meta. WGPU AgentCancel sender/state/UI added with actual React oracle1/1. Sync canonical event-message ownership batch active. Native14/browser10 acceptance pending | Sol window lifecycle; Astra gates |
| Driver and theme editing | Every driver axis and canonical theme field supports React's edit/save/delete/import/export and live preview behavior | Seven-axis driver editor source-stable with96focusedtests reportedpassing; theme compatibility removed and independent expansion/virtualized scrolling source-stable; canonical React theme tests39/39 pass, Mono runtime confirmed; full WGPU build/browser verification pending | Sol driver report; Sol theme execution |
| Representative apps | Window and renderer behavior holds beyond puzzle3d: graph, list/tree, map, 2D canvas, 3D, document surfaces | All fifteen surface families have paint routes but do not yet have current physical acceptance. Draw physical fixture correctly rejects the Puzzle3D host; dedicated Draw, Layout and Note activation targets are identified. Ink clipboard and family cancellation production are source-coherent but lack fresh dedicated runtime acceptance. Canonical Draw covers Canvas2d; canonical Note covers Ink. Layout bridge repair and malformed drop terminal cleanup pass Native48 after fail-first witnesses. Dedicated physical Layout, Draw and Note journeys remain due. | Dedicated activation matrix after checkpoint16 |

## Current Runtime Evidence Limits

- The initial in-app browser comparison used existing wasm artifacts. It establishes that a baseline can paint puzzle3d, not that the current checkout works.
- The first two-step scripted comparison is invalid as a parity gate: React lost its development server connection during boot. Its two reported differences are not scored as renderer defects.
- Restart the owned React listener without HMR for the comparison and activate fresh inputs. Do not restart, terminate or rewrite other agents' listeners or build artifacts as cleanup.
- Reserve full interaction/pixel acceptance until both renderers finish booting from the intended builds and no activation occurs during the measured journey.

## Fresh Reference Recovery

React reference watcher race is reproduced and fixed; focused 24-case watch-policy suite passed. See `📓️astra-watcher-race.md` for two separate config-graph failures discovered by the wider run. CUA confirmed close-both→empty-dock→Display-handle-drag creates a new window. The ticket journey now requires that outcome, rather than clicking any existing tab; see `📓️astra-window-runtime.md`. WGPU retained TreeItem now publishes canonical sort/transfer handle hits for the Handle driver; the paired journey requires the matching transfer handle. Native and browser acceptance remain pending. Current React plugin freshness warning requires a new completed paired activation before final acceptance.

## Integration Census and Scope Clarification

World/terrain suite executed218 tests:215passed,3failed. Two laws inspected scene data before the stepped bridge published it; the picking law incorrectly expected kind priority to outweigh several world units of distance. The latter contradicts the actual React CAD `targetRayScore` distance+epsilon formula. Repairs drive the real bridge and cover nearer-object plus coincident-bound vertex selection; rerun passed218/218.

The older GLB material audit was incorrect: current React GlbInstanceMesh replaces source materials and uses environment metalness/roughness or defaults 0/1. S2 follows that executable authority; retaining source GLB materials is not a parity requirement. Scene shadows are required: React casts/receives them while WGPU has no consumer. Sol scene shadow implementation is active.

## Current Checkpoint — 2026-09-20

React reference journey completed38steps/zeroactionerrors after closing the obstructing Catalogue panel. This remains reference-only evidence. Generic UI-render suite passed134/134 on the integrated raster/shadow checkpoint. WGPU activation5 failed in shared tracing and was repaired; activation6 progressed to Shell and encountered overlapping/in-flight Hub field/initializer/action errors. Hub executor owns reconciliation before another activation. Current renderer-native/UI/World/trace runs are pending.

Theme input mounted component suite passed3/3. CUA verified initialized text values, blur commit, visible unsaved state and Reset restoration, with no new console error. English and German plus explicit Light were exercised on the React reference; WGPU paired locale/appearance acceptance remains open.

Latest reference:40physical steps pass on React with hit ownership and fullscreen outcome assertions. World suite4:219/219pass. Renderer native7:two Hub compile errors repaired; native8 and activation7 running. Independent GPU audit opened full-capacity unchanged upload and unused EngineCanvas target issues; source fixes complete; native/runtime evidence pending. Exact shadow roles, light-frustum caster retention and current Three sampling/bias/profile differences are required follow-up work.

## Latest Integration Boundary

Checkpoint7 diagnostic artifact identities were sealed and remained unchanged. The corrected seven-step journey proves close-all on both renderers and exposes the Display projection transfer defect. Late registerBrushMesh action differences remain recorded but are not interpreted as input regressions. Debug worker overruns and 3–7second publication latency remain an open runtime acceptance concern.

Trace suite now passes49/49 after exact TOML target-header matching. Lock-stalled UI5 and renderer8 were cancelled by verified owned Cargo PID only; fresh UI6 and renderer9 use temporary ticket-local intermediate caches. Activation7 drained with its original frame-worker failure and failed component build; canonical activation8 includes regenerated frame-worker and current stable source. No external process/cache was changed.

Latest: activation8 renderer wasm passed; component prerequisites continue. UI8 passed609/609 with0skipped after recording the exact measured slot-size increase. Renderer10 is compiling next. Engagements canonical reserved-section source is stable; precise inclusion in the concurrent wasm build is not yet established.

## Checkpoint8 Gesture and Shader Gates

The five sealed WGPU/browser artifact identities remained unchanged after paired window and isolated fullscreen runs. Actual close-all passes; new-window drag fails after a real retained transfer-handle press and outside release. Fullscreen fails at OS command routing before browser entry. Both defects have concrete assigned production paths; see `📓️astra-window-transfer-checkpoint8.md`. Shader4 now passes135/135 after repairing literal backslash-newline text in both WGSL constants and validating all six runtime/canonical variants. The broader React engine-contract fail-first check passes5 then fails1 at a concurrent catalogue metadata expectation for a removed schema field; it does not establish a WGPU regression or full React green suite.

## Native13 and Browser9 Boundary

UI11:609/609pass0skip; World6:221/221pass243filtered; shader4:135/135pass. Native13:1145/1160pass15fail0skip, concrete failures assigned. Caption layering now passes with production operator records. New graph negative visibility case correctly fails before its repair. Canonical activation9 failed the omitted accessibility source authority and a Hub overlay UiFlow argument mismatch; both source repairs are complete, boot generator and neutral import oracle pass, replacement activation remains required. See `📓️astra-build-checkpoint9.md`, `📓️astra-renderer13-failures.md`, and `📓️astra-graph-runtime-contract.md`.

## Checkpoint10 Runtime Run

CanonicalWGPU10 andReact4 completed; five renderer artifacts are sealed and unchanged. UI13passed610/610 including actualPuzzle3DSettings retained paint. Paired13-stepSettings/window/fullscreen journey running; see `📓️astra-checkpoint10-runtime.md`. Terra surface inventory confirms all15kinds have paint routes but identifies Canvas2d modifiers/cancel/catalogueDnD/doubleclick and Ink editing/clipboard gaps. Canvasinput andInk text/table execution packets are active; final parity remains incomplete.

## Checkpoint10 Visual Review

The thirteen-step sealed runtime validates actual empty-dock reopening and DOM fullscreen entry/exit in both renderers. Settings numeric and General controls are present; two namespace-sensitive probe assertions were corrected for a fresh run. The paired screenshots still show material differences in General Settings tree layout/expansion/anchor, chrome widths/icons, active window, scene perspective framing/material brightness/grid/reference placement. These remain blocking visual parity gaps; see `📓️astra-checkpoint10-runtime.md`.

## Full44-Step Review

The full sealed10 journey reveals a missing reopened window body and General Settings remaining visible after close. Previous tab-count/click success was insufficient. Scene gesture coordinates now require the actual visible World3d hit registry; stale closed-window mesh diagnostics are not a gesture target. See `📓️astra-checkpoint10-runtime.md`.

## Current Native and Visual Repair Boundary

UI14 passed all 610 tests with no skips. Native17 is compiling the repaired Canvas monotonic-clock path plus the coherent camera-fit source. The new-window body and Settings Tree/upward panel-flow fixes are active. Terra completed the scene shading audit, identifying neutral theme provenance, BRDF/normal math, vertex-colour policy, and World-only ACES as the next execution packet. No newer browser artifact has been activated.

## Active Checkpoint11 Boundary

New Settings Tree, initial window refresh, Canvas, Ink, camera, and latency code has reached the renderer wasm build. Native18 remains red on four concrete laws. World10 is pending. Initial-body and Settings-close browser postconditions still refer to the sealed checkpoint10 failures until a fresh paired run. Dock separator/body spacing and semantic panel/shader differences remain open; see `📓️astra-dock-spacing-audit.md` and `📓️astra-terra-scene-shading-audit.md`.

## Checkpoint11 Runtime Outcome

The new-window normal path passes real browser postconditions, while Settings close still fails because the root panel tab overlaps the footer Toggle. Driver children still ignore collapse and upward flow. Camera fixtures are green, but live Top never fits and Perspective fits late with outline-bounds/direction differences. The new latency aggregate exposes a 2.27-second worker/route-intent scope, which remains an open performance concern. See `📓️astra-checkpoint11-runtime.md`.

## S1 Pixel Boundary

Actual installed Three produced and reproduced 17 schema-validated reference pixels. Actual production WGSL through WebGPU agrees on all 13 opaque cases within one byte, but four transparent cases fail due to the color blending path. The generic shader/render suite passes 136/136. This is controlled pipeline evidence, not full renderer parity; see `📓️astra-three-shading-oracle.md`.

## Encoded Blending and Checkpoint12

The final controlled World3d pixel packet now passes all20rows byte-exact, including transparent nonzero backgrounds and overlap. Its permanent Nx/launch task also passes with actual Three and WebGPU. Activation12rendererwasm passed and its five artifact identities are sealed; the fullgraph and freshbrowser run remain pending. Native20 stopped before tests at a missing DockNode import in the publication fixture. Terra found two further queue/producer negative paths and two stronger Settings test requirements; these are assigned and open in `📓️astra-terra-publication-settings-audit.md`.

## Checkpoint12 Runtime and Final Gates

Full WGPU activation12 and React activation6 passed. Final shader/render136 and UI21's616 tests passed with no skips. The15-step paired browser journey crashes during General Settings reversed tree paint: eager `then_some` computes an out-of-range subtraction. Later window/Drivers/fullscreen outcomes are crash cascades; they are not independent failures or passes. The repair and actual terminal/empty Up-flow paint laws are assigned before the next activation. Allfive sealed artifacts remained unchanged after the run. See `📓️astra-checkpoint12-runtime.md`. Native21 and World13 remain due after coherent camera/publication source markers.

## Checkpoint 13 Current Boundary

All five checkpoint 13 artifact hashes remain sealed. The fresh 15-step paired run passes actual General/Drivers disclosure, Settings footer close, closing both windows and reopening a live World body. Fullscreen fails only after the longer journey and passes an isolated boot/fullscreen sequence; root is tracing focus/input state. Initial GLB models remain absent due to a camera revision invalidating their sealed draw rebuild. The camera3 repair and instance-position producer fix await World15 and fresh wasm/runtime proof.

UI28 passes 619/619 with no skips, including actual General-shaped Select activation. Native22 is compiling Dock spacing, trailing grips and leading cursor geometry with the memory/panel repairs. Actual screenshot review still blocks General tree and closed-control paint alignment, compact footer glass, reference-image opacity, scene framing and initial model presence. The reference decode stall is measured and assigned. See `📓️astra-checkpoint13-visual-audit.md`; full parity remains incomplete.

## Native37 Full Census

Native37 completed all1,220 tests in45.57seconds:1,210passed,10failed,zero skipped. The former host-panel hang and new kernel future/response wake laws pass. Failures are assigned across scene ownership sizing/sourceguards/Actions5, Ink3, General retained-registry harness1, and mounted Winit refusal1. Repairs are underway; no fresh browser activation or complete renderer pass is claimed. See `📓️astra-native37-failures.md`.
