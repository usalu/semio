# Window Recovery Runtime Verification

The original ticket interaction probe's `reopenWindow` only clicked a remaining tab/app switcher and could not establish recovery. It now closes the actual remaining windows, opens Display, expands the window-kind section, drags the real template, and requires exactly one new tab. Missing controls in both renderers no longer count as parity matches; recorded errors or failed boots now fail the process.

The initial corrected React-only run closed both Top and Perspective windows and confirmed zero surfaces. Reopening failed because the test dragged the row center. Fresh CUA inspection established a separate transfer handle (`data-slot=drag-handle`, label `Drag to window`) at the right edge of the row. A real CUA drag from that handle into the empty dock created `puzzle3d-main-3`, with a new Puzzle 3D tab and window controls. The probe now targets that observed handle and records whether the renderer exposes a handle or only a row hit. The second automated run created the new window but correctly failed its test because the observer counted both React’s decorative tab wrapper and its actual role=tab control. The observer now counts only concrete window tab ids. The third run passed all four executed steps with zero recorded failures: boot, dismiss tour, close both windows to zero, and template-handle drag to exactly one new tab. Console output records `beforeTabs: 0`, `afterTabs: 1`, and `dragSource: handle`. This remains a React-only baseline, not measured paired parity.

Current-source follow-up: WGPU retained TreeItem hit registration exposes drag data on its entire row band. React's default tree path uses separate transfer handles, and its row-center drag does not start transfer. This is a concrete retained-tree input/visual parity item for a separate packet; successful recovery alone will not clear it.

Fresh React browser boot is confirmed after watcher repair. Its plugin freshness warning identifies puzzle source modified since the previous completed activation, so this run verifies host interaction and probe correctness, not the final fresh paired acceptance gate. A new paired activation is required after the in-flight lighting/source packets.

## Full Journey Obstruction

The full React reference journey correctly failed its new close-all assertion: after opening Catalogue, the journey left that floating panel covering the Top cap. Coordinate clicks on the cap instead toggled Catalogue rows. Screenshot `astra-runtime/react-full-reference/react/18-window-close-last.png` and steps16–18 show unchanged windows and tree-row deltas. Root added an explicit Catalogue close after the panel checks, before pane/cap gestures. The obsolete `windowControls` chip was removed; its current `measures.unfold` control was already separately exercised. The focused close/reopen journey passed earlier; this full journey failure does not establish a renderer close defect. Rerun pending.

The ticket journey now records per-step rectangles for19 named navbar/footer controls in `steps.json` and emits a separate `geometry.md` with x/y/width/height differences at1600×1000,DPR1. The action-set verdict remains behavioral only. Matching actions is explicitly insufficient for visual/layout acceptance. The full React-only reference2 run is active after the Catalogue-obstruction repair.
