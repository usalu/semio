# Checkpoint10 Runtime Acceptance

Canonical WGPU activation10 completed20tasks successfully in9m56s. React activation4 completed14tasks; five WGPU renderer/worker/browser artifact identities were sealed in `🗑️generated/astra-runtime/checkpoint-10-artifacts.json` and verified unchanged after React activation. The paired thirteen-step journey is running against these artifacts; no subsequent activation or generated-bundle write is permitted during its measurement.

The journey checks actual four-control appSettings publication, General Appearance publication, empty-dock window reopening from the Display transfer handle, and actual browser fullscreen state. Every step retains frame/structure/accessibility diagnostics including visible panel frame stats, in addition to the physical action and outcome snapshots. The old manually opened React/WGPU tabs were closed before measurement to reduce extra test-worker load; no user-owned tab was closed.

UI13 independently passed610/610 with0skips in21seconds, including the actual Puzzle3D Section→4Field→4NumberStepper document through retained ingress, layout, paint, hit publication, exact borders and glyphs. The matching mountedReact fixture passed1/1. Native15 is running against the coherent final Sync transition; native14 failed before tests during that transition and is not an acceptance receipt.

The suite's action-ledger comparison remains distinct from physical outcome assertions. A late asset action or shell-only status publication can explain a ledger difference; it cannot make a missing control or failed window/fullscreen assertion pass.

## Completed Checkpoint10

All five sealed artifact hashes remained unchanged after the thirteen-step run. Both renderers booted, dismissed the tour, opened Settings, selected General, focused and closed windows, reached zero open windows, reopened one window by the actual Display transfer handle, and entered/exited actual browser fullscreen with Ctrl+Meta+F. WGPU published all four appSettings numeric controls. Both reported action errors were probe predicates: React appSettings controls use a panel namespace and WGPU General controls use a window namespace. The recorded controls contain every required field, and both predicates now accept the exact terminal key after its namespace. A fresh runtime rerun is required to validate the corrected assertions. The captured WGPU console contains no pageerror, paint-node fault, missing-action-program, retained-fault=Some, panic or budget-exceeded row.

Native15 completed1163 tests:1159passed,4failed,0skipped in29.585seconds after compilation. Remaining failures are the native Display transfer-handle setup, the Hub source-spelling assertion, and two nested Settings activation fixtures missing their action program. Owners are repairing those laws without weakening actual interaction assertions. Table capture retirement, Graph viewport/slider projection, command modifiers, footer clamping and Agent cancellation passed this integrated census.

The images remain visibly different. General Settings is a compact reversed tree at the bottom-right in React, but a tall stacked field document with expanded Driver controls in WGPU. WGPU chrome widths/icons, initial active window, scene perspective framing, material brightness, grid contrast, reference size and pane decorations differ. These are open visual parity gaps, not accepted by the successful physical window actions.

Latency diagnostics are present but their current scalar-level records flood the256-item ring: settings-open recorded709701 records,709445dropped, and254presenterRetirement entries in a3.6ms window. Its generation37 is a frame generation while worker generation8 is an input generation. The fleet is replacing this ambiguous high-volume diagnostic stream with bounded aggregation and explicit generation domains before using it to attribute input-to-present delay.

A wider44-step paired journey is running with runtime diagnostics disabled against the same sealed artifacts. Its physical outcomes and screenshots are acceptance evidence; absent action-ledger diagnostics cannot prove action equality.

## Diagnostics-Disabled Run Interrupted

The wider run did not reach a WGPU quiet/readiness checkpoint within240observations; React booted in7seconds. It then failed to finish its first recorded step promptly. The owned probe was interrupted to avoid attributing forty-four actions to an unready renderer. No interaction outcome or diagnostic performance comparison is accepted from this run. The current probe's boot timeout reports only a generic convergence failure; its next revision must persist the last observed state, screenshot and console immediately at boot, stop journeys for failed drivers, and distinguish absent worker bindings, renderer faults and ongoing camera/content publication. Manual browser inspection will determine the actual failure.

## Diagnostics-Disabled Root Cause

The focused60-second rerun captured a fully painted Puzzle3D and tour, while `dumpChrome` returned armed=false, generation0 and empty hits/surfaces. This is intentional source behavior: `Interpreter::note_chrome_hit_registry` gates the diagnostic ledger on `runtime_diagnostics_enabled`, unlike the production accessibility mirror. The failed quiet check was therefore an invalid test assumption, not a renderer boot failure. The probe now persists boot state and logs and stops failed-driver journeys; a diagnostics-disabled physical journey requires a different observation route. Do not use this failed run as latency evidence. The full coordinate journey will use diagnostics enabled.

Manual CUA confirmed General selection through the production accessibility tab route and observed its Appearance combo and other fields. The mirror also retains four appSettings steppers after General becomes visible; this visibility discrepancy is assigned to the Terra Settings audit. Native SecondaryAction Expand did not open the mirrored Appearance combo; direct accessibility activation is being checked separately.

## Retained Select Manual Evidence

Pointer activation of General Appearance eventually produces aria-expanded=true. The popup is nevertheless painted at the far-left origin (approximately x0..293,y175..258 in a1280×720viewport) while its trigger is atx979..1273,y225. Popup content also overlaps nearby fields, and the accessibility mirror announces no option/listbox rows. The earlier direct AX attempts only focused the control; they did not show an open popup before the later pointer action. This is assigned for independent retained-overlay origin and accessibility tracing. The temporary manual tab was closed before the wider paired measurement.

## Completed44-Step Run: New Window Body Is Missing

The full journey completed44steps with no thrown action errors, but that is not behavioral parity. All five artifact hashes remained unchanged. Detailed state and image review found two concrete missed postconditions: Settings close leaves the General panel visible, and Display reopening creates a tab with an empty window body. WGPU's new instance has no World3d hit targets through orbit, pan, zoom, pick and context steps; those steps dispatch no scene actions and retain the closed windows' old live-camera snapshots. The next example switch finally publishes the new instance body. React paints and interacts with the newly opened scene immediately.

The new-window screenshot directly confirms an empty body. The old probe only counted a new tab and its surface-point helper used the first stale mesh snapshot, so those five gesture attempts were not valid scene interaction tests. Reopening now requires a new window identity and live scene body; WGPU scene gestures now require its visible World3d pointer registry and use absolute hit coordinates. Settings close now requires its active panel to disappear. The next focused run must execute these stronger postconditions. Window-body publication/retirement is assigned to Sol Chrome after its Canvas packet.

The earlier thirteen-step result is limited to tab creation and actual fullscreen transitions; it does not establish a usable reopened window. Native16 is compiling the stable Canvas, Ink, latency aggregation and four native15-law repairs. No post-checkpoint10 source is yet represented by a fresh wasm build.

## Focused Postcondition Red Run

The corrected eight-step paired run reproduced exactly two WGPU failures: “Closing Settings left its active panel visible” and “New window tab never published its live scene body.” React passed both postconditions. All five artifact hashes remained unchanged after this run. These are now executable regression checks as well as screenshot findings.

Native16 stopped before tests with four Canvas input compile errors: its new code and tests referenced a nonexistent InputState.time_seconds field. The Canvas owner is correcting the clock path. UI14 is running independently against the changed retained input/capture code; renderer native17 will follow after that serialized gate and the clock repair.
