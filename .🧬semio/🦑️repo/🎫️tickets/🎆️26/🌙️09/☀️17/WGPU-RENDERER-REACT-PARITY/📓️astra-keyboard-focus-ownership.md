# Browser Keyboard Focus Ownership

The sealed checkpoint 13 browser run reproduced a focus-dependent fullscreen failure. A four-step boot/dismiss/enter/exit run passes both fullscreen transitions in both React and WGPU. Repeating after close-all, reopening a new World body, orbit, pan and zoom fails both WGPU transitions. The optional page-key receipt shows trusted Control, Meta and f events targeting `DIV` in that sequence; in the passing boot sequence their target is `CANVAS#semio-wgpu-canvas`. Both report macOS and active user activation. The failing worker emits no Shell chord-gate line. The actual accessibility mirror restores renderer focus onto projected nodes, while browser boot attached keyboard listeners only to the canvas.

The repair places one keyboard owner on the runtime root and admits keys from the canvas and accessibility mirror. Native mirror Tab focus, ordinary button Enter/Space activation and editable input/value events remain native. Modifier transitions are forwarded so releasing a modifier after moving into an input clears renderer pointer modifiers. Combobox keys are retained-control events; native defaults are prevented to avoid a second browser activation or edit. Default-prevented and composition events are not admitted. Cleanup removes both listeners. No new runtime dependency is introduced.

The language-neutral schema and 16 vectors cover canvas, focused tree, native buttons, editable inputs, comboboxes, outside-root scope and already-consumed events. The test uses the installed JSDOM and Testing Library user-event implementation for native typing, Tab and button activation, and the actual `createAccessibilityMirror` refresh for the observed canvas-to-DIV focus change. The baseline canvas-only implementation fails seven semantic laws plus one source-wiring fixture problem. After fixing ownership and the fixture file-URL read, the new suite passes 20/20. Adding the pre-existing accessibility/input-wire suites to normal renderer test discovery yields 49/49 across three suites (1.44 s Vitest, 2.6 s Nx). Browser acceptance requires activation 14 and the same close/reopen gesture sequence.

The exact browser-input source is registered in the WGPU taxonomy. Its initial insertion violated the byte-sorted source-list invariant, blocking World16's graph prerequisite before tests; the ordering is corrected, and World17 successfully passed that prerequisite and is compiling.

Evidence: `🗑️generated/astra-runtime/fullscreen-13/`, `fullscreen-reopen-13/`, `keyboard-scope-red-2.log`, `keyboard-scope-green.log`, and `keyboard-scope-regression.log`.

## Focus-Loss Follow-Up

Terra identified modifier latching after focus moves outside the renderer root. Three language-neutral loss vectors (outside focus, window blur, hidden page) first failed with zero release events while 21 existing cases passed. The root owner now records the complete admitted modifier snapshot and emits lossless key-up edges with all modifier flags cleared on those loss paths and disposal. In-root mirror focus changes preserve held modifiers; a microtask avoids treating synchronous mirror replacement/refocus as permanent focus loss. Loss notifications are idempotent and every listener is removed on disposal. The three registered suites then passed 55/55 in 3.01 seconds (5.1 seconds Nx); receipt `🗑️generated/astra-runtime/keyboard-focus-loss-green-2.log`. This includes the two new concurrent P4 mirror cases. Browser runtime verification remains pending checkpoint14.


## Fullscreen Ownership Follow-Up — Checkpoint 14

The actual fifteen-step browser journey now enters fullscreen after closing/reopening windows, but its exit chord targets BODY and fails. Chromium entered fullscreen on the canvas alone while focus belonged to the sibling accessibility mirror. The browser moved focus outside the renderer's keyboard owner. Trusted keyboard receipts establish this boundary; the old canvas-only fullscreen target also excluded accessible controls from the fullscreen subtree.

The new browser fullscreen owner requests fullscreen on the renderer root, preserving both the visual canvas and accessibility mirror. It remembers the focused owned control and restores that control, or the canvas if it was retired, on owned fullscreen transitions. It ignores another element's fullscreen state and removes its listener at lifecycle disposal. The production browser entry uses this owner.

Language-neutral vectors cover canvas, accessible tree and accessible Select focus. The extracted existing behavior failed all three new vectors (24 existing tests passed); the repair passes the full three-suite gate: **60/60**, 2.28 seconds test runtime / 4.3 seconds Nx. A separate Playwright/Chromium oracle bundles the actual production input module and performs two trusted enter/exit shortcut cycles plus native browser exit. All preserve focus on the tree and keep accessible controls inside MAIN while fullscreen. Receipt: `🗑️generated/astra-runtime/fullscreen-owner-browser/receipt.json`; input: `🔬️fullscreen/📜️script.ts`. The complete application must still be rebuilt and rerun; the oracle does not establish that the sealed checkpoint14 artifacts contain this change.
