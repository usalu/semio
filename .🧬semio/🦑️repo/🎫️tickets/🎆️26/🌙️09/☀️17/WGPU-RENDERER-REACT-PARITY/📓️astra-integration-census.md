# Current Integration Census

## World and Terrain

The actual native suite first ran 218 tests: 215 passed and 3 failed. Root repaired the instance-style law to run the production scene bridge and retire its owned surface. Sol repaired the lighting law in the same way; the authored camera assertion remains unchanged. Root corrected the pick-target law: the original solid extends two units towards the ray and must beat the center vertex; after their padded bounds coincide, the vertex must win the kind-priority tie. This follows the actual React CAD `targetRayScore` implementation (ray distance plus priority × 1e-4). No production picking behavior was changed.

The subsequent Nx world/terrain suite ran **218/218 passing**, exit0, 42.5s. Evidence: generated `astra-runtime/world-native-tests-3.log`.

## Canonical Theme

The styling quick command runs a different Bun suite and `theme parse` selected no tests; that run was not a pass. Root mounted the theme source in the existing styling Vitest configuration, selected jsdom for its existing DOM laws, and removed duplicate normal/in-source collection. The restored canonical fixture import had an extra variation selector in the engine path; root corrected the literal to the actual path.

The focused Nx-executed Vitest run passed **5 theme parse tests**, with 34 other tests skipped. It includes the canonical renderer-neutral theme JSON, production React parser and serialization roundtrip. Evidence: `astra-runtime/theme-parse-vitest-2.log`. The full mounted styling suite initially ran36/39passing. The three failures all required Mono. Its production preset glob still pointed to the removed `../theme/*.theme.json` path; the owned JSON lives in the current theme directory, matching the generator. Root corrected the glob to `./*.json`. The subsequent full suite passed **39/39**, exit0 (`theme-styling-vitest-2.log`).

## Renderer Build

Renderer native census6 and WGPU activation4 reached three current API integration errors: a raster admission call precedes its new GPU implementation, and two tutorial graph calls target GraphHost instead of its DagHost member. These errors are assigned to their active owners. A fresh heavy build will wait for their complete source checkpoints. React activation2 passed in6m24s.

## Theme Runtime Follow-Up

CUA reloaded the React reference, opened Settings → Theme and observed both semio and Mono. Selecting Mono changed the live primary color field to `#8c8c8c`; selecting semio restored the reference. Console inspection found a separate existing issue: the theme text, number and alpha rows pass `defaultValue` to Input, while Input always supplies a controlled `value`. Their fields appear empty and React reports both props. A bounded follow-up should use the existing Input lazy value/onLazyChange API for these three consumer sites and retain commit-on-blur behavior. This issue is not claimed fixed.

The existing canonical target for the styling Vitest configuration is `@semio-tech/ui-styling-tokens:test-quick` (the Rust package owns the projection runner); `@semio-tech/ui-styling:test-quick` owns the separate Bun suite. No new executable command is required. Root is checking the former target directly to confirm the mounted laws are part of the existing task surface.

Canonical `@semio-tech/ui-styling-tokens:test-quick --skip-nx-cache` also passed **39/39** through the existing script/task routing in7.7s (`theme-styling-canonical.log`).

## Integrated Raster and Shadow Checkpoint

Both packet APIs reached a complete source checkpoint before the next native/browser build batch. The generic UI-render suite passed **134/134** after that checkpoint (`astra-runtime/ui-render-native-tests-2.log`), including its shader validation and byte-layout laws. UI engine, full renderer and World suites remain running. WGPU activation5 failed in shared tracing: `counters.get_mut` retained a mutable borrow across a match guard calling `counters.len()`. Root moved the capacity observation before the mutable lookup, preserving bounded admission and existing-event updates. Activation6 and the existing trace unit target are running; no fresh WGPU runtime is claimed.

The full React reference journey completed **38 steps, zero action errors**, exit0 (`astra-runtime/react-full-reference-2`). Catalogue is explicitly closed before window-cap interactions. This is reference validation only; all paired parity verdicts remain unmeasured until the fresh WGPU journey. The probe now writes `geometry.md` separately from action outcomes.

## Theme Input Runtime Verification

After reloading the current React source, CUA observed initialized `touch = 0.275rem` and `compact = 0.2rem` fields. Editing touch to `0.28rem` and moving focus with Tab retained the value, enabled Reset, and marked the theme unsaved. Reset restored `0.275rem` and cleared the unsaved marker. Console inspection showed only the older pre-fix warning at `2026-09-19T23:23:00.240Z`; no new controlled/default-value warning was emitted by this verification. The focused mounted component suite separately passed all three text/number/alpha consumers.

The same CUA reference also selected Deutsch through Settings → General and observed `html.lang = de`, localized shell controls and localized theme groups. Selecting Hell survived reload as the explicit appearance. This is React reference evidence only, not a paired WGPU locale/appearance pass.

## Journey Measurement Corrections

Review of the 38-step reference found several silent no-ops: its Command-K and Alt-number anchor keys were stale. The current production `SHELL_KEYBINDINGS` defines `ui.search.toggle = mod+p`, top-left `ctrl+b,meta+b`, and top-right `ctrl+shift+b,meta+shift+b`. The ticket probe now uses those bindings, records fullscreen changes, and marks paired interactions with no action or UI change as unmeasured. It also closes the Actions pane before clicking Search, and checks React pointer hit ownership so an occluding overlay becomes a setup failure instead of a successful click on the wrong control. Focused shortcut/pane reference validation is running; the earlier38-step no-action-error result is not proof of those stale shortcuts.

Fullscreen commands are platform-specific OS command bindings, which take precedence over the shell fallback table: macOS uses `control+meta+f`, Windows/Linux use `f11` (`ShellHelpers::buildOsCommands`). The probe now uses these bindings and requires the actual browser fullscreen state to change. The current full reference run observed successful entry and exit. The initial focused shortcut subset accidentally toggled Catalogue open because it omitted prerequisite steps; `closePanel` is now idempotent and based on actual surface visibility.

Independent Terra review confirmed additional main-path work in `📓️astra-terra-shadow-residency-audit.md`: full-capacity unchanged raster re-offers, EngineCanvas duplicate target memory, explicit shadow roles and light-frustum caster selection, exact map/filter/bias/format behavior. Sol raster and scene lanes own these follow-ups.

The strengthened full React journey completed **40 steps, zero failures, exit0** (`astra-runtime/react-full-reference-3.log` and its output directory). Control hit-ownership checks passed. Command-P opened the actual search dialog; Control-Meta-F entered then exited actual browser fullscreen; anchor shortcuts dispatched shell commands. Search-pane open/close produced its actual input and associated Actions body, confirmed by screenshot. This supersedes the earlier38-step reference for upcoming paired comparison and remains React-only.

World/terrain native suite4 completed **219/219 passing**,243other tests skipped, after the shadow packet. Evidence: `astra-runtime/world-native-tests-4.log`. Its new law count includes the disabled-sun/clamping source checkpoint; this green result does not clear the subsequently audited shadow-role/filter parity gaps.

## Current Native Integration Failures

Renderer native census7 failed at two Hub integration sites: retained plugin borrow across mutable document-status publication, and `expires_at_ms` instead of canonical `DirectorySessionAuthorityV1.expires_at`. Both were assigned to the Hub executor before the next activation.

Trace native suite completed47/49passing, including the bounded counter admission law after the borrow repair. Two existing laws failed: the linker-budget law still searched a removed dev-script stack constant, and the omitted-fields law assumed serde_json map iteration order. Root now checks both exact budget flags specifically in the `[target.wasm32-wasip2]` configuration, updates stale budget documentation, and compares the complete expected JSON object independently of map storage order. The counter capacity law additionally verifies updating an existing event while full without increasing dropped count or table size. Trace rerun2 is active; no pass is claimed yet.

## Stable Checkpoint Entered Integration

Tree handle, raster content-identity/capacity, and Hub source packets are complete. Hub native7 errors are repaired by cloning the program bridge before mutation and reading canonical authority `expires_at`. Activation7 runs as session58386, renderer native8 as session25433. Their logs are `🗑️generated/astra-runtime/wgpu-activate-7.log` and `renderer-native-tests-8.log`. UI census5/session7476 and trace rerun2/session86444 remain pending; no duplicate test jobs were launched.

Tree production remains frozen while the browser compiler consumes it; the same Sol owner prepares the exact shadow audit follow-up. Sol raster finalizes its report. Terra independently audits Hub/Tree current seams. Root updated the paired empty-dock journey to require the semantic transfer handle; the whole-row fallback is removed. Bun syntax bundling passed; the changed probe is not yet rerun.

## Browser First-run Module Ownership

Activation7's frame-worker generation rejected the newly shared `📇️directory/🎓️first-run/🟦️.ts` export as absent from exact browser source authority. Root added that real module beside sign-in/spaces to the `wgpu-frame-worker` browser `sourceModulePaths` and `inputPaths` in taxonomy. The guard is unchanged. Canonical `repo:generator-inputs` passed in32.8s; frame-worker regeneration is running. Activation7's independent renderer compilation is still draining, so no overlapping activation was started.

## Independent Audit Follow-ups

First-run browser frame-worker generation passed48.9s across4tasks after the explicit source ownership repair. Activation7 still drains its original Rust jobs and must then be rerun canonically because its first frame-worker attempt failed.

Terra independently verified raster reuse/reservation/retention production paths and found no reachable defect in scope, but called out helper-only/source-only test boundaries. Sol raster now adds real admission/presentation lifecycle laws.

Terra Hub/Tree audit proved that Shell `route_retained_pointer_press` excludes TreeItem/TreeDragHandle from its EventRouter down/up whitelist, despite publishing those hits. Direct EventRouter unit tests bypass this defect. Root immediately assigned the host routing repair and actual Shell pointer regression law to the Tree owner; the broader shadow schema remains frozen until compiler consumption.

## Activation7 Wasm Producer Passed

Nx reports `@semio-tech/framework-renderer-wgpu:wasm` successful in activation7. The task remains alive finishing its independent component prerequisites, and its earlier frame-worker failure means the whole activation is not a pass. The repaired frame-worker already passed separately. Root will perform one canonical warm activation retry before interpreting fresh browser results.

Sol Tree host ingress production and test batch is source-stable. The new native law `shell_input_tests::published_tree_handle_routes_through_shell_and_preserves_label_selection` renders/publishes the actual Tree document, promotes its hit registry, and drives Shell down/move/up; it is not yet executed. Exact shadow production remains held until the warm activation has consumed the stable renderer.

A1-second sample of the original UI census Cargo pid91743 confirms it is blocked in `prebuild_lock_exclusive`→`LockManager::lock`→`flock`, consistent with shared compilation-unit contention. No external process was modified.

## Sealed Renderer Checkpoint While Plugin Activation Queues

Activation7 completed the WGPU wasm producer. The server configuration reads that producer dist, browser-boot generated output, and frame-worker generated output directly. Recorded their SHA-256 identities in `🗑️generated/astra-runtime/checkpoint-7-artifacts.json` before releasing shadow/accessibility source development. A diagnostic paired journey may consume these completed renderer outputs while plugin component activation remains pending. This cannot establish full current-checkout activation or plugin freshness. No new wasm/generator publication will be launched during that journey; the next canonical activation waits for coherent API batches.

## Diagnostic Journey Timing Correction

The first checkpoint7 journey used the old WGPU boot predicate (first chrome registry) and a fixed1.6second step delay. Screenshot04 shows the Artifact panel only after step03 had already captured no panel change, while the camera also settled later than the boot predicate. Its early verdicts therefore mix publication latency with behavior. Strengthened the WGPU boot gate to require stable camera/content/registry/action signatures and the step gate to wait for a published change and quiet registry, bounded to15seconds with elapsed time/generation recorded. Raw control rows and failure/boot frame and mesh diagnostics now accompany observations. The initial journey remains diagnostic, not acceptance.

## Measured Diagnostic Scheduling Latency

The initial checkpoint7 console records frame-step overruns including 4181.6ms. Improved bounded action publication waits measured roughly3–7seconds for early shell changes under the active shared host load. These are measurements of the debug/diagnostics build, not an attribution to GPU batching or proof of release behavior. Current source batches must be activated before profiling stable input-to-paint latency; a renderer parity claim requires resolving the remaining latency.

## Settled Window Journey and Tree Projection Defect

The corrected7step journey at `paired-checkpoint-7-settled-2` completed. Both booted; all WGPU windows closed through real pointer input and observed tab removal. Catalogue open/close matched. Empty-dock reopen failed because the Display template advertises `tree.drag.sort` instead of `tree.drag.transfer`. Terra traced the entire producer boundary and proved `PanelProjection::tree_item` hard-codes `drag_data: None`, dropping the actual Display producer payload. Solchrome owns the bounded projection fix and producer-through-host regression. Whole-file mtimes alone are not evidence that the relevant producer was stale, since other agents are editing those same files.

The remaining early action-set differences contain late `registerBrushMesh` publication; camera/registry quiet alone is insufficient to isolate autonomous asset delivery. Raw differences stay recorded and are not called input regressions. All five captured renderer artifact SHA256 identities remained unchanged throughout these diagnostic journeys.

Trace rerun2 was still waiting on a shared unit lock after about50minutes. Cancelled only the verified owned Cargo PID49786, allowed its Nx session to exit, and reran the canonical target with temporary ticket-local intermediates. It executed49tests:48passed, one failed because the test matched a target-header mention inside a TOML comment. The law now finds an exact header line. Rerun4 is active in the same temporary cache. No external process/cache was changed.

## Combined Checkpoint8

Tree producer projection, generation-addressed accessibility, exact shadow semantics and gizmo bounds/basis are source-stable. Activation8/session97300, UI6/session5363 and renderer9/session87988 run through canonical Nx with temporary ticket-local Cargo intermediate caches. Verified owned UI5/renderer8 Cargo jobs were interrupted after90/50minutes at shared locks and their Nx sessions drained; activation7 had already exited130. No external job/cache was touched. Trace4 completed49/49passing. Sol window owns detached Sync/TaskManager, Sol chrome prepares Table/BlockList fixtures pending source-consumption release, Sol theme finishes accessibility laws.

## Private-cache Native Serialization

UI6 and renderer9 both reached the same nightly Cargo prebuild lock inside the ticket-local native cache, with no compiler children. A1second UI sample confirmed `prebuild_lock_exclusive`→`flock`. Interrupted only verified owned renderer9 Cargo13828; its Nx exited1 and UI6 immediately resumed actual compiler children. Native tests will now reuse the ticket-local cache sequentially. Activation8 uses a separate cache and continues compiling. This is a validation scheduling correction, not a renderer assertion failure. Accessibility corrected Nx workspace invocation passed2files43tests.

## GPU Law API Repair

UI6 compiled the full current UI and found one test-only wgpu29 API error: the raster residency harness called removed `Device::pop_error_scope` and discarded the new scoped guard. Root now retains the actual `ErrorScopeGuard` for the full GPU law and consumes it exactly once through `pop().await`, matching the installed wgpu29 source. UI7 reruns the complete native UI census using the warm private cache. No assertion was weakened.

## UI7 Full Native Results

UI7 executed609 tests:608passed, one fixed-slot budget law failed. All current gizmo, accessibility, raster GPU and shadow UI laws passed in that run. The measured surface slot is161896bytes, previously161840, a56byte increase; capacity64 and owner520 are unchanged, for3584additional bounded heap bytes. Updated only the committed element measurement; the heap-first and bounded-thread-stack law remains unchanged. Full UI8 rerun follows before renderer native compilation.

UI8 full native census passed609/609 with0skipped in47.9seconds. Renderer10/session79443 now uses the same warm private intermediate cache sequentially. Canonical activation8 renderer wasm passed and still finishes plugin component prerequisites. Engagements canonical reserved-section API is now source-stable; its exact inclusion in the concurrently compiled wasm snapshot is not yet independently established, so fresh runtime validation is required. Table/BlockList production resumed with Solchrome after snapshot consumption.

## Canonical Activation8 Passed

WGPU canonical activation8 completed20tasks successfully in17m40seconds, including renderer wasm, puzzle component, materialization, prepare and activation. All five renderer/browser artifact SHA256 identities are sealed in `🗑️generated/astra-runtime/checkpoint-8-artifacts.json`. React activation3 is refreshing its matching shared component receipt before a fresh paired journey. This wasm checkpoint includes Tree projection, accessibility, exact shadows and gizmo bounds; Engagements/Sync/Task/Table changes require separate source-to-runtime validation; their precise inclusion in that concurrent build is not assumed.

The broad React engine-contract run exceeded the canonical long300second overall budget and was stopped by the runner. It emitted canvas getContext errors and several failed-test markers, but no final test verdict because of the cutoff. A verbose fail-first rerun with15second per-test/hook bounds is identifying the first actionable failure; no broad reference-suite pass is claimed.

React fail-first broad-contract diagnostic completed:5passed then1failed, the catalog artifact creation test expects a removed `schema` property in GIS creation metadata. The production projection now carries canonical kindId/dialect/label. This failure is outside the WGPU packets and no legacy field was restored. The whole React engine-contract suite is not green; focused parity oracles remain separately recorded. Initial attempt with per-test timeout overrides was rejected by duplicate Vitest options before running any test, then corrected fail-first invocation executed in13.8seconds.

## Independent Fullscreen Runtime Failure

React activation3 passed14tasks in5m18seconds. The isolated WGPU checkpoint8 fullscreen journey booted and dismissed the tour, then both Control-Meta-F attempts failed to change actual browser fullscreen state. Console records `keyboard failed: os commands must be dispatched by the shell` for each chord; no OS command reached the action ledger. This is a concrete shortcut routing failure before browser fullscreen handling, not evidence of a user-activation timeout. Solwindow prioritizes canonical OS-command keyboard ingress and regression before latency instrumentation. Fresh paired seven-step window journey is now running.

## Native Renderer11 and Settings Checkpoint8

Renderer11 compiled and ran1151tests:1119passed32failed0skipped (32.416s assertion run). All exact messages are in `📓️astra-renderer11-failures.md`. UI9 stopped at compile: missing ComponentScene arm in retained-hit test taxonomy, assigned Solchrome. World5 runs next in serialized private native cache. Paired Settings General passes React, fails WGPU with confirmed native PanelTab down/up; Terra audits route and producer validation. See `📓️astra-settings-checkpoint8.md`.

World5 stopped before assertions at one new offscreen-shadow law's `&str` key against a `&String` table API. Root corrected the test argument without changing the ownership assertion. World6 passes221/221 (243 tests outside the target filter), including exact-shadow laws. UI10 passes608/609; only dangling in-flight latency test mount fails, now filled by its owner. UI11 runs next. Focused accessibility/nested-path React tests45/45 and canonical React AgentBridge43/43 pass.
