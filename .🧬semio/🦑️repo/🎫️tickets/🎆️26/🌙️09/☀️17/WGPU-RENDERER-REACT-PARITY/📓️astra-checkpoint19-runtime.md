# Checkpoint 19 Runtime Acceptance

**Checkpoint 20 build:** authored scene/glass/overlay ordering passes UI18 **665/665**, and browser-worker tests pass **156/156**. Native136 completed with **1,353/1,362 passed, nine failed, zero skipped**. The refreshed WebGPU bundle built successfully in14m44s and activation is in progress. The close-journal, Find, and retained Select fixes are integrated. See [compositor evidence](📓️astra-authored-compositor-order.md) and [Native136 integration](📓️astra-native136-integration.md). No new browser acceptance is claimed.

**Current browser blocker:** last-window close faults with `input-wait=InteractionCheckout` at 17:42:41.417 UTC. The wider journey is stopped at this runtime failure; [receipt and sequence](📓️astra-native135-and-physical-journey.md).


## Native135 And Wider Browser Update

Native135: **1,343 / 1,362 passing, 19 failing, 0 skipped**, 35.457 seconds. The partial paired physical journey has advanced through panels, dark/German Settings, Driver axes, pane controls and window focus without a new WGPU console error. Visible Actions text, popup overlap and localization defects remain. See [Native135 and physical journey](📓️astra-native135-and-physical-journey.md) for receipts and remaining failures. Overall parity is still **not accepted**.


## Status

**Settings smoke passed; overall parity is not accepted. Goal and ticket remain open.** Fresh WGPU navigation at17:18:35Z boots and remains responsive through Settings close/reopen, App settings values, General, Driver expansion/collapse, Appearance→Dark→Light, and final Settings close. The final snapshot has no settings tree and an unchecked Settings switch; the actual console has no error. Evidence is under `🗑️generated/astra-runtime/checkpoint19-iab/wgpu-input-wait-*`.

Both activation receipts now identify the same completed Puzzle component `1db04e39cf7c07e23ba875a17ea6e44d76b6de72669999fb170aa32ca87d9d1f`. Current paired tabs are WGPU4 and React5, both measured1600×1000. React3 initially remained1280×720 despite the browser override and is excluded from matched visual comparisons. Fresh React5 is the matched reference. Visual differences remain in World/plan framing, edge rendering, icons, labels, Settings coverage and popup compositing. The observed active Top pane styling works. The full57-step journey, eight docking cases and representative surface checks remain unaccepted.

Latest executed gates: services49/49 GREEN (0.242s; Nx16.6s), including exact physical head/body terminal actor-credit retention; browser-worker156/156 GREEN (latest root rerun Vitest3.49s; Nx8.4s). Native134 ran1362 tests:1329 passed,33 failed, zero skipped (31.306s; Nx1m24s). The I/O lost-wake and local-page cleanup laws, presenter return-only completion, retained Map refresh, five engine attach laws and corrected World/focus fixtures passed. UI12 separately executed the actual A→B-abort→C gate law:1PASS,663filtered,0.015s; Nx31.5s.

The tutorial host/public-ID lookup repair and bounded Map retirement fixture correction await Native135. Sol is upgrading further Shell and Interpreter publication fixtures; Terra audits accessibility and visual ownership. Astra continues the broader browser journey and docking checks. WGPU wasm retry passed4m47s; both prepare/activation flows passed. The earlier failed browser candidates remain recorded below as the measured REDs.

## Earlier Acceptance Sequence

**Current browser verdict: smoke FAILED on a subsequent Settings close.** After Driver open/close and Light selection passed, clicking the painted Settings footer button triggered `worker-present-failed: presentation stalled: phase=Render engine=0 upload=2 gpu-cursor=None upload-progress=(0, 0, 0)` at16:49:39.467Z. Input is quarantined. Evidence: `🗑️generated/astra-runtime/checkpoint19-iab/wgpu-render-stall.json` and screenshot. Sol now investigates bounded pre-submit input progress; the watchdog must remain intact. All earlier partial passes below remain scoped to their observed steps.

Driver collapse now also passes: its seven child axis controls disappear, leaving the six General selectors. The physical Appearance selector opens, commits Light, and retires its popup without a new console error. React and WGPU now share1600×1000, Light appearance, closed Display and Driver sections, and open General settings. The aligned screenshots (`react-general-aligned.jpg`, `wgpu-light-selected.jpg` in the current evidence folder) still show visual parity failures: differing World/plan framing and edge rendering, active pane styling, icon/label treatment, and Settings labels/tab coverage. Interaction recovery does not establish visual parity.

**Latest candidate: Driver expansion passes after presenter repair; full smoke is still in progress.** Rebuilt WGPU booted at16:45:15.611Z; the same physical Driver heading click now publishes all seven axis controls and remains interactive with no new console fault. Evidence: `🗑️generated/astra-runtime/checkpoint19-iab/wgpu-driver-open-green.json` and screenshot. Driver collapse and repeated complete Settings journey remain to be checked. The previous failure below is retained as the measured RED.

The terminal-coupled HTTP credit repair passed the complete48-test services suite (0.240s; Nx14.6s). It holds per-actor credit while an abandoned head worker is still running and releases after actual worker terminal. Native132 is running the full renderer census with backtraces, including the stale presenter source law, coherent shared Shell fixture, exact Map host fixture updates and measured slot budget. Native131's local cleanup law failed in the suite but passed once when replaying the same binary alone; this intermittent failure remains unresolved.

**Latest browser verdict: smoke FAILED after repaired boot.** Fresh boot at16:30:10Z rendered both World views without the original seal fault. Physical Settings close/reopen worked; App Settings published the same four numeric values as React (0.005,0.75,256,10), and General opened. Clicking the painted Driver heading at1600×1000 then quarantined the renderer with `worker-present-failed: prepared frame input authority was stale before submit`. Evidence: `🗑️generated/astra-runtime/checkpoint19-iab/wgpu-drivers-fault.json` and screenshot. Presenter invalidation is the new immediate repair priority. Accessibility mirror activation also needs separate follow-up: its Settings button rectangle is offscreen6px high, and locator click did not toggle the painted control; physical canvas click did.

**Browser acceptance failed; repair validation is running.** Fresh WGPU19 quarantined input at boot with `worker-frame-failed: presented input candidate generation exhausted`. Evidence is under `🗑️generated/astra-runtime/checkpoint19-iab/`. Source investigation found that cancellation before presentation left a sealed input candidate owned by the UI engine. The repair returns the exact candidate on superseded, canceled, explicitly closed, and stale completed frame paths, and preserves the specific seal refusal. Native and fresh browser acceptance of that repair remain pending.

Latest validation: Native128 ran 70 tests, with 67 passes and three valid behavioral failures (Map ownership rebase, withheld HTTP headers, canceled local ReadPage). All three repairs are source-applied. Native129 now runs the full renderer census, including these repairs, the boot candidate laws, and the intentionally failing component-close frame-progress law. The six integrated Ink presentation laws, native decode-worker cleanup, and stalled HTTP-body interruption pass. Browser-worker tests pass 155/155. The earlier complete UI11 suite passed 664 tests. A separate renderer wasm rebuild is running.

The new pre-head cancellation API is source coherent. Its complete services suite passes 47/47 (0.268s; Nx21.4s), including real blocked-socket interruption with pool-slot reuse. A new abandoned-head credit regression was added after that run and is untested. Renderer-level GREEN for the three latest repairs remains pending.

Canonical WGPU activation attempt3 passed in20m13s and React activation attempt2 passed in12m54s. Their selected Puzzle artifact digests differ: WGPU `8cb4a771e102438bf48e7adab4bbafc7513cef4c59d3b06c76fe9b9efd32f10b`, React `3f9f639a5e44dd376c60a396c97dc7de878863f019fbbec45592fd251b27db61`. React's completed component is currently staged. The next renderer-specific paired check will rebuild WGPU through its Nx wasm producer and activate both against the same completed component, recording artifact identity explicitly. Activation alone does not establish source freshness or parity.

Next: finish the boot regression repair, run the combined native and browser checks, rebuild the renderer, then repeat fresh boot and the unchanged eight-step Settings smoke journey. The full 57-step journey, eight docking cases, and representative surface family checks follow only after responsive smoke acceptance. Goal and ticket remain open.

## Earlier Build History

Boot-repair publication completed: renderer wasm Nx5m21s; browser boot/frame-worker generation and checks Nx24.2s; WGPU prepare passed with one selected component, browser support, session and17 fonts. WGPU activation used its existing Nx target with dependencies excluded after those producers completed, retaining React's completed Puzzle component. Both receipts now identify `3f9f639a5e44dd376c60a396c97dc7de878863f019fbbec45592fd251b27db61`. Receipt snapshots are under `🗑️generated/astra-runtime/checkpoint19-wgpu-boot-repair-publication/artifact-identities.json`. The fresh repaired WGPU tab has navigated; its runtime verdict remains pending.

Native130 completed the full1359-test census:1306 passed,53 failed, no skips (29.006s; Nx1m22s). All input-candidate retirement/seal laws passed, as did the Map rebase and withheld-header repairs. The local canceled-page test now passes its publication boundary but fails terminal owner cleanup in the worker. The intended component-close frame-progress law is RED. Additional failures cluster around older scene/Chrome/input fixtures and must be triaged without weakening behavior. The complete list and exact assertions are retained in `🗑️generated/astra-runtime/renderer-native130-full/failures.json`; the full census is not GREEN.

The boot-repair renderer wasm producer passed in5m21s (eight tasks, one cached). Browser worker publication and paired activation against the existing completed Puzzle component are next. This build result does not supersede the previous measured boot failure.

Native129 stopped before execution at a test-only compile error: `PreparedRenderInputRejected` has no Debug implementation, so a new fixture's `expect` call was invalid. The fixture now uses an explicit failure branch, and a fourth candidate carrier law covers after-chrome cancellation. Native130 runs the full census from that coherent source. Native129 receipt: `🗑️generated/astra-runtime/renderer-native129-full/run.log` (Nx3m24s).

Services abandoned-head RED executed48 tests:47 passed, one intended failure (0.257s; Nx14.6s). After the actual worker terminates, dropping the response-head future still leaves its outstanding credit held. Sol owns the repair. Receipt: `🗑️generated/astra-runtime/services-native-abandoned-head-red/run.log`.

The React reference booted and rendered both Puzzle surfaces with its current Settings/Display layout. Snapshot, console, and screenshot are saved under `🗑️generated/astra-runtime/checkpoint19-iab/react-boot.*`; the observed viewport is1600×1000. This establishes only the reference boot, not WGPU acceptance. Its log warns about unrelated staged modules absent from this selected activation receipt.

The standalone wasm attempt stopped after 55.3s at a shared Store compile error: a newly added BackboneMessage::Member variant lacked its receive match arm in the sampled source. Current shared source already routes that variant into member_inbox; the concurrent implementation is preserved. The subsequent full activation reached a renderer wasm compile error, described below. Browser cancellation continuation tests passed 153/153, and generate-frame-worker, check-browser-worker, and check-frame-worker all passed before this activation attempt.

## Build And Gate History

The final Ink presenter integration is source coherent and its neutral scope rows validate. Canonical activation attempt3 now runs with one Cargo job alongside the two-job Native126 regression gate, using their separate existing target/build stores. This deliberate overlap resumes browser acceptance sooner; the earlier root-serial convention is no longer a gate for these independent builds. Attempt3 includes bounded presenter input progress before GPU submission, runtime apply fencing during submission/acknowledgement, and preservation of pending presentation witnesses.

Full activation attempt2 completed successfully. Its renderer wasm was compiled before the final presenter progress integration, so this intermediate artifact is explicitly excluded from parity acceptance. Native126 is now running the final integrated Ink preflight/progress/acknowledgement laws, image-worker owner return, stalled-body cancellation with exact transport lease, and the corrected Map registration fixture. A post-integration canonical activation is required next.

Integration review found that an ACK-only Ink barrier is insufficient: the presenter has already submitted GPU work and consumed its witness before the Shell acknowledgement, and a pending presenter prevents new frame transactions from draining queued Ink work. The final change must preflight before submission and advance the exact queued interaction with bounded progress. The running activation will be checked for whether it consumed that final source; no probe may treat the intermediate ACK-only implementation as accepted.

Native125 executed 64 tests: 59 passed and five failed in 2.289 seconds (Nx 3m29s). The new actual-worker image cancellation, stalled TCP cancellation, and two Ink presentation barriers failed their intended behavioral assertions. The Map test still failed at receiver setup. The Ink production barrier is now implemented after that RED and parses; its GREEN remains pending. A second full WGPU19 activation is running with the compile corrections, central browser cancellation gate, and Ink barrier. Native image-worker and transport repairs continue independently.

Run the canonical WGPU Puzzle3d development activation, verify its receipt and selected Puzzle artifact digest against React, and reactivate React if those artifacts differ. Both current listeners respond on React6313 and WGPU6213, but server availability does not establish candidate freshness.

The first paired journey retains the existing 240-second boot budget and 2400ms settle and covers boot, tour dismissal, opening settings, App and General settings, opening/closing drivers, and closing settings. Preserve screenshots, actual consoles, selected artifacts, geometry, and action outcomes under generated/astra-runtime/checkpoint-19-smoke. Only a passing responsive candidate proceeds to the full 57-step main journey, eight docking cases, and the representative surface families. Do not extend waits to hide renderer stalls.

## Outstanding Beyond The Smoke Gate

The native transport audit now confirms that the shared component-close gate suppresses new input and frame construction for unrelated windows while one exact close remains pending. Sol is adding a fail-first native control-flow law and a neutral Chromium oracle; Terra is reviewing which GPU phases require exclusive presentation so the repair cannot revive the closing host. The selected HTTPS backend currently reports a real 15-second per-read deadline rather than immediate interruption. This remains an explicit follow-up, separate from the already-passing plain HTTP body case.

Browser skill setup is active for a fresh in-app visual check at the same 1600×1000 viewport as the parity probe. Tabs are prepared but have not navigated to the intermediate artifact. Canonical activation attempt3 has passed the renderer wasm target and is still building its Puzzle component dependency. Browser UI acceptance remains unperformed.

Native per-request hanging transport cancellation, reference worker-return buffer disposal, and any Map behavioral failure remain independent repair work. Flow26 passed 251 of 253 tests; the concurrent composed child replication implementation and corrected same-byte reload default-state assertion require a fresh Flow gate. Back-to-back window configuration dispatch remains separately unresolved. Fresh browser acceptance must distinguish these shared framework issues from WGPU-specific parity defects.
# Activation Compile Repair

## Integrated Native126 And Browser Validation

Boot diagnostics currently misclassify every Shell input seal refusal as generation exhaustion in `FrameBuildPhase::Chrome`; the Shell also returns a distinct retained-candidate-seal error. Source inspection found that unpresented preparation cleanup does not include its input candidate in terminal ownership, a possible orphaned seal after cancellation; the exact boot cause is under regression investigation, not yet proven. The actual browser DOM and screenshot measured 1280×720 despite the requested viewport override; record this as the observed viewport for this failed run.

The current browser-worker suite passed 155/155 across 11 files after adding the local-page neutral fixture and component-close event-loop oracle (Vitest5.57s, Nx11.6s). These passes do not override the measured browser boot failure.

Fresh WGPU19 in-app boot is **FAILED**, not accepted. Navigation at `2026-09-21T15:59:42.630Z` reached the ready stage, then console at `15:59:48.279Z` reported `worker-frame-failed: presented input candidate generation exhausted`. The page quarantines the surface and refuses input. Evidence is retained under `🗑️generated/astra-runtime/checkpoint19-iab/`. Sol now owns the boot regression and Terra audits input-candidate begin/seal/ACK conditions independently; window-close fairness production work is deferred until this boot blocker is repaired.

Native128 executed 70 laws: 67 passed, three intended behavioral failures (2.349s; Nx 5m8s). Map cleanup completed and exposed `engineRetired=false` after arena index2→3 rebase, while the local Map gesture retired correctly. Root repaired EngineCanvas lookup to use the exact host ID and same-component-host identity. Local ReadPage cancellation published 16384 bytes instead of zero, and withheld HTTP headers blocked the sibling fetch. Both transport failures are now assigned for production repair after their measured RED.

Canonical WGPU activation attempt3 completed successfully in 20m13s (20 tasks, seven cached), including final Ink presenter integration. Its selected Puzzle artifact is `8cb4a771e102438bf48e7adab4bbafc7513cef4c59d3b06c76fe9b9efd32f10b`, rebuiltAt `1790006364002`. React activation now runs to replace its older `18088a78…` artifact. The fresh WGPU in-app tab has navigated and initially reports `renderer-module 5%`; no boot or paired acceptance has been claimed.

Native127 stopped at test compilation (4m25s), before executing any law: the newly added local-file barrier fixture needed crate-qualified names. Those test-only qualification errors are corrected. Native128 now includes the corrected Map CPU-only cleanup, withheld HTTP headers, and in-flight local-file cancellation alongside the 67 passing owner/presentation checks. Production for these three new behavioral regressions remains held until a measured RED.

Native126 executed 68 tests: 67 passed and one Map fixture cleanup failed (2.409s; Nx 4m59s). All six integrated Ink presentation laws, actual running-worker reference cancellation, and actual stalled TCP-body cancellation passed. The Map fixture reached its retained receiver but awaited an external component close bridge that its CPU-only harness did not drive; this is not yet a behavioral retirement result. Native127 runs after bounded CPU cleanup and explicit fixture bridge handback, with the separate withheld-head transport regression added. Map production remains unchanged pending the behavioral RED.

The final browser-worker census after the Ink fixture/schema and presenter changes passed all 154 tests in 11 files (Vitest 10.06s; Nx 19.0s), cache disabled. Receipt: `🗑️generated/astra-runtime/browser-worker-final-ink-presenter/run.log`. Canonical WGPU activation attempt3 is still running; fresh browser acceptance remains pending.

The browser suite now passes all 154 tests across 11 files (test time 3.13s; Nx 11.0s). It includes the third cancellation regression: a response awaiting authority handback cannot be fetched again. The previously failing frame-deadline source assertion used an obsolete single-line delimiter after formatting; its extraction now ends at the same function call regardless of line wrapping. Production scheduling still separates future deadlines from runnable worker continuation.

Native123 stopped before test execution on the shared composed-member Store sync integration. Native124 reruns after an explicit per-document boundary repair; see the shared-member build report. The next browser activation remains pending these compile gates.

The canonical WGPU19 activation failed after 7m35s. Seventeen prerequisite tasks completed, but wasm compilation rejected the new asset-decode serialization error handler: `js_error` takes a string slice, not `serde_json::Error`. The handler now passes the formatted error by reference. This is a compile repair; the corrected wasm artifact and paired browser acceptance remain pending. The first standalone build's earlier shared `BackboneMessage::Member` match interruption is independently resolved in current source.

Native123 now runs the existing retained-owner gates plus the new actual-worker reference cancellation, stalled native TCP body, and two queued Ink presentation laws. The Map fixture now publishes the retained hit targets before identifying its receiver; its substantive retirement assertion has not yet passed. Browser cancellation handback scheduling receives a separate regression review before rebuilding the generated worker.
