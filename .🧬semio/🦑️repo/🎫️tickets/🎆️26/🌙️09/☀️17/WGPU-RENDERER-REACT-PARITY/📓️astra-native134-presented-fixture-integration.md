# Native134 Presented Fixture Integration

Native134 remains RED: 1329 of 1362 tests passed, with 33 failures and no skips (31.306 seconds; Nx 1m24s). Native132 had 1309 of 1360 passing with 51 failures. The complete current failure list is `🗑️generated/astra-runtime/renderer-native134-full/failures.json`.

The I/O contention law, canceled local-page cleanup, presenter return-only mailbox law, retained Map refresh, five engine attach laws and corrected World/focus/modal fixture laws passed. The tutorial semantic-point test now reaches its intended production failure: a generated host ID is incorrectly compared with the public surface ID. Map key replacement still fails gesture retirement. One older watchdog source assertion needs the extended input-progress signature. The remaining failures cover additional Shell/Interpreter publication fixtures and behaviors; they have not been accepted.

The first WGPU build stopped at a stale lockfile check in 7.8 seconds. The existing Nx lockfile-check now passes; the native build updated the workspace lock for the new services test dependency. The canonical WGPU producer has been restarted without changing its lock enforcement.

The retained World sequence fixture assigned `dock_window_plan` after sealing and acknowledging its hit candidate. Window activation now correctly reads `presented_input_geometry`, so that late assignment never became interactive. The fixture now establishes its geometry before the same multi-document paint and acknowledgement.

The multi-pane focus sequence painted each document as a separate complete candidate, then manually marked every pane accessible. That cannot restore the earlier documents' presented authority. The fixture now paints and acknowledges all panes together and keeps that candidate's actual InputState.

The retained modal sequence now publishes another complete multi-document candidate after opening its menu, so the modal flag shares the acknowledged geometry. Nine accessibility targets in the Shell input fixture now come from the actual published accessibility projection, including its assigned generation and node ID; they no longer assume generation 1. The helper is compiled only for tests and reads the same projection used by the browser mirror.

These changes affect test admission and publication only. Production input authority is unchanged. The new engine attach fixtures also follow Interpreter admission and resolve generated host IDs, while Map lifecycle fixtures now seal and acknowledge their retained hits before checking gestures and bounded retirement.

The browser-worker integration suite passed all 156 tests across 11 files at 19:06 local time: Vitest 3.49 seconds, Nx 8.4 seconds. Receipt: `🗑️generated/astra-runtime/browser-worker-input-wait-integration/run.log`. This verifies the current browser contracts; it does not validate the Rust presenter or replace the failed browser smoke.

After the executed tutorial RED, `resolve_tutorial_surface_point` now looks up the engine by its generated host ID and validates the point against that engine's public surface ID. The fixture additionally rejects another surface ID and an internal host ID presented as a public address. GREEN remains pending.

Native134 also exposed an intermittent test-only file-preference read: it searched for `dark` while its asynchronous fixture writer could still be overwriting the file. The test now waits for the actual flush owner's terminal state before its independent serde_json read, retaining the existing one-second bound. Its temporary directory uses the runner's artifact directory and is removed after terminal handback. The production native preference path uses a separate retained file-document service and is unchanged.

The next native gate includes the remaining Map lifecycle failure, tutorial lookup, exact presenter gate laws in the UI crate and further Shell publication repairs. A real pending HTTP body worker now has a separate fail-first actor-credit test; its services run is in progress.

UI12 executed the actual prepared-gate A→B-abort→C law: one pass, 663 filtered, 0.015 seconds; Nx31.5 seconds. The WGPU build retry passed in4m47s. Prepare and activation passed; the completed shared Puzzle component changed to `1db04e39cf7c07e23ba875a17ea6e44d76b6de72669999fb170aa32ca87d9d1f`, so React was prepared and activated against the same component. Both receipt identities are retained under the input-wait publication directory.

Fresh WGPU navigation at17:18:35Z booted without a console fault. Physical Settings close/reopen passed, including the four App numeric values. Full smoke remains in progress. The old browser tabs had been closed externally; fresh paired tabs3/4 use the same selected browser and1600×1000 viewport.

Services pending-body RED2 executed49 tests:48 passed, one intended failure (0.266 seconds). The actor credit became available before the abandoned physical body reader reached terminal. Sol now owns the production repair. RED1 stopped at a missing test-future pin; that compile error was corrected before RED2.
