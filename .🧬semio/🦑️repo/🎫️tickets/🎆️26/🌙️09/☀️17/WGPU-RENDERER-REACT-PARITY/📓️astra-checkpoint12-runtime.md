# Checkpoint 12 Runtime Receipt

The canonical WGPU activation completed all 20 tasks in 13m43s. React activation6 completed all 14 tasks in 5m41s. All five sealed WGPU artifact hashes remained unchanged after React activation and after this browser journey. This is an actual paired Chromium run at 1600×1000 DPR1 against the puzzle3d reference and WGPU dev hosts, not report replay.

The 15-step journey failed with one primary runtime crash. Boot, introduction dismissal, opening Settings, and the app Settings document completed. Selecting General delivered its 40-node document, then the worker panicked at 54.100 seconds in `retained_tree_index`, UI WGPU paint line411: `attempt to subtract with overflow`. The call chain traversed retained tree section/node painting and actual `Ui::frame_into_step`. The eager `then_some` argument evaluates `len - index - 1` for a reversed cursor even when `index >= len`. Sol Window owns the lazy bounds guard and terminal/empty reversed-paint regression laws.

After the panic, published chrome remained at generation28 and renderer readiness became null. General Appearance and Drivers controls never became available. Settings close, window close/reopen, and fullscreen assertions consequently failed; camera gestures published no new actions. These later outcomes are crash cascades, not independent evidence against the pending window, root Toggle, or camera fixes. Drivers-close resolving absent is not a pass. Repeated no-threads mutex panics follow the original paint panic and are not its initiating cause.

Checkpoint12 contains S1 scene shading and encoded attachment blending, the initial Section disclosure/upward-flow packet, and the first bounded publication-refusal packet. It predates the root Toggle anchor-close semantic fix, independent publication cohort/action-credit atomicity fixes, camera2, and Select P3. Those changes require a fresh artifact and runtime run.

Independent gates completed: UI21 passed616/616 with zero skips; the final shader/render suite passed136/136 with zero skips; the permanent scene-shading-pixel-check target passed20 actual Three and20 actual production-WGSL WebGPU rows, all RGBA byte-exact. UI21 missed this actual reversed paint terminal path and does not establish browser parity. Native20 did not compile due to a missing DockNode test import, now repaired; World12 passed225/226 selected tests with one test-only float comparison repair pending rerun.

Generated evidence is under `🗑️generated/astra-runtime/paired-checkpoint-12/`: `steps.json`, `parity.md`, `geometry.md`, `cameras.md`, `latency.md`, per-step screenshots, and `wgpu/console.txt` (first panic line2504). The post-browser hash receipt is `🗑️generated/astra-runtime/checkpoint-12-post-browser-hashes.json`. The goal and ticket remain active.
