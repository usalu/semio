# Layout Catalogue Boundary

Current read-only source confirms the previously audited renderer-to-Layout mismatch remains. Both Canvas2d producers send drag-over with MIME `types` and drop with raw JSON `dragData`. Layout command decoding still aliases `drop_kind` into required `kind`, so actual renderer arguments cannot reach either typed command. The existing retained-input fixture uses a fabricated generic catalogue item and stops at dispatch; the Layout command fixture supplies kind itself. Neither proves the actual catalogue-to-command boundary.

The intended repair must preserve the browser data-store restriction: normal drag-over may disclose MIME types but cannot read the source JSON. A domain-neutral renderer must not synthesize Layout-specific kind. The Layout action boundary should consume the canonical raw drop payload and validate it against its four authored catalogue kinds (page/rect/text/image). Drag-over needs a truthful generic admitted catalogue preview or an explicitly schema-backed preview metadata channel, rather than assuming raw JSON is available during browser drag-over.

The pure CanvasDragOver handler is inert, but the retained command work at editor/🦀️.rs:606 owns the actual transient preview. It projects the pointer through the addressed window camera and writes LayoutWindowTransient; leave and drop clear that owner, with page drops changing the addressed active page. Any repair must test this retained work, not only the pure handler or typed command decoder. Existing direct typed tests and command round-trip fixtures will need handcrafted updates if the canonical payload changes.

The canonical native task is @semio-tech/layout-layout-rs:test. Dedicated Draw, Layout, and Note activations remain scheduled after checkpoint16. No production change or runtime acceptance was made during this inspection.

Reinspection corrects the earlier preview recommendation: the existing Layout catalogue already declares a second empty-valued MIME key, application/x-semio-catalogue-kind.{page|rect|text|image}. Both renderers preserve this types list during drag-over. Root is therefore retaining the typed domain commands and repairing only Layout’s app action decoder to consume that canonical MIME witness and strict raw drop JSON. No domain kind parser is needed in either renderer. Fifteen neutral cases now cover all four kinds, missing/ambiguous/unknown MIME, malformed/extra-field drop JSON and the refused fabricated top-level kind. A mounted Canvas2d React oracle is executing the exact valid envelopes and forbids getData during drag-over. Native Layout1 is the fail-first bridge run.

## Retained Publication and Gate State

Added a retained command law for all four neutral catalogue kinds: actual app action decoder, registered addressed Blueprint window, preview transient publication, peer-window isolation, one document creation, and terminal preview retirement. It awaits execution. Catalogue1 stopped before tests on a test-only Label conversion, corrected to LocalizedLabel. Catalogue2 accidentally used a cold intermediate directory and was stopped after about eight minutes while still compiling dependencies, by its verified owned process tree only; no external build was touched. Catalogue3 reuses native-trace-isolated/build and retains native-layout target/metadata owners for the intended RED witness.

Terra found an additional terminal gap: both real renderer drop producers dispatch a nonempty raw payload but omit a leave on malformed drop; strict app decoding can leave an earlier preview visible. Strict bridge refusal remains the intended creation contract while generic drop-end cleanup ordering is audited. No malformed-drop runtime claim yet.

Catalogue3 and4 still stopped before execution on the producer test label: plugin `Label` is the localization-checked WGPU label owner, distinct from both LocalizedLabel and the bounded UiContract Label returned by ui_label. Inspected the actual constructor; the fixture runtime string now uses plugin `Label::data(kind)`. Catalogue5 is the corrected behavior run. These compilation attempts are not RED behavioral evidence and no production decoder change has been made yet.

Catalogue5 stopped in a concurrent SPR routing edit: RouteFields had been added to PagedAppCommandDecodeState before its step match arm was present in the compiler snapshot. Source inspection immediately afterward found both step and close handlers complete, so no external code was changed. Catalogue6 retries the coherent shared source.

## Reproduced Failure and Decoder Repair

Catalogue6 finally executed the new neutral law and failed exactly at page-over: `canvasDragOver` arguments do not decode, missing field `kind` (one failed,374 outside focused selection). This is the first behavioral RED witness. Root then repaired only the app action decoder: drag-over requires base MIME plus exactly one supported secondary kind MIME; drop parses the owned JSON payload and requires precisely one supported kind field. Canonical typed commands retain their kind string. Removed the obsolete drop_kind alias and zero coordinate defaults. The producer and decoder share the four-item catalogue roster; no renderer parses domain JSON. Native36 full renderer census now runs independently; Layout green gate follows it in the serial compile lane.

Catalogue7 passes all5 selected laws (370 outside selection): neutral decoder matrix, real catalogue producer, all command round-trips, host-control bridge, and the new real retained app preview/drop/window-isolation law for page/rect/text/image. A focused replay with successful-test output enabled is collecting the required runtime DEBUG receipts; it does not broaden the acceptance claim to a physical browser gesture. The React producer oracle previously passed12/12.

Catalogue8 did not execute a test: the wrapper rejected --success-output. Corrected Catalogue9 used the already-supported libtest --nocapture forwarding and passed the retained runtime law; console receipts independently show page, rect, text and image each published the addressed preview, created exactly one item and retired the terminal preview. Evidence: native-layout-catalogue-9-runtime.log. This remains an actual native app-runtime law, not physical browser acceptance.
