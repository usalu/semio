# Checkpoint8 Window Transfer Failure

Both canonical activations passed before the paired seven-step journey: WGPU20tasks and React14tasks. The five sealed WGPU/browser artifact SHA256 identities remained unchanged after the window and fullscreen journeys.

Both renderers close their two windows to an empty dock. React creates one window from the Display transfer handle; WGPU stays empty after an eight-second outcome wait and many new registry generations. The real semantic transfer handle now exists, confirming the projection repair reached this artifact. This does not establish successful transfer.

The actual WGPU input log records a TreeDragHandle press at290.8,112.8 in retained window framework.display.windows, then release at800,500 with hit=None. The later screenshot has the panel at a different vertical position; its handle coordinates must not replace those from the actual input trace. The log also records a deferred noteShellCommand refusal because the panel identifier has no declared application window kind.

Current Shell source returns from retained pointer press before the later decode_window_template_drag/begin_pending_dock_drag branch. Retained move publishes world-catalogue previews but has no analogous new-window dock transfer bridge. Retained pointer release also requires a current hit owner, whereas move honors retained capture. These are concrete production-path gaps; Sol chrome owns a full producer→host gesture→dock commit law and correction, including capture retirement and outside cancellation.

The fullscreen-only journey fails before browser fullscreen: matched OS keyboard commands reach application dispatch and report “os commands must be dispatched by the shell”. Sol window owns canonical shell routing and a host-ingress law. No browser activation workaround is justified by this failure.

The source-repaired shader suite passes135/135 with0skipped. Its Naga law parses and validates all three runtime plus all three canonical WGSL variants. UI8 remains609/609 with0skipped. Renderer10 stopped on22 compiler errors; owner repairs are source-stable and renderer11 is running. No renderer10 assertions ran.

Artifacts: 🗑️generated/astra-runtime/paired-checkpoint-8-windows, wgpu-checkpoint-8-fullscreen, checkpoint-8-artifacts-post-journey.json, ui-render-native-tests-4.log, renderer-native-tests-10.log, renderer-native-tests-11.log.
