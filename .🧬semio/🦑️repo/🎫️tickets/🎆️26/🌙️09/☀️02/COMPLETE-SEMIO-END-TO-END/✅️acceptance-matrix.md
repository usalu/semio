# Complete Semio End-to-End Acceptance Matrix

Root checkpoint: **25981 GREEN4** exact built-tree owner, **91536 GREEN15** closed-member laws, and **77519 GREEN3** runtime typed/rejected-tree closure through exact handback, saturated admission and full384-page re-admission. Flow10 repeats now pass the first exact identity/adoption law; the second first exposed and then closed canonical-fixture retirement before finding `DuplicateWidget`'s lone snake_case wire schema. That schema and every neutral fixture are now camelCase without compatibility support. The next repeat was infrastructure RED before Flow on an in-flight GLTF taxonomy mount, so the full ten-law packet remains unaccepted. Selected factory **68325 GREEN2** and canonical dictionary follow-on **8902 GREEN2** share one hash-bound kernel binary; the public request-owned open operation is source-complete for Flow plus seventeen explicit denials but still lacks a final post-repair native pass. GIS typed codec/proposal integration is **51355 GREEN2** without hub authority/commit; hub exact20 is active. Full all-plugin opening/runtime and end-to-end collaboration remain unaccepted. See `📓️root-exact-built-tree-retirement.md`, `📓️root-flow-child-authority.md`, `📓️root-runtime-tree-retirement.md` and `📓️root-verified-parent-dialect-authority.md`.

Date opened: 2026-09-03  
Ticket: `26/09/02/COMPLETE-SEMIO-END-TO-END`  
Goal: `🎯r2603`

`PASS` requires current runtime evidence from the exact production path. `PARTIAL` means useful implementation or component evidence exists but the end-to-end journey is not proved. `BLOCKED` names a concrete failing prerequisite. A zero-test or skipped suite can never produce `PASS`.

Host persistence remains source-only: unused runtime facade removed, media projection oracle 49400 (4 positive/16 hostile) and typed persistence correlation oracle 89395 (4 requests/16 hostile requests/8 hostile events/15 traces) pass. Native laws are registered but unrun; the live Home/draft IO cutover and retained codec-history ownership remain RED. No area is promoted by these foundation gates; see `📓️sol-os-host-async-persistence-frontier.md`.

Retained dictionary ownership is now a bounded **GREEN2 native sub-boundary (70042)**: 33 real SPR histories plus 11 authority/7 UTF-8 scratch/9 pending-copy and ID scratch traces ×3 grants, pinned visibility/ranges and complete retirement. Source50041/68455/83730 are separately green. Receipt `member-history-dictionary-exact/exact-cargo-laws-DbP7cw/00` records one kernel binary and two exact successful assertions. This does not promote host IO or public member opening: selected factory/schema/space binding, pin membership, typed history hydration and final child transaction remain RED. See `📓️sol-retained-history-dictionary-owner-protocol.md`.

Selected-factory/schema linkage is **GREEN2 native** for its bounded private selection/handoff boundary: required declaration table, same-literal macro emission, empty/direct tables and the selected-schema retained owner are mounted. Final registered **68325** passes both exact laws; receipt `member-factory-identity-exact/exact-cargo-laws-rzrlCE/00`, SHA-256 `1c09a11c36663be9d06f8f806ff36aa19075e4f3beea4b91d84d743be38b1fb4`. Follow-on dictionary **8902 GREEN2** proves exact canonical flags plus 36 owner histories. See `📓️sol-member-factory-selected-identity.md`; no public member/space/pin/typed hydration promotion.

The public loader handoff remains **PARTIAL**. The formerly absent `MemberFactory::Open`/owned `begin_open` API now generates a closed 18-arm operation: Flow alone consumes the incremental snapshot and retained selected-history witnesses; the other seventeen arms deny without consuming or publishing. Receipt `5zy0bu/00` exposed and current source repairs the `REC_COMMIT`/padding Replay defect while retaining an encoded persisted-history denial. The Stdio-owned JPG generator now resolves all 48 generated Rust references against the sole physical `📸️jpg` authority, with registered freshness GREEN. Subsequent receipt `71uBL3/00` exposed and closed an over-broad identifier rewrite. Attempt `2Whpql/00` was externally deleted during execution and is infrastructure-only; the shared exact runner now holds a process-bound ticket-output lease, proven by GREEN25/513 registered assertions. Final cold leased session 87126 retained receipt `54y8Pd/00` through its bounded one-hour build timeout with no diagnostics/discovery; immediate warmed session 48467 retained receipt `iK79bZ/00` and stopped before discovery on the separately owned missing Stdio document mutation barrel `🏷set-style-name/🦀️.rs`. No final runtime pass or public parent publication is claimed. See `📓️sol-public-member-open-handoff-prerequisite.md`.

Flow's retained `addWidget` child factory is **SOURCE GREEN / NATIVE BLOCKED**. Final registered source session 0ccc24 passes the independent AJV model for two accepted and 24 hostile cases, exact inserted kind/position, canonical checkpoint bound and production EN/DE progress; canonical launch generation 6a3be5 and final freshness 05e527 pass. The factory owns one migrated key, one bounded first-step proof and one Child-only publication lane, reuses the typed planner under the retained instance session owner, and closes the legacy dispatch path. Earlier exact-two receipts stopped before discovery on moving Stdio LAS/Semio barrels. Final-current-source session95845, receipt `eDmuyC/00`, passes its oracle then stops at Cargo status101 before discovery on PDF1.4 `ReplacePageText` descriptor/source-owner mismatch plus an absent bachelor-thesis PDF fixture. No native law executed; public `LoadChildren`, trusted bootstrap, browser/WGPU activation, child genesis and restart/reload are not claimed. See `📓️sol-flow-add-widget-retained-factory.md`.

Current provider qualification: linked public API session 26201 selected one law and failed on duplicate dialect claims. The validator/codec-extension repair has current independent oracle evidence (45391: eight claim vectors, 26 receipts/hashes, 13 hostile/no-partial cases), but its seven Rust provider laws remain unexecuted. Strict artifact admission has current independent oracle evidence (30083: 15 vectors/39 first-party roots) and fresh launch generation (41018/89770), but its four actual builder laws remain unexecuted. No area is promoted by these source-only repairs. Details: `📓️sol-native-openable-catalog-provider-v1.md` and `📓️sol-framework-strict-artifact-identity-admission.md`.

| Area | Required journey | Current state | Current evidence or blocker |
|---|---|---:|---|
| Repo control plane | Configured repo MCP initializes, lists/reads goals, and opens/closes tickets | PASS | Exact `.mcp.json` command now starts the owned server with zero argv; `repo-mcp:test-quick` passes, official SDK lists 8 resources/6 tools and reads `repo://goals`, and an independent raw stdio probe read the same resource. This already-running Codex host retains its pre-fix failed connector cache, so its built-in resource call still needs a host reconnect. |
| Honest quick gates | OS dev, hub TS, hub admin, renderer, MCP quick targets each execute nonzero behavior tests | PARTIAL | Independently rerun with `--skip-nx-cache`: OS dev 4 files/75 passed, hub admin 2 files/10 passed, hub TS 1 file/2 passed with only the explicit full E2E gated, renderer quick 1 file/2 passed, and renderer fundamental 1/1 passed. MCP binary resolution/executable contracts pass 5/5; all four absence skips are removed and a missing binary exits 1 before Vitest. Atomic restore clears the former store sync blockers: a fresh MCP binary builds and its quick suite passes 41/41 across five files, including exactly 30/30 real process-backed cases with no absence skip. The area remains partial because the explicit hub E2E is still gated and no integrated release target exists. |
| Registry | Generated plugin/launch catalogs are fresh and taxonomy check has no unresolved diagnostics | BLOCKED | The strict source/build-root gate and fail-atomic executor exist. `stdio` now has green focused laws for the required PackageDescriptor codec and carried Cargo `packageId`, the shared descriptor producer, duplicate-aware owner/marker parsing, self-contained marker verification, pinned exact bytes, registry generation/freshness, and the hub's strict projection. The stable isolated clean-root run still fails before publication: `wasm-component-ld`/wasmparser rejects the monolithic component because its function count exceeds one million; cleanup proves no row or marker escaped. Follow-up measurement shows the 176-variant/77-method `StdioApps` closure expands linearly to 1,702,133 bytes, 254 function tokens and 13,728 match arms rather than pathologically emitting one million functions, while the total crate expansion legitimately exceeds 128 MiB. Actual N=0/2/8 linked-core growth remains unmeasured. The 2026-09-04 source refresh proves the historical removed `DirectoryClient` token-call blocker is gone, but no fresh linked-core/count/RSS terminal exists; optimized-profile and sharding benefits remain nonclaims (see `📓️terra-registry-monolithic-wasm-frontier-refresh.md`). The monolithic Rust suite is separately red on 1,852 artifact-owner serde obligations. Native codec admission correctly remains empty at six declared/zero executable registrations, and the future nonempty bundle still lacks the full explicit factory/codec/capability/extension/format join. After a real root publishes, the remaining catalog failures still require dependency-ordered repair of absent pairs, CAD placeholders, semantic pair disagreements, and the extension `describe` seam before one runtime-loaded immutable provider generation can pass. |
| Plugin host lifecycle | Guest actors, replay owners, relays, scheduler jobs, and effects close without leaks, deadlock, or lost completion | PASS | The registered lifecycle gate passes the independent neutral oracle 6/6, repository production subject 5/5, exact-one selection and execution of 19 lifecycle laws, the complete 245-test library suite (244 passed, 0 failed, 1 ignored), and the all-feature check. Replay outcome collection is order-safe and survived 20 repeated runs across 1/2/4/default workers. This is host-lifecycle acceptance only; verified catalog activation and descriptor-owned command execution remain separate blockers. |
| React OS | Registered launch boots the shell, loads complete live catalog, and opens every declared surface | PARTIAL | Canonical Home/Studio live-manifest identities drive routing, switching, presence telemetry, chrome, panels, and directory gating without app-order fallback; missing or ambiguous identities fail explicitly. The browser backbone now owns hub-bound opens even when the Rust worker resolves and performs authenticated open-plan issue, receipt-only SocketGrant exchange, credential-free WebSocket dial, and exact-Session activation. Its current-source focused packet passes 6/6 and its Chromium runtime proves zero activation before Session. Ordinary ShellHost startup still has no complete verified installed target because the production native codec catalog is empty, so no real-hub successful open is claimed. |
| Native OS | Registered native launch agrees with React on the same surface and collaboration contracts | BLOCKED | The source path consumes strict intent → plan → one-use receipt exchange → SocketGrant authority and keys actors/surfaces by full document scope. Seven native laws, one MCP law, a three-epoch process oracle and a registered launch gate exist; the server eight-law and launch-freshness gates pass. The old Norm 816-error preflight is historical: focused graph, derive and UI suites pass, and a later Norm library check passes before its newest identity cutover. Post-cutover library and actual-factory gates remain pending. The native provider's 26-receipt stdio source closure is not yet a runtime-qualified installed catalog, and no full native/two-user launch succeeds as evidence. EN/DE status/cancel and real native presence remain unproved. |
| Norm surfaces | All fifteen artifact families assemble, render, edit and localize through real registered factories | PARTIAL | Post-cutover canonical library session 13074 exits 0 after 15m41s. Package builder order and all fifteen document identity trees are corrected; 2784 observed the namespace defect first. Public tests use actual plugin factories and compare manifest, schema, dialect and definition registry. The original 90-body fixture missed framework history; 31598 observed that omission, and 32027 passes the exhaustive 30-app/120-body AJV oracle plus five hostile vectors. The two public targets are undergoing a metadata-only compile check; no factory/config native assertion has run. Interactive typed preparation/publication, EN/DE rendering and native activation are not accepted. |
| Artifact opening | Space create/open selects a declared surface and binds the exact hub document | PARTIAL | D0/D1 now expose authenticated catalog-gated open-plan issue plus receipt-only SocketGrant exchange and revalidate the exact selected surface, descriptor digest, catalog generation and row, directory revision, checkpoint, actor and subject before Welcome and on live ticks. The browser Worker strictly validates the complete locally installed package/artifact/surface target, caps and wipes both HTTP responses, dials without URL credentials, and activates only after the matching Session. Current-source evidence is the 6/6 focused packet, independent AJV/Node oracle, and real Chromium synthetic-server runtime. Artifact state and BroadcastChannel ownership are keyed by full UTF-8 `(spaceId,documentId)` with a disjoint local namespace and a two-space collision law. Production startup still supplies zero native codec bindings, so the catalog remains empty, readiness fails closed, and a real browser-to-real-hub success journey is not accepted. |
| Presence UI | Two authenticated clients see the authoritative document roster and accessible status | PARTIAL | Hub/document-wide presence and React `PresenceBar` wiring exist. The v1 server socket derives the actor from a one-use exact-scope grant, rejects forged mutation actors, and fences durable session/share revocation before authority actions. The browser D1 path no longer emits a socket actor or flushes queued mutations after Welcome alone; only an exact matching Session activates and flushes, while mismatch/failure clears stale actor state. A real two-user browser/native presence journey remains unproved because ordinary startup has no nonempty verified codec catalog and native transport remains separate. |
| Document descriptor | Authorized discovery returns one immutable schema/open identity that survives restart and isolates equal document ids across spaces | PARTIAL | Schema-first Rust/TypeScript/JSON forms, append-only `document.announced`, all three backend projections, authenticated reads, and immutable Hello validation exist; focused SQLite/hub/cross-language laws pass. However external creation still supplies the full descriptor. The accepted greenfield boundary replaces that command with a server-owned create intent resolved through the verified catalog/codec, while existing opens consume the immutable stored descriptor through a one-use plan. Full restart/equal-id cross-backend runtime and server-derived initial pair publication remain unproved. |
| Artifact bootstrap | Public cold transfer is a bounded, descriptor-bound canonical `(pack, spr)` pair rather than DB `.spk` | PARTIAL | P2-D is now independently accepted for the exercised SQLite-directory/filesystem-CAS topology: the final-source two-child law proves a complete successor reserve → epoch advance → stage → publish makes an older post-validation conditional delete fail with the exact stale-fence error, and fresh handles recover the published checkpoint plus exact pack/SPR. The isolated Nx packet passes 16 CAS laws, one maintenance law, the all-feature hub check, and the independent schema/Node oracle. PostgreSQL/Neo4j and Windows remain source/compile-only qualifications. The overall journey stays partial because catalog/native provider, scheduled checkpoints, the authenticated active-pair route, tail barrier, and public cold-transfer runtime remain. |
| Share authorization | Share grants are space+document scoped, least-privileged, expiring/revocable | PARTIAL | SQLite/PostgreSQL/Neo4j code now uses 256-bit scoped expiring/revocable grants; document sockets make grants read-only and revalidate live revocation. DB and hub compilation are clean, and after the DB lifecycle repair the complete focused hub law passes 1/1: valid scoped share gets `Welcome`/`Session`, while tokenless, cross-space, revoked, and write attempts retain least-privilege denial behavior. Cross-backend runtime coverage remains incomplete. |
| Directory privacy | Public discoverability is structurally separate from member/author authority and identity-bearing events | PARTIAL | Lane 1's registered `space-public-boundary-check` session 99284 exits 0: four neutral projections, twenty-one hostile mutations, three exact real REST/socket laws, TypeScript narrowing, and the hub all-feature check. The required `public/member/author` discriminator excludes private identity/currentness fields from public data. Public raw events, document currentness/blob access, and unscoped directory `headSeq` heartbeats are denied. Private nonmembers remain 404; D1 alone grants document-open authority. Cross-backend runtime, lanes 2–5, and a real browser two-user journey remain unproved. |
| Collaboration | Two users create/open/edit/checkpoint/reconnect/restart one persisted artifact | BLOCKED | Protected local bootstrap/readiness, server SocketGrant S1+S2, catalog-gated open-plan issue/exchange, and the browser D1 consumer are independently implemented. Browser broker/admin relay and deployed-worker freshness are green; the browser Worker has a bounded offline queue but delivers nothing before an exact Session. Native and MCP entrypoints retain separate authenticated-carrier work. A scheduled checkpoint, nonempty verified first-party codec provider, native supervisor, and actual real-hub two-user reconnect/restart journey remain absent, so synthetic browser runtime plus server unit laws are not promoted to collaboration acceptance. |
| Short outage | Commands retain order or receive explicit cancellation; reconnect flushes with no unrelated event | PARTIAL | Queue overflow/close can still silently drop intent and a quiet reconnect need not flush. Clients install the descriptor-bound public pair atomically, P2-B journals checkpoints/retention, and P2-C now turns both live lag paths into authorized typed control plus close 1013; the real-socket lag/cross-space gate passes 2/2. Native/wasm preserve a deduplicated local outbox. Exact checkpoint request/selection, checkpoint-barrier-to-tail cursor mapping, scheduled live checkpoints, and a full reconnect convergence journey remain absent. |
| Hub persistence | Directory/document contract passes for filesystem/SQLite/PostgreSQL/Neo4j selections | PARTIAL | The DB all-feature check is green; retained page publication/lifecycle laws and SQLite BLOB-affinity/facade laws pass focused runtime. A fixed project-owned fault cause preserves 15 exact backend categories through retained execution. P2-B compiles all SQLite/PostgreSQL/Neo4j checkpoint sources and passes SQLite atomic publication, restart/private-locator rebuild, exact max+1, memory atomicity, and 10,001-event paging laws; public events reject private storage keys. PostgreSQL/Neo4j live integration remains unavailable without Docker, two older sequential SQLite close/CAS siblings remain red, and response/replay interactivity remains incomplete. |
| Admin | EN/DE operator UI controls spaces/users/connections/documents/events against a real hub | BLOCKED | The typed-intent cutover is coherent at source: nine explicit mutations retain principal-derived actors, accepted/terminal audit, operation-id status/cancel, durable-before-kick revoke and cancellable rebuild cleanup. Backend-owned overview counts and every collection/detail read use bounded max+1 cursor pages with principal/route/scope MACs and dynamic 64 KiB fitting; the SPA accumulates spaces and expanded members. API and static relay requests share 64-slot admission, caller/stop cancellation and byte ceilings. The registered backend run passes the independent 5-positive/22-hostile oracle, all six portable exact Rust laws, the hostile relay oracle, SPA 15/15 and the all-feature hub check. Its mandatory PostgreSQL law then fails before any product assertion because the Docker socket is unavailable. The new live SQLite bilingual journey passes its fixture/inventory/intent preflight but aborts before readiness or any browser/API assertion because `Database::open_at` overflows the default worker stack; diagnostic session 98305 traced this to the transitively embedded `[u64;4096]` DB I/O list. A heap-backed fixed-capacity repair and footprint law are under registered test. Live SQLite/Neo4j/PostgreSQL qualification, production OIDC and retention controls remain. |
| MCP authority | Every transport connection has immutable authenticated principal/session/space scopes | BLOCKED | P4-A metadata laws pass, local bootstrap proves an MCP-class one-shot envelope, and the accepted server SocketGrant path provides one-use socket admission with server-derived actor and per-command revocation fencing. Current source consistently uses one ASCII v1 contract, removes raw carriers/legacy routes/tag 0, claims/seals fd3 before activation, protects bridge admission, runs a no-fd3 descendant, and performs real JSON-RPC initialize/redaction. DirectoryStream fences grant/open/hello cancellation, WGPU closes a late dial, and the MCP driver now independently source-proves a post-return cancel/generation fence with exactly-one close and terminal completion handling; the driver law joins the kernel grant/open laws but is not a real network run. Hostile credential-frame laws and deployed-worker freshness/census are green. Stable native/MCP real-child document-transport terminals and per-mutation workspace isolation remain unproved. |
| MCP plugin commands | A descriptor-owned command executes against its exact artifact through the real stdio guest lifecycle | BLOCKED | Current source discovers command ownership but registers the standard action handlers with a default session and hard-coded instance 0, discards generated package/component/descriptor digests, selects the first editor app, grants descriptor requests token 0 with no expiry, and sends a bare command frame with no owner-qualified address or document scope. Native completion is explicitly unwired, so no descriptor-owned command can currently produce an honest guest result. The audited first boundary is one verified folder-backed command with immutable install identity, workspace/document-bound handle, scoped broker lease, real ArtifactHost session, bounded completion/cancel, and a direct-child stdio oracle; hub commands remain ordered behind D0. |
| Hub workspace | MCP enumerates and reads authorized existing artifacts in a space | BLOCKED | P4-A truthfully enumerates an immutable authorized descriptor index, and the server-only P4-B boundary is independently accepted: its verified active-pair reader and exact path-only member/share route enforce 64 MiB preflight, public/private identity proof, ordered bounded no-store framing, fail-closed repeated reauthorization, deadline/disconnect/progress ownership and strict receiver integrity. P4-C is accepted for its bounded descriptor-only cache/mount scope. The first registered run exact-selected all four laws and exposed a real cache-hit self-deadlock; a process stack sample located the retained `if let` actor guard. After explicitly scoping that guard, independent source audit accepted the shared return/revoke linearization and the fresh uncached registered target passed all four receiver/cache/revoke-race/receipt laws, the 3-positive/15-negative independent oracle, MCP checks and hub all-feature check. Verified catalog activation and an exact post-baseline tail cursor still block an actual cold workspace; raw pair bytes remain private. |
| AI map inference | Authenticated user runs typed inference on an existing shared GIS map with progress/cancel | BLOCKED | GIS's bounded codec/proposal component is now native **51355 GREEN2**: one current-source executable proves the private two-receipt closure plus actual controlled CreateRegion proposal/inverse, independent `geo` bounds, progress/cancel/deadline traces and hostile rejection. MCP `inference_get` still returns `channel.not-wired`; hub has no authenticated execution route/readiness, and its generic job map is process-wide/non-durable. The required durable subject/scope/descriptor/catalog/checkpoint/frontier-bound job, leases, private progress, stale/restart recovery and CAS result references remain ordered behind secure carrier, P2-D/P4-B, trusted provider and open plan. |
| AI map mutation | Approved typed mutation is observed by collaborator and undo is isolated/audited | BLOCKED | The 51355 component law proves one controlled typed proposal and inverse only; it cannot auto-apply or become a generic patch. A durable proposal/approval/apply/undo saga must bind expected frontier and deterministic application id, then expose only the committed document edit/undo to collaborators. No approval authority, hub commit, two-user observation or undo isolation is implemented yet. |
| Integrated release | Registered full-stack target proves OS + hub + two users + GIS + MCP + admin | BLOCKED | No current compound can be renamed as proof: hub startup lacks protected bootstrap/readiness, old collaboration uses email hints/static token/fixed ports, React+MCP omits hub, native has no authenticated control receipt, and GIS TS is log-only. The audited target is intentionally deferred until auth, catalog/CAS, open-plan/P2-C, admin, MCP/GIS, and native seams land; then one non-cached existing-script-owned SQLite run must use temp state/ports, redacted bounded traces and complete teardown, with native as a separate required receipt and external backends explicitly non-green when unavailable. |

## Provider identity qualification

Latest infrastructure/source qualification only: exact runner 40832 passes 22 tests/349 expectations with ticket-owned Cargo targets. Selected-loader three-law Rust packet is source-only; its neutral planning oracle 32948 passes 19 cases. Fixture-sweep source extraction 84971 passes 54 preserved rows, 28 moved fleet edges, two async laws, 16 hostile checks and independent 186-directory/365-file discovery; kernel-only M5 bytes remain pinned. Dedicated exhaustive/native and source targets plus launch generation/freshness 16107/46634 are green for registration, not native execution. No member/provider/readiness or exhaustive conformance acceptance changes. See `📓️sol-dsl-fixture-sweep-test-leaf.md`.

Selected-provider planning-only retry 74188 is now GREEN, 19 neutral vectors (2 positive/17 denied) and zero publication after external taxonomy recovery. It proves only the data planner/private staging exercise; no native receipt, fresh bundle or readiness transition is accepted.

Shared exact-law orchestration passes registered session 47607 (21 tests/313 expectations with real process lifecycle evidence). Provider/VCS/strict source oracles 47879/83937/67836 and launch generation/freshness 54962/44053 pass after adoption; production Rust assertions do not follow from runner tests. The new planning-only selected-provider fixture/gate is registered but 61952 exits before dispatch on concurrent WGPU taxonomy `packageGeneration` mismatch. Zero planning cases, VCS receipts, bundle publications or readiness transitions are credited by that RED. See `📓️sol-shared-exact-cargo-law-runner.md` and `📓️sol-native-catalog-selection-foundation.md`.

Member admission has focused native evidence: session 95283 passes thirteen exact laws across schema (4), kernel (5) and plugin (4), backed by independent AJV/Graphlib/JSON oracles. Actual parent references now gate public single-child restore before factory admission; real absent/exact/wrong-slot/unlisted-child cases pass without parent snapshot cloning. Wrong persisted identities reject before typed hydration, failed opened members retain bounded cleanup, and graph sync preserves prior state on rejection. Batch LoadChildren/genesis/receipt, retained factory cancellation and real plugin lifecycle integration remain unaccepted. Flow source checks pass exact identities in twenty fixtures and one demo, but its native run 90024 stopped on two UI font paths before Flow assertions; both paths are repaired. This does not promote integrated frontend, backend, AI or two-user acceptance. Details: `📓️root-member-dialect-authority.md` and `📓️root-flow-child-authority.md`.

WASI/native build-profile routing is independently source-qualified only: registered registry policy 6226 passes three laws (twelve neutral routes, AJV/TOML, resolver parity, release-only identity selection), and owner generation/freshness 59031/47016 pass. OS-dev wrapper tests 99334 remain externally blocked before selection by moved kernel fixture imports. No link, performance, immutable receipt or provider-runtime acceptance follows from this policy result. See `📓️sol-wasi-native-codegen-profile-policy.md`.

VCS's first identity-only source repair has a registered independent oracle terminal (41462, exit 0: one positive, eleven hostile denials including window/renderer substitutions, one pinned protocol dual-hash check). Owner launch generation 15145 and freshness 2406 pass. The guest descriptor Rust law is not yet executed; no VCS receipt, verified immutable row, selected-provider set, readiness or mount is accepted. The stdio linked provider/runtime gate remains separately pending. See `📓️sol-vcs-native-openable-provider-v1.md` and `📓️sol-native-openable-catalog-provider-v1.md`.

## Dependency Order

Newest unmounted source prerequisites: exact built-tree source59295 and launch generation20161/freshness72802 are GREEN; four native laws await deferred mounts and actual execution. The current Flow observer's separate UiValue gap is confirmed, not closed by its old child-page test. Initial-child identity is corrected to nine fields including authenticated space, with73019 GREEN (eight vectors/71 denials/nine UTF-8 boundaries), then typed scope/parent agreement59546 GREEN. The new native identity law is staged in the existing14-law member-dialect gate, not executed. Root60581 is still building its original eight Flow laws and has reached Flow after Stdio; no assertion is yet credited.

Current frontier, superseding the chronological entries below: backend **37902 is GREEN with seventeen exact native laws** (scheduler2 + DB2 + hub13), including canonical full-EOF command and trusted catalog hash-role boundaries, plus its all-feature check in 29m12s. No live GIS route or approval is accepted. Provider **23286 passes five newly mounted foundation laws** (strict retained framing2, request/witness2, tagged-ID1); dictionary records, MemberFactory cutover and public child publication remain open. Root metadata **45758 is GREEN** (zero compiler errors, 12m59s), and native **60581 is active**. Source **81249 passes** the shared bounded tree-observer fixture, including full 384-page child-retirement accounting; the native gate contains **seven unchanged lifecycle laws plus one separate tree-fixture observer law**, all currently uncredited. The global retirement law uses exact-process isolation or serial libtest fallback, while separate UiValue/binding ownership remains under audit; this is not actual UI rendering acceptance. Stable document-scoped initial composition, child-only semantic editing, typed scene observation and real two-user persistence remain required.

Newest backend qualification: **37902 is terminal GREEN, exit 0**, with seventeen exact native laws (scheduler2 + DB2 + hub13) and the final all-feature hub check completing in 29m12s. This includes full canonical command/EOF/actor decoding, actual committed-WAL hash-matched hostile envelopes, and trusted-catalog canonical-kind/SHA-256-vs-BLAKE3 identity laws. Hub SHA-256 is `9284a6880df279cefb8b7d49a7665a5758770fbf9cb6212ab6cb74f319e7bc8e`, receipt `🗑️generated/gis-inference-ledger-run/exact-cargo-laws-oKSdUH/02`. GIS inert receipt source/oracle **27870** passes two protocol receipts/eight hostile projections; native **6903 timed out during its twenty-minute build with zero assertions**, and warm repeat **95245 is terminal RED (Nx 1 / Cargo 101)** before its one-hour build-only ceiling: 193 GIS lib-test compiler errors, no executable/discovery/assertions. Stdio code generation completed. The errors include a production proposal value conversion plus stale test serde/async contracts; no diagnostic points into the receipt module. Latest neutral **35149** passes those same vectors and adds the controlled proposal literal, independent bounds, three interruption traces and seven hostile proposal rejections. Isolated schema RED96682 rejected the real directory UUID; bounded opaque server IDs now fix that source defect in identity and WAL preflight, with no native credit yet. The three new native laws are unrun, and the next whole gate selects twenty. The newer GIS dependency/private adapter is not in the linked inventory and has no hub compile result. The Author read returns no retained submission grant. There is no GIS catalog activation, approved typed command or two-user execution acceptance.

Prior backend qualification: session **75673 has passed thirteen exact native laws** (scheduler2 + DB2 + hub9). The strengthened DB witness closes 128 sequential operations while preserving a retained list/result credit; SQLite4 and all WAL/retirement/committed-chain laws pass. The hub executable SHA-256 is `63854c1c8ae5f0fa1a35fe9b295fc9f858be4ec3fe2416594a1754af91ad713d`, with fresh receipts under `🗑️generated/gis-inference-ledger-run/exact-cargo-laws-O3kwM9/02`. Its final all-feature check timed out at the unchanged 20-minute ceiling during active stdio compilation; Nx exited 1. No all-feature pass or compiler diagnostic is claimed. This resolves the exercised task-capacity/lost-wake/stack/ledger test frontiers only, not a live authorized GIS route, provider admission, approval execution, or two-user journey.

Newest Flow qualification: **36007 reached Flow lib-test compilation and failed with 211 diagnostics**, with no native assertion executed. Root repaired stale helper async/call sites, moved fixtures and imports, `MutationOutcome` access, dependency-only test serde assumptions and label conversions. Repeat30082 was deliberately cancelled during a concurrent-dependency rebuild; metadata90437 stopped at a moved WAV path, then **96604 reached Flow and failed with twelve diagnostics from four type mismatches**. Those source mismatches are repaired and **47773 is active** on the registered metadata-only `check --tests` command before another unchanged seven-law native run. The separate initial-child source packet40255 passes seven coordinate vectors/64 denials/eight UTF-8 boundaries only; no Rust identity helper or author-create receipt is implemented. The live composition audit still proves that Flow's mutable content-hash handles do not implement stable owned-member history and parent load bypasses retained expected-root admission. Lifecycle laws cannot promote editing/persistence acceptance; document-scoped initial composition, typed child-only graph edits and bounded live-child views remain required. See `📓️root-flow-child-authority.md` and `📓️terra-flow-parent-document-pack-hydration-audit.md`.

Latest source refinement: **27914 passes** the new actual-package surface-owner oracle after intended RED76470. The registered Flow packet contains **seven** native laws, including both actual editor and viewer factories under 1/64/4096-byte grants, with no Flow native assertion yet credited after36007's compiler failure. Provider **86667 passes two retained binary snapshot decoder component laws**, not member factory/history or public child-open integration. Provider source **43033 passes** the unmounted strict-profile SPR framing oracle; native framing and caller-owned one-use history verification remain unaccepted. These supersede the six-law and pending-decoder counts below only; integrated acceptance remains unchanged.

Current 2026-09-04 qualification: separate cleanup deleted this ticket's generated receipts and warm targets; durable reports retain the previously observed results, but old receipt links are unavailable. Fresh scheduler2 + DB2 native runs now pass after the native DRR lost-wake repair; hub9/all-feature are still pending a warmed repeat. Flow has source-verified exact content identities, five-lane editor ownership, shared artifact-level document retirement, default-deny viewer ownership forwarding, and real `SemioMembers` viewer registration with Read-only rights (source16411). Six native Flow laws are staged; none has run after cleanup. Retained Flow decoding remains component-only, not integrated `MemberFactory`/public child loading. No integrated frontend, collaboration, backend, or AI acceptance is promoted. See `📓️root-flow-child-authority.md`, `📓️sol-gis-inference-discovery-and-jobs.md`, and `📓️sol-os-host-async-persistence-frontier.md`.

1. Restore trustworthy control-plane and test gates.
2. Close share/directory privacy before expanding collaboration or AI authority.
3. Fix canonical artifact opening and reconcile document-wide presence across Rust, TypeScript, and browser UI.
4. With the authorized schema-first document descriptor implemented, add a distinct canonical artifact `(pack, spr)` bootstrap, hub artifact authority/retention, lag-triggered resync, and atomic restore in native/wasm/browser.
5. Make hub workspaces enumerable/readable through the existing authenticated DirectoryClient and bind MCP sessions to authenticated hub authority.
6. Execute GIS inference/mutation through typed revision-bound job channels.
7. Complete outage/admin/backend/native parity, then run catalog-wide and full-stack release gates.

## React retained Home bootstrap ACK qualification

The shell now retains a hidden Home instance across visible application
switches and accepts a page only after the real retained Home action returns the
exact post-Config `DirectoryProjectionReceiptV1`. Receipt mismatch
closes/destroys the owner; retry, cancellation and unmount preserve the ordering
boundary; accessible EN/DE status has no default-language fallback. Registered
session **48403** is GREEN with 21 independent/AJV checks, 7/7 focused
controller laws, and one exact PluginRuntime terminal-receipt law. Generation
and freshness sessions **85363/98840** are GREEN. This is focused runtime, not a
real Chromium/WASM/hub process result; the Rust Home native sibling and browser
collaboration journey remain unrun.

## Fable Wave Evidence — 2026-09-05 (coordinator `c34c334c`)

Source-level evidence only unless a row says otherwise; every Rust claim below is "written, registered, not yet compiled" because the shared Cargo lock was held by peer builds all afternoon (load average 250–340). A consolidated `cargo check -p semio-hub --bin os-hub --tests --features sqlite` in a private target dir is running to give all four hub-touching lanes one compiler verdict. Reports: `📓️fable-*.md`.

| Slice | Lane report | Current evidence | Still unproved |
|---|---|---:|---|
| P0-F directory command receipt (`DirectoryCommandRequestV1`/`ReceiptV1`, durable claim-or-read on all three backends, bounded browser/native/WGPU transport) | `📓️fable-directory-command-receipt.md` | `directory-command-receipt-check source` 90 assertions (AJV + independent Node SHA-256), OS TS suite 267/267, four browser transport laws, ShellHost slot law, lane-scoped React typecheck clean | hub native laws, process gate, schema/client/WGPU Rust laws never compiled |
| Space administration page + pane (`DirectorySpaceAdministrationPageV1` replaces unbounded detail, role-shaped Home rows, `manageSpace`, retained `DirectoryAdministrationOperation`, scoped-4401 retirement) | `📓️fable-space-administration.md` | `space-administration-check source` 41 checks, OS TS suite 259/259 ×3, React pane 10/10, `semio-framework-os-kernel` compiled 0 errors with the new schema/client | four hub route laws, two WGPU laws, Postgres/Neo4j methods, space plugin role laws never run; no process/browser run |
| P0-C execution-target lease (`DocumentExecutionTargetLeaseFieldsV1`, selection-bound asset routes, browser verified lease with first-party BLAKE3, wasm admitted only via lease → localized renderer-unavailable) | `📓️fable-execution-target-lease.md` | `execution-target-lease-check` 58 hostile + 35 field + 12 byte + 11 lifecycle vectors, browser check green (3 named browser tests), worker suite 57/57, launch/registry fresh | three native laws never compiled; no WASI execution, no WGPU, no process test (trusted-bundle native gate unrun) |
| P1-C/D AI Map proposal server core (frozen binding identity, `HubInferenceRuntimeV1`, four authenticated document-scoped routes fail-closed without a trusted binding, typed server-stamped `CreateRegion`+inverse behind a witness-only committer port) | `📓️fable-ai-map-proposal.md` | `gis-map-proposal-check --source` green (AJV, Node SHA-256, independent bounds, lifecycle, visibility, 11 error codes); ledger oracle green through its own stages | library/bin laws and `--native` gate not yet executed; route-level end-to-end proposal→approval needs a non-`cfg(test)` trusted GIS bundle builder; committer fails closed until the atomic parent+child composition transaction exists |
| MCP inference bridge, GIS Map inference UI port (Slice D), hub native qualification run | `📓️fable-explore-mcp-inference-bridge.md`, `📓️fable-explore-gis-map-inference-ui-port.md`, lanes in progress | packets written; implementation lanes resumed after two usage-limit cutoffs | no evidence yet |

Incident recorded: retiring `DirectorySpaceDetailV1` left `📇️directory/🔌️client/🦀️.rs` and `🌉️mcp/🏠️workspace/**` uncompilable for roughly three hours (reported by a peer session as blocking five plugin tickets); resolved in tree by 13:21. A second incident: the UI-port lane's `Effect::RequestInferenceProposal` left one host match non-exhaustive (`🔌️plugin/🖥️host/📥️imports/🦀️.rs`) for roughly three hours, blocking every hub compile; resolved by 18:10. Lanes now must escape the shared lock only with lane-qualified private target dirs.
