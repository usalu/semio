# Registry Taxonomy Ownership and Rust Mount Verification

## Scope and Current Evidence

The full registry check failed after the opening-scope launcher was generated. The generated catalog and launch-byte check passed independently; it does not qualify the taxonomy or plugin assets.

Read-only inspection identified two checker defects: artifact-level completeness still expects subset-owned schema/IO/examples and a forbidden engine, and Rust mount validation only parses the package entry file instead of following reachable nested modules. Window `☑️options` requirements are current; actual `🎚️options` directories remain separate tree debt and must not be waived.

## Test-Driven Repair in Progress

The first bounded repair will compare exact Cargo-rooted Rust module reachability against Rust's dependency output on language-neutral fixture trees. It must accept nested and inline module mounts, reject missing targets and unmounted components, and never let an unrelated Cargo manifest confer authority. Native production builds remain serialized in the retained lifecycle target; small independent fixture compilation uses only this ticket's generated directory.

The recursive Rust mount repair has now passed nine compiler-backed laws. Artifact/schema/IO owner relocation and valid foreign surface contributions remain pending; no plugin trees or taxonomy vocabulary were changed.

## Unqualified Outcomes

No claim that the full registry, all plugin artifacts, mounted Shell, genuine GIS component, or authenticated collaboration journey passes follows from this checker work.

## Rust Mount TDD Evidence

Registered `@semio-tech/plugin-registry:rust-taxonomy-mounts-check` and its launch configuration. The first native compiler-backed run (session 61677) failed as intended: valid nested, same-line visibility, inline-base, and lexical-decoy trees were falsely reported unmounted; a missing nested target was misreported only as unmounted. Rust itself accepted the positive trees and diagnosed the missing target. Both genuine orphan cases remained rejected.

Production now uses the shared recursive `inspectRustModuleGraph` and lexical module facts, constrained to the canonical Cargo manifest. It follows all Rust sources, requires manifest-proven membership for taxonomy components, and reports unresolved edges from reachable contexts. It no longer invents mount authority from a plugin-root fallback or an unrelated manifest. The first retry passed all seven cases (session 60367). Exact-path and conditional-target strengthening followed as recorded below.

The shared graph currently models `[lib]`, not independent Cargo `[[test]]` targets. Their unmounted findings remain unqualified rather than being classified as proven plugin debt. Artifact ownership, schema-format selection, and native direct IO validation remain a separate pending repair.

## Final Bounded Mount Evidence

The exact-path oracle strengthening first failed on a deliberately unmounted `nested/🦀️.rs` sharing the suffix of a mounted `deep/nested/🦀️.rs` (session 27130). Parsing and normalizing Rust's exact dependency targets repaired the oracle; eight laws passed in session 53117. An inactive `#[cfg(any())]` missing target then reproduced a checker-only false positive (session 2698). Unresolved conditional edges are no longer asserted as unconditional missing modules; their active configurations still require compiler-matrix verification.

The final registered gate passed nine laws with AJV corpus validation and nine direct `rustc` outcomes (session 67730). All per-case DEBUG outcomes were read. Exact capture: `🗑️generated/registry-taxonomy-mounts/rust-taxonomy-mounts-0TffvN`. Signals are forwarded to the active compiler and its 30-second bounded deadline cannot be mistaken for an expected missing-module result; signal/budget branches were not separately exercised in this run. Inputs, metadata, dependency files, and compiler logs are confined to the ticket's generated directory.

This is structural Cargo-library reachability, not runtime authorization or a complete Cargo target/configuration matrix. The shared graph still follows potential conditional edges whose sources exist. Explicit test/bin/bench/example roots and conditional configurations require the pending same-manifest target-matrix work; no directory-name exemption was introduced.

## Launcher Regression Findings and Repair

A broader selected registry test run failed three launcher/profile assertions (session 33759). The dev-distribution generator is registered in taxonomy with preview target `@semio-tech/framework-os-dev:preview-distribution`, but had no corresponding preview launcher. After updating the expected current generator list, the exact test reproduced the missing entry (session 90354). Requiring the repository's Unicode-safe root Nx wrapper then reproduced the existing direct-Nx route (session 14256).

The preview launch seed now includes the missing generator in contract order and routes all 17 generator previews through `bun ./📜️script.ts nx run`. Regeneration passed (session 87654), the focused launcher test passed (session 54438), and generated-byte freshness passed again (session 64262). This qualifies launcher wiring only; it does not claim all 17 preview commands were executed.

The two additional WASM-profile assertions were repaired after that recorded failure. The neutral profile corpus and its closed schema now pin the existing lowpoly/puzzle opt-level overrides exactly; the test still requires the entire base wasm-dev profile and override map to match. Cargo.toml was not changed. The stale artifact-path source-literal assertion was replaced with actual calls to the existing path helper for default wasm-dev and explicit fresh wasm-release paths. The existing two build-argument profile checks remain. The full launcher/profile file passed five tests (session 47845); DEBUG confirmed 12 routing cases, two exact overrides, and two artifact-path results.

Registry generation passed with 59 plugins, 60 playgrounds, and 45 framework packages (session 10521), and generated-byte freshness passed independently (session 44977), before the final preview-launch addition. The full taxonomy gate remains unqualified and was not rerun to imply completion.

## Additional Regression Run

The default fundamental-budget all-file registry unit run was killed at the repository's 15-second suite budget (session 46535), without a complete test result. It is not a green run or an assertion failure diagnosis. The first retry (session 29204) passed `long` as a Vitest filter because this bundle's TestScript does not parse positional levels, and therefore ran no tests. The corrected retry used `SEMIO_TEST_LEVEL=long`, one worker, and all temporary directories redirected into this ticket. It passed all four files and 23 tests in 19.39 seconds (session 89405). No native acceptance deadline was raised.

The auditor is separately measuring the full registry taxonomy check after these bounded repairs. Its verdict must not be conflated with the generated-byte or unit-test gates.

## Required Plugin Root Ownership

The independent full registry measurement reached taxonomy and reported2696 findings before this slice. One proven checker drift was requiring every `pluginChildDirs` entry, including optional `🔨️modules`, as a root component. The taxonomy's actual `pluginRequiredChildDirs` contains only `🎮️commands`. The root contract and command component remain mandatory; an absent or marker-only optional modules lane must not manufacture a missing required component.

Extracted the existing direct-root validation unchanged into a private helper, then added a closed seven-case language-neutral fixture and registered `@semio-tech/plugin-registry:plugin-root-ownership-check`. Its independent SQLite required-minus-present relation checks required-only roots, absent/marker/component modules, missing root, missing/marker command root, and redundant nested contract. The initial real Nx gate was RED (session16518): five cases had the erroneous modules requirement while SQLite matched all expected outcomes.

Production now iterates `pluginRequiredChildDirs`; no plugin tree, vocabulary, root/command requirement, or nested-contract prohibition was relaxed. The same registered gate is GREEN7 with AJV1/SQLite7 (session42445, capture `registry-plugin-root-ownership/plugin-root-ownership-crKcLw`); all DEBUG outputs were read. The launcher is registered at order411.10870, generated catalog/launch regeneration passed (83382), and scoped whitespace checks passed. Full registry unit regression is currently running (session11530). The full taxonomy gate remains unqualified; artifact/standard/subset ownership, schema format/IO drift, manifest target matrix, and real tree debts still need separate work.

Follow-on verification: recursive Rust mount regression remains GREEN9 (session84301, capture rust-taxonomy-mounts-j25FnK), and generated catalog/launch bytes are fresh (67903). The first full unit regression failed an unrelated producer budget contract: descriptor emission interpreted the repository's0/unlimited budget as elapsed expiry. That production issue is repaired and documented in [root-descriptor-emission-budget](📓️root-descriptor-emission-budget.md). Final full registry unit regression session74822 is GREEN4files/24tests, including the new five-case budget law. No full-taxonomy success is claimed.
