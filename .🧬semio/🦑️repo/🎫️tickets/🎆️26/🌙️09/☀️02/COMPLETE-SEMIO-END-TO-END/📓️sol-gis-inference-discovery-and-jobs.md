# GIS Inference Discovery And Server Jobs

## Scope and acceptance boundary

This packet follows `📓️terra-ai-map-current-end-to-end-delta-audit.md` and `📓️terra-two-user-gis-inference-acceptance-packet.md`. The real GIS service deterministically computes feature counts and geographic bounds; it is not a language model and makes no provider/network call. Static discovery is deliberately separate from authenticated execution.

The stdio lane retains ownership of its exact linked provider/catalog set. GIS generation/discovery does not insert a catalog row, turn on hub readiness, or claim a GIS execution receipt. The server packet starts with a schema-first SQLite job ledger, then Author-only authority, a catalog-bound injected GIS executor, private result/proposal projection, and approval through ordinary document commit. MCP execution remains `PLUGIN_UNAVAILABLE` until that server boundary is genuinely wired.

## Discovery TDD

The neutral MCP `💡️inference/🧪️fixtures/🗺️gis-discovery` contract fixes the exact one-service GIS roster and eight hostile mutations. The independent Bun/AJV oracle validates the positive, rejects each hostile mutation, then reads only the committed GIS descriptor. The exact Rust law invokes the real registered `inference_list` tool with the literal fixture and verifies that querying an unopened artifact still fails without execution authority.

Registered oracle session `70372` exited `1` at the expected first RED: the committed descriptor has no service, so `/declared` violates `minItems: 1`. Its neutral positive and eight hostile validations completed before that assertion. The descriptor is not hand-edited and there is no source-scraping fallback.

Registered GIS `describe` session `55424` uses the existing component build/jco/core/descriptor emitter. Both `CARGO_TARGET_DIR` and temporary extraction root are under the active ticket's generated directory; the warm public-boundary target is retained at the coordinator's request. It exited `1` before descriptor emission: the stdio Cargo fingerprint contains 53 errors, beginning with four `MutationLeaf source authority failed: descriptor owner does not exactly match source owner` diagnostics for XML 1.0 valid, SVG 1.1 tiny/basic, and HTML 5 any `set-text`. The stdio owner subsequently verified those four current descriptor-owner pairs agree and requested a current-source retry. No descriptor was hand-edited.

The GIS existing test router now forwards selectors using `resolveTestLevel` and awaits the budgeted test runner; it no longer discards exact-law arguments.

## Current nonclaims

No server inference route, durable job result, approved GIS mutation, two-user socket delivery, browser/WGPU inference UI, MCP execution, PostgreSQL/Neo4j ledger, or model integration is accepted by the discovery fixture. The new registered discovery target is not yet green because descriptor regeneration and exact Rust execution remain pending.

## Initial SQLite foundation

The new hub-owned `💡️inference` module contains paired JSON/Rust/TypeScript closed client requests, immutable server-selected identity hashing, finite monotonic cancellation/progress control, zero-on-drop private byte ownership, and a SQLite ledger. SQLite `BEGIN IMMEDIATE` plus a unique request id establishes durable single-winner idempotency across connections; immutable identity/event triggers prevent ledger rewriting. A fixed 128-job capacity and input/result/proposal byte bounds are checked before admission, terminal cancellation is idempotent, private reads require exact requester/session/generation/full document scope, and expiry or identity drift cannot publish a late result.

The initial ledger corpus has nine **ledger state traces**, not the later nine two-user acceptance journeys. Its payload labeled `bounded-ledger-payload-not-an-executable-proposal` proves only private byte lifetime; it is never accepted as a GIS mutation. The permanent `gis-inference-ledger-check` name and success banner intentionally distinguish this foundation from the eventual `gis-inference-job-check` route/provider/approval gate.

Registered oracle `33334` and identity-schema rerun `62637` exited `0`: nine state traces, eight hostile requests, AJV/TypeScript parser parity, two independent Node SHA-256 checks, and independent deterministic GIS bounds/counts. Registered foundation gate `25709` subsequently exited `1` (Cargo `101`) after native stdio linking: thirteen owned SQLite `ToSql`/`FromSql` errors for unsigned integers and `usize`; no law was discovered or run. Checked `i64` SQL bindings and checked nonnegative safe-integer reads now replace those unsupported mappings. This is source repair, not yet Rust runtime evidence.

Full fixture-schema validation first exposed an owned URI-reference error in registered session `50277`: a relative schema id made AJV resolve the cross-schema reference under the fixture directory. Both schema ids/references now use explicit URNs. Final-source registered oracle `91583` exits `0` with full request/identity/fixture schema validation and the same nine/eight parity counts. The earlier root-router rejection of `cargo-source-directory` scope was source-superseded by its concurrent owner before that rerun; no taxonomy mutation was made here.

Registered launch/catalog generation `73711` and freshness `99352` exit `0`. Direct seed/generated inspection confirms exactly one entry for each new discovery/ledger command and preserves the concurrent Norm config source/test entries. Owned-file `git diff --check` is clean.

## Durable approval outbox foundation

The ledger now records a deterministic mutation id, exact bounded command bytes/hash and proposal hash before a future document submit. Prepared rows survive reopen, prohibit a competing cancellation, remain recoverable after requester expiry, and support bounded four-row cancellable recovery pages plus idempotent committed/abandoned reconciliation. The fourth registered SQLite law exercises this with an explicitly opaque `ledger-only-command-not-a-gis-mutation` payload. It does **not** prove a document WAL commit: the actual durable command-proof consumer, typed GIS mutation/inverse, authority revalidation, submit/fanout integration and restart process journey remain required. No process-local mutex is counted as cross-store atomicity.

Registered oracle `44168` exits `0` with six independent input/identity/outbox hashes. The latest `53367` also exits `0`, adding six literal SQLite integer vectors: zero, JavaScript safe maximum, negative one, safe maximum plus one, signed 64-bit maximum and unsigned 64-bit maximum. Rust laws now use those same decimal vectors for checked SQL read/write boundaries and expiry-add overflow. The permanent exact-one preflight now uses the shared build budget instead of the four-hour orchestrator budget accidentally used by `25709`. All heavy retries are serialized by the coordinator; the warm ticket target is retained.

## Strict GIS artifact identity cutover

Following `📓️terra-framework-strict-artifact-identity-admission-audit.md`, both GIS definition roots and all descendant capability/native-composer identities now agree with the actual app roots `s.gis.gismap` and `s.gis.gisterrain`. Inference metadata, schema composer tests, map kind/port ids and artifact presentation ids use the same canonical vocabulary. Document payload schemas `gis.map`/`gis.terrain` and the distinct `2d.map` structured feature interchange schema are preserved; none is used as a native kind alias. The builder supplies `semio:gis` only after `label`/`version` establish its Ready typestate.

The literal GIS identity fixture has two canonical families and five hostile roots. The existing actual-plugin assembly law now checks both registered definitions, every descendant capability prefix, exact native and codec claims, and four exact app roles. Registered MCP oracle `22918` executes its AJV/independent identity phase (`canonical=2 hostile=5`) then exits `1` at the still-empty committed inference roster. This is not native assembly or regenerated-descriptor acceptance. An initial command `88784` used an incorrect project name and reached no oracle; the corrected project is `@semio-tech/framework-os-mcp-rs`.

## Controlled native GIS computation

`infer_gis_map_controlled` now drives the same coordinate visitor as the existing GIS inference family. It checks caller cancellation/deadline before decoding, at every visited value, and before publishing the encoded result; enforces finite work and a hard 64-level traversal ceiling; and folds bounds without allocating a coordinate-result vector. The native service delegates to that implementation with its request budgets. A zero-on-drop encoded-output guard covers interruption and unwind after computation. This is a source-linked local deterministic GIS service, not a model provider or a hub route.

The new literal control corpus pins ten progress checkpoints and three first-interruption cases (before-decode cancellation, mid-fold cancellation, mid-fold deadline). Registered MCP oracle `24538` passes its identity and control phases (`canonical=2 hostile=5 checkpoints=10 interruptions=3`) then remains terminal RED at the committed descriptor's empty inference roster. A third exact Rust selector in the permanent discovery gate runs the real controlled service over those literals and compares its success bytes with the actual service entrypoint; it has not yet executed while heavy builds are serialized.

The controlled-service corpus now also fixes one deterministic typed `CreateRegion` bounds proposal. `GisMapInference::bounds_proposal` returns the existing GIS mutation type, checks a fixed job identity, collection counts, duplicate region identity and finite geographic bounds, and never applies it. The Rust law compares that actual mutation to the literal, runs its existing inverse law, and rejects reuse after application. Registered `9076` passes the independent typed-proposal calculation before the same descriptor RED. This does not replace server frontier/catalog/Author revalidation and is not an approved document commit.

## Current serialized retry and shared identity alignment

Registered ledger session `27631` uses the retained absolute `space-public-boundary-sol-target`, with the six checked SQL integer vectors present. Its neutral oracle completed before compilation (`traces=9 hostile=8 sqlite-integers=6 hashes=6`); the four exact SQLite laws and separate all-feature hub check are still pending terminal evidence. No other heavy build was launched from this lane.

GIS now exposes its actual `plugin` factory alongside `GisApps`. Its two codec extension claims use the framework's schema-scoped `codec_extension` helper: `7:gis.map:gismap` and `11:gis.terrain:gisterrain`. The identity fixture, independent UTF-8 byte-length oracle and actual assembly assertions share those literal values; ordinary format extension authority is unchanged. Registered discovery oracle `10485` passes identity, framed codec values, controlled progress/interruption and typed-proposal calculations, then exits `1` at the unchanged empty committed inference roster. Native assembly and descriptor generation remain unaccepted.

Current durable-proof inspection found that `ArtifactHistoryView` exposes operation IDs only, and its retained scanner treats `REC_COMMIT` as an ordinary frame while publishing history at `WAL_FRONTIER`. It is therefore not used as an exact committed-command witness. `ArtifactEngine.applied` is also not proof of post-WAL durability. A future approval consumer must verify the committed envelope bytes/hash under the same document authority and retain bounded cancellation/close ownership; no inference approval route or reconciliation authority has been opened on either weaker signal.

The separate `🧾️inference-wal-proof-v1` corpus now fixes sixteen semantic WAL traces and one exact protocol envelope/hash. Its payload is explicitly **protocol-envelope-only, not an executable GIS mutation**. The hostile cases cover missing/altered command bytes, missing begin/commit, mismatched transaction/count, abort, duplicate target, same document in another space, changed generation, cancellation before replay/publication, unflushed bytes, torn tail and record max+1. The independent oracle uses AJV and Node SHA-256 over separately encoded protocol bytes. Registered `22798` first failed on an owned missing local AJV import; after repair, `61634` exits `0` for the full ledger oracle and all sixteen witness traces. No Rust WAL consumer or proof authority is credited by these semantic fixtures.

Session `27631` passed its exact-one discovery preflight for all four named SQLite laws and advanced into the first exact test invocation. That invocation recompiles dependencies after concurrent canonical hash/job paths changed. No assertion result is credited until the retained registered handle reports it.

`27631` is now terminal `1` (Cargo `101`), not a timeout. The first exact invocation stopped in the external `semio-framework-graph` custom build command after the four-law discovery compile succeeded. **Zero SQLite assertions executed; the all-feature check was not reached.** The generated graph/build dependencies changed between Cargo invocations. The permanent ledger gate now builds exactly one `compiler-artifact` test executable, records its path/SHA-256, uses that same executable for `--list` and all four bounded `--exact` runs, and retains the separate current-source all-feature check. This runner improvement has not yet been exercised by a new heavy run; the target remains preserved.

## Retained committed-WAL verifier: unverified source

The new hub `💡️inference/🧾️wal` module reads the actual DB `WalReplayCursor`, which validates the committed SPR prefix before exposing WAL records. Its matcher requires the full space/document key, current generation fence, exact envelope SHA-256 and mutation id, matching begin/commit transaction id and record count, and no incomplete or duplicate target transaction. A private witness is published only after the scan and replay close, and retains the generation fence so later invalidation prevents reuse.

Caller cancellation/deadline is separate from retained ownership: four admission slots bound concurrent verification, dropped callers cancel their operation, and the worker retains an already-admitted storage future instead of dropping it on timeout. It closes each decoded record and then the replay owner before releasing its slot. A stalled backend retains its slot rather than falsely reporting reclaimed ownership. Two new exact laws build real SPR/WAL bytes from the sixteen-trace neutral corpus and check actual replay, cancellation and dropped-caller cleanup. They are **not compiled or run yet**. Source hygiene caught and repaired the fixture include depth, WAL sync segment argument, concrete replay generic, and a drop-law setup that previously cancelled before replay began.

The raw-string committed reconciliation API has now been removed. Its crate-private replacement accepts only that witness, checks the ledger's immutable scope and exact prepared command/mutation identity, and rejects a different or invalidated generation. The fourth SQLite law now stores the same exact protocol envelope as the WAL corpus and obtains a real replay witness before its reopen/reconcile assertions; the envelope remains explicitly non-executable GIS payload. Preparation also checks exact command mutation/document/actor headers. The later authenticated document-gate consumer, typed GIS validation/submit, proven-negative abandonment and restart process journey are still missing.

The coordinator is serializing heavy builds. The ledger gate has adopted the stdio lane's shared repo-library `runExactCargoLaws` helper, removing its duplicate build/list/exact implementation. It now selects exactly four SQLite plus two WAL laws from one executable, then runs the current-source all-feature hub check. Registered oracle `66971` exits `0` with witness-invalidation coverage; `47143` exits `0` after the strict command-hex cutover and cross-fixture command/hash/scope equality check. No additional native build, Rust assertion, model claim or ticket-status change was made for this source step. The shared helper currently requires an absolute ticket-generated `SEMIO_TEST_ARTIFACT_DIR`; the zero-touch launch artifact resolver is being coordinated with its owner.

The ownership law now holds the actual replay cursor after its first decoded record. Its three literal cases (caller drop, explicit cancellation, deadline) require a prompt typed interruption while `active=1` and `close_steps=0`; only an explicit test release lets the retained production cleanup reach `active=0`, positive close steps and unchanged progress `1`. This is source-level strengthening, not runtime evidence. Registered oracle `51444` exits `0` with the three ownership rows.

Current catalog source returns a SHA-256 `generation_id`, not an integer counter. The inference identity therefore now requires `catalogGenerationId` as a 64-lower-hex string; the old numeric field is rejected, not adapted. JSON schema, Rust identity, ledger/outbox/envelope literal hashes and the changed-generation law were cut over together. Four hostile identity vectors cover a numeric value, short hash, uppercase hash and the removed alias. Registered oracle `30243` exits `0`: nine state traces, eight hostile requests, four hostile identities, six SQL integer vectors, six independent hashes, sixteen WAL traces and three ownership cases. The document-generation fence remains separately numeric. Owned diff hygiene remains clean.

The shared runner owner found no authoritative automatic active-ticket resolver; choosing the newest open ticket would be unsafe. Its explicit `SEMIO_TEST_ARTIFACT_DIR` requirement remains intact, and the coordinator owns launch-context binding. The next native retry must supply that absolute ticket-generated path alongside the retained absolute Cargo target. No build was launched while root's coordinated member-dialect metadata check owns the heavy slot.

## Concrete catalog-to-identity construction: unverified source

The crate-private `💡️inference/📇️catalog::identity_from_verified_catalog` now consumes the concrete `VerifiedTrustedCatalog`, not a client catalog DTO or process-global service registry. It resolves the descriptor's exact native codec identity, requires one matching verified package with exact plugin/package/version/component SHA-256, checks the descriptor's declared GIS inference metadata and all five versions, and constructs the immutable identity from that catalog generation, descriptor digest, server session record, server-materialized input and frontier. It checks bounded input, stored-session expiry/revocation shape, full scope and monotonic operation checkpoints.

This constructor is **not Author authorization, a fresh session revalidation, a native inference provider receipt, or an execution grant**. It has no route caller. The later document actor must revalidate the real session/Author role and materialize input at its current authoritative frontier; checkpoint pack bytes alone are not assumed to be the current map snapshot.

The neutral catalog projection corpus has twelve cases: exact facts, cross-space equal document, cross-document, package/version/component mismatch, old kind, wrong document schema, missing service and mismatched owner/algorithm/policy. The Rust law calls the production projection matcher used by the concrete constructor, but does not load a real GIS bundle or execute the constructor end-to-end. Registered oracle `66240` exits `0` for these twelve cases and all current ledger/WAL corpora. The one-artifact gate now selects four SQLite, two WAL and one catalog-projection law; no new Cargo run or native assertion is credited. Routes/readiness/MCP remain fail-closed and unchanged.

The committed witness now explicitly carries and checks job id and proposal hash in addition to full scope, generation, mutation id and exact command hash. Two literal hostile bindings reject a different job or proposal even when the committed command arguments are unchanged. Registered `25007` and `50916` were green intermediate oracle reruns; final-source `67852` exits `0` with ledger `9`, request hostile `8`, identity hostile `4`, SQL integer `6`, WAL traces `16`, retained-ownership rows `3`, witness-binding hostile `2` and catalog projection `12`. The shared runner now prints each stage and its final executable/hash receipt as well as retaining bounded outputs. No Rust execution or new launch-context authority is implied.

Known proof-hardening remainder before integrated approval acceptance: multi-segment replay currently verifies exact document/contiguous segment identities and previous-hash presence, but does not compare a segment header's previous hash with the prior segment's exact SPR commit-chain hash. `WalReplayCursor` validates each segment's committed prefix but does not expose that chain-tip receipt. This precise source limitation was sent to the independent auditor; the sixteen current traces are single-segment and cannot be credited as cross-segment chain validation. The later actor/fanout integration must not treat the current foundation as the complete durable approval boundary.

## Serialized seven-law acceptance attempt

Registered session `28714` is active after the coordinator released the heavy slot. It runs `bun ./📜️script.ts nx run os-hub:gis-inference-ledger-check --skip-nx-cache`, with `CARGO_BUILD_JOBS=1`, an empty `RUSTC_WRAPPER`, the retained absolute `space-public-boundary-sol-target`, and explicit ticket-generated `SEMIO_TEST_ARTIFACT_DIR=…/gis-inference-ledger-run`. The shared runner owns artifact directory `exact-cargo-laws-VfpcO7/00`. All current neutral oracle phases passed before the single native build started: ledger traces `9`, hostile requests `8`, hostile identities `4`, SQL integers `6`, hashes `6`, WAL traces `16`, ownership rows `3`, witness bindings `2`, and catalog projection cases `12`. No native assertion or all-feature result is yet credited. The cross-segment limitation above remains independent of this focused seven-law packet.

Read-only follow-up during that build narrows the WAL guarantee further: `protocol::format::recover` checks header, CRC, commit offsets and sequence linkage, but its fast path explicitly does not read intervening records or recompute the SPR `chain_hash`. `WalReplayCursor` subsequently checks individual frame CRC/back-length and skips `REC_COMMIT`; it also does not recompute that hash. Therefore “committed prefix” above means recovery/framing acceptance, **not independently verified intra-segment cryptographic commitment**. The current witness additionally checks the exact target envelope SHA-256 and WAL transaction markers/count, but recomputed intra-segment commit hashes and cross-segment chain equality both remain prerequisites for integrated approval. The fixture writer also currently uses default SPR flags rather than the DB writer's explicit required-hash-chain flag. The active seven-law packet is left unchanged so its eventual result has a stable source boundary; neither its name nor a green result will erase these qualifications.

Session `28714` is now terminal **Nx exit `1`, build-stage timeout**, not a compiler assertion failure. The retained `build.json` records `status: null`, `signal: SIGKILL`, `reason: timeout` at the unchanged `1,200,000 ms` build ceiling. Final captured Cargo JSON contains **zero compiler errors and zero test executable artifacts**. Native stdio and plugin-host library artifacts completed; the build was in DB when the ceiling fired. Exact-law discovery `0`, Rust assertions `0`, all-feature check **not reached**. No executable/hash receipt exists, so no stale binary is substituted. The owned Cargo/rustc processes were confirmed absent after the terminal. The warm target and generated evidence are preserved under the coordinator's open-ticket retention instruction; no generated sibling or durable report was removed.

The retry command is unchanged and requires the coordinator's next serialized heavy slot:

```sh
CARGO_BUILD_JOBS=1 RUSTC_WRAPPER='' \
CARGO_TARGET_DIR='/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️02/COMPLETE-SEMIO-END-TO-END/🗑️generated/space-public-boundary-sol-target' \
SEMIO_TEST_ARTIFACT_DIR='/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️02/COMPLETE-SEMIO-END-TO-END/🗑️generated/gis-inference-ledger-run' \
TMPDIR='/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️02/COMPLETE-SEMIO-END-TO-END/🗑️generated/gis-describe-scratch' \
bun ./📜️script.ts nx run os-hub:gis-inference-ledger-check --skip-nx-cache
```

The next proof-hardening packet must add literal hostile committed-chain vectors (valid multi-commit/multi-segment, altered non-target record with recomputed CRC but old commitment, altered commit hash with valid CRC, mismatched commit count/length/sequence, missing required-chain flag, and wrong prior-segment tip). Production verification must fold full on-disk frame digests into each commit before any witness is minted, carry only the validated final tip into the next segment check, and retain the existing bounded cancellation/close ownership. Merely parsing the final commit payload or comparing two attacker-controlled hash fields is insufficient. This is a source plan, not an implemented or passing extension of the current sixteen traces.

## Hash-chain hardening checkpoint

That planned extension is now implemented in source but has **not compiled or executed in Rust**. The actual DB `WalReplayCursor` validates each retained segment before exposing any of its records: exact required-chain format, full on-disk frame digests (including varint, CRC and back-length), critical flags, every commit's count/byte length/sequence/previous offset/reserved bytes/recomputed BLAKE3, exact segment document/index and equality with the preceding validated segment tip. Hashing advances by at most one `DB_IO_PAGE_BYTES` fragment per yielded step. The redundant recovery walk was removed from this reader so it cannot perform an unbounded commit scan before bounded verification begins. Corruption latches failure; explicit caller cancellation/deadline/fuel interruption preserves the existing generic resumable-control contract. Admitted pages remain owned until explicit close.

The independent audit found legitimate nonzero-start histories after compaction deletes earlier segments. The generic reader therefore validates a contiguous **retained suffix**, with its initial unavailable predecessor explicitly distinguished from a verified tip. It makes no genesis claim. The inference verifier calls the new `open_genesis` entrypoint exclusively and rejects compacted/nonzero-start proof requests. No unauthenticated checkpoint anchor or silent previous-tip reset is admitted. An authenticated compacted-anchor proof remains future work, not a compatibility fallback.

The new neutral `🌎️hub/🧪️fixtures/⛓️inference-wal-chain-v1` corpus has fourteen chain cases, three hashing-interruption cases and two retained-boundary cases. The Rust chain law generates real bytes through `SprWriter`, checks the same outcome with the existing third-party `blake3` dev dependency, then calls the actual retained inference verifier. Every hostile mutation repairs its frame CRC so CRC-only acceptance cannot satisfy the law. The second new Rust law holds actual replay at its first pre-record hashing yield; caller drop/cancel/deadline must stop at progress `0` and one hashing step, retain active ownership until real close is released, then reach active `0`. It also writes/seals/deletes the actual first segment and requires normal retained replay to succeed while genesis proof rejects, including a locally valid but unanchored prior tip. These are staged runtime assertions, **not passing results**.

The registered Bun/AJV oracle independently constructs and validates the wire frames using the existing dev-tool TypeScript BLAKE3 implementation (exported, not copied), with the `abc` known-answer check. The Rust third-party parity is separate and still pending. Registered session `98479` exited `0` for the fourteen initial chain vectors. Session `32174` exited `0` after the retained-boundary/cancellation extension: chain `14`, hashing ownership `3`, retained boundaries `2`, plus all earlier ledger/request/identity/SQL/hash/WAL/catalog phases. The permanent existing-script gate now selects exactly **nine** Rust laws from one executable: SQLite `4`, WAL `4`, catalog projection `1`, then the all-feature hub check. Existing project/launch commands are unchanged and verified present. No Cargo was launched during the coordinator's member-dialect build slot.

Public approval remains unintegrated. There is still no authenticated inference actor route, native GIS executor receipt, typed GIS approval submission, restart process reconciliation journey, two-user fanout, or model execution claim. Gate `28714` remains the earlier seven-law build timeout; neither the new source nor the green neutral oracle retrospectively changes its zero-assertion result.

Final-source neutral rerun `82488` exited `0` after schema uniqueness was enforced on all three new vector arrays; counts remain `14 + 3 + 2` and all earlier oracle phases pass. Focused diff-check also exited `0`. No native build was started or warm artifact removed. The next registered runtime gate is the existing `os-hub:gis-inference-ledger-check` with the same explicit ticket environment shown above, now selecting nine laws; it still requires the coordinator's build-slot release.

## Approval binding source audit

Positive reconciliation is crate-private and requires the non-serializable private-field WAL witness. Its match checks the exact ledger scope, live document generation/fence, job id, proposal hash, mutation id and command hash. The command hash binds the actor bytes that preparation checked against the immutable requesting user/session. The witness is released to its caller only after the genesis-only cursor completes, the exact target transaction commits, all record/chain checks succeed, and retained owners close. The catalog constructor returns selected identity facts only; it is not a session/Author grant, provider execution receipt, WAL witness or approval capability.

Four additional neutral hostile requests now inject a forged `walWitness`, `genesisVerified` boolean, client `commandHash`, or `catalogExecutionGrant`. The existing closed request schema and TypeScript/Rust decoders must reject each, bringing that corpus from eight to twelve without adding an authority-bearing request form. Their registered runtime result is recorded below after the rerun.

Remaining negative-reconciliation gap: the unused public `abandon_uncommitted_approval(job_id, mutation_id, command_hash)` helper currently accepts raw identities without a proven-negative WAL receipt. No current production caller uses it, but a future recovery actor must not wire it as-is: absence needs an exact fenced scan/retention anchor and durable reconciliation policy. Typed GIS payload validation/submission is also still absent; the prepared fixture payload intentionally proves ledger/hash behavior only, never executable approval. These are explicit integration blockers, not capabilities inferred from the catalog or from a successful hash check.

Registered neutral session `57622` exited `0` on these final request-schema bytes: hostile requests `12`, chain `14`, hashing ownership `3`, retained boundaries `2`, and all prior ledger/identity/SQL/hash/WAL/catalog checks. Focused diff-check exited `0`. Rust remains pending; the coordinator retains the native slot for its warm member-law rerun.

## Nine-law registered runtime attempt

The coordinator released the separate warm hub target while permitting two one-job builds on the host. Registered session `79007` is active with the exact canonical retry command above, `CARGO_BUILD_JOBS=1`, wrapper empty, and the unchanged 20-minute helper build ceiling. Its fresh evidence root is `🗑️generated/gis-inference-ledger-run/exact-cargo-laws-wwMJTA/00`. All neutral phases completed green before compilation: ledger `9`, hostile request `12`, hostile identity `4`, SQLite integer `6`, hashes `6`, WAL `16`, retained record ownership `3`, witness bindings `2`, chain `14`, hashing ownership `3`, retained boundaries `2`, catalog `12`. Native exact discovery, executable/hash receipt, nine assertions and all-feature check remain pending. No unrelated process was interrupted and no sibling artifact was removed.

Session `79007` is now terminal **Nx exit `1`, native `SIGABRT`**. The build succeeded in `11m 23s`; all nine intended names were discovered exactly once in one compiler-produced executable. The first three SQLite laws passed `1/1` each (neutral lifecycle, bounds/identity/expiry/progress, and concurrent durable request winner). The fourth, `inference::sqlite::tests::gis_inference_sqlite_prepared_approval_survives_restart_and_reconciles_exactly_once`, aborted with a real stack overflow. The later four WAL laws, catalog runtime law and all-feature check were **not reached**. This is not a compile failure or timeout, and the successful three laws are not promoted to full ledger/WAL acceptance.

Executable: `🗑️generated/space-public-boundary-sol-target/debug/deps/semio_hub-5928798602853aed`; SHA-256 `0ba4f7b86b70be3f925c198fca0a847feb2e1eee7fd220c0f883b28973e828b8`. Exact build/list/per-law evidence remains in `exact-cargo-laws-wwMJTA/00`. Diagnosis is targeting the retained WAL/fixture future allocation path without increasing stack size or disabling the law. No additional native build has started yet.

## Memory backend constructor stack repair

Read-only disassembly of that exact executable locates nested fixed-table construction: `MemoryStorage::new` reserves `0xcf480` bytes (about 849 KiB), then `MemoryDbIoExecutor::default` reserves `0x1901b0` bytes (about 1.56 MiB). The fixture/replay futures themselves are roughly 24 KiB. These constructor frames exceed the normal test stack before WAL verification starts. The debugger launch attempt produced no trace within its bounded wait and was terminated by its exact owned LLDB/debugserver PIDs; no unrelated process was touched and no runtime debugger result is credited.

The memory executor's remaining six inline fixed arrays now use the same direct heap-backed fixed slices as its existing WAL/retired-WAL arrays. All eight capacities remain 64, the singleton catalog remains inline, and neither process nor operation ceilings were increased. The complete inline-plus-eight-table backing charge is computed and reserved **before** executor allocation. Registration independently measures the actual backing and requires equality with the reserved credit; failed registration retains the owner and its credit for normal retirement. Existing dynamic page ownership accounting is unchanged.

A new neutral DB memory-backing schema/fixture declares all eight tables, their exact capacities, maximum inline size, and exact/one-byte-short/zero remaining-budget cases. The new Rust owner law exercises real process-credit admission, checks the actual native layout against the reserved and registered owner credit, constructs `MemoryStorage` on the normal test stack, and requires terminal close to return its exact ledger entry. The Bun/AJV oracle independently checks the symbolic allocation budget with Node buffers; it deliberately does not claim Rust ABI byte-size equality. The registered ledger gate now runs this one exact DB law followed by the same nine exact hub laws, then the all-feature hub check. This source repair and law are not yet compiled or passing; the original `79007` SIGABRT remains preserved as the first failure.

Registered neutral session `10728` exits `0`: all previous phases plus memory tables `8`, admission cases `3` and schema-hostile cases `3`. Source/launch ownership remains the existing `os-hub` script/project and the two unchanged canonical launch entries; no regeneration or sibling registration change was needed. Current registered runtime session `50446` uses the same retained absolute target and explicit ticket artifact environment, one build job, normal stack and unchanged 20-minute helper ceiling. Its evidence starts at `exact-cargo-laws-JxBlCi/00`; neutral phases are green and the exact DB law build is active. No new native assertion is yet credited.

Session `50446` is terminal **Nx exit `1`, clean native assertion exit `101`**. DB test compilation succeeded in `1m 37s` and exactly one intended law was discovered. The normal-stack constructor, layout equality, exact/one-byte-short/zero admission and registered live-owner credit assertions all completed; `MemoryStorage::close()` then returned `DbError::Closed`. No stack overflow occurred. The nine hub laws and all-feature check were not reached. This clean failure is preserved, not converted to a success claim.

Source diagnosis found a distinct existing close-order bug: blocking task admission was not retained on the backend, so `close_db_io_backend` could retire its executor while the successful `BackendClose` task still needed `close_operation_step`. The backend registry now counts every admitted task (blocking and async-native), bounded by the existing 64 task slots. Admission is returned only after task/result/backend cleanup; backend retirement waits for zero pending admissions, and no new task can enter after close is requested. The terminal waiter is generation-fenced and idempotent if the same control already completed; it cannot close a reused generation. The literal ownership fixture and Rust law now also hold a production admission through a close opportunity, require the executor to remain retained, then return admission and exercise actual `MemoryStorage::close` plus repeated terminal wait. This correction is source-only pending the next exact rerun.

Registered rerun `79790` has now passed the exact DB ownership law **1/1**, normal stack, `0.01s`, with 589 nonselected names filtered after exact-one discovery. Executable `db-39ce2c88d307c030`, SHA-256 `3033b52574a58c1af2ee144449e9564a9279df3001811d2ce73d5b9155c5f821`; evidence `exact-cargo-laws-s7h16P/00`. This proves the constructor/accounting/close correction at the owned law boundary; it does not erase either prior failure. The same live handle has advanced to the nine-law hub build at `s7h16P/01`; hub assertions and all-feature check remain pending. No stack override or ceiling increase was used.

## Next approval integration preflight (read-only)

The coordinator's next bounded packet is the authenticated MAP proposal-to-command commit boundary, after the current nine-law/all-feature terminal. Current GIS source already has `GisMapInference::bounds_proposal`, which validates job identity/counts/geographic bounds and returns only the existing `GisMapMutation::CreateRegion`. No new accepted mutation variant is needed. However, the hub manifest currently links stdio, not GIS, and its generic `ensure_document` helper creates an `ArtifactSpec` without establishing this selected GIS typed-codec authority. A positive approval claim therefore still needs the exact catalog-bound typed document/provider path. Description presence, local inference helpers and generic document creation are not substitutes. The old audit's `📦️bin.rs` path has physically become `🚀️bin.rs`; scope identity already uses the length-framed `(space, document)` key.

The unused raw-id abandonment method still has no caller; the upcoming consumer must replace/remove that unsafe surface rather than mint absence authority from raw ids or `Ok(None)` alone. A valid positive witness remains genesis-only. An empty/missing/compacted history is not automatically an authenticated negative commit witness. Also, the writer's default 512-KiB rotation threshold currently exceeds the storage reader's 496-KiB ceiling: the current proof fails closed at that bound and does not claim arbitrary large-history acceptance. These are source findings, not new route or executor behavior; no inference authority was added during the active build.

## Parked DB retry boundary

Session `79790` is terminal **Nx exit `1`, fifth hub law runtime timeout**. The hub executable built in `16m 14s` and exactly nine law names were discovered. All four SQLite laws passed `1/1` each, including the previously stack-aborting durable prepared-approval/restart/reconciliation law. The next exact law, `inference_wal_proof_executes_literal_committed_transaction_scope_and_cancellation_traces`, reached the unchanged 60-second bound and was killed with `reason: timeout`, `status: null`, `signal: SIGKILL`; no failed assertion was printed. The other three WAL laws, catalog law and all-feature check were not reached. The successful DB memory owner law remains independently credited `1/1` above.

The fresh hub executable is `🗑️generated/space-public-boundary-sol-target/debug/deps/semio_hub-5928798602853aed`, SHA-256 `34e43c0f933aa5ad4938553f97d9ec582e37c34c613b320da83912a095e07f43`; exact evidence is `gis-inference-ledger-run/exact-cargo-laws-s7h16P/01`. No stale executable is substituted. A bounded direct diagnostic of that exact fifth law, session `83085`, sampled the Tokio caller parked and both pool workers idle, then terminated its verified owned PID at 41 seconds (exit `143`). That is diagnostic evidence only, not another acceptance run.

Source inspection identified a lost retry wake: a contended/saturated worker submission retained the exact retry generation but scheduled nothing to drive it. The caller's next poll examined only one unrelated maintenance slot, installed its waker and parked; idle workers could not resume the retained task. The production retry branch now registers one exact-generation callback in the existing worker timer wheel after rejection. Existing eight-attempt and 64-task bounds remain unchanged; stale callbacks cannot act on a reused generation. This is an owner retry bound, not a new claim about the shared timer wheel's aggregate capacity. No stack or budget ceiling was raised.

A new exact DB law deliberately fills one worker's queue, polls the retained operation once, releases the queue, then requires a terminal wake without any unrelated request or maintenance poll. It also requires terminal close to restore the ledger baseline. The neutral memory fixture now declares the eight-attempt/one-millisecond/terminal-after-release contract, and the independent Node event-loop oracle exercises the release/timer ordering. The permanent existing-script gate now selects DB `2` plus hub `9`, then all-feature check. This retry repair and its new law are source-ready, not yet compiled or passing; the WAL timeout remains unresolved until the exact registered rerun proves otherwise.

Registered rerun `74319` is active under the same canonical command and ceilings. Its neutral phases are green, now including memory schema-hostile `4` and timer-retry `1`; DB compilation is active at `exact-cargo-laws-TjtLgh/00`. The coordinator reported external deletion of the entire ticket-generated tree around 15:38. Read-only checks confirm this run recreated the target and receipt directories at 15:39 and is therefore **cold**, not warm. Earlier executable hashes/results remain recorded here, but their original generated files no longer exist. This lane performed no deletion and has not interrupted an unrelated cleanup process. The new generated target is being retained. Temporary `[DEBUG]` trace-stage messages were added to the bounded sixteen-case WAL law before the hub compilation starts, to identify any next stall without exposing private payloads; they will be removed once the failure is localized.

Session `74319` is terminal **Nx exit `1`, clean retry assertion exit `101`**. DB compilation and exact-two discovery succeeded. The memory ownership law passed `1/1` again; the new retry law failed after `2.01s` because the parked caller received no wake after release. Hub compilation, its nine assertions and all-feature check were not reached. This is new runtime evidence, not a successful timer repair.

The clean retry law exposed a second concrete scheduling defect. The native worker's deficit-round-robin selector returned `None` after a single scan when a queued lane had insufficient credit, and the worker treated that as idle. For the fixed 1,024-entry I/O queue, weight `4` against unit cost `8` inserted a roughly four-millisecond park between no-op jobs; the retained DB retry was queued behind seconds of needless idle waits. Native selection now completes at most eight bounded rounds before reporting idle, leaving lane weights, capacity, FIFO order and low-priority reserve unchanged. The cooperative wasm pool still exposes one scan per host turn and is not changed. The shared neutral scheduler fixture now includes the missing I/O lane; a direct native selector law consumes its literal deficit outcomes, and the existing cooperative host-turn law is selected alongside it. The registered gate therefore runs scheduler `2`, DB `2`, hub `9`, then all-feature check. No wait ceiling was increased. These latest scheduler changes remain source-only until the next registered rerun.

Registered session `89373` has now passed the two exact scheduler laws and both DB laws, `4/4` total, with normal stack and unchanged test deadlines. Its async receipt is `exact-cargo-laws-Ibu9fO/00`, executable `semio_framework_async-9879a817b61e6a35`, SHA-256 `2f4865ce39f56970751cc13cab352150bbe7509b1b9e6614a9d2efe79074a7ba`. Its DB receipt is `Ibu9fO/01`, executable `db-39ce2c88d307c030`, SHA-256 `e17f338238b09e64622802f64c743a93d7d34e5d1b69874010b184484fdad63b`. The new parked retry assertion is therefore green on the actual native worker after the bounded selector repair. The same live handle is compiling hub at `Ibu9fO/02`; its nine laws and all-feature check remain pending, so the original WAL trace timeout is not yet source-closed by runtime evidence.

The existing root cooperative scheduler oracle was discovered during this repair. Its physical schema path was corrected to `🧬️schema/🔣️.json` and its exact weight map now includes I/O `4`; the hub oracle reuses this existing exported implementation instead of retaining a duplicate. It requires exactly eight checks (six lanes plus two hostile schema cases). This is a narrow consumer cutover of the shared literal fixture, not a change to wasm pumping or native authority. A separate final-source neutral rerun is recorded next; it does not restart the active native build.

Final-source registered neutral session `60198` exits `0`, including shared scheduler checks `8`, chain cases `14`, and every earlier ledger/WAL/memory phase. The current gate's nine hub runtime laws are still compiling in `89373`; no new approval consumer, route or executor receipt has been added.

The naming owner subsequently moved `async/⏱️cooperative` to physical `async/🤝️cooperative` and updated both Rust includes and the root oracle base. Read-only resolution confirms the six-case fixture, exact schema and I/O row are preserved. Registered neutral rerun `29287` exits `0` on those current paths, with the same counts. The hub build remains on its original bounded handle; the earlier scheduler executable receipt is pre-rename evidence, not falsely relabelled as a new native run.

Before the hub crate compiled, the temporary trace logs were replaced with bounded trace-stage assertions: two seconds for storage admission and four seconds for retained verification, both naming the exact literal trace on timeout. This is stricter than the unchanged outer sixty-second law ceiling and yields an actionable clean assertion even when libtest captures successful output. No temporary `[DEBUG]` log remains in the owned WAL test. Runtime for this source is still pending in `89373`.

Session `89373` is now terminal **Nx exit `1`, hub build-stage timeout** at the unchanged 1,200,000-millisecond ceiling. `Ibu9fO/02/build.json` records `status: null`, `signal: SIGKILL`, `reason: timeout`; captured compiler JSON has **zero compiler errors and zero hub test executables**. The nine hub laws were not discovered or run, and all-feature check was not reached. The two scheduler and two DB law receipts remain valid independent results from this run. Owned Cargo PID `26936` is absent after terminal; no unrelated compiler or generated tree was interrupted or removed. This was a cold hub rebuild caused by the earlier external deletion, not a new source compiler failure or a passing WAL trace.

The coordinator released the next warm retry with the same existing registered target, explicit ticket environment, unchanged twenty-minute ceiling and exact scheduler `2`, DB `2`, hub `9` packet before all-feature check. Existing native fairness, interactive reserve, FIFO and finite-capacity laws were identified for broader regression coverage but are not substituted into this exact repeat or credited here. The post-rename native paths will execute from a freshly fingerprinted package test binary. No new script, project or launch command was added.

Registered warmed session `32401` is active and has passed the exact scheduler `2/2` and DB `2/2` again on the current renamed paths. Evidence is `exact-cargo-laws-xsuhhU/00` and `/01`; the unchanged executable basenames now have SHA-256 `ed51120e4adbaf5f49522cc7d26bd0a45106a77c7adceac0cbf917414c634540` (async) and `4044bb7ee937247fcf4fe96e177bae5e7536b05aa9e1819a3883d841f3802296` (DB). Its neutral phases all passed first. The same handle is building hub at `xsuhhU/02`; nine hub assertions and all-feature remain pending. No approval executor or abandonment authority was added while waiting.

Separate source-only lifecycle review: the shared pool fires its pending timers once during shutdown. A shutdown racing between a rejected submission and subsequent callback registration may require an explicit post-registration shutdown fence; the current new law proves queue release on a live pool, not whole-process shutdown ordering. This was sent to the independent auditor and is not relabelled as a runtime failure or patched speculatively during the current build. The existing WAL cancellation/retirement laws remain required for their narrower retained verifier boundary.

Session `32401` is terminal **Nx exit `1`, clean WAL assertion exit `101`**. Hub compilation succeeded in `18m 43s`, and all nine intended names were discovered exactly once. All four SQLite laws passed; the fifth literal WAL law failed in `0.15s` at fixture WAL append with `Unavailable("db I/O task capacity exhausted")`. This is neither the earlier parked timeout nor a stack abort. The other three WAL laws, catalog law and all-feature check were not reached. Hub executable basename remains `semio_hub-5928798602853aed`, current SHA-256 `2e9a1ed15192888683236338d548a4e94869ff475fc3d237d8bbc9582dc86d88`; build/list/exact evidence is retained in `exact-cargo-laws-xsuhhU/02`. Scheduler `2/2` and DB `2/2` remain separately green as recorded above.

The next TDD witness strengthens the existing memory ownership law with 128 sequential actual storage tasks while a caller retains one list result. It must preserve that exact list, avoid task-capacity exhaustion and return the terminal ledger baseline. The neutral schema now fixes this count and independently exercises task/result separation; it does not claim to model native scheduling. Production task close is currently only enqueued and opportunistically advanced by later submission/poll turns. Diagnosis is distinguishing incomplete task retirement from intentionally retained caller result ownership; neither capacity nor timing limits are being enlarged.

Registered TDD session `4222` confirms the generic defect: scheduler `2/2` passed, then the strengthened DB law failed cleanly at **sequential task 63**, with only one retained list, in `0.01s`. Evidence is `exact-cargo-laws-hEq3vT/01`; later DB/hub/all-feature phases were not reached. The neutral sequential `128` and hostile bound `5` checks passed first. This is not merely an omitted backend close in the WAL fixture.

The source repair adds one shared `DbIoTaskOperation::finish` funnel used by all five storage facades. It retains the result while advancing one cleanup opportunity per poll, returns plain results only after task retirement, and releases caller-owned results only after task payload/backend admission cleanup. The fixed close ring now rotates blocked heads and serializes its exact retirement turn; it cannot destroy a terminal value still awaiting its caller. List backing remains charged while a result lease is live and is freed before result handback. Queue phase publication now precedes worker admission so a fast worker's terminal phase cannot be overwritten by `Queued`. The existing 128-task witness additionally checks exact list contents and retained list byte credit on every iteration. Capacities, stack and time ceilings remain unchanged. These repairs are source-only until the next registered run.

Registered repair run `75673` has passed scheduler `2/2` and DB `2/2`. The strengthened memory ownership law executes its unchanged 128-task witness in `0.03s`; the parked-retry law also remains green. DB executable SHA-256 is `066bc63af4435d82be0f0494c6c8ef88ace8ddc13604114bd08f7badb2276059`, with exact receipt at `exact-cargo-laws-O3kwM9/01`. All neutral phases passed first, including sequential `128` and hostile memory bounds `5`. The same active handle is compiling hub at `O3kwM9/02`; its nine laws and all-feature check remain pending. The prior task-63 failure is preserved above as TDD evidence, not deleted or reclassified.

That same run has now passed **all nine exact hub laws**, after a successful `11m 24s` hub build and exact-nine discovery. The four SQLite laws, sixteen literal WAL traces, dropped-caller/cancel/deadline ownership, fourteen hostile/full-frame BLAKE3 chain vectors with third-party Rust parity, hashing retirement/compacted-suffix boundary, and catalog projection matcher each completed from one executable. Hub SHA-256 is `63854c1c8ae5f0fa1a35fe9b295fc9f858be4ec3fe2416594a1754af91ad713d`, exact receipt `O3kwM9/02`. Total selected native laws are `13/13` with the two scheduler and two DB laws. The same session is still running its final `cargo check --all-features --bin os-hub`; no whole-gate terminal is yet claimed.

This runtime evidence supersedes the earlier source-only qualification specifically for the tested genesis-rooted hash-chain verifier and retained cleanup. It does **not** establish a compacted authenticated anchor, arbitrary-size history acceptance, a GIS provider/executor receipt, production route authorization, typed approval submission, restart process recovery or model-driven MAP execution. The unsafe unused raw-id abandonment API remains unconsumed pending its explicit replacement/removal in the next integration packet.

Session `75673` is terminal **Nx exit 1**, after all **13/13** exact native laws passed. Its final `cargo check --manifest-path Cargo.toml --all-features --bin os-hub` hit the unchanged **1,200,000 ms** budget and returned `spawnSync cargo ETIMEDOUT`. Read-only process samples showed active stdio compilation, not evidence of a target lock; the runner's generic “likely lock contention” banner is not adopted as the cause. No all-feature pass, compile-error count or whole-gate success is claimed. The target and exact `O3kwM9` receipts remain retained; no cleanup or restart was performed.

## Canonical approval command prerequisite

The next authorized integration packet starts with a schema-first, allocation-bounded full command decoder shared by preparation and committed-WAL target matching. The new neutral `✉️inference-command-v1` corpus fixes 8 KiB total, 256-byte text, 64 dependencies, 4 KiB each payload and safe-integer HLC bounds. Twenty literal vectors include full EOF, nonminimal/overflow varints, malformed UTF-8/control characters, dependency/payload limits, duplicate dependencies and exact actor/full-scope/mutation disagreement. Its payload remains explicitly protocol-only, not an executable GIS mutation.

Registered independent oracle `26846` exits **0**: all twenty new vectors, AJV validation and independent Node/WebCrypto canonical SHA-256 parity pass, with every prior neutral ledger/WAL/chain/catalog/memory/scheduler phase still green. Rust command changes and their runtime laws are not yet credited. The next provider work is GIS-package-owned; stdio's exact 26-surface closure remains unchanged.

The decoder is now source-wired into both preparation and matched-WAL verification. It borrows bounded input, uses fixed dependency slots, canonical re-encodes into a wipe-on-drop scratch owner, and validates actor/full-scope/mutation identity. WAL matched bytes are copied only after the 8 KiB preflight into a wipe owner; a matching hash cannot bypass full decoding. The unused raw-id abandonment API was removed, not replaced by an absence shortcut. The existing SQLite outbox law now requires prefix-valid trailing bytes to leave zero prepared rows; two additional exact laws cover twenty decoder vectors and three actual committed-WAL hash-matched hostile commands. These new laws have no native pass yet.

Registered `79133` is terminal **Nx 1 / Cargo 101**: the independent command twenty-vector oracle and all prior neutral phases pass, followed by scheduler `2/2` and DB `2/2`. Hub build stops before its new law selection on the shared graph producer's nested-output `ENOENT` at `graph/🤖️generated/◻️puzzle2d-default/🦀️.rs`. Evidence is `exact-cargo-laws-i5GQxA/02/build.stderr`; this is not a decoder diagnostic. The naming owner concurrently completed exact nested writer/preview/check handling and populated the current twenty-two files. My attempted duplicate patch failed safely; those functions and paths were preserved. Read-only registered graph freshness `94631` then exits **0**, nine manifests. A ticket-local tar snapshot preserves the current nested twenty-two before that check; no graph output was deleted by this lane.

The coordinator authorized a one-hour ceiling solely for the final all-feature build phase, matching measured shared dependency cost. Native exact build remains twenty minutes and every actual law remains sixty seconds. No stack setting, runtime timeout or skip was added. The next shared catalog prerequisite also removes `s.` stripping and aligns codec identity's descriptor-owner package hash with SHA-256, while the independent component `PackageRef` keeps BLAKE3. Six neutral identity-role cases and two existing real loader/resolver laws are being added to the same exact gate; this is still not a GIS provider or route activation claim.

Registered graph freshness `78458` exits **0**, including nine manifest freshness and **3 Bun tests / 60 assertions**. The nested writer remains the naming owner's implementation. This lane added cross-platform drive-prefix rejection and explicit Windows drive/UNC hostile cases, then made the existing freshness command execute that source oracle. No graph generation or output deletion was performed. The naming owner separately retains the prior twenty-two-file byte proof; the ticket tar here is only a current nested-output snapshot, not reconstructed old bytes.

Registered seventeen-law rerun `21547` is terminal **Nx 1 / Cargo 101**. Independent canonical-command **20** and catalog-identity-role **6** vectors pass with the earlier neutral phases; scheduler **2/2** and DB **2/2** pass. The hub build at `exact-cargo-laws-ulFiU4/02` stops before exact hub-law discovery on three stdio WAV `MutationLeaf source authority failed: No such file or directory` diagnostics for SetSnapshot, SetFmt and SetData. No new hub command/catalog native assertion or final all-feature result is claimed. The stdio owner and coordinator received the exact failure; retained targets and receipts were not removed.

The next commit-path census found an important semantic boundary in `db_artifact::diff_entries`: foreign schemas deliberately produce an empty pathmap touched set while their exact command bytes are persisted and relayed. `ArtifactHandle::submit` is therefore durable transport authority, not a GIS mutation interpreter or fresh GIS snapshot source. The GIS package-owned executor must validate and apply the exact typed CreateRegion against the catalog-bound retained base before submission, and a real typed receiver must prove the committed event changes the GIS document. Encoding a generic `db.pathmap.v1` replacement or counting a durable opaque envelope alone would not prove the requested GIS execution. This is a source finding, not a new runtime failure or a request to change generic DB semantics.

## GIS-owned inert native codec prerequisite

`✏️s/🔌️plugins/🌍️gis/📇️native-codecs` now contains the closed schema, literal two-row identity/protocol-byte corpus and opaque Rust receipt producer. Its private Map/Terrain selector cannot be replaced by a caller-supplied factory. Preview constructs both actual artifact declarations, requires the exact single codec capability, document schema and schema-scoped extension claims, and hashes the actual included pack protocol bytes. Consuming a receipt creates the typed `ArtifactCodec<P, Mutation>` and checks its schema/extension before assigning that exact protocol fingerprint. The returned identity projection is data, not receipt construction authority. Preview and consumption do not register codecs globally.

Registered neutral `21813` exits **0** with GIS receipt **2**, hostile projection **7**, AJV and independent Node/WebCrypto SHA-256 agreement, plus all prior neutral phases. The hostile oracle exercises missing/duplicate closure, wrong owner, bare kind, wrong schema/extension and zero fingerprint. It is not yet native hostile-loader parity. The Rust law `gis_native_receipts_bind_literal_two_codec_closure_without_identity_or_factory_substitution` is source-staged under the existing GIS test owner and checks literal identities against both actual typed factories; it has not compiled or run. The hub still has no GIS dependency or linked-provider entry, and no inference activation, typed approval committer, route or readiness claim follows from this source packet.

Current read-only WAV verification and the independent stdio owner both confirm all three renamed source/descriptor roots, aggregate includes and exact descriptor owner equality. Registered warmed seventeen-law repeat `37902` therefore started on the same retained target and has passed its neutral phases and scheduler **2/2** plus DB **2/2**. Its hub build is `exact-cargo-laws-oKSdUH/02`; hub thirteen laws and all-feature check are pending. GIS's new native factory law is outside this seventeen-law packet. The coordinator is concurrently adding two owned Flow mutation constructors in stdio; this may invalidate a later source fingerprint, but does not justify cancelling or relabelling the current process. No target cleanup was performed.

That same `37902` run has now passed **17/17 native laws**: scheduler two, DB two and hub thirteen. The new twenty-vector exact canonical command decoder, three hash-matched hostile committed-WAL commands, and two actual trusted-catalog loader/resolver laws are green alongside all prior SQLite/WAL/retirement laws. The hub executable is `space-public-boundary-sol-target/debug/deps/semio_hub-5928798602853aed`, SHA-256 `9284a6880df279cefb8b7d49a7665a5758770fbf9cb6212ab6cb74f319e7bc8e`; exact discovery, build and per-law evidence are in `exact-cargo-laws-oKSdUH/02`. DB SHA-256 is `76d08943097becfd146453f42dc4e0960fd607b99bc24142a4c0041358bcc960`; async SHA-256 remains `ed51120e4adbaf5f49522cc7d26bd0a45106a77c7adceac0cbf917414c634540`. The same handle is running its one-hour-bounded all-feature check, currently checking stdio; no final Nx exit or whole-gate pass is claimed. This runtime closes the canonical command and catalog hash-role prerequisites only, not GIS receipt execution, a route or approved GIS mutation.

The new inert GIS producer has a distinct existing-script-owned gate: `bun ./📜️script.ts nx run @semio-tech/gis-plugin:native-codec-check --skip-nx-cache`. Its uncached project target invokes only `📜️script.ts native-codec-check`; the shared exact-law helper builds the GIS library without the component entry, requires exactly one named native law and retains its executable/hash under the explicit ticket environment. The launch seed entry is order `411.142`, points at this lane's separate retained target and sets one Cargo job plus wrapper-disabled execution. It has not been run. This is intentionally separate from the seventeen-law ledger gate.

Canonical launch generation `72742` and registered freshness `39009` both exit **0**, using the existing `@semio-tech/plugin-registry` authority. Exact seed/generated inspection preserves GIS `411.142`, the other lane's member-history foundation `419.999`, and all four coordinator Norm entries `407.9`–`407.93`. No generator implementation or unrelated source entry was rewritten by this lane. The earlier other-lane freshness failure after my seed edit is superseded by these generated bytes, not counted as a runtime defect.

Follow-up source hardening limits the receipt schema to the two literal owned protocol paths, rejects duplicate rows, and carries the actual compiled Cargo package version in the immutable identity projection. The oracle resolves the package's workspace-inherited version using Bun's independent TOML parser and adds wrong-version rejection. Registered final-source neutral `27870` exits **0**, now GIS receipt **2** / hostile **8**, with all previous neutral counts unchanged. The intermediate exact-path/unique-row oracle `18074` also exited `0` at seven hostile cases. Native GIS receipt execution remains unrun; these data checks do not activate a catalog or prove CreateRegion application. All-feature `37902` remains on its original bounded handle, with an observed CPU-active owned stdio compiler rather than an inferred lock.

Session `37902` is now terminal **Nx exit 0**: all **17/17** selected native laws and the final `cargo check --manifest-path Cargo.toml --all-features --bin os-hub` pass. The final check finished in **29m 12s**, within its separately authorized one-hour build ceiling. Stdio, plugin-host, DB and hub emitted warnings, not a terminal compiler failure. The command/ledger/genesis-WAL/catalog foundation is therefore accepted at this exact registered boundary; no GIS provider, authenticated approval route, typed GIS mutation or AI/model execution follows from it.

The distinct registered GIS package gate started as session `6903` with the same retained absolute target, one Cargo job and wrapper disabled. It uses `SEMIO_TEST_ARTIFACT_DIR=<ticket>/🗑️generated/gis-native-codecs-exact`, the unchanged twenty-minute native build and sixty-second exact-law bounds. Native GIS factory discovery and execution are pending. No second Cargo in this lane overlaps the completed all-feature run, and no generated target was removed.

The next hub-side selection contract is staged under the existing native-provider owner's `🧪️fixtures/🌍️gis-v1`: eight literal rows for exact GIS ownership, cross-package/plugin, unknown provider, compiled-version mismatch, cancellation and both exact/elapsed deadline boundaries. Registered independent oracle `20600` exits **0**, admits exactly one row and retains all earlier neutral counts. The actual linked-provider law is now in the existing trusted-catalog test module and registered after the prior thirteen hub laws. It deliberately targets the currently missing GIS entry; it has not run, and the loader/provider production code has not yet changed. The queued hub gate therefore contains eighteen laws, while the completed `37902` result remains exactly seventeen. No current-source eighteen-law pass or GIS activation is claimed.

While `6903` compiles, source inspection finds `ArtifactCodec::of` sets its extension from `P::envelope_id()` rather than `P::EXTENSION`. GIS deliberately has distinct `gis.gismap` envelope identity and `gismap` file extension; Stdio already assigns its explicit extension after factory construction. The current GIS receipt law is expected to expose this mismatch, but no assertion has run yet and the active source is preserved. The narrow repair will use the typed artifact's extension authority, not change envelope identity or weaken the literal declaration comparison. Separately, the generic codec apply thunk creates a generic store without the retained close path required for this new executor; constructing the codec is not proof of bounded GIS application. The approval consumer must use a retained typed base and real typed CreateRegion/inverse, not merely invoke that generic thunk.

GIS session `6903` is terminal **Nx exit 1 / build timeout**, not a factory assertion. The shared helper's `2RXdoe/00/build.json` records `status: null`, `signal: SIGKILL`, `reason: timeout` at the unchanged twenty-minute ceiling; GIS exact discovery and assertions were not reached. Owned Cargo `31784` and rustc `38558` are absent after terminal. The compiler was actively building Stdio under GIS's native guest feature set, and the warm target is retained. The independent two-receipt/eight-hostile phase passed first.

After that terminal, the source-proven extension mismatch was repaired: each private factory now derives the filename extension from its actual typed `ArtifactDsl::EXTENSION`, requires equality with the literal receipt, and assigns that value to the erased codec. Schema comparison, protocol fingerprint and the distinct envelope identifier remain unchanged. The native law existed before this repair but has **no observed failing or passing assertion** yet; the timeout is not relabelled as TDD runtime evidence.

The coordinator separately authorized a **one-hour build-only** ceiling for this GIS native gate, matching measured guest-feature Stdio cost under concurrent fleet work. The permanent GIS owner script now uses `3_600_000 ms` for its build while list and law bounds stay `60_000 ms`; other native gates retain their existing ceilings. Warm repeat **95245** is active on the same target, receipt `gis-native-codecs-exact/exact-cargo-laws-xAoeLl/00`. Its independent two-receipt/eight-hostile phase passed; no native assertion yet. No foreign compiler was interrupted.

Hub source now declares the GIS package dependency and contains a **private, unregistered** GIS preview adapter. The loader forwards its selected package version; the adapter compares it with each opaque producer receipt, checks exact owner/unique closure/schema/hash/typed codec equality, and checkpoints before every receipt consumption. The static linked inventory still contains only Stdio; GIS remains deliberately absent pending actual factory proof and the selection red-law cycle. The Stdio twenty-six receipt implementation is unchanged, and this packet does not assert a new Stdio compiled-version binding. The new dependency/adapter have no hub compilation result yet; `37902` is the prior seventeen-law foundation snapshot, not acceptance of these newer source additions.

The P0b client prerequisite is now schema-first under `🧬️schema/✅️approval`: `InferenceApprovalRequestV1` accepts only schema/version, job ID and offered proposal SHA-256, with the existing 1 KiB request ceiling before JSON allocation. No caller actor, scope, mutation, command, frontier, catalog grant or WAL witness is admitted. The language-neutral fixture contains twelve malformed/forged-authority rows plus exact/max-plus-one byte boundaries. Registered independent Bun/AJV/Node oracle **18067 exits 0** (one positive, twelve hostile, two byte boundaries), with every prior neutral phase still green. One exact Rust law is mounted and registered before the GIS selection law; the next hub gate now selects **nineteen** laws. Both new laws remain unrun, and this DTO has no production approval route or execution authority. GIS native `95245` remains active on its original one-hour build handle.

Terra independently reread this staged source and found no new defect in the narrow inert preview/closed approval-request boundary. Its review confirmed the fixed two opaque receipts, declaration/extension/hash revalidation, exact selected GIS version/owner/unique factory and artifact identities, codec equality and checkpoints, and the 1 KiB job/digest-only request. It also confirmed that the linked inventory still excludes GIS and explicitly withheld execution/route/approval authority. This is independent **source review**, not native or process acceptance; `95245` remains pending.

Read-only commit-lock preflight: current document socket processing holds `socket_live_authority`'s subject admission guards across `handle_client_frame` and its Fsync submission. A new approval actor must not acquire a document submission lock and then those subject locks in the opposite order. Both write paths need one documented order (subject admission, full-scope submission admission, fresh authority/frontier validation, Fsync), with the expensive provider execution and committed-WAL replay outside the submission critical section. Existing Session publication explicitly drops its admission guard before entering the live socket loop, so there is no evidence here of a permanent Session guard leak. No command-path or lock implementation was changed during this source preflight.

## Live Author Read Prerequisite

The private `💡️inference/🛂️authorization` check now source-connects immutable job identity to `HubDirectory::socket_session_binding`. It requires the original session, user, authorization generation and exact full document scope, and admits only current Author membership. It races the existing operation cancellation/deadline, checks again after the durable read, refreshes the injected wall clock before accepting session expiry, and fails closed on clock regression. Unavailable/backend status returns a bounded storage error without changing any job or granting a retry privilege. The function returns only `()`: its result is not a retained capability and cannot substitute for the shared subject/document submission guards and immediate pre-Fsync revalidation.

The schema-first sixteen-case corpus covers exact Author, cross-space equal document, cross-document, wrong user/session/generation, spectator/removal/revocation, exact/past expiry, expiry during the read, clock regression, cancellation/deadline and negative time. The staged Rust law creates actual SQLite users, a Studio and membership via directory commands, then issues/revokes the real session and checks no authorization read changes the event head. It has **not compiled or run**. Registered independent oracle `55209` exits **0**, sixteen cases/one admission through Bun SQLite plus AJV; intermediate `29453` passed the initial fourteen cases. The existing registered hub gate now selects twenty laws (the prior seventeen plus approval DTO, Author read and GIS selection). The twenty-law current-source gate is unrun; `37902` remains exactly seventeen and all-feature green on its older source snapshot.

GIS native session `95245` is still active on its original one-hour build handle. The latest owned process census at roughly twenty-seven minutes shows Stdio rustc CPU-active with no captured compiler error, executable or native assertion. No source in that GIS build dependency closure was edited by this lane while it ran; the new prerequisite changes only hub source and fixtures. Warm targets, receipts and sibling generated outputs are retained.

## Real Directory Identifier Correction

The real SQLite test preflight found that `time_ordered_id()` emits thirty-six-character UUIDs, while the original inference identity and WAL target validators required thirty-two hex characters for every user/session/space/document ID. That would reject every normally issued directory session before Author lookup. Request IDs, deterministic job/mutation IDs and SHA-256 fields are different domains and retain their exact existing hex forms.

The server identity contract now uses one closed, ASCII opaque-identifier alphabet (`A-Z`, `a-z`, digits, dot, underscore, colon and hyphen) with a 96-byte maximum, including nonempty frontier edit IDs. This bounded inference subset supports the actual directory UUID and named identifiers without coercion or alternate compatibility decoding. Actor separators, path separators, controls, whitespace and non-ASCII values are rejected. The full framed document key and original-session actor stay below the unchanged 256-byte canonical command text ceiling even at every identifier maximum; the 8 KiB command, 4 KiB identity JSON and other operation ceilings were not increased. The same validator is used by identity admission and WAL scope/actor preflight. Empty head edit remains valid only at ordinal zero.

The schema-first corpus now has eleven values across five identity fields, plus exact composite byte checks. The existing SQLite identity law and WAL target law consume it; the new real-directory Author law uses actual issued UUIDs. Registered `96682` was the isolated clean RED at `directory-uuid/userId` with all other identity fields valid. Earlier `80984` also failed there but had a noncanonical test baseline head ID, which was corrected before the isolated repeat. `43578` and `48563` passed the initial ten-value corpus. Final current-source independent oracle **97867 exits 0** with server identity **11×5**, composite bounds **2**, request hostile **13**, approval hostile **14**, Author **16**, and all earlier corpus counts unchanged. Added trailing-newline negatives passed without requiring another production parser repair. Nx's historical flaky-task notice followed the deliberate RED/GREEN source change; it is not evidence of an unchanged-source nondeterministic assertion.

These schema/WAL/Author changes still have no native result. The queued registered packet remains twenty exact laws, with the identifier vectors added inside existing selected laws. At roughly forty minutes, GIS `95245` remains on the original bounded handle with CPU-active Stdio code generation and no diagnostic/assertion. No compiler, warm target or generated sibling was removed.

The coordinator's initial-composition dependency is recorded, not implemented in this AI packet. Terra's `📓️terra-initial-composition-authority-seam-audit.md` locates authenticated creation authority at the live open-plan/socket seam and finds that `VerifiedDocumentOpenSelectionV1` currently drops the verified app's full dialect. A future retained initial/base receipt must preserve that exact app dialect and bind Author, original session generation, full scope, descriptor and catalog; client `is_author`, surface `instanceId` and cloneable plan `write` are not construction authority. This lane does not add initial composition, modify the other lane's selection contract, or treat a fresh generic DB document as an authenticated typed GIS base.

## GIS Native Compiler Frontier

Session **95245 is terminal RED: Nx 1 / Cargo 101**, `reason: exit`, no signal, before its one-hour build ceiling. Stdio code generation completed and GIS lib-test compilation produced **193 errors / 74 warnings**. There is no executable artifact, exact-law discovery or native assertion. The independent two-receipt/eight-hostile phase remains green. The exact compiler JSON and status are retained in `🗑️generated/gis-native-codecs-exact/exact-cargo-laws-xAoeLl/00`; owned Cargo 42920 and rustc 43553 are absent after terminal. This is a real compiler failure, not the earlier twenty-minute timeout and not a lock inference.

The diagnostic census is 35 derive/attribute errors, 14 removed testkit function references, 110 E0277 trait errors, 22 E0599 method errors, six E0308 mismatches, five E0609 field errors and one ambiguous E0034 call. No diagnostic points into the new native-codec receipt module. Most roots are stale test-only serde assumptions and old async/editor calls, but `GisMapInference::bounds_proposal` also has a real production `serde_json::Value: ToValue` mismatch. That encoder must use the first-party value contract, not a compatibility impl. Test repairs must preserve neutral/third-party comparison, inverse and lifecycle assertions. No current-source GIS native, twenty-law hub, all-feature or execution acceptance follows from this failure.

The encoder now constructs its fixed five-point ring directly as first-party values after its existing finite/geographic bounds checks. The receipt law was moved intact to an explicit `native_codecs` Cargo integration target importing the public GIS API; the permanent `native-codec-check` still selects exactly that one law. This is a narrow public-factory boundary, not a skip or pass of the broad GIS library suite: those tests remain present and their 193-error receipt remains red. Registered repeat **60875** is active with one job, the retained target, one-hour build and unchanged sixty-second list/law limits. Its receipt is `gis-native-codecs-exact/exact-cargo-laws-umVwhl/00`; the two-receipt/eight-hostile oracle passed before the build.

Bounded library-fixture source repairs separately correct fourteen mutation-law paths to `os_spr::testkit` and await every law; the controlled inference test is now async so its inverse check really runs. Seven JSON fixture-to-value calls use the existing explicit value conversion instead of a removed `ToValue` assumption or null fallback. Eleven already-existing test-only serde derives are fully qualified; no runtime serde trait, compatibility alias or test exclusion was added. These changes have no broad-library compile/runtime credit yet. A further semantic hazard remains deliberately open: the current first-party JSON string writer is infallible and maps nonfinite values to null, whereas direct scale laws require rejection so NaN cannot become override removal. That assertion must not be weakened or hidden by a test-only rejection shim.

The controlled-inference law was subsequently moved into the same public integration target, preserving its actual callback trace and adding an explicit typed forward/inverse application plus independent `geo::BoundingRect` comparison. It no longer needs the async testkit inverse helper. The permanent gate now selects **two** public laws; active `60875` loaded the earlier one-law selector, so its eventual result can credit only that original selection. The new law remains unrun. Registered current-source neutral **22914 exits 0**, adding literal controlled proposal one, independent bounds one and interruption traces three via AJV/Bun while preserving every preceding corpus count. This is not a hub execution or approval authority claim.

The same fixture now adds seven symbolic rejection vectors: invalid job identity, duplicate proposal ID, stale counts, absent/nonfinite/out-of-range/reversed bounds. Direct Bun/AJV preflight prints `[DEBUG] GIS direct hostile preflight: schema=1 rejection=7`; this is diagnostic source evidence only. A registered root-router rerun stops before target dispatch because the shared repo-library discovery barrel exports no `generatorPreviewResourceLimits`; no new neutral result is credited, and `22914` applies to the prior positive/interruption source. The shared export failure does not affect already-loaded `60875`.

The shared export was subsequently restored by its owner. Registered current-source neutral **35149 exits 0**, including controlled proposal literal one, independent bounds one, interruption three and rejection seven, with every preceding inference/ledger/WAL/catalog/memory/scheduler corpus unchanged. It supersedes the direct preflight and the pre-rejection `22914` only for neutral current-source evidence; it does not add native, route, approval or model-execution evidence.

The remaining UI fixture census is intentionally not treated as a mechanical `.await` migration: the current SDK explicitly makes `new_app` registry-less/store-only, whereas these GIS UI tests expect typed dispatch/render without an exact registered factory. They require real registered retained ownership and close semantics before runtime acceptance. The coordinator's stable parent-dialect source `29257` is preserved; the next hub twenty-law gate retains the actual trusted-catalog generation/resolution law, while later `open-plan-server-check` native eight remains separate and unrun.

Registered `60875` is terminal **Nx 1 / Cargo 101**, not timeout. Stdio completed, then the current two-law integration source produced one E0597 before executable discovery: the neutral fixture's borrowed interruption error text was passed to the framework error constructor that requires a static code. No native assertion, including the loaded original receipt selector, ran. The source fix maps the schema's closed `cancelled | expired` taxonomy to those exact static literals; unknown values remain terminal. The warm target and `exact-cargo-laws-umVwhl/00` receipt are retained for the current exact-two repeat.

Registered current-source repeat `26771` is terminal **Nx 1 / Cargo 101** before executable discovery, exact-law listing or assertions. The shared Stdio crate still loaded an old `🖼️bmp` include while the taxonomy owner was completing the physical move to `🪟️bmp`; evidence is retained at `🗑️generated/gis-native-codecs-exact/exact-cargo-laws-042LFE/00`. The owning taxonomy lane subsequently completed all Stdio mounts and its current-source census reports zero old references; direct reread confirms the mount and physical root now agree. This is preserved as a transient shared-source failure, not a GIS compiler result. Registered exact-two retry `51355` is active on the retained target with one Cargo job, wrapper disabled, the one-hour build-only ceiling and unchanged sixty-second list/law ceilings. No native assertion is yet credited from it.

Session `51355` is terminal **GREEN, Nx exit 0, exact 2/2**. The owner gate built and discovered one current-source integration executable, `native_codecs-045126a890fb7158`, SHA-256 `795e6eb3ae10267f436cd982a807eb20fb8722b269105f92bfbcae057ba093a7`. Both the literal two-codec private receipt closure and the actual controlled inference/proposal/inverse/interruption law passed under the unchanged sixty-second law bounds. Independent phases also passed receipts `2`, hostile substitutions `8`, literal proposal `1`, bounds parity `1`, interruption traces `3` and rejection vectors `7`. Exact build/list/law evidence is retained in `🗑️generated/gis-native-codecs-exact/exact-cargo-laws-dDPtZo/00`.

Only after that native receipt result, the Hub's fixed `NativeCodecProviderSetV1::linked` inventory admitted the exact `gis` / `semio:gis` entry that already consumes and revalidates those opaque receipts. The Stdio 26-receipt closure remains unchanged, VCS remains absent, and no dynamic or caller-supplied provider form was added. This is source-ready for the queued Hub twenty-law catalog/ledger gate; it does not by itself authorize an approval route or model execution.

Registered Hub packet `39833` is terminal **Nx 1 / Cargo 101**, not timeout. All final-source neutral phases passed, then the two scheduler and two DB laws passed **4/4** under their exact selectors. The Hub library build stopped before executable discovery on a concurrently moved Stdio glTF mutation include at barrel line 2349 (`🔗️🧬️bind-morph-target-attribute`); Hub assertions are **0**, and the all-feature check was not reached. Exact build evidence is retained in `🗑️generated/gis-inference-ledger-run/exact-cargo-laws-EzRvpn/02`; scheduler and DB receipts are in sibling groups `00` and `01`. The taxonomy owner is reconciling this in-flight glTF cutover, so no stale path was patched and no retry was started. The Hub's sixteen laws, including complete parent-dialect catalog identity and GIS provider selection, remain pending current-source runtime evidence.

Registered retry `67994` is terminal **Nx exit 1** before any Rust exact-law build, discovery or assertion. All six independent GIS neutral phases printed green, then the Hub owner script attempted to import the development helper through a Unicode variation-selector spelling that did not match the physical `🧑‍💻dev` directory. Scheduler, DB and Hub assertions are therefore all **0** for this run, and all-feature was not reached. The four Hub-script consumers now use the exact physical directory spelling, including the BLAKE3 oracle, Vite config and UI-script check paths. This terminal is kept separate from the superseded glTF failures and from native GIS acceptance.
