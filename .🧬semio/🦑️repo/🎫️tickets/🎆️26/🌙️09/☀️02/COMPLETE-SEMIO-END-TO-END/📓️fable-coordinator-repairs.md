# Fable Coordinator Repairs — 2026-09-05

Small cross-lane repairs made directly by the Fable coordinator (session `c34c334c`) because they blocked registered gates for every lane. Each is a derived-data or re-export fix, not a design change.

| Time | Repair | Files | Evidence |
|---|---|---|---|
| 18:20 | Restore the four directory view type re-exports dropped when `DirectorySpaceDetailV1` was retired (`DirectorySpaceListEntryV1`, `MemberSpaceViewV1`, `PublicDocumentCatalogEntryV1`, `PublicSpaceViewV1`) | `🧰️framework/🛍️products/💻️os/🔨️modules/📇️directory/🟦️.ts` | targeted `tsc` over the os index + directory module reports no missing-export error (remaining hits are scratch-config artifacts: node types, `.ts` extension flag) |
| 20:45 | Stdio native-codec projection still named the pre-rename `🗿️artifacts/🧊️obj/…` protocol path (directory is `🗽️obj` since the emoji-uniqueness repair); the stdio TypeScript index exported `obj` from the same dead path | `✏️s/🔌️plugins/🗄️stdio/📇️registry/🧬️schema/📜️native-codec-factories.json`, `✏️s/🔌️plugins/🗄️stdio/📦️packages/🟦️typescript/🟦️.ts` | ENOENT in `native-openable-catalog-provider-check` gone |
| 21:05 | Four snapshot protocol files (`bcf`, `json`, `pdf` 1.4, `xlsx`) were rewritten by a peer stdio format wave between 03:36 and 04:34 without repinning `pack_schema_hash`; the Rust registry derives that hash from the protocol bytes and rejects a mismatched definition, so the owner definitions, the projection and the hub provider fixture's `openTarget.packSchemaHash` were repinned to the current byte digests | `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/{💬️bcf,🧾️json,📖️pdf,📕️xlsx}/🧬️schema/📜️artifact-definition.json`, the projection above, `🌎️hub/🗿️artifact-authority/📇️native-openable-provider/🧪️fixtures/🪪️v1/🔣️.json` | `bun ./📜️script.ts native-openable-catalog-provider-check --oracle-only` (hub cwd): `owner-receipts=26 protocol-webcrypto=26 targets=1 hostile-denied=13 no-partial=13`, plus the VCS lane's two oracles, exit 0 |

Still stale and left to the stdio/emoji owner: the generated stdio descriptors `✏️s/🔌️plugins/🗄️stdio/📇️registry/🔣️.json` and `🗿️artifacts/🔣️.json` still carry one `🧊️obj` path each and must be re-emitted by the registered `describe` target, not hand-edited.

Consolidated compiler evidence: `cargo check -p semio-hub --bin os-hub --tests --features sqlite` in a private target dir compiled the whole dependency closure including `semio-s-plugin-stdio`, then stopped on one peer error outside any Fable file — `🧰️framework/🛍️products/💻️os/🔨️modules/🛢️db/🗜️compact/🦀️.rs:1249 E0308 expected &WalWriterPermit, found &ArtifactId` — from the Sol fleet's live WAL-writer-permit packet (every db module rewritten 20:10–20:35). The warm private target (`scratchpad/fable-coordinator-hub-target`) makes the rerun cheap once that packet settles.
