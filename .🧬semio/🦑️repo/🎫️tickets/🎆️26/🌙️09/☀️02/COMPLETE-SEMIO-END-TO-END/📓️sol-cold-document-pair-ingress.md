# Cold Document Pair Ingress

## Scope

This packet owns the schema/kernel/actor vocabulary, reactor-local retained byte owner, dedicated native-host poll input, and the browser worker's private verified-pair transfer. It does not claim a real GIS component, actual Wasmtime component invocation, mounted Chromium renderer, or durable Map edit.

## Contract

The neutral corpus fixes a 64 KiB page, 64-page and 4 MiB aggregate ceiling. Its exact 2 MiB Pack plus 2 MiB SPR pair is independently checked by repository SHA-256, Node `crypto`, WebCrypto and AJV 2020. Eight hostile rows cover duplicate/reordered pages, foreign lifetime, descriptor or length substitution, short middle page, terminal mutation and modulo-slot collision.

The retained reactor owner:

- validates the header and first page before reserving capacity and never pre-zeroes the full pair;
- charges actual reserved vector capacity and page slots against one reactor-wide 4 MiB/64-page budget;
- rechecks the exact Live lifetime on every page, load admission and post-await completion;
- updates Pack, SPR and aggregate SHA-256 incrementally as each single page is accepted;
- remains structurally mounted through the load await by sharing one exact owner and immutable file allocation with the load grant;
- turns revocation during load into a stale/fault result rather than Applied;
- retains capacity through Applied/Faulted/Loading and releases it only after explicit cleanup;
- wipes at most 64 KiB per `close_step`; registry teardown requires every structural byte and close owner to reach bounded terminal release.

The baseline frontier requires a nonempty active checkpoint edit identity and `last_commit_seq <= head_edit_ordinal`; it does not synthesize a best-effort genesis head. Cold ingress is the fourth exact guest-lifecycle close participant. `Event::ColdDocumentPairPage` is separated from generic WIT events and projected only into the dedicated optional poll input.

The browser worker retains a private `VerifiedColdDocumentPair` bound to the exact runtime key, config, socket, client instance, execution-target lease, checkpoint descriptor/aggregate, published Pack/SPR owners, and full baseline frontier. It opens the guest lifetime, requires the exact Captured receipt, ACKs it, then transfers one page per turn. Every intermediate receipt must be the exact page cursor and the terminal receipt must be the exact Applied lifetime, transfer generation, frontier and aggregate hash. Hub bootstrap network chunks and 64 KiB guest pages are intentionally independent counts.

## Native Evidence

Registered source/native commands:

- `@semio-tech/framework-plugin:cold-document-pair-ingress-check`
- `@semio-tech/framework-plugin:cold-document-pair-ingress-native-check`

Native artifact `cold-document-pair-ingress-exact/exact-cargo-laws-E6PBfP` is GREEN10:

- actor laws GREEN3, executable SHA-256 `969eeb254169c032f9fe0e337d45756afae2c6221cbdd287c`;
- plugin laws GREEN7, executable SHA-256 `48702fce336560a218333cafc096aeb7db6220162d38e3cd4c6c0626e22a9d64`.

Root's separately owned native-host adapter gate `Zc4H75/00` is GREEN14, executable SHA-256 `b9846fd97ec2155914be64f87b624330e923beef1fddd949f319755aeb5a4338`. It covers the dedicated WIT split/status conversion and activation cleanup. This is cited as peer evidence and is not represented as an actual Wasmtime guest journey.

## Browser Evidence

Registered browser command:

`@semio-tech/framework-os:cold-document-pair-browser-check`

Receipt `c52509` is GREEN2. One exact law covers authenticated Session activation, stale socket replacement and terminal child retirement. The second passes the full 4 MiB neutral pair through the real `MessageChannel` child protocol as 64 ordered 64 KiB transferable pages, proves the lifecycle ACK precedes the first page, checks all 63 intermediate page receipts plus terminal Applied, distinguishes two Hub network chunks from 64 guest pages, and observes zero reserved child capacity after close. The child component is a controlled mock; no real component or render claim is made.

The strict TypeScript WIT status parser is mounted in the actor suite. Receipt `be62c5` is GREEN1 against six neutral statuses, five semantic hostiles, AJV structural validation and unknown-field denial. Missing cold status now fails closed instead of being coerced to Idle.

The generated shard worker now rejects `frame` before budget acquisition/interpretation when `activationGeneration` differs from the retained actor generation. Its stale-generation/current-generation runtime fixture is GREEN1 (`4bd123`).

The initial Session rerun exposed a detached-owner cleanup defect: transferred zero-length config/quotas views were unconditionally filled in `finally`, throwing after a valid ACK and revoking the replacement lease. Cleanup now wipes only still-owned views; the exact Session law is GREEN (`aab729`).

### Transactional UI patch handoff

The browser worker now retains one exact `UiPatch` decision owner after the cold pair reaches Applied. It emits a private offer bound to runtime key, activation generation, verified surface, session window, sequence and canonical patch receipt, then awaits one bounded Shell result. The Shell resolves the same retained document store, applies the patch transactionally, and returns either the exact applied receipt/revision or the exact rejection type. Only that result becomes the next guest `patch-ack` or `patch-rejected` turn; refusal never invents a receipt and never wipes a pending decision silently.

The private patch decoder admits only exact canonical operation shapes and safe integer fields. It rejects unknown operation tags, extra fields, non-boolean disabled state, malformed child lists and string/fractional revisions before handing anything to the UI store.

Current registered browser-neutral receipt `00a355` is GREEN3: two protocol laws plus the full 4 MiB transfer law. The controlled child reaches Applied, offers a tiled-map root patch, receives one exact applied receipt, then offers a deliberately stale base revision and receives `revisionMismatch` on the next guest turn while the first visible store revision remains intact. This is a real `MessageChannel` and `UiDocumentStore` transaction but still a controlled child, not a GIS Wasm or mounted Chromium acceptance.

The native reverse WIT bridge no longer discards guest patches. One shared converter now drains the sync `emit-patch` sink and returned patches, and the async import sink and returned patches, into the existing bounded `UiTurnPatches` owner. It decodes every one of the eleven canonical operation variants, requires canonical Pack bytes, enforces operation/count/byte bounds, exact component/receipt instance and nonempty surface identity, and receipt pairing before publication. Its registered schema/source gate is GREEN (`ajv=1 cases=7 component=4 ops=11 sync=1 async=1`). Focused native receipt `exact-cargo-laws-7H4AGq/00` is GREEN6 from executable SHA-256 `dfbac660f50a6c8eec262da75fb2f98f3e6015387cf5b6ddb0079bd81dd25ace`. The registered scale producer built the current WIT actor component to 1,022,858 bytes. Real Wasmtime laws now qualify Captured/ACK lifecycle, returned and `host-async.emit-patch` paths, exact surface/receipt, one-claim shard token ownership, dual-channel atomic refusal, and malformed-import drain followed by clean sequence-2 recovery. This is a genuine component-host boundary, but remains distinct from GIS and from a mounted browser.

The repository-wide React typecheck remains red on unrelated/current shared errors. After correcting this lane's readonly port syntax, actor fixture inputs and socket-grant replacement, the focused patch module no longer appears in its diagnostics; that is not promoted to a green package typecheck.

## GIS actor producer frontier

The registered `os-hub:browser-actor-gis-describe-check --source` initially failed because the permanent launch omitted the ticket artifact root required by its real subprocess law, then failed because the directory did not exist. The source launch now supplies the ticket-owned path and the script validates and creates it before the shared process corpus. Current receipt `85c749` is GREEN: AJV=1, TypeScript=1, fast-deep-equal normalization=9, an exact retained-closure/replacement hostile law, and eight real subprocess success/compiler-error/stderr-error/spawn-error/cancel/deadline/output-limit cases. Generated launch freshness receipt is GREEN (`plugin-registry:check-generated`, 59 plugins, 60 playgrounds, 45 framework packages).

On native success the producer now retires its cold Cargo/Vite scratch while preserving only the qualified `component.wasm`, `descriptor.semio` and `closed-actor.mjs` under `🗑️generated/gis-child-real/qualified-gis-actors/<actor-sha256>`. It validates all three regular files against the current component/descriptor/actor receipts before and after atomic directory publication, reports the path in the native receipt, and rejects an existing replaced closure. This is evidence staging only; it neither publishes a Hub current pointer nor grants caller-selected path authority.

No genuine GIS component or closed actor is retained in the ticket yet. Existing `generation-stage-*` three-byte component files are neutral fixtures and are explicitly excluded. The registered native producer is one-job, ticket-isolated and long-budgeted, but the latest admission census found two concurrent full workspace/renderer Cargo checks plus an existing one-job WASI plugin build already compiling the full Stdio dependency graph, with 2.25 GiB of 3 GiB swap consumed. Starting another cold GIS+Stdio producer at that instant would not be a bounded resource admission; its native receipt remains pending until that existing WASI producer releases.

## Registration Evidence

The browser command is registered in the OS project and launch seed at order `411.1499`. Plugin-registry generation receipt `0f5c67` regenerated the 59-plugin/60-playground/45-framework-package catalog and launch file. Immediate freshness receipt `a1d2ef` is GREEN: generated catalog and launch bytes are fresh. Generated launch line 6890 contains the new browser gate.

## Remaining Runtime Frontier

No available receipt yet establishes that an actual closed GIS actor consumes the Hub-published pair in Chromium, calls the real guest loader, mounts a renderer, or survives/revokes across a two-process Hub journey. The next valid proof must first complete the registered descriptor-selected GIS producer on current bytes, then use its authenticated actor body and checkpoint pair through the real worker/Shell route. It must not substitute a static module URL, a three-byte generation fixture, or the controlled child used by this packet.

## Genuine GIS component cold-map boundary (source milestone)

The GIS plugin now owns a schema-first component-cold-map-patch acceptance target. Its native law is deliberately narrower than the final browser claim but uses no mock guest: a fresh receipt-bound semio:gis WASI component must open and ACK its exact lifetime under Wasmtime, accept a real canonical GIS Pack+SPR pair page by page, return the exact Applied receipt, publish a decoded tiled-map@1 scene through the one-claim shard patch owner, accept the exact patch ACK, execute the addressed patchPositions command, then publish a higher-revision scene containing only the replacement marker. A sibling law substitutes the guest lifetime and requires cold-pair.not-live before any patch.

The permanent source target @semio-tech/gis-plugin:component-cold-map-patch-check is GREEN: AJV=1 SHA256=node+webcrypto hostile=5 markers=9. The hostile neutral rows cover package, lifetime, descriptor, action-owner and post-receipt component substitution. cargo metadata --no-deps --format-version=1 is GREEN after correcting the host dependency to the canonical 🛍️products owner. Source success is not component/native/browser success.

The native target @semio-tech/gis-plugin:component-cold-map-patch-native-check is registered with a ticket-owned fresh producer target/stage/diagnostic owner, produceFreshComponentV1, exact staged component+descriptor hash comparison, and two exact Cargo laws on the existing warm public-member-open-sol-target. Launch seed orders are 411.1431/411.1432; generated launch lines are 6815/6823. Plugin-registry generation and the immediate freshness check are GREEN.

No native invocation has started. The WGPU lane owns the single currently authorized Hub GIS/native-artifact build, and a root Hub build is active. This packet will not create a competing full GIS+Stdio graph. Therefore there is still no reusable genuine GIS WASM owner or native cold-map receipt; the final Hub-selected closed actor → Shell/Chromium path remains open.

## Retained Hub bundle admission

The Hub's existing trusted stdio+GIS materializer now joins the two exact cold-map component laws to the same immutable generation it just produced. It decodes the retained current bundle, requires the literal `packages/gis/component.wasm` and `packages/gis/descriptor.semio` closure, verifies both regular files against their retained lengths and SHA-256 digests, runs the exact GIS laws with those paths and digests, then reopens and revalidates both files before candidate Hub startup or current-pointer publication. There is no alternate selector, fixture path or standalone evidence-to-production promotion.

The materializer's former aggregate one-hour literal is replaced by the shared bounded `buildBudgetMs()` policy. Its native and process launch entries retain one Cargo job and now carry the established 24-hour build, 24-hour-plus-five-minute orchestration/command, 32 MiB build-stack and 256 MiB native-test-stack settings. The source launch now owns a ticket-generated artifact root because the neutral fresh-component subprocess corpus writes its evidence there.

Exact lightweight receipt:

- `SEMIO_TEST_ARTIFACT_DIR=.../🗑️generated/trusted-stdio-gis-source bun ./📜️script.ts nx run os-hub:trusted-stdio-gis-bundle-check --skip-nx-cache -- --source`
- terminal session `96956`, exit `0`: neutral bootstrap closure, fresh-stage/process hostiles, generation fence, codec/rotation corpora and `gis-component-cold-map-patch-source` all passed; the terminal explicitly retains the native materialization, candidate Hub and current-pointer nonclaim.
- `bun ./📜️script.ts nx run @semio-tech/plugin-registry:generate --skip-nx-cache`, session `19434`, exit `0`: 59 plugins, 60 playgrounds and 45 framework packages; generated launch refreshed.
- Immediate `@semio-tech/plugin-registry:check-generated`, session `81640`, exit `0`: generated catalog and launch bytes are fresh. Generated launch contains the source artifact owner at line 6599 and the long-budget native/process entries at lines 6607 and 6628.

The exact GIS component laws remain unexecuted because active unrelated DB/workspace/WASI/Stdio compilers make another GIS+Stdio graph inadmissible, and the WGPU lane's authorized Hub GIS native gate is queued ahead of this component gate. Consequently this milestone proves source/schema ownership and pre-publication ordering only; it does not prove a genuine GIS artifact, Hub candidate, browser actor, mounted Shell or Chromium Map render.

## Shell rebootstrap UI-owner continuity

An accepted `artifact-rebootstrap-required` frame now retires the exact runtime-keyed browser actor UI owner after the current document-entry/client-attempt fence and before discarding the mounted session. This prevents the replaced actor generation's retained `UiDocumentStore` from rejecting the fresh actor's base-revision-zero scene. A stale client-attempt frame remains inert and cannot retire the current actor owner.

The language-neutral document-opening-attempt corpus now includes three rebootstrap owner transitions: exact-attempt retirement, stale-attempt refusal, and exact-attempt behavior when no old actor exists. AJV validates the corpus and `fast-deep-equal` checks an independent transition model. A thirteenth source hostile deletes the production retirement operation and must fail the oracle. The schema-first gate produced expected RED `1bdd90` before the Shell repair. The registered `@semio-tech/framework-os:document-opening-attempt-check` then finished GREEN `9bfa25`: AJV=1, 12 opening transitions, three rebootstrap transitions, 13 source hostiles, and four focused Vitest laws (`checks=29`). This proves Shell source/controlled-worker ownership continuity only; it is not a two-peer socket, genuine GIS component, mounted Chromium, or rendered Map receipt.

## Controlled two-peer rebootstrap projection

The language-neutral `gis-map-peer-rebootstrap-v1` corpus fixes one exact shared Map scope, two distinct client attempts, the same published descriptor/checkpoint/pair/frontier for both clients, the expected inference Region, the lifecycle order and explicit nonclaims. AJV validates the corpus and `fast-deep-equal` checks an independent two-client reducer. Seven source hostiles require the worker's exact-scope rebootstrap clear, fresh socket-authority request, bootstrap start, Applied receipt and render drive, plus Shell retirement of only the accepted attempt owner.

The focused worker law sends the real production `RebootstrapRequired` and `ArtifactBootstrap` frame shapes through `handleHubFrame` for each isolated lifecycle. It proves that both clients discard the old pair/frontier/token, close the old socket, publish an exact client-stamped rebootstrap event, reach Live from the same inline canonical pair and retain byte-identical frontiers. A controlled `UiDocumentStore` patch then produces equal tiled-map scenes containing the expected inference Region.

Exact receipt `4b0732` is GREEN: AJV=1, independent peer reductions=2, source hostiles=7, focused Vitest files=2 passed/2 skipped, tests=6 passed/283 skipped and `peer-checks=10`. This is intentionally sequential and uses a controlled scene. It does not establish two simultaneous document sockets, the genuine GIS actor, Chromium, a visible canvas, or actual inference publication/refetch.

## Compiled execution-protocol identity

`PackageDescriptor` now requires `executionProtocol.appChannelVersion`, and the guest-side describe implementation sets it from the compiled Rust application-channel constant. The JSON/Pack descriptor verifier admits only the exact supported version and rejects a missing, null, empty, stale or extended protocol record before catalog publication. The host never adds a current-version label to previously built component bytes.

The exact guest-described protocol is projected through the trusted package, catalog-generation framing, document-open package, plan, execution-target lease, MCP Hub-selected tools/list metadata and the frozen GIS inference binding. Full lease equality includes the version. Neutral issue/plan/lease parsers reject unsupported versions, and the frozen GIS binding digest changed to `24a934f552343221c667cbda44c83ecad1720a13515efb5b789d64af56a2ec0f` because that compiled identity is now covered.

Lightweight source evidence:

- `os-hub:document-browser-actor-identity-check` GREEN: catalog rows 2, plan negative vectors 22, browser hostiles 25, lease manifest substitutions 49 and total lease hostiles 72.
- `@semio-tech/framework-os-mcp-rs:hub-live-catalog-check` GREEN: states 3, hostiles 8, no installed fallback, exact selected execution protocol projected.
- `@semio-tech/plugin-registry:test -- --run ✅️catalog-complete.test.ts` GREEN 12/12 with `SEMIO_TEST_BUDGET_MS=120000`; two earlier invocations hit only the default 15-second harness budget before assertions.
- `os-hub:browser-actor-gis-describe-check --source` GREEN: AJV, strict TypeScript, independent deep equality and eight bounded subprocess outcomes. It did not execute a genuine component.
- `os-hub:gis-map-frozen-binding-source-check` GREEN 54. It is a source/digest oracle only.

The browser's final raw-descriptor admission now independently decodes the canonical Pack bytes and requires an exact one-field `executionProtocol` with app-channel version 14, equal to the already verified lease, before the private execution-target lease can be minted. The positive neutral descriptor was regenerated from its decoded Pack value, changing its byte length from 607 to 656 and its SHA-256 to `fc70eb753a6b216707103bbf92cf0ff23998e63198ec558bf03d294b17458cbe`. A hostile v13 descriptor is served with internally rebased plan, manifest and actor source hashes, so the browser refusal cannot be explained by a superficial byte-digest mismatch. Cross-realm Fetch byte chunks are accepted only when `ArrayBuffer.isView` and the intrinsic brand both prove `Uint8Array`; legitimate Response streams therefore retain the same bounded wiping behavior in the jsdom execution environment.

Current browser admission receipts:

- registered `os-hub:document-browser-actor-identity-check` GREEN: AJV=1, positive=1, manifest substitutions=49, byte vectors=13, lifecycle=11, hostile=73, descriptor bytes=656;
- registered focused renderer Vitest target GREEN: two exact browser execution-target lease laws, including all 73 hostile substitutions;
- registered renderer TypeScript target GREEN after narrowing the decoded package to `DocumentOpenPackageV1`.

Existing checked-in plugin descriptor pairs predate the required field. They remain deliberately unacceptable until their own components are freshly built and `describe()` regenerates both exact descriptor forms; no generated descriptor or catalog was hand-edited. Native catalog, retained actor and two-peer Shell acceptance therefore remain open.

## Authenticated two-Author GIS proposal process gate

The registered `os-hub:gis-map-proposal-process-check` no longer prints a designed-only placeholder. Its process phase now selects the existing real checkpoint-pair producer law, builds the exact SQLite/test-support/native-artifact Hub and MCP child, and calls one bounded permanent harness.

That harness materializes and candidate-validates the retained stdio+GIS generation before loading current. It admits only the selected `semio:gis` Map editor whose compiled descriptor claims app-channel version 14. Under one actual Hub it creates two distinct native+MCP Author identities, announces and mutates one private Map document, publishes its initial exact GIS Pack/SPR through the public checkpoint route, opens two credential-FD MCP children and two authenticated document sockets, and confirms both children read the same canonical initial pair.

Author A then uses the ordinary inference routes to obtain one owner-private offered CreateRegion proposal. Author B is denied A's events/cancel/approval without either private identifier in the response, while B can submit an independent job. A approves; both live sockets must receive the same scope/document/mutation-bound `RebootstrapRequired`, and both MCP children must refetch an equal changed canonical pair whose checkpoint, descriptor and full frontier match that control under the unchanged retained catalog current. MCP child output, capabilities and decoded pairs have bounded owners and explicit wiping/terminal cleanup.

The source oracle mutates eight required process fences (qualified publication ordering, exact protocol, both MCP children, both sockets, private denial, live rebootstrap and equal refreshed pair) and rejects all hostiles. Exact source receipt:

- `bun ./📜️script.ts nx run os-hub:gis-map-proposal-source-check --skip-nx-cache`
- terminal exit `0`: `hostile=16`, `process-source=8`, AJV=1, independent SHA-256 and frozen-binding/committer/source boundaries passed.

The process gate has not been executed. There is no genuine current v14 GIS component, proposal, public approval, two-socket rebootstrap or MCP refreshed-pair runtime receipt yet. The harness explicitly makes no external-provider, Shell scene, WGPU render, in-flight cancellation or durable collaborative undo claim. Collaborative undo remains a separate missing Hub command/history witness boundary; it is not synthesized from the existing local in-memory undo record.

## Retained GIS inference execution

The inference submit route now has a schema-first retained-worker contract at `🌎️hub/🧪️fixtures/🗺️gis-inference-retained-runtime-v1`. Its nine language-neutral traces cover a Running response before completion, actual checkpoint cancellation, author revocation at the final fence, duplicate idempotency ownership, late completion after cancellation, claim heartbeat, admission refusal, runtime close drain and foreign-cancel isolation. AJV validates the closed corpus and an independent TypeScript reducer reproduces every expected event/owner/offer count.

The runtime no longer overwrites a `job_id -> control` map or performs GIS inference inline in the HTTP task. One process-owned supervisor admits the exact job, identity digest and run epoch under a fixed semaphore, installs an outer Tokio task behind a start barrier, and returns a snapshot of the durable Running row before releasing that task. The outer task owns and joins the actual `spawn_blocking` GIS call; the blocking call retains the frozen private Map bytes, claim, control, document gate and live authority dependencies. A duplicate exact request observes the installed owner and cannot replace or remove it. A nonlocal unexpired Running lease is reported unavailable rather than misrepresented as locally retained.

Every real GIS codec checkpoint now reads a fresh process clock and renews the exact SQLite claim in the same transaction as bounded progress; after the progress page fills, a separate exact-epoch renewal keeps the lease live without adding unbounded rows. Success additionally requires the current unexpired lease. Epoch-scoped cancel/fail helpers prevent a late worker from retiring a successor. After compute, the worker reacquires the document gate, revalidates the Author/session, rematerializes the active pair and compares the complete frozen identity before publishing an offer. Request drop is inert. Runtime close latches admission, signals every control and joins every outer/blocking worker before closing the approval committer; it never aborts a blocking task.

The existing real Hub GIS route fixtures now expect Running on submit and poll the private stream for completion. Two additional native laws pause inside the actual GIS codec checkpoint: one proves cancel then bounded join with no late proposal, and one proves runtime close signals and drains the real worker. A third pause law revokes the Author before the terminal recheck and requires a cancelled/no-proposal ledger row. The duplicate-request fixture now holds the real codec at its checkpoint and requires one retained owner before release.

TDD/source evidence:

- Initial registered source run was RED at the deliberately missing retained supervisor/heartbeat/join source boundary.
- Current `bun ./📜️script.ts nx run os-hub:gis-map-proposal-source-check --skip-nx-cache` is GREEN, exit 0: `retained-runtime=9`, proposal hostiles 16 and process-source 8. This is schema/source evidence only.
- Rustfmt parsed all changed Rust files; it reported formatting deltas, including concurrent unrelated Hub-bin regions, so no broad formatter rewrite was applied while other owners were active.

Native compilation and the new pause/cancel/close laws remain unexecuted while the root-owned retained catalog producer and external workspace Cargo processes own the heavy build lanes. The existing two-Author process harness has been updated to accept Running and poll each private stream, but it also remains unexecuted. No in-flight cancellation, genuine retained generation, Shell scene, WGPU render or collaborative undo runtime claim is made here.
