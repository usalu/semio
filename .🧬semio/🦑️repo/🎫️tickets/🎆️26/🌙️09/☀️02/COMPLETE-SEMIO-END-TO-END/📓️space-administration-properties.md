# Space Administration Property Controls

## 2026-09-09 14:08 UTC Full OS Qualification

Full OS8757 is GREEN340/340 across all5 configured files, exit0. Root read the
JSON receipt and verified success/zero failures:111 worker,205 OS wire/root,
22 effect-backbone and two single-law browser handoff files. This snapshot
includes strict scalar/envelope decoding and top-level Pack command field
reconstruction. The additional nested canonical-byte enhancement remains with
WGPU for separate qualification.

Broad renderer type34244 is terminal RED391 diagnostics. This snapshot has no
diagnostics in the owned administration pane, ShellHost, directory schema,
backbone worker, worker tests or envelope I/O tests. No global type pass is
claimed. Concurrent unrelated type repairs remain untouched.

## 2026-09-09 14:04 UTC Combined Gate Qualification

Worker20429 is GREEN9/331 skipped with all three authored DEBUG evidence lines.
Full OS59481 subsequently reports338 passed/2 failed: the decoder law exposed
Pack's lexically sorted map keys crossing the declaration-ordered JSON command
boundary; an older worker test also mistook four quiet timer turns for completed
SHA-256 verification. WGPU owns canonical Pack-to-domain reconstruction. The
worker harness now waits for actual ready/null-request/null-page-read state,
with a bounded2-second assertion waiter. Full OS8757 is active.

Renderer32719 is GREEN18/18 against the strict shared command parser. The earlier
full718 renderer qualification remains exact for its snapshot; this narrow rerun
qualifies the latest command-helper change. Broad type34244 remains active.

Root additionally requested exact serialized-command equality for all10 wire
variants, including nested announce-document descriptor/owner/frontier order;
structural deep equality alone cannot prove Rust-compatible canonical bytes.

## 2026-09-09 13:52 UTC Worker Admission And Page Ownership

Actual worker11048 is genuine RED1/336 skipped: the foreign-space rename reached
POST. Production worker now retains its parsed canonical page, clears that
authority during page loading and termination, and checks ready phase plus the
shared exact-space/per-command capability helper before sealing.37137 is GREEN7/
330 skipped, including28 command/authority refusals and13 unrelated/malformed
records that perform no POST and retain the unsealed ready operation.

Actual worker35029 is genuine RED1/338 skipped: an older successful author page
replaced a newer member page. The worker now owns each page read with a separate
AbortController, cancels the previous read, and ignores both success and failure
from retired reads. Close aborts and removes the exact read. Refresh while a
command is sealed is refused as capacity without a GET. Neutral page-retirement
fixture/schema has independent AJV/fast-deep-equal validation.20429 is the active
worker administration rerun, including four property command receipt/refresh
laws using independently SHA-256 sealed server responses.

MCP resource discovery was repeated at13:50; no repo resources or ticket tools
are available. This existing ticket remains open; no substitute ticket/goal
mutation or completion is claimed.

## 2026-09-09 13:42 UTC Complete Renderer Coverage

Explicit groups are all GREEN:20016=84/84 across8,55900=491/491 across7,
44628=143/143 across4. Root parsed the three JSON receipts and compared their
file union to all19 configured exact paths:19 unique,19 total, no missing or
unexpected files. Total718/718, including18 administration laws. Earlier
automatic shards are excluded from this complete-suite qualification.

Root owns worker integration and has started test-first actual transport refusal
law11048 using the shared neutral admission fixture and AJV/fast-deep-equal
oracles. WGPU owns the separate strict wire decoder. Neither packet is qualified
by the completed renderer tests.

## 2026-09-09 13:28 UTC Boundary And Runtime Verification

The additional genuine RED71969 proves two defects before correction: a null
command throws in the authority helper, and a late parsed ready page resurrects
a closed/null Shell operation. The helper now accepts unknown input and refuses
invalid/noncanonical/extra-field command records through the existing canonical
parser before exact-space/capability matching. The Shell reducer requires a live
non-null exact operation/space. Async page success and failure additionally
recheck the current worker, operation epoch and latest message revision at state
publication; foreign messages cannot invalidate a current verification.

Both neutral fixtures now have adjacent closed JSON Schemas. Tests reject extra
fixture envelope/row fields and validate every admitted/unrelated command against
the canonical directory schema. Ten malformed command rows are refused. Two
neutral late-verification phases prove a closed operation cannot reappear.

Explicit renderer group1 is GREEN84/84 across8 exact files (including18/18
administration), exit0. Its JSON receipt records every file and assertion count;
its console log contains actual DEBUG lines for seven scoped capabilities,
four property commands and receipt-bound local drafts. Group2 is active next.

The earlier automatic shards are not aggregated as complete-suite proof: admin
appeared in both executions, and shard2 also observed the intentionally-red
boundary tests before correction. Current qualification instead names all19
configured files explicitly in three non-overlapping groups (8+7+4) and retains
JSON receipts to verify coverage. No registered test is removed or timeout raised.

## Current Qualification

Renderer51101 is GREEN16/16, exit0, including the four property commands,
seven scoped capabilities/six refusals each, English/German semantic controls,
and draft replacement only when a new canonical receipt is installed. The shared
helper is used by the real Shell mapper; no new DirectoryCommand is invented.
Full renderer32284 is terminal RED at its300000ms process budget, without a
Vitest test summary. No assertion failure or complete-suite qualification can
be inferred from that run. The same complete suite is being rerun with Vitest's
three disjoint shards; shard1/3 is92481. No test is excluded and the registered
per-invocation limit remains unchanged. Console interception is disabled in the
shard runs so the authored runtime diagnostic lines are retained in evidence.

The worker transport admission/retained parsed page integration is assigned to
WGPU after its actual emitted-bundle prerequisite. It is not covered by the
renderer16/16 result. Native Hub same-data-root/full journey remains open.

## 2026-09-09 Test-First Packet

The canonical directory schema and Hub event decisions already define
`rename-space` and `set-visibility`, but the Shell administration pane exposes
only member/invitation operations. This packet uses those existing closed
commands and server-provided capabilities; it adds no new transport vocabulary.

Registered renderer49723 is genuine RED2/12 skipped: the neutral rename command
has no mapped request, and the accessible space-name input is absent.
`root-admin-properties-red.log` records both failures after successful import.

The four neutral command rows cover rename, Unicode name, public and private.
Each must match an independent expected command and pass the existing directory
JSON Schema through AJV. Eleven authority/phase refusals are required for every
row, including a foreign-space page. Empty/control-bearing name drafts are denied.
Testing Library exercises semantic inputs/forms in English and German and checks
that editing never changes the server page before a receipt-confirmed refresh.

Implementation adds per-receipt ephemeral property drafts, localized accessible
name/visibility forms, exact page/space checks before Shell command construction,
capability withdrawal and pending-phase disabling. The current registered rerun
is active. No mounted Hub integration or deletion-success claim is made.

First implementation run5254 passes13 laws but its new UI law fails because the
repository test wrapper has no `screen.queryByRole` method. The assertion now
uses the wrapper's real `getByRole` absence behavior. No UI full pass is claimed.
Broad renderer type61591 remains RED575; there is no diagnostic in the owned
administration pane/test/Shell paths in that snapshot. Build info stays inside
the ticket-generated folder for this run.

The worker audit found a separate receipt-owner gap: its submit path accepted
arbitrary directory commands without binding command space/capability to the
verified administration page. A shared domain admission helper and genuine
worker regression are now part of this packet. Seven neutral admitted command
rows plus six identity/capability refusals each define the exact boundary.

Deletion needs a separately audited committed-then-missing-page terminal, and is
not silently mapped into the ordinary refresh success path in this packet.
