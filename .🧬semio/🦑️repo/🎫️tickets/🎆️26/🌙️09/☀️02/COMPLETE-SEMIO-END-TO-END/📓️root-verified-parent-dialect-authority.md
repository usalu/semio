# Verified Parent Dialect Authority

## Current Qualification

Registered source25660 observed the intended missing-retained-dialect RED. Final source29257 is GREEN: eighteen catalog fields, 869 independently encoded bytes, three dialect-coordinate digest substitutions and eight hostile parent-dialect inputs rejected by both encoder and neutral issuer. Existing issuer, consume, exchange and codec cases remain selected. This is source/oracle evidence; the strengthened native trusted-catalog, ledger and socket laws are unrun. No child creation, initial receipt, new command or public dialect authority is introduced.

## Production Boundary

`VerifiedDocumentOpenSelectionV1` now retains the full `ArtifactDialect` from the verified app descriptor. All three bounded strings are validated. Catalog sort/hash framing includes kind, standard and subset after the artifact pack-schema hash, preserving the existing domain and framing. Changing any coordinate changes generation.

The private `DocumentOpenPlanAuthorityV1` retains that dialect at issuance. Validation binds its kind to the artifact and bounds standard/subset. Both authenticated exchange and live-socket consumption compare the complete retained dialect against current verified selection. Public document-open plans and browser DTOs remain dialect-free; the surface factory instance id is not document identity.

## Independent Fixtures

The existing document-open-plan neutral fixture gains exact parent-dialect objects on catalog rows. Its existing colon-delimited artifact kind is an encoding fixture, not a valid production verified artifact coordinate. The canonical generation is `1019f0371199dd5e744ca74406185e4cc2017f71398225031bd079b99f0116ef`; the browser fixture only updates this generation.

`documentOpenNeutralParentDialect` rejects non-exact row/artifact/dialect keys, missing objects, non-string or empty values, whitespace padding, Cc control characters, values exceeding256 UTF-8 bytes and kind disagreement. Eight negative mutations exercise both hashing and issuer filtering; the neutral issuer must return `component-unavailable`. Its success label `accepted` is local oracle vocabulary: production returns a typed plan body, not that outcome code.

## Verification And Next Boundary

Launch411.1091 runs the existing hub open-plan-server target with `--oracle-only`; it moved from411.1095 because that order belongs to the browser open-plan entry. The native target still selects its existing eight exact laws. Focused diff-check687569 passed. Backend owns a separate GIS target and will coordinate the strengthened hub law execution after its current build. Catalog availability, authenticated author-only initial composition, atomic parent/children publication and real reopen/two-user mutation remain required.
