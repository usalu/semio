# Kernel Extracted Test Authority

## 2026-09-09 14:49 UTC Complete Package Coverage

Full framework15737 is GREEN131/131 across2 files, exit0. Its JSON receipt
records89 facade tests and42 kernel tests, each source exactly once. The new
collection covers all five kernel registration groups: broadcast, router,
activation/metrics/capabilities, registry expansion and I/O graph. No runtime
implementation or public domain API changed.

## 2026-09-09 14:47 UTC Kernel Collection Qualified

Actual kernel86514 is GREEN42/42, exit0, after the discovery repair. Root reads
its JSON receipt to verify the exact source file and named groups. Full framework
package qualification is active next with both the facade and kernel selected
by the normal config. Type83614 is still active; no global type pass is claimed.

## 2026-09-09 14:43 UTC Genuine Discovery Failure

Registered framework19417 exits1 with No test files found for the actual kernel
source. The package config only selected its re-export facade. Root added the
kernel source to both includeSource and coverage.include, keeping include empty
to avoid double collection. The existing registered test command and launch
entry are reused; no separate script or command was introduced.

The current exact kernel runtime test and broad type recheck are active. Type
recheck reuses the finished root-owned ticket build-info cache to preserve normal
incremental validation without excluding any source or mutating another owner.

## 2026-09-09 14:36 UTC Test-First Type Boundary

Registered renderer type34244 captures26 diagnostics in the extracted kernel
broadcast/router/activation/registry/I/O suite. All five registration functions
erase their dependency types. Root replaced those carriers with exact Picks of
the existing first-party modules and restored the existing schema/runtime types.
Only the extracted test file changes; the kernel implementation, dispatch,
exports and test registration remain untouched.

The existing neutral routing, metrics and ownership fixtures remain the runtime
acceptance path. Runtime and type qualification are pending; no pass is claimed.
