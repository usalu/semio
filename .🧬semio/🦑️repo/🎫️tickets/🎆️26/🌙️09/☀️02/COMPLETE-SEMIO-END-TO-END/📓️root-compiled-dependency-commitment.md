# Compiled Stdio Dependency Commitment

## Native Stdio Result — 2026-09-08 06:10 UTC

### Readiness Fixture Protocol Preflight

The programmatic `native_openable_stdio_bundle` fixture still omitted the
mandatory bundle `executionProtocol`, although its descriptor already
carried the current protocol. A new source regression in the existing
dependency gate requires the actual descriptor field to be carried into
that bundle; source59029 reproduced the missing-field regression and full
source50943 passed after the repair. The fixture now also takes its descriptor
manifest version from the actual Stdio receipt, matching the bundle identity
instead of implicitly assuming the Hub version is equal. Native readiness
execution remains unrun. A read-only recursive
census of literal Hub JSON found two bundle-package records and neither
omitted the field. This census does not cover programmatic fixture builders
or claim their runtime validity.

Stdio26394 is terminal. All six exact integration laws passed on executable
`a25705537ee644b148d161e824c3d9d87ab402fb4e2534ca6b92daad77a15f45`
at `exact-cargo-laws-QZEhIZ/00`, including the new nine-vector exact compiled
dependency law, all30 catalog semantics cases and generic builder/Pack
contribution preservation. The actual emitted catalog payload also passed
AJV with the correct `s.stdio.gltf` kind. Each retained law receipt reports
status0, no signal and normal exit.

The overall gate then failed the first import probe: this compiler emits a
metadata stub in the reported `.rlib` and explicitly requires the full
`.rmeta`. Cargo's same artifact event reports exactly one such metadata
file along with feature closure `["full-artifact-catalog"]`. The harness
now selects that reported full metadata path and retains its regular-file,
owned-target and before/after identity fences. This does not turn the failed
probe into a pass. Five import probes and two private projection-budget
laws still require execution. Root catalog30472 now owns the shared build
target; its eight Hub laws remain pending.

## Current Qualification — 2026-09-08 05:50 UTC

### Process Fixture Dependency File Closure

Inspection found two remaining GIS-only copy lists: the native checkpoint
process fixture and its publication CLI consumer copied four files even
though the verified profile now requires the Stdio component and descriptor
as well. This would drop the dependency before any real child could reload
the relocated profile. The schema-first packet now fixes the exact six-file
closure; source assertions compare both copy lists with that neutral packet.
Source1206 is the intended RED run before correcting the two lists.

Session1206 first exposed an undefined helper in the test itself; that
reference was corrected to the existing repository-root path. Session2176
then reproduced the intended missing-Stdio-file assertion. Both copy lists
now retain all six files, and session66389 passed the 21/25/23 source gate
including both list comparisons. The native emitter additionally compares
the relocated directory with the same neutral six-file set and reloads it
through public `TrustedCatalogLoader::load_current`, requiring two packages,
28 codecs and the identical generation. Those new native assertions are
unrun. The CLI applies the component size cap to both whitelisted WASM files.

The native publication retry 94155 is terminal BUILD RED, not still active.
Two inference errors have now been corrected by Home. The third was the new
strict store wire facade being absent from the earlier compiled OS-kernel
frontier. Fresh Stdio build 26394 and catalog-selection retry 30472 are the
current native qualification attempts. The latter includes eight exact
laws for selected providers, actual compiled dependency/catalog commitments,
atomic publication, bounded Pack materialization and profile framing.

Bootstrap source session 1141 failed before assertions in Nx graph loading.
Verbose retry 5506 is active. Earlier source session 90446 passed 25 typed
dependency cases; catalog-selection oracle 22780 also passed the new 21-row
wire-materialization packet. Neither establishes current native behavior.
See `📓️root-bounded-descriptor-materialization.md` for the exact budget and
canonical bridge boundary. The end-to-end goal remains active.

## 2026-09-08 Implementation Frontier

The owner decision is semantic agreement between the actual Stdio descriptor,
the Stdio crate linked into GIS/VCS, and the Hub-native compiled catalog.
Compiler source-epoch provenance remains separate and unqualified.

The schema-first 19-row dependency corpus was RED in session 92884 because
the production resolver did not exist. Session 83543 then exposed a test
diagnostic expectation mismatch (`keys` for the intentionally extra field).
Session 87370 passed all 19 dependency rows, including three admitted cases,
canonical unsigned-64 version triples, exact Pack claims, immutable resolved
identities, and independent DataView framing. UTF-8 ordering includes an
astral/BMP inversion that JavaScript UTF-16 or locale sorting gets wrong.

The profile encoder now frames real concrete dependencies. The same session
then correctly rejected the old zero-dependency fixture digest. The new
fixture digest is `f14b364eee6e3c182713859a02472cb3d121c4a9e24eefcb44b348cada07b103`.
Node SHA-256 and WebCrypto process the same framed bytes; native Rust parity
has not yet run after this change.

Materialization now captures the claims from receipt-hashed descriptor Pack,
resolves GIS → Stdio only after both selected identities exist, and uses the
same vector for bundle and generation summary. Work is in progress on the
final leaf fence, rotation, neutral fixture updates, native exact-dependency
validation, and actual GIS/VCS guest declarations. No full source or native
pass is claimed for this unfinished change set.

## Native Qualification Observation

Root Stdio session 35202 (`exact-cargo-laws-SmVuBv/00`) passed all five exact
integration laws, including 30 semantic catalog rows and generic builder
topic preservation. Its new actual-payload AJV check then failed at codec
7's artifact kind. Import probes and the two private projection-budget laws
were not reached. This is a schema/source discrepancy to inspect, not a
reason to normalize the received runtime identity.

Root Hub publication session 90856 (`exact-cargo-laws-xZP0rM/00`) failed
compilation with E0425 at reactor lifetime line 241: the linked trace crate
does not export `guest_lifecycle_turn_contract_violated` in this build.
The WGPU execution lane is inspecting this shared compile seam. No Hub
publication or durable-undo assertion ran in that session.

## Completed Source Wiring and Authored Native Laws

The materializer, retained receipt, final leaf fence, bundle metadata, and
rotation now agree on immutable descriptor identity, protocol, and exact
GIS → Stdio dependency vectors. Raw Pack re-encoding is checked before any
JSON projection, rejecting duplicate fields and trailing bytes before they
can be normalized away. The local profile requires exactly the two selected
packages, 28 codecs, and one GIS Map open target; Stdio has no dependencies.
The canonical strict version triple is this local profile's policy, not a
claim that the generic Rust version parser has the same input grammar.

Source session 27753 passed the complete bootstrap source gate. Isolated
sessions 94118 and 31871 then passed the native-provider source oracle and
complete bootstrap source gate respectively. The latter covered 19 typed
claim rows, nine neutral native dependency vectors, four raw Pack cases,
two source-derived dependency framing goldens, 28 physical generation-fence
rows, and the publication fence. Native Rust parity is still unrun.

Actual GIS and VCS root assemblies now declare the exact version of the
Stdio crate they link and its compiled semantic catalog contribution. The
descriptor-aware native provider checks both before private factory preview.
The new Stdio dependency law consumes the same nine neutral vectors through
Serde and first-party Pack. The actual GIS/VCS descriptor law covers those
vectors plus exact, missing, duplicate, wrong payload version, and foreign
topic-version catalog cases. It selects the exact topic by name and compares
registry snapshots instead of assuming a globally empty test process.

The shared synthetic native test profile now includes a genuine headless
Stdio metadata package built from its actual 26 codec receipts and catalog
contribution. Both real GIS fixture builders use the same two-package
dependency closure. Its component and actor bytes remain synthetic and are
never component-execution evidence. Its private helper returns JSON fixture
values validated against BundlePackage, without expanding production APIs.

The authored `linked_stdio_gis_descriptor_failures_never_publish_a_partial_codec_closure`
law covers six denied candidates and a final accepted one. It reseals full
descriptors, file hashes, actor source hashes, and generation metadata so
failures exercise catalog/dependency/provider/raw-Pack boundaries rather
than an unrelated byte digest. Successful private preview frontiers must be
empty for rejected Stdio and Stdio-only for rejected GIS. The 28-codec public
registry snapshot must remain unchanged until the complete positive load.
Raw cases carry a duplicate full manifest or a trailing byte. The positive
case requires dependency-first Stdio/GIS order, 28 codecs, and one Map target.
These newly authored native laws have not executed. Rustfmt parsed both
native files; scoped `git diff --check` passed. No native or end-to-end pass
is inferred from those checks.

## Current Native Sessions and Corrections

The schema mismatch in session 35202 was the real `s.stdio.gltf` artifact
identity, while the other 25 current codec kinds use `stdio.*`. The schema
now enumerates the exact 26 compiled kinds; runtime identities were not
renamed or normalized. Five integration laws had passed before AJV failed;
the headless imports and private projection budget laws remained unrun.

Session 62892 failed its build with E0425: its Stdio library compiler read
the older registry frontier before the new dependency API was authored,
while the later integration-test compile read the new test. Both functions
are present under `full-artifact-catalog` in the current source. No laws ran
in that failed session. Fresh six-law retry 26394 is queued behind Hub
publication CLI session 94155 in the same ticket-owned target, jobs=1. The
Stdio registry API is held stable during this retry. Hub 94155 is rebuilding;
no publication result has been observed. The prior missing trace export is
now present in the shared trace source; no unrelated reversion was made.

Source reruns 93873 (bootstrap) and 67243 (native source oracle) both exited
zero after the latest schema and atomic-law source additions. Bootstrap
generation-stage evidence is `generation-stage-SbE0Q3`; its publication
fence evidence is `publication-fence-0vMGFW`. Stdio retry 26394 has artifact
directory `exact-cargo-laws-QZEhIZ/00`. Full compiler-epoch provenance, current real
guest materialization, mounted Shell approval/undo, and second-peer durable
scene observation remain unqualified.

## Focused Gate and Additional Claim Denials

The existing launchable `os-hub:native-catalog-selection-check` now includes
the neutral dependency corpus and the new descriptor-aware, atomic,
two-package-profile and native framing laws alongside its three existing
selection laws. No new executable command or launch entry was introduced.
Its source-only session 96271 exited zero (19 dependency rows and 23 registry
selection rows). This is not a native execution result.

The dependency corpus was then expanded to 25 rows with wrong owner plugin,
package, and version, selected Stdio version drift, missing protocol, and
unsupported protocol. Full source session 90446 exited zero with all 25 rows
(three admitted), nine native vectors, four raw Pack cases, 28 generation
stage rows (`generation-stage-OcdrCA`) and publication fence
(`publication-fence-MWCVwe`). Native dependency and framing parity remain
pending the serialized Cargo builds.
