# OS Host Async Persistence Frontier

## First Source Slice

The current Rust caller census confirms `host_runtime` had no production importer. Removed that unused module, its folder/hub/config/open/close convenience facade, and its two wrapper-only tests (`opens_a_document_and_subscribes_to_its_events`, `app_artifact_config_carries_the_artifact_ref_through`). The actual WGPU owner already awaits `ArtifactHost::open` and `subscribe`; its production code is unchanged. Stale comments now reference the real ArtifactHost owner. The removed open test made no behavioral assertion, and the other only checked the deleted convenience configuration copier.

`MediaContract` has no `ToValue` implementation; adding an import would not repair its projection. The live node-graph payload now uses a narrow explicit presentation projection: artifact kind, media class/form, tagged document or binary wire, optional conversion and its presence flag. No serde/ToValue implementation was added to the persisted contract. Its schema-first neutral fixture has four document/binary × absent/present-conversion rows, with sixteen AJV hostile denials. An exact Rust law builds the actual Workflow and checks the returned scene-edge payload against every neutral row.

## Evidence

- Caller census 13132d / 6ca817: no qualified production `host_runtime` caller; remaining similarly named service/puzzle functions are unrelated owners and untouched.
- Initial registered media oracle `1d65ef`: RED before task dispatch on concurrent `plugin-test-values` exact standard-directory names and duplicate `🧵️send` taxonomy. Zero owned cases credited.
- Registered `@semio-tech/framework-os-host-rs:media-projection-check --oracle-only`, session 49400: GREEN, exit 0, four neutral projections and sixteen strict hostile denials.
- Nonmutating Rust syntax and owned diff hygiene 2467c3: GREEN. No Rust compilation or native law execution is claimed.
- The native target uses the shared exact-one binary runner and explicit `os-host-full`. Launch seed entry is `⚖️gate🖥️host🕸️media-projection`, with ticket-generated evidence/warm target and one Cargo job. Owner generation 72917 and freshness 20928 are GREEN, exit 0.

## Typed Persistence Contract Foundation

The host now mounts `💾️persistence/🦀️.rs`, a closed request/event vocabulary and receipt-consumer state machine. The schema was landed first. Four operations cover create, bind, import and delete. Requests carry an exact window/space/session/catalog-generation scope, safe-integer request counter, bounded deadline, and opaque shell-owned selection ids; no filesystem path or raw action id is part of the wire. Events carry exact correlation, monotonically ordered progress and byte counts, or a typed terminal outcome. Failures use a closed code set, never an OS error/path string. Success requires the commit phase, exact byte count, nonzero SHA-256-shaped digest, exact next catalog generation, and operation-specific target binding. Replays, cross-scope events and cancellation after the commit point do not publish.

This is deliberately a **consumer foundation**, not a mounted kernel Effect or a persistence issuer. The actual shell still needs private selected-path authority, a capacity/replay/cancel owner, fair scheduler, retained file/folder IO, atomic durable publication and Home receipt dispatch. The 64 pending constant is a declared ceiling, not yet a proved runtime capacity limit. A success-shaped input accepted by the pure consumer is not proof that bytes were durably written. No existing Home route has been switched and no catalog/readiness success is promoted.

- Registered TDD source gate 66034: RED at the missing implementation after the independent AJV/trace oracle reached its source guard. Exit 1, expected pre-implementation failure.
- Registered current source gate 66568: GREEN, exit 0; four operation requests, sixteen malformed request denials, fifteen neutral traces, thirty accepted and fourteen rejected events, six expected model publications. The Bun/AJV state model does not import the Rust consumer.
- Final stronger source gate 89395: GREEN, exit 0; retains the same four requests, sixteen hostile requests and fifteen traces, adds eight closed/redacted/digest/safe-integer event denials. Native source also asserts cross-window/space/session/generation rejection leaves pending state untouched. This supersedes 66568 only for source-oracle evidence; Rust remains unrun.
- Two exact Rust laws are registered through the shared runner: `persistence_contract_matches_neutral_scope_progress_and_terminal_traces` and `persistence_contract_rejects_closed_fields_and_cross_scope_receipts`. They are **uncompiled/unexecuted** while root owns the native slot.
- Rust syntax-only parse 7d8e70: GREEN, not compilation.
- Final owned syntax/diff hygiene 73fdfa: GREEN, not compilation.
- Launch seed contains explicit source/native persistence-contract entries with ticket-owned evidence/target. Generation 48195 and freshness 31003 are GREEN, exit 0.

## Remaining Source Work (Current)

### Continuing Exact Owner Retirement

The source-first retirement fixture/schema now defines seven typed string/collection/option/pair shapes under three exact byte/item grants. Initial registered `document-retirement-check --oracle-only` session 3562 returned exit 1 at the missing shared implementation, the intended TDD RED. The implementation now lives in kernel `store/♻️retirement`: typed fields, explicit terminal witnesses, bounded byte release, and shared-root blocking. Stdio consumes those primitives instead of duplicating them; its exact domain field tables and member/store factories remain. Host space/collection/workflow snapshot and mutation field tables are source-landed. Two kernel exact laws cover the neutral byte totals plus zero budgets, shared roots and false terminal/over-budget witnesses; native execution remains pending.

Seven read-only kernel codec/materializer helpers now borrow `ArtifactEnvelopeOwners`; existing envelope callers deref-coerce and no history clone is needed at that seam. `retire_document_envelope` exposes the existing exact private envelope retirement with caller-supplied snapshot/mutation factories. The coordinator's pre-hydration SPR identity validation and single decoded-history helper are preserved. Full host adoption and retained parse-error/cancellation ownership are in progress, not accepted.

Fixture-sweep source/project/script references already resolve the concurrently renamed `🧹️fixture-sweep` and `🧫️fixture` owners. Registered source retry 2c8523 returned pre-target exit 1 because taxonomy `neutral-fixture` and `fixture-shape` reference absent `handback`; no preservation-law result is credited to that attempt. The subsequent source terminal 71071 is GREEN, exit 0: 54 registry rows, 28 moved fleet edges, two preserved laws and 16 hostile source denials, over 186 directories/365 files; inventory SHA-256 `52e4fc3934e8bd0f503d83678f25dc17a9d4c2198b585e21145677b8cb231ac0`. Retirement source gate 18940 is GREEN, exit 0: seven shapes × three grants, 1,262 model byte grants and five declared hostile cases. Those hostile declarations are not credited as native assertions. Owner launch generation 83625 and freshness 87466 both exited 0 for the retirement entries. Later member-open launch entries require a fresh generation.

### Caller-Retained Member Open (In Progress)

The staged kernel vocabulary is `store::member_open::{MemberOpenRequest, MemberOpenAdmissionError, MemberOpenOperation, MemberOpenStep, MemberOpenDiagnostic, MemberOpenProgress}`. Requests own fixed-capacity sealed pages, exact artifact/owner identity, operation/generation, and an absolute expiry. Admission failure returns that exact request. Every subsequent step checks caller correlation, cancellation and real time; a step deadline yields without consuming input. The input cursor reads the canonical length prefix under fuel, derives non-overlapping snapshot/history ranges, and copies at most one page under the remaining byte/fuel grant. It never assembles a whole input `Vec` or uses a one-poll future bridge. Rejection is sticky; bounded retirement retains pages and Unicode identity bytes until their exact accounting completes.

The strict neutral fixture has ten admission cases and eight canonical/malformed framing cases. AJV independently checks structural admission; a BigInt framing model is checked against a separate arithmetic encoder. Exact Rust laws cover input retention, three fuel grants, zero fuel, step deadline, operation/generation mismatch, expiry, cancellation and sticky denial. These laws are registered through the same-binary exact-one runner and passed in native session 38156, as recorded below. TDD 50507 stopped at the then-missing implementation after the admission oracle, as intended. Retry `7d116f` stopped before dispatch on `jco-package-adapter.inputPatterns` ordering (exit 1); no owned case was credited. Expanded oracle session 2320 subsequently passed. Rust syntax-only `bdf5c2` exited 0, not compilation.

The initial expanded source terminal 2320 is GREEN, exit 0, with ten admission and eight framing vectors. The six retained-stage fixture additions then deliberately failed their missing Rust-law source guard in TDD session 70786 (exit 1). Current source terminal 62426 is GREEN, exit 0: ten admission, eight framing and six **declared** retained-stage cases; 102 rejected input bytes. The six stage-model checks do not stand in for their unrun native ownership assertions.

The coordinator's canonical launch generation 95372 is GREEN and includes the member-open source/native entries; freshness 71326 was still pending when reported. At that checkpoint the three exact laws were queued; their later 48020 RED and repaired 38156 GREEN terminals are recorded below. Module syntax `dc22be` and owned diff hygiene `7c6862` exited 0; these are not Rust typechecks.

`MemberStoreOpenRetained` now holds the original `MemberStoreOwners` bundle before hydration, the request, optional initial snapshot/partial edit/parsed envelope owners, initialization runtime, and one active exact retirement. Any stage admission returns its exact rejected input; cancellation latches without dropping typed state. Its close path first drains current/runtime, then partial operations, envelope/initial snapshot, pages and identity, always delegating one grant and verifying terminal witnesses. The exact native law drives real Demo snapshot and mutation codecs before injecting interruption, and counts actual terminal factory invocations at six boundaries, including malformed next mutation and initialized-current rejection. This is still an owner-boundary law, not an end-to-end parser law.

`MemberFactory` and `space_members!` have not yet been cut over. The agreed final API is a caller-retained operation, not an error-only async wrapper: an owned request returned intact on admission failure, `step(&mut self, &mut StepContext)` and an exact bounded close/terminal witness. The old whole-value `ArtifactPack::decode_pack` and async whole-history SPR decoder cannot be hidden in that step. The new retainer is not yet called by that old production helper. Bounded typed parsing, validation/replay, atomic consumption of the owner bundle, and the actual caller abort-slot cutover remain required. The coordinator's identity preflight and `snapshot_ref` accessor are preserved. The three registered native owner laws passed in 38156; production parser activation remains separate. No host persistence, member-open activation or readiness claim follows from these source oracles.

`OsWorkflowStore` and its exclusive alias are unused production facades but are not yet removed. Two associated tests cover meaningful host backbone conflict/cursor persistence, beyond wrapper behavior; they must be rehomed against direct typed document/kernel APIs before removing the facade. The existing conflict reconciliation and text/pack assertions are not discarded to make the compile frontier shorter. Reread found an additional concrete ownership defect in their underlying host helper: `with_backbone_envelope` clones a VCS, then calls `drop(envelope.into_owners())`; `ArtifactHistoryLedger::Drop` now asserts terminal-empty ownership. A nonempty history cannot take that raw-drop path. Simply replacing the wrapper test with a direct store or a hand-built history would expose the same defect, not establish preserved runtime acceptance. The correction needs exact retained document/codec owner retirement, not `forget`, a test-only wrapper, or another one-poll bridge. This is source inspection, not a newly executed panic law.

The live `OsBackbonePort` / `SpaceBackbonePort` and Home create/bind flows remain asynchronous-migration RED. They need an explicit typed kernel/shell request and correlated success/failure/cancel receipt, owned by the existing bounded `submit_shell_io_future` execution and scoped to window/space/session. Only a durable success receipt may publish Home catalog state or navigation. No new `resolve_kernel_future`, `resolve_ready`, raw action-id replay or path-bearing JSON bridge has been added.

Native member/provider/fleet execution remains serialized by the coordinator. This source slice does not claim that the old thirteen-error host frontier is still the exact current compile result, that the host compiles, or that persistence/readiness/client transport is accepted.

## Native Retained-Owner Terminal And Flow Decoder Continuation

Registered `member-open-protocol-check` session **48020** finished **RED**, exit 1. The same-binary build and exact-one discovery succeeded; admission and framing laws each passed once. `os_store::component::tests::member_open_partial_parse_and_initialization_owners_retire_exactly` aborted with `SIGABRT`: the first panic is `ArtifactStoreCursorDisposer::Drop` before terminal-empty, followed by the retained-open destructor during unwind. Evidence is `🗑️generated/host-member-open-exact/exact-cargo-laws-Dd3Ug8/00`. This is a real native rejection-retirement defect, not external compilation failure. There is no three-law acceptance.

The failed path discarded the original, never-installed store disposer after retiring input and typed owners. Source now requires explicit `close_uninstalled_step` plus an exact terminal witness; any disposer whose store close has started refuses this route. The cursor Drop invariant remains unchanged. All three current disposer implementations have the new contract. Runtime repair is pending the next serialized slot; the warm target is released to the coordinator's Flow gate.

The next decoder cohort is the actual Semio Flow snapshot at `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🧿️semio/🏅️standards/🔖️v1/🪆️subsets/✳️flow/🧬️schema/📸️snapshot/💾️binary`. Its existing wire is the `.semio` binary magic, little-endian token length and `stdio.semio.flow.pack v1`, followed by format byte 1, length-prefixed UTF-8 schema, counted nodes and parameters, two little-endian f64 coordinates per node, then counted port-addressed edges. This is not the canonical record-pack protocol; neither a JSON conversion nor structural token validation can replace its typed decoder. The member factory remains unchanged while the genuine retained decoder is developed.

The repaired registered retained-owner rerun **38156 is GREEN**, exit 0, with **three actual native assertions**. Receipt `🗑️generated/host-member-open-exact/exact-cargo-laws-7uV7zQ/00/receipt.json` identifies the one kernel binary SHA-256 `e1ee6914576b5689e60e1117e75a40589667ab0f1d7f0e63b56691ed50c186ed`. All three fully qualified names were discovered exactly once and executed from that same binary: the two `os_store::component::member_open::tests` admission/framing laws and `os_store::component::tests::member_open_partial_parse_and_initialization_owners_retire_exactly`. This supersedes the earlier unexecuted/queued statements for those three laws only. The raw-history/typed-owner rejection boundary is proved; production retained parsing and host Effect/IO remain separate.

Flow decoder schema-first TDD session **13577** reached two exact neutral byte vectors and twelve hostile denials through an independent Bun decoder, `@webassemblyjs/leb128` encoder and AJV validator, then returned the intended RED at the missing Rust-law source guard. After implementation, registered source session **87683 is GREEN**, exit 0, for the same 2/12 oracle. Rust syntax-only succeeded; native Flow decoder execution remains pending.

The new `SemioFlowSnapshotDecode` owns the original sealed member request and its actual partially populated `SemioFlowSnapshot`, advances one input byte per fuel, incrementally validates UTF-8 (at most four bytes per scalar), and retains cancellation/error state. Counts and allocations are explicitly capped at 256 nodes, 512 edges, 64 parameters per node, 4,096 bytes per string and 1 MiB total string bytes. It validates exact envelope/component/version/schema and canonical integer framing. A private exact-EOF witness is required before `take_ready`, which rechecks current authority and returns both the typed snapshot and unchanged owned request. Rejection uses the real Semio typed field retirement and original paged input retirement under one-item/seven-byte laws, not a whole-value drop. The staged exact native law checks original-encoder equality, three fuel grants, all twelve denials, every byte cancellation boundary, stale operation/generation, missing clock/expiry and intact wrong-dialect admission return.

The existing `ArtifactPack` decoder, `MemberFactory` and 18-format macro remain unchanged. This decoder is not yet attached to retained SPR replay/initialization or production member-open. No host durability, Flow mount, all-cohort decoding or readiness claim is made. The permanent stdio `flow-retained-decode-check` target and source/native launch entries are registered; generation 93671 and freshness 85004 passed.

Owner launch generation **93671 is GREEN**, exit 0, and includes the new Flow source/native entries. Freshness **85004 is GREEN**, exit 0; generated launch contains both exact entries at lines 3710/3718. Owned diff hygiene `e733a5` is GREEN. The coordinator reported that the entire ticket `🗑️generated` directory disappeared during its active Flow build around 15:38. This lane did not delete it. The 38156 native terminal and the receipt/hash above were read and recorded before that external deletion; their original raw files/warm binary are now unavailable unless recovered. This does not change the observed three-law result, but it prevents reuse or independent rerun of that historical executable. No new native Flow decoder run has started.

Read-only continuation confirms retained SPR is a separate real decoder requirement: `HistoryReader::open` currently performs LastCommit recovery over the complete byte slice, then whole-log parsing builds dictionaries, edit ordinals and typed records. It cannot be placed wholesale inside the new byte step. The Flow snapshot decoder leaves the owned request intact specifically so verified history framing/dictionary/record interpretation and exact identity preflight can precede replay and final owner-bundle adoption. No `resolve_kernel_future` or JSON envelope authority has been introduced at that seam.

The coordinator confirmed that the generated directory was deleted, not moved; the invalidated Flow process was terminated. After the slot was released, registered Flow decoder session **37645** started against a recreated, explicitly owned `native-openable-provider-sol-target`, one Cargo job and this lane's established 3,600,000ms build cap. The exact decoder law was moved (not copied) into a **public-API integration target**, `flow_retained_decode`, under the same stdio package. It links the real production decoder/types and avoids compiling unrelated stdio `cfg(test)` fixture modules. The same-binary runner still requires one executable, exact-one law discovery and one passing assertion. Current build is cold because the warmed target was destroyed; no native Flow result is claimed yet. The original kernel three-law historical result remains distinct.

## Passed Native Output Retention

The coordinator-requested shared exact Cargo runner enhancement is complete for subsequent runs: native arguments now include `--show-output`, retaining passed `[DEBUG]` logs in each law's ticket-owned stdout without `--nocapture`. Exact listed name, one passed/zero failed/zero ignored terminal, actual exit, target identity and executable hash checks are unchanged. The neutral fixture explicitly owns those arguments and adds captured-output success plus an extra spoofed-terminal denial. AJV rejects substituting `--nocapture`.

- Registered TDD session **85732**: RED, 15 passed/9 failed. The missing flag is the intended failure; it also exposed a fixture-only assumption that two parent traversals always leave `🗑️generated`. The hostile target fixture now derives the actual generated boundary and remains outside it at any ticket evidence depth.
- Registered post-change session **70971**: **GREEN**, exit 0, **24 Bun laws / 416 expectations**. Includes four actual child-process exit/timeout/cancellation/output-cap laws, nineteen neutral runner scenarios, independent AJV and Node/WebCrypto hash agreement.
- `37645` loaded the previous runner before this change and retains its original native arguments. No in-flight process was restarted or silently reconfigured. Future registered native runs collect passed DEBUG output automatically.
- Post-integration-target launch/catalog freshness **37343**: GREEN, exit 0.

## Retained History Continuation Boundary

Current read-only mapping confirms the next cohort must consume the existing SPR bytes, not introduce a replacement record format. `replication/📐️format` writes a 32-byte header, canonical body-length varint, kind/flags/payload, CRC-32C over the body and a four-byte backward-length echo. Every ordinary frame contributes its whole-frame BLAKE3 digest to the next commit; each fixed 64-byte commit payload binds sequence, previous commit offset, covered byte length, record count and chained digest. The existing history writer emits dictionary deltas, document identity, edits/inverse/metadata, changes, checkpoints, alternatives, active selection, cursor, composition and conflicts, followed by a commit.

The retained implementation must separate verified framing from semantic hydration. The first pass can hold fixed header/trailer/commit buffers and incremental CRC/hash state while consuming the original `MemberOpenRequest::copy_history_chunk` pages under real fuel, deadline and cancellation. It must not publish provisional records, dictionary state or a member before a verified commit. The existing LastCommit recovery policy for a torn/uncommitted tail must be represented explicitly, rather than silently treating exact EOF or any structurally valid frame as durable authority. Only a private verified span may authorize the second, typed record pass. Its dictionary references and edit ordinals must be bounded; malformed later fields retain already-decoded owners in `MemberStoreOpenRetained`.

This is a source design boundary, not implemented history parsing. In particular, neither `HistoryReader::open`, `prescan_full`, `decode_history_from`, nor `parse_document_spr` is allowed inside one supposedly bounded step. They currently scan or allocate from complete byte slices. The host's meaningful conflict/cursor codec laws and live asynchronous persistence migration remain blocked on exact retained ownership; their assertions will not be replaced by a structurally valid history fixture.

Cold native decoder session **37645** was still running at the 13-minute process census: Cargo PID 19628, production stdio rustc PID 28026, about four minutes of accumulated compiler CPU and no error diagnostic in the captured Cargo JSON. Kernel and the coordinator's current shared plugin adapters compiled before that frontier. Owned Flow Rust and its compiled fixture remain frozen until terminal. This is progress evidence only, not native test acceptance.

## Flow Native Compiler Terminal And Lifecycle Repair

Registered cold session **37645 finished RED**, wrapper exit 1 / Cargo exit 101. Production kernel, shared plugin and Stdio compiled; the public `flow_retained_decode` integration test failed with exactly three compiler diagnostics: two `E0277` casts incorrectly assumed `MemberOpenRequest: ErasedSnapshotRetirement`, and one `E0308` clock closure captured `now` even though `StepContext` requires `fn() -> Option<u64>`. No native assertion ran. Raw evidence is preserved in `🗑️generated/flow-retained-decode-exact/exact-cargo-laws-KILko7/00/{build.stdout,build.stderr,build.json}`. The native target is retained. This result used the shared plugin bytes compiled before the coordinator's next viewer-forwarding patch; it is not a qualification of those later bytes.

After terminal, the request's existing exact close/witness now implements `ErasedSnapshotRetirement` by direct delegation, and the test clock uses non-capturing function pointers. The audited constructor defect is repaired at its actual boundary: `MemberOpenRequest::admitted_expected()` rejects closing/retired requests as `Stale`, unadmitted requests as `Unsealed`, and only then returns retained identity. The Flow decoder uses that fallible inspection and returns the original request on every admission error; a wrong dialect remains `Identity`. No raw identity clone, fallback, Drop weakening or wrapper decoder was added.

A separate strict lifecycle fixture was staged without changing the Rust/fixture captured by 37645. Independent Bun/AJV/third-party-LEB128 source TDD **53611 is RED**, exit 1 at the deliberately missing native-law guard, after all five admission states and the exact two-page case agreed. The large fixture expands an existing Unicode Flow node label to 4,096 UTF-8 bytes: wire 4,249 bytes, framed input 4,252 bytes over two pages, identity 30 bytes, typed snapshot retirement 4,190 bytes, total **8,472 retired bytes**. The new exact law exercises admitted/unadmitted/closing/retired/foreign requests and cancels after multi-page Ready before handoff; three variable positive byte-grant sequences must sum to exactly the neutral total. It is now selected alongside the original exact law by the same-binary registered gate; zero/ignored assertions cannot succeed.

Post-patch Rust syntax-only **614024** and owned diff hygiene **2f14bf** are GREEN. These are not a typecheck or native result. The expanded registered source rerun **3767 is GREEN**, exit 0: 2 exact wires, 12 decoder denials, 5 lifecycle admission states, the 2-page / 8,472-byte independent accounting case. Production history/replay/initialization, the member factory cutover, final app transaction and host persistence remain unimplemented/unaccepted at this boundary.

Current native rerun **86667** is active on the same explicitly owned target, one Cargo job and the established 3,600,000ms cap. It selects exactly two public integration laws and now retains passed DEBUG output. Its evidence directory is `🗑️generated/flow-retained-decode-exact/exact-cargo-laws-EzML7k/00`. Freshness **25069** returned RED, exit 1, for only the concurrently stale registry output `🧩️plugins.ts`; no launch drift was reported. Owning generation **61696** and subsequent freshness **46380** are both **GREEN**, exit 0 (59 plugin crates, 60 playgrounds, 45 framework packages). Both exact Flow launch entries and their ticket-owned log/target environment are present. These source/generation terminals do not substitute for the pending native assertions.

## Flow Retained Decoder Native Acceptance

Registered session **86667 finished GREEN**, exit 0. The single production-linked `flow_retained_decode` executable was discovered once and hashed as SHA-256 `87ce3e4f9234024319c09e0b1211d391948b10fe15e8b8f81a5f55cbcc0bdf4f`. Both exact laws ran independently with one passed, zero failed and zero ignored:

- `semio_flow_retained_snapshot_matches_neutral_wire_and_retains_failures`
- `semio_flow_retained_snapshot_rejects_retired_requests_and_closes_exact_bytes`

The captured passed DEBUG output confirms two real typed snapshots, twelve hostile cases under three fuel grants, every-byte cancellation retention, five request-admission states, two input pages, and exactly **8,472 bytes retired** under three variable grant sequences after Ready cancellation. The receipt, exact list, actual exits, executable hash and individual captured output remain under `🗑️generated/flow-retained-decode-exact/exact-cargo-laws-EzML7k/00`. This supersedes the active/pending statement above for these two laws only. The shared `native-openable-provider-sol-target` was immediately released to the coordinator's Flow six-law gate; this lane will not launch concurrent Cargo.

This is component acceptance for the actual Flow snapshot decoder and retained request lifecycle, not public parent reload, retained SPR history/replay/initialization, generic `MemberFactory` cutover, host persistence, WGPU/MCP mount or readiness. The current public-parent audit confirms the existing `load_child_pack` path still uses borrowed whole-input `M::open`; it must not be credited as consuming the new retained decoder. That later integration needs the exact parent-slot/generation transaction and owned request cancellation described in `📓️terra-flow-public-parent-semio-members-reload-audit.md`.

## Retained SPR First-Pass Source Slice

The new schema/fixture owner is framework `replication/📐️format/🔎️verification`. Its independent Bun model encodes the current SPR grammar using third-party LEB128 and CRC-32C, independently recomputes chained BLAKE3 through the separate TypeScript hash implementation, and checks its CRC/BLAKE3 published known answers. Two commits bind exactly three opaque records, offsets 91/180, endpoints 166/255, coverage 59/14 bytes, and three/five recovered frames. All **224** prefixes from a complete header through the two-commit EOF recover exactly the last complete verified commit. **22** hostile header/frame/commit/capacity cases reject; nested schema extras also reject. Full CRC-valid commits with wrong sequence, prior offset, coverage, reserved fields or chain reject instead of being reclassified as a torn suffix. The fixture is framing authority only, not semantic acceptance of its opaque record payloads.

Source TDD sessions **53402** and **22447** were owned REDs in AJV strictRequired schema construction, fixed without loosening strict mode. Session **53645** reached all independent cases and then the intended missing-Rust implementation RED. After staging the fixed-buffer Rust verifier and two exact laws, registered `@semio-tech/framework-replication-rs:retained-verification-check --oracle-only` session **99072 is GREEN**, exit 0, for 2 commits / 224 prefixes / 22 hostile cases. Rust syntax-only and owned diff hygiene **b5f727** are GREEN; neither is compilation. A read-only helper import before the registered run had encountered transient taxonomy `packageSourceDispositions` gaps, but the registered gate subsequently dispatched normally; no bypass was used.

The Rust verifier keeps only fixed header/trailer/commit buffers and incremental CRC/BLAKE3 state. It consumes one byte per caller fuel, latches rejection/cancellation, checks every complete frame, and returns public scalar `VerifiedSprSpan` metadata with a private constructor only after the declared EOF. `finish` succeeds once per verifier, but the span is not itself an input-bound one-use authority receipt: borrowed getters remain reusable and a recoverable tail is explicit. It does not own or drop paged input, construct dictionaries/typed history, or make a member eligible for app publication. Two staged exact Rust laws exercise the actual production writer, all prefix/hostile vectors under three grants, zero fuel, pre-EOF denial, repeated handoff denial, short headers and cancellation at all 256 boundaries. The native chain comparison uses the existing **dev-only** third-party `blake3` oracle; no runtime dependency was added.

At this checkpoint the new Rust module is deliberately **not mounted** into `format/🦀️.rs`, avoiding a shared production dependency change while coordinator session 84186 compiles Flow. Its existing script/project and source/native launch-seed entries are registered; the native gate has exact full test names and cannot succeed with zero selection once mounted. Module mount, fresh generation/freshness and native execution remain pending. WAL replay/writer behavior is unchanged; a future shared verifier adoption must preserve the backend's independent cross-segment chain laws. No history hydration, retained factory, host persistence or readiness acceptance follows from this source slice.

The expanded current oracle **16660 is GREEN**, exit 0, with **24** hostile cases: the added CRC-valid bad second commit and corrupt later record prove that a complete invalid suffix cannot silently fall back to the already verified first commit. Source launch generation **43163** and freshness **70099** both exited 0. Native dispatch additionally fails before Cargo while the module is unmounted; zero-test success is not possible. The module remains uncompiled/unexecuted, and the earlier 22-case result is not promoted to current native evidence.

Production ordering is unchanged: the coordinator's existing complete SPR identity/owner preflight must happen before any typed snapshot hydration. Therefore the retained operation must first verify framing and semantic document/composition authority, then drive Flow snapshot decode and retained history/replay/initialization. The standalone snapshot component's `take_ready` is not authority to skip or defer that preflight. No generic factory signature has been changed.

## Semantic Identity Schema Before Hydration

The next source-only contract is staged at OS `spr/📜️history/🛂️identity`, separate from the domain-neutral SPR framing verifier. It uses the existing document/composition payload grammar and the actual Flow cohort identity (`s.stdio.semio@v1/flow`, payload schema `stdio.semio.flow`, parent dialect `s.flow.flow@1/*`, content slot). The independent model handles raw text, dictionary and prefix-UUID id encodings; ordinal ids are denied in document/composition identity. Dictionary bytes/entries, individual identity bytes, pin groups and pin count have explicit caps. Canonical format 1, exact record EOF, known composition presence bits and UTF-8 are required; no old-format fallback is introduced into the retained contract.

Exactly one document header is required. Composition updates retain the format's explicitly documented **last committed overlay wins** rule; final artifact id/schema/dialect/owner must match the requested authority before any typed snapshot decode. The corpus includes an earlier foreign composition followed by an exact final composition, missing owner/dialect/composition, cross-document/schema/parent/slot/kind/standard/subset, Unicode control, duplicate document, dictionary miss/nonminimal index, ordinal/unknown id tags, old formats, trailing/truncated bytes and capacity denial. The model owns no typed snapshot and does not authorize a public app transaction.

Registered source `@semio-tech/framework-os-host-rs:member-history-identity-source` session **4330** passed the first 28-row corpus. After the explicit duplicate-document rule, final current-source session **1831 is GREEN**, exit 0: **5 accepted / 24 denied** rows, independent third-party LEB128 + strict AJV + UTF-8/ID parser. This is a schema/oracle foundation, **not** a Rust semantic decoder law or a bounded native reader. Production whole-history decoder behavior is unchanged; it still has older permissive record parsing that must not be mistaken for this new contract. The permanent source target and launch seed entry are registered; owning generation **92362** and freshness **40968** are both **GREEN**, exit 0, including the semantic source entry.

The coordinator's Flow gate 84186 timed out at its 20-minute build cap with no Flow assertions and retained the shared target. Root retry 36007 owns that target with a one-hour build cap. Per the requested batching boundary, this lane continues to leave the SPR Rust module unmounted and runs no Cargo while the first Flow assertion frontier is being reached. No old test coverage was deleted and no generated target was cleaned.

## Retained SPR Compressed Grammar And Profile Audit

The retained-only profile is now explicit in schema, Rust and independent oracle: canonical v1.0 with only the required hash-chain flag; signed/encrypted required flags and later minor versions reject. Body-length and compressed raw-length varints must be canonical. This intentionally narrows the generic reader's accepted language, and there is no fallback to that reader. Compressed frames are structurally checked only: a valid DEFLATE fixture is independently inflated by Node zlib, but the verifier does not decompress or authorize raw-output allocation. Reserved frame bits, compressed identity codec, codec bits without compression, and compressed commit frames reject.

The strict compressed corpus has ten cases: one valid committed compressed record; missing, nonminimal, overflowing, unterminated and ten-continuation raw lengths; compressed commit; reserved flag; and both codec/compression inconsistencies. Native laws invoke the same fixture through a private helper, compare the positive frame against the production writer and independently compute commit hashes using dev-only `blake3`. Native invocation remains unrun while the module is unmounted.

- Source TDD **76114**: intended RED after independent model success because the compressed native helper was absent.
- Intermediate source **20691**: GREEN, exit 0, with seven compressed cases; not credited for the final ten.
- Final current-source **43033**: **GREEN**, exit 0, from `bun ./📜️script.ts nx run @semio-tech/framework-replication-rs:retained-verification-check --skip-nx-cache -- --oracle-only`: **2 commits, 224 prefixes, 26 general hostile denials, 10 compressed grammar cases**.
- Terra's final current-byte reread source-qualifies bounded framing/profile with no remaining source framing defect. Module mount/native execution, caller-owned input/one-use handoff/cancellation and semantic/decompression authority remain explicit nonclaims. See `📓️terra-retained-spr-verifier-audit.md`.

## Retained History Input Owner — Staged, Unmounted

The next owner lives under `store/🧩️composition/🚪️open/📜️history`. `MemberHistoryVerification` owns the original sealed request, one fixed pending byte and the retained framing scanner. Copying and hashing consume separate fuel units. Every step and handoff rechecks actual operation/generation, cancellation and time; a step deadline yields without transferring ownership. Header-only input is denied because member history requires a verified commit. A recoverable tail remains owned but is not readable through the verified range.

`take_ready` moves request and span together, exactly once, into crate-private `VerifiedMemberHistoryInput`; no standalone span or raw request is returned to external consumers. Its bounded read only exposes the committed prefix and rechecks authority before/after copying. It independently owns bounded retirement after transfer, including semantic-stage refusal before any decoder is attached. Failures before transfer retain the verifier's original request; failures after transfer retain the witness's request. Both reject nonterminal Drop. No semantic decoder, member or app publication is exposed.

The first owner oracle was deliberately strengthened after audit: tail recovery now comes from the full independent structural/CRC/commit-chain model, not precomputed offsets. That test-only model was extracted once into replication's existing script (`inspectRetainedSprNeutral`) and imported under a main guard, without any production Rust codec import or duplicated script implementation. Thirteen explicit owner-state traces carry retained/retired byte counts and handoff counts, including one successful transfer, second-take denial, cancel/stale/expiry before and after transfer, and bounded retirement. The five actual framed input cases include a two-page recoverable tail with exactly **4,531 bytes** of owned input/identity retirement under the neutral grants.

- **92057 RED**: owned source import path had one excess parent traversal; fixed to the verified existing path.
- **19006 RED**: intended TDD missing Rust owner source after 3 accepted/2 denied inputs and twelve initial authority cases.
- **84068 GREEN**: initial source model; superseded by the stronger derived-span/owner-state oracle, not accepted as handoff evidence.
- **83549** and final owner-source **59510 GREEN**, exit 0: **3 accepted / 2 denied derived SPR inputs, 13 owner-state traces, three fuel grants**, exact paged accounting. These remain independent source-model results, not native assertions.
- Replication source **68843 GREEN**, exit 0, after the shared test-oracle extraction: unchanged **2 commits / 224 prefixes / 26 denials / 10 compressed cases**.
- Rust syntax-only **e01887** and owned diff hygiene **c3a04f GREEN**. No typecheck is implied.
- Owner launch generation **44081** and freshness **5312 GREEN**, exit 0. Source/native commands have explicit ticket-owned artifact/target paths and exact-two native discovery; native dispatch refuses the unmounted module.

Terra's current-byte reread source-qualifies owner retention, context fences, private handoff and bounded witness close. Both Rust modules remain unmounted. Root 36007 reached the first Flow compiler frontier after Stdio succeeded, then reported Flow test-only errors; root retains the shared target for that repair loop. This lane has launched no Cargo and makes no native owner, typed-history, public Flow reload, host persistence, transport or readiness claim.

The semantic identity fixture now explicitly owns `expectedOwner:null` for the unowned case rather than changing authority invisibly. Every earlier committed composition is structurally parsed even when a later overlay wins; a new malformed-earlier-overlay row rejects. Dictionary byte and total pin limits now have independent low-limit hostile vectors in addition to entry/group limits. Final registered semantic source **30520 GREEN**, exit 0: **5 accepted / 27 denied** rows; the intermediate 65847 result covered only 30 rows. This is still a schema/oracle foundation, with no mounted Rust semantic decoder.

## Fixed-Buffer Semantic Tagged-ID Primitive

The next real semantic primitive is staged under `spr/📜️history/🛂️identity/🪪️id`. `RetainedHistoryIdV1` has a fixed 256-byte output and incremental UTF-8 scalar state; no decoded `String`, record, dictionary table or input owner is allocated. Raw text, dictionary reference and prefix-UUID tags are parsed one byte per caller fuel with canonical varints, exact index handshake, explicit dictionary-byte feeding, Unicode Cc rejection and canonical lowercase UUID expansion. Ordinals are not valid identity fields. Truncation, wrong dictionary index, index/output/prefix capacity and cancellation are closed diagnostics. The actual retained operation still must bind the requested index to a dictionary derived from its own verified input; this cursor does not mint that authority.

The independent Bun/AJV/third-party LEB128/TextDecoder oracle now owns **20** exact tagged wires: **4 accepted / 16 denied**, including wrong-index and prefix-capacity vectors. The staged exact Rust law exercises all twenty under three fuel grants, zero fuel, both dictionary handshake negatives, every raw-wire and dictionary-byte cancellation boundary, and exact fixed-buffer clearing. The source primitive and one native selector are registered, but the containing history module remains unmounted. No Rust execution, full semantic record/dictionary pass or pre-hydration authority acceptance is claimed.

- **80690** and **41721 RED**: owned schema case-id pattern excluded the digit in `invalid-utf8`; the declared id grammar was corrected. The Unicode raw fixture length was also corrected to its exact twelve UTF-8 bytes before the intended implementation RED.
- **62292 RED**: intended missing Rust primitive after the independent 18-wire model agreed.
- **48607 GREEN**: initial 18-wire source gate; superseded for the expanded corpus.
- Final tagged-ID source **39905 GREEN**, exit 0: **4 accepted / 16 denied** exact tagged wires. No native assertions.
- Rust syntax-only **297755** and owned diff hygiene **8c68d2 GREEN**; neither is a typecheck.
- Owning launch generation **82057** and freshness **71806 GREEN**, exit 0, include both source/native tagged-ID entries with explicit ticket artifact and shared target environment. Native mount guards require crate-private history/identity modules, preserving the non-public handoff boundary.

The coordinator still owns the warmed target for Flow compiler repairs. This lane has made no live kernel/replication module mount and has launched no Cargo since the earlier two-law Flow snapshot acceptance. Next integration remains: a bounded verified dictionary/record pass consuming the retained input, exact final document/composition comparison before typed snapshot hydration, retained mutation/history/replay/initialization, and the app-owned final transaction. The old public member-open/host paths remain explicitly unaccepted.

Final post-guard source reruns all exited 0: tagged-ID **88481** (4/16), retained input **51337** (3/2 inputs, 13 owner traces), semantic identity **54792** (5/27), and SPR framing **36571** (2 commits, 224 prefixes, 26 denials, 10 compressed cases). Exact read-only mount census **e8e3e2** found no retained/history/identity mount in the three existing production parent modules. These terminals are current source-only evidence and do not supersede the unmounted/native-pending qualification.

## Dictionary Ownership TDD And Five-Law Foundation Batch

The new dictionary/record-owner contract and source model are documented separately in `📓️sol-retained-history-dictionary-owner-protocol.md`. The strict neutral corpus constructs independently CRC/BLAKE3-verified SPR histories before semantic interpretation, then tests atomic delta visibility, operation-wide dictionary byte/pin/group caps, immutable request identity, two-page indexing and exact denied-owner retirement. Final source **66812** reaches **4 accepted / 26 denied histories + 11 lifecycle traces × 3 grants**, then intentionally exits 1 at the missing Rust dictionary owner. No dictionary implementation/native evidence is claimed. The tagged-ID non-poisoning `is_complete()` prerequisite remains explicitly deferred.

After root **97196** ended at external MP3 source-authority ENOENT, the coordinator authorized the audited five-law foundation batch. Exactly three mounts were added: public replication `format::retained`, crate-private store `open::history`, and crate-private SPR `history::identity` (whose existing child is crate-private `id`). Dictionary and MemberFactory are excluded. Earlier unmounted statements above are historical; these three foundations are now mounted, with native acceptance still pending.

The new permanent `member-history-foundation-check` reuses all four existing independent source oracles, then calls the shared same-binary exact-law runner with two groups: replication's `protocol` library (two full test names) and OS kernel library (two history-owner suffixes + one tagged-ID suffix, each exact-one resolved). Required terminal counts are `[2, 3]`, never zero-test success. Launch seed entry 419.999 has explicit ticket artifact/target paths and one Cargo job.

Native **23286** is active as of this checkpoint, with the shared `native-openable-provider-sol-target`, wrappers disabled, `CARGO_BUILD_JOBS=1`, and `SEMIO_TEST_ARTIFACT_DIR=<ticket>/🗑️generated/member-history-foundation-exact`. Exact runner root: `exact-cargo-laws-4iwMf6`; group `00` reached replication compilation after all four source oracles passed. This is build progress, not a native test result. No other Cargo was launched by this lane.

Group `00` subsequently completed **GREEN2**: one `protocol` test binary built in **1m43s**, SHA-256 `7fa620f07516ea80e3ee4db442bc9273bf5f4abad22a398f5dda50b053225a35`, with receipt `exact-cargo-laws-4iwMf6/00/receipt.json`. Exact FQNs are `format::retained::tests::retained_spr_verification_matches_neutral_commits_and_torn_prefixes` and `format::retained::tests::retained_spr_verification_rejects_hostile_frames_without_publication`. Each executed one test, passed one, failed/ignored zero. Retained DEBUG output confirms 2 actual writer commits, 224 prefixes at three fuel grants, 26 general hostile cases, 10 compressed grammar cases, and cancellation at all 256 byte boundaries. This is native framing evidence only. Kernel group `01` remains compiling; aggregate five-law acceptance remains pending.

Launch generation **71275 GREEN** was followed by freshness **71079 RED** solely on generated launch after a concurrent GIS seed addition. Backend owner then ran coordinated generation **72742 GREEN** and freshness **39009 GREEN**, preserving the foundation, GIS and Norm entries exactly once. Owned diff hygiene **6c1a96 GREEN**. No generated-target cleanup occurred.

### Final Five-Law Native Terminal

**23286 GREEN, exit 0**. Group `01` built the OS kernel test binary in **6m46s**, SHA-256 `938ea378b72f2e4f69a407f86a8714224dba005ba7528172692fc28a8f3cd29c`, and executed these three exact-one resolved laws:

- `os_store::component::member_open::history::tests::member_history_verification_retains_input_and_bounds_verified_handoff`
- `os_store::component::member_open::history::tests::member_history_verification_rechecks_every_owner_transition_and_retires_exact_bytes`
- `os_spr::history::identity::id::tests::retained_history_id_cursor_matches_neutral_bytes_and_refuses_unowned_resolution`

Each ran one, passed one, failed/ignored zero. Receipt: `🗑️generated/member-history-foundation-exact/exact-cargo-laws-4iwMf6/01/receipt.json`; all list/build/law outputs remain beside it. Runtime DEBUG observations confirm five framed input cases at three grants, independent copy/hash credit, exactly 4,531 bytes of paged input/identity retirement, thirteen retained-owner lifecycle traces, one transfer with repeat denial, twenty tagged-ID wires at three grants, exact UTF-8/UUID handling, foreign dictionary denial, prefix capacity, and every raw/dictionary byte-boundary cancellation. Aggregate gate verifies counts `[2, 3]`, **five native laws**, from the two recorded binaries.

The shared target was released to the coordinator immediately after terminal. No further Cargo was launched. This closes only the mounted framing/input-owner/tagged-ID foundation. It does not close dictionary/semantic record decoding, typed history/replay/initialization, MemberFactory or public Flow reload, app-owned final transactions, host persistence, transports or readiness. Historical full-fleet RED qualifications remain unchanged.

While native compilation ran, only a new **unmounted** dictionary range-index source was staged under `history/🗂️dictionary/📇️index/🦀️.rs`; it is not in either test binary or the five-law selector. It has private fixed 64-entry/1,024-byte pages, aggregate entry/byte caps, atomic delta publication and exact bounded byte retirement, with two local unrun laws. It supplies no input-reading authority and does not implement the missing record owner or tagged-ID completeness API. Its source/native status is separately tracked in the dictionary protocol report.

### Unmounted Dictionary Payload Cursor

The record-local schema/fixture and independent Bun/AJV/third-party LEB128/UTF-8 model now pin 34 wires, stopping offsets, event counts and scratch retirement. Source **67187 RED** found the missing local Rust fixture binding; after both staged laws consumed the exact fixture, registered source **99946 GREEN exit 0** proved **11 accepted / 23 denied × 3 grants**, including held-event fences, canonical multi-byte lengths and retained scratch zero through four bytes. No Rust cursor/index assertion has run. Whole dictionary source **12633** remains intentionally RED at the absent owner after **5 accepted / 28 denied histories + 11 lifecycle traces × 3 grants** passed (including exact BOM identity denial).

Canonical launch generation **88895 GREEN** and freshness **27122 GREEN** include source/native record entries 419.9981/419.9982 and preserve concurrent provider gates. The native record command refuses dispatch until its parent mount exists. Root now owns the target for Flow; this lane made no additional mounted edits or Cargo calls. Next owner dependency is a non-authoritative record-boundary observation derived from the existing retained verifier, plus a non-poisoning tagged-ID completion query; no duplicated SPR/ID parser will be introduced.

Final record source **38374 GREEN** additionally pins every ordered Begin/base/count and Entry/range fact (all 34 rows × three grants). A new unmounted child extension of the existing replication scanner observes its current record metadata without parsing, copying or advancing input; source **54316 GREEN** covers 11 exact observations/absences. Declared raw sizes are not allocation credits and pre-trailer metadata is not CRC/commit proof. Generation **17126 GREEN** and freshness **58798 GREEN** register this later metadata gate too.

Whole dictionary source **8973** remains expected RED at the absent Rust owner after 33 committed histories, 11 lifecycle traces and seven new exact combined scratch/input/index retirement traces passed. TDD **4251** exposed one missing retained UTF-8 byte; **78186** exposed a one-byte fixture input estimate error, both corrected with explicit literal expectations. This source progress is separate from historical native **23286 GREEN5** and does not mount or activate the full dictionary/public-member path. Full details remain in the dictionary protocol report.

Final current-byte source terminals: metadata **64233 GREEN**, payload cursor **58867 GREEN**, scoped diff hygiene **b1d48e GREEN**. Source/native distinction and the missing whole-owner RED remain unchanged; no new Cargo invocation, target mutation/cleanup or mounted module change occurred.

### Actual Dictionary Owner Staged, Native Still Pending

The missing-owner source condition is superseded: `history/🗂️dictionary/🦀️.rs`, semantic `🛂️identity/🦀️.rs` and `🧪️tests/🦀️.rs` now retain the actual input witness, index, existing scanner, pending-copy/ID/UTF-8 scratch, cumulative limits and private one-use semantic handoff. They remain unmounted; no MemberFactory or public persistence path changed. Two unrun native laws construct real production SPR records and use the actual retained input verifier.

Registered source **50966 RED** moved the frontier to the frozen missing non-poisoning tagged-ID completion query. TDD **39760 RED** then identified missing native binding for nine new pending-copy/ID traces. After binding them, **16487 RED** passed the complete independent model—33 histories + 11 authority + 7 payload scratch + 9 owner scratch traces at three grants, plus 14 ordinary visibility events and ten literal ranges—and stopped only at that frozen API guard, before Cargo. Historical native **23286 GREEN5** remains separate.

The metadata child/query/dictionary parent need coordinated mounting and exact native execution after the root build. Expected schema must still be privately derived from the selected closed factory/declaration before any external path; the staged static-string parameter does not establish that authority. Full current protocol and terminal details are in `📓️sol-retained-history-dictionary-owner-protocol.md`. No target or generated directory was removed.

The coordinator subsequently released the freeze and then the native target. Metadata observer, crate-private dictionary parent and non-mutating tagged-ID completion query are now mounted. ID TDD **7897 RED** preceded final source **50041 GREEN** (20 wires +11 completion boundaries); dictionary **68455 GREEN** and metadata **83730 GREEN** are also source-only. **70042 ACTIVE** is the first exact two-law native owner gate in the shared warm target, with ticket-owned receipts and jobs1/wrappers disabled. Mounted bytes are frozen through its terminal. Factory/space binding, pin membership authorization and all typed/public-member integration remain open; no source or mount result promotes them.

**70042 is now TERMINAL GREEN exit 0, exactly two native owner laws.** Kernel test build completed in 1m43s; both full-name direct invocations passed exactly 1/0/0 in 0.39s and 0.10s. Receipt `🗑️generated/member-history-dictionary-exact/exact-cargo-laws-DbP7cw/00`, binary SHA-256 `c3bdb2cacc68c3de832f319e5766ca71c14f1bacbdf13366a45285f135725abe`. Real runtime covers 33 production-writer histories ×3 and 11 authority +7 payload scratch +9 pending-copy/ID traces ×3, with exact visibility/ranges and retirement. Shared target released to root immediately. Separate primitive/query/metadata test functions were not selected; factory/authorized-space/pin membership and typed/public member persistence remain unproven. Full names and counts are preserved in the dictionary protocol report.
