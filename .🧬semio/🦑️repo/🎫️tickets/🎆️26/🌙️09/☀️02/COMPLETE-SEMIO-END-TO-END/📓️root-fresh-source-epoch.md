# Fresh Source Epoch Qualification

The existing producer proves fresh component/core/descriptor bytes in separate empty targets but not a shared Stdio→GIS source epoch. GIS recompiles Stdio with `default-features = false` and `full-artifact-catalog`; the standalone component uses its own root features. Preserve both build targets. This work does not freeze, copy, or lock collaborators' source and does not promise an adversarial instantaneous filesystem snapshot.

## Current Owner and Neutral Contract

The describe owner's `📜️script.ts` now contains `captureFreshSourceEpochV1`: bounded no-follow tuple capture, immutable ordered toolchain/environment/command records, exact per-leg admission and completion, revalidation of every captured source at each boundary, closed-on-failure/cancellation state, and finalization only after every ordered leg. Captured bytes are immediately zeroed. The reused `semanticOwnedInputFileSnapshot` now accepts explicit byte limits and cancellation checkpoints, reads fixed-size chunks through its retained descriptor, and zeroes an incomplete buffer.

The three schema-first fixture files are `🧪️fixtures/🧾️fresh-source-epoch/{🔣️.json,🧬️.schema.json,📌️epoch.schema.json}`. Seventeen physical rows cover ordered shared ownership, changes before/between/after legs, unknown compiler input, missing/replaced/linked/oversized source, argument/environment substitution, duplicate or incomplete completion, cancellation/deadline, and closed owner refusal. Every row preserves a prior pointer sentinel. Additional laws check seven unsafe coordinates and two bounded reader cases. The independent oracle uses AJV, fast-json-stable-stringify, DataView encoding, and WebCrypto SHA-256.

Source85728 was RED: the existing repo `canonicalJson` sorts arrays of objects carrying IDs, which changed the required Stdio→GIS command order. The epoch now uses a private array-order-preserving sorted-key serializer; the oracle also explicitly distinguishes reversed legs. Full registered source60703 then passed, including all 17 physical epoch laws, the two bounded-reader checks, seven hostile path checks, and the existing complete bootstrap/publication source gate. Evidence is `🗑️generated/trusted-stdio-gis-browser/fresh-source-epoch-rgo70s`. This qualifies the owner against controlled source files, not actual Cargo input closure or materialization.

## Remaining Compiler Integration

This owner is tested via the registered existing `os-hub:trusted-stdio-gis-bundle-check --source` branch. It is not yet a production source receipt. A real resolver must bind full locked Cargo metadata/tree selection, exact compiler argument/environment/toolchain identity, mounted Rust roots and source paths, and actual complete compiler-artifact/dep-info/build-script reports for both private targets. Build-script directory inputs require membership witnesses, not file-only hashes. `complete` currently accepts a checked nonempty subset of captured paths; that does not establish report completeness. It rechecks all captured files, so omitting an already-captured file cannot suppress that file's drift check, but the trusted compiler report parser must reject actual inputs that were absent at preflight.

A read-only diagnostic whole-workspace source scan found 56,426 candidate source-like files totalling 1,207,381,991 bytes (40 above 4 MiB). Therefore a whole-repository capture would exceed the deliberate 32,768-file/128-MiB limits and include unrelated work; use the selected compiler closure instead. No whole-workspace source copy was made.

The actual native publication CLI57727 ended RED after Stdio code generation at two Hub inference runtime type mismatches; Home repaired that source frontier, but no retry has run. Registry generate29847 and check-generated50206 both completed successfully. No source-epoch, genuine guest, Windows durability, current activation, or two-author Map completion claim is made here.

## Compiler Report Grammar

The next test packet is 19 schema-first Rust dep-info rows plus UTF-8, byte-bound, and cancellation checks. It admits the actual Rust writer's space-escaped dependency names, raw output target names, matching repeated output rules, exact fake input rules, and escaped environment comments. Windows drive paths are grammar fixtures only, not Windows physical qualification. Unknown/truncated/recipe/continuation syntax is refused rather than guessed. Source56959 ran and was RED at the deliberately unimplemented parser. The bounded strict parser is now implemented; full source89043 passed with all 19 parser rows, three hostile byte/cancellation checks, and the 17 physical-owner rows. Evidence is `🗑️generated/trusted-stdio-gis-browser/fresh-source-epoch-2s2NOZ`. Membership validation uses sets, avoiding quadratic fake-rule scans across the 32,768-input bound. This still does not qualify real Cargo closure or production receipt integration.

The installed compiler identifies itself as Rust 1.99.0-nightly, commit `c4af71034e89a431eeee91125a31ad001379faac`, dated 2026-07-06; Cargo is 1.99.0-nightly commit `2f0e7011e`, dated 2026-07-05. The active native target already contains dep-info with mounted relative `../../` paths and `OUT_DIR` environment comments. Raw compiler paths must be resolved under the proven command working directory before safe repository-coordinate validation; rejecting every raw `..` would reject this repository's actual mount model.

Cargo documents adjacent `.d` files as the artifact dependency record and warns that internal build-directory layout can change. The adapter must bind the selected files to actual compiler-artifact identities rather than glob an arbitrary target tree. [Cargo Build Cache](https://doc.rust-lang.org/cargo/reference/build-cache.html)

Build-script stdout is retained separately in the build directory; rerun declarations can reference entire directories and environment variables. That output needs its own bounded capture and directory-membership witnesses. [Cargo Build Scripts](https://doc.rust-lang.org/cargo/reference/build-scripts.html)

Rust's current dep-info writer emits all output rules over the same input list, fake targets for those inputs, and optional environment comments. Its filename escaping replaces spaces; environment escaping handles backslash, LF, and CR. The parser contract is intentionally this writer's grammar, not a general Makefile evaluator. [Rust Dep-Info Writer](https://raw.githubusercontent.com/rust-lang/rust/master/compiler/rustc_interface/src/passes.rs)

## End-to-End Admission Versus Compiler Provenance

The current read-only audit separates two requirements. Actual component bytes, their guest-produced canonical descriptors, selected exact dependencies, catalog semantics, and final publication CAS are end-to-end admission requirements. A complete source/build-script/toolchain input epoch is a stronger reproducibility/provenance feature; it is not evidence that a functioning compiled pair exists and is not presently wired into publication.

The next implementation packet is a compiled Stdio dependency commitment in GIS/VCS descriptors, produced by each consumer's actual linked Stdio crate and checked against the same private native expectation used to check the Stdio guest. Exact manifest dependencies and bundle generation dependencies must agree before any provider factory preview. The generic builder must retain topics before completing assembly. This directly addresses cross-component semantic agreement without pretending a semantic catalog attests all executable algorithms or compiler inputs.

The audit's full four-leg probe/final compiler-observation protocol remains the design for genuine source-epoch qualification. Its missing directory/build-script/OUT_DIR observations are not silently accepted. The existing physical epoch and dep-info tests remain valid limited tests; no production provenance claim is made.
