# Native Stdio Catalog and Component Assembly

Native publication CLI57727 completed Stdio LLVM code generation after approximately 44 minutes, then failed on two Hub inference runtime E0308 errors. Its evidence is `🗑️generated/trusted-stdio-gis-browser/exact-cargo-laws-xK5ByK/00`. Home repaired the reported source types; no native retry is yet qualified.

The bounded compiler sample found 14 GiB resident memory in the Stdio compiler. Native Hub requested all 36 artifact definitions and 26 codecs through `full-artifact-catalog`, but that same feature instantiated the complete 176-variant, 78-method guest application dispatch. GIS and VCS also directly forward the full feature, so changing only Hub would not remove it.

## Source Separation

The schema-first Home surface fixture now has six feature-union cases. Registered source97242 was RED at the absent `component-app-assembly` feature. Source37108 passed after separating `plugin-root → component-app-assembly → full-artifact-catalog`, gating the plugin/editor/viewer mounts and six Binary/TXT guest declaration functions, and moving component package identity into the catalog registry. All 36 artifact roots and 26 native codecs remain required. Bun's TOML parser and independent Iarna TOML parser agreed; AJV validated the fixture; the complete guard checked 35 dependency consumers and the eight DWG ownership rows.

Native receipts now use the artifact-owned catalog projection without constructing guest apps. This is a source-level separation only; neither its actual Cargo feature closure nor its compile-time/memory improvement has been measured yet.

## Descriptor Verification

The audit found that the native provider ignored the decoded guest descriptor and the loader accepted a kind subset. Nine new neutral descriptor-surface rows require exact complete kinds, allow reordering, and refuse missing/extra/duplicate/foreign identity/schema/label/source format. The native test compares first-party validation against independently serialized Serde values; it is authored but unrun.

The full 36-definition semantic commitment is separate work: PackageDescriptor currently does not carry the ten definition-only roots. A domain-neutral owned commitment must bind actual guest semantics through the compiled descriptor and be revalidated by the native provider. Source hashes or a synthetic fixture descriptor cannot stand in for that proof.

Provider source9232 was RED at the missing complete decoded-descriptor binding. Source96343 passed after adding the exact 26-kind validator before native factory preview; the registered full bootstrap/publication source45556 also passed. The nine new native surface cases remain authored but unrun. The Hub readiness fixture now constructs explicitly synthetic JSON surface metadata and the full native kind projection instead of calling the genuine Stdio guest assembly; its component bytes were already the synthetic `abc` fixture and it makes no guest execution claim.

Rustfmt parsed the changed registry/provider/native-test files but its `--check` returned formatting differences, including pre-existing unrelated lines. It was not a clean formatting result or a Rust type check.

Other owners' concurrent Cargo processes have not been signalled or modified. An updated `memory_pressure -Q` query reports 39% system-wide memory free on the 32-GiB host despite a low raw `vm_stat` free-page count and approximately 20.7 GiB of historical swap. Raw free pages alone overstated the admission constraint. Root is admitting one jobs=1 headless Stdio group while other fleet members remain source-only.

## Full Guest Semantic Commitment

The existing `TopicContribution` contract is explicitly designed for domain-specific declarations inside generic plugin manifests. The compiled descriptor already contains that manifest, and the trusted loader rejects duplicate object keys plus any noncanonical decode/re-encode result. The Stdio owner now emits mandatory topic `stdio.artifact-catalog.v1`; no generic descriptor V2 or omitted-commitment compatibility path is needed.

The guest computes its projection from the same 36 assemblies it hands to its builder. The headless provider recomputes the same ordered definition identities, capability identities/kinds, descriptor-byte SHA-256 values, executable-presence booleans, claims, localizations, and complete 26 native receipt tuples. Process-local executable addresses and source hashes are excluded. The native boundary rejects missing, duplicated or foreign-version catalog topics; compares against the bounded expected tree before cloning/typed decoding; and requires exact typed equality before previewing bindings.

Schema-first source19500 was RED at the missing guest-semantic binding. Source69974 and the new source-only command-selection check67559 passed after implementation. The fixture now has 29 cases including an explicit codec definition identity and a bare reserved topic alongside the valid topic; it checks both JSON and Pack round-trips. Native cases remain unrun. A registered `--stdio-only` submode isolates the native full-catalog integration target with defaults disabled. Registry generate50153 and check94495 passed, including the new launch entry411.10851.

Native group44510 is active under `🗑️generated/trusted-stdio-gis-browser/exact-cargo-laws-AY6eCc/00`; it reached framework-plugin compilation. Five compiler-import fixtures and an exact emitted-rlib/feature-report probe were added after that running script loaded. Therefore even if44510 succeeds, that run cannot qualify the new import probe; a current-script invocation remains necessary. Probe inputs, compiler metadata and output stay under the exact group's ticket-generated directory.

The repeated Home surface source53290 and catalog-root contract95094 passed. The latter exercised its existing WebAssembly/Binaryen contract, not a real Stdio guest. Provider oracle99345 and30785 passed after the identity and namespace/Pack fixture additions; these only validate schema/source boundaries until the native laws execute.

## Native Results and Aggregate Projection Bound

Native44510/AY6eCc completed compilation and passed the claim-authority, complete native receipt bijection, and exact full kind projection laws. The fourth semantic law was terminated by the runner's 60-second deadline: law-3.json explicitly records reason=timeout, not an inferred host OOM. There is no passing group receipt. Native96014/zAdOcl is the diagnostic retry, preserving the same deadline and adding direct per-case stage progress to distinguish validation, Pack encode, and decode.

The semantic audit confirmed that Pack canonically sorts object keys, whereas DslValue retains entry order. The expected-shape comparator therefore intentionally accepts permuted object fields and rejects reordered arrays. A thirtieth neutral row now directly reverses DslValue object entries and independently checks Serde equality and canonical Pack bytes.

Aggregate-budget source79276 reached the intended RED before implementation; source78328 passed afterward. The catalog now preflights exact JSON string/structure sizes on borrowed values before allocating the projection or hashing capability descriptors. It caps the projection at 2 MiB, each owned string at its schema byte bound, and aggregate descriptor hashing at 16 MiB. This bounds projection work; it does not claim the entire library's heap use is limited to 2 MiB. Six independent JSON escaping/UTF-8 rows and four aggregate boundary rows are schema-owned. Two private native laws compare the accountant with Serde and the actual full projection; these remain unrun. The prior post-serialization ceiling remains a defense-in-depth check.

Current native96014 loaded before the actual-payload AJV evidence check was added to the runner. That new check, like real guest describe equality and component execution, requires a later current-script run.

The audit also found that the source inventory spelled OBJ as the nonexistent 🧊️obj path while the Rust SOURCES array correctly compiles 🗽️obj. The new registered roster check reached RED89380; the index was corrected by hand and source4765 passed with all 36 existing files bound to the ordered Rust source list. Scoped rustfmt89482 completed, and the subsequent rustfmt check plus diff check passed. Full bootstrap/publication source64463 passed with the existing 17 source-epoch and 19 dep-info rows; neither is a real compiler-closure receipt yet.

The zAdOcl stage trace localized the cost: exact projection validation took 942 ms, followed by Pack encoding in about 10 ms and decoding in about 2 ms; total exact-row work was 3,461 ms. The field-permutation row took 6,257 ms, and a wrong-schema row took 3,494 ms despite cheap Pack operations. Full expectation regeneration was repeated before and after each matching-topic row. The private validator expectation now uses a process-static OnceLock holding only the compiled immutable payload and its typed catalog. No supplied descriptor is cached; every candidate still undergoes full shape comparison and strict typed decoding. Audit confirmed the expectation depends only on compiled include data, version and static factories, with no runtime environment or filesystem reads. The 30 public-boundary mutation/Pack rows and 60-second deadline are unchanged; this optimization awaits its next native run.

Native96014/zAdOcl ended at the unchanged 60-second deadline during the reordered-codecs case. Its first three laws again passed, and its stage records establish forward progress rather than a decoder loop. The budget lib laws did not run. Source28898 and scoped diff-check passed after the private expectation-cache change. A fresh registered native invocation now includes the budget lib laws, actual emitted-payload AJV schema validation, and exact-rlib import probes.

Native34716/wYztZn passed all four integration laws. The complete 30-case semantic law finished in 3.18 seconds without changing its deadline or skipping any case. The following lib-test compilation failed at an existing Binary set-snapshot test mount that still named the pre-shortening directory; the actual retained fixture is 🪡️rewrites-the-two-46c073. That exact mount was repaired without removing or weakening the test. Consequently the two budget laws and post-run AJV/import checks are still not qualified by34716.

Native29669/4ygM6A again passed all four integration laws, then the lib-test target found a second old fixture mount in CSV set-snapshot. The read-only audit is resolving the complete remaining source-to-existing-fixture map before another lib retry. The runner now performs actual-payload AJV validation and exact emitted-rlib import probes immediately after the integration group, before entering the separate lib group; no later failure can be mistaken for those checks having executed. The current Hub undo source is stable for its native compiler observation, so root is running the existing publication CLI target while this fixture census proceeds.

## Builder-Owned Topics

The generic builder's three-row neutral topic fixture reached RED3058 before implementation. The builder now owns topic contributions across its typestate transitions and installs them into the final manifest before assembly completes. Domain-specific repeated-topic policy remains with each domain rather than a blanket generic restriction. Stdio uses that builder path instead of mutating its manifest after completion. A fifth integration law compares empty/one/repeated-topic payloads against Serde and Pack roundtrip equality; it is authored but not natively run. The actual Hub CLI48083/JmH20X loaded before this new builder-law packet, so it cannot qualify that new law.

Builder source25763 and full bootstrap/publication source89569 passed. The fixture census found 152 additional test-only path attributes (63 direct cfg and89 inherited cfg) plus10 JPEG include strings. Root independently checked every old literal was unique in its current source, its old target was absent, its replacement was a regular no-follow file under the same Stdio test owner, and only the shortened test-directory component changed. All162 literal repairs were applied with scoped patches; all existing tests and fixture files are preserved. The earlier Binary mount repair is additional. The post-patch162-row filesystem/literal verification and scoped diff-check passed. A current registered Stdio qualification is queued on the same owned target after the active Hub publication build.

Current sessions: Hub publication48083/JmH20X is compiling; Stdio35202/SmVuBv is waiting for that same owned target's Cargo lock. The Hub's observed Stdio rustc invocation enables only full-artifact-catalog, not component-app-assembly or plugin-root; merely appearing in rustc's check-cfg allowed-value list does not enable a feature. The queued Stdio run contains five integration laws, independent actual-payload AJV validation, five exact-rlib compiler probes, and two budget lib laws. None of the newly queued checks is reported as passed yet.

Hub48083/JmH20X ended at one reported bin-test name-resolution error in the root-authored synthetic JSON viewer fixture: LocalizedLabel is exported by semio_framework_plugin, not semio_framework. The exact path was corrected and the publication CLI was queued again. No native publication assertion ran; absence of other diagnostics is not a passing undo/runtime test. Stdio35202 can now acquire its target lock.
