# Trusted Browser Actor Catalog

## Current Checkpoint — September 6

Source83166 passed complete actor-aware bootstrap framing,19 staged-file cases,
15 codec-capture cases and11 fresh component owner laws. The independently
computed profile generation is `eca8c78ec518fe21d781a7e175bc77a9b2b9bdb3e4dd4afe3af16dbc952d61e3`;
the four actual profile/initial/stale receipt fixtures are updated together.
Source60086 also passed the26 actor records,8 body/cancellation oracle rows
and6 production rotation-source capture laws with WebCrypto receipts.

The Terra rotation-source finding is repaired: captured bundle bytes must
match the retained SHA-256 before JSON decoding, and the decoded profile and
generation must match the retained receipt. Rotation component/descriptor
reads now use the same stable opened-file capture as actor reads. A subsequent
small caller change moves source capture inside cancellation/cleanup ownership
and uses stable capture for the current pointer too; source82876 passed it.

Write-owner test7177 then failed specifically at cancellation after fsync. The
writer now checks cancellation there and still attempts incomplete-file removal
if closing reports failure. Source69938 passed all7 real filesystem write laws:
64KiB boundary and plus-one, cancellation before open/between chunks/before and
after fsync, and preserving an existing exclusive owner. Source97245 repeats
the complete gate and also passes8 raw actor-length token vectors against AJV.
No zero-progress syscall injection or close-error injection was executed.

Private Rust actor length decoding now delegates to the already bounded domain
length type, preserving JSON Schema/TypeScript acceptance of `3.0` and `3e0`
without relaxing shared primitive integer-token decoding. Native coverage is
added to the current metadata law but remains unexecuted while the Hub build
compiles Stdio. The selected asset law also checks exact retained actor Arc
identity; those synthetic bytes are never executed.

The three actor launch commands are now authored in the launch seed as well as
the generated file; native launches set the ticket artifact directory and owned
warm Cargo target. Registry freshness91581 passed. This prevents a concurrent
registry regeneration from deleting the new launch entries.

Catalog38002/zXlguw failed before discovery because the initially registered
no-default-feature harness omitted SQLite and the native Stdio manifest used
by existing fixture producers. No native assertion ran. The corrected target
uses the actual default Hub feature set;49777/Gt5a6S is active with five exact
laws including26 metadata frames,8 loader body/cancellation cases and existing
selection/generation checks. No native pass or actor execution is claimed.

The remaining audit boundary is Rust catalog no-link/opened-handle ownership;
current digest checks authenticate bytes but do not establish no-link policy.
Required plan/lease propagation and child-Worker containment remain next.

The required private catalog field is being propagated through the existing
initial materializer, immutable generation, rotation and verified loader. It
is not an optional compatibility field. Wasm declarations require a derived
closed body; zero-target Stdio and non-Wasm declarations carry explicit none.
The public plan/lease propagation and isolated Worker activation remain next.

Test-first source receipt67399 failed the new closed-Wasm production case after
AJV admitted it. Source16056 then passed the26-case private metadata corpus
against the shared public schema, plus independent WebCrypto body hashing.
The corpus now also includes fixed closed/none generation encoding digests.
Rust metadata/frame law has been added, not yet natively run.

Current loader edits bind the required private record to verified component
and descriptor SHA-256; admit a separate128MiB actor closure before reading;
hash exact bounded actor bytes after descriptor validation and before native
provider preview; retain private Arc bytes; and add path-free actor metadata
to selections. Progress has four phases per package. No asset endpoint was
added and no actor is executed.

The framing inserts actor tag/schema/codegen/private path/fixed-u64 length/
four digests/count+canonical interfaces before the existing dependency count.
Private path binding changes only the opaque generation, not public selectors.
The production GIS builder now consumes the fresh one-shot copied component
lease, checks its returned component digest, writes the derived body to one
fixed private path and wipes the generated byte owner on every exit. These
producer/loader edits still require complete fixture/rotation/fence propagation
and native qualification; they are not currently accepted end to end.
