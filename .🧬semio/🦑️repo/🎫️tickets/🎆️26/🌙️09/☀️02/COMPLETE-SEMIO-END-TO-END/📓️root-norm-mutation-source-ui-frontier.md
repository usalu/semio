# Norm Mutation Source and UI Frontier

## Current Evidence

The original Norm library compile frontier was 587 errors. Direct payload self-import collisions, missing split-leaf derives, non-const edition identity, and stale UI return types were corrected. All thirty editor/viewer roots now follow the current synchronous authoring traits and return fallible `ComponentTree` results; shared render helpers and leaf windows propagate fallible `BuiltNode` assembly.

The canonical `@semio-tech/norm-plugin:check` invocation first completed with the Nx success banner after the schema correction (`norm-check-schema-restored.log`), though that diagnostic filter returned 1 because its lowercase pattern did not match the capitalized banner. The later unfiltered canonical invocation, session 51509 (`norm-check-concrete.log`), exits 0 after the schema-owned config work and the four stdio descriptor-owner repairs. This is library compilation, not native test execution.

The obsolete generic `SetArtifactMutation`, `ArtifactDiff`, `commit_document`, and their generic-only tests were removed. Concrete norm field mutation paths remain the production authority. All fifteen editors use the framework's uninhabited `NoPresenceMutation`; the fake no-op runtime presence implementation was removed. Empty schema leaves remain available to the app descriptor and truthfully describe `NoPresence`.

EN 1990 and EN 1992 national-annex mutation owners were physically renamed from `set-snapshot` to `change-annex`; mounted modules, source imports, descriptors, and the committed taxonomy were updated. The independent artifact taxonomy gate passes with 392 payloads (371 direct, 21 split), AJV schema validation, and hostile vectors. Historical mutation scenario catalogs still require a separate completeness audit.

The derive source-authority gate passes again: four layout cases, two accepted, two rejected, and four hostile rejections. Kernel provenance validation now permits only the taxonomy's exact payload facet as an alternative to a direct source file; descriptor ownership remains direct. The full replication library suite now passes 242/242, including the source-contract law. Reaching it required correcting three test-module paths to existing files and repairing the causal test descriptor's real owner-relative schema locator. Its independent serde comparison now compares JSON object content rather than insertion order.

The two historical EN 1990/EN 1992 semantic catalogs and feature rows now say `change-annex`, while editor snapshot commands are unchanged. The 40 and 140 Rust fixture includes resolve against the current subset location and actual test directories; a direct filesystem census confirms all 180 exist. This is source-path evidence, not yet execution of the differential suites.

## Remaining Work

The config mutation enum now has exactly one schema-owned `ChangeSelectedCheckIndex` leaf, with a same-semantic inverse and no whole-config snapshot protocol. Its payload JSON schema, descriptor, Rust and TypeScript live together; the aggregate and text/binary codecs are schema-owned. All fifteen editor command tests and the shared emission use the new leaf. The five-case/five-hostile fixture was added first, and its independent AJV oracle passes. The first native attempt reached its 20-minute build ceiling before any assertion, so it did not establish an observed pre-repair test failure. Subsequent attempts exposed shared UI and stdio physical rename gaps before reaching Norm. The UI typed document-retirement source/test filenames were aligned to their already-canonical references; the stdio agent repaired four exact XML/HTML/SVG module paths. The combined configuration/surface native run then failed before assertions because those four stdio descriptors still named their old owners, correctly triggering source-authority rejection and cascading `MutationLeaf` trait errors. Those exact descriptor owners were corrected concurrently and the stdio agent verified their current alignment; this report does not attribute that edit to the agent. Neither integration law has a runtime result yet.

The new public surface fixture names all fifteen variants, thirty editor/viewer app identities, and ninety declared body keys. Its independent AJV/inventory oracle passes with five hostile vectors. The external Rust law constructs the real `VcsArtifactApp<EditorApp<_>>` and `VcsArtifactApp<ViewerApp<_>>` wrappers, checks manifest identity/body correspondence, renders known bodies, checks typed structural tree output using serde, and exercises unknown-body and oversized-input behavior. This test is mounted and awaiting native compilation; it is not a successful render claim. It does not assert localization or assistive-technology behavior.

The config aggregate also has a JSON Schema and TypeScript wire declaration referencing the single payload rather than duplicating it. Its AJV run now checks both payload and operation, including four undeclared/old/extra-key operation forms. The first aggregate-schema run exposed a raw-Unicode relative URI resolution failure; the reference now uses a standard percent-encoded URI that is checked against the decoded physical owner, and the rerun exits 0. The native law includes the same four hostile operation forms but still awaits execution.

The source oracle was then extended to require exactly one `NormConfig` declaration at its schema owner. It first failed against the duplicated runtime/schema structs. The runtime now reexports that schema-owned type, which carries the actual DSL, metadata and value contracts; the second struct was removed. The registered oracle rerun passes. A public native `TypeId` equality assertion independently guards the same ownership boundary once the integration law runs. Nx's red-to-green task-history warning reflects this intentional source/test change, not an observed intermittent assertion.

The surface law now also assembles the actual Norm plugin and compares its registered app identities with all thirty fixture rows. A new source admission assertion first failed because the plugin omitted the required component package identity. The first insertion passed the string-only source gate but compiler session 25861 exited 1: `package_id` exists only on the ready builder, after `label` and `version`. The call now follows that typestate order; VCS and GIS owners received the same concrete compiler finding and corrected their own insertions. Actual assembly and native rendering still require the pending runtime law.

The next namespace source gate, session 2784, deliberately failed on the first incomplete `s.<family>` artifact identity. All fifteen definition roots, capabilities, composer claims, nested schema/IO coordinates, TypeScript and neutral fixtures now consistently use `s.norm.<family>`; the initial repair covered 47 files and the residual descriptive references were also aligned. Separate `computation.norm.<family>` report kinds were preserved. The source stage in session 13074 passes the 30-app/90-body inventory after this repair; its post-cutover library check is still running. That does not establish factory or composer runtime execution.

The auditor correctly identified that direct `VcsArtifactApp::new` bypasses registered action factories. The external surface law now gets every definition from the real plugin manifest, calls `plugin.create_app` for all thirty fixture rows, and renders those actual factory products. It checks exact factory app/document-schema/dialect correspondence, all fifteen definition-registry identities, and an unknown factory rejection. Its 90 known-body, 30 unknown-body and 30 oversized-body assertions remain intact and await execution. The earlier direct-wrapper description above records the initial harness, not the strengthened current one.

Further source tracing found that both surface builders unconditionally inherit `framework.body.history` from `AppBuilder`. The original ninety-body fixture enumerated only Norm-owned bodies and was not exhaustive. Source session 31598 first failed after requiring that inherited body; the corrected neutral schema/fixture then passed in session 32027 with thirty apps, 120 total bodies (90 owned plus 30 history), and five hostile vectors. The public Rust factory law now requires all 120 and also compares the stamped manifest document schema with the factory's schema. No 90- or 120-body runtime pass has been observed. This supersedes all earlier descriptions of ninety as a complete inventory.

The complete native plugin test suite, fifteen-variant renderer activation, all-plugin trusted catalog, real OS/hub process boot, and multi-user runtime remain unproven. Library compilation and source oracles do not establish those outcomes.

## Execution Infrastructure

Norm's test router now resolves the requested test level, forwards remaining arguments, and awaits its budgeted runner. New config source/native targets use the same permanent `📜️script.ts` router. Launch registrations belong in `.vscode/🧩️launch.seed.jsonc`; direct edits to the generated `launch.json` are replaced by generation. The backend agent restored the four Norm gate registrations in the seed.

The two surface source/native targets and their seed launch registrations use the same infrastructure. Registry generation and the independent freshness check both exit 0 with the six Norm gate registrations preserved. The taxonomy check now also verifies each physical owner, source semantic identity, aggregate variant, and payload-schema existence. After the coordinated artifact filename normalization the committed inventory was regenerated and the canonical generate/check pair both exited 0 with all 392 payloads.

The build-policy agent has separated native dev and `wasm-dev` profiles and verified its three registry policy laws plus freshness. Actual WASI linking remains unverified after a bounded scale build timed out in dependency compilation. Current memory observation on the shared 32-GiB host showed roughly 17 GiB occupied by compressed pages representing roughly 97 GiB of logical pages, with several unrelated workspace compilers and this fleet's in-flight compilers. The fleet is holding new heavy launches while existing runs terminate and is continuing source/audit work. No unrelated process was stopped and no warm target was removed. Root library session 13074 remains in stdio metadata compilation; native integration is not launched concurrently.

Session 13074 has now terminated successfully: the post-cutover canonical Norm library check exits 0 after 15m41s, including the ready-builder package identity and canonical artifact definitions. A separate warm-target metadata-only check of the two public integration targets is next; it is deliberately distinct from native assertion execution. The native provider's old in-memory runner has already entered a second Cargo invocation under the new native profile, so the fleet continues to serialize new linking work.

The first public-target metadata check, session 1524, exits 1 with an owned harness diagnostic: `#[async_test]` requires an `async fn`. The surface law now declares it correctly, and session 95134 rechecks the two public targets. Separately, source inspection exposed another invalid harness assumption: populated `BuiltChildren` deliberately refuse Serde serialization because production transport must be retained and paged. The test now projects nodes explicitly with a 4096-node/64-level bound, serializes only each typed component through the independent Serde oracle, and checks every node's rejected children plus sibling-key uniqueness. After each tree is dropped it advances the public UI-contract retirement API under an 8192-step bound. This uses an existing first-party dev dependency and does not relax production serialization or claim retained wire/renderer activation. Actual assertions remain unexecuted.

Session 95134 exits 1 before assertions: the external law correctly found that the Norm crate exposed a private `plugin` module but did not reexport its factory function. The crate now publicly reexports `plugin` and `NormApps`, matching the actual VCS public boundary; the test was not weakened. Warm metadata session 88068 checks both public targets with that fix and the strict-decoder changes below. The projection bound has also been corrected from the earlier arbitrary 4096 to the UI contract's actual 384 retirement slots, with the existing 64-level bound. Projection panic paths now drop and advance bounded retirement before resuming the original assertion failure.

## Strict Config Admission

The language-neutral config fixture now adds thirteen text and twenty-five literal binary vectors. Accepted forms cover omitted optional selection, zero, an ordinary index, maximum `u32`, and harmless text whitespace. Hostile forms cover unknown/duplicate fields, a second operation or trailing tokens, wrong types, overflow, truncated frames, unknown ordinals, trailing bytes, nonminimal integers, unused symbols and explicit binary absence. Text `index=null` is rejected because this DSL field is an optional unsigned integer: clearing is omission, not a new null alias. JSON continues to represent absence as null.

The independent oracle validates the strict fixture and payload schemas with AJV, text with a separately authored grammar, and literal binary frames with Node Buffer and BigInt minimal-varint decoding. Session 35062 first exits 1 on the missing exact-parser source requirement after its vector oracle succeeds; session 36156 exits 0 with five semantic cases, five hostile payloads, four undeclared operation forms, thirteen text vectors and twenty-five binary vectors after implementation. These are independent source/fixture checks, not native codec assertions.

The shared DSL now exposes `parse_exact` without changing the compositional parser. The shared pack layer exposes a closed, exact-body decoder that rejects trailing bytes and undeclared fields while preserving the existing forward-preserving decoder. Shared variant operation decoding uses that closed decoder and requires a byte-identical canonical reencoding, rejecting duplicate/nonminimal/unused data. Norm's schema-owned text codec uses the exact parser; its binary codec delegates to the shared variant protocol instead of maintaining a second binary implementation. The public config law now exercises the literal vectors, state transitions, inverses and canonical reencoding. Compilation and wider shared-protocol regression execution remain pending.

The provider's session 26201 now has real terminal runtime evidence: its production stdio link completed, and one exact public law failed on a duplicate XML dialect claim. This is not a passing provider catalog. The provider owner is repairing capability authority while the backend receives the next serialized heavy-build slot for its four SQLite laws and all-feature hub check. Root continues warm metadata checks and source work; no unrelated process or warm output is removed.

Session 88068 exits 0 after 12m22s: the Norm library and both external config/surface integration targets now compile with the public factory export and strict shared decoders. This is metadata compilation only; neither public test body has executed. Later shared member-factory and parent-dialect work is newer than that successful invocation and requires its own compilation. Shared parser/pack/variant regression assertions have also been added and await kernel test compilation/execution.

The next persistence audit proves the two composed Norm table slots are currently process-local caches. Full member identity/dialect/owner authority is being repaired first in the framework, documented in `📓️root-member-dialect-authority.md`; no empty/zero fallback, local cache or content-hash workaround is accepted as durable child restore.
