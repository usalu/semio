# Framework Strict Artifact Identity Admission

Status: source and independent oracle GREEN; actual Rust builder laws remain unexecuted. No admission, catalog, client-mount or readiness acceptance is claimed.

## Boundary

One internal `preflight_artifact_identity` now rejects malformed roots with `plugin-assembly.artifact-kind` and foreign owners with `plugin-assembly.artifact-owner`. It is called by the old declaration, definition-only and typed declaration-tree channels. The tree now projects app rows without publishing; its global commit follows every channel's preflight, old runtime-plan validation and contribution validation. The commit rechecks tree authority before publication.

The source-owned first-party cutover rebases 39 non-Norm/non-GIS definition roots and their capability identities/native dialect claims. Thirty plugin roots now have exact component-package declarations; Space, Playbook and Reasoning use their artifact's semantic owner identity. GIS's two roots are handled by its separate owner; Norm's 15 are handled by the coordinator. Payload schema identifiers and generated PackageDescriptor mirrors were not blindly rewritten. This is not a descriptor freshness or executable-provider claim.

## Evidence

- Schema-first neutral fixture: 15 admission vectors, including all three channels, malformed ASCII/Unicode identities, ownership negatives and Stdio controls. A separate 39-row source inventory pins definition/package ownership without becoming runtime authority.
- Independent Bun/AJV oracle: explicit segment decoder compared with AJV grammar; no production Rust parser determines its expected outcomes.
- Registered `bun ./📜️script.ts nx run @semio-tech/framework-plugin:artifact-admission-check --skip-nx-cache -- --oracle-only`: sessions 90638 and current post-claim-cutover 30083, exit 0, `cases=15 firstParty=39`.
- Four exact Rust laws added: all channels deny before publication; mixed-channel no publication; neutral fixture parity; owned tree/definition publication. Full gate discovers exactly four full names and one Cargo-receipted executable, then runs each in a separate process and requires exactly one passed law. These are **not yet compiled or run**.
- Launch seed and generated entry agree: owner generation 58308/41018 and freshness 21593/89770 all exited 0. Both concurrent member-dialect entries remain present.
- Final owned source `git diff --check`: exit 0, terminal 30741a.

## Preserved Runtime Frontier

Provider session 26201 is terminal RED (wrapper exit 1/Cargo 101): after a 21m38s current native-profile link, the exact public law ran once and failed in 4.86s on duplicate `dialect:s.stdio.xml@1.0/valid` composer/validator authority. This is a real product-law failure, not an external compile or timeout failure. The current runner retains Cargo's unique executable receipt and runs it directly after exact discovery, avoiding a second Cargo invocation.

No new heavy build was launched: the coordinator requested serialization under current host memory pressure. Historical broad Stdio and all-features D0 RED evidence remains unchanged. The projection now derives its artifact from the exact verified private factory, without a prefix alias. The source-owned authority repair introduces distinct `validated-dialect` and UTF-8-length-framed `codec-extension` claims, preserving ordinary file-format extension authority. All 15 Norm codec rows and the independently owned two GIS rows use the same framing contract.

The current provider fixture adds eight independent collision/framing vectors and a seventh exact public law. Registered oracle 50075 exited 1 before task dispatch because concurrent taxonomy configuration omits `next-app-page`, `next-app-layout` and `next-app-route` dispositions. No owned oracle or Rust law ran in that invocation.

After taxonomy recovery, provider oracle 83316 exposed an owned strict-AJV conditional-required declaration defect. The schema now declares required codec fields in the conditional branch and disallows them for non-codec rows. Current registered oracle 45391 exits 0: eight claim vectors, 26 source receipts, 26 protocol hashes and 13 hostile/no-partial cases. This is independent source/fixture evidence, not Rust execution. The seven provider and four builder laws await the serialized heavy slot and current MemberFactory signature stabilization.

## Remaining Proof

The four-law script now delegates to the shared repo-library `runExactCargoLaws` helper. Shared runner registered session 47607 is GREEN, 21 tests and 313 expectations, including actual child exit/timeout/cancel/output-cap evidence. This removes duplicated Cargo discovery/execution logic; it does not supply the still-missing four native builder assertions. See `📓️sol-shared-exact-cargo-law-runner.md` for explicit ticket-artifact configuration and stage-specific evidence semantics.

Run the four exact builder laws after the heavy build slot is released, including the real Stdio/VCS public-package positive controls. Complete descriptor regeneration through each owner before claiming fresh deployed metadata. Cross-registry concurrency and failures after successful preflight are not claimed as a stronger single-lock transaction by this packet; the proven intent here is rejection before publication for malformed or foreign artifact authority.
