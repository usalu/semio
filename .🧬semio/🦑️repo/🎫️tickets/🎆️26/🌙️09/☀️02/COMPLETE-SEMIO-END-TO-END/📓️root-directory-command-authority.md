# Directory Command Authority

The previous goal turn is progress: it implemented and verified inactive Kernel reservations, quarantine and transition guards, and unlocked registration replies; 24 exact native laws passed. The end-to-end goal remains active and unqualified.

This packet addresses a public Hub authorization race. Current REST authorization happens before the directory writer executes; only RemoveMember's target is gated. A durable demotion or session revocation can therefore overtake an already-authorized command. The intended boundary is one consistently ordered set of user/session/space/member gates, followed by live reauthentication and authorization, retained through durable command completion and exact connection invalidation.

Verification starts with language-neutral traces and a deterministic real-SQLite REST interleaving test: pause an author before the fence, revoke authority through production handlers, resume, and require rejection without an appended event or durable receipt. Converse ordering must serialize the revoker behind an admitted command. The audit is checking every membership writer and lock order; no repair or test pass is claimed yet.

Repo MCP ticket tools remain unavailable. Work continues in the existing ticket without inventing ticket operations. Hub's other execution agent owns space-public-boundary-sol-target; this packet will use a separate native build target.

## 2026-09-07 Verification Continuation

The recovered command had no live process or output folder. The registered Nx target is `os-hub:directory-command-receipt-native-check` (not `directory-command-receipt-check`). Its real run `exact-cargo-laws-q2uxGq/00` exited 101 before Hub tests: three production WGPU accessors in `📦️prepared.rs` were incorrectly test-only, causing five E0599 compiler errors from existing production draw/GPU consumers. Removed only those three `cfg(test)` restrictions and restarted the same registered gate. This is a build failure, not the intended authorization-law RED.

The separate registered source gate completed with 94 checks, including the four schema-validated command authority traces and independent Lodash ordering oracle. Added the inverse native ordering law and deterministic request-attempt semaphore; native behavior remains unqualified until executed.

The second native build `exact-cargo-laws-D1eOZY/00` exited 101 on a missing `validate_mutation_leaf_descriptor_roster_uniqueness` re-export: the current derive macro calls the existing replication function through the OS facade, but two facade export lists omitted it. Added that existing function to both explicit lists and restarted. No implementation or compatibility shim was introduced.

The neutral contract now also covers five exact socket invalidation bindings and replay preservation. The source gate passed 99 checks. Native gate cohorts now run the three authority laws with all database backends but without native artifact execution, followed by the existing full-feature receipt cohort; full integration coverage is retained. The earlier all-feature `IaKJO2/00` build was deliberately cancelled (its verified Cargo process group 9660 only) to apply this test partition, not counted as a pass or a behavioral failure. The new cohort preserves the real REST server, durable SQLite directory, and production socket ledger.

The exact-law runner forbids duplicate package/target entries within one invocation, so the two feature cohorts are separate sequential invocations of that runner, not duplicate groups or a weakened gate. The first live capture is `exact-cargo-laws-amxSN3/00`. No production directory-fence changes have been applied before the intended behavior test.

## Remaining Authority Integration

1. Public command: sorted exact user/session/space/member guards, reauthenticate and reauthorize before any durable claim, retain through append/completion/invalidation.
2. Socket records and document-open plans: add the space binding, invalidate only persisted affected bindings; idempotent replay must not retire a fresh admission.
3. Administrator mutations and invite redemption: the same space ordering, live exact principal revalidation, and verified invite scope from every backend. The current bare invite service call is not qualified.
4. Global directory frames: acquire the message's space together with record bindings, retain through visibility and bounded send.
5. GIS approval: the runtime executor is adding an opaque retained ingress-authority port. The private Hub implementation must carry exact principal/scope evidence and ordered guards; dropping an HTTP future cannot discard an admitted journal owner. Pre-witness abort and post-witness recovery require distinct completion paths. A lexical HTTP guard alone is not sufficient.

The broader goal remains active. The controlled browser patch-handoff tests and source-only GIS publisher tests do not establish the real authenticated GIS-to-Shell-and-peer end-to-end journey.

The DB-only cohort `amxSN3/00` reached current plugin code but failed before Hub laws: two callers awaited the synchronous manifest `get_command` lookup. Removed those two obsolete awaits in `admit_command_wire_with_proof` and `admit_command_json_with_proof`, preserving the surrounding asynchronous admission path, and restarted. The renderer and facade blockers no longer appeared in this compile. Native authority behavior is still unqualified.

`Lua2b5/00` reached the Hub binary and identified 18 compile errors. Root fixed the three new fixture includes (the Hub package is two levels below Hub), restored structural `PartialOrd`/`Ord` on `DocumentScope` for existing ordered socket keys, and added owned startup-error variants for `DatabaseOpenAtRejected`/`DbStorageOpenRejected`. These variants preserve the rejected backend owner rather than formatting it away; explicit retry-close/shutdown at the top-level startup boundary remains an integration obligation. The frontend executor is fixing missing existing directory-schema facade exports; the GIS executor is fixing publisher visibility/clock/result-type errors. No native race result is claimed.

`k06dBr/00` additionally identified the correct retained database error path (`db::engine::DatabaseOpenAtRejected`), now corrected. The missing explicit schema facade exports and publisher clock/result visibility repairs landed concurrently. The next DB-only build `7YHxPf/00` has one remaining compiler error: Axum cannot accept the `post_checkpoint_publication` future as a Handler. The publisher executor and read-only auditor are diagnosing the future's Send boundary. The root native cache is idle after this terminal failure; no authority law has executed yet.

The latest source gate passed 100 checks. The neutral corpus now fixes the exact four ordering cases and five binding cases, and requires 65 distinct subjects in one space to be admitted while the existing per-subject limit remains 64 and the whole ledger remains bounded at 4096. Adding a space invalidation index must not silently cap a whole collaborative room at 64 pending participants. The production fence patch remains unapplied pending the behavioral baseline.

Added two neutral invite-scope cases and a fourth real-SQLite REST law. A caller who can administer space A must not revoke an invite belonging to space B by combining A's space ID with B's invite ID. The law requires an empty 404 response and untouched foreign invite, followed by a successful 202 revocation by B's actual owner. The source gate passed 102 checks; the corresponding backend change is prepared but unapplied until the behavioral RED.

`Znbtxy/00` compiled the added test and used the publisher executor's explicit `Send` assertion to localize the remaining Handler error: the trusted-catalog `apply_operation` path awaits a boxed VCS future that lacks native `Send`. This is narrower than a general async-trait port problem. The executor owns that exact portability repair. The run was terminal before law discovery, not an authority test result.

## Exact Invite Scope: Query-Level RED → GREEN

The registered source gate now executes the production SQLite revocation SQL and its actual bound-parameter order through Bun's independent SQLite driver against the neutral cases. Before the backend repair, this failed exactly at `production SQLite invite scope: foreign-invite`: the foreign-space row was changed. No source-pattern-only success was substituted for that behavioral failure.

The internal `HubDirectory::revoke_invite_as` / `revoke_invite` contract now requires `space_id`, with every dispatcher, decider, caller, and existing native test updated. SQLite and PostgreSQL bind the scope in both the atomic UPDATE and accepted-state lookup; Neo4j matches the scope on the node in both operations. This prevents cross-space revocation and avoids disclosing a foreign invite's accepted state through the fallback query. No compatibility overload remains.

The subsequent registered source run passed 104 checks and emitted exact SQLite behavior: `foreign-invite changed=0 revoked=false`, then `owned-invite changed=1 revoked=true`. The real HTTP law remains in the native cohort; PostgreSQL/Neo4j runtime behavior has not yet been exercised for this repair. Narrow diff whitespace checks passed. The earlier 102-check source receipt predates the production-query test.

The publisher's `SIgh87/00` compile exposed a misplaced derive and generic/native Send obligations; `LmEoxs/00` narrowed the remaining error to `ArtifactStore::fold_current(&self)` retaining an owner with Send-only disposer/retirement interfaces. The executor is preserving single-owner interfaces rather than marking them Sync indiscriminately. These are still compile failures, not native authority-law outcomes. Public-directory fence production changes remain unapplied.

## Native Authority Baseline Reached

After the publisher executor isolated native Send-safe apply/outbound paths, `qFpFTU/00` compiled and discovered the real Hub test binary. The exact native REST law failed as intended: `demotion-before-command` returned 202 instead of 403 after the owner durably changed the caller to spectator. The failure completed in 0.09 seconds and is a behavioral RED, unlike the preceding compiler failures.

The ordered public command fence is now applied: sorted User → Session → Space → Membership guards, exact session reauthentication, live command authorization before durable receipt lookup, and guards retained through execute/receipt/invalidation. Scoped socket grants and pending open plans now carry space and membership invalidation bindings; only newly accepted durable events retire the affected authorities, not idempotent replay. Space indexing does not impose the existing per-subject capacity on a whole room. Administrator principal retention, verified invite-redemption scope, and global frame space guards remain subsequent integration work.

Added the explicit `os-hub:directory-command-authority-native-check` target and matching launch seed entry. It runs only the four database-backed authority laws; the full receipt native/process targets still run their all-feature cohorts. This avoids automatically starting a duplicate heavyweight Stdio build while another worker is compiling WASI. Registry generate and check-generated both passed and regenerated the launch entry.

The invite corpus now includes foreign/owned × pending/accepted. The source gate passed 112 checks, including actual SQLite UPDATE and accepted-state fallback query execution against all four rows. Fresh accepted foreign invites remain opaque (404), while the owning-space accepted invite is 409 with no mutation. The expanded native REST test mirrors this corpus but has not yet run past the first failing race law.

The archive audit was corrected: Archive is intentionally spectator-readable, and normal ArchiveSpace demotes every author. Deleted-space projections remove memberships/shares. Blanket denial of fresh archive grants would break the contract and is not being added. The actual remaining archive bug is a pending Author invite that can be redeemed after archive and recreate author membership; the auditor is defining the transaction-level repair and replay laws.

## Repeated Native Authority Receipt — 2026-09-07

The registered source gate passed 114 checks, including an independent SQLite execution of the exact production invite-revocation predicates and static PostgreSQL/Neo4j scope-and-binding assertions. PostgreSQL/Neo4j service runtime is not established by those assertions.

The four-law native authority cohort passed twice: capture `🗑️generated/directory-command-authority-native/exact-cargo-laws-a3SjWT/00` and repeat `exact-cargo-laws-W0deH7/00`. The repeat compiled current source after the UI converter dependency repair and used executable `os_hub-a39cffcd7b894986`, SHA-256 `1034cba2a51aa2ff1b8536f39acafa3f8c7983b7c9c6973cd1c3250cb2272caa`. Both captures cover pre-fence demotion/removal/session revocation, command-before-demotion ordering, exact affected-scope invalidation with replay/capacity protection, and four actual HTTP invite-revocation scope cases.

An earlier `9hMnsM/00` first-law attempt exceeded the 120-second bound; its sample is retained, and the cause is not proven. The later repeat removed the temporary stage diagnostics while retaining a five-second test-state-open bound. `IpxcTw/00` was a compile failure in the concurrent UI converter, repaired before W0deH7. These failures are not counted as passes. The new native launch entry and generated launch registry check also passed.

The broad goal remains incomplete. Next root boundary: invite redemption must derive scope from the verified stored capability, serialize with exact session/space authority, deny a pending Author invite after archive without denying Spectator archive access, and preserve read-only accepted replay without re-promoting membership.

## Invite Redemption: Reproduced Defects and Current Repair

The neutral invitation transaction contract now contains seven stored-space-state cases, four hint/fence/revocation order cases, and a hostile client-space-state row. An independent SQLite CASE oracle agrees with the TypeScript state/order model. The real HTTP state law first failed in `exact-cargo-laws-SKTe0k/00`: an archived pending Author invite returned 200 instead of 401. After the backend-independent state decision and all three transactional kind reads were added, `hBARZM/00` passed the five-law cohort. Its seven state rows explicitly confirmed pending Author denial, pending Spectator acceptance, exact accepted replay preserving Spectator, other-user conflict, pending/accepted deletion denial, and writable Author acceptance.

The second native RED was `PEu5Il/00`: the recipient session was revoked while redemption was paused, but the route returned 200. The repaired route obtains a capability-verified stored scope hint, acquires the sorted User/Session/DirectorySpaceAuthority/Membership union, reauthenticates the exact original session before the writer, and keeps the authority keys through commitment and fresh response revalidation. The transactional decision independently rechecks stored state; the hint is not an authority lease. SQLite, PostgreSQL, and Neo4j each implement the same selector/secret/actor scope lookup and final kind decision.

`DirectoryService::redeem_invite` now preserves the existing typed `InviteRedemptionCommit` result. The service publishes only NewlyCommitted; the HTTP route synchronously invalidates bindings only for that outcome, without a fallible post-commit membership lookup. Accepted replay returns the original event and preserves newly issued socket grants. Existing service tests were updated to inspect the typed event and distinguish one new commitment from one replay under concurrency.

`FyvNzE/00` compiled and passed three current native HTTP laws: all seven state/replay rows, all three revocations after hint, and admitted redemption before archive with explicit ownership of all four gate keys. Its fourth law failed because the test constructed a wrong-secret token with the wrong prefix shape; the test now substitutes only the selector in another valid minted token. That failed fixture is not a capability-validation pass. The subsequent focused run is still being evaluated.

A new registered `os-hub:directory-invite-authority-native-check` and launch entry isolate the four HTTP invite laws plus the three existing SQLite service transaction laws without full native-artifact/stdio features. The full authority cohort retains every earlier law. The generated launch registry was refreshed. PostgreSQL and Neo4j native service runtime remains unqualified: compilation and hostile source/query checks do not substitute for live database execution.

### Database Lifecycle Diagnostic Remains Open

`NcKpki/00` timed out in the five-second test-state-open bound. The first directory command law now owns its server shutdown and runs the existing explicit Database shutdown before the next row. `AIocrc/00` nevertheless completed the demotion/removal rows and timed out opening the third state. This does not establish a directory authorization deadlock. The independent audit identified two concrete lower-level ownership gaps requiring their own tests: cancellation during FsStorage BackendOpen can drop the task without a returned backend retirement owner; Database shutdown and later FsStorage drop do not await backend terminal retirement. Neither is yet proven to cause this particular third-open stall. The five-second diagnostic was not relaxed and the full authority cohort was not silently retried as an acceptance claim.


## Invite Scope Fence and Cross-Writer Archive Qualification

The four focused invitation HTTP laws passed in captures `cAjVel` and `NfWJDK` with executable SHA-256 `60951343765e7a50f03e728d503087a788bbba097eb853a232357ddc8a3c35d0`, then again in `5PeOVP` with SHA-256 `32e5a5c4ac141a5df12a4dda9714fa16ca4e98dc692852c18db991a87e65bb20`. They cover current archive/deletion policy, session/archive/delete invalidation after the capability-bound scope hint, admitted fence ordering, and capability-bound hints. Runtime `[DEBUG]` output was inspected. These are exact native HTTP receipts, not complete end-to-end acceptance.

The DB-only library compile initially failed on unrelated test feature seams (`8cUAQw`, `0WFuO5`). The execution agents removed generic fixtures' full-Stdio/WAL-test dependencies without suppressing their generic tests. Capture `5boyIX` compiled and passed the existing three invitation library laws (concurrent durable redemption/restart/rebuild, atomic projection failure rollback, publication ordering). Its new archive race law failed during fixture setup because the wrong owner was seeded; that is not a reproduced production defect. The fixture now uses two actual SQLite connections to one ticket-scoped database, seeded correctly; the next native run is pending.

The neutral `backendOrders` contract and TypeScript client fold test were added before the production archive repair. The TypeScript fold reproduced the stale Author display after `space.archived`; the repaired Rust and TypeScript folds now demote the roster and any viewer Author role. The registered source gate passed 167 checks, including two actual TypeScript fold results compared with an independent SQLite membership update and immutable-input assertions.

The cross-writer backend repair remains in progress. A process-local command fence cannot protect a decision made by another Hub writer. The intended event invariant is that one archive event demotes all memberships present in the backend transaction, and an Author membership event cannot commit after archive. A rollback-confirmed typed command-append refusal is required before releasing an idempotent receipt; generic backend errors must not release an uncertain request.


## Qualified Atomic Archive, Rebuild, and Receipt Boundary

The production cross-writer defect was reproduced using two independent SQLite connections in `SpPpEC`: an archive decided before an independent Author invite committed left `Some(Author)` instead of `Some(Spectator)`. After the event-semantic repair, the next native run `LMA5C4` reached and reproduced a separate rebuild defect: its pre-replay checkpoint allowed another connection to acquire the SQLite writer (`false` instead of required writer exclusion `true`). Both are real assertion failures, separate from the earlier fixture setup failure.

The registered `os-hub:directory-invite-authority-native-check` now passed all nine exact native laws, with 176 source/neutral/hostile checks:

- Library capture `QvXpEH/00`, executable `semio_hub-79c85be88bd82b07`, SHA-256 `e08314e2b7be9c39cbd59907f9310301f69983d04267bb1e51a3874888d6d1eb`: five laws passed. The two-connection race covers both archive-after-invite and stale Author-upsert-after-archive, exact idempotent retry, wrong-digest refusal, completed-receipt retention, pure Rust fold, rebuild, and third-party SQLite writer exclusion. The lost-commit-ack law makes a real durable rename, injects the acknowledgement failure, and proves the Pending claim prevents repeated mutation and publication. The three existing invite concurrency/rollback/publication laws also pass.
- HTTP capture `oUuj5c/00`, executable `os_hub-a39cffcd7b894986`, SHA-256 `72ae4acdca5635d26ce7c112e8648a854552775a6759f0e502f7ea4a42f15e98`: all four invitation policy/authority laws passed.

The exact `[DEBUG]` stdout for all new native cases and all HTTP cases was inspected. The current source gate also runs real TypeScript folds against an independent SQLite membership oracle. Scoped `git diff --check` passed. Both PostgreSQL and Neo4j implementations compiled in the DB-only feature cohort; no live PostgreSQL or Neo4j runtime acceptance is claimed.

Production changes: `ArchiveSpace` emits one intrinsically demoting event. All backend projections demote every current Author atomically and reject missing-space archive/member projections or an Author membership into an archived space under their writer transaction. A required `append_decided_events` seam distinguishes acknowledged rollback from uncertain error; the ordinary event-append helper delegates one way and never implies retry safety. Idempotent execution releases only the rollback-confirmed refusal. Exact receipt release includes actor, request, digest, Pending state, and requires one affected row. Special invite projection failures explicitly roll back. Rebuild acquires the SQLite immediate writer / PostgreSQL directory-head row / Neo4j directory counter before counting or truncating, and invalid replayed projection authority is reported as backend corruption. Rust and TypeScript client folds demote the roster and viewer Author role on the archive event itself.

Qualification caveats: `sc3qV7` hit a transient concurrently edited mathematical manifest; a fresh metadata read by the execution agent verified the current direct canonical UI-scene dependency, with no legacy alias added. `HBp2pW` compiled the repair but hit the existing five-second test-state open deadline before its first policy case; no timeout was weakened. The subsequent nine-law run passed. This does not prove that the separate DB open/retirement lifecycle issue is solved. The uncertain-commit test proves safety against duplicate execution, not eventual reconciliation of every Pending directory-command receipt.

Touched archive/receipt slice: Hub directory root and all three backend Rust files; Hub bin test callsite; Hub package `📜️script.ts`; invite transaction fixture and schema; OS directory Rust/TypeScript reducers and their Rust/TypeScript/JSON event contract descriptions. The earlier ticket also contains invitation ingress/authority, project/launch registration, and trusted-catalog feature-cohort changes documented above. Ticket and broad goal remain open; repository ticket MCP tools are still not available in the active tool catalog. Full authenticated native GIS-to-shared-Map/Shell/peer acceptance remains outstanding.


## Resource Pressure and Scoped Cache Retirement

The follow-on older public-command authority regression run `EpR2kV/00` failed during Cargo build with `No space left on device`; it did not execute a test and does not supersede the preceding nine-law success. Native builds were held after the filesystem reached roughly 117 MiB free. The cleanup skill was inspected, but its broad workspace command would remove other agents' active ticket artifacts, so that command was not used. The scoped fallback removed only this task's `🗑️generated/reactor-lifecycle-native-target/debug/incremental` (about 10 GiB), after `ps` found no target owner and `lsof +D` found no open files. The tool rejected a force-style removal; the supported non-force recursive removal completed successfully. This removed rebuildable compiler cache only, not source, executables, test receipts, or reports; the cache can be regenerated, not restored in place. A subsequent filesystem read reported 67 GiB free, likely including concurrent cleanup by other owners; only the approximately 10 GiB root cache removal is attributed here. No other generated ticket tree was deleted.

Next root frontier: the audited global directory WebSocket requires a transient per-message record-plus-space/membership authority union, retained through reauthentication, visibility, and bounded send. It must not permanently membership-index the global socket: revocation in space A must suppress A without closing the socket's access to B. The administrator command principal User/Session fence is being audited in parallel. Current native GIS/approval work remains under the execution agents' reports and is not included in this invitation qualification.

## Global Directory Message Authority: Test-First Boundary

Added the closed language-neutral `semio.hub.directory-message-authority/v1` fixture: membership/session revocation × delivery-before/revocation-before, plus six actual directory wire messages (space event, own-user event, connection, presence, rebootstrap, heartbeat). The registered source target passed AJV validation and four ordering/six wire-scope comparisons against independent SQLite queries. The registered native target uses the real authenticated global WebSocket, actual durable rename events, public membership-removal HTTP, and session revocation; one Hub state owns all four cases. Test-only latches isolate pre-authority and pre-send points. Each revocation must reach its production ingress before the test checks whether the admitted sender blocks it. Teardown closes sockets and explicitly retires the server and Database.

The first DB-only native build is in `🗑️generated/directory-command-authority-native/exact-cargo-laws-JD8ZgL/00`; no native result is claimed yet. The production global sender still has its original record-only fence while this RED is being established. The planned repair computes one transient sorted User/Session/Space/recipient-Membership union before acquisition, reauthenticates and checks membership under that union, and retains it through bounded send. A companion Rust wire-decoding/key-builder law will verify all six fixture messages and unchanged permanent global ledger indices. Scoped directory records must not borrow unrelated message spaces.

Added source/native project targets and ordered launch seed entries. Registry generation initially rejected formatter-added trailing commas in the seed's strict-JSON `devLaunchers` section; formatting with trailing commas disabled restored valid input, and the registered generator then succeeded. No handwritten generated launch edit was used. Root and two execution agents plus one read-only audit agent remain active. The next administrator slice must bind the configured admin principal's exact User/Session—not treat loss of an ordinary space Author role as revocation of configured administrator authority, and not introduce a self-revocation lock cycle.


## Global Directory Socket Authority Qualified

The real WebSocket RED in `JD8ZgL/00` reached and passed membership-before-delivery, then failed delivery-before-membership in 0.11 seconds: removal finished while the admitted outbound sender was paused. The source guard also rejected the original record-only implementation. The subsequent `KktvOY/00` failure was a fixture error, not a production regression: RebootstrapRequired's ArtifactHash wire values are byte arrays, not hex strings. Those three fixture hashes were handcrafted into their actual wire representation.

Production now derives each global message's space, acquires a single sorted transient User/Session/Space/recipient-Membership union, revalidates its exact principal and live ledger under that union, checks current membership, and retains the guards through the bounded send. Rebootstrap control lookup/delivery uses the same scope union. There is no nested record reacquisition. Backend visibility errors are unavailable rather than silently classified as nonmembership. Global ledger indices remain User/Session only; scoped audiences retain only their own scope keys.

`QZSA7l/00` passed both new native laws, and `bMpJs2/00` passed the expanded six-law cohort using executable SHA-256 `3346ef7ba7acdbc251a3f9f6592506953eb271549781361c23ecbe451b94fb79`. The inspected runtime output includes all four ordering rows, real decoding/key derivation for all six wire forms, unchanged global indices and scoped isolation. The added regressions cover scoped ledger invalidation, exact scoped message matching, private membership even in public spaces, and global revocation before replay. Source validation passes AJV plus four independent SQLite ordering cases, six independent wire-scope comparisons, and five hostile-source mutations. Registry generate/check-generated, formatting, and scoped diff checks passed. The previous archive/command source suite also remains GREEN176. PostgreSQL/Neo4j compile is not live service runtime acceptance.

The read-only audit found no remaining source-visible global sender bypass or nested-gate deadlock. This qualifies the tested process-local outbound authority boundary, not every multi-process revocation path or the broad end-to-end goal.

## Administrator Directory Principal: Test-First Boundary

Added `semio.hub.admin-directory-authority/v1` before the production repair. Its five cases distinguish session-before-create, create-before-session, session-before-rename, rename-before-session, and configured administrator authority surviving ordinary space-role demotion. The new registered native law sends real admin HTTP intents, uses production-stage pause/revoke-attempt signals, checks durable event authorship and target projections, and owns one Hub/Database across the matrix. The original execution path is deliberately still present to establish a behavioral RED. The independent SQLite source oracle derives accepted/cancelled outcomes without conflating admin configuration with space Author role. Production must then hold the exact administrator User/Session union with the closed command scope through mutation/invalidation; non-directory self-session revocation must not acquire that wrapper and deadlock itself.


## Administrator Directory Principal Qualified

`KHj0Vc/00` reproduced the production defect over real admin HTTP: after its exact session was revoked, the paused CreateSpace request still returned Succeeded rather than Cancelled (0.08 seconds). The first repair build `QWyclM/00` failed on an incorrect crate qualifier for the constant-time digest helper; that compile error was corrected and is not counted as a runtime result.

The private `acquire_admin_directory_authority` now acquires the complete sorted principal User/Session plus closed command scope/RemoveMember target membership union, then checks configured provider/subject identity and the exact durable session id, user, generation, and expiry under that ownership. Administrator CreateSpace also holds its server-derived target Space key. Ordinary space-role demotion is intentionally not administrator revocation. The helper applies only to CreateSpace and the eight mapped directory intents; they retain its guards through actual DirectoryService append and authority invalidation, with no nested scope reacquisition. The earlier unfenced system-command test helper is now test-only.

The registered native matrix passed in `KeP3lW/00`, executable SHA-256 `5e0c9cebcd0176638e1aa0fda242ff7e0a7ce4992cb9b9c335c778a63716d6a0`. Inspected runtime output: session-before-create cancelled with zero events; create-before-session succeeded with two events; session-before-rename cancelled with zero events; rename-before-session succeeded with one event; configured administrator after ordinary role demotion succeeded with one event. An additional actual HTTP self-user-revocation completed, revoked the caller's session, and did not deadlock on a nested User guard. The AJV/independent SQLite source matrix passed all five rows. Project source/native targets, ordered launch seed entries, registry generation/check-generated, Rust formatting, Prettier, and scoped diff checks passed.

The six-law global directory socket cohort then passed again on that same executable in `62h6TB/00`; all new runtime diagnostics and all six test summaries were inspected. The 176-check command/archive/invitation source suite remains green.

### Regression and Remaining Scope

The broader eight-law public-directory authority run `9JAbu2/00` compiled and passed its first four exact tests, including pre-fence revocations, admitted-command ordering, exact affected-scope invalidation, and invite-revocation ownership. Its fifth law failed before any invitation policy row because `test_state()` exceeded the existing five-second database-open bound. The remaining three laws did not run. This is a partially completed regression cohort, not a full green run, and the deadline was not weakened or blindly retried. The independent DB open/cancellation/terminal-retirement issue remains open.

Read-only audit confirms the new mapped-admin scope and flags the next real frontier: IssueDocumentShare, RevokeDocumentShare, RevokeUserSessions, KickConnection, and RebuildDirectoryProjections still rely on a pre-execution principal reread. They need an intent-wide sorted union/retained-operation design; long rebuild cancellation must not simply hold a Session key for ten seconds and make revocation time out. Those branches are not claimed repaired by this matrix. The auditor is defining their exact race cases and self-target ordering.

Current root slice files: Hub `🚀️bin.rs`, package `📜️script.ts` and `📋️project.json`, both new neutral fixture/schema directories (`🌐️directory-message-authority-v1`, `🏛️admin-directory-authority-v1`), launch seed and generated launch file, and this retained report. The ticket and broad goal remain active. Native genuine GIS cold-open/Map patch and retained approval/recovery qualification continue on the two execution agents; no full Shell/peer end-to-end success is claimed.

## Short Administrator Effects: Physical Test-First Boundary

Expanded the same closed administrator fixture with eight short-action rows: IssueDocumentShare, RevokeDocumentShare, RevokeUserSessions, and KickConnection, each with session revocation before admission and after admission. The registered source gate passes AJV validation, five existing directory-order rows, and eight independent SQLite short-action outcome/key-count/one-shot-secret comparisons. The new native law uses actual HTTP administrator intents and production revocation ingress signals, reads the same physical SQLite directory through an independent rusqlite connection, and checks share/session effects and correlated authentication audits. The kick row observes the production Notify signal; it does not claim a full physical WebSocket closure. One server/Database is explicitly retired after the entire matrix.

Native attempt `q3QEkm/00` failed before compilation because the concurrently added GIS plugin-host manifest dependency pointed to a nonexistent products directory. The GIS execution owner is correcting its path. This capture is not behavioral RED and no administrator production repair has yet been applied for these new rows. The intended repair plans a single sorted/deduplicated principal-plus-intent union before any acquisition, retaining it through the actual effect and grant invalidation without nested Share/User gates. Rebuild remains an explicitly separate long-running checkpoint/cancellation design, not part of this short-action claim.

The GIS dependency path was corrected and workspace manifest resolution passed. Native retry `mytqSx/00` rebuilt the missing compiler cache and reached Hub compilation, then failed on the new test's missing `ShareCapability` qualifier. The test now uses the existing `semio_hub::directory::ShareCapability`; no production behavior changed. Native execution is being retried from that concrete compiler correction. Subsequent commands use the repository Nx wrapper, which explicitly prevents the known non-BMP plugin-worker IPC path corruption; no project namespace workaround was made.

`uCk5Ml/00` compiled and discovered the exact native law, then failed in its physical-directory fixture setup (0.01 seconds): the existing tempdir helper returns a path but does not create it. The law now explicitly creates that ticket-owned parent before connecting SQLite. This setup correction is distinct from both the production authority race and the older Database opening deadline; no authorization assertion had run yet.

`nxRsyO/00` reached actual HTTP execution and reproduced the authority race in 0.29 seconds: `issue-share-transition-first` returned Succeeded after the exact administrator session was revoked, where the fixture requires Cancelled. This is the behavioral RED, separate from the preceding compile and setup errors.

The production repair is now applied. `admin_intent_bindings` exhaustively derives and sorts/deduplicates the principal User/Session union with each short intent's exact target: deterministic created Space, mapped Space and RemoveMember target membership, share scope Space and optional Share, target User for batch revocation, or principal only for kick. Rebuild is explicitly separate. `execute_admin_intent` acquires this union once and reauthenticates the configured principal under it before any short dispatch. The old per-branch Share/User reacquisitions and early releases are removed. The native test additionally probes every derived guard while each action is paused at its actual effect boundary, and checks the neutral exact key count. The existing real self-user-revocation regression remains registered. GREEN qualification is pending.

## Short Administrator Authority Qualified

`0esTMv/00` passed both registered native laws with executable SHA-256 `961b1ec3a6c48eccadfee304e3579fa13d02063bd1d2f8e1536951f7f39fa0e3`. The inspected runtime diagnostics cover all eight new short-action rows: revocation-first cancels with zero physical effect/auth audit/secret; action-first retains every exact union key until its share issue, share revoke, user revoke, or actual kick signal. Share issuance returns a capability that authenticates against the real stored scope. The five prior mapped directory cases and real self-user-revocation also pass without a nested User deadlock. The source target independently passes AJV=1, directory ordering=5, and short actions=8 after the production edit.

This qualifies the tested process-local short-intent authority boundary. It does not qualify long projection rebuild revocation/checkpointing, independent-backend-writer share scope atomicity, cancelled HTTP requests racing an uncertain backend commit, or a full physical kicked WebSocket close. Those remaining boundaries are not implied by the current matrix.

The audit's remaining exact-union coverage item is now included: fifteen actual encoded administrator intents cover every one of the fourteen closed variants plus self-user-revocation. The neutral fixture pins complete sorted key vectors, not just counts; an independent SQLite UNION/ORDER BY oracle and Node SHA-256 verify deduplication, exact removal membership/share scope, deterministic CreateSpace key, and explicit rebuild None. The new pure native wire-decoding/planner law is registered before the two physical administrator laws. Source validation passes all fifteen rows; native qualification is now rebuilding against the newly qualified FS/SQLite opening constructors.

### Current Constructor Regression Result

The expanded administrator cohort in `pf3kjS/00` compiled with executable SHA-256 `1c2f87221a8aec928879bdfa5559d30d50dda118138704290aacd202777dea49`. Its exact fifteen-intent binding law passed (0.00 seconds), and its eight real short-effect cases passed (0.66 seconds); every corresponding runtime diagnostic was inspected. The third, previously qualified mapped-command law failed at `test_state()` before its authority assertions: `admin authority state open deadline: Elapsed(())`, 5.00 seconds. Thus the latest three-law administrator cohort is partial, not GREEN3. No deadline was weakened and no unchanged mapped test was blindly retried.

The separate planned global directory regression then passed all six laws in `de0FhB/00` on that same executable. Inspected diagnostics again show space-A membership removal preserving B, and exact session revocation producing close 4401. These successful socket tests do not erase the administrator opening failure. The root released the warmed shared native target to the queued GIS runtime qualification immediately afterward.

The FS/SQLite registered-opening leak is independently qualified by GREEN40; it did not eliminate the intermittent initial Hub state opening failure. Root and the read-only audit lane are tracing that failure by concrete opening stage and wake ownership. The execution lane now owns transaction-local share-scope validation across backend writers. The long administrator rebuild audit found no existing reusable candidate generation and calls for bounded event-head plus independent CAS-head catch-up before a short revocation-aware publication; it remains unimplemented.
