# Flow AddWidget Retained Factory

## Implemented Boundary

Flow now owns a dedicated one-tool `FlowChildGroupJobFactory` for `addWidget`. The factory uses the framework's existing retained command payload/job, declares only the `Child` publication lane, and retains the issued instance-owner handle until its single synchronous work turn. That work revalidates the captured `content` child as the exact Flow member dialect, bounds text and child size, rejects non-finite coordinates, borrows the sole `FlowEvalSession`, and calls the existing typed `add_widget::handle` planner. It accepts only one matching `ChildEmit` and rejects any parent/config/draft/effect/event/task output.

The registered factory, proof table, job builder, manifest classification and legacy-handler denial are one closed path. `addWidget` is `Migrated`; the legacy foreground path cannot allocate a second session or bypass retained publication. Raw input, checkpoint, cancellation, completion, child-first close and result acknowledgement remain owned by `ArtifactRetainedCommandJob` and the mounted generic publisher.

## Schema-First Evidence

The neutral `flow-add-widget-retained-child-v1` schema and fixture live beside the Flow editor at `🧪️fixtures/🧵️add-widget-retained`. They pin the exact registration, execution contract, `Child`-only publication, one stable `content` coordinate, the exact inserted kind/position, unchanged parent content, the canonical 512-byte checkpoint bound, and explicit English and German progress text bound to the production retained-job preview.

The independent Bun/AJV model accepts the note and slider cases and rejects 24 hostile changes: wrong controller/tool/schema, raw/checkpoint overflow, malformed or trailing wire, overlong kind/neuron text, unknown descriptor, non-finite coordinates, missing/wrong child slot/id/dialect, stale parent/root/child authority, duplicate target, busy/closing owner, and cancellation before work or after planning. Preflight denials have zero session calls; the unknown descriptor is rejected inside the existing typed planner under one retained session borrow. Every denial has zero visibility.

TDD source session **95c961** first completed the neutral model and failed on the absent `FLOW_CHILD_GROUP_TOOL_IDS`. Final registered Nx source session **0ccc24** is GREEN, exit 0: two accepted plus 24 hostile cases, with `runtimeClaims=0`. The intermediate post-tightening RED **72d7f6** caught an incorrectly escaped source witness for the bilingual raw preview before that assertion was repaired.

## Registration

`@semio-tech/flow-plugin:add-widget-retained-check` is registered through the Flow `📜️script.ts` router and `project.json`. Launch seed orders 408.256 and 408.257 provide separate source and native entries; the native entry pins explicit ticket-owned result and Cargo target roots, one Cargo job and disabled wrappers. Canonical plugin-registry generation session **6a3be5** is GREEN; final freshness session **05e527** is GREEN with the complete concurrent seed/launch bytes preserved.

The native selector requires exact-one discovery and execution of:

- `retained_add_widget_factory_is_exact_child_only_and_legacy_closed`
- `retained_add_widget_dispatches_one_acknowledged_child_group_and_retires`

The second law uses a real registered Flow app, existing `content` child and retained operation lifecycle through worker, child publication, result acknowledgement, terminal acknowledgement and bounded app close. It asserts one child mutation, unchanged parent content and no remaining operation.

Initial isolated native session **20748** retained receipt `flow-add-widget-retained-sol-run/exact-cargo-laws-ikkiSD/00` and ended at build stage with Cargo status 101 before discovery or execution. Its sole terminal diagnostic was external to the Flow packet: Stdio aggregate line 2128 mounted absent LAS header schema `🗿️artifacts/☁️las/🏅️standards/🔖️1.0/🪆️subsets/✳️header/🧬️schema/🦀️.rs`.

After the checkpoint/inserted-node/progress source tightening, warmed registered session **13448** retained `flow-add-widget-retained-sol-run/exact-cargo-laws-yqXEWt/00`. Its source oracle passed, then Cargo exited 101 before discovery on the then-current external Stdio aggregate line 6068: absent Semio v1 base IO `🗿️artifacts/🧿️semio/🏅️standards/🔖️v1/🪆️subsets/✳️base/🚪️io/🦀️.rs`.

Final-current-source session **95845** retained `flow-add-widget-retained-sol-run/exact-cargo-laws-eDmuyC/00`. Its two-accepted/24-hostile oracle passed, Stdio then compiled for approximately ten minutes, and Cargo exited 101 before discovery on two PDF1.4 source-authority defects: `ReplacePageText`'s mutation descriptor owner does not exactly match its source owner, with the resulting `MutationLeaf` trait error, and the bachelor-thesis example includes absent `🖼️assets/📄️bachelor-thesis.pdf`. Exactly zero owned laws were selected or executed. These receipts are build-frontier evidence only, not native acceptance, and this lane does not repair the separately owned Stdio taxonomy/assets.

## Acceptance Boundary

This packet covers one retained `addWidget` factory against one pre-existing, testkit-registered `content` child. It does not prove public `LoadChildren`, browser installation, trusted catalog/bootstrap, WGPU mounting, child genesis, restart/reload, global composition history, or atomic parent/member/root exposure. The separate public request-owned Flow opener remains source-qualified only: cold leased receipt `exact-cargo-laws-54y8Pd/00` timed out without diagnostics or discovery, and warmed receipt `exact-cargo-laws-iK79bZ/00` stopped before discovery on the separately owned missing Stdio document mutation barrel `🏷set-style-name/🦀️.rs`. None of those results are reused as AddWidget acceptance.
