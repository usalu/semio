# Hub Admin Removal, Presence and Target Recovery

## Qualification

In progress, 2026-09-05. The dedicated source gate is green (13530 exit 0) after the original expected-red missing-journey gate (99500), initial implementation green (15559), and the read-only audit's fixture/observation corrections. The native journey has not run. It is queued after the active selected-SQLite target group (90466) and the six presence normalization laws, sharing their owned cache without overlapping Cargo builds.

## Composed Native Journey

The schema-first fixture uses the actual seeded `default` space. The independent AJV oracle validates exact membership-removal expectations and compares the fixture scope to the native seed constant. The law exercises two real plan-bound document sockets, server-derived normalized identity, the protected selected-target manifest, and the actual HTTP administrator removal route. It requires immediate 4401 without any intervening authority-bearing binary/text frame, an empty observer roster, failure of both consumed and previously unused grants, and strict typed JSON authorization errors for every target route and new open-plan issue. Observer C remains live and retains its own role/surface/identity.

The test server supports graceful shutdown. Before reconnecting the exact SQLite directory path, the law drops both clients, joins the first server, waits for zero directory owners and sole database ownership, and shuts down the filesystem artifact database. The new Hub starts with empty ephemeral presence/grant/plan ledgers. Both original sessions must still authenticate: B has no membership, C remains an author. B is denied again, while C receives exact selected component/descriptor bytes and valid typed manifest/plan bodies. This qualifies the SQLite directory authority and filesystem artifact database composition only if the native law passes; it is not an all-stores-SQLite claim.

## Files and Registration

- Hub Rust `🚀️bin.rs`: shared test-state constructor, restartable server/retirement helpers, strict denial assertion and the composed law.
- Hub Rust `🧪️fixtures/🛂️admin-presence-target-recovery-v1`: neutral fixture and strict schema.
- Hub Rust `📜️script.ts` and `📋️project.json`: dedicated source/native targets, selected `semio-hub --bin os-hub --features sqlite`, one exact law and 120-second law budget.
- `.vscode/🧩️launch.seed.jsonc`: launch entries 411.073/.074; normal registry generation/check is delegated to the Home lane.

The Home lane completed normal registry generation and immediate freshness verification after the new entries: both exit 0, 59 plugin crates, 60 playgrounds, 45 framework packages. The generated launch contains 411.073/.074. No compiler or runtime pass is inferred from the source gate. The original audit and implementation review are retained in `📓️terra-hub-admin-removal-presence-target-sqlite-recovery-packet.md`.
# Current Native Queue Update

On 2026-09-05 the root cancelled the superseded `execution-target-native/exact-cargo-laws-7VfbyW/00` producer before any Hub executable or selected-law receipt. The exact verified process group was Cargo 97490, sccache child 8837, rustc child 8838, all bound to the root-owned `space-public-boundary-sol-target`; only that process group received graceful SIGTERM. This is cancelled/unqualified, not a Hub law failure or pass. It had spent almost four hours compiling the previous full Stdio source/feature boundary, superseded by the Home import feature split. The target cache is root-owned and idle after cancellation. A read-only Hub dependency-boundary audit is active before the current-source retry; presence6, composed admin recovery1, and exact GisMap preview lib acceptance remain queued.
