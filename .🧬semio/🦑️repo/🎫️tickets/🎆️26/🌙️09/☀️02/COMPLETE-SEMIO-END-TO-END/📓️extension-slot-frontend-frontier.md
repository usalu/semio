# Extension Slot Frontend Frontier

## 2026-09-09 Corrected Production Reachability

The generic resolver is called by Shell refresh, but Terra's repository-wide
source-only author audit found no current app/plugin that authors an Extension
component. Concrete extension nodes are contract/codec/test fixtures. Therefore
this is a dormant unfinished capability, not a demonstrated blocker of the
current two-author Map journey. The earlier wording below about a live blocking
frontend path must not be read as proof of a mounted production extension node.

The generic UI contract deliberately keeps extension addresses opaque. A future
declared OS resolver could use source-identified topic contributions plus loaded
app metadata; no current topic declares that target. Do not replace the generic
contract with plugin/app/body fields or revive the removed rendering API based
on the placeholder alone. Current execution priority is the concrete authored
administration-property gap and exact operation-space worker admission.

## 2026-09-09 11:30 UTC Source Evidence

The current Kernel `resolveExternalSlots` implementation in
`🧰️framework/🔨️modules/🎠️kernel/🟦️.ts` creates or reuses a contributor instance,
but then unconditionally replaces every extension component with an unavailable
text node, even when the plugin and app exist. Its comment explicitly leaves
binary-channel rendering as future work.

ShellHost currently calls this resolver for changed window/panel refresh bodies.
Its slot context fabricates unused empty manifest/turn/output methods to bridge
two different handle abstractions. The renderer engine-contract test deliberately
asserts the unavailable result until the rendering path is implemented. This is
an unfinished reachable source path, not evidence of successful extension UI.

Terra is auditing the current typed actor/surface, action ownership and opaque
extension address contracts before implementation. The correction must remain
domain-neutral, preserve contributor identity and cancellation, and must not
reintroduce the removed per-verb render API or perpetuate the dummy handle
adapter. A real mounted runtime proof remains required after source/test work.

## Acceptance Constraints For The Implementation Audit

- Resolve the current opaque extension address through declared authority; do not
  assume the old plugin/app string split is the current contract.
- Preserve originating and contributing actor/session/surface ownership for
  rendering and actions, including two instances of the same contributor.
- Carry exact component props, locale and terminology through the current typed
  render request without a default language or raw-language fallback.
- Retire cancelled/replaced work and release acquired contributor instances once;
  an old response must not publish into a successor surface.
- Bound recursive work and detect cycles, with progress and cancellation for
  expensive resolutions.
- Replace the unconditional-unavailable success test with schema-backed neutral
  success/failure/retirement cases, independent outcome validation and actual
  mounted contributor rendering/action evidence.

These are acceptance constraints, not a selected API or a claim that current
native actor contracts already support them. The audit must establish the real
contract before implementation.
