# Selected MemberFactory Identity

## Current Boundary

The mounted `store/composition/open/history/factory` leaf selects exclusively from a required `MemberFactory::OPEN_DECLARATIONS` static table. It validates the entire bounded table, rejects duplicate full dialect triples, retains the original verified paged input on denial, and carries the selected declaration through the private dictionary owner and final semantic witness. Only the selected declaration supplies the persisted schema; there is no caller schema or table parameter.

The real Semio Flow row is `s.stdio.semio@v1/flow → stdio.semio`. The existing dictionary mechanism fixture's synthetic `stdio.semio.flow` is not factory authority. All 18 neutral rows are joined exactly to the current owning `SemioMembers` macro declaration.

## Evidence

- Registered source gate session **73433**: RED, exit 1. The independent Bun/AJV model completed the exact 18 source-owned declarations, 16 selection cases and seven retained lifecycle traces at grants 1/7/4096; the intended terminal was the then-absent Rust owner leaf. The leaf was staged afterward. No Rust or native acceptance follows from this run.
- Previous dictionary packet **70042** remains GREEN for exactly two native owner laws, separately recorded in `📓️sol-retained-history-dictionary-owner-protocol.md`. It does not establish this selected-factory boundary.
- Registered source **61723**: RED, exit 1, after the independent 18-declaration/16-selection/seven-lifecycle/nine-persisted-identity oracle completed and both staged native test bindings were present. The exact terminal is `selected identity requires the coordinated MemberFactory declaration API; staged source is not mounted authority` at the host script's `OPEN_DECLARATIONS` preflight. No Cargo ran. The nine semantic rows use literal input/close totals and the staged Rust law calls production `encode_history` and the actual selected-schema dictionary owner; neither is runtime-proven yet.
- Parse-only `rustfmt --emit stdout --edition 2021 <factory leaf>` exited 0 (`991c1f`); output discarded, no edits. This checks syntax, not Rust type checking.
- Expanded registered source **45479**: RED, exit 1 at the same intentionally absent trait API. Before that terminal, the independent oracle completed **18 owning Semio declarations, 21 selection cases, seven selection and five semantic lifecycle traces, nine persisted identity histories**, with literal retirement totals and grants 1/7/4096. The source binding now also checks the two existing `RetainedTestMembers` macro rows; the staged native selection law uses that actual generated type, not only manual fixture factories. A later source reread corrected the unmounted test import depth; runtime remains unrun.
- Launch generation **33233 GREEN exit 0**, freshness **12986 GREEN exit 0** through the canonical `@semio-tech/plugin-registry:generate` / `check-generated` targets. Both new entries and root's document-open-plan source oracle are present. Root was notified that order 411.1095 is also used by the existing browser-open entry; no unrelated ordering was changed.
- Final registered source checkpoint **35663**: RED, exit 1 at the same absent `OPEN_DECLARATIONS` API after all expanded oracle/binding checks above. Parse-only current Rust **651f67 GREEN exit 0**; scoped diff hygiene **26308 GREEN exit 0**. Neither is compilation or native acceptance.
- Coordinated source mount: `MemberFactory::OPEN_DECLARATIONS` is now required, `space_members!` emits it from the exact same four creation/open literals, `NoMembers` owns the exact empty table, all three direct kernel fixture factories own the exact `s.stdio.mesh@1/* → demo/v1` row, the private factory leaf is mounted/reexported, and the raw schema-taking dictionary entry is narrowed to history descendants with `pub(super)`. Registered source mode is GREEN on these mounted bytes.
- Registered native session **24681**: RED, exit 1 after successful compilation and exact-law discovery. The second law `member_factory_selection_retains_input_through_denial_and_handoff` reached its production `encode_history` positive row and found `Malformed` instead of success. This is not selected-factory acceptance. Artifact: `member-factory-identity-exact/exact-cargo-laws-9qe2sV/00`. Diagnosis is a pre-existing wire split: production writes `REC_COMPOSITION` noncritical while the retained dictionary owner and its older neutral writer required it critical. The correction and same exact-two rerun are pending the shared target's current root-owned build.
- Canonical flag repair: the retained profile now requires exact critical flags for `REC_DOC`/`REC_STR_DICT` and exact noncritical flags for `REC_COMPOSITION`, matching the production writer and its declared additive overlay. Three CRC-valid neutral hostiles cover critical composition plus noncritical document/dictionary frames; the owner does not accept either spelling.
- Final registered native **68325 GREEN**, exit 0: both exact selected-factory laws passed through one hash-bound kernel binary. Receipt `member-factory-identity-exact/exact-cargo-laws-rzrlCE/00/receipt.json`, executable SHA-256 `1c09a11c36663be9d06f8f806ff36aa19075e4f3beea4b91d84d743be38b1fb4`, assertions 2. The second law traverses the real `encode_history` positive and eight hostile persisted-identity histories through retained verification, selected static schema, dictionary owner, private witness and exact close.
- Follow-on registered dictionary **8902 GREEN**, exit 0, proves the repaired owner and all 36 CRC-valid production-writer histories, including the three flag hostiles, through two exact laws on the same binary/hash. Receipt `member-history-dictionary-exact/exact-cargo-laws-X2Egwr/00/receipt.json`, assertions 2.
- Final canonical launch generation/freshness session **75194 GREEN**, exit 0. Generated `.vscode/launch.json` contains the selected-factory source/native entries and public-handoff source entry exactly once, while preserving root runtime orders 408.28/408.29 and dialect-oracle orders 411.1091/411.1095. Scoped `git diff --check` is clean.

Command: `bun ./📜️script.ts nx run @semio-tech/framework-os-host-rs:member-factory-identity-check --skip-nx-cache -- --oracle-only`.

## Mounted Batch And Pending Runtime Repair

The declaration batch is mounted exactly as scoped and is native-green for the two bounded selection/retained-handoff laws. No create/open implementation or public operation was added. The repaired lower dictionary owner is independently native-green for its two exact laws.

Exact native laws are being bound to the retained production input and selected-schema dictionary path. The neutral semantic corpus must deny foreign persisted schema/document/dialect/owner before any typed decoder invocation.

Both exact laws are now staged in `factory/🧪️tests/🦀️.rs`: `member_factory_selection_uses_only_complete_closed_declarations` and `member_factory_selection_retains_input_through_denial_and_handoff`. The latter includes production HistoryLog → retained verification → selected static schema → dictionary owner → private witness, with nine success/denial histories and five semantic admission/last-handoff denial traces at all three grants. Factory create/open counters stay zero by design; no typed decoder API is present in this boundary. Launch source/native entries are registered at 419.9991/419.9992 and generated/fresh as above.

The exact macro-authority runtime check uses the existing two-arm `RetainedTestMembers` expansion. The 18 Semio rows are independently source-joined and exercised by a static kernel test factory; this is not native execution of all 18 Stdio stores.

The coordinated source batch is bounded to the store declaration reexport/required trait constant, its macro emission, `NoMembers` empty table, the existing three direct fixture implementations' one exact `s.stdio.mesh@1/* → demo/v1` row, and the private history factory mount. No creation/open method implementation changes or public retained operation cutover are part of that batch. Source preflight requires the same four macro variables and the exact empty NoMembers table; it cannot accept a manually populated Semio runtime table.

## Nonclaims

No public MemberFactory open cutover, typed snapshot/history hydration, space authorization, pin membership, graph publication, host persistence IO, or end-to-end public child opening is accepted. Static declaration selection is not space authority; its eventual caller must already hold the authorized document/space scope. Historical all-features failures remain unchanged.
