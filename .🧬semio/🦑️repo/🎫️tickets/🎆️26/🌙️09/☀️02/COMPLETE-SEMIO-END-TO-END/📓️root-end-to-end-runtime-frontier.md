# End-to-End Runtime Frontier

## Goal Status

The requested whole-repository goal is active and incomplete. Source declarations, source checks, fixture parity, native library tests, process tests, and user-visible journeys are distinct evidence levels. A pass at one level does not establish the next.

The September 5 audit found 59 catalog components (33 plugins and 26 extensions) and 92 artifact roots. The current trusted native provider boundary covers Stdio and GIS only: 28 codecs and one selected Map editor target. It does not establish runtime coverage of the remaining fleet. See `📓️terra-all-plugins-artifacts-os-coverage-p0.md` for counting method and exact evidence.

## Current Four-Slot Fleet

| Owner | Bounded work | Required exit evidence |
|---|---|---|
| Root coordinator/executor | Map editor profile, frozen verified binding, typed Map proposal integration | Source corpus parity; exact loaded-catalog native acceptance; real startup remains separately required |
| Sol execution: Home process | Repair and run retained Home browser/process harness | Actual hub process plus canonical client/runtime event ACK; source fixture alone is insufficient |
| Sol execution: WGPU Home | Retained Home bootstrap, ACK, cancellation and terminal event handling | Exact native laws, then process/host evidence without stale or duplicate ACK |
| Terra read-only audit | Fleet coverage, then typed Map group API/inverse audit | Current-file findings and executable acceptance boundaries; no runtime source mutation |

Executors use the user-requested Sol Extra High model; the read-only audit uses Terra Extra High. No agent may overwrite unrelated concurrent changes, reset Git state, or introduce an old-path compatibility layer.

## Dependency-Ordered Completion Work

1. Obtain fresh native verdicts for the current shared code. The older Stdio compile failure was traced to paths/descriptor owners already repaired by concurrent work. The first frozen-binding build failed on a directory type that was also subsequently replaced. These are stale diagnoses, not fresh passes. The active cached-target binding rerun selects both projection and actual loaded-catalog laws.
2. Run the existing Stdio+GIS materializer and isolated candidate-hub process gate. Require actual fresh raw/core/descriptor hashes, the exact 26+2 native codec closure, one writable Map editor target, authenticated plan issuance, hostile/stale-plan rejection, and unchanged current pointer after a rejected candidate.
3. Make each host activate the exact component and descriptor retained by the authenticated open plan. Browser module URLs and WGPU directory scanning do not establish this boundary. Require one rendered selected Map surface and one authenticated persisted edit from each host before claiming that host works.
4. Finish the composite two-user space/admin journey. Require independent sessions, administrator membership/invite operations, role-bounded projections, persisted artifacts, live presence, reconnect/cancellation, and restart/recovery. Native primitives alone do not establish the journey.
5. Finish durable group publication before approving an AI proposal. This exact Map proposal requires three participants: parent, drawing child, and value child. A two-member pilot is not sufficient. All member state, histories, frontiers, journal replay, and hub fanout must share one committed visibility boundary; sequential apply/undo is not atomic durable publication. Use `📓️terra-map-durable-group-current-frontier.md` to drive crash/recovery tests.
6. Attach server-owned provider credentials and cancellable durable jobs to the frozen Map binding. Keep untrusted provider output private until the typed parent/drawing/value proposal is validated and explicitly approved against live authorization and the retained document frontier. Require failure/retry/cancellation/restart tests and no duplicate publication.
7. Expand from the proven Stdio+GIS slice to the complete catalog, including extensions and every artifact/surface. Use per-component fresh build/descriptor/host receipts and an explicit coverage matrix; never extrapolate two packages into an all-fleet pass.

## Current Evidence

- `os-hub:trusted-stdio-gis-bundle-check -- --source`: passed after switching the profile to the exact Map editor; 2 packages, 28 codecs, 19 hostile cases, 8 cancellation cases, 4 descriptor pairs, 1 stale plan. Materialization and hub startup are not established by this command.
- `os-hub:gis-map-frozen-binding-source-check`: passed with 52 checks and hostile substitution coverage for every one of the 34 frozen fields.
- `@semio-tech/gis-plugin:map-create-region-group-check`: passed with 26 checks after adding four neutral membership cases and their native corpus consumer. A supplied image is preserved, then rejected by this image-free proposal scope without mutation. The exact native typed group law did not run in its earlier Stdio-failing attempt.
- Frozen-binding native rerun `exact-cargo-laws-dFj7qf/00`: failed before tests on a BMP include path already repaired in the current Stdio source. A further exact hub-library run is queued against the same cached target as the Home process binary build; no native pass.
- Home and WGPU: current executors retain their own reports and exact execution receipts. WGPU's first cold build timed out; its cached build is resumed. Timeout is not a compiler verdict or a native test pass.

## Handoff Constraint

Keep this ticket and the explicit user goal open until the complete requested outcome is actually demonstrated. If repo MCP remains unavailable, do not fabricate ticket-close or goal infrastructure actions. Preserve the source corpus and Markdown reports; remove only generated ticket output when the complete ticket is genuinely finished.
