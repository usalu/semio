# Trusted Codec Source Capture

Root fully read `📓️terra-copied-lease-and-trusted-generation-current-audit.md`. The prior reader mapped unvalidated JSON into profile framing, so an invalid hex digest such as `zz` became an empty byte field and could produce self-consistent invalid catalog output.

## Implemented Boundary

The fixed producer now captures both codec JSON files before component production. Each file has a64KiB bound and the pair shares128KiB admission through `readStableBuildFile`. Fatal UTF-8 decoding and first-party projection enforce exact root/row keys, package identities, source-schema field bounds, nonzero lower-case64-hex hashes,26 Stdio plus2 GIS rows, unique projected kind/schema pairs, and coupled GIS Map/Terrain factory/schema/extension/capability/protocol identities. Source byte owners are erased after projection. Only deeply frozen typed rows and the GIS package version survive; the fresh GIS receipt must match that captured version.

Codecs use a deterministic code-unit comparator for frozen output and generation framing. No locale ordering or later source reopen remains in this producer. The existing expected bootstrap generation remains unchanged.

## Registered Evidence

- Session71181 is the expected missing-capture-helper RED after the neutral corpus was added.
- Session75033 is GREEN15 (`🗑️generated/codec-source-gGnH1M`). It independently validates the source schemas with AJV and compares generation digests with WebCrypto/Node. Accepted row permutations and replacement of both source files after capture retain identical frozen output and generation. Invalid/zero hash, duplicates, unknown fields, foreign package/version, crossed GIS identity, invalid UTF-8, oversize and cancellation are refused.
- The corpus distinguishes the existing GIS schema's permissive zero-hash/crossed-enum cases and Stdio schema's duplicate-row case from the stricter semantic projection. Their AJV result is recorded honestly.
- That run also keeps fresh handoff11, generation-stage12 and the existing bootstrap corpus GREEN. Full materialization and Hub activation were not run.

The source ratchet now requires capture before the real producer call. JSON parsing uses the platform JSON parser followed by exact schema/semantic validation; this is not a claim that duplicate textual JSON keys are rejected. No third-party runtime dependency was added.

Remaining work is the mandatory browser actor source/policy/catalog/plan/lease/body contract, contained Worker activation, and real GIS/public-member process qualification. The Hub's current component loan still derives only an independent digest, not an actor result.
