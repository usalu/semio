# Retained GIS Map Committer

## Boundary

The Hub runtime now has a production-shaped, deliberately unregistered `RetainedGisMapApprovalCommitterV1`. One mounted document state owns the exact DB `ArtifactHandle`, parent Map Store, drawing Store, and value Store. Approval can move those owners only through `Ready -> Assembly -> Journal -> Verification -> Committed`; shutdown moves any phase through `Closing` and returns every Store to its exact owned disposer.

The committer rederives the fixed-three GIS work from the canonical Map base, checks the exact ordered `gismap-drawing` and `gismap-value` members, checks the canonical command and deterministic mutation identity, and uses `DurableOwnedThreeStoreMapAssemblyV1`. Its journal sink comes only from the mounted document `ArtifactHandle::durable_group_journal_sink`. It never calls generic submit and returns no receipt until `InferenceWalVerifierV1` finds the sole one-Event committed WAL transaction.

The GIS preparation factories gained a server-derived stamp containing the approved mutation ID and HLT. The parent WAL verifier now compares the physical decision edit's mutation ID as well as actor, author, payload, inverse, and timestamp before reconstructing the command hash.

## Schema-first evidence

- Initial source oracle: RED, exactly `retained GIS Map committer source is missing RetainedGisMapApprovalCommitterV1`.
- Current source oracle: GREEN, `ajv=1 hostile=7 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=6 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`.
- The existing GIS fixed-three native run `exact-cargo-laws-nYkzCz` has group 00 GREEN4 and group 01 still building the full Stdio dependency at the time of this checkpoint. It predates the Hub committer source and is not evidence for this runtime.

## Nonclaims and remaining integration

Production Hub construction still injects `UnavailableGisMapApprovalCommitterV1`. This is intentional. Before registration, the Hub document-write gate must be shared with this owner; startup must mount committed-decision recovery before new approvals; checkpoint publication must consume the same updated parent Store authority; and process shutdown must demonstrate retained close/retry under injected journal and Store retirement faults. No current receipt therefore claims a live Hub `applied=true`, process restart recovery, user-visible Map update, or undo/redo path.

## 2026-09-07 production bridge update

The earlier nonclaim is no longer the current source shape. Production startup now constructs `RetainedGisMapApprovalCommitterV1` with the process checkpoint publisher, and the retained state owns the Hub `DocumentWrite` lease through Assembly, Journal, committed-WAL verification, checkpoint publication, and ledger reconciliation. The checkpoint request carries the already-open actor handle, so publication does not open a second database authority.

The schema-first fixture now fixes the only allowed ordering as committed WAL Event → actor frontier → public checkpoint → ledger applied. It also fixes User → Session → Space authority → Membership → DocumentWrite acquisition and requires the ingress authority to remain live through ledger application. Twelve hostile fixture mutations are rejected. The current neutral source receipt is GREEN (`ajv=1`, `hostile=12`); this is not a native or process qualification.

The runtime now exposes a narrow `GisMapApprovalIngressAuthorityV1` correlation port. Hub's private implementation owns the actual sorted authorization guards; the runtime can only compare immutable scope/user/session/generation. The exact `Arc` is retained inside the operation through publication and reconciliation and is handed into the returned receipt, so an HTTP future dropped after journal admission cannot release authorization while the durable decision is unresolved. Public delivery still requires Hub revalidation after that authority is released.

Each mounted committer document also owns one `InferenceDocumentFenceV1` created from the actor generation. WAL verification clones that mounted fence rather than minting a request-local fence; Store shutdown invalidates it before owner retirement. Generic shutdown no longer closes a journal with a known committed receipt directly: it advances that owner into Verification, checkpoint publication, and ledger reconciliation before Store closure.

Native status remains unqualified for this source. `tYDH57/00` exposed an incomplete feature set and was corrected; `RJKiEm/00` stopped on ENOSPC with no Rust diagnostic; after exact incremental cleanup, `1Ja0Mb/00` reached the full Stdio build but the old 20-minute build budget killed it before discovery. The next run must use the registered 24-hour build/orchestration/command boundary and current Hub private ingress implementation.

## 2026-09-07 retained request-driver correction

The live ingress authority is no longer stored in the document state through checkpoint publication. It remains request-owned through every pre-witness phase. `AwaitingAck` is the exact cutover: the retained Journal stores the receipt, acknowledges its Store host, removes the ingress from both the document identity and request cursor, and retains only immutable decision provenance, the exact three Stores, and the existing DocumentWrite lease through WAL verification, checkpoint publication, and ledger reconciliation. A fresh HTTP delivery must therefore present fresh Hub authority; the lower committer cannot derive authorization from a prior request.

`GisMapApprovalRequestOwnerV1` closes the cancellation gap created by dropping the HTTP future. It owns a unique token shared with the acquired DocumentWrite lease. Drop coalesces one autonomous per-document driver. That driver releases `Ready`/preflight directly, cancels and terminally advances `Assembly` and a Journal without a receipt, and hands back all three Stores before releasing DocumentWrite. If cancellation races `AwaitingAck`, the receipt wins and the same retained owner continues verification, public checkpoint publication, and ledger reconciliation. Verification and publication allow a freshly authorized request to join the immutable committed operation without replacing its old write lease.

Committed-decision recovery reconstructs the immutable operation identity from the exact DB recovered checkpoint plus one atomic ledger outbox/accepted-identity row. Store `already_applied` is deliberately not treated as a public-checkpoint ACK: an all-new three-Store frontier can exist after a crash before Directory publication. Only ledger phase `committed`, which is ordered after the checkpoint ACK, restores `Published`; a `prepared` outbox always resumes `Verification` and idempotent checkpoint publication. Both paths reverify the receipt-local sole WAL Event before ledger reconciliation.

The neutral fixture now pins `runtime-document-maintenance`, the four pre-witness abort phases, `awaiting-ack` cutover, and `verification -> public-checkpoint -> ledger-applied`. Current source receipt is GREEN: `ajv=1 hostile=13 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=6 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`. The registered native library cohort now includes `gis_map_abandoned_pre_witness_request_returns_exact_stores_and_document_writer`, which uses a valid wake-driven poller, drops the real approval future in preflight/Assembly/Journal-without-receipt, and requires an unchanged actor frontier plus exact Store/ingress/DocumentWrite handback. It then drops at the exact acknowledged-receipt cutover and requires the autonomous owner to survive one checkpoint refusal, publish, reconcile, release the writer, and expose the single actor mutation. That new native law has not run, so the autonomous driver and recovery additions remain unqualified.

The SQLite-only test build no longer depends on native-artifact WAL fixtures: the command decoder constructs its neutral envelope locally, while committed-witness reconciliation remains compiled and executed only with `native-artifact-execution`. This preserves native proof without forcing the full Stdio/GIS feature graph into unrelated directory laws.

## 2026-09-07 Hub ingress and terminal teardown

The production approval handler now constructs a private `HubGisMapApprovalIngressAuthorityV1`. It owns the four exact `SocketBindingGatesV1` guards in the shared total order `User -> Session -> DirectorySpaceAuthority -> Membership`. It resolves the bearer before admission, then reauthenticates the exact session generation/expiry and requires the current `Author` membership while those guards are held. It deliberately does not acquire `DocumentWrite`; the retained runtime remains the sole owner of that later gate and therefore cannot be entered through a Hub lock inversion.

The handler passes that owner through `InferenceApprovalRouteContextV1`. A successful lower result is not published immediately: the Hub reauthenticates the same session and rereads the Author role under the still-owned guards before serializing the JSON receipt. A dropped request releases these request-local guards, while the post-receipt runtime driver retains only immutable WAL provenance, the three Stores, and DocumentWrite through checkpoint publication/reconciliation.

Production startup retains a second `Arc` to the inference runtime outside `HubState`. When the server terminates it awaits `HubInferenceRuntimeV1::close()` before shutting down artifact maintenance and before local DB/process owners leave scope. Close failure is a typed `HubError::InferenceShutdown`, not a swallowed teardown result.

The language-neutral fixture now pins `{owner: hub-private-sorted-binding-guards, bindings: [user, session, space-authority, membership], documentWrite: runtime-acquired, cutover: durable-wal-receipt, delivery: fresh-revalidation-under-same-guards}`. Its source gate was observed RED before the production wrapper (`missing HubGisMapApprovalIngressAuthorityV1`) and is now GREEN with `hostile=13`. Targeted Rust formatting is GREEN. The registered bin native cohort adds `gis_map_approval_ingress_holds_sorted_hub_authority_without_outer_document_write`: it holds and probes all four exact Hub gates, proves DocumentWrite is still immediately acquirable, revalidates delivery, releases the authority, performs a real durable role demotion, and requires fresh admission to return `Denied`. This law is registered with `sqlite,test-support,native-artifact-execution` and has not run; no native ingress or teardown claim is made yet.

## 2026-09-07 no-runtime and sole-driver retention

`commit` now constructs a prepared-approval owner synchronously, before its returned future can be polled or dropped. A no-runtime unpolled Drop first attempts the exact synchronous SQLite abandonment. A refusing abandonment retains the complete prepared cursor in the committer's job-keyed parked registry. Once any later commit or explicit close enters Tokio, the parked cursor transfers to an independently spawned maintenance task before any retry await; cancelling that caller no longer drops the sole cursor.

The same rule now covers a request dropped outside Tokio after it admitted document state. The complete identity and request token are parked by job identity while its maintenance key remains live. A later commit or close transfers the exact cursor to process-owned maintenance, which either aborts the pre-witness Store operation and outbox together or crosses `AwaitingAck` and continues immutable-WAL recovery/publication. Both spawned cleanup task types retain the original cursor behind a Drop guard; cancellation of the temporary Tokio runtime reinserts that exact cursor before the task future disappears. A fresh runtime can therefore resume it. The native law performs this temporary-runtime shutdown/reinsert/drain sequence for both a prepared owner and an admitted request owner.

Cleanup capacity is now an admission, not a `HashMap::with_capacity` hint. `commit` reserves one refcounted job slot before constructing or admitting an owner. There are exactly 128 unique slots, matching the ledger job capacity; a 129th distinct job receives typed `Capacity` before any runtime owner exists. Terminal success or abandonment releases its exact reference, while a parked or runtime-cancelled cursor keeps it. The native law fills all 128 reservations, verifies the 129th real commit returns its ingress while preserving the durable prepared row, releases the reservations, and proves reuse. Parked maps are job-keyed subsets of these admissions, and duplicate same-job handoff releases one redundant reference rather than growing either registry. The neutral fixture pins both that capacity and the runtime-shutdown handback rule.

Post-witness publication has one request-token driver. New requests subscribe to the document state epoch and join instead of invoking verifier/publisher concurrently. `close` likewise waits for the live request or maintenance token; the native law pauses the sole publisher, polls close to Pending, proves only two total attempts including the intentionally refused first attempt, then releases one publication and one ledger reconciliation. The abandoned-phase law polls close while maintenance owns cancellation and requires Pending before exact Store/DocumentWrite handback. The publisher receives a bounded attempt lifetime and derives a fresh process deadline; the immutable journal timestamp remains ledger provenance only.

The current source oracle is GREEN: `ajv=1 hostile=13 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=6 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`. This remains a native nonclaim. The dedicated GIS target was not launched because the latest census showed the external full-Stdio compiler plus two workspace-wide Cargo checks and less than 100 MiB of free pages; `space-public-boundary-sol-target` remains idle.

## 2026-09-07 immutable admission and terminal close

The public committer now validates the durable prepared row before reserving cleanup capacity or constructing an owner. The immutable job, mutation, command hash, proposal hash, scope, user, session, and authorization generation must all match the SQLite outbox plus accepted identity. The native corpus includes a same-job command-and-proposal substitution and requires `Conflict`, immediate ingress release, no cleanup reservation, and an unchanged exact prepared tuple.

Terminal close is now an admission fence. It sets the closing latch while holding the cleanup-registry mutex, so a concurrent commit either owns a cleanup slot that close must observe or receives typed `Unavailable`; no commit can appear after close observes the registry empty. Close waits for documents, maintenance tasks, and every unpolled cleanup owner. Every cleanup decrement signals the state epoch after unlocking. The added native law constructs a valid but unpolled commit future, polls close to `Pending`, proves a later admission is refused, drops the first future, and requires exact outbox abandonment, ingress release, empty maintenance/cleanup registries, and DB/pool shutdown before close returns.

The fixture now pins `terminalClose=fence-new-admission-until-cleanup-empty`; its current source oracle is GREEN: `ajv=1 hostile=13 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=7 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`. `rustfmt --emit stdout` parsed the complete runtime and `git diff --check` is clean. Native execution remains pending; no runtime claim is inferred from this source receipt.

## 2026-09-07 canonical admission native boundary

The three production-shaped committer laws no longer reuse the ledger-only text proposal. One shared fixture builder decodes the real canonical GIS Map pack, derives the deterministic fixed-three `CreateRegion` parent/drawing/value work, serializes the exact parent proposal and inverse, and stamps the canonical command with the accepted actor, scope, mutation, schema, and HLC. Full pure preflight now runs before cleanup reservation or request-owner construction. Lower admission also compares the prepared identity's scope, ingress, descriptor, document, head ordinal/edit, commit sequence, chain hash, and input digest against the supplied base before it can mutate the outbox or retain an owner. Hostile direct-call rows cover foreign ingress scope, actor, descriptor, pack, each frontier component, base-frontier substitution, and command/proposal substitution.

Native receipt `exact-cargo-laws-t2n66k/00` built and listed the current 109-test Hub library executable (`SHA-256 76b0cbcf6bb7ed2937f8418ee7d938ec0173145df4b2333962828054a92da4fd`). Laws 0–2 passed exactly once, including the previously failing canonical-pack admission law. Law 3 remained RED after 0.25 seconds: `gis_map_approval_committed_event_reaches_actor_frontier_and_public_checkpoint_before_ledger_apply` returned `Conflict` where the intentionally refusing publisher should return retained `Storage`.

The exact source defect was a cross-domain comparison in checkpoint publication: the DB actor's committed WAL chain hash was compared with the parent Store's content revision. These identities are deliberately unrelated. The check has been removed while retaining actor generation, exact +1 head/commit frontier, mutation ID, receipt-local sole-WAL-Event verification, fixed-three decision verification, Store snapshot pack/SPR hashing, and the publisher's exact frontier/hash postcondition. The law now explicitly requires the WAL chain and Store revision to differ while still reaching the refusing publisher. The post-repair source oracle is GREEN (`ajv=1 hostile=13 committer=7 visibility=7`); native rerun is pending because the shared cache was released immediately to the queued DB capability gate.

## 2026-09-07 applied-checkpoint peer visibility

The retained checkpoint port now has a synchronous `checkpoint_applied` terminal callback. The committer invokes it only after the exact Directory checkpoint has been published and `reconcile_committed_approval` has durably moved the approval ledger to `Approved`, but before the retained DocumentWrite lease is released. A callback error or panic leaves the document in `Publishing` with its Stores and write authority intact, so a later exact owner can repeat idempotent checkpoint publication/reconciliation and retry the notification. A checkpoint publication refusal never reaches the callback.

Production startup gives the GIS checkpoint publisher the same per-document fanout registry and capacity owned by `HubState`. Its terminal callback emits the existing authenticated `ServerFrame::RebootstrapRequired` carrying the exact published checkpoint ID, descriptor digest, and actor frontier. It creates no alternate restoration route: connected clients must use the existing verified checkpoint refetch/rebootstrap path. The approved mutation remains the server-stamped fixed-three `CreateRegion`; no `patchPositions` claim is introduced.

The retained runtime law's observable order is now `public-checkpoint-attempt`, refused attempt, successful attempt, `public-checkpoint-ack`, then `peer-rebootstrap`; it decodes the exact Store-derived checkpoint pack and requires the server-stamped `inference-{job}` Region with `kind=inference-bounds`, then verifies the ledger is already Approved inside the callback. Its pre-witness cancellation cycles require an empty publication/notification trace. The new Hub law subscribes two peers to one document fanout and requires each to receive one byte-equivalent rebootstrap control with no duplicate frame. The language-neutral fixture pins notification only after committed WAL Event, actor frontier, public checkpoint, and ledger apply, and pins no notification for publication failure or cancellation.

The updated source oracle is GREEN: `ajv=1 hostile=14 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=7 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`; `git diff --check` is clean. The registered native cohort now has 14 exact laws. It has not run against this callback/fanout source, and no two-browser Map-scene convergence claim is made from this focused fanout law.

The existing browser-side continuation is `DocumentBrowserActorReservation.transferColdPair`: each independently opened socket/actor reservation can consume verified canonical pages and must reach terminal `Applied` before scene drive. Current controlled worker laws do not connect two real document sockets or render genuine GIS scenes. The next process acceptance must join the new checkpoint-bound fanout to two fresh plan/grant admissions, invoke that cold-pair transfer independently, and compare both resulting Map scenes for the exact Region and published frontier.

## 2026-09-07 initial actor generation fence

Native receipt `exact-cargo-laws-2p8HmD/00` built and listed the current Hub library, then passed laws 0–2 exactly once. Law 3 still returned `Denied` before the intentionally refusing checkpoint publisher could return `Storage`. The fixed-three assembly terminal diagnostic did not execute, excluding the prior Store identity binding branch.

The remaining `Rejected` path exposed a distinct representation bug. A freshly mounted DB actor legitimately starts at authority generation `0`; `InferenceDocumentFenceV1` rejected zero and also used zero as its invalidation sentinel. The fence now stores the exact actor generation, including the initial zero generation, alongside an independent atomic active bit. Invalidation flips only that bit. WAL target validation and committed-witness reuse require both the active bit and exact generation equality, so zero no longer means both “live initial incarnation” and “invalidated.” SQLite ledger reconciliation had the same obsolete zero rejection; it now admits zero while still requiring the active witness and exact generation and while preserving the safe-integer upper bound. The real runtime law now asserts the mounted actor starts at generation zero before exercising the same committed-Event and ledger-reconciliation path; the existing invalidated-witness law still requires `Conflict`.

The language-neutral committer contract pins `mounted-actor-generation-including-zero`, and its source oracle verifies the independent active-bit invalidation rather than a zero sentinel. The focused native law retains the real generation-zero witness through publication and first reconciliation, closes the committer to invalidate its exact fence, then requires a second reconciliation to return `Conflict` and observes exactly one `approved` event. Current source receipt is GREEN (`ajv=1 hostile=15 committer=7 visibility=7`), targeted Rust formatting is GREEN, and `git diff --check` is clean. Native rerun remains pending while the shared Directory backend signatures are compile-in-flight; no runtime success is claimed.

## 2026-09-07 native pre-admission diagnostic

Native receipt `exact-cargo-laws-CnQova/00` built and listed the 109-test Hub library, then passed laws 0–2 exactly once. Law 3 remained RED after 0.27 seconds at the first intended checkpoint refusal: the route returned `Conflict` instead of the publisher's injected `Storage`; no later law ran.

The next native receipt `exact-cargo-laws-MXcv1Z/00` repeated the same build/list and laws 0–2 result, then emitted a bounded test-only discriminator before the same law-3 assertion. It observed `state=missing`, `retained_regions=[]`, and an empty publisher order. The preserved SQLite approval outbox remained `phase=prepared`. This proves the remaining `Conflict` occurs in immutable prepared-request validation or pure fixed-three preflight, before cleanup admission, document-write acquisition, document mount, WAL append, verification, or publication. It does not invalidate the later retained-owner design, but it prevents any native claim for that path.

The source now emits `[DEBUG]` only under the native test build for the exact failed immutable predicate or pure preflight predicate. The source oracle remains GREEN with `hostile=14`; another native run is pending the shared-cache owner. These temporary diagnostics must be removed after the failing predicate is repaired.

Static comparison then found the pre-predicate rejection: immutable admission parsed the accepted identity's frontier chain with `ArtifactHash::parse_hex`. That parser is intentionally for published document blob identities and rejects all-zero hashes, while the DB actor and accepted inference identity correctly use `[0; 32]` as the genesis WAL-chain sentinel. The same incorrect parser also existed in recovered-checkpoint identity reconstruction.

Both admission and recovery now use a private exact 32-byte lowercase-hex frontier decoder that permits genesis zero while still rejecting wrong width, uppercase, or non-hex input; frontier equality remains byte-for-byte. The temporary native diagnostics were removed. The neutral committer schema now names this contract `canonical-lowercase-hex-with-genesis-zero`, includes a hostile nonzero-blob-only mutation, and checks the cross-fixture accepted identity is exactly 64 zeroes. The direct source oracle is GREEN with `hostile=15`; native proof remains pending.

## 2026-09-07 fixed-three Store identity repair

Native receipt `exact-cargo-laws-x6YecW/00` built and listed successfully, passed laws 0–2, and advanced law 3 past the genesis-frontier `Conflict`. The next exact result was `Denied` where the injected first checkpoint-publication refusal must return `Storage`; runtime was 0.28 seconds and no later law ran. The SQLite outbox remained `prepared`, so this was still a pre-WAL rejection rather than a publisher outcome.

The fixed-three Store identity audit found the exact cause. `parent_store`, `drawing_store`, and `value_store` constructed bare envelopes through `create_document_envelope`, which deliberately leaves `dialect` and `owner` empty. Mounting also prefixed the child artifact IDs with the document key. The Store assembly could admit and prepare all three mutations, but `DurableOwnedThreeStorePreparedV1::bind_store_owned` then rejected those anonymous members: the durable contract requires the parent reference `{document-key}!s.gis.gismap@1/*`, the exact child references `gismap-drawing!s.stdio.semio@v1/drawing` and `gismap-value!s.stdio.semio@v1/value`, and each child `OwnerRef` anchored to the parent with the matching role and child ID.

The runtime now stamps those exact envelope dialects and child owners before constructing each Store, and uses the canonical child IDs without a document-key prefix. The production-shaped law now independently proves immutable ledger/base validation and pure GIS preflight before owner reservation, then—after the real retained journal/publication path reaches its injected checkpoint refusal—compares the retained parent/child `ArtifactRef` and both child `OwnerRef` values byte-for-byte. One bounded `[DEBUG]` line remains test-only at the assembly terminal to expose its typed failure if another lower Store invariant rejects the next run; it must be removed after the next native result.

The direct permanent source oracle is GREEN (`ajv=1 hostile=15 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=7 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`), targeted formatting parsed the runtime, and `git diff --check` is clean. This identity repair has not run natively; the shared target is currently owned by the retained-admin cohort.

## 2026-09-07 exact-genesis first checkpoint publication

Production checkpoint publication previously required an already-active Directory checkpoint whose baseline equalled the approval request base. That made the first valid approval for a newly mounted document fail closed even though the DB actor supplied its exact genesis frontier.

The publisher now accepts a missing active checkpoint only when the base belongs to the requested document and is exactly actor genesis: ordinal zero, empty head edit ID, commit sequence zero, and zero WAL chain hash. Once an active checkpoint exists, its scope and baseline frontier must still match exactly. Hostile native predicate rows vary each genesis field independently, and the language-neutral corpus pins `no-active-checkpoint-exact-zero-frontier`; its permanent source guard requires the production helper and call site.

The updated source oracle is GREEN (`ajv=1 hostile=16 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=7 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`). Full-file `rustfmt --check` is presently blocked by concurrent unformatted Directory admin-effect edits in the shared Hub binary; this section's Rust is formatted consistently and `git diff --check` remains the handoff requirement. Native execution is deliberately held until that shared API cohort is source-coherent, so no first-checkpoint runtime claim is made.

Native receipt `exact-cargo-laws-9DQTZ2/00` stopped in the first library build before listing or executing any law. Three in-flight SQLite Directory admin-effect early-return branches produced `AdminEffectCommitV1<AuthAuditRecord>` where the enclosing operations require `AdminEffectCommitV1<IssuedShareToken>`, `AdminEffectCommitV1<()>`, and `AdminEffectCommitV1<IssuedInvite>` respectively (`sqlite/🦀️.rs:984,1039,1695`). The shared target was released immediately and the exact diagnostic was routed to that source owner. This receipt provides no compilation or runtime evidence for the GIS repair.

## 2026-09-08 PostgreSQL and Neo4j atomic artifact genesis

Both scale-out Directory backends now implement the five durable artifact-creation operations introduced by the shared `HubDirectory` port. PostgreSQL stores immutable revisions in `hub_artifact_creation_fact`, with a partial unique document-identity index and a bounded recovery index. Neo4j stores one request node plus immutable fact nodes, with unique request, document-scope, and request-revision identities and a recovery index. Both decode stored facts through the first-party Pack JSON value model and fold no more than the shared three-revision state machine.

Claim and transition writers serialize one user/request key. New claims reject an already occupied document identity and persist the exact server-created accepted intent only once; retries compare the canonical command digest and space binding and never replace the minted identity. Both backends observe server `SystemTime` inside the writer transaction, reject future or expired caller timestamps, and lock/revalidate the live User, exact Session generation, non-archived Space, and Author membership. Cancellation can use a fresh current session for the same user, while the shared transition decider still requires the accepted actor for prepared bytes.

Genesis completion is not composed from generic Directory calls. One backend transaction validates the durable prepared operation, live CAS reservation, deletion fence, empty public scope, and exact authority. It acquires CAS-space before the public Directory writer, persists the dense author/author/system event triple, applies descriptor/index/checkpoint projections, writes the authority journal and private checkpoint, consumes the reservation into the CAS reference/ledger, derives the committed fact from the actual persisted event sequence, and appends revision three before commit. A retry with an existing receipt returns the folded immutable operation without republishing events. Recovery returns only bounded expired Accepted or any nonterminal Prepared intent.

Source evidence is limited to Rust parser success for both backend files and a clean scoped `git diff --check`. No PostgreSQL or Neo4j service was launched, and no native backend law has executed yet. The long-running browser bundle producer is independent and remains a mixed-frontier diagnostic build.

## 2026-09-08 backend lock-time and recovery qualification frontier

The PostgreSQL and Neo4j genesis writers now use the same request → CAS-space → Directory counter/head → User/Session/Space/Membership order. The authority reader locks raw rows before sampling backend `SystemTime`, so a request delayed behind either the request writer or authority writer cannot reuse an earlier pre-lock timestamp across session expiry. The shared fact decider receives that post-lock observed time and stamps every accepted transition from it; caller-provided `recorded_at_ms` remains a future-clock refusal input rather than a persisted clock authority.

Neo4j originally filtered generation, revocation, archive state, and Author role inside its `MATCH ... WHERE`, before the lock-producing mutation. A revoked or membership-less identity therefore returned no row without locking the still-existing User, Session, or Space. The validator now locks and returns those raw nodes first, optionally locks the membership relationship when present, samples time afterward, and only then evaluates every authority predicate in Rust. This keeps conclusive terminalization serialized with the normal authority writers even when the membership is absent.

Ordinary checkpoint append is sealed in both backends: it rejects a missing parent checkpoint or any baseline that is not the exact edited frontier for its scope before reservation/replay handling. Parentless exact genesis can therefore be established only by the dedicated atomic genesis transaction. The supervisor terminalization path rereads and folds the exact intent under the request lock; a live, unexpired Author is not terminalized, while an expired deadline or conclusively dead authority appends one `Failed` fact. A second service racing the same terminalization can only observe that terminal fact and cannot reuse the prepared pair.

Each backend now has one deterministic container-backed law. It injects acknowledgement loss only after a successful commit and requires `Indeterminate`, exact receipt/event re-read, and an idempotent `Existing` retry. It then pauses after request/CAS/counter acquisition, advances a test-only observed-clock sample beyond the locked session expiry before authority evaluation, and requires refusal with the pre-attempt Directory head and Preparing operation unchanged. Production passes no override and samples `SystemTime` only after the same authority locks. The fixture then expires the stored session and lets two independently connected services race terminalization; both must fold one three-fact terminal operation. These laws are registered separately for `postgres` and `neo4j`; their Docker containers use fixture-only credentials and ephemeral loopback ports. They have not run.

The launch registrations also expose the qualified SQLite eight-law phase and the full native phase. During review, three generated launch paths were corrected from a non-existent `🧏️repo` segment to the ticket's actual `🦑️repo` root, and both backend laws were corrected to include the existing `🗿️artifact-authority` operation fixture. Launch regeneration completed successfully. The permanent language-neutral source gate is GREEN: creation cases 34, transaction cases 30, accepted-recovery cases 3, cancellation cases 9, operation transitions 30, and frontier cases 8. This is schema/source evidence only.

The browser bundle job remains active in its private target at the Stdio `wasm32-wasip2` release link. It has already compiled its Hub binary but its source frontier predates later runtime-owned descriptor and native codec identity repairs; any eventual result is diagnostic and requires a current-epoch retry before qualification.

## 2026-09-08 GIS native codec structural receipt

Native catalog receipt `exact-cargo-laws-Gfxjrp/00` compiled for roughly 26 minutes and passed its first three laws. Its fourth law rejected the exact private GIS receipt at `native-openable-provider.rs:360`; the five later laws did not execute.

The rejection was not an editor/package/component identity relaxation. `GisMapSnapshot` and `GisTerrainSnapshot` use handwritten `ArtifactPack` implementations whose inherited `record_spec()` returned `None`. The corrected generic `ArtifactCodec::of` therefore assigned the forbidden all-zero structural `pack_schema_hash`, and `NativeGisCodecReceiptV1::validate` correctly refused it. Both snapshot packs now expose explicit field-number/key/shape records derived from their first-party `DslField` types. The native GIS receipt law requires each codec hash to be nonzero and byte-equal to `store::os_pack::schema_hash(record_spec)`, while preserving the distinct Pack envelope identity.

Targeted Rust formatting and scoped whitespace validation are GREEN. This repair has not yet been compiled or executed natively; the earlier receipt is RED evidence only. The long-running browser build captured the older source frontier and cannot qualify this repair.

## 2026-09-08 exact genesis-to-first-command undo fixture

The lower retained-committer law now identifies its initial actor state as the exact scope-bound genesis frontier before executing the first real server-stamped `CreateRegion` command. Its neutral fixture requires that command and the following durable Undo to advance history by one ordinal each, requires the Undo result to restore the byte-decoded initial GIS Map snapshot exactly, and forbids both a synthetic genesis edit and resetting the edited frontier back to genesis. This remains a Store/WAL/committer law; it makes no public Directory-genesis claim.

The registered source gate is GREEN: the undo oracle reports `AJV=1 exact=6 genesis-first=1 history=2 hostile=8`, and the full proposal oracle reports `hostile=16`. Targeted Rust formatting and scoped whitespace validation are GREEN. Native execution is still pending.

The public route fixture has a separate stale setup under the Hub binary owner's scope. `gis_map_inference_fixture` announces a descriptor, then `publish_gis_checkpoint_for_test` attempts to publish an edited ordinal-one checkpoint through the ordinary checkpoint path with no parent. The new lineage and index requirements correctly reject that setup. The binary owner has been given the exact conversion: publish the dedicated indexed parentless genesis transaction first, then publish the nonempty Map fixture as a normal first edited checkpoint whose parent is that genesis. The lower runtime law is not evidence for that public conversion.

## 2026-09-08 browser render fixture lifetime

The full OS TypeScript suite exposed an intermittent test-owner expiry: the controlled cold-pair law gave its already-activated actor only a 25-second grant, while that single law takes roughly 20–25 seconds under the current runner. A valid live render could therefore reach the hostile render-turn matrix after the actor reservation had correctly retired, producing `missing render lifetime` instead of the intended `render turn limit` boundary.

The fixture now grants 120 seconds and a 125-second retirement bound; the production lifecycle, grant, and render lifetime checks are unchanged. The focused law is GREEN (`1 passed`, `295 skipped`) and the full registered OS suite is GREEN (`296 passed`, four files). No real browser or native guest claim is inferred from this controlled worker test.

## 2026-09-08 active-genesis proposal contract

The production checkpoint publisher had already removed its obsolete `None + zero frontier` admission, but the neutral GIS proposal fixture still required that fallback. The fixture and schema now require `active-indexed-genesis-parent-before-first-edit`. Its source oracle isolates `current_matches_base` and requires both exact scope/frontier equality for `Some(checkpoint)` and `None => false`, preventing the prior parentless approval fallback from returning through a merely-present helper symbol.

The registered proposal source gate is GREEN (`hostile=16`, session `88040`, terminal chunk `d0d70e`). The native catalog selection retry did not reach Cargo: its inferred Framework Graph generation prerequisite rejected unrelated `print-latex-tokens` and `report-actor-network` preview routes. A read-only registered graph preview reproduced the same two errors. No native catalog law or build is claimed from that attempt.

## 2026-09-08 retained artifact-creation progress and cancellation UI

The Shell now projects ordinary artifact creation into a bounded request-ID-keyed UI owner. Issuance inserts the accepted presentation synchronously with the retained worker owner. Later status is accepted only after the existing runtime, client, session, Space, request, and kind checks. A user cancellation keeps that owner, marks `cancelRequested`, posts one exact request/Space cancellation, and disables further clicks; only a server terminal or the fenced Ready/open completion removes the retained owner. A racing Ready after cancellation therefore still enters the existing exact document-open path rather than being rewritten as Cancelled.

The presentation holds at most eight rows. It may reclaim a terminal row but never evicts a live request to display a ninth issue. Accepted, preparing, and Ready are polite status regions; indeterminate, failed, and cancelled are assertive alerts. Because the server phases expose no numeric work counters, the component renders phase text and never fabricates a `<progress>` value. The schema-owned English and German strings are verified through i18next, while an unsupported locale renders no fallback language. The catalog has a separate exact `loading | ready | unavailable` presentation, so an authorized empty catalog is no longer indistinguishable from transport/capacity failure.

The worker publishes loading before its exact catalog request, ready only after the selected-current catalog, and unavailable only while that same client/Space operation still owns the request. Its creation owner retains the real accepted/preparing/ready sequence, routes one exact cancel, and never invents Cancelled when the cancel endpoint refuses. The OS wire parser rejects any catalog phase outside the three schema values.

Registered gate session `13603` is GREEN: six focused renderer/reducer/accessibility laws and four focused real worker transport laws passed. Renderer TypeScript session `21140` is GREEN. The broader OS suite session `66012` is GREEN with all `296` tests in four files. Registry generation session `26784` and generated catalog/launch freshness session `12403` are GREEN. These results prove the controlled renderer and worker boundaries; they do not claim a real browser, Hub backend, or native artifact-creation run.

The private browser producer session `37366` remains active and silent in Stdio WASI compilation. Its captured source frontier predates later runtime-owned descriptor, native-codec receipt, and genesis changes, so any eventual result remains diagnostic until a current-source hermetic rerun.

## 2026-09-08 ticket-owned browser host staging

The development browser can now consume a closed, ticket-owned host stage instead of the repository-global plugin module and activation roots. The Vite boundary accepts overrides only when all three exact paths are present: `SEMIO_TEST_BROWSER_MODULE_ROOT`, `SEMIO_TEST_BROWSER_ACTIVATION_ROOT`, and `SEMIO_TEST_BROWSER_HOST_RECEIPT`, together with `SEMIO_TEST_ARTIFACT_DIR`. Every path must be absolute, canonical, non-symlink, bounded, and contained below that ticket's generated artifact root. A partial, substituted, or out-of-owner override refuses before browser startup; the ordinary development defaults remain available only when no test override is supplied.

The exported `stageTestBrowserHostV1` facade owns the complete stage. It verifies the retained selected GIS component and descriptor against their supplied SHA-256 identities, builds and materializes a fresh Space host, materializes the selected GIS component without rebuilding or reading a global GIS descriptor, stages the Preview2 support module and shard worker, and closes an exact `gis,space` activation. It atomically publishes `browser-host/{modules,activation,extensions}` plus a versioned receipt binding the exact Cargo-produced Space component byte length and SHA-256, the separately served JCO core SHA-256, the Space descriptor Pack SHA-256, activation digest, GIS generation, and GIS current digest. Closure cross-checks the raw/core identities recorded in the freshly emitted Space descriptor against the independently measured owners; final resolution rehashes the complete served tree, core, descriptor, activation, and both selected GIS inputs before returning verified roots. The Space descriptor is also inspected for the exact `createArtifact(name, kindChoice)` dialog contract.

The language-neutral receipt/schema is `semio.os.test-browser-host-staging/v1`; AJV 2020 supplies the independent schema oracle. Controlled hostile rows reject a stale Space dialog argument, a substituted raw component identity, substituted Space descriptor, substituted Space module, altered activation, partial environment, symlink input, and an owner escape. The registered source target `@semio-tech/framework-os-dev:browser-host-staging-check` was GREEN in session `16857` (`1 passed`, `104 skipped`) before the raw/core receipt split; that split is source-complete but still requires a current registered rerun. Launch regeneration session `62944` and generated-launch freshness session `11340` were GREEN before that split.

This evidence qualifies the staging contract and its controlled filesystem boundary only. It has not executed a real Space WASIp2 build, consumed a retained GIS pair, launched Chromium, or proved two peers against the staged host. The Hub process owner will invoke the facade once for one selected GIS current, pass the same returned roots and receipt to the first and restarted peers, and preserve that single host identity across the process acceptance run. The old long-running browser producer remains mixed-frontier diagnostic evidence and cannot qualify this new stage.

The Hub two-author owner is now wired to that facade. It requires the retained current's exact `packages/gis/component.wasm` and `packages/gis/descriptor.semio` coordinates, passes their recorded identities to one staging call before Chromium starts, and supplies the returned roots and receipt to every initial and restarted Shell peer. Each dev process receives the complete four-variable authority; its process receipt carries the same closed browser-host receipt. The fresh Space compilation uses the fixed ticket-local `browser-host-wasi-target` with one Cargo job, no incremental state, and no compiler wrapper; it never falls back to the repository target.

The two-author neutral schema now includes the ticket-owned host stage, eight receipt identities (including raw component length and the distinct served-core digest), and a hostile global-browser-host fallback row. Its isolated source target was GREEN in session `87538` before this receipt strengthening: AJV accepted the fixture and rejected all ten hostiles, while Bun and the third-party `ws` client agreed on all five exact socket-retirement rows. The browser-host target was rerun after the ticket Cargo-owner source guard and was GREEN in session `38289`. Launch regeneration `84765` and freshness `39020` were also GREEN before the receipt strengthening. A ticket-local Nx workspace-data directory was used because the shared graph retained a deleted Stdio LAS manifest; no shared Nx cache was removed.

The older browser producer `37366` ultimately ended RED after 310 minutes. It built its Hub executable but failed during the first fresh Stdio snapshot, before any browser launch: the exact Cargo-produced WASIp2 component exceeded the shipped execution-target cap of 67,108,864 bytes. Its private target was then removed by the producer's normal `finally` owner before the generic error reported the observed size, so the historical byte length is not recoverable. The producer now performs a format-specific pre-capture size check that includes both observed byte length and maximum in the error, while preserving the 64 MiB published component policy; its controlled exact-bound law passed inside the current fresh-staging source cohort. A fresh current build must establish whether the later Stdio closure changes now fit before any process acceptance is claimed.

The `wasm-release` profile already uses size optimization, thin LTO, one codegen unit, symbol stripping, disabled incremental compilation, and object path trimming. The fresh producer stages the exact Cargo component bytes as `component.wasm`; its 64 MiB check is therefore a shipped-component boundary, not a limit on a disposable debug intermediate. The normal web materializer's Binaryen pass operates on JCO-extracted core modules and does not change the retained component identity checked here. No policy increase or unrecorded byte rewrite is justified before a current build reports its exact component length.

A current registered two-author attempt, session `76597`, used the existing 5.9 GiB ticket-private browser cache to avoid allocating a second Hub cache on a nearly full disk. It never entered the task: the Nx child remained idle with no producer or Cargo child for about six minutes. During that interval unrelated active builds reduced disk headroom from 40 GiB to 16 GiB, so only this pre-task Nx process was interrupted. No component, browser-host stage, Chromium process, or runtime law was created or claimed. The launch remains registered against the same ticket-owned root for a later capacity-safe retry; the source namespace repair and 64 MiB component boundary are unchanged.

The strengthened raw/core receipt boundary is now qualified by the registered browser-host staging target in session `13916`: one exact law passed and 104 unrelated laws were skipped. It measures and binds the raw Cargo Space component separately from the served JCO core, verifies both hashes through the freshly emitted descriptor before activation publication, and rejects a substituted raw component identity. The exact H two-author source gate is GREEN in session `1578`: 16 fixture laws and 10 hostiles passed, with the five socket-retirement rows agreeing across Bun and the third-party `ws` implementation. Its first attempt reached Vitest but exposed three deleted taxonomy imports outside staging; the rerun followed narrow repairs to the current nested actor schemas, the merged poll-composition fixture definition, and the merged kernel source-watch fixture definition. These are source/controlled qualifications only: no Cargo component producer, ticket browser stage, Chromium peer, or real document journey ran.
