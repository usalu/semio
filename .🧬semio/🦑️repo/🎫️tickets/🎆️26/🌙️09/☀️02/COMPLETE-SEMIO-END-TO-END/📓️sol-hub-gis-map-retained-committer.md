# Retained GIS Map Committer

## Boundary

The Hub runtime now has a production-shaped, deliberately unregistered `RetainedGisMapApprovalCommitterV1`. One mounted document state owns the exact DB `ArtifactHandle`, parent Map Store, drawing Store, and value Store. Approval can move those owners only through `Ready -> Assembly -> Journal -> Verification -> Committed`; shutdown moves any phase through `Closing` and returns every Store to its exact owned disposer.

The committer rederives the fixed-three GIS work from the canonical Map base, checks the exact ordered `gismap-drawing` and `gismap-value` members, checks the canonical command and deterministic mutation identity, and uses `DurableOwnedThreeStoreMapAssemblyV1`. Its journal sink comes only from the mounted document `ArtifactHandle::durable_group_journal_sink`. It never calls generic submit and returns no receipt until `InferenceWalVerifierV1` finds the sole one-Event committed WAL transaction.

The GIS preparation factories gained a server-derived stamp containing the approved mutation ID and HLT. The parent WAL verifier now compares the physical decision edit's mutation ID as well as actor, author, payload, inverse, and timestamp before reconstructing the command hash.

## Schema-first evidence

- Initial source oracle: RED, exactly `retained GIS Map committer source is missing RetainedGisMapApprovalCommitterV1`.
- Current source oracle: GREEN, `ajv=1 hostile=7 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=6 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`.
- The existing GIS fixed-three native run `exact-cargo-laws-nYkzCz` has group 00 GREEN4 and group 01 still building the full Stdio dependency at the time of this checkpoint. It predates the Hub committer source and is not evidence for this runtime.

## Nonclaims and remaining integration

Production Hub construction still injects `UnavailableGisMapApprovalCommitterV1`. This is intentional. Before registration, the Hub document-write gate must be shared with this owner; startup must mount committed-decision recovery before new approvals; checkpoint publication must consume the same updated parent Store authority; and process shutdown must demonstrate retained close/retry under injected journal and Store retirement faults. No current receipt therefore claims a live Hub `applied=true`, process restart recovery, user-visible Map update, or undo/redo path.

## 2026-09-07 production bridge update

The earlier nonclaim is no longer the current source shape. Production startup now constructs `RetainedGisMapApprovalCommitterV1` with the process checkpoint publisher, and the retained state owns the Hub `DocumentWrite` lease through Assembly, Journal, committed-WAL verification, checkpoint publication, and ledger reconciliation. The checkpoint request carries the already-open actor handle, so publication does not open a second database authority.

The schema-first fixture now fixes the only allowed ordering as committed WAL Event → actor frontier → public checkpoint → ledger applied. It also fixes User → Session → Space authority → Membership → DocumentWrite acquisition and requires the ingress authority to remain live through ledger application. Twelve hostile fixture mutations are rejected. The current neutral source receipt is GREEN (`ajv=1`, `hostile=12`); this is not a native or process qualification.

The runtime now exposes a narrow `GisMapApprovalIngressAuthorityV1` correlation port. Hub's private implementation owns the actual sorted authorization guards; the runtime can only compare immutable scope/user/session/generation. The exact `Arc` is retained inside the operation through publication and reconciliation and is handed into the returned receipt, so an HTTP future dropped after journal admission cannot release authorization while the durable decision is unresolved. Public delivery still requires Hub revalidation after that authority is released.

Each mounted committer document also owns one `InferenceDocumentFenceV1` created from the actor generation. WAL verification clones that mounted fence rather than minting a request-local fence; Store shutdown invalidates it before owner retirement. Generic shutdown no longer closes a journal with a known committed receipt directly: it advances that owner into Verification, checkpoint publication, and ledger reconciliation before Store closure.

Native status remains unqualified for this source. `tYDH57/00` exposed an incomplete feature set and was corrected; `RJKiEm/00` stopped on ENOSPC with no Rust diagnostic; after exact incremental cleanup, `1Ja0Mb/00` reached the full Stdio build but the old 20-minute build budget killed it before discovery. The next run must use the registered 24-hour build/orchestration/command boundary and current Hub private ingress implementation.

## 2026-09-07 retained request-driver correction

The live ingress authority is no longer stored in the document state through checkpoint publication. It remains request-owned through every pre-witness phase. `AwaitingAck` is the exact cutover: the retained Journal stores the receipt, acknowledges its Store host, removes the ingress from both the document identity and request cursor, and retains only immutable decision provenance, the exact three Stores, and the existing DocumentWrite lease through WAL verification, checkpoint publication, and ledger reconciliation. A fresh HTTP delivery must therefore present fresh Hub authority; the lower committer cannot derive authorization from a prior request.

`GisMapApprovalRequestOwnerV1` closes the cancellation gap created by dropping the HTTP future. It owns a unique token shared with the acquired DocumentWrite lease. Drop coalesces one autonomous per-document driver. That driver releases `Ready`/preflight directly, cancels and terminally advances `Assembly` and a Journal without a receipt, and hands back all three Stores before releasing DocumentWrite. If cancellation races `AwaitingAck`, the receipt wins and the same retained owner continues verification, public checkpoint publication, and ledger reconciliation. Verification and publication allow a freshly authorized request to join the immutable committed operation without replacing its old write lease.

Committed-decision recovery reconstructs the immutable operation identity from the exact DB recovered checkpoint plus one atomic ledger outbox/accepted-identity row. Store `already_applied` is deliberately not treated as a public-checkpoint ACK: an all-new three-Store frontier can exist after a crash before Directory publication. Only ledger phase `committed`, which is ordered after the checkpoint ACK, restores `Published`; a `prepared` outbox always resumes `Verification` and idempotent checkpoint publication. Both paths reverify the receipt-local sole WAL Event before ledger reconciliation.

The neutral fixture now pins `runtime-document-maintenance`, the four pre-witness abort phases, `awaiting-ack` cutover, and `verification -> public-checkpoint -> ledger-applied`. Current source receipt is GREEN: `ajv=1 hostile=13 node-sha256=2 independent-bounds=2 preview=1 lifecycle=9 committer=6 visibility=7 errors=11 approval-rejections=11 cross-fixture=1`. The registered native library cohort now includes `gis_map_abandoned_pre_witness_request_returns_exact_stores_and_document_writer`, which uses a valid wake-driven poller, drops the real approval future in preflight/Assembly/Journal-without-receipt, and requires an unchanged actor frontier plus exact Store/ingress/DocumentWrite handback. It then drops at the exact acknowledged-receipt cutover and requires the autonomous owner to survive one checkpoint refusal, publish, reconcile, release the writer, and expose the single actor mutation. That new native law has not run, so the autonomous driver and recovery additions remain unqualified.

The SQLite-only test build no longer depends on native-artifact WAL fixtures: the command decoder constructs its neutral envelope locally, while committed-witness reconciliation remains compiled and executed only with `native-artifact-execution`. This preserves native proof without forcing the full Stdio/GIS feature graph into unrelated directory laws.

## 2026-09-07 Hub ingress and terminal teardown

The production approval handler now constructs a private `HubGisMapApprovalIngressAuthorityV1`. It owns the four exact `SocketBindingGatesV1` guards in the shared total order `User -> Session -> DirectorySpaceAuthority -> Membership`. It resolves the bearer before admission, then reauthenticates the exact session generation/expiry and requires the current `Author` membership while those guards are held. It deliberately does not acquire `DocumentWrite`; the retained runtime remains the sole owner of that later gate and therefore cannot be entered through a Hub lock inversion.

The handler passes that owner through `InferenceApprovalRouteContextV1`. A successful lower result is not published immediately: the Hub reauthenticates the same session and rereads the Author role under the still-owned guards before serializing the JSON receipt. A dropped request releases these request-local guards, while the post-receipt runtime driver retains only immutable WAL provenance, the three Stores, and DocumentWrite through checkpoint publication/reconciliation.

Production startup retains a second `Arc` to the inference runtime outside `HubState`. When the server terminates it awaits `HubInferenceRuntimeV1::close()` before shutting down artifact maintenance and before local DB/process owners leave scope. Close failure is a typed `HubError::InferenceShutdown`, not a swallowed teardown result.

The language-neutral fixture now pins `{owner: hub-private-sorted-binding-guards, bindings: [user, session, space-authority, membership], documentWrite: runtime-acquired, cutover: durable-wal-receipt, delivery: fresh-revalidation-under-same-guards}`. Its source gate was observed RED before the production wrapper (`missing HubGisMapApprovalIngressAuthorityV1`) and is now GREEN with `hostile=13`. Targeted Rust formatting is GREEN. The registered bin native cohort adds `gis_map_approval_ingress_holds_sorted_hub_authority_without_outer_document_write`: it holds and probes all four exact Hub gates, proves DocumentWrite is still immediately acquirable, revalidates delivery, releases the authority, performs a real durable role demotion, and requires fresh admission to return `Denied`. This law is registered with `sqlite,test-support,native-artifact-execution` and has not run; no native ingress or teardown claim is made yet.
