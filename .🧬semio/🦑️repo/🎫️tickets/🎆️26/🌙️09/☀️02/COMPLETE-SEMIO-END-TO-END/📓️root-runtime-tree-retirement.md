# Runtime Tree Retirement

## Current Qualification

Registered source27403 and corrected repeat19045 reached the intended missing-runtime-mount RED. After mounting, source3299 is GREEN and native **77519 GREEN3**. Receipt `🗑️generated/runtime-tree-retirement-exact/exact-cargo-laws-c4oABu/00`; executable SHA-256 `495dd86a233d575909ac8b35a3e69fe3d7a456652d84fa5a6f5df77b223f138f`. Each exact law passed once: 18 component variants × three grants with node/tree/held occupied sources preserved and exact bytes; 384-page partial owner survived both take/close and queued maintenance before full re-admission; saturated cleanup admission preserved exact typed source/fault until one bounded handback slot became available. The independent seven-transition Buffer ownership/handback model, real zero-grant no-op, seven hostile schema mutations and shared384-page/nine-field contract oracle also pass.

## Boundary

Replace only `SurfaceTreeRetireCursor` with one retained `BuiltTreeRetirement`, transferring through concrete node/tree/held source slots after checking the owner is empty. Drive an existing owner before taking another source. Public cancellation/Drop must preserve the entire owner in the existing reconciliation handback registry; no additional queue, global drain or generic callback is allowed.

Some rejected states have no handback reservation. Their public close must obtain an existing bounded nonblocking reservation before taking source; full/busy admission preserves the exact source. Queued maintenance already has an external slot reservation and must not reacquire the mutex while it drives the retained state.

## Limits And Residual Work

The old eager4097-slot stack is replaced by the contract owner's fixed384-slot stack allocated only for an actual tree. The current usage census does not charge either traversal allocation; this change cannot claim full resident-byte accounting. Contract allocation cost and typed grants remain independently bounded.

The native result proves the direct runtime cursor integration and existing handback path only. It does not fix `ComponentTreeProducer` partial frames, ordinary traversal iterator final release, `FlatPresentedNode` retirement, or successful `build_record_owned` disposal of rejected children. Those exact ownership paths require subsequent integration and tests. See the independent `📓️terra-surface-tree-retirement-owner-audit.md`.

## Registration

Existing runtime package script/project owns `tree-retirement-check`; launches408.28/408.29 select source and native respectively. The private hub dialect oracle order moved from duplicate411.1095 to411.1091. Provider owns generation/freshness for these entries.
