# Retained GIS Browser Qualification

## Required Boundary

The existing native GIS describe gate produces an independent component and closed actor. It cannot qualify the actor bytes selected by a separately materialized/published trusted generation. The next bounded gate must materialize once, run the mandatory exact native cold-map laws, publish that candidate, then read and verify that same current's component, descriptor and closed actor before executing the existing dedicated-child Chromium describe law. It must neither rebuild the actor nor accept arbitrary client-selected asset paths.

The shared browser proof will preserve real body hash/length validation, exclusive source/result transfer, stalled-body cancellation, descriptor normalization/equality, child capacity retirement and browser/server teardown. Retained current and all generation bytes must still match afterward. This is a genuine retained-actor qualification only; authenticated worker, cold-pair application, Shell rendering, Map mutation and two-peer collaboration remain explicitly outside this intermediate proof.

Closed six-row language-neutral schema/fixture is prepared under trusted-catalog/retained-gis-browser. Source test and independent equality oracle are next, before extracting the shared browser proof or adding the registered browser execution mode. No new native/browser run has started; GIS retains the next native slot while Home's backend source change is in flight.

## Implemented Boundary and Source Evidence

The shared `proveBrowserActorGisChildV1` now consumes an already-owned actor byte buffer, exact actor SHA/length and component/descriptor parents. It reuses the actual dedicated-child load/invoke flow, stalled-body abort, exclusive source/result transfers, normalized guest descriptor comparison, zero child capacity, and browser/server teardown. The standalone producer gate calls this same helper and retains its own fresh receipt separately.

`proveTrustedGisClosedActorV1` reads only the literal GIS leaves of the exact published current, validates the full generation closure (including actor policy/import metadata and both parent digests), invokes the shared child proof without building assets, then revalidates the same current and full generation. The new `--browser` mode follows shared native cold-map qualification and candidate publication in the ticket-isolated data root. Browser evidence is not a new publication invariant.

Registered target: `os-hub:trusted-stdio-gis-bundle-browser-check`; launcher order411.109616 beside native/process targets.

- Source session51830: intended RED at the missing retained-browser helper, after prior publication20 traces passed.
- Source session73212: parser RED because insertion selected the source oracle's quoted marker rather than the real function marker. Corrected by anchoring the actual newline; no runtime attempt occurred.
- Source session84587: GREEN complete trusted-bundle source suite, including AJV1/equality6/node+WebCrypto SHA and7 hostile source mutations for this fixture. These are neutral/source checks, not live replacement or browser evidence.
- Source session34994: standalone GIS describe regression GREEN (AJV1, TypeScript1, independent equality9, normalization9, existing physical retained-input closure checks and8 lightweight subprocess laws).
- Registry generation session23846: GREEN59 plugins/60 playgrounds/45 framework packages and launcher generation. This is generation/freshness work, not full taxonomy or59 assets qualification.
- Scoped whitespace check: GREEN.

Terra independently found no current identity P0 in the retained-browser helper. Full metadata is validated by the generation fence even though the child needs only execution bytes and their exact identity. Teardown calls still depend on normal browser/server close completion; no leak was demonstrated, and no live stalled-child proof is claimed yet.

No actual materializer/native cold-map/Chromium run has started for this new mode. Full authenticated worker, cold pair, Shell renderer, UI Map mutation and two-peer acceptance remain unproved.

Registry freshness session74958 passed after generation, and the later full source session12221 also passed with the new collaboration specification consistency check. Read-only preflight found the configured Chromium1234 executable present and rustc1.99.0-nightly (c4af71034, 2026-07-06) with wasm32-unknown-unknown/wasip1/wasip2 targets installed. These checks do not launch a browser or build assets. Installed Vite `createServerCloseFn` destroys its tracked open sockets before closing the HTTP listener; the current helper still awaits browser close before reaching Vite close, so the bounded teardown limitation remains.
