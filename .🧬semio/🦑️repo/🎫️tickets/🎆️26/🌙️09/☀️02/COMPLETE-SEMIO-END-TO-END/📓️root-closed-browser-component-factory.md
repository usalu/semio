# Closed Browser Component Factory

## Current Boundary

The GIS executor audit requires a separately attested, closed browser actor bundle. This first implementation is its build-time component factory substrate, not the actor bundle, descriptor/catalog admission, private Shell handoff or live GIS rendering.

Installed JCO1.27 exposes an `instantiation: async` mode that places component state inside its `instantiate` function. A first in-memory Bun probe18495 produced JCO output but exited1 because a JCO dependency uses Node's `process.binding("tcp_wrap")`. The Node probe27858 exited0 and exposed the exact generator shape. Permanent qualification now runs JCO under Node through the existing bounded captured-process runner; Bun remains the package manager and Nx the task runner.

## Implemented Substrate

`🔌️plugin/🌐️browser-bundle/📜️script.ts` owns the build-time API and neutral test. It accepts a JCO instantiation source plus an exact closed set of core bytes, validates bounds and literal core selectors, removes only the recognized default fetch resolver, and rejects extra declarations, imports, dynamic executable loading, network loader references and missing/duplicate/extra core assets.

The emitted private `instantiateFreshComponent` contains immutable chunked core bytes. Every invocation creates fresh JCO state and Wasm instances. It decodes in bounded chunks, reports progress, checks cancellation before/after compilation, zeroes staging arrays and drains all started core promises before returning or throwing. It exports no URL or code-selection capability itself. This structural gate is not a sandbox proof for arbitrary JavaScript; the intended input is the trusted build-time JCO output.

## Qualification

- Initial registered20313 was infrastructureRED on an incorrect relative import; corrected.
- Registered1442 was the expected test-first RED: real JCO output reached the not-yet-defined factory.
- Registered48948 GREEN: strict AJV fixture, genuine JCO component extraction, two independent instances sharing one generated module, native WebAssembly independent oracle matching `[1,2,1,3,2]`, eight hostile closure denials and precompile cancellation. Evidence: `🗑️generated/browser-component-factory/browser-component-factory-Pir6iv`.
- Stronger rerun90548 GREEN: explicit measured compile/instantiate counters show zero/zero before compilation and one/zero when cancelled during compilation; staging bytes are observed zeroed. Build pre-cancellation also executes zero progress callbacks. Evidence: `🗑️generated/browser-component-factory/browser-component-factory-ZqFzIR`, reported eight denials and three cancellation checks. This is Node-hosted genuine JCO/Wasm execution, not a browser Worker or GIS actor run.

The permanent target is `@semio-tech/framework-os-dev:closed-browser-component-factory-check`; launch seed411.082 supplies ticket-generated output ownership. Normal registry generation/freshness is assigned to the Home lane.

## Next Integration

Create an actor-local first-party host shim and a fresh-instance WASI import factory, combine them with this component factory and the current bridge API, then materialize and attest one browser bundle. Extend the descriptor/trusted-catalog/open-plan/lease schema and exact protected asset route. Only after private broker-to-Shell activation and canonical typed command publication should `renderer-unavailable` be replaced. The genuine `patchPositions`→Hub acknowledgment→close/reopen law remains required.

## Current Actor Host Foundation

Registered21907 was the expected RED for an explicit async input: the original wrapper omitted that modifier. Actual installed JCO output in retained evidence is a plain `export function instantiate` using a generator/promise runner, so requiring an async modifier would reject valid current output. The implementation now preserves the supplied form, rejects generator declarations/unexpected modifiers, reparses the reconstructed body and rejects residual `import.meta`. Registered90807 GREEN exercised both forms as real Node-hosted Wasm instances plus nine closure denials; evidence `browser-component-factory-aOTVjD`.

The new first-party runtime `🌐️browser-bundle/🌐️host/🟦️.ts` owns identity, sequence, pending requests and streams inside one activation closure. It exposes typed host ports and per-activation pure/host-async imports plus exact reply settlement and explicit close. Request and stream capacities are each128; sequence does not wrap. Close linearizes before cancellation, rejects pending requests, retires even unconsumed streams, and prevents future dispatch. Generation-qualified request IDs cannot settle another activation's table. Dispatch failures remove the exact pending entry.

Registered68069 was the intentional missing-runtime RED. Registered50581 GREEN reported AJV=1 and10 actor-host lifecycle laws, followed by the genuine JCO counter/native-WebAssembly oracle (two instances, nine denials, three cancellation checks). Evidence `browser-component-factory-hUmzoN`. Host lifecycle observations are Bun-hosted; the counter oracle is Node-hosted. Neither proves canonical async result lowering, a WASI actor adapter, browser Worker execution, protected bundle integrity or GIS publication. The new host has not replaced the production materializer.

Follow-on source now requires explicit tagged host results (an error pack cannot become an implicit JCO success) and synchronous queued/closed cancellation admission instead of awaiting a potentially unbounded relay acknowledgment. Reader retirement still awaits owned stream cancellation. These changes and strict import-interface admission are under active qualification; a genuine canonical JCO import fixture is assigned to the Home execution lane.

Registered82442 passed the expanded12 host laws, then failed as expected on absent import-interface admission. Registered98920 GREEN qualified the tagged-result host changes and exact build-time interface manifest: only literal `imports[canonical-versioned-interface]` accesses are allowed, the caller's admitted set must match exactly, and the emitted factory validates exact own runtime import records before starting compilation. It also passed all11 hostile source/core cases, both function forms and the independent Wasm counter oracle. Evidence `browser-component-factory-GtcHCj`. The positive canonical imported actor case remains assigned separately; this receipt does not substitute for it.

Registered39132 GREEN additionally runs a strict standalone TypeScript program over the first-party host runtime (standard ECMAScript/DOM libraries, no ambient dependency types) and proves runtime extra-interface rejection starts zero compiles/instances. Host12 and counter/oracle/11 denials remain GREEN; evidence `browser-component-factory-nljvaK`. The initial execution-lane actor-import fixture uses retained `semio:jcoprobe` slowEcho/fetchBody adapters. Review found that its tagged results are tested only in JavaScript while Wasm sees numeric/plain-stream adapters. It is therefore not integrated as production result-ABI qualification; the lane is advancing to a genuine tagged-result component boundary.

The next neutral actor-factory fixture uses a genuine `semio:framework/pure@1.0.0` component import through a small core Wasm module. Registered78881 reached genuine JCO output and failed as expected because `closedBrowserActorBundle` was not implemented. The new build-time function now embeds the admitted component and first-party host into one ESM via Bun, rejects any residual module graph, and exposes only `activate`. Each activation owns a bounded invoke/close/reply interface instead of leaking a raw component. Unknown WASI interfaces fail closed pending the fresh Preview2 adapter. Registered20507 is active; there is no actor-bundle runtime verdict yet.

Registered20507 RED exposed a real generator contract: default JCO removes the version from its emitted import key. Exact versioned `map` entries preserve that key without loosening the manifest. Registered3647 GREEN then ran two activations from the same closed ESM through the actual canonical pure import: `[13,14,22,15,23]` matched an independently instantiated native Wasm oracle; closing A rejected its next call while B returned24. It reported six actor laws and110311 source bytes, evidence `browser-actor-factory-LpIjh2`; host12/strict TypeScript plus counter/11 denials/three cancellation checks also passed (`browser-component-factory-gc6DCr`). This is Node-hosted closed actor execution with a pure import, not a Browser Worker, async-result actor, WASI adapter or attested GIS target. Close/error/abort retention remains under a fresh read-only audit.

## Actor Retirement And Actual JCO Result Convention

Terra's lifecycle audit found that host-close rejection skipped active invocation draining and that external abort closed only the host. Root added language-neutral actor phase/count traces and actual pure-import reentrant/abort/capacity laws. Registered38857 was the expected missing-`progress()` RED. The wrapper now reserves an invocation before guest entry, fences queued entry after close, owns abort at actor level, and always drains admitted calls and clears the component in `finally` even when host close fails. Its read-only bounded `progress()` exposes only phase and active count. Close remains idempotent and preserves host retirement errors. Noncooperative execution still requires Worker termination; this wrapper is not that bound.

The execution lane's genuine canonical host-async component exposed JCO's actual result lowering: successful host promises must contain the WIT payload, and rejected promises contain the WIT error bytes. The tagged transport envelope must not be returned as the payload. Registered97948 was the expected envelope-versus-payload RED. `resolveEffect` still validates an explicit tagged envelope, while the first-party host's imported request functions now resolve `ok.val` or reject `err.val` directly. No versionless alias or compatibility adapter was introduced.

Registered46728 GREEN: strict TypeScript and12 host laws;10 actual pure-import closed-actor laws with native Wasm oracle (`browser-actor-factory-BPbXFE`,111075 bytes); component oracle,11 hostile cases and three cancellation checks (`browser-component-factory-ejCqAp`). The actor observations include admitted reentrant close, abort-to-terminal without caller close, and32 queued invocations cancelled before zero guest entries. Genuine pending async guest/error retirement and WASI imports remain separately unqualified; the standalone async guest is under the execution lane, not included in this receipt.

## Bounded Stream Admission And Early Return

Registered94773 was the expected RED: with one materialized stream and127 outstanding stream requests, a129th resource request still dispatched. The host now reserves stream capacity before dispatch and releases each pending reservation exactly once on success/error/close/dispatch failure. It cannot accept a stream and then send an unowned fire-and-forget cancellation merely because its table is full. Buffered stream copies share a1MiB activation budget; yielded stream chunks and their backing arrays are bounded to64KiB. Returning or throwing into an unused generator explicitly retires its reservation and bytes, without depending on a generator `finally` that never started. Active iterator retirement also drains cancellation/completion together.

Registered74711 GREEN: strict standalone TypeScript,16 host laws (including reservation, aggregate bytes, unused return, oversized backing denial),10 actual pure-import actor laws, and the independent component/native-Wasm oracle with11 denials and three cancellation cases. Evidence: `browser-actor-factory-FPTMXi` (112731 bytes) and `browser-component-factory-DxcWKr`. Actual WHATWG ReadableStream and Array.fromAsync provide the independent stream behavior in the host trace. Worker isolation and noncooperative cancellation remain separate requirements.

The execution lane's separate raw canonical async fixture is now GREEN at `browser-actor-import-exact/actor-import-tQ9aw0`: two genuine Wasm guests round-trip successful/error packs and consume successful/error canonical byte streams. It carries14 WASI imports in addition to host-async; current closed actor rejection of those unsupported imports is also executed. The raw runner's vendor Preview2 oracle is not a production import implementation or a closed-actor async receipt.
