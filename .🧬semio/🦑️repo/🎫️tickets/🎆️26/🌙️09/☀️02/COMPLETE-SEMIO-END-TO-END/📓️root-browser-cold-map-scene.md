# Browser Cold Map Scene Activation

## Reproduced Boundary

The production browser document actor verified and transferred the full cold pair, then only reconciled patches returned by the terminal page. It never submitted the actual WIT `surface-visible` event. The genuine GIS component law explicitly requests that event before expecting its lazy `tiled-map` patch; the reactor source marks the surface dirty on that event and reports pending cooperative reconciliation as `more-work`.

Expanded the existing closed cold-pair neutral corpus/schema with initial render ordering, the verified window-kind address, a 256-turn bound, two lazy wake turns, ACK/rejection ordering, and expected revision/node kind. The actual worker-class test now uses a lazy protocol child instead of a child that unrealistically renders on the final load page. AJV validates the corpus, and independent existing `fast-json-patch` derives the final revision/node-kind view compared with the actual UiDocumentStore.

The registered `@semio-tech/framework-os:cold-document-pair-browser-check` reproduced behavioral RED in session 42262 (exit 1): after all cold pages were Applied, actual render events were empty instead of `surface-visible, wake, wake, patch-ack, patch-rejected`. The failing law ran in 506 ms; two sibling lifecycle/patch-owner laws passed. This is a real TypeScript worker-path result with a mocked child, not genuine GIS WASM or browser rendering acceptance.

## Production Repair

After exact Applied and any existing patch feedback, the owned reservation now requests its verified lifetime/window-kind surface, reconciles acknowledged/rejected patches through the existing receipt path, and submits Wake while the reactor reports more work. Every turn retains current-owner checks and exact cold-ingress validation. The drive is capped at 256 child turns; existing child invocation deadlines, cancellation, and reservation expiry remain in force. It does not hardcode a GIS surface or add another restore path.

Native GIS, actual Shell DOM/canvas, second-peer visibility, mutation ingress, and external effect/timer servicing are not qualified by this slice. The separate audit identified missing live-checkpoint notification after GIS approval; the GIS execution owner is taking that follow-on boundary. The full goal and ticket remain active.

## Browser Worker Qualification

After the repair, the registered focused worker cohort passed all three selected tests in session 86218. The neutral corpus and existing worker law were then expanded with five hostile render boundaries. The verbose run in session 62975 also passed all three selected tests (281 unrelated tests skipped); its runtime diagnostics were inspected: 64 exact pages / 4,194,304 bytes, lifecycle ACK, Applied, `surface-visible,wake,wake,patch-ack,patch-rejected`, one accepted tiled-map revision and exact rejection of the stale-base patch. Installing the identical owner again produces no extra render events. The actual UiDocumentStore revision/node kind match the independent fast-json-patch oracle.

Hostile runtime diagnostics: continuously MoreWork terminates at exactly 256 turns; stale ownership before the first turn issues zero calls; ownership lost after one reply issues no follow-up; unexpected cold ingress and a faulted turn stop after one call each. The child-worker mock is explicitly a protocol test double, not genuine GIS WASM. Teardown restores zero actor/byte reservation capacity. Scoped formatting and whitespace checks passed. The existing target and launch entry already cover the expanded test, so no new executable registration was required.

The auditor corrected the separate two-peer acceptance wording: the currently sanctioned AI approval creates an inference region, not a positions mutation. Peer qualification must assert that exact created region and frontier in both Map scenes. The genuine component's separate `patchPositions` law must not be mislabeled as the AI approval workflow.
