# Real GIS Closed-Child Describe Qualification

## Boundary

The new registered gate is `os-hub:browser-actor-gis-describe-check`, with `--source` (default) and `--native`. Launch seed orders411.1095051/411.1095052 and generated launch entries are current (registry97658/21396 GREEN).

This isolates fresh GIS component→closed browser actor→real dedicated Chromium Worker→`describe.describe`. It does not start Hub or establish authentication, rendering, Map mutations, storage, undo, MCP or collaboration. GIS itself depends on Stdio's full artifact catalog, so avoiding the Hub binary does not avoid that expensive WASM dependency.

## Source Contract

The new first-party helper under `browser-bundle/🧾️describe` requires canonical guest/staged Pack, exactly three runtime hash fields set to empty strings, exactly three final staged SHA-256 fields, and full canonical byte equality after blanking only those staged hashes. It rejects oversized descriptors before activation using the child result cap minus8 bytes framing.

Nine language-neutral cases include guest/staged trailing bytes, invalid staged hash, nonempty runtime hashes, metadata/extra-field changes and exact output capacity. AJV validates the corpus, strict TypeScript checks the helper, and the existing fast-deep-equal development library independently computes each expected metadata relation. No new runtime dependency was introduced.

## Receipts

-48191 expected TDD RED: helper absent.
-80573 GREEN7 neutral cases.
-80469 GREEN7 with strict TypeScript.
-54845 GREEN7 including independent semantic oracle.
-18036 GREEN9 including staged-side corruption.
-73010 native run was intentionally cancelled early via SIGINT to its verified owned Bun process65103 so the next fresh run contains the local transport deadline and stalled-body retirement law. It ended `fresh component cancelled at build`, not a GIS runtime failure.
-38704 terminal BUILD RED: Cargo101 before actor derivation/Chromium. Complete Nx receipt `🗑️generated/nx-cache-root/terminalOutputs/566036778679722794` has ~13k compiler-warning lines but no Rust error diagnostic; the inherited producer output is insufficient. The gate's exact fresh work/stage/Vite directory was removed in finally; static source/fixtures and Nx log remain. Next: bounded structured Cargo capture in the fresh producer, then a new fresh retry.
-Root format check6382 found formatting differences; scoped format97577/check7468 GREEN. Post-format GIS source11217 GREEN9. Registry generation97658/check21396 GREEN.

## Native Ownership

The gate consumes `produceFreshComponentV1`'s private lease exactly once to derive the closed actor; no caller component path or peer artifact is accepted. It checks actor/component/staged-descriptor SHA and lengths, then serves only those derived actor bytes on a fixed local test route.

Chromium first reserves a real child and aborts a deliberately stalled local body under250ms, requiring returned capacity without browser teardown. It then verifies exact local response headers/length/SHA under a10s deadline, transfers the source once, invokes the actual two-level describe export, requires one detached result transfer, normalizes the returned Pack against the staged receipt and closes to capacity0/0. The local route is a test transport, not the protected Hub actor route.

Owned source/guest/actor/descriptor buffers are wiped as applicable. The current native run removes only its own validated mkdtemp build/stage/Vite directory after terminal cleanup. The prior cancelled run predates that cleanup and remains under ticket generated output for eventual ticket cleanup. No peer cache or input fixture is removed.

## Pending Integration

Backbone's shared reader is GREEN7 with16 production ownership cases (87087), and the broader browser document-open regression42724 is GREEN6 and private reservations are GREEN16+7. Production Session-gated actor activation is still the next implementation slice. The helper is not yet connected to a production lease/Session, and renderer-unavailable remains truthful.

## Structured-Capture Retry

The fresh producer now has eight real subprocess laws qualified (45976), including cancellation, deadline, spawn failure, exact transcript retention, and64MiB aggregate output retirement. Source/normalization remains GREEN9. Native retry65957 is BUILD ACTIVE with retained Cargo JSON/stderr/outcome under `🗑️generated/gis-child-real/fresh-process-HwPaH5`, outside gate work cleanup. Earlier38704 remains BUILD RED with no established compiler cause. See `📓️root-fresh-component-process-diagnostics.md`.
