# Retained Host Lifecycle Authority

The active end-to-end goal remains unqualified. This packet extends the typed guest fault transport into a real native scheduler-held input. Browser retry and cold document loading remain separate integration work.

## Contract

Retry is authorized only by the exact bounded typed `plugin.reactor-turn-deadline` fault and the original single lifecycle event. The same event, registration identity, grant budget and byte credit must survive. Later work for that actor cannot overtake it; one eligible peer at the same priority can run between attempts. Cancellation and unregister revoke the original registration, never a successor.

The implementation will retain retries in the existing fixed pending lane rings rather than introduce a second uncharged queue. All deferred authorities capture the admission-time registration identity. Native laws will cover exact order, input equality, grant equality, same-ID reuse, cancellation and capacity.

## Current Native Runs

- Lifecycle session 7493, `reactor-lifecycle-native/exact-cargo-laws-vaDvmp/00`: BUILD ACTIVE; 20 laws registered, no verdict yet. Root temporarily owns Home’s `public-member-open-sol-target` cache.
- Typed fault session 61794, `guest-fault-native/exact-cargo-laws-GFajeA/00`: BUILD ACTIVE; two native transport laws have not run.

## Model Guidance

OpenAI Docs was used only to check the requested model names. Official [Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol) and [Terra](https://developers.openai.com/api/docs/models/gpt-5.6-terra) pages were fetched; both list `xhigh`. The app’s exposed tool metadata additionally accepts `ultra` for Sol. Existing fleet assignments are retained: two Sol execution agents and one Terra read-only auditor. No claim is made that a tool changed the main task’s model.
