# Retained Host Lifecycle Authority

The active end-to-end goal remains unqualified. This packet extends the typed guest fault transport into a real native scheduler-held input. Browser retry and cold document loading remain separate integration work.

Latest terminal root verification: 24 exact native actor/host laws passed in `guest-fault-native/exact-cargo-laws-8Xhogo`: actor executable SHA-256 `e2603588a27946264a83e1e92e50ca1d89f24a121b17456b625b78b82f567fb5`, host `ec3abbcc1ca58a73ee6585b9c821264f05bb4f10166e58a727a4112473a3fe0d`. The two actor laws cover six neutral reservation traces; the 22 host laws cover concrete activation failure/registration ownership, both reply-wake lock boundaries, routing/concurrency and retained lifecycle input. No root native test process remains running at this checkpoint.

## Contract

Retry is authorized only by the exact bounded typed `plugin.reactor-turn-deadline` fault and the original single lifecycle event. The same event, registration identity, grant budget and byte credit must survive. Later work for that actor cannot overtake it; one eligible peer at the same priority can run between attempts. Cancellation and unregister revoke the original registration, never a successor.

The implementation retains retries in the existing fixed pending lane rings. All deferred authorities capture the admission-time registration identity, budget and byte charge. Native laws cover exact order, input equality, grant equality, already-admitted stale ownership, cancellation and capacity. This does not yet fence delayed transport frames after a raw ID is reused; the Terra identity audit records the wire-key repair.

## Current Native Runs

- Lifecycle `reactor-lifecycle-native/exact-cargo-laws-vaDvmp/00`: terminal GREEN build (22m24s) and all 20 exact law receipts status 0, independently read on September 7. The native physical-close, publication-only continuation, submit-refusal, receipt and lifecycle laws all ran. Root released Home’s cache after confirming no process held it.
- Typed fault `guest-fault-native/exact-cargo-laws-GFajeA/00`: terminal BUILD RED; two private `semio_framework::dsl` references and one `Vec`/`FixedCommandPage` test mismatch. These references were corrected, but no native transport law ran in this capture.
- Retained-input source check session 45646: intentional RED at missing native retained authority module, after AJV and four independent Lodash ordering traces agreed. Native integration is being implemented; this is not a runtime pass.

The prior goal turn is classified as progress: it changed typed transport and native retained-authority code and produced compiler evidence. On continuation, old tool session handles were missing; current process state and terminal receipts were checked instead of assuming they were still live.

## September 7 Host Verification and Follow-Up

- `guest-fault-native/exact-cargo-laws-c3Atb1/00`: terminal GREEN, eight exact native laws, executable SHA-256 `629d78cc795bacd9c9183e145affc914dac482b82d0e225b9fae449e7e0e05e1`. This supersedes build-only failures `pZFWgc`, `a7M94r` and `cTpk9Y`; those were cold-status type/test-constructor integration and an old test-call signature, not passing native runs.
- The native input adapters split cold pages into the dedicated WIT/owned input and preserve typed ingress results. They compiled in the eight-law host build, but converter-specific and actual Wasmtime cold-component execution are not yet proved by those laws.
- Executor registration now acknowledges physical admission, returns incoming owners on refusal, and retains cancelled receivers in the original fixed ring. Shutdown-without-a-later-tick cleanup and activation-future cancellation remain open.
- Source session 42859 was intentionally RED at the missing shared activation module. Source session 1661 is GREEN: 10 fault cases, four retained ordering traces and five neutral activation cases. Shared `activation::install_actor` now validates the assigned shard before instantiation and retires concrete failure paths for both native headless and WGPU facades. Native five-case execution is still pending.
- Home reported a separate terminal GREEN cold ingress run `cold-document-pair-ingress-exact/exact-cargo-laws-E6PBfP`: three actor laws and seven plugin laws. These are not evidence of a complete browser or native-host journey.

## Expanded Native Gate

`Ksl824/00` built successfully and passed the exact registration-owner and unknown-actor tests, including runtime `[DEBUG]` traces: three registration paths retain/return the owner and release queue credit, and 256 unknown grants allocate zero bookkeeping entries and call no guest. The next converter test failed in its neutral fixture reader because actor lifetimes use Rust snake-case serialization; the reader was corrected to construct the semantic lifetime explicitly.

`4Dj9OW/00` then reached the intended semantic RED: the native WIT converter accepted an empty cold-pair fault that the canonical actor status rejects. The converter now invokes canonical `ColdPairIngressStatus::validate` before returning. A fresh 13-law run is required; the two earlier partial runs do not qualify the five activation cases or full cold-input conversion.

Scoped Rustfmt and Prettier completed, and `git diff --check` returned status 0 for the modified native host and both activation facade scopes. These are syntax/format checks, not native runtime evidence.

## Thirteen-Law Terminal Result

`guest-fault-native/exact-cargo-laws-enqoWg/00` is terminal GREEN: all 13 exact native laws ran once and passed. Executable SHA-256: `8c4adc700a8781d2b7d250c0288c199d7a9168795f4517abf71013c9d36b4ff8`. Captured `[DEBUG]` output independently confirms all five activation rows, three registration paths, 256 rejected unknown actors, six cold status variants, five hostile authority cases, three malformed hash lengths, dedicated cold-page input/duplicate/length rejection, and the retained retry laws.

This qualifies the shared native handoff's concrete error returns and the WIT conversion functions, not cancellation of that handoff, native component execution, complete frontend rendering, or either outer facade crate's full integration build. An inline-wake registration law is being added for the audited terminal reply mutex boundary.

## Terminal Reply Lock Regression

`Sp8mOG/00` reached native RED when an inline oneshot wake observed the executor state lock still held. Terminal refusal now removes one owner under the lock, releases the lock before sending, and restores an unclaimed owner with its exact original byte charge. `BXpjYL/00` was a compile-only failure in that change (`OwnerKey` was mistaken for byte charge); charge is now measured under the lock during the pop. `Zc4H75/00` built and passed all 14 exact laws, including `[DEBUG] registration terminal reply inline-wake=1 mutex-free=1 exact-owner=1`.

That fourteen-law run is terminal. Generated receipts remain under this active ticket; they must be removed only when the ticket is genuinely finished. No modifying Git operation or ticket/goal completion was performed.

## Next Integration Boundaries

1. Replace the facade's borrowed Kernel access with an owned Kernel coordinator and a bounded command queue. Keep the activation operation, cancellation flag, guest cleanup and Kernel retirement under one owner. A `WorkerPoolUse` must fence shutdown until the close cursor is terminal. A guest-only detached cleanup task cannot safely deactivate a Kernel still exposed by `kernel()` / `kernel_mut()`.
2. Carry the physical native allocation nonce across Grant, Envelope and Unregister and bind it only after actual registration. The existing local nonce rejects already-admitted stale work but does not reject delayed raw transport bytes after an ID is reused. Checked Kernel ordinal/generation exhaustion and inactive-before-bind scheduling are also required.
3. Implement the native WIT UI-patch inverse. Current Wasmtime output still discards emitted and returned patches even though the guest now emits the agreed node-ID operations. Preserve one-patch capacity, exact patch receipts, bounded payload decoding and owner-bearing failure; verify real component rendering rather than only adapter conversion. Terra is exploring this boundary read-only.
4. Home continues browser cold transfer and actual loading. WGPU/Hub execution continues committed-event recovery, retained document-write ownership, exact receipt-local journal verification, checkpoint frontier publication and applied acknowledgement. The production GIS committer must not claim an applied mutation before that publication acknowledgement.

## Kernel Reservation Foundation

The current continuation is progress: a language-neutral reservation schema and AJV/Lodash oracle drive two real native Kernel tests. `Dt0329` passed two actor and fourteen host laws. `vJfeCI` repeated the gate after adding mismatched guest-identity refusal; runtime output shows the exact incoming owner returned without consuming a registration number. Both native facades now reserve an inactive Kernel actor, instantiate on its assigned shard, then bind the allocation returned by physical registration. The pure Kernel is no longer clonable, so its in-flight reservation identity cannot be duplicated via a Kernel clone.

The six-trace expansion reproduced native RED in `sUWjrX/00`: `suspend` accepted an unbound reservation, permitting a later resume to bypass admission. Guards now reject reservation-current suspend/resume, completion, exclusive placement and extension linking. Physical bindings cannot be silently moved to another shard, and binding rejects an actor already quarantined. The follow-up `3rFsfj` gate passed all 16 selected actor/host laws, including runtime traces for bypass refusal and quarantined binding.

Bounded pre-bind mailbox buffering is intentional and tested; only dispatch is suppressed. `ActorShardKey` is trusted host data constructed from an actual registration result, not an external security capability. Delayed wire-frame validation is still separate work. Cancellation during genuine guest instantiation or queued registration remains open until the coordinator retains the operation independently of the facade caller. The `async` spelling of pure Kernel methods alone is not proof that they can return Pending; the audit traced the callees and confirmed current reserve/bind/abort complete in one poll. Only genuine guest and queued-registration suspension is treated as cancellation exposure.

Repo MCP resources and ticket tools were rechecked and remain unavailable. The existing ticket stays active; no ticket operation is claimed.

## Unlocked Registration Poll and Final Native Gate

The normal admission wake test reproduced native RED in `rAJBd8/01`: its oneshot receiver observed the executor state mutex held. The executor now marks one explicit polling owner, takes the retained future out of the mutex, performs one poll while single-flight admission remains held, then restores the exact pending future or completed shard. `lCaAjx/01` passed this new wake test, then reached the next intended RED: a previously quarantined reservation was physically instantiated and panicked at bind. The host now preflights Activating status before instantiation and aborts that reservation without a guest allocation.

`MjZA3N` passed 17 exact laws after that repair. `8Xhogo` then passed all 24 selected laws after scoped formatting: two actor laws plus 22 host laws. Added regression coverage includes fixed stack authority, a real shared-pool guest turn, interactive-before-background ingress, 200 actors across four shards, 60 suspend/resume actors, 32 concurrent senders, and coalesced/stale-generation wake claims. Runtime output was independently read for six reservation traces, six activation cases, four exact registration-owner stages, unknown actor rejection, cold conversion and retained retries.

`hmUBsS` is a build-only failure, not a native test result: the current run package inherited the async-macro dependency without a corresponding workspace declaration. The exact existing macro package was registered in root Cargo.toml; subsequent native builds succeeded. No dependency package or version was added.

Scoped Rustfmt, Prettier and `git diff --check` completed successfully. The existing Nx guest-fault-check target and launch registration now run the expanded gate; no alternate permanent script was created.

The native coordinator and wire-incarnation fence remain open. Native WIT UI-patch output is still discarded. Home is repairing the corresponding worker-to-Shell one-owner patch/receipt path, and the Hub agent continues durable GIS checkpoint publication. No real browser GIS artifact or full public end-to-end journey is claimed. Terra's new audit identifies a public directory authorization-versus-demotion race; that security packet is the next root integration priority. Root has only inspected Hub sources so far, not modified them.

## Model Guidance

OpenAI Docs was used only to check the requested model names. Official [Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol) and [Terra](https://developers.openai.com/api/docs/models/gpt-5.6-terra) pages were fetched; both list `xhigh`. The app’s exposed tool metadata additionally accepts `ultra` for Sol. Existing fleet assignments are retained: two Sol execution agents and one Terra read-only auditor. No claim is made that a tool changed the main task’s model.
