# Flow Child Restore Authority

## Current Native Frontier

Canonical source **81193 RED →74681 GREEN**, then final integrated **76923 GREEN**. Fixed object arrays now sort lexically, ports are camelCase and nonempty in all five digest vectors, and absent neural trees emit the same explicit null as the typed DSL. Native10 **89964 is terminal RED at its first selected law**, receipt `🗑️generated/flow-child-identity-exact/exact-cargo-laws-ZFwhlb/00`: the actual executable panicked before identity assertions because temporary `ArtifactChild` handles dropped the final `Arc<FlowWorkingScene>` without retiring its strict ordered-map owner. Zero laws passed. The law now requires and extracts the sole derived local owner into `SceneRetirementFactory`; per-grant handles remove their shared owner before Drop while the exact outer root remains retained for final retirement. Registered source10659 is GREEN after this repair; native remains unverified. Exact byte/hash assertions are unchanged. This test repair does not fix production `FlowDiff::absorb`, which can replace a strict local child owner by assignment while generic `ArtifactChild` retirement covers only id/target; that remains a separate production RED. Initial-child source79263 and native91536 GREEN15 remain valid component evidence.

Current repeats advance that frontier without completing the ten-law packet. Repeat3 built executable SHA-256 `f30176038fe03078f5ed1aecfb579f819138c70352993ccd1dcc4a0c7096f192`; its first identity/adoption law passed exactly once, then the canonical artifact law failed on a final neural `Dictionary` dropped by its own test fixture. The helper's compile-time Arc mismatch was first repaired by passing the extracted exact `Arc<FlowWorkingScene>` directly to `SceneRetirementFactory`. Repeat4 built SHA-256 `e0d04aa303698868c98dc38146c0bb8b22d99d33090b7a4a622597637e2daa57`; law0 again passed, bounded Flow retirement closed every rich canonical fixture owner, and law1 then exposed `DuplicateWidget` as the sole snake_case `FromValue` payload among ten camelCase mutations. The schema and every neutral fixture now use camelCase with no compatibility alias. The strengthened source gate validates that payload with its existing JSON Schema through AJV, separately pins the `duplicateWidget` discriminator, and rejects the former snake_case shape; its final rerun is GREEN. Repeat5 passed the complete source/oracle phase but ended infrastructure RED during Stdio compilation on an in-flight GLTF `bind-default-scene` taxonomy move; no native assertion ran. Evidence roots are `exact-cargo-laws-H25TgI/00`, `exact-cargo-laws-shDxOY/00`, and `exact-cargo-laws-5bZ0qB/00`. A final repeat waits for the taxonomy and mounted public-member-open trees to settle.

Flow **60581 reached the actual test executable** and failed its first exact identity law at retained/artifact line293. Receipt directory: `🗑️generated/flow-child-identity-exact/exact-cargo-laws-GMefOT/00`. The typed DSL scene encoded sorted object keys and camelCase `inputPorts`/`outputPorts`; the old neutral vectors and retained borrowed encoder still specified declaration order and snake_case ports. No Flow law passed in this run. Repair must align all three representations and preserve exact bytes/hash/adopted-owner assertions; semantic-only comparison would hide the digest mismatch. The next seven laws were not run.

The separate exact UI tree-owner run **25981 passed four native laws**, documented in `📓️root-exact-built-tree-retirement.md`. Initial-child identity passed in91536 against independent BLAKE3: eight fixed vectors, 72 coordinate substitutions, 65 denials, three scope agreements and two full-width frames. Kernel executable SHA-256 `2d2ab46014e9143deaf435862a394d39f1f04f9ba37c88038d57efe34c37ae5b`, receipt `🗑️generated/member-dialect-exact/exact-cargo-laws-qGbJOZ/01`. Identity confers no creation authority. Root owns the shared Cargo target for89964; provider stages its factory-selection leaves without mounting them meanwhile.

## Scope

Native owner-fix repeat **33185 is infrastructure RED before Flow compilation**, receipt `🗑️generated/flow-child-identity-exact/exact-cargo-laws-clKpkc/00`: concurrent taxonomy temporarily moved the Stdio BMP artifact while its Rust barrel still named the old path. No assertion ran. The taxonomy owner completed the coherent `🖼️bmp` → `🪟️bmp` move across all Stdio mounts and a live census now finds zero old references. This does not supersede89964's actual ownership RED or verify its repair; another native repeat is required.

Follow-on to the thirteen-law native member gate in `📓️root-member-dialect-authority.md`. This packet connects real Flow parent fields to exact Stdio child identity. It does not yet establish public Flow app restore, complete multi-member batches, mounted viewer loading, or child lifecycle owner catalogs.

## Source Changes and Evidence

The content-identity neutral schema now declares the `content` slot, child/target id equality, and exact `s.stdio.semio@v1/flow` coordinate. Its five existing canonical JSON/SHA-256 vectors remain unchanged and are independently verified with Node crypto. AJV rejects both the static `flow-content` target and the former four-segment artifact-kind spelling.

Registered source run **42946** observed the expected RED on the actual constructor's static target. The ordinary digest constructor now sets target id to the complete child id. The retained preparation cursor builds both id strings through the same bounded digest spelling, preserving byte grants rather than cloning a completed string in one step. Both schema child annotations now use the canonical three-segment artifact kind. Editor and viewer hooks explicitly project their actual snapshot fields.

Registered source repeat **27908 passed**. This is source/oracle evidence, not native runtime evidence. Twenty before/after JSON mutation fixtures, the demo DSL target and the reused-owner fixture now name their existing exact child ids; no compatibility fallback or migration script was added. The demo's actual child payload completeness remains unverified.

## Native Verification

`@semio-tech/flow-plugin:child-identity-check` is registered through the package's `📜️script.ts`, project target and launch seed (408.25). It selects two exact native laws: the existing retained scene identity/adoption law, strengthened to compare full target identity; and a new actual editor/viewer projection law that rejects id, slot, kind, standard and subset substitutions. Snapshot roots in the latter retire through Flow's bounded snapshot owner.

Native run **90024** is active on the root's warm target with one build job and a twenty-minute build ceiling. Evidence root: `🗑️generated/flow-child-identity-exact/exact-cargo-laws-PjHr0n`. No native Flow assertion has passed yet. Registry generation **95372 passed**, including the provider's previously pending launch entries; generated-output consistency still needs its separate check.

Native **90024 ended RED before Flow compilation**: WGPU text includes for Noto Emoji shards 10 and 11 were one directory level short. Both exact existing font paths were inspected and corrected to match their sibling includes. This is build-path repair, not a Flow assertion result. The released root build slot goes to the provider's three retained member-open kernel laws before Flow repeats. Source **77478 passed** the expanded 21-asset AJV check, and registry consistency **71326 passed**.

## Durable Owner Catalog TDD

Source run **29235 failed as intended** because the actual Flow editor supplied no document owner catalog. The new language-neutral store-owner schema covers document/config/draft lanes, UTF-8 payloads of 8,192 bytes, and grants of 1/64/4,096 bytes. AJV validates the fixture, while Node Buffer independently checks TextEncoder byte counts and the page-retirement model.

Flow now supplies all six durable owner/disposer hooks. Document and config catalogs use their existing typed incremental retirement factories; only the zero-sized `NoDraft` and `NoDraftMutation` use the explicitly bounded generic catalog. The native fixture asserts both draft types are zero-sized. Existing preparation tests now call the same catalog constructors. Source repeat **33956 passed**; this is not native lifecycle acceptance.

A third exact native law exercises the actual `EditorApp<FlowPlayApp>` forwarding hooks for all three durable stores and all three grants, including a nested scene payload. Every close step is budget-checked and must reach terminal-empty. Native run **69720** is active; none of these Flow assertions has passed yet.

## Native Build Dependency Repair

Native **7989 ended RED before Flow compilation**, evidence `🗑️generated/flow-child-identity-exact/exact-cargo-laws-usXeA1/00`: the render crate referenced font assets removed by concurrent taxonomy work. The current font catalog maps each family, subset and regular-weight outline. Both native text consumers now resolve their thirty include paths against those actual catalog destinations. A read-only existence check confirmed all thirty files; no assets were recreated and no fallback path was added. Provider run **38156 passed all three retained member-open kernel laws** before releasing the warm target for root run 69720.

## Concurrent Cleanup Invalidated Native Evidence Paths

At approximately 15:38 local time on 2026-09-04, the separately requested `Clean workspace artifacts` task removed this ticket's `🗑️generated` directory (its recorded size was 186,497,446,531 bytes) and the repository target. This was deletion, not a taxonomy move. Its cleanup output listed different spellings of this ticket path as protected, not this exact active path. The cleanup task has been notified of the exact path and requested not to repeat cleanup here; no cleanup implementation changes were requested.

Root run **69720 reached Stdio compilation but no Flow assertion** before deletion. Its Cargo PID 11607 and exact compiler descendants 13343/13344 were terminated after confirming that the target and receipt paths had disappeared; unrelated builds were not touched. Treat this run as infrastructure-invalidated, not an assertion result. The durable reports retain previously observed pass/fail outcomes and hashes, but their old generated receipt links are now unavailable. The backend has recreated the generated folder for a new cold test run. Native checks must produce fresh receipts rather than claim that deleted artifacts remain available.

The presence-owner follow-on schema and independent Buffer/TextEncoder oracle were staged first. Source run **83358 failed as intended** on missing Flow presence owner hooks. Flow then gained a typed local/peer snapshot factory using shared `FlowRetirement` for its identifier vector. A new domain-neutral `PresenceStoreOwnedDisposer<P>` delegates to the existing retained store protocol, enforces per-step grants, and fences completion to the exact installed terminal pointer and generation. The domain explicitly supplies its empty terminal and predicate; no generic whole-payload drop is inferred.

Source repeat **27450 passed**. A fourth selected native Flow law covers three rosters (empty, Unicode unshared, Unicode shared) at grants 1/64/4096, preserving a captured roster until release, asserting exact retired bytes, rejecting publication after close, and checking sticky terminal completion. This is staged native evidence, not a pass. The corresponding 69720 parent Bun process remained active after its invalidated child build was terminated; exact parent PIDs 11604/11405 were subsequently terminated too. No unrelated process was stopped.

## Empty Transient Owner TDD

The neutral transient schema declares a four-step zero-payload lifecycle (zero item grant, zero byte grant, exact retirement, sticky completion) plus rejection of a foreign store and a reset terminal owner. Source **36598 failed as intended** on Flow's missing transient disposer hook. The framework now exports a narrowly typed `NoTransientStoreDisposer`, never a generic whole-store disposer: its payload and mutation are statically zero-sized/no-drop, and its store contains the exact empty root and scalar generation. A tuple-size equality additionally checks current layout without assuming summed field sizes or a stable `repr(Rust)` field order; it is not an unsafe layout conversion.

The adapter retires the old empty store in one item grant, retains a weak witness to the installed terminal, and rejects foreign/reset ownership instead of claiming terminal completion. Flow explicitly opts into that adapter. Source **17029 passed** before the added reset vector, and **68935 passed** with it; a fifth native Flow law consumes the trace, checks the original root is retained under zero grants, and closes all admitted terminal owners. Native verification remains unrun after the external cleanup.

## Shared Document Owner Extraction

Source **63394 failed as intended** because the reusable document owner catalog was absent at the artifact boundary. Flow now owns that catalog and its snapshot, scene, and mutation factories under `🗿️artifacts/🌊️flow/♻️retirement`. Only domain types are imported. Snapshot production ownership moved out of the editor; the existing editor-side file now contains the identity/lifecycle tests only. Scene and mutation factories are defined once, and their one-item root handoff delegates variable payloads to the existing shared `FlowRetirement` cursor.

The editor's retained preparation, scene copy/hash and command recipe remain editor-owned. They now import those exact domain factories. The old editor mutation-retirement match was removed; its queue delegates to the same domain mutation decomposition, so there is no duplicated ten-variant owner table. Source repeat **1254 passed**. The reusable modules use absolute crate domain imports to respect the package's component/re-export wiring.

## Viewer Owner Forwarding and Member Registration

Source **16264 failed as intended** on missing viewer document owners. `ArtifactViewer` now declares default-deny document/config catalogs and document/config/presence/transient disposers plus peer presence retirement. `ViewerApp` forwards every declaration, and owns its own statically empty `NoDraft` adapter (not an author-visible mutation capability). A narrowly typed framework `NoPresenceRetirementFactory` retires zero-payload local/peer roots; Flow's viewer explicitly pairs it with the shared presence close adapter.

Flow's viewer declares the same artifact-level document catalog as its editor, explicitly bounded empty config owners, and exact presence/transient close authority. `viewer_with_members<V, M>` now preserves the concrete member fleet in the runtime factory while requesting document Read only. The ordinary `viewer<V>` routes through the same implementation with `NoMembers`; Flow's actual runtime variant and registration both select `SemioMembers`.

Source **53150 passed** the owner-hook oracle. A sixth native law uses the actual plugin builder/factory with its required explicit test package id, checks its document rights against the neutral read-only schema, receives the typed Flow viewer enum, and drives the real VCS five lanes plus framework interaction to observed Complete/terminal-empty under fixed grants. This law is staged, not passed. The complete source repeat **16411 passed**; the provider owns the current native target for its repaired retained Flow decoder laws.

## Remaining Integration Work

Flow's new editor/viewer lifecycle catalogs and runtime member registration still need native verification. A public real-Flow/SemioMembers restore law must source an owned member, close the source app, open it through a fresh app's public boundary, and close all retained roots under bounded grants. Retained request ownership still has not replaced the legacy borrowed-pack `MemberFactory::open` path. These are unaccepted work, not reasons to relax the lifecycle invariants.

## Native Fixture Call-Site Readiness

The current public VCS `snapshot` and Flow `flow_content_snapshot_from_working` declarations are synchronous. Seven stale snapshot awaits and one stale content-construction await were removed from Flow's existing composed-app fixture and duplicate-widget tests after validating their concrete `FlowApp` type. Independent `serde_json::Value` oracle parsing remains unchanged. Source repeat **69307 passed**; these repairs have not yet been compiled by the pending six-law native gate.

## Actual Package Surface Closure

The isolated owner laws did not exercise the editor's full evaluation/interaction instance. A new neutral surface-owner contract requires both actual package factories at grants 1/64/4096. AJV and stable-stringify validate its canonical contract plus four hostile variants. Source **76470 failed as intended** on the absent native law, then **62269 passed** after adding it. The seventh law calls the real `plugin()` assembly, consumes the complete two-role manifest, verifies each runtime enum variant, creates a fresh app per grant, and drives all app ownership stages to Complete/terminal-empty. This is still staged evidence, not a native pass.

The reviewer caught that VCS defines a zero-item close call as unchanged Pending even after termination. The fixture now checks that result, preserves the terminal witness, and separately checks repeat Complete under a positive grant. Source registration guards cover both concrete `SemioMembers` factory bindings. Provider native **86667 passed its two retained decoder component laws** and released the warm target; root native **84186** is active for all seven Flow laws. Neither run establishes retained public child-open integration.

## Live Composition Authority Remains a Separate Red Boundary

**D0 scope correction before native mount:** Terra proved that ArtifactRef/OwnerRef and CompositionGraph do not structurally include space identity. The initial-child identity now uses **nine** length-prefixed fields, beginning with authenticated `scope.spaceId`, rather than the eight-field primitive described below. Source27574 observed the intended missing-space RED; **73019 is GREEN with eight fixed vectors,71 denials and nine UTF-8 boundaries**, including equal raw document ids in two spaces yielding distinct full child digests. The unmounted Rust law now stages72 coordinate substitutions and65 native-representable denials against the independent dev-only `blake3` oracle. JS hashes remain first-party; Buffer/DataView independently verify framing. The future author-only request must obtain the space from authenticated DocumentScope and bind it into the private receipt; hashing a supplied string does not grant authority. No compatibility path was introduced because no native caller or persisted asset used this staged primitive.

Typed input hardening then reached intended RED53144 and GREEN59546: the private helper now takes the repository's `DocumentScope`, `ArtifactRef`, and `ArtifactDialect`, checks bounded document-id agreement before hashing, and maps those fields internally. Three neutral agreement cases are mirrored by AJV and the staged Rust law. A raw typed DocumentScope is still data, not authentication. The existing registered member-dialect native target now stages this one extra kernel law (four schema + six kernel + four plugin =14), while the prior13-law acceptance remains historical. Module mount and test-only `blake3` dependency are deferred until the running Flow60581 terminal; no new native result follows.

Exact built-tree retirement source is staged separately in `📓️root-exact-built-tree-retirement.md`: final UI source59295, launch generation20161 and freshness72802 are GREEN; four native laws are registered but unmounted/unrun. The running Flow60581 binary build remains on its original eight laws and currently has no compiler diagnostic or assertion terminal.

The initial-child identity Rust primitive is now staged **unmounted** under `store/🧩️composition/🌱️initial/🪪️identity/🦀️.rs`. Registered source28775 observed its missing-file binding RED; source92798 is GREEN after adding the bounded eight-field/ordinal implementation. Its native law consumes the existing seven neutral coordinates, compares all 56 field substitutions and two full-width frames with a separate `blake3` dev oracle, and denies 58 native-representable invalid inputs; it is **unrun**, and the planned test-only dependency/module mount is deferred while60581 compiles. The helper streams at most eight 256-byte fields into the existing first-party hash implementation, includes no mutable pack/checkpoint/surface input, and grants no creation, load or publication authority. Stable initial composition still needs the authenticated parent request, schema-owned seeds, fixed staging/abort ownership and private atomic receipt.

The tree observer has a separate confirmed lifecycle gap: real Flow TreeItem actions and node bindings can own UiValue maps, while the current helper only drains BuiltChildren. Terra's typed-retirement audit identifies the reusable contract-internal `UiTypedRetire`/`UiTypedRetirementCursor`, not a global UI-value drain. An exact BuiltTree retirement owner is being designed with fixed retained-page traversal, both ordinary/rejected children, and typed component/binding/menu cleanup. Text-only observer fixtures and the running eight-law gate do not close this newly identified boundary.

Newest terminal: metadata **45758 is GREEN**, zero compiler errors, with Cargo finishing in 12m59s and Nx exit 0. This supersedes the active status below. Native **60581 is now active** on the released shared provider target with one Cargo job, explicit in-process Nx graph discovery, and the unchanged one-hour build ceiling. It selects the seven existing lifecycle laws plus the distinct tree-observer law. Output is `🗑️generated/flow-child-identity-native-foundation.log`; no native Flow assertion is yet credited. Source **81249** passes the final tree oracle. Separate audit is checking binding observation and UiValue retirement beyond the helper's explicit BuiltChildren scope; neither is assumed closed by text-only tree fixtures.

The existing Flow broad-test route now supplies `RUST_TEST_THREADS=1` through the shared runner's existing environment parameter. This closes the no-nextest fallback's same-process global-retirement race without limiting nextest's parallel scheduler across isolated test processes. No local test-only lock, skipped test, new command, or global runner change was introduced. This route adjustment is source-qualified only until a broad execution is observed; the exact eight-law gate remains unchanged and isolated.

Provider foundation **23286 passed exactly five native laws** and released the shared target. Root metadata **45758 is active** against the now-mounted foundation and coherent MP3/mutation-source authority batch, with `NX_DAEMON=false NX_CACHE_PROJECT_GRAPH=false`, same target and one Cargo job. Output: `🗑️generated/flow-child-test-typecheck-foundation.log`. The flags bypassed a phantom duplicate renderer project: source54162 failed graph discovery for a non-existent U+FFFD path; filesystem census proved only canonical `📺️renderer`, and in-process uncached48318 reached the intended test assertion. No cache, directory, process, or canonical source was deleted. The naming fleet owns the graph-decoding diagnosis.

Terra's early-error audit exposed an insufficient tree cleanup bound: observation may reject a root before visiting its retained descendants. The shared observer now budgets the entire fixed authority, `384 * (32 + 1) = 12,672` node/page turns, not the old 8,192. Its new fixture pins a shallow saturated tree with 384 pages and 12,257 contained nodes; native source first counts its 12,641 close turns, then recreates it and requires early rejection to leave terminal-empty. Source48318 reached the intended missing-cap RED and **86066 passed** after repair, with all prior source fixtures; native remains unrun. The exact-one runner isolates this global-authority test. A broad single libtest process must run these tree users serially; separate per-test processes do not share this static. The registered broad runner selects nextest when available and falls back to cargo test, so qualification must record the actual runner. No local-only mutex was added to hide that shared scope.

Metadata **97196 ended RED before Flow** after MP3 source owners moved while Stdio was in flight. All four initial errors reference the pre-move mutation paths; the naming owner confirmed the complete production batch afterward. No speculative path or derive changes were applied. Provider now owns the shared target for foundation gate23286 (protocol2 plus kernel3); root has no concurrent Cargo process.

The queued render-observation repair is now source-staged. A shared `plugin::testkit::project_and_retire_fixture_tree` extracts the existing Norm pattern into one reusable helper returning a plain JSON string: explicit bounded keys/components/children walk, typed-component tag validation, duplicate/rejected-child denial, and retained-page drain after success, rejection or panic. Flow and Norm fixtures use the same helper. This is a test observation, not complete UI transport or an app-specific scene decoder. New strict neutral tree vectors cover two positive trees and two structural denials; source41522 first exposed a strict-required schema error, source13462 reached the intended missing-helper RED, and **2092 passed** after implementation, including three hostile schema contracts and all earlier Flow source rows. Scoped diff hygiene passed. The existing native gate now retains all **seven unchanged lifecycle laws plus one distinct tree-fixture observer law**; none has run in the current source state. Old main/compiled/preview scene assertions still need typed `semio_framework_ui_scene::decode` rather than obsolete substring/top-level JSON probes.

Metadata **47773 ended RED with only two private-field diagnostics** in the real duplicate-widget emission. Stdio's typed `InsertNode` and `InsertEdge` expose no public constructor; their fields remain crate-private. Each now has a simple owned `new` constructor, and Flow transfers its already-typed node/edge through those constructors without serializing, cloning, exposing fields, or changing mutation semantics. Repeat **97196 is active**, same metadata command/target/jobs1, output `🗑️generated/flow-child-test-typecheck-third.log`. Source-contract rerun **70449 passed** with all existing neutral/oracle rows; that is not native acceptance.

The test-only `ComponentTree.root` serialization change fixes its compile-time type but does **not** make old render tests runtime-correct: populated `BuiltChildren` intentionally reject whole-tree serde. Terra identified the existing Norm test's bounded explicit node projection and retirement drain as the proper reusable pattern. The render fixture needs that ownership-aware observation plus typed surface-payload assertions rather than old top-level node-graph JSON paths. This remains a separate queued repair; none of the seven lifecycle laws is being presented as rendering or collaboration acceptance.

Metadata **96604 ended RED at Flow with twelve diagnostics, four underlying causes**, after Stdio completed. The source fixes use the actual `ComponentTree.root` for test-only serde encoding, explicitly instantiate the default-capacity `UiFixedList<BuiltNode>`, distinguish the viewer's contract surface enum from its manifest enum, and borrow the retained `Arc<FlowSnapshot>` pointee when constructing `ArtifactView`. The generic `paired_apps` fixture call also now awaits its real asynchronous constructor. Repeat **47773 is active**, same registered metadata check, target and one Cargo job; output is `🗑️generated/flow-child-test-typecheck-second.log`. Neither metadata compilation nor these repairs establish native behavior, and old UI JSON assertions still require actual execution against the current tree shape.

Metadata check **90437 ended RED before Flow** on one concurrently moved Stdio WAV inference path: the physical directory is now `⏱️duration`, while the Rust package module referenced `⏱duration`. The exact existing file was verified and only that module reference repaired; the naming owner was notified. Repeat **96604 is active**, same registered command/target/jobs1, with output at `🗑️generated/flow-child-test-typecheck-repeat.log`. This failure does not reclassify the pending Flow repairs or lifecycle assertions.

The compiler-repair review corrected an over-broad helper conversion: VCS dispatch/render/window/context-menu/selection operations are asynchronous even when their entry is spelled `pub fn ... -> impl Future`. Their test helpers and downstream calls now retain explicit awaits; only pure fixture constructors, command rows and surface-value builders are synchronous. App factory creation remains async, while `AppActionRegistry::from_definition` and store snapshots are synchronous. Action arguments now use the actual `DslValue` boundary, and duplicate continuations retain the original typed effect arguments rather than converting them to a second JSON owner. The actual composed undo fixture drives typed dispatch/undo/redo directly instead of passing a `SemioMembers` app to the unrelated `NoMembers` helper. This is current source repair under metadata check90437, not acceptance of any of those historical behavior tests.

Root repeat **30082 was deliberately cancelled before Flow compilation** after concurrent dependency edits forced another full code-generation rebuild. Only its verified owned process group 92838 was terminated; Cargo, Bun and Nx PIDs 92838/92812/92620 exited, and unrelated builds were not stopped. Receipt `🗑️generated/flow-child-identity-exact/exact-cargo-laws-QtLLGH/00` is not a test result. The compiler repair loop now uses the already registered Flow `check --tests -j 1 --message-format=json` command on the same target to obtain metadata-only diagnostics before repeating the unchanged exact seven-law native gate. No test has been filtered out or relabelled as passed.

Native **36007 reached Flow lib-test compilation and failed with 211 compiler diagnostics**; Stdio and the preceding dependencies completed. No Flow executable/assertion was credited. The fresh receipt is `🗑️generated/flow-child-identity-exact/exact-cargo-laws-7xS1B3/00`. The dominant causes are stale async test helpers/call sites, obsolete module/import paths, dependency-only `cfg(test)` serialization assumptions, two UI label conversions and moved action-cohort fixtures. The scoped repair keeps genuinely asynchronous app/member creation awaited, makes purely synchronous helpers synchronous, consumes `MutationOutcome::diff()` explicitly, and uses first-party typed values with third-party neutral JSON encoding rather than adding runtime serde support. Repeat **30082 is active** on the same target and exact seven-law gate; no new feature or lifecycle acceptance follows until it finishes.

Separately, source **40255 passed** seven stable initial-child coordinate vectors, 64 denials and eight exact UTF-8 boundary encodings through the existing registered member-dialect source target. The schema/fixture/script live under store `🧩️composition/🌱️initial/🪪️identity`. The domain is `semio.initial-child.v1`, with eight length-prefixed UTF-8 coordinate fields, a bounded ordinal and full BLAKE3-256 output. Buffer/DataView and AJV independently check framing/admission; the hash currently has only the existing TypeScript implementation and its known-answer check. No Rust helper, independent native BLAKE3 parity, authenticated author-create request, receipt pack commitment, schema cardinality admission or viewer reopen behavior is implemented by this source packet. Terra's `📓️terra-initial-child-identity-audit.md` preserves those distinctions.

Native **84186 ended RED on the build ceiling**, not an assertion: the recorded Cargo status is null, signal SIGKILL, reason `timeout`, with zero captured compiler errors. The twenty-minute run passed the repaired UI dependencies but spent its remaining time compiling Stdio; no Flow executable was discovered and no Flow law ran. Its receipt remains at `🗑️generated/flow-child-identity-exact/exact-cargo-laws-UJh1NI/00`. The owned Cargo and Stdio compiler processes exited; the target is retained.

The Flow gate's build-only ceiling is now 3,600,000 ms, matching the already established retained decoder gate and accounting for the measured Stdio plus UI/Flow dependency closure. Exact discovery, per-law execution timeout, zero-ignored requirement, output bounds, process cleanup and all seven assertions are unchanged. A longer build allowance is not a runtime acceptance claim.

Source repeat **95611 passed**, and the scoped Flow/adapter/UI-font diff whitespace check exited zero. Native repeat **36007 is active** on the same absolute target with one Cargo job; its new receipt directory is `🗑️generated/flow-child-identity-exact/exact-cargo-laws-7xS1B3/00`. The provider's new SPR verifier remains unmounted during this run. No Flow native assertion has been credited yet.

The subsequent current-tree audit establishes that the seven focused laws cannot accept Flow persistence or editing. Public parent loading still parses complete pack/SPR inputs and resets the store without retained expected-root authority. Parent wire data omits the local scene owner; normal renderers and mutations use that owner instead of the actual child view. More fundamentally, composition indexes a stable child artifact identity while Flow's ordinary edits remint its content-derived id without publishing a new child, and duplicate-widget modifies the child while leaving that digest-derived parent id unchanged.

The long-term repair must use one stable, owner-stamped `SemioMembers::Flow` created through an initial parent-plus-ChildGenesis transaction. Semantic graph edits belong to that child; parent fields remain parent-owned. A content digest may identify a checkpoint, not impersonate a mutable member id. A render-only cache refill or rehash is explicitly not the accepted repair. The current tests establish exact local identity/catalog and retirement contracts only; their content-digest creation fixtures will need reshaping when stable member initialization lands. The next schema/protocol work must use the coordinator's actual genesis identity/admission and retained receipt lifecycle, not invent a global id or a parallel graph. Evidence: `📓️terra-flow-parent-document-pack-hydration-audit.md`.
