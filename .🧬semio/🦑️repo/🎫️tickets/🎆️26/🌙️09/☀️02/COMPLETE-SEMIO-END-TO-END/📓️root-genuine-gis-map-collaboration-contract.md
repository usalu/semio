# Genuine GIS Map Collaboration Contract

## Draft Acceptance Contract

The schema-first contract is at `🌎️hub/🗿️artifact-authority/🔏️trusted-catalog/🧪️fixtures/🤝️gis-map-collaboration`. It binds one published stdio+GIS generation to two distinct authenticated author Shells, actual cold-pair/guest application and scene ACK, owner-scoped inference cancellation and approval, ordered WAL/frontier/checkpoint/ledger publication, live two-peer rebootstrap and ordinary document undo.

This is a test specification, not runtime evidence. It has no synthetic passing actor, scene, event or checkpoint. The eventual process harness must collect its observations from the actual production paths; schema validation alone cannot qualify any of them. English and German peers explicitly exercise the UI without a default language.

A second Author may submit its own inference job, but cannot read, cancel or approve the first Author's private job. Both live sockets must precede the mutation. Catalog generation remains fixed while each subsequent document-opening Session/grant/pair receipt is fresh. The same public Map scene/frontier must be visible after approval and again after ordinary undo.

The contract preserves the exact server-stamped CreateRegion/inference-bounds boundary. A guest patchPositions test does not qualify AI approval. It also keeps all59 plugin assets, a real external model provider, and live PostgreSQL/Neo4j qualification separate from this representative journey.

No process runner is wired to this new specification yet. Home owns subsequent authenticated worker/Shell composition after its current admin lifecycle repair; root owns the prerequisite retained-generation native/browser gate.

## Specification Validation

Registered trusted-bundle source session12221 passed. AJV2020 validates the closed specification and rejects8 hostile contract changes;8 field/order comparisons agree with independent fast-deep-equal and the existing retained-browser/approval fixtures. This is specification consistency only, not an observed integration trace. No composed process harness is present yet.
