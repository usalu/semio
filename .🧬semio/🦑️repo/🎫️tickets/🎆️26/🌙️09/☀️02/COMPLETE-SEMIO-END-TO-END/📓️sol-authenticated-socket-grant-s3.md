# Authenticated SocketGrant S3 Implementation

Date: 2026-09-03  
Ticket: `26/09/02/COMPLETE-SEMIO-END-TO-END`  
Goal: `🎯r2603`

## Status

S3 is in progress and is not release-complete. The browser worker-private broker and admin browser relay boundaries are green. Native source is implemented, including ordinary-runner environment sanitization, but its compiled WGPU entry/runtime evidence remains externally compile-blocked. The launch seed/output no longer contains `S_USER`; the new admin and native-environment entries are present in the seed, but the latest output regeneration is blocked by unrelated plugin host discovery. The MCP and distinct local bridge carrier cutovers are source-complete and independently source-accepted, but remain uncompiled and without the actual-child document/reconnect runtime pass. Production hub mounts and first-party wire decoders are v1-only, and the deployed WGPU `🟨️frame-worker.js` is freshly generated without its former test-only legacy DirectoryClient carrier. Server-side admin correctness remains assigned separately, so release remains unclaimed.

## Browser Boundary

`bun nx run os-hub:browser-broker-check --skip-nx-cache` passed the relay oracle and five focused worker laws. The covered behavior includes the worker-private exact typed port protocol, one-use digest ratchet and explicit advance acknowledgement, every-generation expiry, capacity, replay, lost/uncertain response rebootstrap, cancel-after-send, duplicate initialization, redaction, raw-local spoof refusal, and a running hostile worker shard.

## Native Source Boundary

- `LocalHubCredential::read_inherited` seals Unix fd 3 with `FD_CLOEXEC` before reading; Windows duplicates it non-inheritable and closes descriptor 3. Fixed-format session/socket/actor validators operate exclusively on checked byte ranges, so hostile UTF-8 cannot split a scalar at protocol offsets. A drop guard zeroes partially or fully read framed bytes and the one-byte trailing probe on every exit. A second recursive drop guard zeroes every decoded `DslValue` string/key on both validation error and success, while the validated capability string allocation is moved, not copied, into the credential's zero-on-drop box. Focused laws assert exact decoded wipe counts, a two-byte scalar crossing offset 43 is Unauthorized without panic, partial allocation wipe is exactly 8 bytes, and complete/trailing raw cleanup is exactly body length plus one probe byte. Execution is awaiting an operable Nx preflight.
- The process-entry claim is a class-bound `OnceLock` containing the terminal success or failure. Concurrent callers initialize one read only, a later wrong-class caller is refused, and a failed read cannot retry descriptor ownership.
- WGPU claims the native credential before argument-driven plugin/renderer activation. The real direct binary probe creates a descendant and proves fd 3 is closed.
- `ArtifactHost` receives the credential and non-serializable `HubSocketGrantSource` before a document can dial. The runtime probe checks not-ready before injection, still-not-ready with only the credential, then ready only with both authorities.
- Native directory/document dials issue a fresh v1 receipt, offer exactly `semio.socket.v1` plus the grant, verify the selected protocol, send tag-7 `SocketHelloV1`, install the receipt actor, wait for its matching `Session` confirmation before stamping mutations, clear actor/confirmation/session color on every bootstrap or transport failure, and reissue on reconnect.
- WGPU native execution is a directly supervised built binary, not `cargo run`, and the source launch seed registers `os-hub:dev-secure-native`.
- The ordinary WGPU native runner removes every ambient user/origin/token/session/credential/bearer/capability/authorization/cookie environment carrier before direct binary launch while retaining an explicit benign sentinel. The native entry rejects any such carrier before fd-3 claim, argument parsing, plugin selection, smoke, scale, or renderer activation. Registered session `14071` executed a poisoned actual child through the same supervisor and passed the absence/preservation plus source-order laws.
- The process-backed document oracle requires three fresh grants/sockets/tag-7 hellos: its first mutation is queued before any `Welcome`/`Session`, it rejects a wrong first `Session` actor, keeps an explicit first-connection/pre-Session command counter at exactly zero, accepts exactly one actor-stamped mutation only after the second connection's matching `Session`, forces a close, then accepts a second mutation only after a third receipt/reconnect.

## Compile And Runtime Evidence

- Session `81668`: isolated ticket-local WGPU build reached two owned `E0382` diagnostics because `space_id` and `document_id` were moved into the grant closure before v1 URL construction. Both were fixed by constructing the credential-free URL before moving the scope into the protected issuer.
- Session `29625`: rerun exited 1, but warning volume truncated the actual diagnostic from the returned console; it is not counted as attributable evidence.
- Session `48990`: the rerun compiled the owned kernel/store changes without the former errors, then failed in an external B-rep fixture at `engine/🦀️.rs:1267,1271`, where one-field patterns matched the now two-field `Entity::Curve` and `Entity::Surface`. The coordinator authorized the compile-restoration-only change to `(_, _)`. The command was:

  `CARGO_TARGET_DIR='.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️02/COMPLETE-SEMIO-END-TO-END/🗑️generated/native-target' RUSTC_WRAPPER='' CARGO_BUILD_RUSTC_WRAPPER='' bun nx run @semio-tech/framework-renderer-wgpu:native-build --skip-nx-cache -- --scale`

  The temporary log used `tee` without `pipefail`, so the outer shell incorrectly returned zero although Cargo/Nx explicitly exited one. This session is recorded as red/external and is not green evidence. Subsequent logged commands use `set -o pipefail`.
- Session `14348`: the focused class-bound claim law terminated red after the 1,200,000 ms command budget. Its first diagnostic was external to S3: `E0277`, `ProgramContributionEntry: serde::Serialize` was not satisfied while compiling `semio-s-imperative`; nextest then reported the budget kill. No focused test executed, so no claim-law pass is claimed.
- Session `21032`: after replacing the stale serde serializer with the first-party `ToValue` JSON encoder, the first compile-restoration attempt failed because its module path was written as unavailable `dsl`; the path was corrected to the crate's existing `semio_framework_os_kernel::os_pack::json` namespace.
- Session `58806`: the resulting incremental focused claim-law rerun reached active stdio-plugin codegen, but a concurrent cleanup deleted its absolute `native-target` directory while rustc PID `57664` was still writing it. The session was terminated cleanly with exit 130 and is infrastructure-invalidated; it yields no compile or law result. Subsequent native work exclusively uses the unique absolute ticket path `🗑️generated/native-socket-grant-sol-target` and does not recreate the generic target.
- Session `17568`: focused claim-law cold rebuild terminated red after the 1,200,000 ms nextest-list budget; no focused law executed. Current unrelated stdio/B-rep edits produced 18 diagnostics. The exact first diagnostic recovered from the unique target fingerprint is `E0063`, missing field `tol` in `BrepVertex`, at `✏️s/🔌️plugins/🗄️stdio/🗜️artifacts/🧿️semio/🏅️standards/🔖️v1/🪆️subsets/✳️brep/🧬️schema/🔺️diff/🦀️.rs:764:8`. Later diagnostics include missing `tol`/`coedges`/`next_label`, a changed function arity, missing `Vec3::to_vec`, and inaccessible-field patterns. This external compile boundary is red; the 3.8 GiB unique target is preserved for the incremental rerun after its owning source is restored.
- Native actor-epoch source now clears the receipt actor, Session confirmation, and color in both `fail_artifact_bootstrap` and EOF/transport failure branches. Matching `Session` now immediately flushes the pre-Session outbox; no unrelated later trigger is required. Its focused law drives both failure branches, attaches real loopback WebSockets, observes no command before each connection's matching `Session`, observes one actor-stamped batch with each queued mutation exactly once after `Session`, confirms the outbox is empty, acknowledges the first batch, forces EOF, and repeats the no-send/one-send law with a newly issued reconnect actor. Execution is pending the current isolated compile.
- Plugin registry generation session `71854` passed: 59 plugin crates, 60 playgrounds, 45 framework packages, and regenerated `.vscode/launch.json`. Freshness session `90231` passed, and a source/generated census found no `S_USER` or `VITE_S_USER` in either launch file.
- The MCP source lane now removes the hub credential from `HubOptions`, argv, workspace state, and `DirectoryClient::set_token`; claims the class-bound fd-3 credential at process entry; authenticates the real `DirectoryClient`; injects both the credential and grant source into `ArtifactHost` before returning a hub workspace; uses the v1 directory grant/protocol/tag-7 path; and builds before directly launching `semio-os-mcp` rather than using a Cargo intermediary. The hub-owned actual-child oracle builds the MCP binary, creates an authorized hub space and document, delivers the framed credential on fd 3, waits for the authenticated workspace/directory and document sockets, drives initialize plus real artifact mutation/snapshot JSON-RPC operations, persists the head, forces reconnect, and requires a new one-use grant selector digest and sync-session identity while retaining the same authenticated actor. A poisoned parent environment is filtered before spawn while a benign sentinel is retained; the MCP child verifies this itself, then launches a descendant that must see neither fd 3 nor any protected environment carrier. Stdout is JSON-RPC only and all bounded diagnostics are scanned for credential/poison bytes. `os-hub:dev-secure-mcp` is registered in `project.json` and the source launch seed. Execution remains blocked before this process law by current Cargo-root drift.
- MCP HTTP/bridge source no longer parses `--token` or `--bridge-token-file`, retains no workspace/transport token string, and never places bridge authority in the URL, query, environment, file, or log. HTTP and WebSocket admission reference the sealed `LocalHubCredential`; `/bridge` is exact and query-free and requires the exact ordered `semio.mcp.bridge.v1, <proof>` subprotocol offer while selecting only the public protocol. The React `AgentBridge` uses the URL and protocol tuple separately and rejects poisoned Vite token configuration. This boundary is source-accepted but not yet runtime evidence.
- The Rust directory stream now terminally closes on cancellation while a grant/dial is pending. A late successful socket is explicitly closed instead of being installed or reconnected; the focused law observes exactly one close and a permanently closed stream. Execution is pending an available Nx/Cargo boundary.
- The MCP `NativeHubBindingDriver` now fences the narrower post-Hello/post-return race before socket installation: it captures a nonzero authenticated authority generation, rechecks both caller cancellation and the exact generation immediately before `complete_dial`, terminally closes the stream so the returned socket is explicitly closed once on either mismatch, and uses the returned stream turn as control flow. Reconnect dials refresh invalidated authority before opening rather than installing a socket under generation zero. The focused law observes exactly one close for both cancellation and authority turnover, an unchanged binding generation/state for the cancellation branch, and no HTTP refresh side effect. Nx session `83636` was a malformed focused invocation and reached neither Cargo nor the law. Corrected session `75137` reached the Rust runner but failed before compilation because the unrelated FEM workspace dependency chain points to missing `🧰️framework/🔨️modules/🔢️number/📦️packages/🦀️rust/Cargo.toml`; no driver-law pass is claimed.
- The first post-MCP launch regeneration attempt failed before writing output while concurrent repository taxonomy edits temporarily violated `packageSourceDispositions["root-pytest-config"]` and `["root-eslint-config"]`. A reread found both current validator-to-owner mappings exact, so no unrelated edit was made. Retry session `85235` passed with 59 plugin crates, 60 playgrounds, and 45 framework packages; `.vscode/launch.json` now contains the generated `os-hub:dev-secure-mcp` registration. `bun nx run @semio-tech/plugin-registry:check-generated --skip-nx-cache` then exited zero and reported the generated catalog/launch bytes fresh. A source/generated census remains empty for `S_USER` and `VITE_S_USER`.
- MCP check session `35998` exited 1 before Rust because its runner still referenced the old `fixtures/🧱️binary-gate.json` path after the fixture moved to verified `🧫️fixtures/🧱️binary-gate.json`. The runner was mechanically corrected. The immediate rerun (chunk `30e6e1`) again exited 1 before the target because Nx project configuration found concurrently missing unrelated `♻️mit-bestand/🧺️demonstrator/package.json` and plugin-store `package.json`; no Rust compile or law result is claimed.
- MCP check session `89617` reached a cold Rust compile and exited 1 on an external UI-contract source break: `📋️copy/🦀️.rs:236` includes nonexistent `../🧬️🧬️typed/🦀️.rs`. No MCP law executed.
- Focused credential/wipe session `54191` exited before Cargo because the current repository taxonomy validator says `generatorContracts["plugin-registry"].previewTarget` does not route exactly to `bun ./📜️script.ts preview-generated`. The source laws are present but no pass is claimed.
- The WGPU frame-worker owner resolves the verified physical `../../🧵️🚀️frame-worker/🟦️.ts` source and the current double-registry plugin catalog path. Its Bun browser build defines `import.meta.vitest` as `undefined`, excluding test-only module graphs from the deployed worker instead of shipping DirectoryClient tests and their former credential carrier. The generator/checker rejects `/directory/ws?token=`, the token field, and the bearer header copy, then requires production worker markers. Sessions `86269`, `43831`, and `60690` respectively exposed a transient duplicate Nx project, a stale plugin registry path, and the now-invalid assumption that the frame-only worker must contain directory protocol strings. Session `86739` then ran the Nx `generate-frame-worker` and `check-frame-worker` targets sequentially and exited zero. The shipped artifact is 965,257 bytes; an independent byte census found zero token/query/bearer/Hello carriers and found `semioWgpuWorkerBootstrap`, `duplicate-boot`, and `InteractiveWorkerScheduler`.
- Atomic source cutover deleted the tag-zero `ClientFrame::Hello` variant/encoder/decoder, document `/ws` and `/directory/ws` handlers/mounts, and bearer-actor server fallback. Hub commands now require the receipt actor, the first frame must be tag-7 `SocketHelloV1`, and malformed/tag-zero frames close unauthorized. The obsolete TypeScript bearer E2E scenario was removed and replaced by a decoder negative plus a real-hub 404 law for both deleted paths. Runtime documentation now names the v1 routes and frame. The shipped worker is fresh under session `86739`; the Rust server and decoder negatives remain uncompiled.
- Admin browser authority is now held only by a dedicated loopback relay. `os-hub:dev-secure-admin` obtains an `admin-relay` local credential, starts the relay, and opens an exact one-use fragment nonce. The SPA clears that fragment before exchanging it for an opaque host-only `HttpOnly; SameSite=Strict` cookie, uses same-origin cookie requests, and has no bearer/session storage path. The relay maintains the capability only in memory, exact-allowlists bounded admin routes, requires same-origin evidence for unsafe methods, clears authority on expiry/upstream 401/stop, and never forwards authority with static assets. Unsupported browser locales stop at an accessible bilingual EN/DE chooser; translation lookup has no default-language fallback. Registered session `42976` passed the runtime replay/expiry/raw-local/CSRF/redaction oracle and 10/10 focused Vitest laws, including serialized poll abort/no-successor behavior.
- Admin relay check sessions `62248`, `67578`, `80540`, `81819`, `5305`, `86841`, and `22018` progressively exposed one owned parser error, concurrent taxonomy fixture/config path drift, a missing exact styling alias, and the quick transform budget. The parser/config/alias defects were corrected and the focused gate was assigned its truthful `long` level before session `42976` passed. Plugin-registry regeneration session `65055` then exposed a stale current-directory import and was mechanically repaired; session `87007` remains red before writing because current package discovery finds zero host metadata rows. Therefore `.vscode/launch.json` is not claimed fresh for the two newly added seed entries.

No native runtime pass is claimed until the actual `semio-wgpu-native --socket-grant-probe` completes with the three-grant trace.

## Remaining Atomic S3 Work

1. Complete and record the isolated WGPU build, focused claim laws, descriptor descendant probe, and process-backed actor/reconnect probe.
2. Keep the freshly generated `🟨️frame-worker.js` owner/check target green while completing runtime caller proofs; do not reintroduce test-only graphs into the deployed worker.
3. Compile and execute the MCP cancellation, HTTP/bridge, and actual direct-child fd3/environment/JSON-RPC laws on stable source.
4. Preserve the accepted admin browser relay boundary while the separately owned server-side admin packet finishes bounded cross-backend connection projections, removes N+1 lookup, and supplies delayed/simultaneous/stale/auth plus localized lifecycle laws.
5. Compile and execute the landed raw legacy 404 and decoder-vector negatives; census all persisted/binding credential fields and remove any remaining active production carrier without adding aliases.

S3 must remain unclaimed while any caller, legacy route, tag-0 decoder, URL/argv carrier, or generated launch credential hint remains.
