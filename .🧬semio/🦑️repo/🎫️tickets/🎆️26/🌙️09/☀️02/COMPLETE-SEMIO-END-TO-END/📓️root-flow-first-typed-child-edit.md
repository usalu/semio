# Flow First Typed Child Edit

## Current verdict

The language-neutral source contract is GREEN. Native compilation and the exact dispatch law are still unverified while the shared Stdio dependency is blocked by concurrent glTF taxonomy drift.

This packet converts only the ordinary Flow `addWidget` command. It does not claim that the remaining Flow content mutations are identity-only, that child genesis/public reopening is complete, or that parent/member/graph publication is atomic.

## Contract

- The parent `content` coordinate remains unchanged.
- The handler reads the already-admitted `SemioFlowSnapshot` child.
- The Flow host result must be exactly one appended typed node with unchanged prior nodes and edges.
- The handler emits one `SemioFlowMutation::InsertNode` through `ChildEmit`, with no parent `FlowDiff`.
- Generated IDs are derived from the admitted child snapshot, so reconstructing the host for a second same-kind insert produces `note_3` after `note_2` instead of colliding.
- The fixture contains two independently derived node cases, one reconstructed-host repeat, six denial identities, and six hostile schema/semantic mutations.

## Evidence

The first source run reached the intended RED assertion because `addWidget` did not read the typed child. After the implementation, `@semio-tech/flow-plugin:test-source --skip-nx-cache` completed with exit 0 and printed:

`[DEBUG] Flow child add-widget contract: 2 typed node rows, 1 reconstructed-host repeat, 6 denials, 6 hostile fixture rejections; native dispatch remains separate`

The permanent exact gate is `@semio-tech/flow-plugin:child-edit-check`, registered in the project and launch seed. It selects only `add_widget_dispatches_one_typed_child_edit_without_repointing_parent_content`.

## Remaining

1. Reach the Flow crate after the concurrent Stdio/glTF dependency repair and fix any owned compiler diagnostics.
2. Run the exact native law into this ticket's generated evidence directory.
3. Regenerate `.vscode/launch.json` from the launch seed after the current concurrent registry/taxonomy edits stabilize.
4. Convert the other ordinary Flow child operations and remove the owner-bearing `content` and whole-`artifact` fields from durable `FlowDiff`.
