# VCS Native Openable Provider V1

## Boundary and ordering

This packet follows `📓️terra-multi-provider-verified-catalog-blueprint.md`. It begins with the guest-owned package identity and one canonical artifact identity. No VCS provider is linked to the hub before its descriptor, private factory receipt, verified selected-profile closure, and immutable catalog row are admitted. The existing stdio gate remains independent.

## First identity slice

- Strict neutral authority fixture: exact `vcs` / `semio:vcs`, `vcs.vcs` schema and artifact kind, `s.vcs.vcs@1/*` viewer/editor surfaces and isolated execution.
- Independent AJV oracle rejects eleven hostile identity substitutions, including viewer-window and renderer substitution. WebCrypto and Node SHA-256 agree on the pinned checked-in snapshot protocol bytes. These are protocol/source identity checks, not native codec execution or a fresh Wasm build receipt.
- The builder declares `.package_id("semio:vcs")`; the one `ArtifactKindSpec` factory now derives its id from `VCS_DOCUMENT_SCHEMA`. The two stale source-owned expectations are updated with no alias.
- A public-API integration law builds the real plugin and decodes bytes from the real guest descriptor function. It checks package, plugin, activation, artifact and both surface identities. Exact-one discovery/execution is registered through the existing VCS script and Nx project; the launch seed is updated.
- The law also compares the actual Io codec's nonzero pack-schema hash to the production record schema, and checks exact schema/extension identity. The checked-in protocol-file SHA-256 is a separate neutral-source digest, never substituted for that native record-schema hash.

## Evidence

- Registered oracle-first run: `bun nx run @semio-tech/vcs-plugin:native-openable-identity-check --skip-nx-cache -- --oracle-only`, session 57661, RED exit 1 after the independent positive=1 / hostile-denied=9 / protocol-webcrypto=1 oracle. It correctly exposed the missing guest package declaration and old artifact kind before their repair.
- Post-repair registered oracle session 79461 and the owner launch-generation invocation stop before target execution, exit 1: `generatorContracts["external-step-assets"].outputRoots` and `generatorContracts["wgpu-frame-worker"].inputPatterns` must be unique and lexically ordered. This is an external taxonomy-preflight RED, not a VCS law result. No alternate runner or handwritten generated launch was used.
- After the shared preflight recovered, registered post-repair oracle session 33518 is GREEN, exit 0: positive=1, hostile-denied=9, protocol-webcrypto=1.
- Owner generation `bun nx run @semio-tech/plugin-registry:generate --skip-nx-cache`, session 15145, is GREEN exit 0. The generated launch contains `⚖️gate🪪️native-openable-identity🌿️vcs`.
- Final neutral fixture owns the pinned protocol digest explicitly; its registered oracle rerun 86479 is GREEN, exit 0 with the same 1/9/1 counts.
- The descriptor-owned viewer role/window and isolated-Wasm target extension is GREEN in registered oracle session 41462, exit 0: positive=1, hostile-denied=11, protocol-webcrypto=1. The pending native law checks exact canonical roles, surface derivation and the actual viewer window; no target is published.
- Owner `check-generated`, session 2406, is GREEN exit 0; generated registry and launch bytes are fresh.
- Rust integration law and generated component/descriptor freshness remain pending.
- Full registered VCS gate 83982: RED exit 1/Cargo 101, exact selected=0, on the same stdio XML `✍️🔤️set-text` missing include as the provider gate. Its independent oracle passed again. The exact four stdio include edges were reconciled to current physical files before retry; no VCS Rust law is credited.
- Current-source registered oracle 60238: GREEN exit 0, positive=1 / hostile-denied=11 / protocol-webcrypto=1. The native guest-descriptor law remains pending; no new receipt or readiness is published by this terminal.
- Source integration correction after coordinator compilation of the same typestate builder: `package_id` is available only after `label` and `version`. The VCS call now follows those two calls. Earlier neutral oracle results did not prove Rust typestate compilation; the public native law remains the required proof.
- Post-builder-order registered oracle 99557: GREEN exit 0, positive=1 / hostile-denied=11 / protocol-webcrypto=1. This remains source identity evidence only.
- Future native execution now retains exactly one test executable from Cargo's structured compiler-artifact receipt, then executes that exact binary and requires a one-passed/zero-failed terminal. Compilation diagnostics preserve Cargo's status and first three errors. No first-found binary or second compilation can silently change the selected law. The native law has not yet exercised this runner.

## Explicit nonclaims

VCS identity execution now uses shared repo-library `runExactCargoLaws` for its one explicit integration-test target. The shared runner is GREEN in registered session 47607 (21 tests, 313 expectations), including real child process lifecycle evidence; the VCS Rust law itself is still pending. No duplicate private runner or temporary artifact fallback remains in the VCS command. See `📓️sol-shared-exact-cargo-law-runner.md`.

No VCS receipt/provider, selected-provider resolver, fresh raw/core/descriptor pair, immutable two-package bundle, hub readiness, client mount, or all-plugin activation is accepted by this identity slice. Checked-in generated descriptor JSON remains stale until the owned describe emitter succeeds; it is not authority.
