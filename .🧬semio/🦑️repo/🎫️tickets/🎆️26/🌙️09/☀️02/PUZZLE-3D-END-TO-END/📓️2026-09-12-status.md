# 🧩️ Puzzle 3d end to end — status (2026-09-12, claude-code session 8acfb894)

Companion to `📓️2026-09-11-claude-coordination.md` (chronological log). Numbers are from the release React app on
`http://127.0.0.1:6013/?plugin=puzzle3d`, driven by `🔍️browser-probe.ts --battery --reload-between-groups`.

## Scoreboard

| build | wasm contents | battery | faults |
|---|---|---|---|
| #43 (start, 2026-09-11 14:40) | previous session | 6 PASS / 8 FAIL of 14 steps | 16 (all `pre-admit the exact operation slot`) |
| #44 (B0) | slot admission | 6 / 9 + 70 new steps | 45 (`every slot already owns a live operation`) |
| #45b (B0+B2+B3+B8) | retirement | 40 / 40 | **0** |
| #47 (+B9/B12/B15/B16) | guest lanes | 46 / 34 | 0 |
| #48 (+B17/B21) | completion lane | 51 / 32 | 0 |
| #49 (+B20…B26) | surface identity, mesh, pacing | 53 / 35 | 0 |
| #51 (+B29/B30) | dock chrome, navbar id | 50 / 33 | 0 |
| #53 (+B31/B32b; host B33) | dispatch gate | **63 / 25** | 0 |
| #55 (+B34/B35/B36) | interaction scope, brush gate, settings capture | 61 / 34 (locale death) | 11 |
| #56 (+B38; host B39/B40/B41/B43) | segmented export, pane overlay, locale, ids, bridge unwrap | **64 / 27** (checkpoint, 1 744 s) | **0** |
| #57 (+B42; host B45) | fill cancel reaper, shard cancelJob reply, panel handle | **74 / 25** | **0** |
| #58 (+B44) | instance residency + delta lane, structural fingerprint | 73 / 25 | 0 |
| #59 (+B46) | GLB pick bounds, topology memo, outliner clamp | 67 / 32 (reactor livelock after first gesture — B48) | 0 |
| #60 (+B48) | reconcile grant release, selection lane, mesh loop | 64 / 33 (still livelocked: peer's ♻️reconcile ladder — B52) | 0 |
| #61 (+B52) | document retirement grant priced from the value, bounded stall | **71 / 25** | **0** |
| #61 + host B53/B55 (B55's run "#64") | Tailwind `@source` (z-index classes never emitted), export row filter, probe ordering | **89 / 10** | **0** |
| #62 (B54/B56/B57/B58 guest) | publication ladder slice, per-object invalidation, alias retired, reservation sizing, un-hide | pending | |

## Architecture changes landed (performance limits)

- Typed-operation admission: first vacant slot then a congruent id (B0); a single retirement site driven by the turn
  driver, the close ladder and maintenance (B8); mounted operations runnable until removed (B16); the completion
  carries its history patch (B21); latest-wins rebase keeps its key (B21).
- Retirement grants for every ladder (table ladder 1 092 → 5 turns) + host zero-progress fast-fail (B2); settle
  quiescence instead of the empty-required stop (B14: switch 86 s never landing → 5.6 s, warns 2 505 → 17).
- Guest command pacing: whole ingress ladder per turn + many typed-operation units per turn (52 → 4 turns per
  command, B24); brush meshes cross once per geometry, back-pressured (queue 162 → 2, click wait 44 s → 10 s, B22).
- Host refresh honours cached body hashes (projection 132 ms ×51 → 4 ms ×3, B4); completion-scoped refresh, no idle
  storm (B27); app-declared interaction scope (hover no longer repaints the shell, B34); dispatch gates actually gate
  (hover storm 72+85 queued turns → 3+3, B33).
- Fixed owners backed by ≤ 64 KiB sub-pages (B3); per-surface view state (B25); per-instance utility publication (B9);
  one document decoding for topology and render (B9).

## Windows and tools — user-facing state on #53 (fresh page unless noted)

| area | state |
|---|---|
| Top + Perspective windows, distinct cameras, orbit/pan/zoom, per-window camera | works |
| Window options (grid, snap, LOD, vortex show/direction, sun), projection | works (rail drafts B12, Tree fix B10) |
| Settings panel, steppers → focused pane | works (B10/B12/B15); value→rail in full run pending B36 |
| Example switch, undo/redo (chrome + document), history panel | works (B7/B21/B26/B30) |
| Selection → Inspection, lock flag row | works in fresh lanes (B9/B25); full-run pending B36 |
| Context menu (plugin rows, focus/zoom) | works (B11/B18/B35) |
| Outliner select / hide / show | works (B18/B20/B27) |
| Catalogue open / rows / add / drag-drop | works (B11/B27/B29); add does not select what it adds (B36) |
| Add Object dialog, dynamic kinds | works (B11/B26) |
| Duplicate / delete / focus | works (B31/B33/B34) |
| Gumball translate, Relocate utility | works (B5/B28/B34) — gumball intermittent at 30 s in the full run |
| Brush utility + preview, Volume Brush + target volume | arm works; preview lands with #54 (B35); add volume works (B33) |
| Fill tool (arm, apply, history) | works (previous session + B13) |
| Engagement command line (`fill n`, `brush`, `clear`, Escape) | works (B29/B31/B35); render after effect pending B37 |
| Export (`<example>.json` download), Import (same-file idempotent, distinct adds) | works in fresh lanes (B13/B26/B28/B29/B30) |
| Locale EN/DE (chrome flips, no default leak) | works (B13); probe control reachability pending B36 |
| Clipboard copy/cut/paste (`mod+c/x/v`) | works (B6/B21); full-run pending B36 |

## Landed since (host-live unless noted)

B36 settings capture + `addObjectKind` selects (guest), B37 exactly-one merged refresh follow-up, B38 segmented export
(guest) + probe recipes, B39 per-instance leftover overlay, B40 locale switch no longer hot-swaps every plugin,
B41 instance-qualified projection/measure ids + `data-published-value`, B43 bridge option unwrap + segmented-download
contract leaf. Peer-drift repairs by the coordinator: registry generator import depth, `@semio-tech/ui-react` package
entry re-export, element imports after the react target relocation.

Also landed: B42 shard `cancelJob` reply + granted fill-teardown reaper (guest #57), B45 panel resize-handle overlap +
probe ensurePanel/unfold repairs, B47 `uiIntentPayload` scalar eviction (Settings were mute) + probe fixes, B49 first Skip
unpressable (placement clamp, WindowChrome stacking) + dock-column reserve for the measures rail, B50 camera lane
diagnosis (reactor livelock). Coordinator peer-drift repairs: registry generator import depth, `@semio-tech/ui-react`
package entry, element imports, `wire-turn` relocation, styling vite builder test import.

B48 landed the reconcile-grant leak (one refused commit made a surface permanently un-reservable) and the mesh loop;
B51 swept 53 session `[DEBUG]` lines and the brush-preview flood (0 errors, renderer 1037/6 baseline).

B52 found the livelock was ours and pre-existing: the document retirement ladder was priced with a 4 096-byte grant
while it retires 6 416-byte records a page frees whole → leaf-starved forever; fixed with a value-priced grant and a
named stall fault (#61: camera, projection, settings→rail all PASS).

B53: the export lane was green — the press never scrolled to the row (recipe) + the watcher served every edited module
pre-edit (`rename`→`add`, fixed); B54: publication ladder 21 → 4 turns, per-object scene invalidation, history O(n²)
removed; B55: one Tailwind `@source` line restored every `z-*` class (context menu painted under the canvas);
B56: synthetic `1:window` alias retired, refusal reasons named — the real ceiling is three 8 MiB reconcile
reservations per 32 MiB registry against thirteen mounted surfaces.

B57: export/import chain, example switch and un-hide all green live (probe budgets + repeated fold press); B58: every
reconcile reservation was priced at the 8 MiB maximum → repriced from the tree (13 surfaces admit at 1.7 MB).

## Open (owned)

B59 large `importFixture` (141 KB) silently no-ops on the wire; #62 (B54/B56/B57/B58 guest) deploy + final battery
(chained: serve recycle for the watcher fix, materialize, battery); ticket close (delete tool outputs under both
`🗑️generated/` spellings, keep scripts and reports).

## Follow-ups outside this ticket

Pre-session `performInvocation` / `command ingress` console families (43 % of a tail) in PluginRuntime; the
framework's one-item publication grant (~100–235 host↔guest turns per mutation at 1 object); a guest pull lane for
large payloads (`http-request` mapping + AsyncTask); `🎨️r3f` vitest config cannot load (strip-only TS import).
