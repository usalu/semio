# ⏯️ Wave W3-F1: force-layout consumers (reasoning, dag, trinity)

Status: **in progress** (interim). Layout consumers reasoning, dag and trinity/jack are re-pointed and green;
trinity/jack `runQuery` and trinity/rewriting rule application are being converted.

Logs: `T/🗑️generated/W3-F1/`.
