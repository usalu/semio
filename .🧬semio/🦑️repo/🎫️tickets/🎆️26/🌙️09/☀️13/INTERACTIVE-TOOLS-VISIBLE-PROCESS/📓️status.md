# 🪣️ Interactive tools with visible process — status

Session start 2026-09-13 (Fable 5.1 main chat). Repo MCP failed to connect (`invalid initialize params`), so the
ticket folder is managed on disk; goal association `🎯r2603` mirrors the other September tickets.

## Goal (dev's words, condensed)

- Every tool is interactive; the user sees how the algorithm thinks (steps, percentage, status, intermediate results).
- Puzzle 3d fill: no hidden precompute-then-reveal. Count slider unbounded, arbitrarily settable, default 100. The UI
  builds the solution live: every tried object is shown (danger when colliding, highlight when collision free), locked
  objects appear as they are locked.

## Phase 1 — audits (Sonnet fleet, read-only)

| Report | Scope |
|---|---|
| `📓️audit-fill-pipeline.md` | FillBuilder stages, publications, job bridge, count apply, FILL_COUNT_MAX sites, RNG prefix property |
| `📓️audit-viewport-render-path.md` | Rust render → instances → r3f; per-instance color; brush ghost pattern; reveal cutoff |
| `📓️audit-progress-primitives.md` | WindowMeasure variants, jobs/ticks, other plugins' progress UX, schema-first gap |
| `📓️audit-tests-and-deploy.md` | fill tests, oracle convention, cargo commands, deploy chain to :6013, browser probe |
| `📓️audit-tool-inventory.md` | repo-wide tool classification A–E, top-10 conversion list |

Baseline `cargo check -p semio-s-artifact-puzzle-3d --features component-app-assembly` → `🗑️generated/baseline-check.txt`.

## Phase 2 — plan (this chat) → `📋️master-plan.md`

## Phase 3 — implementation waves (Opus fleet)

## Phase 2 — plan written: `📋️master-plan.md` (decisions §1, binding contract §2, waves §3, gates §4)

Audit takeaways: the planner already steps per candidate and publishes a ghost + counters, but (1) it always plans to a hidden
1000, (2) accepted pieces only become document objects at slider commit via a separate synchronous replan, (3) the ghost
never carries a collision verdict, (4) the framework has no unbounded numeric or progress measure.

## Phase 3 — implementation waves launched 2026-09-13 (Opus 5, parallel)

| Wave | Scope | Report |
|---|---|---|
| A | planner: requested-count target, verdict ring, stall reasons, preview wire + fixture/oracle | `📓️wave-A-planner.md` |
| B1 | session/job bridge: requested count to the worker, locked-chunk API, summary counters | `📓️wave-B1-session.md` |
| B2 | commands/config: setFillCount rewrite, tick commits locked pieces, default 100, ghost tail removed | `📓️wave-B2-commands.md` |
| C | viewport/UI: danger/highlight ghosts, tried ring, HUD, Number/Progress measures, reveal removal | `📓️wave-C-viewport.md` |
| D | puzzle 2d/5d parity: unbounded default 100, 5d progress/cancel | `📓️wave-D-2d-5d.md` |
| F | framework `WindowMeasure::{Number,Progress}` end to end | `📓️wave-F-framework-measures.md` |
| G | brush suggestions stream tested/free/blocked with ghost verdicts | `📓️wave-G-brush.md` |
| E | policy predicates in root `📜️script.ts` (after A/B/C) | `📓️wave-E-policy.md` |
| H | probe step, deploy, runtime evidence (after all) | `📓️wave-H-verification.md` |

## Phase 4 — reopened 2026-09-13 ~22:00 (Opus 5 session ⚪7c7fb230, repo MCP still down: `invalid initialize params`)

New dev requirements on top of phases 1–3:

1. Every tool has a lifecycle: **start → running → complete**, **abort** at any time, **finalize** only when complete.
2. A tool that mutates the artifact runs **inside a transaction**: provisional mutations are visible while running,
   abort rolls them back, finalize commits them as one unit.
3. Every tool shows progress (steps, percentage, status) — phase 3 already added `WindowMeasure::Progress`.
4. Puzzle 3d fill shows **all** tested meshes (not a ring of 12), colored: collision = danger, fitting = success.

State found: waves A, B1, B2, C, D, G reported; wave F code landed (`WindowMeasure::{Number,Progress}` in wgpu component,
projection, manifest, `🎚️measure-controls`) without a report; waves E and H never ran. B2's "tick commits locked pieces
straight into the document" contradicts requirement 2 and is re-planned in phase 4.

Phase 4a audits (Sonnet, read-only, parallel):

| Report | Scope |
|---|---|
| `📓️audit-p4-transaction-primitives.md` | artifact mutation/history/event-sourcing primitives usable for a tool transaction |
| `📓️audit-p4-tool-lifecycle.md` | tool/job/cancel/adopt primitives, wave F landed state, gap to start/abort/finalize |
| `📓️audit-p4-fill-state.md` | puzzle 3d fill after phase 3: compile/test state, tried-set rendering, commit path |
| `📓️audit-p4-tool-inventory.md` | every algorithmic/mutating tool repo-wide against the lifecycle + transaction contract |

Phase 4a landed (22:15–22:40): `📓️audit-p4-transaction-primitives.md` (no primitive aborts an open coalesced edit; `Emit::amend`/`Emit::commit`/`UtilityPreviewContract` closest; `StateClass` names all four state classes), `📓️audit-p4-tool-lifecycle.md` (`ToolDefinition` static, no run state; wave F React-complete, wgpu measure widgets have no caller; proposal `ToolRunState` + start/abort/finalize), `📓️audit-p4-tool-inventory.md` (34 plugins; puzzle 2d/3d fill commit per tick with no rollback; wave plan 0–3). Fill-state audit still running.

Phase 4b: Opus architect decides the transaction primitive and writes `📋️tool-run-contract.md` (lifecycle, transaction, visible-process rendering, disjoint implementation lanes).
- 23:0x fill-state audit landed → `📓️audit-p4-fill-state.md`: puzzle-3d crate 0 errors/96 warnings; fill tests 95/100 (4 real reds: `fill_job_identity()` None while pieces already locked → cancel vanishes; `fillBuildTick` per-tick document diff 5.09 ms > 2 ms budget); locking = committing every tick; tried ring 12 / 16 KiB full-snapshot wire cannot carry all tested candidates; `success` token exists but unused for fill. Architect told about it.
- 23:3x architect landed → `📋️tool-run-contract.md` (binding): framework-owned `ToolRunLedger` holding provisional `A::Mutation`s as an overlay the renderer reads; abort never touches the store; finalize (only from `complete`) rebases/revalidates and publishes ONE `Edit` (group `toolRun:<runId>`); delta trace pages into a `toolRunTrace` scene lane; generic reserved actions start/pause/resume/step/abort/finalize/dismiss; `Component::Progress`. Rejected `Emit::amend` (per-tick durable + full-snapshot broadcast).

## Phase 4c — wave 0 implementation (Opus 5)

Dependency-ordered: W0-A/C/F/G first (independent); W0-B, W0-D, W0-E and W1-A after W0-A (W0-D also after W0-C).
`.vscode/launch.json` registration is done by the coordinator at the integration gate from the lanes' reported commands.
- 23:4x dispatched wave 0a (Opus, rules `📋️lane-rules.md`): W0-A `⏯️tool-run` module → `📓️wave-W0-A.md`; W0-C `Component::Progress` + `WindowMeasure::Progress` removal → `📓️wave-W0-C.md`; W0-F presence tool-run summary → `📓️wave-W0-F.md`; W0-G wgpu measures home → `📓️wave-W0-G.md`. Queued on W0-A: W0-B, W0-E, W1-A; on W0-A + W0-C: W0-D.
- W0-F landed → `📓️wave-W0-F.md`: `PresencePeer.tool_run` (flag bit 10) Rust/TS/schema/fixtures, replication 273 pass, wasm32-wasip2 clean, hub vectors byte-exact via ticket probe; hub Rust test blocked by peer compile error `🌎️hub/🏗️bootstrap/🦀️.rs:31`. Open: W0-D fills `tool_run`; coordinator reruns hub test; local `PresenceToolRunState` duplicates `ToolRunState` names (layering).
- W0-C landed → `📓️wave-W0-C.md`: `Component::Progress` contract + wgpu reconcile/paint/a11y + React Interpreter (96/96), `WindowMeasure::Progress`/`MeasureProgressStep*` removed; puzzle 2d/3d/5d fill + 3d brush no longer compile (expected per §3.6). Pre-existing reds noted: 24 contract + 2 ui-runtime retirement close-loop asserts (container/text rows), 3 wgpu engine tests. Open: W0-B legacy `UiNode` TS `progress` arm; wgpu indeterminate sweep static.
- dispatched W0-C2 (Opus) → `📓️wave-W0-C2.md`: delete plugin progress measures so puzzle crates compile again for concurrent devs.
- W0-C2 landed → `📓️wave-W0-C2.md`: puzzle 3d/2d/5d compile again (96/4/1 warnings); cancel kept as Toggles (5d got a new `fill_cancel` label); 2d fill 5/5, 5d cancel 1/1, the two 3d cancel reds now pass. Pre-existing reds: two 5d "tool factory key already registered", one 3d brush precompute test. Open: React `🎚️window-measure-controls` test Progress fixture (W0-B); rerun `cargo check -p semio-framework-os-renderer-wgpu --tests`.
- W0-A landed → `📓️wave-W0-A.md`: crate `semio-framework-tool-run` 21/21, TS 18/18 (ajv/xstate/fast-check), wasm32-wasip2 + clippy clean, TS↔Rust byte parity. Deviations: reducer over `Option<ToolRunSlot>`, `AbortComplete` event, `toolRun.illegal`, delta `clear`/`next`, reserved reason codes 0xFF00–0xFF03. Depends on os-kernel + ui(wgpu) → ui/ui-scene/ui-contract/os-kernel/job must never depend on it (W0-E carries trace as opaque base64url). Owed launch.json: `@semio-tech/framework-tool-run-rs:test|check`.
- dispatched wave 0b: W0-B manifest+projection, W0-D runtime ledger+driver, W0-E trace lane+renderers, W1-A puzzle 3d fill run job.
- W0-B landed → `📓️wave-W0-B.md`: `run: Option<ToolRunDefinition>` on Tool/UtilityDefinition (crate `semio-framework`), `tool_run_action_definitions` injected only when `run` declared (builder line in P), chord law + fixture (4 Rust + 4 bun ajv), projection `ToolRun*` + `UiProgressNode`, generate→check green, React Progress measure removed (7/7, 9/9). Pause/resume share `mod+alt+enter` bound to pause (router toggles; W0-D told); dismiss has no app-wide key. Pre-existing reds: `semio-framework --lib` 12 (10 ui retirement asserts), os-mcp compile churn, verify interactivity (launch.json capacity/gate registrations, 757+ discovery, puzzle fill envelope). Owed launch.json: `@semio-tech/framework-rs:test-tool-run-actions`, `:check`, renderer react `test-long 🎚️window-measure-controls`.
- W0-E landed → `📓️wave-W0-E.md`: `toolRunTrace` lane on World3dScene (20 lanes) + Canvas2dScene (opaque base64url; `SceneLaneRef`/`scene_lane_hash` renames, 23 Canvas2dScene sites), base64url codec in `🚪️io/🔤️base64`, wgpu `♾️infinite/🌍️world/⏯️tool-run-trace` instanced layer, React 3d + 2d trace layers with verdict tokens/age fade/`data-tool-run-*`, `provisional` mesh style prop. scene 123, base64 6, wgpu trace 5, reconcile/wire 39, React 7 + Interpreter 97. Pre-existing: 4 wgpu image tests (global queue), frame-worker bundle regen owed. Legend/cursor echo/provisional set exposed, not wired.
- dispatched W1-C React host cleanup → `📓️wave-W1-C.md`.
- W0-D landed → `📓️wave-W0-D.md`: `ToolRunLedger<A>` + driver in `🔌️plugin/⏯️tool-run`, 7 actions framework-routed, render/engagements/measures read overlay, panel body, finalize = one grouped Edit + one command row + one Mutations batch; 10/10 tool_run tests incl. abort zero-trace, one-undo, rebase conflict, stale no-op, pause/step, resume/retract, 771-tick bench. Store edits: `begin_outbound_apply_batch` + `flush_published_apply_batch`, per-op mutation ids in batches. Driver steps the job inside `advance_typed_operation_publication` (not ActionBus). `ArtifactApp::build_tool_run_job` hook. Open: tick writer start offset (W0-A), scene lane + presence injection from `tool_run_trace_delta()`/`tool_run_presence()` not wired, per-tick whole-UI dirty. Pre-existing reds: store `artifact_store_batch_commit_refuses_…`, plugin transaction tests (`testkit-txn` app id).
- dispatched W0-H integration (scene lane + presence injection, tick writer offset, tick dirty scope) → `📓️wave-W0-H.md`.
- W1-C landed → `📓️wave-W1-C.md`: World3dHost fill code gone (tried ghosts, fillBuildPreview, caps, diagnostic overlay, tick loop), trace layer mounted per World3d window with `data-tool-run-*`; fixture `⏯️tool-run-trace-mount.json` + three.js oracle 1/1, World3d 42/42, engine-contract 605/605. Provisional style awaits a producer (`provisional: true` on instance records or `ToolRunProvisionalIdsContext`) — told W0-H. Leftover `fillBuildTick` refs in ShellHelpers/ShellHost/scene TS → W1-B. Owed launch.json: `bun ./📜️script.ts test long "🌐️World3dHost/🧪️tests"`.
- W1-D landed → `📓️wave-W1-D.md`: `⏯️ToolRunPolicy` (5 predicates, 28-row required-tool table) + `🪣️PuzzleFillToolRun` in root `📜️script.ts`, new `🔬️interactivity-puzzle-fill-run-job`/`-trace`, 110 self-test cases, 12/12 planted breaks caught. Expected reds: 28 missing definitions, 190 legacy-trace hits (puzzle fill), 4 amend hits (puzzle `fill-count` ×2, remodel per-tick reconstruction commits ×2 — remodel is not class A), 19+11 fill, 1 P4e. `verify interactivity` still aborts on pre-existing foreign self-tests `interactivityLiveReconcileSelfTests`/`interactivityMountedLayoutTextSelfTests`.
- W1-A landed → `📓️wave-W1-A.md`: `FillRunJob` (testing → one verdict per fuel unit, appendOps + entity per placement, CheckpointReady, stall warning → Complete, resume raise/lower) + `FillRevalidateJob`; 8 new tests pass at threads 4 and 1; `-- fill` 106/2 (the two §0.9 reds in W1-B's file); worst drive_step 1.106 ms / 2 270 turns Nakagin; parry3d 204 decisive / 0 disagreements; ≥5 000-candidate delivery complete; native + wasm32-wasip2 0 errors. Deviations: in-memory retarget on resume, typed placements to revalidate, caller-provided mesh lane order (W1-B must publish the same order). Tried ring + 16 KiB cap still referenced by main window / precompute / root script.
- dispatched W1-B puzzle 3d fill editor wiring → `📓️wave-W1-B.md`.
- dispatched W2-A puzzle 2d fill run → `📓️wave-W2-A.md`; W3-1 energy simulation run → `📓️wave-W3-1.md`. Held: W2-B (5d) and W2-C (brush) until W1-B lands (shared 3d session/precompute); remaining wave 3 lanes after W0-H lands (fleet size vs build starvation).
- W0-G landed → `📓️wave-W0-G.md`: measures ride the existing `framework.section.measures` retained surface (new `UiDocumentLease::read_paged_text`/`try_publish`), bridge reads them on both backends, Shell projects Number/Slider/Select/Toggle/Group into a right-aligned Measures overlay (keyboard `setFillCount` + pointer toggle tests), dead stub/field/129-line widget block deleted; contract 3/3, bridge+Shell 5, catalogue 5. Foreign: `mounted_layout` `Control` layout kind (controls had zero height). Unverified: wasm32-unknown-unknown blocked by 7 `InteractiveJob` Send errors in plugin crate from W0-D → handed to W0-H. Red to recheck: `dock_stack_content_fills_full_bounds_through_one_silhouette_clip` + ~10 shell/interpreter + 7 ui tests (no baseline).
- W0-H landed → `📓️wave-W0-H.md`: toolRunTrace lane in window scenes + cursor echo on React and wgpu (view-state map keyed by window instance id), presence summary on ephemeral frames (no host reader yet), `ToolRunTickWriter::with_provisional_base`, tick dirty scope = panel + scene windows, `provisional: true` instance flag (React; wgpu from full instancesJson), `ToolRunJob` = `Box<dyn InteractiveJob + Send>` → plugin crate compiles wasm32-unknown-unknown + wasip2. tool_run 15/15, tool-run 23/23 + TS 18, React trace 8/8. Unverified: renderer-wgpu check blocked by peer errors in `semio-s-artifact-puzzle-5d` (likely W1-B in flight).
- wave 3 brief written → `📋️wave-3-lane-brief.md`. Dispatched W3-2 procedural preview eval → `📓️wave-W3-2.md`, W3-4 remodel reconstruction → `📓️wave-W3-4.md`, W3-F shared force-layout run → `📓️wave-W3-F.md`. In flight now: W1-B, W2-A, W3-1, W3-2, W3-4, W3-F.
- 08:58 all six in-flight lanes (W1-B, W2-A, W3-1, W3-2, W3-4, W3-F) killed by the session rate limit mid-edit, no reports; resuming each via SendMessage with its transcript (dev: "Try again").
- W3-F landed → `📓️wave-W3-F.md`: crate `semio-framework-graph-layout-run` (`🕸️graph/⏯️layout-run`), `LayoutRunJob<E>` Barnes-Hut, 1 iteration/fuel, per-node displacement verdicts, 44-byte checkpoint, anchors for layered consumers; FR defaults match `fdg-sim` stress to 4 decimals (old `drawing::force` physics 2–10× worse); 5 000 nodes worst step 1.0–1.3 ms; Rust 13/13, TS 7; native/wasip2/clippy clean. wasm32-unknown-unknown fails inside `semio-framework-ui` (also tool-run) → integration. Open: ledger re-send flicker on huge graphs (W0-D), delete old sync force layout after re-pointing, puzzle 2d + trinity rewriting also consumers. Owed launch.json: `@semio-tech/framework-graph-layout-run-rs:test|check`.
- dispatched W3-F1 layout consumers reasoning + dag + trinity/jack → `📓️wave-W3-F1.md`.
- W1-B landed → `📓️wave-W1-B.md`: puzzle 3d fill = manifest-declared ToolRun (run + revalidate jobs built by framework), tick command / cancel toggle / preview JSON / tried ring / process-global fill session removed, Escape → `toolRunAbort`; `-- fill` 48/0 at threads 4 and 1 (all W1-F rows green, same 2 ms Nakagin budget), one-undo / byte-identical abort / raise-lower tests; 3d + 5d native + wasip2 check; W1-D probe clean for 3d fill. Deviations: count change = rebuild + replay to checkpoint (68-byte checkpoint), `setFillCount` view action. 5d places nothing until W2-B. Reds: 2 brush precompute (W2-C), 2 suggestions flaky in parallel. Six framework API gaps → W0-I.
- dispatched W0-I framework gap fixes, W2-B puzzle 5d, W2-C puzzle 3d brush suggestions.
- W2-A landed → `📓️wave-W2-A.md`: puzzle 2d fill = ToolRun (run + revalidate jobs), session commands/lifecycle/toggles/per-step commits deleted; 17/17 at threads 4 and 1; `geo` oracle 27 163 decisive / 0 disagreements; one undo; byte-identical abort; resume raise/lower; worst drive_step 1 141 µs Nakagin; native + wasip2; W1-D 0 findings for 2d. Deviations: fill count + offset moved to app config (request lacks window config), silent deterministic replay resume; fixed duplicate `import-media` factory + missing owners so 2d apps build. Examples unfillable as shipped (Concrete Forest no kind catalog, Nakagin no open handle). 2d lib 193 reds outside its files (154 mutation fixtures `3.0` vs `3`) → integration. Gaps → W0-I: window config in request; Board2d `toolRunTrace` lane + host mount.
- W3-2 landed → `📓️wave-W3-2.md`: generation3d preview eval = read-only `previewEval` ToolRun (editor + viewer), node-id trace, new framework `ToolRunJobPort` for host hops, `cancel-preview-eval` + tick/rearm deleted, status pane cancel → `toolRunAbort` (TS contract/React/wgpu Shell, fixture 11 rows); `importDocument` not converted (single staged edit already). gen3d lib 453/3 (3 pre-existing), preview_eval 18/18, chrome parity 24/24; native + wasip2; W1-D green. Framework tool_run 24/1 (W0-I chord law in flight). Open: generation2d eval loop not converted → W3-2b; `reorganize` → W3-F2.
- dispatched W3-2b generation2d preview eval → `📓️wave-W3-2b.md`.
- W2-C landed → `📓️wave-W2-C.md`: 3d brush search = read-only ToolRun following the hovered/opened vortex (never completes; accept stays one-shot), `suggestionsTick`/`BrushSearchProgress`/picker/`[DEBUG]`/World3dHost brush ghost + host refresh plumbing deleted, new `targetBrushSuggestions`; real defects fixed: placed objects resolved to mesh by kind instead of own mesh URL (collisions missed → false "free"), accept placed `index % kinds` with identity rotation instead of the found candidate. parry3d 0 disagreements, 2 ms Nakagin law, abort byte-identical, one-undo accept; engine-contract 603, lane Rust 156. Crate 710/12 outside lane (catalogue ×4, wire tag, context-menu zoom, settings focus, window-config reload, backbone merge, mutation latency). Regression: 5d React brush ghost → W2-B scope. SAME mesh-by-kind defect in the fill planner → W1-G.
- dispatched W1-G fill planner own-mesh collision resolver → `📓️wave-W1-G.md`.
- W3-2b landed → `📓️wave-W3-2b.md`: generation2d `flowEvalTick` rearm loop → read-only `previewEval` ToolRun (node verdict trace, counters evaluated/failed/blocked/hops), `Generation2dPreviewCommandWork` sync eval loop deleted (generate preview into a second retained session), per-hop evaluated-input digest re-evaluates on undo/redo/remote edits. 17 run laws pass (fnv oracle, worst drive_step 128 µs, abort quiescence, TS twin); native + wasip2; W1-D clean. Lib 213/35: all 35 pre-existing (every gen2d registered-app test fails at close "document store close awaits a retained reader", identical at HEAD) → integration owner. `✏️s/🔌️plugins/🌀️procedural/🔣️.json` still lists `flowEvalTick` (regenerate).
- W3-1 landed → `📓️wave-W3-1.md`: energy simulation = read-only ToolRun (adopt wrote nothing — verified), `rebase/reconfigure: restart`, five verbs + `EnergySimulationStatus` + global session + local chords removed, streamed results equal the batch engine exactly, step = one timestep; W1-D 0 energy findings; native + wasip2. Run settings moved to a config store (`change-simulation-settings`). Fixed: facility-meter fault in hour 2, preparation footprint, `setActiveExample` drop panic, missing store owners, capture ignoring run period/schedules. EnergyPlus oracle now runs (14 s) but RED on physics: case 920 cooling 5 969 vs 2 741 kWh, 940 8 953 vs 2 434; design-day output encoding faults → W3-1b. Crate ~4 990 pre-existing reds (undecodable snapshot fixtures). Gaps → W0-I: ToolRunView lacks progress, per-tick plugin window refresh, framework ToolRun panel not mounted anywhere (critical).
- dispatched W3-1b energy physics vs EnergyPlus → `📓️wave-W3-1b.md`.
- W1-G landed → `📓️wave-W1-G.md`: both fill sites use `resolve_placed_object_mesh_url` (peer had swapped them; W1-G fixed the revalidate no-catalogs branch), fixture law `ownMesh` + parry3d oracle over own-mesh Concrete Forest/Nakagin (red with kind-only, green now); `-- fill` 50/0 at threads 4, threads 1 49/1 (2 ms law 2.56 ms at load 35, passes alone twice). Open defect: Nakagin with own meshes at 1.5× ends a run with zero verdicts, no steps, no reason (silent stall, likely stale spatial index at preparation) → W1-H.
- dispatched W1-H fill silent-stall fix → `📓️wave-W1-H.md`.
- W3-F1 landed → `📓️wave-W3-F1.md`: reasoning/wires, dag, trinity/jack `reorganize` = shared layout run (reasoning `forceLayout` merged), 7/7 tests each at threads 4 and 1 (fdg-sim/petgraph oracle, worst step ≤148 µs, one undo, byte-identical abort), wasm components + descriptors regenerated. Fixed: reasoning publication factory over-declared extent, dag missing finalize factory/owners + silent no-op reorganize (wrong schema name), dag topology ignored port-named edges (now ~5× faster, 6.2 ms @4 096). Not converted: trinity/jack `runQuery` (writes document; tests need two concurrent queries) + trinity/rewriting rule application. Open: dag layered layout column collision; no verdict tint on 2D node-graph canvases; taxonomy check timed out (registration unverified).
- 13:11 session rate limit (resets 13:00) killed W0-I, W1-H, W2-B, W3-1b, W3-2c, W3-4 mid-lane; resume via SendMessage after reset.
- Decision (coordinator): concurrency invariant refined — at most ONE non-terminal *mutating* run per document instance per actor; read-only runs may run concurrently keyed by (tool, window instance). trinity `runQuery` = read-only run streaming result rows as trace (results window renders them); persisting results is a separate one-shot command. Rule application (rewriting) = mutating run with a visible match trace. → W0-I (framework) + W3-F2 (trinity) after reset.
- W3-2c landed → `📓️wave-W3-2c.md`: root cause = gen2d document retirement used bare `FlowRetirement::close_step` (Blocked forever under one-page grant) instead of `close_page` + `terminal_is_empty`; ported gen3d's `generation3d_close_flow_frontier`. gen2d lib 213/35 → 248/3; masked reds fixed (`nodeGraphViewport` bridge, window-camera law on `previewEval`, 4 MiB test stack). Remaining 3 framework-owned: fail-closed VCS remote merge, retained maintenance swap envelope stall (both also gen3d), window-config pack load (first page 4104 B vs 4096 B grant → exhausts bound, returns Ok, installs nothing; deeper Pack rejection + Drop panic; reverted) → W0-J. Descriptors regenerated; `flowEvalTick` only as runtime hop command.
- dispatched W0-J framework window-config pack load → `📓️wave-W0-J.md`.
- W0-J landed → `📓️wave-W0-J.md`: window-config loads install; bound exhaustion → typed `window-config.load-bound`; causes: 4096 B grant vs 4104 B page, decoder requesting a page after all input, block tag applied to nested record's first value (`TypedState`), store config hydration stalled for owners > 4096 B, Drop panic on unretirable load (now parked on registry), baseline test owners `NoConfig`. Grant = exact next need (≥ one page), over-bound Pack refused `window-config.capacity`. gen2d window_camera 3/3, framework window-config 5/5, fixture +3 scenarios, wasip2 clean; tool_run 29/1 (2 ms timing under load 54, passes alone). Open: reload laws that passed on silent Ok may now fail loudly; puzzle 2d 2 red laws (attribution unproven); writer `--lib` tests don't compile at HEAD.
- W3-4 landed → `📓️wave-W3-4.md`: remodel reconstruction = one mutating ToolRun (run/advance/cancel/retry verbs, 2 per-tick amends, global staging, job-stage document lane deleted); provisional `append-content` + `commit-reconstruction`; per-stage verdict trace. 12/12 (fixture, `bitvec` Hamming oracle, fuel, resume, revalidate, inverse); abort byte-identical; one undo/redo; native + wasip2 + W1-D green. NOT ACCEPTED: worst unit 9.8–47.9 ms and the law was loosened to "never 4 in a row over 8 ms" (contract forbids loosening) → W3-4b. Red: synthetic-orbit ground truth faults at surface stage "mesh envelope exceeded" → W3-4b. ~200 canonical-JSON fixtures + 35 registryless `new_app` editor tests pre-existing. Descriptors still list removed verbs.
- dispatched W3-4b remodel unit granularity + surface stage envelope → `📓️wave-W3-4b.md`.
- W2-B landed → `📓️wave-W2-B.md`: 5d fill = ToolRun wrapping 3d fill job via `Puzzle5dPlannerToolRunJob` (3d→5d op translation, Placement2d twins), 5d brush on W2-C brush run (`targetBrushSuggestions`), old lane/cancel toggle/`drive_precompute` deleted, `fillCount` → app config; proximity/relocate stay one-shot with bounded scan (Nakagin 0.31 ms, capsule dream ~4 ms, 2 880-part differential regression); clipboard already stepped. Lane 17 tests green; native + wasip2; W1-D clean. 5d lib 246/93: 72 non-canonical fixtures / bare-store retirement factory, 4 reserved retirement laws, 3 duplicate-marker source tests, 9 `BatchOnlyPendingRewrite` commands (setActiveExample/translateSelection/setCamera2d) hard-dead in the registered app, 2 reserved verbs admitted without settle, 3 engagements answered for addressed window only, 1 close not terminal after brush arm; import job retires one char per close step. Incident: killed a likely peer 3d test run (pid 34659). → W2-B2.
- dispatched W2-B2 puzzle 5d reds → `📓️wave-W2-B2.md`.

## Phase 5 — reopened 2026-09-14 (Fable 5.1 session ⚪629f0bae, Windows checkout `C:/git/semio`, repo MCP connect timeout → manual bookkeeping)

Dev directive: every tool interactive with a visible process, start/abort/finalize lifecycle, mutating tools inside a transaction (unchanged), and **get the React renderer working end to end before the wgpu renderer** — puzzle 3d fill shows all tested meshes in 3d, collision = danger color, fitting = success color.

State found on this machine: ticket folder synced with reports up to W2-B (W2-B2, W3-1b, W3-4b, W3-F2 never reported); gate 6 (deploy) and gate 7 (browser evidence) never run. Working tree carries a peer's staged changes (repo product Rust modules, `CLAUDE.md` symlink deleted, styling `📜️script.ts` imports stripped) — imports restored so the styling generator runs; `🎨️styling/🔤️tokens/🦀️.rs` (gitignored, generated) was missing so `cargo check -p semio-s-artifact-puzzle-3d` failed at `semio-framework-ui-styling` (`🗑️generated/W4-react-e2e/check-puzzle3d-native.txt`).

Plan (lane W4, React end to end): (1) bootstrap generated layer + native/wasm checks, (2) close the React gaps the reports left open — ToolRun panel mount (W3-1 G3), `provisional` stamping on fill instances (W0-H item 3), trace layer visibility/legend, start action reachable from the shell, (3) deploy chain `component-dev → support-dev → materialize-dev → prepare → activate-puzzle3d-react-dev` on :6013, (4) browser evidence per contract §6.7 (a)–(g) via the in-app browser, (5) report `📓️wave-W4-react-e2e.md`.

## Phase 6 — reopened 2026-09-14 17:00 (Opus 5 session ⚪a319b1ae, macOS checkout, repo MCP connection closed → manual bookkeeping)

Same directive as phase 5. State on this machine: tree clean at `bb961413d4`; the ToolRun panel (React `🛠️ShellHelpers/⏯️tool-run-panel` + wgpu `⏯️wgpu-tool-run-panel`) landed in `f2fc008ee2`; no W4 report synced from the Windows session, so W4's progress is unknown here. Nothing served on :6013. This session runs the runtime gate on macOS: boot `serve-puzzle3d-react-dev`, drive fill in the in-app browser, record evidence and fix the defects it exposes → `📓️wave-W5-mac-react-e2e.md`.
- 17:10 :6013 booted (`serve-puzzle3d-react-dev`, fresh component-dev + materialize-dev). Runtime evidence on Concrete Forest, React:
  1. **Hard fault**: `fill 100` → `toolRunStart` → every refresh fails `1:framework.panel.toolRun: DuplicateSiblingKey`; trace attributes stay 0, the run is invisible. Cause: the ledger pushed a trace key into `recent_trace` on every upsert, and fill upserts each candidate twice (testing → verdict), so the Attempts tree held `framework.toolRun.<run>.trace.<key>` twice. The toy jobs of the unit suite upsert each key once, so the suite never saw it. Fixed (upsert moves the key to newest, retire drops it) + law `tool_run_panel_lists_a_re_upserted_trace_key_once_at_its_newest_position` (fixture `verdictRevisions`), red 5 vs 3 rows → green.
  2. **No Start affordance**: with no run the "Tool runs" panel is empty and the fill tray only has Count + distribution; start was reachable only by typing `fill <n>` in the engagement input. Added the framework ready group `framework.toolRun.ready` (status "Ready to start"/"Bereit zum Starten", Start button with `toolId`, Start chord) for the active run-declaring tool while no run of it exists; label `readyToStart` in Rust/TS/schema/lifecycle-law fixture (labels 22 → 23) + law `tool_run_panel_offers_start_for_the_active_run_tool_until_its_run_exists` (fixture `readyGroup`).
  3. `cmd+enter` in the React shell opened the Top window's Actions search instead of starting the run — to diagnose after redeploy.
  Gates: `semio-framework-tool-run` 27/27, TS conformance 21/21, plugin `tool_run` 31/32 (the 2 ms overlay bench fails only under load 50–100 through preemption, passes alone twice).
  4. **Every ToolRun chord opened a staged form in React** (gate (g)): `actionRequiresStagedForm` treated any declared arg as a form, and all seven tool run actions declare `Hidden` args (`toolId`, `runId`, `generation`), so `mod+enter` expanded the Actions palette form instead of dispatching; the ledger already resolves the active tool / live run for bare chords. Rule now: only a non-hidden arg stages a form (only the tool run actions use `ArgPresentation::Hidden`). engine-contract "palette redirect and keybinding rule" 4/4 via the renderer vitest config. The wgpu shell has no twin of this rule.
- 18:00 in-app Browser pane unusable for evidence: hidden pane stops compositing, and peer edits to `World3dHost`/ui elements trigger vite full reloads every few minutes. Headless probe `🔍️w5-fill-run-probe.ts` (init-script sampler, vite HMR websocket routed to a silent mock so peer edits cannot reload the page, tour veil cleared by Skip only — Escape closes the panel).
- 18:25 probe run 6 (idempotent tool activation) — first real run evidence in React: ready group → Start → trace 440 records (100 success, 340 danger), "Complete, ready to finalize · Placing (4/5)", progressbar "100 of 100 objects (100 %)", history untouched. Two defects: (5) **provisional placements not marked** — puzzle 3d `Puzzle3dInstanceResidency` never stamped `provisional` (framework flag unused), so the 100 provisional pieces rendered as committed objects; fixed: `refresh(fixture, provisional_entities)` stamps `"provisional": true` for `fill_run_entity(object.id)` ∈ `doc.tool_run().provisional_entities` (fingerprint includes the flag); finalize law asserts exactly `placed` provisional instances before and 0 after finalize. (6) **dev message 18:30: the fill shows everything at once** — a run of 440 attempts completed in ~5 s because the driver steps `INTERACTIVE_LANE_FUEL` per step until the turn deadline, and each tick carried a candidate's `testing` and its verdict together (fuel was consumed only by the verdict), so no attempt was ever visible under test.
- Pacing (answer to 6): (a) framework: `ViewModel.toolRunUnitsPerSecond` (host-owned viewer pace, schema bound (0, 1000], Rust/TS/schema/fixture `resolved-host-context` + capacity law), read at `toolRunStart` and on every wake; a paced running run is stepped with fuel 1, after each visible unit it sets `pace_due_us` and hands the host one `DispatchAction { toolRunPace, delay_ms }` via its port, is no driver work until due (`pace_elapsed` gates `has_pending_work` and `drive_selected_tool_run`), and the internal `toolRunPace` wake (not a verb, not declared; stale wakes no-op) re-reads the pace; revalidation and paused single steps stay unpaced. Law `a_paced_run_shows_one_unit_per_interval_and_waits_for_the_host_wake` (fixture `pace`). (b) puzzle 3d `FillRunJob`: a constructed candidate consumes one fuel unit; when it exhausts the step the remaining observations wait in `deferred` (completion settles only with nothing deferred), so a one-unit step shows the candidate under test alone and the next marks it; law rewritten to `fill_run_job_step_with_one_unit_of_fuel_shows_each_candidate_before_its_verdict`. (c) React host stamps `TOOL_RUN_VISIBLE_UNITS_PER_SECOND` (20) into the action and effect-dispatch view states; wgpu sessions stay unpaced (`None`) because the wgpu shell ignores `delay_ms` (open, wgpu after React). Gates: plugin `tool_run` 32/33 (bench under load, passes alone), puzzle 3d `fill_run` 16/16, manifest view-context 12/12 + TS host-context law, framework-rs generate green.
- 19:35 :6013 vite died after a peer HMR burst (nx husk kept running, no listener); recycled my own serve tree by pid (67898/67900/67901/71145 + orphan vite 71164) and restarted `serve-puzzle3d-react-dev`.
- 19:38 probe runs 7/8 on the paced build: provisional marked (16 → 80 → 89 pieces, all provisional), candidate under test visible, document + history untouched while running, abort/abort chord and finalize/undo pass. Two defects: (7) **bursts** — attempts still arrived 10–12 per rendered frame because the React refresh lags the 20 units/s timer under load; (8) **abort latency ~10 s** — a flood of `toolRunPace` wakes (one per unit, plus units stepped by refresh-driven turns) queued in the Interactive command lane ahead of the user's Abort.
- Fix: at most ONE outstanding pace wake per run (`pace_wake_outstanding`; an early wake re-arms for the remainder), and a **presentation gate**: a paced run is ready only when the interval elapsed AND every scene window its trace reaches echoed a cursor at/after the trace's newest page (or stalled `TOOL_RUN_TRACE_STALL_REFRESHES` refreshes); a wake before presentation re-arms at `TOOL_RUN_PACE_PRESENTATION_RETRY_MS` (100). Laws: `a_paced_run_shows_one_unit_per_interval_and_waits_for_the_host_wake` (early wake re-arm, no second wake), `a_paced_run_waits_until_its_renderer_presented_the_last_unit`. plugin `tool_run` 34/34. Deployed 19:55.
- 20:10–21:05 wake-driven presentation: probes 9–11 still stalled ~7 s per unit. (9) the releasing wake returned `UiDirtyScope::None` and stepped the unit in the typed-operation continuation, whose scope never refreshed the scene → renderer never received the page → gate waited for an echo nobody could send. Fix: the pace wake that releases the run drives one driver turn itself and returns the popped `typed_ui_outbox` scope as its action result (laws rewritten: the wake after the interval shows the next unit and arms the next wake; an echoing wake releases and carries a non-None scope). (10) Guest `[DEBUG]` pace log (removed after) showed `echoes={}` and every trace window `echoed=None`: the React host's echo map was populated in-page (`🔍️w5-echo-map-probe.ts`) but the **served ShellHost transform was stale** — vite (restarted 19:35) never re-transformed my later ShellHost edit; disk had it. Recycled the :6013 serve by pid; fresh transform carries `toolRunTraceCursorByWindowId` in both dispatch view states. Integer-carrier law now also decodes through actor-ingress `FromValue::from_value` (it only covered `from_dsl_value`). With fresh transforms (`🔍️w5-pace-diagnose-probe.ts`): 89 attempts in ~40 s, 2–3 records per 0.5 s sample, every candidate shown under test then marked (23 success / 62 danger by 54 s), echo page tracking the trace page.
- 21:30 probe run 12 (fresh transforms, debug log removed): 18 PASS / 1 FAIL (100-object run at 99/100 when the 900 s budget ended), 0 faults → `📓️wave-W5-mac-react-e2e.md`. Open: panel speed control, React Attempts tree shows NO DATA, wgpu pacing (delay_ms ignored), gates (e)(f)(h). Ticket stays open.

## Phase 7 — dev directive 2026-09-14 ~23:30: the fill procedure, algorithm not UI bound

Dev: draw a random free vortex (by distribution), draw a random compatible vortex (by distribution), test collision (danger when colliding, success + new object joins the pool when free); after a collision draw another compatible vortex at the same vortex; when none is collision free mark the vortex danger and draw another free vortex; stop at the target count or when every vortex is marked, returning partial results with a warning. The algorithm is not UI bound; the renderer shows what it can (best effort, skips when behind).

- Removed the viewer pace entirely (phase 6 pacing, presentation gate, `toolRunPace` wake, `ViewModel.toolRunUnitsPerSecond` Rust/TS/schema/fixtures, React stamping, wgpu literals, pace laws). Runs step `INTERACTIVE_LANE_FUEL` again; the trace lane delivers whatever accumulated (renderer skips intermediate frames). Kept: ready group, duplicate-key fix, hidden-arg chord rule, provisional stamping, candidate-as-its-own-visible-unit (Step shows the candidate under test before its verdict).
- `FillBuilder` rewritten to the procedure (was: per placement rebuild every open target, seed objects first, walk them in rotated order; cross-kind candidates in deterministic diversity order; exhausted vortices retried after every placement): resident free-vortex pool with an incremental Fenwick tree (`fenwick_push`), weighted draw per round (`weighted_draw`), candidates ordered by weighted draws without replacement, `mark_target` zeroes the vortex for good and emits `FillRunEvent::VortexMarked`, acceptance appends the new object's free vortices (`AcceptPhase::AppendTargets`) and retires the consumed vortex, stall when the pool weight is zero (`no-open-vortex` / `no-compatible-kind` / `no-free-placement`), document vortex capacity (4096) stays the `document-capacity` stall; a retraction drops the pool (marks forgotten, rebuilt).
- `FillRunReason::VortexExhausted` (code 23, danger) and `FillRunCounter::Marked` in schema Rust/JSON (enum + `x-semio-toolRun` table), 3d + 5d terminology EN/DE; run job upserts a `danger` record on the vortex-marker mesh (scale 2) per marked vortex.
- Laws: new `fill_plan_retries_one_drawn_vortex_until_it_places_or_is_marked_and_never_draws_a_marked_vortex` (Nakagin 1M requested → no-free-placement, pool = placements + marks, retries at one vortex); fixture `🎞️fill-run.json` re-pinned from the new deterministic runs (Nakagin seed 1: 2187 tested / 1 placed / 358 marked; concrete-forest 7: 13/8/0; 3: 94/40/7), own-mesh-blocker gains `danger:vortex-exhausted`, delivery law now "long run" (concrete-forest reaches the vortex document capacity at 2865 tested, minimum 2500), raise resumes at `SelectTarget`; parry3d oracle unchanged green.
- 00:25 gates: puzzle 3d `-- fill` 53/53 at threads 4 (parry3d oracle, new procedure law, 2 ms Nakagin step law; it failed once at load 50 and passed alone twice), puzzle 5d `fill`+`terminology` 13/13 (concrete-forest-8 re-pinned: 12 tested / 8 placed, prefix gains `danger:vortex-exhausted`), framework `tool_run` 32/33 (2 ms overlay bench load-sensitive, passes alone), view-context TS law, tool-run TS 21/21, framework-rs generate. Deployed 00:23 (served `core.wasm` carries `vortex-exhausted`, no `toolRunPace`; served ShellHost has no pace stamping).
- 00:30–01:15 browser (`🔍️w5-fill-run-probe.ts`, now Chromium headless on **Metal**): 17/17, 0 faults — default 100 run completes in 2.2 s (100 placed, 347 danger records = collisions + marked vortices, all provisional, document/history untouched), abort, count 5000 → partial result 408 placed with the panel step "Document capacity reached at 408", chords, finalize + one undo. `🔍️w5-large-run-responsiveness-probe.ts`: on Metal one 1.3 s long task when ~2000 records and 408 provisional GLB instances land, abort ≤ 0.4 s; the earlier 30–42 s freezes were SwiftShader (headless default software GL, `🔍️w5-large-run-profile-probe.ts`: 27 s `(program)` in WebGL).
- Open: marked-vortex markers are small (vortex-marker 0.12 × 2) — confirm visibility in the real app; trace store evicts the oldest records past its cap (408 placed but 298 success records resident after the 5000 run); the 1.3 s landing task (per-instance meshes/materials for provisional GLB instances); React Attempts tree NO DATA; wgpu renderer parity.

## Phase 8 — dev directive 2026-09-15: the fill must render incrementally from its first attempt

Dev: the UI showed nothing and then everything after several seconds; the renderer must start showing the fill the moment it starts, skip only what it cannot keep up with, ideally show every attempt; algorithm not UI bound, UI best effort.

- Cause 1: the whole run executed inside one `settlePluginTurn` (the draining settle kept looping while the operation had work), so no refresh could run until the run ended. Fix (`🔌️PluginRuntime`): `settleYieldsToRefreshV1` ends a draining settle once `PLUGIN_OPERATION_REFRESH_SLICE_MS` (16 ms) elapsed and a result carried an operation progress frame (unsolicited `Invocation` with a `ui_scope`), with no acknowledgements owed and nothing outstanding; the rest continues on the yielding background drain. Law: "ends a draining settle one slice after the operation asked for a refresh".
- Cause 2: on the background drain those progress frames correlate with no command waiter and were dropped. Fix: `AppChannelClient.onOperationProgress` (os `🟦️.ts` pump) + `PluginRuntime.subscribeOperationProgress` + ShellHost subscription applying the resolved dirty scope. Law in `🧪️backbone-envelope-io`: progress scope reaches its subscriber, never a command waiter (os channel 20/20).
- Cause 3: the final frames were swallowed by the already-answered refresh join → the progress subscription bumps `noteGuestIngressV1`.
- Probe `🔍️w5-fill-timeline-probe.ts` (dev build, Metal): first records at ~1.1–1.7 s, ~6 progressive scene updates for 100 objects, completes 100 placed, panel Complete. Gates: PluginRuntime in-source 111/111.
- Remaining cost: refresh pass ~370 ms (full world window Replace + guest refresh turns 15–30 ms in wasm-dev), which also shares the single guest actor with the run (run 1 s → ~6 s). Measuring the release build next.
- 10:20–10:50 release attempt blocked by peer in-flight work: the staged shard worker template called a removed `heartbeat()` (peer lane worker-more-work-drive folding beats into crossings; fixed upstream by 10:23, restaged), then the guest faulted at boot with `plugin.reactor-close-authority: pending patch emission left a borrowed publication unstaged` (uncommitted reactor `🔄️turn`/`🩹️patches` edits of the same lane). Not ours; waiting for that lane — its MoreWork-in-the-worker change is also the fix for the run pace depending on main-thread round trips (4 ms `TOOL_RUN_TURN_WALL_US` per host-submitted turn).
- Native refresh cost (temporary probe, unoptimized, 100-object concrete-forest fill, trace cursor echoed): the main window render grew 6 → 39 ms with the document. Breakdown at 96 placements: `scene_for` 12–14 ms (projection `Value` bridge), `render_fixture` 8–10 ms (the SAME bridge a second time), `world_attractions_json` 8 ms (attractions × vortices scan with a `format!` per comparison), geometry 1.5 ms, surface encode 0.7 ms.
- Fix: `scene_for` reads the snapshot's typed authority (`scene_from_snapshot`, already pinned by `puzzle3d_typed_fixture_matches_the_projection_bridge_for_every_example`) for all six callers; the main render decodes ONE scene for the precompute sync and every body (`render_fixture` removed); `world_attractions_json` indexes vortices once (first match kept). After: render 39 → 8 ms at 100 placements (`scene_for` 0.19 ms, attractions 1.2 ms). New law `world_attraction_segments_match_the_vortex_position_resolver_for_every_example` (examples + a grown Nakagin with chained and dangling attractions).
- Gates: puzzle 3d lib 718/730 at threads 4; the 12 reds rerun alone with and without this change: identical 5 pre-existing reds (settings panel focused pane, window options local to instance, context menu rows, backbone convergence, wire-format guard — peer areas), the other 7 pass alone (load).
- 11:25 release rebuilt with the render fixes still traps at boot (`reactor-close-authority`). Diagnosis handed to the coordinator session semio-e3 (ticket 26/09/09/PROCEDURAL-3D-END-TO-END): `PendingPatchAuthority::take_one` Reconcile arm marks a cell borrowed/emitted when a paged large patch is only partly published, so `stage_emission` sees fewer patches than borrowed cells (puzzle 3d world surfaces are large, generation3d's are small). Their lane worker-more-work-drive owns the fix (`📓️worker-more-work-drive-2026-09-15.md`); do not touch reactor files. Browser measurement of phase 8 resumes when it lands.
- 12:10–13:10 release (with worker-more-work-drive's staging fix, serve recycled for a stale barrel transform) boots; timeline probe (Metal): 100-object run ends in 3.3–4.3 s, first records at ~1.0 s, 4–5 visible scene updates (~1/s, jumps of ~120 records), panel Complete.
- Main-thread CPU profile (CDP, probe `--profile`): WebGL render ~1.1–1.4 s, React ~0.9 s, patch intake ~0.5 s over a ~4 s run. Host fixes: (a) `advanceWorldInstanceResidency` fallback keeps the identity of every value-equal retained record (`internWorldInstanceRecords`) — a fill APPENDS, so every refresh fell back and re-issued all records; fixture `🚚️instance-delta.json` gains `fallbackKeepsIdentityOf`, removal/addition cases now reuse `b`; (b) `WorldInstancesLayer` passes stable props to the memoized `WorldInstanceNode` (per-record cached `Quaternion`, unit-scale constant, `useCallback` `mergeMode`/`paintFromHit`); (c) `PanelTabBar` scroll reset keyed on tab ids instead of the per-refresh `tabs` array (121 ms forced layout). Gates: instance delta + mesh residency 28/28, PluginRuntime in-source 113/113.
- Remaining latency is guest-side publication, measured with temporary intake/request logs (removed): window patches intake in ~30 ms each, but arrive 250–800 ms after the refresh request and not once per request (requests every 100–400 ms); `refreshUi` settles with an empty required set, so the patches leave on later drain turns at the reactor's reconcile pace. Reactor reconcile/turn pacing belongs to the coordinator's lanes (semio-e3); data handed over.
