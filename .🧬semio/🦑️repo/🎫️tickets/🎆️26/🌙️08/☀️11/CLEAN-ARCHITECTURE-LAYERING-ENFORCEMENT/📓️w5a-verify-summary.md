# W5a Post-Wave Verification — Summary

## 1. Cargo check — no regression from Wave 5a's own files; pre-existing/churning failures elsewhere

Full `cargo check --workspace` run (exit 101), tail 300 lines saved to
`📓️w5a-verify-cargo-check.txt`. Two crates fail, same two crates as every prior wave's baseline:

- `semio-framework-os-kernel-db` — 1 error, identical to baseline: `couldn't read
  .../🛢️db/.../../../📄️document/🦀️component.rs: No such file or directory` (known document-module
  churn from another concurrent session).
- `semio-compose-rs` (`compose/client/lib/rs/lib.rs`, a generated file) — 22 errors this run, all
  `unresolved crate dsl`/`vcs`. Baseline and w4b both showed only 5-6 of these; w4a6 showed 25 (same
  order of magnitude as now). Line numbers for the `dsl`/`vcs` references shift between runs (e.g.
  `716`/`851`/`7816` this run vs different lines in the w4a6 file) — this confirms the file is being
  actively regenerated by another concurrent process, not something Wave 5a touched. **None of the
  25 errors reference playbook, puzzle-5d, launch.json/package.json, icon concepts, i18n, or e2e** —
  nothing in Wave 5a's scope is implicated. Per the shared-live-tree rule this is noted, not chased.

**No compile regression attributable to Wave 5a.**

## 2. Agent progress files — all 4 read

### a. Playbook relocation — STOPPED, zero edits (correct call)
Agent found the move was not safely scoped: os-flow's own `vcs` component depends on
`crate::playbook::*` internally (moving would force os-flow → playbook-plugin, a new backwards
layering violation), and 32 files across 4 crates (`procedural` ×26, `forms` ×3, `flow`-plugin ×1,
`playbook`-plugin ×2) consume `flow::playbook::*` directly with no existing dependency on
`semio-s-plugin-playbook`. Made zero file changes; `git status` confirmed clean. Correctly
recommends this be split: first an architecture decision (plugin→plugin dep allowed, or a new
shared lower-level crate), then the mechanical move. **Fully deferred, nothing landed.**

### b. puzzle-5d-react move — landed
Moved 5 files via `git mv` into `✏️s/🔌️plugins/🧩️puzzle/📦️packages/🟦️typescript/🎯️targets/⚛️5d-react/`,
fixed `$schema` depths, `repository.directory`, `sourceRoot`/`cwd`, `tags`, and the shared-library
relative import. Fixed the root `package.json` workspaces entry and the sketchpad vitest alias.
Verified: package's own test suite passes standalone (1/1); the sketchpad test that imports the
moved package fails, but for a **pre-existing** reason (`@semio-tech/framework-platform-core` and
5 sibling aliases already pointed at a non-existent ASCII tree before this move — confirmed by
inspection, not fixed, out of scope). `nx show project` also pre-existing-broken (unrelated Nx
duplicate-name conflict on `infinite-world-r3f`, another session's in-flight rename). Left
`.cursor/*` historical references and generated `bun.lock`/`.nx` cache untouched by design.
**Fully landed as scoped**, two pre-existing unrelated breakages documented, not fixed.

### c. launch.json / package.json generation — partially landed, deliberately simplified
Full generation (steps 2-3) was not attempted: `PlaygroundEntry` has no `label`/`order`/`command`/
`envVar` fields, and neither editing 58 crates' Cargo.toml nor heuristically guessing those fields
(port-env-var name isn't a deterministic function of variant id) was judged safe — guessing wrong
would silently break another dev's dev-server env wiring. Took the documented fallback instead:
removed real duplication only. Cross-referenced `.claude/launch.json` (29 script names, out of
scope, left untouched) against the seed's `devLaunchers`, found 15 `dev:*` package.json scripts with
no live consumer besides the seed, inlined their commands into the seed, deleted the 15 scripts, and
regenerated `.vscode/launch.json`. Verified by diff (only the 15 `command` fields changed, byte-
identical to what the deleted script ran) and by in-process byte-compare of `generateLaunchJson()`
output against the written file. `plugin-registry:check` still fails, but for pre-existing,
unrelated Rust wiring/schema violations in other plugins (confirmed no `.rs` files touched).
**Landed: 15/~40 duplicate scripts collapsed.** Full generation and `.claude/launch.json`
consolidation explicitly deferred to a dedicated future ticket (needs per-crate TOML metadata
authoring across 58 crates).

### d. icons / i18n / e2e — landed with one flagged out-of-ownership regression
- Icon concepts: split `ICON_CONCEPT_ASSIGNMENTS` (38 generic entries kept) from new
  `PLUGIN_DOMAIN_ICON_CONCEPTS` (47 plugin-identity entries) in both the framework and ui-mirror
  files, verbatim move, no icon id changed. Flagged (not fixed, out of scope): the barrel
  re-export and Storybook's `Icons/story.tsx` don't yet know about the new export, so Storybook will
  visibly render only 38/85 icons until a follow-up wires the barrel — compiles fine, no crash.
- I18n: renamed schema key `ui.search.category.studio` → `hostApp` in the I18n component and
  repointed the two react-target locale bundles (DE/EN) that define it.
- **Real regression, out of ownership, documented not fixed**: `ShellHost/🟦️component.tsx` (not in
  this agent's file list) has two call sites (lines 4729, 4736) still passing the literal string
  `"ui.search.category.studio"` to `shellLabel(...)`, which is typed against the schema-derived
  union — **this file will now fail to typecheck** as a direct, predictable consequence of the
  rename. Needs a 2-line fast-follow (swap `studio` → `hostApp` in both string literals).
- e2e split: located but did not move the studio-specific Playwright region (`os/dev`'s
  `📜️script.ts` lines 1523-1696, region `SpaceE2eVerify`) — file not in ownership, confirmed
  self-contained and move-ready for a follow-up.
- Verification: scoped `tsc --noEmit` on the 4 touched files shows 235 pre-existing errors (missing
  ambient types resolvable only in the full project graph, plus one unrelated pre-existing
  `surfaceContextMenu.architecture` mismatch), zero of which reference `studio`/`hostApp`/
  `ICON_CONCEPT_ASSIGNMENTS`/`PLUGIN_DOMAIN_ICON_CONCEPTS` — renames introduce no new in-scope
  errors.

## 3. Regressions found

One, already self-flagged by its author: **`ShellHost/🟦️component.tsx` lines 4729/4736 will fail to
typecheck** after the `studio` → `hostApp` i18n rename (agent d). Not a cargo-check regression
(TypeScript, not Rust) so it didn't show up in section 1. Small, mechanical, 2-line fix.

## Follow-up work needed (one bullet per item)

1. **Playbook relocation**: architecture decision needed first — either allow `procedural`/`forms`/
   `flow` plugins to depend on the `playbook` plugin directly, or introduce a third, lower-level
   shared crate for the playbook-domain module that both `os-flow` and `s-plugin-playbook` can
   depend on without either depending on the other. Only then is the physical move mechanical.
2. **puzzle-5d-react move**: none needed for the move itself; the two pre-existing breakages it
   surfaced (sketchpad's broken ASCII-tree aliases; Nx's `infinite-world-r3f` duplicate-name
   conflict) belong to other, unrelated tickets/sessions.
3. **launch/package generation**: dedicated ticket to add `label`/`order` (and ideally `command`/
   `envVar`) fields to `[[package.metadata.semio.playground]]` across all 58 playground crates, then
   fully generate `devLaunchers`; separately, consider regenerating `.claude/launch.json` from the
   same registry catalog so its remaining 29 pinned `dev:*` scripts can also be collapsed.
4. **icons/i18n/e2e**: (a) fast-follow the 2-line `ShellHost/🟦️component.tsx` string-literal fix
   for the `studio`→`hostApp` rename (real regression); (b) wire `PLUGIN_DOMAIN_ICON_CONCEPTS`
   through the assets package barrel and `Icons/story.tsx` so Storybook renders all icons again;
   (c) move the `SpaceE2eVerify` region out of `os/dev`'s `📜️script.ts` into the space plugin.

## Go/No-Go for next wave

**Conditional GO**, with one required fast-follow before it's fully clean: the `ShellHost`
typecheck regression from item 4a above. Everything else is either fully landed (b, partial c/d) or
safely deferred with zero edits made (a). Cargo check shows no regression traceable to this wave.
