# w3.5 — CAD extensions: add `.contributes_topic("cad.computer", ...)` alongside `Contribution::CadComputer`

## Scope
Chain a `.contributes_topic("cad.computer", serde_json::json!({...}))` call right after the
existing `.contributes(Contribution::CadComputer{...})` call in the `bundle()` builder chain,
at 4 sites under `✏️s/🔌️plugins/📐️cad/🧩️extensions/`. This is the open-payload replacement path
for the closed `Contribution` enum (`ExtensionBundle::contributes_topic` builder method,
`🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🦀️component.rs:7062`).

## Field mapping
`Contribution::CadComputer` fields (from `🧰️framework/🔨️modules/🛂️manifest/🦀️component.rs:2759-2769`):
`app_id`, `module_id`, `label`, `icon_id`, `computers_json` → camelCase JSON keys
`appId`, `moduleId`, `label`, `iconId`, `computersJson`.

Topic string confirmed per-crate from each `📦️packages/🦀️rust/Cargo.toml`'s
`[package.metadata.semio] contributes = ["cad.computer"]`.

`serde_json` was already imported/used (`serde_json::to_string(&computers_manifest())`) in all
4 files, so no new `use` statement was needed — used the existing crate path
`serde_json::json!` directly.

## Files edited
1. `✏️s/🔌️plugins/📐️cad/🧩️extensions/🏢️aec-building/🦀️component.rs`
   - crate: `semio-s-plugin-cad-aec-building`
   - added `.contributes_topic("cad.computer", serde_json::json!({ "appId": HOST_APP_ID, "moduleId": MODULE_ID, "label": "AEC Building", "iconId": "building", "computersJson": serde_json::to_string(&computers_manifest()).unwrap_or_default() }))` after the existing `.contributes(Contribution::CadComputer{...})` call in `bundle()`.

2. `✏️s/🔌️plugins/📐️cad/🧩️extensions/📐️spatial-shape/🦀️component.rs`
   - crate: `semio-s-plugin-cad-spatial-shape`
   - added `.contributes_topic("cad.computer", serde_json::json!({ "appId": HOST_APP_ID, "moduleId": MODULE_ID, "label": "Spatial Shape", "iconId": "box", "computersJson": serde_json::to_string(&computers_manifest()).unwrap_or_default() }))` after the existing `.contributes(...)` call.

3. `✏️s/🔌️plugins/📐️cad/🧩️extensions/🏛️aec-building-structure/🦀️component.rs`
   - crate: `semio-s-plugin-cad-aec-building-structure`
   - added `.contributes_topic("cad.computer", serde_json::json!({ "appId": HOST_APP_ID, "moduleId": MODULE_ID, "label": "AEC Building Structure", "iconId": "landmark", "computersJson": serde_json::to_string(&computers_manifest()).unwrap_or_default() }))` after the existing `.contributes(...)` call.

4. `✏️s/🔌️plugins/📐️cad/🧩️extensions/🔥️aec-building-energy/🦀️component.rs`
   - crate: `semio-s-plugin-cad-aec-building-energy`
   - added `.contributes_topic("cad.computer", serde_json::json!({ "appId": HOST_APP_ID, "moduleId": MODULE_ID, "label": "AEC Building Energy", "iconId": "zap", "computersJson": serde_json::to_string(&computers_manifest()).unwrap_or_default() }))` after the existing `.contributes(...)` call.

No other files touched (tests, Cargo.toml, etc. all left as-is — instructions scoped edits to the 4
`component.rs` files only).

## Verification
Ran:
```
cargo check -p semio-s-plugin-cad-aec-building -p semio-s-plugin-cad-aec-building-structure \
  -p semio-s-plugin-cad-spatial-shape -p semio-s-plugin-cad-aec-building-energy
```
Result: `Finished` — clean. All 4 target crates compiled with **zero** errors and **zero**
warnings attributable to them (only pre-existing unrelated warnings inside
`semio-framework-plugin`, e.g. unused-variable/unnecessary-unsafe lints in
`🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📦️packages/🦀️rust/../../🦀️component.rs`,
which predate this change and were not touched).

## Status
Done. All 4 sites updated and verified clean via `cargo check`. Not closing the ticket
(instructed not to call `ticket_close`/`ticket_reopen`).
