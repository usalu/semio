SUPERSEDED — do not apply this patch. See 📓️w1-e-report.md.

This patch (W1-B, Task 1) was cut against the ORIGINAL (wrong) shape, where the native codec was
mirrored under BOTH 📥️import/🧩️deserializers/<facet>/<repr> and 📤️export/🧵️serializers/<facet>/<repr>.
The W2-P pilot proved that shape is not implementable (ArtifactDsl/ArtifactPack are single
bidirectional traits; Rust allows exactly one impl per type) — see the ⚠️ CORRECTION in 📓️design.md §1.

W1-E re-derived the change for the CORRECTED shape (native codec sits directly under
🚪️io/<facet>/{📝️text,💾️binary}, unsplit; import/export exist only for foreign dialects) and landed it
DIRECTLY — both 🔣️taxonomy.json and 🔍️discovery/🟦️component.ts are inside W1-E's own boundary, so no
patch file was needed for the walker/taxonomy half this time. The one downstream break this caused in
repo-lib's 🧪️index.test.ts (out of W1-E's boundary) is a separate, still-open patch:
  🔧️patches/w1e-index-test-io-semantic-collection-dirs.txt

Full diff, walker reasoning, and the real-disk proof (scratch directory + buildSemanticCensus) are in
📓️w1-e-report.md. This file is kept (rather than deleted) only as a pointer so nobody re-applies the
rejected mirror shape it used to describe.
