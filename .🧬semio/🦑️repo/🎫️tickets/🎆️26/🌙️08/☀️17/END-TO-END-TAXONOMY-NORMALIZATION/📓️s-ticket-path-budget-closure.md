# S-Ticket Path-Budget Closure

## Exact 31-entry population

The stale full inventory's 31 ticket/governance path-budget findings are four bounded groups:

| Group | Entries | Deterministic closure |
|---|---:|---|
| one historical ticket slug plus manifest | 2 | shorten the path identity while retaining the full title in `🎫️ticket.json`; rewrite exact ticket-ID references |
| historical window-policy fixture | 12 | compact the fixture's redundant app/fixture/mode scaffold while preserving its semantic window branches and bytes |
| leaked collision execution fixture in this ticket | 16 | retain the dangerous-path cases in the stable language-neutral golden and remove the materialized disposable checkout during final transaction cleanup |
| one Phase-5 audit report | 1 | move to a concise semantic report directory and physical Markdown leaf |

## Canonical decisions

The 264-byte historical ticket directory:

```text
26/04/09/FIX-WINDOWS-NATIVE-DEV-AND-BUILD-FLOWS-REPO-MCP-AND-AGENT-HOOKS-SKETCHPAD-DESKTOP-CODA-DESKTOP-AND-COMPOSE-GRASSHOPPER-REMOVE-UNIX-ONLY-COMMAND-ASSUMPTIONS-AND-VALIDATE-MONOREPO-COMMANDS-ON-WINDOWS
```

becomes the concise identity:

```text
26/04/09/WINDOWS-NATIVE-DEV-BUILD-AND-MONOREPO-COMMANDS
```

The manifest title remains byte-identical and continues to carry the full historical scope. This is a ticket-ID change, so every live structured reference must resolve through the full move map; historical prose that merely quotes the old path remains evidence unless it is an actual resolvable link.

The window fixture root retains `🧪️window-policy` but projects the redundant:

```text
🎛️apps/🧪️fixture/🎭️modes/🧪️mode/🪟️windows
```

to its evidenced semantic owner `🪟️windows`. Component filenames then follow the universal physical-leaf rule. The compact fixture still contains the component-window, options, actions, presence, utilities, and window cases; no test meaning is dropped.

The long Phase-5 report becomes:

```text
📓️p5b-owner-liveness-audit/📝️.md
```

Its internal title and content remain unchanged.

## Dangerous-path fixtures

The active ticket's `🧪️s-test-collisions-*` checkout is execution residue, not permanent authority. The stable repo-lib JSON golden already carries the long-path, reserved-name, Unicode, VS16, case-fold, and collision strings without requiring them to be checkout nodes. Final cleanup may remove the disposable checkout only after verifying the permanent pure-path tests cover the same negative cases and recording the checkout tree digest in the transaction journal.

## Acceptance

- zero ticket/governance paths above 240 UTF-8 bytes;
- zero physical reserved-name or trailing-dot/space sentinels;
- full historical ticket title/report content retained;
- exact structured references resolve after ticket/report moves;
- dangerous path strings remain covered in permanent language-neutral tests;
- no disposable nested Git checkout survives final cleanup;
- empty second plan.

Compose and `temp/compose` were excluded and not read. This packet is based on exact paths in the canonical inventory plus targeted live preimage inspection; it made no Git or physical-tree mutation.
