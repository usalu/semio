# goal-nonrepo — hypothesis CONFIRMED, distinguishable, fixed
Bases ARE distinguishable: `inspectRustJoinArgumentSpans` records ANY `.join("literal")` untied to its receiver; `rustManifestReferenceTokens` only ever tries `CARGO_MANIFEST_DIR` proof, so every other base gets "unproven" as one bucket. Added `inspectRustNonRepoJoinBaseSpans` (🔍️discovery/🟦️component.ts) proving root ∈ {`env::temp_dir()`, `env::var[_os]()`, `env::args()`, a bare `fn` parameter, one bounded same-file helper-fn hop for those}, wired into 🧹️normalization/🟦️.ts's `rustManifestReferenceTokens` to SUPPRESS (not just resolve) matching rows before emission.
Cleared, verified via real fail-before(blocked)/pass-after(absent) through the actual emitter (`📜️goal-nonrepo-verify.ts`, git-HEAD vs current): vector-converter `main.rs` 12/15 (`target`/`source` are `fn convert()` params, dispatched only via stored fn-pointer — no manifest anchor); stdio svg+dxf generators 4/4 (`Path::new(out_dir).join(id)`, `out_dir` a `fn` param); os `dsl/derive` `mutation_source_authority_tests::materialize()` 27 rows (one-hop traces `fixture_workspace()`'s own `env::var_os("SEMIO_TEST_ARTIFACT_DIR").map(PathBuf::from).unwrap_or_else(std::env::temp_dir)`).
Still correctly blocked (genuinely unknown, not cleared): vector-converter's `leaf.path()`/`entry.path()` accessor chains (3 rows, untraced); a SECOND `materialize()` in `mutation_aggregate_source_authority_tests` that pulls its base via tuple-destructuring a QUALIFIED cross-module call — no interprocedural tracing attempted, stays blocked.
Regression checks: `Path::new(env!("CARGO_MANIFEST_DIR")).join(…)` still proven/rewritten (zero overlap with the new non-repo set); a synthetic opaque-callee base still blocks; `independent-map-in-for-of-source-conservatively-suppressed` untouched.
Tests: `🧪️rust-finite-target-consumption` 86/86 pass (added missing dependency wiring for the new function + its ambient TS decl). `🧪️rust-physical-reference-context` 32/32 unaffected. `🧪️rust-writable-path-authority`: pre-existing broken BEFORE my change too (`rustSyntaxTokens is not defined`, confirmed via `git show HEAD` on that file — never wired that dependency; out of this fix's scope, matches prior slice's "stale pinned-hash checkpoint" finding).
Files: 🔍️discovery/🟦️component.ts (+`inspectRustNonRepoJoinBaseSpans`), 🧹️normalization/🟦️.ts (wiring), 🧪️tests/🧪️rust-finite-target-consumption/🟦️.ts, 🧪️tests/🧪️rust-writable-path-authority/🟦️.ts (dependency wiring only, still pre-existing-broken).
