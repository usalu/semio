# Fresh WGPU Current-Source Lifecycle Preparation

## Scope and Status

This is a new acceptance input, not reconstruction of any lost source snapshot, plan, journal or test. The source authority is the reviewed 32-preimage catalog `a640e9b8…673cf` and paired schema `64a11e3b…25945`. Only this new semantic ticket folder is owned by the lane. Production source, schema/catalog, WGPU outputs and the completeness guard remain unchanged.

The executable currently exposes **only read-only inspection**. No fixture, derived executor, compiler process, plan or transaction is created. Heavy work is held for the coordinator's transaction timing wave and explicit executor-boundary review. The seven phase declarations are acceptance requirements, not passing results or implemented transaction commands.

## Reusable Existing Contracts

- Production normalization exports: `inventoryTaxonomy`, `planTaxonomy`, `applyTaxonomyPlan`, `parseTaxonomyPlan`, `canonicalJson` and `noFollowTreeDigest`.
- Production discovery: exact package catalog/source/canonical authority, projected generator input parser/view, generated-leaf authority and optional ignored-output authority.
- Actual root producer: `renderWgpuBrowserBundles`, `renderWgpuPackageArtifacts`, `runWgpuPackageGenerator`, and stable OS-owner Nx preview/generate/check routes.
- Permanent transaction-v2 suite: fixed ticket admission, same-ticket attempt ordinals, exact byte/mode snapshots, retained canonical plan artifacts, progress `current`, durable interruption boundaries and child-process accounting. Its fixture helpers are private; importing the test module would register the full 62-case aggregate, so this runner will not import it as a helper library.
- Permanent `🧪️tests/🔍️filesystem/🟦️component.ts`: no-follow fixture traversal and byte-order sorting, usable only below the new validated disposable run root.
- Existing language-neutral WGPU retirement, output and phase vectors, plus the new current-source authority vector: referenced directly, not copied from vanished historical tests.

The expected structural packet is exactly 31 authored moves, one catalog-bound generated-source retirement and one WGPU regeneration producing five tracked plus one ignored artifact. Reference edits must be captured as a new canonical plan, independently parsed and explicitly reviewed before the first transaction. A historical edit count is not reused.

## Required Fixture-Only Execution Boundary

The public planner still appends one unconditional `nested-cargo-generation-unresolved` row inside `buildReferenceEdits`. Apply rederives authority using the same planner. There is no existing public fixture override. Removing an unresolved row from a returned plan would therefore be both insufficient and an invalid authority waiver.

Proposed for coordinator review, **not implemented**: compile a new fixture-owned normalizer executor from freshly fingerprinted production source. An independent TypeScript AST check must locate exactly the documented completeness clause in exactly `buildReferenceEdits`. Only that clause may be replaced in compiler input for the disposable executor; all package semantic, reference, generator, preimage, journal, membership and terminal checks remain unchanged. Exported executor entry points must reject any `repoRoot` outside the exact new no-follow run repository and reject changed schema/catalog/input fingerprints. The production file and its clause are never edited. A process-global environment switch, generic suppression list, filtered plan, alternate package identity or production fallback is forbidden.

The inspector identifies the precise AST span and current source hash without modifying or compiling the module. No guarded planning starts until the coordinator approves this boundary.

The coordinator classified any such derived-executor result as **fixture rehearsal only**, never current-production acceptance. Current production still rejects WGPU planning. A future guard-retirement decision and unchanged-production executor gate would be separate explicit work. The inspector's nine language-neutral confinement cases include production/acceptance/old-fixture roots, different run identity, lexical parent traversal, relative roots and no-follow symlink/file ancestors. The latter two use an in-memory filesystem-kind view; no symlink or fixture is created to test them.

## Closed Phase and Failure Requirements

The scenario declares separately observable capture/compiler, plan/review, rollback, fully staged interruption, first durable edit interruption, commit and fresh canonical empty-plan phases. Each phase records its complete diagnostic result; an empty-plan failure prints every unresolved row. Every subprocess must terminate and be joined before the next phase.

The existing **120000 ms complete positive-lifecycle bound is one monotonic end-to-end deadline**. Splitting observation into phases does not reset or raise it; pauses within that positive execution count too. Execution-boundary and fixture-design approval precedes launching the positive run. Negative matrices remain independently required; no case is silently dropped to fit the positive lifecycle budget.

The new fixture is built only from exact current no-follow source leaves and the verified tool/compiler closure. Any required root workspace metadata construction must retain the same WGPU package/Cargo/Nx identities and be reviewed explicitly. Unrelated production generated consumers are not silently copied as stale bytes or removed from the schema to make the fixture pass. If a minimal closed fixture cannot legitimately own an activated generator, that is reported before execution rather than bypassed.

Current compiler acceptance must distinguish actual source/canonical browser compilation, native build-script/operand parity, Cargo metadata and full WGPU crate compilation. A metadata parser or small Rust probe is not evidence of a full crate build. The exact compiler route and its input closure are part of the next reviewed fixture-construction step.

## Retention and Run Control

Use the local VS Code launch after opening this semantic folder, or its named Nx target. No `nx exec` is used. New run identities go below `🧾️runs`; fixture roots cannot alias the production repository or earlier ticket runs. Scripts, scenario inputs and Markdown remain. Generated outputs are disposable only after their active validation/recovery purpose finishes; transaction artifacts stay intact until safely terminal and explicitly disposable.

## Read-Only Inspector Result

The initial inspector and the expanded guard/confinement inspector both ran through the named fixture Nx target and exited 0. The expanded run at `2026-08-27T14:34:12.736Z` verified the current 32-file census and all pinned source preimages, independent JSONC parsing of the authored scenario/schema/catalog/vectors, the unique TypeScript AST guard, four hostile guard mutations and nine root-confinement cases. Guard mutations were only strings submitted to the AST inspector; no derived executable was emitted or run.

Exact observed identities:

- Schema: `64a11e3b97c9b21c26093d82d017ef8478f2ef0af7dd4fe383ca455d08a25945`.
- Catalog: `a640e9b8e5154f0cb6276931d55564c8284b6ff45f10b366d089831d431673cf`.
- Normalizer: `ead932b049d1c18bb9d484c7c8bc5cb2d893b4adb8c1f8abb1f84eb747a68cfd`.
- Sorted source identity census: `2fb85daa5597cd50436b3dc23e33e6bc1224882602a3f6ff0d37ce6024f2237a`.
- Exact guard clause: `f3434285de359048066fe89572c49bd44f5451914855ff09254eccebf5b5ea4c`; UTF-16 source offsets `[324972, 325174)` in that normalizer identity.

The inspector found **54 retained negative vectors** and **18 newly declared retirement/membership/resume cases**. Those are coverage requirements, not 72 executed lifecycle tests. Only the inspector's actual AST/confinement assertions ran. Source/current compiler, producer/output, rollback/resume, membership and final empty-plan execution remain pending.

Exact command, working directory this semantic folder:

```sh
NX_DAEMON=false NX_ISOLATE_PLUGINS=false bun '/Users/ueli/Documents/semio/node_modules/nx/bin/nx.js' run '@fixture/wgpu-current-source-lifecycle:inspect' --skip-nx-cache
```

All inspected production fingerprints were rechecked unchanged at the end. No fixture repository, plan, journal, transformed executor or compiler output was created.

The final inspector, including the fixed confined directory names, complete existing-ancestor checks and explicit single monotonic deadline declaration, reran successfully at `2026-08-27T14:35:47.127Z` with identical production/source identities. After all inspector processes exited, only seven enumerated disposable files in this folder's `.nx` cache and their empty directories were removed; they can be regenerated by Nx. All seven authored input/script/config/report files remain. The lane has no running process and is released for coordinator review; compiler/lifecycle work remains held.
