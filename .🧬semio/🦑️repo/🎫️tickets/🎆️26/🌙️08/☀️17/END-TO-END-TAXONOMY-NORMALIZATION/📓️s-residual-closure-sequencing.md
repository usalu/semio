# Residual Closure Sequencing

## Stable Boundary

The schema-first mutation, CAD, Draw, and generator-preview authorities are closed before any physical repository apply. The direct focused integration boundary is seven passing tests, zero failures, and 190 assertions. Strict taxonomy v7 validation reports zero schema problems and exactly three semantic path-projection contracts.

The previous full inventory is not a current acceptance artifact. It predates the CAD, Draw, preview, mutation-registry, and concrete-manifest closures. Its 103,892 entries and 41,539 findings remain useful only for ordering the residual work:

| Finding | Previous count | Closure authority |
| --- | ---: | --- |
| `file-kind-unresolved` | 15,767 | emoji-first/contextual file-kind selection, exact contracts, ticket-evidence kinds |
| `path-too-long` | 14,511 | mutation/CAD/Draw projections, compact ticket profiles, misplaced-root relocation |
| `semantic-stem-unresolved` | 7,358 | owner-local semantic membership and parent-owned stem elision |
| `directory-kind-unresolved` | 1,373 | structural kinds and owner-local semantic membership |
| `directory-kind-ambiguous` | 915 | parent/ancestor contextual selection |
| `semantic-stem-ambiguous` | 853 | contextual semantic-directory selection |
| `package-implementation-destination-unresolved` | 390 | recursive language-specific package purity plus explicit semantic owner |
| `package-role-unresolved` | 341 | strict grammar classification and exact configuration contracts |
| `package-implementation-file` | 16 | planned move outside package adapter boundaries |
| `symlink-absolute-target` | 13 | first-class non-dereferencing symlink-target edits |
| `trailing-dot-or-space` | 1 | exact redundant-evidence removal |
| `windows-reserved-name` | 1 | serialized negative fixture plus checkout-path retirement |

## Ordered Closure

1. Implement CAD/Draw projection planning from the shared discovery authority and prove exact 220-move, reference, rollback, cancellation, resume, and convergence behavior without applying the live tree.
2. Re-run a current immutable inventory and group remaining findings by exact resolution cause. No prior count may be reused as a current pass claim.
3. Close exact fixed-contract and emoji/context disambiguation gaps before adding new semantic registry data. In particular, repository-wide `📜️script.ts`, `📋️project.json`, package manifests, and governance files must be exercised by production tests rather than duplicated as broad suffix exceptions.
4. Load owner-local semantic members from schema-owned manifests and registries. Unknown members remain blocking; no wildcard emoji fallback is admissible.
5. Add first-class symlink-target and redundant-preimage operations to the transaction model. These operations must participate in plan digest, preflight, affected-state digest, rollback, cancellation, resume, and empty-second-plan verification.
6. Relocate embedded ticket roots only under the exact evidence-preserving authority recorded in `📓️s-misplaced-ticket-roots.md`; many-to-one content-identical rows become explicit redundant preimages.
7. Plan the complete live repository, require zero unresolved findings and zero collisions, then apply exactly that frozen digest.
8. Replan from the post-state and require zero moves, edits, regenerations, symlink edits, removals, and unresolved findings before transitional compatibility logic can be deleted.

## Safety Invariants

- Actual `compose/**` and `temp/compose/**` remain untraversed, unread, and unmodified; their intentional absence requires no digest.
- No Git state mutation is part of normalization.
- The physical mutation, CAD, and Draw trees are changed only by the final journaled plan, never by manual moves.
- A current-tree census is taken only after all active shared writers reach a stable boundary.
