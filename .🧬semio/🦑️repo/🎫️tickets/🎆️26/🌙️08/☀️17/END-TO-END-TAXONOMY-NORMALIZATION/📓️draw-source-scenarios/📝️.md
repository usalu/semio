# Authored Draw Source Scenarios

## Boundary

This implements the first bounded action from the [durable Draw audit](../📓️s-draw-durable-acceptance.md): self-contained source-scenario input, without changing the production layout or switching live Draw acceptance to canonical. CAD remains explicitly in its live source phase. Historical mapping and purity catalogs, production taxonomy, discovery and normalizer code are not owned by this work.

The new permanent input is the repository library's `🧪️tests/🧪️draw-source-scenario/🔣️.json`, governed by its sibling `🧬️schema/🔣️.json`. Its eleven hand-authored members and five minimal consumers cover Cargo configuration, Rust module references, Nx cwd/glob fields, dependency-user paths and the TypeScript path collection. It is not a copy of the old 242,790-byte source tree or generated plan output.

## Retained-Run Authority

The coordinator approved this exact semantic parent: `📓️draw-source-scenarios/🧪️runs` beneath the active ticket. Only artifact-projection fixture callers may select it through an optional parameter; other normalization fixtures retain their existing defaults. Each run must have no-follow ancestry and a unique owned directory, and record its outcome in Markdown. Authored inputs and all failure/recovery evidence are retained. No generated output is to be deleted in this lane yet, including terminal output, until exact manifests are reviewed.

Before any materialized run, direct directory inspection confirmed that this parent was not an existing symlink or competing fixture location. Loose prior ticket-scoped Draw fixture inputs were absent. No missing input was restored and no broader cleanup was performed. Either actual Compose tree remains opaque; isolated fixture sentinels are separate inputs.

## TDD Evidence

The first actual uncached Nx invocation selected only the two new pure tests (`authored Draw source`). Result: **1 passed, 1 failed, 47 assertions, 715 ms**. The schema/independent JSONC, TypeScript and TOML parser oracles passed. The source-isolation test failed because the old helper returned the real Cargo manifest instead of the authored minimal input. No materialized fixture was allocated for this red run.

The implementation now makes the input choice explicit (`live` or `authored-draw`), rejects cross-contract use, unknown placeholders and absent authored members, and does not fall back between input sources. The synthetic canonical-authority test uses authored Draw input; the live source filesystem-parity test retains its real source read and historical preimage assertions. Subsequent checks and materialized run outcomes will be recorded here.

The subsequent pure source-builder test was red because it observed a live Draw source read; the guard also disallows both live root Cargo/dependency inputs and external Draw package reads. The retained-parent test was red before its exact-parent implementation existed. After the changes, the full pure/authority selection passed **6 tests, 0 failures, 124 assertions in 1.14 seconds** through the named uncached Nx route. Its language-neutral retention cases cover a real directory, symlink, non-directory file, missing directory and unreadable ancestor; an unapproved lexical parent is rejected before inspection or creation.

The shared fixture now reads only the explicitly permitted CAD source files and its four CAD consumers from the live workspace. All eleven Draw files and five external consumers come from the new authored input. The two synthetic Cargo preimage assertions bind those authored bytes; the separate live Draw authority test still binds the historical real Cargo hashes. No location, adapter, old/new value, configurable-entry count or historical mapping assertion was dropped.

The six materialized artifact callers select the approved parent through the optional argument. Their previous blanket recursive deletions have been replaced with a finalizer that verifies the exact parent, unique run directory device/inode and unchanged regular Markdown report before recording completion. It retains all files, including generated and recovery evidence. Other normalization fixture defaults and callers are unchanged.

## Materialized Prerequisite

The first materialized mapping case was **0 passed, 1 failed in 5.08 seconds** through the named uncached long-test route. The planner stopped before producing a plan because its unchanged registry input-discovery contract required the declared registry implementation entry, which the old shared fixture did not supply. This was not a Draw mapping failure. The run and its failed outcome remain at [the exact retained run](🧪️runs/🧪️s-test-artifact-projection-plan-cxHWVT/📝️.md); no input or output was removed or rewritten to conceal the failure.

The coordinator approved one explicit fixture prerequisite: the language-neutral input now authors a dependency-free TypeScript compiler root containing exactly `export {};` followed by a newline. Materialization writes those bytes only at the cloned schema's single declared registry implementation entry. The production discovery contract remains intact. This is a fixture-only compiler input, not a simulated registry publication. The independent TypeScript parser checks the seed, and every retained artifact plan asserts that no plugin-registry regeneration is selected. The next materialized run is held until the concurrent transaction gate reaches its terminal outcome.

After the coordinator released that window, the expanded pure packet passed **6 tests, 0 failures, 130 assertions in 2.35 seconds**. The next materialized case reached all existing mapping and reference assertions but failed its final no-unresolved assertion: **0 passed, 1 failed, 86 assertions in 6.82 seconds**. Its sole error was the strict Rust resolver refusing the copied CAD interaction-spec's manifest-relative join because the old shared fixture supplied no governing Cargo module context. This run remains at [its exact retained directory](🧪️runs/🧪️s-test-artifact-projection-plan-k0GN5b/📝️.md).

The approved prerequisite now declares one small authored Rust library mount and its exact Cargo `lib.path` in the language-neutral fixture. It uses the already-existing external consumer manifest coordinate and retains that manifest's package name. The copied CAD source bytes and its `CARGO_MANIFEST_DIR` join operand are unchanged. The mount's independent TOML and nightly Rust AST parser facts match the repository's module facts; the graph proves one governing manifest, including the correct root module context. The new source-builder test first failed on the missing mount (**0 passed, 1 failed, 1 assertion in 1.339 seconds**). After materialization was added, the final pure packet passed **7 tests, 0 failures, 141 assertions in 1.182 seconds**. Intermediate oracle assertions were corrected to distinguish inline module contexts from unique manifest ownership and to decode the Rust AST's explicit Unicode escapes; no runtime grammar or existing test assertion changed.
