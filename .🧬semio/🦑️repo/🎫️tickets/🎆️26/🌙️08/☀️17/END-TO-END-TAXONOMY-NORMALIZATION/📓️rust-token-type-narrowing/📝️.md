# Rust Token Type Narrowing

The interactivity task reported strict TypeScript errors on `kind` and `text` in the identifier branch of `inspectRustJoinArgumentSpans`. Both literal predicates declared `token is RustToken`, although they accept only a subset of Rust tokens. TypeScript therefore excluded the entire RustToken type from the false branch, making the identifier token unreachable (`never`).

The language-neutral Rust-reference fixture now names both affected functions and six token cases. A permanent regression extracts their actual predicates and token types, runs TypeScript semantic checking with strict options, and compares independently compiled Bun/TypeScript runtime results. No predicate condition, Rust grammar, ownership admission, or reference output is intended to change. The existing Rust gate/launch registration is reused.

The intended red run reproduced **both TS2339 diagnostics** exactly: `kind` and `text` on `never`. It reported **0 passed, 1 failed, 2 assertions in 7.76 seconds**; the semantic check also exceeded Bun's default five-second case limit on the loaded host. The fixture now loads only its required ES5 and ES2015-core standard libraries, omitting unrelated default DOM declarations. No timeout or diagnostic filter was relaxed.

Both predicates now return `token is RustToken & { readonly kind: "string" }`. Their runtime conditions are byte-identical to the pre-fix expressions. The focused strict semantic and independent runtime test passed **1 test, 28 assertions, zero failures in 2.23 seconds** (session `95245`), printing its `[DEBUG]` success marker. The entire strict React project was not rerun by this lane; the coordinating task will rerun its own gate.

The root startup boundary is also coherent enough for the exact interactivity gate: uncached `workspace:verify-interactivity --args='tool-jobs --self-test'` passed with 940 self-tests. See [root compiler consistency](../📓️root-script-compiler/📝️.md) for the actual run and independent compiler results.

Actual uncached `@semio-tech/repo-lib:test-rust-physical-reference-context` completed successfully after the annotation fix: **20 tests, 155 assertions, zero failures in 30.47 seconds**, exit zero (session `39663`). The strict narrowing case took 631 milliseconds. The suite also reran real Cargo readers, rollback/retry/commit, empty replanning, cancellation, missing/ambiguous/symlink ownership negatives, newly relevant incoming references, and the independent `syn` oracle. This is the complete current registered Rust packet, not a strict React-project typecheck.

## Independent Coordinating-task Confirmation

The interactivity task (`01a0236e-373b-7fe0-969e-dd69a1e193b7`) independently reran the exact uncached tool-jobs gate and confirmed the same 940 self-tests and 33/255/25 ownership counts. Its full strict React rerun confirmed **both discovery TS2339 diagnostics are gone**. That project remains red for seven tutorial interaction joins and one test-first missing retained-operation module owned by the interactivity task; neither was attributed to taxonomy. Its separate actor regression passed 60/60. These are reported independent results from the coordinating task, not extra runs performed by this lane. Active WGPU source edits and strict missing-active-output checks remain preserved.
