# Historical Package Identity and Retention Recheck

## Current boundary

The four genuine production golden inputs were re-read after the reported ticket-input disappearance and remain regular files with their exact previous hashes:

| Input | SHA-256 | Bytes |
| --- | --- | ---: |
| Remaining-package purity census | `49087f2dee618c3c8392ce3bc25c348af87d5380fe16523ed61ee6c02448f577` | 241861 |
| Original nested-Cargo authority | `88619870710263fe4e968714a903dace8c2dc79c619fdc5df87950cf21653972` | 16186 |
| CAD/Draw projection vectors | `1410a74ccc87561fd4a4b91db7d503614fe21ddce8bc78dee923d8237820f3e0` | 122279 |
| README/LICENSE authority | `051394741822e92d51f3bda15ce64d84c236582c6927335c9c5e0ac3c18a1da4` | 53514 |

The following ticket inputs are absent (`ENOENT`) although their parent directories and Markdown reports remain: the Draw causal-classification JSON, purity-census JSON, purity test, README authored-correction test and vector, CAD/Draw preimage archive, and the previously recovered historical taxonomy snapshot at `🧪️s-test-ticket-important-exact-apply-9IU3He`. The exact cause of disappearance is not established. Prior green test reports remain historical results, not current runnable evidence. No missing compiler output or disposable scanner report was recreated.

The old full Draw plan and older nine-case README packet are not being claimed as freshly reproduced. The lost historical taxonomy/archive checks require exact input recovery or an explicitly redesigned permanent fixture; no guessed old bytes are accepted.

## Genuine permanent input regression

The bounded replacement lives at [the canonical test](../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧪️tests/🧪️historical-package-owner-identity/🟦️.test.ts) and its adjacent language-neutral `🔣️.json`. It uses the surviving immutable purity golden, not a reconstructed census.

A representative 1907-byte source preimage was independently re-read and matched the census's mapping 29 SHA-256 `66814d3e84264f1ae203b848737771517160d7a050643504d8f8fd57da456c4a`. The permanent input retains those exact bytes as a declared base64 payload with hash and size; it does not depend on the historical source filename continuing to exist.

Current package discovery is tested independently in a tiny isolated repository. The source is relocated, a new implementation is added, bytes change, files are retired outside the package boundary, and the original fixture state is restored. Git-admitted current rows agree with fast-glob; WebCrypto independently checks changed-byte hashing. The historical snapshot and retained preimage remain unchanged at every transition. These tests do not claim a fresh whole-monorepo census.

## Exact prefixed-owner authority

The original Draw capture proposed four full-path changes in mapping 29. A later isolated plan, after the quote-boundary parser correction, additionally detected the owner-path suffix inside the `packageId` field. Thus the earlier 125-edit capture did not include this newly detected unsupported span.

The coordinator approved the distinct `recorded-package-owner-identity` representation. It requires the exact `unmarked:` prefix and a source role. The complete JSON document digest and exact pointer bind the complete package identifier; only the derived repository-relative suffix span is returned as a coordinate. Wrong prefixes, absolute or escaping suffixes, empty suffixes, nested prefixes, and destination-role misuse fail closed. This does not classify an entire package identifier as a physical path or allow arbitrary substrings.

The initial permanent test failed at the unimplemented declaration contract: **0 pass, 1 fail, 2 assertions, 141 ms**. After the closed union, validator, and pure resolver changes, the three-case packet passed **3 pass, 0 fail, 65 assertions, 380 ms**. Ajv independently checks the declaration shape; jsonc-parser independently supplies full-value offsets from which the exact suffix range is derived. The production purity evidence row had not yet been added at that checkpoint.

## Permanent registration and runtime proof

The production registration test first failed with **0 pass, 1 fail, 1 assertion, 155 ms** because `remaining-package-purity-history-v1` was absent. The added row binds the unchanged 241861-byte golden and precisely five pointers: the original four path fields plus the separately approved fixed-prefix package-owner suffix. It grants no broader fixture ownership.

After registration, the permanent five-case packet passed **5 pass, 0 fail, 81 assertions, 4.21 s**. The exact command was:

```sh
bun test '/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧪️tests/🧪️historical-package-owner-identity/🟦️.test.ts'
```

The isolated Draw lifecycle uses eleven structural source paths from the surviving CAD/Draw input and minimal synthetic source bodies, with the independently verified 1907-byte preimage at the one relevant historical source. It does not recreate or claim the missing 229-entry preimage archive. The plan has eleven moves, one ordinary-neighbor edit, no historical-golden edit, and no regeneration. An injected after-edits failure rolls back; the same ticket then commits successfully; the canonical second plan has zero moves, edits, or errors. Adding an undeclared owner-identity field to the bound fixture, even with an updated fixture-only digest, yields `frozen-coordinate-evidence-unowned` and never edits the historical document. The completed fixture transaction output directory is deleted only after terminal commit and empty replan; test input files remain.

## Nx and launch integration

A sixth language-neutral case declares the Nx target and both launch entries. It compares the native JSON parser with jsonc-parser for project metadata, then checks identical seed/output launch definitions. Its preimplementation run failed **0 pass, 1 fail, 2 assertions, 119 ms** on the absent Nx target; the focused green run passed **1 pass, 0 fail, 7 assertions, 250 ms**.

The existing package `📜️script.ts` now dispatches `test historical-package-owner-identity` directly to the absolute canonical test path through the existing bounded runner. `test-historical-package-owner-identity` invokes that route. Both `.vscode/🧩️launch.seed.jsonc` and `.vscode/launch.json` contain the matching `4_gate` entry at order `410.07`, after artifact support `410.06`.

The actual routed command completed successfully:

```sh
NX_DAEMON=false NX_NO_CLOUD=true bun nx run @semio-tech/repo-lib:test-historical-package-owner-identity --skip-nx-cache
```

Console result: **6 pass, 0 fail, 88 assertions, 4.94 s**, followed by Nx's successful-target result. This reran the declaration, historical bytes/pointers, exact production registration, launch parity, independently current census, and rollback/retry/empty-replan cases. Fresh workspace taxonomy validation also returned `[]`. No fixture process remains. These results do not constitute a new full-repository plan or production apply authorization.

## Updated output-retention disposition

The new AGENTS instructions retain genuine inputs and Markdown research while disposing of completed tool-generated output. This changes the disposal policy, not the meaning of existing historical reports.

All 35 other-ticket JSON outputs were re-read and still match their 35 registered SHA-256 values. The nearest owning ticket manifests are all **open**:

| Nearest owner | Output count | Current state |
| --- | ---: | --- |
| Microkernel Pooled Actor Plugin Runtime | 2 | open |
| Phase 2 Resumable Job And Progress Protocol | 2 | open |
| Resumable FEM Job Graph | 3 | open |
| Interactive Job Runtime Refactor | 28 | open |

The exact per-target paths and hashes remain in `frozenCoordinateEvidenceContracts` under the 35 `draw-evidence-` keys. The producer/consumer semantics are recorded in [the causal review](📓️s-draw-reference-evidence-semantics.md): scan or verifier observations, not JSON replay inputs; S1 scan/apply mode remains unproved. Their temporary freeze-proof test was our own new consumer, not evidence that the reports were pre-existing runtime inputs.

The coordinator explicitly forbids disposal of another active ticket's outputs. Therefore these 35 files and exact registrations remain while their owners are active. This blocks disposal only; it does not block normalization. Future owner-authorized disposal can remove obsolete registrations in the same reviewed change. No other-ticket output was deleted or relabeled as a permanent input.

All six own-ticket compiler bundles also still match their prior hashes and are mode `0755`. They are completed compiler outputs, not permanent JSON evidence. Their exact consumer/terminal-work review and retirement are assigned to the artifact lane. No bundle has been deleted by this lane.

That paragraph records the preceding checkpoint. The coordinator subsequently reassigned the six-output retirement to this lane; the completed scoped removal, exact recovery blobs, and ten historical report-link substitutions are recorded in [Completed Compiler Output Retirement and Historical Report Links](📓️s-completed-output-retirement-and-historical-links.md). No other-ticket output or genuine input was removed.

## Safety

No production move, generator, global incoming capture, actual Compose access, or real-workspace Git mutation was performed. Tests only create isolated inputs inside this ticket. Existing Markdown reports and genuine golden inputs remain unchanged.
