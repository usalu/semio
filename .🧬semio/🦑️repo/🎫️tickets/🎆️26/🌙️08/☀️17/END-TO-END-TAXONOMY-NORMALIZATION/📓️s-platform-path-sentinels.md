# S-Platform Path Sentinels

## Trailing-space evidence

The one physical trailing-space basename is a 38,467-byte FEM test log in ticket `26/08/05/FEM-PLUGIN-MIGRATION-TO-CRATE-AND-TAXONOMY-CONSOLIDATION`. Its SHA-256 is `d5246ed036182a3505508fe7c264d1da42fac2d46b2ff391ca3c251cbbebf7fc`.

The bytes are already retained three times under non-blank sibling names:

- `🧪test-final2.txt`;
- `🧪️test-final2.txt`;
- `fem_test_out.txt`.

All four files have exactly the same digest and byte count. The blank basename therefore carries no unique content, semantic identity, mode, or link identity. It must not be trimmed into an empty filename and must not be materialized on native Windows. The final plan should treat it as an exact redundant-evidence removal while retaining the three byte-identical named records and recording the four-way preimage digest in the transaction evidence.

## Windows-reserved evidence

The stale inventory's only reserved device path is `CON.ts` inside a retained disposable collision fixture below the active normalization ticket. Its value is a path-policy sentinel, not a production TypeScript source. Cross-platform permanent coverage must keep the string in the language-neutral normalization fixture and pure policy tests, not as a checkout path.

Leaked execution fixtures containing reserved path nodes must be consolidated into serialized ticket evidence and then removed by the final transaction cleanup. The semantic test remains mandatory; only the filesystem-hostile materialization is transitional.

## Acceptance

- zero physical trailing-dot/space paths;
- zero physical Windows-reserved path segments;
- the serialized negative tests still reject both classes before mutation;
- the FEM log content remains retained under three named records with the exact digest above;
- the final transaction ledger records any redundant-evidence removal with its exact preimage.

Compose and `temp/compose` were not read. The audit used only the exact ticket paths above and made no Git or workspace mutation.
