# Energy Support Acceptance Run Ownership

The permanent Energy support tests are being given an explicit semantic retained-run parent before further materialized runs. Existing semantic vectors, source-phase readiness, transaction behavior, compiler assertions and time budgets remain unchanged. The production Energy owner is not being moved or switched to canonical acceptance in this change.

New fixture and oracle roots will live below this report's `🧾️runs` directory. Every exact ancestor is checked without following symlinks. Authored fixture inputs and all active or failed transaction evidence remain retained. The existing narrowly scoped completed Cargo-output retirement contract stays separate; failed compiler runs must retain their output. No earlier fixture or missing evidence is recreated.

## Executed Ownership Checks

The new closed language-neutral owner/prefix/rejection vector was checked with independent Ajv and fast-glob assertions. The first bounded actual Nx invocation (session `74727`) failed before allocating a fixture: **0 passed, 1 failed, 2 assertions, 266 ms**, because the new allocator did not exist. After the exact owner and no-follow helper implementation, the focused actual invocation (session `5121`) passed **2 tests, 24 assertions, 291 ms**, with 17 other cases filtered. Commands explicitly selected `nx exec --projects=workspace --skip-nx-cache` and the absolute permanent test path.

That focused check also executed the existing exact successful-output retirement contract against its newly authored dummy Cargo lock and dummy binary. Only those fresh disposable test outputs were removed; authored inputs and Markdown remained. Foreign-role roots and linked output targets were rejected without deleting their sentinels. No historical or unrelated path was removed.

The additional failure-disposition regression failed as expected (session `51163`): **0 passed, 1 failed, 12 assertions, 678 ms**. Passing `completed: false` to the old one-argument helper still retired its newly manufactured dummy failure outputs. The helper now requires the completed disposition before retirement, and the actual Rust oracle passes its result to that helper. The language-neutral contract explicitly requires retaining failed runs. No historical compiler output was involved.

The corrected focused packet (session `42184`) passed **2 tests, 26 assertions, 602 ms**, with 17 unrelated cases filtered. It proved complete failed-run retention alongside exact successful-output retirement and unsafe-root rejection.

## Full Named Gate and New Source Drift

The actual uncached `bun x nx run @semio-tech/repo-lib:test-artifact-support --skip-nx-cache` (session `55126`) finished **18 passed, 1 failed, 147 assertions, 7.81 s**, exit 1. The small source fixture completed rollback, retry, actual Nx generation/check, runtime `[DEBUG]` observations and an empty second plan in 4.866 s. All source-ownership negatives and new retention cases passed.

The complete live-owner case stopped at its existing exact byte precondition: **383574 current bytes versus 383479 reviewed bytes**, a 95-byte increase. The physical node and 195-file checks had passed; no full-owner transaction or Rust compiler oracle was reached. The expected source digest/bytes were not silently refreshed. Current source drift must be compared to retained inventory and reviewed before a fresh full-owner acceptance claim. All new run inputs and failed evidence remain beneath this report's `🧾️runs` owner.

No actual Energy reference plan or production move is claimed.
