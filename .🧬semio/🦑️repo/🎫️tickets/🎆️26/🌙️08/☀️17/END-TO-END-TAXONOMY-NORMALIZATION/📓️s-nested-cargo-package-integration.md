# Nested Cargo Package Integration

## Scope and Status

The production taxonomy, discovery and normalizer consume the exact **32 WGPU + 4 JCO source mappings**, **27 purity mappings**, **five adapter templates**, **two source-owned WGPU splices**, and **one derived semantic registration leaf**. The composed catalog is `🧪️nested-cargo-package-projection/🔣️.json` below the repo library TypeScript fixtures, SHA-256 `100ab48d49f0cb7fa91eed19dcb03803988d253929b00217e8c3acd3590338f4`.

JCO has source/canonical compiler proof and a committed, rollback/retry/idempotence fixture. Its dependency baseline is now correctly modeled as authored ratcheted policy state with an exact users-only coordinate projection; no automatic baseline-refresh producer is authorized. This is **not production apply readiness**: a fresh actual-ticket live plan with real generator closure is still required. WGPU source/canonical structural authority is implemented; its complete functional projection remains blocked as listed below. No production package move or real-repository Git mutation was performed by this lane.

## Authority Implemented

- `semanticPackageProjectionCatalog` validates no-follow regular catalog identity, SHA-256, exact package/count/mapping ownership, source hashes and sizes, case/NFC/VS16 collisions, path budgets, adapter classifier roles, and the JCO generator's exact manifest/input/output binding.
- `semanticPackageProjectionAuthority` validates complete source or destination facts, exact Cargo/Node/Nx identities and configured targets, standalone JCO workspace/WIT authority, WGPU workspace membership and Vitest metadata, and all package/ecosystem/target entry registrations. Missing, extra, duplicate, changed, symlink, gitlink, foreign and collision facts produce no actionable mappings or adapters.
- WGPU `rust-build-entry` is explicitly registered in the package boundary, Rust ecosystem and WGPU target. The normalizer passes the complete `discoverySchema` into package authority; its smaller operational schema intentionally does not carry all ecosystem fields.
- `projectNestedCargoPackages` projects only catalog leaves. It checks physical children without following links and admits only the exact declared WGPU ignored boundaries. No parent-directory move carries ignored files along.
- The JCO lock relocation preserves its exact source hash/size; it does not authorize an external lock writer or arbitrary lock edits.
- Root `📜️script.ts` implements JCO preview/generate/check. The existing OS Rust router/project forwards the commands, and `.vscode/🧩️launch.seed.jsonc` registers them. This lane did not regenerate `.vscode/launch.json`.

## JCO Runtime Correction

The original purity proposal had two real compiler failures: an `include!` wrapper rejected the implementation's inner documentation with **five E0753 errors**, and a source-relative WIT operand resolved to a nonexistent location. Installed `wit-bindgen-rust-macro` **0.57.1**, `src/lib.rs:205–208`, resolves its operand against `CARGO_MANIFEST_DIR`. The schema golden and composed catalog therefore retain the manifest-relative `🧬️schema/📜️world.wit` operand; only the prose link is rebased.

The exact generated package entry is:

```rust
//! 🧩️ JCO probe guest package adapter.

#[path = "../../../🧩️component/🦀️.rs"]
mod component;
pub use component::*;
```

Both original and canonical packages passed `cargo check --lib --locked --offline` on the native target and `wasm32-wasip2`: **1 test, 0 failures, 5 assertions, 94.89s** including the first wasm dependency compilation. Compiler artifacts remain under the ticket's `🧪️nested-cargo-compiler-target`. No runtime dependency was added.

## Generator Activation and Source Coordinates

`jco-package-adapter.projectionActivation` requires the exact catalog package and manifest paths. First creation activates only when all four exact hash/size-bound JCO moves are present. An unrelated root-script edit while JCO remains source-only does not create an adapter. A canonical package remains eligible for producer-input regeneration. Missing or contradictory manifests and partial projections fail closed.

`isFrozenSourceCoordinateToken` uses a validated catalog digest plus exact JSON pointer, value and token span. Only declared source-coordinate locations are retained. The same token in an unrelated schema reason or an added catalog property is still rewritten; the whole file is not ignored. `jsonc-parser` independently verifies fixture token offsets and pointer paths. Schema identity is normalized repo-relative, not an absolute-path fallback.

The regression initially found five erroneous schema edits: JCO `projectionActivation.sourceManifestPath` and four source `inputPatterns`. They wrongly activated `graph-catalog`. The cause was comparing a repo-relative consumer with absolute `LoadedTaxonomy.path`. That comparison is fixed. Temporary graph-catalog fixture isolation was removed; the correct JCO fixture plan again has exactly **four moves, two edits, one JCO regeneration**, with no schema edits or graph regeneration.

The language-neutral packet is `🔣️nested-cargo-generator-activation.json` in this ticket. It also distinguishes exclusion IDs from paths: `compose/` and `temp/compose/` are opaque; literal `temp-compose/` and a nested `compose` basename are not additional exclusions. These tests operate on path strings, not those physical trees.

## Transaction Boundary and Retained Evidence

Same-ticket retries use fresh ordinal attempts. Changing admission tickets is not equivalent and is not silently recovered. The fixture proves source bytes restored after an injected post-generation failure, generated adapter removal, retry ordinal `000002`, exact Cargo metadata identity/root/target parity and an empty second plan.

The integration exposed three transaction problems fixed by the root lane: removal of an already-absent generated output whose parent is absent; moved-and-edited destination shadow ordering in logical generator inputs; and package authority running before the exact eventual source-parent prune set was projected into its view.

The last issue is pinned by `🧪️nested-cargo-integration-dhSRzW/🧪️evidence/🔣️empty-source-plan.json`. An empty old JCO `🧬️schema` produced four post-plan findings: package authority rejection, unresolved canonical library directory, and unresolved canonical WIT role/stem. The public inventory still rejects that unplanned empty directory and any nonempty child. Removing only that exact empty fixture directory restores clean canonical inventory (**1/0/5, 2.75s**). Root's private apply inventory now supplies only the previously proven prune set; final set recheck, journal binding, cleanup and rollback semantics remain intact. See `📓️s-source-parent-prune-journal.md`.

Root's focused committed lifecycle rerun passed **1/0/23, 23.75s**, including Nx generate/check twice. A subsequent full integration run exposed the complete-schema caller issue described above (**10 pass, 6 fail, 122 assertions, 36.62s**). The corrected source inventory/prerequisite/canonical packet is **3/0/15, 3.07s**. The final full integration rerun passed **16 tests, 0 failures, 151 assertions, 80.00s**, including the transaction (41.66s) and warm native/wasm checks (8.16s). This process loaded before root mechanically replaced the remaining19 partial-schema casts at unrelated discovery callsites; root owns that subsequent AST boundary regression.

Other retained failures include `🧪️nested-cargo-integration-p5nssk` (absent-output-parent rollback), `🧪️nested-cargo-integration-mYc5eq` (generator input membership), `🧪️nested-cargo-integration-PLTp1P` (four immutable catalog source rows falsely counted as stale), and `🧪️nested-cargo-integration-ais68K` (the five erroneous schema edits). Diagnostic plans are canonical bytes and passed explicitly as `planArtifactPath`; a prior noncanonical diagnostic file was rejected before journal mutation.

## Verification Commands and Results

### WGPU Registration Compiler Boundary

The whole original `🦀️lib.rs` is an exact hash/size-bound preimage for the new library package adapter. The original renderer glue's first line is a second exact splice preimage. The `wasm_bindgen_futures` alias stays at crate root by being merged into the renderer's include-expanded source. The renderer and its private aliases are not moved into a new Rust module.

The first attempted macro merge failed a real compiler check: a `#[macro_export]` declaration inside `include!` cannot be accessed through the existing `crate::action_args_json!` paths under `macro_expanded_macro_exports_accessed_by_absolute_paths`. The failed fixture is retained as `🧪️nested-cargo-integration-oFwLQc` (**0/1/6, 2.20s**). No lint suppression was added.

Instead the catalog binds the macro's exact original bytes and source hash to `🧊️renderer/📇️registry/🦀️.rs` below the WGPU semantic owner. The library adapter loads that physical module with `#[path]` before including the renderer at crate root. The source alias and macro are each retained exactly once. This is a schema-owned output declaration, not yet a generator authorization.

`rustc --edition=2021` independently compiles and executes both original and canonical registration arrangements; both print `7`. The probe keeps the actual `crate::action_args_json!` call and provides two explicit minimal fixture interfaces for `serde_json` and `semio_framework`. This proves macro registration/expansion parity, not the entire renderer's compilation. Canonical authority rejects missing or changed registration bytes and missing/commented alias splices. The native compiler positive is **1/0/8** within the focused packet.

The Rust classifier now treats `macro_rules!` definitions as implementation. The purity golden retains its historical initial-role census and explicitly records the two current source-role resolutions (WGPU library macro implementation and Vitest declaration). The derived registration remains outside the package boundary; all five package adapters retain glue roles.

Two further TDD boundaries were closed: an already-present derived leaf outside source scope initially escaped the physical collision census (**2 pass, 1 fail, 22 assertions, 6.87s**, including the compiler/canonical positives), and its historical `originSourcePath` initially received a reference rewrite (**0/1/10, 7.65s**). The exact derived destination is now included in no-follow occupancy checks, and only its digest-bound JSON pointer/value/span is frozen. The occupancy rerun passed **1/0/2, 1.97s**. A five-case packet had **4 pass, 1 timeout, 49 assertions, 23.61s** under concurrent load; the timed-out case was that same new default-5s inventory case (6.19s). No timeout was increased, and its isolated rerun passed as stated. The nine-case frozen packet first exposed the expected historical/current role distinction (**8 pass, 1 fail, 511 assertions, 15.28s**); the golden/test now distinguish them explicitly.

All package production edits were coherent before these tests: fresh discovery and normalizer imports, `validateTaxonomy(loadTaxonomy()) === []`, and exact catalog census2. The temporary schema mismatch encountered by other lanes was reported and closed before their fresh CLI captures.

### Commands

All current test invocations use absolute paths. With `T` expanded to this ticket's absolute directory, the exact command forms are:

```text
bun test <T>/🧪️nested-cargo-package-authority.test.ts <T>/🧪️nested-cargo-package-purity-authority.test.ts --timeout 30000
bun test <T>/🧪️nested-cargo-package-integration.test.ts
bun test <T>/🧪️nested-cargo-package-integration.test.ts --test-name-pattern 'canonical adapter and WIT operand compile'
bun test <T>/🧪️nested-cargo-package-integration.test.ts --test-name-pattern 'frozen source coordinates|activation coordinates|unrelated producer reference'
```

- Original structural/purity baseline: **9/0/540, 5.26s**. After the derived registration integration, the latest frozen packet is **9/0/558, 4.08s**. It still verifies the exact 191 WGPU / 3 JCO historical consumer census; only digest-bound integration catalogs and exact validated phase-coordinate declarations are excluded from that historical count.
- Latest complete integration file: **18/0/175, 34.20s**, no skipped cases and no increased timeouts. JCO rollback/in-ticket retry/Nx generate+check/commit/Cargo metadata/empty replan took13.64s; WGPU source/canonical macro compiler parity1.83s; JCO source/canonical native+wasm compiler checks5.31s. The earlier16-case packet is retained above as a dated integration checkpoint, not the final gate.
- New authority API was initially red; first implementation **4/0/58, 3.13s**, then inventory consumption **5/0/61, 3.62s**.
- Source-coordinate and activation packet: **3/0/25, 11.56s**.
- Opaque path/ID regression: red **0/1/6, 1.99s**, then green **1/0/8, 0.55s**.
- Missing configured-entry registration regression: red **0/1/5, 0.685s**; composition plus registry prerequisites green **2/0/24, 0.684s**.
- The generated-consumer guards independently reject JCO dependency-snapshot references and WGPU bare-root `bun.lock` references. Generated files are not patched as authored text.

Independent test oracles are Cargo metadata/compiler, `@iarna/toml`, `fast-glob`, and `jsonc-parser`, all behind test imports. The integration suite has no skipped cases.

## Fixture Isolation and Execution Deviations

Fixtures explicitly isolate the unrelated plugin-registry contract to one sentinel input and omit its catalog selector because they do not materialize that producer. This is disclosed unit-integration scope, not proof of live registry output closure. The registry lane separately proved its real producer projected transaction. Live production plans retain all generator contracts.

One early Bun invocation omitted the required absolute/explicit-relative filename syntax and searched **168659 filenames**, matched no tests and finished in **6.11s**. No Compose content was intentionally opened, but that discovery deviation means an absolute zero-traversal history is not claimed. All later test filenames are absolute. One cold compiler attempt timed out; only the owned Cargo process was interrupted, the failure was retained, and the later native/wasm runs completed without increasing transaction timeouts. Fixture Git signing is disabled per command; real Git configuration/state is untouched. No retained evidence directory was deleted.

## Remaining Production Gates

### Concurrent WGPU Source Drift at the Final Full Gate

The post-policy full21-case run completed **19 pass,2 fail,213 assertions,67.17s**. Every JCO/policy/transaction/compiler case passed. The two failures share one newly changed real WGPU source leaf: `🧪️browser-frame-transport.test.ts` below the old WGPU source root. Exact source authority correctly rejects it; consequently the WGPU plan never reaches the later `bun.lock` generated-consumer guard asserted by the second failing test.

The frozen source has15004bytes, SHA-256 `65b94ae37128fa193b4ce4c465564ba819a520dc73d89fbf73a2f30b054650e5`. The observed source now has16208bytes, SHA-256 `0c424a9c55af60b7d16b2f0dd7d567a9c394115031bbd9fc21ac0abb660fe143`. A bounded comparison with the retained `🧪️nested-cargo-integration-oFwLQc` source copy shows the assertion for `OsHost::into_retirement` replaced by an `admittedRetirement` predicate and four negative mutations proving `try_into_retirement` admission and terminal-empty close sequencing. The source mtime is2026-08-27 07:00:33; this lane did not edit that production test.

No frozen hash or source bytes were silently refreshed/reverted. WGPU live authority is now intentionally fail-closed pending coordination of that concurrent source update. Schema/discovery remains frozen during root transaction11918. The earlier **18/0/175** full gate and **9/0/558** frozen gate remain valid historical checkpoints, not a claim that this newer WGPU source still matches their frozen authority.

- **JCO:** the last live plan had four moves, two edits, JCO adapter plus plugin-registry regeneration, and an unresolved `🔒️dependencies.json` consumer. That historical blocker is now addressed locally by exact users-only policy-state projection, not a baseline refresh. The fixture is four moves/three edits and retains exact metadata/ratchet identities through rollback/retry/commit. See `📓️s-dependency-snapshot-ownership.md`. A fresh live capture with the actual ticket admission and real generator closure is still required.
- **WGPU:** source/canonical authority,32 exact mappings, the source-owned library/renderer splices, exact semantic macro registration leaf and focused Rust compiler parity are implemented. Three newly created package adapters and the registration leaf still need exact producers; complete authored consumer closure, worker/boot phase-correct generation, full renderer compiler checks, transaction and empty second plan remain unproved. The last pre-global-closure live plan had31 moves/72 edits, plugin-registry + worker regeneration, and18 findings; later guards and source splices are not included in that stale count. It must be recaptured, not inferred.
- A fresh JCO global-closure capture was interrupted via its owned PID after approximately10000/70441 candidates, before artifact publication (exit130). It had clean11-entry source inventory; this does not establish plan counts. That process loaded before the generic lane's subsequent candidate/parser performance changes and did not produce a plan artifact. No process from that capture remains.
- The subsequent diagnostic no-ticket capture was stopped on root coordination for host pressure: owned PID63699, SIGINT, exit130 at10100/70881 incoming candidates, no plan artifact. It is not apply authority. No new global capture will run until root/Draw clear; the next JCO capture must use the actual ticket admission.
- No production apply is authorized by these fixture successes alone. Continue the exact WGPU subcontracts and live generated-consumer closure without approximating missing authority.
