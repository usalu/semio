# Remaining Package Purity Authority

## Decision

The remaining Git-admitted package source census outside the frozen WGPU/JCO structural and purity packet is stable by identity and classifier role across two consecutive fresh reads:

- 229 candidate leaves.
- 209 classified implementation leaves.
- 20 classified unresolved leaves.
- Source-set SHA-256 `e70688f49384d8a0898cfc6f574a22ffb02b5bb943d0d3801c3e4072b7c537df` over ordered `[role,path]` tuples.
- 1,050 admitted package files after excluding this packet's own golden evidence path.
- Exact WGPU/JCO exclusion: 72 source/destination identities from 36 frozen structural mappings.

The language-neutral ledger is `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🧫️fixtures/🧪️remaining-package-purity-authority/🔣️.json`, SHA-256 `49087f2dee618c3c8392ce3bc25c348af87d5380fe16523ed61ee6c02448f577` at handoff. It contains 228 exact destinations and one exact no-destination blocker. No production source, taxonomy, discovery, normalizer, transaction, Git, Compose, or temporary Compose content was changed or traversed by this packet.

The ledger is explicitly non-authoritative and not apply-ready. Three production sources changed across the release boundary, including new hashes for both previously declared drift sources. Their byte hashes are intentionally `null` and all observed hashes are retained in `sourceByteDrift`. Two immediate full-source reads at `2026-08-27T01:38:18Z` and `2026-08-27T01:38:27Z` matched, but this does not demonstrate completed shared work after the post-release changes. Every source outside the exact drift cohort remains pinned.

## Census Reconciliation

The router's earlier 269 implementation plus 23 unresolved observation is not reused as current acceptance evidence. The current bounded, Git-admitted scan excludes all exact WGPU/JCO structural source and destination paths and filters physical scanner output through Git admission. It therefore excludes ignored build products and directory-level migration markers.

| Root | Implementation | Unresolved | Total |
| --- | ---: | ---: | ---: |
| `✏️s` | 22 | 10 | 32 |
| `🧰️framework` | 175 | 10 | 185 |
| `♻️mit-bestand` | 3 | 0 | 3 |
| `🌎️hub` | 9 | 0 | 9 |
| Total | 209 | 20 | 229 |

| Language boundary | Leaves |
| --- | ---: |
| Rust | 125 |
| TypeScript | 88 |
| JavaScript | 13 |
| Python | 1 |
| Go | 1 |
| .NET | 1 |

## Exact Dispositions

| Disposition | Count | Authority |
| --- | ---: | --- |
| Move domain implementation beside package boundary | 113 | Domain owner directory plus physical language leaf |
| Move test implementation | 27 | Owner `🧪️tests/🧪️<case>/<physical-leaf>` |
| Move benchmark implementation | 1 | Owner `🏃️benches/🏃️<case>/<physical-leaf>` |
| Extract implementation and rewrite library adapter | 22 | Owner implementation plus package `📚️library/<physical-leaf>` |
| Extract implementation and rewrite binary adapter | 4 | Owner entrypoint plus package `💾️binary/<physical-leaf>` |
| Extract Cargo build implementation and rewrite builder adapter | 3 | Owner and package `🏗️builder/🦀️.rs` |
| Normalize registration/facade to package library | 17 | Cargo `lib.path` and pinned adapter preimage |
| Normalize compiler registration to package library | 6 | Proc-macro crate-root authority |
| Normalize package declaration | 1 | Rust backend-alias declaration |
| Retain fixed Python declaration | 1 | Python import contract |
| Normalize externally fixed tool configuration | 29 | Exact Vite, Vitest, Next, or PostCSS filename contract |
| Normalize configurable tool setup/runner | 4 | Explicit script/config operand |
| Block empty package root | 1 | Owner must remove the empty Go/Nx scaffold or implement its command router |
| Total | 229 | |

The empty blocker is `🧰️framework/🛍️products/🦑️repo/🔨️modules/💻️client/🔌️mcp/📦️packages/🐹️go/📜️script.ts`. Its adjacent `📋️project.json` is also zero bytes; no destination is invented.

Two target packages require target-first structural projection before their source decisions can apply:

- `🧰️framework/🔨️modules/🖱️ui/🎯️targets/⚛️react/📦️packages/🟦️typescript`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🎯️targets/⚛️react/📦️packages/🟦️typescript`

The Draw proc-macro decision composes with the frozen CAD/Draw projection and uses its shorter exact destination `…/✏️editor/🪆️1-any/…/✨️macros/📦️packages/🦀️rust/📚️library/🦀️.rs` rather than rebuilding a destination from the legacy source prefix.

## Destination Integrity

- 228 exact destinations; one explicit no-destination blocker.
- Zero exact, NFC, case-folded, or variation-selector-insensitive collisions across destinations and derived adapters.
- Zero preexisting non-identity destinations.
- Zero paths above the 240-byte budget.
- Maximum destination is 210 UTF-8 bytes, the Draw proc-macro library destination.
- Every non-fixed destination ends in a physical language leaf such as `🦀️.rs`, `🟦️.ts`, `🟦️.tsx`, `🟨️.js`, `🟨️.mjs`, or `🔷️.cs`.
- 16 otherwise exact destinations retain an explicit `owner-semantic-emoji-registration` blocker because a generic `🧩️` placeholder must receive owner signoff before schema integration.

## Reference And Generator Authority

The packet does not invent reference authority. Every non-identity destination remains blocked until exact authored and generated consumer identities, occurrence counts, byte preimage hashes, and adapter rewrites are frozen.

Required adapters are Cargo TOML, Node package JSON, Nx JSON, TypeScript/JavaScript modules, Rust path/module attributes, Python imports, .NET project metadata, and external-tool CLI/config discovery.

Existing generator-input ownership is exact:

| Generator contract | Candidate inputs |
| --- | ---: |
| `actor-typegen` | 1 |
| `framework-manifest` | 1 |
| `plugin-registry` | 31 |
| `styling-tokens` | 1 |
| `ui-contract` | 14 |

No Git-admitted candidate is a registered generated output. The ignored styling Python output and other ignored generated leaves do not inflate acceptance counts.

## Fail-Closed Blockers

| Blocker | Sources |
| --- | ---: |
| Exact reference preimage census | 227 |
| Manifest entry preimage and adapter template | 52 |
| Generator input destination update | 48 |
| External tool entry contract | 33 |
| Owner semantic emoji registration | 16 |
| Target-first package structural projection | 10 |
| Concurrent source byte drift | 3 |
| Empty package owner remove-or-implement decision | 1 |

The exact drift cohort is:

- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🧪️index.test.ts`
- `✏️s/🔌️plugins/🗄️stdio/📦️packages/🦀️rust/📦️glue.rs`
- `✏️s/🔌️plugins/🎬️sequence/🗿️artifacts/🎬️sequence/🏅️standards/🔖️1/🪆️subsets/✳️any/✏️editor/🌉️wasm/📦️packages/🟨️javascript/🧪️tests/🧵️retained-actions.test.js`

Observed hashes are stored in the golden. None of the changes altered a source identity or classifier role.

## Verification

The ticket-local Bun test is `🧪️remaining-package-purity-authority.test.ts`. It uses:

- Ajv to validate the language-neutral golden shape.
- Two fresh repository classifier reads over independently enumerated Git-admitted package files.
- fast-glob exact-path parity for all 229 physical sources.
- SHA-256 checks for every released source and explicit drift-ledger validation for the concurrent sources.
- Structural WGPU/JCO and CAD/Draw golden hash checks.
- Exact resolution counts, destination canonicalization, path budget, preexistence, generator matches, and fail-closed blockers.

```text
bun test --timeout 30000 './.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/🧪️remaining-package-purity-authority.test.ts'
5 pass, 0 fail, 1,959 expect() calls, 29.02 s
```

The suite proves the stable role/path census and the explicit drift handling. It does not claim apply readiness or source-byte authority for the three concurrent leaves; the golden's decision state is `non-authoritative-concurrent-source-byte-drift`.

### Root independent rerun

After the scoped-normalization test owner released the concurrently edited repo-lib test leaf, the root coordinator reran the frozen packet:

```text
bun test --timeout 60000 './.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/🧪️remaining-package-purity-authority.test.ts'
5 pass, 0 fail, 1,959 expect() calls, 16.94 s
```

This confirms the explicit three-row drift boundary and the other 226 preimages. It does not promote the three null-hash rows to apply authority.
