# Note Slice — 49/87 Rows
(A) 33/33 fixed at `normalization/🟦️.ts:6489`. Real condition: guard compared SOURCE fileKind vs DESTINATION fileKindId. `payload.schema.json` always resolves `json-schema` (its own chain outranks `.json`) while the destination wants `json` by design (`🧬️schema/` conveys it) — could never agree, so it failed 100% of the time before any real content check ran. Fix: check only `nodeKind==="file"`; JSON/dialect/descriptor checks below unchanged (verified: bad `$schema` still throws its own error).
(B)+(C) 12/16 fixed in `🔣️taxonomy.json`: registered `generator`/`generator-crate`/`probes` (repo-wide gap, all 29 plugins, verified via `🖍️draw`/`🌊️flow`); widened `retained.parentKindIds` +`editor`; added `descriptor` to `GENERIC_SEMANTIC_STEMS` (precedent `component`); added fileKind `semio-command` for `.cmd.semio` (52 identical files, precedent `semio-dsl`).
NOT fixed — correct refusal (4 stem + 1 dir, `🏭️generator/🦀️note-oracle-codec/src/`): Cargo files, src/, 6 named `.rs`. `rust-binary-entry` proves the taxonomy wants bare `🦀️.rs` per module, not a `src/` folder; needs a handcrafted Rust restructure, out of scope here.
Verified both ways via isolated `inventoryTaxonomy` fixtures + scoped `clean taxonomy inventory`. `validateTaxonomy`=0 problems, no regressions found on other plugins.
