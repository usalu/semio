# goal-ten — A: fixed (2/2), B: 7/7 confirmed correct refusals (unchanged), C: not fixed

**(A)** Real defect confirmed: `packageLocation` (🧹️normalization/🟦️.ts) only recognized a package root via a literal `packages` path segment — a proxy for "this dir owns a Cargo.toml", not the property itself. `🦀️note-oracle-codec` is a `generator-crate`-kind dir (goal-codec-report's restructure) directly under `🏭️generator`, never under `packages/🦀️rust`, so `packageInfo` was null and `cargo-manifest`'s `package-root` scope never matched → both rows fell through to bare "Cargo" semantic-stem resolution → unresolved. Fixed by recognizing a `generator-crate` dir (parent slug `generator`) as a Rust package root, same structural property one level shallower. Separately found Cargo.lock had **zero** applicable fixedFilenameContract anywhere in the schema for any non-root, non-ticket package (only `root-cargo-lock`/`ticket-cargo-lock` existed) — added `package-cargo-lock` (`**/Cargo.lock`, `package-root`/🦀️rust). Verified fail-before/pass-after against real taxonomy.json (`📜️goal-ten-package-location-verify.ts`): both rows resolve (`cargo-manifest`, `package-cargo-lock`); regression check on a real `packages/🦀️rust/Cargo.toml` is byte-identical old vs new. `validateTaxonomy()` = 0 problems. This is repo-wide latent (~15+ other plugins' generator/probe/bridge crates share the pattern per `git ls-files | grep Cargo.lock`), not note-specific — those aren't yet kind-only-leaf migrated so aren't surfaced today.

**(B)** Re-examined all 7 with fresh eyes, no new provable angle: vector-converter `:146,:155` — base is `DirEntry::path()` from a real directory-enumeration loop (arbitrary artifact-leaf names); hardcoding would defeat its actual purpose. `mcp:52` — same, `entry.owner_root` from a live plugin-registry scan. `run/component.rs:2787` — traced `test_repo_root()`'s ancestor-walk to its production twin `find_repo_root()` (📦️bin.rs), whose own doc states the walk exists so a direct `cargo run` "from an arbitrary working directory still resolves correctly" — deliberately resilient, not a fixed depth; hardcoding via `concat!(env!(CARGO_MANIFEST_DIR),...)` (the pattern already used elsewhere for a genuinely-fixed `include!`) would remove that resilience, a real behavior change. `derive:344,351,1900` — confirmed "no interprocedural tracing" is the actual limitation (not merely module-boundary), so even de-modularizing the qualified call doesn't help; the only route to "provable" is duplicating the sibling `materialize()`'s env::temp_dir resolution inline, which is prover-gaming, not an honest rewrite. All 7 unchanged, correct refusals, still blocking as designed.

**(C)** Not fixed. Confirmed `invokeGeneratorPreview`'s `timeout: 60_000` and `GeneratorContractSpec` are unchanged since goal-last25 — no per-contract override field exists. A global bump masks real hangs on smaller contracts (unchanged reasoning); a proper per-contract field could live under `inputDiscovery.previewInput` (which already carries `maxBytes`/`maxOperations` for `plugin-registry`) but any value would be an unjustified guess without measuring the real preview under the 40 232-input scan — which requires the same repo-wide enumeration this session is forbidden from running. Deferred with the scope-narrowing work in §5/§8, as before.

Files: 🧹️normalization/🟦️.ts (`packageLocation`), 🔣️taxonomy.json (+`package-cargo-lock`). Script: 📜️goal-ten-package-location-verify.ts. No plan/apply run.
