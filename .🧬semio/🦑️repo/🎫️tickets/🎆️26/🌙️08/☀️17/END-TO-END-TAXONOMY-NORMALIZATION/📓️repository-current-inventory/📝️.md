# Repository Inventory Capture

## Subsequent Evidence Loss

At the coordinator's later August 27 readback during Energy source-drift review, the exact `📊️repository-current-inventory-02` directory and its manifest were absent. The adjacent `🧾️repository-current-inventory-02-aggregates` directory, this report, and the authored verifier remain. The earlier successful full shard readback below is a historical observation, not a claim that its 3,636 raw shards are still available. This task's coordinator issued no deletion of that inventory. The disappearance time and cause are not established; no replacement manifest, shards, or historical verification result has been fabricated. A new inventory would be a separately identified current run, not restoration of this capture.

## Current status

The first whole-repository inventory completed classification and reference collection, then **failed publication**. It is observational and nonbinding: all four observed source/schema identities changed during the run, and no whole-repository manifest or payload was published. Do not use this capture as apply authority.

| Completed phase | Count |
| --- | ---: |
| Directories | 52,826 |
| Admitted leaves | 81,192 |
| All inventory entries | 134,018 |
| Textual files processed for references | 61,029 |

Changing-leaf counts, the file/symlink split, violation groups, and owner rankings are **not available from this attempt**. Their summary step followed publication, which threw; the process then ended without retaining the full in-memory inventory. No earlier census is substituted for those missing facts. The next coordinated capture must calculate and emit diagnostic aggregates before publication.

## Exact boundary

The immutable baseline `9f449b10659b95148c8bcb3f91ce583bf7446973` was freshly verified as a commit. Observed HEAD was `a8d1caf41f68204e73ff5e47ce40c5f543ed442d`. The baseline is a provenance anchor; projected path changes would be measured against taxonomy-normalized paths, not as a Git diff against that commit.

The existing `inventoryTaxonomy` API ran with repository root `/Users/ueli/Documents/semio`, the current normalization ticket, no source scope filter, one worker, progress reporting, the fixed baseline, and `excludedTreeDigests: []`. Its exact lexical opaque exclusions were checked to remain the two schema-authorized roots before publication. No opaque-tree digest, global plan, generator preview, apply, or Git mutation was requested.

This inventory admits existing Git-index paths, nonignored untracked paths, schema-declared ignored generator outputs, and explicit current-ticket paths. It does not redefine every ignored cache file as source inventory.

The runner prefix was `NX_DAEMON=false NX_NO_CLOUD=true bun ./node_modules/nx/bin/nx.js exec --projects=workspace --parallel=1 --skip-nx-cache --skipSync -- bun -e`, invoking only existing APIs. Calling the Nx binary through Bun avoids the root package's inherited `npm_lifecycle_event=nx` dispatch. No permanent or temporary script file was created.

The distinct intended publication root was `../📊️repository-current-inventory`; it was absent before the run and independently confirmed absent after failure. The cancellation sentinel `../⛔️repository-current-inventory.cancel` was never created. Session `76590` and worker PID `82051` ended with exit **1**.

## Failure and preservation

Capture interval: `2026-08-27T11:10:46.652Z` through `2026-08-27T11:34:53.383Z`, **24 minutes 6.731 seconds**. The last inventory event was `complete 134018/134018`.

`publishTaxonomyInventoryArtifactShards` called `buildTaxonomyInventoryArtifactShards`, which rejected the inventory before any publication write:

> Inventory top-level violations must equal the canonical violations derived from entries.

The normalizer orders violations with locale comparison of path, code, and message. The publisher independently orders the full identity using UTF-8 bytes for path, code, severity, and message, then compared order-sensitive incremental digests. A new two-row, order-only language-neutral regression reproduced this exact rejection: **0 passed, 1 failed, 2 assertions**, with four tests filtered out, in 1.83 seconds. This is a demonstrated consistency defect, not evidence that records may be omitted or changed.

The existing Draw manifest remained byte- and inode-identical: **1,852 bytes**, SHA-256 `45868e4389e435f2cc38deeca174ac64307c4db2075e63fd13e7dbc8b3d9fabb`, inode `133080765`. Its [retained manifest](../📊️taxonomy-inventory/📊️shards/🔣️.json) was not replaced. No production source, fixture input, report, transaction artifact, or other ticket output was deleted by the capture.

## Observed source drift

These are exact before/after observations, not an atomic code snapshot or a claim that all files stayed stable between reads. All observed modes were `0644`; each listed inode remained the same.

| Source | Before bytes / SHA-256 | After bytes / SHA-256 |
| --- | --- | --- |
| Root router | `2810720` / `b9a21352c0a502cf261146201341719a507d4275cb8a44093ebe0597a613882d` | `2813661` / `8cc7f1ed60e9c1b6f4caeff92b0437bf631289ae8ad1151ec91c5778f01e013b` |
| Taxonomy schema | `307947` / `23c2fd865f9305e931bdbc5b19cb0c87986b48bb91487f2418d98c86153b0a3c` | `307988` / `83e9b379828d3944b2bad91da6806a6a70202bd603d39fd0fc89f3c25c55dc88` |
| Discovery implementation | `563442` / `d8f645c697bd03b6ecd736e703053f033d348bf4b0fe5648c4bdb058206a2cc0` | `569068` / `4e8147f8476e3aa3c007b4ab799ba81731a7480fb82a141c7176048185989faf` |
| Normalization implementation | `801192` / `53c78c447d48ff56f99635e8e3539ab8e89435312c8e9fa1efdf0f4fdba84cad` | `806134` / `d22228ec13487ea68f9644dbd35adb6682ff5dfe45a6d3f242f2a08c8d5d54af` |

The source paths are the root router `📜️script.ts` and the repo-library schema, discovery, and normalization implementations. Their observed inodes were respectively `116368958`, `129218887`, `109708023`, and `129693956`.

## Performance and repair gate

Reference throughput varied markedly: one stretch processed roughly 40–65 files per ten seconds, while later stretches advanced thousands of files per progress interval. A one-second read-only native sample at `2026-08-27T11:24:27.673Z` showed active Bun work, a 3.7 GiB physical footprint, and a 4.3 GiB peak. Most JIT frames were unsymbolized, so this sample does not identify a JavaScript hotspot. It streamed to stdout; no profile artifact was created by this lane.

The coordinator authorized and this lane implemented a root-shard-only fix with no normalizer, plan, or journal ordering change. The supplied top-level violations are compared as a unique full-identity set; changed, missing, extra, and duplicate records still fail closed. The artifact's canonical inventory digest names exactly the published reconstruction, with source-path-byte-ordered entries and full-identity-byte-ordered violations. The original inventory/source-tree digests and every metadata/entry value remain intact; the caller object is not mutated.

Two additional test-first integrity failures were found inside the same publisher boundary. The shared normalizer serializer sorts identifier-bearing arrays, so it both concealed a co-reordered descriptor/payload sequence and changed nested coordinate arrays during artifact serialization. The shard implementation now has a local object-key canonical encoder that preserves array order, an order-sensitive descriptor ledger, and an explicit owner/part ordering check. It does not change the shared normalizer serializer or introduce a runtime library dependency. Even after an attacker recomputes the reversed manifest's ledger and canonical bytes, the order check rejects it.

The permanent gate is `@semio-tech/repo-lib:test-inventory-artifact-shards`, registered through the existing package router and both launch catalogs at `4_gate / 410.10`. Its language-neutral input is `🧪️tests/🧪️inventory-artifact-shards/🔣️.json` under the repo library. SHA-256 at the final run:

| Input | SHA-256 |
| --- | --- |
| Language-neutral contract/vectors | `6bee72253480db370315943852da859bb73af876b0802ad1b7a097d08c42f733` |
| Permanent test implementation | `c11ebc7a897c3e70d90eb167627bf9a52f32d9d01e0fd4ad20c3fd20cf06029d` |

Independent validation uses Ajv, `fast-json-stable-stringify`, `fast-glob`, and WebCrypto SHA-256 only in tests. The packet includes both permutations of five violation-order cases, seven exact violation-set rejection mutations, nested metadata/entry preservation, co-reordered descriptors, and nine artifact-corruption mutations. A real isolated inventory producer produced five entries/five violations with locale-different ordering and passed exact record comparison. The physical publisher test verified path closure and rejected an inconsistent replacement before any output change.

| Runtime checkpoint | Result |
| --- | --- |
| Original order-only TDD | 0 pass, 1 fail, 2 assertions; 1.83 s |
| First actual five-case run | 4 pass, 1 fail, 133 assertions; 1.346 s; failure was shared-object aliasing in the new test fixture |
| Corrected fixture plus two integrity regressions | 5 pass, 2 fail, 140 assertions; 2.28 s; both added failures were genuine production defects |
| Final registered, uncached Nx packet | **8 pass, 0 fail, 156 assertions; 743 ms** |
| Existing pure shard regression subset | **4 pass, 0 fail, 16 assertions; 653 ms**; three unrelated/cleanup-bearing cases filtered explicitly |

The intermediate five-output WGPU startup rejection was not bypassed. Actual guarded root imports succeeded after that owner restored startup coherence. An earlier isolated source-region diagnostic had also passed ten permutations and seven rejection mutations, but it is not substituted for the actual registered runtime result above.

Final registered command:

```sh
NX_DAEMON=false NX_NO_CLOUD=true bun nx run @semio-tech/repo-lib:test-inventory-artifact-shards --skip-nx-cache
```

The router resolves the permanent Bun test to an absolute path. The existing pure subset was separately invoked with an absolute test path:

```sh
NX_DAEMON=false NX_NO_CLOUD=true bun ./node_modules/nx/bin/nx.js exec --projects=workspace --parallel=1 --skip-nx-cache --skipSync -- bun test '/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/🧪️inventory-artifact-shards.test.ts' -t 'builds deterministic|byte-sorts|splits oversized|rejects duplicate'
```

All publisher test/inspection processes exited and the source region was released. The coordinator subsequently released the parser-performance gate and authorized the second whole-repository capture below. No missing whole-census aggregates from the first attempt are claimed.

## Preserved Draw artifact after the strict fix

A fresh read-only check confirmed the retained manifest's exact original hash, size, and inode shown above. Both payload hashes still match their descriptors: `6a1d8f3efecf9e8c7991a28ba16ce1e2b06f3eb5e505c81523faabfc915097cb` (22,317 bytes) and `de715ca46876715e3aa97567233062c7106841e9acc21e208af735b3c0f2910d` (709 bytes).

The corrected validator now rejects that historical manifest with `Inventory shard descriptors are not in byte-sorted owner/part order.` Its earlier successful validation was subject to the now-demonstrated array-sorting blind spot. Nothing was reordered, regenerated, deleted, or repinned in the Draw artifact. Preserve it as historical capture evidence, not as a newly validated artifact under the corrected ordering contract.

## Completed publisher-test output retention

The clean skill was read. Its workspace-wide command would violate this task's explicit preservation boundary, so it was not run. Cleanup was limited to two regular generated files from the completed failed test fixture `../🧪️inventory-order-publication-1Fscu4`, after fresh hash checks. `apply_patch` removed only these files; `rmdir` then removed only their three empty output ancestors:

| Removed path below that fixture | Bytes | SHA-256 |
| --- | ---: | --- |
| `📊️inventory/📊️shards/🔣️.json` | 1,110 | `8615e8f0ca96572dec82a9d15068f37f95b0697a66e21ee656c0e0b8e141817e` |
| `📊️inventory/📊️shards/🔖️98d2179a926d609594e3bd831d4dbe3dd306f77a8ad5da06223e4f5b0abe6964/🔣️.json` | 822 | `98d2179a926d609594e3bd831d4dbe3dd306f77a8ad5da06223e4f5b0abe6964` |

The 1,389-byte fixture input `🔣️.json` remains regular and unchanged at SHA-256 `7c7cbef2a5541ed6668ce3b6fb0b3a36b6b05b9be2f6abe32f8368eef2294b89`. No byte backup of the two disposable outputs was retained; the inputs and corrected tests remain available for rerunning the test, without promising byte-identical restoration of the old failed output. Successful publisher tests remove only their verified generated shard output while retaining inputs. No other ticket output, report, source, or recovery artifact was removed.

## Second coordinated capture

Started `2026-08-27T12:06:49.934Z`, session `56331`, worker PID `70726`, with one inventory worker, the same verified immutable baseline, and no scope filter. Initial enumeration found 55,546 directories. This is in progress, not a completed census.

The distinct publication root is `../📊️repository-current-inventory-02`. The absent cancellation sentinel is `../⛔️repository-current-inventory-02.cancel`. After classification but before invoking the publisher, the evaluation will write and independently hash-check the complete aggregate record at `../🧾️repository-current-inventory-02-aggregates/🔣️.json`; console output also reports total counts, every violation code, and the forty largest owner batches. All owner rows remain in that aggregate record even if publication later fails. No standalone script file was created.

Startup identities were freshly read, with modes `0644`:

| Source | Bytes | SHA-256 |
| --- | ---: | --- |
| Root router | 2,815,702 | `645fd95ffc82516104b92e2d93712d51b77035b2d8e8667b3bcab907caad8ffd` |
| Taxonomy schema | 308,176 | `0cc60034727ec4c9d18c1e0a565d72f18594263f7ff2e080a885c13b5d4a42c5` |
| Discovery | 582,797 | `31672e99b7813dc53828b9e828b7095816215fd4f517ffd950751002e871d519` |
| Normalization | 806,262 | `f30cacec0a1dec2da37f7d895a386ce48f8ed732a1df361ae851840f01c0e96a` |
| Nested-Cargo projection input | 73,336 | `f64f2f654e5dc12d57b72148b023b2c9a20d6fd8f147293dac871e2529b2319a` |
| README/LICENSE owner input | 53,514 | `051394741822e92d51f3bda15ce64d84c236582c6927335c9c5e0ac3c18a1da4` |

Both declared catalog hashes matched their schema pins before inventory. The original Draw manifest retained its original hash and inode at startup. Schema/catalog were coordinated as frozen; a disjoint preapply helper may still change in shared source. Before/after observations will therefore be reported explicitly, without treating this observational inventory as transaction authority.

## Bounded audit of the uninstrumented interval

This is a read-only source audit while session `56331` remains active, not a benchmark or a proven JavaScript hotspot attribution. The last progress event reached all 83,689 leaves; `referenceGraph` emits its zero-progress event immediately on entry, and that event had not arrived when this audit was taken. The interval therefore contains the owner projections and directory hashing between those two calls. No production code, timeout, inventory scope, or collision rule was changed.

Use `N = 139,235` for the enumerated directory-plus-leaf count, not a final published count. The exact package catalog pin still supplies 36 distinct WGPU output paths and 5 JCO output paths; WGPU has 32 mapping rows, 4 adapters, and 1 derived row, with one duplicate path in their union.

| Source location | Repeated work actually present in source | Bounded interpretation |
| --- | --- | --- |
| [Nested-Cargo projection](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4846) and [collision authority](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️component.ts:3787) | For each package, occupied-path construction checks `candidates.some` for every inventory path despite already constructing an admitted set. The source-layout collision loop then creates an output array for each occupied path and calls NFC/locale-lowercase/VS16 folding up to four times for each path/output pair. | Membership is `O(N × C)` for package candidate count `C`; collision comparison is `O(N × O)` with repeated expensive folding. If both registered packages take their source-layout branches, `4 × N × (36 + 5) = 22,834,540` is a loose upper bound on fold calls before exclusions/early matches. This is a static bound, not an observed invocation count. |
| [Source mutation bundles](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4455), [catalog lookup](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4381), [bundle validation](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4410) | A full entry scan finds each subset's catalog; each scenario's bundle check filters the full entry collection; each accepted scenario scans it again to project descendants; the first scenario per mutation also filters the whole collection for mutation children. | Work scales with admitted subset/scenario/mutation counts multiplied by `N`. Counts of matched scenarios were not observed in the running worker. |
| [Canonical mutation validation](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4581) | Each realized canonical scenario calls the same whole-entry bundle check and then scans all entries again for its descendants. | The same structural repetition exists independently on the canonical branch. |
| [Ticket-important directory validation](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:5073) and [owner documentation validation](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4961) | Every important/history directory filters all entries to obtain direct children. Each invalid governed documentation directory performs another full child scan. | `O(K × N)` for relevant directory count `K`; a reusable exact-parent child index would preserve membership without rescanning unrelated entries. |
| [CAD/Draw catalog projection](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4743) and [path collision preparation](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️component.ts:1758) | Each matching catalog root scans all entries for nodes, rebuilds/sorts the occupied list, and builds its folded occupied-key set. Canonical root discovery scans entries again per artifact root; directory projection filters mappings per directory. | Repeated whole-inventory preparation exists, but the collision helper computes occupied folds once per root, unlike the nested-Cargo path/output loop. |
| [Initial mutation source recognition](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:4270) | Each candidate directory filters/folds the full registered owner-name list before validating source segments. | `O(D × R)` for directory and registry sizes, with repeated registry-key work. No claim that this dominates runtime. |
| [Directory hashing](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts:5322) | The child map is constructed once. Each directory hash receives only its direct children, which are sorted locally. | This portion is already indexed; the inspected code does not rescan all entries per directory. |

The strongest narrowly scoped optimization candidate is invocation-local reuse of the exact existing Unicode-fold results in nested-Cargo collision authority, plus using its already-admitted exact set for occupied membership. This must preserve the same comparison rules, allowed-directory identity, problem ordering, missing/symlink behavior, and source preimage checks. Replacing locale folding, filtering away foreign paths, or skipping collisions is not justified. No candidate optimization has been implemented or benchmarked by this lane.

The helpers in this interval are not passed progress/cancellation callbacks. The inventory checks cancellation before the projection sequence, but the inspected sequence does not report individual projection completion or poll the sentinel within those loops. Future instrumentation should expose that work; this audit does not relax the existing cancellation or timeout contracts.

### Exact two-second native sample

At `2026-08-27 14:25:43.181 +0200`, `/usr/bin/sample` observed the exact active PID `70726` for two seconds at one-millisecond sampling intervals. The command completed successfully without interrupting the inventory. Its generated output is retained temporarily at `../📊️repository-inventory-02-projection-profile/📄️.txt`: **168,317 bytes**, SHA-256 `eca49190a247c986188527ee64c5ce816506af43dfe2d5792689a01a42cd4351`.

The profile reports 1,482 main-thread samples and a 4.7 GiB physical footprint/peak. Main-thread stacks contain ICU locale-tag canonicalization, Unicode lowercase conversion, and normalization. Bun and JIT caller frames are unnamed, so the profile cannot identify the JavaScript function. Idle Bun pool-thread wait totals must not be misread as a blocked main thread. The sampled operation family is consistent with the repeated-fold source candidate above, but that association remains an inference. The profile stays available while the capture is active and will be retired only after its useful evidence has been summarized and the owning work is terminal.

## Completed Publication and Independent Coordinator Readback

After the agent stopped with a usage-limit error, the coordinator found no active capture process. The aggregate checkpoint records classification completion at `2026-08-27T12:44:08.231Z`, 37 minutes 18.297 seconds after start. Publication is present; the previous in-progress statements above are historical. The expired process session cannot supply a recovered exit code, so publication integrity was verified anew rather than inferred from file existence.

The retained read-only `📜️script.ts verify` was run through uncached Nx exec (session 66632), exiting 0 in **12,928.00 ms** of script time. It checks every exact ticket ancestor with no-follow metadata, the complete physical shard closure, every payload's independent WebCrypto SHA-256, the current strict production validator, and an independently streamed `fast-json-stable-stringify` canonical inventory digest. It recomputes all aggregate totals and every violation-code count. It does not revisit the captured physical source files or turn the observational capture into transaction preimage authority.

| Retained record | Bytes | SHA-256 |
| --- | ---: | --- |
| Published manifest | 2,181,545 | `9378c63ee71e6bb51de1811d8ffc1ae8e5919c0d614a9a278a915144f44322a1` |
| Complete aggregate checkpoint | 913,420 | `9bcfde835bd22e9e6295da87bac7468eba2a6a09f24bb890e8fa16a5b6526c92` |

The 3,636 shard payloads total **235,286,953 bytes** and reconstruct canonical digest `1def2e83d224590fc884a2df40d731d72c543fbf18deefc02f3eaca7a4bd263f`. There are 3,613 owner rows and 20 violation codes. Baseline remains `9f449b10659b95148c8bcb3f91ce583bf7446973`; source changes observed during classification remain explicitly recorded in the checkpoint.

| Measure | Count |
| --- | ---: |
| Entries | 139,235 |
| Directories | 55,547 |
| Regular files | 83,586 |
| Symlinks | 102 |
| Changing leaves / directories | 49,876 / 11,324 |
| Violating entries / leaves | 53,613 / 37,105 |
| Errors / warnings | 64,722 / 10,617 |
| Unique violations | 75,339 |

Largest error codes are `path-too-long` (28,290), `semantic-stem-unresolved` (20,982), `directory-kind-unresolved` (3,939), `package-implementation-destination-unresolved` (3,892), and `semantic-stem-ambiguous` (2,621). The complete owner/code breakdown remains in the aggregate checkpoint; no owners were filtered away. Compose and `temp/compose` have no admitted entries. This is a valid retained census with substantial remaining work, not an empty normalization plan or completed monorepo acceptance.
