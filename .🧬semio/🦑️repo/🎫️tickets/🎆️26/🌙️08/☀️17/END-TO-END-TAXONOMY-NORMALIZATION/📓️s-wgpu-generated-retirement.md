# WGPU Generated-Source Retirement and Producer Closure

## Current Boundary

No production apply, production generator write, global inventory or Git mutation occurred in this lane. Current live package sources are preserved. The WGPU completeness guard remains enabled; a blocked draft is not represented as executable.

The catalog now declares exactly one generated-source retirement for WGPU and none for JCO. Its SHA-256 is `a1093d99d21337acababece03c3d48b42c9edc2c67603452637b76f321328dc8`. The declaration selects the existing generated worker mapping, generator `wgpu-frame-worker`, exact canonical destination and mode `0644`; source hash and size come from the mapping. Neither mapping destinations nor source hashes changed in this addition.

`nested-cargo-generated-source` is distinct from `exact-owner-generated-source`. The latter retains README's byte-and-mode-identical output requirement. The new kind binds catalog path/hash, package identity, generator identity, canonical destination, frozen source preimage and authority digest. A canonical worker can have regenerated bytes different from the obsolete source. Preview output hashes remain independently frozen and checked through the existing generator transaction. `preview.staleRemovals` may not escape declared output roots.

The planner requires the complete 32-file exact package source before admitting the one retirement. Ordinary moves exclude this generated leaf. Apply additionally requires exactly one generator owning its disjoint canonical output; no retirement can be authorized solely by the new catalog record. Catalog bytes remain in both affected-state digests. The existing staging/rollback mechanism owns removal preimages; no journal format or pruning rule changed.

## Reproducible Inputs and Current Tests

The earlier ticket-local nested-Cargo integration test and JSON inputs were physically absent after the context/retention-policy transition. Their reported results are historical evidence, not current reruns. A new, explicitly bounded packet was established at `🧪️wgpu-generated-retirement.test.ts` with language-neutral `🔣️wgpu-generated-source-retirement.json`; it is not presented as a replacement for the entire prior 21-case matrix.

Current commands use absolute test paths:

```text
bun test /Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/🧪️wgpu-generated-retirement.test.ts
```

- Reestablished TDD red: **2 passed, 1 failed, 26 assertions, 1.79 s**; retirement count was zero instead of one.
- Retirement implementation first reached the intended count but the test incorrectly compared unsorted in-memory plan arrays with the canonical parser's sorted arrays. The assertion was corrected to canonical bytes, without changing production ordering.
- First complete green: **3 passed, 0 failed, 37 assertions, 2.76 s**.
- Catalog-negative and individual wire-field binding extension: **4 passed, 0 failed, 57 assertions, 2.06 s**.

Independent `jsonc-parser` validates the catalog and canonical wire bytes; TypeScript AST resolution verifies the generated Node entry's exact target; `@iarna/toml` verifies the source Cargo identity. Negative catalog cases cover missing/duplicate declarations, authored-source substitution, different destination, different generator, mode drift and extra keys. Seven source/occupied/symlink/workspace producer mutations reject. Individual wire field mutations reject without resealing their authority digest; fully resealed adversarial apply cases remain part of the future complete transaction gate.

## Four-Leaf Writer

`semanticPackageGeneratedLeafPreview` emits only three new package adapters and the semantic macro registration, excluding the moved-and-edited Cargo library adapter. Source and canonical admission use exact catalog facts and no-follow reads, plus root Cargo/Node workspace identity for WGPU. Missing owned generated leaves are represented only by their exact registered templates during canonical preview; an occupied source-phase output or changed canonical template rejects.

Root `writeWgpuPackageLeaves` is a bounded internal producer building block, not a newly activated CLI. It requires the canonical Cargo package, checks all existing outputs before any write, creates absent files exclusively with `wx` and mode `0644`, and never overwrites drifted bytes/modes. The actual fixture runs Nx generate/check twice against this production function.

- Missing-writer red: **0 passed, 1 failed, 1 assertion, 0.215 s**.
- First implementation exposed a missing explicit taxonomy argument at the catalog call: **0 passed, 1 failed, 3 assertions, 0.313 s**. Fixed by taking one validated taxonomy snapshot and passing it to both helpers.
- Actual writer/Nx focused green: **1 passed, 0 failed, 22 assertions, 6.00 s**.

This canonical fixture deliberately materializes exact package facts; it is not a WGPU normalization apply or proof of complete worker regeneration. No guard was removed to make it pass.

## Browser Dependency Probe

A bounded read-only import-graph probe used the existing no-follow, opaque-first input view. It did not enumerate the repository. Two actual source entrypoints were followed: frame worker and browser boot.

The existing registry scanner hardcodes the TSX parser. It rejected a valid TypeScript-only file with `Unexpected if` at line 66. The corrected diagnostic selected `ts` for `.ts` and `tsx` for `.tsx`; no production registry scanner behavior was changed.

With exact workspace bindings for `@semio-tech/framework` and `@semio-tech/framework-os`, the probe found 31 module inputs and no missing relative input. Remaining undeclared bare imports are `@semio-tech/framework-replication`, `ajv`, and `@webassemblyjs/leb128/lib/leb.js`. The latter two occur inside OS inline tests; literal dynamic `node:fs`, `node:url`, and `node:path` imports also occur. A static-graph approximation must not silently ignore these. The next producer step must prove the actual Bun bundler's read set and test-branch behavior before selecting exact input authority.

## Browser Producer TDD Checkpoint

The actual in-memory Bun compiler, with the explicit `import.meta.vitest = undefined` production profile, follows exactly 35 source modules and three validated workspace manifests. No external runtime dependency is admitted. `🔣️wgpu-browser-profile.json` is the language-neutral input authority. Independent esbuild metafiles confirm the same 35-module union for the worker and boot entrypoints; repeat Bun builds produce identical bytes.

The source/oracle test first failed because the production helper was absent, then passed with **1 test, 11 assertions, 0.330 s**. The expanded browser packet reached **2 passed, 1 failed, 44 assertions, 3.03 s**: source/oracle and hostile-input/cancellation tests passed; projected-versus-physical canonical compilation failed before plugin resolution because Bun inferred its build root from a canonical entrypoint parent that does not yet physically exist. No destination directory was created to hide the error. The candidate fix explicitly sets the existing repository root in `Bun.build`.

A fresh rerun on 2026-08-27 was blocked at module import by a concurrent, unrelated `projectCargoProviderManifest` strict-mode reserved identifier (`const package`, discovery line 6650): **0 passed, 1 failed, 1 error, 0.152 s**. The owning coordinator and adjacent lane were notified. This is not a browser test result or a reason to weaken generator authority.

After that external owner renamed the binding, the actual browser packet passed: **3 passed, 0 failed, 50 assertions, 2.30 s**. The fix is only the explicit existing `root: repoRoot` build option. A separate in-memory probe also produced one clean artifact from a virtual entrypoint under an absent parent; neither probe nor production preview wrote a file or created the canonical hierarchy. Source/oracle, projected-versus-physical canonical bytes, undeclared/unused module rejection, opaque/escaping/duplicate profile rejection, symlink rejection, and cancellation at input and bundle boundaries passed.

Command for the expanded packet:

```text
bun test /Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/🧪️wgpu-generated-retirement.test.ts --test-name-pattern browser
```

## Retention and Disposal

Keep the two reproducible packet inputs, copied fixture scripts/configs and authored source inputs, and this Markdown report. New fixture directories use the exact prefix `🧪️wgpu-generated-retirement-` below this ticket. Their compiler/Nx/tool output files will be identified and deleted only after their active use finishes. No broad ticket deletion, other lane's output removal, or active transaction cleanup is authorized here. Current experiments have no production transaction journal.

## Package Producer Integration Checkpoint

The schema now registers one stable owner, `@semio-tech/framework-os`, for four catalog leaves and the tracked worker plus ignored browser boot bundle. It requires the exact browser profile, `package-projected-inputs-v1` with 64 MiB / 200,000-operation bounds, and source/canonical activation. Source activation requires every catalog row to be represented exactly once: 31 authored moves and one generated-source retirement. It does not activate for unrelated producer-input changes while only the old package exists.

The shared projected-leaf parser/view now has a closed producer identity union; the registry's public wrappers still require `plugin-registry`. WGPU payloads require `wgpu-frame-worker`, never impersonate the registry, and use the same no-follow move/edit/removal checks. The worker retirement is outside preview stale removals, whose output-root boundary remains unchanged.

- Schema TDD: missing package authority **0 passed, 1 failed, 1 assertion, 0.234 s**; paired schema/parser green **1 passed, 0 failed, 9 assertions, 0.679 s**. An intermediate schema validation correctly rejected the canonical worker as a self-input; that exact output path was removed from the input list, not exempted from the rule.
- Shared package projection and combined pure artifact preview: **2 passed, 0 failed, 39 assertions, 17.50 s** after missing-helper red **0 passed, 2 failed, 2 assertions, 2.13 s**.
- Actual combined writer TDD: missing runner **0 passed, 1 failed, 23 assertions, 3.34 s**; production runner through fixture Nx generate/check twice **1 passed, 0 failed, 41 assertions, 14.38 s**. All six outputs had exact preview bytes and mode `0644`; malformed canonical Cargo identity rejected.

The combined producer synthesizes only its declared generated worker and four registered template leaves while validating all canonical authored package facts. It never reads obsolete worker bytes as an input to browser compilation. The Nx command router and package scripts are registered; the launch seed has matching preview/generate/check entries. The generated `.vscode/launch.json` was not hand-edited or regenerated globally here.

This remains a component/producer proof, not a completed WGPU normalization transaction. The package completeness guard remains enabled. Complete authored task/consumer closure and genuine rollback → retry → commit → empty replan are still required.

## Still Required

The combined deterministic worker/boot producer and exact projected-preview protocol have component evidence. Narrow generated-launch parity, complete authored task/consumer operands, real projected-plan readiness, and genuine WGPU rollback → retry → commit → empty replan remain open. The draft still fails closed, and no full-ticket production WGPU/JCO readiness is claimed from this packet.

## Reviewed External Plugin-Bridge Refresh

The first 11-case producer packet completed with **11 passed, 0 failed, 197 assertions, 32.58 s**. A subsequently added transaction-readiness case was red; that historical green does not include it. A concurrent user edit then caused fresh root startup to reject five absent canonical WGPU tracked outputs, because exact source authority no longer matched the published package catalog. The source guard correctly failed closed. An intermediate source-phase contract restored startup while the authored change was reviewed; the complete canonical contract was retained as the reproducible input `🔣️wgpu-package-generator-contract.json`, not discarded or represented as complete.

Only `🟦️typescript/🐚️plugin-bridge.ts` differed among the 36 WGPU/JCO mapping preimages. Its retained old bytes are in `🧪️wgpu-generated-retirement-HT3ZIM` at the original package source path. The exact change is three source splices: add the existing `AppChannelRequestSequence` import, create one owner in `loadPluginModule`, and pass that owner as the second argument to `AppChannelClient`. The current OS API requires that second argument and reserves monotonically increasing query/cancellation identities across client recreation. No import module, semantic mapping, user implementation, or generated worker source changed in this refresh.

- Old source: SHA-256 `46963d8345ab343daaca9be0d867c7a1d8045ecb5ff5cf8d1beae43c8a787435`, 25,226 bytes.
- Reviewed source: SHA-256 `e383b9b555dca8dbd7c5cd54864651b6271010da3d91cef01697d01770b8a9ce`, 25,329 bytes.
- Composed catalog: `a1093d99d21337acababece03c3d48b42c9edc2c67603452637b76f321328dc8` → `4cde8fbe8eb3a2332954bdca0e1e5da35f0f996ecefc5ec73bf4b322cb224a58`.

`🔣️wgpu-plugin-sequence-refresh.json` records the three exact splices, old/new identities, current constructor operands, and language-neutral sequence results. Independent TypeScript AST checks prove the same import-module graph, the function-lifetime owner, and the actual constructor signature. The actual class is compiled with TypeScript and executed against the expected sequence vectors. The acceptance test first failed only at the stale catalog row (**0 passed, 1 failed, 17 assertions, 0.376 s**), then passed after the one-row refresh (**1 passed, 0 failed, 17 assertions, 0.427 s**). The independent Bun/esbuild browser read-set and deterministic output test also passed (**1 passed, 0 failed, 11 assertions, 0.357 s**). The historical purity fixture retains its original source hash and was not rewritten.

The canonical contract is now republished. A new missing-generation-authority negative failed (**0 passed, 1 failed, 10 assertions, 0.956 s**) and the validator was restored to require explicit generation and activation for this exact producer. Fresh full `loadTaxonomy()` workspace validation, `validateTaxonomy()`, and an actual root-script import succeeded, exit 0 in 0.561 s; the stable owner is `@semio-tech/framework-os`. No placeholders or missing-output guard relaxation were introduced. Full normalization transaction readiness is still not claimed.

## Exact Metadata and Authored Consumer Closure

The reviewed-source/schema/browser packet passed **3 tests, 39 assertions, 0 failures, 1.96 s**. The real projected preview then failed with precisely `WGPU Node/Nx workspace identity drift | WGPU exact Vitest configuration authority drift`. Its retained fixture was `🧪️wgpu-generated-retirement-PWGNYn`, with 31 moves, one retirement, and 88 reference edits. Inspection proved that the generic JSON adapter changed the package export identity key `.` to the empty string, while the semantic-owner Nx input glob and three Vitest config operands were missing.

The source-hash-bound package reference helper now preserves the export key as an identity, derives the semantic-owner glob from the catalog, and projects the three exact Vitest call sites and mapped test selectors. These implement the canonical Node/Nx/Vitest authority already required by `semanticPackageProjectionAuthority`; no broad JSON-key or script-string exemption was added. The focused metadata test failed at the export key (**0 passed, 1 failed, 21 assertions, 3.58 s**), then passed with independent JSONC and TypeScript AST checks. The subsequent two-case packet had **1 passed, 1 failed, 41 assertions, 10.58 s**: all six actual projected producer outputs were admitted; only the 18 separately listed Rust/prose findings kept readiness red.

Actual launch rendering—not a hand-edited generated file—passed **1 test, 16 assertions, 0 failures, 1.388 s**. The production `generateLaunchJson` rendered the existing seed/playground data in memory. Independent JSONC parsing proved preview/generate/check routes, `4_build` ordering 206.14/206.15/206.16, package scripts, and exact Nx script commands. This is narrow route parity, not a full repository registry freshness claim.

`🔣️wgpu-authored-source-fragments.json` registers 17 reviewed, source-preimage-bound fragments. Ten correct exact prose coordinates; five preserve build-script manifest/OUT_DIR operations; one preserves the native asset manifest base; one corrects the stale native plugin-directory default to the actual dev producer's `🔌️plugin-modules` owner. The original native fallback pointed at `../../../dev/js/plugin-modules`, which is not the current producer. That correction is explicit, not claimed as byte-identical behavior for the broken default. Configured `SEMIO_PLUGIN_MODULES` behavior must remain identical.

The pure fragment helper first failed as missing (**0 passed, 1 failed, 1 assertion, 1.051 s**), then passed **1 test, 69 assertions, 0 failures, 3.31 s**. It checks source and canonical spans, exact full source hashes, unique/nonoverlapping spans, and foreign/generated/extra-key/duplicate/source-drift rejection. Independent TypeScript comment-free AST and TOML parsing confirm that the prose edits preserve executable and manifest semantics. Catalog SHA-256 is now `f602c0fbfeb7f01c7af2d4ac5a7f6ebcf9740b382bb21cafe6a6ece602483507`.

The first integrated run rejected conflicting catalog affected-state rows because the new historical `sourcePath` fields were being treated as consumers (**1 passed, 1 failed, 70 assertions, 7.27 s**). The fix adds only their exact JSON pointers under the digest-verified catalog to existing frozen-coordinate authority. The next actual projected readiness/31+1 packet passed **2 tests, 25 assertions, 0 failures, 8.91 s**. The readiness test still explicitly permits the mandatory `nested-cargo-generation-unresolved` guard: retiring/rebinding the old task-owned independent browser producer and genuine transaction rollback/commit remain required.

The first actual Rust compiler packet failed in its fixture (**0 passed, 1 failed, 3 assertions, 9.54 s**): the canonical manifest directory used by the compiled build script had not been materialized, so filesystem traversal through that absent directory could not reach the logo. The bounded compiler fixture now creates its explicit canonical manifest base. No production directory or preview placeholder was created.

The corrected actual Rust compiler packet passed **1 test, 13 assertions, 0 failures, 10.62 s**. Nx compiled and executed the actual source/canonical build scripts and native-directory probes. Four generated build outputs agreed byte-for-byte, hidden icons stayed excluded, configured plugin-directory behavior stayed unchanged, and the canonical default matched the actual dev producer. The deliberately stale original default did not match and is not represented as parity.

## Sole Package Producer Retirement

`🔣️wgpu-tool-producer-fragments.json` records 14 additional exact source-owned fragments and language-neutral toolchain/retired-owner vectors. They retire six duplicate compiler functions, three renderer generation classes/routes/targets, and three duplicate Nx producer dependencies. Trunk build/serve and browser freshness route to the registered package producer. Existing native build/run and browser test command bodies remain byte-identical. The canonical Vitest configuration selects the three exact semantic test leaves under the semantic owner; executable source files in the real WGPU package remain untouched until a reviewed transaction applies.

The root producer now resolves and validates the explicitly requested workspace's schema, rather than implicitly loading the current process workspace. Its shared exact Bun identity helper first failed as missing (**0 passed, 1 failed, 1 assertion, 1.69 s**). An intermediate fixture lacked its required Nx root manifest (**0 passed, 1 failed, 8 assertions, 3.45 s**); adding that explicit fixture input yielded **1 passed, 0 failed, 8 assertions, 5.57 s**.

The independent TypeScript/JSONC fragment test reached the intended missing-registration red after all source/canonical spans, native-body preservation, removed compiler/target identity, and semantic selection checks passed (**0 passed, 1 failed, 32 assertions, 2.84 s**). The exact catalog now includes 31 authored fragments (17 prior + 14 producer fragments), with SHA-256 `f64f2f654e5dc12d57b72148b023b2c9a20d6fd8f147293dac871e2529b2319a`. The full generation-unresolved guard remains enabled pending actual canonical tests and transaction lifecycle proof. No global capture or production WGPU apply was performed.

The subsequent four-case packet passed the registered fragments, metadata, and 31+1 plan cases but failed the projected preview (**3 passed, 1 failed, 97 assertions, 15.50 s**). Direct same-plan projection localized a newly introduced external input: actor `🧵️shard-client.ts` now imports `🚪️lifetime/🟦️component.ts`. The reviewed runtime module has no further imports; its test-only imports remain eliminated by the explicit production profile. Adding only this exact module to the schema/browser input authorities produced **2 passed, 0 failed, 128 assertions, 17.72 s** for real projected preview and independent Bun/esbuild closure. The current graph is 36 modules, not the earlier 35.

Actual canonical Nx generation succeeded, and the actual browser-worker task ran **32/32 tests** in the two exact semantic files. The seven-test package route failed before tests because esbuild found two duplicate declarations in the shared root script (`policyCollectGluePathTargets`, `policySnakeToCamel`); the coordinator owns that unrelated correction. The whole canonical route case was **0 passed, 1 failed, 120 assertions, 32.16 s**, retained at `🧪️wgpu-generated-retirement-ZE2wJ8`. It is not reported as a green package test. The duplicate four-leaf prototype writer is being retired in favor of the six-output producer; its previous repeated-writer checks are superseded by the actual combined Nx generation/freshness case and the new strict source/generated-drift negative.

The actual transaction fixture uses only its disposable Git repository, a fixed ticket admission root, an exact retained plan artifact, and inherited generator environment explicitly rooted in that fixture. Its first baseline commit attempt inherited host signing and failed (**0 passed, 1 failed, 3 assertions, 7.15 s**); fixture-only `--no-gpg-sign --no-verify` prevents that host interaction. It then reached the intended unconditional completeness-guard red (**0 passed, 1 failed, 4 assertions, 16.50 s**), with no transaction started.

## Subsequent External Rust Source Drift

Three additional external source files changed after the prior coherent workspace loader. The exact old bytes remain in `🧪️wgpu-generated-retirement-HT3ZIM`; no source or catalog preimage was silently overwritten:

| Source leaf | Frozen SHA-256 | Observed SHA-256 | Bytes |
| --- | --- | --- | --- |
| `📦️glue.rs` | `b4869021b74b86466fbeef3798547bb2cb40381482f03d47f8fd14acf00a7790` | `3e3f4f573ca65dcffdec53e4194cf08c3bdf60ecf207b12c11ae8e36f8e85e01` | 828737 → 828740 |
| `🦀️frame_job.rs` | `4c0602df7cab3a8a912605a221a730006cb613d07b208060556fd76ae195d23f` | `b6dbed27d9431e82bc53eebe7bff2e122447e402f01892bec0b1fafc1e58528b` | 34039 → 34039 |
| `🦀️surface_lane.rs` | `d6131ff4d56d636a30ebe586e698449799e8391a49a240cb8df9c4b9e2daf3e7` | `1c5681eb8938cd3fe4dbef2ebe6a2662223f02ea12207c4ecd4ac5e61f5b1b20` | 22434 → 22434 |

The diffs migrate job wall-clock fields/constants from milliseconds to microseconds. At observation time the changes were incomplete: frame-job parameters still used `now_ms`, and surface resize supplied its `*_MS` constant to a `step_budget_us` field. These edits belong to the external owner and remain untouched. Full source authority correctly rejected them; the root workspace validator consequently rejected the five not-yet-created canonical outputs. The new sole-producer negative was blocked at module import (**0 passed, 1 failed, 1 error, 3.15 s**), not evaluated. A startup-only source-phase ownership distinction is under review; full plan/apply preimage authority must remain strict.

## Source-Phase Startup Proof

`semanticPackageSourceOutputPhase` now distinguishes pending outputs from implementation-content authority. It accepts only the exact catalog-bound 32-path Git admission census, no-follow regular source paths, exact Cargo/Node/Nx source manifest preimages, exact root workspace membership, and absent canonical mapping/adapter/derived/output destinations. A non-Git fixture fails closed. Ignored tooling is neither traversed nor pruned. Body-only source drift can permit unrelated startup; full package preview and planning still reject the exact changed preimage. No schema/catalog hash or source implementation changed in this distinction.

The new language-neutral `🔣️wgpu-startup-phase.json` and `🧪️wgpu-startup-phase.test.ts` first exposed a missing helper, then a missing `execFileSync` import, then unrelated fixture registry authority. After fixture isolation, the original eight-vector loop and expensive scoped-plan case exceeded Bun's default five-second test bound (**0 passed, 2 failed, 320 assertions, 12.25 s**). Splitting each negative vector into its own test and setting the scoped-plan test's explicit 15-second bound yielded **9 passed, 0 failed, 328 assertions, 19.53 s**. The positive case took 14.93 s; individual negatives took 0.29–1.22 s. These are executed results, not a performance qualification. Actual live `loadTaxonomy()`, schema validation and root import also succeeded while strict full mutation authority retained the external Rust mismatch.

After the coordinator removed two duplicate root function declarations, the canonical-route plus sole-producer-negative packet yielded **1 passed, 1 failed, 239 assertions, 47.29 s**. The negative passed; the seven-test route exceeded its 15-second task budget. A narrowed actual Nx rerun in retained fixture `🧪️wgpu-generated-retirement-XUT3gU` then produced a concrete module-import failure: root `WORKSPACE_ROOT = import.meta.dir` was undefined under Vitest, and the following `join` threw `ERR_INVALID_ARG_TYPE` at root line 125. A verbose repeat took 3.79 s in Vitest and ran no tests. No task budget was increased to conceal this failure.

Canonical bootstrap also still encounters three eager policy filename derivations calling full workspace `loadTaxonomy()`. The approved repair is to use the existing schema-validating `loadCatalogTaxonomy()` only for those vocabulary constants, with an actual fixture-owned module regression. Explicit workspace validation must continue rejecting absent active outputs; no canonical output will be represented as healthy merely to bootstrap its producer. Catalog/schema remain frozen for concurrent read-only captures.

The fixture-owned root regression first corrected two harness mistakes (the expected error wording, then one relative discovery import), then reached the intended eager-load failure: **0 passed, 1 failed, 116 assertions, 5.74 s**. The production repair changes only the three policy filename vocabulary derivations to explicit `loadCatalogTaxonomy()` and obtains root `WORKSPACE_ROOT` through portable `dirname(fileURLToPath(import.meta.url))`. The subsequent actual Nx/runtime packet passed **2 tests, 247 assertions, 0 failures, 13.00 s**: all **39 canonical Vitest tests** ran in the three exact semantic leaves, and the actual fixture-owned root rendered six previews before output creation while explicit local `loadTaxonomy()` still rejected absent active outputs. Independent TypeScript AST checks cover all three vocabulary derivations. A separate live workspace-validation timing was **786.60 ms**, with no problems; this does not explain the earlier scoped-plan test's 14.93 s duration.

## Actual Retirement Preflight Boundary

After the 39-test canonical gate, the unconditional WGPU completeness placeholder was temporarily removed for the unchanged actual transaction test. It failed before journal creation: **0 passed, 1 failed, 5 assertions, 6.67 s**, `Evidence-removal structured incoming reference set changed` at the old generated worker. The placeholder was immediately restored; no transaction began and no production apply was attempted.

The exact plan is retained at `🧪️wgpu-generated-retirement-UlF85H/🧾️retained-plan/🔣️.json`. Calling the unchanged lexical helper through in-memory TypeScript compilation returned exactly three physical prestate consumers: source `🌐️index.html`, `📜️script.ts`, and `🟦️typescript/🟦️boot.ts`. The plan already carries exact-preimage edits for all three: HTML `@620` and boot `@603` point to the canonical generated worker; the task's whole old producer block `@5038` is replaced by the sole-producer delegation. The preflight helper currently reads physical pre-edit bytes, using its authority plan only to enumerate candidates. The failure therefore requires phase-aware exact planned-reference accounting; ignoring these consumer paths or weakening the physical terminal stale-reference check would be incorrect.

Two earlier memory-only attempts to expose the private helper were unsuccessful (Bun data-URL import reported `NameTooLong`, and runtime virtual-module resolution rejected the diagnostic module). They wrote no files and changed no production source. The successful independent TypeScript CommonJS evaluation preserved all helper logic and returned the three exact rows above.

## Exact Phase-Aware Retirement Revalidation

`removalReferenceProjection` and `evidenceRemovalIncomingReferences` project only regular-file consumers already bound by exact plan edits and matching move preimages. Pending bytes require the complete original hash, size, mode and exact nonoverlapping edit spans. Applied bytes require their exact journal backup and independently recomputed postimage. Source and destination maps remain separate so move cycles resolve by physical transaction phase. Malformed offsets, conflicting preimages, wrong move phase, changed pending bytes and changed applied/backup bytes reject. The callback is used only for generated-source retirement preflight; terminal stale-reference scanning remains physical. Symlink consumers are not projected. Staged consumers inside the excluded executor tree are validated by transaction tuples, not claimed as callback-scanned public files.

The language-neutral `🔣️wgpu-retirement-reference-phase.json` and independent TypeScript evaluation test first failed with a missing helper (**0 passed, 13 failed, 14 assertions, 2.08 s**), then passed **13 tests, 31 assertions, 7.57 s**. Adding physical terminal/undeclared-consumer coverage passed **14 tests, 37 assertions, 2.95 s**. A move-cycle positive exposed a real one-map identity bug; separate source/destination maps fixed it. The subsequent 15-case run was **14 passed, 1 failed, 36 assertions, 12.62 s**: the six-scan integration test exceeded its default five-second bound at 5.82 s during concurrent full scans. It is being split into independent phase/foreign/symlink cases, not reported as green.

The broader non-lifecycle package packet was **20 passed, 1 failed, 839 assertions, 49.94 s**. Its one failure came from a manually reserialized fixture Nx manifest breaking an exact authored fragment. Materializing the real plan instead fixed that case: **1 passed, 0 failed, 139 assertions, 18.66 s**. The complete packet has not yet been rerun green.

## Actual Rollback, Interrupted Attempt and Recovery

Fixture `🧪️wgpu-generated-retirement-XDLdXS` proved first-attempt six-output generation/check and injected rollback, restoring every affected source byte/mode and removing all generated outputs. The retry interruption harness mistakenly checked `progress.completed` instead of the actual `progress.current`; its child continued to verification and exceeded its 30-second subprocess limit. The whole case was **0 passed, 1 failed, 77 assertions, 67.94 s**, not a lifecycle pass.

The exact attempt-two journal at `🧪️tests/🧾️taxonomy-transaction/🔖️073d0ecffd02ca0206e67e15c38c6bbff6129df6bddfb1e6b0339806d742be12/🔂️attempts/🔢️000002/🔣️.json` was resumed with its retained plan and fixture-owned environment. Recovery exited 0, committed 31 moves, one retirement, 112 edits and one six-output regeneration, and passed actual Nx freshness. No elapsed-time claim is made for that separate recovery. The fresh canonical-owner replan was still nonempty: pure package authority rejected the exact ignored boot output and its generated parent, producing cascading generic findings. This is not complete canonical acceptance.

The corrected lifecycle input now interrupts after all 31 moves and the retirement are staged, then again after the first exact edit. It checks foreign incoming references fail without changing the journal at both phases, then resumes commit and requires a fresh empty canonical-owner plan. Those new complete assertions remain pending until executed. The mandatory WGPU completeness guard remains enabled.

## Canonical Ignored Output Boundary and Dated Fixture Snapshot

On 2026-08-27 a fresh fixture using current external browser inputs failed before the new boundary assertion: retained UI `🧰️framework/🔨️modules/🖱️ui/🧬️contract/🧵️retained/🟦️component.ts` now imports `./🛡️validation/🔬️graph/🟦️component.ts`, which is outside the frozen 36-module WGPU profile. Result: **0 passed, 1 failed, 113 assertions, 7.90 s**. The live source remains untouched; schema/catalog remain frozen for concurrent full captures. Live WGPU readiness is blocked by this independently changed graph and the earlier exact Rust preimage mismatches.

`🔣️wgpu-browser-fixture-inputs.json` is an explicitly dated, diagnostic-only manifest of 33 external input files already retained in the committed XDLdXS fixture. Tests read those exact hash/size-checked bytes to isolate transaction behavior from continuing external changes. These are ticket-only fixture inputs, not a new production authority or fallback. Any lifecycle result using them applies only to that exact prior snapshot, not current live WGPU readiness.

The canonical-output vector is `🔣️wgpu-canonical-output-authority.json`. The intended red rejected exactly the ignored boot leaf and parent (**0 passed, 1 failed, 116 assertions, 11.22 s**). `semanticPackageProjectionAuthority` now permits only the optional ignored path derived from the exact package browser entry mapping and matching ignored generator root. All mapped/tracked files remain required. Unknown siblings, symlink/directory outputs, wrong inclusion and missing active worker remain negative cases. Shape coverage passed in the next two-case run, while the freshness case correctly failed because no preview ran: **1 passed, 1 failed, 238 assertions, 31.09 s**.

The paired `generatorPlanning` change verifies package output freshness whenever canonical WGPU ownership overlaps the inventory scope, including when the ignored leaf is missing. It uses the existing exact package activation and deterministic preview; no-op output equality produces no regeneration, while missing/different bytes or mode cannot yield an empty plan. Schema/catalog remain unchanged. Focused runtime validation of that change is in progress.

That validation exposed the exact empty-parent edge case: removing boot left its immediate generated directory physically present but absent from Git's leaf census (**0 passed, 1 failed, 118 assertions, 20.44 s**). The shared `semanticPackageIgnoredGeneratedOutputPaths` now supplies the exact allowed immediate parent to package inventory. It is admitted only when a no-follow regular directory is proven empty; unknown children, wrong kind and symlinks still reject. No arbitrary ignored directory is skipped.

The complete freshness case then passed **1 test, 132 assertions, 0 failures, 54.00 s** (test body 51.96 s) using the dated XDLdXS input snapshot. It proved a fresh no-op plan actually invokes deterministic preview; missing boot, wrong bytes and mode each fail producer freshness and schedule only the exact WGPU regeneration. Unknown ignored siblings, symlink boot and missing active worker remain negative. No timeout was increased; the existing explicit 60-second case bound was retained. This is functional evidence under concurrent full scans, not a throughput claim.

The split retirement preflight packet passed **17 tests, 42 assertions, 0 failures, 6.43 s**. Terminal scanning remains physical; an undeclared incoming HTML consumer remains visible, changed planned bytes reject, and a planned symlink consumer still produces its physical old-target incoming row. Splitting the previous six-scan test into three independent cases retained all assertions without increasing the default per-test timeout. The complete snapshot lifecycle is being rerun separately with the corrected progress field; the WGPU placeholder will be restored after that diagnostic run pending coordinator review.

## Coordinator Adoption After Agent Interruption

At 2026-08-27T13:43Z, the assigned agent had stopped with a usage-limit error and no matching fixture process remained. The exact retained `🧪️wgpu-generated-retirement-6LbGtY` journals show attempt one rolled back at revision 243 after the injected `after-regenerations` failure, and attempt two committed at revision 161 with 31 installed moves, one staged retirement, 25 edited files and one completed regeneration. Its plan digest is `4e275bf535a76a7bbc1b41086a5192f797b1144c2915621c7875255d962a6970`.

This terminal journal observation is not a recovered test-run result or proof that the final canonical empty-plan assertion passed. The subprocess session is no longer available. All fixture inputs, journals, backups and retained plan remain intact. No recovery or rollback was needed for this already committed fixture. The coordinator restored the temporary `nested-cargo-generation-unresolved` guard in the live normalizer pending complete current-source package acceptance; schema and catalog pins were unchanged. No production WGPU move was performed.

## Recovered Final Result and Later Fixture Disappearance

The original output handle was subsequently recovered read-only. The actual run was **0 passed, 1 failed, 103 assertions, 130.28 s**; its 128.634-second body exceeded the unchanged 120-second test bound. It reached the final canonical plan assertion with `[moves, removals, edits, regenerations, unresolved] = [0, 0, 0, 0, 1]`; Bun also reported one dangling process killed. Staged resume, first-edit resume, foreign-reference/journal immutability, committed outputs and final Nx check reached their assertions, but the final empty-plan assertion failed. The single unresolved row was not printed by that assertion and is not inferred to be harmless. This recovered result supersedes the earlier unavailable-output observation, not the failed verdict.

A coordinator-requested bounded read-only replan then failed before inventory: the exact `6LbGtY` root and its retained plan/journal parent had disappeared (`ENOENT`). The `XDLdXS` retained input root was also absent on a direct no-follow check. This lane issued no deletions and did not reconstruct a plan/journal or invent the missing unresolved row. Current Rust/browser drift hashes and complete bounded diffs had been captured before the disappearance. Ticket Markdown, test files and diagnostic input manifests remained. The live completeness guard stays restored; retained fixture-based tests are no longer reproducible until their missing input provenance is safely restored or replaced by a newly reviewed current-source fixture.
