# Root Script Compiler Consistency

## Finding

Actual canonical WGPU Vitest integration reached the root producer import, where esbuild rejected two duplicated top-level implementations: `policyCollectGluePathTargets` and `policySnakeToCamel`. Bun had accepted the root script, so this was a compiler-consistency failure rather than a WGPU mapping or generator-output failure.

The two glue helper bodies are textually identical. The snake-case helpers are not: the earlier regular expression retains some repeated/trailing underscores, while the later split-based implementation drops them. The bounded test checks the actual Bun Rust/Protobuf schema extractors before any change and independently compiles the effective final helper with Bun and esbuild. Consolidation must preserve that observed behavior, not silently choose a different field-naming policy.

## Test-first Packet

The language-neutral input and permanent tests are under `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧪️tests/🧪️root-script-compiler`. They cover unique concrete top-level declarations, full-script Bun/esbuild compilation, nine field-name cases, actual schema-extractor runtime output, bounded glue traversal with a `fast-glob` oracle, and the registered Nx/launch route.

The first run was **0 passed, 5 failed, 23 assertions in 3.06 seconds**. It confirmed exactly the two duplicate symbols and the actual esbuild diagnostics. Registration was absent as expected. Two further failures were separate: process-global root import rejected five missing canonical WGPU outputs, and the fixture incorrectly expected `.rs` inside its root for an inline `#[path = "."]` declaration (the existing helper returns a sibling `<root>.rs` path). The package lane owns the source-phase generator-contract issue. The bounded duplicate-removal fixture was corrected to three real external module targets; no glue traversal behavior was changed.

Before deletion, actual Bun module loading of both original field-helper declarations confirmed the final split-based behavior for all nine vectors. The corrected glue fixture independently passed. That focused run reported **1 passed, 1 failed, 30 assertions in 7.79 seconds**, with only the unrelated guarded root import still failing.

The redundant second glue helper and earlier shadowed field helper were then removed. The surviving function bodies are unchanged; the dead-example policy's existing documentation now attaches to its actual function, and the surviving field helper has one docstring. Full-root Bun/esbuild compilation, unique concrete declaration census, and glue parity passed **3 tests, 8 assertions, zero failures in 726 milliseconds**. The gate is registered in the existing library router/Nx project and both launch catalogs, group `4_gate`, order `410.11`. Full runtime and registered execution await the separate package startup correction.

After the package lane's source-phase startup correction, actual uncached `@semio-tech/repo-lib:test-root-script-compiler` passed **5 tests, 44 assertions, zero failures in 2.57 seconds**, exit zero (session `31437`). Both actual Rust and Protobuf extractors printed the expected field names. The independent full-script compiler check and all registration assertions passed.

The interactivity task's exact Nx gate was also run uncached with `--args='tool-jobs --self-test'` (session `83901`). It passed with **33 exact-factory-proof owners, 255 custom rows, 25 generic rows, and 940 self-tests**, exit zero. This confirms the previously blocked root startup and that specific interactivity gate, not the full interactivity build or repository completion.
