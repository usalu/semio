# goal-chain — 4/4 fixed, both mechanisms REWRITABLE/provably-non-referential, not suppressed-detection

**(1) Chain preservation, mechanism A** — derive.rs **344, 351×2, 1900**: fixed. `passThroughSuffix` (🔍️discovery/🟦️component.ts) gained zero-arg `parent`/`to_path_buf`/`clone`/`as_path`/`to_owned`; the bound-identifier match in `rootEnd` now routes through it. Real gap wasn't just the chain: `mutation_source_authority_tests::materialize("valid", ..)` is tuple-destructured (`let (workspace, _, leaf) = ..`), never bound before — added tuple-pattern `let` support, a mod-scoped qualified one-hop (`resolveQualifiedFunctionBody`, disambiguates the two same-named `materialize`s), and literal-narrowed match-arm elimination (`tupleTailPositions`): the callee's `match case {..}` is proven a no-op for THIS call because "valid" equals none of its string arms and `_ => {}` is empty — sound constant-propagation, not "assume happy path" (refuses if any arm could match). SUPPRESSED (non-referential), not merely undetected: fail-before(blocked)=true → pass-after(suppressed)=true, verified against real emitter both directions.

**(2) Repo-root helper hop, mechanism B** — run/component.rs **2787**: fixed, REWRITABLE (base=[], real oldValue→newValue join target), not suppression. Read `test_repo_root()`: it is NOT a fixed hop to `CARGO_MANIFEST_DIR` as first assumed — it's a dynamic ancestor-walk to `nx.json` (confirmed: repo's one canonical root marker, no nested copy exists). Added `rustRepoRootAncestorWalkHelperNames` (shape-matched, not name-keyed) to both `inspectRustManifestPathReferences`/`Candidates`; a same-file zero-arg call to a matching helper is now treated as a proven repo-root base. `find_repo_root()`'s extra `SEMIO_REPO_ROOT`-env-var branch deliberately does NOT match (verified via synthetic) — that path isn't provable the same way.

Correct refusals unchanged (verified): mcp:52, vector-converter:146/155, all still blocked.

Tests: `🧪️rust-finite-target-consumption` 86/86. `🧪️rust-physical-reference-context` 18/32 pass — remaining 14 all share one root cause: concurrent uncommitted `taxonomy.json` edit (`cargo-integration-test` contract), confirmed via `git diff`, unrelated to this change; fixed the one real regression it caught (a stale declare-stub in that test needing `rustRepoRootAncestorWalkHelperNames`). `📜️goal-chain-verify.ts` (ticket root): 15/15 checks pass, fail-before/pass-after both directions, zero regressions, soundness-boundary synthetics included.

Files: 🔍️discovery/🟦️component.ts (passThroughSuffix, tuple-let, resolveQualifiedFunctionBody, tupleTailPositions, tupleCallNonRepoPositions, rustRepoRootAncestorWalkHelperNames, wired into both CARGO_MANIFEST_DIR provers); 🧪️tests/🧪️rust-physical-reference-context/🟦️.test.ts (added missing declare stub). No plan/apply run.
