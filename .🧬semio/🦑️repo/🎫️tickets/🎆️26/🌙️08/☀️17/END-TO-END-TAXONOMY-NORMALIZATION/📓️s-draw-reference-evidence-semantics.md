# Draw Reference Evidence Semantics

## Subsequent retention and input-state update

The following causal audit remains historical evidence. The newer AGENTS policy and fresh input checks are recorded in [Historical Package Identity and Retention Recheck](📓️s-historical-package-identity-input.md). The machine-readable audit input linked below is currently absent, so its old link is not evidence of current availability. All 35 output owners are open; the coordinator retains their exact observation registrations without relabeling them as permanent inputs. Purity now has a separate canonical genuine-input regression, and the four originally detected full-path spans are supplemented by one newly detected prefixed-owner suffix. No old result is silently presented as a fresh run.

The subsequent [completed-output and historical-link closure](📓️s-completed-output-retirement-and-historical-links.md) records removal of the six exact completed compiler outputs after current-caller and recovery checks, and replacement only of the ten approved inline-code spans with historical links to the same frozen source coordinates. It records all before/after hashes and spans; surrounding report facts remain unchanged. No executable-output freeze or blanket Markdown exemption was introduced. The original blocked-plan classification below remains a record of that earlier state, not a fresh apply-ready plan.

## Decision

The retained full Draw plan's 77 proposed edits in 35 other-ticket JSON documents are historical report output coordinates, not replayable pending edits. Their exact current bytes and 77 source-value pointers are proposed for explicit frozen-coordinate registration. They have **not** been registered by this audit; coordinator approval is required.

The four purity-fixture coordinates are different: its ticket test uses the recorded paths to perform fresh discovery, open files, hash bytes, and assert destination absence. Freezing those paths alone would knowingly leave a live consumer broken. The fixture and test require a deliberate authority redesign before the Draw move.

The six retained root bundles contain executable source-path reads. Four have direct authored build-output evidence; two have only inspected bundle-format/name evidence, with no recovered original build command. None is eligible for the JSON evidence registry. The ten inline coordinates in two other-ticket Markdown reports describe historical topology or files already touched; they are historical prose, but the current JSON-only registry cannot own their spans.

No other-ticket JSON, Markdown, bundle, purity fixture, producer, source leaf, or retained plan was modified. No producer was executed and no global capture, production generation, or apply was started.

## Exact packet and reviewed coverage

The immutable input is [the complete Draw plan](🧾️draw-full-ticket-reference-plan/🔣️.json), file SHA-256 `cda9fa728e0bce791ef0759822ab692871852656ea49f3be407a8678ca00e42b`, 11,458,773 bytes, plan digest `15ab99bf1429bb43af069c72eca2d157340f847fe5657e31259bf29c2546d288`. It records 11 moves, 125 reference edits, one plugin-registry regeneration, and 18 blocking unsupported-syntax findings. It is not apply-ready.

[The machine-readable causal classification](🧪️draw-reference-evidence-classification/🔣️.json) retains every reviewed document's exact repository path, SHA-256, mode, size, half-open UTF-16 and UTF-8 spans, value hashes, JSON pointers or syntax nodes, original plan indices, producer evidence, and proposed contracts. It avoids duplicating old Draw path literals: the original plan and exact source document remain their authority.

| Group | Files | Relevant spans | Retained-plan indices | Disposition |
| --- | ---: | ---: | --- | --- |
| Interactivity verifier outputs | 33 | 66 | edits 11–76 | Exact historical source-pointer registration proposed |
| Async-test scan report | 1 | 2 | edits 0–1 | Same, with explicit recorded absolute root |
| S1 deasyncify result report | 1 | 9 | edits 2–10 | Same; original scan/apply mode not established |
| Remaining-package purity fixture | 1 | 4 | edits 121–124 | Live consumer redesign required |
| Retained root bundles | 6 | 6 | unresolved 2–7 | Executable payload; separate authority decision required |
| Other-ticket Markdown reports | 2 | 10 | unresolved 8–17 | Exact historical prose, not blanket document exemption |

The other Draw plan operations are outside this classification: 24 runtime/build edits, eight dependency-baseline users-coordinate edits, and 12 frozen CAD/Draw catalog coordinates. The two current-ticket report findings at unresolved indices 0–1 were already corrected through coordinator-authorized links to the catalog's explicitly historical source field. The original blocked plan remains unchanged.

## Thirty-three interactivity outputs

All 33 are under the INTERACTIVE-JOB-RUNTIME-REFACTOR ticket. The relevant fields are two exact `/globalPayloadStores/<index>/file` pointers per document. The documents lack an own `schemaVersion` property and contain verifier counts, factory contracts, remaining-command observations, self-test counts, failure reports, and global-payload-store observations.

The inspected root producer establishes the direction of data flow:

1. `toolJobGlobalPayloadStoreInventory` derives `file`, `line`, declaration text, and owners from scanned Rust source text.
2. `toolJobCoverageRun` obtains the current production-file inventory and filters those observations into `globalPayloadStores`.
3. `VerifyScript.runToolJobCoverage` computes a fresh report before reading the optional `--output` argument, then writes that report. The JSON output is not read as an input or replayed.

The most recent inspected root producer preimage is SHA-256 `3b49d102671dbc39737a4160a2ad5d09a865025d507a1a9266fb8888e8322941`, 2,748,964 bytes, mode `0644`. Anchors and contemporaneous line numbers are in the manifest; the shared root script can change concurrently, so those positions describe this pinned observation, not a perpetual current-state promise.

An exact-filename search over the relevant tickets' authored Markdown and executable text found 18 authored hit lines for the 33 filenames, with no executable report-input reference in that bounded search. Some reports record the exact verifier `--output` command; others describe count checkpoints or byte-equality checks. For files with no recovered original invocation, attribution is explicitly limited to shared output structure and inspected producer semantics. This audit does not claim universal absence of external consumers.

Some historical filenames were reused as output destinations. The proposal freezes only the exact current reviewed bytes, not a claim that each filename was immutable throughout its history. Any later rewrite changes the digest and must fail closed until deliberately reviewed again.

## Async-test scan output

The microkernel `async-test-attr-scan-repo-wide.json` is SHA-256 `ed82b4199446f8ae3bbe8dc9ab03a4e4d70fb9ea24cbc040e45492303b56ba5a`, 10,742 bytes, mode `0644`. Its two relevant pointers are `/manifests_needing_dev_dependency/50` and `/manifests_needing_dev_dependency/51`.

The document explicitly says `mode: scan`. Its producer accepts source roots and mutually exclusive scan/apply options, scans source files anew, and prints the resulting report to stdout. Apply mode also rescans source roots; it does not consume this JSON report. The authored macros/block-on report explicitly identifies this artifact as a saved read-only scan.

The fully inspected producer is `async-test-attr.py`, SHA-256 `18f1d7d18de647617ab1a30deec02c35960ae6babef1d50bfb26c456315313f9`, 8,238 bytes, mode `0644`. No Python code was executed.

## S1 report: why edits is not an executable plan

The microkernel `sol-fleet-s1.json` is SHA-256 `38d8baecb28c9b9c98d8c59224eb9d5ab231bf9d0a06fc07ebd82fef0eaf23b2`, 249,363 bytes, mode `0644`. The nine relevant pointers are `/edits/853/file` through `/edits/861/file`.

Its root fields are `totals`, `by_trait`, and `edits`. An edit contains only `file`, `line`, `trait`, and `fn`; it has no preimage, replacement payload, or replay operation. The inspected producer `deasyncify-external-impls.py` derives those rows while processing source text. Its optional report file is opened write-only after the scan/apply processing. It has no report-input argument or JSON replay implementation.

The producer SHA-256 is `cad6af0d6189e3b67bc8d84a8d396ff3d2f38851433f094e7b2a3e00a694eed2`, 9,818 bytes, mode `0644`. The exact original invocation and scan/apply mode were not recovered. The artifact's count of 892 therefore does **not** prove that 892 edits were applied. The justified classification is a non-replayable scan/apply result report, not an executable pending edit plan.

Both microkernel JSON documents record absolute coordinates rooted at `/Users/ueli/Documents/semio`. Proposed rows explicitly bind that historical root using `representation: recorded-repository-absolute`; it is validated lexically and never resolved against the filesystem. Their absent version property is represented by `schemaVersion: null`, which means property absence, not a null property or fallback version.

## Purity fixture remains a live input

The remaining-package purity fixture is SHA-256 `49087f2dee618c3c8392ce3bc25c348af87d5380fe16523ed61ee6c02448f577`, 241,861 bytes, mode `0644`. It declares `non-authoritative-concurrent-source-byte-drift`, not an approved move authority. The four Draw coordinates are `/mappings/29/0`, `/mappings/29/4`, `/mappings/29/5`, and `/mappings/29/6`.

The complete ticket-local `🧪️remaining-package-purity-authority.test.ts` was inspected. Its live behavior includes:

- Fresh package-problem discovery and Git-admitted path-set comparison against the golden.
- Independent fast-glob enumeration of the golden's source identities and physical source-byte hashing.
- Assertions that nonidentity destinations do not yet exist.
- Current taxonomy and catalog hash assertions and generator-input matching.

This is affirmative consumer evidence, not a hypothetical risk. A historical freeze without changing the test's model would preserve obsolete physical reads after Draw normalization. No frozen row is proposed for this document. The live test was not executed during this bounded review because it launches broader live discovery and its original taxonomy pin has already changed.

## Six compiled bundles

Independent TypeScript parsing located each blocking escaped coordinate as a real `StringLiteral` in the `drawEditorSources` array within `toolJobCoverageRun`. That array feeds `policyReadFileSafe(root, file)`. Each bundle has a Bun shebang, executable mode `0755`, an `import.meta.main` guard, and root command dispatch. These strings are executable code, not comments or passive JSON observations. Their source uses ordinary JavaScript Unicode escapes; JSON-escaped logs should not be mistaken for a different double-escaped source representation.

The two exact build commands and two additional successful-build reports identify `root-cli-v2`, `root-transaction-v2`, `root-transaction-attempt`, and `root-sharded-inventory` as retained build proofs. No exact creation-command reference was found for `root-boundary-v2` or `root-transaction-source-preimage` in the bounded search of 180 direct ticket files. Their build-output origin is an inference from format and name, not a recovered command.

All six full hashes, sizes, line numbers, AST containment, and exact spans are in the manifest. No execution reference was found in the bounded ticket search, which does not prove that execution is impossible or unused. Required next authority is an explicit build-snapshot/retired-execution classification or an owned regeneration decision; neither generic JSON freezing nor silently editing generated JavaScript is justified.

## Two historical Markdown reports

The microkernel Draw report is SHA-256 `2ca9d03d6ffd71415c92e7622b5a9a501fa92d6a09d22be62332e5205a93d268`, 17,054 bytes, mode `0644`. Its seven spans are line 28's historical owned-fork explanation and six entries under “Files touched.”

The microkernel extensions report is SHA-256 `ea1c90d528328109f0c3df8ff71d97cd56d672766fde61ac9a237affb5ffb2b2`, 25,332 bytes, mode `0644`. Its three spans are entries under “Files touched (created/updated).”

MarkdownIt independently classified all ten as inline-code spans, and exact original offsets matched the retained plan's unresolved records. The surrounding text describes completed changes, not pending command operands. Recommendations elsewhere in either report do not convert these specific historical coordinates into live references.

Only exact historical spans are candidates for preservation. The entire Markdown documents must remain ordinary incoming consumers for any unowned path-bearing content. No other-ticket prose has been rewritten.

## Registration and remaining work

The manifest contains 35 proposed closed registry rows and 77 exact source selectors: 33 relative rows and two rows with the explicit recorded-root representation. Their absent document-version property is deliberate. These proposals need coordinator approval before insertion; there is no wildcard fixture exemption.

Before a production Draw transaction can be considered, the registry must validate the approved rows and reject digest/pointer/unowned-span drift; the purity fixture's live test must be redesigned; the executable bundles and historical Markdown require explicit decisions; and the full plan must be rederived under the final coherent schema and current preimages. This review does not turn the retained blocked plan into an executable plan.

The read-only boundary was exact retained-plan owners, their concrete producer files, authored reports and bounded filename searches. Actual Compose trees remained opaque. The only writes made by this classification are this report and its ticket-local machine-readable evidence.
