# Artifact Empty-Facet Primary-Leaf Integration

## Scope and Authority

Baseline remains `9f449b10659b95148c8bcb3f91ce583bf7446973`. Actual `compose/**` and `temp/compose/**` are entirely opaque and were not read, traversed, restored, or changed. No real Git mutation or production physical artifact move was performed.

The frozen corpus has 4,346 exact `📌️empty.md` source leaves below literal `✏️s`. The existing mapping ledger remains `c5704e063c10949b903a518c9a8400d3a599d0a72174a2b6857bf015ce9d09db`. Sources project to the primary Markdown leaf in the same semantic owner, using `windowEmptyFacetFileKindId` rather than an independent filename convention.

The new schema contract is `artifact-empty-facet-primary-markdown-v1`, with `contractKind = semantic-facet-primary-file`. It declares seven complete owner-path forms: plugin commands, artifact surface facets, artifact mode facets, artifact window facets, engine mode facets, engine window facets, and extension-window facets. Every directory capture resolves through the existing semantic kind/member registry; literals, named facets, complete path depth, source root, file kind, and exact source filename are checked. Arbitrary basename lookalikes are unclaimed. Destination occupancy is checked in the direct no-follow owner, including NFC/case/VS16-folded collisions.

The live structural census matched all 4,346 source mappings with zero unclaimed cases:

| Owner Form | Leaves |
| --- | ---: |
| Plugin Commands | 33 |
| Artifact Surface | 1,022 |
| Artifact Mode | 1,184 |
| Artifact Window | 2,082 |
| Engine Mode | 4 |
| Engine Window | 17 |
| Extension Window | 4 |

The old generic semantic-stem set admitted `empty` independently of owner structure. The new counterfeit-owner test exposed that bypass; only `empty` was removed from that generic set. A whole admitted-tree census using the shared scoped Git pathspec and both opaque exclusions found exactly 4,346 live regular `empty`/`empty.test` extension-bearing leaves, all governed by this contract, and no other affected empty stems. Other generic stems were not changed.

## Authoring, Not Generated Output Ownership

`new surface` writes absent starter files and preserves existing content. It is an authoring command, not a reproducible generator for authored artifact files. The 3,228 old `Generated by` byte markers are provenance text, not output-root ownership; their bytes remain unchanged during normalization.

The schema explicitly records the command as `create-if-absent` authoring. Its new empty-facet content says `Authored by`, its renderer still derives the filename from `windowEmptyFacetFileKindId`, and exclusive creation prevents a concurrent creator's content from being overwritten.

The separate `plugin-registry` generator still consumes the plugin tree. Its registered outputs remain its own generated catalog and `.vscode/launch.json`. The normalization plan preserves this input-consumer relationship and requests a normal frozen preview; it does not exempt the changed inputs or manufacture artifact output ownership.

## Exact Consumer

The one live lexical consumer is commentary inside region `✏️👁️Surfaces` of the energy Rust package glue. Its exact path, region, full line template, and plugin owner are declared in the facet contract. A governed energy facet move admits one preimage-bound Rust `ReferenceEdit`. Wrong regions, duplicate old markers, and changed prose fail closed. The actual consumer is not edited until a transaction applies it.

## Tests and Transaction Boundary

The permanent language-neutral authority fixture has 19 positive/negative owner vectors. The ticket-local integration test compares their outcomes with an independent Ajv JSON Schema oracle and tests isolated transactions with an Nx-owned registry preview fixture.

Initial red: the new pure authority export did not exist. After implementation, the structural owner/validation packet passed: 2 tests, 0 failures, 80 assertions.

The first transaction reached the intended source move plus independent generator-consumer operation, then exposed an existing rollback failure: after injected `after-moves`, generator handling tried to stat the original moved input. The fixture and journal are preserved under `🧪️empty-facet-O1wHME`. The coordinator fixed this in the shared transaction layer by resolving frozen generator inputs through exact journal-proven source/stage/destination or validated edit-backup paths; frozen preimage comparisons remain enforced. The same lifecycle and exact-consumer tests now pass without weakening assertions.

The first authoring test failed because the surface authoring operation was not exposed for direct isolated verification. Its scoped export and authoring-only creation changes are implemented and verified by actual dry-run/create/repeated-create execution.

The authority, schema, and real authoring-operation packet subsequently passed: 3 tests, 0 failures, 89 assertions. A malformed-contract case then exposed a nullable directory-capture validator access; it was fixed and the 7-malformation packet passed with 14 assertions. The corpus's new structural-owner test passed against all frozen source mappings. Its independent fast-glob enumeration hit Bun's default five-second timeout under simultaneous live planning; only the large corpus timeout was raised to 120 seconds, with no assertion disabled.

One isolated Nx check printed success and then exited with a SQLite foreign-key error, triggering safe rollback. No shared Nx cache path was established: `NX_*` overrides were unset and the fixture had its own `.nx`. The test fixture now explicitly disables its daemon and target cache; no production Nx settings were changed. A subsequent late negative fixture captured a shared schema edit midway through the test process, so fixture taxonomy bytes are now snapshotted once at module load. These are test-environment stabilization changes, not exemptions from generator checks.

Final executed evidence:

| Check | Result |
| --- | --- |
| `🧪️artifact-empty-facet-integration.test.ts` | 7 passed, 0 failed, 140 assertions, 49.26 seconds |
| `🧪️artifact-empty-leaf-authority/🟦️.ts` | 5 passed, 0 failed, 22 assertions, 12.69 seconds |
| Scoped `git diff --check` | Passed |

The seven integration cases cover 19 language-neutral structural vectors plus the independent Ajv oracle; strict schema and authored provenance; real create-if-absent authoring; malformed-contract rejection without throws; byte/mode-preserving rollback, retry, commit, and empty replan; folded occupancy, counterfeit owner, changed input, and symlink negatives; and exact Rust consumer rollback, commit, canonical replan, drifted prose, and wrong-region rejection. Both actual fixture Nx `generate` and `check` commands ran successfully in the final lifecycle tests.

No production physical move or reference apply occurred in this packet. Its integration and isolated transaction boundary are ready for the coordinator's guarded apply sequence. The original corpus report remains the immutable observation; this document records the integration delta. All owned schema/parser/helper/reference insertion sites are released for coordinated work.

## Clean Live Bounded Plan

A read-only production probe of `✏️s/🔌️plugins/🔋️energy/🗿️artifacts/🔋️model/🏅️standards/🔖️1/🪆️subsets/✳️any/👁️viewer/🎚️config/📌️empty.md` completed with 12 inventory entries, one exact primary-leaf move, one exact energy surface-region Rust prose edit, one registry regeneration, and zero unresolved findings. No production file was moved or edited.

| Evidence | Value |
| --- | --- |
| Plan Digest | `130c7dfafc379940b46d05fe9a057cc89276aa1ed33c7c87ff3141fadf2dd703` |
| Source Tree Digest | `20fc7e1f6307daae9f20352633ab376913ac1bd0c7054b7e9fab06f3de94b0be` |
| Generator Contract | `plugin-registry` |
| Generator Inputs / Preoutputs / Outputs | 107,695 / 10 / 10 |
| Preview Manifest Digest | `e3225404de2d5597a474d7fac654afc9dfa65ab2db91c19dd7f235a4059d4add` |
| Consumer Location | `artifact-empty-facet-energy-surfaces:521:1@28845` |

This is a probe, not a frozen production apply artifact. A fresh plan is required before applying because shared source files and generated inputs are changing concurrently.

## Changed Files

- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔣️taxonomy.json`
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️component.ts`
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🟦️.ts`
- `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🧫️fixtures/🧪️artifact-empty-facet-authority/🔣️.json`
- `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📇️registry/📜️script.ts`
- Ticket `🧪️artifact-empty-facet-integration.test.ts`
- Ticket `🧪️artifact-empty-leaf-authority/🟦️.ts`
- Ticket active/history important tests: exact owned-file contract registry assertions only.
- This report and retained isolated test fixtures.
