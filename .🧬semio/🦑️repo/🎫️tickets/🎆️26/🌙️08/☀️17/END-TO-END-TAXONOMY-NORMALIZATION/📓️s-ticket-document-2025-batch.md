# 2025 Ticket Document Candidate Batch

The read-only CLI inventory covered `.🧬semio/🦑️repo/🎫️tickets/🎆️25` using the immutable baseline `9f449b10659b95148c8bcb3f91ce583bf7446973` and one worker. It completed with 752 inventory nodes: 280 directories and 472 files.

The retained plan `🧾️ticket-document-2025/🔣️.json` contains 236 moves, all under `ticket-document-primary-markdown-v1`, and zero reference edits, regenerations, removals, or unresolved findings. Source-tree digest is `7f7916eef207a1095399e0b83ce034033834dd26145a718ca3ca2ed223fe548a`; plan digest is `9567e0a56a87812fe43196d1e74292b24a9a98ad79e23d551ff029e9fccc80fd`.

No production move was performed. This candidate was generated before the new generic cross-scope incoming-reference closure was released. It must be rebuilt and validated through that closure before application; it is not current apply authority. Historical ticket manifests and existing content/mode preimages remain unchanged. This supersets, but does not apply, the earlier 70-document November candidate.

## First Global Incoming-Reference Attempt

The first real global-closure retry completed the same 752-node scoped inventory, then stopped before producing a new plan. The new repository-coordinate detector encountered a deliberately malformed `.git` marker retained inside its own independent negative-test fixture (`🧪️incoming-reference-gQnEsE/.../SCOPED-INCOMING/escape/.git`). It treated that fixture payload as parent-repository ownership metadata and rejected the absent gitdir.

This failure was sent to the incoming-reference lane. An independently proven nested repository must remain a distinct coordinate owner; malformed nested test payload cannot confer ownership or be mistaken for parent repository metadata. Relative and absolute references escaping toward real parent targets must still be scanned. No blanket ticket or permanent inventory exclusion is authorized. No production leaf moved and no new apply authority was emitted by this attempt.

## Bounded Parser Performance Retry

After the ownership and immutable-plan boundaries passed their focused tests, the coordinator retried the same scoped plan with explicit ticket admission so its incoming evidence matches the future apply context. Inventory again completed at 752 nodes. The expanded incoming census contained 84,975 candidates, including ordinary retained ticket material. Progress reached 10,000 candidates, then stalled in structured JavaScript parsing, matching the parallel CAD capture's identified 851-KiB retained shell-host bundle.

The coordinator verified the complete command line of its sole process, PID 27651, and terminated only that obsolete read-only attempt with SIGTERM. It exited 143 without producing a new plan or moving production files. The incoming-reference lane is correcting the expensive parser pattern, not excluding the retained bundle. This incomplete attempt is not current apply authority or performance acceptance.

## Complete Global Candidate and Exact Preflight

The subsequent explicit-ticket CLI attempt completed all 88,007 repository-wide incoming candidates and exited zero. Its complete retained plan is `🧾️ticket-document-2025-global/🔣️.json`: 236 moves and zero reference edits, regenerations, removals, symlink changes, embedded-root operations, or unresolved findings. Its plan and source-tree digests exactly match the earlier candidate above. Thus global incoming closure introduced no additional operation for this batch; immutable historical plan coordinates were preserved.

A separate current-schema parser and physical preflight verified the canonical plan bytes, immutable baseline, exact 236 owner-document mappings, every source SHA-256/size/mode, 280 no-follow ancestor directories, and every absent destination. The source documents total 737,840 bytes. The ordered source/destination/preimage membership digest is `0271bb3f77cfa3685e0d97d7064923c745cec6bba7907c771ade4a7b4f100282`. The plan file is 172,483 bytes, SHA-256 `ab6d7c869cbc2bc9445cc6b9b549b099217950ae191595982e92b2d3b7f7b8a6`.

This preflight did not move a file. Application must still perform its own fresh schema-owned operation/digest rederivation and transaction checks. A later schema change is not permission to bypass that exact comparison, and the complete monorepo/performance gates remain open.

## Guarded Apply Started

After the complete incoming-reference packet passed eighteen tests and 136 assertions with its temporary diagnostic removed, the coordinator invoked the root CLI apply with this exact retained plan, baseline, digest, ticket, and an explicit cancellation path. The fresh pre-move rederivation is scanning 91,988 current incoming candidates. No result or production-move count is claimed until the command's final outcome and independent postchecks are recorded below.

## Independent Neighbor Preimages

While that guarded command remained in its pre-move scan, a bounded no-follow census verified all 236 source document hashes, sizes, and modes against the retained plan again. It also recorded the 236 neighboring ticket manifests, including content hashes, sizes, modes, and physical inode identities. The census contained exactly 472 regular files and no symlinks.

The neighboring preimages are retained at `🧾️ticket-document-2025-global/🔍️unchanged-manifests/🔣️.json`, 67,125 bytes, SHA-256 `75f476f658d33d2ba2158f97d091cf673fd8aa0af26a0c8ebd05de0c5a5d1bde`. They provide an independent post-apply check that manifest contents and identities were not altered. This census is read-only and does not replace the transaction's own authority checks.

## Approved Moves Installed; Verification Running

The command completed its fresh global authority rederivation with the unchanged 236-operation plan. Attempt `000001` then staged and installed the documents and reached journal state `verifying`, revision 715. Its source-parent prune list is empty. Final repository-wide incoming-reference verification is still running; the command has not yet reported a committed outcome.

An independent read-only check at that state verified all 236 destination hashes, sizes, and modes against the original plan, all 236 absent source paths, and the unchanged contents, sizes, modes, and inode identities of all 236 neighboring manifests. The journal independently contains 236 prepared, staged, and installed move IDs. These are provisional post-install observations, not a substitute for terminal verification and a durable committed state.

## Durable Commit and Independent Postchecks

The guarded root apply completed successfully with exit code zero: `state=committed moves=236 relocations=0 symlinks=0 removals=0 edits=0 regenerations=0`. Attempt `000001` is durably committed at journal revision 716. Its journal SHA-256 is `d0b1ff9c4889422f859d74e60c0dcfe02db35dfab265ac8f3e70daa23b9d846c`. The staging directory is absent and the source-parent prune list remains empty.

A separate post-commit no-follow check validated 280 ancestors and all 236 destination content hashes, sizes, and modes against the retained approved plan, totaling 737,840 bytes. All 236 original source leaves are absent. All 236 neighboring manifests still match their independent preimages, including physical inode identities. The prepared, staged, and installed move-ID sets each exactly equal the approved plan's complete 236-ID set. The original plan and neighboring-preimage records retain their recorded hashes.

## Fresh Empty Plan

The root CLI then ran a new one-worker plan for the entire 2025 ticket scope, with the same immutable baseline and explicit active ticket. Session `61552` exited zero. It inventoried the same 752 nodes and emitted **zero moves, embedded roots, relocations, symlink edits, removals, reference edits, regenerations, and unresolved findings**. The independently retained result is `🧾️ticket-document-2025-global/✅️post-apply/🔣️.json`, plan digest `d86367d5757ba65eb2d1c345776577ee95a74d4dbd9bd4a2a01fc1e9b7af9b34`.

This completes the 236-document owner batch, not the monorepo goal. No Compose path was inspected or restored, no Git mutation was performed, and no other ticket evidence was retired. The broader package, artifact, historical-evidence, full-repository, and performance gates remain open.
