# Energy Support Leaf Authority

## Semantic Decisions

The three unresolved Energy leaves have distinct, inspected ownership. The simulation event document is a six-event language-neutral fixture with hostile-case assertions. The demo text begins with `semio energy.model.dsl v1`; its compound extension is a physical format, not a semantic stem. The mutation payload is a draft-07 JSON Schema referenced by the same owner's version-one mutation descriptor.

The taxonomy now registers the `events` semantic directory and the `semio-dsl` physical kind with the complete `.dsl.semio` extension. The existing asset-subject rule supplies the example's semantic directory. The payload projection uses the existing canonical `mutationPayloadSchemaLocation`, but only for an exact mutation-owner path, one admitted regular descriptor, matching descriptor version/owner/semantic identity, the exact payload pointer, and the declared JSON Schema dialect. Duplicate or escaped authority fields cannot confer ownership. Ordinary missing-descriptor leaves retain their independent unresolved classification; no legacy runtime lookup was added.

The language-neutral vector is [artifact support authority](../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🧫️fixtures/🧪️artifact-support-leaf-authority/🔣️.json). It records exact source hashes and sizes, semantic destinations, schema fields, and eleven hostile input cases. The three source leaves remain physically unchanged.

## Test-First Evidence

The first harness run lacked an isolated Git marker and was rejected for a parent-repository-relative path; no source mutation occurred. After confining the fixture with its own Git repository, the intended red run reported **0 passed, 2 failed, 7 assertions in 0.792 seconds**: all three source leaves were unresolved and the payload contract was absent.

The first implementation passed **2 tests and 29 assertions in 1.277 seconds**. Independent `fast-glob` reproduced the admitted files, and Ajv independently validated the three mapping records and the payload schema's positive/negative values.

Three added descriptor forgery cases then failed as intended: **0 passed, 3 failed, 15 assertions in 1.352 seconds**. They demonstrated that a payload pointer alone did not prove owner, semantic identity, or version. After those exact bindings were added, the packet passed **13 tests and 98 assertions in 7.27 seconds**, including missing/duplicate descriptors, wrong pointer/owner/identity/version, malformed JSON, wrong/missing/duplicate schema dialects, and non-mutation ownership.

## Bounded Live Inventory

A fresh read-only one-worker inventory of the complete Energy model owner contains 332 nodes and 195 files, with **195 changing leaves and zero violations**. Its physical source-tree digest remains `0789753378492eab7d16bc8246232558be1b60e0ba1ff25ac4f49a0868d3df51`; inventory digest is `a6ac98e1b4014b158afa2b182e129d7ec5365aeca4e11f431c80a63c16bbdf22`. This is an inventory, not a production transaction plan. Incoming references, generator previews, and guarded application remain separate gates.

## Lifecycle Verification In Progress

The new isolated lifecycle proof uses the three exact production support preimages, the actual descriptor, a real external Bun runtime reader, and actual fixture Nx generation/check commands. Its first assertion exposed a test expectation mistake: reference edits are stored at the descriptor's canonical destination, not its original source path. A separate plan readback verified the correct four edits; the assertion was corrected without changing production code or plan authority.

The complete support packet and existing component integration packet are running from absolute test paths. Their final outcomes will be appended. All fixture trees are retained in this ticket. No actual Compose path was inspected, no production Energy file moved, and no real repository Git mutation was performed.

## Recorded Completion and Continuity Check

The preceding execution recorded the support packet at **14 passed, 0 failed, 137 assertions in 47.27 seconds**, including four moves/four edits, rollback, retry, commit, actual Bun readers, fixture Nx generation/check, and empty replanning. The component packet recorded eight passes and two failures: one newly detected Rust assertion-message reference, and an ordinary canonical JSON descriptor without a `payloadSchema` field. The latter was corrected by retaining ordinary classification when no payload authority is declared; its classification subset then recorded **2 passed, 0 failed, 48 assertions in 8.68 seconds**.

The first complete 195-file isolated owner plan remained red with eleven unsupported references: five leading TypeScript JSDoc paths, the Rust assertion diagnostic, and five local Rust array/join paths. Its recorded result was **0 passed, 1 failed, 9 assertions in 34.12 seconds**. No finding was filtered and no production Energy plan was applied.

After continuation, an exact filesystem check found the former ticket test input absent and its last process ID unknown. Other lanes independently found several of their prior ticket inputs absent too. None reported deleting them. These observations do not identify the cause. Historical run results above are not presented as a fresh rerun of missing inputs.

## Current Permanent Test Input

The replacement test is now a canonical permanent leaf at `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧪️tests/🧪️artifact-support-leaf-authority/🟦️.test.ts`. It uses the surviving language-neutral vector and a single coherent schema snapshot. Exact support and descriptor source bytes are handcrafted as UTF-8 input fields in that vector, preserving the three prior hash/size assertions. Fixture construction no longer depends on live source filenames remaining present after normalization.

The new leading-documentation authority admits only unescaped single-backtick paths in the leading complete JSDoc block. It rejects ordinary comments, comments after code, quoted comments, double/escaped backticks, incomplete comments, non-path code, and backslash escapes. Positive spans match independent TypeScript comment parsing and CommonMark inline-code parsing. The fresh focused run passed **1 test, 22 assertions, 0 failures in 213 milliseconds**. The reconstructed complete classification packet passed **14 tests, 77 assertions, 0 failures in 1.71 seconds**. The exact input-snapshot hash check passed again after embedding the source bytes.

The first replacement test invocation had an incorrect repository-parent count; it failed before test execution and was corrected. The reconstructed lifecycle fixture subsequently exposed its own unsorted generator keys, missing owner project manifest, and noncanonical preview key order. Existing production validators rejected each. A further assertion incorrectly expected an injected failure to throw, whereas the transaction API returns a `rolled-back` result. It now checks the returned state and exact journal error. None of these harness corrections weakened production guards.

## Revised Retention Rule

The latest supplied AGENTS instructions supersede the earlier blanket retention wording: keep authored input files and Markdown audits/reports, and remove disposable generated outputs after the relevant work finishes. Active transaction/recovery artifacts remain necessary until safely terminal. Outputs in another open ticket are not deleted by this lane. No broad cleanup, production Energy move, actual Compose access, or modifying shared-repository Git command occurred during this continuity work.

## Current Lifecycle and Registered Gate

The replacement lifecycle now proves four moves and five edits, including the external consumer's leading JSDoc path and the descriptor's payload pointer. It executes the real Bun reader before changes, after an injected `after-edits` rollback, and after same-ticket retry/commit. Each observation reports six events, the original DSL header, and `ReplaceModel`. It verifies source restoration, destination bytes/modes, exact zero-operation replanning, and actual fixture Nx generate/check-generated console output.

One run completed both Nx commands but Nx then reported a SQLite foreign-key constraint error; the transaction correctly rolled back. The fixture now sets `NX_DAEMON=false`, matching the existing transaction suite. The subsequent focused lifecycle passed **1 test, 32 assertions, 0 failures in 4.18 seconds**. This does not establish the cause of the earlier Nx database error or relax its failure handling.

The new language-neutral execution contract first failed because the Nx target was absent (**0 passed, 1 failed, 1 assertion in 290 milliseconds**). The library's existing `📜️script.ts` router now invokes the permanent test using an absolute test path. Its `📋️project.json` target and both launch catalogs register `test-artifact-support` in gate group `4_gate`, order `410.06`. Actual uncached `bun nx run @semio-tech/repo-lib:test-artifact-support --skip-nx-cache` passed **16 tests, 116 assertions, 0 failures in 4.44 seconds** and Nx exited successfully. The launch output was updated with the same narrow seed entry; full registry regeneration remains a separate monorepo gate.

A fresh exact comparison confirmed all four handcrafted input byte strings equal their current live source bytes. The descriptor is 721 bytes, SHA-256 `f7f2ad3108142c662eba58616d2ba4395f7722c3f7bd067596367ad5a2876566`; the other three hashes remain those above. A new bounded live inventory again reports **332 nodes, 195 files, 195 changing leaves, zero violations**, with the unchanged physical source-tree digest. The full owner's reference closure, real registry preview, reviewed production plan, and application remain uncompleted.

## Complete Owner Reference Closure and Runtime Gate

The immutable-local Rust path analysis now uses the actual Cargo manifest and inline module mounting context. Its first complete-owner run rejected five unsupported local-array path references (**0 passed, 1 failed, 13 assertions, 1.488 seconds**). After the contextual adapter landed, the exact 195-file owner plan passed with zero unresolved findings (**1 passed, 0 failed, 16 assertions, 2.04 seconds**). This is isolated planning, not proof of the owner's runtime assertions or a production apply.

The no-follow physical owner walk contains 329 nodes, while inventory contains 332: inventory includes four ancestor contexts and omits one empty untracked directory. The language-neutral readiness record now distinguishes these counts explicitly. No directory was deleted to reconcile them.

Extending the owner test through rollback, retry, commit, and a real `serde_json` Rust assertion oracle exposed fixture Nx task-history failures before reaching the Rust oracle. Two runs failed at the committed-state check with **409 assertions**, taking **18.70 seconds** and **24.15 seconds**. The transaction rolled back in both cases. A read-only immutable SQLite inspection showed successful generation history but no check history; it did not establish the database failure's complete cause.

## Current-Process Generator Environment

A direct Bun `execFileSync` probe demonstrated that omitting `env` lost environment changes made in the parent process: two assigned test variables were absent from the child. Thus the previous fixture assignment of `NX_DAEMON=false` had not reliably reached apply-time subprocesses. An attempted history-store disable suffered the same issue and was removed; **no `NX_DISABLE_DB` bypass remains**.

The language-neutral execution record now declares a dedicated sentinel. Both actual fixture generate and check commands require it. The test-first run failed inside the generator with `missing current generator environment` (**0 passed, 1 failed, 20 assertions, 10.18 seconds**). The normalizer now explicitly copies the current environment for generation and both check branches, matching the already-explicit preview environment. The same rollback/retry/runtime lifecycle then passed **1 test, 32 assertions, 0 failures in 8.48 seconds**; both real Nx commands printed their expected `[DEBUG]` output and Nx reported success. This proves current-process environment propagation without changing transaction failure handling, caching policy, or the database configuration.

The complete-owner Rust oracle is being rerun. A zero-unresolved plan still leaves the authored descriptor filename assertion as a plain string expectation; the runtime oracle is required to decide and verify its semantic correction before production normalization.

## Runtime-First Assertion Correction

With current environment propagation, the complete owner successfully rolled back, retried, committed, and replanned empty. Actual fixture Nx generation and checking both succeeded. The independent Cargo/`serde_json` oracle then executed the exact assertion line extracted from the committed Rust aggregate and failed as intended: the descriptor contained `🧬️schema/🔣️.json`, while the assertion expected `🔣️payload.schema.json`. This run reported **0 passed, 1 failed, 412 assertions in 37.71 seconds**, with Cargo's real exit code 101 and assertion diagnostic.

The authored Rust assertion now compares the descriptor's resolved owner-relative target with the expected owner-relative payload target. It retains the exact correspondence check while expressing both filesystem coordinates through the proven Rust path adapter. No arbitrary assertion strings were admitted as references. The aggregate changed from **3,314 bytes**, SHA-256 `a8e7ed83e937d3c045910727917a1494e03caa3188384f10939c905856f7b71b`, to **3,380 bytes**, SHA-256 `be6754b13443cd43592ac24f6b7d1da81f530626748ead99cdf8c8dcb5b2d14c`. Only that line was edited.

Fresh bounded inventory still has **332 nodes, 195 files, zero violations**. Total source bytes are now **383,479**, and the source-tree digest is `4a4ed81731e59bb7773a9e242caab31f8c16eddc3517e6de8ee43228d85d6a42`. The readiness vector was updated to these observed post-edit values; the preceding values remain documented above. The complete runtime rerun remains required.

The corrected complete-owner lifecycle passed **1 test, 413 assertions, 0 failures in 57.45 seconds**. It verified all 195 source restorations after injected rollback, same-plan retry and commit, empty replanning, actual Nx generator/check output, and the exact Rust assertion through Cargo/`serde_json`. No production Energy leaf was moved.

This added full-owner lifecycle includes a fresh Cargo oracle build and belongs to the repository's existing `long` level. The execution contract first rejected the old target command (**0 passed, 1 failed, 1 assertion, 2.54 seconds**). The existing route now consumes the explicit level through `resolveTestLevel`, and its Nx command is `bun ./📜️script.ts test artifact-support long`. The registration check then passed **1 test, 7 assertions, 0 failures in 2.11 seconds**. The fundamental fifteen-second budget and transaction aggregate fourteen-second watchdog are unchanged. Actual complete uncached Nx verification follows.

The first new Nx launch exited before test execution: concurrent WGPU contract integration declared five canonical tracked generated outputs not yet present in the source package state. The existing workspace validator rejected them at root startup. The package lane was notified to restore exact pre-normalization/generator authority; no placeholder outputs were created and no validator was bypassed. The preceding direct complete-owner result remains valid for its loaded snapshot, but the updated registered full packet is not yet green.

After the source-phase WGPU contract was restored, actual uncached `bun nx run @semio-tech/repo-lib:test-artifact-support --skip-nx-cache` completed successfully: **17 tests, 529 assertions, zero failures in 42.05 seconds**, and Nx exited zero. The small runtime lifecycle took 4.81 seconds; the complete 195-file owner lifecycle took 34.86 seconds. Both fixture generators and check commands reported success. This closes the current registered isolated Energy packet, not the real monorepo incoming-reference, registry-preview, production-application, or final repository gates.

## Completed Oracle Output Retention

The permanent Cargo oracle now validates its exact ticket-owned root and both disposable outputs before deleting anything. It rejects unowned roots and symlink outputs; it keeps the authored `Cargo.toml` and `🦀️.rs` inputs. The language-neutral retention record and independent filesystem oracle cover positive and hostile cases. The focused test went from **0 passed, 1 failed, 1 assertion** to **1 passed, 0 failed, 11 assertions**. Historical completed compiler outputs were separately retired with exact no-follow checks; see [Energy oracle output retirement](📓️energy-oracle-output-retirement/📝️.md).

Actual uncached Nx rerun of the complete current packet (session `59479`) passed **18 tests, 540 assertions, zero failures in 95.98 seconds**, exit zero. The small lifecycle took 14.60 seconds; the complete 195-file lifecycle took 70.48 seconds. Both real fixture Nx generator/check commands succeeded, and the Cargo oracle completed. These are observed loaded-host durations, not a performance acceptance result. The real monorepo Energy reference capture remains in progress, and no production Energy move has been applied.

## Real Reference Capture Exit

The real scoped capture in session `13994` subsequently exited **1**. Its requested plan path `🧾️energy-current-reference-plan/🔣️.json` is absent, so it produced no reviewable plan and authorizes no application. The final progress output was truncated around the specific inner diagnostic; the outer Nx failure is retained in the conversation but does not identify its cause. A read-only check of Nx terminal caches did not recover this capture. Do not infer a particular failure or report full reference closure from this attempt. A future retry will retain its bounded diagnostic output in the ticket until the exact result is summarized.
