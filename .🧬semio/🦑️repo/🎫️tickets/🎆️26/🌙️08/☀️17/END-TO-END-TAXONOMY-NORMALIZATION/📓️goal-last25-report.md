# goal-last25 — A: 6 fixed/7 correct refusals, B: fixed, C: fixed (33 plugins), D: not fixed

**(A)** `inspectRustNonRepoJoinBaseSpans` (🔍️discovery/🟦️component.ts) had a real defect: `let x = f(x)` deleted `x`'s binding BEFORE evaluating the RHS, so shadowing re-binds (`let out_dir = PathBuf::from(out_dir)`) could never see the old proof — fixed order in both `visit` and `bodyReturnsNonRepo`. Added a `test_support::tempdir()` suffix rule (any `::`-prefix; the one real `pub fn tempdir()`, 🏪️store/🦀️component.rs, is itself `env::temp_dir().join(...)`). Cleared (proven non-repo, verified `📜️goal-last25-nonrepo-shadow-verify.ts`): m0/m3-describe-fixture `:53,:54`/`:49,:50` (CLI-arg→shadow chain), `run/📦️bin.rs:140` (fn-param repo_root→shadow), `mcp/registry/component.rs:162` (test_support::tempdir chain). Correct refusals, unchanged (genuinely unknown — cross-module for-loop var `mcp:52`; dynamic `nx.json` ancestor-walk loop `run/component.rs:2787`; qualified cross-module tuple-destructure, exact precedent from `📓️goal-nonrepo-report.md` — `derive:344,351,1900`; dynamic `DirEntry::path()` accessor, exact precedent — `vector-converter:146,155`).

**(B)** `directory-kind-ambiguous` — added `"members-of-plugins"` to `test-case`'s `parentKindIds` (🔣️taxonomy.json), same pattern as the existing `"members-of-elements"` entry. Root cause: every plugin-root folder (🗒️note included) classifies as the generic `members-of-plugins` overlay, which was in NEITHER `test-case` nor `test-fixture-member`'s parent list — repo-wide latent bug (confirmed identical on 🌊️flow, 🧩️puzzle, 🧱️block, 💠️lowpoly, ➗️mathematical), not note-specific. Verified via `📜️goal-last25-directory-kind-verify.ts` against real taxonomy.json.

**(C)** `package-role-unresolved` on note's `glue.rs` — real defect in `classifyGlue`, affects all 33 plugin `glue.rs` files, not just note. Two fixes to `RUST_DECLARATIVE_STATEMENT_SEQUENCE`: (1) `+`→`*` quantifier — a `mod` block whose body reduced to nothing after its own declarative children were stripped (e.g. `pub mod any { mod component; pub use component::*; }`) was rejected as "not declarative" for being *empty*, poisoning every ancestor wrapper up to the file root; (2) recognized the framework's own `plugin_exports!` macro call (any `::`-qualifying prefix) as declarative wiring — confirmed by literal text in all 33 files, never locally defined. Verified `unresolved`→`declaration` on all 33 (`📜️goal-last25-glue-classify-verify.ts`); confirmed no file regresses out of `implementation`/`thin-delegation` (both gates run strictly before the declarative check, so the fix cannot mask real code).

**(D)** `generator-preview-invalid` — root cause confirmed: `invokeGeneratorPreview`'s hardcoded `timeout: 60_000` (🧹️normalization/🟦️.ts:6959) is shared by every generator contract. `plugin-registry`'s own preview (`🔌️plugin/📇️registry/📜️script.ts`) walks the same repo-wide catalog `📓️goal-final-status.md` §5/§8 already names as the 40 232-input scan needing scope-narrowing. **Not fixed**: a blanket/global bump is a band-aid masking hangs on every other (smaller) contract, and a per-contract override needs schema+type+validation plumbing beyond this row — the real fix is the scope-narrowing §5/§8 already defers as separate, larger work.

Tests: `🧪️rust-finite-target-consumption` 86/86 pass (twice, after each row-A edit). `🧪️rust-physical-reference-context` 30/32, both failures are `rustc`-subprocess 5s-timeout flakes (different tests fail each run) — pre-existing. `🧪️historical-package-owner-identity` 24/26, 2 failures are an unrelated frozen-coordinate error-message drift (concurrent-session edit elsewhere in the shared file, nothing near my changes). Files: 🔍️discovery/🟦️component.ts (shadow-order fix ×2, test_support::tempdir rule), 🧹️normalization/🟦️.ts (RUST_DECLARATIVE_STATEMENT_SEQUENCE ×2 fixes), 🔣️taxonomy.json (`members-of-plugins` added to `test-case`). No apply/plan run (forbidden). Scripts left at ticket root: `📜️goal-last25-nonrepo-shadow-verify.ts`, `📜️goal-last25-directory-kind-verify.ts`, `📜️goal-last25-glue-classify-verify.ts`.
