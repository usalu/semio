# Transaction V2 Current Qualification Readiness

## Latest Result — Owned Run Implemented, Qualification Still 0/3

The coordinator authorized the output-ownership-only implementation after the initial inspection below. It is complete: every new bundle, fixture, template, marker, registry, and parity probe now lives under a fresh no-follow `🧾️runs/🔖️<uuid>` owner in this report directory. The exact PID-plus-UUID identity is propagated to all shards. Invalid IDs, relative roots, occupied runs, root/ancestor symlinks, non-directory roots, and cross-run fixture ownership reject. No generated output or recovery evidence was deleted.

The single authorized unfiltered actual uncached aggregate failed the unchanged fourteen-second watchdog. Its seven captured source/input identities were byte-identical before and after. All four shards were stopped at 14.040–14.047 seconds; the router duration was 14.126 seconds and the complete command wall time was **17.41 seconds**. The retained registry contains **48/98 unique expected boundaries**, no extras and no duplicates. Independent readback found all **33 registered child PIDs absent**. Qualification remains **0/3**; no retry or budget/partition change was made.

Retained measured run: [outcome](🧾️runs/🔖️7b6c5ad5-eb15-4103-9f94-4e47636795da/📊️outcome/🔣️.json), [before identities](🧾️runs/🔖️7b6c5ad5-eb15-4103-9f94-4e47636795da/📷️before/🔣️.json), [after identities](🧾️runs/🔖️7b6c5ad5-eb15-4103-9f94-4e47636795da/📷️after/🔣️.json). The eight complete child stdout/stderr transcripts are under that run's `📓️shards/🔢️1` through `🔢️4` owners. Every active/failed fixture recovery tree remains present.

## Initial Inspection Decision

The strict qualification remains **0/3**. This inspection did not run the transaction gate: the current router necessarily creates a loose direct ticket child named `🧪️transaction-v2-bundle-*`, and the permanent suite separately creates loose direct ticket fixture/template/marker children. There is no existing retained-run-owner override. The coordinator explicitly required a semantic retained-run owner for new outputs and instructed this lane not to modify or execute a conflicting router before coordination.

Only this Markdown report was created. No test, router, schema, catalog, normalizer, compiler code, old fixture, or output was edited or removed. No old bundle was executed or reconstructed. No actual Compose path was accessed. The released 26-case collision gate remains unchanged.

## Fresh Non-Executing Coverage Check

At `2026-08-27T14:04:46.539Z`, an inline read-only Bun diagnostic executed through the existing Nx `exec` runner. TypeScript parsed the actual current router and permanent suite; no test body, allocation function, normalizer, or old compiled artifact was executed. It extracted the four literal filters, fourteen static titles, and closed phase arrays, then independently expanded all generated titles. Every bare and describe-qualified title matched exactly one filter. All 98 boundary identities were unique and exactly equal to the current golden's key set. This is a successful static source/JSON check, not a Bun test or timing qualification.

| Shard | Selected tests | Golden boundaries | Unchanged concurrency |
| --- | ---: | ---: | ---: |
| 1 | 6 | 6 | 5 |
| 2 | 18 | 29 | 6 |
| 3 | 19 | 34 | 6 |
| 4 | 19 | 29 | 6 |
| Total | 62 | 98 | One wave |

The golden still contains 63 transaction ledgers and nine workspace ledgers. Its bytes match the later harness report, not the obsolete earlier frozen golden in the original performance-preflight report.

The target remains `@semio-tech/repo-lib:test-transaction-v2`, executing `bun ./📜️script.ts test transaction-v2`. Both launch catalogs retain the uncached target at order `410.05`. The current four-way partition already contains the previously approved one-case transfer to shard 1; another rebalance is neither requested nor justified by the retained evidence.

## Exact Budget and Qualification Boundary

The router's 14,000 ms watchdog starts after both bundle builds and the schema copy, immediately before its single wave of shard spawns. It stops every owned shard process group and registered nested child on failure or deadline, awaits shard exits, checks for surviving registered children, and then requires exact sorted 98-boundary equality for the unfiltered target. Filtering arguments bypass the full-matrix comparison and cannot qualify.

The retained independent acceptance disposition requires three complete actual uncached runs with identical normalization source, permanent test, golden, and router bytes, each with total wall time below 15 seconds. The internal 14-second window is not the whole Nx command: compilation, startup, final checks, and process shutdown also consume wall time. A successful single exploratory run would therefore not by itself complete qualification. Current schema and transitive dependency identities should also be observed before/after trials; the historical four-file rule must not be used to conceal relevant concurrent changes.

## Retained Timing Evidence

The following are historical report observations, not fresh execution or claims that their old fixture directories still exist. No old fixture root was needed for this assessment.

| Recorded state | Observation | Qualification |
| --- | --- | --- |
| Before one-case transfer | Shards 1/3/4 completed in 9.31/9.39/12.53 s; shard 2 hit 14.01 s; 96/98 boundaries | Failed exploratory aggregate |
| Transferred case alone | One pass, 22 assertions, shard 1.07 s | Filtered correctness only |
| After one-case transfer | All four shards stopped at 14.03–14.08 s; 61/98 boundaries; transferred pair present | Failed exploratory aggregate |
| Original independent qualification lane | No trial started due source-identity drift and saturated host | 0/3 |

Sources: [harness retention and rebalance evidence](../📓️s-transaction-harness-retention.md), [exact partition review](../📓️s-transaction-shard-rebalance-proposal.md), [performance preflight](../📓️s-transaction-v2-performance-gate.md), and the final signoff section of [independent transaction review](../📓️h-transaction-v2-review.md). Earlier sections in those reports document superseded implementations and are not current defects merely because their history remains preserved.

The earlier shard-2 observation identified the missing killed/recovered canonical-attempt-publication pair, but not whether that case was slow, queued, or waiting for a shared fixture. The later all-shard timeout does not isolate any single slow phase. There is **no causally established current timing bottleneck** beyond failure to complete the coordinated workload within its unchanged deadline. It would be incorrect to infer a new timing defect or choose another transfer solely from test counts.

The fresh host sample had ten logical CPUs, load averages `15.6284 / 28.0083 / 31.3262`, 3,173,744,640 free bytes, and 34,359,738,368 total bytes. This is one read-only observation, not a quiet-host guarantee or proof of the historical timeout cause. No process was interrupted or reconfigured.

## Exact Output-Ownership Blocker

| Current source site | Allocation/authority |
| --- | --- |
| Router `transactionV2BundleRoot` | `mkdtempSync` directly under the ticket with `🧪️transaction-v2-bundle-`; compiled aggregate, child normalizer, schema snapshot, PID/boundary registries live there |
| Suite `fixture` | Shared templates/preparations and per-case clones directly under the ticket; run ID is embedded in their names |
| Suite `cloneFixture` | Another direct-ticket clone allocator |
| Suite `activeReferenceFixture` | Direct-ticket shared active-reference root and preparation marker |
| Suite `killedMixedGenerator` | Direct-ticket mixed-generator marker directory |
| Golden/parity test | Four real direct-ticket bundle allocation probes, plus one direct-ticket parity fixture |
| Suite `retainFixture` | Requires `dirname(root)` to equal the ticket itself; a nested retained-run owner currently rejects |
| Harness neutral vector | Explicitly pins the direct-ticket parent/prefix, including rejection of nested ownership |

`SEMIO_TRANSACTION_V2_RUN_ID` supplies identity, not an output-root boundary. The existing schema/module/PID/boundary environment values do not relocate fixture/template roots. Running with a different working directory or redirecting stdout would not solve this. Changing only the bundle helper would also leave the other allocations outside the retained-run owner.

## Next Bounded Fix and Measurement

Coordinate one output-ownership-only harness change, test-first, before any fresh aggregate:

1. Allocate one fresh no-follow run identity under a semantic ticket owner such as `📓️transaction-v2-current-readiness/🧾️runs/🔖️<uuid>`, with distinct nested bundle, fixture, template, marker, and registry ownership. This is a proposed path, not an allocated run or an approved schema change.
2. Pass that exact run root to all four shards and nested fixture helpers; validate its canonical ticket ancestry and UUID identity without symlink traversal. Retain the current run-ID entropy and sibling-run isolation. Update the neutral retention vector and in-memory allocation parity test coherently; preserve all 62 titles, 98 boundaries, filters, concurrency, timeouts, assertions, and transaction golden bytes.
3. Keep authored fixture inputs and Markdown reports; do not add broad deletion of the run owner. Disposable outputs may be considered only after terminal process/transaction ownership is proved under the coordinator's separate retention authority. This lane performs no cleanup.
4. Once the output boundary is approved and coherent, run exactly one unchanged, unfiltered, actual uncached target, recording full command wall time and existing per-shard/per-test output under that run owner. Pin before/after inputs. If it fails, first classify the last completed cases and missing boundary pairs from that run rather than changing budgets or partitioning.

If further performance work is then needed, the source-backed narrow measurement points are shared-template preparation/wait, synchronous `cpSync` plus recursive symlink rebasing per clone, fixture planning, child startup/marker wait, and recovery. The current suite performs these inside the watchdog; no retained measurement apportions their costs. Profiling those exact phases is a diagnostic candidate, not evidence that any one can be safely skipped or optimized now.

## Current Input Identities

All sizes and hashes below were read in the same non-executing diagnostic. They are observations, not a new immutable acceptance freeze. Source continues to be shared with other lanes.

| Input | Bytes | SHA-256 |
| --- | ---: | --- |
| TypeScript router | 18,608 | `81d1c7a7cf3ae710d9b2b426eb7bb94392d943a23357d9d007d1e57aaf89fda5` |
| Permanent transaction test | 85,660 | `fcd2efda6519fa265c3e91e69f24c2d38a342f6d1c183cf5f4ebf637debe282f` |
| Transaction golden | 403,163 | `7b700d79e5474417f0c92ddce61f5ffdd24603af56241d0fbdc3cdd5ba560296` |
| Harness vector | 1,836 | `a1f46fc48423841bae670330d1f968d530454ae9725d12e802449612ad07a665` |
| Taxonomy schema | 308,176 | `0cc60034727ec4c9d18c1e0a565d72f18594263f7ff2e080a885c13b5d4a42c5` |
| Normalizer | 811,391 | `ead932b049d1c18bb9d484c7c8bc5cb2d893b4adb8c1f8abb1f84eb747a68cfd` |

No transaction-gate process was started. The inline diagnostic exited zero and left no owned process or generated output file. Await coordinator direction on the exact output-ownership change; the present inspection is complete.

## Authorized Ownership Implementation

The existing permanent router allocates the semantic owner exclusively and checks every existing ancestor with `lstatSync` before descending. It preserves the existing PID-plus-UUID run identity, while the root name uses its UUID suffix. It passes `SEMIO_TRANSACTION_V2_RUN_ROOT` to each shard. Suite fixture helpers validate that root against the exact repository, ticket, report, run identity, and no-follow directory chain before allocation. Direct fixture/template names no longer duplicate the run ID, since the enclosing owner already supplies it. `retainFixture` now requires the exact current run parent and a real non-symlink directory.

The existing golden/parity case was extended rather than adding or dropping a transaction title. It compiles and executes the actual allocator, root validator, and retention helper with both Bun and TypeScript. All positive and hostile allocation probes are confined within the active run's isolated allocation fixture; hostile roots are not installed in the real run collection. Payloads, symlinks, directories, and failed probes are retained. The former four empty allocation-directory deletions were removed.

The neutral harness vector now declares the semantic owner, root environment, UUID/identity rule, and exact negative cases. The four filters, 62 unique titles, 98 boundary keys, golden contents, concurrency caps `5/6/6/6`, failure-stage assertions, child deadlines, and 14,000 ms watchdog remain unchanged. The permanent transaction golden is still 403,163 bytes with SHA-256 `7b700d79e5474417f0c92ddce61f5ffdd24603af56241d0fbdc3cdd5ba560296`.

The router retains per-shard stdout/stderr, before/after input hashes, and a terminal outcome including missing/extra/duplicate boundary keys. Stream write errors use the existing coordinated stop path so transcript capture does not bypass owned-child termination. The measured input set includes router, permanent test, normalizer, discovery, schema, golden, and neutral harness; no runtime external dependency was introduced.

### Test-First and Focused Evidence

The initial absolute-path Bun test, selected through Nx `exec --projects=workspace`, failed before any old loose allocation: **0 pass, 1 fail, 1 assertion, 201 ms**, because the run-root validator did not exist.

Actual uncached ownership/parity gate after implementation: **1 pass, 0 fail, 1,030 assertions, 299 ms**. Final unchanged-source rerun with retained transcript capture: **1 pass, 0 fail, 1,030 assertions, 254 ms**, shard 0.26 s. A separate actual incomplete-plan filter passed **1/0/6 in 1.59 s**, proving a real template and fixture clone use the new owner.

Focused run owners retained:

- `🔖️4f9ef60f-79f9-4604-8532-fd2cead0e1de`: first ownership/parity success.
- `🔖️118f0279-6c89-424b-939d-addaed05ac7a`: incomplete-plan success; terminal outcome reports unchanged inputs.
- `🔖️3af124f1-eed7-4ad0-883f-7124e161bedd`: final ownership/parity success with capture.

An intervening invocation used `--args='keeps … enumeration|rejects incomplete plans …'`. Nx `run-commands` expanded the `|` as a shell pipeline, attempted to launch a command named `rejects`, and produced an EPIPE. Only the golden case ran and passed; this command was **not** a valid combined test result. Its run `🔖️18f987d7-16e8-4635-93b7-52eff0b6d850` is retained. Subsequent filters used no shell metacharacter, and the measured full gate used no `--args`. This invocation deviation is not relabeled as a transaction semantic failure.

### Single Measured Unfiltered Gate

```sh
/usr/bin/time -p env NX_DAEMON=false NX_NO_CLOUD=true bun nx run @semio-tech/repo-lib:test-transaction-v2 --skip-nx-cache
```

Run identity `91160-7b6c5ad5-eb15-4103-9f94-4e47636795da`; router start `2026-08-27T14:32:44.393Z`, terminal `2026-08-27T14:32:58.519Z`. Exit 1; `/usr/bin/time` recorded `real 17.41`, `user 21.49`, `sys 10.98`. The parent paused other taxonomy-owned tests, compilations, and captures for this wave; no claim is made that unrelated host workloads were quiet.

| Shard | Selected | Emitted completed-case lines before kill | Exit duration | Slowest completed case |
| --- | ---: | ---: | ---: | --- |
| 1 | 6 | 5 | 14.045 s | After-regenerations rollback, 5.419 s |
| 2 | 18 | 3 | 14.042 s | Embedded-root staging rollback, 5.790 s |
| 3 | 19 | 8 | 14.047 s | Backup-write mkdir recovery, 9.412 s |
| 4 | 19 | 12 | 14.040 s | Restore canonical exchange recovery, 7.860 s |

These 28 emitted completion lines are partial observations, not a passing aggregate. No individual failing-test line was emitted before the watchdog, and killed shards did not emit complete Bun assertion summaries. Therefore no complete pass/fail/assertion total is claimed.

The 50 missing boundary keys comprise 21 complete killed/recovered pairs, one recovery-only key (`transaction-backup-retained`), the final mixed-generator commit, and six rollback keys. Exact missing pairs:

```text
transaction-attempt-preparation-children
transaction-attempt-preparation-mkdir
transaction-edit-canonical-exchange
transaction-edit-exchange
transaction-edit-inner-exchange
transaction-edit-write-mid
transaction-edit-write-prepared
transaction-initial-journal-candidate-written
transaction-initial-journal-canonical
transaction-initial-journal-canonical-exchanged
transaction-initial-journal-write-mkdir
transaction-initial-lease-json-candidate-written
transaction-initial-lease-json-canonical-exchanged
transaction-initial-lease-json-write-mkdir
transaction-initial-lease-prepared
transaction-initial-wal-mkdir
transaction-lease-canonical-published
transaction-lease-json-candidate-written
transaction-lease-prepared
transaction-terminal-committed-stage-removed
transaction-terminal-rolled-back-stage-removed
```

The missing rollback keys are `after-edits`, `after-relocations`, `after-symlink-retargeting`, `before-verify`, `caught-attempt-canonical-published`, and `caught-journal-previous-exchanged`. Full prefixed keys are in the retained outcome. Both boundaries for the previously transferred canonical-attempt-publication case are present; it completed in 3.929 s.

### Interpretation and Next Bounded Diagnostic

Current evidence points to work spread across every shard, not one misplaced case. Shard 2 completed only its first three synchronous rollback callbacks before the deadline; shards 3 and 4 spent several seconds in initial active-reference recovery cases, and the mixed-generator case had killed/recovered evidence but had not committed its second ordinal. Per-case elapsed times include asynchronous overlap and cannot be summed into a causal CPU profile.

The next bounded investigation should instrument or profile one exact current fixture's preparation, plan, injected apply, and rollback durations under this retained owner, especially embedded-root rollback and shared active-reference preparation. It must use the existing full workload and preimages, then explain observed cost before any optimization. This run does not authorize another shard transfer, a raised timeout, dropped assertion, weakened golden, or additional full run. The seven input hashes were unchanged during the wave; the full identity documents remain available for a later controlled comparison.

Changed files in this implementation are only the permanent transaction suite, its neutral harness-retention vector, the TypeScript package router's run-owner/allocation/capture splices and required imports, and this report. No schema/catalog, transaction engine, or root compiler region was edited. No output was deleted. Source ownership is released; no test or capture process remains owned by this lane.
