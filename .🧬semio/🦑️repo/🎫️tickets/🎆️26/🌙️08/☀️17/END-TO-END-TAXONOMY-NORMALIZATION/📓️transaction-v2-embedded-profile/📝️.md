# Embedded-Root Rollback Cost Diagnostic

## Scope

The coordinator authorized one selected unchanged transaction workload: `rolls back after-embedded-root-staging with non-empty phase authority`. This is a diagnostic, not the 62-case/98-boundary aggregate or a qualification run. Production normalizer, discovery, router, permanent test, schema, catalog, and golden inputs are not edited. No old bundle is executed and no evidence is deleted.

The ticket-local [diagnostic command input](📜️script.ts) and [language-neutral instrumentation contract](🔣️.json) derive compiler inputs by adding only balanced function-entry/finally timing probes. Every original source byte is retained and mechanically restorable; the complete authored test calls—including assertions and their timeouts—must remain byte-identical. The embedded fixture callback is not transformed, preserving its `toString()` template identity and fixture preimages. Bun and TypeScript independently compile the neutral return/throw cases.

The selected test retains its 15,000 ms case timeout, concurrency setting of six, and the 14,000 ms process watchdog. The allocator is extracted from the actual package router without importing or dispatching the router, and creates the existing no-follow semantic `📓️transaction-v2-current-readiness/🧾️runs/🔖️<uuid>` owner. Derived compiler inputs, schema snapshot, bundle, CPU profile, timing data, transcripts, and before/after evidence stay inside that owner.

The CPU sampler uses Bun's installed external profiler at its default 1,000 µs interval. Timing probes only append in-memory rows and flush once at process exit; no timing probe writes inside a measured function. Nested timing totals overlap and must not be added naively. Root and Draw may execute concurrent work, so absolute timings are diagnostic only.

## Status

Inputs prepared; self-test and the one selected workload have not run yet. No active profiler process exists.
