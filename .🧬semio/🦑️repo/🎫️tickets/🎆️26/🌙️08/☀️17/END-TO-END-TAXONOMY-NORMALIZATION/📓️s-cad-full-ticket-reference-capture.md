# CAD Full-Ticket Incoming Reference Capture

## Completed observation

The full-ticket CAD source-scope plan completed in **694.032 seconds** with the exact active ticket directory included in inventory authority. The scan enumerated 88,009 incoming candidates and reached the real registry projected-preview producer. It did not perform a production apply or generation.

| Measure | Observed value |
| --- | ---: |
| Scoped inventory nodes | 312 |
| File moves | 209 |
| Reference edits | 337 |
| Blocking errors | 4 |
| Registry input nodes | 34,170 |
| Registry content files | 246 |
| Registry output nodes | 10 |

Inventory digest: `b4ad2c8d0dd78ae80a39c540fd146320dad7942c1b5521d7930bc38ba969ff5b`.

Plan digest: `b98c27acd3d2828f82c67594497f317ea914a10d411e8f78e7cd09355c7ab758`.

Registry input-array digest: `b4d030b38e5181064fb72fde93bb545c09e6a2a5720a559d5adf5ea46a5eae6a`.

Registry preview manifest digest: `fa3d69c809d5eee5d66307c3661fda77c7ea956896aa39e65d9077126ea110aa`.

The exact result summary is retained as a SHA-256-bound gzip-base64 record in `🧾️cad-full-ticket-reference-capture/🔣️.json`. This capture wrapper retained counts, digests, and all four violations, not the full 337-edit plan body. The process has exited, so the full body cannot be recovered from it. The next candidate must retain the complete canonical plan before any apply decision.

## Four report-coordinate blockers

All four errors were unsupported Markdown references to the historical CAD source catalog directory, in current-ticket authored reports:

- `📓️h-cad-draw-reference-surface.md`, original line 32.
- `📓️h-cad-example-path.md`, original line 40.
- `📓️s-cad-draw-scoped-consumer-closure.md`, original line 35.
- `📓️s-next-production-lane-router.md`, original line 56.

The coordinator authorized correcting these research documents. The duplicated historical source coordinates now link explicitly to the frozen CAD catalog's `/projections/0/sourceRoot` field. The two paired Draw source coordinates in the same edited report sections likewise link to `/projections/1/sourceRoot`. Historical source meaning was preserved; proposed canonical destinations were not relabeled as old scopes. A MarkdownIt-based read-only check resolved all six links to the exact existing catalog and a nonempty string at the declared JSON pointer. The frozen projection catalog SHA-256 remains `1410a74ccc87561fd4a4b91db7d503614fe21ddce8bc78dee923d8237820f3e0`. No Markdown exemption was added, and no historical plan was modified.

These authored report fixes have not yet been validated by a complete fresh CAD plan. The captured plan was blocked and is not apply-ready. Its operations would require exact current rederivation and all normal preflight gates even if the only source changes were unrelated schema additions.

## Performance and capture boundary

Two earlier concurrent captures were stopped only after exact process-identity verification when the generic parser's greedy full-line glob expression stalled on a retained bundle. The generic reference owner fixed that parser independently and verified its boundary tests. The successful capture above passed that prior hotspot. To reduce host memory pressure, the less-advanced concurrent Draw process was then stopped by exact PID/command identity; it returned exit 143 and produced no plan. CAD alone continued to completion.

The remaining runtime cost was broad incoming-reference discovery, particularly retained JSON evidence. No evidence directory was excluded to make this capture finish. No actual opaque Compose tree was read or traversed, and no real Git mutation occurred.

## Frozen catalog coordinate census

A bounded read of the unchanged CAD/Draw projection catalog found exactly **210** strings containing the historical CAD source root: `/projections/0/sourceRoot` and 209 `/projections/0/mappings/*/sourcePath` values. The analogous Draw count is **12**: its source root and 11 mapping source paths. Every matched value is a whole coordinate beginning at that source root; none is a partial-string prose occurrence.

This establishes the exact typed CAD catalog source spans, not the complete captured edit ownership. The captured 337 edits exceed the earlier 75 exact declared consumer edits by 262. The 210 catalog coordinates explain only part of that difference; the remaining 52 cannot be classified without a retained complete plan body. No inferred exemption or edit approval follows from these counts.
