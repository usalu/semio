# Energy Reference Capture

The retained `📜️script.ts` is an observational wrapper around the existing registered `workspace:clean-taxonomy-plan` command. It keeps the exact immutable baseline, complete Energy owner scope, one worker, ticket, and guarded cancellation sentinel. It does not alter inventory/reference admission, generator authority, plan parsing, or apply behavior.

Each new invocation allocates a fresh run below this report's semantic `🧾️runs` owner, after no-follow directory checks. Child stdout/stderr are preserved, with a bounded diagnostic tail printed after exit so progress volume cannot hide the failure. Each run has its own Markdown outcome. Exact before/after identities cover the root router, schema, discovery, normalization, and nested Cargo catalog. A stable flag is only a before/after observation, not an atomic snapshot guarantee. No apply is invoked, no Compose path is inspected, and no repository Git mutation is made. The read-only `preflight` command checks authority identities without allocating a run.

The prior session `13994` failed without publishing its requested plan and its inner diagnostic was lost to output truncation. This wrapper addresses diagnostic retention only. No automatic cleanup is part of it. Any later disposal of completed generated outputs requires separate exact-path ownership review; authored inputs, Markdown and active recovery evidence remain protected.

## Historical Capture

Session `11372` started the actual uncached Nx capture at **2026-08-27T12:06:07.832Z**, using the unique run root `🧪️energy-reference-capture-TWoVr9`. Its plan is `🔣️.json`, exact cancellation sentinel is `⛔️cancel`, and lossless combined output is `📋️output.txt` inside that root. The initial owner inventory completed **332 entries, including 195 files and 145 textual reference candidates**. Whole-monorepo incoming-reference discovery remains running; these initial counts are not complete reference closure and no apply is authorized.

| Startup Input | Bytes | SHA-256 |
| --- | ---: | --- |
| Root script | 2,815,702 | `645fd95ffc82516104b92e2d93712d51b77035b2d8e8667b3bcab907caad8ffd` |
| Taxonomy schema | 308,176 | `0cc60034727ec4c9d18c1e0a565d72f18594263f7ff2e080a885c13b5d4a42c5` |
| Discovery | 581,201 | `2c4eb79dbae521cdf88fb6a6f98dccb0c4265c8215348c625b2f29b76a2426e1` |
| Normalizer | 806,262 | `f30cacec0a1dec2da37f7d895a386ce48f8ed732a1df361ae851840f01c0e96a` |
| Nested Cargo catalog | 73,336 | `f64f2f654e5dc12d57b72148b023b2c9a20d6fd8f147293dac871e2529b2319a` |

Schema and package catalog edits are held during this capture. The package lane may independently repair only the evidence-removal preapply helper; any resulting normalizer byte drift must be recorded as such rather than described as an unchanged source snapshot. Draw and global inventory captures run concurrently. All generated evidence remains necessary while this process is active.
# Terminal Capture Adoption

Preservation correction, 2026-08-27T13:56Z: the exact `🧪️energy-reference-capture-TWoVr9` directory subsequently disappeared during concurrent work. A fresh no-follow check now returns ENOENT for its after-record. The terminal observations below were read before that disappearance; they do not imply that the raw capture is still available. This task's coordinator and agents issued no deletion of that capture. Other active tasks have been asked for ownership/recovery information; no replacement evidence has been fabricated.

Before its disappearance, `🧪️energy-reference-capture-TWoVr9/🔎️after.json` recorded exit 1 at `2026-08-27T12:41:23.754Z`. The lossless output reached all 99,913 incoming candidates and all 244 reference consumers before Bun rejected `input.tsx:69:5`, `Unexpected if`. No plan artifact was published. Before/after source identity stability was false. This was not a cancellation or a successful reference plan. The raw log and both identity observations are no longer available at their former paths.

The corresponding source is `🧰️framework/🔨️modules/🌱️value/🗂️ordered/🔢️numeric/🟦️component.ts`; its generic arrow in a static block is valid TypeScript but not TSX. The scanner has now been corrected to select grammar by physical extension without fallback. The source implementation was not rewritten. The permanent regression and successful actual registry-closure evidence are recorded in [the registry grammar report](../📓️registry-import-language/📝️.md). That closure is not a new Energy plan or pilot acceptance.

## Fresh Run Invocation Boundary

Use an absolute script path and `bun x nx exec --projects=workspace --skip-nx-cache -- bun <absolute-script> preflight` (or `capture`). The first read-only probe used a bare relative script argument and Bun could not resolve it. The second used an absolute path but omitted the project restriction; Nx selected a nested Draw project and the new exact-working-directory check rejected it before allocation. A separate inline diagnostic also failed at shell argument parsing without running its body. The installed Nx `exec` implementation explicitly chooses all projects when no project selector is present; the bounded invocation must select only `workspace`. None of those failed probes created a run or started a capture.

The corrected bounded preflight (session `74882`) exited zero. It observed schema SHA-256 `0cc60034727ec4c9d18c1e0a565d72f18594263f7ff2e080a885c13b5d4a42c5`, normalizer `ead932b049d1c18bb9d484c7c8bc5cb2d893b4adb8c1f8abb1f84eb747a68cfd`, and the still-unrefreshed nested Cargo catalog `f64f2f654e5dc12d57b72148b023b2c9a20d6fd8f147293dac871e2529b2319a`. No run was allocated; the full capture remains pending the coordinated WGPU authority update.
