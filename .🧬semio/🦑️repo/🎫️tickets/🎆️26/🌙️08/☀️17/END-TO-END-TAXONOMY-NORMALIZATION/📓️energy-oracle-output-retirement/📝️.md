# Energy Oracle Output Retirement

## Exact Scope

The three completed Energy pointer oracle runs produced the six disposable targets below. Their source manifests and Rust inputs remain authored execution inputs. The first run established the real assertion failure; the second passed after the semantic correction; the third passed inside the registered 17-case Nx gate. All three Cargo subprocesses exited before this audit.

The cleanup skill's broad workspace command would exceed this task's exact scope and Compose exclusion. Only the following no-follow-checked outputs are authorized for retirement. No transaction journal, recovery dependency, other ticket output, source fixture, report, or actual Compose path is included.

Paths are relative to this ticket. All six nodes are on device `16777230`; lockfiles are regular mode `0644` and target roots are directories mode `0755`. Recursive enumeration found no symlinks or special nodes.

| Exact target | Inode | Files | Bytes |
| --- | ---: | ---: | ---: |
| `🧪️energy-pointer-oracle-31Tc1d/Cargo.lock` | 133034991 | 1 | 2,651 |
| `🧪️energy-pointer-oracle-31Tc1d/🎯️target` | 133034994 | 146 | 19,895,463 |
| `🧪️energy-pointer-oracle-9sPItR/Cargo.lock` | 133050561 | 1 | 2,651 |
| `🧪️energy-pointer-oracle-9sPItR/🎯️target` | 133050564 | 158 | 20,313,663 |
| `🧪️energy-pointer-oracle-cMlQBi/Cargo.lock` | 133064797 | 1 | 2,651 |
| `🧪️energy-pointer-oracle-cMlQBi/🎯️target` | 133064801 | 158 | 20,313,484 |

Total: **465 output files, 60,530,563 bytes**. All three generated lockfiles have SHA-256 `882646d31f3b3dce02904ac6d5e91943d4de2893b7d4a6b324345801db1b1b6a`. Separate `lsof +D` checks of all three exact target trees returned no open files. This does not authorize removal of any parent fixture directory.

## Retained Inputs

Each fixture retains its 189-byte `Cargo.toml`, SHA-256 `ceb99cdba09bb47c23efe804e1d0f6fdd5a9ce4ff4522659118fb1236f1660cc`, including the exact `serde_json = "=1.0.149"` dependency and isolated workspace declaration.

The red-run `🦀️.rs` input is 382 bytes, SHA-256 `9341c9b432dc77fdab6db64d843f192b3b337013825295fa5dc9489424473007`. Each green-run Rust input is 448 bytes, SHA-256 `f001c15f3b76a7b0e45e8814ccfe8914ff3fba6255828cd217898164ebe6b959`. All six input files are regular mode `0644` and are excluded from the removal set.

Compiler outputs are reproducible from these retained inputs; exact removed binary bytes are not being separately backed up. Runtime and assertion outcomes remain recorded in [the Energy verification report](../📓️x-energy-support-leaves.md).

## Completion

All six exact targets were removed after the no-open-file checks; the lockfile-only `lsof` check also returned no open handles. No parent fixture directory was removed. Fresh `lstat` checks returned `ENOENT` for all six outputs, and each fixture now contains exactly `Cargo.toml` and `🦀️.rs`.

All six retained input hashes, byte counts, and modes match the preflight values above. The three compiler output trees and generated lockfiles are rebuildable, not separately backed up. No source input, transaction evidence, report, actual Compose path, or shared Git state was changed by this retirement.

## Permanent Oracle Teardown

The Energy oracle now performs the same narrow teardown after its Cargo subprocess exits. The language-neutral execution contract distinguishes the two retained inputs from the two disposable outputs. The helper rejects a root outside the exact ticket/prefix and validates all existing output node kinds without following links before deleting any output. A forged target symlink therefore preserves the adjacent lockfile and its external sentinel.

The added test first failed because the helper was absent (**0 passed, 1 failed, 1 assertion, 221 milliseconds**). After implementation, the focused test passed **1 test, 11 assertions, zero failures in 258 milliseconds**. Independent `fast-glob` confirmed that only the two authored inputs remained in the positive fixture. The unowned-root and symlink preflight cases remained untouched. This is focused teardown verification; a fresh complete registered packet is still needed after the concurrent graph changes settle.
