# Current Scoped Draw Reference Capture

## Authorized boundary

This is a fresh read-only production capture for the exact Draw source owner recorded by [the frozen catalog's `/projections/1/sourceRoot`](../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🧫️fixtures/🧪️cad-draw-path-projection/🔣️.json#/projections/1/sourceRoot). The scope value was passed without an entry or operation filter. The fixed baseline `9f449b10659b95148c8bcb3f91ce583bf7446973` was verified to be a commit. The exact active ticket is `26/08/17/END-TO-END-TAXONOMY-NORMALIZATION`; worker count is one.

The registered uncached Nx target is `workspace:clean-taxonomy-plan`. It calls the existing root `📜️script.ts clean taxonomy plan` router with the baseline, catalog-resolved scope, ticket, and explicit plan/cancellation paths. The root API supplies `excludedTreeDigests: []`; it does not inspect or opt into either intentionally opaque Compose tree. No apply or production generator was requested. Generator previews, if selected by the plan, remain their normal read-only authority check.

The intended full canonical plan is [🧾️draw-current-reference-plan/🔣️.json](🧾️draw-current-reference-plan/🔣️.json). Its cancellation sentinel is the sibling `⛔️cancel`; it was not created. The full plan body, not just counts, is required before any operation audit. These are active planning outputs and are retained while review is pending.

## Startup and current progress

An initial schema preflight correctly rejected the in-flight WGPU candidate contract because five tracked generated outputs were absent: builder Rust, binary Rust, TypeScript library, renderer registry Rust, and generated frame-worker JavaScript. No guard was bypassed. The package lane then restored its source-phase contract and confirmed valid fresh startup. The subsequently launched official Nx task picked up that coherent state and progressed into scoped inventory.

Scoped inventory completed **29 entries: 18 directories and 11 files**. Incoming-reference completeness then enumerated **88,809 candidate paths**. This is the incoming consumer closure for the narrow Draw source scope, not a whole-repository normalization plan or widened source inventory. The prior historical-input registration and the [six-output/ten-span closure](📓️s-completed-output-retirement-and-historical-links.md) are current prerequisites; neither an old blocked plan nor an old test count is being reused as fresh readiness evidence.

The first full capture has now exited **1**, before any final plan was written. Incoming-reference analysis reached a different fail-closed boundary: `Rust module ownership has a non-directory or symlink ancestor` at the exact retained fixture `🧪️rust-path-Kem4kq/pkg/Cargo.toml` in this ticket.

The fixture root is a directory, mode `0700`, inode `133028117`; `pkg` is a directory, mode `0755`, inode `133028131`. The exact Cargo node is a symlink, mode `0755`, inode `133028137`, target text `../actual/Cargo.toml`. Only lstat and readlink were used for this check; the target was not opened. The fixture also has its own `.git` entry. No fixture input was deleted or altered.

The observed call chain is `incomingReferenceSnapshot` → `referenceTokensIncludingUnsupported` → `rustTokens` → `rustManifestReferenceTokens` → `rustReferenceGraph` → `inspectRustModuleGraph` → `assertLexicalInputOutsideOpaque` → `assertNoFollowAncestors`. The reference-graph owner and coordinator received this exact failure. It is not the already resolved WGPU startup issue. No symlink guard or candidate authority was bypassed.

The intended full plan path was independently checked and is absent (`ENOENT`). Therefore there is no move/edit/removal/regeneration/unresolved operation packet to approve yet. No generator preview, production generation, or apply was reached by the failed plan.

## Retained scoped inventory

The separate registered, uncached `workspace:clean-taxonomy-inventory` target completed with exit **0** using the same exact catalog-resolved scope, active ticket, and one worker. It persists the narrow inventory without entering the failed incoming-reference plan stage. Its [canonical shard manifest](📊️taxonomy-inventory/📊️shards/🔣️.json) and [generated inventory summary](📓️taxonomy-inventory/📝️.md) remain active review inputs.

A fresh call to the exported production `validateTaxonomyInventoryArtifactShards` reconstructed the retained inventory and passed canonical-byte, complete available-shard closure, ordering, uniqueness, count, and digest checks. The available payload list was independently enumerated using direct directory entries, with nonregular or symlink nodes rejected before reading. Result: **29 entries, 18 directories, 11 files, zero violations**. This was a retained-artifact validation, not a new test-suite claim or another broad scan.

| Retained authority | Exact value |
| --- | --- |
| Manifest bytes / SHA-256 | `1852` / `45868e4389e435f2cc38deeca174ac64307c4db2075e63fd13e7dbc8b3d9fabb` |
| Inventory digest | `a66920d165007afb51bf6583b0fb113913ea34c560232a648734ba29de198930` |
| Source-tree digest | `25defb08725cc7a5dc60e733444024078ed1b039c607c383c1d3aaa9d8b6a9c4` |
| Reconstructed canonical inventory digest | `f7fe75b3e1c3db4c083ba7286abe90d45e313571c52d61932a40a528fbb3427c` |
| Shard-ledger digest | `e668936abfd304fdd6f32791475ee819fda3fc4fafa6b6cc15a7f6d7f51ea5aa` |
| 27-entry owner shard bytes / SHA-256 | `22317` / `6a1d8f3efecf9e8c7991a28ba16ce1e2b06f3eb5e505c81523faabfc915097cb` |
| 2-entry area shard bytes / SHA-256 | `709` / `de715ca46876715e3aa97567233062c7106841e9acc21e208af735b3c0f2910d` |

The retained metadata uses taxonomy schema version `7`, the exact scoped source owner, and only the two authoritative opaque path exclusions; `activePathExclusions` is empty. Validation did not touch either excluded tree. Both capture processes have ended. No cancellation sentinel, production mutation, full plan, or readiness approval was produced. Another plan attempt requires the incoming Rust graph boundary to be resolved without deleting the genuine negative fixture or weakening no-follow checks.

## Coordinator Destination Review

The retained eleven-file mapping consistently relocates this command beneath the editor's `🪆️1-any` semantic profile. Cargo manifests, Nx project manifests and task routers retain their registered tool-contract filenames. The two Cargo entry implementations move beneath their package's `📚️library` directory as physical Rust leaves. The command, state machine and macro implementation files become `🦀️.rs` beneath their existing semantic owners. No domain owner is collapsed into a file stem, and the Rust package boundary remains distinct from its domain implementation.

This reviews destination intent only. Incoming consumer edits, generator previews, removal authority and final operation preimages cannot be approved until a complete fresh plan exists. No operation filter or hand-selected partial plan is authorized by this review.

A separate coordinator readback validated every source path beneath the exact scope, rejected symlink ancestors before content reads, and recomputed all eleven hashes and byte counts. All matched the retained inventory: **242,790 source bytes, no content drift**. This is a bounded source-preimage check, not a complete fresh plan or runtime result.

## Graph-Corrected Retry

A fresh registered, uncached Nx capture was launched after the [Rust ownership-scope release](📓️y-energy-rust-reference-context.md) and the coordinator's CLI cancellation forwarding correction. It uses the same exact source scope resolved from the retained shard manifest, fixed baseline, active ticket, one worker, explicit plan output, and sibling cancellation sentinel. No operation or entry filter was supplied. The sentinel was absent at launch and has not been created.

The retry completed its **29-entry source inventory** and enumerated **93,310 incoming candidates**. It passed the previously fatal retained Rust-fixture range without deleting that fixture or weakening its governing-owner negative. The prior failed attempt and this retry are separate observations; the old failed attempt did not produce a full plan.

The retry has now exited **1** after completing all **93,310 / 93,310 incoming candidates**. Its earlier bounded liveness check observed exact CLI PID `7530`, elapsed **16 minutes 31 seconds**, using **67.2% CPU**, with the last completed checkpoint at **44,100 / 93,310**. That elapsed value is an intermediate observation, not the final duration.

A read-only metadata check inspected the next hundred immediate entries in the separate microkernel ticket's sorted record range. Many are regular JSON files between roughly one and four megabytes; no content or symlink target was opened by this metadata check. This explains why a low candidate-count increment alone is not evidence of a dead process, but it does not establish any one record as the measured hotspot. The coordinator separately owns the narrowly tested JSON parsing optimization. This retry keeps its already loaded reference rules.

After the incoming scan, `semanticPackageProjectionCatalog` rejected **`Nested Cargo catalog digest drift`**, called from `buildReferenceEdits` → `planMoveReferenceAuthority` → `planTaxonomy` → the root CLI. The long-running process retained its startup schema authority while the package lane updated the catalog. No old loaded hash was captured, and none is inferred from unrelated artifacts. This is a late catalog-authority failure, not a recurrence of the unrelated Rust-owner failure.

A fresh no-follow read of [the nested Cargo catalog](../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/📦️packages/🟦️typescript/🧫️fixtures/🧪️nested-cargo-package-projection/🔣️.json) found **73,336 bytes**, SHA-256 **`f64f2f654e5dc12d57b72148b023b2c9a20d6fd8f147293dac871e2529b2319a`**, matching the current on-disk schema declaration. This matching current pair is not a fresh full-loader or source-preimage validation: the package lane separately reported current external Rust source changes still under review.

The requested plan output and cancellation sentinel were independently checked after exit; both are absent (`ENOENT`). No full plan body, self-digest, final operation counts, generator preview, production generation, or apply was produced. Shared generated default summaries are not used as this capture's result authority. The capture is terminal and does not approve application. A new attempt waits for a coordinated stable catalog/source-authority window; no automatic retry was launched.

## Subsequent Coordinated Capture

After the coordinator established the next stable schema/catalog window and the JSON parser gate was released, a separate [lossless Draw capture](🧾️draw-reference-capture-kmddtz/📝️.md) began at **2026-08-27T12:07:05.026Z**. Its unique output/log/sentinel, full before/after identities, exact exit outcome and complete operation audit belong to that new report. Neither terminal attempt above is relabeled as a successful plan.

That capture ended **exit 1** at **2026-08-27T12:42:06.535Z**, after **99,975 incoming candidates** and **43 reference-progress records**, at the registry TypeScript/TSX parsing boundary. There is still no final Draw plan. Its schema and catalog identities stayed fixed, but three implementation identities changed; the exact record is in the linked report. The [current acceptance audit](📓️s-draw-durable-acceptance.md) confirms unchanged eleven-file source preimages and identifies the canonical-only test transition still required before a reviewed real move.
