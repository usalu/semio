# Combined Regression Launch Current

runtime1086 verified unique package targets and selectors, then detected the combined launch entry missing from the current VS Code configuration. The sequential regression command remained at gate order 411.5. Restore the combined laws-fleet command immediately after it at 411.51 using JSONC edits that preserve other launch entries and comments. No matching permanent generator source was found for this literal launch label. A fresh scope validation follows.

All complete Rust sources parsed before guarded writes. Strict compilation and runtime validation remain pending.

- .vscode/launch.json
