# Example Mounts

Pass572's proposed patch did not apply because the Animate example test path changed concurrently. Follow-up inspection confirmed that concurrent edits already mounted both Puzzle 3D example sources and tests, fixed Animate's example test path, and corrected the Procedural Generation 3D main-window constant. No pass572 source edits were applied. The existing fixtures and tests remain authoritative; native revalidation is still pending.
