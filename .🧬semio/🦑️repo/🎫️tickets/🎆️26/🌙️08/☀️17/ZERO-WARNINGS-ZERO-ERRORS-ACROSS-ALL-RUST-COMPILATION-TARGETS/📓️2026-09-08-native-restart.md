# Native Restart

Pass604 validated all 48 current Rust files from the view decoder, AVI/Semio parameter and DAG fixes with source hashes unchanged. The current ticket runner also passed Bun TypeScript parsing. Native582 was cancelled through its own control path and exited 143; its final receipt is explicitly cancelled. The replacement native604 worker started as PID 92861 only after the prior worker exited.

Native582's partial diagnostics identified the private decoder, 38 unused render parameters and the DAG undeclared-field/pattern failures. Those failures prevented its dependent plugin checks, so it is not a complete verification result. The cache and diagnostic evidence were retained for native604.

A source audit found no remaining rest-pattern comma followed by Default::default() among Rust files in the framework and S at this checkpoint. Nx's existing ignore file already excludes ticket directories; no ignore configuration was changed. Its ticket-local file map was 21 MB, so no generated-output exclusion change was justified.

Current native compilation, links, Clippy and runtime verification remain pending.

Native604 emitted successful, newly checked library artifacts for semio-framework-artifact-infinite-dag and semio-framework-plugin (profile.test false, fresh false). The previous DAG field/pattern errors and unused decoder warning did not recur in those library checks. This does not establish their test-target or runtime success. Two newly reported store-sync qualifications were corrected separately in pass605 and still require fresh verification.
