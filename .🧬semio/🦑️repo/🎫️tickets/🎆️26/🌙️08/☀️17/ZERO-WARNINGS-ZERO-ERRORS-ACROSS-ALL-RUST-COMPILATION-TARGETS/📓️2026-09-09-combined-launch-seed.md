# Combined Regression Launch Seed

The current launch and its .vscode/🧩️launch.seed.jsonc both contain the sequential regression entry but omit the combined entry. The seed is now present and holds the literal sibling command. Insert the combined laws-fleet entry immediately after that sibling at order 411.51 in both files, preserving JSONC comments and other entries. Parse and verify exactly one combined entry in each result before guarded writes.

All complete Rust sources parsed before guarded writes. Strict compilation and runtime validation remain pending.

- .vscode/🧩️launch.seed.jsonc
- .vscode/launch.json
