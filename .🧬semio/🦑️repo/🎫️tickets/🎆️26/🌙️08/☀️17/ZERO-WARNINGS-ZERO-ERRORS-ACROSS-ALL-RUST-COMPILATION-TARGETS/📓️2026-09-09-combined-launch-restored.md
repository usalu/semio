# Combined Regression Launch Restored

The current launch configuration again lacks the combined regression entry while the existing laws-fleet implementation and previously validated 74-group catalog remain present. The sequential regression command remained at gate order 411.5. Restore the combined laws-fleet command immediately after it at 411.51 using JSONC edits that preserve other launch entries and comments. No matching permanent generator source was found for this literal launch label. A fresh scope validation follows.

All complete Rust sources parsed before guarded writes. Strict compilation and runtime validation remain pending.

- .vscode/launch.json
