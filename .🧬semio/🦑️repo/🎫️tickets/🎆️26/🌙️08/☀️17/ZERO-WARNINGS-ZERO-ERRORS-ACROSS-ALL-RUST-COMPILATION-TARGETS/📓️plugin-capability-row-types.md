# Plugin Capability Row Types

Pass 452 gives the existing five-part borrowed capability row a shared ArtifactCapabilityRow name alongside ArtifactCapability in the plugin API, and uses it in 22 artifact definitions. Tuple layout, literals, validation, allocations and capability registration order are unchanged. The alias uses only repository-owned and standard-library types. Compiler validation and existing definition admission tests remain pending.
