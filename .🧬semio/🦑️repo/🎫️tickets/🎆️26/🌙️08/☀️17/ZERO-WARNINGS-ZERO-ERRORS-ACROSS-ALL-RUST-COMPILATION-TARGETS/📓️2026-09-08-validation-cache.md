# Validation Cache

Pass598 points the ticket's exact runtime-law runner at the same generated/derive-target Cargo directory used by native582. Sequential checks, links and runtime tests can therefore reuse eligible Cargo dependency/build-script/procedural-macro artifacts. Cargo remains responsible for distinguishing profiles, features and compiler settings. The active build directory was not moved, copied or deleted.

Changed file: `📜️script.ts` in this ticket. Test selection, compiler flags, assertion logic and executable fingerprint validation are unchanged. Actual runtime execution and cache reuse are pending.
