# Rust Warning Cleanup — Current Verification

The goal remains active. No complete warning-free build or link is claimed.

The latest completed native renderer check (287) passed all renderer targets, including native-bin, with zero errors and 235 dead-code diagnostics. Browser288 passed with zero errors and 141 dead-code diagnostics. These builds precede the latest map, graph and renderer cleanup. The full native 63-package check307 completed with errors, including mixed dependency/caller signatures and GIS/VCS standalone test imports (corrected in pass335). Fresh native339 is queued; the last full successful native fleet check223 had zero errors and 519 warnings.

WASI306 checked the retained-command framework after pass304 and exposed 2,045 strict Clippy diagnostics: 26 Surface, 6 editor, 26 OS and 1,987 Stdio. Earlier lower counts were limited by dependency failures and did not cover Stdio. Passes310/312/315 address the Surface/editor diagnostics, and pass323 addresses the remaining OS input APIs. Pass319 applied 1,057 reviewed machine-applicable suggestion groups across 509 Rust files, using exact compiler source-line and UTF-8 byte matching. Ownership, futures and representation changes were excluded. A fresh 59-plugin strict check324 is running, with native339 queued behind it. See [the suggestion audit](./📓️compiler-suggestion-review.md).

The retained-command API now groups shared step borrows and owned creation inputs; all 14 signatures, 15 work-step calls and 38 payload constructors were updated. The audit is in [retained command inputs](./📓️retained-command-inputs.md). This preserves payload ownership, capacity admission and close/reducer behavior; the seven selected native Plugin laws passed after this refactor in native317.

Recent completed runtime results:

- Native294: 14 Plugin laws passed, including checkpoint schema/oracle validation, cold-pair admission, cancellation/close ownership and the routed-inference roster; the Infinite themed icon lookup also passed.
- Native275: 7 Procedural ID, domain-cache and canonical compiler laws passed; the build retained 33 dead-code warnings.
- Assets289: all 286 deterministic generated outputs matched fresh generation.
- Native317: the Imperative Effect bundle contribution law passed after the production Pin import repair.
- Native317 Surface tests could not build because a JSON fixture still used the changed ToValue bridge. The editor multibyte test failed because its fixture counted U+1F600 plus U+FE0F as one Rust char. Pass321 fixes the fixture conversion and scalar-boundary vectors; rerun325 passed all 13 selected Surface laws and all four editor laws.

Renderer pruning remains conservative. Pass320 found four disconnected functions after tracing references throughout the renderer engine; pass321 initially removed those four, and pass327 restored two that are referenced by serde attributes. Two disconnected helpers remain removed. Functions reached by tests, platform entry points and ownership paths remain under review. See [the renderer inventory](./📓️renderer-warnings-current.md) and [reference audit](./📓️renderer-unreferenced-functions.md).

Earlier verified laws and all exact receipts are preserved in [the detailed report](./📓️2026-09-07-verification.md). Known unresolved runtime failures include worker-pool contention, Forms timing, Draw capacity, WFC checkpoint publication, Process/Space/Imperative retirement, and Playbook retirement/classification. Native executable build/link332 is running; WASI component link validation remains required. Pass330 restricted 51 source-audited renderer fixture items to tests and removed two unreferenced private types. Native337 is validating the OS catalog/codec edits with the full host feature enabled. Generated outputs remain in the ticket while verification is active.


## Pass 344 — Completed Native Link And OS Laws

Native executable build native-link332 completed with exit code 0. Its compiler diagnostics are inventoried separately; successful linking does not establish zero warnings. All six selected OS runtime laws passed with os-host-full in pass337, covering codec fixtures, deterministic/paged conversion, interruption, draft demotion, and space catalog operations.

Strict WASI pass324 completed with 904 diagnostics: 903 in Stdio and one OS redundant clone. Pass343 fixes the OS clone and three Stdio suggestions; fresh verification remains required. Pass342 removes Note’s duplicated unused-import warning. See 📓️note-retained-import.md and 📓️fresh-compiler-followups.md.

Native link332 succeeded with 95 warnings: 93 dead-code and two unused-import diagnostics. Its successful link is not yet warning-free.


## Pass 372 — Current Verification Receipts

- Native executable link332 passed with 95 warnings (93 dead code, two now-test-only imports corrected in pass350).
- Native full all-target check339 completed with two E0507 integration-fixture errors and 209 dead-code warnings. Both envelope errors are corrected in pass354; fresh native369 is running.
- Browser347 completed successfully with 88 warnings: 86 dead code, one unused mut and one unnecessary parentheses. Pass366 fixes the latter two.
- Strict WASI357 completed with 12 early-lint errors; these blocked the complete type-level lint inventory. Pass366 addresses them, and strict WASI368 is running.
- OS337: all six selected runtime laws passed with os-host-full.
- Framework358: both composer chaining and conflict-preservation runtime laws passed after synchronous registry publication.
- Store358: the codec fixture failed before registration (demo.doc vs declared extension demo); pass367 corrects that assertion and its fresh envelope ownership. No later store laws ran in that group.
- Stdio348 built successfully but used a nonexistent test path. The verified baseline executable was then run with its actual component-qualified test path in pass362 and reproduced missing registration. Stdio370 uses the corrected test name.
- Pass370 is running the updated store, Stdio JSON/restore/registration, GIS and VCS runtime laws. None are yet claimed passing.

The ticket’s existing laws command now includes these exact library/integration targets. No new executable command was introduced.


## Pass 396 — Current Compiler Receipts

The completed strict WASI pass 368 failed with 293 compiler diagnostics (the commentary total of 294 was off by one). This is a complete type-level inventory from that run; subsequent edits require a fresh pass.

{
  "error clippy::unnecessary_wraps": 16,
  "error clippy::type_complexity": 66,
  "error clippy::unnecessary_sort_by": 1,
  "error clippy::needless_pass_by_value": 66,
  "error clippy::ptr_arg": 3,
  "error clippy::doc_lazy_continuation": 25,
  "error clippy::manual_strip": 1,
  "error clippy::explicit_counter_loop": 16,
  "error clippy::needless_range_loop": 27,
  "error clippy::map_unwrap_or": 6,
  "error clippy::drop_non_drop": 1,
  "error clippy::large_enum_variant": 5,
  "error clippy::too_many_arguments": 24,
  "error clippy::collapsible_match": 4,
  "error clippy::format_in_format_args": 6,
  "error clippy::module_inception": 1,
  "error clippy::manual_clamp": 3,
  "error clippy::should_implement_trait": 1,
  "error clippy::iter_cloned_collect": 1,
  "error clippy::needless_update": 6,
  "error clippy::unnecessary_unwrap": 2,
  "error clippy::unnecessary_to_owned": 1,
  "error clippy::vec_init_then_push": 2,
  "error clippy::manual_memcpy": 2,
  "error clippy::neg_cmp_op_on_partial_ord": 2,
  "error clippy::while_let_loop": 2,
  "error clippy::field_reassign_with_default": 2,
  "error clippy::result_large_err": 1
}

Native fleet pass369 remains running; its partial inventory currently contains 212 warnings/errors. Do not treat a partial stream as a pass. The fresh strict WASI pass387 is queued behind its Cargo target lock. Native law pass370 also remains running in the separate link target. Kernel round-trip failure370 now reaches an existing parsed envelope terminal-shell Drop in the ArtifactCodec compile/print bridges, after correcting the fixture extension; the later two Kernel registration laws were not reached. No production retirement shortcut or fabricated factory was added. The earlier Stdio358 run failed its stale test selector; 370 uses the verified full selector.

Applied and audited since the earlier overview: 376 borrowed absorb inputs; 378 integer clamps, slice copies, sorted-key comparison and doc indentation; 380 optional matches, formatting and collection expressions; 383/384 domain type aliases; 385 standard FromStr and compactness module naming; 388 glTF test qualification; 390 canonical Flow decoder admission; 391 counted iteration; 392 borrowed error formatting; 393 borrowed diff inputs. The source-guard rejections for 380/383/384/390 happened before any writes and each successful corrected batch ran once. Details and file inventories are retained in 📓️stdio-expression-followups.md and 📓️stdio-owned-representations.md.

Pass396 also removes two redundant store:: qualifications from the GIS native-codec law using its existing ArtifactPack import. Renderer pass395 is a read-only reachability audit, not a source change. Full native, browser, and WASI link verification and zero-diagnostic checks are still required. The goal and ticket remain open.


## Pass 401/402 — Native and Stdio Receipts

Native fleet369 completed successfully in 41m08s with zero compiler errors and 212 warnings: 209 dead code and three unused qualifications. The three qualifications are fixed in passes388/396. Renderer400’s 48 test-helper scopes and five unused function removals postdate this build and require fresh verification. Browser401 and native-renderer402 are queued behind strict WASI387 in the shared check target.

Native law370’s Stdio group passed all four exact tests: typed binary composer registration; registered GIF migration through the store; glTF rejection messages against neutral JSON; and glTF apply/restore JSON plus inverse and carrier size. This verifies the registration355, rejection360, and glTF363/364 changes in a linked executable. The existing Kernel round-trip failure remains documented; GIS/VCS groups are still running.


## Pass 406 — Validation Follow-Ups

Pass405 audited all 25 documentation insertions, repairing six Markdown lines and four PDF lines; 15 were already correctly indented. New byte-span checks must verify line/column offsets in addition to matching source text, including empty insertions.

The first GIS native-codec law in pass370 failed at the ArtifactEnvelope terminal-shell Drop assertion. It reaches the package-owned genesis path before the explicit parsed-envelope test helper; the second GIS inference law was not reached. VCS is still building. These are runtime failures, while the completed native369 compiler fleet had zero errors. No test success is inferred from compilation.


## Pass 407 — Complete Strict WASI and Browser Results

Strict WASI check 387 completed with 104 Clippy errors plus one alias-visibility and one redundant-qualification error (106 total). Ten documentation continuation diagnostics and the latter two issues have subsequent guarded corrections in pass 405. Browser check 401 completed successfully with 68 warnings: 63 dead-code and five unused-import diagnostics. Native renderer 402 and the remaining VCS runtime laws are still running. The compiler inventory is captured before further edits; zero-warning verification remains incomplete.


## Pass 419 — Native Codec and Geometry Laws Dispatched

Selected sixteen existing laws covering JPEG transforms, an independently generated DEFLATE fixture stream, spline interpolation, exact cylinder intersections, IFC/DWG/EPW mutation codecs and native bytes, synchronous registration, and glTF neutral rejection/restore fixtures. This build starts after pass 418. Pass 402 native renderer check completed with zero errors and 147 warnings (146 dead-code and one unused import subsequently removed in 408). Pass 370 VCS failed the artifact-envelope retirement assertion; its second law did not run.


## Pass 421 — Fresh WASI Check After Carrier Changes

Run 416 failed with six type-inference diagnostics at three PNG temporary collections. The current shared source already explicitly collects those temporaries into vectors; no additional edit was needed. Run 421 starts from that corrected source and includes the carrier changes in 418. Native numerical and codec-law builds 419 and 420 remain in progress.
