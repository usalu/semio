# Window Config Runtime Import

Native714 found the component runtime's new LoadWindowConfig branch referenced WindowConfigPack without importing its sibling app contract. Added the existing public type to the runtime import list. Rust syntax parsed; compiler and runtime validation pending.

- 🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🦀️.rs
