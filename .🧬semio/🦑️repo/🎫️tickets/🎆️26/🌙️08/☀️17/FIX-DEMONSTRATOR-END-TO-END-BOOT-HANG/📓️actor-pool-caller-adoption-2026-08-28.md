# Actor Pool Caller Adoption

## Executed Boundary

Latest verification:full actor152PASS/4FAIL156 at01:45:04,with fifteen stable selected inputs; strict42 (16UI builder/page counters,19UI fixtures,7tutorial), no Shard/Kernel-input diagnostics. Later focused10/4 at01:46:04 also verifies ordinary UI forwarding of clean-refusal abandonment. [Complete current evidence](./📓️field-payload-runtime-2026-08-28.md). The four payload callers remain blocked specifically in the unreleased builder/page allocation path, not instance or payload admission.

Current mandatory payload join:10PASS/4FAIL156 at01:42:04,with fifteen stable inputs. The four callers now complete exact source-fixture scope440/936 and payload11-phase872 admission, then reach the remaining old UI builder counters. New active/revoked close and clean-refusal abandonment cases pass. [Exact evidence](./📓️field-payload-runtime-2026-08-28.md). Earlier scope rejections below were transient during the UI-owned cutover, not current failures.

Newest source checkpoint, start01:31:22: the expanded input selector ran7PASS/5FAIL/142skip154,1.02s,Nx1. All three new private association/constructor foundation groups pass. The five scope-dependent cases now reject at the previous368-byte instance construction phase while UI's own payload-slot constructor is being changed. Eight selected inputs remained stable during this run. The exact UI cause/new coherent phase packet is not yet verified; this lane preserved its fixture and notified the UI coordinator. Subsequent strict has55 diagnostics, none in Shard or Kernel input. See [source foundation evidence](./📓️field-owned-resident-payload-contract-2026-08-28.md). The earlier147/4 and46-diagnostic results below are historical, not the current tree.

Latest continuation: after the coherent UI scope release, this lane reran the unchanged callers and observed4PASS/4FAIL/142skip150,754ms,start01:13:50; all failures were their omitted fourth grant argument. The four callers now consume the actual UI slot fixture's nine separate grants296/64/64/64/264/64/368/64/128, with no scope exposure before the final phase. They verify exact repeat identity, private original host/activation/lifetime and the same-ledger864/14/14 addition using strict Ajv and an independent Immer arithmetic oracle.

The joined selector actually ran5PASS/4FAIL/142skip151,1.86s,start01:15:24. The added empty-scope case completed its declared16-turn close bound and returned only the scope's charge while preserving the original captured response. The four payload cases now fail at UI's separately unreleased `ownerAvailable(pool.capacity.maxOwners)` inside `beginPayload`, after every scope admission assertion. Logs: `🧪️actor-scope-caller-red-1.log` and `🧪️actor-scope-caller-join-1.log`. This is not a nine-test pass.

The subsequent entire actor target actually ran147PASS/4FAIL,151tests/ninefiles,5.50s,start01:17:05,Nx exit1. Only those same four payload cases failed; all existing activation lease/transport tests and the new original-composition cases executed successfully in this run. Log: `🧪️actor-activation-scope-full-1.log`. Seven of eight selected source inputs stayed stable, including Shard `9767db6290339a6df6371c642b50a6f2a6c50e2acdfece89a3d90bc5f2a18d5a`, composition schema/fixture, neutral, input source and slot schema/fixture. UI resident changed from `d887933f943511e126050a112dbf304bb652d28dcc3908994d74084d1f319262` to `5731a2b01045433c2e97aa2763abc79d906bbe8abcae22d80b0a85dd20334745` during the wider capture. This is not an atomic current-tree result.

Renderer strict then exited1 with46 diagnostics:20 remaining old UI payload/page counter accesses,19 old private-pool fixture constructors in UiDocumentStore, and seven tutorial joins. It has no Shard caller/matcher/controller or PluginRuntime diagnostic. Log: `🧪️actor-activation-scope-strict-1.log`. The eight actor child-admission errors in the preceding70-error run are cleared at this exact source boundary; full strict remains RED. No all-app or live guest credit follows.

## Earlier Pool-Only Checkpoint

The four actor-owned native-input fixture callers no longer call the private pool constructor or create independent local pool allowances. The existing actor test harness now returns the same ledger it actually injects. Each caller uses actual `OwnedUiResidentPool.begin` through the14 schema-declared controller/pool preparation turns, then the exported UI metadata plus installation work. Exact private composition, installed owner and total charge are checked before child admission.

The original eight-test OwnedKernelReturnInput selector executed4PASS/4FAIL/139skip147,781ms,start00:52:48. All four failures were invalid private pool construction. After the caller edits the same selector executed4PASS/4FAIL/139skip147,1.37s,start00:53:42. Pool preparation/identity/charge assertions completed; all four failures advanced to the UI-owned `ownerAvailable(pool.capacity.maxOwners)` at resident34/71. The child cutover is in progress. Logs are `🧪️actor-pool-caller-red-1.log` and `🧪️actor-pool-caller-join-1.log`. This is not an eight-test pass.

The subsequent strict run exited1 with70 diagnostics while UI source advanced to the new four-argument instance admission result. The four private-pool constructor diagnostics are gone, replaced by eight actor caller diagnostics for the staged `bindInstance(owner,activation,lifetime,grant)` result API. The remaining diagnostics are UI in-progress InstanceWitness/private-dispatch/counter/fixture work and the existing tutorial joins. There is no new captured-return constructor/controller diagnostic. Log: `🧪️actor-pool-caller-strict-1.log`.

The scope API hold at this historical checkpoint has now been superseded by the verified nine-phase join above. The UI payload API and its exact source-field association remain separately unimplemented. No old counters, compatibility constructor, structural type cast, default ledger, quota change or widened loop cap was introduced. The four failures remain visible; no full actor/React or six-app readiness is claimed.
