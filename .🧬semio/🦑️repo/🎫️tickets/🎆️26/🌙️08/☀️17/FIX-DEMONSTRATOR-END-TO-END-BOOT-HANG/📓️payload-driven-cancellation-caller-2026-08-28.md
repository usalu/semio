# Payload-Driven Cancellation Caller

The existing actor integration caller reproduced its actual failure at03:23:17:0PASS/1FAIL177skip178,735ms,Nx1. It repeatedly called the builder's close method and expected terminal ownership without driving the original payload's admitted evidence and source-binding protocol. All12 selected RED inputs stayed stable. The production source correctly blocks this incomplete close.

The caller now consumes the released UI evidence, builder, payload and scope fixtures. It admits the actual private cancelled-input token through11 separately granted phases; drives7 source/evidence handshakes and9 registration phases through the original payload; then drives17 builder-binding phases. The source remains unconsumed and incomplete throughout cancellation. After evidence settlement the fragment has detached but builder binding is still held; only after binding settlement does the stale builder's continuation reject. The original raw response, content and field remain retained, and no native InputAck is exposed.

The language-neutral authority fixture/schema now declares that original-payload driver, detached-fragment/builder expectations, eight payload registration phases and two pool-own close phases. Remaining payload close is derived from the existing nine source-release phases plus those eight registration phases. Pool close is derived from the existing scope retirement count plus its two own transitions. No production quota or arbitrary test loop cap was increased. Strict Ajv and independent Immer conservation checks execute in the caller.

The focused caller passed1/177skip178 at03:25:18,765ms,Nx0. Its attempted pre-hash output was truncated, so this focused run is not described as a complete pre/post source capture. The subsequent full actor run completed176PASS/2FAIL178 at03:25:45,2.69s,Nx1. The remaining failures are unchanged copied-payload and copied-page-boundary paths; they require the UI-owned page/reader admission work. All22 selected broad-run source/fixture endpoints stayed stable through strict completion. The UI test file itself was not among those22 and its new beginPage tests appeared in strict output.

Renderer strict currently reports46 diagnostics: the prior41 UI-counter/private-pool/tutorial joins plus5 new UI beginPage declarations under active TDD. No Shard/output/Kernel-input diagnostic appeared. This change modifies only the actor caller and its neutral authority fixture/schema; it does not implement receiver admission, copied-page streaming, native close, browser rendering or demonstrator readiness.

Raw retained logs: actor-payload-cancellation-red-1, green-1, join-1 and strict-1 in this ticket. The runtime coordinator's separate combined UI20PASS at03:21:11 is delegated evidence, not our full renderer run.
