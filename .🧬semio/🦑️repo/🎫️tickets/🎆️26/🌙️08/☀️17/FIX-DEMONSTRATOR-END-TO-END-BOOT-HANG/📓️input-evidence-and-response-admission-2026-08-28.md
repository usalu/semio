# Input Evidence and Response Admission

## Current Ownership Slice

The next source-owned slice is the exact two-way detachment of `OwnedKernelReturnInputRelease`, the existing UI copied/cancelled token and their original fragment. UI owns its token/evidence capsule, builder/page/reader admission and same-field preallocation check. This lane owns Field/Fragment/Release state and source-side predicates. The following is a proposed contract for coordination, not source or runtime readiness.

The current chain is Field.release and Fragment.release to Release.proof, then UI token to Evidence(fragment,field,builder). Fragment also points directly to Field. Local UI receipt clearing cannot sever these source-held aliases. Current source `advance` clears the field's references after consuming a range but leaves the fragment/release/token cycle intact. Cancellation does not consume that framing range. Neither path currently supplies a cross-owner terminal observation.

## Proposed Source Contract

Reuse the existing `OwnedKernelReturnInputRelease` as the sole phaseful observation. Do not allocate another proof, use a structural callback, add a pool or change a wire format. Proposed source phases are issued, sourceDetached and settled. The existing three fields become nullable fragment, nullable proof and kind, with one new phase field. Fragment keeps its six-field shape but its field backlink becomes nullable. Field keeps its twelve-field shape and retains its original fragment/release until the handshake completes.

Source calls proposed for exact naming agreement:

- `field.detachInputRelease(release,proof,grant)` accepts only its exact installed release, original fragment and original UI token. The UI must already privately hold this exact release. A copied range must have reached its actual consumed boundary, or the field must have entered close and permanently abandoned further range reads; a cancelled release already permanently closes that range. One64-byte grant clears the release's variable backlinks and the fragment's field backlink, then marks sourceDetached. This is not raw-page retirement or InputAck.
- `OwnedKernelReturnInputRelease.matchesSourceDetached(release,field,proof)` requires the actual field-held observation and the UI token's direct private original-observation match. It is recovery observation after a wrapper throws, not a mutable replay.
- `field.settleInputRelease(release,proof,grant)` accepts only sourceDetached plus UI's direct private proof that its original evidence no longer retains fragment/field/builder and still holds that same observation. One later64-byte grant clears source Field/Fragment release slots and marks settled. Mutating replay rejects.
- `OwnedKernelReturnInputRelease.matchesSettled(release,proof)` requires settled scalar-only state and the UI token's still-held exact observation. UI then clears its observation before its capsule root/record refund. A branded settled object alone is insufficient.

Required UI-side predicates, spelling proposed: existing Copied/Cancelled classes each expose `matchesRelease(token,release)` and `matchesSourceDetached(token,release)`. The first is private original-observation identity, installed only after the existing issued-release verifier accepts the exact token. The second additionally requires that token's actual dependency-detached phase. No public setter/factory or callback may establish either. UI decides its internal capsule/parent phases and charges before implementation.

The source release constructor must install in both original source slots before finalization. A finalizer or wrapper fault retains that exact observation and first original raw fault; it cannot mint a replacement. Replay of the existing release call may recover the same issued observation. Refusal keeps original ownership. The proposal does not claim arbitrary-fault cleanup.

Logical fixed source inventory under the existing16-byte-record/eight-byte-field model: Release3-to4 fields40-to48 bytes; Fragment remains6 fields64 bytes; Field remains12 fields112 bytes. The initial proposal omitted the record overhead on the first two totals; this correction is arithmetic, not a changed layout. These are unadmitted source inventories, not a new granted quota. Original variable field metadata, parser, output and raw backing remain separately unadmitted. UI evidence/capsule/controller metadata is explicitly additional. No refund is authorized by this inventory.

Tests to declare before source edits: copied-not-consumed refusal; copied-consumed detachment; partial-copy cancellation; zero-length range; short grants; exact foreign token/fragment/field/observation rejection; operation revocation; after-actual-release/detach/settle wrapper faults; constructor finalizer loss; next-range refusal before settlement; settled replay/no ABA; source and UI aliases remain charged through their distinct detach phases. Positive tests must use the real UI-minted proof, not a structural substitute.

## Independent Response Work

The released neutral mandatory admission cells permit a separate actual-output-roster reservation slice. The current `CapturedReturn` has16 fields and allocates `OwnedActorTurnOutputs` before any neutral resource admission. `sendCapturedReturn` still decodes a whole result from a Uint8Array, and the producer does not yet mount the canonical one-response credit. The existing metadata schema declares only floors: roster176/1/1, pending560/4/4, framing1424/9/9. It excludes new cells, records, fault containment, fixed response backing4161, transfer ownership and future controllers. These values cannot fund the whole captured-return state.

The bounded independent slice is schema-first adoption of a same-ledger, strongly registered original output-roster admission controller under the existing captured instance. It must retain prepared cells and exact rejected or wrapper-lost results, install the real output shell before fallible finalization, and expose no dispatch permission before its record is installed. Its close proof must distinguish an empty/unposted roster from any posted or unknown response. No public neutral record capability or arbitrary terminal predicate is acceptable. The exact controller census and parent bootstrap need review before code, rather than borrowing the existing UI controller's208 bytes.

This can progress without UI builder readiness, but it does not justify mounting the old whole-result path. Live dispatch still requires the sole canonical binary response grammar, pre-reserved receiver backing and metadata, exact original activation/worker/request credit, producer transfer ownership and finite first-violation containment. No InputAck, no-response retirement, complete source cleanup, live renderer or fresh guest proof exists from this review.

## Evidence and Preservation

The aligned source methods are now implemented against the actual UI private predicates. The second full actor foundation run actually produced155PASS/4FAIL159,3.48s,start02:09:57,Nx1, with all nine selected inputs stable. All three new source evidence groups pass: strict neutral traces, actual AST inventory and real private-field foreign/proxy/no-getter refusal. The four remaining actor failures now reach UI's phased builder admission and incorrectly expect an immediately returned builder; the old undefined `admitBuilder` failure is superseded for this capture. No positive copied/cancelled capsule or source release-constructor fault test has executed yet. Those require UI's charged positive evidence API, not a fabricated proof.

Current source input `fcdb0a7be3c136cdda47db58cb89faa54f29b528adb7b1c78610f6b5edb01c73`; Shard remains50c4600c; UI/pages `039714deecc830cfb7dc0ff09571ec5a9d9ebdb8bcc9e96f36d199e5a939e321` / `55a82fa2b2465fde0cc7af1b80b55913b4904f4f10bfd806d2eda83ef5e0f029`; neutral and four declaration files remain as captured below. The first155/4 run at02:09:04 used sourcee75bfd97; subsequent settled-observation recovery was corrected to accept the UI's same original still-held observation through its source-settled phase and rerun. No stable-source claim is made for that earlier first run.

Renderer strict separately executed41 diagnostics:15 remaining UI resident counter references,19 UI fixture private-pool constructors and7tutorial joins. No Shard, Kernel input or newly aligned evidence predicate diagnostic remains. Logs: `🧪️actor-input-evidence-foundation-2.log` and `🧪️actor-input-evidence-strict-1.log`. This is not a complete typecheck pass.

Source behavior now retains Field/Fragment slots after semantic range consumption. Detach requires the original issued release and UI's private observation; it clears the source's proof/fragment backlinks and the fragment's field backlink. Settlement requires the same UI evidence's dependency-detached phase and only then clears source slots. The same Release object is reused; mutating replay refuses, and settled observation additionally requires UI's exact still-held release. Release construction installs both source slots before finalization and records the first original raw fault. This is current source behavior, not positive runtime proof or source/raw-memory admission.

The initial approved schema-first packet is at Kernel return/content/input/`🧾️release`: contract schema, contract, fixture schema and fixture. Three neutral traces cover copied, cancelled and copied-then-close range detachment. Strict Ajv validates both declarations; Immer independently reduces every alias/phase transition, and BigInt independently verifies the16+8*n logical inventory. The source constructor and positive private UI handoff were unimplemented at the initial RED checkpoint below; the newer source foundation above supersedes only that declaration gap.

The existing registered full actor gate actually ran153PASS/6FAIL159, nine files,3.00s,start02:01:11,Nx1. All nine selected source/schema inputs were stable. One new neutral evidence test passed. Two new source tests genuinely failed: the actual Release still has three fields rather than the declared fourth phase, and all four proposed source methods are absent. These are source/API preimplementation REDs, not positive runtime ownership evidence. The other four failures are UI's in-progress builder join, now `ReferenceError: admitBuilder is not defined`; this supersedes their earlier removed-counter failure for this exact capture. No UI-owned repair was attempted here.

Retained log: `🧪️actor-input-evidence-red-1.log`. Stable Shard/input/UI/neutral/pages hashes respectively: `50c4600c3c830b38887934e1eb70ee4fe872787f0ce3dc6960d21cf2ee249e52`, `66be4f85b09079dd920c3854f7ad9286dcf9b556c7b4a6a3e8884dc78df91e82`, `8488b01c0734b58fea8ed3a96d21ef103f109df97a6ad1b7daa7f23ff36758ad`, `6957021837e6e5c731cec0530bb109694cff4ef6b25a9ecb0a9f02f37c1c8840`, `1d40f7737fd786ab4dd0e48f151553807265b0c0cd7acebe2957eb5ecdeae55c`. Release contract-schema/contract/fixture-schema/fixture hashes: `0b2853c860eb05b957520dd97ff24a4902f69e95c66598d11c7add7f470c4098`, `2f964e47d6a673ef6f2cb209e8d5071611680de8bfebe4cc919b1f6d3bb61178`, `e8c7dfa9e77c0d4cb73d2345e1111a5c3e5552ac9869989aa570e2170c3bc823`, `644a09582afc6a48341fd03ff043642f191db01f4814125f44d793e716fac05e`.

The runtime owner additionally identified proposed1536 logical bytes per256-byte UI destination page: retaining every page of an8MiB field would require48MiB for pages alone, exceeding the32MiB shared composition before other owners. This source design retains next-range settlement gating but does not require whole-field accumulation. UI owns a bounded incremental semantic-consumption/retirement design or a demonstrated smaller schema maximum; no quota increase or whole-field fit claim is made here.

The prior source/UI results and their limits remain in `📓️field-payload-runtime-2026-08-28.md`. All ticket inputs, logs and caches are retained; no native compiler, cleanup, generated publication or WGPU repin was started.
