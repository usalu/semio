# Pool Admission Cell Integration Plan

## Executed Shard Adoption Checkpoint

**Superseding controller-accounting checkpoint:** the first green below had an actual early-refund flaw, reproduced by a later10/1RED. The separate original controller record/cell is now mounted in the same ledger and the strengthened focused gate passed11/136skip147,1.68s,start00:46:38, with seven selected inputs stable. Controller208 plus record264 plus cell296 remains charged at768/10/10 after healthy pool close and ordinary `disposeAll`; the pool's own UI192 plus record264 plus cell296 is separate752/13/13. No controller final-refund API or surrogate witness was introduced. Full evidence, exact phases and source hashes: [Retained Controller Root Contract](./📓️retained-controller-root-contract-2026-08-28.md). The following six-field/912-byte text is preserved historical evidence, not the current inventory.

The following planning sections describe the earlier R23 boundary. The actual R27 mandatory-cell resource contract was subsequently read completely and adopted in the original Shard controller. The refreshed schema-first test packet ran RED with 1 pass / 8 failures / 136 skipped / 145 total, 2.41 seconds, start 00:33:42. After implementation the same focused command passed 9 / 136 skipped / 145 total, 1.13 seconds, start 00:35:31. Exact retained logs are `🧪️actor-cell-handoff-red-1.log` and `🧪️actor-cell-handoff-green-1.log`. This is an executed focused composition result, not a full actor, UI, live inbox, native guest, timing or six-app pass.

The original Shard now holds six private fields, including its admission cell. The existing actor logical metadata model is 64 bytes per record plus 16 per field, so its source-checked controller envelope is 160 bytes / one slot / one owner. TypeScript's actual syntax tree independently enumerates those six fields. Together with the current exported UI pool 192/4/4, neutral record intrinsic 264/3/3, and separate admission cell 296/6/6, the complete admitted packet is 912 bytes / 14 slots / 14 owners. Strict Ajv validates the neutral declaration and Immer independently sums the envelopes. This is logical accounting, not a physical heap certificate; no production capacity changed.

Preparation has six separately granted transitions: bootstrap296, exact cell observation64, claim64, claim observation64, record construction264, and typed original-result observation64. Only a later zero-work prepared result permits the UI constructor. The cell is stored before claim removes the bootstrap pointer. Real wrappers call the original bootstrap, claim or record method and then throw each of null, undefined, false, zero and an unread object with an 8193-byte descendant. Tests recover the exact original cell and record; no resource admission is replayed, no replacement is allocated, and an unrelated peer still admits and fully closes.

`captureUiResidentPoolFault(error)` retains the first exact raw value, accepts same-value observation, and rethrows a distinct subsequent value to preserve caller ownership. The UI constructor owner has this concrete signature for pre-install failure handoff. The later cell handoff spends its own 64-byte grant; neither the catch nor a successful intrinsic close certifies unknown-error retirement. Healthy close joins exact UI witness, original record detach, intrinsic completion, separate record observation, 64-byte result/resource alias detach, 296-byte cell retirement and final 64-byte controller unlink. Fault-held cells remain charged at 296/6/6 with no terminal witness after intrinsic cleanup. The actual original fault is not cleared.

The nine groups cover required ledger identity, source inventory and phased preparation, private UI witness rejection/replay, canonical constructor rejection, parent-driven close, actual close-transition exceptions, actual detachment-getter exceptions, fifteen wrapper-after-return cases, and the resource/cell alias frontier. Closed resource `terminalIsEmpty` is explicitly false before alias detach and true afterward. The original cell's private retirement witness must exist before final parent unlink.

Post-run source hashes: Shard `022b80e33e71a7ddf4397c6d9f61d08546a3d709d376c0779972b1ea79e16e2b`; composition schema `7f8c33acb1cad7c74a7642c9ef85e4c69559c84e5bf63865af582c5373ca229a`; fixture `8b48bc44175e3fb420493b684d0c234b28cd31abc420c459ace2ee36eb973bb8`. These were captured after the run, not misrepresented as pre/post stability. A fresh renderer strict gate is running separately. Neutral child-cell forward quarantine and UI child/payload adoption remain peer-owned live work; this focused packet does not certify those gaps.

No native compilation, browser-policy workaround, generated publication, source restoration or evidence cleanup occurred.

### Renderer Caller Join and Current Strict Boundary

The two actual RendererResidentComposition tests reproduced the old three-argument resource call failure: 0 pass / 2 failures / 660 skipped / 662 total, 4.39 seconds, start 00:36:46. Both callers now prepare, retain and claim their exact cells before resource admission, and close intrinsic records, eight-byte aliases and cells separately. The unchanged shared ledger rejects new bootstrap after close. The same focused command then passed 2 / 660 skipped / 662 total, 6.05 seconds, start 00:37:45. Logs are `🧪️renderer-cell-caller-red-1.log` and `🧪️renderer-cell-caller-green-1.log`. Two independent neutral fixture records now retain 1456 bytes / 24 slots / 24 owners, including both admission cells; this does not change the production 32MiB capacity. Immer validates the conservation arithmetic.

Post-run hashes are PluginRuntime `eab0e5365916a82d78fcf89cb4dcb996e0b6dc14a32a550270365871439dc2f0` and renderer fixture `448cd4f9a53ebfb2161b24acbd99908e130743316ef6b57f2930f653f56c88d1`. Only the two tests changed in PluginRuntime; no live scheduler, return producer or guest initialization was mounted.

The separately executed strict gate before those two caller edits reported 54 diagnostics: tutorial7, four actor fixture private-pool constructors, 22 UI pool migration fields, 19 UI fixture private-pool constructors, and the two now-repaired renderer resource arities. No Shard cell/controller or new test typing diagnostic appeared. `🧪️actor-cell-handoff-strict-1.log` is the exact retained result, not a current full strict pass. A further cancellation-frontier test is now staged against the exact caller source, including a closed shared ledger that refuses before creating any admission.

## Read Source Boundary

The actual R23 neutral bootstrap source now exposes `prepareAdmission(consumer, partition, grant)`, `preparedAdmission(consumer)` and `claimAdmission(consumer, cell, grant)`. `OwnedResidentAdmission` is privately minted and linked into the original ledger, including its one pending bootstrap slot, before result/witness/facade finalization. A failed bootstrap retains its exact original fault and cannot be claimed. A separate close step releases the pending bootstrap pointer without freeing a faulted cell's charge. `retainFailure(error, grant)` preserves the first exact thrown value and refuses a different value without claiming its ownership.

All five resource methods still have the earlier return-only signatures in this read. Mandatory admission-cell parameters, the resource back-links, typed recovery access and result/resource alias retirement remain an unreleased source join owned by UI. This document is integration planning, not implemented Shard admission. The currently tested 592/8/8 record-only price is historical to that API; the proposed 296/6/6 cell and eight-byte resource link must not be silently added as unverified final accounting.

## Original Shard Phases

The existing Shard controller is the exact neutral consumer identity; a foreign ledger must still refuse before any cell allocation. Preparation, private cell observation, claim, resource admission and exact resource-result observation need separate grants. The original controller stores the cell before the claim can remove the bootstrap pointer. It must not spend one grant across bootstrap, pool record construction and pool facade finalization.

A wrapper that calls the real bootstrap then throws leaves the cell in `preparedAdmission(originalShard)`. The parent must retain the actual thrown value, recover only that original pending cell and hand the same value over on a later admitted turn. A wrapper that throws after claim cannot strand the cell because the Shard already owns it. A wrapper that throws after record construction must recover the original record from this stored cell, never re-run admission, substitute a fresh record or close the entire shared ledger.

The source-facing resource result must give typed exact recovery. Current `OwnedResidentAdmissionResult.root` is `object | null`; consumers should not certify a record with a structural cast or borrow unrelated factory authority. The final resource packet should expose a private-brand typed record accessor or equivalent canonical type verifier, preserving the exact cell/result/root pair. This request is routed to UI through the runtime coordinator; no competing neutral declaration is added here.

## Close and Fault Boundaries

The original Shard must close a recovered unused record independently of live peers. An installed UI pool still requires its actual private terminal witness before record detachment. Neutral intrinsic retirement, resource-to-cell alias detachment, cell-to-resource result release, cell retirement and final controller unlink are distinct phases; a successful resource close cannot erase the charged cell or its original fault. The final concrete API must define these steps before mounting.

All new private Shard fields need source-derived controller metadata; adding a cell reference changes the prior five-field controller inventory. Published record/cell/resource metadata must be consumed rather than guessed. Fault cells must stay charged after pending-pointer release. Existing unknown exceptions and whole returned output roots are not retired by a format-valid result or a private neutral storage witness.

No native compilation, live inbox, InputAck, guest instantiation, output publication, compatibility route or cleanup is performed by this planning packet.
