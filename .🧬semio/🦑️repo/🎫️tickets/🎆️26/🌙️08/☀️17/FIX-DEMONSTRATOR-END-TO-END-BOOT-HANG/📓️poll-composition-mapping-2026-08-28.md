# Required Poll Composition Mapping

## Scope

The isolated TypeScript mapper is `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/📥️poll/🏘️composition/🟦️component.ts`. It consumes the actual four canonical schema/contract/fixture files under common Kernel `📥️poll/🏘️composition`, including the newly declared exact opening cancellation row. There is no new wire layout, compatibility union, default capacity, guest initialization export or production producer mount.

`pluginPollCompositionToWit(value)` maps the required existing field's six checked safe integers to WIT BigInts. `pluginPollCompositionFromWit(value)` rejects negative, non-BigInt and above-MAX_SAFE WIT values before conversion. Both preserve nested bytes/slots/owners/control fields and reject a control partition exceeding total on any axis. The repository-owned `ResidentCapacity` type is reused. Runtime code has no external library dependency.

The mapping selects only fixed own data fields. It rejects missing fields and accessors without invoking their getters. It does not enumerate or retire unknown wrappers, prove private owner identity or establish hostile Proxy provenance. All original roots, parser/output metadata and allocation admission remain caller-owned. The two frozen output records are scalar snapshots, not a guest memory permit. Identical numeric configuration does not authorize a different activation or admitted guest owner.

## Executed Tests

The schema-first missing-functions RED ran all four selected tests: 0 passed / 4 failed / 68 skipped / 72 total, 2.57 s, start 00:10:07. Three tests reached the missing mapper; the fourth reported the same absent exports through actual TypeScript semantic diagnostics. Log `🧪️plugin-poll-composition-red-1.log`.

The implementation GREEN passed 4 / 68 skipped / 72 total in 2.43 s, start 00:10:54, log `🧪️plugin-poll-composition-green-1.log`. Strict Ajv validates the original declaration and examples. Lodash independently resolves and constructs the declared field order; Node Buffer independently writes/reads all six u64 words at zero, split and MAX_SAFE values. The 48-byte Buffer exists only in tests and is not an alternate poll ABI. Every scalar position is tested against unsafe/noninteger/wrong-dialect values and the three cross-partition refusals. Actual module syntactic and semantic TypeScript diagnostics are empty.

A full OS-dev regression is running separately; no full result is inferred from these four tests. The independent native/runtime implementation, canonical poll mounting, pre-instantiation admitted guest owner and all producer guards remain pending in the runtime lane. No browser/Wasm/all-app content or eight-millisecond claim is made.

## Source Checkpoint

Post-focused hashes: mapper `a58f9d31fe064eebc699e35fdc10989849f308a0389dc52b7971b344e8b3638f`; common schema `25b59b7eed494629d98ff57dc04bddf5e430b92ef465c5555b6af608f94aae5d`; contract `bd762811704901d0b027d28fe7e83281ff928402a7b2b237c006e4a6c22454dd`; fixture `0252fd3e7e64dcb5b05ccad95291e4b2df06284dca69e37a9e7dc94174bff4fe`; fixture schema `064d26d9f64509e715d9980a4601825f54202db3a8c76723b311b03005d5efd0`; OS-dev test config `d1a3dae4b4586455153ffc41708a4a7ba59a2d5575370df395a5b67850d79416`.

The existing OS-dev test target collects the new in-source module once. Taxonomy was asked to register the exact focused launch command at the next checked adjacent slot 400.96 without executing it. All source inputs, logs, caches and active-ticket evidence are preserved.
