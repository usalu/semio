# Plugin Return WIT Mapping

## Exact Scope

The actual type-only return-page WIT interface was read before implementation. Plugin/📤️return/🟦️component.ts exports pluginReturnDriveToWit and encodePluginReturnResult. The first maps a canonical drive to exact WIT nesting and bigint transport request fields. The second validates selected WIT data fields and encodes the single canonical fixed actor result. Protocol fault accepts only malformed-control or mixed-control; positive u64 authority, safe transport IDs, exact captured activation, control/fault contradictions and fixed page padding remain checked.

No original WIT result is released by these functions. Unknown wrappers remain owned by the caller. Selected data-property reads avoid accessor invocation and whole-key enumeration but do not prove hostile Proxy safety, immutable input, physical allocation admission, producer provenance or retirement. Page encoding still needs the canonical codec's admitted fixed allocations. No whole semantic turn is copied or decoded.

The existing generated bridge and poll signature were not changed. These functions are not mounted and are not a compatibility branch. Actual native retained-source ownership and generated invocation must be joined atomically later.

## Executed Tests

- 🧪️plugin-return-wit-red-1.log: four failures / 62 skipped / 66, 6.11 s, start 21:01:06. Three missing-helper failures; one test-only jsdom Buffer realm mismatch.
- 🧪️plugin-return-wit-red-2.log: all four missing-helper failures, 3.88 s, start 21:02:23, after converting fixture bytes to the actual Uint8Array realm without relaxing production validation.
- 🧪️plugin-return-wit-green-1.log: four passes / 62 skipped / 66, 3.64 s, start 21:03:31.
- 🧪️plugin-return-wit-full-1.log: full OS-dev test-long 67/67, two files, 39.33 s, start 21:07:44. Includes the fifth actual mapping-module strict syntactic/semantic TypeScript check and existing 62 tooling/controlled generated-worker tests. The module-only compiler check does not certify every dependency or project.
- 🧪️plugin-return-wit-strict-1.log: renderer strict remains red with exactly the seven known tutorial diagnostics. The earlier builder and UI fixture errors are clear.

Shared language-neutral drive/result/page vectors are consumed. Independent webassemblyjs LEB128 and Node Buffer reproduce the drive fields and fixed page bytes; strict Ajv validates canonical result values. Tests cover all six drives, thirteen fixed results, both page envelope lengths, contradiction/refusal enums, request narrowing, activation mismatch, noncanonical wrappers and untouched unknown roots.

## Source Boundary

Mapping TS: 49dca19b07f32b15bd4c6bf397f6a5da7830f2eff53595cadd12c4bd2f6c3756.

OS-dev config: 3ae75e1e4d07e5f10b04b02c9ac6d24c0c0f5398e1b0bc95c3017bbf1d163345.

Taxonomy registered the exact PluginReturnWit focused command at launch order 400.7 without running it. No materializer, generated output or WGPU pin changed here.

## Remaining Work

The private Field/Fragment/Release classes must derive from the actual original Shard return owner, not public numbers, an arbitrary output queue or a structural reader. Dedicated captured retirement authority must outlive operation revocation while retaining exact activation, worker incarnation and original native API. Exact input ownership, shared UI resident admission, native source-before-WIT retention, live renderer mounting and all six fresh app checks remain open.
