# Original Receiver Admission

## Current Executed Boundary

The per-response record is admitted under the original captured return. Its six laws pass in our run and in the runtime coordinator's independent03:15:29 replay. The record funds Slot, facade and outcome only. The current `send` still creates a Promise, its resolving functions, PendingEntry, pending Map/Set registrations and outbound wrapper before posting. `handleMessage` accesses an object discriminator and request identifier before capturing the original wrapper. These are source findings, not a bounded receiver or live transport proof.

The canonical replacement response already has one binary grammar: magic73727201, result/fixed-fault discriminator, activation ULEB64, actual dispatched request ULEB53, and one canonical result or fixed fault. Its maximum is4161 bytes. The existing response/result framing classes can inspect indexed bytes without making a payload copy. Their1424-byte logical fixed census is separate from raw capacity, transport registration and domain state. Native return origin and the current control request are distinct authorities.

## Proposed Canonical Execution Model

Use the existing scheduled actor turn as an event-driven request owner. Do not create a new Promise, resolving closures or async reaction chain for each canonical returned request. A typed submission step moves the already-admitted original request to posted; the original worker callback captures its response; later granted turns advance parsing and expose a fixed report. Ordinary unrelated actor requests remain outside this replacement. There must be no object-result/binary-result compatibility union for canonical returned requests.

The original output Slot retains the concrete request before constructor finalization. Its declared record envelope must include every added Slot link, request field, fixed parser/projection and dispatch-registration role before those objects are allocated. The exact same composition ledger funds the neutral owner, admission cells and maximum external backing. The output's current448-byte domain subset cannot silently fund this larger graph. An empty/cancelled Slot, a posted message, routing removal or a detached transfer is not a refund witness.

Submit authority remains typed: execute/retry requires the original active operation lease; poll/cancel/inputAck/retiredAck requires the captured old return/worker authority. No arbitrary Event[] plus allowClosed, no actor-name lookup and no synthesized native return identity. The eventual scheduler cutover must preserve the original submitted event/input owner; this report does not grant a raw events array bounded ingress authority.

## Inbox Correlation Boundary To Settle Before Source Mount

Superseding mixed-ingress review: [Shared Worker Inbox Ownership](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/FIX-DEMONSTRATOR-END-TO-END-BOOT-HANG/📓️shared-worker-inbox-ownership-2026-08-28.md) records the actual producer/consumer matrix, source-bound shell minima, generated concurrent-traffic trace and all authored caller categories. The single-canonical-return policy below is historical, not an approved routing solution. It cannot identify an ordinary reply, heartbeat, host-effect frame, platform error or malformed root. No worker-wide serialization or receiver mount is approved; exact pre-discrimination ownership and each traffic class's transfer remain required.

An incoming whole buffer does not identify its pending owner until its header is read. Parsing before original-root capture repeats the existing ownership gap. A per-request reply channel would introduce another protocol and is excluded. A worker-level original ingress slot is therefore required before dispatch.

The preferred concrete policy is at most one outstanding canonical returned request per worker incarnation. Other actor work is not granted new authority by that policy. The private original worker slot points at the exact admitted pending request before post, so capture can precede all header reads and pending lookup. The request remains registered after a post wrapper throws, since a throwing post does not prove non-delivery. Header correlation then checks exact activation plus actual dispatched sequence; protocolFault remains correlated by that transport header without inventing native identity.

The worker also needs one separately charged original protocol-violation slot. A first foreign, duplicate, oversized or arbitrary response seals further admissions and retains its original root without replacing the still-outstanding expected response. A later genuine credited reply may enter retirement custody without live publication. Arbitrary uncredited flooding beyond this finite contract is not claimed bounded. The worker ingress/quarantine controller must retain its own charge until genuine original worker retirement; it cannot borrow a request record that later refunds.

This serialization and worker-controller ownership are a concrete proposal for coordinator review, not mounted behavior or a claim that the pre-existing Shard/worker construction is admitted. The fixed worker controller inventory and exact pending request inventory must be schema-bound before code. No new wire identifier, WIT ABI, capacity increase, shared output publication or native change is proposed.

## Neutral Raw Custody Join

The released neutral primitives require exact retained admission cells for beginOwner and reserveExternalBacking. External maximum4161 costs4489 bytes before its cell, neutral owner, parser and domain metadata. beginReceive consumes its own64-byte phase before post; adoptTransferred consumes a separate128-byte phase and performs real intrinsic transfer/detachment. The resulting private custody receipt is intrinsic storage evidence only. It is not worker identity, delivery success, semantic field consumption or native InputAck.

Closing/faulted posted slots still receive late backing into retirement custody. No-response after beginReceive stays blocked without an exact original transport terminal witness; neutral cancellation cannot discharge it. Semantic readers must remain registered under the original response owner and detach before raw scrub/refund. Unknown wrappers and first arbitrary failures remain strongly retained and charged; this design does not invent a generic destructor for arbitrary JavaScript values.

## Required RED/GREEN Packet

Before mounting, declare strict language-neutral phase/conservation/identity vectors, check arithmetic with an independent implementation, and bind fixed fields to actual TypeScript AST. Exercise zero/short grants, shared-ledger saturation, constructor/finalizer faults before and after actual mutation, original post then throw, same-name activation reuse, same-slot worker replacement, foreign/duplicate response, malformed header, closed/faulted late custody and exact pending-root retention. Use real MessageChannel transfer for the valid backing path, not instanceof or format validity as provenance. Prove no post before all receiver/ingress registrations and the receive fence, and no premature refund when a response or semantic reader remains owned.

The immediate narrow caller follow-through is separate: the actor cancellation test must drive released UI evidence and builder retirement through its original payload using the canonical fixture grants. The two copied-page tests remain RED until UI page/reader admission is actually implemented. Neither caller repair nor this receiver declaration establishes demonstrator content.
