# Original Activation Composition Ownership

## Declared Boundary Before Implementation

The UI scope admission must distinguish two genuine activation leases from different Shard clients even when both clients share one ledger, actor name and numeric generation. The existing frozen structural activation facade has no private original-client identity. The planned `ShardClient.matchesActivation(client, activation)` checks only privately minted original ownership. It does not call `assertActive`, resolve the current actor map, inspect a current route, or treat matching metadata as authority. Existing operation validity remains independently enforced before dispatch and after settlement.

The existing public `ShardActorActivationLease` shape stays unchanged. One privately minted implementation replaces its plain object. Its actual per-lease inventory is the four existing public fields plus one new private original-client field; the constructor, prototype, mint token and factory/checker functions are module-level support, not per-lease extra roots. A TypeScript AST census will check this inventory and the exact declared shape. No activation allocation or metadata is presently funded by the separately admitted pool controller record. This packet will not claim live activation-construction admission, constructor-fault cleanup, memory release, or a whole-Shard terminal witness.

The schema-first neutral cases will cover own/foreign clients sharing a ledger, same-name reactivation, route reassignment and same-slot worker replacement. Original ownership must survive revocation while ordinary operations remain revoked. Structural/prototype/proxy forgeries and constructor replay without the private mint must reject without invoking public getters or proxy traps. Strict Ajv and an independent Immer state oracle will validate the same declared outcomes.

The runtime coordinator released its imported-source hold after its independently executed five scope groups. This lane then authored the two missing-matcher cases before implementation: `ShardResidentComposition` actually failed11PASS/2FAIL/137skip150,1.44s,start01:11:46; both failures were the absent `matchesActivation` method. The implemented direct private original-client check passed13/137skip150,863ms,start01:13:00, Nx exit0. Logs: `🧪️activation-composition-red-1.log` and `🧪️activation-composition-green-1.log`.

Five selected inputs were stable before and after that GREEN: Shard `8e1e28ced5f2cf77916932a5e7a98d08dacce46bbe4e6453cd00b538944222c1`; composition schema `3c6cc4246e1e6a841d0158e103056bc2706843d5866294791e8e5a77affafcc8`; composition fixture `e93ea7a4f1ad39703b126a9c9847cc63f9c1afce9ae062cb2eb453309bf4827f`; UI resident `ca902d48be0ea17a8e0cd04768fda8a58441290861d1022f282d80c1b3028620`; neutral resident `6957021837e6e5c731cec0530bb109694cff4ef6b25a9ecb0a9f02f37c1c8840`.

The concrete callable is `ShardClient.matchesActivation(client:unknown,activation:unknown): activation is ShardActorActivationLease`. It rejects prototype, structural, proxy and revoked-proxy activation forgeries, as well as foreign/proxy clients, without invoking their traps. Reflected construction with a foreign mint fails. Actual same-name reactivation, exclusive route reassignment, same-slot worker replacement and disposal keep original composition identity while old command turns refuse without another post. These are controlled transport/source tests, not live guest or memory admission proof. The separate four caller join consumes the released UI nine-phase scope fixture; payload construction remains unreleased.
