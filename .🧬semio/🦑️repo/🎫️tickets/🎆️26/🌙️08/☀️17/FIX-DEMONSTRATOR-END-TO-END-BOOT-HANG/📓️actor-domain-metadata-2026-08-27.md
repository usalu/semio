# Actor Domain Metadata And Construction Ownership

The complete approved shared-resident-domain-record-contract was read before work. The actor will reuse its existing OwnedActorTurnOutputs/OwnedActorTurnOutput roster, not introduce a parallel test-only pending shell. Private neutral record capabilities remain inside typed composition; no public record getter, structural retirement callback or raw input acknowledgement shortcut is permitted.

## Actual Constructor Regression

The existing reserve method constructed and finalized an output before linking its slot into the strong typed roster. A finalizer that throws before or after the actual Object.freeze call therefore left the constructed shell outside that roster.

The language-neutral output fixture now declares both fault boundaries, registration/install/finalization ordering, retained slot count, next sequence, capability visibility and exact empty-cancellation/replay behavior. A real in-source regression calls the production queue and injects the finalizer fault. RED1 actually produced four passes and one failure, 130 skipped / 135 total, nine files, 1.01 s, start 23:30:08: the parent had zero slots instead of one after the shell was constructed.

The narrow repair links the slot and commits its sequence/count before entering the output constructor. The constructor installs its exact handle into that already retained slot before fallible finalization. The original pending owner is recoverable through the existing roster after either injected fault. No neutral metadata admission or live transport was added by this ordering repair.

GREEN1 then actually passed all five selected output laws, 130 skipped / 135 total, nine files, 600 ms, start 23:31:27. The later full actor gate passed all 136 tests in nine files, 4.57 s, start 23:37:02, after the additional source inventory test below.

## Source-Bound Metadata Inventory

The new actor response credit metadata schema and fixture declare a logical 64-byte record plus 16-byte field model. TypeScript's independent AST verifies every current fixed output roster, pending-entry, output-slot, output-handle, outcome and two framing-parser field. An independent BigInt calculation verifies current source floors of roster 176 bytes / one slot / one owner; pending 560 / four / four; framing 1424 / nine / nine. Executing the real response parser over canonical vectors observes the maximum seven-object / 21-field frozen projection graph, priced separately from both parsers. None of these values is a final live admission envelope or measured JavaScript heap size.

The packet separately declares construction ordering, exact private-witness refusal laws and install/detach wrapper-fault recovery. Those domain-metadata refusal laws are authored, not yet executed against mounted record consumers. The neutral record overhead, 4161-byte raw backing, typed terminal witnesses, fixed fault/emergency records, future constructor state and live diagnostics are explicitly not hidden in these source floors.

The first metadata gate executed ten selected passes and one strict-TypeScript failure: the new AST fixture lost type narrowing and its Object.freeze spy accepted unknown as object. Both test typing errors were corrected without assertions or production changes. The full 136-test result includes the actual response-module strict semantic check.

## Remaining Typed Terminal Work

Existing cancelEmpty only withdraws an unused output slot. It is not a domain retirement witness: immutable owner aliases and all returned response/outcome roots still need the exact typed terminal join. Metadata admission and private detach/refund integration remain under the concrete neutral API release. The pending/parser/projection schemas being authored must separately charge their fixed records and preserve source roots through constructor, install and detach failures.
