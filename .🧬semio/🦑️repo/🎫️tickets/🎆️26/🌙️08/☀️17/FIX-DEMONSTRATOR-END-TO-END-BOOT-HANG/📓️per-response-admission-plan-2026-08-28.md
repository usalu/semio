# Per-Response Admission Under The Original Return

## Next Concrete Boundary

The current output roster retains response roots and original faults, but `reserve()` allocates each Slot and facade without a resident record. The original1280-byte return reservation covers only its instance role, CapturedReturn, roster and facade. It does not fund any output, Promise, receiver backing or parser. The output parent-fault repair is verified separately; it does not close this admission gap.

The next schema-first slice will make the existing output reservation phased and mandatory, under the exact ledger already held by the original Shard. No second ledger, transport format, WIT ABI or whole-result live mount is introduced. The public captured-return facade will expose a bounded `reserveResponse(grant)` step; ordinary execute/retry/poll/cancel dispatch must consume its original privately held ready slot and refuse before post when no slot is ready. No arbitrary output argument or callback grants dispatch authority. All authored controlled callers must adopt this required phase sequence.

## Original Parent And Recoverable Construction

The existing OwnedActorTurnOutputs roster gains five fixed fields: exact ledger, admission cell, admission record, admission phase and first raw admission fault. These fields are charged in the enclosing return reservation before the roster exists. The actual roster is the admission consumer and record shell, installed before a Slot or output facade is allocated. It is never a structural completion callback.

The controller captures the prepared cell before claim and recovers resource results through `cell.result.record`. Each consumed child phase returns immediately. Prepare/claim/reserve/install followed by a wrapper throw keeps the same original roster, cell, record and first raw fault. Identity-only matchesShell is used for recovery; matchesLiveShell gates forward child construction. Closing at any prefix cannot construct another Slot or publish a ready handle.

After the record is installed, construct and link the Slot into the original roster before any external finalizer, copying the exact cell/record into two new Slot fields. Construct the output facade into that Slot before freeze. A separate publication observation may expose the same handle. A lost final return is recovered from the actual retained tail and phase, not by allocating another Slot. Sequence increments exactly once when that Slot is linked. A fault-held admission fences this controller; no retry creates a replacement identity.

## Declared Inventory To Validate Against Source

The actor model remains64 bytes per fixed record plus16 per declared field. It is a logical charge, not a measured JavaScript heap limit.

| Role | Proposed Fields | Logical Bytes | Slots / Owners |
| --- | ---: | ---: | ---: |
| Existing roster, including five admission fields | 12 | 256 | 1 / 1 |
| One Slot, including exact cell and record | 13 | 272 | 1 / 1 |
| One output facade | 1 | 80 | 1 / 1 |
| One outcome record reserved before later construction | 2 | 96 | 1 / 1 |
| Per-output domain subset | 16 | 448 | 3 / 3 |

The enclosing return subset therefore increases by80, from1280 to1360/13/13. One output's domain448 plus neutral record264 and cell296 is1008/12/12. The existing pending census becomes608 only as a source inventory: it also contains PendingEntry160, which is NOT admitted by this448 subset. The earlier640 proposal was an arithmetic error corrected before acceptance. Raw4161, intrinsic raw owner/slot/cells, parser/projection1424, pending Promise and closures, outgoing event/message ownership, protocol-fault containment and control partition are still separately required. No numerical allowance is increased; these charges consume the existing shared32MiB composition.

## Retirement Limits

The original roster-shell record cannot be detached merely because a response is cancelled, an output has no value, or a route is disposed. Its per-output role needs an exact typed terminal join for that Slot, all response/input descendants and issued aliases. This first slice will preserve the record and cell, and cannot retain the old empty-cancellation unlink as a successful final refund. Retained cancelled roots must remain counted and strongly owned; no slot reuse or hidden uncharged retirement list is permitted.

This is an admission subset, not a liveness completion. The subsequent receiver slice must admit PendingEntry/Promise/controller and raw backing before the actual post, capture the canonical binary response under one-response credit, and retire each exact reader/source before refund. Production remains unmounted until that join. A no-response posted slot still requires real original transport retirement evidence; a detached transfer is not delivery or native InputAck.

## Test Packet Before Source Cutover

Declare language-neutral phase/grant/conservation/capacity/sequence vectors and validate with strict Ajv and independent Immer arithmetic. Bind each census to actual TypeScript AST fields. Execute genuine missing-phase or uncharged-allocation RED before changing production. Cover zero/short grant at every phase, changed or foreign ledger rejection, concurrent shared-ledger bootstrap, exact lost-return recovery, before/after finalizers with null/undefined/false/zero/opaque faults, closing at every prefix, capacity/sequence exhaustion, no premature dispatch, no public record/cell escape and no refund from empty cancellation. Keep the earlier raw-outcome and parent-fault laws while correcting their obsolete uncharged reservation setup.

The UI fixture's captured-return baseline must change only with the actual exported return fixture and coordinated source release. No UI-owned builder/evidence implementation is changed by this packet.

## Executed Initial RED

The first ActorResponseAdmission run actually returned1PASS/2FAIL172skip175 at03:04:38,716ms,Nx1, seven selected inputs stable. Strict schema/independent conservation passed. The real captured roster allocated one Slot under a zero-item grant while the contract requires zero, and the actual AST still had the old seven-field roster. This is preimplementation runtime/source evidence, not a missing-import failure. Source cutover is now in progress; no coherent API release is claimed yet.

## Executed Source Release

The implemented source passed the initial3 groups at03:11:05,1.30s, then the expanded6 groups at03:12:31,929ms,Nx0. The expanded laws exercise all11 exact grant phases, no premature dispatch, the original installed roster, retained empty cancellation,24 ledger/roster closing prefixes and30 genuine wrapper/finalizer arbitrary-fault cases. The broad actor run then completed175PASS/3FAIL178 at03:12:59,2.83s,Nx1; the unchanged three failures are UI cancellation/copy/page integration. All15 selected broad-run inputs stayed stable. Existing original-output7 and parent-construction2 laws also passed within that run.

The source API is now `source.reserveResponse(grant): ResidentStep`. Its first11 separately granted phases are296,64,64,64,264,64,64,64,272,80,64. Only the final phase returns ready; the source holds the actual output privately before returning. Repeating an unused reservation returns ready0. A subsequent response after the prior one was consumed requires a separate64-byte next-admission phase, then the same11 phases. Execute/retry/poll/cancel refuse without the actual privately ready slot. The original worker/activation controls remain distinct from operation liveness.

The enclosing `reserveReturn` fixture remains12 phases with its roster construction grant changed176→256 and total1360/13/13. One response consumes1008/12/12 in the same ledger. Actual AST inventory verifies roster12 and Slot13, with no hidden per-response callback. The existing pending source census is608, including the still-unadmitted PendingEntry160. Cancelled slots stay counted, linked and charged. This release does not implement their final typed retirement, receiver/PendingEntry/Promise/raw/parser admission, native InputAck or production mounting.

Renderer strict checking completed with the same41 diagnostics (15UI counter accesses,19UI private-pool fixtures,7tutorial joins), no Shard/output/Kernel-input errors. Raw source and test logs remain in the ticket. Shard SHA7afa3143644036f4f6486bc8be860a37fbe12e9afdfb8f635253f5e3abf03089; output SHA55a104f4877689e212d04aaa925e8ce91a6beeb687a11edbd1126e0bf97c89c6; return fixture SHA9f21bd7e7468a091b24c6a03e1f5661a849156b883185fa18450f05d4b5b12e3; response fixture SHA0d5537911a8182d0b880225ba6dbcf7d6ddd035392ea786e164b556e28eae575.

## Independent Coordinator Replay

The runtime coordinator reports an independently executed ActorResponseAdmission6PASS/172skip178 at03:15:29,858ms,Nx0, two selected files out of nine collected. All65 selected pre/post files stayed stable, including the Shard/output hashes above, neutral72b0e0ba and UI3222c75d. Its short source hold is released. This is delegated independent evidence; our own six-group and broad runs are recorded separately above. No PendingEntry, Promise, raw backing, parser, live dispatch or final refund follows from either result.
