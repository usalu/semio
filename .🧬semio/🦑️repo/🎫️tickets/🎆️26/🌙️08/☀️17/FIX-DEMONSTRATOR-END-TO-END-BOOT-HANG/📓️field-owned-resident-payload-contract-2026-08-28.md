# Field-Owned Resident Payload Association

## Executed Foundation and Source Release

Current successor: real field-bound payload admission plus active/revoked17-phase close and clean-refusal abandonment now execute in the actor selector10PASS/4FAIL156 at01:42:04,with fifteen stable inputs. Only the old builder counter path remains in those four failures. Source close-only unbound abandonment uses the same observation/proof and permanently consumes the association; it is not successful installation. [Exact runtime history, hashes and limits](./📓️field-payload-runtime-2026-08-28.md).

Latest replay, start01:31:22: the input selector actually ran7PASS/5FAIL/142skip154,1.02s,Nx1. All three new foundation groups passed, including the actual14-field InputOwner AST inventory and30 real finalizer faults. Five scope-dependent cases now reject during the UI-owned instance constructor at the old368-byte construction phase; this is a later source cutover, not another association-foundation failure or a verified cause. The UI owner was notified; no fixture grant or UI source was changed by this lane. Log: `🧪️field-resident-foundation-current-1.log`.

Eight selected inputs stayed stable during that run: Shard `419448407f1b38ecfb27c7f2136de3e174669420b2771bad87f325282e9af544`; input `07874d4649a305107695ffcdc50a53e3c9197ed39959857a1f97c9b7b76614c0`; UI `681a79e9cc22c5331ac14f1b55a4736806f67836b73973bdf9eb395a1eb8a60a`; neutral `6957021837e6e5c731cec0530bb109694cff4ef6b25a9ecb0a9f02f37c1c8840`; association declaration/contract/fixture-schema/fixture `e12f587c3bf7bfc9cde3ca37d677cd0af983f11751245593cbba8b0b079e6dcf` / `5a288884f41eca466efe4b59ed04d530a46554120d3338c4c7192e7a3dc1d4d3` / `a1c8710483759bfc443f32b5557287b082a61b893e43f734ae2edec26ecb82cd` / `a5d94bacbc4340890a9316c4aa1e6458afa13d568cbcf1fe9a39fe80b1fd1c0a`.

The subsequent strict run reports55 diagnostics: the previous46 plus nine UI staged payload caller/API diagnostics, with no Kernel input or Shard diagnostic. Log: `🧪️field-resident-foundation-strict-1.log`. The original-owner fixed subset is now separately declared128B/1slot/1owner (record16 +14fields×8), an8-byte increase from13fields. It is not included in the152-byte field/observation subset and neither subset is live admitted memory.

The source now implements the agreed private association APIs and `OwnedKernelReturnPayloadDetachment`. The first neutral/negative packet actually ran6PASS/5FAIL/142skip153,842ms,start01:23:49: the neutral schema/Immer oracle passed and the new genuine field test failed at missing `matchesResidentPayload`; four existing payload failures remained. A second schema-first constructor packet ran6PASS/6FAIL/142skip154,2.22s,start01:26:33, additionally reproducing false original-parent installation during a real field finalizer.

After the source repair the same selector ran8PASS/4FAIL/142skip154,2.17s,start01:28:56. All three new foundation groups passed, including30 before/after finalizer cases across actual Field/Observation/Fragment and null/undefined/false/zero/object faults. The four failures remain the unconverted UI payload counters. Logs: `🧪️field-resident-association-red-1.log`, `🧪️field-resident-construction-red-1.log`, `🧪️field-resident-foundation-green-1.log`. The last filename identifies the new foundation, not a passing overall command: Nx exited1. Positive genuine payload association/retirement still awaits UI's implementation, so no bidirectional runtime success is claimed.

Original source field, detachment and fragment are installed in their existing parents before their finalizers. InputOwner gains a separately inventoried first arbitrary-fault field (13→14); fixed own validation codes are assigned without allocating/inspecting an error, and unexpected failures are retained without reading message/getters. `OwnedKernelReturnContent.matchesFault` checks exact original identity, not retirement. Observation `matchesOwner` verifies construction ownership only; `matches` additionally requires detached phase and exact original field/payload; `matchesSettled` also requires UI's private retained observation. None is a substitute for whole input close.

Post-run hashes, not represented as pre/post stable: input source `07874d4649a305107695ffcdc50a53e3c9197ed39959857a1f97c9b7b76614c0`; Shard tests `0fa721351b15258df16d288113b14a14fed327c66b7c0e8df53db022944c43b9`; UI resident `a7ce38ef08897fd24bc71154180368ae988e8813963a41c931784485929b3973`. A subsequent test-only addition checks the actual14-field InputOwner AST inventory; its new run is reported separately when complete.

## Source Review and Proposed Callable Boundary

The following preserves the preimplementation design; the source foundation above now implements its declared APIs but is not live admission evidence. Before this packet the field had ten private fields and owned its builder/fragment/release. Its `beginClose` still only marks closing; final input-owner retirement remains open. The existing UI payload still uses the removed per-pool counters; the four JSON files under UI resident/📦️payload declare its next replacement. No new runtime registry or compatibility path is introduced.

Use the original privately minted field as the serialized payload request identity. Proposed source calls are `field.installResidentPayload(payload,grant)`, `field.residentPayload(scope)`, and `OwnedKernelReturnInputField.matchesResidentPayload(field,payload)`. Install accepts only UI's direct private `OwnedUiResidentPayload.matchesField(payload,field)` plus the field's original host/activation/lifetime and the already-admitted exact scope. UI's original parent slot must own the exact payload shell before install; field association must exist before later finalization. Recovery returns the same privately bound facade only to its genuine original scope. A successful install followed by a wrapper throw must not allocate or bind a replacement. There is no public neutral record/cell capability.

Before implementing, UI must release exact scope/payload matching predicates that check private identity rather than public owner getters. The field gains an exact payload reference plus one preadmitted detachment-observation reference. A new source observation is required to avoid treating a returned boolean/null as completion. Its identity is installed with the original association, never allocated on the close path.

## Required Detachment Phases

1. UI's preadmitted `OwnedUiResidentPayloadSourceRelease` proves the dependent body empty, while its payload and the original field association remain retained and charged.
2. `field.detachResidentPayload(payload,proof,grant)` verifies that exact private proof and changes only the original association to a stable detached observation. A post-mutation wrapper throw is recovered through the same field-owned observation; no replacement observation is minted.
3. UI verifies and stores the exact observation before clearing its field reference. Its private source-release witness changes to a distinct source-detached phase; this is not full payload retirement and is not a public boolean.
4. A separately granted source settlement consumes that exact private source-detached proof, clears the observation's payload/proof references and the source payload reference, and marks the original observation settled. UI may proceed to its record retirement only after matching its privately retained original observation and the source's private settled phase. Foreign/replayed observations must fail even after strong references have been cleared.
5. The source keeps its own observation metadata charged until its genuine final input-owner close. That close must also detach actual content, builder, fragment and release descendants; a small facade or field `complete` flag is insufficient.

The observation must not have a permanent field backlink. Any temporary payload/proof reference is cleared before UI record refund. UI must retain the exact observation in its already-charged parent slot through the handshake; a checker for a branded settled observation alone is not adequate without that private original association. Exact observation phase/verifier names and its field census remain a joint schema decision before implementation. If preserving replay identity requires another pointer or receipt record, it must be declared and charged rather than hidden in a closure or WeakMap.

## Accounting and Tests

Concrete declaration packet is now authored under Kernel return/content/input/📦️payload: `🧬️schema.json`, `🧬️contract.json`, `🧪️schema.json`, `🧪️fixture.json`. It fixes the source method spellings above, `OwnedKernelReturnPayloadDetachment`, and UI's two-argument `SourceRelease.matchesDetached(proof,payload)`. Settled source replay rejects; post-settlement wrapper recovery observes the same private observation instead of retaining proof references for replay. UI also supplies direct private `Payload.matchesSourceDetachment(payload,observation)` for settled validation. The observation has exactly three fields: temporary payload, temporary original UI proof, and phase; it has no field backlink. Twelve source fields plus this observation are the declared fixed subset152/2/2 under record16/field8. This is not transitive metadata, source admission or physical heap accounting.

The current source ten-field inventory is `state,value,builder,fragment,release,consumed,advanced,start,complete,closing`. Two proposed association references make twelve. All observation fields, phases and any new proof records need a complete schema census before pricing; UI's proposed312-byte payload is not assumed to fund source metadata. Source field construction remains unadmitted until the actual captured return/pending parent adopts the shared ledger. The existing actor activation facade144 model and UI record16/field8 model are distinct declarations, not physical heap measurements.

Neutral laws must cover foreign scope and field refusal before allocation; short grants; exact same-field retry; unrelated-field busy refusal; install-before/after throw; cancellation at every admitted phase; genuine body-empty proof versus full terminal; observation-before/after wrapper faults; foreign/replayed proof rejection; both source and UI aliases before refund; and independent source/raw-page retirement. No native InputAck follows from any payload or fragment token.
