# Shared Renderer Composition Ledger

## Boundary

This packet adds mandatory exact composition identity and a real charged UI-pool preparation record. It does not mount the binary response inbox, native page acknowledgement, guest memory admission, or end-to-end app content. Existing output/parser metadata inventory remains separate; the full output record/terminal adoption is still required.

## Required Actor Binding

`ShardClientOptions.residentLedger` is required. The constructor invokes a captured neutral capacity getter, whose private field access rejects missing, structural, prototype-only and Proxy values before creating workers. `ShardClient.matchesResidentLedger(client, ledger)` checks the original private field directly. No optional ledger, actor-created allowance, per-return pool or public ledger property is added.

The language-neutral composition schema/fixture and actual production test first failed because the old constructor accepted a missing ledger. RED1: zero passes, one failure, 136 skipped / 137 total, 1.37 s, start 23:41:55. GREEN1: one pass, 136 skipped, 733 ms, start 23:42:46. Strict Ajv validates the declaration; actual getter/private-field rejection and exact identity are executed, not inferred from schema shape.

All authored callers are being changed, including the shared worker bootstrap, Kernel and TaskManager fixtures, controlled worker benchmark, React and WGPU. UI owns its nativeInstanceFixture constructor change. A compile/typecheck must verify the complete caller join before a coherent release.

## Pool Preparation Ownership

The original Shard has five fixed private fields for pool record, exact pool shell, phase, terminal witness and first fault. Their declared logical envelope is 144 bytes, one slot and one owner, additional to UI's 168/3/3 and the neutral record's 256/3/3. Actual shared admission therefore reserves 568 bytes, seven slots and seven owners before a pool can be allocated.

`prepareUiResidentPool` rejects a foreign ledger before allocation, refuses zero/short grants, and forwards a consumed 256-byte neutral reservation as pending. A subsequent prepared check is ready with zero consumed work; an existing shell cannot authorize a second allocation. A reservation exception seals preparation and retains the original fault. `ownsUiResidentPool` means exact shell custody, not successful neutral installation or terminal ownership.

The actual missing-method RED1 was one pass / one failure, 136 skipped / 138 total, 945 ms, start 23:47:16. GREEN1 passed both selected tests in 1.19 s, start 23:48:49. Immer independently reproduces the sum of all three envelopes. No actual UI pool was installed by those two tests. Concrete install, private terminal witness, detach/observation, separate refund and abandoned-construction close remain the next coupled UI/Shard join.

## Explicit OS Renderer Policy

The OS renderer policy selects one combined host ledger of 33,554,432 bytes, 262,144 slots and 262,144 owners. Its 65,536-byte / 1,024-slot / 1,024-owner control partition is carved out, not additional. Raw replies, destination pages, metadata and scratch must all draw from this ledger. React and WGPU import the same module-owned ledger, not two instances of a shared factory. A closing ledger is never replaced while old ownership remains.

The source basis for retaining rather than increasing the byte ceiling is the existing UI document/resident schema: surface 8,388,608 and aggregate 33,554,432 bytes. Those existing UI policies are not changed. This is a new stricter combined host admission policy, not a claim that previous JavaScript ownership used this ledger, that all apps fit, or that logical accounting measures physical heap. Saturation must remain explicit. The native per-turn memory budget and old wallMs=100 enforcement are separate and unchanged; neither confers guest admission or an eight-millisecond certificate.

The first renderer test failed collection on the intentionally absent policy module. Two later attempts failed collection on an incorrect relative import depth. A subsequent actual policy test passed while the Node import test failed on Vite's rewritten HTTP asset URL. The test now resolves its original module filesystem path explicitly. The final focused renderer gate passed 2 tests / 660 skipped / 662 total in 8.87 s, start 23:50:59, log `🧪️renderer-composition-green-4.log`. This proves same-realm policy identity, controlled record conservation and no replacement after ledger close in Node; it does not execute the WGPU renderer or guest.

## Canonical Rejected Admission and Parent Close — August 28

The preceding 568-byte inventory is a historical checkpoint. UI's source now includes the actual WeakMap table and binding objects: the exported pool envelope is 192 bytes / four slots / four owners. Together with the unchanged controller 144/1/1 and neutral intrinsic 256/3/3, the actual record admission is now 592 bytes / eight slots / eight owners. The actor schema, fixture, direct export comparison and independent Immer arithmetic agree. No pool policy limit was raised.

Neutral R21 returns explicit step/facade admissions. A rejected record is a retained closing object, not permission to construct the pool. The real finalizer-fault test reproduced Shard's incorrect conversion of rejected-plus-record to pending/prepared. The source now stores the exact rejected facade, preserves rejection and prohibits retry or pool construction. Its parent-owned `closeUiResidentPoolStep(grant)` closes only the unused original record; it never closes the shared ledger or substitutes a fresh reservation.

The same parent method drives an installed exact UI pool without relying on an abandoned caller. Begin-close, one child close call, private witness observation, record begin-close, detach, exact observation, intrinsic refund and parent unlink are separate phases. Each child receives one grant and retirement metadata is not inspected in that child's completion turn. Direct public/foreign/premature witness refusal remains covered.

The actual RED was 3 passing / 2 failing / 136 skipped / 141 total in 571 ms, start 00:03:12: rejected admission was incorrectly pending, and the parent close method did not exist. GREEN passed all 5 selected tests / 136 skipped / 141 total in 1.11 s, start 00:04:05. Logs: `🧪️actor-rejected-pool-red-1.log`, `🧪️actor-rejected-pool-green-1.log`. This is focused empty-pool and intrinsic rejected-record recovery, not full actor/React or live app proof. An earlier full actor 138/138 and exactly seven tutorial strict errors predate the active UI pool cutover and are not current full-pass evidence.

Post-run hashes (not a pre/post source-stability certificate): Shard `696877fc4d66e548f8099f30c9136a5ca8be99c3aaf24b9bdedbd664cd7d8aed`; actor composition schema `1cd94443465674ea0b7eb25415644b0871076668848b0b366ffb254686d81a61`; fixture `536e9f16f2c64501ebc731dc1d72974be4e46a95e57f3077977f0a8da8464f0d`; neutral `23c6485bfcd2d75eb5c7b028bbb822b1dfc68e3c386fe65560e8f9076c08ae8c`; UI resident `9817f350cf9d70de002bdff19cdf72edb4909d00c08d27a9cb4f12c62e0bb468`.

## Exceptional Ownership Still Open

The constructor fault in the rejected-record regression is the closed primitive `null`. It does not certify retirement of arbitrary exceptions discarded by the current neutral catch clauses. That shared first-fault retention work remains UI-owned. A wrapper that calls the real reservation then throws before returning its result is a separate missing retained handoff; permanent fencing is not successful independent close. Likewise an exception after actual refund still needs separately funded ownership before exceptional-close credit. No live inbox or native InputAck may be inferred from these tests.

The actual next regression threw `null` after the real record `beginClose`. Shard's prior `null` sentinel let a subsequent call continue closing. RED: 5 passes / 1 failure / 136 skipped / 142 total, 910 ms, start 00:05:49. The private marker is now a unique symbol; the first actual thrown value is retained without inspecting or coercing it. GREEN: 6 passes / 136 skipped / 142 total, 835 ms, start 00:06:35. The test covers null, undefined, false, zero and an object with an 8,193-byte descendant and unread getter; the faulted record stays charged while another client's record can close. Logs `🧪️actor-parent-fault-red-1.log` and `🧪️actor-parent-fault-green-1.log`. This is containment, not arbitrary fault retirement.

A direct release-observation test then reproduced a thrown value escaping after the real private detachment getter: RED 6 pass / 1 fail / 136 skipped / 143 total, 790 ms, start 00:14:38. The exact observation is now inside the retained catch boundary. GREEN 7 pass / 136 skipped / 143 total, 1.05 s, start 00:15:20. After the first rejected turn, an exact later detachment observation can be recorded separately, but the original fault still prevents refund and leaves the exact pool/record owned. Logs `🧪️actor-pool-observation-{red,green}-1.log`. No fault cleanup is certified.

The fresh Kernel caller regression suite passed 49/49 in two files, 2.16 s, start 00:06:51, `🧪️kernel-composition-callers-1.log`. The fresh renderer strict check failed with 52 diagnostics: seven tutorial, four actor-owned old UI-pool fixture constructors, 22 active UI pool property conversions and 19 UI fixture constructors. No new controller, OS policy or required-ledger caller diagnostic appeared. `🧪️actor-composition-strict-2.log` retains the exact output. The actor fixtures will consume the UI owner's concrete phased instance/payload API when released; there is no compatibility constructor.

Taxonomy registered the exact RendererResidentComposition test at launch order 400.95 without running it. Final reported seed `f69991f4c42aa0395abf359e41b2d8900b337031e94c3130ffdb9ef1a947608c`, launch `a32147679f3ae9b1d55872399c8931dbead93c4ac63312a3efcf48ab4461e115`; this is delegated registration evidence, not a fresh renderer or output test.

## Preservation

No native compiler, generated plugin publication, WGPU pin, Git mutation, cleanup, deletion or evidence relocation was performed. All logs and source inputs remain in the active ticket. The native pre-instantiation capacity proposal is queued for read-only review after this caller packet; the host ledger cannot retroactively fund Wasm static memory.
