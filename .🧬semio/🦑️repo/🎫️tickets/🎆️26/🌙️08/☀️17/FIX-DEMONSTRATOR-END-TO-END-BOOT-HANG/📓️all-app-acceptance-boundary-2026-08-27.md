# All-App Acceptance Boundary

## Current Authored Pane Roster

The actual demonstrator brand, build router, and generated playground catalog were re-read while the canonical return schema was being coordinated. The six visible panes remain:

| Pane | Runtime Variant | Plugin | App |
| --- | --- | --- | --- |
| Generator | procedural3d | procedural | s.procedural.procedural3d@1/*#editor |
| Koordinator | koordinator | demonstrator | s.cad.cad@1/*#editor |
| Aggregator | aggregator | demonstrator | s.puzzle.puzzle3d@1/*#editor |
| Aussuchen | aussuchen | demonstrator | s.sourcing.curate@1/*#editor |
| Bearbeiten | bearbeiten | demonstrator | s.process.process3d@1/*#editor |
| Verfolgen | verfolgen | demonstrator | s.gis.gismap@1/*#editor |

Generator's manifest/brand variant remains generator, while its explicit runtime mapping is procedural3d. The other five runtime variants are unchanged. The build router builds the shared demonstrator closure plus the distinct procedural runtime and all declared pane engines. Its authored module-layout expectation includes nine plugin roots and thirteen extension roots; those are dependencies, not twenty-two visible panes.

## Required Runtime Result

For every pane, fresh consumed artifacts must open with a guest-issued lifetime before exposing a handle, publish actual requested window and panel content through the owned intake/read source, complete an example or meaningful interaction, and close/reopen without losing the original activation's pending work or removing a replacement instance. The visible application must leave loading, surface real terminal failures, and produce runtime console evidence without fabricated content or empty descriptor fallbacks.

Transport tests, Node VM generated-code tests, descriptor existence, a native library compile, and isolated common-Kernel close laws do not establish those six live outcomes. Current success is not claimed for any complete freshly rebuilt all-pane run.

## Diagnostic Consumer Cutover

The retained ticket's older `probePlugin` script was inspected but not executed. It still sends pre-capture Open data, constructs surface/revision-only UI ACKs, and consumes whole result arrays. Its five-app `all` list is scoped to the demonstrator plugin and excludes the separately booted procedural Generator. That script cannot be reused unchanged as canonical lifecycle/issued-patch/paged-return or all-six proof.

The next fresh component probe must adopt the same canonical owner/page/input-ACK schema and real lifecycle receipts as production, preserving its existing 4096-turn cap and exact failure reporting. It must not become a side ABI, a whole-copy production bridge, or a browser-policy workaround. No source or artifact was changed by this inventory.

## Native Boundary Reviewed

The runtime coordinator's complete `📓️coordinator-kernel-transport18-and-resident-permit3-2026-08-27.md` was read. It reports common-Kernel 18 ownership/handback laws and isolated resident permit 3 laws. It explicitly leaves raw patch transfer, native WIT result handoff, real resident adoption and final guest close open. Those delegated native results are not fresh Plugin or live pane verification.
