You are a GPT 5.6 Sol Extra High execution worker in a coordinated task. Read /Users/ueli/Documents/semio/AGENTS.md and applicable nested instructions. Work only in the shared workspace; no git mutations, worktrees, AGENTS edits, external messages, ticket lifecycle changes, or goal lifecycle changes. The ticket is already OPEN through actual repository MCP, after repo://goals was read, associated with 🎯aioptimizedrepo:
/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️23/END-TO-END-TESTING-REFACTOR
Read 📓️testing-taxonomy-plan-2026-09-12.md. The user requires only tests, fixtures, examples, oracles as testing categories. Remove testkit and legacy testing/support buckets, not just rename them into misleading categories. Tests use <semantic-owner>/🧪️tests/<case>/<implementation>. Fixtures are testing-only examples under owner 🧫️fixtures outside case folders. Production static data uses 🖼️assets; production code must not depend on fixtures/test support. Canonical examples use 📚️examples. Canonical oracle collection is 🔮️oracles (plural); supersedes any audit statement of singular 🔮️oracle, 🧪️oracle, ⚖️oracle. Oracles must be real independent references/comparators; case-specific support belongs with actual test implementations. Generic reusable test algorithms must have semantic ownership with test-only exposure, not a disguised helper bucket. No legacy API, deprecated alias, wrapper, duplicated compatibility layer or retained migration script.
Use bun as package manager, nx as task runner. Only script filename 📜️script.ts. All temporary files, logs, compiler output, Nx/cache/temp paths under this ticket/🗑️generated/testing-taxonomy/<lane>. Retain input scripts/configs and Markdown reports in the ticket. Private bounded Nx workspace exists at ticket/🧑‍💻coordination/🧫️nx-fixture with project layout-probe; use NX_DAEMON=false NX_ISOLATE_PLUGINS=false NX_WORKSPACE_ROOT_PATH=<that path>, NX_WORKSPACE_DATA_DIRECTORY and NX_CACHE_DIRECTORY under your generated lane; bun nx exec --projects=layout-probe -- bun <ticket 📜️script.ts> command. Register permanent public commands using existing root script/project/launch conventions if you introduce any.
Other agents edit the same working tree: preserve unrelated changes and continue. Root owns global coordination; a native Sol worker owns taxonomy SSOT, testing guards/schema/neutral vectors, discovery and normalization. Root owns schema Ajv comparator relocation, UI conformance examples->fixtures, empty core legacy dirs, root 📜️script.ts references and small framework oracle manifest aliases. Do not edit those owned scopes; publish exact requested cross-lane path/symbol changes in a small ticket Markdown handoff file named 📓️<lane>-handoff-2026-09-12.md and notify via stdout. No further subagents: all four concurrency slots are already allocated. Do not call new-thread tools.
New feature behavior requires neutral test vectors and third-party independent oracle comparison. Verify relocated readers with actual runtime output. Do not claim unrun tests or hide unrelated failing suites. Keep a complete exact authored file ledger (removed source, created destination, modified consumer paths) as fenced JSON in your Markdown report plus byte-preservation hashes for moves. Report progress and meaningful failures in ticket Markdown, then return only report path when your bounded scope is complete.

LANE framework: Read 📓️framework-testing-taxonomy-audit-2026-09-12.md. Implement all OS framework testkit/helper structures in that audit (13 roots, nested cases/corpora, machine testing-support and Directory/reactor helpers), plus core 🎒️pack/🧨️testkit and its exposure. Split static Rust-shaped vocabulary/corpora into nearest-owner fixtures and actual executable tests into canonical cases. Remove unconditional testkit exports. Break DB FaultStorage's production dependency on testing infrastructure properly (correct injectable abstraction or test gating), not a renamed feature. Remove OS pack compatibility testkit layer. Reconcile JCO harness/scale/browser-bundle sources, test-owned package/build outputs and metadata. For JCO and any public Rust module renames, immediately write precise old->new paths/symbols to your handoff report so root/guard/plugin workers update their consumers. You own every framework/OS consumer required by your changes except native guard's metadata scope and root 📜️script.ts. Plugin worker owns ✏️s consumers; provide symbol mapping before changing them. Root owns core UI conformance example corpus and schema comparator, so leave those. Final report 📓️framework-testing-taxonomy-execution-2026-09-12.md. Work until this scope is implemented and verified, treating shared compile failures accurately.
