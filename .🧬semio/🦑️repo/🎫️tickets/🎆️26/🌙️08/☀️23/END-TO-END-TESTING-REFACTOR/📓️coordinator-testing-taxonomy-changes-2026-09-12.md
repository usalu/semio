# Coordinator Testing Taxonomy Changes — 2026-09-12

The root command router now discovers one canonical oracle directory and uses `testOraclesDirName`. Owner-specific overrides were removed from discovery, dependency classification and subset catalog lookup. Oracle dependencies still require test-only ownership or non-production declarations; an actual production declaration remains a conflict. Canonical test cases and fixture roots are recognized directly, while old probes/generator buckets receive no permanent compatibility exemption.

The schema document comparator and UI conformance corpus have separate retained exact move and runtime reports. Framework and plugin workers own their assigned relocation scopes and report direct metadata/path changes for integration.

## Exact Authored Paths

```json
[
  "📜️script.ts"
]
```

## Verification Pending

The guard worker is updating the language-neutral oracle-ownership vectors and extracted root function checks against the new three-argument APIs. Final directory/reference coverage, semantic classification and native/runtime verification remain required before completion.

## Empty Legacy Directories

The four observed legacy Rust tests directories were removed using atomic rmdir, which would reject a concurrently added file. No source file was deleted.

```json
[
  "🧰️framework/🔨️modules/🔀️dispatch/📦️packages/🦀️rust/tests",
  "🧰️framework/🔨️modules/📐️geometry/📦️packages/🦀️rust/tests",
  "🧰️framework/🔨️modules/🗺️surface/📦️packages/🦀️rust/tests",
  "🧰️framework/🔨️modules/🖱️ui/🧬️contract/📦️packages/🦀️rust/tests"
]
```

## Shared Test API Policy Consumers

Root policy delegates now validate artifact_app_laws::new_app/new_app_with_registry/meta and use matching diagnostic identifiers. Dead protocol/testkit allowlist entries are removed. DB fault-testing policy parameter/diagnostic language and its direct self-test use the semantic fault-storage name. Updated files: `📜️script.ts` and `🧰️framework/🛍️products/💻️os/🔨️modules/🛢️db/🗄️storage/🧪️tests/🔬️interactivity-db-io/🟦️.ts`.

## Root Policy Consumer Runtime

The actual root delegate checker, extracted with the TypeScript parser, accepted all three canonical artifact-app-law calls and rejected three nondelegating forms. The actual interactivityDbIoSelfTests export ran its existing negative and positive policy vectors successfully.

## Concurrent Core UI Wiring Observation

The intermediate census reported52 missing native test links during a concurrent UI semantic refactor. Read-only reinspection found all52 links already resolve to current canonical tests. No core UI file was changed for those rows. Final acceptance must use a fresh census.
