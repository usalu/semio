# TXT Canonical Root and Surface Contract

## Authority

TXT-CANONICAL-ROOT-23 completes the physical direct-owner cutover for the five existing TXT operations without changing payload behavior, wire tags/opcodes, aggregate variant names, or inverses. The latest source policy explicitly derives kind-only primary filenames for aggregate Rust, TypeScript, GraphQL, JSON Schema and Protobuf. FND14 also independently requires the canonical aggregate Rust primary. A general utility that can render stemmed filenames does not override these primary-source authorities.

Current taxonomy also declares `mutationPayloadSchemaLocation` as the semantic child `🧬️schema` with JSON primary `🔣️.json`. The canonical payload locator is therefore `🧬️schema/🔣️.json`, as returned by `mutationPayloadSchemaRelativePath`. The old `🔣️payload.schema.json` is a normalization input, not the final destination. The normalization projection's recognition of input filenames is not proof that runtime/source policy accepts both names. This packet changes no shared normalization or taxonomy code.

## Frozen Physical Layout

All ordinary primaries within the TXT mutation tree use the exact taxonomy kind-only names: `🦀️.rs`, `🟦️.ts`, `🔗️.graphql`, `🔣️.json`, `🛰️.proto`, `📖️.grammar.semio`, and `📡️.protocol.semio` where those surfaces exist. Derive these values from taxonomy in tests; the written names here contain no spaces after their dots.

Each direct leaf keeps its metadata descriptor at its own `🔣️.json`; its payload schema moves to `🧬️schema/🔣️.json`. Rewrite all descriptor pointers and aggregate schema references to the single canonical destination. Optional text/binary child primaries also become kind-only and are explicitly mounted by their direct leaf. Root codec infrastructure remains generic framing, lookup and delegation.

The exact direct owner names are:

- `📥️insert-line`
- `🗑️remove-line`
- `✏️set-line`
- `🔚️set-line-ending`
- `↩️set-trailing-newline`

The coordinator selects the end and return symbols for the last two owners to remove the existing duplicate pencil identity. Validate each name through the current taxonomy/new-mutation name mechanism and assert the five prefix identities are unique. Their semantic names, aggregate variants, text opcodes and binary tags remain unchanged. Update descriptor emoji/owner, provenance expectations, current oracle/catalog identity references and all physical import/mount paths together. Do not retain old directories, aliases, or duplicate descriptors.

The aggregate Rust primary owns the five explicit public canonical leaf mounts and local reexports. Remove those five sibling mounts from STDIO glue. Retain visible root aggregation and generic mechanical application; strengthen the count-only roster test to exact semantic/variant/descriptor correspondence. Generic text/binary modules must remain explicitly reachable. No new per-kind behavior, payload declarations, central inverse switch or descriptor literal is permitted in the aggregate.

## Write Ownership and Sequencing

The packet owns the TXT mutation tree, only mutation surface include references in the TXT schema-owner Rust component, only the TXT mutation module block in STDIO Rust glue, and the TXT subset oracle/catalog's affected physical owner identities. It also owns dedicated ticket-local schema/vectors/probes and its evidence report. Any additional consumer discovered outside this list is reported to the coordinator before editing. Root script, shared discovery, taxonomy, normalization, public traits/derive/registry, snapshot/diff/inference implementations and unrelated files are excluded.

TXT-PROTOCOL-22 currently owns the root binary protocol and framing tests and is compiling an actual-source probe. TXT-CANONICAL-ROOT-23 may prepare its schema-first neutral fixture, read-only/red checks and exact edit map immediately, but must wait for the coordinator's explicit source-hold release before production edits. After that release it absorbs the completed protocol files into the canonical physical cutover, preserving the verified correction. Root alone runs Cargo.

## Required Evidence

Use a language-neutral exact five-operation matrix. Establish red results on the current physical layout. Validate accepted names and canonical filenames from current taxonomy. After the cutover, verify exact directory/variant/descriptor/source ownership, all fourteen metadata fields and relative provenance, canonical payload pointers, AST-backed TS/GraphQL/Proto/schema parity using existing third-party test libraries, and actual text/binary round trips without weakening malformed-input tests.

Compile and execute actual production sources with the genuine kernel/proc-macro artifacts where possible. Update a fresh ticket-local runtime harness for the new mount layout; old binaries and historical fixtures are retained as checkpoint evidence, not silently rewritten as current proof. The coordinator will independently run the registered TXT schema selection and relevant language-agnostic feature suite. All commands use Bun and scoped Nx; all temporary sources/logs remain in the ticket. No actual excluded path may be read or materialized. No full-root or monorepo acceptance is inferred before the remaining mandatory metadata/registry and complete runtime gates pass.
