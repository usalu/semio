# GIS 2D Presence Root Review

## Executed Result

The root replay passed 1,024 source/schema/reference assertions, including 46 behavior-reference assertions, in `🧪️gis2d-presence-direct-47/🧫️runs/1787861906573`. This is not native execution.

The preceding root source RED at `🧫️runs/1787861402584/📄failure.txt` proved that the native helper did not consume all shared laws. The repair changes only the native test body and its source-coverage assertions, not production mutation semantics.

## Native Contract Strengthening

The leaf-owned helper now iterates all four domain laws through both the concrete leaf and aggregate. It compares exact diff, after-state, warning code/severity, changed status, stored inverse vector, and reverse inverse application. It exercises text and binary roundtrips, every truncated binary prefix, and ordered diff absorption with an empty identity. Strict aggregate valid and invalid vectors are also consumed.

The five native tests remain unexecuted. Source review does not certify parser, codec, derive or runtime behavior.

## Stable Source Anchors

| Input | SHA256 |
| --- | --- |
| Presence native tests | 6e7998a6b90259086990d564583ca0ace69d2825fa00b901250e8ac7446d5f07 |
| Source controller | aed9a83eb59eac3a043319f2f7c2c76b989b7a698766afa90fc98b1edcb6a0f9 |

Production source, descriptors, schema and shared vectors were unchanged by this follow-up. The complete source gate receipt contains their first/final hashes. Native testing remains gated on the shared Plugin dependency readiness.

## Registration

Proposed source-only launch row: `⚖️gate🌍️gis🗺️presence🧪️direct-fixtures`, order `410.496`, group `4_gate`, using the existing Bun/Nx controller command. Taxonomy remains the sole seed/output writer; registration is not test execution.
