# TypeScript Declaration Union Reference 73

This refines the synthetic 72 fixture only. It is not an actual VCS/GIS union
proof and changes no descriptor, source API, collector, or production binding
projector.

The closed schema, canonical vectors, and controller are at
`🧪️typescript-declaration-surface-73/{🧬️schema/🔣️.json,🔣️.json,📜️script.ts}`
with SHA-256 `aca210bec1b447d58598a78d7c9370725934be0ca13ad74ea1ccc799e1b2637c`,
`4a55a2551233a34cab93338ae12301099c36603a92b5b46495700cb7035e563c`, and
`3f7f15a612d7ddb11004be1b141d39db67d5446c6cc06b9ed7a6fdd34900d985`.

The controller extracts and executes the current 72 reference join helpers
from their captured source instead of retyping them. Its bounded current-source
RED is retained at
`🧪️typescript-declaration-surface-73/🧫️runs/🧫️run-bc71e383-e37f-4f72-8fe9-cb9d01e27d4a/🔣️failure.json` with
terminal error:

```text
current72 reference violates union desired law:
vcs-union-selected, gis-union-selected, shadowed-generic,
optional-discriminant
```

The ambiguous competing-alias case was already rejected by 72 because it did
not support unions; it remains a required explicit negative rather than being
reported as an additional old acceptance bug.

The same fixture then passed the corrected ticket-only symbol reference in
8 checks. Retained output:
`🧪️typescript-declaration-surface-73/🧫️runs/🧫️run-d228b466-8fdb-402e-961b-23ff7f85c786/🔣️result.json`.
It selects exactly one union member by checker symbol and canonical leaf source,
requires a non-optional literal discriminant, and rejects zero or multiple
matching members. Results are 2 bound (VCS and GIS union positives) and 3
rejected (generic shadow, optional discriminant, competing aliases).

The extracted legacy run reads standard libraries only under the explicit
installed TypeScript library directory. Every actually read library file is
nofollow-read and SHA-256 captured in `result.json`; this is selected dependency
capture, not a claim of complete TypeScript dependency stability. The terminal
capture records post-capture failures explicitly. No native command ran.
