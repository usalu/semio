# Workflow Actual Source Runtime Gate

The retained controller mounts the real Workflow component and its direct mutation roots rather than copying their payloads, descriptors, implementations or tests. It is a bounded complement to the actual `semio-framework` Cargo build: the external framework supplies common media types, while all Workflow and Run tests are compiled directly from their current source owner.

Execution requires an explicit coherent artifact map from the completed root-owned framework build. The controller fingerprints the full mutation source closure, the mounted component, the client/controller, and all supplied library/metadata pairs before and after compilation and runtime. It requires exactly forty-seven compiled tests: twenty-two component tests, twenty-three leaf metadata tests and two aggregate structural tests. Both complete-variant codec test names must be present before execution. Every run receives a new retained directory. No source, target or previous evidence is removed.

The initial preparation checkpoint did not execute this gate. The later result below does not claim complete schema/language parity or proper leaf ownership for behavior still centralized in WorkflowDiff/RunDiff.

The independent controller review corrected CRLF roster normalization and required sibling metadata validation plus both rlib/rmeta extern arguments before execution. Its protocol/store/DSL imports deliberately reproduce the kernel facade aliases in the real framework glue. This is not a separate proof of direct lower-protocol dependency identity.

## Executed Checkpoint

After the actual framework build passed, the controller compiled, listed and ran exactly forty-seven actual-source tests in `🧪️workflow-actual-source-34/🧫️run-nWfvM8`, all passing. Every process exited 0 with no signal or spawn error, and the full source/artifact fingerprint map remained unchanged. Both complete-variant text codec tests ran, as did all twenty-three metadata tests, both aggregate roster tests and the existing application/inverse/document-codec checks.

The fresh framework pair was `libsemio_framework-0ff4ad5272f93f3d`, rlib SHA-256 `a7f619b4087ec448faff3c1ced8021a738a598961db430e145d56f84db07e20b` and rmeta `f5e27e84d66c5a8c9abcb43eb0b55515c340ba3a524888158af9550a7597b69d`. The fresh kernel pair was rlib `395aa2e918d0c725e11d648dbb914dea7e9f9ee9130b5847315299a56662db08` and rmeta `707c8e9b63c17f3681256362580b82f36c476b0b0f286cf06078ec9b1fbc0d1f`. The exact additional inputs and retained compiler, listing and runtime logs are named in that run's `🔣️results.json`.

This passing existing suite does not cover the independently identified duplicate-start, Automation JSON casing or first-insertion inverse contract gaps. Those remain separate explicit checks, not implied acceptance from the forty-seven-test result.

## Independent Contract Red And Compiler Release

The same root controller then compiled the independent public Run client in `run-contract` mode using the same paired artifacts. `🧪️workflow-actual-source-34/🧫️run-fr2fEk` records compiler exit0, runtime exit101 and unchanged source/artifact fingerprints. All seven named checks executed: metadata, codecs, replacement inverse and sealed admission passed; Automation JSON casing, total inverse for a first node-record insertion and duplicate-start admission failed. The final count is four passed/three failed, not an empty test-harness pass. The review lane independently reproduced the same results with its own paired invocation.

Root explicitly released the shared compiler slot to the demonstrator after both clients completed. Workflow/Run and kernel sources remain held through its next scoped Flow gate. Required metadata adoption and existing-test readiness are verified; the three new semantic failures, aggregate-schema gaps and remaining mutation owners are not complete.

## Strict Run Schema and Admission Checkpoint 35

The subsequent Flow dependency gate completed red before tests and explicitly released the shared target. The Run author added strict nested serde, the camelCase RunTrigger field contract, leaf-referenced aggregate JSON Schema and one typed checked-admission path using `RunDiff::apply`. Root caught an attribute initially applied to `WorkflowDiff` instead of `RunTrigger`; the author reverted only that accidental edit and fixed the exact RunTrigger declaration before compilation.

Root independently replayed the Nx-scoped schema controller: six leaf positives/nineteen negatives, five aggregate positives/twenty-two negatives and all twenty-three authoritative descriptors passed. The actual framework Cargo build in `🧪️workflow-registry-integration-32/🧪️workflow-compile-35.log` exited0 in42.93s. Current Workflow component SHA-256 is `b929cc35dc143bc42905af40c46362d03dbf80277aaf6a5dee44e44acdfd95ff`; the separately edited RunSink component is `7b0366fce282226bca7462479052bfe1e281d18e8f76aec6a01c9b38ad37223e` but is not compiled by that framework-library target.

The refreshed actual-source controller now requires forty-nine tests, including the two new Workflow-mounted Run serde/admission tests. RunSink's new typed-error test belongs to its separate runner module and is not included in this count. The exact paired framework artifact has rlib SHA-256 `8552e0d202ff4b60233d47652e9bb3cce4f9d0ec14dfa577da82677d69af0e7c`, rmeta `f16302686cbea66941f3971bb017a4800dde8cf0dab66faf1a80456b38163d44`; kernel rlib is `757b6412e5f7b21c78d91eeb32d47eba757e76d3da5279c51a4712c99ab6161f`, rmeta `634999dc113c4916e619289f276d43aa1a6f5f0e422369b2de4f2a857bf4a5d6`. The source runtime and independent contract/algebra clients remain pending at this checkpoint.

The actual-source suite then compiled, listed and ran all49 tests successfully with stable hashes in `🧫️run-Hxgtqr`. The independent public Run client in `🧫️run-0gHPXz` compiled and ran all7 checks, now6passed/1failed: only the existing first-insertion total-inverse assertion remains red. The new schema/casing and duplicate-start checks pass without weakening their original expectations.

The independent algebra client in `🧫️run-1v2Tai` compiled successfully and reproduced3real failures: lost first log under absorb, lost Start fields under absorb, and incorrect middle-node order after replacement inverse. Its source/artifact hashes also remained stable. `📓️run-algebra-35.md` records the exact neutral expectations and observed results. The separate complete52-case Rust serde/schema parity probe is being prepared; the Ajv counts are not substituted a complete Rust parity claim.

The separate public Rust schema client then compiled and ran all52 cases successfully in `🧫️run-dSE9RN`, with unchanged source/artifact/vector hashes. Its exact counts are6leaf-positive/19leaf-negative/5aggregate-positive/22aggregate-negative, matching the independent Ajv acceptance/rejection expectations. Positive serde output was compared for exact JSON equality, not just successful decoding. This closes the bounded Run JSON parity gap; it does not close the4 remaining independent inverse/composition failures or establish runner/Plugin/Infinite readiness.

After the final client finished, exact process-name checks found no active `cargo` or `rustc` process. Root releases the borrowed compiler slot again; all target and run evidence remains retained. New Kernel lifecycle edits are separately peer-owned and are not represented by the historical framework-build35 snapshot.


## Run Algebra Checkpoint 36

Root reviewed the bounded `RunDiff::Sequence` composition and in-place Finish replacement changes, then obtained an explicit new compiler slot. Framework build36 completed successfully in8.90s (`🧪️workflow-registry-integration-32/🧪️workflow-compile-36.log`). The Workflow source SHA-256 is `7b4c1109194eb5187d9b228bd791343d146fb25f3ffa711e91a5e623bd2a49fe`. The framework rlib/paired rmeta hashes are `fd7c334049241f7538461777b484463ca1970a5ab133e9867069dd0443f3d371` / `204e11c1b93c3e60b4be3cb0ae7e0f4ed564339fe906ff9f5d3a39a0f161567f`; kernel hashes are `1a8455fce43a3ca00188cfae3a5eb21bf4a5e2b975e72d9300ed0d27fcd99cfa` / `d47a9ddd3c012217059ae6e0d8020e7c72fb1fbd3701c5b6e458dbed83fe1f4e`. The Cargo root rlib alias and supplied deps rlib hashes matched exactly.

All four fresh clients compiled and executed with stable source/vector/artifact hashes:

- `🧪️workflow-actual-source-34/🧫️run-Zp4EDK`: exactly54 listed and54 passed, including all five new composition/rejection/order laws.
- `🧪️workflow-actual-source-34/🧫️run-ILbi4I`: all3 independent algebra vectors passed, with the original expected outputs unchanged.
- `🧪️workflow-actual-source-34/🧫️run-ZfJAUO`: all52 independent leaf/aggregate serde cases passed.
- `🧪️workflow-actual-source-34/🧫️run-Ob0SVs`:6 of7 public contract checks passed; first insertion's total-inverse assertion remains red (runtime101). No fabricated removal operation or weakened assertion was added.

The bounded algebra repair is accepted, not complete Run ownership. Ambient application-time clock reads, centrally owned application branches and the shared typed inverse-availability transaction remain open. Runner RunSink's separate test and Plugin/Infinite readiness are not covered by this framework build. After all clients ended, PID-only exact-name checks found no root Cargo/rustc process; the borrowed compiler slot is explicitly released with all artifacts retained.
