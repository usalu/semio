# Taxonomy Ticket Preservation Accounting

## Scope

Read-only response to the taxonomy task's report that three retained directories disappeared around 2026-08-27T13:56Z. No restoration, relocation, cleanup, or shared-source overwrite was performed for this inquiry.

The reported parent is `/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION`.

## Observations

- Direct filesystem checks found the parent present and all three exact directories absent: `🧪️wgpu-generated-retirement-6LbGtY`, `🧪️wgpu-generated-retirement-XDLdXS`, and `🧪️energy-reference-capture-TWoVr9`.
- A bounded search of Markdown, retained scripts, and logs in the two active tickets found references in the taxonomy reports, but no recovery location. Immediate directory-name inspection of both ticket roots found no matching relocated directory.
- Listing `/Users/ueli/.Trash` was denied with `EPERM`; its contents were not inspected. No broader filesystem or external backup search was performed.

## Mutation Task Accounting

- The coordinator has no recorded action targeting these directories or cleaning the taxonomy ticket. This is task accounting, not a complete filesystem audit proving the cause of disappearance.
- The GLTF lane reports no removals or relocations and only retained writes in its leaf and this mutation ticket.
- The TXT lane reports no removal, relocation, or cleanup targeting these paths. The earlier documented removal of two TXT executables in this mutation ticket is a separate event and does not explain the three taxonomy directories.
- The provider lane reports no removal, relocation, or targeting of these paths. It identified test-helper cleanup branches limited to their own `semio-cargo-provider-binding-*` / `semio-metadata-source-provider-*` temporary directories when `SEMIO_TEST_ARTIFACT_DIR` is absent; it reported no taxonomy-ticket reference in those branches.
- All three lanes were explicitly told to preserve active-ticket authored fixtures, captures, and transaction/recovery evidence and not to restore or overwrite shared sources.

## Outcome

No exact recovery location or cause is known to this task. The missing raw evidence has not been reconstructed, replaced, or represented as currently available. The preservation request remains in force during independent mutation work.

## Further Active Evidence Loss

During the Workflow/Run continuation on 2026-08-27, the registry reviewer reported that `🧪️registry-contract-review` disappeared after its neutral/reference and current-contract runs. The coordinator independently confirmed that directory absent, with the sibling `📓️registry-contract-review.md` still present. The reviewer reports no deletion or relocation; the provider lane separately reports no targeting of it. Those recorded runs are historical observations, not currently reproducible retained evidence.

Exact coordinator checks also found `🧪️mandatory-contract-target`, `🧪️mandatory-contract-cutover`, and `🧪️repo-status` absent. The planned reuse of the current-contract compiler target and retained repo MCP controller is therefore unavailable. Earlier reports of completed checks are not new runs, and their missing raw artifacts must not be represented as available. No replacement or restoration was performed.

Taxonomy reports no exact relocation or active cleanup information and no targeting of this mutation ticket. It enumerated only seven cache files in each of two new taxonomy WGPU check folders and manufactured compiler-output test sentinels in its own acceptance run directory as its own disposals. It additionally reports its `📊️repository-current-inventory-02` directory missing while sibling reports and new nested semantic run owners survive. These are coordination reports, not proof of cause or disappearance time. Both tasks continue to protect authored inputs, results, captures and recovery evidence.

The new `🧪️workflow-registry-integration-32/📜️script.ts` subsequently disappeared after it completed the fresh kernel build and eleven derive tests. Its two run directories and logs remained present on direct inspection. The queued Workflow invocation failed with `Module not found` before any Cargo compile began; that attempt is not a compiler failure. The coordinator did not recreate the missing file and launched a fresh explicit scoped Nx/Cargo check with a new retained log instead. No cause or recovery path is known.

Separately, the registry reviewer observed a temporarily missing/replaced kernel rlib while the coordinator's explicitly authorized Cargo build was in flight. The build completed successfully and produced the confirmed new rlib/rmeta pair. That transitional library observation is explained by the active build and is not classified as an unexplained evidence disappearance.

Taxonomy subsequently reported Energy's `mutate-energy-model-1/component.feature` and `🦀️component.rs` absent while canonical `🥒️.feature` and `🦀️.rs` retained the same reviewed bytes. The reported hashes are `5b29888f07e95841032d3bd1229aeced8cd6bcfc11e626b82336ed7604d1c6e7` and `c28219276b76eea08fb12cc403996cb3dca17d6f160d296880a88d33a6225952`. This is a peer observation, not an independently attributed mutation-lane move. No current lane has an Energy write assignment; root communicated no known authored boundary or recovery location and did not restore names or refresh taxonomy pins.
