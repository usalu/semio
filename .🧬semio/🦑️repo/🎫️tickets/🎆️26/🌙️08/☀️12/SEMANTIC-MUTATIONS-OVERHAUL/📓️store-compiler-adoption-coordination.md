# Store Compiler Adoption Coordination

## Ownership

The taxonomy task and demonstrator task requested mutation-lane ownership of the missing required `DESCRIPTORS` and `descriptor` on `SpaceHistoryMutation`. The demonstrator task is `01a03391-2d5d-7093-af02-e591cd23efb5`; it has explicitly deferred this declaration and is waiting for verified readiness before full publication.

The assigned implementation lane owns the exact Store history mutation declarations, direct leaf tree, immediate construction/codec/test consumers, schemas, and relevant tests. The coordinator owns shared trait/derive/vocabulary and serialized Cargo gates. A separate lane independently reviews the Store runtime boundary without production edits.

## Shared Contract Checkpoint

- Both `Mutations` derive mirrors now generate required full descriptor rosters and active-variant selection, `From<Leaf>` conversions, and existing mechanical behavior/semantics delegation. They use the existing independent aggregate source authority, full per-leaf source/provenance validator, variant/semantic identity checks, and roster uniqueness. Generic parameters and where clauses are preserved.
- Eleven genuine derive-crate tests passed, including eleven language-neutral syntactic cases inspected through the third-party `syn` AST. Evidence: `🧪️mandatory-contract-cutover/🧫️run-ReSb7k`.
- The pre-change compiled vocabulary rejected `commit`, `switch`, and `restore` in all three expected red cases (`🧫️run-NHOGHP`). The shared table now contains their literal records `Committed`, `Switched`, and `Restored`. The previously documented mirrored mutation-verb table no longer exists in the current root script; no replacement duplicate table was introduced.
- The first fresh current-contract OS kernel library build passed in 57.43 seconds, with warnings (`🧫️run-BzuBM7`). This clears the reported required-item/verb errors at that compiled snapshot, but does not establish final Store runtime acceptance after subsequent leaf corrections.

## Open Store Review Corrections

The initial six-leaf source packet was not accepted from its static seven-vector roster check. Actual payload schema files, required nullable-field deserialization, leaf-owned tests/regions, and generic envelope validation remain part of the packet. The pre-existing inactive-alternative inverse bug must be fixed with an explicit restoration plan and regression; a known failing law must not be omitted to obtain a pass.

The broader mandatory registry transaction, complete source-provider policy, remaining owners/language surfaces, and global monorepo gates are not completed by this Store repair. No full publication success is claimed by this task.

## Current Integration Evidence

The three neutral contract datasets passed their Ajv schemas (`🧪️mandatory-contract-cutover/🧫️run-ncZeyX`). Genuine current-kernel compiler probes now accept both the required base metadata API and semantic-kind inheritance; all five vocabulary cases pass with stable before/after kernel artifact hashes (`🧫️run-cXpYB7`).

An independent current-kernel public client reproduced the inactive-alternative inverse bug, while its six-operation metadata/From/text/binary check passed (`🧪️store-history-independent-review/🧫️run-lIcuoz`). This red runtime result is retained rather than replaced by the source repair. A second fresh library build passed in 20.52 seconds (`🧫️run-cbOGhf`); final source-stable client replay remains required.

The author is correcting descriptor truthfulness: the two multi-operation inverse contributors declare `plan`; all six list the real Rust/JSON-schema/text/binary surfaces and `apply-only` participation. Null optional wire selectors describe the current generic envelope, not completed native codec-contribution ownership. Ordered-removal restoration and dangling historical references remain explicit semantic inventory issues, separate from required-metadata compiler readiness.

## Reserved Concurrent Flow Changes

Demonstrator task `01a03391-2d5d-7093-af02-e591cd23efb5` owns the new checked admission/generation result from `flow::sync_host_flow_extension_contributions` and its exact `FlowConfigMutation::SetContributions` diff arm in the Flow editor config component. That arm must preserve base config and emit a `MutationOutcome` error on admission failure. Any later mutation migration must preserve those concurrent semantics; this task has made no competing Flow edit.

## Verified Store Compiler Readiness

The final source-stable kernel library build passed in 12.67 seconds with warnings (`🧪️mandatory-contract-cutover/🧫️run-OuSmAP`). The independent public client then passed both tests with successful compilation/listing/runtime and stable source/artifact hashes (`🧪️store-history-independent-review/🧫️run-Pcrs7G`). Its retained binary SHA-256 is `7de991e778b0f656cda2d1d94f39dd66f6a33d41884c217bccdd0ffe4ad3bf0b`.

The client asserts the six-entry required `Mutation::DESCRIPTORS` roster and each active variant's full descriptor equality; actual provenance and schema paths; `From<Leaf>` conversions; all six text/binary codec round trips; required nullable restoration and unknown-field rejection; and the named last/sole-entry inverse cases. The intermediate harness delimiter error is retained at `🧫️run-mT8Alm`; it was corrected before the passing runtime replay and did not require a production change.

This verifies removal of the reported Store E0046 and subsequent approved-verb E0080 blockers. It is not a full publication result, an exhaustive Store history-law result, or completion of the monorepo mutation goal. The open semantic and codec-ownership inventory above remains required work.

Separately, a genuine current-kernel/current-proc-macro generic owner compiled and ran both String/u8 instantiations, testing metadata, `From`, apply, and inverse delegation with a preserved where clause (`🧪️mandatory-contract-cutover/🧫️run-DX38Sz`). Full adversarial aggregate/client and registry/source-policy propagation remain open.

The coordinator independently reran the final public-client harness through scoped Nx. It again compiled/listed/ran both tests successfully with stable complete Store input/kernel hashes (`🧪️store-history-independent-review/🧫️run-KkRU3w`). This is the final bounded readiness checkpoint sent to the demonstrator and taxonomy tasks.
