# SPR Testkit Direct Fixture Contract — Checkpoint 40

## Exact Scope

Root owns only the five concrete mutation fixtures and their existing law-test consumers in framework OS SPR/🧪️testkit/🦀️component.rs, plus their new adjacent test domain. Production law helpers are unchanged. Runtime's OS-kernel R1 is the retained native metadata red; it is not the base-framework Kernel law result.

The direct domain keeps the original i64 snapshot. AddCounter is lawful: ordered structural deltas retain intermediate checked-add admission, and wide differences are decomposed into representable steps. The stored mutation inverse order follows Store's reverse traversal. This preserves the valid MIN boundary without saturating, wrapping or changing the snapshot type.

Four separately named direct payloads retain deliberate test behavior: AddMissingCounter emits the existing missing-target error; AddUncheckedCounter intentionally applies despite that absent target; AddObservedCounter retains skipped shared call state and intentionally produces unequal successive outcomes; AddRejectedCounter emits the existing fatal outcome. Metadata describes these exact fixture commands and does not assert that negative law fixtures are lawful. Their no-inverse/incorrect-inverse behaviors and existing should-panic expectations remain in force.

One transparent aggregate owns only variant routing. Each direct leaf owns its payload, descriptor, text codec and behavior. Binary support is not claimed or added where no current fixture caller uses it. JSON schemas are closed and language-neutral. No Plugin, production Store, Kernel lifecycle or command-staging edit is part of this packet.

## Acceptance

Retain all existing positive and should-panic law tests. Add schema/neutral tests before source mounting, exact direct descriptor and text/serde checks, ordered diff admission/inverse boundary tests and a no-inline-concrete-implementation check. Native OS-kernel execution remains pending the complete fixture cohort and an authorized compiler slot; no readiness is inferred from source checks.

## Source Mount And Executed Gates

All five direct leaves and the transparent CounterMutation roster are now mounted. The five inline manual Mutation implementations were removed and every observed current caller was rewired. Original negative-test names and should-panic expectations remain. The new CounterDiff preserves ordered checked additions, including exact wide between/inverse decomposition; its original i64 snapshot is unchanged.

The source/neutral gate ran red37of49 before sources/descriptors/mounting in 🧪️spr-testkit-direct-40/🧫️run-iFxH54. After mounting and adding actual aggregate-schema negatives, the expanded gate passed84of84 in 🧫️run-ta8Ct8 with stable complete inputs. It compiles the five real payload schemas and aggregate schema with Ajv, validates all actual descriptors, compares independent JSON parsers and exact BigInt boundary expectations, and checks retained negative consumers. The receipt explicitly says rustExecuted:false.

Seven new Rust tests are source-present: five direct descriptor/text/serde/outcome contracts, ordered counter algebra, and MIN inverse Store-order proof. They and the existing positive/negative law tests still require actual native execution. The aggregate carries no binary claim because current law-test callers do not use that surface. No cleanup, evidence deletion or compatibility alias was introduced.
