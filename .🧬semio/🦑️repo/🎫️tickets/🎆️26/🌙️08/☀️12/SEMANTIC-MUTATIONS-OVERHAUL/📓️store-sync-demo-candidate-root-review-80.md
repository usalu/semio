# Sync Demo Candidate74 — Root Source Review

Candidate74 is implementation-ready for a **coordinated source-mount review**, not mounted, Rust-parsed, compiled or native-tested. Root read the complete candidate report, both full Rust leaves, all candidate schemas, the complete controller/splice contract and the retained221 check roster. It independently compared the receipt values and fresh27 input identities/hashes/ancestor identities at 2026-08-28T04:56:43.191Z; all remain equal. The scalar33/semantic-counterexample4/schema57/normalization6 vectors are exactly the earlier73 values.

## Retained Reference and Independent Readback

Actual earlier candidate reference:221/221, no failure, receipt SHA256 `d0730304c6a43171854390eceb036e0d293b90a3745532672d25d61877b7d847`. Root's [current readback](./🧪️store-sync-demo-candidate-root-review-80/🔣️readback.json) verifies all221 named outcomes,37 numeric value/counterexample comparisons,57 actual Ajv expected/observed admission pairs and33 saved Decimal outputs. Full before/final receipt tuples are equal. These are reference/schema/source checks, not execution of the Rust bodies. Six normalization records and the record/codec laws remain explicitly native-unrun.

An initial readback command completed but its tool-output representation was truncated; parsing that returned text failed. The following compact read-only capture above is independently complete. No fixture, source, budget or expected result changed in response.

## Exact Private Module Topology

Only the existing outer private cfg(test) owner gains an explicit current-directory anchor and its real child mount:

```rust
#[cfg(test)]
#[path = "."]
mod tests {
    #[path = "🧪️fixtures/🧮️demo/🧬️mutations/🦀️.rs"]
    mod demo_mutations;
    use self::demo_mutations::{DemoMutation, SetN};
```

The aggregate accesses `super::{DemoDiff, DemoSnapshot}`, with `pub(super)` enum/re-export. The leaf type and n field use `pub(in super::super)`, exactly the existing tests owner. No existing Snapshot/Diff/field visibility is widened. The current Plugin inline-owner anchor is a real source precedent, not proof this new mount compiles. Current outer Sync tests has no external path/include/out-of-line mount to rebase. Native compiler validation of the new topology/derive remains outstanding.

Root's [in-memory forward/inverse proof](./🧪️store-sync-demo-candidate-root-review-80/🔣️inverse.json) applied no file changes. Exactly25 proposed intervals are the owner anchor/mount, deletion of the old inline enum and manual Mutation implementation, and22 original value-expression constructor wraps. Predicted source:267402B SHA256 `d99de55bac711d3e96693ef26dc3860661becb0f2c2e1912d83f3f701be9be64`; actual current source remains267795B `62f31952ccdc84de0b2d6e63e39374ae1baedaec0f7304ff926836dd203806e6`. Reversing only those25 intervals produces that exact full original hash. This predicted endpoint must not be reported as a mounted source endpoint.

The full Snapshot/Diff, OpText and OpBinary intervals are byte-identical; production prefix hash remains9cd4cb1c7f6c0646fe3ab1850c69215fa68dc7c662fbc461b7023a7f8fc467f8. All22 expressions and full codecs remain unchanged, including one-byte format1/ordinal0. None timestamp and conservative may_emit_foreign_steps=true are explicit at the leaf and delegated by the existing derive. No fallback/default/alias or ticket include is introduced into production.

## Concrete Leaf and Domain Laws

Canonical aggregate candidate:593B ac0d9d147982cd134f3597ee1c5b6caa7adc44db86916068a1fff40a4bffd20e. Canonical leaf candidate:16335B 4a33d2fc4c2aa755168c6bec7ad85235b2cf6a5f312a983b30751bd7d14787f4. The aggregate is a visible single SetN(SetN) roster using actual Mutations/DslOps derives. The direct leaf owns payload, set/n semantic identity, actual Diff contribution, pre-state inverse, generated DslRecord codec contribution and authored descriptor/provenance join. Existing outer codecs remain generic framing/delegation.

The adjacent pure domain schema excludes ticket evidence/source pins. The real14-field descriptor declares rust/json-schema/text/binary, not an invented TypeScript surface. Payload/Snapshot/aggregate unknown fields remain accepted; the test envelope is closed separately. The11 codec input values include i32 bounds, zero and existing representative values; six malformed text cases, four malformed binary headers and every encoded truncated prefix are authored against the actual APIs, but not yet executed.

Four proposed native selectors are:

- direct_sync_set_n_descriptor_and_provenance
- direct_sync_set_n_scalar_inverse_and_absorb
- direct_sync_set_n_serde_text_binary_and_timestamp
- direct_sync_set_n_foreign_capability_preserved

Each body uses the actual leaf/aggregate/MutationDiff/serde/OpText/OpBinary APIs. The tests do not copy the Decimal model into Rust. No four-pass, codec correctness, generated provenance or actual missing-item compiler claim follows from source review. Base77e's accepted native syn law is separate.

## Taxonomy and Remaining Coordination

Root read taxonomy's complete [eight-path report](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/📓️store-sync-demo-direct-leaf-path-admission/📝️.md) and root review. Candidate74 preserves all eight Packet73 paths. The two required shared vocabulary strings are 🧮️demo in the fourth-level module-member contract and 🔢️set-n in schema members; current6d06 rejects them. Hypothetical8/8 plus necessity/negative/context checks are not a catalog release. Taxonomy/Runtime now coordinate a separate schema-first vocabulary join.

Do not mount until that vocabulary release and Runtime's exact source review. Only the eight approved leaves and the25 outer-tests intervals are proposed; no Store Fresh/registry/wrapper, production Sync detach/funded-parent, command/Interaction or unrelated native source change. The production detach join remains held separately, so no redundant OS compile is requested. Any eventual native selection must exclude the existing wire-write/delete test and missing-actor fixture namespace.

A separate ticket-only structure verifier is being prepared for actual pre-mount desired RED and post-mount source GREEN; it is not run or a replacement for native validation. Registration410.533 is queued as the unchanged unmounted-candidate reference command, not a mount command.

