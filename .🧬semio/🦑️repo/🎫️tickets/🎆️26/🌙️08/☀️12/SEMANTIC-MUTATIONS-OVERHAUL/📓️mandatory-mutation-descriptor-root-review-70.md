# Mandatory Descriptor Root Review 70

Root read the complete report69 including its correction, then report70, all160 controller lines,37 schema lines and12 vector lines. The current actual base trait independently shows the empty DESCRIPTORS default at119, descriptor fallback at123–126 and sentinel at229. These are three forbidden compatibility source observations, with no authorship or native-result attribution.

The accepted future source edit remains only the two required associated declarations and deletion of the sentinel plus their fallback-specific docstrings. Keep the existing slice type, optional timestamp and other unrelated trait semantics, real leaf provenance, derived aggregate API, and genuinely uninhabited explicit-empty implementations. The earlier head/tail roster redesign is rejected, not scheduled.

## First Executed Source Packet And Its Limits

The agent's first ticket-only source inspection reports the three actual fallback facts, using the existing repository Rust tokenizer/pairer extracted from its TypeScript implementation. TypeScript parses only those TypeScript function declarations, not Rust. No syn, rustc or Cargo ran.

The seven JSON rows in that first packet are deferred case labels, not executed missing-implementation fixtures. Their schema does not bind each id to its intended subject/category. The receipt's fixed six-assertion field does not independently establish six executed assertion calls: the source has three explicit initial assertions and an aggregate violation check. Therefore retain that result as bounded source facts plus successful schema validation, not seven behavioral tests or native RED.

Root requested a superseding, closed neutral declaration-facts schema with explicit inputs and expected violations, wrong-id/category/missing/duplicate negative cases, measured check accounting, and an unmounted syn body inspecting the same actual lower trait. Preserve the first raw run and hashes; do not relabel them after strengthening.

## Compile And Caller Boundary

The real OS6 compile is a separate92-error/zero-test observation. Its outer Sync DemoMutation currently inherits the fallback. The approved84 fixture patch fixes only real synchronous method/call joins; it must not fabricate Demo metadata or claim direct-leaf adoption.

Restoring mandatory items is expected to expose genuine omitted declarations in any manual implementation that still depends on the default. Those must be assigned and adopted under actual direct-leaf descriptors, not suppressed with another default. A current complete caller roster is not yet established; the pending two-call SourceIndex observation supplies source membership/content evidence only, with Rust semantic identity and off-facet completeness still separate.

Before the shared trait edit: finish neutral/syn source preparation, send exact include/caller-plan footprint to the runtime coordinator, obtain the sole native compiler window for the narrow real desired-law check, then preserve actual RED and GREEN separately. No shared trait/derive/facade source change or native command is authorized by this review alone.
