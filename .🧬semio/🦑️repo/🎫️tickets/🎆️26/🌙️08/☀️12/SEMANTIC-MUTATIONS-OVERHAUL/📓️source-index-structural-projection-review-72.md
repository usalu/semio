# Source Index Structural Projection Review 72

## Observed Source, Not Timeout Attribution

Root read the complete current `mutationTaxonomyStructuralDirectories` declaration in root `📜️script.ts` around20702, and its call in `mutationTaxonomySourceIndex`'s returned object. The helper constructs a new Set from every existing supporting-observation Set on every add:

```typescript
supporting.set(path, new Set([...(supporting.get(path) ?? []), observation]));
```

For many distinct files sharing one ancestor, earlier support values are repeatedly copied. The helper is evaluated after the selected-file capture loop, while SourceIndex builds its return value. It currently has no progress or cancellation operand. These are direct source observations; no phase run has yet identified this helper as the cause of the earlier whole-pair timeout.

The existing source-admission collector reports named stages before untracked enumeration, ignored-generator admission, explicit-ticket admission and physical source observation. It returns only after the pure admission projection and membership digest. The next phase diagnostic can distinguish that admission boundary from the injected post-admission SourceIndex region, but cannot by itself name one expression inside either region.

## Bounded Follow-Through

A separate ticket-only neutral desired-law packet is being prepared for exact directory/supporting-observation projection. It must preserve explicit directory observations, file-mode filtering, repository-boundary exclusion, duplicate support and raw UTF-8 byte ordering. An existing third-party grouping/uniqueness implementation will serve as the finite output oracle. Instrumented Set-construction iterator work may make repeated-copy growth observable without increasing runtime deadlines or claiming a wall-clock benchmark.

No root/N/D/shared helper change is authorized by this review, no actual SourceIndex call has been made for this packet, and no allocator/native performance or full-census conclusion follows. A progress/cancellation join must be designed and tested separately if a source repair is justified; an output-only equality test does not prove responsiveness.

Root source endpoint observed: c539f565831cce420d5c755f2efe54362956d0f0d769a29ea3805a2e9f62d005. The first failed pair and later import-only completion remain separate historical observations in reports68/69.
