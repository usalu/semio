# Source Admission IO Gap Regressions 52

## Current Availability And Executed Boundary

The old ticket directories and their linked runs below are unavailable, not retained retrievable evidence. They were not restored. The following original paragraphs are historical observations only. Current canonical tests and newly authored controllers are independently documented in [the source release](./📓️admission-source-release-54.md): the final injected-IO suite actually passed nine tests and 24 assertions at each direct and package entry point, with strict schema validation and stable captured source endpoints. Earlier deficient test revisions remain separate from that final result. No physical-workspace or global mutation coverage is inferred from injected IO.

## Historical Packet

This packet is ticket-only. It does not edit the normalizer. Its controller extracts and transpiles selected current normalizer declarations, then invokes them with injected Git/filesystem callbacks; it is not a full-workspace inventory.

The retained pre-change behavioral RED is [run-ETOKqc](../🧪️source-admission-io-gaps-52/🧫️run-ETOKqc/🔣️.json). It copied and executed source SHA `7e5275065ec8c5b545e3a03f2d6efd243ddff8ac30636827d6f840ca65a6acac`: raw Git spelling was rewritten, unsafe untracked records were silently dropped, FIFO was a file, EACCES was swallowed, nested `.git` was recursed, the collector reread taxonomy bytes, and a virtual opaque scope reached filesystem setup. The source copy and per-declaration SHA-256 slices are retained in that run; the separately observed end hash was `9afb17907b1e14d9fdc5bfe14faa77b95f9121de1463306695fb706a9a248bd4`.

The live corrected replay is [run-9hXTGk](../🧪️source-admission-io-gaps-52/🧫️run-9hXTGk/🔣️.json), source/end SHA `342e780b71b6bd0fc9e6cc66b151e58fa9e78ecf0e149846ab297fe62659b0fe`. It executed malformed UTF-8, missing-terminal-NUL, and interior-empty Git-record rejection; raw physical spelling; anchored literal untracked exclusions before `--`; FIFO-to-`other`; EACCES propagation; symlink-ancestor stop before descendant lstat; nested `.git` terminal observation; directory identity drift rejection; opaque full-setup rejection before callbacks; and loaded taxonomy content-hash return without a reread. The six neutral inputs remain schema-validated. This validates only these injected declaration boundaries, not physical Git or filesystem integration.

The hardened current-only replay is [run-xxmdJZ](../🧪️source-admission-io-gaps-52/🧫️run-xxmdJZ/🔣️.json). It removes the subject override and legacy branch, captures every ticket input through lexical any-case `compose` exclusion, ancestor `lstat`, and `O_NOFOLLOW`, and checks every input hash again at completion. It reports no input drift and no behavioral failure for normalizer `342e780b71b6bd0fc9e6cc66b151e58fa9e78ecf0e149846ab297fe62659b0fe`. The retained-old parser comparison covers `lineLocation`, `regexTokens`, `htmlTokens`, `referenceTokens`, `unsupportedReferenceTokens`, `indexedLineContent`, and `indexedLineStarts`; all seven SHA-256 slices are unchanged. Per-run Markdown receipts are retained with the JSON evidence.

Command executed:

`bun ./node_modules/nx/bin/nx.js exec --projects=workspace --skipNxCache -- bun .🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️source-admission-io-gaps-52/📜️script.ts`

## New Canonical IO Test

The former ticket directories are unavailable and were not recreated. The canonical replacement is new source, not recovered evidence: [IO test](../../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🧪️tests/🧪️source-admission/🧪️io/🟦️.ts), [neutral cases](../../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🧪️tests/🧪️source-admission/🧪️io/🔣️.json), and [schema](../../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧹️normalization/🧪️tests/🧪️source-admission/🧪️io/🧬️schema/🔣️.json).

The first new-suite `9/9` did not execute the collector hash/full-setup or candidate-ancestor assertions and is deficient. It is not evidence for those laws. The revised source now has an exhaustive case dispatcher, executes unsafe untracked rejection, candidate symlink rejection before descendant observation, collector return of `taxonomy.input.contentHash`, and full opaque setup rejection before filesystem callbacks. The revised scoped Nx/Bun run completed `9 pass, 0 fail, 18 expect() calls`; it is new source evidence, not recovery of any missing run.
