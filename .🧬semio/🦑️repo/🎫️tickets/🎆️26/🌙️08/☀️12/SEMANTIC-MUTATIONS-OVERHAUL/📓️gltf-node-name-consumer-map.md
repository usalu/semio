# glTF Node Name Consumer Map

The direct owner is `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🧊️gltf/🏅️standards/🔖️2.0/🪆️subsets/✳️any/🧬️schema/🧬️mutations/✏️🔘️change-node-name`.

Permitted aggregate changes are the TypeScript import and union member, the protobuf import and `oneof` member, and the JSON-schema reference. The GraphQL aggregate is only a comment roster; its direct leaf facet carries the phase operation surface. The Rust aggregate remains a type reexport and variant with no change. Root owns the one shared Rust glue mount.

No additional production consumer was found. Aggregate text and binary codecs are test consumers only.
