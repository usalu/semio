# Mutation Source File-Fact Projection Boundary 64

The next source change is a pure classification of the existing complete source admission, not another inventory. Root read the independent source-kind and coverage/API audits plus the exact test proposal. The new function will sit beside `mutationTaxonomySourceFiles` in the root script and compose that existing regular-file projection with the public taxonomy `fileKindIdForSourcePath` classifier. Its rows retain raw `sourcePath`, nullable `fileKindId`, and nullable `fileRole` for every admitted regular100644/100755 non-gitlink file. Unknown suffixes remain explicit rows, not implicit exclusions. No filesystem read, parser, provider inference, source mutation or new root/ignore list belongs in this function.

The return role uses the existing full taxonomy file-kind role vocabulary, not only the six current ecosystem source lists. In particular, C/C++/headers/Swift/SQL/shell source, WIT/GraphQL/JSON/protobuf schemas and grammar/specification roles must remain visible. `.jsx` has no current registered kind and remains null/null; a language guess is forbidden. A regular executable100755 is accepted; the earlier source-kind research bullet conflating executable with nonregular was incorrect and is not adopted.

The schema-first canonical test triplet is owned by the test agent under `library/🧪️tests/🧬️mutation-inventory/🧪️source-file-facts`. It will validate a closed neutral schema, compare hand-authored expected rows with a test-only independent minimatch longest-chain projection, and run the actual missing-export RED before any root implementation. Complete/rejected admission, raw Unicode spelling and byte ordering, longest extension chain, unresolved/tied kinds, all current source extensions, nonregular/gitlink exclusion, and virtual Compose exclusion are required. Compose is never probed physically. No test result is claimed yet.

Expected root write footprint after the RED: the one public owned row type, the one pure function adjacent to existing `mutationTaxonomySourceFiles`, and the classifier import. Existing source-index capture/consumer graph/output shape are not silently changed by this primitive. The subsequent capture integration must be a separate schema-reviewed cutover because `files` currently controls graph inputs and the public source roster/digest. It must reuse the same admission and exact captured taxonomy bytes, retain explicit unsupported/unclassified coverage, and include newly captured bytes in source-stability evidence. A new parser will not receive credit merely because a filename was selected. No N/admission/parser or native Store code is part of this root footprint.

Current root source before this proposed change remains `5eb9cbfff2f505be52eef456cb6c26a310622f0fabff291a5277306c47d779e4` at the reviewed observation. It will be reread immediately before a narrow patch; no pin restoration is authorized. The independent output schema audit also found missing existing taxonomy/descriptor source-roster role values; that output-schema repair is recorded for its own actual regression, not hidden in this projection.

## Initial Test Packet Review

The first actual reference ran4/4 and the subject reached the intended missing-export failure. Root read its complete schema/vector/test sources before any implementation. That finite observation is not accepted as full test-contract readiness: the claimed NFD path was a literal backslash-u sequence; the Compose rejection was hand-labeled rather than obtained from the existing pure admission projector; modeled rows were cast through unknown while omitting mandatory admission fields; the Gitlink directory used index160000 as a physical mode; and the pure fixture unnecessarily called full workspace `loadTaxonomy`. These are test-authoring gaps, not evidence of production collector failures. The test owner is correcting them with honest captured/supplied identities, actual in-memory admission rejection, explicit typings and retained raw outputs. Root source remains unchanged pending that reviewed correction and actual desired RED.
