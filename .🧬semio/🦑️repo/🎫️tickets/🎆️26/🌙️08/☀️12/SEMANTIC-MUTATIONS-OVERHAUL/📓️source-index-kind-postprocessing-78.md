# Source Index Kind Postprocessing 78

This packet defines one cache key only: exact schema identity plus source path. The neutral schema/vector proves same-schema path reuse, separate-schema nonsharing, and add/remove/order preservation with an Ajv and Lodash reference. It does not alter admission, SourceIndex, source membership, cancellation policy, or the pair controller.

The optional retained-artifact measurement opens only before/after source-path JSONL strings, the retained manifest/receipt, and current D discovery/taxonomy inputs after their hashes match the retained endpoints. It never opens a candidate source file. A child is bounded by a 30-second cancellation request and five-second terminal grace; stdout/stderr and the terminal receipt are retained exactly.

The retained artifact review does not independently recompute membershipDigest: the original admission digest includes diagnostics that pair snapshots do not retain. Its schema endpoint join is manifest-to-manifest, not a schema-role-row-to-receipt proof, and its critical-six result is retained metadata rather than a fresh source identity comparison. This packet does not widen any of those claims or recompute sourceTreeDigest.

## Single Bounded Measurement

The one permitted Nx/Bun measurement retained [run-IDh8CO](../🧪️source-index-kind-postprocessing-78/🧫️run-IDh8CO). Neutral Ajv/Lodash validation completed for all three finite cases: same-schema reuse required three classifications, separate schemas required two, and ordered add/remove required four. The child then entered actual `D.fileKindIdForSourcePath` classification over retained path strings only.

At the fixed30-second parent boundary the cancellation file was written. The child observed it at its 1024-row checkpoint and exited1 within the five-second grace (`terminal=cancel-requested-exit`); stdout contained no completion marker and stderr retains the exact cancellation exception. All captured inputs remained stable. This is not a classifier result, cache measurement, performance attribution, or a reason to change the budget. No candidate source file, SourceIndex, admission, or pair call occurred, and no retry will be run.

## Harness Correction

Run78 cannot evidence D classification cost: its harness incorrectly recomputed the 386KB taxonomy SHA-256 inside every classification-loop iteration, including the uncached path. The actual pair snapshot does not perform that per-path schema hash. Its neutral reference also imported Lodash without using it, and its persisted stdout/stderr were JSON-wrapped rather than byte-for-byte raw logs. These are harness defects, not observations about D or pair behavior. A new unexecuted packet79 supersedes the proposed measurement mechanics; run78 remains only a retained cancellation/capture observation.
