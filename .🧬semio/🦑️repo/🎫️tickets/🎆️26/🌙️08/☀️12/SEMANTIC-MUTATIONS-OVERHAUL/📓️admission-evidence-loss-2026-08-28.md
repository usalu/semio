# Admission Evidence Loss

## Exact Observation

During the 2026-08-28 continuation, immediately after the source-admission projector repair and package-test mount preparation, independent root and agent reads returned ENOENT for three active ticket directories:

- `🧪️source-admission-51`
- `🧪️source-admission-io-51`
- `🧪️source-admission-io-gaps-52`

The ticket itself and sibling Markdown reports still existed. The root's exact `ls -ld` check reported all three directories absent; it did not recursively search the workspace. The pure-projector agent independently found its reviewed neutral vector file missing before the canonical mount. No cleanup, deletion, relocation, Git mutation or restoration was run by the root. Cause and exact disappearance time are not established.

## Prior Executions Versus Current Availability

The conversation's actual terminal output recorded IO28, canonical18, the old behavioral IO RED, the current helper replay, adversarial RED113/126, and the corrected adversarial GREEN126/126. The package-entry test actually found zero matching tests before the new import. Those observations remain historical executions; their ticket artifacts and controllers are currently unavailable and cannot be cited as retrievable proof.

The production normalizer, canonical vectors/test/schema, and the library package test entry remained separate surviving source boundaries. The root will verify them afresh. No old run result, missing source bytes, fixture history or recovery evidence will be fabricated. If exact authored vectors are still retained in agent memory, mounting those reviewed data into their intended canonical source is new source work, not restoration of the lost ticket evidence.

The commands registered at launch orders 410.497–410.499 point into the missing directories. Their field registration history is preserved, but they cannot currently execute. Taxonomy and runtime coordination tasks were notified; no source restore, pin refresh, cache deletion or cleanup was requested.
