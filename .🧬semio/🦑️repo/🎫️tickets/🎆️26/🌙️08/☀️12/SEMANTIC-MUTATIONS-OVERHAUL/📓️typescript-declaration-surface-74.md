# TypeScript Declaration Boundary 74

This ticket-only packet supersedes the 73 reference boundary only. The schema,
vectors, and controller SHA-256 values are
`835919e50c65dfb2fae03636e55075e113b493034e3fbc866e2b8848cbb2b3d2`,
`a53c4a49d57cde5f763137657f5fe8cf69368f94cd983d386ae11ee8cbd86a36`, and
`0eddf0cc7f1bed40bb2ed7087a198410a6cccb37978a7d995dcdea139605a8f6`.

The controller owns a 25-second parent/child deadline. The parent retains its
terminal stdout/stderr in each exclusive run directory. The true current-73
helper RED is retained in
`🧪️typescript-declaration-surface-74/🧫️runs/🧫️run-a7cbdbd7-04dd-4ac7-bb20-5eff1fa6c036`:
it accepts an optional keyed property and a non-exported aggregate. A
non-exported leaf already rejects under 73 and remains an explicit negative.

The corrected ticket-only reference rejects all three negatives by requiring
non-optional keyed property plus exported aggregate and leaf. It pre-admits
every TypeScript standard-library byte with nofollow reads before Program
construction, then serves those bytes from memory; all library hashes are in
the green child receipt
`🧪️typescript-declaration-surface-74/🧫️runs/🧫️run-6a55d069-c96e-4d39-aab1-3f07fbc59db8/🔣️child.json`.
The same receipt separately captures read-only current VCS six-member and GIS
two-member aggregate source closures. This is selected dependency capture, not
full TypeScript dependency stability or actual descriptor-binding proof.

The scoped Nx/Bun parent completed in 28.31 seconds, within its child deadline.
No production descriptors, paths, APIs, D/S/N code, or native commands changed.
