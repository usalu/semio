# FND-DERIVE-ATTRS-05 Evidence

Both mirrored derive sources now return `syn::Result` from mutation/composite attribute parsers, reject unknown and duplicate keys, and propagate parser diagnostics through `compile_error!` before macro generation. Cargo was not run as required; source compilation remains pending the serialized kernel gate.

The permanent neutral fixture is [`🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/✨️derive/🧪️tests/🧬️mutation-attributes/🧫️fixtures/🔣️cases.json`](../../../../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/✨️derive/🧪️tests/🧬️mutation-attributes/🧫️fixtures/🔣️cases.json): 7 mutation and 3 composite rows covering valid input; unknown behavior/key; duplicate snapshot/diff/schema/op; repeated-container duplicate; and malformed value. Each negative row carries the expected diagnostic substring.

Four permanent unit-test functions consume that one fixture: `mutation_attrs_tests::parses_mutation_fixture_exactly` and `composite_attrs_tests::parses_composite_fixture_exactly` in `/Users/ueli/Documents/semio/🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/✨️derive/📦️packages/🦀️rust/📦️glue.rs`, mirrored under the same module/function names in `/Users/ueli/Documents/semio/🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/✨️derive/🦀️component.rs`. The fixture is `/Users/ueli/Documents/semio/🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/✨️derive/🧪️tests/🧬️mutation-attributes/🧫️fixtures/🔣️cases.json`. The glue source uses the source-relative `../../🧪️tests/...` include and the mirror uses `🧪️tests/...`; the tests use `serde_json::Value`, so the existing dev-only `serde_json` dependency is sufficient.

No Cargo command was run by the executor, per serialized-gate instruction. The no-Cargo fixture verification printed `[DEBUG] fixture schema=1 cases=10 mutations=7 composite=3 diagnostics=8`, and a root `📜️script.ts` module import passed. A previous no-Cargo `rustc --crate-type proc-macro` replay against the real glue source is retained under `🧪️derive-attrs/🧪️rustc`; its explicit dependency selection omitted paired metadata artifacts and reported `only metadata stub found for rlib dependency`. That failure does not establish incompatible transitive dependencies and is not parser runtime evidence.

## Independent Registered Gate

The coordinator ran `SEMIO_BUILD_BUDGET_MS=3600000 SEMIO_TEST_BUDGET_MS=180000 SEMIO_TEST_ARTIFACT_DIR=<ticket>/🧪️derive-attrs-nextest bun nx run @semio-tech/dsl-derive-rs:test-quick --skip-nx-cache -- --lib --build-jobs 1`. It passed both compiled glue-source tests and all ten neutral rows: two tests passed, zero skipped. The mirror contains the same two test functions but is not a second compiled crate; four executed tests are not claimed. Evidence: `🧪️derive-attrs-registered-retry.log` and retained nextest artifacts `🧪️derive-attrs-nextest/semio-nextest-fnarno`.

The first registered attempt failed in shell argument forwarding before Cargo because an unquoted nextest filter reached the shell. Its transcript remains in `🧪️derive-attrs-registered.log`; removing that unnecessary filter produced the actual passing run. This bounded strict-attribute packet is accepted. Source ownership has moved to FND-SOURCE-AUTHORITY-07; full metadata propagation is still pending.
