# GIS Configuration Direct Leaf Source Review 41

## Source Result

The seven GIS map-editor configuration operations now have direct owner folders containing Rust payload/behavior, authoritative metadata, and payload JSON Schema. The aggregate is a transparent tuple roster deriving mandatory metadata and DSL delegation. The old inline mutation declaration and manual behavior switch are removed.

The direct kinds are `set-layer-visibility`, `set-camera`, `set-render-mode`, `set-vector-style`, `set-lod-mode`, `set-layer-stroke-scale`, and `set-locale`. Their generic text codec uses those exact full opcodes, with no old short-spelling aliases. The binary ordinal roster remains0..6 and native tests assert each descriptor's tag.

Root read all seven leaf implementations, the aggregate, ordered sparse diff, mounted native tests, and the three actual constructor consumers. The view, example and locale command callers are rewired to concrete leaf payloads. A scoped GIS source search found no remaining old struct-variant constructors for this aggregate.

## Behavior And Schema Contract

Visibility and stroke-scale payloads use required nullable fields. Null removes the map entry; a non-null value stores the exact override, including explicit true or1.0. Inverses capture the actual optional entry rather than conflating absence with the display default. UI commands retain their existing default normalization and scale clamping.

The whole-record config diff is replaced by ordered sparse structural deltas. Empty is a true identity; absorption appends structural steps and preserves rejection order. It neither replays mutation payloads nor substitutes a full snapshot. Non-finite scale assignments are rejected and cannot serialize into null removals. Fifteen new source-mounted native tests cover direct descriptor/provenance/codecs, neutral envelopes/state vectors, exact map inverses, no-op identity, composition and non-finite rejection. Existing configuration tests remain and use the new diff application contract.

The live Rust config and its Rust schema already had seven fields. Their JSON, TypeScript, GraphQL and protobuf companions still advertised five retired selection/hover fields. Those four companion files now match the actual seven-field record; no runtime interaction/selection ownership or default locale behavior was changed.

## Executed Independent Gate

The author's strengthened actual-schema/source gate passed548assertions. Root then tightened repeated-read hash checking so an input reread cannot replace its earlier preimage, normalized descriptor path comparison for Windows, and checked protobuf's exact field count and retired snake_case fields.

The independent root rerun passed **553 assertions** in201.056ms with stable captured inputs. It compiles the actual seven payload schemas, aggregate and diff schema with Ajv; checks nine positive and seven negative envelopes; executes required/unknown-field rejection per leaf; parses19JSON inputs independently with jsonc-parser; and compares neutral forward/inverse and ordered-diff results against jsonc-parser's own modify/applyEdits implementation.

The full receipt is [the independent source run](./🧪️gis-config-direct-41/🧪️runs/🧪️neutral-2026-08-27T18-20-09-986Z.json). All previous controller failures and intermediate passes remain retained.

| Source | SHA-256 |
| --- | --- |
| Config component | `ed7b4d11d5297e986c84f60f3cf871b3425adc0b1e127f04315254594ad73456` |
| Direct aggregate | `07bc3d14e5e9996e733a0f77d9b8d87fb39d10a3d6ace31460480ef8f6f1e1c1` |
| Ordered diff | `6286ef562430931f00dea7a79175a226a0728ddcdb29e0e6046aeb9f2319e82e` |
| Native test source | `615501f2fe426b8cef959f07cc81145534274eaaba9d8b4d993bf51c7e92220e` |
| Root-reviewed controller | `deb5641b1fb388b238aaca4c6c14a798a97a995a5911dd4b64b83fcb4084da44` |

The registered launch command is `⚖️gate🌍️gis🎚️direct-fixtures`, order410.39; taxonomy joined the exact command into both canonical seed and generated launch without executing it.

## Remaining Native Boundary

This is a coherent source/schema checkpoint, not compiled GIS readiness. No GIS Rust test, Plugin build, UI command runtime, full publication or monorepo acceptance was executed. The real GIS configuration/native tests and caller integration must run after the independently owned Plugin/lifecycle source boundary is released. No compatibility default, lifecycle edit, cleanup, source restoration or cache removal was performed.
