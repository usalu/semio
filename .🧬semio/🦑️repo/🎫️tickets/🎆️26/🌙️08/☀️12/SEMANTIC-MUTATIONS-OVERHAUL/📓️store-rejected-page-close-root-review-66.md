# Rejected Page Close Preparation — Root Review 66

The new packet is ready for runtime-owner review, not mounted or native-ready. Root read the complete schema, all three cases/23 close observations, the complete435-line Rust file, the complete reference/source controller, the preparation report, and the actual current wrapper/cursor/returned-owner implementations. No Store production source was edited and no compiler was launched.

The source packet and exact future child-module include are in [preparation66](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/📓️store-rejected-page-close-preparation-66.md). That report identifies all four exact inputs and the two proposed native selectors. The include remains adjacent to Store's existing owned_schema_record_tests, not inside its inline tests module.

## Independent Receipt Review

Root independently read both complete JSON receipt files with no-follow ancestor/file checks, parsed their checks and outputs, recomputed receipt and wrapper-source hashes, and compared the recorded endpoint identities. The displayed review omitted raw page hex in favor of byte lengths and SHA256; all raw hex remains in the original receipts. This read is an inspection, not a second test execution.

| Run | Actual recorded result | Root receipt SHA256 | Recorded endpoints |
| --- | --- | --- | --- |
| reference-KHtCPn | 57/57 | 20ca4528ef6db6a6516241d1d5fd12b25c0268b81d5f714b060c65f4af04a5b0 | 4 stable |
| source-1nwmS9 | 63/63 | 2dd51eb1fd07fcce6ccf49a8f4cb3cb2aea11c59b128800a0b2eb090e87f563d | 5 stable |

Both check counts agree with the full check arrays, no failed check is recorded, and both captured wrapper texts hash to their declared hash. Page lengths are4096;4096+14;4096+4096+4096. Each corresponding numeric released-byte sum equals4096,4110,12288. The additional six source checks are text markers only, not Rust parsing or compilation.

The reference implementation uses Decimal.js arithmetic and jsonc-parser normalized JSON parity with strict Ajv2020 shape rejection. It neither calls the Rust wrappers nor derives expected vectors from them. This supports the numeric/sequence contract only; real owner transfer is left to the unrun native laws.

## Source Review

The registered harness constructs the actual authority then rejects it, observes exact field address/ticket, demonstrates page refusal before reclamation, detaches the exact returned owner and separately closes it. The unadmitted harness constructs the actual rejection wrapper directly. Both use genuine counted field/token owners and original record-page storage/content witnesses. Collected discrepancies precede bounded real-owner teardown and final assertions.

Current wrapper record branches still call the record cursor without checking maximum_bytes. The underlying token cursor removes at most one actual tail page. The fixture deliberately preserves the4096 grant granule even for the14-byte tail, counts actual released bytes, and retains existing zero-item terminal behavior. This is a source prediction of the desired RED, not an executed failure.

No test models production page retirement; the Subject enum only dispatches existing wrapper APIs. No Drop suppression, forgotten owner, cold drain, cloned-root disposer, byte-budget increase, or bypass is introduced.

## Next Authorized Boundary

Await runtime's exact domain/include and compiler-window agreement before mounting. The requested future first action is the test-only include and actual two-law native RED. Only after observing that result should the two record-page branches be repaired within the already coordinated ownership scope.

R17 ArtifactCodec/backbone changes remain Retained-owned. FreshField/FreshVcs, poison/recovery registry work, command/Interaction/return paths, full Plugin publication, and missing retirement49 evidence remain excluded. Existing caches, source, and all historical failed evidence stay preserved.

## Checked Cleanup Bounds Follow-Through

Runtime requested replacing fixed8/32 cleanup loops before mounting. Root read the complete [bounds correction](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/📓️store-rejected-page-close-bounds-66.md), all added helper bodies, and each changed loop. They use checked phase sums: returned owner2; unadmitted rejectionN+3; registered rejectionN+5; refused unstarted authorityN+6, where N is the actual current page count. The extra authority phase corresponds to its separate terminal-state turn. Expected vectors, strict Drop guards, and production Store remain unchanged.

The authored Rust boundary is now ce76c55dbfa74756365226a8be5bcc0c7155853c40336bdf9df4cea583f8cd4f. Agent's one actual source/reference rerun is63/63 at run-sGevqh; that controller does not execute the Rust bound functions. Earlier receipts and the3183a23b source observation remain historical. No native or mount claim is made.

Root has granted runtime's separate OS-kernel6 compiled-source hold. Store and these unmounted test inputs stay unchanged through its explicit terminal release. The Store two-law RED remains queued behind that run and the separate taxonomy path-admission decision.

Runtime subsequently explicitly released the OS6 source hold: its compiler stopped before tests with92errors/66warnings and zero native tests executed. Runtime reports586 selected endpoints and membership stable. This is a reported compile boundary awaiting exact diagnostic ownership, not six behavioral failures or a root-executed result. Rejected-page remains unmounted; no include/repair follows merely from the compiler error count.
