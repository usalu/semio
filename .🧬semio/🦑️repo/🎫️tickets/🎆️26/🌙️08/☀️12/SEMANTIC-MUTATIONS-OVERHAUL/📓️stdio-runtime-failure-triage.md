# STDIO Runtime Failure Triage

## Executed Boundary

The fresh registered `@semio-tech/stdio-plugin:test-quick --lib --no-default-features --build-jobs 1` gate compiled and selected5996 tests, with10 skipped. It stopped at fail-fast after164 executed:158 passed and6 failed. This is an actual failing integration checkpoint; failures are not labeled unrelated baseline without evidence.

The coordinator then ran the exact retained executable through Nx and nextest with fail-fast disabled, four assertion workers and a180000ms total budget. This diagnostic reuse did not rebuild current source. Its first wrapper attempt stopped before assertions because `bun nx exec` inherited the package's `nx` lifecycle name. Invoking the same registered root router directly (`bun ./📜️script.ts nx exec`) avoided that lifecycle ambiguity.

The continuation reached at least completed index2324 of5996 before the total budget expired. The log contains70 assertion failures and11 individual30-second timeouts; no complete-suite pass/fail total is inferred. An independent post-run hash check confirmed that the reused executable still matched `db935e3b010938680a552d9a075edaa2d41b8b981f1686eb7c837214c823f0d3`. The budget helper exits the process on failure, so the script's finally block did not report that check; the coordinator performed it afterward.

Evidence is retained in `🧪️stdio-library-runtime-fresh.log`, `🧪️stdio-library-runtime-continuation/🧪️root-runtime.log` (wrapper failure), `🧪️root-runtime-retry.log` (executed partial continuation), `🔣️checkpoint.json` and `🔣️failures.json`. No other worker's processes were stopped. The helper terminated only this diagnostic's process tree at its budget.

## First Six Failures: Read-Only Diagnosis

Luna inspected the current sources and returned the following evidence. These are queued repair decisions, not fixes or passing reruns.

### BMP Grammar

The canonical mutation grammar requires a nested `entry { ... }` block. The direct insert-palette-entry and replace-palette-entry payload fields omit `#[dsl(block)]`, and printing produces inline `entry=b=...` instead. Both leaves require matching nested record grammar/codec tests; the first failure exposes insert while replace shares the omission.

Sources: `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🖼️bmp/🏅️standards/🔖️v3/🪆️subsets/✳️any/🧬️schema/🧬️mutations/📝️text/📖️component.grammar.semio` and sibling `📥️insert-palette-entry/🦀️component.rs`, `🎨️replace-palette-entry/🦀️component.rs`.

### Binary Registry

The binary artifact's synchronous register function discards the async future returned by `register_composer_entries`. No registry insertion occurs. The existing guaranteed-ready bridge is a candidate, but the executor must validate readiness and propagate/handle the result; merely discarding it again is not a repair.

Sources: `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/💾️binary/🦀️component.rs` and `🧰️framework/🔨️modules/🚪️io/🦀️component.rs`.

### BCF Canonical Fixtures

The BCF demo parses to the expected snapshot but its serialized bytes differ. Current ZIP encoding sorts entries and deflates non-image members; the shipped DSL/pack assets contain older stored/uncompressed ZIP representations. The owner should validate canonical output against an independent ZIP oracle and update both authored demo assets together. This is not permission to weaken canonicalization or silently bless generated bytes.

Owner: `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/💬️bcf`; demo asset paths end in `📚️examples/🎬️demo/🖼️assets/🗣️example.dsl.semio` and `🎒️example.pack.semio`.

### Binary Absorption Domain

The cartesian law composes a two-byte replacement snapshot with a splice at offset1 removing2 bytes. The second operation is invalid on that intermediate state, although valid on the original five-byte base. Keep strict invalid-range validation. The sequential law must distinguish valid composition from rejected second steps and separately assert typed rejection and unchanged state; simply unwrapping every cartesian pair is false.

Sources: binary Raw Any mutation aggregate and its sibling `🔺️diff/🦀️component.rs`. Its central SetSnapshot/Splice representation also remains in the direct-leaf conversion queue.

### BMP Missing Palette Targets

The old regression fixture labels index99 remove/replace as graceful no-ops, while current direct leaves emit nonempty diffs and palette validation rejects the missing target. This is a contract mismatch. The coordinator does not accept the fixture comment as sufficient justification for silently returning a successful empty diff. The leaf must specify a typed non-applicable/rejected outcome, and laws must cover that outcome explicitly. A genuinely intended no-change result requires explicit domain evidence and a diagnostic. Do not weaken externally malformed diff validation.

Sources: BMP V3 Any `🧪️tests/🧬️mutation-regressions/🦀️component.rs`, direct remove/replace palette leaves, and schema diff validation.

## Additional Failed Owner Packets

The partial continuation also exposes failures in DEFLATE streaming, DOCX, DWG, DXF, GIF registration, glTF inference numeric representations, HTML inverse behavior, IFC, JPG subset roster, JSON absorption/i-json inverse, LAS missing-index handling, all nine PDF1.4 direct law tests, PDF1.7 IO/diff/H-signature/X-conformance, PNG inverse, PPTX grammar/fixtures/subsets, and Semio BREP vertex/edge restoration. The exact test roster is in `🧪️stdio-library-runtime-continuation/🔣️failures.json`; it is not an exhaustive diagnosis.

The nine PDF1.4 direct runtime failures specifically revoke any inference that its earlier static checks proved behavior. Long GIF/IFC/MP4/PDF cases timed out under the quick profile and require appropriate level classification or bounded performance correction, not ignored tests. Full current-source compilation and all repository gates remain required before completion.
