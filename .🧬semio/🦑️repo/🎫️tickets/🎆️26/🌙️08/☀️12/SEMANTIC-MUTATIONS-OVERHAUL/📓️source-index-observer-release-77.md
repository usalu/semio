# Source Index Observer Ordering — Release77

## Exact Controller Change

Only [ticket pair controller65](../🧪️source-index-before-after-65/📜️script.ts) changed, at the three prepared sites: an explicit metadata projection; wrapping the real SourceIndex result in that projection; both projected calls before any snapshot materialization.

Before `d9cd27afb325fa28abf5d4d0f14897fbf1892855435459757e1e610eb9fcef46`,26051bytes.
After `f99a4322625afe2b490a800fb9bbe6b1b739e46a35c26142db3405884d0867a7`,26480bytes.
Exact whole inverse recovers the prior d9cd27 controller. The [fresh nofollow preimage](../🧪️source-index-before-after-65/🔣️observer-preimage-76.json) and [three-site inverse proof](../🧪️source-index-before-after-65/🔣️observer-inverse-76.json) are preserved.

The returned metadata object contains only admission, roots, files, sourceRoster, sourceTreeDigest, taxonomySchema, mutationDescriptorSchema and ledger. The two schema byte buffers remain available to the existing snapshot classifier. The first full SourceIndex object's bytes/contents/directories maps no longer escape the projection into the second invocation. This is an object-reachability statement, not forced GC, measured heap release or a new memory limit.

All original snapshot/JSONL/drift/manifest writers, row comparisons, default source admission, cancellation path,120000ms cooperative limit,15000ms owned-child grace and terminal handling are unchanged. No ignore pattern, output exclusion or caller scope changed. Registered410.527 command fields remain unchanged; its current controller availability hash is now f99a4322…, not historical d9cd27.

## Actual Unchanged Desired Law

Root read complete packet76 schema/vector/controller and actual current-source RED before mounting. The same frozen desired predicate ran after the repair through Bun/Nx, exit0, [run-dPsg1u](../🧪️source-index-intercall-write-order-76/🧫️run-dPsg1u). Complete result and terminal capture were read: two direct projected invokes precede all snapshot/direct materialization; two snapshots and existing three nested JSONL writes/one before manifest remain; metadata projection returns a distinct object with the exact eight keys, omits the three content owners, retains schema buffers and passes its VM timeout self-test. Failure=null and all three captured source/vector/schema inputs are stable.

The neutral Ajv/Lodash union case keeps independently admitted outputs; it demonstrates four inter-call artifacts under the old order versus none under deferred materialization. It is finite source/model evidence, not yet an actual paired census.

## Preserved Limits

The controller still buffers progress until the final marker. Schema and source content are persisted as hashes/lengths, not full byte snapshots. If capture2 fails after capture1 returns, the deferred first manifest may also be absent; absence cannot localize failure to the first API call. A late cancellation is passed unchanged into any entered next call; no new after-return cancellation check or cancellation reset was added.

One fresh pair remains pending the short peer N source window. It will retain all observed drift and will not claim raw-byte replay, semantic completeness or whole-workspace stability. This ticket-only edit does not change rootS b505, N01fd, D5ef, base77e, canonical derive files or shared native code.

