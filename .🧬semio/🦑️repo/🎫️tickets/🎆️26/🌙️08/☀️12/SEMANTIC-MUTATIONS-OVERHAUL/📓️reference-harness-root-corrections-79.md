# Reference Harness Corrections — Root Review79

## Classification Harness78 Is Not A Performance Attribution

Root read the complete [78 report](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/📓️source-index-kind-postprocessing-78.md), controller and three neutral vectors. Its actual child terminated after the fixed30-second cancellation request, with no completion result. That failed run remains preserved.

The harness computes `hash(taxonomyBytes)` inside every path iteration, including its uncached loops. That repeatedly hashes386313bytes for each path; the original pair snapshot has no such per-path hashing. The cancelled experiment therefore includes substantial harness-only work and cannot attribute its deadline to D's classifier.

The controller imports Lodash groupBy but never calls it. Its three neutral cases execute its own Map reference, so the report's stronger claim of executed Lodash validation is not supported. The log helper JSON-encodes the stdout/stderr strings; those files are encoded strings, not byte-for-byte raw stream files. Its eight captured inputs also omit the controller itself.

A new ticket-only79 revision is preparing a once-per-subject schema hash, real invoked independent Lodash grouping, exact controller capture and raw pipe logs with stage events. Root must read that complete source before any second measurement. No retry, pair call, collector/classifier edit, cache mount, or budget increase is currently authorized.

## TypeScript Reference75 Remains Incomplete

Root read its complete28-line/10073-byte controller and all23case rows. The first retained attempt failed on a capture-only query requiring manufactured Aggregate where actual VCS exports VcsMutation. No production source was changed.

The failure is not only that query:

- The outcome function never obtains the TypeChecker or resolves aliased symbols. The keyed branch accepts a syntactically named property without proving its leaf binding; the literal branch matches a small list of names.
- The generic-shadow case is not rejected by actual symbol identity.
- wrong-provider and incomplete-syntax IDs have no distinct source construction, while gis-union-selected constructs the ordinary keyed alias rather than a union. Those rows do not exercise their named situations.
- Actual captured imports are remapped to arbitrary virtual leaf filenames without matching the aggregate's original import specifiers. They remain capture-only, not actual binding evaluation.
- The child rebuilds fixture sources and rereads library files after the programs for its claimed input receipt. Those post-hoc reads do not establish the exact bytes each program actually used.
- Parent pipe collection starts after awaiting exit, which can deadlock on large diagnostic output. A cleared timer alone does not repair that transport boundary.

Root froze75 and declined a query-only rerun as a complete integration. The existing failure is retained; no semantic success or production readiness is inferred. A future coherent packet must bind all program inputs before use, construct every named case distinctly, resolve imports/aliases via the TypeChecker, preserve the combined positives and negatives, and retain bounded concurrent pipe collection/terminal ownership.

Neither incomplete reference packet blocks disjoint Sync Demo authoring. They must not be used to mark direct-leaf coverage or an exhaustive census green.

