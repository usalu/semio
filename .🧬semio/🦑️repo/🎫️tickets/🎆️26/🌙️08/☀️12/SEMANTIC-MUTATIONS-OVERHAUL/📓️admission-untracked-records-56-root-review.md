# Git Untracked Record Repair Review

Root read the complete 329-line controller, 339-line neutral schema, and original 610-line vectors before production changes. The packet invokes actual AST-extracted helpers and the unmodified public admission wrapper in a newly owned isolated Git fixture. It does not census the real workspace.

## Executed RED

The first run `🧫️run-VW4niB` reached grammar/physical checks, then a controller null-result assertion prevented the remaining groups. Its failure is retained and is not complete RED evidence.

The null-guard-corrected run `🧫️run-y6ppvv` completed every group with no harness exception: reference 51/51, grammar 22/31, physical paths 12/12, marker observation 1/12, directory byte-name walk 0/1, and isolated Git integration 1/5. Root independently read the failed groups, exact probes, and raw Git byte receipt. Marker rejection happened before observation, so those rejections do not count as successful mismatch checks.

The isolated Git command produced 42 bytes with the exact NFD directory marker `café-nested/`, followed by `ordinary.txt` and `taxonomy.json`. The unchanged wrapper rejected the marker as an invalid path. Separately, the actual Git decoder dropped leading U+FEFF from `efbbbf666f6f00`, and the directory walker changed `owned/U+FEFFfile.txt` to `owned/file.txt`. The original strict physical path predicate passed all twelve checks unchanged.

## Bounded Production Footprint

1. Preserve fatal UTF-8 decoding and NUL framing, while retaining leading U+FEFF as filename data at the two source-admission decoder sites.
2. Replace private `sourceAdmissionUntrackedPaths` with rows carrying raw physical `path` and an explicit `directoryMarker` fact. Decode exactly one transport slash; retain strict physical path rejection.
3. Preserve the marker through origin union and require the final existing no-follow observation to be a directory. File, symlink, absence, other kind, or unsafe ancestry must reject.
4. Update only the canonical IO tests' exact private-helper references. No legacy alias, alternate admission policy, root/skip list, reference-coordinate/parser change, shared Git mutation, or filesystem content read is introduced.

Two new first/later-record BOM parity vectors are being frozen before the final RED capture. Production N remains unchanged at review time. The Plugin command-close native packet is independently held for runtime's sole compiler.
