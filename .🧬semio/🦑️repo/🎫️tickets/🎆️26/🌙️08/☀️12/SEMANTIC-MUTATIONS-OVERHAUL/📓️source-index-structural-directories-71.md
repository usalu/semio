# Source Index Structural Directories 71

## Source Observation

Current root `📜️script.ts:20702–20718` derives structural directories only while constructing a completed SourceIndex return. Its `add` helper creates a fresh `Set` from the current support set plus one observation on every supporting-edge addition. This is a source-complexity concern, not an established explanation for pair timeout `run-8GPcU6`; no SourceIndex diagnostic is run by this packet.

## Neutral Contract

[Vectors](../🧪️source-index-structural-directories-71/🔣️vectors.json) and [schema](../🧪️source-index-structural-directories-71/🧬️schema/🔣️.json) freeze five finite cases: many files sharing ancestors, duplicate support, explicit directory support, gitlink/non-file skips, and raw Unicode UTF-8 byte ordering. The controller compares a Lodash `groupBy`/`uniq` reference output with an exact extracted TypeScript copy of the current production projection.

The extracted path instruments only the one current `new Set([...(existing), observation])` expression. It counts constructor calls and values copied into new sets, producing deterministic finite work facts. The `shared-ancestor-copy-growth` law permits at most 32 copied values but the current exact expression copies 72; this is a desired source-only RED, not a benchmark, timeout diagnosis, or behavioral-output mismatch. The reference/output laws still require the same directory/support rows.

Progress and cancellation are deliberately outside this pure return-time projection packet. Any future source correction must separately retain those SourceIndex/API contracts. No root, N, D, canonical source, SourceIndex invocation, inventory call, or timeout budget is changed here.

## Executed Bounded Evidence

Reference/schema command:

```text
bun ./node_modules/nx/bin/nx.js exec --projects=workspace --skipNxCache -- bun ".🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️source-index-structural-directories-71/📜️script.ts" reference
```

It exited 0 against root S SHA-256 `c539f565831cce420d5c755f2efe54362956d0f0d769a29ea3805a2e9f62d005`, validating the closed neutral schema and all five Lodash reference outputs. The initial malformed schema attempt and the initial oversized data-URL extraction attempt were controller-preparation failures, corrected before this source law ran; neither is a projection result.

Actual exact-source extraction command:

```text
bun ./node_modules/nx/bin/nx.js exec --projects=workspace --skipNxCache -- bun ".🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️source-index-structural-directories-71/📜️script.ts" source
```

It exited 1 at the intended source-only law: `repeated-copy growth shared-ancestor-copy-growth copied=72 maximum=32`. Before that assertion, the extracted current function matched the neutral directory/support output, expected 16 Set constructions, and expected 72 copied values. This RED is finite source extraction evidence only; it does not measure a real repository run or establish the reason for `run-8GPcU6`.
