# Sync Demo Direct Leaf — Root Preparation Review78

Root read the complete [packet73 report](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/📓️store-sync-demo-direct-leaf-packet-73.md), full controller, neutral schema and every vector/source-evidence record, all four proposed payload/intrinsic/aggregate/descriptor JSON files, and the prior topology plan. This is preparation review only.

## Executed Evidence Kept Separate

The retained reference command reports196/196, Nx0; the desired source command reports196/230, Nx1. Root independently compared every common check object: no difference. The34extra source checks are exactly eight absent proposed files, absent mount/import, old inline enum/manual implementation, and22old constructors. All33Decimal outputs match between the two retained runs. The16input hashes match across runs; each source-run leaf identity/hash and ancestor identity/mode matches its endpoint. Root read all named projections, including the34failures. Root did not rerun either controller or compile Rust.

These source failures are a pre-adoption boundary, not Rust semantic failures. Base77e declares required metadata but the current Demo implementation does not. This is an observed source finding, **not a newly executed E0046**; the accepted native R2 parses the base declaration via syn and does not compile Demo.

## Preserved Contract

The proposal preserves the current flat SetN serde envelope, required signed-i32 n, omittable/null Diff.n and existing unknown-field acceptance. All33scalar/diff/absorb/inverse-sequence cases,57schema cases and six explicitly unexecuted normalization predictions are reviewed. The latter are not serde observations.

The actual14-field descriptor schema—not a replica—validates the candidate metadata. Same-value replacement remains applied without messages. None timestamp and actual may_emit_foreign_steps=true must survive aggregate forwarding. The full current OpText/OpBinary bodies stay in Sync byte-for-byte, including format1/ordinal0 encoded as one u8; substituting the generic varint facade is not authorized. All22existing value expressions stay unchanged apart from the transparent tuple wrapper.

## Next Ticket-Only Bodies

The executor is preparing one concrete aggregate and SetN leaf plus four narrow native laws. It must separate a reusable domain-neutral fixture/schema from packet73's ticket-only evidence, source-hash, unmounted-status and proposal envelope. The canonical fixture must not claim historical execution or embed the preparation controller's current-source pins.

No canonical eight-path mount, Sync edit, intrinsic visibility change, derive/glue edit, Cargo invocation, whole outer namespace, wire writer, missing-actor fixture or Retained production change has been authorized.

Runtime has approved read-only taxonomy review of the exact eight paths. Taxonomy received that request; no vocabulary mutation is inferred.

## Privacy Topology To Settle Before Mount

Runtime suggested the real Plugin convention: add an explicit path="." anchor to the existing private tests module and mount demo_mutations inside it. The child can then name its true parent DemoSnapshot/DemoDiff and private fields, avoiding the four previously proposed intrinsic pub(super) qualifiers.

The agent found the current Plugin example (anchored app and its external mutation_fixture child) and is checking effective aggregate/leaf visibility and generated provenance. This is source topology research, not compiler proof. The concrete candidate, actual includes, exact22-splice inverse and taxonomy path release must be reviewed before any shared source write.

Retained's Sync detach/funded-parent join remains separately owned and incomplete. No redundant OS compiler attempt is requested. All73 originals and earlier failure evidence remain preserved.

