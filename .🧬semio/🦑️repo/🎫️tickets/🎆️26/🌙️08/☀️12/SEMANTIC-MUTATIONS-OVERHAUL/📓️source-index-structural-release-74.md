# Mutation Structural Directory Accumulation — Release74

## Exact Repair

Only the private `mutationTaxonomyStructuralDirectories` local `add` closure in [root script](/Users/ueli/Documents/semio/📜️script.ts:20702) changed. It reuses the existing per-path Set rather than copying every prior supporting observation for every insertion.

Before S: `c539f565831cce420d5c755f2efe54362956d0f0d769a29ea3805a2e9f62d005`,2832773bytes.
After S: `b505485c63f9c0d2da273446f44802466b032f2af87e8275a63fb7dcf7a3cb66`,2832845bytes.
One unique72-byte net addition; exact whole-file inverse returns the prior c539 hash. No membership predicates, gitlink treatment, input types, sorting, alias/parser/admission/SourceIndex collection or output-role sites changed. Existing Set aliases remain private until materialized into sorted output rows.

The [fresh source preimage](../🧪️source-index-structural-directories-73/🔣️source-preimage-74.json) and [post-source/inverse proof](../🧪️source-index-structural-directories-73/🔣️source-inverse-74.json) retain full selected identities and the exact old/new block. All other S bytes are unchanged; no restoration occurred.

## Actual Unchanged Test

Root read complete final packet73 controller/schema/five neutral vectors and all ten RED outcomes before editing. Source-only RED `run-tAxLC5` had72 iterator values for16 support edges, above the unchanged32 ceiling under both compilers. Every neutral output already matched Lodash; the failure was finite repeated-copy work, not wrong membership.

The exact unchanged controller/schema/vectors ran once through Bun/Nx after the edit, exit0. [run-yiBPdv](../🧪️source-index-structural-directories-73/🧫️run-yiBPdv) retains all ten Bun/TypeScript outcomes. Every output matches the independent Lodash projection and adjacent Ajv-valid expected rows, and every derived work bound passes. The shared-ancestor case now records2Set constructions and16iterator values rather than16constructions/72values. Duplicate support, explicit directory, gitlink/non-file exclusion and raw UTF8 ordering also pass unchanged.

Actual VM timeout self-test passed. Per-case extracted calls use the remaining30000ms ceiling; the successful complete controller reported878ms. This is not a end-to-end performance claim. Root reviewed the complete outcomes plus terminal failure=null, captureError=null, selected stableInputs=true. Selected source/controller/schema/vector captures are unchanged during the run. Installed test dependency closure is not exhaustively captured.

Controller: `da107a7b65e16e1afcfd30c7b029f52934a9e7c5163a5d4a6fe1d33657ba8e8e`.
Schema: `75fa5b1ba0773d4ea362907d737a7bcb5ccbe2cdc28d9aad77cc54bddd4d2960`.
Vectors: `10a6584c3aa60c1e02a7bbe01967d9fd77c991d2976e28886da0ce667237b15c`.

## Limits

This fixes only repeated Set copying during structural projection, not every cost of path construction, sorting, source collection or reference scanning. Progress/cancellation laws for those larger phases remain separate.

Phase70 completed admission only after its25-second cancellation and stopped at the first selected-file-loop cancellation check. It never reached this structural projection. Therefore neither this repair nor the finite test establishes the cause of pair68's original120-second timeout. No SourceIndex/pair/whole-roster execution was repeated in this repair, and no native compiler ran.

Native base77e/canonical3/glue147e are untouched by this TypeScript-only edit. All historical failure logs and caches remain preserved.

