# Workflow And Registry Integration 32

## Observed Gates

The root-owned scoped kernel build completed successfully using the demonstrator's explicitly loaned `🧪️target-aggregate-current`, two jobs and the default dev profile. The surviving log is `🧪️workflow-registry-integration-32/🧫️run-fJ4e4x/🧪️cargo.log`; Cargo reports completion in 1m38s. The observed coherent pair was rlib `3093e2079441bbb95dcaa978cc066d01daf612e93c7df7ff0248533a4e86638c` and rmeta `684abd57149b2b06d6fb1b326926b38552b2ad0338b79ad58e251ff11c3d283c`.

The actual derive crate then ran eleven tests, all passing with no filtered tests. The surviving log is `🧪️workflow-registry-integration-32/🧫️run-NF2yQk/🧪️cargo.log`. This includes the syn-inspected generated aggregate/registration contract and the genuine SHA-256 source-token oracle. It does not execute the generated miniature registry fixture as a kernel test.

The independent immutable registry client compiled, listed and ran nine tests against that confirmed kernel pair, all passing with stable source/artifact fingerprints. The detailed distinction between the genuine pre-change compiler red, an in-flight artifact-presence rejection and the final runtime green is recorded in `📓️registry-immutable-32-prechange.md`.

After the framework attempt completed, the fresh feature-unified kernel pair was independently hashed as rlib `1894b38a9d4a0e52ae1ca44f9747593a18580cb7cbb86794d188c7273df2ee69` and rmeta `9bde8070560b507ca15c74ef996cb9974a27726258b50958120c95e88e14a8da`. The source-mounted Mini gate then compiled, listed and ran exactly two tests, both passing with stable complete source/artifact fingerprints in `🧪️registry-mini-generated/🧫️run-4UxFWF`. This proves real generated From/metadata/provenance/application/inverse wiring and generated registration conflict propagation without replacement. The separate nine-test registry gate supplies multi-entry atomicity evidence; the one-leaf Mini gate does not claim that by itself. No source-owned generic fixture was executed in this continuation.

## Workflow Compiler Red

The subsequent actual `semio-framework` library build failed with Cargo status 101: seventy-five errors and eighteen warnings. Its complete retained transcript is `🧪️workflow-registry-integration-32/🧪️workflow-compile-33.log`. The new direct leaves contain twelve empty emoji descriptor fields, invalid `crate::os_spr` references in this host, and missing imports for inverse sibling payloads. These are mutation-lane defects, not evidence that the downstream Flow gate is ready.

The direct-owner executor is released to repair those exact definitions, validate all twenty-three descriptors against the authoritative schema, and adopt the async RunSink caller boundary without a blocking wrapper. No compatibility alias is authorized. The independent Run review also identifies Automation serde casing, duplicate-start admission and a partial inverse claim; those require separate actual runtime evidence before semantic acceptance. Root remains the only Cargo owner and will explicitly release the borrowed slot before the demonstrator reruns Flow.

## Preservation Boundary

The controller that ran the successful kernel and derive commands was absent before the Workflow invocation. The original run directories and logs survived. Root did not restore or overwrite the missing controller; the Workflow command ran directly through scoped Bun/Nx with a new non-overwriting transcript. A later inspection also found the earlier result JSON files absent. No deletion, relocation or recovery location is established. All active ticket, source, target and cache contents remain protected.

The framework attempt can rebuild the kernel with different feature unification. Every subsequent isolated client must confirm a coherent current library pair and fingerprint it before and after execution; the earlier pair above is historical evidence only.

## Repaired Compiler And Existing Runtime Suite

The repaired actual `semio-framework` build passed in44.83s, with no compiler errors and six new-leaf unused-variable warnings. `🧪️workflow-registry-integration-32/🧪️workflow-compile-34.log` retains the complete Cargo JSON artifact stream and successful build-finished event. The source-mounted existing Workflow/Run suite then ran exactly47tests, all passing with stable source and library hashes; `📓️workflow-actual-source-34.md` records the current artifacts and exact evidence.

This confirms the required Workflow/Run metadata compiler adoption and the existing suite at this checkpoint. It does not erase the earlier75-error transcript, accept missing aggregate schemas, or resolve the additional independent Run contract checks.
