# Mutation Source Index Capture 66 Plan

## Actual Seam

[`mutationTaxonomySourceIndex`](../../../../../../../📜️script.ts) accepts an injected complete `TaxonomySourceInventory` as its third parameter. Its current selection still combines mutation-root evidence with a suffix regex, while [`mutationTaxonomySourceFileFacts`](../../../../../../../📜️script.ts) already classifies every admitted regular file through the captured taxonomy.

The isolated test will inject admission only; it will not invoke the collector, Git, or a repository census. Its physical fixture will contain copied, hash-verified current taxonomy and mutation-descriptor schema bytes, then small authored bytes under the ticket for source/schema/specification representatives. A single mutation-facet Rust file supplies existing root evidence. The fixture also contains existing JSON/Markdown evidence, an unknown JSX regular file whose file fact is null, and no actual nonregular or Compose target.

Expected future selection is the current file-fact projection for roles `source`, `schema`, or `specification`, unioned with the existing evidence selection. It must populate the same index files, bytes, UTF-8 contents, source roster, and digest. A changed captured Python byte must change the digest. A rejected Compose admission and cancellation must stop before source capture/progress.

## Proposed Footprint

- Canonical neutral triplet: `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🧪️tests/🧬️mutation-inventory/🧪️source-index-capture/{🟦️.ts,🧬️schema/🔣️.json,🔣️.json}`.
- Ticket-only physical controller and retained runs: `🧪️source-index-capture-66/📜️script.ts`.
- This report and retained receipts only.

No root source, admission collector, taxonomy, descriptor schema, manifests, Git state, native build, or Compose path will be changed or traversed.
