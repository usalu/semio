# TypeScript Owned Binding Prerequisites 84

## Actual Read-Only Parser Boundary

Root reread the full identity proposal67, including both corrections, and the current declaration-facts types plus parser typeParameters, members, structure, imports, declarations and result methods at [discovery](/Users/ueli/Documents/semio/🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️component.ts:6479). This is bounded source inspection, not a new off-facet census or executed parser result.

The current facts are intentionally syntax summaries:

- typeParameters validates names/constraints/default syntax but does not retain a binding scope or parameter names in public facts.
- members consumes an optional-property marker and type annotation but exports only member spellings, not the optional marker or property's referenced type.
- structure preserves authored union member strings, but has no explicit intersection form. A non-reference/non-object/non-union intersection becomes unsupported-type; the summarized diagnostic path then records unsupported-type-node.
- Imports retain named imported/local spellings, source specifier, scope path and span without resolving their provider.
- The exported facts explicitly do not claim resolved types or mutation identity.

Consequently the future production resolver cannot honestly reproduce the frozen81 symbol/optional/intersection cases from the existing summary fields alone. The TypeScript compiler reference83 is only an independent test oracle being authored; it does not fill these production facts.

## Required Single-Parser Follow-Through

After the compiler reference executes on all frozen cases, prepare one schema-first, domain-neutral extension at the existing declaration parser. Reuse its scanner, token pairs, type grammar and source-coordinate authority. The missing information is scoped type parameters, exact authored union/intersection members, referenced identifier use sites and property optionality/type spans. Do not add a competing string parser, identifier-name heuristic or TypeScript runtime dependency.

The production mutation join must consume the same captured SourceIndex bytes and descriptor authority. It may resolve only an explicitly supported named import and its actual provider/export from that captured view. Unsupported syntax or unresolved scopes remain explicit completeness failures. Canonical path identity remains taxonomy-derived; virtual fixture coordinates are test inputs, not descriptor path overrides.

No new production API spelling or schema version is selected by this note. Changing the direct descriptor schema requires a complete concrete caller/asset adoption plan, not acceptance of both legacy and new schemas. Existing physical VCS/GIS TypeScript surfaces still lack the required descriptor language/binding declaration; the two captured cases remain capture-only/unsupported.

## Limits

No parser, resolver, descriptor, S/N/D production source, canonical leaf, source index or test expectation was changed. No TypeScript Program or native compiler ran. The existing21 manufactured expected outcomes are not converted into passing implementation results by this source review.

