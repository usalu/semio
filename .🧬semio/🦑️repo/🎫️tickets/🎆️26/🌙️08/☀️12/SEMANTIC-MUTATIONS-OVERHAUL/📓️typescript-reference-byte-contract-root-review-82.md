# TypeScript Byte Contract — Root Review 82

## Accepted Data Boundary

Root read the complete [packet 81 report](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/📓️typescript-reference-byte-contract-81.md), all manufactured file strings and queries, both canonical captures, and the complete final schema including its family and cardinality constraints. The frozen schema is 26981 bytes, SHA-256 `22ae8e17bb0375c95fe80d9d42504b53a36c827aad170b50b08446bf630ab5e7`; records are 56372 bytes, SHA-256 `aca7b4396c3e0e0c66e95bbe329a1e04c0b0cbf86fac28dffff08e397a4baadc`.

These are concrete, closed test inputs, not a semantic binding pass. All 23 ID/family/status triples retain the original packet75 roster. The 21 manufactured files-plus-query subjects are distinct; their authored expectations are 5 bound, 15 rejected and 1 unsupported. The two real captures remain capture-only. The omitted nonexported aggregate and nonexported leaf cases are now explicit.

## Executed Independent Root Readback

At `2026-08-28T05:13:39.873Z`, root's read-only Bun expression exited0 in1.05 seconds. [Complete retained result](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️typescript-reference-byte-contract-root-review-82/🔣️.json).

- JSON.parse and jsonc-parser agree; strict Ajv accepts the final records.
- All 201 unknown-property counterexamples reject. The receipt retains every object pointer and its actual rejection. Root did not rerun the agent's additional18 negatives; its219-check statement remains a separate delegated observation.
- All23 roster triples match75, and all21 manufactured subjects are distinct.
- All18 captured canonical files match their recorded bytes and hashes under fresh lexical/no-follow reads. The eight direct descriptors still omit both TypeScript required-surface membership and a TypeScript binding declaration.
- Schema and record hashes remain unchanged across the check.

No TypeScript Program, TypeChecker, semantic harness, native command or production edit ran. The root check validates data and current capture identity only.

## Required Next Reference Boundary

Use the supplied strings as the sole case subjects, never case-ID source construction. Capture every TypeScript compiler/library input before use and resolve modules only within that captured source map. Actual parse diagnostics must independently confirm the declared syntax completeness; a false completeness field cannot bypass parsing.

The semantic reference must inspect authored AST member multiplicity before TypeChecker union collapse, resolve each selected use-site symbol before named-import alias resolution, and prove canonical provider plus exported symbol identity. Type shape alone is insufficient. Aggregate and leaf export presence, optional properties and unsupported namespace/default forms remain explicit distinctions.

Captured VCS/GIS data keeps its original exported names and import specifiers. Its absent descriptor language authority must remain an unsupported binding disposition, not be inferred from physical source presence. No descriptor change is approved by this review.

A future ticket-only reference controller must retain concurrent bounded pipe collection and terminal ownership. Registration precedes execution. Packet75 and all old failed evidence remain unchanged; no canonical mount, census or all-mutation readiness follows from this data review.

