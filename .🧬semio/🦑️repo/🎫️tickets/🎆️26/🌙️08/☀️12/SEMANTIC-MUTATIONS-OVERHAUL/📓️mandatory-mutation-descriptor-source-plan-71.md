# Mandatory Descriptor Desired-Law And Source Plan 71

Root fully read the superseding172-line declaration-facts controller,94-line closed schema,33-line vectors, the complete revised syn body, the actual derive Cargo manifest, current base-trait default/sentinel region and the three concrete Plugin no-state implementations.

## Exact Source Defect And Minimal Repair

Base source: [replication Mutation](/Users/ueli/Documents/semio/🧰️framework/🔨️modules/📡️replication/🎮️mutation/🦀️component.rs), SHAe5f2f9ce74cc305bcbc23c0d99ab70cc2af54cf299a561f7910d56a7dbbd8385,57347bytes.

Future source change, after the independent desired-law run:

1. Keep `const DESCRIPTORS: &'static [MutationLeafDescriptor];` with no initializer.
2. Keep `fn descriptor(&self) -> &'static MutationLeafDescriptor;` with no default body.
3. Remove UNDECLARED_MUTATION_LEAF and only the metadata fallback-specific docstrings.

No new roster type, optional metadata, facade/derive algorithm change, inferred descriptor, source-provenance change or alternative compatibility path. Preserve the unrelated timestamp/causal/foreign-step APIs and all other base bytes by exact inverse proof.

## Current Concrete Caller Boundary

Root's exact read confirms Plugin NoConfigMutation9522, NoPresenceMutation9603 and NoTransientMutation9689 are genuinely zero-variant enums. Each already explicitly declares the empty slice and an exhaustive `match *self {}` descriptor. They need no change; this is honest absence of concrete mutation values, not a fallback exception.

The approved outer Sync DemoMutation remains a concrete known missing-metadata caller. Its14/65 signature/call repair is separately released at62f31952…; it did not add descriptors. When the base items become mandatory, this caller needs its own real direct-leaf adoption. Do not invent metadata in the84 patch or restore defaults to avoid its compiler diagnostic.

A whole-graph omitted-implementation census is not established: the real SourceIndex pair timed out without a snapshot. Other callers must be checked against their actual source/compiler evidence and assigned real direct-leaf joins. An AST law passing does not prove all callers compile, and an explicit empty inhabited roster remains a separate structural-policy breach.

## Executed Neutral/Owned-Inspector RED

Closed declaration-facts schema SHA77b9a6038889e917b09d6db6154c6d2f18551cf2f138850aaa67dc5061850a69,3783bytes; vectors SHA65220ea34a799d625ccb45bc4c846c059ab6c757c9629680350ad1b4d9d7d55b,1380bytes; controller SHA953309e8c49fea7330db5dded63bd65ac6d517d3a60357a84fad6e2ee42ccfeb,11691bytes.

Actual source-only run-c8f1c0e9… failed at its14th counted check with all three expected current violations. The earlier13 checks covered the complete fixture schema, four concrete fact vectors' violations/acceptance, three schema negatives and the required accepted vector's existence. The final source-stability assertion after the desired-law assertion was not reached; the catch independently captured equal five input hashes, recorded stable=true. Do not report14 passes.

The retained [failure receipt](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️mandatory-mutation-descriptor-70/🧪️declaration-facts/🧫️runs/🧫️run-c8f1c0e9-3dd1-4047-8707-ee3ba6c38088/🔣️failure.json),3185bytes, SHA2f39f6bc0990e4f8a7e2fbd85d4cffa1901023b53d1513f95f7482beb2f4a946, records the actual three true facts. TypeScript only extracts the repository-owned Rust tokenizer implementation; it does not parse Rust. Ajv validates the neutral contract but is not the independent Rust parser.

## Proposed Independent Native Mount

Still unmounted: [revised syn body](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️mandatory-mutation-descriptor-70/🧪️declaration-facts/🦀️syn-test.rs),2968bytes, SHA2bc2741f078ebeacd894dbf6e69fc3c292ba38d46881fbbeda1ec8858d7c30ae.

Proposed canonical owner:

```text
🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/✨️derive/🧪️tests/🧬️mandatory-mutation-descriptor/
  🦀️.rs
  🔣️.json
  🧬️schema/🔣️.json
```

Taxonomy is asked only for read-only path/member review at this point. Once that boundary and the runtime coordinator approve a mount, the sole derive glue addition would be:

```rust
#[cfg(test)]
#[path = "../../🧪️tests/🧬️mandatory-mutation-descriptor/🦀️.rs"]
mod mandatory_mutation_descriptor_tests;
```

The real existing derive package already depends on syn2 with full/extra-traits, serde_json and test-only serde derive. No dependency change is needed. The new test parses the actual base source through syn, requires exactly one public Mutation trait and exactly one of each named item, reads expected camelCase facts from the adjacent neutral JSON, and prints exact [DEBUG] actual/expected JSON before the assertion. No copied/stub Mutation trait is defined.

Proposed one-test native selector:

```text
package: semio-framework-os-kernel-dsl-derive
library test: mandatory_mutation_descriptor_tests::mutation_trait_matches_adjacent_required_declaration_facts
jobs: 2; existing sole-executor target/default profile
```

This independent AST test should actually fail on the current defaults before any base repair. Its later GREEN would prove the real source declaration shape and independent fact agreement, not Rust implementation missing-item diagnostics or all-plugin compilation. Those real consumer compiler checks remain separate.

No Cargo/rustc or canonical path/glue/base source mount has occurred. Request the coordinator's bounded test-only mount/native RED window after taxonomy path release; only then take the exact base source repair and rerun the unchanged law. No Plugin/Store/runtime/lifecycle/codec write is included.

## Exact Path Review And Pending Vocabulary Join

Root fully read taxonomy's212-line path report/query at `END-TO-END-TAXONOMY-NORMALIZATION/📓️mandatory-mutation-descriptor-path-proposal/📝️.md`, SHA5ddc83461ec3a58da2dd06ca20494285ec2252db7b8c9c5539ef9d601055b33b, and runtime's complete coordinator approval. This is review of their executed path evidence, not a root rerun of that oracle.

Root independently checked the current catalog at7800b09ba8644260ba818e0aff7c51bbe9e6271a0bb374b3595790baa3b577d7/386265bytes: `members-of-tests.ownerKindIds` is exactly `["tests"]`, the current member list has10 names and contains no `🧬️mandatory-mutation-descriptor`. The approved minimum is one exact member-name addition. Its existing resolution scope is ancestor-wide under tests, not restricted to the derive directory. No new kind/pattern/alias, dependency or physical exception is included. Taxonomy owns the desired-path RED, one-name patch, unchanged GREEN, inverse and loader release.

The proposed canonical leaves and glue remain unmounted while that source release is pending. Root's no-follow03:27capture retained exact current glue17448e95/basee5f2 and all three prepared leaf bytes. POSIX path resolution independently lands both the glue module and the leaf's actual-source include at the intended existing coordinates. Runtime approved only the previously specified test3+glue mount after the coherent vocabulary release; the actual native test requires a subsequent separate sole-executor lease. Base default removal is still deliberately unmounted.

The path report's Actor bootstrap supplement is outside this mutation test packet and conveys no Actor membership or source-move authority.
