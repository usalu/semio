# Mutation Output Ticket Role Boundary 65

## Selected Narrow Cutover

The output ticket is an artifact destination, not an authored-source declaration. The current mutation CLI incorrectly forwards it to shared source admission. Root read the taxonomy lane's complete output-ticket review and the actual root option/inventory flow, as well as the provisional broader role-split64 proposal.

The selected first correction is deliberately smaller than that provisional proposal:

1. Rename the owned direct `MutationTaxonomyInventoryOptions.ticketDir` field to `explicitTicketDir` with all actual direct callers updated, without an alias.
2. Map that explicitly named direct input to the unchanged shared normalizer `ticketDir` input.
3. Stop supplying the CLI output ticket to source admission in `runMutationTaxonomyCli`.
4. Preserve output publication paths and the specifically selected assignment-ledger path under the output ticket.

There is no new `--authored-ticket` flag, shared CLI option, extra positional argument, ticket resolver, or output/source overlap policy in this first packet. The CLI previously had no separately declared authored-ticket option; its output selection was being reused accidentally. Direct inventory/index callers retain explicit source-input capability. The shared normalizer's tracked/nonignored-untracked/declared-generator/explicit-ticket union remains unchanged. Normal taxonomy CLI, apply, reference capture and generic ticket/path helpers remain taxonomy-owned and untouched.

## Membership Semantics

Removing the automatic explicit-ticket input does not exclude a pathname that has independent tracked, nonignored-untracked or declared-generator authority. No output skip list is introduced. Absence tests must use ignored output-only candidates, with positive controls for independent origins. An explicitly selected assignment-ledger file is a separate input whose digest may legitimately affect output; it is not permission to traverse its ticket ancestor.

The first regression will instrument only the real shared source-admission call in an isolated child, preserving the rest of its actual export family, and stop with a known sentinel before physical capture. It must expose the current output-only forwarding as an actual RED. It may prove request construction, not a completed filesystem census, source-roster membership or publication. Additional existing fixture/caller tests must retain their actual explicit authored input through the field rename. No meaningful success may be inferred from a mock collector or a missing-export test.

## Source And Test Status

S is still the coherent file-facts64 source `fdb34f8e4a9d1696915dc18d804876ed80a1f46c6d09d365f92b24914c5a991d`. This document does not claim an implemented role repair. The role test owner is preparing the actual forwarding RED and exact callsite list before any production patch. The earlier flag/NFC-overlap design is retained as a provisional, unselected design, not silently rewritten into this contract.

