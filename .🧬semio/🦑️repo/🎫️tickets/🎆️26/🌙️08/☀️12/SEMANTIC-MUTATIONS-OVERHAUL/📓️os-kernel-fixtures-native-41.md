# OS Kernel Mutation Fixture Native Gate 41

## Executed Boundary

The explicitly loaned Demonstrator target was used for one jobs2/default-profile `semio-framework-os-kernel --lib --no-run` compile and, only after success and stable captured inputs, exact selected Store/SPR tests.

The compile succeeded in **50.50 seconds**. The native test executable enumerated successfully; **94 tests were selected, 89 passed, one failed, and four were not run** because the controller stops at the first unexpected failure. The captured source set was identical before compilation, before each executed test, and at completion. This is the recorded Store/SPR and supporting source set, not a claim to capture every transitive compiler input.

The controller and full receipt are [the native gate](./🧪️os-kernel-fixtures-41/📜️script.ts) and [the retained run](./🧪️os-kernel-fixtures-41/🧫️run-wKD0uM/🔣️.json). The run preserves raw compiler diagnostics, exact selected test names, each test's stdout/stderr, and before/after source hashes. Expected should-panic law tests returned success and are counted as passes; their diagnostic output is not an unexpected failure.

The89passes consist of59command checks,14testkit checks and16Store checks. Seven further directly affected testkit law checks were absent from this first selection; the next registered gate includes them explicitly rather than implying they already passed.

## Actual Failure

`os_store::component::tests::direct_store_fixture_absence_inverse_boundary` failed in the shared fixture codec round-trip helper at Store component line19616. It attempted to parse `bump-n delta=4`, which the current aggregate codec rejected as an unknown operation line. Exact evidence is [test089](./🧪️os-kernel-fixtures-41/🧫️run-wKD0uM/🧪️test-089.log).

This is a genuine native Store canonical-text join failure, not a lifecycle error, not a parser-controller construction failure, and not justification for an alias. The designated Store fixture reviewer now owns the narrow current-vocabulary correction and a regression that also catches this mismatch in the neutral gate. No success is claimed for the failed test or the four unexecuted tests.

The earlier reported bytes-first codec concern was independently checked against current source. All four current Store fixture codecs already call `decode_record_body(body, &spec, options)`; no argument-order production or fixture edit was required. The actual-API neutral check passed378assertions before this native run.

## Compiler Release

Root explicitly released the borrowed compiler slot to Demonstrator immediately after the terminal result. A fresh exact-name PID check found no Cargo or rustc process. All target/cache/log/evidence inputs remain retained. Plugin, Infinite, Flow VCS, GIS and runtime lifecycle native readiness are separate; no broad publication was attempted.

## Second Executed Gate

After the AddN canonical text repair, the next explicitly loaned gate compiled in **32.858 seconds**. The selection included the seven previously omitted affected testkit laws: **101 selected, 98 passed, one failed, and two were not run**. Captured inputs remained identical. The prior absence/inverse failure passed, as did all seven added positive and negative testkit laws.

The remaining failure was `os_store::component::tests::direct_store_fixture_severity_and_rejection`, which attempted the stale text `clean-n n=7`. [The second receipt](./🧪️os-kernel-fixtures-41/🧫️run-1nmg5m/🔣️.json) and [test098](./🧪️os-kernel-fixtures-41/🧫️run-1nmg5m/🧪️test-098.log) retain the exact native evidence. A read-only opcode census then found six noncanonical Severity/Validated leaf keywords; their repair includes every advertised opcode and a generic neutral-vector native codec helper, without accepting the old spellings.

Root explicitly released this second compiler slot immediately. The next shared DSL integer gate requires its own scoped loan; neither this result nor the GIS553 source/schema assertions establishes Plugin, Flow or GIS native readiness.

## Corrected Native Green

After all six Severity/Validated text opcodes were corrected to their semantic names, root independently replayed the actual schema/source gate:473assertions passed in [run-QS8cnK](./🧪️store-fixtures-39/🧫️run-QS8cnK/🔣️result.json). The current native helper exercises all seven canonical/rejected text vectors across Demo, Severity and Validated aggregates and verifies exact descriptor tags.

The explicitly loaned OS-kernel native selection then passed **101of101**, with no failed or unexecuted selected tests and stable captured inputs. Its compile check took0.802s against the just-built default-profile target. [The final receipt](./🧪️os-kernel-fixtures-41/🧫️run-GnS8MB/🔣️.json) retains all selected names, raw logs and source hashes. Expected should-panic negative laws passed without weakening their assertions.

The same loan separately established the checked-integer DSL regression's six-test red-to-green result, documented in [its report](./📓️dsl-checked-integers-41.md). Root explicitly released the target after these terminal results and confirmed no Cargo/rustc process remained. This completes the selected Store/SPR fixture packet, not the entire OS-kernel suite, Plugin/lifecycle integration, or monorepo mutation acceptance.
