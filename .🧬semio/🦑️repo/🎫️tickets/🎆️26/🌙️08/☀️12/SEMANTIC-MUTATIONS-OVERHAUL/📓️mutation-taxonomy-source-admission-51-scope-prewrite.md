# Mutation Taxonomy Source Admission 51 Scope Repair Prewrite

## Authorized Declaration Footprint

The correction will edit only [`📜️script.ts`](../../../../../../📜️script.ts) and the existing ticket controller/schema/vectors/report.

- `mutationTaxonomySourceAdmission` will always request one full canonical `inventoryTaxonomySources` membership, never forward a comma-separated mutation scope.
- `mutationTaxonomySourceIndex` will accept an optional injected canonical admission for an actual behavioral regression. It will parse and validate comma-separated requested roots after admission, select those roots only, retain external consumer files from the same full admission, and capture regular-file bytes through the existing `semanticOwnedInputFileSnapshot` authority.
- `mutationTaxonomySourceFiles` remains pure and admission-driven.
- `policyFindAllMutationsDirs` will remain pure when injected, use byte ordering, and create a facet only from a directory observation or a proper ancestor of a regular file.
- `mutationTaxonomyInputPath` and `mutationTaxonomyScope` will reject every case-folded `compose` segment before filesystem access.

No normalizer/IO collector, Plugin source, generic filesystem API, structural analyzer behavior, global inventory, Cargo, or native process is in this write set.

## New Behavioral Cases

The mounted-source controller will exercise the actual exported source index with an injected full admission and a real ticket-local regular-file workspace:

1. two comma-separated requested mutation roots select both roots while retaining an external `catalog.rs` consumer from the same full admission;
2. `Compose`, `cOmPoSe/child`, and `owner/COMPOSE/child` reject before any admission-derived content snapshot;
3. a regular file named `🧬️mutations` is not a facet, while a real `🧬️mutations` directory and a regular descendant leaf are;
4. absent and symlink observations create neither content inputs nor phantom roots;
5. source capture uses the existing descriptor/ancestry-stable `semanticOwnedInputFileSnapshot`, rather than a separate `lstat` then `readFileSync` race.
