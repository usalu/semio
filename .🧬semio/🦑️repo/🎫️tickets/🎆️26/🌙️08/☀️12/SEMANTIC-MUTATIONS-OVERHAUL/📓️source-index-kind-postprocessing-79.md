# Source Index Kind Postprocessing 79

This unexecuted revision supersedes only the measurement mechanics of78. It reuses78's frozen schema/vector, now with an actual Lodash `groupBy` reference over exact `(schemaIdentity,path)` keys. It computes the retained taxonomy identity once before all four loops, captures this controller alongside all retained/current inputs, writes child stdout/stderr as raw bytes, and emits start/end pipe events for uncached-before, uncached-after, cached-before, and cached-after loops.

The proposed subject still reads only retained source-path JSONL strings and verified D/taxonomy inputs. Its parent retains the same30-second cancellation request plus5-second grace. It does not alter controller65, D, N, SourceIndex, admission, cache behavior, source membership, or pair policy. Root review is required before any execution.

## Evidence Revision

The preserved preimage controller SHA-256 is `99b44109d70cbd63093297caa40750e6c28279df88d67504e0952180c37dbbad`. This revision changes only evidence mechanics: restore ancestor postread identity checks; authenticate final manifest bytes/hash against the retained receipt result; authenticate both retained source-path JSONL path/SHA-256/byte/row endpoints against their respective manifest artifacts before spawning; retain complete after-capture tuples in the receipt; and add a `preflight` mode that validates only the frozen neutral schema/Lodash reference without starting a child. The measurement command remains unexecuted.

The separate Nx/Bun `preflight` mode exited0 with `[DEBUG] kind postprocessing79 neutral preflight complete`. It started no child and did not read D, taxonomy, retained pair artifacts, or candidate sources.
