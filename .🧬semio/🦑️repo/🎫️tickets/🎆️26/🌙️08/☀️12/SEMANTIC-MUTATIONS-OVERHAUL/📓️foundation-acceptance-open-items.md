# Foundation Acceptance Open Items

The initial foundation tests prove the implemented subset, not completion of every foundation requirement in the governing brief. The coordinator identified these remaining acceptance gaps while the direct-owner fleet continued. None is waived.

## Scaffolding

`newScaffoldMutationTree` currently creates comment-only TypeScript, GraphQL, protobuf, text, binary, and test companions. Its binary tag remains null and the function does not run the scoped structural verifier. Required closure: real visible schema/union/codec/test contributions, duplicate identity rejection, deterministic/idempotent behavior, and an all-policy postcondition. Test skeletons must never be accepted as completed mutation behavior.

## Mutation Derive

The current authoritative `Mutations` derive checks single-field tuple variants and the leaf's semantic kind/approved verb. It does not yet enforce the full direct-owner descriptor/wire contract or emit the specified leaf conversions. The source and package glue both expose this implementation; their ownership and generation relationship must be resolved before edits. Shared contract changes require coordinator review and a coordinated consumer cutover, not default metadata or compatibility shims.

## Inventory

The inventory currently leaves `commandsEditorsViewers` empty and all fleet assignment fields unassigned. Consumer discovery and ticket-ledger reconciliation still need to satisfy the full inventory contract. Counts of direct components are not evidence that all consumers or behavior have moved.

## Policy Hardening

Completed corrections now reject nonexistent/escaped/opaque/symlinked selected scopes, report missing aggregate sources at high severity without aborting the inventory, reject inline aggregate payloads and empty aggregate files, and catch orphan folders even with no enum variants. Exact token-based Rust codec identity matching is under regression verification; non-Rust surface-content validation and helper purity still require independent acceptance audit.

## Assignment

The foundation lane will finish Sequence, then audit these current authoritative mechanisms and submit a bounded test-first closure packet. It must not mutate shared APIs until the write set and consumer impact are frozen. The coordinator continues independent policy and integration verification.
