# Store R17 Codec And Backbone Ownership

Root read the complete runtime proposal `INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️os-kernel-r17-owned-repair-proposal-2026-08-28.md` and the current canonical Store declarations. No Store edit or native compile was made by this task during this review.

The actual Store file is `🧰️framework/🛍️products/💻️os/🔨️modules/🏪️store/🦀️component.rs`, not a nested OS-kernel Store path. `ArtifactCodec.compile_dsl` and `print_mirror` currently erase futures without `Send`; their two nested implementation thunks also omit the enclosing constructor's stronger bounds. `replace_backbone_retained` currently allocates its retirement box after replacing the live backbone. `detach_backbone` currently clears the envelope descriptor before the fallible revision bump and returns the taken backbone. The generic wrapper later forwards that result. These source observations agree with the proposal; they are not fresh compiler results.

There is no active or reserved mutation-lane write in these codec slots/thunks, retained-backbone replacement/detach methods, exact backbone retirement owner, or the corresponding preadmitted displaced-owner slot usage. Runtime/Retained may own their bounded schema-first/native-test-first correction and necessary exact forwarding/call-site joins. Preserve real `Send` worker requirements and derive required concrete generic bounds from compiler evidence; do not use unsafe Send, local execution fallback, new worker pools, or compatibility APIs. Changes to additional codec slots or unrelated registration contracts require their own concrete consumer evidence and an announced footprint.

The detach acceptance boundary remains transactional ownership: admit the original backbone's exact empty retirement owner and destination before taking any channel/message owner; preserve descriptor/revision/backbone/session/request state on occupied destination, overflow, allocation/construction error, or unwind; only successful transfer authorizes the session state transition and one detach request. Use the existing displaced-retirement domain and a real bounded close path. A returned original owner must not be ignored, cloned for disposal, or silently dropped. A Send compile result does not prove bounded codec work, cancellation, or retirement.

The pending mutation-lane Store packet remains separate and unmounted: `FreshFieldDecoder`, `FreshVcsAuthority`, `ArtifactEnvelopeFieldDecoderRegistry` poison/quarantine/returned-ticket recovery, existing envelope/catalog grammar and exact owned-field cleanup. This release does not transfer those regions or permit weakening their ownership laws. Retained should publish the exact source delta and compiler/test selection before a fresh native source capture; this review itself imposes no Cargo slot or source hold.
