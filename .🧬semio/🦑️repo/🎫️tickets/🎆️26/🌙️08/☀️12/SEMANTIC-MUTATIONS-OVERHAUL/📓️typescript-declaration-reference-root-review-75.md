# TypeScript Declared-Surface Reference — Root Review75

## Reviewed Evidence

Root read complete packet74 controller/report plus schema and vectors from73, and the current aggregate-binding proposal's final correction2. The74 endpoint is ticket-only; no production resolver, descriptor schema, or canonical mutation change is approved by it.

Packet73's finite union fixture obtained a real old-reference RED for VCS/GIS synthetic union selection, imported-name shadowing, and optional discriminants, then an8-check ticket reference result. Packet74 separately demonstrates two additional current73 acceptances: optional keyed property and unexported aggregate. The unexported leaf negative already rejected. Historical counts and positive fixture identities remain separate.

## Concrete Remaining Gaps

- Packet74 `strictSynthetic` creates a TypeScript Program, but its final result checks only AST export/optional flags. It does not obtain the checker or verify that the used type resolves to the intended leaf symbol.
- Its three manufactured negative cases do not rerun73's union positives or combine the fixes into one binding algorithm.
- `capturedClosures` hashes actual VCS six-import/GIS two-import paths. It does not execute binding checks against those actual source closures; capture alone is not source-case acceptance.
- The parent races a25-second timer but never clears it after a child exit. Whole command28.31seconds is not evidence of a28.31-second child completing within25seconds. After kill, no explicit awaited exit code is retained.
- Standard-library bytes are guarded before each Program's reads, but later independently recomputed library hashes are not a stable before/after binding of every exact cached Program input. No full dependency stability is established.

The three74 boundary negatives are useful finite observations, not a coherent production reference or a released declared-surface join. All original packets/runs remain preserved. No new revision or production mounting has been requested from the agent while root reviews the combined contract.

## Required Next Integrated Boundary

A future single closed neutral packet must exercise the same combined checker-symbol resolution across existing positives, shadow/optional/export/ambiguity negatives, and distinct actual captured source cases. It needs one pre-admitted in-memory dependency view whose exact bytes are hashed at use, no path override in descriptor authority, and a terminal parent receipt with cleared timers/awaited exit. Production must remain a repository-owned pure projector over the single captured source index; no TypeScript runtime dependency or second identity ledger.

This review does not authorize schema expansion, canonical renames, source collector changes, Cargo, native readiness or off-facet completeness.

