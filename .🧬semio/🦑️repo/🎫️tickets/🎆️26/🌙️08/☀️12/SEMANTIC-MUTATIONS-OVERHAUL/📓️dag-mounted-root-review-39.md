# Mounted Dag Packet — Independent Root Review 39

## Reviewed Source

Root read the transparent fourteen-variant aggregate, all fourteen direct Rust leaves, the ordered DagDiff/DagDelta application implementation, generic text/binary routing, and all eight new owner tests. Delete/Disconnect retain captured indices, Delete stores descending-edge inverses before node creation to match Store's reverse traversal, and Rename preserves the exact endpoint suffix. DagDelta remains structural data rather than a replayed mutation envelope. No second concrete codec mutation enum remains.

The actual Infinite crate aliases protocol and dsl to the OS kernel; its leaf trait paths therefore differ from the OS kernel's own test fixtures, where command traits are available under crate::os_spr. Root checked the actual Cargo/glue source rather than inferring an alias error from the leaf spelling.

## Independent Executions

Root reran the current authored schema gate:16actual schemas compiled,1294assertions,0failures. Root reran the mounted-source gate:155assertions,0failures. Both verify complete captured input hashes across their run. Logs are retained in 🧪️dag-mounted-38/🧪️root-schema-39.log and 🧪️root-mounted-39.log.

Root also reran its unchanged six-case schema regression, originally red before the schema repairs. All6passed with stable inputs in 🧪️dag-schema-independent-36/🧫️run-BEoIkY. These are actual Ajv/schema and source/neutral executions, not Rust or Store runtime execution.

Current reviewed component hash:7f8304396e52b870749cca06cb20d0a3121def313f1f1a5a15602997169426b0. Aggregate hash:7c0e42a4f5681443b36c62988b9e8d43d367ad427c0c1730bba7bcfd83de06e3. Owner neutral fixture hash:6df87fc18008631f04783b1e0917e3d75d897ca05d99456e6cd1d5ebc82d6919.

## Remaining Acceptance

Native execution of the14leaf contracts,8owner tests and existing Dag VCS tests remains pending runtime's Plugin/lifecycle coherence and an authorized compiler slot. Exact u64 endpoint behavior is only source-present in Rust tests; JavaScript numeric validation does not prove it. Missing-target empty-diff behavior is preserved, and all14descriptors remain apply-only. File-region structuring still needs final consistency cleanup before whole-owner acceptance. No full Dag/runtime or monorepo acceptance is claimed.
