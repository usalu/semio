# Source Index Pair Post-Structural Suitability 75

## Reviewed Inputs

The current root S entry is SHA-256 `b505485c63f9c0d2da273446f44802466b032f2af87e8275a63fb7dcf7a3cb66`. Its only relevant change is the `mutationTaxonomyStructuralDirectories` add closure at `📜️script.ts:20702`: an existing support `Set` is mutated with `add`, while a new `Set` is constructed only for a first support edge.

The unchanged AST-extracted structural controller recorded [run-yiBPdv](../🧪️source-index-structural-directories-73/🧫️run-yiBPdv): all five frozen cases under both Bun and TypeScript compilers matched the Ajv/Lodash reference and stayed within their derived iterator ceilings. The raw receipt binds that observation to S `b505…`, stable selected inputs, and a passing VM timeout self-test. This is finite projection evidence only.

The proposed full pair controller remains SHA-256 `d9cd27afb325fa28abf5d4d0f14897fbf1892855435459757e1e610eb9fcef46` at [📜️script.ts](../🧪️source-index-before-after-65/📜️script.ts). It invokes the public `mutationTaxonomySourceIndex` twice, serially, at lines 226 and 228 under one 120-second cooperative window and a 15-second owned-child grace.

## What One Fresh Pair Can Establish

If both calls complete against stable selected inputs, the controller retains complete admission-observation JSONL rows, including ordered origins, index entries, repository boundaries, and generator outputs; exact admitted source-path JSONL rows; source-roster rows with role/path/SHA-256; source-tree and membership digests; schema endpoint hashes; and full admission/path/roster drift rows. It therefore can make a bounded, exact row-and-digest comparison of admission and source membership for those two calls.

It cannot establish direct raw-byte equality for every indexed source: `mutationTaxonomySourceIndex` builds byte/content maps, but `snapshot` persists only source-roster SHA-256 rows, paths, summary digests, and schema path/SHA-256/byte-length endpoints. Neither snapshot artifacts nor critical capture persist raw schema or source bytes. A matching roster hash is a hash-based source-content observation, not a retained pair of every source byte. It also remains neither a semantic-completeness result nor an explanation of a timeout.

The deterministic structural fix removes the demonstrated repeated-copy work from one post-file-loop projection. It makes a fresh unchanged-budget pair meaningful to attempt after the source endpoint release, but does not prove either SourceIndex call will complete within that fixed total window.

## Missing Progress Evidence

The pair child's progress callbacks at lines 222–224 accumulate only in local objects. The child writes its only stdout marker after both SourceIndex calls, both snapshots, drift, and the final manifest at line 243. It writes `before-manifest.json` only after the first SourceIndex returns and its snapshot writes complete.

Consequently, a future incomplete pair still cannot distinguish root/discovery import, first invocation entry, admission internals, post-admission root/schema/root selection, selected-file snapshot loop, first snapshot serialization, or second-call entry from its child stdout. Empty stdout means only that the final marker was not reached; absent `before-manifest.json` means only that the first call-plus-snapshot boundary was not completed.

Phase70 remains separate evidence: its injected single-call experiment recorded canonical admission completion after 26.401 seconds, then cancellation at the first selected-file-loop check. That isolates neither the historical pair timeout cause nor a guaranteed duration for the unchanged pair.

## Suitability

No run was performed. With no budget increase, fallback, or scope reduction, one fresh pair is suitable solely as a bounded admission/source-membership-and-hash-roster observation after the peer source endpoint release. A terminal failure must remain a retained incomplete observation, not a membership result or a performance attribution.

On any completed future result, duplicate source-path and source-roster role/path keys require independent validation against the whole retained JSONL arrays: the controller's drift `Map` comparison is not itself proof that duplicate-key rows were preserved distinctly.
