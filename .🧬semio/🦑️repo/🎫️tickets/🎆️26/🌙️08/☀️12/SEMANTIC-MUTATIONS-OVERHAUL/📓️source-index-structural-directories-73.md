# Source Index Structural Directories 73

## Superseding Law

This packet supersedes the exploratory exact-expression probe71 as a future regression candidate. Its canonical neutral vector is [🔣️.json](../🧪️source-index-structural-directories-73/🔣️.json); it does not require the current Set expression, exact constructor count, or current repeated-copy measurement to remain unchanged.

The closed neutral schema preserves five finite projection laws: shared ancestors, duplicate support, explicit directories, gitlink/non-file exclusion, and raw UTF-8 byte ordering. Ajv validates the vectors and Lodash `groupBy`/`uniq` independently derives the required output rows.

## Exact Source Adapter

The controller uses the TypeScript AST to select only the actual unmodified `mutationTaxonomyStructuralDirectories` and `mutationTaxonomyCompare` declarations. It evaluates that source twice, through Bun and TypeScript transpilers, in a bounded VM. A lexical `Set` replacement is supplied by the VM, not by rewriting the target declaration. The replacement preserves Set behavior while observing constructor count and iteration over existing support sets.

Each case derives `supportEdges` directly from its supplied observations. The desired iterator ceiling is `supportEdges × maximumIteratorMultiplier`; no vector embeds an expected current bad metric. All ten compiler/case outcomes are retained before a final source-law failure is reported. This is finite work instrumentation, not a wall-clock benchmark or an explanation for pair timeout68.

The controller enforces a 30-second internal deadline, no-follows and hashes selected controller/vector/schema/root inputs before and after, and writes exclusive `raw.json`, `result.json`, and `failure.json` under its owned run before surfacing any RED. Progress/cancellation policy remains deliberately outside this return-time projection law. No SourceIndex invocation or production edit occurs.

## Preserved Finite Observation

The final bounded command was:

```text
bun ./node_modules/nx/bin/nx.js exec --projects=workspace --skipNxCache -- bun ".🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️source-index-structural-directories-73/📜️script.ts"
```

The earlier [run-8I6amL](../🧪️source-index-structural-directories-73/🧫️run-8I6amL) remains preserved as a finite source-only observation: all ten Bun/TypeScript compiler/case outcomes were retained and `shared-ancestor` exceeded its 32-value ceiling with 72 iterator values in both compilers. It used `new Function` and checked the deadline only between cases, so it is not the bounded regression endpoint.

Two earlier 73 runs remain retained but are not law evidence: `run-uq8xy7` injected a non-lexical Set and falsely observed zero metrics; `run-8ySrGD` exposed a controller temporal-dead-zone error before a result.

## Executed VM-bounded Source-only RED

The superseding execution is [run-tAxLC5](../🧪️source-index-structural-directories-73/🧫️run-tAxLC5). It evaluates each extracted declaration call with `node:vm` and the remaining 30-second budget, and its VM timeout self-test passed. Its [raw JSON](../🧪️source-index-structural-directories-73/🧫️run-tAxLC5/raw.json) captures current S SHA-256 `c539f565831cce420d5c755f2efe54362956d0f0d769a29ea3805a2e9f62d005` and the exact AST-extracted declarations. Its [result JSON](../🧪️source-index-structural-directories-73/🧫️run-tAxLC5/result.json) retains all ten outcomes: every output matched the Lodash/Ajv contract, while `shared-ancestor` measured 72 iterator values against 16 support edges × multiplier2 = 32 in both compilers. Its [failure JSON](../🧪️source-index-structural-directories-73/🧫️run-tAxLC5/failure.json) records the intended RED, stable selected inputs, no capture error, timeout self-test success, and 525ms elapsed.

The selected root and ancestor stability comparison uses only device/inode identity; their full metadata remains forensic capture. Exact selected files additionally require unchanged identity, bytes, and SHA-256. This remains a finite source-only RED, not a wall-clock benchmark or a demonstrated cause of the phase70 cancellation.
