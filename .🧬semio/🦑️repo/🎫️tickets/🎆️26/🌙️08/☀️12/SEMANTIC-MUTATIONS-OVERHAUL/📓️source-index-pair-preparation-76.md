# Fresh Source Index Pair Preparation 76

One unchanged controller65 (`d9cd27afb325fa28abf5d4d0f14897fbf1892855435459757e1e610eb9fcef46`) is suitable for a new bounded observation after source endpoint release. It will invoke the public SourceIndex twice serially, retaining complete admitted observation rows, selected source paths, role/path/content-hash roster rows and explicit changes. The fixed120-second cooperative allowance and15-second child-terminal grace remain unchanged. No scope narrowing, injected membership, fresh cancellation token after cancellation, fallback, or retry is planned.

Root read the complete controller and the independent [suitability review75](source-index-pair-post-structural-suitability-75.md). Correction to that review: neither ordinary source bytes nor raw schema bytes are persisted by this controller. Schema entries retain only path/hash/byte length. Therefore every content comparison is hash-based; no raw-byte replay proof is claimed. Root will independently validate uniqueness and the full retained arrays because drift Maps alone could collapse duplicate keys.

The deterministic structural-support repair is proven only by the unchanged finite10-case suite and exact root inverse. Phase70 did not reach that helper, so the old pair timeout cause remains unproven. This new attempt is justified by the repaired repeated-copy cost, not an assumed causal diagnosis or promised completion.

The current child buffers progress callbacks and emits only its final marker after both calls/snapshots/drift. An absent marker is not evidence that import or SourceIndex invocation did not start. Any terminal failure will preserve raw output, cancellation, selected input tuples, partial manifests and unavailable boundaries separately.

Status at authoring: no new pair has started. Root S is released b505485c…; taxonomy is completing a separate N required-lease-entry change. Start only against its reported coherent endpoint and record the actual fresh source hashes, without a whole-workspace freeze claim. Registered410.527 is the existing command. The later observer-effect finding below supersedes the initial assumption that no controller edit is required. Native base77e/canonical3/glue147e remain separate and unchanged.

## Pre-run Observer-Effect Finding

Before any new pair, root ran exact read-only Git checks for three existing run JSONs: structural73/run-yiBPdv/raw.json, phase70/run-y7MmVK/receipt.json, and pair65/run-8GPcU6/critical-before.json. All have zero index rows, `git check-ignore --no-index -v` exit1/no output, and `git ls-files --others --exclude-standard` explicitly lists each. They are concrete nonignored-untracked inputs, not automatically excluded merely because their parent is an output ticket.

Current d9 snapshot writes three JSONL artifacts and before-manifest.json after the first SourceIndex call and before the second. Under the required shared admission union these newly materialized outputs can legitimately enter the second observation set (and .json meets the root evidence-file selector). This is source/transport evidence of an observer effect, not an executed paired-census result. It also does not explain the old timeout.

The planned unchanged pair is deferred for a bounded test-only ordering repair: capture both metadata views before materializing their reports, releasing each full source byte map after projection. Do not modify ignore policy, omit independently admitted outputs, add a root skip list, or change membership authority. Keep the existing time limits. A neutral/source desired-law packet is being prepared; no new controller or pair is yet approved or run.

