# 🟦️ TypeScript Declaration Test Mount

## Bounded Source Ownership

The production packet is a single new declaration-facts region in `🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/🔍️discovery/🟦️component.ts`, between the existing RustStructure and Discovery regions. It must not change N's path-reference lexer, source admission, Rust metadata/provider resolution, registry imports, root census or native source. The existing TypeScript package facade already exports D; no second runtime export join is required.

The ticket57 schema and independently authored compiler-reference vectors are being extended before production implementation. Valid empty input is a complete empty declaration summary, not a missing-file assertion. Unrepresented alias/type/body syntax must retain explicit incompleteness. Source spans are UTF-16 half-open offsets in unchanged source text. Declaration facts do not establish mutation/provider identity or global census completeness.

## Canonical Test Footprint

After the reviewed neutral packet is coherent, mount one canonical owner at library `🧪️tests/🧪️typescript-declaration-facts` with an empty TypeScript test leaf, neutral JSON vectors and nested schema. A neighboring test-only oracle leaf will own the TypeScript compiler projection so canonical tests and retained ticket controllers can reuse one implementation without importing the test runner or adding a runtime compiler dependency. Earlier ticket inputs and receipts remain preserved as historical evidence; no migration script or restored source is needed.

The canonical test must validate both the independent reference and actual D facts against the same closed schema and authored values, retain exact diagnostic and declaration spans, and verify source ordering and invalid-language refusal. It must not replace goldens with observed subject output or count an absent export as a pass. Root will review source boundaries and actual errors before publishing a readiness claim.

The existing package router is `📦️packages/🟦️typescript/📜️script.ts`, with `TestScript.run` branches dispatching `runTestBudgeted`. Its Nx target authority is `📋️project.json`; package commands call Nx. The full package test owner is `🧪️index.test.ts`. The planned narrow joins are one `test typescript-declaration-facts` branch, one target, one package command and one test import. No existing test branch or transaction helper will be rewritten.

Taxonomy owns the canonical launch seed/output join. Supply exact row fields and controller hashes only after these source files exist and parse; a registered row is not evidence that its command passed. No new launch row is currently claimed as registered.

## Current Evidence And Limitations

The existing actual-subject test is RED for missing `inspectTypeScriptDeclarationFacts`. The independent old oracle passed 14 authored cases but omitted some forms that could hide declarations or aliases; the read-only review records those gaps. The new schema/reference packet must establish its own desired-law RED/GREEN before the production parser result is accepted. No full-workspace content census, native compile, scaffolding or mutation move is part of this source/test mount.

## Executed Canonical Mount

The preceding status is the pre-implementation checkpoint. The exact 28 authored vectors and schema are now mounted in the canonical owner. Their byte hashes match the preserved ticket originals: vectors `d2e0f14760acaced4fc376b69b7ff2a82d77e0cf7b4850fddb85d8857a72fe48`, schema `22bf492d7445532cae0d86f2735224d1f0e20dd167a2b02ecb7dbbe4c63b3774`. A single neighboring test-only compiler oracle now serves both the canonical tests and ticket57; no runtime compiler dependency was added. Type-safe modifier/initializer narrowing and explicit void callbacks changed no expected facts. Ticket originals and prior receipts remain untouched.

Actual canonical reference R1: 29 passed / 29 filtered, 420 assertions, exit0,222ms. Actual full R1: 58 passed /0 failed,873 assertions,exit0,236ms. Actual full R2 adds strict standalone new-region and independent-oracle source-type checks:60 passed /0 failed,879 assertions,exit0,871ms. Complete terminal outputs are preserved in the neighboring `📓️typescript-declaration-test-mount-61` folder. This is not full-package type checking or mutation identity resolution.

After eliminating the duplicated ticket oracle, the actual ticket57 subject command passed28/28 plus reference28/28 again at `🧪️off-facet-typescript-declaration-census-57/🧫️run-MQICfr/🔣️result.json`. All five captured hashes agree before/after, including canonical oracle `90eb895166df3e9bb659b070e7a028c4e3bddb8971635a7566e85d309e2e56d1` and revised controller `3ae174f1c9bb50b8dce421ac9e604e0a4735d564b39cfb07e180b8f18e650e7f`. This capture is five selected inputs, not the complete imported dependency graph.

The new package branch, target, package script and full-test import are mounted and were actually executed through Nx. Endpoint hashes: router `7525e38f04b572ed3d24195ee7efaa4c513d1f648e815e6bf2fa7f59664f5e88`; project `cdd9cf079bf900f7539ea65c77a83f5e4a164626b691955b13a72fc65e157331`; package `ee524ae11df31331cb151e024191c0826b15a3ea15c73c92ffa6d0ed57ebcfd1`; index test `6519c832d011c814ad8ace8e068ea364921847b96bbd006eb1adc297d501fc9d`; canonical test `a224c567034dd221ff63509e9a1dcc9519fa1e089caba83e9308e632a04c6454`.

Independent self-review reproduced two malformed-member inputs incorrectly reported complete by initial D `2ef678c682fc621f14a7f8557ef16f98ce19a22ab6a10530c73c9a96c304ec3e`. Those findings remain open in a separate schema-first fail-closed packet. Initial60 green must not be presented as exhaustive grammar or census acceptance. Production root inventory has not been changed to consume these facts.

## Reviewed Current Parser Checkpoint

The preceding open-member status is historical. The new malformed12 canonical packet reproduced73PASS/12FAIL, then passed after actual member/header grammar consumption. A separate valid-but-unsupported predicate case reproduced87PASS/1FAIL because the subject falsely labeled it a syntax error; conservative recovery corrected that diagnostic. The known valid index-signature precision issue then reproduced89PASS/1FAIL before an explicit unsupported boundary replaced the incorrect computed-property classification. All old fixtures and failure outputs remain retained, with no expected-value replacement.

Current root verification: canonical90PASS/0FAIL/1065assertions/3.11s,10selected inputs stable; unchanged bounded reference5/5 and subject5/5 with4selected inputs stable. The full current-source report is [current-r4-green](./📓️typescript-declaration-test-mount-61/🧪️current-r4-green.md). The prior independent root88 and reference-only44 checkpoints remain separate historical receipts. New-region identity is `1d655b87e4947defca6d0a1e84354870b47d5f8655c8f0764b26f60b9c877b11`; observed whole-D663a38f5 also contains taxonomy's disjoint source-manifest correction. No new mutation/provider identity, whole-TypeScript support or repository census completeness is claimed. The next bounded packet classifies existing admitted source rows; capture/inventory integration remains unfinished.
