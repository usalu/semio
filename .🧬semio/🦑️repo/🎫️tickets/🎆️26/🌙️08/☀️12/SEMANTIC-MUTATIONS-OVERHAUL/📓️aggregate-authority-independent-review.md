# Aggregate Authority Independent Review

The private aggregate resolver shares workspace, compiler-path, taxonomy, filename, and workspace-token authority with the accepted leaf derive. It requires the canonical primary directly in the mutation collection, not a leaf primary or historical filename. It does not yet activate mandatory aggregate metadata or registry behavior.

The first registered run failed with `E0382` in a virtual excluded-path fixture. The second compiled and ran all ten tests, with nine passing and one incorrect diagnostic expectation for the outside-collection fixture. Both red logs are retained. The corrected fixture remains inside the ticket and still requires rejection; no OS temporary directory was created to force a different boundary.

The coordinator's final registered derive command passed ten tests, zero skipped, exit 0: `🧪️aggregate-authority-registered-final.log`, Nextest run `9f48a7f0-2979-47c3-98c9-8021006816db`. The neutral aggregate fixture includes eleven source-location cases; the two aggregate tests also compare shared leaf/root authority and distinct physical-workspace tokens.

Because common authority/token code changed, the coordinator rebuilt the actual kernel and proc macro in the ticket target. `🧪️aggregate-authority-kernel-build.log` records exit 0, 25.42 seconds, 33 warnings. Fresh genuine lower/kernel compiler clients then passed twelve primary vectors, the foreign-workspace case, and the kernel facade case: `🧪️aggregate-authority-public-replay.log`, run `🧪️public-leaf-derive/🧫️run-VYqgqV`, exit 0. These clients compare all fourteen descriptor fields and all six provenance fields against independent expected values and Node SHA-256 results.

This accepts FND-AGGREGATE-AUTHORITY-14 only as source-authority infrastructure. Mandatory base Mutation metadata, source-policy provider resolution, aggregate per-leaf enforcement, conflict-safe full metadata registration, and production root conversions remain required.
