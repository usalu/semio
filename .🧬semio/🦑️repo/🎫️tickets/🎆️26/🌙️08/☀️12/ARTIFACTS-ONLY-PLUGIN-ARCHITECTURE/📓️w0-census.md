# W0 Census — Artifacts-Only Plugin Architecture (APA)

Synthesized 2026-08-12 from five read-only scout reports (`📓️w0-a-escape-hatch.md`, `📓️w0-b-plugin-shape.md`, `📓️w0-c-purity.md`, `📓️w0-d-sdk-surface.md`, `📓️w0-e-peer-state.md`) plus `📓️w1-mechanism-design.md` and `📓️status.md`. This file is the single authoritative dispatch document for every downstream APA wave. Line numbers are a snapshot as of 2026-08-12 ~15:20–15:32 — two peer tickets (UNIFIED-COMPOSABLE-ARTIFACT-SYSTEM, SEMANTIC-MUTATIONS-OVERHAUL) are editing this tree concurrently and lines WILL drift. Every finding below carries a grep-able anchor string alongside its line number for exactly this reason — re-find by anchor, don't trust the line number blind.

---

## 1. Verdict

**APA W1 (framework mechanisms) is clear to start now, scoped to framework-side files only, negotiated per-file with UCAS.** APA W3 (per-plugin migration) is **blocked on all 33 plugins today** — zero clear a skeptical NOW test (scout E, §5).

Unblock signals, per wave:
- **W1** (`ArtifactDeclaration`, `Registrar`, capability gating, `genesis()`): needs UCAS's **C1 unfreeze** for `🔌️plugin/🦀️component.rs`, and a per-file negotiation for `🎠️kernel`/`🚪️io`/`🧬️schema` (UCAS currently owns these per their hot-file table). UCAS already ceded registration-consolidation ownership to APA (`📓️status.md` "Cross-session negotiation: SETTLED"), so this is a scheduling gate, not a design dispute.
- **W2** (policy regions, report-mode only): APA holds the `📜️script.ts` write slot **now**, first in a three-way order APA → UCAS-W6 → SMO — announce before/after, do not queue behind the others.
- **W3** (per-plugin migration): gated **per plugin** on `UCAS wave4-report exists AND SMO lane released AND stdio green`. Today: stdio itself is red from UCAS's in-flight subset renames, and every plugin crate depends on stdio, so **no plugin is compile-verifiable right now, including `🪐️space`/`🔋️energy`** which SMO's own status.md contradictorily calls both "released" and "running" in the same file (§6 below) — do not trust either claim without a fresh ping. `🗄️stdio` itself is UCAS's hard exclusive claim; never touch without their "roster frozen" broadcast.
- **W4** (escape-hatch deletion, both `💻️os` copies): gated on all W3 call sites converted.
- **W5** (flip policies gate-blocking, flip `pluginChildDirs`, full verify, close): gated on everything above green.

Bottom line for a dispatching agent: **start W1 framework work now on files UCAS doesn't currently own; do not touch `🗄️stdio`, any plugin root/app/artifact tree, or `🔌️plugin/🦀️component.rs` until the unfreeze signals in §6 fire.**

---

## 2. Violation inventory

### Class A — OS-host registry escape-hatch family

Scope: the 14-function `register_*` family duplicated byte-for-byte between `🧰️framework/🛍️products/💻️os/🦀️component.rs` (ROOT) and `🧰️framework/🛍️products/💻️os/🖥️host/🦀️component.rs` (HOST). 137 grep hits (`rg -n --type rust "register_mesh_exporter\(|register_mesh_importer\(|register_mesh_dwg_export_handler\(|register_mesh_dwg_import_handler\(|register_solid_exporter\(|register_solid_importer\(|register_2d_export_handlers\(|register_dwg_import_handler\(|register_app_io\(|register_os_media_export_handler_kind\(|register_os_media_import_handler_kind\(|register_artifact_descriptors\(|register_artifact_descriptor\(" .`) = 28 definitions (14 fns × 2 files) + 109 call sites. Of 109: 40 internal to ROOT/HOST, 69 external. `register_os_fixture_json` checked separately: +2 more external sites. Of the 71 total external sites: **2 compliant, 61 production violations, 8 test-only (excluded)**.

| Plugin | Call sites | Kind(s) | Verdict | Anchor |
|---|---:|---|---|---|
| 🏭️process | 1 | `"3d.process"` | **COMPLIANT** — own `⚙️engine` | `register_mesh_dwg_import_handler("3d.process"` — `process3d/🦀️component.rs:44` |
| 🌀️procedural | 1 | `"3d.procedural"` | **COMPLIANT** — own `⚙️engine` | `register_mesh_dwg_import_handler("3d.procedural"` — `procedural3d/🦀️component.rs:649` |
| 💠️lowpoly | 15 (7×`"3d.lowpoly"` + 8×`"3d.mesh"`) | own kind + co-declared ownerless kind | **VIOLATION** — wrong layer (`🔧️setup`, not artifact `⚙️engine`) | `pub fn register_lowpoly_exports() {` — `🔧️setup/🦀️component.rs:10-24` |
| 🧩️puzzle | 18 (2d:2, 5d:8, 3d:8) | `"2d.puzzle"`/`"5d.puzzle"`/`"3d.puzzle"` (all self-owned) | **VIOLATION** — wrong layer (app files, not artifact `⚙️engine`) | `register_puzzle2d_exports` `◻2d/🦀️component.rs:1366`; `🖐️5d/🦀️component.rs:2004`; `🧊️3d/🦀️component.rs:2686` |
| 🪐️space | 3 (`register_app_io`×1, `register_os_fixture_json`×2) | n/a (app registration + fixture registry) | **VIOLATION** — wrong layer (command handler + plugin root) | `fn apply_app_registrations` `🎮️commands/🧭️navigation/🦀️component.rs:78`; `ensure_space_fixtures_registered()` `🦀️component.rs:30-31` |
| 🎪️demonstrator | 25 (5+8+2+10 across 4 panes) | `"3d.process"`/`"3d.procedural"`/`"2d.map"`/`"3d.cad"` — **none owned** | **VIOLATION** — foreign-plugin registration, worst offender | `🎪️panes/🏭️bearbeiten/🦀️component.rs:34-38`, `🌱️generator/🦀️component.rs:20-27`, `🗺️verfolgen/🦀️component.rs:19-20`, `📐️koordinator/🦀️component.rs:20-29` |
| 🌍️gis | 0 | `"2d.map"` (owns, unregistered) | **GAP** — demonstrator is sole handler; needs new `🚪️io` code | `rg` against `✏️s/🔌️plugins/🌍️gis/` for the family: zero matches |
| 📐️cad | 0 | `"3d.cad"` (owns, unregistered) | **GAP** — demonstrator is sole handler; needs new `🚪️io` code | `rg` against `✏️s/🔌️plugins/📐️cad/` for the family: zero matches |

Cross-ownership note: `"3d.process"`/`"3d.procedural"` are double-registered (owner + demonstrator both write the shared `HashMap` — last-writer-wins, a correctness risk beyond layering). `"3d.mesh"` is architecturally ownerless — co-declared in `💠️lowpoly/🗿️artifacts/💠️lowpoly/🦀️component.rs:265` (`mesh_artifact_kind()`) alongside lowpoly's real kind, consumed as a schema tag by `📸️remodel`/`🌍️gis`/`🌀️procedural` but registered by no one but lowpoly itself.

`MeshExporter`/`MeshImporter`/`SolidExporter`/`SolidImporter` SDK trait+struct family: cleanly framework-owned, one file each (`🧰️framework/🔨️modules/🔺️mesh/🦀️component.rs:811-882`; `🧰️framework/🔨️modules/🧊️3d/📐️brep/🧰️kernel/🦀️component.rs:1238-1316`), 6+8 implementors, zero plugin overrides — no violation here, UCAS has ceded its deletion to APA per status.md.

### Class B — Plugin directory shape

SSOT: `🔣️taxonomy.json:376-381` `pluginChildDirs = ["🛂️manifest","🎟️capabilities","🔧️setup","🎛️apps"]` — the inverse of APA's target — enforced by 3 live call sites: `📜️script.ts` (soft, `function policyPluginRootShapeBreaches(repoRoot: string): BreachRecord[] {` :4646), OS registry codegen (soft, `const pluginChildDirs = TAXONOMY.pluginChildDirs;` :1224), and a **hard `assert!` gate** at `🔌️plugin/🦀️component.rs:2226-2235` (`let plugin_child_dirs = string_array(&taxonomy, "pluginChildDirs");`) that panics on mismatch — any wave deleting these facets must flip taxonomy.json AND this assert together, deferred to **W5** per status.md.

All 33 plugins carry the standard 7-piece baseline (`🎛️apps`/`🎟️capabilities`/`📦️packages`/`🔧️setup`/`🗿️artifacts`/`🛂️manifest`/root `🦀️component.rs`). 30/33 `🔧️setup` facets and 32/33 `🛂️manifest` facets are 1-line doc-only stubs; all 33 `🎟️capabilities` facets are doc-only stubs (zero real `.capability(` calls repo-wide). Exceptions and extra dirs are the per-plugin table in §3 below (column 2/3) — reused verbatim from scout B's table, not repeated here to avoid duplication.

Real-code exceptions to the doc-only rule:
| Facet | Plugin | Lines (real) | What | Anchor |
|---|---|---:|---|---|
| `🔧️setup` | 🌍️gis | 6 | 4-call fanout to artifact engines + app schemas | `pub fn register_gis_exports() {` |
| `🔧️setup` | 💠️lowpoly | 20 | 15-call fanout, all `register_mesh_*`/`register_document_codec_for_app` (Class A violations) | `pub fn register_lowpoly_exports() {` |
| `🔧️setup` | 📕️norm | 48 | 45-call fanout across 15 standard artifacts (register_pilot_languages / register_artifact_schema / register_artifact_inferences ×15 each) | `pub fn register_norm_exports() {` |
| `🛂️manifest` | 🗄️stdio | 344 | 28-entry `FormatDescriptor` catalog, region `🔖️FormatCatalog` | `pub fn stdio_format_descriptors() -> Vec<FormatDescriptor> {` |
| `🎟️capabilities` | *(none — real call lives at plugin root instead)* | — | `🪐️space/🦀️component.rs:63` `.local_backbone_storage()` inside `pub fn plugin()` — the one real capability call repo-wide, in the wrong facet entirely | `.local_backbone_storage()` |

26 `role="extension"` crates under `<owner>/🧩️extensions/<name>/📦️packages/🦀️rust/` across 6 plugins (flow=9, imperative=5, process=4, cad=4, sourcing=3, playbook=1), every one verified `extends="<owner>"` by direct Cargo.toml grep — structural-axis question **open** (§6).

### Class C — Purity (IO/state outside artifacts)

Scope: `✏️s/🔌️plugins/` (7848 `.rs` + 5461 `.ts`/`.tsx` files). Test-only hits classified separately and excluded below.

| Sub-class | Real prod count | Concentration | Anchor |
|---|---:|---|---|
| Filesystem (`fs::`) | 35 | 26 in one file (animate's video-cache/mp4 pipeline), 6 more in animate's present engine, 3 in a `build.rs` | `🎞️animate/🗿️artifacts/🎬️present/.../⚙️engine/🎥️video/🦀️component.rs` (multi-line, e.g. `fs::create_dir_all(&scene_dir)` at `:50`) — **inside `🗿️artifacts`, sanctioned location** |
| Env/process | 3 (non-tooling) | `SEMIO_ASSET_BASE_URL` in gis `🎛️apps` (violation — wrong layer); 2× `temp_dir()+process::id()` in animate `🗿️artifacts` (sanctioned location) | `std::env::var("SEMIO_ASSET_BASE_URL")` — `🌍️gis/🎛️apps/◻2d/.../🗺️map/🦀️component.rs:77` |
| Network (`fetch(`) | **2** | Both inside `🎛️apps`, not routed through an artifact — **cleanest violation in the census** | `resolvePresentationAssetUrl(embodiment.src)` — `🎞️animate/🎛️apps/🎬️present/📺️renderer/⚛️react/🟦️component.tsx:3303,3352` |
| Mutable ambient state (real, not write-once) | ~79 sites: `thread_local!`×6, `Mutex<`×17, `RefCell<`×37 live (10 more dead doc-comment), `Cell<`×2, `Atomic*`×17 | 44/79 inside `🎛️apps` specifically | see per-plugin table below |
| `render()` mutation | 1 | thread-local scratch mutated mid-render, `&ArtifactView` is immutable at the trait level | `refresh_texture_cache(projection)` — `💠️lowpoly/🎛️apps/💠️lowpoly/🦀️component.rs:343` |
| `type Draft =` bypass | **54/54 = 100%** | Every app uses `NoDraft`/`NoDraftMutation` — systemic gap, not per-app bugs | `rg "type Draft\s*="` |
| `seed()` | 1 impl total | Pure, dispatches through normal `ArtifactCommand` path | `fn seed(store: &mut ArtifactStore<VcsSnapshot, VcsDemoMutation>)` — `🌿️vcs/🎛️apps/🌿️vcs/🦀️component.rs:183` |

Per-plugin real-mutable-state breakdown (only plugins with a hit; 0 elsewhere):

| Plugin | Location | Payload | Anchor |
|---|---|---|---|
| 🖍️draw | `🎛️apps` thread_local | `RefCell<DrawSession>` gesture FSM | `static DRAW_SESSION` :164 |
| 💠️lowpoly | `🎛️apps` thread_local | `RefCell<LowpolyScratch>`, mutated inside `render()` | `static LOWPOLY_SCRATCH` :48 |
| 📐️cad | `🎛️apps` thread_local + `🗿️artifacts` Mutex | `RefCell<u64>` preview tick; `OnceLock<Mutex<String>>` last-contribution slot | `static CAD_PREVIEW_SEQ` `🎛️apps/📐️cad/🦀️component.rs:947`; `last_cad_computer_contributions_json` `🗿️artifacts/.../⚙️engine/🦀️component.rs:851-852` |
| 🧩️puzzle | `🎛️apps` thread_local×2 + Mutex×5 + LazyLock registry | `PUZZLE3D_MESH_REGISTRY` (written from sibling `🎮️commands/🖌️brush/`), memo fields, 2 play sessions | `static PUZZLE3D_MESH_REGISTRY` `🧊️3d/🦀️component.rs:68`; `pub static PUZZLE3D_MESH_REGISTRY: LazyLock<Mutex<...>>` `🧊️3d/🦀️component.rs:68` |
| 🌊️flow (extensions/math) | non-standard `🧩️extensions/` subtree | `Cell<u64>` RNG seed | `static ENTROPY_SEED` `🧩️extensions/🧮️math/🦀️component.rs:291` |
| 🪐️space | `🎛️apps` OnceLock+Mutex ×2 + LazyLock side-effect gate at root | `shared_presence_peers`, `shared_studio_ports` (process-global, cross-session) + `FIXTURES: LazyLock<()>` used as a run-once gate | `🪐️space/🦀️component.rs:24` (`static FIXTURES`); `🎛️apps/🪐️space/🦀️component.rs:115-116`; `🎛️apps/🏠️home/🦀️component.rs:73-74` |
| 🌀️procedural | `🗿️artifacts` Mutex | `static LAST: Mutex<String>` | `procedural3d/.../⚙️engine/🦀️component.rs:60` |
| 🏭️process | `🗿️artifacts` Mutex×2 | `CONTRIBUTED_MACHINE_CATALOGS`, `LAST_PROCESS_CONTRIBUTIONS_JSON` | `process3d/.../⚙️engine/🦀️component.rs:137-138` |
| 🪵️sourcing | `🗿️artifacts` Mutex×2 | `CONTRIBUTED_SOURCING_MODULES`, `LAST_SOURCING_CONTRIBUTIONS_JSON` | `🗂️curate/.../⚙️engine/🦀️component.rs:590-591` |
| 🎞️animate | `🗿️artifacts` Mutex + Atomic | `Arc<Mutex<f64>>` rate field; `Arc<AtomicBool>` closed flag | `⏱️rate/🦀️component.rs:499` |
| 🔱️trinity | `🎛️apps` Cell + non-standard `🗣️language-service/` OnceLock | `Cell<i8>` log-throttle; cached-parse slot | `last_logged_lod: Cell<i8>` `♻️rewrite/🌍️world/🦀️component.rs:292`; `OnceLock<GraphManifest>` `🗣️language-service/🦀️component.rs:14` |
| (17 apps) | `🎛️apps/<app>/🌉️wasm/` | `store: RefCell<XStore>` wasm-bindgen bridge boilerplate | grep `store: RefCell<` — flagged UNVERIFIED whether this is a framework-mandated mechanism (W1 concern) or per-app (W3 concern) |
| 15 apps' `AtomicU64`/`AtomicU32` ID counters | remodel/shooting/note/architect/forms/puzzle2d/3d/5d | monotonic-ID pattern, low severity | grep `AtomicU64\|AtomicU32` |

5 non-standard plugin subtrees flagged as independent structural violations (neither `🎛️apps` nor `🗿️artifacts`): `🌊️flow/🧩️extensions/`, `📖️playbook/🧩️extensions/` (+4 more owners, §B), `🔱️trinity/🗣️language-service/`, `🖍️draw/🔄️fsm/✨️macros/`, `🧩️puzzle/🔨️modules/`.

### Class D — SDK surface & dependency graph

Tail re-export block `🔌️plugin/🦀️component.rs:9630-9654`: **exactly 2 glob re-exports** — `pub use semio_framework::*;` (:9645) and `pub use ui_wgpu::wgpu::*;` (:9654) — both flagged for replacement with the curated list. 4050 raw `semio_framework_plugin::` occurrences across the plugin tree reduce to **302 distinct imported symbols** (27 used by all 33 plugins) — full dedup table in `📓️w0-d-sdk-surface.md` §2, this is the M3 curated-list source material.

Cargo census: 63 plugin/extension `Cargo.toml`s. **17 crates depend on the forbidden host crate `semio-framework-os`:**

| Plugin | Symbols used (count) | Notable |
|---|---:|---|
| 🪐️space | 36 | Dominant consumer by far — structural outlier, near-host crate (`APP_REGISTRATIONS`, `OsSpaceCatalogEntry`, VFS root id, backbone port wiring) |
| 🎪️demonstrator | 8 | All `register_*` (Class A violations) |
| 🧩️puzzle | 6 (cfg-gated, wasm32 excluded) | All `register_*` (Class A violations) |
| 🖨️raster | 5 | `DwgColor`/`DwgDrawing`/`DwgEntity`/`DwgGeometry`/`rasterize_svg_to_png_base64` |
| 📏️layout | 4 | `DwgColor`/`DwgDrawing`/`DwgEntity`/`DwgGeometry` |
| 💠️lowpoly | 4 | All `register_mesh_*` (Class A violations) |
| 🎞️animate | 3 | `dwg_drawing_to_svg`/`rasterize_svg_to_png_base64`/`title_card_svg` |
| 🌍️gis | 2 | `DwgColor`/`DwgEntity` |
| 🌀️procedural | 1 | `register_mesh_dwg_import_handler` (compliant call, still needs SDK re-export) |
| 🏭️process | 1 | `register_mesh_dwg_import_handler` (compliant call, still needs SDK re-export) |
| 🎥️shooting | 1 | `rasterize_svg_to_png_base64` |
| 📸️remodel | 1 | `OsMediaExportResult` |
| 🗒️note | 1 | `svg_to_dwg_bytes` |
| ✒️writer | 0 | Declared, unused — UNVERIFIED why; likely pure Cargo purge |
| 📐️cad | 0 | Declared, unused — UNVERIFIED why; likely pure Cargo purge |
| 🔱️trinity | 0 | Declared, unused — UNVERIFIED why; likely pure Cargo purge |
| 🖍️draw | 0 | Declared, unused — UNVERIFIED why; likely pure Cargo purge |

Union of real usage = 50 symbols (full list in `📓️w0-d-sdk-surface.md` §3.2) — this is the M3 re-export candidate list for `semio-framework-os` types/fns plugins actually need.

WIT `interface host`: 17 import funcs. Plugin-world (`HostState`): 4 capability-checked (`backbone-send`, `backbone-poll`, `engine-derive`, `engine-read`), 7 implemented-ungated, 5 unimplemented, 1 permanent-stub (`read-asset`). Extension-world (`ExtensionHostState`): only `log`/`now-ms`/`backbone-status` work; everything else hard-errors, including the 4 capability-checked funcs.

`HostEffect`: 22 variants (`🎠️kernel/🦀️component.rs:247-387`). 258 construction sites; `LoadDocument` alone = 91 (35%). 6 variants never constructed (`OpenWindow`/`CloseWindow`/`RequestSync`/`SetPanel`/`SpawnPluginInstance`/`ReplayShellCommand`). `🧩️puzzle` is the single most prolific constructor. Per-variant proposed `CapabilityRequirement` mapping in `📓️w0-d-sdk-surface.md` §5 (proposal, not policy — `ArtifactKind` may need a new "Window/Shell" member, W1/W2 to confirm).

Registration functions (13 named, full file:line/signature/call-count table in `📓️w0-d-sdk-surface.md` §6): `register_document_codec_for_app` = 44 call sites (undercounted by naive grep due to turbofish form — corrected); `register_studio_port` is **not** a framework SDK fn, it's `🪐️space`-local; `set_io_fallback_dispatcher` = 0 plugin call sites (host-only today).

---

## 3. Per-plugin work packet table (33 rows — this IS the W3 dispatch list)

Legend: **Clearance** values are from scout E §5, re-verify before dispatch (this snapshot decays in minutes). `Size` = migration effort estimate (S/M/L).

| Plugin | Facet dirs to delete | Extra dirs to relocate (destination) | Purity fixes | Registry call sites to convert | Cargo deps to purge | SMO/UCAS clearance | Size |
|---|---|---|---|---|---|---|---|
| ✒️writer | `🛂️manifest`,`🎟️capabilities`,`🔧️setup` (all doc-only stubs; also delete legacy root `🛂️manifest.json`, 445B) | none | none found | none | `semio-framework-os` (0 uses — pure purge) | **LATER** — compile-red on `📌️panels/📄️document` glue mount (§4 peer report); Wave C running | S |
| ➗️mathematical | same 3 stubs | none | 1 active apply-and-capture leaf per SMO quality-audit (coordinate w/ SMO, not APA's to fix) | none | none | **LATER** — Wave C "remaining", not launched | S |
| 🌀️procedural | same 3 stubs | `🎮️play` (near-empty, likely placeholder — confirm before delete; → `🗿️artifacts/<kind>/📚️examples/` if real) | none | `register_mesh_dwg_import_handler("3d.procedural")` already compliant (`procedural3d/🦀️component.rs:649`) — just needs SDK-token conversion once M2 lands | `semio-framework-os` (1 symbol: `register_mesh_dwg_import_handler`) | **NEVER-WITHOUT-HANDOFF** — absent from SMO's current released/held lists, ping before touching | S–M |
| 🌊️flow | same 3 stubs | `🔨️modules` (1 file, TS compute leaf → needs artifact-kind naming, UNVERIFIED destination); `🧩️extensions` (9 crates — extension-axis, hold for §6 ruling) | `Cell<u64>` `ENTROPY_SEED` in `🧩️extensions/🧮️math/🦀️component.rs:291` (non-standard subtree) | none | none | **LATER** — compile-red on glue mount; Wave C+R partial done | M |
| 🌍️gis | same 3 stubs; setup has real 6-line fanout (`register_gis_exports`) to fold into artifact `⚙️engine` | `🔨️modules` (1 file: `🏔️terrain` → `🗿️artifacts/gismap/🏅️standards/🔖️1/⚙️engine/terrain/`, high confidence) | `SEMIO_ASSET_BASE_URL` env read inside `🎛️apps` (`🗺️map/🦀️component.rs:77`) — needs relocating behind an artifact-owned config path | **GAP, not conversion**: owns `"2d.map"` but has zero self-registration — new `🚪️io` code needed under `gismap`'s own tree (demonstrator is currently sole handler) | `semio-framework-os` (2 symbols: `DwgColor`,`DwgEntity`) | **LATER** — Wave R2a DONE (170/170 lib tests); Wave C remaining app+config debt not launched | M |
| 🌿️vcs | same 3 stubs | none | none — this is the one clean `seed()` impl in the fleet | none | none | **LATER** — compile-red on glue mount | S |
| 🎞️animate | same 3 stubs | none | **2 live `fetch()` calls inside `🎛️apps`** (`🎬️present/📺️renderer/⚛️react/🟦️component.tsx:3303,3352`) — highest-priority purity fix in the census, must route through an artifact; 32 `fs::` calls + 2 `temp_dir()+process::id()` already inside `🗿️artifacts` (sanctioned location, lower priority) | none | `semio-framework-os` (3 symbols: `dwg_drawing_to_svg`,`rasterize_svg_to_png_base64`,`title_card_svg`) | **LATER** — Wave R2b; Wave C remaining app+config debt not launched | L |
| 🎥️shooting | same 3 stubs | none | none | none | `semio-framework-os` (1 symbol: `rasterize_svg_to_png_base64`) | **LATER** — Wave R2a DONE; Wave C running | S |
| 🎪️demonstrator | same 3 stubs; also has no AGENTS.md/README at all | `🎪️panes` (7 files → `🎛️apps/<app>/📌️panels/`, need to confirm which single app owns them — not yet enumerated) | none | **25 sites, delete all**: `🏭️bearbeiten`(5, `"3d.process"`)→belongs to 🏭️process; `🌱️generator`(8, `"3d.procedural"`)→🌀️procedural; `🗺️verfolgen`(2, `"2d.map"`)→🌍️gis (new code, no existing self-reg); `📐️koordinator`(10, `"3d.cad"`)→📐️cad (new code, no existing self-reg) | `semio-framework-os` (8 symbols, all `register_*`) | **LATER** — Wave C running (shooting/demonstrator/lowpoly lane) | L |
| 🎬️sequence | same 3 stubs | none | none | none | none | **LATER** — compile-red on glue mount | S |
| 🏗️fem | same 3 stubs | 8 one-file compute dirs (`➗️formulation`,`🏗️model`,`📏️elements2d`,`🔢️sparse`,`🕸️mesh`,`🧊️elements3d`,`🧮️analyses` → `🗿️artifacts/fem/🏅️standards/🔖️1/⚙️engine/<name>/`, high confidence); `🖥️app-surface` (shared across fem2d+fem3d apps — **NEEDS RULING**, no APA slot fits, `bannedNameStems` forbids the obvious fallback) | none found | none — no `register_*` family found in fem | none | **NEVER-WITHOUT-HANDOFF** — partial UCAS coverage per stale snapshot, absent from current released/held lists | L |
| 🏛️architect | same 3 stubs | none | none | none | none | **LATER** — Wave R2c DONE; Wave C running (105 app-debt, 44 already cleared) | S |
| 🏭️process | same 3 stubs | `🧩️extensions` (4 crates — extension-axis, hold for §6 ruling) | `Mutex<Vec<...>>` `CONTRIBUTED_MACHINE_CATALOGS`/`LAST_PROCESS_CONTRIBUTIONS_JSON` in `🗿️artifacts` (sanctioned location, real mutation — lower priority than app-tree hits); `fn kernel(&self) -> &Mutex<Brep>` accessor | `register_mesh_dwg_import_handler("3d.process")` already compliant (`process3d/🦀️component.rs:44`) — SDK-token conversion only; note demonstrator double-writes this same kind, coordinate removal order | `semio-framework-os` (1 symbol) | **LATER** — Wave R2b; Wave C remaining | M |
| 💠️lowpoly | same 3 stubs; setup has real 20-line/15-call fanout — heaviest single-plugin Class-A violator | none | thread_local `LOWPOLY_SCRATCH` mutated **inside `render()`** (`🎛️apps/💠️lowpoly/🦀️component.rs:343`, `refresh_texture_cache(projection)`) — the one render-mutation violation in the census; Draft-lane migration size **medium** per scout C | **15 sites** (`register_lowpoly_exports`, `🔧️setup/🦀️component.rs:10-24`): 7×`"3d.lowpoly"` fold into lowpoly's own artifact `⚙️engine`; 7×`"3d.mesh"` need a **new dedicated `mesh` artifact** (own `🗿️artifacts/…mesh…`, own `🚪️io` tree) since `"3d.mesh"` is architecturally ownerless today | `semio-framework-os` (4 symbols, all `register_mesh_*`) | **LATER** — Wave C running (shooting/demonstrator/lowpoly lane) | L |
| 💡️reasoning | same 3 stubs (also legacy root `🛂️manifest.json`, 351B) | none | none | none | none | **LATER** — Wave C remaining | S |
| 📋️forms | same 3 stubs (no AGENTS.md/README) | none | none | none | none | **NEVER-WITHOUT-HANDOFF** — gap in SMO coordination lists | S |
| 📏️layout | same 3 stubs (also legacy root `🛂️manifest.json`, 238B) | none | none | none | `semio-framework-os` (4 symbols: `DwgColor`,`DwgDrawing`,`DwgEntity`,`DwgGeometry`) | **LATER** — Wave R2b; Wave C remaining | S–M |
| 📐️cad | same 3 stubs | `node_modules` (delete/gitignore, build cache); `🔨️modules` (14 files, **not enumerated this wave**); `🖼️assets` (211 files, needs artifact-kind mapping, **not enumerated**); `🧩️extensions` (4 crates, extension-axis); `🧫️fixtures` (1 PNG → `📚️examples/…/🖼️assets/`); root `🔣️machine.json` (210KB, **CANNOT CLASSIFY** without reading contents) | `OnceLock<Mutex<String>>` `last_cad_computer_contributions_json` in `🗿️artifacts` (sanctioned location); thread_local `CAD_PREVIEW_SEQ` in `🎛️apps` | **GAP, not conversion**: owns `"3d.cad"` but has zero self-registration — new `🚪️io` code needed (demonstrator is sole handler today) | `semio-framework-os` (0 uses — pure purge, UNVERIFIED why declared) | **NEVER-WITHOUT-HANDOFF** — absent from SMO's current lists entirely, gap not clearance | L |
| 📕️norm | same 3 stubs; setup has real 48-line/45-call fanout (clean pattern, already thin) | `🎚️config`,`👥️presence` (shared default for 15 apps — **NEEDS RULING**); `📄️artifact` (shared engine core feeding 15 standard artifacts — **NEEDS RULING**, blocked on same shared-code question); `🖥️app-surface` (**NEEDS RULING**, same as fem) | none | none — setup's 45 calls are all pure fanout to `crate::artifacts::<x>::engine::register_*`, no direct `register_*` family calls | none | **LATER** — Wave M "norm: all 15 facets… running" | L |
| 📖️playbook | same 3 stubs | `🧩️extensions` (1 crate, extension-axis) | none | none | none | **NEVER-WITHOUT-HANDOFF** — SMO has a fully scoped, unexecuted plan to move ~452 framework lines into this plugin's root; landing APA mid-flight here collides catastrophically, wait for SMO to execute first | M |
| 📜️imperative | same 3 stubs | `🧩️extensions` (5 crates, extension-axis) | 1 active apply-and-capture leaf per SMO quality-audit | none | none | **LATER** — Wave M "odd remainder", not yet launched | M |
| 📸️remodel | same 3 stubs (no AGENTS.md/README) | none | none — consumes `"3d.mesh"` schema as data tag only, no `register_*` calls | none | `semio-framework-os` (1 symbol: `OsMediaExportResult`) | **LATER** — Wave M "odd remainder", not yet launched | S |
| 🔋️energy | same 3 stubs | **`⚙️engine` at plugin root, 50 submodules — the single largest violation in the census** → `🗿️artifacts/energy/🏅️standards/🔖️1/⚙️engine/<module>/` for each, high-confidence destination pattern, high effort | none | none | none | **LATER, not NOW despite SMO's "released" claim** — SMO status.md self-contradicts (§6); ping SMO for explicit confirmation before treating as clear | L |
| 🔱️trinity | same 3 stubs | `🌳️ast`,`🔤️lexer`,`🗣️language-service`,`🧮️executor` (single-file each → fold into one trinity artifact's `⚙️engine`, high confidence); `🔨️modules` (23 files incl. `🔌️jack/🐚️shell`+`🔌️jack/🧠️lsp`, medium confidence, needs artifact-kind naming) | `OnceLock<GraphManifest>` cached-parse in non-standard `🗣️language-service/` subtree; `Cell<i8>` `last_logged_lod` in `🎛️apps/♻️rewrite/🌍️world/🦀️component.rs:292` | none | `semio-framework-os` (0 uses — pure purge, UNVERIFIED why declared) | **LATER** — Wave M "space+trinity: running", actively worked right now (newest file across either peer ticket) | L |
| 🕸️dag | same 3 stubs | none | none | none | none | **LATER** — Wave M "singles" lane running | S |
| 🖍️draw | same 3 stubs (also legacy root `🛂️manifest.json`, 476B; no AGENTS.md on the extra dir) | `🔄️fsm` (10 files incl. a nested `✨️macros` sub-crate with its own `📦️packages/🦀️rust/` — crate-boundary nuance, needs a specialist not a plain directory move → likely `🗿️artifacts/draw/🏅️standards/🔖️1/⚙️engine/fsm/`) | thread_local `DRAW_SESSION` gesture FSM in `🎛️apps` (Draft-lane size **medium**, FSM type already exists, needs relocating into typed `Draft`); separate `store: RefCell<DrawStore>` wasm-bridge field | none | `semio-framework-os` (0 uses — pure purge, UNVERIFIED why declared) | **NEVER-WITHOUT-HANDOFF** — absent from SMO's current lists | L |
| 🖨️raster | same 3 stubs (no AGENTS.md/README) | none | none | none | `semio-framework-os` (5 symbols: `DwgColor`,`DwgDrawing`,`DwgEntity`,`DwgGeometry`,`rasterize_svg_to_png_base64`) | **LATER** — Wave M "singles" lane running | S–M |
| 🗄️stdio | same 3 stubs; manifest facet has real 344-line/28-entry format catalog → belongs under `🗿️artifacts/<kind>/…`, not `🛂️manifest` | `📇️registry` (1 file, `📇️catalog.json` — may be taxonomy-legal under `rootDataDirNames`, **recheck before flagging as breach**) | none | none | none (stdio is depended-ON, not a dependent) | **NEVER-WITHOUT-HANDOFF — DO NOT TOUCH.** UCAS's hard exclusive claim, mid-restructure this minute; every other plugin depends on it being green | L (blocked) |
| 🗒️note | same 3 stubs (no AGENTS.md; legacy root `🛂️manifest.json`, 513B) | none | none | none | `semio-framework-os` (1 symbol: `svg_to_dwg_bytes`) | **LATER** — Wave M "note running" (fresh scratch cargo-check files, <15 min old at census time) | S |
| 🧩️puzzle | same 3 stubs | `🔨️modules` (1 file: `🎲️board-2d` → `🗿️artifacts/puzzle2d/🏅️standards/🔖️1/⚙️engine/board-2d/`, high confidence); `🧫️fixtures` (4 files, medium confidence, not enumerated) | **Largest Draft-lane migration in the census (size large)**: `PUZZLE3D_MESH_REGISTRY` (Mutex<HashMap>, written from sibling `🎮️commands/🖌️brush/` — cross-module writer), 3 memo Mutex fields, 2 thread_local play sessions, 6 RefCell scratch/transform fields; puzzle is also the single most prolific `HostEffect` constructor (`SetActiveUtility`×11, `SetActiveTool`×9, etc.) | **18 sites** across 3 app files (2d:2 `◻2d/🦀️component.rs:1366`, 5d:8 `🖐️5d/🦀️component.rs:2004`, 3d:8 `🧊️3d/🦀️component.rs:2686`) — same-plugin relocation, move each block into that kind's own `🗿️artifacts/<kind>/🏅️standards/🔖️1/⚙️engine` | `semio-framework-os` (6 symbols, cfg-gated wasm32-excluded) | **LATER** — Wave M "puzzle running" | L |
| 🧱️block | same 3 stubs | none | none | none | none | **LATER** — Wave M "block running" | S |
| 🪐️space | same 3 stubs; **the one real capability call lives at plugin ROOT, not the facet** (`.local_backbone_storage()` `🦀️component.rs:63`) — capabilities facet dissolves cleanly, zero code to move | none | `LazyLock<()> FIXTURES` used as a run-once side-effect gate at plugin root (`🦀️component.rs:24`); `Mutex<HashMap>` presence-peer + studio-port registries in `🎛️apps` — process-global, cross-session-lifetime, **flagged UNVERIFIED whether Draft is even the right target**, likely needs a framework-level shared/ephemeral capability answer, not a per-app move | `register_app_io` (1 site, `🎮️commands/🧭️navigation/🦀️component.rs:78`, wrong layer — command handler not artifact engine); `register_os_fixture_json` (2 sites, plugin root `🦀️component.rs:30-31`, wrong layer — not `🔧️setup`, not artifact engine) | `semio-framework-os` (**36 symbols — dominant consumer**, functions as a de facto second host-adjacent crate; `os-host-full` feature flag; structural outlier for W1/W2 attention, not a simple re-export gap) | **LATER, not NOW despite SMO's "released" claim** — SMO status.md self-contradicts (§6); being actively worked right now per fresh scratch files | L |
| 🪵️sourcing | same 3 stubs | `🧩️extensions` (3 crates, extension-axis) | `Mutex<Vec<...>>` `CONTRIBUTED_SOURCING_MODULES`/`LAST_SOURCING_CONTRIBUTIONS_JSON` in `🗿️artifacts` (sanctioned location) | none | none | **LATER** — Wave M "singles" lane running | M |

**Recurring cross-plugin items not repeated per-row:** (1) the `🛂️manifest`/`🎟️capabilities`/`🔧️setup` triple-stub deletion is mechanical and identical for all 33 — only the 4 plugins with real code (gis, lowpoly, norm, stdio) need actual logic migration, not just deletion; (2) `extension-axis` (6 owners, 26 crates) is a single open policy question, not 26 separate decisions — see §6; (3) `shared-across-sibling-apps` code (fem's `🖥️app-surface`, norm's `🎚️config`/`👥️presence`/`📄️artifact`/`🖥️app-surface`, draw's macro crate) is a single open policy question — see §6.

---

## 4. Framework work packets (W1 mechanism items)

Source: `📓️w1-mechanism-design.md`. Each item names its exact target file, region, and what it replaces.

| # | Item | Target file | Region | Replaces |
|---|---|---|---|---|
| M1 | `ArtifactDeclaration` struct + builder (`kind`,`schema`,`inferences`,`composers`,`formats`,`subset_validators`,`languages`,`document_codec`,`migrations`,`child_slots`,`link_slots`,`capabilities`) | `🧰️framework/🛍️products/💻️os/🔨️modules/🔌️plugin/🦀️component.rs` | new region `🔖️ArtifactDeclaration` | Every plugin's imperative `register()` fn in `⚙️engine/🦀️component.rs` — becomes a pure `declaration() -> ArtifactDeclaration` data fn |
| M1 | `child_slots`/`link_slots` derivation via `.composition::<Snapshot>()` — **no public slice setter**, pulled from `<T as ArtifactCompositionFields>` | same file, builder | same region | A hand-written slot list (rejected by design — would silently drift from the `#[derive(ArtifactSchema)]`-emitted truth) |
| M1 | `PluginBuilder` changes: delete `setup: Option<fn()>` + `.setup()` + the `if let Some(setup) = self.setup { setup(); }` call; add `artifacts: Vec<ArtifactDeclaration>` + `.artifact(decl)`; `build()` walks declarations in fixed order (schema→inferences→formats→subset validators→composers→languages→document codec→migrations) and validates ownership (`artifact_kind == decl.kind`, `decl.kind` matches `s.<plugin_id>.<artifact>`) | `🏗️builder/🦀️component.rs` | (existing builder region) | The 33 hand-written `🔧️setup` fanout functions; makes the lowpoly-style ownership violation structurally unrepresentable |
| M1 | Retire `artifact_kind(ArtifactKindSpec)` **builder method** (type itself stays until UCAS's W6 media cleanup — separable) | `🔌️plugin/🦀️component.rs` | builder region | — |
| M2 | `Registrar(())` seal type + `__framework_internal_mint()` | `🎠️kernel` (if reachable from os-kernel) **or** `🚪️io` (dual-mounted into both `semio-framework` and `semio-framework-os-kernel` — fallback if kernel isn't reachable; **decide by compiling, not reasoning**, record result in W1 report) | new | Convention-only "don't call this" discipline |
| M2 | `&Registrar` first param on all 10 registration fns: `register_composer_entries`, `set_io_fallback_dispatcher`, `register_subset_validator`, `register_format_descriptors`, `register_artifact_schema_descriptor`, `register_artifact_inference_descriptor`, `register_app_schema_descriptor`, `register_language`, `register_document_codec`, `register_document_codec_for_app`, `register_dialect_migration` | `🚪️io/🦀️component.rs`, `🧬️schema/🦀️component.rs`, os-kernel registry files | (existing fn signatures) | Plugin-reachable unsealed registration |
| M3 | Replace 2 glob re-exports with explicit curated list (derived from scout D's 302-symbol dedup table, `📓️w0-d-sdk-surface.md` §2) | `🔌️plugin/🦀️component.rs:9645,9654` | tail re-export block | `pub use semio_framework::*;` and `pub use ui_wgpu::wgpu::*;` |
| M3 | Purge `semio-framework-os` from all 17 dependent `Cargo.toml`s; add explicit re-exports for the 50-symbol union (`DwgDrawing`, `rasterize_svg_to_png_base64`, mesh types, etc. — full list §2 above) from `semio_framework_plugin` instead | 17 plugin `Cargo.toml`s (§2 table above) | — | Direct host-crate dependency |
| M3 | Target end-state plugin `Cargo.toml`: `semio-framework-plugin` + os-kernel aliases (`store`/`dsl`/`protocol`/`pack`/`spr`/`vcs`) + `semio-framework-3d` where needed + `serde` — nothing else framework-side | all plugin Cargo.tomls | — | — |
| M4 | Replace `ArtifactApp::seed(&mut ArtifactStore<...>)` (`:4614`) with `fn genesis() -> Vec<Self::Mutation> { Vec::new() }`; `VcsArtifactApp` applies returned mutations through normal dispatch at construction | `🔌️plugin/🦀️component.rs` | ArtifactApp trait region | The one direct store-touching `seed()` (vcs) — Draft-lane half specified separately in `📓️draft-lane-spec.md`, executes W3 per plugin |
| M5 | Generalize `has_backbone_access`/`has_engine_access` into `has_capability(ArtifactKind, Rights)`; gate all 17 WIT host imports (`read/write-artifact`→Document, `read-asset`→Asset/Read, `write-blob`/`read-blob`→Asset/Write\|Read, `network-fetch`→Network, `open-window`→Window, `invoke-action`→Invoke, `io-dialects`/`io-compose`→Document/Read) | `🔌️plugin/🖥️host/🦀️component.rs:288-295` — **⚠️ this is UCAS's `IoRouter` file**, not `💻️os/🖥️host/🦀️component.rs` (which is APA's) — negotiate before entering | existing capability-check fns | Ungated `write-blob`/`read-blob`/`backbone-status`/`io-dialects`/`io-compose` |
| M5 | Map each of 22 `HostEffect` variants to a `CapabilityRequirement` (proposal in `📓️w0-d-sdk-surface.md` §5); effect executor checks grant, returns `Fault` for undeclared effect | `🎠️kernel/🦀️component.rs:247-387` (enum) + effect executor | `HostEffect` region | Ungated parallel side-effect channel that makes today's capability system decorative |
| M5 | Thread `granted_capabilities` from `ActionContext` into command dispatch | `CommandContext` (source itself notes "commands don't yet carry a capability grant model") | — | — |

Ordering/gates table (verbatim intent from `📓️w1-mechanism-design.md`): M2 type+placement and `&Registrar` params are blocked on UCAS claim negotiation; M1 declaration+builder, M3 curated re-exports, and M4 `genesis()` are blocked on **UCAS C1 unfreeze**; M3 Cargo purge is blocked per-plugin on SMO+UCAS clearance (§3 above); M5 is blocked on UCAS negotiation for the `IoRouter` file specifically.

Verification requirement for every M-step: `CARGO_TARGET_DIR=<ticket>/🎯️target cargo check -p <crate>` then `cargo test -p <crate> --lib`, real output pasted into the report — never bare `cargo`. Plugin-side verification is impossible until UCAS broadcasts "roster frozen" (stdio red ⇒ every plugin red); treat plugin redness as churn, prove no error originates in your own boundary, report `blocked-churn` rather than claiming pass/fail blind.

---

## 5. Agent checklist (verbatim from scout E §6)

1. **Derive crates keep two byte-identical copies.** `<module>/✨️derive/🦀️component.rs` and `<module>/✨️derive/📦️packages/🦀️rust/📦️glue.rs` must be identical — Cargo compiles the *glue* copy only, so editing `component.rs` alone silently does nothing. Verified true for `🧬️schema/✨️derive` and `🗣️dsl/✨️derive`. After any edit: mirror by hand, then `diff -q` the pair before reporting done.
2. **`mcp__repo__file_integrate` mis-mirrors** — it wraps the whole file in a nested `mod helpers {}`, duplicating everything. Do the derive-crate mirror above by hand, not with that tool.
3. **`#[link(...)]` is unusable as a custom field attribute** — `link` is a built-in Rust attribute (extern-block FFI); applying it to a struct field is a hard compile error (E0659/E0539/E0459), not a lint. If APA ever needs a link-style attribute of its own, name it something else entirely (UCAS landed on `#[link_slot(...)]` for the same reason).
4. **Additive struct fields still break struct literals** — serde's `#[serde(default)]` affects only (de)serialization, never Rust struct-literal construction. After adding any field to a struct that is constructed via literal syntax anywhere, `grep -rn "TypeName {"` across the **whole workspace**, not just your own crate, and fix every literal (or file it as a shared-file request if outside your boundary).
5. **Adding an enum variant is expensive wherever the enum is matched exhaustively.** Before deciding to add one, run `grep -rln "EnumName::"` and read the hit count — UCAS measured `Shape::` at ~20 files and dropped a planned variant addition entirely rather than touch all of them (their design deviation D1).
6. **The repo auto-commits the whole tree periodically** (commits `🐙️ueli🎆️26🌙️06☀️04🚩️<n>`, monotonically incrementing — currently at **492**). `git status --porcelain` reports **clean** for work that landed and got swept into a commit minutes ago — it is NOT a churn detector and an empty status does NOT mean "nobody touched this file." If your own edits vanish from `git status`, they were committed, not lost — never run a git-modifying command to "recover" them (forbidden anyway). Use `git log --oneline -5 -- <path>` and `stat -f '%Sm' <path>` (macOS) to detect real churn instead, and never assume a clean tree means a file is safe to blind-overwrite — read it first.
7. **`CARGO_TARGET_DIR` is per-ticket and shared across concurrent checks within that ticket** — the lockfile serializes them, so `"Blocking waiting for file lock on build directory"` during a scoped `cargo check` is normal and should be waited out, not killed.
8. **The semantic-mutation vocabulary is a closed, comment-scanned list.** `SetSnapshot`, `NoMutation`, and public `CollectionMutation` are banned repo-wide, and the policy rule that enforces this **greps raw file content including comments and docstrings** — merely naming one of these in prose (even to say "don't use X") trips it outside files exempted from the scan. APA doesn't author `🧬️mutations/**` facets, but any docstring or region comment APA writes anywhere in a plugin must still avoid these three literal tokens.
9. **The approved composition-verb vocabulary is closed**: `create`/`delete`, `extract`/`inline`, `bind`/`unbind`, `change`. Notably **not** `connect`/`disconnect` (a link fills a *named slot*, not an edge-collection row) and **not** `update` (reserved for an inseparable ≥2-field facet rewritten atomically — a single-field re-pin is `change`, even when that field's value is an enum-with-payload that superficially looks multi-field).
10. **Hot-file ownership is binding and enforced by convention, not tooling.** UCAS's `📌️important.md` names specific owner-agents for `🔌️plugin/🦀️component.rs`, `🚪️io`/`🧬️schema`/`🛂️manifest`/`🎠️kernel`/`📡️spr`/`🏪️store`/`🌿️vcs`/`🗣️dsl`, and stdio's `📦️glue.rs`/`📦️index.ts`/`📇️catalog.json` — everyone else is read-only on those files and must file a "sharedFileRequests" entry rather than edit directly. APA will need to negotiate its own entries into (or explicit exceptions from) this table before touching any of them, especially `🔌️plugin/🦀️component.rs` and app `🦀️component.rs` files.
11. **`MutationMeta` (`📡️spr/🎮️command/🦀️component.rs:389`) is a shared struct with two concurrent stakeholders** — SMO's Wave-0 `semantic_kind`/`label` fields (both still unconditionally constructed `None` everywhere) and UCAS's `group_id`. Both sides' protocol is "notify the other before touching this struct again" — if APA needs a field here, ping both peers first, not just one.
12. **Report shape convention** (used by all three tickets): what changed, files touched with file:line anchors, verification commands + their literal output, a `## sharedFileRequests` section (file, region, reason, patch-file path) for anything outside your own boundary, a `## Concurrent-churn observations` section, and an honest pass/fail — SMO's R1 report is the cleanest example of the "I could not verify X, here is exactly why" pattern to imitate rather than a false "compiles/passes" claim.
13. **`📜️script.ts` (repo-root) has a three-way write-order agreement already in place**: SMO's status.md records the proposed order as **APA → UCAS-W6 → SMO**, one writer at a time, announce in-channel immediately before and after each session. APA should expect to go *first* here, not last, and should announce accordingly rather than assuming it can queue behind the others.
14. **Docstrings need a leading emoji** (already in this ticket's own rules block, restated here only because both peer reports demonstrate it in practice) — e.g. UCAS's own EditRef doc-comment starts `/// 🔖️...`; match that convention exactly when annotating any new APA-side type.

---

## 6. Open questions / conflicts

### Scout disagreements / things two scouts each independently flagged
- **Extension-axis question (B, C, D all touch it, none resolve it):** is `🧩️extensions/` (26 crates, 6 owner plugins: flow=9, imperative=5, process=4, cad=4, sourcing=3, playbook=1) an APA violation requiring dissolution into `🗿️artifacts/`, or a sanctioned third axis alongside apps/artifacts? Scout C independently flags 2 purity hits (flow's `ENTROPY_SEED`, trinity's not-in-this-axis `🗣️language-service`) that live inside exactly this kind of non-standard subtree — the structural question blocks a clean purity answer too.
- **Shared-across-sibling-apps code (B flags this twice, once for fem once for norm, independently):** where does code shared by ALL of a plugin's apps live (fem's `🖥️app-surface` used by fem2d+fem3d; norm's `🎚️config`/`👥️presence`/`📄️artifact`/`🖥️app-surface` used by all 15 norm apps)? Neither `🎛️apps/<app>/` (too narrow) nor `🗿️artifacts/` (wrong axis) fits, and CLAUDE.md's `bannedNameStems` (core/common/util/shared/base/lib/impl) forbids the obvious fallback names. **Blocks W3 for fem and norm specifically** until ruled.
- **`newArtifactChildDirs`/`newArtifactComponentDirs` vs `artifactChildDirs` (B, flagged, not adjudicated):** taxonomy.json already carries two artifact-child shapes side by side — the old flat `["🧬️schema","⚙️engine","🚪️io","📚️examples"]` and a newer `["🏅️standards","📚️examples"]`. Which is authoritative for APA's W3 mass migration is not decided by this census.

### UNVERIFIED items (scout-flagged, not independently re-checked)
- Whether HOST's dead `#[cfg(not(feature = "os-host-full"))]` stub registrars are truly unreachable across every build config (A) — checked only the two textual `#[cfg]` conditions, not the full Cargo feature-unification graph.
- Whether wasm32 target-cfg excludes any puzzle/lowpoly production call sites beyond the one guard spotted (A).
- Whether the framework should treat `build.rs`/standalone `bin.rs` siblings (puzzle's icon-copy build script, trinity/jack's CLI bin) as in-APA's-jurisdiction at all (C) — 5 fs/env hits live in these two files specifically.
- Whether the doc-comment claiming a prior `Command::new("ffmpeg")` removal in animate's video engine (`🦀️component.rs:923-927`) is accurate provenance — the *current* zero-hits finding stands regardless (C).
- Why 4 of the 17 `semio-framework-os`-dependent crates (writer, cad, trinity, draw) declare the dependency with zero actual uses — possibly stale, possibly reached only via workspace default-features or a build script (D).
- Whether the 17 apps' `store: RefCell<XStore>` wasm-bridge pattern is framework-mandated boilerplate (→ W1 concern) or hand-written per app (→ W3 concern) — not traced (C).
- Scout B's own extra-dir enumeration is incomplete for `📐️cad/🔨️modules` (14 files) and `📐️cad/🖼️assets` (211 files) — flagged "not yet enumerated this wave," destination for both is a placeholder guess, not a verified mapping.
- 210KB `📐️cad/🔣️machine.json` — contents never read, **CANNOT CLASSIFY** stands as-is.
- `🗄️stdio/📇️registry` may be a false-positive violation entirely — taxonomy.json has a `rootDataDirNames` entry that may already legalize it at owner-root; scout B explicitly asks for a recheck before treating it as a breach.

### APA design vs. UCAS in-flight work — direct collisions
- **UCAS's composition primitives are the ~60%-landed state APA's `child_slots`/`link_slots` design depends on** (io/kernel/schema primitives: yes — `ArtifactRef`/`ArtifactKindId`/`EditRef`/`UndoGroup.member_edits`/`ChildSlotSpec`/`LinkSlotSpec`/`ArtifactCompositionFields` all verified present; store-level `ArtifactChild`/`ArtifactLink`/`CompositionCoordinator`/`OwnerRef`: verified **absent**, only forward-looking comments at `🏪️store/🦀️component.rs:741,2541`). No `declare_artifact!`/`plugin!` macro exists anywhere despite UCAS's plan once describing one — confirmed ceded to APA, `w1-mechanism-design.md` already designs around this correctly.
- **`🔌️plugin/🦀️component.rs` itself is APA's eventual target for M1/M2/M3/M4** but is simultaneously UCAS's hot-claimed file per their `📌️important.md` ownership table — the C1-unfreeze gate in §1 is this exact collision, already negotiated (status.md) but not yet triggered.
- **`🔌️plugin/🖥️host/🦀️component.rs` (M5's target) is UCAS's `IoRouter` file**, distinct from `💻️os/🖥️host/🦀️component.rs` which is APA's — easy to confuse, flagged explicitly in the mechanism design doc; negotiate before entering.
- **SMO's ratchet wants `DraftMutation: SemanticMutation`**, currently blocked waiting on APA's answer to whether `DraftMutation` stays `NoDraftMutation` or becomes real — `📓️draft-lane-spec.md` is out for SMO review per status.md, resolution pending, not yet confirmed settled by SMO's side.
- **SMO's own `📓️status.md` self-contradicts on `🪐️space`/`🔋️energy` clearance** — its "Cross-ticket coordination with APA" section claims both "Released to APA now… no further SMO work queued," while its own Wave M lane table in the same file lists `"space + trinity: running"` and `"energy-model… running"`. Scout E flags this explicitly and recommends a direct ping to SMO to reconcile rather than trusting either half — **do not dispatch W3 on space or energy until that ping resolves it**, even though these are nominally the two closest-to-clear plugins.
- **UCAS's `📓️smo-clearance.md` is self-described as stale** and already disagrees with SMO's live status.md in both directions (marks space/energy NOT-clear while SMO claims released; marks cad/draw/forms/fem/procedural CLEAR while SMO's current lists don't mention them at all). Treat as historical color only.
- **4 plugins (writer/vcs/flow/sequence) have a still-open dangling glue mount** (`📌️panels/📄️document` referenced, only `📌️panels/📄️artifact` exists on disk) that SMO's R1 wave explicitly declined to fix, calling it "pre-existing, out-of-scope… renamed to `📄️artifact` by a different concurrent session" and squarely APA's own future territory per SMO's own coordination note. This is new work for APA, not a pre-existing blocker to wait out — grep-anchor: `grep -n "📌️panels/📄️document" "✏️s/🔌️plugins/<plugin>/📦️packages/🦀️rust/📦️glue.rs"`.
</content>
