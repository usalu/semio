# Framework Module Retention and Engine Frontier

## Checkpoint

- Repository `HEAD`: `07873f842a5a99ac2f69c1648c21f36ebf260bdb`.
- Full deterministic census regeneration remains paused while root, OS renderer, Flow, glTF, and plugin registrars are moving.
- Protected prompt, framework kernel/machine/platform, OS renderer, and repo-library-index paths remain quarantined.

## Scoped taxonomy evidence

The narrow `framework.module.action-argument-resolution` scope reports zero components because the framework module collection lacks its canonical manifest. It is therefore not a structural release.

The framework report resolves 18 components and 248 errors with zero warnings. Framework enforce exits non-zero with the same findings. The new action-argument-resolution component is represented by these exact unresolved conditions:

- `collection-manifest-missing` at `🧰️framework/🔨️modules`;
- `manifest-child-missing` for `framework.module.action-argument-resolution`;
- `module-production-consumer-minimum`, because the current graph adapter does not traverse the root package assembly to terminal consumers.

A partial framework module manifest is forbidden by exact manifest/tree bijection. Registration waits for complete sibling classification and the consumer-graph repair.

## Read-only direct-module audit

The live graph contradicts the stale census for `#⃣hash`. It has five direct production package dependents and live terminal call sites in framework, OS, puzzle, and animate responsibilities. It remains a framework-owned shared module.

The `🔺️mesh` scene protocol remains shared and public. Its `organizeContextMenu` policy is a distinct UI-host responsibility, but its framework package assembly and renderer consumers are dirty and quarantined.

The `🕹️interaction` declaration and state-machine mirror remains shared. Its Rust declarations are mounted by framework assembly and TypeScript transitions are directly consumed by Tree. Runtime interaction state remains separately owned by the protected OS wire/platform area.

## Issued leases

`H-01` writes only `🧰️framework/🔨️modules/#⃣hash/🦀️component.rs` at SHA-256 `b2a4e93df9604705736a503a24402e3fdd6b978c270024ff0529f857e1f9179f`, plus a unique ticket report. It adds contract tests without changing behavior or ownership and validates the framework Rust quick-test Nx target.

`E-01` is a read-only audit of `🧰️framework/🛍️products/💻️os/🔨️modules/⚙️engine/🦀️component.rs` at SHA-256 `32b7ecd98ddbb9858e91a7060bd72b299c7fb1dcb3245ac92e9c709042eb7237`. It maps the mixed cache/host, representation, and continuous-draft responsibilities to terminal production consumers before any structural lease is issued.
