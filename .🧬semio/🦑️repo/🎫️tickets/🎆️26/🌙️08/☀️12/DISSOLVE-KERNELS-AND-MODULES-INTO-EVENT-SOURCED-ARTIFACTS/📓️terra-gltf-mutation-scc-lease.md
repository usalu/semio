# Terra glTF Mutation SCC Lease

## Baseline And Scope

- Owner: `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🧊️gltf/🏅️standards/🔖️2.0/🪆️subsets/✳️any/🧬️schema/🧬️mutations`, its 28 command children, the direct local consumers, and the final `🚪️io/🧬️mutations` transport boundary.
- Repository, `✏️s`, and `🗄️stdio` instructions were reread. No deeper glTF instruction file exists.
- The central taxonomy prerequisite is present before the source move: `🚪️io/🧬️mutations` is an I/O collection with `transport` direction, and text/binary artifact-spec paths point to the final I/O transport locations.
- The scoped baseline is 27 components, 64 errors, 0 warnings: the independent artifact-root manifest finding plus exactly 59 mutation SCC findings.
- `git status --short` found no pre-existing dirty file under the mutation source tree or final mutation-transport destination. Concurrent root/taxonomy/generated-glue changes are protected baseline work.

### Source Hashes

| Source | SHA-256 |
| --- | --- |
| Root Rust | `798f29df72fcf8da72dae049158b4c918e6555a4dac41444b5ff31d78b5ce834` |
| Root TypeScript | `1ed5e59e1556689a4d95ebc4bf1c4ed9fc74ec73012c033780bd3a5540d84463` |
| Root GraphQL | `1dee4c09445fce25b761ee500a6d0156cdb3161782cabdc4719afe7c2b36d84b` |
| Root JSON Schema | `63711362c46c0fe67611bb937f1c4ba679087719f3fb35f44bd9c535057bb05c` |
| Root Protocol | `69a551ecfb8455385013f667035ff37753decf43f4adfb0a3bc2c4b21ba6939a` |
| Planning Rust | `71543c1f4d2651821ae7e871d8b89ef7ae8f94ad2d1b5a1b8f4a2838afac1ebf` |
| Text transport Rust | `b812234438201aeec613dd7145440b2b4c09bc4745dea07f892ff2bd8cc346ce` |
| Binary transport Rust | `a5b8454ee6823fd4896b15451023b5f60444cd09ec1f68adefe95d406583fe74` |
| Complete pre-move mutation-tree source inventory | `8c93aa9824f4eb61a60dcd9cc3bf13f60ba6eb951d54190f252e6486f9643524` |

The inventory contains 28 terminal command directories, each with Rust and TypeScript mutation/diff/inverse facets.

### Direct Referrers

- `🧬️schema/🦀️component.rs`: five mutation descriptor facets and the builder's mutation application call.
- `🧊️gltf/🦀️component.rs`: public `GltfMutation` export and mutation grammar/protocol descriptor rows.
- `🧬️schema/🔺️diff/🦀️component.rs`: owned transport/aggregate source-location documentation.
- `🚪️io/🦀️component.rs`: mutation grammar/protocol conformance tests and demo cases.
- Central-only, not writable here: `📦️packages/🦀️rust/📦️glue.rs` mutation/module/I/O mounts and root `📜️script.ts` generator source entry.

## Planned Atomic Ownership

- Promote the closed aggregate, planning, and five aggregate contracts to subset-LCA module `🔨️modules/🧭️mutation-dispatch` (`s.stdio.gltf.module.mutation-dispatch`) with all 28 terminal mutation consumers.
- Fold every command's six private Rust/TypeScript facets into immediate canonical command leaves and remove retired facet directories.
- Move the text and binary protocol boundaries to `🚪️io/🧬️mutations`; no codec runtime remains under the schema collection.

## Source Implementation And Registrar Handoff

- Folded every command's Rust and TypeScript mutation/diff/inverse facets into its immediate `🦀️component.rs` and `🟦️component.ts` leaves, preserving event, diff, and inverse regions as private implementation. Removed 87 verified-empty retired facet, planning, and schema-codec directories after their content had moved.
- Moved the aggregate Rust, TypeScript, GraphQL, JSON Schema, Protocol, and planning implementation into `🔨️modules/🧭️mutation-dispatch`. The dispatch module's manifest declares exactly the 28 terminal mutation IDs as production consumers.
- Replaced the mutation collection root with its sole canonical `x-semio.members` manifest. It names exactly the 28 on-disk command directories and contains no authored language leaf.
- Moved text and binary codecs, contracts, grammars, and protocol descriptions into `🚪️io/🧬️mutations/{📝️text,💾️binary}`, whose collection manifest declares two `io` members with direction `transport`.
- Repointed all writable local consumers: schema facet descriptors and builder, artifact public export and descriptor rows, I/O conformance cases, and moved-location documentation. No compatibility alias or forwarding source was added.
- The tree audit found one variation-selector mismatch between the `set-snapshot` manifest member and its directory; corrected the member to the exact on-disk canonical name before release.
- Submitted the central-only request at `🔧️patches/terra-gltf-mutation-scc-registrar-request.md`. It requires removal of the root/planning/text/binary and 84 nested facet mounts, adds exactly 28 direct command mounts, one `modules::mutation_dispatch` mount, two `io::mutations` transport mounts, and replaces the one root generator-input entry. Generated glue and the root script were not edited in this lease.

### Pre-Registrar Validation

- JSON parsing and manifest/tree bijection passed: 28 immediate commands, 2 transport leaves, and the dispatch consumer set exactly equals the command member IDs.
- All four JSON documents in the moved mutation SCC parsed successfully; scoped `git diff --check` passed.
- Static referrer sweep found no stale direct runtime imports to `schema::mutations::{GltfMutation,apply_gltf_mutation,text,binary,planning}`.
- No retired command-facet, root planning, schema-text, or schema-binary directory remains.
- Final source SCC fingerprint: `068d5321f7a15c782540a83db5b1c207f5a62d8fa8a8a9f50fef4c2b0a35aca4`.
- Cargo, Nx, and the scoped taxonomy report are intentionally pending central registrar completion because the current generated glue still mounts retired paths.

## Post-Registrar Integration And Release Evidence

- Central registrar ownership confirmed its update without a source-side generated edit: the live glue mounts the 28 direct command leaves, one `schema::modules::mutation_dispatch` module, and the two `io::mutations` transport leaves. It has no mount to the retired mutation-root Rust leaf, planning leaf, or schema text/binary codec leaves.
- Corrected the 15 direct inference and geometric-analysis Rust imports from the stale three-level module traversal to the canonical `schema::modules` traversal. The subsequent referrer sweep found no `super::super::super::modules` traversal and no old `schema::mutations::{GltfMutation, apply_gltf_mutation, text, binary, planning}` runtime import.
- `mutation_dispatch` now exposes the cohesive crate-internal planning API directly from its owning planning facet: `check_index`, `locate_node_owner`, `reject`, `remap_references`, `remove_checked`, `shift_insert`, `GltfSemanticMutation`, and `IndexFamily`. This is a direct owner API, not an alias or compatibility wrapper.
- `cargo check -p semio-s-plugin-stdio` completed successfully after the importer/API correction (`Finished dev profile`; 1,084 existing shared warnings and no Rust error).
- The original configured `bun nx run @semio-tech/stdio-plugin:test-quick` completed successfully. Its saved Nx terminal output records `bun ./📜️script.ts test quick` and `[stdio] TypeScript facade exposes 36 catalog artifacts.` with no failure. A later retry was intentionally terminated after it waited behind an unrelated shared `cargo check --workspace`; that retry is not used as validation evidence.
- JSON parsing and collection bijection were rerun successfully: all four moved JSON documents parse; mutation membership, immediate command directories, and the dispatch entry's production-consumer set each contain exactly 28 entries; transport membership and immediate directories each contain exactly two `io` leaves, both with `direction: transport`.
- Retired mutation root contracts, planning, schema text/binary codec directories, and all command `🦠️mutation`/`🔺️diff`/`↩️inverse` facet directories are absent. Scoped tracked and untracked whitespace checks passed (`git diff --check` and `git diff --no-index --check`).
- The post-registrar `bun ./📜️script.ts verify taxonomy report --scope s.stdio.gltf` completed with 58 components, 7 errors, and 0 warnings. No mutation SCC finding remains. The seven independent findings are one glTF artifact-root `manifest-child-missing` finding plus `module-consumer-graph-mismatch` and `module-production-consumer-minimum` pairs for `💡️inference-measures`, `🕸️mesh-topology`, and `🧮️vector-operations`.
