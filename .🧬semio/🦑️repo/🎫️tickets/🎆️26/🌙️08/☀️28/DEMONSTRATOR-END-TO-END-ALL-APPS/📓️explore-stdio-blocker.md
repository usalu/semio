# Stdio Plugin Split Status Exploration
Date: 2026-09-04  
Ticket: `26/08/28/DEMONSTRATOR-END-TO-END-ALL-APPS`

## 1. Separate Artifact Standard Subset Implementations Ticket Status

**Ticket path:** `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️02/SEPARATE-ARTIFACT-STANDARD-SUBSET-IMPLEMENTATIONS-AND-FIXTURE-TEST-EVERY-MUTATION/`

**Status:** `open` (from 🎫️ticket.json)

**Quote from ticket.json:**
```json
{
  "title": "Separate Artifact Standard Subset Implementations And Fixture-Test Every Mutation",
  "emoji": "🪆️",
  "status": "open",
  "description": "Every single artifact standard subset implementation must be separate (own io/schema/mutations/oracle/fixtures/tests under 🏅️standards/🔖️<version>/🪆️subsets/<subset>/), never shared or collapsed at the artifact level. Every single artifact mutation must be tested over fixtures (real-world example, handcrafted, or generated by a third-party oracle library). Audit the whole repo for violations — notably tests living at artifact level (e.g. ✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/📄️pdf/🧪️tests/mutate-pdf-1-4-a) instead of at subset level, subsets without their own implementation, and mutations without fixture-backed tests — then fix them all."
}
```

The ticket is **OPEN and IN PROGRESS** — it contains extensive research markdown files (at least 20 documented phases A through G covering framework design, architecture, mutations, fixtures, and oracles) but no completion summary.

## 2. Complete Semio End-to-End Demonstrator Status

**Ticket path:** `.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️09/☀️02/COMPLETE-SEMIO-END-TO-END/`

**Key findings from master plan and acceptance matrix:**

### Remaining Critical Blockers for Release (from Acceptance Matrix):

| Area | Current State | Blocker |
|------|---------------|---------|
| **Registry** | Monolithic stdio component exceeds function limit | **BLOCKED** — stdio has 36 artifact families; `wasm-component-ld`/wasmparser rejects the monolithic component because its function count exceeds one million. Clean build at -O optimized profile only (never debug). |
| **React OS** | Catalog incomplete | PARTIAL — 19 incomplete rows in registry. |
| **Native OS** | Old API retirement blocker | **BLOCKED** — Every real open still fails at retired `ProgramBridge::attach_backbone`. |
| **Artifact opening** | Partial, server-only boundary incomplete | PARTIAL — D0 kernel/all-features preflight red because concurrent stdio/B-rep edit graph cannot resolve `issues_scoped_to_new_solids`. |
| **Collaboration** | No two-user journey evidence | **BLOCKED** — Full native/MCP document transport, usable server open plan, nonempty verified provider, native supervisor remain absent. |
| **Short outage** | Partial recovery | PARTIAL — Exact checkpoint request/selection, scheduled live checkpoints, full reconnect convergence remain. |
| **Admin** | PostgreSQL + stack overflow | **BLOCKED** — Mandatory PostgreSQL law fails (Docker unavailable). New SQLite bilingual journey aborts before readiness because `Database::open_at` overflows default worker stack. |
| **MCP authority** | Process-level scope only | **BLOCKED** — Stable native/MCP real-child document-transport terminals and per-mutation workspace isolation remain unproved. |
| **Hub workspace** | Cache self-deadlock fixed | PARTIAL — Verified catalog activation and exact post-baseline tail cursor still block actual cold workspace; raw pair bytes remain private. |
| **AI map inference** | Zero wiring | **BLOCKED** — MCP `inference_get` deliberately returns `channel.not-wired`; hub has no route/readiness; generic job map is process-wide/non-durable. |
| **Integrated release** | No compound target | **BLOCKED** — No current end-to-end compound without old collaboration patterns and missing seams. |

### Dependency Order for Completion:
1. **Restore control-plane** (repo MCP entrypoint)
2. **Close share/directory privacy** (before collaboration)
3. **Fix artifact opening** + **reconcile presence** (React + native + Rust parity)
4. **Add canonical `(pack, spr)` bootstrap** + **hub artifact authority**
5. **Make hub workspaces enumerable** (authenticated DirectoryClient binding)
6. **Execute GIS inference/mutation** (typed revision-bound job channels)
7. **Complete outage/admin/backend/native parity** → **full-stack release gate**

## 3. Stdio Plugin Artifact Structure

**Path:** `✏️s/🔌️plugins/🗄️stdio/`

### Artifact Families Count: **36 families**
- ☁️las, ☁️ply, 🌐️html, 🌦️epw, 🎒️zip, 🎞️gif, 🎞️pptx, 🎥️mp4, 🎨️svg, 🎵️mp3, 🏗️ifc, 💬️bcf, 💾️binary, 📄️pdf, 📄️txt, 📊️csv, 📐️step, 📑️tsv, 📕️xlsx, 📜️docx, 📝️md, 📰️xml, 📷️jpg, 📷️png, 📼️avi, 🔊️wav, 🔣️json, 🖊️dwg, 🖊️dxf, 🖼️bmp, 🖼️tiff, 🗜️deflate, 🟪️stl, 🧊️gltf, 🧊️obj, 🧿️semio

### Crate Structure: **Still Monolithic**
- **Single root package:** `✏️s/🔌️plugins/🗄️stdio/📦️packages/🦀️rust/Cargo.toml`
- **Multiple subset-level crates:** 34 subset-specific Cargo.toml files exist under individual artifact/standard/subset/generator/engine paths
- **NOT a workspace split:** The main plugin is a single `cdylib`/`rlib` that pulls in all subsets; individual subset engines are separate crates for granularity but still linked into the monolith.

### Main Cargo.toml Quote (from `📦️packages/🦀️rust/Cargo.toml`):
```toml
[package]
name = "semio-s-plugin-stdio"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Stdio plugin - zero-app library of well-known file-format artifacts"

[package.metadata.component]
package = "semio:stdio"

[lib]
crate-type = ["cdylib", "rlib"]
path = "🦀️.rs"

# The plugin remains a single component link target that does NOT split by artifact family.
# Subsets are separate crates (Cargo.toml in each subset's engine dir) but still compose into
# the monolith's single wasm component.
```

## 4. Build Profile Overrides: Stdio `codegen-units` Workaround

**File:** Root `Cargo.toml` (profile blocks)

**Key quote — `[profile.dev.package.semio-s-plugin-stdio]`:**
```toml
[profile.dev.package.semio-s-plugin-stdio]
codegen-units = 1

# 🩹️ `stdio` also needs one dev codegen unit to avoid rust-lld's `ElemSection::writeBody` crash.
# This does not make its full debug component admissible: even at one unit that monolith crosses the
# component parser's one-million-function ceiling. Owned component publication therefore uses the
# optimized `[profile.wasm-release]`; debug builds remain compile/check inputs, never catalog bytes.
```

**wasm-release profile (for published components):**
```toml
[profile.wasm-release]
inherits = "release"
opt-level = "s"
lto = "thin"
codegen-units = 1
strip = "symbols"
incremental = false
trim-paths = "object"

# Explicit override for kernel:
[profile.wasm-release.package.semio-framework-os-kernel]
codegen-units = 1
```

**Interpretation:**
- **dev profile:** `codegen-units = 1` prevents rust-lld crash but monolith still exceeds 1M function limit — dev builds never published.
- **wasm-release profile:** `opt-level = "s"` + `codegen-units = 1` + external `wasm-opt -Oz` post-pass **is** used for catalog publication.
- **The monolithic component remains unpublishable** even at `-O` because registry audit (acceptance matrix) shows the parser rejects it.

## 5. Recent Activity: Another Session Is NOT Actively Working on Stdio

### Recent Git Activity (since 2026-09-03):
- **Last commit touching stdio:** `03100691d5` on 2026-09-03 18:13:43 (author: ueli)
- **Commit size:** 53 modified files, mostly artifact/subset schema/mutation/fixture updates in semio brep subset
- **File mtime check:** ~20 stdio files modified within last 8.5 hours, but no indication of a concurrent refactor-in-progress

### Conclusion:
**No active concurrent session working on a split.** The last touches are infrastructure updates (registry, project.json, script.ts). The monolith is static as of yesterday afternoon UTC+2.

---

## Summary

| Question | Answer |
|----------|--------|
| Is the split done? | **No.** Stdio remains a monolithic `semio-s-plugin-stdio` cdylib. Individual subset engines are separate crates but still compose into one wasm component. |
| In progress or not started? | **IN PROGRESS** at the research/design layer (ticket 26/09/02/SEPARATE-ARTIFACT-STANDARD-SUBSET-IMPLEMENTATIONS-... is open with extensive phase docs) but **NOT IMPLEMENTED** as a split crate structure. |
| Blocker for end-to-end demonstrator? | **YES, CRITICAL.** Acceptance matrix: Registry area is **BLOCKED**. Stdio's monolithic component exceeds the wasm component parser's 1M function limit, preventing publication to the catalog. The workaround (publish only wasm-release, never debug) hides the blocker at build time but surfaces at catalog validation. No split = no published registry = no registered artifacts = demonstrator cannot enumerate/use stdio families. |
| Recent work by another session? | No active concurrent work detected. Last touch was 2026-09-03 18:13:43. |

## Remaining Unknowns
1. **Why has the split not been implemented?** The ticket is open with design docs but no implementation phase. Dependency on registry/artifact-opening fixes?
2. **Is split-by-family the right fix?** Acceptance matrix suggests the issue is function count; a family split would reduce each component individually, but **how many families can link safely?** No sharding strategy found.
3. **Fallback: Can wasm-opt post-processing reduce the monolith?** Currently used for size only (`-Oz`); no evidence of function-count reduction testing.
