# 📓️ Status — 2026-09-16 session (Fable coordinator)

Ticket reopened manually on disk (repo MCP failed to connect this session: CONNECTION_CLOSED).
Goal: demonstrator working end to end including ALL plugins and extensions in its runtime closure.

## 10:05 — orientation
- Demonstrator is now Nx-driven: `@semio-tech/mit-bestand-demonstrator:dev` → `activate-dev` → `prepare-dev`
  → `framework-os-dev:prepare-{aggregator,aussuchen,bearbeiten,generation3d,generator,koordinator,verfolgen}-react-dev`
  + `activate-generator-react-dev`. Runtime script: `♻️mit-bestand/🧺️demonstrator/🔨️modules/🧩️runtime/📜️script.ts`.
- Pane catalog (`🔨️modules/🧩️runtime/🔣️.json`): generator→generation3d(procedural), koordinator/aggregator/aussuchen/bearbeiten/verfolgen → demonstrator plugin.
- On disk: `🔌️plugin/📦️packages/🟦️typescript/dist/dev/🔌️plugin-modules/` has NO `🎪️demonstrator` module; `dev/dist/runtime/react/dev/` has NO `generator` activation. Both must be produced.
- Machine: load ~10, a peer serves `sourcing react dev` on :6081. Nothing on :6029.

## 10:40 — BLOCKER: Xcode 27.0 installed 04:53 today, license unaccepted → every native link fails
`semio-framework-os-font-assets:build` (bin `dump-guestslim-typst-fonts`) failed: `linking with cc failed: exit status 69 …
You have not agreed to the Xcode license agreements`. `/Library/Preferences/com.apple.dt.Xcode` shows the license was accepted
for 26.6; `/Applications/Xcode.app` is now 27.0 (27A266a). This kills every build script / native bin, and `python3` too.
Fix for the dev (needs sudo, I cannot do it): `sudo xcodebuild -license accept`.
Workaround used for this session's builds (per-process env, no system change): `DEVELOPER_DIR=/Library/Developer/CommandLineTools`
— CLT clang 21.0.0 links a test program fine. run1 of `activate-dev` stopped (SIGINT to its process group); run2 restarted with the override.

## 10:55 — audits landed (see 📓️explore-2026-09-16-build-pipeline.md, 📓️explore-2026-09-16-closure-and-extensions.md)
- Closure = 21 components (8 plugins + 9 flow ext + 4 process ext). `🎪️demonstrator` module never materialized; `gis` wasm stale vs 09:56 commit.
- `cad-extension-*` (topic `cad.computer`) and `sourcing-module-*` (`sourcing.module`) unreachable: nobody `consumes` those topics → Opus worker wiring them (Cargo metadata + manifests + runtime discovery if missing) and building the 7 components, then registry regen.
- `SKIP_PLUGIN_BUILD`/`SKIP_ENGINE_BUILD` are dead → launch.json `mit-bestand-demonstrator-fast`/`-noengine` are stale, to be removed.
- run2 (`activate-dev`, DEVELOPER_DIR override) passed fonts; in `cad-plugin:component-dev`.

## 11:20 — compile health + fix wave 1 dispatched
- Compile-health lane (📓️fix-2026-09-16-compile-health.md): 28/29 closure crates clean; `semio-s-plugin-demonstrator` had 13 errors
  (manifest imports `<host>::editor::<app>` / `::viewer::` that were private or absent) → fixed by making `editor`/`viewer` `pub` in
  cad/process/sourcing and adding `pub mod editor` re-exports in gis/procedural/puzzle. 29-crate wasm check finished, 0 errors.
- launch.json: removed dead `mit-bestand-demonstrator-fast` / `-noengine` entries.
- Workers running: cad/sourcing extension topics (+7 extension components), InteractionView threading (sourcing preview, puzzle/procedural inspection, cad HUD),
  gis TiledMapHost selection/hover dispatch + example catalogue, generator flow eval session.
- run2 in `demonstrator-plugin:component-dev`; machine load ~100 → workers told to cap CARGO_BUILD_JOBS=4.

## 12:05 — run2 built all 21 components (40m49s) but failed the final activation gate
`Demonstrator activation does not contain its exact runtime union`: the receipt listed 21 components while the registry had just been
regenerated by the extension-topics worker (closure now 28: + cad-extension-{aec-building,aec-building-energy,aec-building-structure,spatial-shape},
sourcing-extension-{beams,slabs,windows}; see 📓️fix-2026-09-16-cad-sourcing-extension-topics.md). Also found there: the committed
`📐️cad/🧩️extensions/🏢️aec-building/🔣️.json` was a placeholder (`pluginId: "empty"`) — replaced with the real descriptor. run3 started.

## 12:20 — run3 `activate-dev` SUCCEEDED (10m52s, 84 tasks): receipt = exact 28-component union. Dev server started on :6029.

## 12:50 — :6029 serving; first browser pass
- Fixed: demonstrator vite config passed only `receiptDirectory` to `semioActivationVitePlugin` (framework widened the options 09-15) → server crashed
  with `options.components.map`. Added `demonstratorActivationComponents()` in the node-only `♻️activation/🟦️.ts` (first attempt in the browser-shared
  runtime module pulled `node:path` into the page — moved).
- Boot: generator READY (flow contributions installed, previewEval started), koordinator READY. aussuchen / bearbeiten / verfolgen CRASH at app creation:
  `genesis_child_pack … derived child dialect 's.stdio.semio@v1/kit' is not declared by this app's member roster` (demonstrator bundle roster ≠ standalone plugin roster)
  → Opus worker on it (📓️fix-2026-09-16-demonstrator-member-roster.md). Many `program load failed … timeout 30 s` are load-induced (load avg 100-160 with the fleet compiling).
- Worker reports landed: generator eval (audit stale, laws added), gis selection dispatch (real return-path bug fixed + example catalogue; NOTE staged 🔣️.json manifests
  for gis/demonstrator still carry `reuse-map` → needs component restage), interaction-view threading (framework `window_engagements` hole + gis threading fixed).

## 13:00 — pane results with the current bundle (browser, one tab, fronted)
| pane | shell | windows/content |
|---|---|---|
| generator | READY | preview meshes 3.7 KB / 707 B instances, status "Abgeschlossen · Netze werden tesselliert (2/2)" |
| koordinator | READY | 4 windows, meshes 41.9/46.9/0.8/3.2 KB with instances |
| aggregator | READY | Top + Perspective, mesh url lists + instances |
| aussuchen / bearbeiten / verfolgen | CRASH | member-roster panic (worker running) |

## 13:40 — wave-2 reports
- aggregator catalogue (📓️fix-2026-09-16-aggregator-catalogue.md): audit item stale (catalogue was never empty); real fixes: add-object-kind read only `meshUrl`,
  templates seat at `point` not `position`. 734→739 tests.
- aussuchen chrome (📓️fix-2026-09-16-aussuchen-ui-chrome.md): most chrome already existed; added curate/uncurate row buttons, Curated sort, restock button. 129→142 tests.
  BLOCKER found: sourcing `setContributions` refused by the 96-byte config envelope → sourcing modules never reach the Pool → new worker
  (📓️fix-2026-09-16-sourcing-contributions-envelope.md) also verifying cad.computer contributions.
- gis worker: 244/0; `gis_map_live_envelope_submit_pump_swap_…` HANGS (>110 min) — belongs to the GIS-2D-END-TO-END-BUILD peer.
- still running: member roster (demonstrator component rebuilding), koordinator object mutations, sourcing contributions envelope.

## 14:05 — ALL SIX PANES BOOT on the rebuilt bundle (demonstrator wasm 13:23, 28-component receipt)
- Member roster fix (📓️fix-2026-09-16-demonstrator-member-roster.md): `ArtifactEditor::Members`/`ArtifactViewer::Members` — roster is a property of the app.
- Server had wedged (vite listening, never answering — the known post-restage wedge); recycled by pid, serve script run directly (`serve-6029-2026-09-16-run4.txt`).
- aussuchen READY: Pool 10 rows, Beams/Windows/Slabs toggles, −/+ curate buttons, preview mesh 804 B. verfolgen READY (2 canvases). bearbeiten READY: workpiece mesh 7.0 KB,
  Werkstatt/Katalog/Inspektion tabs. generator/koordinator/aggregator as before.
- Next: full six-pane landing boot, extension checks (workshop machines, cad computers), acceptance e2e run, final restage after the sourcing-contributions worker.

## 14:25 — extension checks + concurrent-boot defect
- bearbeiten Werkstatt panel: MASCHINEN = Generic Saw/Drill/Attacher, Circular/Table/Band/Chain Saw, Drill Press, CNC Router, Doweling Jig, Screw Gun
  + CONCRETE / METAL / ROBOTIC sections → the four process extensions land at runtime.
- Six-pane simultaneous boot: panes booting while another shard compiles the 210 MB demonstrator wasm hit `timeout loading demonstrator after 30002 ms with no progress`,
  and the retry/close path then traps the actor: `runtime close cleanup faulted for instance N: the cleanup pump made no progress for its whole stall credit
  [zero-progress] (elapsed 0us, ceiling 8000us)` → Plugin Recovery. Each pane alone boots fine. Worker dispatched
  (📓️fix-2026-09-16-concurrent-boot-timeout-and-close-fault.md).
- test-e2e run1 started 14:05 (rebuilds cad + demonstrator components first because sources changed).
- koordinator chrome offers "Form", "Gebäude", "Energie", "Tragwerk Klassisch" = the four cad extensions (spatial-shape, aec-building, -energy, -structure) → cad.computer packs land.

## 15:05 — e2e run1: 1 passed / 8 failed; wave 3 dispatched
- e2e failures: `[DEBUG]` console.error lines counted as errors (filter fixed in spec); drift guard read `🪧️brand.ts` from the wrong dir (fixed);
  overview intro-skip click not stable; generator node-graph `data-fixture-json` widgets=0; aussuchen `framework.window.sourcingPreview` host locator never visible;
  verfolgen map canvas paints one flat colour (tiles in headless?). → worker "acceptance suite" (📓️fix-2026-09-16-acceptance-suite.md).
- sourcing contributions worker (📓️fix-2026-09-16-sourcing-contributions-envelope.md): sourcing lane fixed (144 tests), cad never had the bug; BUT the host kernel
  `scopeContributionsJson` reachability cut drops every capability-topic pack (process.machines / cad.computer / sourcing.module) → process's 11 machines are BUILT-INS,
  not contributions. Plus a ~4 KiB retained-config close cliff. → worker "kernel capability contributions + close cliff" (📓️fix-2026-09-16-kernel-capability-contributions-and-close-cliff.md).
- concurrent-boot worker still running. verfolgen crashed on a single-pane boot after the 14:5x restage — investigating.
- 14:40 concurrent-boot worker landed (📓️fix-2026-09-16-concurrent-boot-timeout-and-close-fault.md): load clock now fed by streamed descriptor bytes + shard
  liveness beats, one descriptor request per (plugin,url); close stall needs 8 zero steps AND 8 ms real time, retirement counts as progress, a faulted close no
  longer kills the actor's turn. Restaged 14:30. Serve pegged at 99.7 % CPU (wedge after host-edit burst) → recycled again (run5).

## 15:20 — full six-pane landing boot on the 14:30 bundle + recycled server: NO crashes
Observed READY beacons in one page load: generator, koordinator, aggregator, bearbeiten, verfolgen (aussuchen suspended by the page's idle-poster
mechanism before its beacon was sampled; resumed via its card to confirm). Load avg ~8.

## 20:15 — session process restarted at ~20:05 (kernel worker had died on the 16:45 rate limit; resumed by SendMessage)
- Supervisor restarted (launch.json `mit-bestand-demonstrator-supervised`). Bundle on disk 19:42.
- Console now shows `contributions publish {"plugin":"demonstrator","instanceId":2,"outcome":{"status":"installed","chars":29909,"kinds":[]}}` → the cad
  capability pack reaches the koordinator app (D1 kernel fix live). Remaining: `setContributions` admission ceilings (kernel worker), process3d catalog dedupe (worker).
- 20:20 landing page, one load, MutationObserver from the start: generator, koordinator, aggregator, aussuchen, bearbeiten, verfolgen ALL READY (idle-suspended afterwards). Load avg 50 (peer test fleets).
  Only remaining console error class: `setContributions command failed demonstrator typed command raw JSON exceeds its registered retained-page admission` (kernel worker resumed on it).

## 22:40 — final bundle (21:52) verified in the browser
- Landing page, one load, observer from start: ALL SIX panes READY. Every demonstrator instance logs
  `contributions scoped pack {"chars":29909,…,"consumes":[…,"process.machines","cad.computer","sourcing.module"]}` → `contributions publish … status installed`;
  no admission errors in the current boot. Kernel worker (📓️fix-2026-09-16-kernel-capability-contributions-and-close-cliff.md): capability packs scoped by the
  receiver's `consumes`; retained-field close drains in grant-sized pages (65 KiB config closes); `CONTRIBUTIONS_COMMAND_RAW_WIRE_BYTES = 49_152`.
- process3d: `installed_catalogs` dedupes by catalog_id (📓️fix-2026-09-16-process3d-catalog-dedupe.md), 308 passed.
- Duplicate serve supervisors (peers started the launch entry) stopped; one supervisor (pid 80164) keeps :6029 alive.
- Acceptance e2e run3 launched 22:20 on the final bundle.
- 22:50 bearbeiten (deep link, fresh load) Werkstatt panel: MASCHINEN (11) + CONCRETE + METAL + ROBOTIC — one section per catalog id, no duplicates.
  The pane chrome now hides panel tabs behind a "Panel" toggle (peer change today) — probes must click it first.

## 2026-09-17 00:15 — Energie (energy) + Statik (fem3d) panes added (Fable coordinator, goal: fourth top-right / fourth bottom-right)
- Design: NO change to `semio-s-plugin-demonstrator` (energy/fem have `dependsOn: []`; bundling would mean a 40-min wasm rebuild of a contended crate).
  Pane rows `energy`/`fem3d` in `🔨️modules/🧩️runtime/🔣️.json` (+ `🧫️pipeline.json`), `DEMONSTRATOR_PANES` in `🪧️brand.ts` reordered
  generator, koordinator, aggregator, **energie**, aussuchen, bearbeiten, verfolgen, **statik**; `DEMONSTRATOR_GRID_COLUMNS = 4` in `🟦️.tsx`;
  brands `entwerfen-mit-bestand-energie` (example `bestest-600`, window `energy.model.3d`, icon `sun`) and `-statik` (example `concrete-forest`,
  window `fem3d-model`, icon `fem-app`) with German introductions; `ENTWERFEN_MIT_BESTAND_BRAND_IDS` = 8; os-dev `🏷️brand` SHELL_BRANDS + engine-contract
  test updated; intro copy "vereint acht Werkzeuge".
- BLOCKER solved: `readDemonstratorActivation` read only the `generator` lane receipt (= closure of pluginId demonstrator, 28) → union (30) could never match.
  Now a demonstrator-local UNION receipt merges lanes `generator` + `energy` + `fem3d` (`♻️activation/🟦️.ts`, published to
  `♻️mit-bestand/🧺️demonstrator/dist/♻️activation/dev`, re-published by a demonstrator vite plugin when a lane receipt changes);
  `📋️project.json` `activate-dev` depends on `activate-{generator,energy,fem3d}-react-dev`, `prepare-dev` on the two extra `prepare-*` targets.
  Dry run: 30 plugin ids across 3 lanes. See 📓️fix-2026-09-17-energie-statik-runtime-union-receipt.md, 📓️fix-2026-09-17-energie-statik-brands-and-grid.md,
  📓️fix-2026-09-17-energie-statik-acceptance-cases.md (PANE_CASES energie: 4 windows incl. new `tree` surface kind; statik: 2 world3d windows).
- Browser (:6029, serve recycled by the supervisor 00:09 with the new vite config): overview grid 4×2 renders all 8 cards; `/#energie` READY —
  BESTEST 600, 61 tree rows, 1 zone row, 3D model painted, intro "Das Energiemodell"; `/#statik` READY — example "Betonwald" (concrete-forest),
  Modell + Ergebnisse (Case: dead) both painted, intro "Das Tragwerksmodell". Load avg 100–120 (peer fleets), boots take 15–30 s.
- Found (out of slice): `✏️s/🔌️plugins/🔋️energy/🔣️.json` descriptor is stale (omits `energy.model.3d`, mislabels `framework.window.tree`) — needs a describe regen.
- `activate-dev` run1 (log `🗑️generated/activate-dev-2026-09-17-run1.txt`) started 00:07, queued behind the nx daemon graph rebuild under load.
- 01:05 `activate-dev` run1 (25m14s, 80 tasks, 3 cache hits): `activate-energy-react-dev` and `activate-fem3d-react-dev` completed ("changed", fem component
  rebuilt); the demonstrator vite union plugin re-published `dist/♻️activation/dev/🔣️receipt.json` live (30 rows, new energy/fem shas) without a restart.
  Run FAILED on `demonstrator-plugin:component-dev` + `procedural-plugin:component-dev`: `error[E0599]: no method named try_id found for struct TreeItemBuilder`
  (procedural generation3d catalogue panel) — caused by a PEER's staged, uncommitted edit of `🧰️framework/🔨️modules/🖱️ui/🧬️contract/🏗️builder/🦀️.rs`
  (mtime 23:52), not by this change; the staged 22:39 demonstrator/procedural modules keep serving. Re-run `activate-dev` once that peer lands.
  Runtime agent (rate-limited at 00:55, report complete) also: `activate-lanes-dev` (nx:noop) aggregation target so `activate-dev.dependsOn` keeps the
  two-entry contract; `🧫️cases.json` extensionIds drift (cad/sourcing extensions) repaired; one-line framework contract edit in
  `⚡️caching/🧪️tests/⚡️cache-contracts/🟦️.ts` (root set now follows `DEMONSTRATOR_RUNTIME_TARGETS`). Demonstrator vitest (coordinator re-run): 4 files / 21 tests pass.
- Not run here: Playwright `test-e2e` (a peer's run3 is still live on the old bundle and it rebuilds components → blocked by the same try_id error).
- 01:30–01:55 landing-page (overview) boot investigation with headless probes (`🔍️landing-eight-pane-probe.ts`, `🔍️pane-ready-probe.ts`,
  `🔍️landing-stall-console-probe.ts`, `🔍️landing-pending-requests-probe.ts` in this folder): the sequential queue works (a new pane every 35 s,
  order generator, koordinator, aggregator, energie, aussuchen, bearbeiten, verfolgen, statik), generator + koordinator reach READY, but later
  panes ended "booting"/"error" in three runs. Root causes are environmental, not the new panes: (1) the supervisor's 15 s liveness curl fires while
  vite streams the boot's wasm under load 50–120 → recycle mid-boot (01:50:04, serve pid 26272→29491, page force-reloaded, shells → error);
  (2) peer HMR bursts on ShellHost force `page reload 🟦️.tsx` (01:34:34) mid-boot; (3) the peer e2e run3 log ends in ENOSPC at 23:05 (disk was full;
  42 GB free at 01:45). Connection-pool exhaustion ruled out (3 in-flight requests). Every pane boots READY alone via deep link (aggregator 24 s,
  koordinator 8–23 s, energie, statik). Supervisor made tolerant (45 s probe, three misses ≈ 2 min before recycle) and restarted.
- 02:15 ROOT CAUSE of the landing-boot stall (headless `🔍️landing-sse-probe.ts`, no serve recycle, no HMR during the run): every `FrameworkOsShell`
  opens TWO permanent EventSources on subscribe (`/🔌️plugin-modules/watch` + `/🧩️extension-modules/watch`, `createDevPluginSource`/`createExtensionSource`
  in `🎠️kernel/🟦️.ts`, since fe7c8a8f8b 09-05). HTTP/1.1 dev server + Chromium's 6 connections per origin: shell 1 → 2, shell 2 → 4, shell 3's pair
  fills the budget (they connect only 20 s later) and every later fetch on the page (descriptor `🔣️.json`, `.core.wasm`) queues behind six idle streams →
  the 3rd pane and all later ones stay "booting" with no console output (the idle deadline is forgiven by other shells' shard beats; the 300 s ceiling
  never surfaced within the probes). Page main thread idle (0 ms latency), no web locks, no IndexedDB, only 2 non-SSE requests in flight.
  Independent of WHICH panes: with the old 6-pane order the stall is the same (energie is 4th). Fix in flight (Opus worker): one shared watch
  EventSource per url per page with cached-snapshot replay for late subscribers → 📓️fix-2026-09-17-shared-plugin-watch-eventsource.md.
- 02:35 FIXED + VERIFIED: page-shared watch EventSource (`subscribeSharedWatchStream` in `🎠️kernel/🟦️.ts`, one stream per url per page with
  cached-snapshot replay; 9 new unit tests green, `@semio-tech/framework-kernel test-quick` 67/68 with the one failure the pre-existing ajv
  `NonZeroU64` reference in `📤️return/📦️content`). Landing page, ONE load, headless, no recycle: generator 11 s, koordinator 44 s, aggregator 78 s,
  **energie 111 s**, aussuchen 150 s, bearbeiten 185 s, verfolgen 220 s, **statik 250 s** — ALL EIGHT READY; only 2 SSE streams on the page;
  content: 61 tree rows, 11 zone/table rows, 15 canvases, 12 world3d hosts. Remaining console error classes are peer-owned
  (`setContributions … exceeds its exact output cap` on koordinator, `[DBGARCH]` archive-load debug polls).
- Open for peers: `activate-dev` full re-run still fails on `demonstrator-plugin`/`procedural-plugin:component-dev` (`TreeItemBuilder::try_id` is a
  `HasBase` trait method; the procedural generation3d catalogue panel call site lacks the trait import — peer refactor of `🖱️ui/🧬️contract/🏗️builder/🦀️.rs`);
  the served 22:39 modules + union receipt keep :6029 valid. Energy descriptor `✏️s/🔌️plugins/🔋️energy/🔣️.json` is stale (missing `energy.model.3d`).
