# GIS Startup and End-to-End Verification — 2026-09-12–13

The ticket was reopened through the repository MCP after reading `repo://goals`. Its existing r26-03 association is retained.

## Final Result

The public development command now starts the real GIS application end to end. The verified invocation was `SEMIO_RENDERER=react bun nx run workspace:dev -- gis 2d`, matching the React renderer in the reported failure. The server remains available at http://127.0.0.1:6040/.

- The source watcher registers immediately, then the complete development dependency graph builds, materializes and activates GIS. The server reported one fresh component matching its activation receipt, Vite ready in 16,684ms, and HTTP 200.
- The public browser rendered the world map and fixture positions. Image mode visibly changed to raster imagery and its command settled at 22:24:25 UTC. After a watcher activation refresh, Combined mode rendered again; disabling Labels visibly removed geographic labels and settled at 22:25:21 UTC. Labels were restored, then zoom settled at 22:25:39 UTC. Earlier actual-app checks also verified Vector mode, Figure Ground style and marker selection.
- All 17 map tests pass, including idle repaint and complete pan cancellation. Startup, graph-import, watcher environment/readiness, native watch filtering, atomic artifact publication, Cargo/WASM output isolation and cross-process resource-lease checks passed as detailed below. The authored Nx patch matches the installed package; authored diff whitespace checks pass.
- The source fixes cover graph discovery/readiness, task-versus-watcher daemon routing, compiler-cache exclusions, publication contention, private final compiler outputs, two Rust compile errors, and map redraw/cancellation behavior.

Validation ran on macOS arm64 with Bun 1.3.14. Native Windows/Linux runtime execution was not tested. Middle-button browser dragging was not available through the documented browser automation API; cancellation was exercised through the actual parsed handler and scheduler. GIS still emits the shell's `no-operator-graph` contribution diagnostics; no other errors appeared during the final public-server command checks. The public source watcher was observed activating changes and refreshing the application. Earlier circular-import HMR fallback messages and a retired 6041-host reload failure are recorded only as historical diagnostics, not as failures of the final 6040 verification.

The chronological investigation below preserves failed attempts and intermediate pending states; this final result supersedes those statuses.

## Scope

Run the public `bun nx run workspace:dev -- gis 2d` command successfully, verify the actual GIS application and map interactions in a browser, and fix failures encountered on that path. Earlier ticket measurements used a standalone map harness and explicitly left the GIS development application unable to boot; those measurements do not establish current end-to-end success.

## Initial Evidence

- The reported launch blocks in `NxScript.run` awaiting `watch process waiting...`, with a fixed 120-second deadline.
- Installed Nx registers its watcher only after awaiting its project graph.
- The custom project provider calculates script import closures independently for every target. Its parser cache retains import text but each closure walks, reads, and resolves the shared files again. Many targets reference the same command router.
- A first reproduction failed even earlier on concurrent Print taxonomy edits. A subsequent launch passed that point and is under observation.
- No existing test or old status claim is treated as evidence of current runtime success.

## Validation Plan

1. Freeze repeated-discovery and cross-pass source freshness expectations in a language-neutral fixture.
2. Compare closure results with esbuild and verify bounded file reads within one graph pass.
3. Re-run the public launch and inspect browser console, map painting, zoom, pan, and tile transport.

## Results

- The new regression check failed before implementation: graph discovery had no pass-scoped script input cache.
- Added a per-graph cache for resolved file imports and complete command closures, shared across project targets. Independent calls and subsequent graph passes get a fresh cache.
- The regression check passed through Nx in an isolated ticket-owned test workspace: 40 overlapping closure requests read each file once, match esbuild, agree with native Node execution, and refresh changed transitive imports on the next pass.
- The first full-workspace test attempt spent over a minute in graph construction and then encountered an existing dependency cycle between `@semio-tech/framework-os-kernel` and `@semio-tech/value-derive-rs`. The bounded fixture workspace allows the regression check to execute independently of that workspace graph issue.
- Reproduced the exact watcher timeout with `SEMIO_RENDERER=react bun nx run workspace:dev -- gis 2d`. Without that terminal setting, the same command selects the WGPU path in this environment.
- Replaced the arbitrary readiness deadline with readiness/exit/error handling, progress every 15 seconds, and existing process cancellation. Four language-neutral lifecycle cases pass, including a simulated 150-second cold startup, split readiness output, error exit, and cancellation. The readiness marker is checked against installed Nx's actual watch implementation.
- The live React invocation passed watcher readiness after roughly 60 seconds and started the GIS task graph with 19 dependencies. Flow WASM compilation completed.
- The watcher incorrectly scheduled activation for compiler cache `.lock` writes. Added explicit compiler and map cache roots to `.nxignore`. The `ignore` library and Nx's native watcher both validate cache exclusion while retaining source edit events. The native fixture supplies the equivalent exclusion globs directly with inherited ignore processing disabled, because its required ticket location is itself ignored by the parent repository.
- The ignore configuration update restarted the daemon and interrupted that in-flight build. The public React launch has been restarted with the new configuration.
- Re-enabled `TiledMapHost` in the existing React renderer long suite and corrected its broken relative import. Vitest executed all 11 map tests successfully (13ms test time; ~30s including module transforms).
- Full GIS application boot and browser checks remain pending.
- The command import regression also passes through the full workspace graph with `NX_DAEMON=false bun nx exec --projects=workspace --excludeTaskDependencies --skipNxCache -- bun <ticket>/📜️script.ts imports`. The explicit dependency exclusion makes this diagnostic run once on the selected workspace, avoiding unrelated package-cycle traversal.
- Registered all ticket verification modes (`imports`, `watcher`, `map`, `graph`) in VS Code as **🗺️gis verification**, with matching authored seed and generated launch entries. Both JSONC files parse successfully.
- The live watcher and initial startup raced while publishing browser-support artifacts, exposing an `EEXIST` failure in the synchronous publication lock. A language-neutral publication fixture reproduced the missing wait/cancellation behavior. Publication now uses existing SQLite resource leases, asynchronous copying, ownership validation under the lease, and awaited callers. The focused fixture passes cancellation and contention and compares delivered bytes with Python file reads. All 18 direct publication callers/helpers have valid async boundaries.
- Flow, Surface and Editor WASM builds completed in the independent GIS preparation target. The original development supervisor was stopped after its failed browser-support task; preparation continues before restarting the public launch.
- Resource lease checks pass across Bun, Node and Python: shared readers, exclusive writers, independent resources, cancelled waiters, killed owners, cold initialization, bounded repeated storage, identity guards and ordered multi-resource acquisition.
- The real `@semio-tech/framework-os-dev:prepare-gis2d-react-dev` target succeeded with its 17 prerequisites, including GIS component compilation and browser materialization (18m18s in this shared cold-build run, 5/18 cached). The public development command has been restarted for browser validation.
- Added `NX_PROJECT_NAME` and `NX_FILE_CHANGES` to the authored Nx patch's task-environment exclusions. The existing native Nx fixture failed before this change and passes afterward, while `NX_LOAD_DOT_ENV_FILES` still changes graph identity. This prevents watcher callback bookkeeping from repeatedly invalidating the graph. The complete authored Nx patch matches the installed package in a reverse dry-run check.
- Activation of the already-prepared GIS application succeeded via `NX_DAEMON=false bun nx run @semio-tech/framework-os-dev:activate-gis2d-react-dev --excludeTaskDependencies --output-style=stream`. It validated the component, session, support and fonts and published the runtime receipt. A second actual app server is being launched on port 6041 through the existing serve target, while the public watch invocation continues on its configured port 6040.
- The actual application served on 6041 and rendered a vector map with geographic labels and fixture positions. A wheel gesture produced a completed `setCamera` invocation. Keyboard selection of Image mode exposed `typed-operation failed: validation failed: batched publication requires preinstalled fixed applied and revision capacity`; the optimistic control changed but the map retained vector rendering, so this is not counted as a successful mode change.
- The store source was newer than the compiled GIS component (22:53 versus 22:41 local). The current shared source already contains cursor-capacity preservation and a targeted `retained_latest_wins_cold_rebase_preserves_admitted_cursor_capacity_for_next_publication` regression. Rebuilding the actual GIS preparation target against that source before deciding whether further store changes are needed.
- The shared daemon kept restarting target graph requests for more than 12 minutes while independent Nx requests completed. Watched task processes now compute their own Nx graphs (`NX_DAEMON=false`) while the source watcher retains the daemon. Ordinary commands preserve their daemon selection. Four language-neutral process cases pass against installed Nx's native daemon-enable decision; the existing readiness, ignore and resource-lock suites still pass. Restarted the public launcher with this routing correction.
- The current rebuild encountered a concurrent viewport-schema edit with an invalid derived `Default` bound. The shared source already contains its manual generic `Default` correction; no duplicate viewport edit was made here.
- All-workspace subscriptions including global files no longer request an initial project graph before registering. Project-scoped and project-only subscriptions retain graph validation. Three language-neutral cases execute the installed Nx registration method and verify graph reads and subscription contents. The new case failed before the patch and passes afterward, together with the complete focused watcher/publication/lease suite; reverse dry-run verifies the authored patch matches installed Nx.
- The next exact public React launch reached `watch process waiting...` immediately after daemon startup and launched its independent task graph. The previous rebuild also exposed E0502 in store envelope cleanup. A concurrent edit corrected that borrow before this task's proposed change applied; no duplicate store edit was made.
- The same public launcher completed Flow WASM, browser support, schema generation and GIS session staging. Native font compilation then waited on Cargo's shared uplifted-artifact lock, observed through its open lock files, before reaching a new E0308 failure in WGPU tree painting. Renamed the local control rectangle so it no longer shadows the borrowed `UiControlNode`; this preserves the control's enum type for selection and text painting. The existing launch continues its other prerequisites while the watcher queues source-triggered activation.
- Artifact Cargo builds now put uplifted outputs inside their existing private capture directory, while `.cargo/config.toml` keeps compiler intermediates shared. This removes contention on the workspace's final-output lock. The language-neutral publication fixture compiles a real small executable with native Cargo as its oracle, checks identical delivered stdout, verifies the shared compiler directory remains, and verifies private outputs retire. It failed against the former shared output directory and passes with the change. The complete focused watcher/publication/lease suite passes again. Restarted the exact public GIS command after those checks.
- Both source-triggered and initial Flow WASM builds then waited on the same final-output lock. Applied private final-output capture to `runWasmPackWebBuild` as well, with cleanup in `finally` and exceptions for compiler/missing-output failures. Three language-neutral cases exercise the actual wrapper with controlled compiler outcomes; the success artifact is validated by native WebAssembly. All three cases and the complete startup/publication/lease suite pass. Restarted the public command with both compiler paths corrected.
- The fresh GIS component compiled successfully in 10m02s, then materialized and activated. The actual React application on port 6041 loaded this component. Image and Vector mode commands and wheel-camera commands settled successfully with Invocation/Ephemeral frames. The previous fixed-publication-capacity error did not recur in these commands.
- The public launch completed 16 prerequisites but Puzzle WASM failed while copying missing shared incremental `.o` files. No shared compiler cache was deleted. A retry instead encountered concurrent Pack export edits: initially missing exports, then a duplicate `RetainedPackChunkEntry`. Removed only the duplicate and restarted the public command. Full public-command success remains pending at this point.
- Browser verification found that an idle map retained labels after the visibility command settled. `MapRenderer` appearance setters and selection synchronization updated WASM without waking the demand frame scheduler. Five language-neutral repaint cases failed before the correction (four unchanged frame counts, one absent renderer synchronization boundary). The existing Vitest clock drives the real scheduler and verifies both renewed rendering and return to idle. All 16 map tests passed after frame invalidation was added. The vector-style fixture subsequently uses the valid `figureGround` schema value.
- Live hot reload applied the renderer change. At 22:04:38 UTC, `toggleLayerVisibility` settled and the screenshot showed geographic labels disappear without a camera gesture; fixture position labels remained because they belong to their separate layer. Restoring Labels and selecting Figure Ground settled at 22:05:08 UTC and visibly changed the map palette. A further wheel-camera command settled at 22:05:45 UTC and changed geographic scale.
- The shell still emits `no-operator-graph` contribution diagnostics for the GIS document. These are separate from the successful map commands and have not been represented as a clean console. Browser middle-button panning has not been exercised because the available documented drag control supports left drag only; the 11 pre-existing map tests cover cache/debounce/upload behavior, not browser panning.
- The corrected vector-style fixture passes in the complete 16-test map suite. Temporary camera tracing showed the wheel result `{x:0.0010359116022099456,y:-0.03574952080279631,zoom:709.5200000000001}` applied unchanged after the GIS command returned. Selecting the fixture marker completed `interactionSelect` at 22:09:00 UTC and retained the camera. The temporary camera logs were removed afterward.
- Actual development tile routes returned HTTP 200: `/osm/0/0/0.png` was a 6,927-byte PNG independently recognized by the system `file` utility as 256×256; `/vt/0/0/0.pbf` returned 79,865 bytes with `application/x-protobuf`.
- A cancelled middle-button pan cleared pointer state but retained the continuous-frame reason. The regression invokes the actual TypeScript-parsed cancellation callback through an EventTarget while driving the real scheduler with Vitest: frame count grew from 125 to 187 after cancellation before the fix. Added the missing `endContinuousInteraction("pan")` to the cancellation handler. The complete map suite passes all 17 tests (469ms test time, 41.75s including transforms).
- Extended cancellation verification to require releasing the WASM interaction too. That check failed with zero `pointerUpScreen` calls; the handler now forwards the local pointer position before clearing state. All 17 tests pass again (231ms tests, 15.40s total). No temporary camera logs remain.
- The public retry completed Flow, Surface, Editor and Puzzle WASM, font staging, and GIS component compilation without the previous compiler/cache errors. Browser materialization and activation are the remaining launch checks at this stage.
- Final public-command verification completed: initial activation published one GIS component, the server returned HTTP 200 on 6040, and the browser passed the mode/layer/camera checks recorded above. The source-watch callback then also successfully ran `activate-gis2d-react-dev` with its 18 prerequisites, materialized the browser bridge and descriptor, and published another changed GIS activation. The application refreshed successfully. The separate 6041 verification server was stopped; the public 6040 development server is left running. Ticket-generated logs, compiler probes and temporary outputs were removed after the results were recorded; authored scripts, test configuration inputs and reports are retained.
