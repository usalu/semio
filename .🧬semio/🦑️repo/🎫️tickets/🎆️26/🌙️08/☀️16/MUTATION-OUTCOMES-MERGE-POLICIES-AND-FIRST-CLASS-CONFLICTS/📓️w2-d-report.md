# Lane 2-D (React shell UI + i18n) — C9 report

## What landed

1. **`EventFeedHost`**: `FEED_TONE_CLASS` gained a `fatal` tone; exported `mintMutationMessageFeedEntry(message, id, timestampMs, iconId?)` mints an `EventFeedEntry` from a `MutationMessage` (`title` = raw code, `detail` = English prose, `tone` = level 1:1). No live wasm pipe delivers `MutationMessage[]` into an `EventFeedScene` yet (that's a different lane's `AppFrame::Invocation.messages`/backbone-worker wiring), so this is the ready adapter, not yet an active consumer inside this lease.
2. **`ChromePanels`**: new `🔖️ConflictsPanel` region — `ConflictsHostApi`, `buildConflictsTree`, `FRAMEWORK_SETTINGS_CONFLICTS_TAB_ID`; `createFrameworkSettingsPanelTab` takes an optional third `getConflictsHost` param (omitted-when-absent, same idiom as `getDefaultAppsHost`). Each conflict row: kind + first message label, Accept/Discard buttons, and — when selected — an inline `ConflictDiffPreview` (see DiffViewHost below). Also added the merge-policy `Select` row to the General settings tab (`ui.mutation.policy.setting.label`, feature-detected on `host.mergePolicy`/`host.setMergePolicy`).
3. **`ShellSync`**: `SyncAttachCardProps` gained `quarantinedConflicts: readonly Conflict[]`; the popover renders an amber `ui.conflict.quarantined` badge with a count when non-empty.
4. **`DiffViewHost`**: new `🔖️ConflictDiff` region — `conflictDiffText(conflict, current)` builds `{before, after}` from a `Conflict` (Quarantined → pretty-printed envelopes; Degraded → edit-id placeholder, there's no separate incoming payload once already applied); `ConflictDiffPreview({before, after})` reuses the host's own `diffLines`/`UnifiedDiff` internals directly — no full `DiffViewScene` component-scene node needed. `ChromePanels` wires it into the selected conflict row.
5. **`ShellHost`**: `TRANSIENT_NOTICE_TONE_CLASS`, `MUTATION_REJECTED_FAULT_CODE`, `mutationCodeLabelKey`; `showTransientNotice` widened to `Severity`; `isMutationRejectedFault`/`showMutationRejectedNotice` wired into **both** gesture-dispatch `.catch` blocks (`onAction`'s `plugin.handleAction` and `onCommand`'s `plugin.handleCommand`) — one toast per gesture, tone = `fault.severity` (mirrors `DispatchReport.worst`), body = first cause's `ui.mutation.code.*` label + its prose, falling back to `ui.mutation.rejected.body` when the fault carries no `causes` yet. `event.kind === "conflict"` (the legacy `SyncConflict` bag) now duck-types a forward `conflicts` array field into `SET_CONFLICTS`, falling back to the old warning log — activates the moment `🟦️backbone-worker.ts`/`💻️os/🟦️component.ts` (a different lane's lease) start sending a real roster through that event. `dispatchSetMergePolicy`/`dispatchResolveConflict` added, mirroring the existing `dispatchSetDefaultApp` "event-sourced local fold + feature-detected `PendingAppChannelMethods` forward" pattern (`PluginWasmHandle` doesn't expose `setMergePolicy`/`resolveConflict` yet — same documented gap as `openArtifact`/`setDefaultApp`). Built `conflictsHost`/`quarantinedConflicts` memos off new `Shell` selectors and wired them into `ChromePanels`/`ShellSync`.
6. **`HistoryTable`** (`🖱️ui/🧱️elements/📜️HistoryTable`): `HistoryColumn.mutationLevel?: "info"|"warning"|"error"|"fatal"`; `HistoryLevelBadge` renders a small uppercase tone chip next to the description when set.
7. **`Shell`**: `TransientNotice.kind` widened `"info"|"error"` → `Severity`. New `MergeState` slice (`mergePolicy`, `conflicts`, `selectedConflictId`) + actions (`SET_MERGE_POLICY`/`SET_CONFLICTS`/`SET_SELECTED_CONFLICT_ID`) + `mergeReducer`, wired into `shellReducer`/`initialShellState` (seeds `DEFAULT_MERGE_POLICY`). Selectors `selectQuarantinedConflicts`/`selectOpenConflicts` — "a peer batch is being held" is derived (`Quarantined` + `Open`), never a separately stored flag.
8. **Config triad** — `💻️os/🎚️config/🧬️schema/🧬️mutations/🛡️change-merge-policy/{🦠️mutation,🔺️diff,↩️inverse}/🦀️component.rs` (new files; the facet's own aggregator `component.rs` already existed and pointed at these). `ChangeMergePolicy{policy}`, diff = no-op Warning when already active else full replace, inverse restores the base policy. Matches the sibling `📌️set-default-app` triad's shape exactly. **Confirmed still unwired into any crate's `glue.rs`** (documented explicitly in the facet's own top-level file, same status as its `set-default-app`/`clear-default-app` siblings) — not part of any cargo build yet, so no `cargo check` is possible; verified with `rustfmt --check` instead (clean, see `🧪️w2-d-rustfmt.txt`).

## i18n keys added (schema `🖱️ui/🧱️elements/📚️I18n/🟦️component.tsx` + both bundles in `🖱️ui/📦️packages/🟦️typescript/🎯️targets/⚛️react/📦️index.tsx`)

`ui.mutation.level.{info,warning,error,fatal}`, `ui.mutation.code.{targetMissing,noOp,partial,clamped,duplicateId,invariant,cascade}`, `ui.mutation.policy.{laissezFaire,normal,vigilant}.{label,description}`, `ui.mutation.policy.setting.label`, `ui.mutation.rejected.{title,body}`, `ui.conflict.{panel,accept,discard,quarantined,degraded}` — every leaf is a `UiLabelValue` (`{label:{normal,beginner}}`), present in **both** `de` and `en` bundles, `satisfies UiTranslationSchema` discipline preserved (a missing German string is a compile error by construction).

## Verify

- **`@semio-tech/framework-renderer-react`** (`ShellHost`/`ChromePanels`/`ShellSync`/`DiffViewHost`/`EventFeedHost`/`Shell` live here): `bunx vitest run --config 🧪️vitest.config.ts` from `📦️packages/🟦️typescript/🎯️targets/⚛️react` (direct invocation — `bun nx run @semio-tech/framework-renderer-react:test` hits the same "nx path starved" 15s quick-budget kill lane 2-C hit; direct run per the brief's explicit allowance): **306 passed, 9 failed, 315 total**. All 9 failures are in files/areas this lease never touched (`adaptPluginHandle` wire encoding, form builder card styling, VFS scene rendering, a chai-matcher setup issue, `resolveWindowActions`, `commandCategories`/`buildCommandCategoryTabs` label/arg-shape drift, two mit-bestand asset-path regexes) — none mention mutation/conflict/merge-policy. Raw output: `🧪️w2-d-vitest.txt`.
- **`@semio-tech/ui-react`** (`📚️I18n`, the bundles, `📜️HistoryTable` live here) — this package ships its own `TypecheckScript` (`bun ./📜️script.ts typecheck` → `tsc --noEmit -p tsconfig.json`), which is what actually caught a real bug: **my first pass nested the new `mutation`/`conflict` bundle keys as SIBLINGS of `ui:` instead of children of it** (an extra `}` landed one line too early), so `t("ui.mutation.level.info")` would have resolved to nothing at runtime despite the JS parsing fine. Fixed in both `de`/`en` blocks (re-indented, moved inside `ui: {…}`) and reverified — `bun ./📜️script.ts typecheck`: 185 errors before and after the fix, same count, all pre-existing (`🛂️manifest`'s generated types, `@xyflow/react` `Node` mismatches, `TutorialTracks.document`, a pre-existing `surfaceContextMenu` bundle/schema key drift unrelated to mutation/conflict) — zero mention `mutation`/`conflict`/`HistoryTable` after the fix (there were exactly 2 before, both now gone). Raw output: `🧪️w2-d-ui-react-tsc.txt`. `bun ./📜️script.ts test`: **510 passed, 10 failed, 520 total** — all 10 failures are unrelated (`UnifiedGumball` math, icon hover-animation catalog, `CanvasPickMenu`, navbar spacing, `VirtualFileSystem`/tree icon rendering). Raw output: `🧪️w2-d-ui-react-vitest.txt`.
- Whole-workspace `bunx tsc --noEmit -p tsconfig.json` (root): 19 pre-existing syntax errors, all in `✏️s/🔌️plugins/🔱️trinity/…`, `✏️s/🔌️plugins/🗄️stdio/…`, and the vscode extension — zero in any file this lease touched, before and after the i18n fix. Raw output: `🧪️w2-d-tsc.txt`.
- `rustfmt --check` on the 3 new Rust files: clean. `🧪️w2-d-rustfmt.txt`.

**Lesson for future i18n bundle edits in this file**: always run the package's own `typecheck` script after editing `📦️index.tsx`'s bundles — `vitest`'s esbuild transform does not catch object-literal excess-property placement, only `tsc` does, and a misplaced key here is a silent runtime i18n miss, not a crash.

## Known gap (explicitly out of this lease)

The real wire delivery of `MergeReport`/`Conflicts` frames into `ShellHost` (so `conflicts`/`quarantinedConflicts` ever populate outside a test) lives in `💻️os/🟦️backbone-worker.ts` + `💻️os/🟦️component.ts`'s `ArtifactEvent` (still typed as the pre-C5 `SyncConflict` bag) — not in this lease's file list. Everything on this side is built against the frozen contract types and duck-types the forward field so it activates with zero changes once that lane lands. Same for the guest-side `Fault("mutation.rejected")` + `causes` population (`🔌️plugin/component.rs`, C9 Guest section) and `PluginWasmHandle.setMergePolicy/resolveConflict` (`PluginRuntime`, `adaptPluginHandle`).

## Files touched

- `🧰️framework/🔨️modules/🖱️ui/🧱️elements/📚️I18n/🟦️component.tsx`
- `🧰️framework/🔨️modules/🖱️ui/📦️packages/🟦️typescript/🎯️targets/⚛️react/📦️index.tsx`
- `🧰️framework/🔨️modules/🖱️ui/🧱️elements/📜️HistoryTable/🟦️component.tsx`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🧱️elements/EventFeedHost/🟦️component.tsx`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🧱️elements/DiffViewHost/🟦️component.tsx`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🧱️elements/ChromePanels/🟦️component.tsx`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🧱️elements/ShellSync/🟦️component.tsx`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🧱️elements/Shell/🟦️component.tsx`
- `🧰️framework/🛍️products/💻️os/🔨️modules/📺️renderer/🧑️‍🎨️engine/🧱️elements/ShellHost/🟦️component.tsx`
- `🧰️framework/🛍️products/💻️os/🎚️config/🧬️schema/🧬️mutations/🛡️change-merge-policy/🦠️mutation/🦀️component.rs` (new)
- `🧰️framework/🛍️products/💻️os/🎚️config/🧬️schema/🧬️mutations/🛡️change-merge-policy/🔺️diff/🦀️component.rs` (new)
- `🧰️framework/🛍️products/💻️os/🎚️config/🧬️schema/🧬️mutations/🛡️change-merge-policy/↩️inverse/🦀️component.rs` (new)

Logs: `🧪️w2-d-vitest.txt`, `🧪️w2-d-tsc.txt`, `🧪️w2-d-rustfmt.txt` (this folder).
