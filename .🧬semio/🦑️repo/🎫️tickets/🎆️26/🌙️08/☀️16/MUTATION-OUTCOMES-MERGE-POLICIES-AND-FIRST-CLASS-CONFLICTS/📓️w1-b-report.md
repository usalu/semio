# W1-B — History Persistence (C7)

**Landed** (`📡️spr/📜️history/🦀️component.rs` only, region `🔖️Model`/`🔖️Conflict`/`🔖️Message`/`🔖️Edit`): `REC_CONFLICT = 0x42` (confirmed free — 0x40/0x41 taken by `REC_CURSOR`/`REC_COMPOSITION`, repo-wide grep for `0x42` was empty pre-edit). `HistoryLog.conflicts: Vec<HistoryConflict>` and `HistoryOpMeta.messages: Vec<HistoryMessage>` (new presence bit6, bits 0-5 pre-existing). `encode_conflicts`/`decode_conflicts` mirror `encode_cursor`'s `format u8(=1) | payload` shape, one **whole-Vec frame** (not one-frame-per-conflict, non-critical, omitted entirely when `conflicts` is empty) containing `count varint + entry*`, entry = `id(idfield) | kind u8 | status u8 | actor_count+actor(idfield)* | hlt(3×varint u64) | edit_id_count+edit_id(idfield, ordinal-eligible)* | envelope_count+(len+bytes)* | message_count+message*`. `HistoryMessage` wire: `level u8 | code(idfield, dict-interned) | message(strfield) | target_count+target(strfield)* | op_index presence+[varint]`. `code` alone routes through the existing `write_id_field`/`DictBuilder` machinery (same as `doc_id`/`op_id`); `message`/`target` are plain strings per spec.

**Tests written** (region `🧪️Tests` → `🔖️Conflict`/`🔖️Message`): `conflict_payload_round_trips_both_kinds_byte_identically`, `encode_conflicts_round_trips_with_dict_and_ordinal_refs`, `encode_conflicts_round_trips_empty`, `a_log_without_conflicts_writes_no_conflict_record` (scans real frames via `FrameCursor`, not a byte-window guess), `op_meta_messages_round_trip_every_severity_and_target_shape` (levels 0-3, target lens 0/1/3, `op_index` None/Some), `op_meta_without_messages_writes_no_messages_section`, `message_code_interning_does_not_grow_the_dictionary_linearly` (500 identical codes → `dict.len()==1`, <10 bytes/message).

**BLOCKER — could not execute the acceptance commands to green.** `cargo check -p semio-framework-os-kernel` and `cargo test ... -- os_spr::history` both fail crate-wide (single-crate `semio_framework_os_kernel`) because 4 struct literals **outside my lease** don't list the new required fields: `🏪️store/🦀️component.rs:2385` (`HistoryOpMeta`, needs `messages: Vec::new()`), `:2523` (`HistoryLog`, needs `conflicts: Vec::new()`), `📡️spr/🧪️testkit/🦀️component.rs:216` (`HistoryLog`, needs `conflicts: Vec::new()`), `📡️spr/🦀️component.rs:225` (facade's own test, `HistoryLog`, needs `conflicts: Vec::new()`). Plus one **unrelated pre-existing** break, not caused by me: `📡️spr/🧪️testkit/🦀️component.rs:977` `AppFrame::Error` missing `report` (lane 1-C's C8 in flight). Per lease rules I did not edit any of these. Real command output, including both full error lists, is in `🧪️w1-b-cargo.txt`. My own file produced **zero** compiler errors/new warnings in both runs (only one pre-existing, unrelated `unused_mut` warning at line 1212, untouched by me). Tests above are written and structurally reviewed but **unexecuted** — I will not claim they pass without a real run; re-run once the 4 sites get their one-line field default.
