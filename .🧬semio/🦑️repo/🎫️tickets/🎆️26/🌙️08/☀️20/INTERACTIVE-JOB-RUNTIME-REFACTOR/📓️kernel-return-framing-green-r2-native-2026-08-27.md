# Kernel Return Framing and Existing Dialects R2

Actual canonical `@semio-tech/framework-rs:test-wire-retirement-native --args='--lib return_content_ -- --nocapture'` completed with **4 passed, 253 skipped**, 0.047s. Process 6626 exited 0.

The four selected tests cover the two new fixed-header/split-reader laws and two existing-dialect probes. The latter exercise actual AppFrame Invocation encoding and typed PresenceUpdate fields; this is not a full retained return encoder or guest/Wasm delivery gate.

Actual output footer:

```text
Summary [0.047s] 4 tests run: 4 passed, 253 skipped
NX Successfully ran target test-wire-retirement-native for project @semio-tech/framework-rs
```

Raw output: `🧪️member-kernel-return-framing-green-r2-native-2026-08-27.txt`. The usual unchanged master target/environment was used. Kernel/Actor source hold is released to the owner for the next schema-first message tests. No cleanup, Git mutation or generated publication occurred.
