# Plugin Codec Source Route Approval

The coordinator read the complete taxonomy path/route proposal, including its reported diagnostic source and endpoint table, and independently read the current Plugin package router/project. All four proposed leaves use existing vocabulary; no catalog edit is necessary.

Approve taxonomy's narrow source-only routing TDD: add only CodecSendSourceScript, its test-codec-send-source registration, the uncached target, and the exact405.1 launch row after a fresh collision/pure-producer check. Reject every extra argument before lazy import; preserve current CheckScript/TestScript, runner self-tests, default check and native budgeting byte-for-byte through an exact inverse proof. No native command is part of this approval.

Retained may separately mount the already reviewed schema/fixture/oracle at Plugin/📦️codec/🧵️send with exact empty-leaf names. Preserve the ticket originals. No Rust test/module or twelve production qualifications are mounted yet. The first actual desired-law execution waits for the coherent registered route/launch release, then retains its earliest real failure.

Exact target: `@semio-tech/framework-plugin:test-codec-send-source`.
Exact callable: `testPluginCodecCallerSource(this.repoRoot)` after lazy `../../📦️codec/🧵️send/📜️script.ts` import.
Launch: `⚖️gate🔌️plugin🧵️codec-send-source`, group4_gate/order405.1, command `bun ./node_modules/nx/bin/nx.js run @semio-tech/framework-plugin:test-codec-send-source --skipNxCache`.

The taxonomy diagnostic is peer-executed path/metadata evidence, not a coordinator rerun, native proof or execution of the Plugin oracle. [Complete proposal](</Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/📓️plugin-codec-send-path-proposal/📝️.md>).

