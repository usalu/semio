# UI Parent Slot Review

The coordinator read the complete current shared-ledger adoption report, actual pool and child source, metadata contract, and all four new parent-slot schema/contract/fixture files.

The declared slot is one logical record16 plus seven fields8 each: requestOwner, cell, record, entry, phase, failure and witness. Its72 bytes / one slot / one owner are consistent with the UI logical model. The proposed pool envelope is264/5/5, but the verified controller R1 still imported the192/4/4 pool; the new slot was not mounted during that run.

The accepted implementation sequence is schema/model first, then actual first-child admission and retirement. The already funded parent slot retains the exact request, cell and record before fallible construction. A live child enters an admitted intrusive roster before exposure. On close the slot captures original child authorities and private terminal witness before unlink or refund; intrinsic record close, result-alias detach, cell retirement and final parent observation remain separately granted turns. Any new parent reference and every later Instance/Payload slot must be included in its own catalogue, not implicitly paid by the pool's72 bytes.

One slot serializes transitions, not the live child count. An occupied unretired fault slot cannot be overwritten or called terminal; existing rostered children and readers remain owned. Further final handoff may block while that fault remains. The cold nine-event Immer trace and12 listed future laws are not actual concurrent-admission, bounded fault retirement, page-reader or live UI proof.

The coordinator released the imported-source hold after independent controller11/147 and authorized test-first implementation of this exact slot. The peer must consume the exported pool metadata and update its exact oracle at the coherent264 release. No default pool, second ledger, structural terminal callback, old local-counter compatibility path or increased capacity is authorized.
