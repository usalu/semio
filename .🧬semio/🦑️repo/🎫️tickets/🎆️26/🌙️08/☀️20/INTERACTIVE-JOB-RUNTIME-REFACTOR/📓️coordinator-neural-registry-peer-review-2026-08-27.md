# Neural Registry Peer Review

The demonstrator peer owns the new SharedRegistry/RegistryRetirement seam and its Flow consumer integration. The coordinator read the complete registry source and four registry tests, the relevant ValueRetirement implementation, and the actual native log summary:12passed,35skipped,.136s in the peer's `🧪️registry-retirement-green-2.log`. The8other cases cover value/cache/domain retirement. No timing certificate or fresh Flow consumer pass is inferred.

Shared readers keep the raw Arc private. Every Drop calls Arc::into_inner; the unique final reader hands the root into a preallocated OnceLock. The explicit cursor waits for all reader aliases to leave, then owns ordered registry collections, schema/default/value fields and operator-domain retirement. Child completion does not terminate the parent while queued value owners remain. Unsupported dynamic payloads are rejected and retained.

An unresolved exceptional-owner boundary was reported to the peer: RegistryRetirement and ValueRetirement suppress their terminal guards during unwinding while retained roots live behind ManuallyDrop. A cursor locally unwound from a worker needs an exact recoverable/pre-admitted handoff, or structural retention outside catch_unwind demonstrated by a regression; otherwise live roots can become unreachable. No cold Drop workaround, peer source mutation or competing registry implementation was made. This issue does not invalidate the observed normal-path tests, but prevents full fault-resilience credit.

The peer requested Flow maintenance/close consumer adoption remain reserved until its `retire_flow_extension_registries_step` entry point is ready. Both refactor runtime lanes were notified. One peer Flow compile is active in its existing target; root fleet remains on small incremental native cohorts and TypeScript/source checks. Latest read-only disk observation18GiB.

The peer subsequently accepted the exceptional-owner distinction and is keeping Flow retirement cursors in the static registry state's retained queue, outside catch_unwind. A regression where a dynamic operator panics after transferring its payload is being added; no generic cursor-local unwind guarantee is claimed. The following Flow compile stopped at the Mutation owner's vocabulary E0080, before tests. The peer reported an earlier full Neural47/47 run; the newer `🧪️neural-registry-full-2.log` actually read by the coordinator reports48passed/0skipped,.556s. This is not the blocked Flow integration gate.

The coordinator then read the added `supervising_cursor_recovers_operator_fault_after_exact_payload_handoff` body and its actual PASS line. It keeps the supervisor outside catch_unwind, catches the injected operator fault after exact text transfer, then drains with1-byte grants, checks remaining bytes equal the payload length and observes one final operator drop. This proves that supervisor-owned fixture boundary; Flow's static queue and lock recovery still need their own compiled/runtime proof.
