# Process Observer Lease: Read-Only Executor Review

Request ID8530a55d-cdd8-4645-87c7-29080e1e0b64. I read the complete ticket driver/report and both C/Rust oracle inputs. No driver mode, native compiler, FFI observer, child probe or signal was executed by this review. The sole native slot is idle after WGPU R17; execution still requires parent's explicit go.

## Actual Scope

The native mode creates one fresh mkdtemp owner beneath the packet's own 🧪️runs. Its original120000ms deadline starts in the driver before source capture/allocation; preparation and private compiler work share that deadline. It uses the existing runTestBudgeted body extracted from the real repository script, not a replacement timer. The inner observer probe has one6000ms budget reserved within the original total.

C compilation uses the installed macOS SDK and actual sizeof/alignment/six offsets. The separate Rust workspace pins test-only libc0.2.189, builds offline into an explicit fresh private target, then compares the same layout. Its private [workspace] prevents joining the main Cargo workspace; no product dependency is added. Explicit --target-dir overrides an inherited target variable. The executor should retain CARGO_BUILD_JOBS=2; no profile/stack/thread/budget override or warm-target assumption is needed.

The probe spawns three authored subjects (leader, nonleader, decoy). Each has a2s self-deadline and is normally released by stdin; the reader checks actual ChildProcess identity/birth twice and closed terminal state. The driver schedules no subject signals. Existing emergency runTestBudgeted timeout still calls the repository's PID-group killBudgetTree, which is expressly not a newly proved signal capability. A detached subject leader is outside its parent worker process group; its authored self-deadline is therefore material, not a proof that the parent watchdog can reclaim arbitrary descendants.

## Preservation And Limits

Source/marker/SDK/proof checks record no-follow ancestor and descriptor identities. Output records are fresh/exclusive beneath the new run owner; command stdout/stderr are capped to1MiB stored bytes. Cooperative cancellation is checked between phases; an in-flight compiler relies on the outer existing timeout. The120s bound is driver-entry scope, not external Nx qualification. There is no retry, full-wave follow-up, planned cleanup, or Linux/Windows native claim.

This read-only review found no scheduling conflict with the idle sole executor. It does not certify successful offline dependency resolution, actual C/Rust layout, FFI binding, PID identity, subject closure, or timeout safety. Those remain actual native results if explicitly authorized. WGPU R17 source holds were released and its semantic enqueue test remains unexecuted.

## Exact Read Inputs

Driver SHA2562ec0422bfb3a237dbd25a4bf9ad32184ccc1460c124f0271d58a0757371764bb; report105e293f4a6698e002f01b8d1a87f09e369409cc1eaeaf28f6c6c380f0f3644f; C6e8a4d0273376b204294c8b659b4a974f81a63bc7c79c160b9e778c58bafa373; Cargo dbb6f970b3b807e8b84acee62213bbf45fce965544d93c2369a3b0f6e35cfaf4; Rust47e328ef75cc974ebfb6c0c62517887364ae6a49308092ca0823dd4eb4d66de0. These match the packet's declared four native input hashes; the report itself is independently hashed. This is not a new before/after nine-input execution capture.

Driver: .🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/📓️transaction-current-readiness-2026-08-28/📓️process-observer-packet/📜️script.ts

Canonical proposed invocation is the packet's existing Nx workspace exec route with native and the exact approved lease UUID. It has not been invoked.
