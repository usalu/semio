# Canonical Return Protocol-Fault Boundary R3

Actual corrected fixed-result diagnostic exit 0:

```text
[DEBUG] return strict AJV PASS, independent LEB drive6 result13 page2 PASS, contradictions8 rejected, maximum4138
```

This adds two authority-free fixed protocol-fault results and rejects a protocol-fault carrying an unrelated capacity fault. Invalid origin/identity fields are not echoed or replaced. The runtime must reject before admitting new ownership or executing semantic work, and preserve all previous owners. Three neutral malformed-drive expectations are staged; native codec and actual admission tests remain separate gates.

The existing content oracle was extended after clarifying that WIT `pack` is opaque framing with field-owned semantics. Invocation is encoded as an actual AppFrame byte layout inside Effect::SendMessage, never parsed as a generic wire-value document. Presence includes own hover/selection, peer data, UTF-8 node/label strings and TTL, and is not a UI ACK or document mutation.

```text
[DEBUG] content strict AJV PASS; independent frame6 uiBegin1 scalar3 Invocation1 presenceUTF8_2 PASS; native and paged-source gates separate
```

## Fixed Result Evaluation

```javascript
(async()=>{const fs=await import('node:fs');const {default:Ajv}=await import('ajv');const {default:leb}=await import('@webassemblyjs/leb128/lib/leb.js');const dir='/Users/ueli/Documents/semio/🧰️framework/🔨️modules/🎭️actor/';const read=p=>JSON.parse(fs.readFileSync(dir+p,'utf8'));const ajv=new Ajv({strict:true});for(const p of ['🚪️lifetime/🧬️schema.json','📄️page/🧬️schema.json','📤️return/🧬️schema.json'])ajv.addSchema(read(p));const f=read('📤️return/🧪️fixture.json');const validate=ajv.compile(read('📤️return/🧪️schema.json'));if(!validate(f))throw Error(JSON.stringify(validate.errors));const num=n=>{const b=Buffer.alloc(8);b.writeBigUInt64LE(BigInt(n));return Array.from(leb.encodeUIntBuffer(b));};const origin=o=>[...num(o.activationGeneration),...num(o.requestSequence)];const id=i=>[...origin(i.origin),...num(i.returnSequence)];const ctl=c=>[f.controlTags[c.kind],...(c.kind==='inputAck'?[...id(c.receipt.identity),...num(c.receipt.pageSequence),...num(c.receipt.length),Number(c.receipt.final)]:id(c.identity))];for(const v of f.wireVectors){const got=Buffer.from(v.value.kind==='execute'?[0,...origin(v.value.origin)]:[1,...ctl(v.value.control)]).toString('hex');if(got!==v.hex)throw Error(JSON.stringify({got,want:v.hex}));}for(const row of f.resultVectors){const v=row.value;let b=[f.resultTags[v.kind]];if(v.kind==='protocolFault')b.push(f.resultEnums.fault.indexOf(v.fault));else if(v.kind==='refused')b.push(...origin(v.origin),f.resultEnums.fault.indexOf(v.fault));else if(v.kind==='pending')b.push(...id(v.identity),f.resultEnums.reason.indexOf(v.reason));else if(v.kind==='retired')b.push(...id(v.identity),f.resultEnums.completion.indexOf(v.completion));else b.push(...ctl(v.control),f.resultEnums.outcome.indexOf(v.outcome),f.resultEnums.fault.indexOf(v.fault));if(Buffer.from(b).toString('hex')!==row.hex)throw Error('result vector '+row.hex);}const validateResult=ajv.getSchema('semio.actor.retained-return.v1#/definitions/result');for(const v of f.resultContradictions)if(validateResult(v))throw Error('accepted contradiction');for(const r of f.pageResultVectors){const v=r.receipt;const prefix=Buffer.from([2,...id(v.identity),...num(v.pageSequence),...num(v.length),Number(v.final)]);if(prefix.toString('hex')!==r.prefixHex)throw Error('page prefix');const payload=Buffer.from(Array.from({length:4096},(_,i)=>i<r.pageLength&&r.pattern==='mod37plus11'?(i*37+11)%256:0));if(prefix.length+payload.length!==r.wireBytes)throw Error('page wire length');const page={length:r.pageLength};for(let block=0;block<64;block++){const words={};for(let word=0;word<8;word++)words['word'+word]=payload.readBigUInt64LE(block*64+word*8).toString();page['block'+String(block).padStart(2,'0')]=words;}if(!validateResult({kind:'page',receipt:v,page}))throw Error(JSON.stringify(validateResult.errors));}console.log('[DEBUG] return strict AJV PASS, independent LEB drive6 result13 page2 PASS, contradictions8 rejected, maximum4138');})()
```

Invocation: `SEMIO_RETURN_PROTOCOL_FAULT_EVAL=<source above> NX_DAEMON=false NX_CACHE_PROJECT_GRAPH=false NX_ISOLATE_PLUGINS=false bun x nx exec --projects=workspace -- bun -e 'await eval(process.env.SEMIO_RETURN_PROTOCOL_FAULT_EVAL)'`.

## Field Dialect Evaluation

```javascript
(async()=>{const fs=await import('node:fs');const {default:Ajv}=await import('ajv');const {default:leb}=await import('@webassemblyjs/leb128/lib/leb.js');const root='/Users/ueli/Documents/semio/';const a=root+'🧰️framework/🔨️modules/🎭️actor/';const k=root+'🧰️framework/🔨️modules/🎠️kernel/📤️return/📦️content/';const read=p=>JSON.parse(fs.readFileSync(p,'utf8'));const ajv=new Ajv({strict:true});for(const p of ['🚪️lifetime/🧬️schema.json','📄️page/🧬️schema.json','🚪️lifetime/🩹️patch/🧬️schema.json'])ajv.addSchema(read(a+p));const wire=read(k+'🧬️wire.json'),f=read(k+'🧪️fixture.json');for(const [s,v] of [['🧬️schema.json',wire],['🧪️schema.json',f]]){const validate=ajv.compile(read(k+s));if(!validate(v))throw Error(JSON.stringify(validate.errors));}const num=n=>{const b=Buffer.alloc(8);b.writeBigUInt64LE(BigInt(n));return Buffer.from(leb.encodeUIntBuffer(b));};for(const r of f.recordVectors){const b=Buffer.from(r.bodyHex,'hex');const got=Buffer.concat([Buffer.from([r.tag]),num(b.length),b]).toString('hex');if(got!==r.frameHex)throw Error(JSON.stringify({got,want:r.frameHex}));}const ui=f.uiBegin,l=ui.receipt.lifetime,surface=Buffer.from(ui.surface,'utf8');const begin=Buffer.concat([num(l.activationGeneration),num(l.instanceId),num(l.guestLifetime),num(ui.receipt.patchSequence),num(surface.length),surface,num(ui.baseRevision),num(ui.revision),num(ui.operationCount)]).toString('hex');if(begin!==f.recordVectors[1].bodyHex)throw Error('uiBegin mismatch '+begin);for(const r of f.scalarOperationVectors){const fields=[Buffer.from([r.opcode]),num(r.node)];if(r.children){fields.push(num(r.children.length));for(const c of r.children)fields.push(num(c));}if(Buffer.concat(fields).toString('hex')!==r.hex)throw Error('scalar mismatch '+r.name);}if(wire.records.some((r,i)=>r.tag!==i)||wire.uiOperations.some((r,i)=>r.opcode!==i))throw Error('noncanonical declaration ordinal');const inv=f.invocation;const pieces=[Buffer.from([1]),num(inv.inReplyTo)];for(const name of ['output','diagnostics','uiScope','historyPatch','messages']){pieces.push(num(inv[name].length),Buffer.from(inv[name]));}const frame=Buffer.concat(pieces);if(frame.toString('hex')!==inv.appFrameHex)throw Error('Invocation frame mismatch');const effect=Buffer.concat([num(0),num(0),num(inv.shellInstance),num(frame.length),frame]);if(effect.toString('hex')!==inv.effectBodyHex)throw Error('Invocation effect mismatch');if(Buffer.concat([Buffer.from([5]),num(effect.length),effect]).toString('hex')!==inv.effectRecordHex)throw Error('Invocation record mismatch');for(const [text,hex] of [[f.presence.value.nodeKey,f.presence.nodeKeyUtf8Hex],[f.presence.value.peers[0].label,f.presence.labelUtf8Hex]])if(Buffer.from(text,'utf8').toString('hex')!==hex)throw Error('Presence UTF8 mismatch');console.log('[DEBUG] content strict AJV PASS; independent frame6 uiBegin1 scalar3 Invocation1 presenceUTF8_2 PASS; native and paged-source gates separate');})()
```

Invocation: `SEMIO_RETURN_DIALECT_EVAL=<source above> NX_DAEMON=false NX_CACHE_PROJECT_GRAPH=false NX_ISOLATE_PLUGINS=false bun x nx exec --projects=workspace -- bun -e 'await eval(process.env.SEMIO_RETURN_DIALECT_EVAL)'`.

Targeted `git diff --check` on the changed tracked Actor, Kernel, reactor, host and WIT files also exited 0. New untracked schemas/modules are not covered by that tracked-diff check. No logs or other evidence were deleted.
