# Local Interaction Contract Source Checkpoint

## Current Boundary

### Failure Ownership And Close Prerequisite, 27 August 13:33

Synchronous page-consumer throws and malformed frame decoding now use the same exact cancellation/Closed ownership path as asynchronous consumer rejection. The real RED logs record the uncaught synchronous callback and invalid tag 255 before the correction; full canonical OS then passed 216 tests with thirteen lifecycle sequences (`🧪️local-interaction-consumer-frame-green-r1-2026-08-27.txt`). The coordinator independently reran those 216 tests and full React 521 tests. The prior quick React watchdog timeout is retained as a timing result, not evidence of a localized receipt hang.

Plugin native r8 passed 19 tests, 429 filtered, 74 seconds compilation and 0.33 seconds tests (`🧪️member-local-interaction-r8-native-2026-08-27.txt`). This includes actual registered query/continuation ownership and the explicitly cold typed transaction receipt test plus encoded transaction rejection. It does not turn the unimplemented transaction ingress into an interactive pass.

Fatal outcome iterator error/end and whole-turn transport failure still require a true generation-bound instance close witness. Existing destroyApp/ShardClient.dispose cannot supply one. The active prerequisite is documented in `📓️instance-lifetime-close-contract-2026-08-27.md`: actor wire/client lease tests pass 58 in the full actor suite, and two native exact-app-close witness tests are queued. Full native reactor/UI/host completion, the single-observer AppChannel failure join, all sixteen ordinary ingress routes, atomic restore, six old interaction tails, and tutorial/native Shell consumers remain unfinished. Older chronological sections below are superseded by this evidence.

### Exact Receipt Authority And Actual Ingress Finding, 27 August 13:07

The native registered query now passes: 18 plugin tests, 429 filtered, 22.17 seconds compilation and 0.53 seconds tests (`🧪️member-local-interaction-r6-native-2026-08-27.txt`). The new test drives actual encoded query ingress, accepts the real multiplexed initial Done/Ephemeral/Started outcome without dropping frames, advances the existing continuation scheduler, ACKs more than 4096 semantic bytes through real commands, and closes the actual app/quarantine owner. This is native query evidence, not browser or restore evidence.

The previous empty ordinary completion assumption was incorrect. Native setMergePolicy emits Done(seq); ApplyEnvelopes emits Done(seq) plus unsolicited MergeReport/Conflicts. Their renderer mocks were stale. Empty/FIFO completion is now removed entirely: every exact pending numeric receipt in an outcome resolves once, response frames for other sequences are excluded, and uncorrelated notifications alone never complete a waiter. Transaction payloads additionally match the pending command's exact txn_id and response kind. Strict language-neutral fixtures reproduced the empty/mixed-outcome error, then full OS passed 216 tests with ten lifecycle sequences, six checked allocator cases, reopen/old receipt rejection, and an independent high-sequence/u64 LEB128 oracle (`🧪️local-interaction-mixed-receipts-green-r1-2026-08-27.txt`). The required allocator is explicit in both React and native bridge owners; cancellation identity is reserved atomically before admitting a query.

The native transaction receipt test exposed a different, real open gate: PagedAppCommandDecodeCursor only admits tags 0–4, 6–9, 15, 16, 27, and 29. TransactionPrepare/Commit/Rollback tags 17–19 and setMergePolicy tag 25 are rejected before dispatch with `plugin.command-route-state-machine-required`. Exact evidence is `🧪️member-transaction-receipts-r2-native-2026-08-27.txt`. Earlier missing-Done assertions in this test were therefore ingress rejection evidence, not a clean success-branch RED. No decoder or admission check was weakened.

Three cold transaction success branches now emit companion Done(seq), preserving their exact transaction payload and Error semantics. The fixture is explicitly renamed `local_interaction_cold_transaction_receipts_and_encoded_route_rejection`: it preserves the real encoded rejection assertion, then uses the existing typed decoded owner only inside a cold unit test to exercise receipt branches. That source is unrun after the rename; there is no registered interactive transaction success claim. Real bounded paged ingress for 17–19 and 25 is an assigned next packet, not hidden behind this fixture. React transaction mocks now model the correct typed branch receipts rather than hardcoded Done(0).

Remaining client failure ownership includes synchronous page-consumer throws and decoder/iterator failure cancellation. These are not covered by the current success/abort/receipt laws. Atomic full/sparse restore, retained topology index, six old interaction tails, tutorial consumers, and native Shell parity remain unfinished.

### Receipt And Native Checkpoint, 27 August 12:48

The first live query ownership join passed 17 native plugin tests, 429 filtered, 49.06 seconds compilation and 0.09 seconds tests (`🧪️member-local-interaction-r4-native-2026-08-27.txt`). Replication semantic and codec coverage passed eight native tests, 211 filtered, 1.91 seconds compilation (`🧪️member-local-interaction-wire-r3-native-2026-08-27.txt`). These include saturated reply admission and exact wrapper emitted/retired accounting. They do not cover the later native receipt correction.

The renderer's legitimate empty ordinary command outcome exposed a demultiplexing regression. Each query command now emits a separate `Done(seq)` receipt on every branch; dispatch reserves two frame slots before admission for receipt plus optional immediate rejection. The client matches explicit response sequence values, ignores wrong/duplicate receipts, and still resolves truly empty ordinary outcomes. Query-only unsolicited frames do not consume ordinary waiters. The narrow native receipt patch was coordinated during the CAD dependency build, but is native-unverified.

A disposal-before-Read-receipt fixture reproduced a second ownership bug: disposing rejected the internal receipt waiter and erased the query before Started supplied its cancellation token. The test observed RED (`🧪️local-interaction-receipt-disposal-red-2026-08-27.txt`). Disposal now rejects ordinary waiters while retaining query receipt ownership and the subscription until cancellation/closure. Canonical full OS r5 then passed all 213 tests in 1.34 seconds, including eight language-neutral lifecycle sequences, strict schema validation, and independent LEB128 receipt-byte oracle (`🧪️local-interaction-client-full-r5-2026-08-27.txt`). Full React confirmation remains with the renderer owner; OS success is not renderer success.

The coordinator approved the next required handle-lifetime AppChannelRequestSequence owner. Request and ordinary sequence counters currently reset per client, leaving a real reused-app-ID delayed Started/Done gap. The new explicit owner will span all recreated clients in each adapter/native bridge, with checked lossless u64 request IDs and wire-safe numeric ordinary sequences, no global registry or per-client fallback. Runtime query-generation uniqueness already rejects old native ACK/cancel authority, but cannot alone authenticate the client's first Started reply. Reopen/delayed receipt/page and exhaustion fixtures are required before claiming this fixed.

An actual registered query/continuation native test is authored but unmounted while CAD holds dependencies. Atomic full/sparse restore, a retained topology index, all six old interaction semantic tails, tutorial consumers, and native Shell parity remain unfinished. Later paragraphs preserve earlier chronological checkpoints and are superseded by the exact evidence above.

### Live Transport Checkpoint, 27 August

The first live query channel join is now mounted, but its native suite is still running. It adds AppCommand tag 29, AppFrame tag 23, and channel version 13 with matching TypeScript and shared version fixture. Commands carry only a fixed request or full authority token (maximum 142 bytes); replies contain at most 4096 protocol bytes, while the concrete producer emits 256-byte pages. The app's single query slot captures exact document, config, and interaction Store leases under its exclusive owner. It returns all three through their actual Store maintenance witnesses before a terminal reply.

The query-generation allocator belongs to PluginRuntime, not the app, so reopening the same app id cannot reset ACK/cancel authority. It checks exhaustion before accepting a read. Started/page/ACK/cancel tokens include that generation and all captured identity fields. The UI-cache authority now increments before its live insert/replacement, and app close invalidates authority before cache retirement. No app topology callback runs in capture initialization. The real retained topology index and restore publication are still absent.

The existing typed-operation continuation scheduler drives the new query, with a bounded round-robin slot alongside operation publication. Waiting for an ACK does not schedule duplicate pages. A live reply is marked sent only inside successful admitted output delivery: one frame slot and a 512-byte concrete producer buffer are reserved first, encoding uses an exact-capacity writer, and failed admission leaves Started/page/Closed pending. The fixed protocol writer rejects insufficient capacity before touching any bytes. Wrapper progress distinguishes emitted bytes from retired bytes and released owners; a real partial-encoder-error fixture checks those counts instead of turning emitted bytes into retirement credit.

The client now demultiplexes query frames before ordinary FIFO waiters. `readLocalInteractionPages(consume, signal)` waits for each page consumer before ACK, defers cancellation until the exact Started token arrives, and waits for Closed before completing. Disposal during pre-Started capture similarly keeps the subscription until cancellation is acknowledged. This is a byte-page API, not yet a typed whole-capture parser or the tutorial restore API. It has no polling timer or Presence fallback.

Executed evidence so far:

- Plugin query/capture/retirement r2: 9 passed after the shared structured partial-write error API repair; log `🧪️member-local-interaction-r2-native-2026-08-27.txt`.
- Replication semantic/inner transport: 7 passed, 0 failed, 211 filtered; 3.49-second compilation. Initial r1 failed on one temporary-borrow test expression; exact correction is retained in r2. Log `🧪️member-local-interaction-wire-r2-native-2026-08-27.txt`.
- Plugin live r3: 13 passed, 3 failed. The three new fixture setups omitted InteractionState's required hover map; the transport fixture deliberately excludes hover. Tests now add an explicit empty hover map rather than loosening decoding. The next mounted selector has 17 tests, including the new live partial-error accounting law; it is not yet claimed passing.
- Client targeted RED: one missing `readLocalInteractionPages` failure and one passing wire test. GREEN: two tests pass, including all five language-neutral lifecycle sequences and strict schema/independent LEB128 byte oracle. Logs `🧪️local-interaction-client-red-r2-2026-08-27.txt` and `🧪️local-interaction-client-green-r1-2026-08-27.txt`.
- The ordinary OS test target initially recursively inferred its own package script. Only this executor's validated process group was terminated. The approved narrow fix is package `nx.includedScripts: []`, preserving both the package script and explicit task target, plus awaiting the existing async test runner. Canonical full OS regression is now running; no unrelated task runner or peer process was altered.

Remaining: native confirmation of this outer join, an actual registered end-to-end query/continuation test, retained topology ownership/index, exact atomic full/sparse restore, replacement of the six old interaction semantic tails, typed renderer capture/restore consumers, and native Shell parity. The following sections preserve earlier staged source checkpoints; their unrun/one-byte-workaround notes are historical, not the current claim. The generic Store reader now returns `ArtifactCanonicalJsonEncodeError { written_bytes, reason }`; the query uses real bounded multibyte chunks and retains the physical error prefix. The shared repair has 18 native tests passing under the other executor.

### Native Ownership Gate

The coordinated plugin executor ran `local_interaction_`: **9 passed, 0 failed, 429 filtered**, 10.46 seconds compilation and 0.06 seconds tests. The log contains the expected caught panic from the strict live-owner Drop law, not a suite failure. Evidence: `🧪️member-local-interaction-r1-native-2026-08-27.txt`. This verifies the mounted capture2, retirement4, and query3 ownership laws described below, including the actual hostile partial encoder error. The older unrun notes describe the staged checkpoints, not the current native gate. Live channel registration, restore publication, topology-root preparation, and tutorial integration remain unfinished.

The strict language-neutral contract and Rust/TypeScript semantic transport types now live in `🧰️framework/🔨️modules/📡️replication/📡️wire/🏠️local-interaction/`. This is the schema-first prerequisite for a bounded live producer, not a claim that the query or restore route is registered or working.

The state contains exactly selection, active mode, and active granularity maps. Hover is excluded and no peer-broadcast filter is involved. Full restore replaces every map; sparse restore requires all three nullable fields per mentioned domain. Null removes the exact entry while an unmentioned domain stays unchanged. Anchor identity, comma-containing ids, empty selections, private domains, and a restored Multiple mode are preserved.

Authority consists of the actual existing native `u32` app instance id, native `u64` generation represented as canonical decimal text in JSON, and complete 32-byte interaction/document/topology revisions represented as lowercase 64-character hexadecimal strings. The source inspection corrected an initial speculative string app-instance type to the real `ActionMeta.instance_id` type. Decimal generation rejects values beyond `u64::MAX`, including values JavaScript numbers cannot represent exactly.

The page schema names a channel-compatible decimal-u64 request id, ordinal, exact identity, terminal flag, and at most 4096 payload bytes. Runtime page admission, sequence ownership, cancellation, and typed output transport remain to be implemented. Cold serde decoding is explicitly not retained ingress.

## Executed Source Evidence

Canonical target: `@semio-tech/framework-replication-rs:test-local-interaction-source`.

- Twelve language-neutral cases compare the owned TypeScript cold semantic implementation with the existing third-party Immer immutable-update oracle. The twelfth preserves an anchor that belongs to the domain but is no longer in the selected-id vector.
- Nine hostile schema mutations are rejected, including unknown fields, omitted nullable fields, duplicate ids, numeric/overflowed generations, and truncated revisions.
- The large fixture contains an actual 6,650-byte Unicode domain key and matching semantic ids; it is not padded ingress.
- Five independent stale cases cover app instance, interaction generation, interaction revision, document revision, and topology revision. All reject without changing the prior state.
- Initial route attempts failed on relative imports/schema strict-required construction and are retained as failed setup evidence, not semantic RED evidence.
- The generation-overflow test was then added, observed RED, and corrected by exact decimal-u64 schema admission.
- Final canonical source run r4 exited 0 with `cases=11 hostileRejections=9 oracle=immer semanticKeyBytes=6650 nativeRuntimeClaims=0` in `🧪️local-interaction-source-green-r4-2026-08-27.txt`.

Three native cold tests are authored and mounted: shared fixture restore parity, lossless/full identity serde, and required nullable/unknown-field rejection. They have not run. Their dedicated target is `test-local-interaction-native`; no Rust compiler was launched by this executor. The shared replication mount and `DomainSelection` unknown-field rejection were coordinated before the other executor's registered-plugin build.

## Explicit Remaining Live Work

The existing `InteractionState` still uses ordinary BTreeMap ownership. The plugin's six reserved interaction routes still clone full state, resolve whole topology, normalize selection, and publish through whole-state mutation tails after the generic permit. Neither a retained raw-byte scan nor these cold composition helpers make those routes bounded.

The next implementation must share immutable roots, use the approved ordered-map retained lookup/update/retirement primitives for arbitrary long keys, capture or incrementally build a topology index, and validate both interaction and document/topology authority again at atomic Interaction-lane publication. Existing `validate_state` clamps/prunes and cannot implement exact tutorial restoration.

The live read producer must own an exact Store snapshot-read lease and retire/return it through the Store maintenance witness; it must not clone the entire state or call a whole app-topology callback in initialization. The native Shell and React tutorial consumers need the same real paged query and atomic full/sparse restore APIs, current-authority retry/ACK sequencing, and complete schema migration. Tutorial fields and consumers are not yet renamed in this checkpoint, avoiding a partially wired compatibility layer.

## Isolated Capture And Retirement Source Packet

New modules under `plugin/🕹️interaction/` are mounted after coordination with the registered-plugin executor. They remain native-unverified:

- `📖️capture` retains a real `SnapshotRead<InteractionState>` under the approved Store canonical reader. Borrowed native map/vector iterators serialize only the three local maps plus fixed-width identity. There is no whole-state clone, whole JSON serialization, ordinal map lookup, topology callback, or duplicated raw-pointer traversal. Generation/hex formatting has a fixed 212-byte maximum and never touches domain payloads. Caller-owned output buffers receive at most the actual granted bytes and the reader's 256-byte chunk ceiling.
- Capture close retires all Store-owned borrowed frames before handing the exact read back to its registry. A retained return witness prevents cursor terminal completion until registry maintenance reclaims that read. The Store's own retirement owner remains responsible for any transferred snapshot cleanup.
- `♻️retirement` introduces domain-aware state, snapshot-Arc, and whole-state mutation retirement factories. One fixed frontier moves a map entry, one id, or one allocation per turn, drains initialized UTF-8 bytes separately, and releases empty reserved String/Vec storage with one metadata item. Last Arc ownership uses `Arc::into_inner`; shared aliases release without recursively dropping payloads. The outer owner has a strict terminal-empty Drop guard.
- The exact Store owner catalog now replaces `bounded_config_store_owners<InteractionState, InteractionConfigMutation>` in the app constructor. The old catalog dropped the complete value after a page-sized grant. No live query route or six-route semantic rewrite is claimed by this catalog change.

Four actual retirement tests and two actual Store-backed capture tests are authored and mounted but unrun. They cover 1/64/4096-byte grants, exact byte totals, shared/final Arc ownership, empty reserved allocations, strict live-drop rejection, full canonical output with private hover excluded, cancellation before/mid/after encoding, worker transfer, read-return maintenance, and Store terminal cleanup. No runtime success is inferred from this source. The existing plugin test router accepts the exact `local_interaction_` filter; its launch seed entry is `⚖️gate🕹️local-interaction🔌️native`.

The added retirement language-neutral fixture independently totals 73 and 33,265 semantic bytes. Its strict schema rejects two hostile variants; the existing third-party Lodash aggregation oracle agrees with the separate field walk. Canonical source r2 passed with the existing eleven semantic and nine hostile contract cases as well. Log: `🧪️local-interaction-retirement-source-r2-2026-08-27.txt`.

Canonical source r5 reran the expanded twelve semantic cases, nine hostile contract cases, two retirement vectors, and two hostile retirement cases successfully. Log: `🧪️local-interaction-source-r5-2026-08-27.txt`. This remains source-only evidence; no native query, publication, or tutorial runtime claim is made.

## ACK-Owned Query Page Source

`plugin/🕹️interaction/📃️query` now contains the next mounted source unit, coordinated before the plugin executor's next native suite. It retains one fixed 256-byte page under the same frozen capture, refuses to advance while that page awaits ACK, and compares request/ordinal plus all five identity fields. A successful ACK hides the page immediately, then bytewise retirement precedes reuse. Cancellation hides a pending page immediately and drains it before closing the capture/read-return witness. There is no payload collection or whole-document serialization in this owner.

The query obtains its identity from the exact capture rather than accepting an unrelated second identity. The coherent mount added a fixed identity field/accessor to capture. The source writer requests at most 256 one-byte encoder outputs per turn so even an encoder failure cannot conceal a partially written page extent; it records each successful byte before proceeding. This is a fixed bounded local loop, not a run-to-completion loop over the document.

Three further native tests are authored and mounted but unrun: actual Store output and seven-field wrong-ACK/replay laws across two semantic roots and grants 1/64/4096; zero-grant/cancel/worker-transfer/read-return closure at prefixes 0/17/4097; and a real canonical encoder whose second source value throws after the first Unicode value and second key have been written. Query retirement must equal actual encoded bytes, including that failed partial page. The strict language-neutral query fixture passes its source oracle for two roots and three partitions, plus three hostile schema variants and the exact partial-error prefix. Existing Node crypto hashes independently agree for full versus paged canonical bytes. Source r1 failed on a misspelled fixture case reference (setup failure); corrected r2 passed and r3 passed after adding the hostile partial-error law. Logs: `🧪️local-interaction-query-source-r1-2026-08-27.txt`, `🧪️local-interaction-query-source-r2-2026-08-27.txt`, `🧪️local-interaction-query-source-r3-2026-08-27.txt`.

The shared generic canonical reader still has a separate error-count limitation: its multi-byte encoder can write a prefix before returning an error without returning that prefix length. The query's one-byte writes avoid that ambiguity locally; this is not a claim that the generic reader issue is fixed. No shared Store source was edited during the compiler hold.

Launch seed and generated launch now include the plugin native selector `local_interaction_`. Registry generation succeeded in `🧪️local-interaction-launch-r2-2026-08-27.txt`.

## Topology Input Authority Source

The agreed topology authority is a fixed-input token, not a hash of constructed topology content: SHA-256 over UTF-8 `semio.local-interaction.topology-authority.v1` plus NUL, the exact 32-byte document revision, exact 32-byte config revision, and checked UI-cache generation as eight little-endian bytes. App topology receives only document/config views; query capture must retain those exact immutable roots as well as the interaction read. A restore still needs a real retained topology index and final current-token validation.

The isolated `🔐️authority` module implements nonwrapping pre-mutation generation admission and explicit close invalidation. It is not mounted yet and does not yet guard the existing cache writes. Its five schema-first vectors distinguish document-only, config-only, and UI-cache-only changes plus maximum generation; Node crypto source oracle passed, including three hostile schema rejections. Three native tests are authored but unmounted/unrun, including overflow-before-cache-mutation and closed-authority rejection. Log: `🧪️local-interaction-authority-source-r1-2026-08-27.txt`.

The live transport will use distinct read/ACK/cancel commands and page/closed/rejected frames, advanced by the existing continuation scheduler. Waiting for ACK must not spin the scheduler or emit duplicate pages. The current AppChannelClient's FIFO outcome dispatcher skips unsolicited frames and therefore needs explicit query demultiplexing before shifting ordinary command waiters; silently feeding query frames into that FIFO is not acceptable. This join is not implemented yet.
