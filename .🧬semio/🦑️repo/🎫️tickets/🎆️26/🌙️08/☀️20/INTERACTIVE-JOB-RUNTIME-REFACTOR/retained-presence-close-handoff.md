# Retained Presence Close Handoff

## Current Verified Checkpoint

CAD's four concrete Presence close laws now pass natively:4 passed,0 failed,304 skipped,0.049 seconds test time (`🧪️member-cad-presence-close-r3-native-2026-08-27.txt`). This is the actual CAD binary after the harness migration, not only a framework fixture. Nextest retained exact binary metadata in `🧪️native-artifacts/semio-nextest-OWZGpo`. The four laws cover exact empty Draft/Transient owners, CAD variable-length local roots, worker-unwind owner preservation and nonempty peer rosters. The real CAD factory/constructor/close law then failed natively:0 passed,1 SIGABRT,307 skipped,1.317 seconds test time (`🧪️member-cad-constructor-r1-native-2026-08-27.txt`). No constructor or full-CAD-suite success is implied.

The constructor's primary failure is the real `setContributions` command declaration: its app-level CommandDefinition was explicitly BatchOnly, followed by an inapplicable window-only `.action_interactive_job(...Migrated)` setter. Exact owner/controller/schema/factory joins succeeded, but the generated migrated declaration was absent. The source correction classifies the actual command as Migrated, removes the no-op action setter and adds fixture-derived command preflight plus command-scope route assertions. The existing language-neutral route fixture already requires this exact Config route to be migrated. The proof validator is unchanged.

The secondary constructor unwind abort is a separate open ownership obligation: proof admission currently occurs after strict owned Store allocation, so a rejected constructor reaches nonterminal InteractionStore Drop. A valid CAD declaration does not prove rejected constructors close correctly. Admission must precede those allocations or retain an exact abort-close owner; no Drop guard is weakened or masked by this packet.

CAD constructor r2 now passes:1 passed,0 failed,307 skipped,0.039 seconds Nextest/0.02 seconds libtest (`🧪️member-cad-constructor-r2-native-2026-08-27.txt`). The actual console records `[DEBUG] CAD activation joined 25 exact app factory rows including setActiveUtility and completed bounded close`. Metadata is retained in `🧪️native-artifacts/semio-nextest-3RHHAJ`. This confirms the corrected real command declaration, exact factory activation and terminal close under the production grant. It does not certify the other307 skipped CAD tests, arbitrary child publication, rejected constructors, fresh Wasm/browser or latency limits.

The complete Presence group passed twelve native tests, zero failures,884 filtered,16.90 seconds compilation and0.01 seconds test time (`🧪️member-presence-peer-commit-green-r2-native-2026-08-27.txt`). It includes opaque read returns, captured-worker cancellation while the Store stays open, overlapping local/peer roots, exact rejected owners, distinct installed close factories, and exact base-root/factory publication freshness. The canonical-reader/sealer group separately passed eighteen tests after its partial-error byte-accounting repair. The latest-wins group passed ten tests and refreshed interaction callers passed nine before DAG's subsequent live transport additions. CAD close/constructor execution remains pending; none of these scoped gates proves arbitrary child-group atomicity, all-app coverage, fresh Wasm or latency limits.

The canonical source selftest passed929 checks with33 exact proof owners/255 custom rows/25 generic rows (`🧪️member-peer-capture-binding-green-r1-selftest-2026-08-27.txt`). Full census r5 emitted its JSON and remains RED:773 rows,351 source-bounded,315 batch-only,2 forbidden,269 remaining commands,929 selftests and11 top-level failures (`📊️member-mounted-source-full-census-r5-2026-08-27.json`, paired `🧪️member-mounted-source-full-census-r5-2026-08-27.txt`). The stale dispatcher/helper and capture-location failures were repaired, with stronger private base/factory authority obligations; command classifications did not change. Source classification is not runtime completion. The detailed entries below preserve the investigation sequence; initial proposals are superseded by the verified ownership protocol and later native checkpoints.

The replication local-interaction gate passed seven tests, zero failures,211 filtered,3.49 seconds compilation and0.00 seconds test time (`🧪️member-local-interaction-wire-r2-native-2026-08-27.txt`). The expanded plugin interaction gate compiled and ran sixteen tests: thirteen passed and three new live tests failed before exercising their live behavior because their actual InteractionState fixture omitted its required `hover` field (`🧪️member-local-interaction-r3-native-2026-08-27.txt`). DAG owns the exact fixture repair and coherent wrapper-progress/output-admission/channel join. CAD execution follows that coherent dependency checkpoint; no production schema default or validation bypass is authorized by this fixture failure.

The next coherent joined plugin checkpoint passed seventeen interaction tests, zero failures,429 filtered,49.06 seconds compilation and0.09 seconds test time (`🧪️member-local-interaction-r4-native-2026-08-27.txt`). DAG repaired only the exact missing fixture field, added the real partial-error live-wrapper law, and joined the outer channel/runtime source with bounded output admission. These seventeen tests exercise captured Store/query/root and live-owner laws; they do not by themselves prove a real app/runtime frame roundtrip, fresh Wasm or renderer consumption.

The matching replication gate passed eight tests, zero failures,211 filtered,1.91 seconds compilation and0.00 seconds test time (`🧪️member-local-interaction-wire-r3-native-2026-08-27.txt`), including atomic exact-output admission. CAD's four-law run then started against that snapshot (`🧪️member-cad-presence-close-r1-native-2026-08-27.txt`); it is still compiling, with no CAD test result yet. A subsequently identified ordinary-command Done-receipt fix remains held for the next plugin source window and is not included in these passes.

CAD r1 subsequently reached the actual CAD test crate and failed to compile with82 errors/106 warnings; no CAD tests ran and no build timeout occurred. The condensed task log is `🧪️member-cad-presence-close-r1-native-2026-08-27.txt`; exact recovered compiler renderings and normalized per-location records are `🧪️member-cad-presence-close-r1-diagnostics-2026-08-27.txt` and `🧪️member-cad-presence-close-r1-diagnostic-index-2026-08-27.txt`. Errors are in older harnesses:41 editor-root,12 mutation tests,11 artifact panel,10 demo tests,2 CAD root,2 mutation binary, and one each snapshot text/binary/catalogue/inspection. No error targets the new concrete Presence retirement module. The coordinator assigned migration of all these harnesses without ignored tests, removed assertions or weakened production guards.

The receipt patch was subsequently permitted as one narrow existing-channel branch during CAD codegen, with two response slots admitted before receipt plus reply. A new registered query test then compiled in the18-test plugin run:17 passed,1 failed,38.67 seconds compilation and0.22 seconds test time (`🧪️member-local-interaction-r5-native-2026-08-27.txt`). The new test expected only Done but the real exchange returned Done, an empty Ephemeral update and the query's Started token together. DAG owns correction of that fixture's multiplexed frame consumption; the runtime must not discard these valid frames to satisfy the assertion.

Read-only CAD preflight confirms the actual Nx graph resolves `@semio-tech/cad-plugin:test` to `nx:run-commands`, `bun ./📜️script.ts test`, with argument forwarding (`📊️member-cad-nx-route-preflight-2026-08-27.json`). The router awaits `runCargoTestBudgeted` and forwards the remaining arguments. The next exact commands are `bun x nx run @semio-tech/cad-plugin:test --skip-nx-cache --args='-- retained_cad_presence_close_'` (four laws), followed by `--args='-- retained_factory_proofs_activate_'` (one real constructor law). Both use the ticket-owned warm target/artifacts and3600000ms build budget. Source callsites install separate exact local/peer Presence factories, call the two-argument authority-derived Store close, and install explicit NoDraft/NoTransient owners. This is preflight only, not a native CAD result.

## CAD Harness Migration Checkpoint

The registered query rerun passed18 tests, zero failures,429 filtered,22.17 seconds compilation and0.53 seconds test time (`🧪️member-local-interaction-r6-native-2026-08-27.txt`). The corrected fixture consumes every multiplexed Done/Ephemeral/Started/page frame and drives actual registered binary dispatch, more than4096 semantic bytes, exact page ACKs and registered close. Later transaction receipt work remains a separate source/native boundary.

All82 initial CAD diagnostics have source repairs staged for the next canonical compile. Test helpers and their callers now await actual asynchronous construction, dispatch, render, Store loading/backbone and mutation laws. Manifest checks use actual window-scoped actions, retain per-window uniqueness and nonempty coverage, and preserve each classification assertion. UI tests explicitly unwrap fallible assembly, then inspect TreeItem props. Demo roundtrip implementations match the actual async trait; the existing ignored DSL fixture was not newly ignored or removed. Viewer tests now call the real framework law helper with a test-only explicit Noop default; production command defaults are unchanged. No production guard or API was weakened.

Changed CAD harness paths, relative to the CAD plugin root: `🦀️component.rs`; subset `✏️editor/🦀️component.rs`; editor panels `📄️artifact`, `🛍️catalogue`, `🔍️inspection`; schema `🧬️mutations/🦀️component.rs`, `🧬️mutations/💾️binary/🦀️component.rs`, `📸️snapshot/📝️text/🦀️component.rs`, `📸️snapshot/💾️binary/🦀️component.rs`; and demo `📚️examples/🎬️demo/🧪️tests/🦀️test.rs`. Scoped diffcheck passed. These are source repairs, not a CAD test pass; the four concrete close laws and actual constructor remain next.

## Transaction Receipt Boundary Before CAD R2

The coordinator's isolated UI parity gate passed1 native test,0 failed,90 skipped,0.034 seconds Nextest time (`🧪️member-ui-component-default-parity-r2-native-2026-08-27.txt`), covering the peer-authored component/default fixture via serde. The first attempt failed in the shell before Cargo because forwarded unquoted `-E test(...)` contained parentheses; the retry used the existing helper-safe `--lib -- typed_wire_neutral_component_defaults_match_serde --nocapture` filter. Exact metadata is retained in `🧪️native-artifacts/semio-nextest-pr2bDB`. CAD constructor r2 then started with its corrected actual command declaration and console capture disabled.

DAG added a nineteenth interaction test for three companion transaction receipts. Its first run failed with zero Done frames; the three successful cold transaction branches then gained exact Done(seq), but the19-test rerun still passed18 and failed1 (`🧪️member-local-interaction-r7-native-2026-08-27.txt`,12.43 seconds compilation/0.16 seconds tests). A targeted diagnostic rerun exposed the actual encoded Error(seq1): `plugin.command-route-state-machine-required`, because transaction command kinds17–19 lack an admitted route-specific retained decoder (`🧪️member-transaction-receipts-r2-native-2026-08-27.txt`,10.97 seconds compilation/0.07 seconds tests). This is correct fail-closed behavior, not a missing receipt on a successful encoded transaction. The decoder was not broadened. DAG owns a separately scoped cold typed-owner receipt law plus encoded-denial law; interactive transaction completion remains open.

CAD r2 started after the coordinator approved proceeding on the stable production snapshot. It stopped at compilation with eight remaining harness errors and81 warnings, without running tests (`🧪️member-cad-presence-close-r2-native-2026-08-27.txt`, exact renderings in `🧪️member-cad-presence-close-r2-diagnostics-2026-08-27.txt`). Three backbone calls now move `Backbones::Memory`, two nested Store dispatches await their actual futures, and UI tests inspect the actual ComponentTree root. The current direct CadCommand dispatch is synchronous, so its three test-only helpers and callers now remain synchronous rather than wrapping ready work in decorative futures. Canonical r3 reruns the four-law filter after these corrections; no production API or guard changed.

## Scope

CAD's concrete presence value contains three variable-length strings. Its local-root factory now retains shared snapshots until uniquely owned, moves one exact string field into a byte cursor per metadata turn, retires at most 4096 initialized bytes per turn, and releases empty allocations separately. The actual field shape is unchanged. Exact NoDraft and NoTransient owners are installed explicitly on CadPlayApp; no generic default admission was added.

The new language-neutral fixture contains empty/default/24-KiB Unicode values plus nonempty peer-roster cases. The Rust domain test uses serde_json as an independent field/byte oracle. The root verifier adds strict Ajv validation and Node Buffer byte-count parity. Native and verifier execution remain pending coordinator scheduling.

## Minimal Shared Seam

PresenceStore currently exposes only cloned local/peer roots. A close disposer cannot obtain exact ownership of either root, so app-only code cannot safely close a nonempty roster. Proposed Store-owned `begin_retirement` transfers the old local root and old peer root into a retained owner and installs a caller-supplied, domain-validated empty terminal local root and empty roster. A close-started state rejects subsequent mutation/publication admission. Captured old readers remain unchanged.

The retained owner waits on shared local and peer roots without spinning. Once the peer root is unique, its fixed root metadata moves into the existing PresencePeersRetirement owner. That owner retires one peer entry and its exact domain snapshot factory; it does not drop an entire populated roster. Every incomplete owner remains retained. Completion requires both detached roots and nested retirement owners to be empty.

ArtifactEditor now exposes ArtifactApp's `build_presence_peer_retirement_factory` as an exact default-None hook with EditorApp forwarding, so CAD and other concrete editors can install their actual domain factory. This is not an implicit local-root fallback.

The consumed peer-commit API now returns the exact untouched commit when the Store has closed. That commit carries its exact snapshot factory and can become a retained root cursor. The sole mounted caller returns rejected metadata into its existing metadata-retirement slot and the typed commit into its existing rejected-owner slot; no monolithic payload drop or rollback is used.

## Remaining Shared-Entry Boundary

The existing PresencePeersRetirement uses Arc::try_unwrap while retaining each shared entry. Two independently retired roster roots that share an unchanged entry can therefore wait on each other. The current tests cover shared readers of one detached root and rejected commits containing newly created entries, not arbitrary overlapping-root retirement. The coordinator requires this correction before any CAD close credit.

The concrete peer correction retains every replaced published roster root until its captured readers release it, then retires one entry reference using atomic Arc::into_inner. A shared entry is released without waiting; whichever exact root/entry cursor receives the final owner retires its actor and typed snapshot. Candidate cancellation must use this same entry owner, not ordinary final drops. Public owned-root Clone and Store Clone must be removed, because untracked copies could otherwise become final readers outside that ownership protocol. Temporary candidate replacements may release only references whose exact new-root or retired-entry counterpart remains locally owned.

Local roots have the same problem when a cancelled operation owns a read while the Store remains live. Their proposed correction uses the existing SnapshotRead lease registry for captured reads rather than exposing Arc. Dropping a mounted read returns its lease to the registry, which retains the root until a live maintenance turn retires one owner. Retained Store replacements and preparation reads must participate too. Cancellation must finish without closing the Store, and final domain retirement must occur exactly once. The language-neutral fixture now contains overlapping roster and live-Store cancelled-read laws; their implementation and execution remain pending.

## Checkpoint Boundary

The coordinator's compound envelope-read gate passed both tests. Shared Store/plugin/CAD integration is now source-coherent and held for the executor's native exact registered-dispatch run, followed by the complete ten-test latest-wins filter and two Store close tests. Four CAD close laws and the actual constructor close remain queued. Full CAD constructor close is intentionally unclaimed until executed.
# Shared Alias Prerequisites — 2026-08-27

The current detach seam is not yet an all-app Presence-close approval. Two retired roster roots can share an unchanged entry; retaining both aliases while waiting for `Arc::try_unwrap` strands both cursors. Local-root capture has the analogous problem when an operation must close while the live Store remains open.

The next coherent unit transfers every displaced published peer root to an exact retirement cursor, removes public mutable-owner cloning, and retires each shared entry using atomic `Arc::into_inner`. Captured root readers keep their immutable roster unchanged until returned; only their specific old-root cursor waits. Candidate cancellation and displaced entries must use the same typed retirement path. No sampled reference count is an ownership proof.

That peer unit is now implemented in source: every successful peer commit returns a cursor holding the previous published root, public Store/root cloning is removed, and entry payloads live in guarded move-only shells. Each cursor releases a shared entry with `Arc::into_inner`; exactly the final cursor receives the actor string and typed snapshot. Candidate cancellation routes both candidate and displaced entries through the same owner. Actor retirement counts exact UTF-8 scalar bytes, and each roster/retirement metadata probe consumes at most one item. Existing initial-publication tests now explicitly close their empty displaced roster cursor rather than dropping it.

The new `retained_presence_overlapping_rosters_retire_shared_entries_once_across_workers` test covers a held first reader while the second overlapping roster finishes, then a barrier-started cross-worker final-owner race. Both cases compare serde roster output and require exactly two peer snapshots/eight actor bytes while the Store remains open, followed by explicit local Store close. Scoped diffcheck passed; native execution is still blocked by the external warm-target deletion, so this is a source-ready unit, not runtime approval. The local Arc/read-registry migration remains required before CAD multi-user close can be certified.

Local captures will use the existing opaque SnapshotRead registry rather than public Arc aliases. Returning a cancelled worker's read must finish while the Store remains open; live maintenance owns final payload retirement. Presence preparation base reads also need this opaque capability, otherwise they remain a final-owner escape.

Source validation identified two prerequisite races in the shared reader protocol. Drop/explicit return published `returned` before releasing their reader Arc. Concurrent maintenance could remove and release the registry Arc first, leaving an ordinary reader destructor as the final payload owner. All four return paths now use one private helper that releases the reader alias while the unreturned registry still owns its guard, then publishes the returned flag. `ErasedSnapshotRead::into_typed` now transfers the unreturned slot atomically under the existing try-lock and exact index/generation validation, without first publishing return. A contended transfer restores the unchanged unreturned read.

Three native tests are source-ready: all four public returns raced against cross-worker reclamation; an injected barrier inside the shared helper after alias release proves the guard remains unreturned until publication; and contended cross-worker erased transfer proves restoration and unique final ownership. The language-neutral fixture records the ownership event order, and strict Ajv plus an independent alias ledger validate it. Source mutants remove alias release, publish a premature transfer return, or discard a restored lease. The canonical Nx verifier passed807checks (`🧪️member-presence-reader-return-selftest-2026-08-27.txt`). Native execution remains pending the combined shared source checkpoint; no atomic-final-owner Presence adoption is claimed before those native laws and the remaining root integration.

The authoritative launch seed now includes focused read-return, cold-rebase, latest-wins, registered-dispatch, and CAD Presence-close gates. Canonical `@semio-tech/plugin-registry:generate` completed successfully and regenerated the launch catalog (`🧪️member-presence-launch-generate-2026-08-27.txt`). This only registers native gates; it does not execute them.
# Local Presence Final-Owner Checkpoint — 2026-08-27

Native registered-dispatch r15 passed one test, zero failures,432 filtered, in0.04 seconds (`🧪️member-latestwins-registered-r15-native-2026-08-27.txt`). Runtime DEBUG confirms both same-target supersession and different-target independence rebase their actual registered worker, publish count97 with generation1→2, and close every retained owner. Lost-vacant-slot and foreign-slot-preserved cases retain the foreign reservation and retire seven UTF-8 bytes exactly before rejection. This is the first executed registered-dispatch pass on the current snapshot; the full latest-wins group, ten Store Presence laws, DAG's six interaction laws and CAD close/constructor gates remain pending. Nx's historical flaky-task annotation is not evidence that this passing test itself behaved nondeterministically.

The full ten-test r16 run reached native execution but aborted after the older fairness fixture supplied a freshly allocated factory instead of the Store's installed exact factory. The strict identity guard correctly rejected admission; strict ArtifactStore Drop then rejected incomplete fixture cleanup during unwind. The fixture now passes the installed factory retained by the app, matching the real publisher. No production guard was weakened. `🧪️member-latestwins-all-r16-native-2026-08-27.txt` retains the failure; it is not a full-group pass.

Exact canonical commands for this checkpoint use `bun x nx run`, `--skip-nx-cache`, the ticket-owned `🧱️cargo-target-cad` target and `🧪️native-artifacts`, `CARGO_BUILD_JOBS=1`, a3600000ms build budget, and disabled Nx daemon/project-graph cache/plugin isolation. The r15 target is `@semio-tech/framework-plugin:test --args='retained_latest_wins_registered_dispatch_rebases_worker_and_publishes_real_document -- --nocapture'`. The complete plugin group uses `--args='retained_latest_wins_ -- --nocapture'`. The Store target is `@semio-tech/framework-os-kernel:test --args='retained_presence_ -- --nocapture'`. Current CAD source contains four `retained_cad_presence_close_` laws, not three; the actual constructor gate is additional.

Store Presence r2 passed all ten tests, zero failures,881 filtered,24.72 seconds compilation and0.00 seconds reported test time (`🧪️member-presence-all-r2-native-2026-08-27.txt`). Runtime DEBUG covers all four read-return paths, contended transfer, actual mounted cancellation while Store23 remains live, overlapping local23/31 roots with live47, held and cross-worker overlapping peer roots, all seven64-KiB-capacity actor-admission vectors, and original-factory1/foreign0 after the originating publication closes. The preceding r1 was compile-only RED on a test `unwrap()` requiring Debug for the existing worker-pump fault; its test-only repair uses an explicit panic branch and changes no production API.

Plugin latest-wins r17 passed all ten tests, zero failures,428 filtered,28.93 seconds compilation and0.05 seconds test time (`🧪️member-latestwins-all-r17-native-2026-08-27.txt`). Runtime output confirms the actual registered count97 rebase/supersession cases, all six8192-byte scope distinctions,65 completed targets under64 live slots, contended finish and maintenance, fair second-publisher progress, five Presence and ten Document/ACK boundaries, oversized empty raw allocation release, and UTF-8/lost-reservation rejection. This certifies those focused native laws; generic whole-object command cleanup, arbitrary child-group atomicity, app-wide dispatch coverage, fresh Wasm and latency limits remain separate open gates.

DAG's nine local-interaction capture/retirement/query tests passed with zero failures,429 filtered,10.46 seconds compilation and0.06 seconds test time (`🧪️member-local-interaction-r1-native-2026-08-27.txt`). The expected strict live-Drop panic was caught by its law. These tests predate the subsequently repaired shared canonical partial-error API; refreshed concrete-caller execution is queued. The cold-rebase capacity regression passed one test,890 filtered,0.21 seconds compilation and0.03 seconds test time, with runtime DEBUG confirming a real second retained publication. The older combined Presence/Transient one-item publication regression also passed one test,890 filtered,0.19 seconds compilation and0.00 seconds test time. Their retained logs are `🧪️member-store-cold-rebase-r1-native-2026-08-27.txt` and `🧪️member-presence-publication-regression-r1-native-2026-08-27.txt`.

The next prerequisite repair, canonical partial-error byte accounting, completed native TDD RED→GREEN and its whole canonical module passed eighteen tests. See `canonical-reader-error-progress.md` for the exact contract, three new laws and retained logs. CAD close/constructor execution remains pending the refreshed callers and coherent dependency snapshot.

CAD preflight then exposed a separate close-authority defect: `PresenceStore::begin_retirement` accepted a caller-selected factory and used it for detached local roots, returned local reads and peer roots alike, ignoring the two installed factories. The new language-neutral `closeFactoryBinding` law reproduced this before implementation: a foreign same-P factory retired both roots while the installed local and peer factories retired neither. The RED log is `🧪️member-presence-factory-binding-red-r1-native-2026-08-27.txt` (zero passed/one failed,28.10 seconds compilation,0.02 seconds test time); cleanup completed before the failing assertion.

The corrected close API accepts only the terminal root and concrete terminal validator. It derives the installed local and peer factories separately, rejects missing local authority or a nonempty roster without its peer authority before detaching, and retains the local factory for returned reads. A rejected peer commit retains its own original peer factory. Empty peer metadata needs no invented domain disposer. All existing Store/plugin/CAD callsites were migrated together; no fallback or compatibility API remains. The new law returns a local read after detach, distinguishes both original factories, keeps a foreign same-P factory alive, and expects local1/peer1/foreign0. Strict schema, independent owner-count oracle and five source mutations cover this boundary.

The complete Presence group now passed eleven native tests, zero failures,884 filtered,20.69 seconds compilation and0.03 seconds test time (`🧪️member-presence-factory-binding-green-r1-native-2026-08-27.txt`). Runtime DEBUG confirms local1/peer1/foreign0 after detach and all ten preceding reader/overlap/rejection laws remain passing. This supersedes the ten-test close snapshot for original-factory authority. CAD close and real constructor execution still require their separate native gates.

The canonical source selftest passed885 checks after that repair (`🧪️member-presence-factory-binding-r1-selftest-2026-08-27.txt`), retaining33 exact proof owners/254 custom rows/25 generic rows. The canonical launch generator also succeeded from the authoritative seed, adding the focused canonical-reader/sealer gate; its retained log is `🧪️member-canonical-reader-launch-r1-generate-2026-08-27.txt`.

A further read-only audit identifies an unclosed adjacent publication boundary: `publish_peer_commit` currently checks only `close_started`, not the originating installed factory or exact base peer root. A foreign same-P Store commit or a stale concurrent publication can therefore replace another root; previous-root cleanup follows the commit factory. The eleven passing laws do not test this foreign/stale commit admission, so they are not all-peer-publication security evidence. The concrete next prerequisite is a commit-bound retained base-root identity plus exact installed factory join, preserving rejected commit ownership for bounded close. This finding was sent to the coordinator before CAD multi-user completion is claimed; implementation/native evidence remains pending.

## Peer Publication Freshness Follow-Up

That adjacent defect was subsequently reproduced and repaired. The new `peer-commit.json` fixture covers a foreign factory, the same factory used by another Store, stale sibling candidates, and a fresh successful handoff. Native RED accepted the foreign commit incorrectly (`🧪️member-presence-peer-commit-red-r1-native-2026-08-27.txt`, zero passed/one failed,32.37 seconds compilation,0.02 seconds test time); exact owner cleanup completed before the failing assertion.

Every candidate now retains its immutable base roster Arc, and the private commit carries that base plus its original factory. Publication checks both identities before modifying the Store. The retained base prevents allocation-address reuse from resembling freshness. Successful publication first installs the displaced root in its exact retirement cursor, then releases the commit's base alias. Rejection preserves the complete commit, whose retained close releases the base alias before disposing its candidate. Candidate cancellation also releases its own base alias explicitly. No generic undo or fresh-factory substitution is used.

The first full rerun passed all four new vectors but exposed an older late-commit fixture that waited for Store close before ever advancing the rejected commit still holding a base-reader alias. Its test scheduler now advances both exact retained owners, checking the unchanged one-item/4096-byte grants; production ownership checks were not loosened. The next full rerun passed all twelve tests. DEBUG confirms unchanged foreign/stale destinations, the stale base being held until rejected-owner close, successful publication with no stranded alias, and cooperative late-commit/Store close retiring exactly two snapshots. The runtime log is `🧪️member-presence-peer-commit-green-r2-native-2026-08-27.txt`.

Source898 adds the strict fixture, independent identity/count oracle and hostile missing-base, missing-factory, premature-alias-release and lost-rejected-owner mutations. CAD's native close and constructor are still separate pending gates; the full atomic child-group publisher remains fail-closed.

The current full source census completed and emitted `📊️member-mounted-source-full-census-r2-2026-08-27.json`, with stdout `🧪️member-mounted-source-full-census-r2-2026-08-27.txt`:773 production command rows,350 source-admitted bounded,316 batch-only,2 forbidden,269 remaining,851 selftests. It correctly exits RED for the remaining real/shared gates; these counts are unchanged from the earlier823-selftest full snapshot. Source admission is not native all-app or bounded-latency evidence. The coordinator separately identified canonical-reader error-after-partial-write accounting as another shared prerequisite; its API repair is queued after the current native ownership checkpoint.

Current checkpoint:851 canonical source selftests pass (`🧪️member-presence-rejected-owner-r3-selftest-2026-08-27.txt`). Rejected-owner factory authority is now bound at minting: each private bundle retains its originating publication's exact factory, and consuming `into_retirement()` accepts no caller-selected factory. The public publication-based `retire_rejected` API was removed. A second native peer-rejection test keeps another same-P factory live, closes the original publication, then proves only the original factory retires the rejected payload. This brings the focused Store `retained_presence_` set to ten native test functions; none has executed on this snapshot yet.

r14 reached the newly changing derive crate and failed on E0308 (`Option<PathBuf>` supplied where `&Path` is required). Subsequent read-only inspection found the peer had already added explicit `Some`/`None` handling with fail-closed compiler diagnostics for missing source provenance. The log is retained as `🧪️member-latestwins-registered-r14-native-2026-08-27.txt`. No native pass is inferred. DAG's separate interaction-retirement module and exact Store owner catalog are now mounted and coherent; the next plugin compile also includes its six native interaction tests, which require a separate filter run.

Latest coherent checkpoint:847 source selftests pass. The rejected-peer module described below is now mounted, not staged: all five `adopt` rejection branches retain both actor and typed Presence, and the active plugin preserves the exact rejected-owner cursor. The fixture now has six cases, distinguishing full-roster rejection from full-created-owner rejection when replacing an existing actor. Its actual-adoption native test remains unexecuted. Source gate log: `🧪️member-presence-rejected-owner-r1-selftest-2026-08-27.txt`; scoped diffcheck passed.

The coordinator-requested seventh vector additionally transfers and explicitly closes the candidate commit, then attempts another `adopt` against the same now-empty publication. It verifies candidate-none rejection retains the actor allocation and Presence. This is seven vectors inside one native Rust test, not seven executed tests. The follow-up canonical source gate passed848 checks in `🧪️member-presence-rejected-owner-r2-selftest-2026-08-27.txt`.

Native r11 ended before tests with one unrelated kernel E0432: the DSL facade imports a `MutationLeaf` derive that the derive crate does not export. The full retained log is `🧪️member-latestwins-registered-r11-native-2026-08-27.txt`. The coordinator assigned the narrow facade/derive join to DAG, which temporarily owns the compiler lease for its dependency check. Registered-dispatch r12 follows that source/check boundary; no Presence runtime approval is claimed.

The first registry regeneration attempt again encountered the intermittent missing Nx target. A graph refresh and canonical retry succeeded; `🧪️member-presence-local-launch-generate-r2-2026-08-27.txt` records generated `launch.json`, including seed404.3 `retained_presence_`.

Native r12 did not start Cargo: Nx reported duplicate OS-dev projects where one path contains replacement characters (`🔨���modules`) instead of the real emoji directory. Read-only `ls` and repository file enumeration found no such malformed physical directory. The retained r12 log records this graph failure; no native test ran and no cache/file was deleted. r13 retries the same canonical target with project-graph caching and plugin isolation disabled, matching the successful registry retry flags.

r13 passed Nx but stopped during Cargo manifest loading: an external derive-to-hash dependency resolved outside `🧰️framework`. The manifest already contained the correct seven-parent path by the subsequent read-only inspection, and `ls` validated the exact hash manifest through that path. No manifest was edited by this executor. The retained r13 log documents the transient boundary; r14 retries the unchanged canonical test against the now-resolving source.

The local root no longer escapes through a public `Arc<P>` accessor. `local_read()` issues an opaque `SnapshotRead<P>` only with an installed exact retirement factory. Ephemeral preparation receives an opaque `ArtifactEphemeralBaseRead`; publication validates the same installed factory identity. Captured reads return to the exact registry before live maintenance or detached Store close selects one returned owner. CAD retirement atomically releases a shared alias with `Arc::into_inner`, and only the final owner retires its actual strings under the byte grant. Store roots and returned cursors are guarded against implicit nonterminal destruction.

Live plugin maintenance now includes returned local reads, including the previously idle fast path, and app close pumps returned reads before closing mounted workers. The Store detach transfers an already-active returned cursor and the same reader registry. Empty-domain test owners now perform real detach/close rather than only assert an empty roster.

New native laws are authored but not yet executed: a real mounted worker captures Presence23, runs, cancels and reaches terminal while its Store remains open; two local replacements preserve captured23/31, accept cross-worker reader returns, and retire exactly two old roots before closing live47. Existing shared-reader, peer-overlap and CAD string laws were migrated to the new ownership boundary.

Canonical source-only gate passed834 checks at `🧪️member-presence-local-final-owner-r1-selftest-2026-08-27.txt`. This includes strict language-neutral fixtures, independent Ajv owner-count/byte oracles, and hostile raw-alias/foreign-factory/lost-return/shared-wait mutations. No native or Wasm approval is implied. The next native snapshot is plugin registered-dispatch r11, then the complete latest-wins and Store Presence laws. Earlier r10 failed on missing compiler artifact directories before any test ran; the independently tested cleanup guard now protects the active ticket, without establishing the historical deletion cause.

The peer admission concern identified at the prior834 checkpoint was that `PresencePeersPublication::adopt` dropped a rejected actor String while returning only typed Presence. The current847 source checkpoint repairs that ownership loss; runtime verification remains open and is separate from the shared-entry final-owner tests.

The test-first vectors are in `👥️presence/🧪️peer-admission.json` and its strict schema: accepted short UTF-8, rejected empty actor, rejected260-byte Unicode actor, pruning-incomplete, full roster and full-created ownership, each with at least65536 bytes of backing capacity and the unchanged1item/4096byte grant. The rejection authority owns both the actor String and typed Presence. Its actor transfers without copying to a retained byte cursor, then the exact domain factory receives Presence; no rejection branch discards the actor, and no empty backing allocation requires capacity-sized byte credit. The plugin retains this full cursor, not only the decoded Presence.

The implementation and actual-adoption test are in `👥️presence/🚫️rejection/🦀️component.rs`, mounted after r11 ended. `PresencePeerAdmissionRejected` guards both exact owners; initialized bytes retire under1-byte grants, and the empty allocation releases separately with1item/0bytes. The six-case native test exercises actual `adopt`, verifies the rejected String pointer/capacity and payload identity, retires both rejected and candidate owners, and counts the target Presence exactly once. Hostile source checks reject dropped actors, false capacity-byte accounting and a missing active-plugin owner. Native execution remains required before this module is runtime evidence.
