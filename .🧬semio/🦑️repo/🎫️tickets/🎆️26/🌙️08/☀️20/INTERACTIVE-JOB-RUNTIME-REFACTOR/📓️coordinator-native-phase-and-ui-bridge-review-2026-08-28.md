# Coordinator Phase-Grant And UI Bridge Review

## Native Primary: Draft Review, Not A Released Gate

Root read the current draft's six primary/recovery methods and list/release/close helpers. These source reads occurred during Dag's implementation, not an atomic source capture or a native execution.

The draft retains original list ownership; successor pin acquisition precedes predecessor release, and exact alias CAS precedes found-pin release. Closing drains retained recovery before a blocked admission head, and primary pointer ownership changes to pointerless Releasing before physical free. Full work/load/inverse review still awaits the source-coherent release.

One concrete accounting finding was routed: begin_primary_recovery read header stamp/type while declaring only cursor+pin work. Dag accepted narrowing that phase to constructor-authenticated original list-page metadata and the pin atomic. H-charged traversal/capture retain actual header identity checks. This is not an increase to budgets or a test-only identity shortcut.

Root fully read and approved the [aligned phase test proposal](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️resident-primary-aligned-phase-test-proposal-2026-08-28.md), with the positive/4096 work preflight moved before the allocator observer is armed. The old test applied a node-allocation-sized short grant to the now-separate reserve phase. The corrected law independently checks zero-item, zero-byte, one-short and exact grants for reserve and allocation; all three layout/3alloc/3free/all-axis checks and the record half remain. Adjacent two-phase neutral declarations and an actual existing-model reference are required. No type/alignment compatibility branch or known-stale native rerun is authorized.

## UI Bridge: Retained Current-Implementation Evidence

Root fully read [R215–R216](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️renderer-children-and-strict-bridge-r215-r216-2026-08-28.md) and all16 detailed result/close traces in its JSON. The UI owner's executed result is three accepted null/bool packets and thirteen malformed packets rejected, against its independent restricted third-party LEB bridge oracle. All16 close traces terminate and its five selected hashes match. Root did not rerun that cursor here.

This is the existing whole-buffer cursor, not a new paged decoder. Generic Store fallback-Null source cannot establish strict UI acceptance. Opcode4 uses canonical list/u64 field bytes, while the actual RetainedUiChildIdsCursor accepts a whole BigUint64Array; they are not interchangeable. The original wire and typed owners remain the intended whole-field grammar owners. Variable symbols/frames/output and same-reader receipts still require their own admitted lifetime, not an opaque scalar substitute.

The source-oracle helper packet is now being made strict-ready and collected as exactly three real ticket tests. Canonical four JSON leaves/three source tests/400.999 remain pending root review of that coherent packet. Duplicate --config refusal in the existing renderer route is recorded as a setup failure. Ticket collection may reuse the existing shared runVitest budget helper's explicit config parameter under Nx; no new runner or budget is allowed.

## Boundary

No native run, compiler success, primary runtime success, canonical scalar mount, new whole-field parser, raw InputAck, worker receiver or full-refactor acceptance follows from this review. Native R1's65 compile errors/zero executed tests and exact80-input capture remain the actual prior compiler boundary.

