# Tutorial Retained Interaction Root

## Current Runtime Boundary

Actual local interaction queries already capture immutable interaction/document/config roots, deliver fixed pages with exact generation-bound Started/page/ACK/cancel identity, and wait for native root retirement. The seven current React errors are still real: two missing complete selection snapshots and five ShellHelpers references to removed selectionJson. They must not be cleared with empty maps, casts, remote Presence, or ordinary interactionSelect replay.

The backend exact restore producer is not mounted. The six existing reserved semantic tails still clone whole InteractionState, build/validate topology and mutate ordinary BTreeMaps. A new synchronous JSON concatenation/decode in the tutorial consumer would not repair that boundary.

## First Typed Root Checkpoint

Use the existing approved shared OrderedMap primitive for the complete three-map local state: selection, activeMode and activeGranularity. Map clones retain exact immutable entries, not domain key strings or DomainSelection vectors. Wire semantics remain the existing LocalInteractionState schema. The private/local domain, comma-containing id and exact anchor remain content; hover remains the current local hover and is not part of captured restoration.

The first strict fixture lives beside the local-interaction contract in `🌳️root`. It reuses the actual existing sparse-anchor-comma-private source case and expects zero payload copy for root clone, zero semantic bytes retired by a shared alias, and exactly 73 domain-key/value bytes at final retirement. Grants 0/1/64/4096 cover owner preservation and byte accounting. The 73 bytes include keys in all three independently owned maps, granularity/id/anchor Strings and active-granularity Strings, but not enum spellings or JSON punctuation. Cold decode/construction is explicitly not interactive work.

The fixture also reuses the existing semantic-Unicode-over-page expected state, with **33,265 actual domain/id/granularity/anchor String bytes**, not padded input. The canonical local-interaction source target passed (`🧪️local-interaction-root-source-r1-2026-08-27.txt`): Lodash/Node byte measurement agrees with both 73 and 33,265 bytes, Immer preserves the unchanged source projection, and five hostile privacy/anchor/ownership projections are rejected. This is source-oracle evidence only.

Two native root tests were mounted first with the new API absent for a compile-only RED checkpoint. That RED also exposed the existing CausalAddOp test fixture's missing mandatory Mutation descriptor; it now has exact additive metadata, a real fixture schema, and no invented binary ordinal (the existing wire is format byte 1 followed directly by signed i64 little-endian). The root implementation was then mounted. The compiler owner's canonical root R2 log reports **2 passed, 0 failed, 220 filtered, 0.01s** after 5.62s compilation (`🧪️member-tutorial-root-green-r2-native-2026-08-27.txt`), independently read by this lane. This is exact root/retirement unit evidence, not a Store cutover or tutorial/restore runtime pass.

## Sparse Three-Map Update

The strict fixture and three native laws were mounted with the update API absent for compile-only RED (`🧪️member-tutorial-update-red-r3-native-2026-08-27.txt`, fourteen absent-API diagnostics, no tests executed). The implementation is now mounted. One domain key is shared by pointer across the three ordered updates; values are already-owned Arc payloads. Comparison bytes and retired bytes are separate counters. The candidate stays private until all three fields finish, and cancellation never publishes. Every cancellation frontier must retire exactly **101 semantic String bytes** for the existing private/anchor/comma fixture: 73 bytes in the old root plus 28 bytes in the newly admitted domain/selection/granularity payloads. The final candidate preserves exact payload pointers. Long Unicode keys are compared under 1/64/4096-byte grants, with no semantic cap.

The compiler owner's canonical update R4 ran **3 passed, 0 failed, 222 filtered, 0.02s** after 4.07s compilation (`🧪️member-tutorial-update-green-r4-native-2026-08-27.txt`), independently read by this lane. Source oracle R1 also passed four immutable Immer comparisons, five hostile schema projections, and the independent Lodash/Node 101-byte ownership sum (`🧪️local-interaction-update-source-r1-2026-08-27.txt`). No frontend tutorial, topology validation, Store publication or native callback timing claim follows from these unit passes.

No canonical Store model cutover, restore publication, or tutorial consumer success is claimed yet. After the retained update/lookup and exact domain retirement join, replace the canonical Store root with coherent ordinary cold-boundary cleanup. Final restore must validate exact current interaction/document/config/topology authority, not historical tutorial identity. Full restore clears absent domains; sparse restore preserves other domains; topology construction itself needs a retained provider or captured immutable index.

## Regression and Production Leaf Checkpoint

The local protocol regression R5 passed **13/13, 212 filtered, 0.04s** (`🧪️member-tutorial-local-regression-r5-native-2026-08-27.txt`). The exact causal descriptor R6 passed **1/1, 224 filtered, 0.00s** after 0.49s compilation (`🧪️member-tutorial-causal-descriptor-r6-native-2026-08-27.txt`). Both actual footers were read by this lane.

The real production `SetInteractionState` leaf now lives in Plugin `🕹️interaction/🧬️mutations/🔁️set-state`, using `dsl::MutationLeaf` and compiler-minted provenance from its adjacent fourteen-field descriptor and payload schema. The aggregate uses that leaf descriptor and delegates cold apply/diff/inverse. Its schema truthfully describes the current four-field stored InteractionState, including hover; it does not pretend that the later three-map Store cutover has happened. Native acceptance is the exact `local_interaction_mutation_leaf_descriptor_and_exact_codecs_are_owned` test and remains queued/running separately, not covered by the protocol gates above.

Canonical source R2 passed the schema and independent JSON/Immer comparisons (`🧪️local-interaction-mutation-source-r2-2026-08-27.txt`); this is not native codec or retained publication evidence. The coordinator temporarily prioritized the missing guest-captured native close lifecycle boundary. Tutorial Store adoption and the seven real renderer joins remain queued, not replaced by placeholder snapshots or historical generation tokens.
