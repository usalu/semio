# Coordinator Worker Bootstrap Refinement R1

## Independent Declaration Replay

Read the complete refined report, all four JSON declaration/fixture files, every28 normative trace,22 gate ending,13 neutral case and4 factory-value case, the full212-line controller, and the existing Shard root/pool recovery and preparation methods.

Executed the unchanged actual launch400.996 command, Nx exit0. Actual output reports strict Ajv2, current TypeScript layouts3, normative traces28, identity8, funding7, endings22, existing neutral executions13 and test-only actual factory values4. All8 selected complete pre/post hash rows are equal. Full tool chunks and hashes are [retained](./🧪️coordinator-worker-bootstrap-refinement-r1-output-2026-08-28.md).

This is declaration/reducer/primitive evidence only. Immer invokes the same normative reducer, not an independent runtime implementation. The four actual JS return-value cases do not invoke a platform Worker or the actual Shard factory. No actor/fullstrict/native/browser/timing or live bootstrap result is inferred.

#### Accepted Narrow Next Boundary

Allow schema-first actual same-client retained-cell TDD and implementation for `ShardClient.prepareWorkerBootstrap(grant)` only. Shared original controller208→224/retained784 funds the purpose16 word before either child uses it. Eight worker-specific words128/retained688 are separate; future one-worker full subset4384 is not the3616 increment. UI-only total1608 remains future until the exact source/schema/fixture release; no quota increase.

The existing eager Shard/fake-worker constructor remains explicitly unadmitted setup. This method may not allocate a new Worker, handler, SAB, destination, request, factory contract, runtime receiver or public neutral capability.

Required actual REDs precede production correction: missing method and original-controller closing-prefix misuse of recovery-only `matchesShell`. Use real neutral prepare/claim/reserve/install before throwing wrappers, retaining original cells and exact first faults. The funded shared purpose serializes only preparation/claim; no worker-wide execution lock.

## All Endings And Additional Review Conditions

The22 endings must consume actual private observations on distinct grants, not the fixture's descriptive strings. Preserve pending cells after blocked/refused/ambiguous calls; a null lookup after a throw is not a never-allocated witness. Healthy pending release differs from terminal cell retirement; fault-held release frees serialization only and keeps its charge.

Add actual UI-close-then-worker-first and worker-partial-cancellation-then-UI interleavings. Current shared phase is UI-named; UI `retired` must not erase the still-live original shared root or force a second root. Record/cell/ledger close or first fault must prevent new descendants while preserving recovery identity.

Before cancellation implementation, declare its exact metadata close driver, private phase and cell/record retention. A UI close method cannot provide worker retirement authority. No installed worker-controller or shared-root refund is authorized without the genuine domain terminal witness. Any additional state word must be inventoried and repriced before use.

Coordinate current actor fixtures and UI baseline at one coherent released boundary. Do not preemptively edit the UI owner's fixtures or silently activate1608 as a capacity policy.

## Explicitly Held

All `beginWorker`, Worker construction/factory replacement, callback binding, active ingress, first-violation runtime, ordinary/effect/canonical destination admission, raw ACK and queued-callback drainage remain held. The declaration's first-event retention cannot price unknown descendants or arbitrary floods. Factory return validation does not retire original arbitrary results/errors.

No source was edited by this coordinator review. Root sent the precise next-boundary decision to Demonstrator and notified UI. No new source hold, native lease, cleanup or publication follows.

