# Owned Node Index Source Review

The coordinator read the complete new `OwnedUiNodeIndex` wrapper and its two test bodies. The wrapper holds a private persistent `NumericIndex<OwnedUiNode>`. Read/lookup results capture their own node before the underlying index can release its last entry. Final entry retirement transfers that node into `UiNodeRetirement`; an inner completed retirement is returned as aggregate pending until both index and node owners are empty. Its one-item/4,096-byte wrapper grants remain bounded. The executor's two-test checkpoint is scoped evidence; the coordinator has not independently rerun that new pair yet.

Before live operation adoption, a concrete inherited capacity defect remains: NumericIndex's `retain` and AVL entry reference increments are unchecked JavaScript numbers; root capture also retains ID and order roots sequentially. Adding only a later throw would lose earlier increments. The wrapper likewise captures its input node before NumericIndex edit admission. The renderer executor accepted a test-first repair using fixed-size cumulative reference plans for the bounded AVL allocation step, atomic two-root preflight, and wrapper preflight before input capture. Saturation probes must remain private and preserve source/input ownership on rejection.

The operation schema and test now exist ahead of their implementation. Strict React R17 correctly reports the missing operation module; this RED is retained and not hidden with a type exclusion. After numeric capacity laws pass, implementation continues through owned operations, tree/hash traversal, ordered notification and publication ACK in both live renderers. The earlier seven-field and full React checkpoints do not certify this still-unmounted path.
