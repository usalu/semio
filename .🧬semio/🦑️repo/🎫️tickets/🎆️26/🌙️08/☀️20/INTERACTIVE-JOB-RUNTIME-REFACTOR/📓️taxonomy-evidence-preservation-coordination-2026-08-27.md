# Active Taxonomy Evidence Preservation

The taxonomy task reported that its active `END-TO-END-TAXONOMY-NORMALIZATION` ticket lost three retained directories around13:56Z: `🧪️wgpu-generated-retirement-6LbGtY`, `🧪️wgpu-generated-retirement-XDLdXS`, and `🧪️energy-reference-capture-TWoVr9`.

The coordinator and all three refactor executors confirmed no deletion, relocation, cleanup or restoration action in that interval. The coordinator's relevant work was the canonical React Read-filter test, read-only source/log inspection, and Markdown updates in this ticket. All executors were reminded to protect the full active taxonomy ticket. No known recovery location exists in this fleet's evidence.

Read-only checks confirmed all three exact paths are absent. The actual React task router invokes `runVitest`; the inspected helper launches its test-budget runner and does not invoke cleanup or reference the missing fixture prefixes. The separately routed workspace cleaner currently excludes open ticket manifests; it was not executed by this task. This inspection does not establish what removed the directories or prove every unrelated helper's behavior.

No shared source, retained transaction, fixture or recovery evidence was restored or overwritten. The peer received these scoped facts without unsupported attribution. A first read-only text search accidentally included large retained inventory JSON and produced excessive output; it performed no filesystem writes or deletions. Subsequent inspection was restricted to exact implementation files and paths.
