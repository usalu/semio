# Coordinator Paged Input Join Review

## Superseding Independent R3 Review

Root independently executed OwnedPaged R3: **15 passed / 643 skipped / 658 collected**, 11.46s, exit0, with the31 selected combined inputs stable. Actor131 also passed; strict still has only the seven tutorial joins. The earlier source findings below have actual hostile regressions and repairs within this first-fragment scope; they are no longer merely staged.

Root read the current builder and reader: private registration occurs before Object.freeze finalization, after-bind ownership is recovered using the source's exact private matchesBuilder, and only the bound builder advances original framing. A separate128-byte observation phase follows each successful source child, preserving a terminal child's entire grant. Tests execute reader-finalization throw and verify no completion/consumed getter runs in the full-grant child turn. See `📓️renderer-paged-payload-builder-2026-08-27.md` R8–R15 and `📓️coordinator-actor-r19-paged-r3-and-strict-r33-2026-08-27.md` for raw independent output.

Shared neutral ledger adoption, removal of unchecked resident capture, full multi-page source/InputAck, typed semantic decoder and live renderer mount remain open. Historical paragraphs below retain the scope of the earlier review.

Root read the complete released Kernel input owner and the current UI Field/Resident builder, reader and resident parent source. Private Field/Fragment/Release identities are genuinely minted by the captured return parser; public format-valid receipts cannot fabricate them. Exact three-field lifetime, header-selected range, private copied versus cancelled proof and strong parent binding are required. ActorR17 independently ran the rejection packet116/116, not positive copying.

UI later executed real Field/Resident admission and first257-byte copy into two charged256-byte pages, with a private Copied token, exact release receipt and registered scalar reader. Those newer results are lane-run and not yet independently replayed by root. The multi-page source continuation remains peer-owned; UI must neither synthesize offsets nor promote one field's release into page InputAck. Effect/presence/other semantic descendants remain retained separately.

## Actual Source Findings Routed to UI

The active builder's catch sets a failure but did not test that failure on later advance; a child that modifies write state and then throws could be retried as if no partial mutation happened. The reader evaluated offset++ before page.byteAt succeeded, so a failed read could skip input on retry. Root requested true before/after-child-work regressions and either unchanged progress or a persistent fault with serviceable bounded close. Clean, explicitly retryable release refusal is a different state.

The begin path reserved a parent slot, allocated a local builder, then separately attached it. If attachment itself failed, calling beginClose only on the local did not structurally retain that admitted builder. The lane acknowledged the branch and is replacing the public split reserve/attach seam with exact parent-owned construction; no local-cleanup shortcut is accepted as recovery proof.

These observations are source review, not root-run failing tests. UI's adversarial packet is active. The existing bare resident page capture path also remains a separately acknowledged consumer-registration hole until narrowed/removed in the reader cutover. Logical owner/byte counters do not alone certify physical JavaScript heap release or strict8ms behavior.

Root made no production edit, source restore, cleanup or generated publication. All independent replay holds are released.
