# Mandatory Descriptor Native R1 Preparation

## Ready For Review — Not Executed

Prepared one exact existing-route command and an85-file input/capture plan. No Nx/Cargo/native invocation, test listing, source oracle, target/metadata edit or production change occurred. No native lease or source hold is inferred. Await a separate root GO and the coordinated compiled-source/metadata hold before fresh capture.

Read completely: [test review](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️coordinator-mandatory-descriptor-test-review-2026-08-28.md), [path approval including catalog release](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️coordinator-mandatory-descriptor-path-approval-2026-08-28.md), [canonical mount review](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️coordinator-mandatory-descriptor-mount-review-2026-08-28.md), Mutation's mount73 and source-plan71, canonical61-line Rust test, adjacent33-line vectors/94-line schema, actual derive router/project/Cargo and shared runner filter/build/run bodies.

Current direct reads confirm the mounted canonical test and exact glue module. Base Mutation still has the initializer119, default body124 and sentinel229. That is source observation, not a native AST result. The reviewed test should reject those facts, but an earlier compile/selection failure must be reported as the actual result.

## Exact Prepared Command

Working directory: /Users/ueli/Documents/semio.

```sh
NX_DAEMON=false NX_CACHE_PROJECT_GRAPH=false NX_ISOLATE_PLUGINS=false CARGO_BUILD_JOBS=2 CARGO_TARGET_DIR='/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧱️cargo-target-cad' SEMIO_TEST_ARTIFACT_DIR='/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧪️native-artifacts' SEMIO_BUILD_BUDGET_MS=3600000 RUST_BACKTRACE=1 SEMIO_COVERAGE=0 SEMIO_TEST_LEVEL=exhaustive bun x nx run @semio-tech/dsl-derive-rs:test --skip-nx-cache --args='--lib --no-fail-fast -E "test(=mandatory_mutation_descriptor_tests::mutation_trait_matches_adjacent_required_declaration_facts)" -- --nocapture'
```

Machine-readable [capture manifest](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧪️mandatory-descriptor-native-r1-capture-manifest-2026-08-28.json) contains the exact command, argument arrays, environment, all85 paths, expected critical hashes,28-member domain census and proposed output paths. It is a plan artifact, not a captured before/after result.

The one exact selection is:

```text
mandatory_mutation_descriptor_tests::mutation_trait_matches_adjacent_required_declaration_facts
```

The shared existing -E test(=full-name) expression selects exact equality, not a prefix or full derive module. No second selector, feature flag, no-run option, profile override, stack/thread workaround, coverage instrumentation, package addition or target relocation is proposed.

## Actual Router And Budget Attribution

[Existing derive router](/Users/ueli/Documents/semio/🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/✨️derive/📦️packages/🦀️rust/📜️script.ts:6) passes resolveTestLevel's rest to runCargoTestBudgeted([semio-framework-os-kernel-dsl-derive],repoRoot,rest). The existing test target forwards arguments to that router. No target was created and test-exports/test-exports-source are not used.

With the current shared nextest branch, the prepared arguments partition as:

- Build: --lib. The warm command lists binaries-only JSON for the one package/profile under the existing build budget.
- Execute: --no-fail-fast -E test(=full-name), then -- --nocapture, through those retained binaries metadata.
- Explicit exhaustive/coverage0 keeps the existing exhaustive900s per-test profile and removes no tests via level exclusions. Build budget stays3600000ms. CARGO_BUILD_JOBS remains2; existing .cargo rustflags/sccache/rustc thread settings remain untouched.
- Native target remains the existing master 🧱️cargo-target-cad; artifacts remain under master 🧪️native-artifacts.

The runner's cargo-nextest availability probe is part of that existing invocation, not a separately launched command here. If it is unavailable or routing changes, retain the actual failure/branch; do not silently retarget or broaden selection. The normal runner's binaries-only metadata is not a test-name roster. Actual test selection/count/outcome must come from the eventual exact invocation; source count1 is only the authored roster.

The runner uses fail status output by default. --nocapture is the existing supported forwarded libtest argument to retain the selected test's DEBUG stream. Preserve what actually appears; do not reconstruct absent passing stdout or infer1 execution from a successful compile.

## Native Source Closure And Noncompiled Provenance

The prospective capture distinguishes:

| Category | Current selected input count | Scope |
| --- | ---: | --- |
| Compiled repository Rust files | 5 | Derive package glue, existing macro-export test module, new mandatory test module, hash package glue and hash implementation. |
| Rust files embedded as text | 3 | Derive primary, DSL facade and actual replication Mutation. Their containing crates do not become native dependencies through include_str. |
| Embedded JSON fixtures | 8 | Existing source/aggregate/leaf-json/leaf-derive/attributes/mandatory-mutations/macro-export fixtures plus the new adjacent facts. |
| Remaining files | 69 | Native manifests/locks/config, domain schema/controller provenance, registered runner/loader/catalog and already-reviewed projection boundaries. These are not69 compiled Rust inputs. |

The exact owner-crate source root is Cargo [lib].path=📦️glue.rs, not the primary derive source. The existing macro_export_tests module still compiles and embeds the primary, glue and DSL facade even though its test is not selected. The new mandatory test embeds the original lower Mutation source through its actual relative include; it does not parse a copied stub.

The sole local Cargo dependency is semio-framework-hash (its own dependency blake3 remains governed by Cargo.lock). Existing syn(full,extra-traits), quote, proc-macro2, serde_json, test serde and sha2 are retained. No OS kernel, Store, Plugin, runtime, resident or WGPU crate is selected. The WGPU projection files inherited from the reviewed shared-loader boundary are captured as overinclusive noncompiled provenance only; no WGPU validation/test claim follows.

No include search found another literal Rust or JSON input in these two crate domains beyond the listed closure. Token-generation quote blocks containing include_str!(#dependencies) are macro output templates, not executed include macros in this selected test. Re-enumerate actual derive/hash domains and rescan path/include declarations at fresh capture; if a new dependency appears, attribute it before dispatch.

The85-file envelope is a full declared repository-native source/include capture plus selected loader/projection provenance, not a trace of every JavaScript import or every installed third-party/tool binary. The framework package's current export resolves to 🟦️glue.ts; that manifest/glue and repo loader/discovery/Nx plugin are included. Bun/Cargo locks, toolchain/config and actual eventual binaries metadata define the tool/dependency boundary. Do not call this an immutable whole-workspace or installed-tool audit.

## Fresh Capture And Terminal Rules

On explicit GO, capture all85 original regular-file bytes and identity tuples before dispatch and after terminal, including full path/hash/length/device/inode/mode/nlink/mtime/ctime and read stability. Open/read/fstat the same leaf without following a leaf symlink; reject unstable/nonregular inputs. Re-enumerate the28 current derive/hash members before/after and compare membership rather than only rehashing the first list. Capture actual selected metadata/loader changes as changes; do not overwrite an old snapshot or erase an announced unrelated provenance delta.

No successful before/after tuple equality is claimed by this preparation. Fresh base/test/glue hashes must match the explicit mount/hold packet. Preserve all raw tool chunks immediately in ticket Markdown plus exact-string JSON, then any compiler diagnostic artifact and actual nextest metadata. Record the actual outcome as pre-Cargo, compiler RED/zero tests, selection refusal/zero tests, or named semantic result. Expected three true/three false facts must never substitute for observed DEBUG output. Release source/catalog holds promptly at terminal; no retry, repair, Wasm, OS or broad derive run is inferred.

## Direct Preparation Hashes

| Source | Actual SHA256 |
| --- | --- |
| Canonical test | 2bc2741f078ebeacd894dbf6e69fc3c292ba38d46881fbbeda1ec8858d7c30ae |
| Adjacent vectors | 65220ea34a799d625ccb45bc4c846c059ab6c757c9629680350ad1b4d9d7d55b |
| Adjacent schema | 77b9a6038889e917b09d6db6154c6d2f18551cf2f138850aaa67dc5061850a69 |
| Derive glue | 147e1f68f59f9165bd209440070ca16deeb8bc39f366eaa3a75798e884e0b122 |
| Derive router | 3e34a6b8706ef602de63b1a8d443e2413f0f6d51b979ebc7b93e4897d89467bd |
| Derive project | 520db987113520f28419a7e5317136187cc49cd924ad505b8335fc714e13dce2 |
| Derive Cargo | 75ed143dd5d75f405d3ab5e4e4800085d94f6814ec212bece289181db21e70da |
| Actual lower Mutation include | e5f2f9ce74cc305bcbc23c0d99ab70cc2af54cf299a561f7910d56a7dbbd8385 |

These are read-only preparation observations, not a source freeze or native pass. Plugin12 qualification edits and its separate source-oracle invocation remain on hold pending the registered codec route release. Store detach's known parent/receiver blocker is unchanged; no redundant OS compile is proposed.
