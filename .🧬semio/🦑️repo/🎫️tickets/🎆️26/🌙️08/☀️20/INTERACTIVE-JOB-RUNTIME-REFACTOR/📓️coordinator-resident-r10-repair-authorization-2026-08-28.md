# Native Resident R10 Repair Boundary

Root read Dag's complete production-delta review, the existing authority's original root/gate/close branches and existing Send erasure, and the unchanged seven-law packet (including the remaining concurrency, poison and exact-layout test bodies). R10's actual baseline is the semantic RED: Data allocation152→0 and charge152/1/1→0 in the same call;17 prior tests passed. Control remains unexecuted in that baseline.

## Authorized Source and Test Slice

Authorize only the proposed single inline `release: Option<ResidentRelease>` replacing `retiring` in the same native authority; checked original-source detach and Destroy/Free/Refund/Clear transitions; concrete empty-shell destruction functions; exact original Layout preservation; dynamic native-layout inventory; and specialized LedgerState-only close access for already-pointerless poisoned residue. No new root, allocation pool, blanket unsafe Send implementation, capacity/stack/grant increase, or public refund method.

The existing authority already has `unsafe impl Send for LedgerState`; extending its privately gated state is not permission to add a new blanket erasure guarantee. Every admitted allocation originates in the unchanged concrete Send-qualified constructors, and all destruction eligibility/alias/admission checks must remain under the original gate. No generic poisoned mutable access may escape. Ordinary access stays poisoned. Under poison only validated pointerless Refund/Clear or exact empty-root terminal observation may proceed; other original sources remain retained and blocked.

The pointer must remain in the original inline release state through the allocator call. Precompute every fallible subtraction and grant sum before taking any source or freeing. After deallocation returns, install the pointerless diagnostic stage without another fallible call. No allocator-unwind test or callback is introduced. Actual shell payloads/links must already be empty before their destructors are invoked.

Separately authorize mounting the unchanged seven-law child in the existing test module plus cfg(test)-only fixed observation hooks, retaining the sole existing global allocator and unchanged baseline. Expected source roster25=17 original+1 baseline+7 new. This is not permission to rewrite any prior test expectation, choose larger arbitrary drain bounds, or claim25 executed. If a derived bounded iteration count changes, present that exact test-bound delta rather than weakening a law.

No native command is authorized by this source approval. Dag must release exact source/test hashes and any compiler-relevant deviation after source work; root reviews before the sole Retained executor receives its next bounded command. Preserve original R10 outputs and all existing inputs.

## Dependencies Still Open

Opening/root backing preadmission, privately issued RuntimeAppCell→Store field/FIFO receiver, and typed channel/backbone retirement remain separate. The OS Send/helper candidate cannot justify discarding a returned backbone or sending a detach request before the original funded handoff. No Store detach, Plugin trait, shared TS pool, WGPU output, or taxonomy pin change belongs to this repair.

