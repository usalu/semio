# Coordinator Canonical Return Result Oracle R1

## Actual Gate

Exit0:11 fixed result vectors, two fixed page envelopes of4103 and4138bytes, seven invalid combinations, and all224 combinations of four control kinds/four outcomes/fourteen faults match the exact schema. Independent webassemblyjs ULEB and Node Buffer supplied binary oracles; strict Ajv remained enabled.

```text
[DEBUG] actor-return-result-oracle {"vectorCount":11,"pageSizes":[4103,4138],"contradictions":7,"controlCombinations":224,"scope":"format and JSON schema only, no retained state machine or native host execution"}
```

All three captured files were stable:

```text
16dd2ae43e260da23de7aca82895b10b9482bec966dc27f1e3b86ef9b4ebdb38  🧰️framework/🔨️modules/🎭️actor/📤️return/🧬️schema.json
0cd86b864db778320073464f2d911cfc5d52b0beb3d6e077ad3e0465e8669045  🧰️framework/🔨️modules/🎭️actor/📤️return/🧪️schema.json
286cca66aaf417e3fb38fe25f76d09af74513cea7bba4fbe47d4105afaeaba75  🧰️framework/🔨️modules/🎭️actor/📤️return/🧪️fixture.json
```

Successful poll yields pending/page/retired. A fixed control response for poll is only blocked/refused with a non-none fault. Other accepted/duplicate controls require no fault; refusal/block requires a fault. These format checks do not establish runtime authority, replay/frontier behavior, page lifetime, callback timing or native retirement. TS result implementation is delegated against this one canonical packet.

```sh
SEMIO_RETURN_RESULT_ORACLE_EVAL='(async()=>{const {readFileSync}=await import("node:fs");const {Buffer}=await import("node:buffer");const {default:Ajv}=await import("ajv");const {strictEqual}=await import("node:assert");const base="/Users/ueli/Documents/semio/🧰️framework/🔨️modules/🎭️actor/";const read=p=>JSON.parse(readFileSync(base+p,"utf8"));const schema=read("📤️return/🧬️schema.json"),fixture=read("📤️return/🧪️fixture.json");const ajv=new Ajv({strict:true,allErrors:true}).addSchema(read("🚪️lifetime/🧬️schema.json")).addSchema(read("📄️page/🧬️schema.json")).addSchema(schema);const check=ajv.compile(read("📤️return/🧪️schema.json"));strictEqual(check(fixture),true,JSON.stringify(check.errors));const valid=ajv.compile({$ref:schema.$id+"#/definitions/result"});const lib=await import("@webassemblyjs/leb128/lib/leb.js");const encode=(lib.default??lib).encodeUIntBuffer;const u=v=>{const b=Buffer.alloc(8);b.writeBigUInt64LE(BigInt(v));return Buffer.from(encode(b));};const byte=n=>Buffer.from([n]);const origin=v=>[u(v.activationGeneration),u(v.requestSequence)];const identity=v=>[...origin(v.origin),u(v.returnSequence)];const receipt=v=>[...identity(v.identity),u(v.pageSequence),u(v.length),byte(v.final?1:0)];const control=v=>[byte(fixture.controlTags[v.kind]),...(v.kind==="inputAck"?receipt(v.receipt):identity(v.identity))];const enums=fixture.resultEnums;let vectorCount=0;for(const row of fixture.resultVectors){const v=row.value,chunks=[byte(fixture.resultTags[v.kind])];strictEqual(valid(v),true,JSON.stringify(valid.errors));if(v.kind==="refused")chunks.push(...origin(v.origin),byte(enums.fault.indexOf(v.fault)));if(v.kind==="pending")chunks.push(...identity(v.identity),byte(enums.reason.indexOf(v.reason)));if(v.kind==="retired")chunks.push(...identity(v.identity),byte(enums.completion.indexOf(v.completion)));if(v.kind==="control")chunks.push(...control(v.control),byte(enums.outcome.indexOf(v.outcome)),byte(enums.fault.indexOf(v.fault)));strictEqual(Buffer.concat(chunks).toString("hex"),row.hex);vectorCount++;}const pageSizes=[];for(const row of fixture.pageResultVectors){const prefix=Buffer.concat([byte(2),...receipt(row.receipt)]);strictEqual(prefix.toString("hex"),row.prefixHex);strictEqual(row.pageLength,row.receipt.length);const payload=Buffer.alloc(4096);for(let i=0;i<row.pageLength;i++)payload[i]=row.pattern==="zero"?0:(i*37+11)%256;const wire=Buffer.concat([prefix,payload]);strictEqual(wire.length,row.wireBytes);strictEqual(wire.subarray(prefix.length+row.pageLength).every(x=>x===0),true);pageSizes.push(wire.length);}let contradictions=0;for(const row of fixture.resultContradictions){strictEqual(valid(row),false);contradictions++;}let controlCombinations=0;for(const kind of ["poll","inputAck","cancel","retiredAck"])for(const outcome of enums.outcome)for(const fault of enums.fault){const c=kind==="inputAck"?{kind,receipt:fixture.receipt}:{kind,identity:fixture.identity};const value={kind:"control",control:c,outcome,fault};const success=outcome==="accepted"||outcome==="duplicate";const expected=(success?fault==="none":fault!=="none")&&!(kind==="poll"&&success);strictEqual(valid(value),expected,JSON.stringify(value));controlCombinations++;}console.log("[DEBUG] actor-return-result-oracle "+JSON.stringify({vectorCount,pageSizes,contradictions,controlCombinations,scope:"format and JSON schema only, no retained state machine or native host execution"}));})()' NX_DAEMON=false NX_CACHE_PROJECT_GRAPH=false NX_ISOLATE_PLUGINS=false bun x nx exec --projects=workspace -- bun -e 'await eval(process.env.SEMIO_RETURN_RESULT_ORACLE_EVAL)'
```

