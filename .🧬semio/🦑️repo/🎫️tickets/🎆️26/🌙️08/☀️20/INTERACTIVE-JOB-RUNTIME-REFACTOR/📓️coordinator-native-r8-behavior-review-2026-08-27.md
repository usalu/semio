# Native Plugin R8 And Actual Behavior Review

## Compile Versus Execution

R8 genuinely compiled the Plugin test binary in44.02s, exit0, after the Mutation-owned PluginApp import repair. Its926 selected inputs are retained, not claimed as an atomic full dependency closure. Root read the compile report and actual later test footers. The590 warnings are not dismissed; dropped Future/Result work remains separately assigned.

The original construction tests genuinely failed0/2 in0.23s: the preallocation probe saw Some(false) instead of Some(true), and injected shell-allocation failure left the original live root absent. After the four-line ordering repair, the same assertions passed2/2 in0.29s with515filtered. Root read both actual raw outputs. This only proves shell construction precedes exact live detachment in these tests, not aggregate admission or final worker callback retirement.

## Mounted Output Ownership

The later producer R2 run actually executes3PASS/1FAIL,515filtered,0.05s. Original cancel/Drop generation close, exact structural child-unwind retention and incomplete-source preservation now pass. The original saturation case now completes its cleanup and reaches the intended admission assertion (accepted true instead of false). Root read the actual output/footer at `🧪️member-plugin-mounted-output-admission-r2-2026-08-27.txt`. Shared pool reservation before producer remains a clean semantic RED and is the next owned implementation, not a completed output pipeline.

Actual live-output run was0/3 in0.17s. Two intended assertions failed: child unwind lost the structural Slot/Box/reservation, and incomplete source tuple extraction lost remaining tree/reservation owners. The saturated shared-pool case failed close cleanup for instance71 before its final admission assertion; it is not yet a clean admission-semantic RED. Root read the actual assertions and footer. The owner traced cancellation leaving generation0, which the terminal handback refuses. That is a production cancellation defect to repair, not permission to invent a fixture generation or relax close.

## Exact Old-Binary Mutation Inventory

The actual list selected24: dummy5, surface8, transaction10 and keyed-close1. Combined execution reported3passes then stack overflow/SIGABRT, with no complete24 footer. All24 were then isolated in fresh processes against the unchanged old R8 binary, SHA256 d823a6688cebb72036ae14c98c235c9cf4e93fae4b4c51ca9bba099371d39c00 before/after; no Cargo rebuild, stack increase or test serialization flag was used. Dag's construction source repair had landed after the earlier explicit release, so current source is not falsely called the R8 snapshot.

Isolated outcomes:4processPASS,19SIGABRT,1ordinaryFAIL. The viewer-law process pass is vacuous because its async helper was unawaited. Only the dummy metadata, shared surface dialect and keyed no-state disposer cases get their narrowly named executed-law credit. Primary failure classification:10missing-factory,7strict StoreDrop,1PresenceDrop,1viewer unknown-key/read-only mismatch,1stack overflow. Root read the full24-row table and representative actual primary outputs. Full commands/list/all outputs remain in the executor report. Fixture factory/retirement/await/vocabulary repairs are Mutation-owned; no strict guard or semantic assertion weakening is authorized.

The five separately requested warnings skip real checkpoint/request-cancellation/subset-registration work. Root read Dag's concrete source diagnosis; exact awaited state/registry tests are queued, not counted as existing law success.

## Evidence

- [R8 compile and full raw output](./📓️plugin-native-inventory-r8-2026-08-27.md)
- [Construction RED raw](./🧪️member-plugin-close-construction-red-r1-2026-08-27.txt)
- [Construction GREEN raw](./🧪️member-plugin-close-construction-green-r2-2026-08-27.txt)
- [Live output actual RED](./📓️plugin-mounted-output-admission-red-r1-native-2026-08-27.md)
- [Exact24 list and all execution evidence](./📓️plugin-mutation-fixtures-r8-binary-inventory-2026-08-27.md)
- [Dropped-Future source diagnosis](./📓️plugin-dropped-future-warnings-r8-2026-08-27.md)

Next: retained construction/admission, producer ownership and fixture repair reruns. The existing-registry allocation test has since genuinely executed RED0/1,517filtered,0.05s: the actual constructor initialized1024slots and retained4,202,496bytes without resident admission. Its exact raw output is `🧪️member-plugin-registry-backing-red-r1-2026-08-27.txt`; a paged same-registry/real Open-owner admission correction remains source design, not a runtime pass. No full Plugin, all-app, native/Wasm/browser lifecycle or callback timing acceptance is claimed.
