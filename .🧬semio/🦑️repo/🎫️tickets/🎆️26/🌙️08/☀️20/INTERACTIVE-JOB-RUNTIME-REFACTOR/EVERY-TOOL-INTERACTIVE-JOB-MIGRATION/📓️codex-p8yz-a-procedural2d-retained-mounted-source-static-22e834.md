# P8yz-a Procedural2d Retained Mounted Source/Static Implementation

Date: 2026-08-25  
Scope: Procedural2d retained mounted ingress plus its separately authorized shared canonical-pack prerequisite. No Cargo, Nx, Wasm, browser, build, root-script, or peer-caller command was run.

## Result

The sole Procedural2d whole-buffer Wasm caller is removed. The production census is exactly **10 occurrences: one shared fail-closed definition plus nine untouched peer callers**.

The production bridge now exposes begin/preflight/admit/seal/poll/take/resume/retry/explicit output ACK/load ACK/cancel/incremental close with exact operation/page/item/byte/output/control credits. Preflight precedes producer copy. Progress/checkpoint/preview are bounded latest-wins pages; terminal output is lossless. Operation/generation/base/parent authority is validated immediately around atomic maintenance publication, including stale/ABA/handle-loss recovery, last-valid visibility, displaced-store retirement, and terminal-empty closure.

The worker-owned session consumes `P2D2`, rejects `P3D3` before semantic allocation, and passes every following byte unchanged into the canonical SPK source, anchor, segment/DEFLATE, catalog, value, and fixed typed Procedural2d cursors. The mounted route has no full hex, full byte-vector, `RecordValue`, `decode_pack`, or `decode_document` intermediate. The binary batch convenience decoder is test-only, and mounted Store visibility excludes the UI-forbidden batch authority.

Fixed owners cover every persisted snapshot field, all widget variants and recursive dictionary/cluster/generation payloads, synapses, layouts, all fourteen mutations including 2d-only `clear-widget-layout`, and history/cursor/conflict/child/control/output authorities. Each structural grant exposes at most one scalar byte, length byte, string character, or item opportunity.

`Procedural2dStoreInitializationAuthority` incrementally copies the initial snapshot, seeds history metadata, directly replays applied forward mutations over every semantic field, observes inverse/redo lanes, restores cursor/checkpoint state, retires displaced values, and validates publication authority immediately before construction. The production law admits a non-empty canonical envelope through the real `VcsArtifactApp` maintenance step, verifies all-field/all-14 semantic replacement and digest, explicitly ACKs, and separately proves missing/wrong-operation/wrong-generation/wrong-base/wrong-parent candidates fail closed with last-valid visibility.

## First Independent RED Remediation

The principal retained snapshot law now explicitly appends `retained-synapse` from the appended `retained-neuron` and its declared `out` port to the appended `retained-preview`. This is semantically consistent with the Flow output-preview convention. The non-empty synapse assertion remains.

After typed handoff, the law verifies:

- exact synapse row count;
- exact appended synapse row equality;
- a field-delimited digest over id/from/from-port/to/to-port;
- full typed snapshot equality;
- terminal-empty close.

The recursive neural dictionary, output-preview dictionary, cluster tree/flow, and nested generation JSON remain non-empty. The language-neutral mounted ledger now records all five principal synapse proof obligations.

The verifier also now restricts its bridge scan to the production region before `MountedLaws` and constructs forbidden spellings from fragments. Consequently, required or forbidden strings inside the test cannot make the reachability law pass or fail by self-reference.

The audit's P2c statement is stale at the source/static boundary. `terra-sixth-independent-p2c-remediation-source-static-audit-2026-08-25.md` is GREEN for the source/static route while explicitly deferring final Phase-2 acceptance to native/Wasm/browser/timing matrices. No accepted P2 source was changed.

## Owned Sources

- Procedural2d editor Wasm bridge and domain editor hooks;
- Procedural2d retained snapshot and all-14 mutation binary owners;
- the two Procedural2d language-neutral law ledgers;
- shared retained pack format/facade/value cursor sources and their language-neutral ledger, as the authorized prerequisite.

No Procedural3d, shared plugin/script/Cargo manifest, root verifier, or peer raw caller was edited by this packet.

## Verification

Passed after the RED remediation:

- scoped `rustfmt --edition 2021 --check` on the seven shared/P2 Rust sources;
- Bun parsing and exact predicates for all three JSON ledgers: tags `00..17`, all fourteen mutations with `clear-widget-layout`, `P2D2`/`P3D3`, nested owners, validated swap/ACK/close, and the five synapse proof rows;
- source-contract harness proving the exact retained all-tag, corrected snapshot, P2 all-14, authority, production-swap, and reachability functions retain executable test attributes and required assertions;
- domain-local production-region route/reachability predicates with zero mounted whole-decode edges and all five retained layers;
- scoped `git diff --check`;
- exact raw-caller census of **10**.

The exact Rust laws remain directly executable by the final permitted Cargo matrix:

- `retained_value_vm_covers_every_wire_tag_and_terminal_empty_close`;
- `non_empty_canonical_snapshot_round_trips_one_grant_at_a_time`;
- `every_fourteen_variant_decodes_through_retained_structural_grants`;
- `authoritative_publication_rejects_stale_generation_aba_and_parent`;
- `vcs_artifact_app_non_empty_retained_maintenance_swap_is_authoritative_and_fail_closed`;
- `domain_local_static_verifier_rejects_raw_routes_and_proves_two_dimensional_coverage`.

A dependency-free behavioral harness is not possible because those laws depend on monorepo Store/pack/plugin types and the async test macro. Cargo execution and runtime claims remain deliberately unmade under the packet constraint.

## Audit Boundary

P8yz-a is audit-ready at the requested source/static boundary. The independent audit can inspect the canonical retained route, typed all-field/all-14 ownership, direct initializer replay, real maintenance replacement, explicit ACK/close, corrected non-empty synapse proof, and exact census without a known source/static blocker.
