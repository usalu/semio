# Native Restart Owner Review

## Verified Boundary

The coordinator read the complete R3 report, all 66 lines of the retained raw stream, and the actual 170-line restart fixture source. R3 executed exactly `checkpoint_restart_`: two passed, zero failed, 522 unselected, 0.087 seconds, Nx exit zero. This is executor-run evidence independently inspected by the coordinator, not a second native execution.

The earlier boundaries remain distinct: R1 stopped at four missing const-generic joins plus the independent duplicate declarations import; R2 compiled and ran one pass / one transient-disposer failure. R3 follows the exact true-mode disposer adoption and resolves that observed failure. The ordinary false-mode fixture remains registry-less and has no restart factory proofs.

## What Actually Ran

The concrete factory-owner law passed. The transient test then exercised zero-item and 4095-byte refusal, preservation of the original root, the granted structural store handoff, an intentional caught panic after that method returned, later original-root release, terminal completion, and foreign-store rejection. The printed panic is followed by the test's successful result; there is no secondary abort in the retained stream.

The two declared 4096-byte phases are fixture accounting, not physical allocator measurements. This does not prove an internal allocation failure, poisoned-lock recovery, arbitrary transient descendants, timing, the complete checkpoint/restore tail, or all Plugin behavior.

## Evidence and Coordination

- [R3 executor report](./📓️plugin-restart-two-r3-native-2026-08-28.md)
- [Complete retained raw output](./🧪️member-plugin-restart-two-r3-2026-08-28.md)
- [751 selected input capture](./📓️plugin-restart-r3-selected-inputs-2026-08-28.md)
- [Prior two-law R2](./📓️plugin-restart-two-r2-native-2026-08-28.md)

The capture is selected and non-atomic, not a complete dependency manifest. Captured Plugin main was `099212bbc6eb4bb2a5205fe109588989f71f36d89313fb1b449da9838c4a821f`; Store was `0ed0d7a78c833c1081825c598de3a5dde36ecc858a2e1448c5695899358efd0d`.

Mutation and Dag source holds were explicitly released at terminal. Dag continues the actual checkpoint path with the original app retained outside the fallible body, publication and exact acknowledgement driven, and close completed before any collected error is asserted. That next source and runtime packet remains unverified here. No cleanup, source restoration, quota increase, stack change, or generated publication was performed.

## Subsequent R4/R5 Boundary

The coordinator read the complete R4 executor report including its full captured tool output, the actual constructor catalog call, and the new fixture publication/close helper. The complete checkpoint selector executed zero passes / one failure / 523 unselected, 0.296 seconds, SIGABRT. Its first fault is the exact catalog join: owner, controller, schema, uniqueness and typed factory agree, but generated_migrated is false. The fixture's authored TestCommand tool roster omitted applyCountFromTask.

The strict Interaction ArtifactStore Drop then panics during construction unwind. This occurs before the caller receives the app, so holding an already constructed app outside the later fallible publication body does not cover it. The command roster correction does not fix or certify that generic constructor-failure ownership boundary. No artifact result, UI scope or ACK was reached in R4.

Same-snapshot restart regression R5 is executor-reported two passes / 522 unselected, 0.137 seconds, Nx zero; that report remains separate from R4. Dag owns the exact fixture roster and added generated-roster assertion, with no catalog predicate relaxation or default-mode proof promotion. Fresh full checkpoint plus changed const-owner assertion is queued under a new short source hold.

See [R4 full output report](./📓️plugin-checkpoint-tail-r4-native-red-2026-08-28.md) and [R5 regression report](./📓️plugin-restart-regression-r5-native-2026-08-28.md).

## R6 Successful Actual Checkpoint Path

The coordinator read the complete [R6 executor report and full captured output](./📓️plugin-checkpoint-tail-r6-native-green-2026-08-28.md). The exact full checkpoint selector now passes one / 523 unselected,0.057 seconds,Nx zero. Actual runtime DEBUG is `outcome=Ok((1, 1, 1, 1, 7)), closed=true, close_fault=None`: one artifact result, one UI result, one full UI scope, one terminal receipt and restored count7. The inspected helper checks each exact result acknowledgement and actual app close before asserting these values.

The corrected roster path therefore reaches and completes the previously unexecuted tail. This is a native fixture checkpoint/requeue/registered-publication result, not guest bootstrap, whole Plugin, arbitrary command decoding, physical-work metering or strict per-callback timing. The R4 generic constructor-unwind defect remains open.

R6 captured864 selected inputs before dispatch, including the Store subtree; still not an atomic complete dependency closure. Main was `2ad816977def25ded3175c87c0f7d03344f1bae57549689b17388adf871736ca`. R7's changed generated-roster assertion is separately executor-reported one pass /523unselected,0.014 seconds,Nx zero; both holds were released.
