# Extension Router Retained Boundary

Read-only check for the demonstrator owner: plugin `🖥️host/⏳️imports.rs` has `run_router_effect` around line 593, forwarding to `effects::run_router_effect_job` with compute/runtime services, scope, operation context and router handler. It maps Complete, Cancelled, Fault, DeadlineExceeded and WorkerLost to the host import result. `host_async::invoke_extension` around line 849 captures the call, checks cancellation, converts payload to a whole String, then uses that router job.

The coordinator and demonstrator subsequently read the callee in `🖥️host/⚡️effects`: after checking cancellation, `run_router_effect_job` returns `Fault("router effect retained-session pump is not mounted")`. Thus this is an imported, retained-named **placeholder**, not mounted scheduling or execution. It is not evidence that ExtensionBundle's synchronous evaluator or generic actor Event::Request has a bounded retained implementation. The payload conversion is also whole-input work. No files in this peer-owned invocation area were changed by this lane. The demonstrator owns the real canonical Request/Completed paging and bounded-job implementation; AppCommand ingress authority must not be borrowed for it.
