# Native Admission And UI Scope Review

## Later Actual Checkpoint

Independent coordinator scopeR1 actually passes5/664unselected669,4.82s,Nx0,27selected hashes stable; see `📓️coordinator-ui-scope-r1-2026-08-28.md`. The pending no-cell defect below was reproduced and repaired before this run. All short source holds are released.

The coordinator also read the complete Mutation command-close native R2 output:2PASS/4FAIL/524unselected,.219s,noabort. Command size is one byte; the short and zero byte rows both use zero, not distinct positive-short grants. Both exact one-byte cases report zero released bytes, and both zero-byte rows mutate when blocked was required. The exact owner repair remains with Mutation; allocator overhead, final completion retirement and timing are outside this six-law result. See `📓️txn-command-close-six-r2-native-red-2026-08-28.md`.

The native resident backing proposal requires an additional concrete ordering rule before implementation: an erased strong consumer Arc cannot be finally released merely because another record's S slot is empty. The actual C slot must transfer to an exact preadmitted typed parent, and the original ledger bootstrap must survive callback/caller loss without recursively dropping linked pages. Dag is declaring that typed handoff and actual drop-counter tests; no missing native API is claimed mounted or passing yet.

## Native Next Packet

The coordinator read the complete `native-admission-next-packet-2026-08-28.md`, the actual capacity-only Rust authority, and both current shared resident contracts. The next bounded native packet is the existing prepare/read/claim admission-cell and typed-record contract, not a second runtime or wire protocol. Dag owns source and test declarations; Retained remains the sole native executor after the UI-host baseline.

Native backing, stable exact consumer identity, actual layout and bounded allocation must be declared before implementation. The TS logical 296/264-byte prices are not native layout facts. The composition root bootstrap exclusion must be explicit, and no unbounded registry, public counter refund, numeric-equality authority or final cold payload drop is authorized. Typed registration must precede fallible domain construction; first original failure ownership and resource/result-alias retirement remain separate.

The first concrete consumer remains the existing Plugin Opening/registry construction shell. R6's passing checkpoint roster does not repair the R4 generic constructor-unwind loss. Canonical native return production and host pre-instantiation admission follow this real consumer; an isolated capacity or permit test is not the endpoint.

## UI First Scope

The UI owner reports actual first-scope R14 one-law success, followed by R15's real repeated-bind-after-WeakMap-fault failure and R16's three selected passing tests. R16 covers the empty lifecycle, ten cancellation prefixes and seven constructor/handoff fault vectors. These are delegated scoped results, not a coordinator independent rerun or full renderer acceptance.

The staged callable is `pool.bindInstance(owner, activation, lifetime, grant)` returning `{step, scope}`. The declared nine separate grants are 296,64,64,64,264,64,368,64,128; only the final phase exposes a scope. Payload admission is still under cutover. Pool metadata is now 264/5/5; the separately funded Shard controller remains 768/10/10 and has no final-close refund authority.

Read-only coordinator review found that a closed ledger can refuse admission without creating a cell while the parent remains in its preparing phase. This was routed for an actual no-cell cancellation/close regression, not declared a runtime failure from source alone. The UI owner is running that law together with concurrent same-composition admission; canonical capacity saturation is blocked, while a closing ledger rejects.

The remaining composition check is explicit: a genuine activation from another Shard must not spend this pool. Demonstrator owns a direct private original-client matcher on the actual activation lease. Ownership must survive revocation, while operation activity remains a separate check. No actor-name lookup, structural owner field or capacity equality provides this proof.

## Released Execution Queue

Taxonomy released the UI-host registered package/source/launch packet after its actual six source tests. Retained then reports the registered source target actually passes 22 neutral, seven hostile and three logical frontier cases, and has dispatched the unchanged five native EventQueue baseline laws. No native baseline result is claimed here.

Mutation is mounting only its six command-close tests; Plugin fixture capture is held until that exact mount release. Dag's native resident test-first packet is queued behind the UI-host baseline. No root compiler, cleanup, source restoration, generated publication or WGPU repin was performed.

The CAD missing-disposition census finding was assigned to taxonomy's narrow Rust source-parser lane with exact returned-local ownership and hostile fixtures. All independent factory/schema/publication checks remain required; no runtime or all-app credit follows from a source grammar repair.
