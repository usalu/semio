# Live Tool-Job Cohort Ledger

Date: 2026-08-26  
Authoritative evidence: `📊️coordinator-official-tool-jobs-live-2026-08-26.json`  
Command: `bun ./📜️script.ts verify interactivity tool-jobs --format json --output ...`

## Official checkpoint

The global gate is RED. The current census contains 50 production macro hosts, 776 macro rows, 648 literal registrations, 774 unique command rows, 16 factories, 3 dispatch sites, 161 admitted rows, and 724 remaining live registrations. All eight framework-reserved routes are accepted; 35 app-owned `import-media` routes remain fail-closed.

The official failure vector contains 157 rows: 101 exact proofs without matching truthful `Migrated` declarations, 19 `Migrated` declarations without an exact proof, 33 forged schema identities, and 4 aggregate framework/global blockers. The fourth blocker is the exact Draw ownership rejection; the other three are the process-global payload census, app-owned import routes, and remaining live registrations. Artifact member creation/open ownership is absent from the coordinator-owned run. This categorization is evidence for packet assignment, not permission for bulk metadata relabeling. The verifier currently executes 413 hostile/oracle self-tests.

## Remaining command registrations by owner

| Owner | Remaining |
| --- | ---: |
| Puzzle | 0 |
| Space | 70 |
| Lowpoly | 48 |
| Norm | 45 |
| CAD | 41 |
| Block | 39 |
| Shooting | 39 |
| Remodel | 37 |
| FEM | 37 |
| Note | 36 |
| Flow | 35 |
| Process | 33 |
| Forms | 27 |
| Procedural | 26 |
| Draw | 25 |
| Architect | 21 |
| Layout | 20 |
| Animate | 18 |
| Sequence | 17 |
| GIS | 17 |
| Raster | 16 |
| Sourcing | 15 |
| DAG | 13 |
| Imperative | 11 |
| Reasoning | 10 |
| VCS | 10 |
| Trinity | 9 |
| Mathematical | 7 |
| Writer | 2 |

Puzzle's static remaining count is zero, but the cohort is not accepted end to end. The latest source checkpoint classifies all 99 routes with command-specific persistent semantic cursors or an explicit constant-time proof: Puzzle2D 3/3, Puzzle3D 53/53, and Puzzle5D 43/43. Puzzle3D has 51 explicit cursor routes plus the bounded `addTargetVolume` and `openAddObjectDialog` routes; Puzzle5D has 43 explicit cursor routes; Puzzle2D has retained force/example cursors plus bounded `addNode`. The former 3D/5D precompute and cache routes now retain their scan, catalog, normalization, sum, build, cycle, suggestion, fill-count, brush-mesh, and tick cursors across grants instead of treating preparation as an atomic prefix. The language-neutral fixtures currently carry 25/107/72 vectors and 9/82/53 hostile mutations for 2D/3D/5D respectively, including maximum-plus-one, cancellation, fault, and repeated-close boundaries. `applyBoardEvents` retains byte/event decode, lookup, edge deletion, nested brush work, and nested close across grants, but per-event serde decode, camera whole-clone, growable owners, and missing runtime evidence remain explicit strictness risks. The root static verifier remains clean. The latest proven focused compile is the passing 89/99 tree after two attributable catalog type mismatches were repaired; a fresh 99/99 native compile and the complete native/Wasm/timing/cancellation/replay/lifecycle matrix are still pending. The verifier must reject complex wrappers before this row can be treated as final reactivity evidence.

## Framework/global blockers

The four remaining non-catalog blockers are the exact Draw semantic rejection, process-global payload ownership, 35 app-owned import routes, and the aggregate 724-live-registration failure. The last serialized official artifact above honestly reports 41 constructs. The current worktree census reports 39 after `DRAW_SESSIONS`, `ACTIVE_DRAW_SESSIONS`, and the persisted `gesture_checkpoint_json` lane were replaced by a per-`VcsArtifactApp` `DrawInstanceOperationOwner` backed by `FixedOperationRegistry<DrawGestureOperationOwner, 64>`. This reduction is not yet accepted: root source audit found render-time whole-`DrawSession` cloning, whole `serde_json::from_slice` after the byte scan, and a one-grant generic `command.dispatch` wrapper. The gate must reject those shortcuts until preview uses a fixed immutable projection and each of the six gesture routes has command-specific bounded decode/reducer proof. No live payload is exempted as immutable. Live FEM2D is absent after the exact gate recognized the existing fixed visual worker, canvas snapshot pages, freshness validation, displaced/rejected leases, dirty-render consumer, one-face mesh helper, stiffness observation, snapshot-return witness, and hostile max/+1/cancel/stale/replay/sub-8-ms laws. Artifact-store structural disposal, ArtifactEnvelope ingress, peer interaction roots, universal retained ownership, the Draw `.spr`/`.ops` caller, and the representative Present/Writer/Jack/Trinity/GIS/Raster callers are also absent in the coordinator-owned run. The actor progress/preview overlay blocker is absent after fixed replay ownership, presenter ACK/abort, and hostile route/generation/worker validation were joined to the live path. Runtime instance/actor authority, member creation/open ownership, hard-bounded nested-payload close, paged command ingress, owned media export, and reactor task/request/open-instance close are absent after fixed authority recognition, bounded retained-field disposal, exact accepted Download ACK transfer, formatting-tolerant fixed-reserve validation, mounted one-pump media ownership, and executable real-registry close laws.

The shared typed-command packet's decode/admission, exact proof identity, framework-reserved routes, import pre-job preparation, full prepare/job/commit ownership, persistent foundation, immutable roots, child roots, paged ingress, retained worker turn, execution limits, commit freshness, and compiler-witness predicates are absent from this official failure vector.

## Expanded app universe

The focused app gate independently discovers 101 descriptor app contexts plus 68 launch-only product surfaces, including 11 Compose surfaces, for 169 total surfaces. The latest action join remains RED at 4,754 descriptor rows: 2,781 unclassified dispositions plus 1,372 migrated declarations without an accepted owner-local/shared production route, or 4,153 failures. The 68 launch-only products do not yet contribute an action ledger equivalent to the descriptor rows, so this command-registration ledger is not the final all-app acceptance matrix.
