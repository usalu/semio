# Interaction Direct Leaf Native Gate

## Current Scope

Exact filter: `local_interaction_mutation_leaf_descriptor_and_exact_codecs_are_owned`. No wider plugin or application gate is claimed.

R1 exited before Rust: Nx reported `Cannot find project '@semio-tech/framework-plugin-rs'`. The current on-disk plugin project is named `@semio-tech/framework-plugin`.

R2 used the current project and exited 1. Its redirected text log was absent when inspected, and the previously read R1 text log was also absent. No deletion, cleanup, relocation or restoration was performed by this lane. The R2 cause is unknown; no compile or runtime result is inferred.

R3 reruns the same exact filter with tool-captured output, to be retained below. Actor/plugin/Kernel/WIT production remains held with the owning agent until the dependency checkpoint.

## R3 Captured Result

R3 exited 1 before compiler: `error: the argument '--lib' cannot be used multiple times`. The current package router already appends its fixed `--lib`; the supplied selector must omit the duplicate flag. The actual command was `cargo test --manifest-path Cargo.toml --lib --lib local_interaction_mutation_leaf_descriptor_and_exact_codecs_are_owned -- --nocapture`. No Rust test ran.

R4 invokes the same current project and exact selector without the duplicate `--lib`; no router/source changes were made.
