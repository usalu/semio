# Current Renderer Full Green R185 and Strict R186

After the one test-only retained-controller correction, **R185 passes735/735 tests across all five files**,136.38s,start05:55:14,Nx0. R186 strict remains Nx1 with exactly the seven known tutorial diagnostics and no UI resident/paged/actor diagnostic.

```sh
bun x nx run @semio-tech/framework-renderer-react:test-long --skip-nx-cache --args=--run
bun x nx run @semio-tech/framework-renderer-react:typecheck --skip-nx-cache
```

The same selected107 files were captured before dispatch and after each terminal. Every selected hash matches in both runs. Complete raw outputs and manifests are `🧪️renderer-current-full-r185-2026-08-28.{txt,json}` and `🧪️renderer-current-strict-r186-2026-08-28.{txt,json}`. This is not an exhaustive dependency-tree or physical-memory certificate. Existing test budgets were unchanged; no retry for warnings/advisories.

The preceding actual RED is retained: R180 had734PASS/1FAIL because one authored composition assertion incorrectly expected shared-controller credit to disappear after per-pool retirement. R184 then independently exercised the focused correction (1PASS/734skip), deriving controller retention from the current canonical composition fixture and checking it remains through disposeAll. The exact +316-byte authored diff and reconstructed original source hash are in `📓️renderer-composition-r184-2026-08-28.md`. No runtime source, quota, price, release authority or native code changed.

Current UiDocumentStore SHA256:`100dae341b4112c50683105637c043a14c0149158366fc61bd1320343e34f435`. The earlier nineteen API-join migration remains covered by these actual735 tests. The seven strict tutorial errors remain open; no empty state/default or old selectionJson compatibility was introduced.

## Separate Scalar Proposal

The requested declaration is in `📓️renderer-scalar-decoder-reader-declaration-2026-08-28.md` and `📄️scalar-decoder-declaration/{🧬️contract,🧬️schema,🧪️fixture,🧪️schema}.json`. Its43 independent oracle vectors and fixed-record census are declaration-only. The proposed decoder, original reader receipt, extra payload/reader words, prices, cancellation driver and typed output are **not runtime-mounted**. No scalar implementation, genuine split-reader pass, source-page InputAck, native UI ACK, live renderer cutover or whole-instance final witness follows from R185.

All local test processes are terminal. Source is coherent for root's next review; no new imported/collected edits are in flight.

