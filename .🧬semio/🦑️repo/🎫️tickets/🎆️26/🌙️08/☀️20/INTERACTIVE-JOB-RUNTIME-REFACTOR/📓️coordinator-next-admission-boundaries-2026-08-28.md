# Next Admission Boundaries

## Review Scope

Coordinator review of the released contracts and owner notices; this file records no new runtime test. Existing independent records remain authoritative for their exact snapshots. No production source, quota, native command, output, or generation preimage was changed by this review.

## Per-Response Subset

The Demonstrator's current `📓️per-response-admission-plan-2026-08-28.md` is approved for schema-first implementation under the original return and its existing ledger. The original roster must receive its exact resident record before constructing any output Slot or facade. Identity-only shell matching is recovery evidence; live-shell matching gates forward construction. Parent-before-finalizer installation, first arbitrary-fault retention, separate child/observation grants, closing-prefix refusal, and lost-return recovery remain mandatory.

The declared actor model is logical accounting, not measured heap usage:

| Subset | Bytes | Slots / Owners | Boundary |
| --- | ---: | ---: | --- |
| Enclosing return | 1360 | 13 / 13 | Includes the roster's five new fixed admission fields. |
| Per-output domain | 448 | 3 / 3 | Slot272, facade80, reserved outcome96. |
| Per-output including record and cell | 1008 | 12 / 12 | Domain448 + record264 + cell296. |
| Pending source census | 608 | Unadmitted | Domain448 + PendingEntry160; the earlier640 proposal was arithmetic error. |

The facade's required `reserveResponse(grant)` prepares only its privately retained response slot. No arbitrary output/cell argument, callback, or existing command witness may authorize dispatch. Empty cancellation must preserve funded records, cells, aliases, and the counted roster entry; this subset provides no final refund or reuse witness.

Pending Promise/closures, raw backing4161, parser/projection1424, actual receiver admission, worker one-response credit, protocol-fault containment, and native input ACK remain separate prerequisites. The existing32MiB composition ceiling is unchanged. This is not permission to mount an interim whole-result transport.

The current report records genuine initial RED1PASS/2FAIL/172skip175 at03:04:38: a zero-item grant still allocated a Slot, and the AST retained the old seven-field roster. Implementation and caller cutover are in progress; no coherent new source release was claimed in that notice.

## UI Join During The Cutover

UI reports R77's exact17-grant builder-binding law GREEN1/693skip694 at03:06:19. Its later R78 stopped during return-fixture setup at `roster-held`, before the new quarantine law. R79 adds15 in-flight Shard response caller diagnostics to the prior41 plus one separately corrected UI test typing. These are delegated outcomes, not coordinator reruns.

UI must wait for the released return/response fixture, mandatory API and exact grants, then change its own fixture once. No widened grant, fallback reservation, or current-source full-green claim follows from the earlier binding law. Copied evidence, incremental page/reader consumption, raw source retirement, and final builder refund remain distinct.

## Native Parent And Store Repair

Standalone native resident17 remains first in the sole-executor queue after taxonomy releases the actual loader repair. Its previous attempt stopped before Cargo, so it has no executed17-test result. OS-kernel's six staged owner-crate laws also await their registered budgeted route and source release.

The exact native Store repair is shared with Dag's actual RuntimeAppCell/Opening parent-admission work. A preallocated displaced-slot roster or postallocation `admitted_bytes` does not fund the new retirement shell. The original Store must hold the charged typed shell and exact slot before taking a backbone; original descriptor, generation, revision, payload and unsent detach request survive refusal/unwind. The same private native resident family must provide the parent transfer; no second pool, structural callback, ignored returned owner, or observed byte-count authority.

Retained owns only the released Store codec/retained-backbone surface and actual Directory import/Send diagnostics. Mutation's FreshField/FreshVcs/decoder-registry/envelope grammar remains excluded. Dag and Retained are coordinating the common parent contract before any production change.

The reviewed OS-kernel metadata proposal uses a separate native route with the existing level/budget helper and fixed `--lib --features sync,ureq`, preserving the old route/default features. Three lazy zero-argument domain source commands provide independent schema/reference checks. Initial compilation may stop before the six semantic laws: import/Send repairs follow actual compiler evidence, and backbone behavior must reach actual RED before its implementation is changed.

## Restore Boundary

Coordinator independently executed the canonical source target:14 semantic cases and13 hostile checks, Nx0,19 selected files stable. See `📓️coordinator-canonical-restore-source-r1-2026-08-28.md`. Original12 case identities and expectations remain preserved; prior ticket58/current peer59 counts are separate historical packets.

The reviewed framework restore contract keeps ordinary typed command ingress/result ACK, a fresh five-field command base, and a content-only persistent direct leaf. Existing framework-command refusal, document-only Store preparation, and BTreeMap/retained-root representation are real missing joins. No new AppChannel/WIT transport, historical generation authority, cold runtime conversion, or private-state broadcast is authorized. Actual resident parent admission precedes this live implementation.

## Tests And Completion

No additional tests were run for this review. Native execution remains serialized, active evidence is preserved, and all phase/master tickets and the end-to-end goal remain open.
