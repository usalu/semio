# Active Ticket Evidence Disappearance —2026-08-27

## New Observations

Around16:47–16:50local the canonical full command census R4 returned its expected RED exit1 and printed a successful write of `📊️coordinator-full-command-census-r4-2026-08-27.json` inside this ticket. Immediate independent Bun file read returned ENOENT, and exact filename enumeration found no file. The write route uses `resolve(this.root, output)` then `writeFileSync` before throwing its failure list; the workspace root is the root script URL directory. No output relocation or cleanup appears in that route or its router.

Earlier in this same turn, the coordinator successfully read four native Drop RED logs, focused handback GREEN7 and full UI R7 RED16/1. A subsequent directory enumeration no longer contains those `🧪️` logs. Earlier mounted census R5 JSON is absent too. Current Markdown reports, source files, ticket manifest and cargo-target directory remain present; this is not a claim that every cache file remains intact.

The ticket manifest was independently read and says `status: open`, title `Interactive Job Runtime Refactor`, goal `🎯r2602🎯runningsketchpad`. The root cleanup protection source rejects any candidate intersecting a non-closed ticket. No cleaner was invoked as a diagnostic. A process snapshot found no running cleanup command; that is not historical attribution.

Root performed no deletion/relocation. All three fleet lanes and the taxonomy, demonstrator and mutation peers explicitly confirmed none. Peers also observed missing active-ticket evidence. No known recovery location exists and no sources were restored or overwritten. Repository source work continues.

## Evidence Still Retained

Exact command output for root OwnedScene4 and prior React/source gates is embedded in their coordinator Markdown reports. Handback4RED→7GREEN and fullUI16PASS/1FAIL/98notrun source/output review is in `📓️coordinator-ui-atomic-handback-r4-2026-08-27.md`. R4 full-census stderr/exit are retained in the coordinator tool capture; a separate stdout-derived fresh census will preserve machine-readable summary in Markdown without asserting that the missing JSON was recovered.

Read-only disk checks moved from15GiB to14GiB. The executor subsequently reports24GiB; that increase alone does not identify who or what removed evidence. No automatic cleanup, cache purge or permission change is authorized by this incident report.

## Permanent Input Repair and Further Read-Only Inspection

The missing ticket input also caused canonical source census R5 to fail before execution. The permanent verifier now joins the surviving native domain checkpoint fixture/schema rather than depending on ephemeral ticket evidence. Exact native fixture bytes were preserved; schema version and work-size constants were corrected against those known bytes and strict Ajv/DataView/Node Buffer tests. Independent source R22 passes1009 checks; full census R6 completes with the existing twelve failure entries and270remaining registrations. This repairs the input dependency and does not recover the disappeared file. See `📓️coordinator-source-r22-and-full-census-r6-2026-08-27.md`.

Taxonomy separately inspected root cleanup dispatch/protection, inventory shard publication, nx.json and three local Nx plugins. No unconditional cleanup appeared on the inspected import/discovery paths. Exact obsolete digest-child retirement beneath an output's shard directory cannot alone explain an absent publication root or unrelated checkpoint paths. That scoped inspection neither identifies a cause nor certifies uninspected plugins or external processes. Its source hashes are retained in the taxonomy ticket's `📓️evidence-loss-route-inspection/📝️.md`.

The mutation peer subsequently reports its fresh workflow integration controller script disappeared while its directory and two completed run directories remained. It separately attributed a transient missing kernel.rlib to its active compiler and explicitly did not classify that artifact as external loss. Root did not independently inspect that peer controller directory and records this as peer evidence, not a root observation. A later root read-only df reports45GiB available; this still establishes no actor or cause. Current native UI/Scene logs and new coordinator reports were successfully read/written at the later checkpoint. No cleanup, reconstruction or permission change followed.
