# UI Adoption of the Composition Resident Ledger

## Current Boundary

The neutral ledger's mandatory-cell protocol passed member R33 and coordinator R5. The UI pool, instance scope and first empty field-owned payload are joined. Member payload R32 proves actual captured-field admission and two-way source settlement before exact872-byte intrinsic retirement; scope R33 passes seven laws at its updated376-byte envelope. Builder/page/reader/evidence remain mid-cutover from independent counters. This report does not claim copied-payload storage adoption, native page acknowledgement, live renderer mounting or a physical heap bound.

The fixed UI metadata catalogue is staged at `ui/contract/retained/resident/metadata`. Its neutral fixture names each planned fixed record and field. Logical accounting is 16 bytes per record plus eight bytes per declared field, with one slot and owner per record. This is an explicit logical representation, not JavaScript object sizing or Rust `size_of`. Actual implementation must remain within the declared inventory; a changed fixed layout requires updating the catalogue and oracle. Intrinsic record overhead is an additional 264 bytes, three slots and three owners; each exact admission cell adds 296 bytes, six slots and six owners. Backing and registered intrinsic reader charges are separate again.

| Typed record | Domain bytes | Slots/owners | Explicit contents |
| --- | ---: | ---: | --- |
| Pool | 264 | 5 | Fixed state, facade, empty binding table, preadmitted terminal witness and parent-funded instance-transition slot |
| Instance scope | 376 | 6 | Fixed state, facade, exact lifetime value, binding entry, terminal witness and its own payload-transition slot |
| Payload | 312 | 4 | Fixed21-field state, facade, one phaseful witness and its own child-transition slot |
| Builder | 264 | 2 | Fixed private fields, record phase and terminal witness |
| Reader | 112 | 2 | Fixed private fields, record phase and terminal witness |
| Page wrapper | 232 | 4 | Fixed state, facade, builder list cell and terminal witness |
| Input evidence | 152 | 3 | Exact evidence state, facade and terminal witness |

The pool is not silently treated as an additional uncharged composition root. Its actual original composition must retain its pending construction slot before the pool shell is allocated. The shared ledger charges its metadata record first. The shell enters both that exact typed slot and the neutral strong record before fallible initialization/finalization. The original composition, not a caller-only factory, drives abandoned construction to terminal. The Demonstrator owns the ShardClient/composition registration seam; no structural callback or arbitrary `isRetired` predicate is proposed. The pool record capability stays solely in that Shard private slot, not in the public UI facade or an exposed preparation token.

## Planned Canonical UI API

The owning composition supplies its existing `OwnedResidentLedger`. No UI capacity object, per-operation pool, guessed default or fallback remains after cutover. The existing 24 UI fixture constructors and four actor fixture constructors must change coherently; actor fixtures remain their source owner's region.

The settled composition-only factory is `OwnedUiResidentPool.begin(client, ledger, grant) -> {step, pool}`. The client must already own that exact injected ledger through required `ShardClientOptions.residentLedger`; `ShardClient.matchesResidentLedger(client, ledger)` verifies its private identity. There is no actor-constructed or per-pool ledger. The UI exports `uiResidentMetadataEnvelope("pool")` for the peer to reserve the actual canonical metadata allowance.

The agreed peer methods are `prepareUiResidentPool(ledger, grant)`, `installUiResidentPool(pool, grant)`, `ownsUiResidentPool(pool)` and `releaseUiResidentPool(pool, witness, grant)`. Preparation owns the neutral record in the original private client slot before shell allocation. The UI forwards a consumed preparation step and returns pending; construction/install only starts on a later ready step that consumed no work. An existing or failed installed shell cannot authorize a second allocation. `OwnedUiResidentPool.matchesComposition(pool, client, ledger)` checks the actual private UI pair. The preadmitted private `OwnedUiResidentPoolRetirement` is the final release authority; the peer validates its exact client/ledger/pool identity before record detach and separate intrinsic refund. These are agreed API names, not yet a source-ready concrete join.

The retained pool now exposes grant-bearing `bindInstance(owner, activation, lifetime, grant)`. The next scope API is proposed as `beginPayload(actualPrivateField, grant)`, followed by grant-bearing payload page admission, each returning a retained admission result. Builder and reader admission likewise receive actual work grants. Each admission reserves the declared domain record, retains its pending construction state and installs the shell before exposing a facade. When admission takes several turns, the exact parent retains the pending slot rather than retrying an allocation from scratch.

The UI instance scope owns actual intrinsic storage registrations on the same ledger. Fixed page allocation uses `OwnedResidentOwner.reservePage(logicalLength, grant)` and pays the full 256-byte backing even for partial or empty logical extents. It never allocates a separate UI `Uint8Array`. A page's reader can only be created through an already strongly registered concrete consuming owner, which calls the intrinsic `beginRead`. The unchecked old UI `page.capture()` is removed rather than forwarded. A live older consumer may still read after producer close; the producer cannot retire its backing before that exact consumer closes.

## Exact Terminal Ordering

Every typed owner first closes its own reader/child registrations, clears source, parent and sibling links, and validates its private terminal witness. Only then may its privately retained neutral record call `detach(originalShell, grant)`. A separate granted turn closes the neutral record and observes its intrinsic retirement witness. Record capability and detach authority never appear on the public UI facade.

An actual detach followed by a throwing wrapper recovers through the record's preadmitted `detachment` observation against the exact original record and shell. It does not repeat mutation or allocate a replacement proof. A neutral retirement observation may retain only an already domain-empty facade; it certifies registration retirement, not physical collection. Parent completion waits for both the actual typed terminal proof and the intrinsic registration witness.

Child `blocked` and `rejected` outcomes remain visible. Child work at the full grant is returned unchanged; completion observation, record detachment and wrapper unlink/refund each have their own subsequent admitted phase. Zero or short grants never consume a completed child's wrapper obligations.

## Evidence Is a Separate Shared Lifetime

The actual source graph is `KernelReturnInputRelease.#proof → UI Evidence{fragment, field, builder}` and `Fragment.#field → Field.#state → original source, host and activation`. Clearing `builder.#copyProof` does not release that graph. Each copied/cancelled evidence object therefore has its own charged strong registration, rather than borrowing the builder's fixed record indefinitely.

Its terminal protocol needs both the original UI observation and the original source consumer's private detachment. Only after both can the evidence clear fragment/field/builder references and detach its exact record. This is distinct from the input-copy token, raw page InputAck and semantic publication ACK. The UI owns evidence construction and terminal validation; the peer owns source close/receipt detachment. A small identity facade cannot hide an immutable `InputOwner` graph after source close. No callback-shaped or boolean witness substitutes for the concrete pair.

## Verification Plan and Active Work

The metadata selector exercises seven neutral catalogue entries using strict Ajv, BigInt field accounting, Immer conservation and real `reserveRecord` admission/refusal on one ledger. It deliberately has no installed UI shells and is not the concrete consumer gate. R1 executed one failed test at the unimplemented catalogue; R2 passed one test with 658 skipped, 659 discovered, in 6.43 seconds. R4 additionally includes hostile unknown/prototype names and a non-coercing object rejection; it passed one test with 658 skipped in 4.83 seconds. Full logs are `🧪️renderer-shared-metadata-red-r1-2026-08-27.txt`, `🧪️renderer-shared-metadata-green-r2-2026-08-27.txt` and `🧪️renderer-shared-metadata-green-r4-2026-08-27.txt`.

Canonical strict R3 exited one with exactly seven existing tutorial diagnostics and zero metadata/UI-resident diagnostics, retained in `🧪️renderer-shared-metadata-strict-r3-2026-08-27.txt`. This is not a zero-error whole-renderer claim. The static catalogue is now source-coherent; actual pool/child/evidence constructor adoption remains in progress.

Concrete adoption must additionally execute constructor/finalization and caller-loss recovery at pool/scope/payload/builder/reader/page/evidence boundaries; premature/foreign/replayed domain-witness rejection; actual install/detach-then-wrapper-throw recovery; independent old readers; source/UI evidence handoff; simultaneous pre-Open raw response, destination pages and scratch on one composition ledger; all three capacity/control axes; and bounded teardown without local counter refunds. The existing paged source/reader/fault/continuation tests remain required during cutover.
# R27 Neutral Cell and UI Metadata Follow-Through, 2026-08-28

The canonical neutral five-resource signatures now require a preheld privately branded admission cell; exact APIs, updated resource charges, first-fault retention and separate terminal alias detachment are in `📓️shared-resident-admission-cell-and-fault-contract-2026-08-28.md`. No old nullable/return-only overload remains.

The UI metadata oracle and its neutral fixture were migrated to actual cell bootstrap/claim and typed `cell.result.record` recovery. Its exact capacity includes each schema-owned UI envelope plus 264 bytes / 3 slots / 3 owners of intrinsic registration and 296 / 6 / 6 of cell custody. It compares the cell envelope against the canonical neutral contract, uses strict Ajv and BigInt field inventory checks, and uses Immer against actual ledger usage. Exhaustion now refuses the next real bootstrap, not an uncharged old record call.

Actual `@semio-tech/framework-renderer-react:test-long --args='--run -t OwnedResidentMetadata'` R7 exited 0: **1 passed / 661 skipped / 662 discovered**, five files (one selected), 4.21 seconds, start 00:27:49. Full stdout: `🧪️renderer-shared-cell-metadata-r7-2026-08-28.txt`. This is metadata/cell accounting only, not an actual UI child-construction or full-renderer checkpoint.

Remaining source boundaries are explicit: the UI Pool's instance/payload/page methods are still mid-cutover from the old local counters, and old test constructors await the new composition fixture. Pool construction currently creates its WeakMap before the canonical parent shell installation; a genuine before/after-install constructor-fault law and moving that field initialization after installation are queued once the actor's required-cell preparation driver is coherent. The actor driver and original private record/cell capability stay peer-owned; no UI record getter, default pool, empty participant, or structural release callback is introduced.

## R8–R10: Actual Pool Shell Construction and Fault Retention

The R27 pending WeakMap boundary above is now exercised and repaired. Neutral `poolConstructionFaults` names three frontiers: binding-table allocation, terminal-witness finalization, and pool finalization. R8 ran `@semio-tech/framework-renderer-react:test-long --args='--run -t OwnedResidentConstruction'` and failed one test: the actual WeakMap constructor threw before `ShardClient.installUiResidentPool` could retain the original shell. Its output is `🧪️renderer-pool-construction-red-r8-2026-08-28.txt`.

The constructor now initializes the minimal private state with `bindings:null`, installs that exact shell through the actual composition-owned record, then allocates the WeakMap and finalizes witness/facade. The original pool retains the exact arbitrary thrown root. The factory catch calls the released actual `captureUiResidentPoolFault(error)` before returning rejection, so the composition does not depend on the failing caller to keep the error. Later granted parent close hands the same root into its original cell; no refund or final UI witness is issued while the fault remains. The pool's field count and 192-byte envelope are unchanged. Binding storage is cleared on healthy terminal close and included in the terminal emptiness check.

R9 actual GREEN: one selected test, 662 skipped, 663 discovered, five files, 4.16 seconds, start 00:42:56. All three frontiers executed. Original shell ownership, exact error identity, 63-byte refusal, separate 64-byte fault handoff, bounded subsequent close, absence of retirement, and unchanged charged usage are checked through the real Shard parent. Output: `🧪️renderer-pool-construction-green-r9-2026-08-28.txt`.

The existing composition test now consumes the source-owned actor fixture's explicit preparation/release phases and dynamic total instead of the stale 256-byte one-step record assumption. R10 actual GREEN: one selected test, 662 skipped, 663 discovered, 3.73 seconds, start 00:44:10. Output: `🧪️renderer-pool-composition-r10-2026-08-28.txt`. Its private witness, foreign rejection, exact multi-phase release and Immer conservation remain exercised. Actor controller accounting is being separated into its own still-funded record by the peer; this R10 result is historical to the captured existing controller layout and does not certify that future split.

These focused runs do not certify whole-renderer typecheck or the unfinished instance/payload/page/builder/evidence migration. No constructor compatibility path, duplicated ledger, or source/publication acknowledgement was added.

## Child Registration Frontier: Parent-Funded Fixed Slot

The peer's controller-record correction applies equally to UI children: putting a child's admission/record/close controller inside that same child's domain envelope and then refunding the domain record would release its control funding too early. The next typed child implementation therefore needs a fixed, precharged parent slot that survives child record/cell retirement. It cannot just copy the old local counters into a `reserveRecord` wrapper.

The intended parent slot strongly stores the exact pending request owner, neutral admission cell, record, typed shell and phase before any fallible construction/handoff. Bootstrap, cell observation, claim, claim observation, record admission and record observation remain separate granted turns. An admitted typed child enters the original parent's intrusive roster before field/finalizer work. Its domain record funds its concrete state, facade, lifetime/binding entry and witness while live. No caller-selected record or structural terminal predicate is exposed.

For retirement, one fixed parent slot first takes the exact child record/cell/witness before the now-domain-empty child clears its record/parent/sibling references. The parent then unlinks that exact child and drives record detachment, intrinsic close, 8-byte result-alias detach, cell close and final observation on separate grants. This fixed slot remains funded by the parent throughout; it is not refunded with the child's domain record. A failed handoff leaves the original child rostered. A distinct fault stays in the original still-funded parent/child admission custody; it cannot be replaced by a string or charged to an already-refunded record.

Only one such admission/retirement slot is active per parent. Concurrent live children are not limited to one: their charged intrusive entries remain independently owned. Slot contention is bounded backpressure, not a whole-roster scan, a per-operation ledger, or a reduced document/domain limit. External exact-child close must use the same private driver and slot as parent close, so an app-owned scope can finish without waiting on an unserviced unrelated global pool scan. Page readers must register their concrete consuming owner before exposure; the old unchecked capture entry will not survive this cutover.

This is the next source design boundary, not implemented behavior. The pool catalogue must explicitly add this fixed slot before its allocation, and the peer's dynamic `uiResidentMetadataEnvelope("pool")` must consume the changed charge. The current 192-byte pool envelope covers the currently implemented shell only; it is not authorization to allocate the future child controller for free.

### Staged Slot Contract and Exact Limits

The staged schema/contract/neutral oracle now live under `ui/contract/retained/resident/📨️slot`. No production slot has been allocated, and the public pool envelope is still 192/4/4. The proposed extra fixed record is 16 logical header bytes plus seven 8-byte fields: `requestOwner` exact object reference, `cell` exact neutral admission reference, `record` exact neutral record reference, `entry` private roster-entry reference, `phase` finite enum word, `failure` first exact root or private sentinel, and `witness` exact private domain witness reference. Total: 72 bytes, one slot, one owner; proposed pool total 264/5/5. These are declared logical widths, not JS object-size claims.

An empty slot has null request/cell/record/entry/witness, its private no-fault sentinel, and phase empty. Admission stores the original request before invoking canonical bootstrap; a lost result is recovered through the original pending admission identity. It cannot replace a foreign busy request. Successful construction transfers the exact cell/record into the already charged child roster entry; only a later exact observation clears the slot. Retirement reverses that handoff before unlink or refund. Cancellation before a record exists closes the original cell; after record admission it closes the original unused record; after shell installation it drives that original shell and descendants first. No cancelled caller must remain alive to service these transitions.

The slot is never reused while it holds an unretired fault. Other already admitted children remain strongly present in their charged entries; retained read aliases remain valid and intrinsic child cleanup can still be serviced. New admission or final registration handoff may remain blocked behind that explicit held fault. The contract does **not** promise that an arbitrary fault can be retired, nor that another final registration release can bypass an occupied fault slot. Complete parent retirement remains unavailable until all such exact obligations can terminate. This is explicit bounded backpressure, not loss of an admitted child or a successful empty close.

The nine-event neutral trace admits and registers two independent live children, captures the first into the parent slot before its domain unlink, observes its refund before slot reuse, then faults during the second child's retirement while retaining both slot and roster entry. Its cold oracle uses strict Ajv, BigInt inventory arithmetic and Immer immutable transitions. The model does not execute a future runtime child driver or prove the listed twelve runtime laws; those remain the next TDD gate after envelope coordination.

Actual model R11: `@semio-tech/framework-renderer-react:test-long --args='--run -t OwnedResidentSlotContract'` exited 0, one selected test / 663 skipped / 664 discovered, 7.14 seconds, start 00:49:49. Full output is `🧪️renderer-parent-slot-contract-r11-2026-08-28.txt`. Production slot allocation and the pool264 envelope are still unmounted; this is the schema/model release only.

### First Actual Scope Admission R12–R14

The subsequent implementation mounts the exact seven-field pool slot and changes the public pool envelope to **264/5/5** together. The existing instance envelope remains304/5/5, with its actual twenty state fields now named in the metadata oracle. This supersedes the preceding unmounted R11 status, not its historical evidence.

The mandatory callable is `pool.bindInstance(owner, activation, lifetime, grant): {step, scope}`. For one empty scope, the fixture-linked phase grants are296,64,64,64,264,64,368,64,128 bytes. Only the final phase returns the privately owned scope. Earlier phases return null; a repeated successful bind returns the exact same scope with ready/0. The instance contributes864/14/14 including its neutral record and admission cell. Closing captures the original record/cell into the parent-funded slot before domain unlink, then observes domain proof, exact detach, intrinsic record retirement, alias detach, cell retirement and final slot clearance separately. `scope.closeStep` and pool close drive the same original slot.

R12 was actual RED: zero-grant bind reached the removed local capacity counter (`pool.capacity.maxOwners`) instead of refusing before mutation. R13 was actual RED after implementation: the child claim returned ready/64 before the wrapper's observation/publication. R14 fixes the admission-only forwarding to pending while preserving blocked/rejected and child work accounting. Canonical `test-long --args='--run -t OwnedResidentScope'` exited0, **1 passed /664 skipped /665 discovered**, 6.44 seconds, start01:00:43. Full logs are `🧪️renderer-scope-slot-red-r12-2026-08-28.txt`, `🧪️renderer-scope-slot-r13-2026-08-28.txt`, and `🧪️renderer-scope-slot-r14-2026-08-28.txt`.

The executed law uses actual Shard preparation, real native-lifetime authority, zero-grant conservation, all nine phases, Immer exact-ledger accounting, stable repeated binding, and complete empty-scope registration retirement. It does **not yet** prove concurrent children, cancellation at every prefix, after-install/handoff faults, or the unfinished payload/page/builder migration. Those old payload methods still reference removed local counters and are not a released API. Whole strict/React remains an explicit in-progress cutover.

### Scope Fault, Cancellation, Concurrency and No-Cell R15–R20

R15 added all ten cancellation prefixes and seven actual constructor/handoff fault frontiers. It executed two passing tests and one failing test: after a real `WeakMap.set` completed and its wrapper threw, the repeated bind looked up the half-published scope before checking its private fault/phase and incorrectly returned ready. R16 changed repeated-bind authority to the exact private scope state (`live`, no closing or first fault, healthy original cell), and all three selected tests passed. Original arbitrary faults transfer unchanged to the exact admission cell; cancelled/failed shells remain owned. The lower record can close unused storage when installation never happened, while fault-held cells and the parent slot remain charged.

R17's added concurrency fixture failed before scope behavior because Shard correctly permits only one instance per activation. The fixture was corrected to open a second real actor activation in the **same original client and ledger**, preserving the native invariant. R18 then passed all four prior/concurrent tests, but reproduced the coordinator's concrete no-cell close defect: canonical admission rejected an already-closing ledger without a cell, and the UI remained in `preparing` indefinitely. The repair records `bootstrap-rejected` from that actual return. A later64-byte observation checks only the original pending consumer: it retains any original cell or clears the known no-cell slot. Unknown throwing returns remain fault-held; null alone is never treated as a successful cancellation proof.

R19 canonical `test-long --args='--run -t OwnedResidentScope'` exited0, **5 passed /664 skipped /669 discovered**,8.95 seconds,start01:07:14. It covers real native concurrent scopes and slot contention, exact864-per-scope conservation/refund, ten cancellation prefixes, seven real faults (before/after record install, scope finalizer, before/after owner attachment, before/after binding insertion), and actual closing-ledger/capacity no-cell outcomes without consuming an unrelated neutral record. Capacity denial is the canonical blocked result, not rejected. Logs R15–R19 remain alongside R14.

R20 canonical `test-long --args='--run -t OwnedResidentMetadata'` exited0, **1 passed /668 skipped /669 discovered**,6.24 seconds,start01:07:25. Strict Ajv, BigInt field inventory and Immer actual neutral reservations validate pool264 and the current seven declared envelopes. Full output is `🧪️renderer-scope-metadata-r20-2026-08-28.txt`. This metadata census is not a claim that the still-old payload/page/builder implementation has adopted those envelopes.

The next composition safety join is a peer-owned direct private `ShardClient.matchesActivation(client, activation)`; current scope admission verifies genuine UI owner/lifetime and live activation but does not yet reject a genuine foreign-client activation. No structural fallback is allowed. Further payload admission needs its own parent-funded slot and actual metadata declaration; the pool's72 bytes do not fund descendant controllers. Whole strict and full actor/React remain intentionally unreleased during this migration.

### Next Payload Admission Design (Not Source-Released)

The next mandatory payload request should be keyed by the actual private `OwnedKernelReturnInputField`, not an anonymous repeated `beginPayload()` call: `scope.beginPayload(field, grant): {step, payload}`. The field already binds the exact original host, activation and lifetime. Its identity lets a lost-return retry recover the original payload and prevents a different caller's field from adopting the pending slot. The scope retains a charged exact field-to-payload registration; no caller-selected arbitrary request object, cold copied document or new field mint is admitted. A second independent field remains independently admissible. A second builder using another field's payload is a private-owner rejection, not a fallback association.

This is a proposed API decision, not a staged method or a claim that existing caller fixtures have migrated. Existing production callsites are not mounted; current calls are resident/paged/actor tests. A parent-owned field reservation/association may be preferable if the source already exposes a private exact UI-payload binding; the peer must confirm before adding such a source handshake. Neither side may invent public structural attachment authority.

The instance must fund its own child slot before allocating it. The unused `childObserved` word can become an exact pending-slot reference, but the actual72-byte child slot is additional to the current304 envelope. A field binding table and per-payload entry, if used for constant-time retry lookup, also need explicit catalogue records; do not hide them inside the72-byte slot or claim a whole roster scan is bounded. An alternative is source-owned private payload association with exact install/read authority, preserving a one-slot parent transition and eliminating a duplicate UI mapping. Both require a concrete privately checked construction handoff before the payload escapes.

Payload metadata must enumerate its actual cell/record/witness, parent slot reference, first fault and future child-admission slot. Builder, page, reader and two-party evidence will each retain their own original typed registrations and neutral admissions until their descendants and fault roots are discharged. The next source packet must update constructor grants and fixture arithmetic with these exact fields; current232/3/3 is not authorization to add controller records for free.

The proposed source-owned association is now represented in unmounted `resident/📦️payload/{🧬️schema,🧬️contract,🧪️fixture,🧪️schema}.json`. With the existing unused instance word reused as its pending-slot pointer, the instance envelope becomes376/6/6 (304 plus its own72). The proposed payload has21 actual state words, one-field facade, two-field witness and its own seven-field child slot:312/4/4, plus neutral296/6/6 and264/3/3 =872/13/13. This includes current builder/reader-reservation words until their concrete migration removes them; it does not pretend they already implement neutral reservations. None of these new prices is exported by the running metadata catalogue yet.

The source association needs a **dependent-body release proof**, not a false full-retirement witness while the payload still points to its original field. Proposed UI authority: `OwnedUiResidentPayload.matchesField(payload, field)` for construction; a privately minted `OwnedUiResidentPayloadSourceRelease.matches(proof, payload, field)` only after payload-owned bytes/builders/readers/evidence have truly retired. The proof stores only the original payload and phase; it validates the still-owned exact field through private payload state, avoiding an immutable proof that permanently retains the whole source graph. The source then detaches only its exact original payload binding and retains an idempotent private observation. UI observes that exact detachment in a later granted turn before clearing its field reference or moving to neutral record retirement. A historical observation need not confer current-read authority after the UI clears its field. Exact source method names and source record charges remain peer-owned and must be released before this handshake is mounted.

The pure declaration R21 canonical `test-long --args='--run -t OwnedResidentPayloadContract'` exited0,1 passed/669 skipped/670 discovered,4.96 seconds,start01:11:34. It executes strict Ajv and BigInt/Immer price composition only; no312-byte payload or376-byte instance was allocated by that test. Log: `🧪️renderer-payload-contract-r21-2026-08-28.txt`.

Diagnostic full strict R22 exited1 with67 diagnostics: seven existing tutorial joins, twenty old UI resident local-counter references, nineteen old UI pool-constructor fixture calls, nineteen actor diagnostics including the peer's intentionally missing activation matcher and staged caller joins, plus two new fixture-only implicit generic freeze parameters at UiDocumentStore845/888. The latter now have explicit generic `<T>(value:T):Readonly<T>` signatures, preserving actual runtime finalizer injection; that correction awaits the next strict run. Log: `🧪️renderer-scope-strict-r22-2026-08-28.txt`. This records the active migration honestly; no exclusion, compatibility constructor or replacement capacity counter was introduced.

### Admission Wrapper Fault and Composition Authority R23–R28

R23 reproduced a distinct fault path: a wrapper throwing before canonical bootstrap left the original fault in the funded parent slot, but close reported blocked while waiting for a nonexistent cell. R24 makes close inspect an existing first fault before normal success observation. It recovers only the original pending admission/result when present, hands off the exact error in a separate turn, and closes typed registrations without refunding the fault cell. With no admitted cell, it reports rejected while retaining the original fault in the parent slot. All six before/after bootstrap, claim and record-wrapper vectors execute; canonical scope R24 passed6/665 skipped/671,8.99 seconds,start01:15:12.

R25's foreign-composition instrumentation failed because the real private activation owns a frozen `assertActive` closure rather than a prototype method; it did not execute the target assertion. The corrected R26 law used two real Shards sharing the exact same ledger and numeric lifetime. It reproduced unauthorized296-byte admission from the foreign client (6 passed/1 failed). The source now checks the peer's direct private `ShardClient.matchesActivation(originalComposition, activation)` before owner matching, liveness calls or admission. Separate hostile Proxy reads remain zero. R27 canonical scope passed **7/665 skipped/672**,5.51 seconds,start01:17:34. Logs R23–R27 are retained, including the instrumentation error.

The phaseful payload witness declaration now explicitly describes one preadmitted two-field object (original payload plus finite phase). It serves source-body release and later private domain proof on distinct phases; there is no second uncharged proof object. Actual field association remains peer-unreleased and unmounted.

Full strict R28 exits1 with **46 diagnostics**: seven existing tutorial joins, twenty remaining old resident local-counter references and nineteen old UI pool-constructor fixtures. Actor diagnostics and both generic-finalizer fixture errors are absent. No new scope production/test diagnostic remains. Log: `🧪️renderer-scope-strict-r28-2026-08-28.txt`. This is a narrower active migration boundary, not a passing typecheck.

### Payload Private Declaration R29–R30

The source-owned association schema, contract, neutral fixture and fixture schema at Kernel return/content/input/payload were read completely. Final spelling is `OwnedUiResidentPayloadSourceRelease.matchesDetached(proof,payload)`. The source privately retains the original payload and same proof in its original observation until settlement. UI's `matchesSourceDetachment(payload,observation)` must identify the exact original observation still held in its parent slot. Settled source calls reject replay; a wrapper throwing after settlement is recovered by observing that same stored observation, not by replay or a newly minted proof.

R29 canonical `test-long --args='--run -t OwnedResidentPayloadBrands'` actually failed1/672 skipped/673 discovered,4.54 seconds,start01:22:53, because `matchesField` did not exist. The staged source now declares direct private-brand `matchesField`, `matchesScope`, `matchesSourceDetachment`, and the single two-field `OwnedUiResidentPayloadSourceRelease` with `matches`/`matchesDetached`. Reflection/prototype/Proxy identities cannot manufacture its private state. Its constructor is module-minted, stores the exact original witness before finalization, and has no public factory. No positive construction or phase transition has been mounted yet; all five APIs are rejection-only until real admission joins.

The payload's planned21-field state and seven-field child-slot type are now declared. The slot's phase-specific `witness` identity is temporarily `unknown` because the concrete source observation class is not yet exported; no public setter or structural observation constructor exists. It will become the exact source class union on source release. The existing old `beginPayload()` still reaches its removed local-counter boundary and is not a usable allocation path; no312-byte payload or376-byte scope is authorized by this declaration. Runtime metadata prices remain pool264/instance304/payload232. Full strict remains the last actual46-error cutover boundary, not a new passing claim.

The parent slot reuses `requestOwner` for the field, `entry` for payload state, and `witness` for original source observation until settlement. The payload's own witness field retains its original phaseful proof throughout. Only a later granted transition may replace the slot observation with the domain proof after exact source settlement. This declares no eighth reference and no uncharged second witness.

R30 canonical identical selector exited0:1 passed/672 skipped/673 discovered,7.13 seconds,start01:26:38. It executes structural, prototype and Proxy payload rejection; forged release rejection; reflective constructor rejection; and zero hostile getter calls. Full logs are `🧪️renderer-payload-brands-r29-2026-08-28.txt` and `🧪️renderer-payload-brands-r30-2026-08-28.txt`. This is declaration/mint rejection evidence only, not positive payload admission, terminal discharge or a whole strict/renderer run.

### Actual Field-Owned Payload Admission R31–R35

R31's real captured-field fixture reached the old removed local-counter access even for a zero grant, and failed1/673 skipped/674,3.81 seconds,start01:29:00. The replacement is mandatory `scope.beginPayload(field,grant): {step,payload}`. It checks exact private field/host/activation/lifetime and uses the original scope's precharged72-byte transition slot. Eleven fixture-declared grants are296,64,64,64,264,64,272,64,64,104,64. The272-byte turn constructs184 bytes of state plus24 of facade and performs64 of exact neutral installation. Source installation64 and its observation64 are separate. Only after the source binding is proven do72-byte child-slot and32-byte witness construction/finalization consume104; final publication64 is again separate. No work grant is reused for another child call.

The actual source observation is now `OwnedKernelReturnPayloadDetachment`, not private unknown storage. The parent holds field/entry/observation in the agreed seven slots. Close proves empty typed body, detaches the original source binding, observes that exact source object, clears the UI field, settles the same source object using the original phaseful witness, observes settlement, then replaces the observation with the same domain witness. Only afterward does it unlink the payload and drive its original neutral record/cell retirement. The neutral witness certifies intrinsic registration release, not physical facade collection or the independently retained raw source.

R32 actual GREEN1/673 skipped/674,4.77 seconds,start01:34:04, verifies all eleven grants, no premature public payload, exact field/scope identities, stable retry identity, Immer872/13/13 accounting and complete empty-payload registration refund only after source settlement. Logs R31/R32 are `🧪️renderer-payload-admission-r31-2026-08-28.txt` and `🧪️renderer-payload-admission-r32-2026-08-28.txt`.

The runtime metadata and neutral fixtures now agree on scope376/6/6 and payload312/4/4. Scope `firstChild.prepareBytes` is296,64,64,64,264,64,440,64,128; total scope936/15/15 includes its neutral record/cell. R33 scope regression passed7/667 skipped/674,5.30 seconds,start01:34:44. R34 exact metadata/Ajv/BigInt/Immer regression passed1/673 skipped/674,5.66 seconds,start01:34:45. Logs are `🧪️renderer-payload-scope-r33-2026-08-28.txt` and `🧪️renderer-payload-metadata-r34-2026-08-28.txt`.

Strict R35 actually exits1 with46 diagnostics: tutorial7, remaining builder/page local-counter16, old UI fixture constructors19 and four actor calls of the removed zero-argument payload signature. There are no new payload implementation diagnostics in that snapshot. Log: `🧪️renderer-payload-strict-r35-2026-08-28.txt`. Later cancellation/fault tests are a separate source boundary.

Cancellation without a source binding may bypass source settlement only from the retained `shell-installed` frontier, before UI has invoked source installation. It is carried as `source-never-installed`, not inferred solely from a negative association matcher. Once an install was attempted, an absent binding/observation remains an explicit ambiguous-source rejection. Clean source refusal after an attempted install is conservatively retained until a concrete source unbound proof exists; no negative structural result refunds it.

R36 `OwnedResidentPayload` actually passed5/671 skipped/676,3.64 seconds,start01:37:30. It includes the positive admission, all12 cancellation prefixes, four exact source-install/finalizer faults, forged private brands and schema/Immer arithmetic. Original faults transfer exactly once to their original charged admission cell; failed scopes do not complete or refund. Full output: `🧪️renderer-payload-cancel-fault-r36-2026-08-28.txt`.

R37 adds a real clean source refusal: the original field begins close after the payload shell was installed but before source installation. The source then rejects installation without accepting the payload. Actual test result is RED1/676 skipped/677,3.91 seconds,start01:38:48, at `resident-payload-ambiguous-source`. It has not refunded or invented an unbound witness. The peer owns the schema-first extension of the existing source detach method to prove its original private unbound observation and then use the unchanged two-way settlement. Full output: `🧪️renderer-payload-refused-source-r37-2026-08-28.txt`.

R38 adopts the released same-method source proof: attempted source admission now calls canonical `detachResidentPayload` with the genuine same payload body witness. Only the source's private original unbound-or-exact-bound observation can accept that request; the UI does not infer success from a negative matcher. Observation, UI field clear, same-proof settlement and exact settlement observation remain unchanged. Canonical `OwnedResidentPayload` now passes **6/671 skipped/677**,4.51 seconds,start01:43:27. This includes the actual previously failing clean refused-source close and all prior five laws. Output: `🧪️renderer-payload-unbound-green-r38-2026-08-28.txt`. Neither this join nor the source's independently retained raw InputOwner is a source metadata-admission, raw-page ACK or live renderer certificate.

### Settled Field Admission R39–R40

R39 reproduced the coordinator's preallocation finding with the actual original source field after full payload retirement: repeated admission returned pending296, not rejected0. The source would reject a later installation, but the UI had already allocated a new admission. The neutral declaration now requires rejection before admission when that exact field owns its original detached or settled source observation. Runtime uses the source's direct private `OwnedKernelReturnPayloadDetachment.matchesOwner` against the existing observation; it does not infer retirement from an absent payload or fabricate a new source status. Never-invoked source installation remains a distinct retryable frontier.

R39 actual RED1/677 skipped/678,5.52 seconds,start01:50:22 is retained in `🧪️renderer-payload-settled-field-red-r39-2026-08-28.txt`. R40 full `OwnedResidentPayload` passes **7/671 skipped/678**,3.75 seconds,start01:50:55, including three exact same-field attempts with unchanged Immer ledger state and original observation identity. Output: `🧪️renderer-payload-settled-field-green-r40-2026-08-28.txt`.

The next concrete callables, actual proposed field inventories and source-evidence dependency are in `📓️renderer-builder-page-reader-ledger-plan-2026-08-28.md`. Those planned prices/APIs are not yet source-released. Existing old builder/page counters and fixture constructors remain the known migration boundary; no current full strict/renderer claim is made.
