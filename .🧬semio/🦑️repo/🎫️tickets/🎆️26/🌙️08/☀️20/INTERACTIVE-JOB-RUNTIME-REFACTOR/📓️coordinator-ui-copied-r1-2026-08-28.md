# Coordinator Copied R1

## Actual Independent Result

The existing registered React `test-long` route executed **5 passed / 730 skipped / 735**, one selected file and four skipped files, **3.77s**, start05:09:22, Nx0. No retry. Exact returned chunks and all before/after hashes are retained in [the output record](./🧪️coordinator-ui-copied-r1-output-2026-08-28.md). Color warnings and the Nx flaky advisory are preserved; neither is a reason for an automatic repeat.

Coordinator compared all92 complete selected path/hash rows before and after the run: both hash commands exit0,92rows each, byte-identical output. This extends prior ActiveInput88 with the four copied declaration/fixture files; it is not an exhaustive transitive dependency graph or whole-workspace atomicity proof. Both short source holds were explicitly released immediately after the postcapture.

## Review And Executed Laws

Read all four copied-domain JSON files completely, the five actual tests and shared producer/reader fixture, the changed producer source/evidence forwarding, and the entire R146–157 release section.

- Real257 bytes pass through256+1 destination windows; Node Buffer compares actual reader bytes with the original payload. Eleven-phase original Copied admission precedes its one source receipt. Every actual source byte and its64-byte observation occur on separate granted turns, then the source/UI/record handshakes retire the capsule before final range observation/EOF.
- Empty input allocates no destination page and still calls the actual source advance method once for zero-byte completion; no synthesized EOF.
- Eight normal/revoked cancellation prefixes keep the same Copied token and exact already-consumed0/1 count. No Cancelled substitute or synthetic progress is credited.
- Four child-result cases preserve blocked0, rejected4096, actual pending4096 and over-grant4097 outcomes. Independent observation is not charged to the child's already-used grant.
- Two before/after real source-advance wrapper faults retain the exact original exception and source state; fault-held records remain charged.

The source counters, private original field/proof association and separate parent observations—not textual phase names alone—support these laws. The neutral fixture is schema-checked with strict Ajv; Buffer and Immer validate bytes/accounting. This is the tested bounded range, not proof of arbitrary malformed-input or timing behavior.

## Remaining Work And Handoff

No raw input ACK, native next-page source mint, whole-input retirement, semantic PACK parser, live renderer, fresh guest output or eight-millisecond guarantee follows. Source lifetime and controller allocations remain separately owned. Root did not rerun strict; the agent's R15126-diagnostic result is delegated history.

UI is authorized to handcraft the nineteen old authored fixture joins against the released phased authorities while preserving all meaningful laws and charged unknown-fault cases. Current whole-buffer semantic decoder needs its own schema, same-ledger parser/output admission and true early-reader consumption; an accessor replacement is insufficient. The bootstrap purpose224 repricing is still declaration-only and cannot be silently assumed in those fixtures.

