# New Permanent Typed-Command Fixture

## Evidence Boundary

The included ticket input `🧪️shared-typed-command-full-operation-v1.json` is missing. A scoped filename search found historical reports, not an authoritative surviving fixture. No cleanup, deletion, restore, or reconstruction of unknown lost bytes was performed. The historical partial and foundation reports were read; their old acceptance statements are not current evidence.

This packet authors new permanent evidence at `plugin/🧵️retained-command/🧪️full-operation/🧪️fixture.json`, beside a strict Draft 2020-12 schema. Its `evidence` field explicitly distinguishes it from recovered ticket data. Only the exact missing include was redirected; existing native owned/serde oracle bodies and their forty-row assertion remain unchanged.

## Declared Laws

| Family | Rows | Required boundary |
|---|---:|---|
| Output census | 7 | Empty/single/max/+1, Unicode max/+1, escaped semantic bytes |
| Grant | 4 | Zero fuel, expired deadline, cancellation, one unit; all six phases |
| Freshness | 3 | Exact, document revision, generation reuse |
| Admission | 4 | Available/full slot, missing/excess roots |
| Publication | 3 | Exact ACK, retained retry limit, exhausted retry |
| Lane trace | 9 | Every declared lane, sequential exact receipt ACK |
| Raw pages | 3 | Exact pages, overrun, incomplete prefix with owner return |
| Fault | 3 | Empty, ASCII cap/+1, multibyte boundary |
| Close | 4 | Insufficient/exact scalar byte grant, zero owner grant, repeated terminal |
| Total | 40 | Existing exact count preserved |

The older partial report described four output cases. The current native test requires thirty-three non-output rows plus forty total, so the new fixture explicitly supplies seven output rows. This is not a claim that the missing file contained these exact seven inputs.

## Verification Status

The prior Plugin R4 compile failure proves the missing include blocked compilation. New native execution is pending the coherent Kernel/WIT source boundary and the compiler owner's queue. Required existing selectors are `language_neutral_empty_single_max_and_plus_one_match_the_test_only_oracle` and `every_language_neutral_hostile_row_executes_the_owned_state_machine_and_serde_oracle`; the latter also executes real bounded fault truncation. The serde oracle is test-only and introduces no runtime dependency. No native or runtime pass is claimed by this new source checkpoint.
