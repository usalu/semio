# Coordinator Shared Decode Reuse Boundary — 2026-08-27

The coordinator fully read the Mutation owner's [partial-decode proposal](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/🧪️flow-decode-ownership-45/📓️partial-decode-proposal.md).

Its 117 research checks are not native decoding or cleanup execution. The substantive gap is ownership of completed fields and collection prefixes when a later field, semantic conversion, outer container or trailing token rejects. Complete-mutation retirement cannot retroactively own a value that never became a mutation. A synchronous seed alone is not a resumable parser. Duplicate field occurrences must not be erased by a map before validation.

The owner's subsequent source inventory identified existing Store authorities. The agreed direction is to audit and reuse them before proposing any new decode API:

- OwnedSchemaRecordCursor at Store component.rs around5402.
- ArtifactEnvelopeMutationFieldAuthority/Target around5813/5826.
- ArtifactEnvelopeOwnedFieldCatalog around5840.
- ArtifactEnvelopeDecodeAuthority around7421.

The coordinator located these declarations in the actual Store source; Dag explicitly confirmed no active/reserved edits to those decoder/catalog regions. Mutation may research their typed-field adoption and prepare a concrete reuse packet. Shared production changes must be announced before mounting so in-flight native inputs remain attributable.

Dag retains PagedAppCommandDecodeCursor/DecodedAppCommandOwner in SPR channel component.rs around1184, PluginCommandIngress, the sixteen ordinary route-specific command decoders, and native Interaction root/update/Store publication. The Store envelope decoder/catalog is not under that reservation. Kernel canonical return framing is output-side and confers no input admission authority.

No parallel transaction API, global/TLS cleanup sink, unbounded cold drain, compatibility decoder, schema relaxation or borrowed command witness is approved. Exact field/catalog lifetime and full-frame EOF ownership still need executable native tests. This report is coordination/source review only; no compiler, runtime source mutation, cleanup or publication was performed.

## Fresh Decoder Error and Unwind Follow-Through

The coordinator read the peer's fresh-field review and actual FreshFieldDecoder source. Its active.take followed by fallible accept/publish, unexpected completion, and partial final assembly must not detach original authorities before later checks can fail. The separate FreshVcs path has the same issue for active history authorities. Mutation additionally found a close-time if-let Snapshot over active.take that discards a nonmatching history arm, plus registry mutex poisoning on callback unwind; these latest findings remain source research, not executed semantic failures.

Root approved a bounded true-TDD repair of the two existing fresh decoder implementations and their tests. Every owned arm, ordinary refusal, false/unexpected RecordComplete, required-slot preflight and callback unwind must preserve structural serviceability. Restoring a local only on Err or borrowing in place while the enclosing mutex remains poisoned is insufficient. No competing Dag reservation covers these two implementations. Shared command/Interaction/catalog changes beyond this exact scope still require a concrete coordination boundary.

## Separate Grammar Packet

Mutation's explicitly loaned Demonstrator target compiled OS-kernel in107.863s and executed four grammar tests:0PASS/4FAIL. Two actual semantic REDs prove escaped-equivalent ASCII keys reject and outer/nested trailing commas complete. Two other failures were test-page setup against the existing4096-byte admission, not production defects. Root did not run that compiler.

The subsequent frozen grammar-only source is Store SHA0ed0d7a78c833c1081825c598de3a5dde36ecc858a2e1448c5695899358efd0d: shared string_token_equals plus outer/nested KeyAfterComma transitions. The peer reports212 actual reference checks; native GREEN remains pending. FreshField/FreshVcs production changes have not been mounted in this packet. No new source authority, ownership cleanup or Plugin/guest acceptance is inferred from grammar success.

The peer subsequently executed the corrected four native grammar laws:4/4PASS after63.768s compile with captured Store source stable. This is peer-run grammar acceptance only; root has not independently replayed that target. The lent compiler slot was released.

Root also confirmed no overlap for the existing ArtifactEnvelopeFieldDecoderRegistry try_admit/with_owner/take_returned_ticket poison follow-through. Bounded recovery must retain original owners and quarantine partially mutated state from admission/publication unless invariants are actually revalidated. Blind clear_poison and permanent Contended are both insufficient. That repair remains test-first future source, not part of the grammar result.
# 2026-08-27 Registry Recovery Decision

Root read the complete Mutation owned-field packet, including real registry callsites and the by-value consuming-callback limitation. The approved existing-family implementation uses a private Normal versus Close classification. Poison quarantines admission/accept/finish; only actual validated bounded close/witness or exact returned-ticket detachment may proceed. In-flight returned mask/generation pairs are inconclusive and retryable, not repaired or treated as permanent corruption. Invalid saved bookkeeping retains every original owner. Normal work resumes only after poisoned owners are detached into close-only ownership and the registry is empty and revalidated.

The exact proposed Store child test module and FreshField/FreshVcs/registry cohort are approved for native-first work after the current seventeen Plugin fixture repairs release. The two rejected/unadmitted page-close byte-grant observations need their own actual zero/short-byte laws before correction. No command/Interaction/catalog overlap, new registry family, blind clear_poison, unconditional disposal, or native credit from the 1732 reference checks is authorized.
