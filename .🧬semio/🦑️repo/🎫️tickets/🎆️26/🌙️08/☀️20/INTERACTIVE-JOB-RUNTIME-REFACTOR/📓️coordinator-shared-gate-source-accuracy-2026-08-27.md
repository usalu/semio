# Shared Gate Source Accuracy

## Confirmed Stale Source Checks

The current full census is correctly red overall, but not every individual diagnostic proves a current production defect. The coordinator inspected the actual guard and production bodies after the R2 census.

- `toolJobPublicationFreshnessBeforeEveryTurn` searches for an asynchronous publisher. The current `publish_mounted_typed_operation_unit` is synchronous, and its native source-anchored law deliberately rejects awaits. The guard therefore cannot see its existing full revision/generation validation before pending or newly begun publication.
- The retained branch in `toolJobFullOperationBounded` requires `.last().cloned()`. Current publication transfers owned items with `.pop()`, and the native production-anchor law forbids clone-then-pop for artifact, config, draft, Presence, and Transient mutations. The source gate currently rewards a removed unsafe ownership pattern.
- Several dispatch checks require all worker construction and capture to be inside `dispatch_typed_command_inner`. Latest-wins admission now calls the separately named `start_typed_command_operation`; the source checker must resolve this exact binding rather than treat any same-named helper or unrelated token as proof.

The publication executor owns a narrow guard repair after the current runtime checkpoint. Each extraction must retain hostile mutation coverage for missing authority, freshness after a resumed advance, replacement of owned transfer with cloning, missing worker context, and removed cancellation/terminal checks. This is not permission to force the global pipeline gate true.

## Genuine Uncompleted Boundaries

The generic retained-command constructor still reserves maximum raw storage up front; decoding and whole-owner close paths remain separate unbounded work. ArtifactRetainedCommandJob's domain-specific command, snapshot, config, interaction, context, and emitted owners still require exact retirement contracts rather than one-item whole drops. Child-group publication remains deliberately fail-closed; native history/cursor observation is not mounted atomic group/log/undo proof.

The actual registered rebase test also fails the fixed cursor applied/revision capacity gate. The executor located cold `sync_cursor` cloning the applied vector into an allocation with no pre-admitted spare capacity. The correction must preserve admission during cold reconstruction, never allocate inside the live publication step. Its native regression must rerun.

Presence local capture cancellation, shared roster final ownership, full tutorial local-state capture/restore, raw-wire sub-page close/capacity release, and the actual renderer whole-document patch path remain active implementation obligations. Native/Wasm/browser/performance evidence is required independently of these source checks.

## Evidence

The authoritative current census artifact is `📊️coordinator-full-command-census-r2-2026-08-27.json`. The coordinator read the guard functions in root `📜️script.ts`, mounted publisher/production-anchor tests in the plugin component, Store preflight and `sync_cursor`, and the retained-command component. No source-only analysis is reported as an all-app runtime pass.
