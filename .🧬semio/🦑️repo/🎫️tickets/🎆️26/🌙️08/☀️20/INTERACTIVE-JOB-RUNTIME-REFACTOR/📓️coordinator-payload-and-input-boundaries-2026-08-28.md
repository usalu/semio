# Coordinator Review: Payload Association And Native Input Boundaries

## Subsequent Concrete Review

Resident R6 now executes a real foreign refill schedule:13 cases run,12 pass and one fails with `accepted=true`, one consumer destructor during root release, and the original root terminal. The coordinator read the complete report, raw output and exact test. This confirms the public `Arc<Mutex<Option<C>>>` surface is structurally invalid: an empty observation cannot prevent later mutation before the final erased owner is released. The replacement must use a privately issued, phase-qualified, non-owning consumer capability and an actual same-parent registered field receiver. Holding a lock across last-owner destruction, sampling strong counts or adding another emptiness predicate is not accepted.

The selected native plan uses explicit `Layout` backing and separates allocation, initialization, source transfer and final page release. The actual receiver may only be minted by the original admitted parent for its exact inline typed field; no public `&mut Option<C>`, arbitrary projection callback or same-capacity envelope establishes funding. The actual Plugin `RuntimeAppCell`/Opening registry and its first four Store owners remain the required construction join. A private resident-package type change is not whole constructor or callback-tail acceptance.

For WGPU metrics and input publication, the read-only census found nine completion-revision producers plus scene/input writers and frame, maintenance, BeginGpu and ACK readers. The proposed guard must cover the real per-operation write set, with unchanged fields modeled explicitly. A staged test that pauses between two independent scene/input producer calls was rejected before mount: the actual existing test and redraw path permit scene2/input7 as a valid committed state. Accepted metrics and observed build input are different frontiers. Any new guard must be one allocation-free try-access held only across a fixed no-fail commit, never callbacks or unbounded work. Busy readers preserve exact installed owners; ACK reads before taking its witness. No torn-transaction native failure was claimed or executed from the rejected tuple test.

The coordinator read the pinned-native exact-layout plan and corrected the scope: the installed default Global allocator records exactly its requested Layout/capacity. No native default over-capacity event has been reproduced; the actual observed allocation fault is the first-access64B mutex allocation. Injected larger-layout contract cases must be labeled separately. Real exact-layout allocation-request observations, failure preservation and the public consumer's foreign-thread repopulation race remain the next native tests.

The current paged-builder source still has catch blocks that replace arbitrary exceptions with strings and old pool counter methods. Its linked PageCell wrappers and copied/cancelled Evidence/token objects are real metadata, independent of page bytes. The UI owner was asked to include exact first-fault custody and those records in its single-ledger child cutover. No builder success is inferred from the empty-payload14 gate.

The source evidence graph still retains release→UI token→fragment/field/builder alongside field/fragment release references. UI-local reference clearing cannot certify that graph detached or refund its envelope. Source and UI owners are settling one exact two-way evidence release, separately granted, before final source/evidence credit. No structural callback, copied receipt alone or boolean terminal surrogate was approved.

### Current Evidence Foundation And Remaining Direct Builder Alias

The coordinator read the complete source foundation at inputfcdb0a7b and the actual UI predicates. The peer's full actor gate is155PASS/4FAIL159; its three neutral/AST/private-refusal groups pass, while four callers still require phased builder admission. UI's exact `matchesRelease` and `matchesSourceDetached` declarations are independently included in the coordinator's4-law UI gate. No positive admitted evidence capsule or source detach/settle execution is inferred from those negative laws.

The source now keeps the original Field/Fragment release slots after semantic consumption. The single Release clears its proof/fragment backlinks only during source detach; source slots clear only after the UI proves its variable evidence dependencies detached. UI keeps that same original release through its own settled observation. Release4fields48 bytes and Fragment6fields64 bytes are unadmitted source inventories, not implicit UI allowance.

One additional concrete alias remains: `OwnedKernelReturnInputField.#builder` is assigned by `bind` and has no clearing path in this source. Completing the evidence handshake does not remove this direct source-to-builder reference. Both owners were told to keep final builder metadata/refund blocked until a separately verified close-only source-binding detach, preferably reusing the original preadmitted BuilderWitness. A public unbind boolean, local builder `field=null`, or brand-only terminal result cannot authorize that refund. This is a source finding, not an executed new native/runtime failure.


## Source Payload Contract

Root read the complete four Kernel input/payload JSON declarations and the peer's complete field-owned payload report. The agreed source subset is twelve Field fields plus one three-field observation, 152 logical bytes / two slots / two owners. This is not transitive resident admission, raw-response custody or a page-input ACK.

The settled private API uses `SourceRelease.matchesDetached(proof, payload)`. Mutation replay after settlement rejects. Recovery after a wrapper throws observes the same privately held observation; it does not rebind the field or retain a second replay proof.

The existing seven-field UI parent slot retains the original field in `requestOwner`, payload in `entry`, and source observation in `witness` through source settlement. The payload retains its one phaseful two-field UI proof. Only a later granted observation may replace the slot's source observation with the private UI domain retirement witness. Source observation payload/proof references must already be cleared; no ABA, early overwrite or brand-only settled proof.

Closing permits only exact detach/settle work, not new input or installation. Field completion, builder copying and source alias settlement remain separate from whole-response retirement.

## Concrete UI Declaration Release

Root directly read the actual five UI private predicates and SourceRelease class. UI reports actual missing-method RED then one selected negative test PASS (672 unselected, 673 total) at 01:26:38. This is delegated runtime evidence, not an independent root test. The tests cover forged/prototype/proxy/reflected-mint rejection without getter calls. No positive payload admission or source phase transition was credited.

The exact exports were relayed to the Demonstrator source owner. The subsequent concrete Field/Observation release reports eight selected PASS and four remaining old payload-counter failures; positive admission remains UI-owned work.

Original Field/Observation/Fragment parent installation before finalizers and the InputOwner first arbitrary fault are approved within existing source ownership. The separate InputOwner thirteen-to-fourteen field change is not hidden in 152 or charged implicitly to the UI pool. Unread arbitrary failure retention is containment, not arbitrary-fault retirement.

## Native Input Affine Commit Review

Root read the full cross-crate input report and actual `trace::Watchdog::finish/report`. The proposed shared UI-host measured facade owns one real watchdog and a narrow writer. WGPU's private composite owns that facade plus the actual freshly acquired receiver guards; all prevalidation precedes fixed no-fail publication. No caller-supplied verdict, unrestricted mutable queue or second independently started timer is authorized.

Actual `Watchdog::finish` samples the end clock before telemetry and any subsequent queue/WGPU writes. That pre-write verdict cannot certify the total callback duration. The executor must specify and test the terminal publication/telemetry tail using the same original guard. An overrun after publication cannot be reported as a rollback or a fully accepted callback. The strict 8000-microsecond equality refusal remains unchanged.

The numeric input-root byte grant is fixed-work/preflight only, not resident funding. No partial empty-constructor cutover may leave an old enqueue path able to allocate implicitly. The complete queue/candidate/actual-caller join remains required.

## Native Resident Candidate Source Review

Root read all 447 lines of the first native resident structural candidate. Its missing-API R1 actually failed compilation with E0432 and executed zero tests; the report and complete captured output were read. No native green or Opening mount follows.

Two concrete additional pre-live issues were routed to Dag:

- Both one-element Vec allocations can observe larger capacity only after allocation and then fail an extra resident/work charge. Retaining that allocation is not proof it was preadmitted. Use exact-layout backing or an actually reserved upper bound.
- `consumer_empty` releases its mutex guard before dropping the erased Arc. A foreign mutable Arc can repopulate the Option after the empty observation, relinquish its own alias, and leave the root's drop to synchronously destroy C. Public mutable aliases do not provide an immutable empty witness. Private registered consumer/destination authority is required before live adoption.

Known structural-only limitations remain: a borrowed facade disappearing while its enclosing root survives is not a proof for destruction of a nonterminal enclosing root; `&mut Option` is a placement destination, not funded registered ownership. The next real consumer must be the actual retained Opening/worker parent.

## Independently Read Native Outcomes

Root read the full reports and captured outputs, without rerunning Cargo:

- Resident admission R1: one E0432 group, five warnings, zero tests.
- UI-host input-root R6: 31 missing module/method/field diagnostics, eleven warnings, zero tests.
- UI-host browser-envelope R4: one existing native envelope/retry/preflight law PASS, 67 unselected, 0.015 seconds, Nx exit zero. This is not a browser run; the five original input-admission failures remain.

## Compiler Coordination

The sole fleet native executor explicitly released its slot after terminal input-root R6. One serialized isolated syn oracle compile was lent to taxonomy in its own ticket-local target; the fleet executor will wait for explicit terminal release. No shared Cargo output, cleanup, source restoration or WGPU pin/publication was authorized.

## Selected Read Hashes

These six sequential hashes are a selected read snapshot, not an atomic transitive input closure. UI/source implementation continues after this capture.

```text
e12f587c3bf7bfc9cde3ca37d677cd0af983f11751245593cbba8b0b079e6dcf  🧰️framework/🔨️modules/🎠️kernel/📤️return/📦️content/📥️input/📦️payload/🧬️schema.json
5a288884f41eca466efe4b59ed04d530a46554120d3338c4c7192e7a3dc1d4d3  🧰️framework/🔨️modules/🎠️kernel/📤️return/📦️content/📥️input/📦️payload/🧬️contract.json
6c80cc436a0755e3ad11232d769c914ffa97391236e4917724479218efff8ec1  🧰️framework/🔨️modules/🎠️kernel/📤️return/📦️content/📥️input/📦️payload/🧪️fixture.json
3ae0a0f1de68b6cf3db95fe70aab599268899385754b61e9b87fa00803a71345  🧰️framework/🔨️modules/🎠️kernel/📤️return/📦️content/📥️input/📦️payload/🧪️schema.json
a7ce38ef08897fd24bc71154180368ae988e8813963a41c931784485929b3973  🧰️framework/🔨️modules/🖱️ui/🧬️contract/🧵️retained/💾️resident/🟦️component.ts
d087f4ecf905d97394e8fcfd4753f60f064a5d3cb5172a6a818be218157cb298  🧰️framework/🔨️modules/⏱️trace/🦀️component.rs
```
