# Retained Renderer Preparation Checkpoint

The new `UiDocumentStore/retained` module now executes typed snapshot hydration and private patch preparation using captured numeric-index roots. UTF-8 accounting advances through source code units without allocating a whole encoded string. Index lookup/path-copy, subtree removal, visited/on-path bookkeeping, sibling-key hashing plus exact collision comparison, and validation traversal each suspend between bounded work units. The explicit traversal preserves the existing reverse-child visitation and violation order. Unchecked snapshot hydration remains unchanged semantically.

Canonical targeted run: `bun x nx run @semio-tech/framework-renderer-react:test --skip-nx-cache --args='--run -t Retained UI patch preparation'`, with the existing nonisolated Nx discovery environment. Executed r1: 18 passed, 10 skipped in the selected source file, exit zero, 4.33 seconds total / 3.70 seconds test execution. This is not the full React suite. Full stdout is `🧪️renderer-retained-patch-r1-2026-08-27.txt`.

Strict Ajv validates eight language-neutral scenarios at 256/4096-byte grants: large-table leaf replacement, whole-subtree deletion, cycle, missing child, long Unicode duplicate sibling key, intentionally unchecked dangling snapshot, depth quota, and stale revision. Exact result/violation order compares with the existing reference applicator. Immer supplies independent accepted mutation output. TextEncoder confirms the 45,056-byte Unicode fixture. A separate law cancels admission/accounting/application/validation/candidate phases while preserving captured old readers.

This packet is not live publication yet. Store/React/runtime and wgpu-web still use the old path; retained decode, output tree/hash, exact (actor, instance, surface) publication, bounded notification, and ACK join are next. No interactive migration credit is claimed here.

The engine and fixtures subsequently moved into framework UI `contract/retained` and are reexported by the framework facade; there is no renderer-to-renderer dependency. Violation output now uses its own persistent numeric index, preserving exact insertion order with retained output ownership instead of growing a native array. Tests alone materialize that index for comparison with the old reference. One moved-fixture import path was initially one level too high (r2 failed collection), was corrected, and the subsequent full React run passed 506/506 across four files (4.87 seconds total). Typecheck r2 returned exactly the seven pre-existing tutorial producer joins, with zero retained-module errors.

Three publication laws were then added: ACK exists only after exact root/revision swap; a transaction cannot be rebound to a different actor-instance-surface owner; stale concurrent and cancelled candidates cannot publish or ACK. The canonical `--run -t Retained UI` selection passed 21/21, 10 skipped, 4.60 seconds total. Its full stdout is `🧪️renderer-retained-publication-r1-2026-08-27.txt`. This is a tested private publication substrate, not a live runtime wiring claim. Numeric readers now additionally offer numeric-sorted traversal with the exact immutable insertion ordinal, needed to preserve notification order without scanning every unrelated node; that newest numeric extension awaits its next canonical rerun.

Remaining adoption audit: the current typed UiPatch/UiNodeRecord inputs still expose mutable JavaScript object/array aliases. The live private wire decoder must construct immutable owned fields before publication; otherwise an outside holder could mutate a component property after validation. This is not solved by capturing the persistent table allocation. Generic pack decoding, dynamic-object traversal/string ownership, and the final JavaScript GC boundary still need their own controlled ownership/byte frontiers. The private generator nesting is fixed by the source algorithm, while graph depth lives in an explicitly retired linked stack. Browser memory admission and actual eight-millisecond timing still require evidence. These are explicit implementation frontiers, not assumed solved by logical counters.

Numeric-index r3 now passes 12 semantic/grant cases plus the earlier 37 lifecycle, 2 ordinal, 3,072 differential and 5 invalid-ID cases, with strict TypeScript zero. The new negative-zero fixture reproduced a RED `-0` versus Map `+0` mismatch, then passed after normalization in the numeric-key boundary. It covers replacement, ordered reading, and lookup with both signed-zero spellings.
