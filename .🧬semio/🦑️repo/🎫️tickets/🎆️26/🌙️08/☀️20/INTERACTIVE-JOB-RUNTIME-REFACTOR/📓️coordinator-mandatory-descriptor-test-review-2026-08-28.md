# Mandatory Mutation Descriptor Test Review

## Decision

Approve the bounded test-only mount after taxonomy releases the three exact canonical leaves. Keep the base trait unchanged for the first independent native RED. No native lease is issued by this review; the sole executor must first publish the exact registered command and captured source boundary.

The coordinator read the complete plan, 94-line schema, four vectors, 172-line owned-inspector controller, complete failure receipt, and complete 2968-byte syn test. The actual base defaults/sentinel and all three explicit zero-variant NoConfig/NoPresence/NoTransient implementations were rechecked. Existing derive Cargo, project/router, and the current test-module insertion location were inspected.

## native AST Test

The proposed test parses the actual base source through the existing dev/tooling syn dependency, requires one public Mutation trait and one of each named associated item, and compares the three facts with the same adjacent accepted neutral vector. It prints exact actual/expected facts before asserting. Its source-relative include resolves from the proposed canonical test directory to the current replication Mutation source; the glue path resolves from the existing Rust package to that test.

This is an independent source-declaration test. It does not compile implementations against a required associated item, establish an exhaustive caller census, or certify all Plugin/OS behavior. Those checks remain separate. Existing serde/syn/serde_json dependencies suffice; no dependency or runtime API change is needed.

## Actual Prior Evidence

The peer's retained source-only receipt failed on check14, after13 preceding checks, with all three actual facts true and five selected input hashes equal. The coordinator read that exact receipt. It is not14passes, native execution, or a newly rerun result.

Reviewed native body: `2bc2741f078ebeacd894dbf6e69fc3c292ba38d46881fbbeda1ec8858d7c30ae`.
Reviewed base: `e5f2f9ce74cc305bcbc23c0d99ab70cc2af54cf299a561f7910d56a7dbbd8385`.

## Exact Next Boundary

Only the three admitted test leaves plus the cfg(test)/path/module insertion beside the existing derive test include may be mounted, preserving the original glue preimage through a unique inverse proof. Then the sole executor may prepare, but not yet execute, the existing `@semio-tech/dsl-derive-rs:test` route with `--lib` and exactly `mandatory_mutation_descriptor_tests::mutation_trait_matches_adjacent_required_declaration_facts`.

A later base repair is limited to the DESCRIPTORS initializer, descriptor default body, undeclared sentinel and directly associated fallback docs, following the actual independent RED. The slice API, unrelated base bytes, and honest zero-variant implementations stay unchanged. The concrete Demo metadata omission is a separate real direct-leaf join; defaults may not be restored to suppress its diagnostics.

The unrelated OS production detach blocker does not justify a redundant OS compile. This isolated derive selection must not execute the existing write/delete wire test, the missing actor fixture, or any other native selector.

A subsequent read-only Rust-source search under framework (excluding compose) found the sentinel name only in the base trait's own doc, default body and constant. This is a named-reference search, not an exhaustive implementation census or macro/compiler proof. The exact existing glue insertion neighborhood was reread without truncated output.

[Peer source plan](</Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL/📓️mandatory-mutation-descriptor-source-plan-71.md>).
