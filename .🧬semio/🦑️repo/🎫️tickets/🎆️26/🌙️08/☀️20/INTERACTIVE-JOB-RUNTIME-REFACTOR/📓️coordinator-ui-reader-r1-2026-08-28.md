# Independent Resident Reader R1

## Executed Boundary

Coordinator executed the registered React test-long route exactly once with the OwnedResidentReader selector. Actual result: 14 passed, 706 skipped, 720 collected; one test file passed and four skipped; start 04:16:24, duration 3.41s, Nx exit 0. The complete selected 77-file pre/post hash lists are byte-identical. This is a selected capture, not a complete transitive import closure or an atomic workspace claim.

```sh
NX_DAEMON=false NX_CACHE_PROJECT_GRAPH=false NX_ISOLATE_PLUGINS=false bun x nx run @semio-tech/framework-renderer-react:test-long --skip-nx-cache --args='--run -t OwnedResidentReader'
```

The coordinator read the complete four reader declarations, all fourteen selected tests, and the actual reader admission, access, binding, alias and close implementation before execution. Both short UI and imported actor/input source holds are released at this terminal boundary.

## What Was Exercised

The real private reader is admitted on the same payload ledger before source EOF/page publication, with separately phased 720-byte reader registration and 432-byte page-reader alias registration. The scalar 257-byte window test reads genuine bytes while the source field remains unconsumed and incomplete. The laws cover premature/foreign proofs, all fourteen preparation cancellation prefixes, body/binding faults, short and oversized child grants, original alias preservation after revocation, parent quarantine, and the actual 11-field reader/one-field facade/two-field witness inventory.

The declarations also contain lengths 0, 1, 255, 256, 257 and 8193; this selected runtime gate must not be represented as executing every declared length. It does not prove automatic producer/page binding, an early semantic decoder, raw input acknowledgement, full stream/source retirement, native guest behavior, full renderer strictness, physical heap reclamation or callback timing.

## Evidence Handling

Raw tool results, including command output and all selected hashes, are preserved in [the output record](</Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧪️coordinator-ui-reader-r1-output-2026-08-28.md>). Initial posthash inspection used an insufficient tool output budget and was truncated; comparing that truncated string with the complete prehash output was not evidence of source drift. A fresh complete 77-line readback with a sufficient output budget is equal. Both the truncated inspection and complete readback remain preserved, and no test was rerun to replace evidence. Repeated NO_COLOR/FORCE_COLOR warnings are present in the actual output.

## Next Boundary

UI page/builder binding remains a separate schema-first TDD slice. It retains the same declared words and charges, uses separate source-read and destination-write grants, and requires original reader/page aliases to detach before storage release. The six-test OS native packet has been dispatched to the sole executor only after the separately granted native source hold; no native result is inferred here.

