# Runtime Opening CUT1 Declaration Review

## Read Boundary

Root read the complete CUT1 report, all189 staged Rust lines,232 schema lines,174 fixture lines,124 controller lines, and exact R1/R2 tool JSON. Root also read the actual native resident ConsumerPage/ConsumerHeader/prepare/capture implementation410–575 and root close/structural-empty implementation170–360. No source or native command changed during this review.

The source-only R1 genuinely rejected missing cut1; R2 reported5 cases/26 transitions/10 Buffer vectors/5 native names and the original7/39 reference. The staged native bodies were not executed or compiled. R1/R2 have no pre-run capture, and their post-GREEN body hardening is not a native or source replay result.

## Direction Accepted In Scope

The original-root primary descriptor and stamp must be installed before the first fallible return. A neutral role rather than Plugin-specific root types is appropriate. The same-root ordinary consumer path must remain distinct. Four1024-byte actor chunks retain the4096-byte actor limit; the proposed16-slot pages retain1024 logical slots per registry. These are declaration choices, not measured native layout fit or full actor ingestion.

One primary anchor per root remains explicitly a first-cut limitation: no live multi-runtime host restriction or separate-ledger workaround is authorized. Actual primary-root metadata, pending descriptor, recovery cursor and release associations must be included in native Layout and structural emptiness. Existing R11 pointerless-only poisoned progress cannot be broadened implicitly.

## Required Pre-Mount Hardening

1. The proposed recovery cursor holds next/found NonNull pointers across granted calls. The current resident access gate protects a single call, not those pointers across a concurrent close/free. The report states the required safety outcome but does not specify its enforceable alias or release barrier. Specify exact original-root pin/clear/close ordering, charge every alias or cursor write, and stage a real paused-recovery versus close test proving no stale dereference or early refund. A root-held raw address alone is not a pin.

2. The native layout test independently measures only three domain types while the proposed plan supplies all remaining allocated-node and work frontiers. Before native execution, declare the exact actual observation implementation and independently compare all three registry T instantiations, their real neutral node/admission Layouts, actor chunks, root/consumer metadata and actual Destroy/Free/write extents. The test must not merely compare two values synthesized by the same planned helper. Preserve4096 grant and capacity; oversized actual layout must produce an explicit failure with retained cleanup.

3. The allocation-failure law accepts any preparation error as failure_seen. Require evidence that the specifically selected actual System allocation was reached after reservation and returned null exactly once, including requested Layout, original reservation/partition, retained charge and no dropped probe event. A capacity/identity/unimplemented-path error must not satisfy the law. Keep the allocator non-unwinding and keep probes fixed/nonallocating.

4. Publish concrete cfg-only probe/fault-hook data and call-site declarations before mounting tests. Preserve exact cleanup/fault results and distinguish a source model from native behavior. The Rust test currently refers to numerous unimplemented observation methods, so it is not yet a compiler-ready bounded missing-API packet.

These are ticket-only declaration/test repairs. No primary/runtime/allocator implementation, canonical include, native lease, Store detach change or live cutover is authorized by this review. After the coherent packet is reread, a separate smallest actual native boundary can be selected.

## Follow-Through

Dag owns this bounded ticket hardening. The existing original Opening7 and resident25 remain untouched. The separate source-owned scalar decoder/reader proposal and Plugin codec qualification gate do not authorize any CUT1 runtime changes.

## Allocator Wording Clarification

The exact desired injected event is the real runtime allocation request entering its actual allocator boundary after original reservation. A deterministic null return before delegation must be reported as allocatorEntered=true, systemDelegated=false, nullReturned=true, not as an underlying System allocation failure. Preserve separately any genuine delegated System-null observation. The test must prove the selected Layout/reservation, exactly one attempted injected null, retained charge and zero dropped events; a fabricated prepare error or allocate/free-and-report-null remains forbidden. This clarification does not authorize a runtime allocator implementation or native command.
