# Generation-Bound Instance Close Contract

## Current Source Boundary

### 2026-08-27 Strict Clock And Exact UI Checkpoint

The four deterministic close laws now passed **4/4 native tests**, 452 filtered, 0.02 seconds (`🧪️member-instance-lifetime-terminal-green-r2-native-2026-08-27.txt`). This includes the newly mounted optional monotonic-clock law and the strict shared 8,000 µs equality rejection, plus terminal-publication precedence and exact owner restoration. It is not a cold app-close timing result. The next original-only diagnostic initially selected three tests because one skip name was wrong; that invocation cannot count as the required two-test cold gate. A corrected fresh invocation is coordinated with the compiler owner.

That mis-selected R5 invocation actually failed **1 passed / 2 failed**, 453 filtered, 0.38 seconds. Its new 15-phase traces are still useful measurements, without relabeling its scope: one 22,004 µs callback spent about 21,955 µs after app close returned and before typed job return; another 23,483 µs callback returned from the typed job at 29 µs but from the driver at 23,459 µs. The measured app close itself took approximately 2 µs in both samples, and stage/maintenance boundaries approximately 1 µs each. The first interval contains close-outcome/payload construction; the second contains the driver/Watchdog/trace tail. These intervals identify locations, not a proven allocator or mutex cause.

Source inspection at those boundaries found that the four-byte close checkpoint currently calls `payload_from_bytes`, allocating a new 16 KiB page source and initializing a 256-slot payload envelope. Its admission ledger is atomic, not a mutex. After typed job return, the driver drops `Watchdog`, whose sample registry uses a blocking mutex and first-site BTreeMap insertion, and records the checkpoint through another blocking trace-ring mutex. These are candidate operations for finer instrumentation or bounded replacement; no prewarming-only fix or ceiling change has been made.

The corrected original-two-only R6 gate selected exactly two tests and again failed with SIGABRT after callback faults (`🧪️member-instance-lifetime-close-cold-r6-native-2026-08-27.txt`). One 8,519 µs callback completed an outcome close at marker 5 = 87 µs and reached marker 6 at 8,515 µs: that interval includes the payload terminal scan, empty outcome destruction, checked-out authority resume and pump guard release. A second 19,965 µs callback returned from `session.step` at 19,951 µs while all typed-job markers were zero: the job was not entered, so its body and inner Watchdog are not the location for that sample. Deadline/cancellation preflight and driver authority handoff remain within the measured boundary. These traces rule out a single established allocation or app-close cause.

Twenty-four fixed test-only markers now additionally bracket terminal checks, empty outcome Drop, session resume, pump return while holding its mutex, mutex release, and session step entry/return. A seventh native close law is authored, not run: an actual `RuntimeCloseCleanupJob` and its `BatchJobSession` produce the source payload; while another thread holds the pump mutex, the outer callback must preserve the exact session and returned four-byte payload allocation. Cleanup is completed before assertions. This is an ownership/contention unit law with an injected test clock, not a claim about real elapsed timing or allocator contention. Trace/job held-mutex and callback-owned verdict repairs are owned by the coordinated peer.

The exact native UI descendant cursor separately passed **8/8 tests**, including the normal-path contention correction, then the entire native UI contract suite passed **99/99**, zero skipped (`🧪️member-ui-value-full-r4-native-2026-08-27.txt`). Exact slot/epoch, nested shared aliases, intrinsic descendant traversal and terminal guards are exercised. Typed node/patch/document adoption, the old global cleanup helper, host aggregate and close ACK remain unfinished. See `📓️native-ui-exact-retirement-2026-08-27.md` for the precise boundary. Current actor source/schema tests passed **63/63** in four files, 1.96 seconds, start 15:44:34 (`🧪️actor-fixed-list-schema-r1-2026-08-27.txt`); the new fixed-list native law remains unmounted during the cold diagnostic snapshot.

### 2026-08-27 14:22 Cold Timing And Clock Authority

The warm combined close run passed **5/5 native tests**, 450 filtered, 0.32 seconds (`🧪️member-instance-lifetime-close-r3-native-2026-08-27.txt`). It did not explain the earlier cold failure. Running only the original two app-close tests in a fresh process reproduced outer callback failures at **155,839 µs** and **26,938 µs**, followed by a strict Store Drop abort (`🧪️member-instance-lifetime-close-cold-r4-native-2026-08-27.txt`). The measured interval concentrates in `BatchJobSession.step`; the existing markers alone cannot distinguish app cleanup from tracing/driver bookkeeping. The cold end-to-end timing gate remains RED.

Fifteen fixed atomic test-only markers now distinguish pump/admission/driver/retirement phases, before/after `set_stage`, before/after maintenance cleanup, before/after app cleanup and the typed job return. These new narrower markers have not yet produced a cold runtime trace.

The current source uses the shared optional microsecond clock for **both** outer close and maintenance callbacks, replacing unconditional `std::time::Instant` in these wasm-reachable paths. Two preflight samples reject missing/backward clock authority before app work; the final sample is checked before any success publication. Clock failure preserves the owned state and publishes fault, with no zero-time success fallback. Both callers use the shared `interactive_step_contract_violated` predicate owned by the trace module. The strict plan requires **elapsed < 8,000 µs**, so the updated fixture faults at equality as well as above it. The trace owner's corresponding implementation/native gate is coordinated separately.

The current strict fixture/source run passed **62/62 actor tests**, four files, 4.78 seconds, start 14:22:01 (`🧪️actor-instance-close-strict-clock-source-r1-2026-08-27.txt`). This source run covers missing/backward/equality fixture outcomes and the independent third-party comparison. The newly added native optional-clock law and the current strict predicate have not yet been rerun together with the native close tests.

### 2026-08-27 14:05 Worker Fault-Correctness Packet

The next canonical actor run passed **62/62 tests in four files, 1.91 seconds** (`🧪️actor-instance-close-fault-source-r1-2026-08-27.txt`). Two additional strict source fixtures independently verify callback verdicts with Lodash comparison and native UI descendant text/page counts with Lodash deep traversal. They do not execute the Rust UI arena.

The native app lease diagnostic rerun compiled and failed **0 passed / 2 failed**, without abort: `🧪️member-instance-lifetime-close-r2-native-2026-08-27.txt`. Both recorded outer callback fault origin 1, at **9,147 µs** and **38,260 µs**, generation 2, with no inner fault payload. This establishes a real elapsed-limit failure, not its internal cause. The runtime limit remains 8,000 µs.

Two deterministic native laws then reproduced separate correctness defects: `🧪️member-instance-lifetime-terminal-red-r1-native-2026-08-27.txt`, **0 passed / 2 failed**, 0.05 seconds after compilation. The inner close callback exposed terminal status before the outer watchdog verdict; the terminal pump also chose Complete progress over a Fault outcome. The production correction returns a candidate status from the inner callback and publishes exactly once after the elapsed verdict, and gives fault/cancel outcomes precedence over progress. The same owner guard now spans terminal preflight, detachment, `Arc::try_unwrap` and exact shared-root restoration. Both `mem::forget` failure paths were removed; nonterminal app or maintenance owners stay captured.

The two corrected laws plus a new preflight/shared-allocation law now passed **3/3 native tests**, 452 filtered, 0.02 seconds (`🧪️member-instance-lifetime-terminal-green-r1-native-2026-08-27.txt`). Test-only fixed atomic timestamps record eight bounded callback phase checkpoints: pump entry; before/after session admission; after job step; after outcome checkout; after payload/session retirement; after pump return; after final app handoff. These are relative to the admitted callback's monotonic origin, while the unchanged outer `Instant` measurement remains the final elapsed authority. No native timing credit follows from the deterministic unit laws.

### 2026-08-27 13:50 Checkpoint

The current canonical actor run passed **60/60 tests in four files, 1.68 seconds**, including exact retry after synchronous transport refusal and worker refusal, the generated worker's activation freshness test, and the strict publication-owner fixture checked against Lodash's independent sequence operation. Log: `🧪️actor-instance-close-publication-source-r1-2026-08-27.txt`. The earlier 58/59 counts below are historical snapshots. The generated-worker test executes the real worker source with an injected JavaScript ActorApi, not Wasm.

The native app close lease gate is **RED**, not pending or passing: `🧪️member-instance-lifetime-close-r1-native-2026-08-27.txt` compiled and aborted after a real close-worker fault, followed by the strict Store Drop guard during unwinding. No two-test success count exists. Fixed-size test diagnostics now identify all worker fault origins, elapsed time, pump state and the first fault payload bytes; rerun is queued after the compiler owner's real microsecond-clock migration. Neither the eight-millisecond runtime limit nor the terminal guards were weakened. The payload admission ledger is per session, so a process-global admission collision is not established.

Three native fixed-wire mirror tests, one exact published-patch handback test and one immutable reactor-slot scope test are authored but not executed. `SurfaceReconcilePublishedPatch::close_step` now retains its surface, reservation credit and handback through separate steps instead of dropping the wrapper and enqueueing hidden cleanup. `PendingPatchSlot` captures the instance at admission, so clearing the payload's surface cannot detach its remaining cleanup from the instance. The shared fixture includes this ownership sequence; passing its source oracle does not certify these Rust implementations.

Native nested UiValue/document/turn-patch retirement is still unfinished. In particular, a collection's current arena slot may disappear before nested List/Map handles released by its final page have drained. An exact root-aware descendant owner is required; global arena emptiness, a cleared map, and discarded cleanup return values remain invalid completion witnesses. The worker has no mounted aggregate close endpoint yet, and public host destruction still has not adopted an exact app/reactor/UI/transport completion join.

The actor lifetime close schema and hand-authored fixture live under `framework/actor/🚪️lifetime`. The fixed wire codec and actor-client receipt lease are implemented; the worker/native aggregate endpoint and public host close join are not yet implemented. The protocol contains only fixed-size numeric authority: an activation generation, u32 instance id, wire-safe numeric request sequence, and the native close generation returned on acceptance. Actor names are not copied or looked up to authorize a delayed close. The worker slot is retained by the private owning lease; a response from a replacement slot cannot satisfy that lease.

Full canonical actor tests passed 58 tests across four files in 1.62 seconds (`🧪️actor-instance-close-lease-r1-2026-08-27.txt`). Eight strict fixture sequences cover acceptance without terminal completion, early retired messages, foreign workers, stale activation, wrong request and close generations, and duplicate acceptance. A second test reuses an actor name and proves an old captured lease posts no close to the replacement. The independent existing LEB128 implementation produces the same four exact wire encodings. An earlier full 56-test checkpoint is retained separately; the attempted lease RED first encountered an incorrect source import path, so it is not a successful semantic failing-test demonstration.

`ShardClient.captureInstanceClose` captures the private activation record and original worker once. A checked u64 activation counter survives actor name reuse within the client, and request sequence overflow rejects before posting. The idempotent native-close promise settles only from an exact accepted/retired pair on the captured worker. Generic request results do not settle it. It intentionally does not remove routes or claim host UI cleanup. Existing ordinary `dispose` remains an unfinished boundary until the real endpoint and host aggregate replace all callers.

The new native `PluginInstanceCloseLease` captures a Weak identity of the exact RuntimeAppCell, compares it before detachment, and retains the admitted RuntimeCloseWorkerState as a terminal witness. It does not clone the app or retain a strong live snapshot alias. Checked generation admission precedes mutation; repeated begin on the same lease is idempotent and cannot affect a later same-ID app. Its terminal query verifies the cell and worker session/outcome owners are empty, independently of quarantine removal. Two native tests are mounted and queued with the compiler owner, not yet claimed passing. This witness covers the app/worker subtree only; reactor, UI, ingress/publication transport, and host descendants still must join it.

The close request must be checked against the exact actor activation before submitting InstanceClose. Accepted acknowledges ownership transfer only. Retired requires the real app, reactor, native UI, host UI, ingress, publication, and transport owners to be terminal. Neither actor-map deletion, generic worker dispose, TurnStatus Idle, nor an unexamined Promise resolution is proof. A single handle-owned completion observer retains the exact generation-bound lease until the retired acknowledgment is consumed; AppChannelClient fatal recovery does not open a second subscription.

Existing production defects were read directly: React and native bridge destroyApp paths delete routing/UI maps and call void ShardClient.dispose; its worker message only removes actor bookkeeping. The native reactor begins multiple cleanup owners but ignores several result values. PluginRuntime's real close worker has an exact Arc-rooted completion state, but the transport currently returns before that state and the other descendants join. These are implementation prerequisites, not permission to fake a receipt.

## Required Implementation Join

1. Activate a checked nonrepeating generation and retain a private worker/actor route lease. A late close callback must never perform a fresh actor-id lookup.
2. Admit one fixed close request and retain its receipt capacity before native detachment. Keep the actor executable for cleanup turns.
3. Join RuntimeCloseWorkerState, ReactorCloseState, per-instance native UI/patch owners, typed ingress/results, and the renderer's retained UI aggregate. Every participant exposes its actual terminal witness; generic callbacks reporting arbitrary booleans are not authority.
4. Emit and retain the exact retired receipt under transport backpressure. Only the matching owner ACK permits native/worker/map release.
5. Inject that real lease into AppChannelClient. Fatal iterator end/error requests close and waits for retired proof before rejecting the query and dropping its page/receipt ownership.

The CAD constructor checkpoint finished before the narrow native lease module was mounted; its two tests are coordinated with the sole compiler owner. Existing native UI arena functions expose global retirement status, not an exact per-instance completion lease. Reading them as an instance witness would be incorrect; the concrete descendant handoff remains open. The subsequent ordinary-ingress packet still owes all 16 unsupported tags (5, 10–14, 17–26), with their actual retained semantics and retirement, not only a decoder whitelist.
