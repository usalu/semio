# Generator Ownership And DOM Checkpoint

## Executed Evidence

Canonical workspace interactivity self-tests passed722 after the generation lifetime guard and shared Flow retirement laws. Exact factory source join remained33 owners,254custom rows,31generic rows. The first run failed because a hostile source mutation changed only one of two Arc::into_inner occurrences; the hostile now removes every occurrence and the gate passes.

Canonical renderer Nx/Vitest run for graph parameter plus graph slider passed7tests,0failed,347skipped,354total. The new mounted FlowGraphCanvasHost test executes all three controller/surface scopes: Flow, Procedural2d, Procedural3d. It verifies actual small setGraphParameter dispatch for keyboard and pointer drag, exact widgetId/value plus transport surfaceId, accessible name, focused thumb, stable scoped ID, no documentJson call, no whole-fixture action, no pointer-up/cancel commit, and no80ms commit timer. A delayed older slider-overlay response cannot replace the newer displayed value. The initial drag test failed because its simulated root bounds omitted the actual track bounds; corrected geometry produced the expected value8. The final seven-test run had no post-unmount canvas error after adding exact session/canvas/request guards.

These are DOM tests with a repository-owned mocked wasm task boundary, not live wasm/native backend verification. The three backend command registrations and retained publication recipes remain unfinished; no shared UI end-to-end completion is claimed.

## Generation Lifetime Guard

ProceduralGenerationRoot now keeps its optional immutable Arc in ManuallyDrop. Nonfinal references release through Arc::into_inner, avoiding last-reference races. An empty final state has constant teardown. A nonempty final state must enter explicit retirement: an invalid ordinary drop panics without recursively destroying its JSON payload; unwinding does not trigger another panic. GenerationRootRetirement similarly guards its entire owned state and releases the empty state only after terminal close.

The root still has transparent serde wire representation, O(1) sharing, and crate-private unique cold-builder mutation through Arc::get_mut. The explicit cold retirement method is used for full-artifact snapshot replacement. P3 Store owned/Arc snapshot retirement now delegates fixture members to shared FlowRetirement and generation data to its JSON cursor; Arc final ownership uses Arc::into_inner. The mounted typed decoder hands an abandoned completed candidate into that same retirement rather than dropping it whole. Existing decoder partial stacks still have independent bounded-close gaps and receive no new interactivity credit.

Four generation native tests are authored: original wire/allocation and unique-builder laws, plus invalid final-owner/cursor-drop and12phase zero-grant/worker-transfer close laws. They have not been executed by this executor.

## Shared Flow Retirement And Numeric Semantics

Framework Flow now owns FlowRetirement/FlowOwner, covering fixture schema, widgets, synapses, layout keys, GUI chrome and labels, previews, nested neural trees/dictionaries, string vectors and sets. One close processes one structural owner or at most the supplied string-byte grant. Pop-first BTree metadata operations inspect no key bytes; destination-map copying is not certified by this retirement code. Its guarded frontier cannot recursively destroy live payloads during unwinding. Flow's app-local retirement still needs to delegate these domain branches when the shared copier/map adoption lands, rather than retain duplicate implementations.

The strict language-neutral fixture has long labels and three long Unicode map keys and an independent Node JSON byte-total oracle. Two native tests are authored for serde typed parity, exact cumulative released bytes at1/4096grants, worker transfer, zero-grant preservation, and invalid-drop guard behavior. They have not run.

The shared set_widget_slider_value helper edits only a selected widget's numeric fields. FlowHost delegates to it. It preserves existing sensible range expansion, clamps consistently, and never touches ID or label. Nonfinite or invalid numeric ranges fail without changing the widget. The language-neutral fixture covers all three consumers and in-range, positive expansion, and negative expansion. One native serde-backed numeric law is authored, not run.

## Remaining Dependency Matrix

| Layer | Current status |
| --- | --- |
| Shared DOM small command | Seven scoped DOM/a11y tests passed; live wasm unverified |
| Flow backend | Peer owns exact typed command/preparation |
| Procedural2d backend | Not registered yet; must use shared retained fixture units |
| Procedural3d backend | Not registered yet; immutable generation root and retirement source ready |
| Retained selected-widget/synapse/fixture copier | Pending approved OrderedMap adoption; no BTree insertion workaround |
| Exact latest-wins publication | Peer owns runtime admission/cancellation; full String identities require retained key copy/lookup and cancellation retirement |
| Generation editing and whole-fixture import | Separate unfinished retained rewrite; not credited by slider prerequisites |

No Cargo, rustfmt, compiler, modifying git command, new permanent script, or new runtime dependency was used.

## Six Explicit Noninteractive P3 Seams

The six P3 rows nodeGraphEdit, addGeneration, removeGeneration, renameGeneration, updateGenerationValues, and selectGeneration no longer carry generic bounded-first-step proof credit and are explicitly BatchOnlyPendingRewrite. nodeGraphEdit still parses the complete operation array, clones a host fixture and computes a whole-fixture diff. The generation routes still build the complete form projection, clone generation state or evaluate the selected generation and publish derived preview/config values without a retained path-copy/preparation cursor. Sharing the unchanged generation root for an eventual slider mutation does not fix those routes. Other old P3 generic rows remain outside this six-row correction and are not certified by this packet.

Targeted git diff --check for the changed frameworkFlow, P3, NodeGraph, test-adapter and root verifier sources passed with no output.

After the six-row correction, canonical workspace interactivity self-tests passed722 again with33owners,254custom rows and25generic rows.
