# Owned UI Wire Decoder Checkpoint

Canonical targeted execution: `bun x nx run @semio-tech/framework-renderer-react:test --skip-nx-cache --args='--run -t OwnedWire'`, with `NX_DAEMON=false`, passed five tests, zero failures, 509 skipped, 514 discovered, in 3.78 seconds. Complete output is `🧪️renderer-owned-wire-green-r2-2026-08-27.txt`.

The five tests cover five existing Rust canonical byte vectors, ten hostile framing/value vectors, strict Ajv fixture validation, existing pack-codec parity, Immer/JSON round trips, a nested payload larger than 4,096 bytes, immutable nested members, an own `__proto__` data field, cancellation at every step of a nested decode, and fixed native scalar/collection boundary checks. The native limits are read from `action.rs`, not inferred from the generated TypeScript array/string aliases. Shared buffers, partial subviews, non-transferable WebAssembly memory, and already detached aliases are rejected. Ownership transfer of a full transport-owned buffer explicitly relinquishes all aliases; this API cannot discover unrelated aliases for its caller.

Earlier execution history is retained honestly: r1 selected zero files because a multiword filter was split into positional filters; r2 and r3 failed test collection on incorrect imports. Neither is a behavioral RED. Actual r4 executed four tests and failed the positive byte-accounting assertion because the decoder discarded its computed step work. Returning the actual step result repaired that failure. The first subsequent run had four passes and one test-source URL failure; the fixed canonical source path produced the five-test pass above.

The decoder uses a private explicit frame chain, a persistent numeric symbol table, one varint byte or one bounded scalar/member/index step per advance, and immutable property definitions before exposing a complete root. It does not invoke the existing recursive `decodePackValue` in production. Native 512-byte text decoding/comparison and at-most-256-entry container metadata fit the real 4,096-byte grant; grants below 4,096 honestly block. No credits accumulate toward a later oversized operation. Cancellation closes frames first, then root references, parent-before-child construction owners, symbol owners, and actually zeroes the input backing bytes in pages before releasing that buffer. This is logical ownership/work accounting, not a physical JavaScript GC or eight-millisecond allocator certificate.

The retained-preparation typecheck r3 separately has exactly the same seven pending tutorial/local-interaction producer joins and no new retained preparation/publication diagnostics. New decoder typecheck r1 completed with ten diagnostics: those seven plus three concurrent OS-root query/oracle diagnostics reported to their owner (two Uint8Array versus readonly-number-array parameters and one missing third-party oracle declaration). There are zero decoder or UiDocumentStore diagnostics. This is not a green whole-renderer typecheck. Targeted `git diff --check` for the retained domain and UiDocumentStore passed.

## Remaining Live Boundary

The next checkpoint implements `SurfaceDoc.bytes`: a 32-KiB `UiFixedBytes` payload serialized as a numeric list, separate from the ordinary `UiValue` 256-member container rule. The decoder's explicit `component` and `node` profiles recognize exactly the `doc.bytes`/`component.doc.bytes` position and prepare a private 256-byte-page owner. Its page-index allocation has at most 128 entries. No dense 32,768-element array, fake Array proxy, or final concatenation is used. The default value profile still rejects oversized ordinary collections. The profile is not yet a complete typed component validator, and cannot by itself mint publication authority.

`SurfaceBytePages` canonical quick tests passed three tests, zero failures, 514 skipped, 517 discovered, in 5.51 seconds (2.86 seconds tests), recorded in `🧪️renderer-surface-byte-pages-r2-2026-08-27.txt`. They compare seven page-boundary lengths through 32 KiB with Node Buffer, validate the strict neutral fixture with Ajv, exercise ten cancellation positions, reject four invalid lengths and invalid byte values, and decode the full native numeric-list wire in both positions. A captured byte owner survives decoder closure; only the final explicit owner retirement zeroes and releases one page per step. Direct use of a closed owner rejects. Ordinary immutable JS object references still have the separately stated physical-GC caveat.

The initial page test attempt and an isolated no-reply channel regression test never reached Vitest because a concurrent discovery edit temporarily used the reserved identifier `package`; the current discovery owner corrected it. No discovery changes were made in this packet.

Whole-renderer retries are not green: one 30-second verbose run completed all owned decoder/preparation tests but stalled in the next existing merge-policy channel test. The channel owner confirmed their new empty-outcome filter dropped legitimate empty replies, and owns the correction. Moving the new codec imports from module-level await into async test bodies removes an unnecessary collection dependency edge, but the evidence does not establish that an import cycle caused the stall. The subsequent full quick retry also hit its unchanged process watchdog. Complete logs remain in `🧪️renderer-owned-wire-full-cycle-r1-2026-08-27.txt` and `🧪️renderer-owned-wire-full-r2-2026-08-27.txt`.

No production caller has switched while the exact typed schema/default normalization join and retained-byte consumer projection remain incomplete.

The rest of the assigned renderer path remains active work: exact semantic wire-op assembly, immutable typed record authority, retained tree/hash, bounded notifications, actual React and wgpu publication/ACK, and actor/instance/surface isolation. The isolated publication cursor's previous 21-test pass is not live integration credit. The old whole-object/whole-map paths remain visible until replaced coherently.
