# OS R2 And Mutation Boundaries

## Reviewed Candidate, Not Native Green

Coordinator read the complete [Retained R2 source candidate](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️os-kernel-r2-owned-source-candidate-2026-08-28.md) and the actual Store thunk and Plugin constructor regions. Directory's runtime import, the two erased Send-future slots/thunks and two test-helper repairs are source-mounted. The actual OS6 result remains92 compiler errors and zero executed tests at its earlier captured boundary; no new compiler attempt has run.

The two typed codec thunks retain owned snapshots/mutations and shared envelope borrows across awaits. Their proposed Send/Sync requirements have concrete source reasons, but no fresh compiler proof yet. Coordinator's direct Plugin census also finds `builder::foreign_document_codec` as an upstream caller of `DocumentCodecSpec::foreign`. Retained is now tracing the complete concrete generic caller chain before a method-local qualification plan. No blanket ArtifactApp/Mutation trait expansion, local-future fallback, associated Diff bound or production body change is approved by that inspection.

The funded original-parent Store/FIFO detach and typed channel retirement remain separate required work. Removing the current await or discarding its returned backbone would not repair ownership. Native GO remains held until the relevant source packet and the independently owned fixture repairs are coherent.

## Outer Sync Fixture Packet

Mutation owns exactly84 of the earlier diagnostics; Retained's two fixture helpers remain outside that packet. Mutation independently parsed the full161-record compiler stream. Coordinator accepts the corrected source attribution: Backbone::send still exists; the two E0599 diagnostics concern trait scope, not established API removal. Preserve the same ACK/FIFO semantics and current channel ownership.

The84 plan is still awaited before its production-test edits. FreshField/FreshVcs/registry/grammar/Interaction, production SyncSession and Retained codec/backbone regions remain excluded. The approved rejected-page cfg(test) mount has separately completed, as recorded in the [mount review](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️coordinator-store-rejected-page-mount-review-2026-08-28.md). Both page wrappers remain unchanged for a future genuine two-law semantic RED.

## Independently Confirmed Mandatory-Metadata Regression

Coordinator directly read current [Mutation trait](/Users/ueli/Documents/semio/🧰️framework/🔨️modules/📡️replication/🎮️mutation/🦀️component.rs:107) and its descriptor sentinel. Current SHA256 is e5f2f9ce74cc305bcbc23c0d99ab70cc2af54cf299a561f7910d56a7dbbd8385.

Line119 defaults DESCRIPTORS to an empty slice. Lines123–126 default descriptor to UNDECLARED_MUTATION_LEAF. The current comment explicitly justifies this as letting unconverted handwritten implementations compile; the sentinel begins at line229. That is compatibility behavior, contrary to the requested mandatory/no-legacy contract. Source inventory warnings do not make an undeclared implementation satisfy the required typed declaration.

This is a separate finding, not attribution of authorship or a fresh runtime result. Root has no known active owner/hold on that exact base-trait region. Mutation has been asked for a separate schema/desired-law and concrete caller plan before changing it; it must not be silently folded into the84 fixture packet or the codec Send/Sync join. No base-trait source was edited by coordinator.

## Execution And Preservation

Only read-only source/report reads and hashes were performed here. The initial two-path ripgrep included a nonexistent old spr path; it returned exit2 after finding the real replication source. Subsequent exact-path reads/hash completed successfully. No absent-path assumption, compiler result or source change follows from the failed path lookup.

All reports, existing source, caches and historical evidence are preserved. No source hold, compiler lease or ticket completion follows from this review.

#### Exact Outer-Fixture Plan Approval

Coordinator subsequently read the complete plan67, the original fourteen Demo implementations and fresh-channel FIFO test, current ArtifactDsl/ArtifactPack/MutationDiff/OpText/OpBinary traits, ByteReader synchronous methods, chained pack/text fixture expressions, and actual Backbone/ChannelBackbone methods. The plan's distinction between58 compiler-proven not-a-future sites and seven directly source-proven masked awaits is explicit; neither is being counted as seven new compiler diagnostics.

Approve only the planned14 async qualifiers,65 exact synchronous-call await tokens and one test-local Backbone import, after its closed scalar/inverse schema and independent Decimal/Ajv baseline are captured. This is an exact source/API join, not permission to remove awaits globally, modify method bodies, widen visibility, invent descriptors or weaken expected FIFO/inverse/codec values. Retain complete old/new source and the exact token/in-memory inverse proof; all remaining tokens, assertions, literals and production/excluded helper regions must match.

The actual channel sends remain awaited and their first/second ACK ownership order stays unchanged. Its blocking lock/value-only error behavior is not thereby accepted as a general retained send contract. The current full outer namespace is not authorized to execute: its wire-fixture test writes/deletes outputs and its actor fixture path is missing. Those require separately reviewed changes/ownership; neither may be silently skipped then reported as full success.

Mutation must send a coherent source release after the bounded patch; the sole native executor still owns all compilation. This source permission adds no OS run to the current standalone resident25 attempt. The mandatory base-trait metadata repair remains a separate packet.

