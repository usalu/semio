# Native Inventory And Ownership Review

## Superseding R8 Behavior Boundary

The later R8 binary genuinely compiles; construction2 has actual RED→GREEN, live-output3 actual RED, and the exact24 old-binary fixture inventory exposes aborts plus an unawaited viewer test. Those results supersede the earlier precompile statements below without deleting their evidence. Current root review is `📓️coordinator-native-r8-behavior-review-2026-08-27.md`; registry allocation0/1 is also now an actual semantic RED. No full Plugin or all-app acceptance is inferred.

## Actual Plugin R7 Terminal Boundary

Root read all eleven actual E0599 diagnostics and the compiler footer. R7 exited1 (Cargo101) before any native test ran. All eleven concern existing PluginApp methods on VcsArtifactApp<TxnApp> in the canonical transaction fixture; Rust explicitly identifies the implemented trait as missing from scope. The affected calls include take_pending_transaction_proposal, transaction_prepare/commit/undo/redo, attach_backbone and ingest_operations. No semantic replacement method is appropriate.

The exact one-import fixture join is assigned to Mutation. All source holds were released; construction2 and live-output3 remain unexecuted. R8 is queued only after the narrow coherent source release. The717 selected nested inputs and complete7,951-line native output are preserved in `📓️plugin-native-inventory-r7-source-inputs-2026-08-27.md`, `📓️plugin-native-inventory-r7-2026-08-27.md` and `🧪️member-plugin-native-inventory-r7-2026-08-27.txt`. This is not an atomic complete dependency snapshot or native lifecycle acceptance.

## Fresh Plugin R7 Source Release

Mutation explicitly released its17 repaired R6 fixture joins. Root read the complete `SEMANTIC-MUTATIONS-OVERHAUL/📓️plugin-r6-joint-source-release-50.md` and independently observed Plugin component SHA `49e052ef1f38628104ace5c03b047b895556dee14108c8ca91b36c21f976b103`. The exact typed no-state store, private app-local fixture topology and shared codecs are released as source only. Their four source gates do not execute native tests.

The sole native compiler was directed to fresh PluginR7 no-run, followed by the real construction/live-output cohort if coherent. Mutation Plugin/Store/derive and Dag's compiled Kernel/Plugin/async/job source holds apply through that bounded compile. Store prototypes and new registry/Pending tests remain unmounted. No Plugin result is inferred before its actual terminal output.


## Superseding Full Runtime R81

Root read the actual R79 semantic failure (zero node credit produced one patch), R80 passing unchanged-limit test, the cfg(test)-only checked monotonic child census projection, and the full R81 footer. R81 executes **120 passed / 0 failed / 0 skipped**, **3.109 s**, exit 0: all119 previous cases plus the independent node-credit law. R78 remains its exact earlier119-test boundary.

The projection charges retained child node/byte census, not all physical traversal, temporary storage or wall time. No production transaction API changed. Native Plugin remains uncompiled after the current17-fixture repair, which is explicitly unreleased. See `📓️transaction-node-credit-r79-r80-native-2026-08-27.md` and `📓️runtime-full-exhaustive-r81-native-2026-08-27.md`.

## Superseding Full Runtime R78

Root subsequently read the complete focused transaction execution, the actual full R78 footer, the cfg(test) module gate, and the authored real-job/transfer source. R77 executes all sixteen transaction cases successfully; R78 executes **119 passed / 0 failed / 0 skipped**, **1.874 s**, exit 0, using the unchanged exhaustive target and no-fail-fast routing. This supersedes R76's six failures at the new source boundary; the original R76 evidence remains intact.

The fix replaces only the cfg(test) transaction oracle's direct unsealed candidate path with an admitted real reconciliation job, a metadata-only canonical root reader, granted in-place Ready transfer, and paired payload/Published cleanup. The transaction module is still not a production route. Cold test helpers are not callback-timing proof. Live Plugin output/key admission, native lifetime aggregate and all-app integration remain unfinished.

See `📓️transaction-canonical-oracle-cutover-2026-08-27.md`, `📓️runtime-full-exhaustive-r78-native-2026-08-27.md` and their retained raw logs.

## Plugin R6

Root read the actual raw native no-run diagnostics. The previous contributed-module path error is gone; compilation now ends with 19 errors and no test execution. All source holds were released immediately.

Seventeen are assigned to Mutation: six actual TestConfig/TestDiff/TestSnapshot module imports, one private ArtifactPack path, five no-state typed disposer/factory mismatches, and five private fixture count accesses. Dag owns the obsolete SetCount constructor in its live-dispatch fixture; Retained owns the removed publish method in the reactor test helper. Repairs must preserve actual contracts and retirement, not add defaults or compatibility APIs.

The 708 selected input hashes are in `📓️plugin-native-inventory-r6-source-inputs-2026-08-27.md`. The execution report and original text are `📓️plugin-native-inventory-r6-execution-2026-08-27.md` and `🧪️member-plugin-native-inventory-r6-2026-08-27.txt`. The selection is not an atomic complete Cargo dependency snapshot.

## In-Place Ready Transfer R73

Root read the actual three-test output: **3 passed / 115 skipped**, **0.108 s**, exit 0. The transfer test performs the real move inside a caught callback, retains both receivers outside it, checks refused source identity, and reports a declared 8776-byte transfer with the original shell allocation preserved. The two exact handback saturation cases also execute.

Production ownership-returning take_ready is removed; a cfg(test) oracle delegates the same granted in-place API. The authored Plugin caller cutover is source-only until the next real Plugin compile. Producer-branch partial take, lifetime binding, output preadmission and per-tracker backing remain separate open work.

See `📓️runtime-output-handback-preadmission-2026-08-27.md` and `🧪️member-runtime-ready-transfer-green-r73-native-2026-08-27.txt`. A subsequent actual R74 test exposed Ready bypassing cancel/stale/fuel/deadline checks; that later test is not retroactively included in R73.

## Actual Entries Callback Unwind R3

Root read both the changed native test and its actual output: **1 passed / 264 skipped**, **0.038 s**, exit 0. Unlike the earlier R2 fixture, the catch closure now performs the actual reserved payload placement and partial freeze (or next empty reservation), then panics. The original externally retained entries are subsequently drained and exact value/pointer/length triples checked. This repairs the earlier evidence gap without claiming a mounted native return producer.

See `📓️kernel-return-source-entries-unwind-r3-native-2026-08-27.md`.

## Full Runtime R76

Root read the actual full-run footer and six failure diagnostics: **119 executed, 113 passed, 6 failed, 0 skipped**, **4.889 s**, exit 1. This supersedes the earlier fail-fast partial run, not as a full green.

All six failures are in the existing cfg(test) transaction oracle: bulk one-surface projection, stale-revision intent, intent-following publication, deterministic surface order, superseding staged presentation, and independent surfaces. Five assert an expected patch count of one against zero; the deterministic-order comparison differs between two serialized initial patches and an empty list. These failures remain real and must not be excluded or weakened.

The exact output is retained in `📓️runtime-full-exhaustive-r76-native-2026-08-27.md`. Existing canonical-root and inline accounting tests ran in the same suite, but this does not prove the unfinished live Plugin producer, native lifetime aggregate, browser, all apps, or strict interactive timing.
