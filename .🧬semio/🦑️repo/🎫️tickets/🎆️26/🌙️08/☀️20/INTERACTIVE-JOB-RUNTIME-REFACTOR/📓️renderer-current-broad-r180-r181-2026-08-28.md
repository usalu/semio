# Current Renderer Broad Gate R180–R181

R180 is the first full renderer gate after the nineteen authored API joins and the released shared224 composition. Actual result: **734 PASS / 1 FAIL / 735**, five files (four passed, one failed),138.89s,start05:49:31,Nx1. R181 strict exits1 with exactly the seven known tutorial diagnostics. No production/parser/price change preceded these runs.

Both commands reused existing budgets:

```sh
bun x nx run @semio-tech/framework-renderer-react:test-long --skip-nx-cache --args=--run
bun x nx run @semio-tech/framework-renderer-react:typecheck --skip-nx-cache
```

The selected107 inputs from root's authored17 manifest were hashed before dispatch and after each terminal; all107 match in both runs. This is a selected-source manifest, not an exhaustive full renderer dependency proof. Exact manifests and complete output are in `🧪️renderer-current-full-r180-2026-08-28.{json,txt}` and `🧪️renderer-current-strict-r181-2026-08-28.{json,txt}`.

## Actual Failure

`OwnedResidentComposition retains the exact prepared pool and separates metadata admission from construction and final refund` fails at UiDocumentStore915:108. The test expects zero after per-pool retirement, but actual usage is **784 bytes /10 slots /10 owners**. This is the current shared controller's retained envelope; the full assertion/source is being inspected before any fixture edit. No forced controller disposal, quota change or zero-ledger compatibility is justified by this failure.

The other734 tests passed in this actual run. Color environment warnings and the experimental Transform Types warning are preserved. No automatic retry occurred. Full failure details are in the raw log, not suppressed as a flaky advisory.

## Strict

The seven unchanged errors are brand151, React test index6922, and ShellHelpers2102 (two),2153 (two),2197. No current UI resident/paged/actor API diagnostic was emitted. This is not a clean strict gate and not a native/live semantic publication claim.

