# Real CAD Constructor Checkpoint

## Superseding R2 Result

The real constructor R2 now passes one test, zero failed, 307 skipped, 0.039 seconds nextest duration (0.02 seconds libtest). The coordinator read the complete actual log and test body. The runtime DEBUG line confirms 25 exact app-factory rows including `setActiveUtility`, followed by terminal bounded close using the production one-item/4,096-byte grant. Log: `🧪️member-cad-constructor-r2-native-2026-08-27.txt`; retained nextest metadata: `🧪️native-artifacts/semio-nextest-3RHHAJ`.

The repair declares `setContributions` as Migrated at its actual CommandDefinition, not via the action-only setter, and keeps exact factory/owner/schema validation. This proves real CAD construction, exact registry admission and bounded close. It does not execute all 25 commands, run the full CAD suite or establish fresh Wasm/browser behavior. Rejected-constructor preflight/abort ownership remains an independent obligation; the earlier failure below is retained.

## Earlier R1 Failure

The coordinator read the actual native logs, not only executor summaries. The canonical CAD presence/close R3 filter passed four tests in the real CAD test binary, 304 skipped, 0.049 seconds; Nx exited successfully. The preceding R2 eight compile diagnostics were test harness API drift and are now past that gate.

The separate real constructor test remains RED: zero passed, one failed with SIGABRT, 307 skipped, 1.317 seconds. Primary failure is `interactive-job.catalog-authority` for `setContributions`: exact owner/controller/schema/factory/type joins and uniqueness are true, but `generated_migrated=false`. Unwinding then drops an owned interaction Store without its terminal-empty witness and aborts. The strict Drop invariant is not to be relaxed.

The executor traced the manifest mismatch to an app-level command still declared BatchOnly; a later action-only classifier does not modify that command. The assigned fix is to declare the actual command's classification consistently with its real retained Config factory, remove the inapplicable action setter and add exact app-command-scope assertions. No proof-validator bypass is authorized. Constructor preflight should occur before strict owned Store allocation, or failed construction must itself retain cleanup; the secondary unwind defect remains a real ownership obligation.

Evidence: `🧪️member-cad-presence-close-r3-native-2026-08-27.txt`, `🧪️member-cad-presence-close-r2-diagnostics-2026-08-27.txt`, `🧪️member-cad-constructor-r1-native-2026-08-27.txt`. The sole compiler executor continues isolated UI contract parity and the exact CAD repair/rerun. No CAD startup, all-CAD suite, fresh Wasm or browser success is inferred from four close tests.
