# Plugin Codec Oracle Review

## Review Boundary

The coordinator read the complete 274-line schema, 269-line fixture, 287-line oracle, and staging report, then rechecked all three hashes and the actual twelve Plugin/builder declaration locations. The packet is suitable for a registered source-only RED after its canonical path and package route are released. Neither mounting nor execution is implied by this review.

The graph/reference law uses strict Ajv and dev-only Lodash, independently expressed from the direct required-site traversal. The actual-source law is distinct: twelve exact local omissions, eleven other boundary mutations, two lexical decoys, one in-memory positive, and three schema-hostile cases are authored but unexecuted.

The parser skips comments and literals and verifies exact selected container/header/body tokens. This is a lexical source law, not a Rust compiler or macro/name-resolution proof. Normalizing the reviewed twelve headers before body comparison correctly keeps nested codec bodies in the same check. It ignores whitespace/comments; a later production patch still needs exact byte-delta/inverse review and native type-checking.

## Scope

Eight associated mutation sites and four bare mutation sites require local Sync qualifications. No broader ArtifactApp/ArtifactEditor/ArtifactViewer/base Mutation bounds, associated Diff bounds, function-pointer replacement, local-future fallback, or Store/Sync detach edit is approved. The real selected declarations currently remain unqualified; no source-oracle failure has run yet.

Canonical test-only mounting may proceed only after taxonomy supplies exact admitted paths and the package/launch route. The first run must retain its actual earliest failure; a schema/selector mismatch is not to be relabeled as the intended twelve-site RED. The Send-but-not-Sync compiler fixture remains a separately authored and reviewed test with genuine metadata, not a default or empty-descriptor shortcut.

## Reviewed Inputs

- Schema: `4653711cb2163f8765e7cdeef9db228f7866856e683ec1fd6be4cabf3cd6fb54`
- Fixture: `6f9c1d2f30fbbc916ec1caebf97e8ca237550932888ec3bfc24b86fc5313518f`
- Oracle: `729147662971128dac2ad70c3789b359697a4281fce919787d92e4db9bdcf273`

[Full staging report](</Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️plugin-codec-source-oracle-staging-2026-08-28.md>).

No native lease, source hold, production change, or test invocation occurred during this review.

#### Independent Canonical Mount Readback

After the test-only mount, the coordinator independently ran three separate `cmp` commands between the preserved ticket originals and canonical schema, fixture and source oracle. Each exited0 with empty output (chunks0543a1/a98bfb/95aa4d). A fresh six-file SHA256 read returned the reviewed hashes4653711c…,6f9c1d2f…,72914766… for both copies. The complete mounted release report was read. This verifies byte preservation only; the registered source oracle, hostile cases and native compiler have still not been run by this check. Taxonomy's exact registered metadata release remains the prerequisite for the actual source RED.

