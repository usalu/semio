# Canonical Return Framing Boundary

The current Actor return4 native codec is GREEN; full Actor exhaustive is still pending the explicitly recorded empty replay-log fixture close correction. Return source capture, full typed encoding/retirement and poll mounting remain open.

The next small Kernel native packet is the required fixed record-header cursor, not a replacement semantic grammar. Its authority is the existing `semio.kernel.return-content.v1` declaration (`tag:u8`, canonical unsigned LEB64 body length), the six authored record vectors and the independent Buffer/webassemblyjs framing oracle already recorded in `📓️canonical-return-result-oracle-r2-2026-08-27.md`. New `📤️return/📦️content/🧪️framing.rs` mounts two missing-API tests. They require exact 1/64/4096 grants, no zero-grant output mutation, maximum eleven-byte header, every input split without consuming body bytes, rejected noncanonical/overflow/unknown tags, exact consumed-byte accounting on failure and sticky fault state. The cursor owns only fixed header bytes/scalars, never a borrowed raw page. A parsed body length grants no allocation or semantic publication permission.

The two tests await the sole compiler at the next coherent UI/runtime dependency snapshot; implementation has not been mounted ahead of their RED. Their eventual success will not imply complete section sequencing, packed UI-field encoding, source-root retirement or live transport.

Source inspection confirms that UI's current retained contract exports typed close/copy/compare but no retained typed PACK encoder. Store's `pack_rt::encode_wire_value` clones into a field-1 record and calls `encode_record_body`; that cold path builds/sorts the symbol table, clones symbol strings into an index, and allocates the entire output. It remains an independent semantic byte oracle only. Store's retained borrowed canonical JSON cursor is a different format and is not being reused as PACK output. Native typed return work must retain its original structural root and provide bounded field/symbol traversal before the old whole-WIT output can be removed.

## Subsequent Compiler Boundary

The preceding pending state is historical. Actor's corrected empty replay-log fixture subsequently passed its focused gate, then explicit exhaustive Actor R7 passed 112/112; the additional 224-combination control law remains separately pending. Common Kernel framing R1 then reached its intended single E0432 missing `super::return_content` error before tests, as recorded in `📓️kernel-return-framing-red-r1-native-2026-08-27.md`. Only after that RED, the fixed header writer/reader was implemented and mounted. Its GREEN and the two existing dialect tests are queued with the sole compiler; no native framing pass is claimed yet. The next borrowed SendMessage packet is documented separately in `📓️canonical-return-message-record-2026-08-27.md`.

The subsequent canonical Kernel R2 executed all four framing/dialect tests: 4 passed, 253 skipped, 0 failed, .047 seconds, exit 0. Actual compiler output is retained in `📓️kernel-return-framing-green-r2-native-2026-08-27.md` and `🧪️member-kernel-return-framing-green-r2-native-2026-08-27.txt`. This verifies the fixed header cursors and existing Invocation/presence dialect oracles, not complete native return encoding. The next message-only cfg(test) mount was made after that source hold released.
