# Actual Runtime Opening Parent — Staged RED Contract

## Status And Scope

Ticket-only schema/test staging. No native command, production implementation, Plugin dependency edit, canonical test include, Store edit, or resident17 edit is part of this packet. The current private resident17 candidate is still uncompiled; its preceding R7 stopped in taxonomy before Cargo. The proposed tests below must follow that exact17 gate, not replace it.

The actual parent is `plugin_runtime::RuntimeAppCell<PA>` in Plugin `🦀️component.rs`, owned by the original `PluginRuntime<PA>::instances` slot. There is currently **no Opening type**. `Plugin::create_app` returns a whole `PA`; `plugin_create_app_with_id` binds it and only then allocates/inserts `Arc<RuntimeAppCell<PA>>`. `PluginRuntime::new` also allocates all three 1024-slot registries before composition admission. Neither ordering satisfies this packet.

The actual `VcsArtifactApp::with_registry_on_bus` owns document/config/draft/interaction stores as locals before tool catalog validation. The observed checkpoint R4 catalog failure and strict Interaction Store destruction during unwind remain genuine defects. Earlier checkpoint R6 success only followed a corrected valid catalog; it did not repair constructor failure ownership.

## Exact Parent Fields

Keep one RuntimeAppCell allocation for Opening, live operation and closing; do not create another app-owner family or move the root into a callback. Its required private state is:

| Original parent field | Concrete ownership |
| --- | --- |
| resident registration | One exact composition-issued registration covering the actual cell Layout and its inline receiver fields, not a capacity number. |
| opening | Original typed construction variant for the selected `PA` branch; for `VcsArtifactApp<A,M>`, this is `VcsArtifactAppOpening<A,M>`. |
| instance source | Exact private resident consumer of the original stable app-allocation handle; its `AppInstance<PA>` backing is separately funded before construction. No public mutable Option/Arc or whole-app factory return. |
| retiring instance | One inline optional stable app-allocation handle selected only by this parent's private close-receiver method. |
| opening retirement | Original partially built fields stay in `opening`; closing changes access, not their address or ownership. |
| close/maintenance state | Existing pumps, exact queued/running submission receipts, first fault and final-shell release state. |

`VcsArtifactAppOpening<A,M>` is not a successful substitute PluginApp. It is the construction state of the existing VcsArtifactApp. It owns the actual app input, registry/bus input, app/config/draft/interaction IDs and envelopes, document/config/draft/interaction Store slots, their real disposal factories, factory/catalog registration progress, first error/panic payload, and subsequent presence/transient/registry construction fields. A Store is initialized directly into that original slot. Replacing the constructor with a future that still owns these as locals does not implement the contract.

The closed `PA` factory roster must select the corresponding typed construction variant before invocation. The present `(AppDefinition, fn(&AppDefinition)->PA)` cannot implement this. A single revised factory descriptor supplies its concrete construction Layout, installs the matching variant into the already-registered cell, and advances that variant. No arbitrary public field projection or type-erased `FnOnce` owning partially constructed stores is accepted. The closed-enum/trait factory change must be reviewed separately before production mount; the test packet uses the existing concrete `VcsArtifactApp<TestApp<true>>` branch.

## Charge Before Producer Work

The one original composition root is externally retained through final close. Runtime registration reserves the requested `Layout` of all three registry arrays and the exact cell/Opening/receiver/consumer/admission descriptors **before allocation**. Slot counts remain 1024. Uninitialized storage is initialized in granted increments; an allocator-success flag or observed capacity is not upstream permission. Actual requested Layout and allocation/deallocation observations are the physical accounting contract, not allocator heap-bin estimates or the TS logical record prices.

Each later allocation needs its own preadmitted exact child: identifiers/envelopes, Store roots/metadata, factory registry entries, faults, pumps and callback submissions. Parent inline funding does not include arbitrary descendants. Before the next producer advances, both its output field and retained-error/rejection destination are registered. Zero/short grant, insufficient capacity and cancellation leave the original source and reservation unchanged. No Budget, virtual integer, implicit default total, static UI counter or second resident pool can issue this funding.

Fixtures use an explicit composition and already-owned declaration inputs; input construction is setup, not a claim that current bundle/bootstrap producers are admitted. A funded test fixture must account its declaration inputs separately before transfer. Native process-static UI32MiB composition extraction and host pre-instantiation remain separate prerequisites; this packet does not install another UI budget.

The fixture's64MiB total/8MiB control is an isolated finite capacity used to exercise private parent identity and refusal. It is not derived from, added to or substituted for the native UI32MiB envelope, and it is not a live host/guest capacity decision. No native Layout inventory has yet shown that the whole fixture fits. If the actual requested backing does not fit, the test must capture refusal/retained originals and fail its positive construction expectation after cleanup; it must not raise capacity or promote a declared byte number into a permit.

## Privately Issued Receiver

The source is the original private consumer for the stable allocation containing `AppInstance<PA>`. Only the funded original RuntimeAppCell issues its `RuntimeInstanceRetirementField<PA>` receiver. The descriptor binds original composition registration, exact parent allocation, exact source consumer, private field selector `retiring_instance`, and current close phase. Same numeric ID, same capacity, same Rust type, address containment or an arbitrary Option borrow cannot construct it.

Do not assume `size_of::<AppInstance<PA>>() <= 4096`. A move larger than the fixed grant must not silently increase that grant. The concrete representation must retain the separately funded stable app allocation and move only its small exact ownership handle. Use the canonical typed resident record backing, not a second external Box allocation protocol. Its actual Layout includes record/Option/alias metadata, not just AppInstance. Native layout observations must verify this representation before production. The staged transfer law requires `0 < required_move_bytes <= 4096` and tests one byte short; it does not authorize whole-PA memcpy. The current record API's whole-S install is insufficient for a large cell/app: the original parent needs private typed in-place field initialization with separately granted progress and exact partially initialized field retirement, not a completed oversized S on the stack.

The hardened law names the actual private production handle type `RuntimeInstanceAllocation<OpeningApp>` (still a proposed missing type), measures `Layout::new::<Option<that type>>()` independently of `required_move_bytes`, and compares it with both original source and destination slot Layouts. The test's native field observer must derive all remaining write-region Layouts from the actual typed metadata fields, never from the required-byte API or a repeated constant. Their checked sum must equal required_move_bytes and remain<=4096 before transfer is attempted. A cfg observer then records the actual completed transfer writes for comparison. Undeclared, oversized or mismatched work is collected as failure without attempting a larger grant. These Layout/probe methods remain unimplemented; no measurement result is claimed.

Receiver installation and source movement are separately granted and charged. Transfer revalidates both originals, phase, vacancy and full physical move extent before detachment. Closing revokes new source writes/receiver minting; exact previously issued close receivers remain usable only for their original close. Source emptiness is not parent retirement: the real app must finish its Store/command/output close and its typed terminal shell must be released inside the same measured callback before its final clock sample. A late clock fault retains the exact released-shell state and staged acknowledgment; it does not reconstruct an app or mark it retired.

A moved PluginRuntime facade still refers to its original stable registry/cell backing. No pointer to the movable facade is authority. Losing a borrowed access/receiver return does not destroy or orphan the parent; the original registry retains it and the same private slot recovers the descriptor. Replacement/same-ID reuse cannot acquire the old receiver.

## Callback Tail Is A Different Required Witness

Existing `RUNTIME_CLOSE_COMPLETE` and `PluginInstanceCloseLease::is_retired` inspect cell/pump state but cannot prove no future weak upgrade or captured callback-tail Arc. Existing WorkerPool `try_submit` has no successful per-submission completion receipt. Its scheduler must retain a preadmitted exact submission record and seal closure retirement only after invocation/unwind, retained inner panic handoff, and captured closure-tail destruction. The original quarantine root remains until that receipt, typed descendants and final original cell shell have retired. Pool-wide idle, strong-count samples and a boolean set inside the callback are not substitutes.

The staged tail test pauses the **actual scheduled callback's captured tail**, not an unrelated thread or a fabricated completion bit. A cfg-only interlock may pause this existing scheduler boundary and observe it; it may not mint a production terminal receipt. Until the shared WorkerPool extension is reviewed/mounted, this test is expected to expose a missing API or real refusal, never synthetic success.

## Staged Native Surface

The ticket Rust source proposes these crate-private operations on the actual runtime: `prepare_resident`, `prepare_app_open`, `capture_opening`, `advance_app_open`, `read_opening`, `begin_opening_close`, `advance_opening_close`, `prepare_instance_retirement_field`, and `handoff_instance_for_close`. Each mutating operation receives the existing canonical `ResidentGrant`; registrations come from the actual borrowed original `ResidentLedgerRoot`, not the grant. `prepare_app_open` resolves the existing factory descriptor and returns step progress, not an app/future owner. Capture is a private exact-slot key. Reads expose actual construction fields read-only. These names are proposed native methods, not a new WIT/owned-poll ABI.

The staged `VcsArtifactAppOpeningInput<A>` is a retained input tuple for the actual constructor (`A`, `AppActionRegistry`, `ActionBus`), not a PluginApp or success implementation. The concrete fixture provides this already-owned input and uses the exact same `prepare_app_open` called by the revised factory descriptor; actual production descriptor/type matching is mandatory, not bypassed by a test-only path. The hardened test file supplies only drivers that call real input/constructor/close steps and capture their results; opaque `close_to_empty_for_test`/`finish_app_open_for_test` helpers were removed. No alternate disposer, direct state reset or success bit is authorized.

Each driver gets a native inventory of original requested/captured Layout regions, original slot count and the declared typed ownership-transition plan. Its checked attempt bound is `sum(ceil(region.size/4096)) + logical_slots + declared_ownership_transitions + 3 terminal checks`. The inventory must come from actual registered descriptors/source fields, not observed success, a budget API or the64MiB capacity ceiling. There is no100000 fallback. Exhaustion records incomplete retained ownership; even reaching the bound proves no liveness, physical throughput or strict timing. Actual terminal witnesses are still required. A blocked external child is an explicit incomplete test, not permission to increase retries.

Forward observations are captured in Result/Option values while original inputs, runtime slots and roots remain outside the fallible body. A refused call that unexpectedly takes an input records None rather than unwrapping it. Oversize/grant errors return to the outer cleanup path. Every original runtime is then drained, including unexpected retained slots, and remaining exact input owners are closed only through their actual close methods. Cleanup records its first fault/overspend and continues its captured work allowance without asserting. The first post-cleanup assertion reports nonterminal cleanup explicitly; only successful exact cleanup permits intended ownership assertions. It does not clear a nonterminal owner to make the test pass. Callback rendezvous is resumed/joined even if paused observations fail; the late-clock probe is restored before cleanup. A separate test-only release observer survives root retirement without dereferencing a freed parent key.

The cfg-only constructor hook must run after an actual field placement inside `advance_app_open`, with the original parent outside catch_unwind. The tail hook must pause an actual scheduler-owned captured tail with a bounded rendezvous and release/join even when observations fail. Release counters observe actual shell destruction/allocator release; they cannot themselves mint authority. The late-clock hook replaces only the clock of the same actual close callback. The tests currently reference these missing observation seams and therefore are staging, not executable evidence.

The tests are intended to be included inside existing `plugin_runtime::plugin_builder_contract_tests`, where the actual `TestApp<true>`, registered restart fixture and real Store retirement factories are visible. They are **not currently included**. The Plugin crate has no `semio-framework-value-resident` dependency yet; one canonical workspace dependency edge is required after resident17 and metadata-owner review before an actual missing-parent-API compile. No duplicate `#[path]` ledger compilation is allowed.

The ticket fixture's symbolic identity values are reference-model labels only. Native tests use privately issued actual keys/receivers, real Store snapshot weak references, actual source pointers and allocation observations. Model success is not funding, callback timing or live constructor evidence.

## Source Boundary And Queue

Future narrow production region: Plugin main RuntimeAppCell/RuntimeInstanceRegistry/PluginRuntime and create/open/close entry points; actual VcsArtifactApp constructor and selected factory descriptor/closed construction variant; existing Plugin lifetime lease. Shared resident changes, if needed for private parent receiver registration, are a separately reviewed follow-on **after immutable17**. Shared WorkerPool receipt work remains separately coordinated, not included in this production packet.

Do not edit Directory/codec/backbone/Store detach implementations, UI/WGPU or Retained's R17 work. Store displaced-owner shell funding must join this same parent/typed-field authority, not a second budget; its exact receiver type is being coordinated with Retained.

Retained's read-only handoff identifies the exact child: `ArtifactStore<P,Mutation>::displaced_retirements.owners`, currently `VecDeque<Box<dyn ErasedSnapshotRetirement>>`. `replace_backbone_retained` reserves an entry, detaches `backbone`, then allocates `Box<ArtifactStoreBackboneRetirement>`. The required private `StoreBackboneRetirementField<P,M>` binds the original parent, closed Store selector (document/config/draft/interaction), original Store child and its displaced reservation `(slot,generation,count=1)`. Its concrete shell retains Backbones/message queues/current message/payload fields. The requested shell Layout and changed FIFO entry backing must be admitted before either allocation or detach.

Use one typed Backbone entry in that same FIFO, carrying the `ResidentRecord<ArtifactStoreBackboneRetirement>` handle plus exact binding; this canonical record is the ONE shell backing, removing this owner's Box rather than wrapping S in another Box. The requested Layout is the actual canonical RecordNode<S>, including Option/alias metadata. Existing `ResidentRecord<S>` is the family to extend after17; its current Option handoff is not sufficient. Exact typed shell retirement, final shell destruction, original Layout deallocation and receipt refund are separate granted phases. The last allocation receipt stays in the original root descriptor, outside S, through Free then Refund. Other erased FIFO owners stay distinct, and no second queue/pool or inheritance of old1024×Box entry funding is proposed. Retained owns the concrete Store implementation and its separate test packet.

Retained also verified that immutable resident17 currently debits `state.release` before `record.release`/deallocation in the same branch. That is **not** the separately retained Free→Refund protocol required here. Preserve17 and treat this as an explicit follow-on RED, not an existing capability. Likewise SyncSession's Tokio broadcast receiver Drop can walk unread slots and drop payloads; clearing its Option or draining `try_recv` is not a proved bounded child close. Those real descendants must retire through their own reviewed owner protocol before a parent receipt can be terminal.

Queue order: taxonomy repair → unchanged resident17 → reviewed Plugin dependency/test-only include → exact `runtime_opening_parent_` missing-API/behavior RED. No native execution has occurred for this packet. The large Interaction Store lane/three-map restore cutover stays unmounted.

## Executed Reference And Unexecuted Native Roster

`📓️runtime-opening-parent-reference-r3-2026-08-28.md` records actual scoped Nx exit0 for the **preceding packet hashes**: strict Ajv + Immer seven cases/39 transitions/two hostile checks and seven staged native names, four selected source hashes stable. R1 path failure and R2 accidental all-project repetition/interrupt remain preserved unchanged. The subsequent bounded hardening changes Rust/fixture/schema only and has not been re-executed, per the no-extra-retry instruction. The unchanged controller and seven ownership cases are not a new passing-source claim. The seven Rust functions remain ticket-only and have not been compiled or run.

The current constructor-unwind law targets original typed-field custody. Arbitrary panic payload final disposal is not proved by a test-local catch result; production integration must install the actual first fault in the preadmitted original parent fault slot and retire it with its domain proof. No assertion or test driver may replace that handoff with a clear/Drop shortcut. This remains an explicit review boundary before the native packet mounts.
