# Worker Metadata Preparation Close Review

The coordinator read the complete actual-preparation report and both new canonical schema/fixture files, including all seventeen cancellation prefixes, six interleavings and twenty-two gate endings.

The exact original-client `closeWorkerBootstrapStep(grant)` contract is accepted for the previously approved actual preparation TDD. It closes only worker-metadata preparation. It must not call UI close as worker proof, construct workers/handlers/SABs, expose neutral capabilities, or grant request/receiver admission.

Cancellation preserves the shared root for UI. A healthy unused worker cell may retire through distinct pending-release and private-witness observation turns. Once any worker record exists, installed or not, it remains charged with its cell and controller state; the stopped record-held phase returns blocked and never refunds it. Unknown wrapper-throw followed by null remains ambiguous. A returned canonical pending-release plus an actual exact observation may clear serialization without declaring a fault-held cell retired.

The newly named close driver uses existing phase fields, not another state/witness allocation. Any source deviation requires its actual field census and pricing before release. Shared224/784 and UI1608 remain prospective until the actual source/fixture checkpoint; the existing eager client/worker construction is outside this admission subset.

The current missing-method and shared-prefix7/8 closing tests must execute before implementation, followed by the real original-cell endings/interleavings. This review is not a test pass. No native execution, broad source hold, worker runtime cutover, controller terminal refund or raw input ACK is authorized.

[Peer contract](</Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/FIX-DEMONSTRATOR-END-TO-END-BOOT-HANG/📥️worker-bootstrap/📓️actual-preparation.md>).

