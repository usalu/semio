# Canonical Existing-Root Unwind R55

Actual R55 selected all four canonical runtime laws and passed: **4 passed, 104 skipped**, 0.107s. Session 4604 exited 0. The fixture repair only added injection at the actual pending-patch branch; no production semantics or guards were changed.

The new test retained the cursor structurally outside `catch_unwind`, injected after real comparison/copy/source-returned/candidate-returned work, checked original document bytes unchanged, then completed retained retirement for every frontier. Actual debug records report all four named phases with `exact-current-unchanged=true retained-close=true`.

The other three laws reran full child-grant boundaries, nine actual reconciler/readers and old-reader replacement. Fresh component/binding completion is a distinct remaining sibling gate, not certified by this result.

Canonical `SEMIO_COVERAGE=0 @semio-tech/ui-runtime-rs:test --args='exhaustive --lib surface_canonical_document_ -- --nocapture'`, unchanged target/environment. Raw `🧪️member-runtime-canonical-unwind-r55-native-2026-08-27.txt`.
