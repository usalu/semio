# Exclusive Compiler Queue

## Current Lease and Gate Results

Latest native sequence: deterministic terminal/exact-owner **3/3**, combined close **5/5**, fake-clock registered 500-microsecond **1/1**, real-native-clock registered 500-microsecond **1/1**. The coordinator read all output footers and scoped sources. However, requested original-two-only cold R4 is **RED/SIGABRT** at 155,839/26,938 microsecond outer callbacks, with the expensive phase inside BatchJobSession.step. These five-test and registered passes are not a general timing certificate. The compiler is released for coherent finer phase diagnostics, strict 8,000-microsecond boundary repair, actual host/WASI/bare-clock mounting and exact native UI descendant retirement. Publication executor remains the sole fleet compiler owner.

Exact native lifetime close R2 supersedes the R1 abort as the latest executed result: **0/2 passed**, 449 filtered, 0.35 seconds, after a clean 3m26s microsecond-consumer compilation snapshot. Both failures are outer callback overruns (9,147 and 38,260 microseconds), with no inner fault and no SIGABRT. The coordinator read the runtime output. The publication executor retains the sole Rust lease for the transport executor's two deterministic terminal-order RED laws, then a coherent exact-owner repair rerun, real consumed host/WASI clock execution and the registered 500-microsecond path. Core microsecond driver 4/4 and source clock fixtures do not establish those host/plugin joins. No fleet runtime ceiling is relaxed.

The latest exact results supersede the running-state paragraph below: CAD real constructor R2 **1/1 PASS**, 307 skipped, 0.039 seconds, with actual DEBUG evidence for 25 exact registered factory rows and bounded close. Expanded plugin local-interaction R8 **19/19 PASS**, 429 filtered, 0.33 seconds; its nineteenth law proves cold typed transaction receipts and honest rejection of the still-unsupported encoded route, not encoded transaction success. Exact instance close R1 is **RED**, two tests started then primary cleanup fault and strict Drop abort. The coordinator directly reviewed all three outputs. See `📓️coordinator-cad-constructor-r1-review-2026-08-27.md` and `📓️coordinator-instance-close-native-r1-review-2026-08-27.md`.

The sole compiler lease remains with the publication executor. Exact-close diagnosis and microsecond clock/deadline work are current; `📓️coordinator-monotonic-clock-host-audit-2026-08-27.md` records the live owned-Wasm zero-clock defect. Native UI parity remains 1/1; all-app/Wasm/browser gates remain open.

The publication executor retains the sole fleet Rust lease. Real CAD close R3 passes 4/4 after the harness compile repairs; the separate constructor R1 fails at the exact setContributions command classification join, followed by strict Store Drop during unwind. The command declaration repair is source-ready and constructor R2 is running. Isolated native UI typed-default parity passes 1/1 (18 component vectors plus style/accessibility defaults), 90 skipped, 0.034 seconds; the coordinator read both the complete test and actual output. Latest replication is 8/8 and expanded registered query is 18/18. Kernel group 6/6 and member 3/3 remain scoped prior passes. No coordinator or other fleet Rust process competes; unrelated peer Cargo processes are preserved. See `📓️coordinator-cad-constructor-r1-review-2026-08-27.md`. Older queue paragraphs are historical.

Next exact ownership work: actual microsecond step/deadline precision (legal sub-millisecond grants currently floor to zero), generation-bound joined app-close completion, and the still-disabled full Child Store snapshot/revision/generation/log/undo group. Playbook has the same app-command/action-classifier mismatch as CAD and is assigned for a scoped declaration/test repair; no blanket admission is authorized. Fresh Wasm and all-app runtime gates remain required.

## Latest Successful Native Cohorts

The latest verified native Presence cohort is now **12/12**, including exact peer publication base/factory freshness. Canonical reader/sealer partial-error accounting is **18/18**; the cold-rebase prerequisite, latest-wins r17 **10/10**, and plugin local-interaction **9/9** also executed. The coordinator read the actual outputs; later reports supersede historical paragraphs below. See `📓️coordinator-peer-publication-freshness-2026-08-27.md` and `📓️coordinator-ownership-gates-followup-2026-08-27.md`.

The publication executor retains the sole fleet Rust lease for replication contracts, then Dag's coherent expanded plugin local-interaction cohort, then actual CAD close/constructor compilation. Dag owns transport/API mounting and coordinates stable plugin source windows. No coordinator or other fleet Rust process is being launched; unrelated peer compilers remain untouched.

Subsequent exact results: replication local-interaction **7/7** passed; expanded plugin cohort **13 passed / 3 fixture decode failures** needs the coherent progress/channel rerun. During the plugin source window, the independent warm kernel group cohort passed **6/6**, 890 filtered, 0.02 seconds after 9.50 seconds compilation; coordinator read the actual output (`🧪️member-group-read-regression-r3-native-2026-08-27.txt`). This covers history/cursor/compound-envelope observation, not the still-disabled mounted atomic Child pipeline. The executor also reports member regression **3/3**, and has preflighted the actual CAD target, four close-law filter and real constructor filter. CAD has not run yet.

## Current Registered Path Pass and Follow-On Queue

Registered latest-wins r15 passes one test, 432 filtered, 0.04 s runtime after 3m40s compilation. The coordinator read its actual DEBUG publication/cleanup output. Full r16 then aborts on a foreign local-Presence factory supplied by the fairness fixture; the exact installed-factory correction retains the production guard. The publication executor holds the single fleet Rust lease for Presence ten, then the full latest-wins ten and mounted interaction capture/retirement/page tests. Current evidence is summarized in `📓️coordinator-native-renderer-checkpoint-r15-2026-08-27.md`; the r11 dependency paragraph below is historical after an external peer added and repaired a real MutationLeaf macro.

## Safe Verification Prerequisite

Latest execution: registered-dispatch **r11 exited 1 before tests**, with one unresolved `dsl_derive::MutationLeaf` facade import. Root read the compiler diagnostic and actual derive exports; `📓️coordinator-native-r11-dependency-review-2026-08-27.md` records the narrow repair assignment. The Flow executor may use a short dependency-only check lease coordinated with the publication executor; the latter resumes r12 once that join and its rejected-actor owner packet are coherent. Artifact protection is not the current blocker.

This prerequisite is now satisfied within its stated scope: the coordinator reviewed protected-ancestor/subtree checks, candidate filtering before shallow deduplication, and the immediate repeated deletion admission. The canonical existing library test independently passes one synthetic test / 58 assertions, exit 0; full stdout is retained in `📓️coordinator-active-ticket-protection-test-2026-08-27.md`. No real cleanup was run. The guard is not an atomic cross-process filesystem transaction, and the historical cause of artifact loss remains unknown. The publication executor owns the next native build after its ongoing opaque local-Presence seam reaches a coherent source checkpoint. No coordinator or other fleet Rust build is competing.

A read-only Terra scout confirmed that the ordinary workspace cleaner ignores the existing open ticket manifest when selecting nested directories over 10 MiB for recursive deletion. This is a proven dangerous code path, not proven historical attribution of the current loss. The Flow executor is implementing a closed-ticket-only, fail-safe cleanup guard with schema-first tests; no workspace cleanup is authorized or being run. The full warm `debug` directory is absent, so the next Rust run may need a cold rebuild. The publication executor holds the next compiler lease while completing peer ownership source work; root is not starting a competing build.

## Artifact Loss and Current Source Checkpoint

The Flow metadata check exited 1. Its original log disappeared, but exact Cargo fingerprint diagnostics were recovered to `🧪️flow-core-detailed-check-recovered-r1-2026-08-27.txt` and read by the coordinator. Flow test compilation has eighteen errors: include paths, missing test-macro dependency, stale awaits, unqualified mutation calls, one map type, one genuine missing await, and six Playbook DslValue fixture mismatches. The earlier Brep 171-error layer is no longer the reported blocker. These are compile results, not executed Flow tests; repairs are assigned.

After the cohesive raw-wire, fixed-capacity, reader-return and obsolete-effect source checkpoint, latest-wins r10 failed before tests because Cargo could not write its dependency fingerprint: the warm target's entire debug/.fingerprint directory had disappeared. The executor reported no full disk and no active fleet Rust command. The coordinator independently confirmed disappearance of the original Flow check log and its own raw-wire and 473-test React logs; no fleet agent reports deleting them. The cause is not established. Observed results are preserved in Markdown reports; absent original logs are not reconstructed as complete evidence. Unrelated rust-analyzer Cargo checks are left untouched.

The source checkpoint now has the exact SnapshotRead alias-release-before-return ordering, atomic erased transfer, three native race laws, cold cursor capacity preservation, UTF-8/slot rejection laws, and sub-page raw-wire retirement. All new native laws remain pending. The publication executor retains the next Rust lease; no competing coordinator build is running.

## Current Lease and R9 Runtime Failure

The Flow executor now holds this fleet's short metadata-check lease for canonical `semio-framework-os-flow-core:check --args=--tests`, after the raw-wire source checkpoint. The publication executor has no active fleet compiler and is repairing the r9 capacity failure in source. The lease serializes only this fleet; unrelated peer Cargo processes are preserved and do not invalidate the rule.

Registered latest-wins r7 exposed premature pending-key transfer, r8 exposed premature fixture-job close completion, and r9 reached real Store publication but failed its preinstalled applied/revision capacity guard after the rebasing edit. No completed ten-test pass is claimed. The coordinator inspected r9's runtime diagnostics directly. Its exact log is `🧪️member-latestwins-registered-r9-native-2026-08-27.txt`; the prior 9-pass/1-fail full cohort remains historical evidence, not the current registered-path result.

The refreshed full command census is RED: 773 rows, 106 bounded, 316 BatchOnly, two forbidden, 513 remaining, 796 self-tests, eighteen failure categories. The six-entry ledger decrease is solely a Procedural3d BatchOnly reclassification; accepted command rows did not increase. See `📓️coordinator-command-census-review-2026-08-27.md`.

## Current Run: Flow Dependency Failure, Publication Rerun

Latest-wins r6 is **RED/SIGABRT**, not a completed ten-test result. Fixing the manifest exposed the next actual-path invariant: the fixture established a two-core process pool, then normal app startup requested the machine's ten-core configuration. The checked global pool correctly rejected the mismatch; strict Store Drop during unwind then aborted the process. The fixture now uses the same process-entry configuration as the real mounted path. The publication executor has the exclusive compiler lease for this repair and cohesive Presence/CAD adoption; the coordinator is not launching a competing Rust build. Log: `🧪️coordinator-latest-wins-native-r6-2026-08-27.txt`.

Expanded latest-wins r5 completed: **9 passed, 1 failed**, 414 filtered, 0.39 s runtime after 1m53s compilation. The registered-dispatch fixture lacks a required window declaration; it fails at construction before dispatch. The other nine laws pass. The fixture-only correction is source-ready and a ten-test rerun follows the active two-test compound-envelope native gate. Full log: `🧪️coordinator-latest-wins-native-r5-2026-08-27.txt`.

The shared Flow native r1 gate exited 1 before any of its eleven selected tests executed. Native stdio code generation completed under the unchanged 60-minute build-only allowance; the next dependency, `semio-s-plugin-flow-extension-brep`, failed with 171 diagnostics, including incompatible `Operator::evaluate` signatures and missing Brep methods. This was a compile failure, not a timeout or a test pass. Exact retained output: `🧪️coordinator-flow-shared-native-r1-2026-08-27.txt`. The Flow executor is diagnosing the actual API/feature join without disabling required extensions.

Copier, Flow retirement, numeric parameter, session retirement, four generation-root laws, and three parameter-intent laws remain native-unverified. No completed Flow test metadata exists to reuse. The coordinator now owns the single compiler lease for the expanded ten-test `retained_latest_wins_` cohort; the two compound-envelope read tests follow. Real CAD close adoption is being implemented before the constructor retry. Peer compiler processes are not stopped or modified.

Coordinator source gates now pass 785 tool-job self-tests, the 42,405-byte Flow session fixture, and the Flow app oracle including four parameter cases/ten hostile rejections and two independent Node Buffer retirement oracles. These are not native or browser gates. Logs: `🧪️coordinator-tool-job-self-tests-r11-2026-08-27.txt`, `🧪️coordinator-flow-session-source-r2-2026-08-27.txt`, and `🧪️coordinator-flow-parameter-source-2026-08-27.txt`.

## Verified Native Gates

Full neural r3 is **PASS: 43 passed, 0 failed, 0 filtered**, 0.01 s runtime after 2.43 s compilation. This includes the newly added evaluation-owner law. It does not certify the still-incomplete retained Flow host evaluation or all seven extension boundaries. Log: `🧪️coordinator-neural-full-native-r3-2026-08-27.txt`.

Compound-envelope read r1 is **PASS: 2 passed, 0 failed, 874 filtered**, 0.02 s runtime after 1m37s compilation. All four capture/commit/abort fixture cases run, including a commit injected during initial snapshot serialization and rejection of foreign history/cursor authority. This certifies the scoped compound history/cursor observation, not the still-pending Store snapshot/revision/generation group or mounted atomic child operations. Log: `🧪️coordinator-group-envelope-native-r1-2026-08-27.txt`.

Exact factory proof r5 is **PASS: 1 passed, 0 failed, 417 filtered**, 0.01 s runtime after 21.95 s compilation. This executes the hostile exact-runtime-authority test after the synthetic command classifications were repaired. It is not the real CAD manifest constructor gate. Log: `🧪️coordinator-plugin-proof-native-r5-2026-08-27.txt`.

Latest-wins native r4 is **PASS: 4 passed, 0 failed, 414 filtered**, 0.06 s runtime after 44.55 s compilation. Exact fixture draft ownership repairs the previous abort without weakening close guards. Full debug evidence covers six target scopes, five Presence boundaries and ten Document/ACK cases. The separate registered-factory admission/rebase/collision/fairness pipeline remains pending. Log: `🧪️coordinator-latest-wins-native-r4-2026-08-27.txt`.

Current neural full regression r2 is **PASS: 42 passed, 0 failed**, 0.02 s runtime after 1.14 s compilation. Explicit batch/error ownership scopes restore the previous regression while keeping strict final-owner guards. The expanded value/cache source oracle also passes. Logs: `🧪️coordinator-neural-full-native-r2-2026-08-27.txt` and `🧪️coordinator-neural-retirement-source-r2-2026-08-27.txt`. Flow and retained evaluation remain separate gates.

Latest-wins r2 compiled but aborted at app cleanup. Exact r3 reproduction identifies a missing TestApp draft-store disposer at the fixture's app close, followed by strict Store Drop during unwind. The executor is repairing fixture ownership; no native latest-wins pass is claimed. Full retained log: `🧪️coordinator-latest-wins-native-r3-2026-08-27.txt`. The separate source-only self-tests pass 762, not a full app census.

Latest-wins native r1 failed at plugin compilation with 15 owned-map namespace E0433 errors; none of its four new tests executed. Source namespace repair and the next coherent integration boundary are assigned to the publication executor. Log: `🧪️coordinator-latest-wins-native-r1-2026-08-27.txt`.

Previous full neural regression r1 was **RED: 14 passed / 28 failed / 42 total**. The seven focused retirement laws passed while existing evaluation and cold scope ownership was incomplete. R2 above supersedes this result without deleting its failure evidence. Log: `🧪️coordinator-neural-full-native-r1-2026-08-27.txt`.

- Neural value/cache retirement: **7 passed, 0 failed, 35 filtered**, 0.01 s runtime after 53.04 s compilation; two source fixtures/four hostiles also passed independently. Full neural regression and host integration remain separate gates. Log: `🧪️coordinator-neural-retirement-native-2026-08-27.txt`.

- Expanded owned ordered collections r2: **16 passed, 0 failed, 192 filtered**, 1.46 s runtime after 9.97 s compilation. This adds shared/final release, exact shared input, eight-worker final-owner racing, and ordered-set laws to the eleven original tests. Primitive adoption is authorized; Dictionary/Flow/Generator integration tests remain pending. Log: `🧪️coordinator-ordered-map-native-r2-2026-08-27.txt`.

- Repaired canonical native r6: **22 passed, 0 failed, 847 filtered**, 0.22 s runtime after 37.12 s compilation. All four reader laws now pass, including the unchanged closing-transfer regression and the new unclosed-drop/unwind guard. The two expected panic messages are caught by the passing guard law. Reader adoption is authorized for Flow/Generator typed roots; this does not certify their integrations. Log: `🧪️coordinator-store-canonical-native-r6-2026-08-27.txt`.

- Expanded retained-group native gate r2: **4 passed, 0 failed, 864 filtered**, 0.00 s runtime after 41.99 s compilation. Both history laws and both new cursor laws execute. The expected caught panic confirms mutation is rejected while a provisional group is staged. This certifies history/cursor ownership and decision visibility, not the still-pending mounted atomic group/log/undo pipeline. Log: `🧪️coordinator-group-cursor-native-r2-2026-08-27.txt`.

Expanded canonical r5 compiled in 1m27s and started 21 tests. The new reader's large-map and cancellation cases passed, but the test named rebound-root rejection failed in its second, cancellation-close half: `take_root()` returned Some after traversal depth reached zero but before the encoder cleared its root identity/started state. The reader's destructor then panicked during unwinding and aborted the process. This run is **RED**, not 21/21. The earlier 18-test run remains historical evidence, and the new reader cannot be adopted until repaired and rerun. Log: `🧪️coordinator-store-canonical-native-r5-2026-08-27.txt`.

- Owned OrderedMap: **11 passed, 0 failed, 192 filtered**, 0.84 s runtime after 20.21 s compilation. Native long-key grants, lookup, AVL invariants, all-phase cancellation, exact final-owner transfer, strict Drop guards, and worker transfer executed. The independent source/oracle gate also passed. Shared Flow/Dictionary adoption is now authorized, but not yet completed. Log: `🧪️coordinator-ordered-map-native-2026-08-27.txt`.

CAD constructor r2 compiled the updated OrderedMap/kernel/plugin and native host dependencies but exceeded the canonical 20-minute build budget during active stdio code generation. No CAD test ran; the wrapper's generic lock-contention suggestion is not the observed cause. The coordinator is moving the eleven small OrderedMap native laws ahead of the next CAD attempt to unblock shared adoption. CAD remains mandatory and will be retried with a larger cold-build budget, not a changed runtime watchdog. Log: `🧪️coordinator-cad-constructor-native-r2-2026-08-27.txt`.

Latest startup attempts: plugin r4 compiled and ran one test, but the synthetic fixture was rejected for unclassified commands (0 passed, 1 failed, 411 filtered). The fixture now declares honest BatchOnly classifications before its one-row hostile authority promotion; r5 is pending. CAD constructor r1 stopped upstream on two new OrderedMap reference-type errors, now source-corrected; no CAD test ran. Logs: `🧪️coordinator-plugin-proof-native-r4-2026-08-27.txt`, `🧪️coordinator-cad-constructor-native-r1-2026-08-27.txt`.

- Expanded nextest helper gate: **2 passed, 0 failed, 282 filtered**, 25 assertions in 8.79 s. The second law covers explicit ticket-local artifact locations and retention; future nextest calls use `SEMIO_TEST_ARTIFACT_DIR` inside this ticket. Log: `🧪️coordinator-nextest-artifact-native-2026-08-27.txt`.
- Provisional group-history ledger: **2 passed, 0 failed, 861 filtered**, 0.04 s runtime after 2m22s compilation. Both direct-reader/serialization atomic visibility and one-owner abort/foreign-decision rejection executed. This tests history storage only; cursor, snapshot, revision, log, and mounted child-group integration are not yet certified. Log: `🧪️coordinator-group-history-native-2026-08-27.txt`.
- Full kernel `canonical_` rerun: **18 passed, 0 failed, 843 filtered**, test runtime 1.88 s after 3m29s compilation. This includes all eleven current sealer laws (seven original plus four borrowed-map laws), the repaired load/reset interior-ABA regression, the new cold-seed duplicate-owner/preflight regression, and five other canonical laws. The two previously aborted load/seed cases now pass. Log: `🧪️coordinator-store-canonical-native-r4-2026-08-27.txt`. New provisional group-history tests were authored after this compilation snapshot and require a separate run.
- Test execution filter router: **1 passed, 0 failed, 281 filtered**, 20 assertions with five language-neutral nextest command vectors and an independent Node parser. The coordinator first reproduced an Nx self-recursion caused by inferred package scripts overriding the explicit target; the scoped package inference exclusion repaired the canonical Nx route. Exact log: `🧪️coordinator-nextest-filter-native-2026-08-27.txt`.
- Store retained members: **3 passed, 0 failed**, `retained_member_` filter through Nx. Ordered exact-owner publication, stale/cancellation rejection, and real history reservation without partial visibility plus bounded abort all executed. Runtime: 0.13 s; log `🧪️coordinator-store-member-native-2026-08-27.txt`. This is not the still-unimplemented atomic child-group decision/log/undo pipeline.
- Store canonical sealer: **7 passed, 0 failed**, exact `canonical_edit::tests::` filter through the awaited kernel Nx test route. Includes actual 16 KiB/64 KiB semantic data, canonical oracle parity, checkpoint replay, exact authority/allocation binding, every-phase cancellation, and one-byte Unicode retirement. Full test code compiled; the exact run finished in 0.32 s after 35.84 s compilation. Log: `🧪️coordinator-store-sealer-native-r3-2026-08-27.txt`. This does not prove the planned recursive-map traversal extension or app root preparation.
- `semio-s-plugin-stdio --lib`: passed. Two PDF mutation record descriptors were corrected to the canonical past-tense `Added` and `Inserted` names; other concurrent stdio repairs were preserved.
- `semio-s-plugin-cad --lib`: passed, final exit 0 in 2m 27s using the ticket-owned `🧱️cargo-target-cad`. The first app-level pass found exactly one `InteractionState.selection()` misuse; the corrected field lookup compiled.

## Current Queue

Flow reached its own crate after its shared dependencies compiled, then failed with 212 errors (exit 101). The first layer includes missing owned `semio-framework`/`semio-framework-job` dependencies, incorrect plugin-root imports, and remaining decorative asynchronous implementations of synchronous editor/store/schema traits. Its command handlers were mechanically aligned with the synchronous `app_commands!` dispatch signature, but that did not clear these wider interfaces. Exact compiler diagnostics are retained in `🧪️coordinator-flow-native-compiler-r1-2026-08-27.txt`. The repair packet and rerun are pending. Sequence, Note, Process3d, Lowpoly, Writer, small app cohorts, and newly rotated app cohorts follow. Wasm and runtime gates remain outstanding.

The Sequence task-runner validation was `NX_DAEMON=false CARGO_TARGET_DIR=<ticket>/🧱️cargo-target-cad bun x nx run @semio-tech/sequence-plugin:test-quick --skip-nx-cache`. It reached the real nextest binary-build phase, then failed upstream in stdio with six E0624 errors: three private `between` associated functions and three private `is_empty` methods. It did not reach Sequence tests and does not revoke the earlier, differently featured native library check. Exact locations were not emitted by the captured nextest wrapper; the Flow executor now owns the exclusive compiler lease to obtain the detailed dependency diagnostics, repair that layer, and rerun Flow. A separate executor owns the retained child/member publication seam.

The coordinator also ran `NX_DAEMON=false bun x nx run workspace:verify-interactivity --skip-nx-cache --args='tool-jobs --self-test'`: exit 0, 618 self-tests clean. This verifies the updated cross-file factory resolver and hostile source laws; it does not execute Rust app code or certify maximum-envelope latency.

### Current Shared-Tree Recheck

The Flow executor's warm native rerun has compiled the changed Store/kernel and plugin libraries and reached stdio again. The invocation has not yet finished, so it is not a Flow pass. After that pass exposes or clears app-level errors, the queue includes the seven canonical-sealer tests, member publication tests, adversarial exact-factory tests, and the real CAD manifest-constructor/bounded-close regression. Wasm and fresh browser verification remain required.

That Flow run ended with two upstream stdio errors; the coordinator took the compiler lease. The first broad `canonical_` run compiled but aborted in an older canonical-revision test whose fixture lacked mandatory Store retirement owners. That fixture now installs its exact owners and closes both stores; its rerun remains separate. A second exact attempt was stopped before Rust by a concurrent peer JCO helper import with one too many parent directories; the import alone was corrected and peer additions preserved. The third, exact seven-test sealer run passed as listed above. Retained-member tests are now running; plugin/CAD startup tests follow.

The three retained-member tests passed. The older canonical-revision test then failed again, now inside production cold seeding: a valid first operation can reuse its Edit ID, strict DAG insertion rejects that duplicate, and the error path drops an already-populated DAG. Its exact log is `🧪️coordinator-store-revision-aba-native-2026-08-27.txt`. The Store executor is repairing cold-seed identity admission/ownership without weakening terminal Drop checks. The plugin's exact factory-proof native test is currently compiling its existing Wasmtime oracle dependencies; CAD follows.

Generated temporary projects in other ticket folders repeatedly polluted Nx's workspace graph. The executor added the explicit root ticket-scratch exclusion to `.nxignore`, verified 183 real projects with Flow present and the duplicate transaction fixture absent, and preserved all fixture files. This removes graph discovery of test scratch; it does not bypass any real project task.

## Child Publication Finding

`ChildContentView::{revision,typed_read,dialect,slots,is_empty}` only access a captured immutable root and now return synchronously, allowing app-owned work cursors to read exactly one child item without a future or whole-child clone. This does not itself make child publication bounded: the typed child-output lane still invokes `dispatch_emit_group`, which clones/decodes/previews/applies a whole child operation group. That path must be replaced by a retained member-owned publication cursor before newly converted Child routes receive migration credit.

No all-app runtime, 8 ms, or zero-dependency completion claim is made here. Compiler tasks owned by this fleet are serialized; unrelated peer builds are left untouched.

### Exact Factory Proof Compilation

The third plugin attempt compiled the coherent kernel/cursor library and the new hostile helper, then found one remaining sibling-private type use in the older copied-owner assertion. That assertion must also move behind the test-only authority helper. No runtime assertion ran in this attempt. Log: `🧪️coordinator-plugin-proof-native-r3-2026-08-27.txt`.

The second plugin attempt was interrupted at compilation by a concurrent, incomplete ArtifactCursor root conversion (21 missing-field struct-literal errors); it did not reach the plugin test. The coordinator is waiting only for that coherent source boundary before the next compiler launch while continuing independent review. The explicit fixture-scope fix is source-ready. Log: `🧪️coordinator-plugin-proof-native-r2-2026-08-27.txt`.

The plugin test reached its own crate after compiling its existing Wasmtime test-oracle dependencies, then failed with 15 private-access errors in the newly authored factory-proof fixture. No factory-proof test ran. The executor is correcting fixture placement/access without exposing private runtime authority. The captured console chunk was truncated by the command tool; all error diagnostics were recovered from Cargo's exact `output-test-lib-semio_framework_plugin` fingerprint record. The combined diagnostic record is `🧪️coordinator-plugin-proof-native-r1-2026-08-27.txt`.

The next root-owned native invocation is the kernel `canonical_` filter: the seven original sealing laws, the four borrowed-map laws, the repaired load/reset identity regression, and cold-seed ownership coverage. CAD real-constructor tests follow. These new gates remain unverified until their individual executions finish.

## Preparation Grant Finding

The mounted typed publication path supplies exactly one item and 4,096 bytes per Store advance/close turn (`TYPED_OPERATION_RESULT_PAGE_BYTES`). App preparation owners must make forward progress under that actual grant at their maximum admitted envelope. A 32/64 KiB whole-root clone cannot be justified by a declared cap, and requiring a grant larger than production ever gives would strand the job. The active cohort executors have been instructed to use byte-cursor preparation and retirement rather than enlarging the shared grant. Earlier preparation cohorts need the same runtime-capacity audit before final acceptance.

### Confirmed Flow Counterexample

Source inspection found `FlowDirectStoreWork::close_step` requiring `maximum_bytes >= FLOW_STORE_MAX_TEXT_BYTES` (16,384) merely to retire either retained preview vector. Production supplies 4,096, so this path can remain blocked indefinitely even for a tiny retained vector. Its Store preparation also calls whole-root preparation and sealing in one advance and reports one completed byte regardless of actual work. These are unresolved runtime defects, separate from native compilation, and must be repaired and tested with the actual production grant before Flow receives a runtime pass.

The active scalar-Config cohorts may instead admit a strictly measured complete envelope that fits one production turn. That is a safe limited admission policy, not completion of support for larger valid configurations. Larger configurations and vector-backed roots still require resumable preparation and retirement.
