# Remaining App Command Ledger

The coordinator parsed the retained current full-census JSON directly. Its gate is RED, with 773 rows, 771 unique rows, 350 bounded classifications, 248 accepted command rows, 315 BatchOnly classifications, two forbidden rows and 270 remaining commands. These are different, overlapping projections; their counts must not be summed into a completion percentage. The separate source self-test count is979 and does not certify command execution.

## Remaining Commands by Plugin Family

| Family | Remaining rows |
| --- | ---: |
| Remodel | 39 |
| Shooting | 37 |
| Puzzle | 37 |
| Forms | 29 |
| Draw | 26 |
| Procedural | 23 |
| Block | 23 |
| Architect | 21 |
| Raster | 16 |
| Space | 14 |
| Lowpoly | 4 |
| CAD | 1 |

Of these,235 have no explicit factory/bounded-first-step/disposition proof;25 have classification but no exact registered app-owned retained reducer factory/builder;10 lack the exact publication contract or preparation owner. The CAD row includes a reported source scanner mismatch after real command semantics replaced a no-op action setter; its repair remains queued, not accepted by exemption.

The twelve current failure categories also include immutable child-content roots and terminal retirement, Jack and Trinity retained envelope routes, exact child snapshot ownership inventory, instance/actor ownership, nested close payloads,16 process-global payload stores, six publication-lane contracts, four preparation owners and35 fail-closed media importers. The four pipeline booleans in the JSON are source predicates, not proof that these remaining failures are resolved.

## Execution Order

Finish exact job verdict, typed ownership and live renderer publication boundaries before adopting more app reducers. Complete ordinary ingress, tutorial/interaction restore and exact end-to-end close joins alongside the app cohorts. Then reopen the full census against coherent current source and execute the corresponding real app/native/Wasm/browser paths. No command is made interactive by relabeling BatchOnly, installing a no-op setter, adding a source exemption or weakening the hard callback ceiling.

Fresh demonstrator evidence is separate: the coordinator read the actual Sourcing2/2 native footer and DEBUG output in the peer's `🧪️sourcing-owned-close-1.log`: demo-stock publishes10 rows and empty-curation0, with exact close; the second test covers shared56-byte presence retirement. The peer's catalog rebuild is only3/22, with extension actor exports and Space mutation descriptors under repair. Fresh Flow clock and complete all-app browser workflows remain pending.
