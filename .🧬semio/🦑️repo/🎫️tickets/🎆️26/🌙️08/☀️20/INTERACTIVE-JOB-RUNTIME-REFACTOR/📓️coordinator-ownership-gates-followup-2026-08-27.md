# Subsequent Ownership and Gate Verification

## Canonical Partial Error Accounting

The coordinator read the typed error, reader and sealer implementation and the actual green native output. The error now carries initialized `written_bytes` separately from its reason. Both indexed and borrowed encoders report the exact failed-chunk prefix; the reader increments its cumulative count on success and failure, marks failure, and keeps root ownership for explicit close. The sealer retains failed prefix work in its transcript/checkpoint while refusing prepared authority.

The canonical_edit group passes **18 tests**, zero failed, 876 filtered, 0.17 s runtime after 30.20 s compilation. DEBUG output proves eleven initialized bytes under grants 1/7/256/4096 in both source modes, and fifty-eight failed sealer prefix bytes under grants 1/7/4096 without minting authority. Existing reader/cancel/replay/large-domain laws also execute. The guard panics are intentional and caught. Log: `🧪️member-canonical-error-progress-green-r1-native-2026-08-27.txt`. The executor separately reports a post-API plugin-query nine-test pass; its source consumers are coordinated with the interaction packet.

## Distinct Original Presence Close Factories

The coordinator read the updated detach/retirement module and actual eleven-test output. `begin_retirement` no longer accepts a caller-selected factory. It captures the installed local factory and separate peer factory before detaching, rejects absent required authority before mutation, and uses the local factory for late returned local reads. Peer-root cleanup keeps the peer factory. Empty/no-peer stores do not invent an app fallback.

The new test first reproduced foreign=2/local=0/peer=0, then the full retained-Presence cohort passes **11 tests**, zero failed, 884 filtered, 0.03 s runtime after 20.69 s compilation. DEBUG output is foreign=0/local=1/peer=1, including a read returned after detach. Log: `🧪️member-presence-factory-binding-green-r1-native-2026-08-27.txt`.

A separate adjacent source defect is assigned next: peer commits still need exact originating base-root and installed-factory authority, so stale or foreign same-type commits cannot replace another Store root. The eleven close/reader tests do not establish that publication law. The executor will preserve every rejected owner and explicitly retire base aliases before proceeding to CAD verification.

## Static Gate and Real Timing Caveat

The renderer executor repaired the three stale reconcile bindings without changing production capacity or step budgets. The coordinator read its actual gate output: the scanner now proceeds and reports nineteen thread-spawn findings, all in standalone test-source files. It does not propagate their parent cfg(test) module gates. Those findings have not been broadly suppressed. The scanner also currently treats any cfg expression containing the word test as test-only, which would wrongly exclude not(test) or mixed production feature conditions. A sound test-scope projection with hostile fixtures is assigned; unknown production reachability must remain included.

Separately, the actual reactor allows at least one reconcile opportunity even at zero fuel, batches up to 1,024, checks its outer deadline only every 64, and constructs inner contexts with an unlimited deadline. A source predicate counting one drive call in the loop is not an eight-millisecond whole-poll proof. Fake-clock/fuel laws and actual mounted timing remain required. These are recorded source findings, not claimed measured violations.

The independent full React checkpoint remains 506 passing tests. The latest full command census remains RED and all-app/fresh-Wasm/zero-external gates remain open. No ticket or goal is complete.

