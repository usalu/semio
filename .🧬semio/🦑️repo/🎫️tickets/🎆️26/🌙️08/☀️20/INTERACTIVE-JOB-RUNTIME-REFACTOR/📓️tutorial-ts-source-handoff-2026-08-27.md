# Tutorial TypeScript Source Handoff

The coordinator proposed moving the seven authored TypeScript tutorial joins to the UI lane after its decoder grant packet. This lane has no active TypeScript edits. Native `InteractionConfigMutation`, `SetInteractionState`, retained root/update and future Store publication remain here. The coordinator's strict R32 still reports the exact seven existing diagnostics; no new pass is claimed by this handoff.

Canonical complete state and sparse restore types are in `🧰️framework/🔨️modules/📡️replication/📡️wire/🏠️local-interaction/🟦️component.ts`: `LocalInteractionState` owns domain-keyed selection, active mode and active granularity. `LocalInteractionRestore` distinguishes full replacement from sparse domains, with explicit nullable fields for clearing. The approved tutorial content field is `localInteraction: LocalInteractionState`, not selection-only JSON. Historical tutorial content never carries live publication authority.

The actual TypeScript query seam is `AppChannelClient.readLocalInteractionPages(consume, signal)` in `🧰️framework/🛍️products/💻️os/🟦️component.ts`; it returns the exact current `LocalInteractionIdentity` only after its query closes. Page input/ACK and handle-lifetime request sequence ownership remain necessary. Query page data must become independently owned typed state before its input callback resolves; no whole-result join is implied by that API.

There is no mounted live restore-command API yet. `applyLocalInteractionRestoreCold` is explicitly cold composition and cannot substitute for retained topology validation or atomic Store publication. Native root/update unit gates passed earlier, but the root has not been installed into the live Store. A genuine restore must capture fresh document/config/interaction/topology authority and validate the exact current token atomically; privacy remains local-only with no Presence broadcast.

`ShellHelpers/🟦️component.tsx` still references the removed `selectionJson` at its capture/apply/change paths (around 2102, 2153, 2197). Authored fixture and brand joins are part of the same cutover. Preserve full-versus-sparse absence semantics, comma-containing IDs, modes, granularity, anchors and cancellation. No empty-selection fallback, compatibility alias, or synchronous old-view copy is authorized.
# Authored Ordering Coordination

UI's cold authored composition contract now explicitly names `domainOrder: "utf16-code-unit-lexicographic"`, including numeric-looking keys `10`/`2` and astral `😀` versus U+E000. Native Rust UTF8/BTree/codepoint ordering is not equivalent to that contract. A future native TutorialUiSnapshot/Change producer that shares authored ordering must implement and test the exact UTF16-code-unit comparator; current retained interaction root/query traversal is not claimed to do so. This cold authored order is neither live restore update scheduling nor generation authority, and historical fixture identities remain unusable for live restoration.
