# Mandatory Descriptor Canonical Mount Review

The coordinator read the complete mount73 report and all eight measured mount-proof rows, then read the canonical Rust test body and exact glue join. Three independent `cmp` commands exited0 with no output against the preserved prepared Rust, vector and schema. Fresh hashes match canonical2bc2741f/65220ea3/77b9a603, glue147e1f68 and unchanged basee5f2. This is source verification, not native execution.

The only glue insertion is the reviewed134-byte cfg(test)/path/module block after macro_export_tests. An independent read-only stream removal hashes to `17448e95b31aab2692a8d3917bec20245647cdd23128b62b16b2bc8a140a8be3`, the complete prior glue preimage17448e95. The first coordinator inverse command had an unescaped Perl delimiter and failed before processing source; its downstream empty-stream hash is explicitly not an inverse result. The corrected delimiter/pipefail command above exited0 and did not modify files.

The native law parses the actual lower Mutation source with existing syn, prints its exact three facts with DEBUG, and compares against the adjacent required-items-accepted row. Base defaults and sentinel remain deliberately present. An expected semantic RED would prove that source contract only; compilation failure, no test selection or unavailable output must be reported separately. No concrete-caller/auto-trait/OS/Plugin readiness is inferred.

The source mount is accepted for one separately authorized native run through @semio-tech/dsl-derive-rs:test, --lib and exact mandatory_mutation_descriptor_tests::mutation_trait_matches_adjacent_required_declaration_facts, sole executor, existing master target and budgets. This review does not itself launch the command. Preserve all source inputs through its terminal; no base edit before actual RED.

## Exact Native Dispatch Authorization

The coordinator subsequently read the complete native R1 preparation, all85 selected paths and28 derive/hash membership names, and actual shared nextest argument-partition/runner bodies1540–1688. Mutation explicitly holds the compiled/base inputs; taxonomy holds the catalog, loader, budget, derive metadata and both current launch files. Pending400.997 publication is deferred. The sole Retained executor was authorized to run that exact one-test command once with jobs2, the existing master target, build budget3600000, coverage0 and exhaustive profile. Fresh source capture is mandatory; there is no retry, repair, broader selector or follow-on native authority. Outcome remains pending at this authorization note.

