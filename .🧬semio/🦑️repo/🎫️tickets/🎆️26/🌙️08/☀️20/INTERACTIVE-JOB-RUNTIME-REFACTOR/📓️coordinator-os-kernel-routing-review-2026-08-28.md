# OS-Kernel Routing Review

Root read the complete [substantive source RED/proposal](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/📓️os-kernel-source-native-routing/📝️.md), [separate final-expectations packet](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️17/END-TO-END-TAXONOMY-NORMALIZATION/📓️os-kernel-source-native-routing/📓️final-expectations/📝️.md), and both current OS Rust package router/project files. The current additive four classes use the existing test-level resolver and budgeted native helper, and three exact lazy zero-argument source-oracle exports. Existing test/check/scalar-wire and package-generation routes remain separately preserved.

Approved source boundary: the two package metadata files, the five exact semantic member names in the two existing registries (second-level codec/backbone; third-level runtime/send/detach), plus separately collision-reviewed canonical launch registration. There is no Cargo/dependency/profile/target/budget change, no alias from existing differently spelled codec, and no broader semantic pattern. Historical version1 absence observations must remain historical; the final live-admission oracle is a separate packet.

The native route selects only semio-framework-os-kernel --lib with sync,ureq, matching the actual WGPU dependency edge. It delegates rest arguments to the existing helper; it does not newly prohibit overriding feature/target flags. The coordinator's eventual six-law call will not pass overriding build flags. Actual compile diagnostics and runtime tests remain authoritative; inert helper tests do not prove Send or runtime ownership.

The router is source-present but not yet credited as a coherent metadata release or an executed domain source oracle. Root has made no new source/native call. After the metadata owner's final receipt and vocabulary/launch join, root will run the three registered source oracles, then assign the sole retained executor the separate six-law native RED. No runtime production fix precedes that result.

No source edits, output generation, pin refresh, cleanup or extra compiler authority resulted from this review.

## Launch Proposal Check

Root parsed both current `.vscode/launch.json` and canonical `.vscode/🧩️launch.seed.jsonc` with jsonc-parser: zero diagnostics, proposed4_gate orders401.1–401.4 free, existing401 Store check preceding them. The four proposed names/commands/type=node-terminal/request=launch/cwd=${workspaceFolder} are approved as registration only. Taxonomy owns the fresh collision check and separate producer-equality join.

At this read the peer's inbox-inventory400.995 existed only in the seed. Root notified both owners to preserve it and keep its publication separate; taxonomy confirmed its prior separate five-row request was already in progress. No row was written or silently published by root. Metadata's current19PASS/2RED remains the desired vocabulary failure boundary, not final live admission or native success.
