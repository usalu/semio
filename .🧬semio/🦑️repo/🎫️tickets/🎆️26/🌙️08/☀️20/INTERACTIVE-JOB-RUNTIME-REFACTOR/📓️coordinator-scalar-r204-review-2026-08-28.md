# Coordinator Scalar R204 Review

## Executed Result

Root independently replayed all four fully reviewed frozen declaration controllers using the existing renderer Nx project. The command started at 2026-08-28T04:47:51.791Z and finished at 2026-08-28T04:47:52.272Z; terminal b5e442 exited0. All seven complete selected file identity/hash tuples match before and after. The complete unedited tool start/terminal output and parsed receipt are retained in [proof](🧪️coordinator-scalar-r204-review-2026-08-28.json).

This is declaration/reference execution, not a runtime scalar, private reader receipt, renderer-wide, native, or heap result. The earlier R199 root result remains historical and is not relabeled.

- Values:43, with25 accepted and18 rejected; strict Ajv2 and independent value oracles.
- Validation:8 fixture semantic/shape negatives and2 serial-domain negatives.
- Receipt model:91 traces,2238 rows with Immer parity.
- Close:40 original child suffixes;14 construction prefixes and121 steps;242 short-grant and242 original-fault checks.
- Exact alias extension:139 refusal cases,7 unlink-replay refusals and7 omitted-unlink refusals. Decoder992 and original payload880 charges are unchanged declarations.

Root fully read the [author report](📓️renderer-scalar-alias-and-callers-r200-r204-2026-08-28.md), ticket replay controller, all of the new fourth program, and the current construction/close declaration. The first three programs are byte-identical to the already fully reviewed R199 programs. Root also read the actual two copied Shard test bodies and source payload generator; no edits were made there.

## Decision

Accept the narrowly corrected declaration: state installation retains BOTH original pending.entry and payload.scalar; prefixes7..13 now need their explicit64-byte dual-alias unlink before record refund. Pending cell/record/witness remain available after that unlink. The exact model refuses foreign/missing/one-sided aliases and omitted unlink. The normal full-close plan already contains its explicit unlink and keeps its37 reader-ready turns/3440 work bytes.

The model intentionally abstracts facade/state identity and does not execute private brands or real reference detachment. Its body-empty proof, caught arbitrary fault custody and resource refund remain runtime obligations. It cannot authorize final physical memory release.

The four current ticket JSON hashes are:

- 🧬️contract.json: b1274e014fe26ae97247176095c828602133da31aea089540e3b8769a7fb135d
- 🧬️schema.json: 55126005fc0c9a568e551be0a2e3664c71d8933a32713f936972963c7004cf38
- 🧪️fixture.json: 977ee08e7817f85b80aa4b987e8fa97aa5deea00a0f47e3b3596685a4220c663
- 🧪️schema.json: 70e69baaf15be71182d9b629d75eb28a5684937c3f3581e2230bc6c4f38a0991

## Canonical And Consumer Boundaries

Read the full taxonomy scalar path proposal: four kind-only leaves under resident/🔢️scalar, with one ancestor-wide retained membership in the existing third-level module vocabulary. Renderer test-long already routes the quoted selector; its passWithNoTests option requires observing the three actual collected names and results, not accepting an empty exit0. Canonical sources and launch remain unmounted in this root run. Ticket status/schema identities need an explicit paired provenance change before canonical placement; no silent rewrite of R199/R204 artifacts.

The current113 resolved reader-related calls include transport-byte preservation and real cursor/fault/close laws. Scalar-only mandatory caller replacement is not yet approved. Do not add a second production consumer just to preserve test count, reinterpret arbitrary fields as unrelated scalar types, weaken validation, fake a consumer, or retain an optional unowned reader overload. A source-based destination/observation design is being reviewed separately. No such runtime/API/price change occurred in this packet.

## Subsequent R205 Source Decision

Root subsequently read the entire [opaque-destination decision](📓️renderer-opaque-destination-decision-r205-2026-08-28.md). Withdraw the extra opaque-transfer consumer: the real paged builder already owns original fragment-to-resident-page copying. Existing same-ledger neutral leases can independently observe those actual destination windows; typed Surface bytes require PACK grammar provenance and cannot accept arbitrary field octets. The original byte goldens remain unchanged. This is a test/ownership design decision, not an executed test rewrite.

The actual UI reader window, cursor, serial, exception and private-binding laws cannot be replaced by observing writes. They require the genuine operation-field grammar owner, which remains a separate unmounted proposal. The scalar arithmetic packet alone does not authorize the113-site cutover. Root requested that concrete grammar/parent/variable-owner seam and an explicit canonical provenance-only pointer proposal before implementation.

The exact one retained taxonomy member now has a separately authorized schema-first desired-law packet; scalar canonical sources/tests and launch400.999 remain held. No native hold or compiler lease was issued by that vocabulary authorization.

## Native Work

Root read the entire [independent primary recovery audit](📓️resident-primary-neutral-independent-audit-2026-08-28.md). Its six required law groups were passed to the source owner for the same proposed seven-law packet. Those are coverage/design conditions, not new measured native defects. Canonical resident e23/e81 and prior25 results remain separate; no compiler lease or new native command was issued.

## Subsequent R209–R210 Provenance Review

Root fully read the15-pointer paired canonical provenance proposal. Its runtime-unmounted statuses and normative source-oracle registration wording avoid false assertions about whether files or launch rows presently exist. Accept that exact provenance-only scope, preserving the four frozen originals and43 semantic vectors. The reported R210 paired-schema/inverse preview remains delegated evidence, not a semantic replay. R209's stopped repeated orchestration remains an orchestration failure, not a semantic RED.

The three planned source cohorts must preserve all four R204 controllers: scalar declarations/value laws,91 receipt traces, and both close arithmetic AND exact original-alias model. Root requested concrete ticket-only helper/test bodies and preservation evidence before canonical mount. The real whole-field grammar/variable-owner seam remains separately required before mandatory reader cutover. Canonical files/tests/400.999 remain held through the short resident native capture; taxonomy's lexical member release alone does not mount them.
