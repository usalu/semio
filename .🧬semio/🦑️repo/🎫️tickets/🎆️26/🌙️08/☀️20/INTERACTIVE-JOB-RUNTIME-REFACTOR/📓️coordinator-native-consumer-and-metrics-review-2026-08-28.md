# Coordinator Native Consumer And Metrics Review

## Resident Foreign Refill Boundary

The coordinator read the full Resident R6 report, captured output and exact adversarial test. Thirteen native cases executed: twelve passed and the foreign-refill law failed with `accepted=true`, `consumerDropsDuringRelease=1` and `originalRootTerminal=true`. The test uses the real public consumer alias and another thread; it performs explicit cleanup before assertions and does not infer the failure from counts alone.

The selected correction is a ledger-owned exact-layout typed consumer slot with a privately minted, non-owning, phase-qualified access facade. Closing permanently revokes writes before the root can release the final page. Allocation, initialization, transfer and deallocation remain separately granted. No public `Arc<Mutex<Option<C>>>`, arbitrary destination callback, empty predicate, final-Arc lock hold or capacity-equality receiver is retained.

An actual receiver must be privately minted by the original admitted parent for one exact inline typed field. Its address, extent and alignment must be inside that parent's admitted layout, but range containment alone must not become a public field-selection authority. The actual RuntimeAppCell/Opening implementation must own the field descriptor and its final retirement. The current Plugin factory still constructs whole local Store owners before RuntimeAppCell registration, so the first four Stores and their envelopes must move under an already-installed Opening record. Core private-slot tests do not establish that integration.

## Metrics And Publication Frontier

### Semantic Correction Before Any Native Mount

The staged tuple law is not an accepted failure oracle. Root read the actual redraw producer and the existing `runtime_presentation_authority_and_candidate_identity_change_independently` test. Scene invalidation and observed build input are independent operations today: scene2/input7 is a valid committed state after a scene change, and redraw may later advance the observed build input to8. Pausing between those two existing calls does not reproduce a partial single metrics transaction. No WGPU test mount or native execution occurred.

The new transaction must name its actual per-operation write set. Completion publication plus scene invalidation belong to enqueue/finish; metrics also requires the original EventQueue, surface lane and frame-generation changes to commit together. Observed build input is not automatically the accepted metrics generation. Existing independent updates must retain their semantics. The exact packet must interlock inside one real composite commit, with unchanged fields explicitly modeled, before it can claim a partial-write RED or successful old-or-new observation.

The coordinator read the full reader/writer census. The live WGPU graph contains nine completion-revision producers, scene/input writers, completion consumers, frame and maintenance readers, presenter BeginGpu and ACK readers, and a later snapshot-sink authority. The proposed shared guard must protect the actual write set of each operation; this is not a claim that every intermediate scene/input pair is invalid.

The coherent replacement is one preadmitted committed-state owner with allocation-free try-access. Every authored producer reserves checked successors through that owner and every reader observes one complete tuple or returns Busy/Poisoned while preserving its exact installed owner. The fixed queue/surface/presentation commit may run under the guard only after all fallible work and capacity checks finish; callbacks, GPU work, retirement and unbounded processing remain outside it.

Required native interlocks pause after each internal field write. A foreign reader must return Busy, never block or spin, then observe the whole new tuple and exactly one completion after release. Preflight refusal exposes the whole old tuple and retains the source. ACK reads before taking its witness; BeginGpu and maintenance preserve their candidates on Busy. Poison and final-reader retirement require their own original-owner tests.

No WGPU source edit, output publication, lifecycle acceptance or callback-duration credit follows from this review.

## Output Roster Plan Boundary

The coordinator also read the peer's proposed captured-output roster plan. Installing the original instance as the exact record shell before constructing CapturedReturn, the real output roster or its facade is the correct constructor ordering. The proposed1280/13/13 subset must be proved by neutral census and actual source/finalizer tests before implementation. It explicitly excludes pre-Open allocation, per-output metadata, raw4161-byte backing, parser/projection state, Promise/controller roots and UI destinations.

The return-domain record must stay charged through constructor faults, an empty roster, route revocation and dispose posting. It cannot be exposed to live dispatch until the canonical per-request output reservation, binary receiver and one-response credit are installed. No synchronous whole-result fallback or current-map authority is accepted.
