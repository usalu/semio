Paged Mesh3d cutover census — 2026-08-23 source-only checkpoint

Authority invariants present in ui-scene/math.rs
- fixed owner slots: 256
- fixed owner page size: 16 KiB
- fixed owner bytes: 16 MiB
- fixed process pages: 4,096
- generation + revision + per-slot epoch ABA witness
- schema-first begin/allocate/write/seal
- abort and ready-owner close retire one page per step with terminal-empty witness

Completed production seams
- ui-scene ray pick, UV interpolation, component marquee, and instance marquee consume Mesh3dLease items directly.
- PreparedRenderUpload::Mesh carries only a Mesh3dLease; no CPU Vec is copied into the packet.
- GLB semantic materialization writes directly into the global paged authority, one page/item per turn.
- renderer Ready transfers the sealed lease directly to the exact target World3dState.
- canonical World publication preflights mesh/version/interaction fixed slots before mutation and hands the exact lease back on closing/capacity/stale.
- canonical World interaction and primary render consumers read typed lease items directly.

Exact remaining `Mesh3d` symbol census
- ui-scene/math.rs: 5 references.
  - 3 are the obsolete Vec-backed public type/impl/conversion at lines 954/968/986.
  - 2 are the legacy test fixture at lines 1925/1926.
- infinite/world/component.rs: 17 references.
  - the only production Vec-backed Mesh3d producer is the face-overlay constructor.
  - terrain output now counts one source triangle per turn, allocates one authority page per turn, writes one position/normal/index per turn, and publishes one sealed band lease at a time. Its current JSON-to-flat terrain input decode remains a separate RED source boundary.
  - placeholder geometry is no longer a Vec-backed producer: an analytic fixed-schema cursor allocates one authority page or writes one typed position/normal/index per invocation, then atomically publishes the sealed lease.
  - The WorldMeshBuffers->Mesh3d paint-bake path and old apply_glb_bytes/ingest route were deleted; URL GLB input now has only the retained renderer decoder/publication route.
  - remaining references after the production constructors are legacy test fixtures/source scans.
- infinite/component.rs: 0 references.
  - the mounted crate root now reexports the canonical `world` module instead of compiling a second 4,700-line World implementation. No bespoke root producer, store, hit-test, upload, or legacy Mesh3d path remains mounted.

Field-pattern census (conservative; includes non-Mesh payload structs and test oracles)
- infinite/world/component.rs: 76 `Mesh3d::`/return/HashMap/direct-field patterns.
- infinite/component.rs: 0 such patterns.
- These counts are intentionally not called zero and are the deletion gate for the Vec-backed type.

Current atomic publication boundary
- `publish_world3d_mesh_lease` derives the stable version from generation/revision.
- It rejects and returns `{ fault, id, value: Mesh3dLease }` before mutation if close is active or any fixed registry cannot admit.
- Mesh registry, version registry, and interaction metadata each produce an observed slot/epoch plan.
- Commit runs only after all three plans exist; replacement mesh ownership moves into the one-page close owner.
- `publish_world3d_asset_mesh_lease` additionally rejects a decoder lease whose revision no longer equals the World interaction revision.
- Renderer saturation restores the exact lease into the retained GLB probe for FIFO retry; stale publication transitions the probe into cursorized lease/response close and records a deterministic frame fault.

Explicit RED residuals
- Face-overlay and legacy test producers do not yet use a fixed producer job and the current single blocked internal lease slot is not a complete saturation authority.
- Placeholder production now uses the paged lease authority, but its one-item cursor is only advanced from `render_world_3d`; an explicit scheduler-ready signal still has to guarantee progress when no other invalidation exists.
- GPU mesh upload now retains one exact generation/lease cursor and transfers one vertex or index item per presentation turn. Whole-lease vertex/index loops and the all-upload loop were removed.
- The active GPU upload cursor has an exact close witness and is drained before World mesh authority close. GPU cache eviction/replacement still uses dynamic HashMap traversal, non-mesh atlas/raster uploads remain contiguous, final render/submit remains one opaque step, and the full pending-packet/GPU-table close witness is absent.
- Legacy Mesh3d and its tests cannot be deleted until both component censuses reach zero.
- PNG/JPEG/MVT/SVG semantic outputs, cache retirement, presenter/GPU close, and realm terminal close remain incomplete.
- World3d snapshot producer remains explicitly unclaimed; no JSON fallback is being introduced.

Source-only checks run at this checkpoint
- rustfmt --edition 2021 --config skip_children=true: canonical World source — passed.
- rustfmt --edition 2024 --config skip_children=true: renderer glue — passed.
- scoped git diff --check for those two files — passed.
- Cargo, Nx, Wasm, browser, network, and root lint — not run by instruction.

Mounted-root and placeholder checkpoint — 2026-08-23
- `infinite/component.rs` compiled Mesh3d/direct-field/direct-GPU census: 0.
- canonical analytic placeholder cursor kinds: box, plane, 16-segment cylinder, 16-segment cone, subdivision-1 icosphere.
- ownership: exact generation/revision Mesh3dWriteToken; one page allocation or one typed item per step; write fault and realm close both enter page-stepped abort; production Drop asserts terminal empty.
- publication: only a sealed Mesh3dLease enters the observed-slot World transaction; saturation retains the rejected lease in the existing exact blocked owner.
- authored differential fixture compares every generated position/index and normal tolerance with the prior geometry oracle, plus interrupted cursor terminal close. Fixture is unrun because Cargo remains prohibited.
- scoped rustfmt and git diff --check passed for the mounted root and canonical World files.

Terrain output checkpoint — 2026-08-23
- one retained tile cursor owns the decoded flat source, tile identity, terrain revision, ten reserved mesh generations, current band, triangle/item cursors, and exact write-token/lease state.
- count, page allocation, source scan, typed write, seal, publish, band advance, and source retirement are persistent phases; no production terrain-band Mesh3d or output Vec exists.
- stale terrain revision, invalid source indices/floats, publication saturation, interruption, and realm close all enter the same exact authority abort plus four-allocation/source-id retirement path.
- authored fixtures cover two-band differential geometry, one-page lease close, malformed source rejection, and interrupted terminal close; unrun because Cargo remains prohibited.
- remaining canonical production Mesh3d constructor: face overlay only. Root census remains zero.

Face-overlay output checkpoint — 2026-08-23
- non-test production `Mesh3d::from_buffers` constructors: 0.
- face overlay uses a generation/revision/draw-generation witnessed paged cursor; fixed category
  metadata replaces dynamic buckets and one typed item/page/boundary advances per call.
- exact face-route negative scan: no `Mesh3d::from_buffers`, `FaceOverlayBucket`, selection
  `HashSet<String>`, or `Vec<f32>` bucket.
- mounted root Mesh3d census remains 0.
- obsolete public Vec-backed Mesh3d type, cfg(test) terrain/legacy draw oracles, and cfg(test) World
  fixtures remain explicit non-production references. They have not been compile validated and are
  the next deletion/type-gate packet.

Atomic-family and wake checkpoint — 2026-08-23 (supersedes every earlier count above)
- exact production-and-test spelling census for the deleted legacy type/constructor:
  `rg -n '\bMesh3d\b|Mesh3d::from_buffers|-> Mesh3d\b' 🧰️framework --glob '*.rs'` — 0 matches.
- the ui-scene and canonical World differential inputs are explicitly named
  `LegacyMeshOracleData`, exist only under `cfg(test)`, and publish through the paged writer before
  reaching production consumers. No old public Vec mesh type, constructor, reexport, field
  consumer, or compatibility adapter remains.
- face-overlay categories publish beneath generation-qualified staging keys. The visible generation
  and its three fixed colors change only after every nonempty category is sealed and published.
  A partial or stale superseding family leaves the last complete generation visible. Dynamic realm
  retirement clears one color/scalar owner per grant and includes the visible/retired generation
  witnesses in terminal-empty.
- terrain band draws are visible only after the complete tile-family marker is published. A partial
  ten-band family therefore has no visible draw. Normal style replacement still lacks last-valid,
  presenter-witnessed generation retirement and remains RED.
- the independent Sol audit rejected the original per-frame `WorldCursorWake`/boolean handoff.
  That claim is superseded by a single `WorldCursorWakeAuthority` retained in `RuntimeMailbox`.
  Per-frame build contexts borrow a shallow authority clone; the non-Copy generation token is
  transported through AppFrameAfterChrome -> AppFrameBuild -> AppFramePreparation ->
  AppFramePresentation -> AppPresentStep -> OsHost. App presentation acknowledges only the exact
  pending generation, stale/duplicate tokens cannot consume a rearmed generation, and OsHost
  coalesces only to a newer generation. Native RESOURCE_READY takes the retained token once after
  requesting redraw; browser `request_frame` is the sole final boolean projection and takes the
  same token once. Realm close retires the host token, presenter token, pending authority token,
  generation, and acknowledged generation on separate grants before terminal-empty.
- source fixtures authored for three-nonempty-category atomic/stale face publication, retained
  partial-generation ownership, complete-marker terrain visibility, consecutive-frame generation,
  128-request wake-storm coalescing, stale/duplicate ACK rejection, close terminal-empty, and
  mutations for per-frame authority recreation, token-to-bool erasure, missing ACK, lost token,
  missing newest-generation coalescing, missing close take, and non-consuming browser projection.
  They remain unrun because the serialized Rust gate is outside this packet.
- source gates passed: exact legacy/obsolete-state negative scans, required-fixture/wake-path
  positive scans, rustfmt check for math/canonical World/winit/frame-job/os-host/browser-worker,
  renderer-glue parse via rustfmt stdout, and scoped git diff check.
- Cargo, Nx, Wasm, browser, network, and root lint were not run by instruction.
- remaining RED: presenter-witnessed normal face/terrain replacement; retained retirement of old
  generation-qualified registry leases outside realm close; typed terrain input; dynamic
  HashSet/HashMap/frame-vector retirement; PNG/JPEG/MVT/SVG semantics; pending packet/GPU table,
  atlas/raster/cache and opaque render/submit close; realm terminal-empty; real Wasm/browser timing.
