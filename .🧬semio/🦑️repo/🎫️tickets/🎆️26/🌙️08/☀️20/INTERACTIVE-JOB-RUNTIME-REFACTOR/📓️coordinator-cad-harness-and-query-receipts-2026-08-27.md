# CAD Harness and Query Receipt Checkpoint

## Actual CAD Gate

The sole fleet native build reached the real CAD test crate and stopped on **82 compiler errors before any test ran**. The coordinator read the recovered diagnostic index. Errors include obsolete mutation-test helper names, synchronous tests calling current async APIs without awaiting, obsolete AppDefinition/BuiltNode fields, invalid async-test return shapes and attempts to serialize assembly errors. No four-close or real-constructor pass is claimed.

Evidence: `🧪️member-cad-presence-close-r1-native-2026-08-27.txt`, full recovered `🧪️member-cad-presence-close-r1-diagnostics-2026-08-27.txt` and `🧪️member-cad-presence-close-r1-diagnostic-index-2026-08-27.txt`. The publication executor owns the complete harness repair and retains the sole fleet Rust compiler lease. Production ownership guards and assertions must remain intact; no test exclusions or compatibility APIs are authorized.

The coordinator subsequently read all ten CAD harness file diffs. They await current helpers, use the canonical viewer testkit, inspect actual TreeItem properties, and check retained action declarations once per declaring window scope. No new test ignore or production guard relaxation was observed. An already-ignored default DSL fixture and existing tests asserting addObject/translateSelection no-ops remain independent unfinished CAD/all-app obligations; a successful close/constructor gate will not certify these features. The repaired source still requires the native rerun.

## Query Evidence and Corrected Attribution

The fresh expanded plugin run executed **17 passed / 1 failed**, 429 filtered, 0.22 seconds after 38.67 seconds compilation. The new registered channel test expected only Done(1), but the actual legal exchange also delivered Ephemeral and Started. Its fixture must consume all multiplexed messages instead of imposing a false one-frame exchange boundary. Evidence: `🧪️member-local-interaction-r5-native-2026-08-27.txt`.

The earlier renderer timeout at setMergePolicy is real, but the initial attribution to a legitimate native empty completion was incomplete. Coordinator source review now confirms `set_merge_policy_frames` returns **Done(seq)**, while the React mock returns an empty array. Therefore preserving empty FIFO completion solely for that mock is not evidence of correct native behavior. The previous query-regression report's empty-reply interpretation is superseded by this finding.

The client currently resolves only one pending waiter for an outcome containing several correlated replies; a combined query receipt and ordinary reply can leave another waiter unresolved. An unsolicited query message accompanied by uncorrelated Ephemeral/UI frames also must not shift an ordinary waiter. The executor is adding exact hostile tests and auditing native command receipt coverage before removing ambiguous FIFO assumptions. Old, duplicated or wrong receipts must not consume new-client waiters.

The executor's native audit subsequently identified exactly three transaction success replies that carry transaction ID without command sequence: Prepare, Commit and Rollback. The approved correction adds a companion existing Done(seq) to these branches, preserves transaction-ID payload validation, and removes ambiguous empty/FIFO completion from the client. Existing stale mocks must emit the real native receipts, not have a wrapper manufacture them. Every exact pending sequence in a multiplexed outcome must resolve once; uncorrelated side notifications cannot complete requests.

The new required handle-lifetime AppChannelRequestSequence is source-reviewed: checked safe-integer command sequences, lossless decimal u64 request IDs, atomic read/cancel sequence reservation and shared ownership across client recreation. The executor reports 216 OS tests passing; independent coordinator execution is still pending the next receipt checkpoint. A raw JS reference is not a native query/renderer retirement owner.

## Source Gate

The corrected registered query cohort subsequently passed **18/18**, 429 filtered, 0.53 seconds after 22.17 seconds compilation. The coordinator read its actual output and entire registered test: real binary query ingress, multiple immediate reply frames, continuation pages over 4 KiB, at-most-256-byte pages, each ACK, terminal root return and bounded app close. Evidence: `🧪️member-local-interaction-r6-native-2026-08-27.txt`. This does not yet cover all registered cancel/saturation/reopen cases or the browser client seam. The coordinator also independently executed OS TypeScript **216/216**, recorded in `📓️coordinator-os-client-full-r2-2026-08-27.md`; the additional receipt audit remains open.

The coordinator independently ran **929 source-contract self-tests**, exit zero, with 33 exact factory owners, 255 custom rows and 25 generic rows. Exact command-helper binding now also requires private peer commit base/factory authority. Evidence: `📓️coordinator-tool-job-selftests-r15-2026-08-27.md`. This is not all-app runtime or 8 ms proof.

The complete renderer, paged restore, tutorial/native-shell joins, real CAD close/constructor, fresh Wasm, all-app workflows and hard timing gates remain open.
