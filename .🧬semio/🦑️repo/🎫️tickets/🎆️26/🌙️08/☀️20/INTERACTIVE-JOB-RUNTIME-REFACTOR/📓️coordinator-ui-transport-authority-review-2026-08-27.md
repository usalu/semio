# Exact UI Transport and Final Release Review

## Source Review

The coordinator read the current GuestLifecycleCell, canonical Kernel UiTurnPatches/transport ownership, ShardClient original-turn capture, UI whole-patch owner, document comparison and renderer source contract on2026-08-27. This review is not a native guest runtime pass.

GuestLifecycleCell now calls a sealed domain terminal-release step before its final real-clock verdict. A separate private owner_released witness retains exact completion across a late timing failure while the Retired receipt and staged ACK stay mounted. Its four authored laws remain unrun. Only the audited domain implementation may release a terminal shell; skipping ManuallyDrop on unwind is not recoverable ownership by itself.

ShardClient records original results in a WeakMap for provenance. At the reviewed86-test boundary that is not strong ownership for uncaptured successful, open-fault or ACK-returned payload roots. The demonstrator owns a separate pre-dispatch output-reservation implementation and raw descendant retirement. Native input acceptance/retirement tokens prove only the transferred known input, not unknown wrapper fields or whole arrays. Frozen metadata and object identity do not establish immutable original content.

Kernel UI_TURN_PATCHES_MAXIMUM is exactly1 and UiTurnPatches uses that fixed list. IntoIterator and transport take_owner currently can release their source slot before raw descendants have retired. Slot/global absence is not an exact close witness. Dag owns structural keyed handback adoption. Current native PatchAck and PatchRejected select by surface/revision; rejection can reset the replacement instance if a late old event is accepted.

## Approved Issued-Patch Authority

One canonical ActorUiPatchReceipt contains the full ActorInstanceLifetime and a runtime-issued checked positive u64 patchSequence. Encoding is four canonical unsigned LEB128 fields in activationGeneration, instanceId, guestLifetime, patchSequence order, maximum35bytes. The optional bounded-length uiPatchReceipt field follows lifecycleReceipt in Actor/Kernel TurnResult. WIT carries ui-patch-receipt with lifetime and patch-sequence.

Zero output patches requires no receipt; one output patch requires exactly one receipt. Missing, foreign, malformed and conflicting receipts are rejected before publication while the original owner remains retained. PatchAck and PatchRejected carry the exact producer receipt; surface/revision remain payload checks, not ownership identity. Host code never synthesizes authority from revision, ordinal, current handle or actor-name equality. TypeScript uses exact BigInt sequences.

Dag owns schema, shared vectors, Rust, Kernel and WIT. Demonstrator owns TypeScript codecs, materializer mapping and host event producers. UI copies the actual producer receipt through its private source/ACK token. Native and host tests must cover same-activation instance/revision reuse, malformed lengths/LEB128, sequence exhaustion, replay, rejection and exact child handback. No second ABI or old-format compatibility is allowed.

## Native UI Evidence Reviewed

R44 document component comparison actually passes3/3 with all18variants, exact lease/ordinal/node identity,1/64/4096byte grants, seven cancellation frontiers and arena contention. Full256-frame initialization plus root moves is15,224bytes with zero allocation. Its later fourth final-reader law is included in fullUIR47.

R42 actual allocator adversary retains65,536bytes with zero initialized length, reports the allocation on failure and closes through the typed owner. Its cohort passes5/5; this is not resident Process fit.

UiPendingPatch R46 actually passes2/2: all18components and six injected-unwind frontiers. The owner is1,744native inline bytes, admitted separately. Its exact operation-page backing metric does not claim complete resident ownership. FullUIR47 actually passes143/143,0skipped,.512seconds. Runtime reconciliation and hard latency remain separate gates.

Current SurfaceReconcileReadyPatch::publish/PublishedPatch::acknowledge consume owners; bool close uses its own32KiBgrant and whole Surface clearing. The publication executor is replacing these with in-place, caller-granted typed retirement before Dag mounts them. No success is inferred from current global handback emptiness.

## Live Renderer Integration

The coordinator approved the complete contract in 📓️renderer-live-owned-source-contract-2026-08-27.md. Host refresh results carry exact OwnedUiInstanceSurface references, not copied BuiltNode trees or a parallel document store. Renderer owns root/node/scene subscriptions, retained intake and Shell/Interpreter consumption; demonstrator owns capture/create/destroy/scheduling and exact submissions. The single host aggregate must own actual displayed content before its final witness is accepted. Root facade/actual native-operation-to-DOM laws are the next bounded prerequisite. TypeScript WGPU-web adopts the same ownership surface; native Rust remains its canonical independent implementation.

Independent fullReactR20 is616/616,actorR9 is86/86 at their captured boundaries; strictR29 remains7tutorial. These do not close the still-unmounted live, native guest, raw-root, all-app, platform or strict8ms gates. All evidence is retained; no cleanup, generated publication or Git mutation was performed.

## Subsequent Reviewed Checkpoints

The independent source-only R25 gate passes1009 self-tests at33proof owners/255custom/25generic rows; the full census remains its separate RED. Independent root-source DOM R1 passes1 with617skipped/618discovered,7.77seconds, using real host ownership and a mock worker, not fresh native code. Its exact command/output is in 📓️coordinator-owned-root-source-r1-2026-08-27.md.

Runtime handoff R27 passes2/2 and R29 passes96 with the same one intentional physical inline-census failure excluded. Root read actual output and current in-place publish/ACK source. Fixed48KiB cursor and64KiB retained-state bounds are unchanged. Arbitrary Drop/global handbacks and complete old-record/resident-accounting paths remain open. The three canonical UI wasm32 checks R48 compile successfully; no runtime Wasm credit is inferred.

The root review found that initial intake close forwarding replaced a child's rejected result with pending. The UI owner reproduced that exact failure before repairing forwarding and retaining the original failure/owner. Its following4-test cohort passed; the genuine notification-failure retry law is still in progress. A new intake cannot be called live-ready from the prior happy-path tests.

The output reservation foundation retains values/errors after submit but is not yet mounted in ShardClient. Root additionally identified the lower pending-settlement boundary: handleMessage deletes pending and grafts worker errors before the awaiting send caller runs. Original response-envelope ownership must be installed before that validation/grafting/external continuation, not only after awaiting a synthesized value/Error. Demonstrator owns this adoption and raw descendant retirement.

The first attempted held-arena Kernel test used the wrong OS-kernel Cargo target. Its30fixture errors are real but did not exercise the base-framework laws. See 📓️coordinator-kernel-target-correction-2026-08-27.md; the sole compiler is retrying the actual semio-framework member through the existing pure-native router. No extra compiler, cleanup or source restoration was used.

## Corrected Native and Intake Boundary

The correct common Kernel router actually executed both held-arena laws: DropR2 fails0/1/244skip and normal-closeR3 fails0/1/245skip. Root read the R3 raw output: the original owner is recovered before asserting, and `waited` is true when false is required. These are semantic failures in semio-framework, not the separate thirty OS-kernel fixture diagnostics. Dag is repairing fixed preowned handback and nonblocking admission/normal-close; no native aggregate success is inferred before reruns.

The runtime owner's separate existing-record REDs also executed. R30 reports `allocation-before-admission=32768`, `rejected=false`, and an unchanged source with zero resident credit. R31 reports one turn and zero allocation debit for comparison/copy of32KiB records differing only at their final byte. Each fixture closes/reclaims its retained owners before asserting. The repair must replace old retained-record authority with the canonical document root, not add a second cloned current tree. Process fit, resident accounting and strict timing remain unproved.

UI intake R8 is now source-coherent at five passing tests/618skipped/623discovered,7.88seconds, and its strict gate has exactly the seven known tutorial diagnostics. Root read the complete R1–R8 report. It includes the real rejected-to-pending RED, persistent failure retention, actual failed notification retry, and later publication. Fixed construction/lookup charges are logical traversal bounds, not measured VM allocation or latency proof. The newer issued-patch receipt token/fixture adoption is assigned before the coordinator's combined rerun.

Independent actorR10 passes91/91 with stable codec/foundation/ShardClient hashes. The later peer host receipt checkpoint reports92/92 and producer61/61; root is independently checking the newer actor boundary. Both remain host/generated-source tests, not fresh component execution. Strong output reservation is still unmounted; original guest results before generated normalization and original host response envelopes before settlement are distinct required retained owners. Unknown wrapper fields cannot be discharged using a cursor for known UI fields.

## Verified Release After Receipt Adoption

Independent actorR11 collected the newly authored response-envelope missing-method RED (92passed/1failed); R12 then actually passes93/93, six files,1.00second with stable source hashes. Independent OwnedInstanceR4 passes9/615skip624 and OwnedIntakeR2 passes5/619skip624,8.35/8.09seconds with four stable integration hashes. These latter runs include actual producer receipt bytes and the private UI token adoption; the scene JSON work was not selected. Full outputs are in 📓️coordinator-issued-receipt-intake-and-actor-r12-2026-08-27.md.

The native commonKernel R4 repair actually passes12/12,235skipped,.253seconds, including both held-arena REDs and the typed Unicode descendant law plus nine transport/serde regressions. Root read the actual output and current fixed atomic handback/try-lock/source cursor. The remaining native transport arena still uses lazy initialization and blocking locks; consuming transfer and exact descendant feedback remain separately assigned. A poisoned or blocked pump must not be promoted to terminal success. No Plugin aggregate or timing pass follows from these twelve laws.

Dag's follow-up inspection found no general actor return pager in ActorRust/CommonKernel/WIT: opaque whole vectors and typed whole WIT results remain. Native host convert_poll_success and execute_turn also currently discard returned WIT UI patches. Preserving a new receipt while defaulting these patches to empty would violate the approved pairing. The exact native retained conversion is required and assigned, not replaced with empty success. The coordinator has not executed fresh guest or browser code at this checkpoint.

## Canonical Return Paging and Root Credit Direction

Root read the complete Demonstrator canonical-return-paging proposal and approved one replacement schema on the existing poll/event protocol. Variable typed results must be retained before whole WIT lifting. A return owner binds the captured activation, originating request and checked native serial; UI semantics additionally keep their full guest lifetime/issued patch receipt. Page input-release ACK is not a semantic UI publication ACK. Control ACK turns must not recursively produce another variable result needing another ACK. No second guest export, old/new result union, command factory witness or arbitrary wrapper traversal is accepted.

Root read the exact existing WIT fixed page representation:64named blocks of eight u64words,4096payloadbytes, little-endian. Only that neutral storage shape may be shared. Its current JavaScript helper eagerly creates all command pages, so it is not a retained return builder. Return length/padding, source/host admission and metadata work still require canonical schema, independent byte oracles and actual timing. Dag owns the single schema/field release; Demonstrator owns the matching TS storage/consumer and generated mapping. The native discarded-patch handoff must join this paged cutover without an interim whole-copy bridge.

Native document assemblyR52 passes4/143skip,.236seconds, including the real metadata-accounting RED→GREEN:7,568bytes allocated versus1,152bytes initialized. FullUIR53 passes147/147,0skipped,1.048seconds; root read actual output. Root reviewed and approved the concrete UiResidentPermit design: the same64slot/32MiB aggregate ledger factors into neutral UI ownership; rootowner1 remains inside the canonical document until final-reader and typed descendant retirement, while patchowner2 preserves the existing paired obligation. Candidate/output overlap and metadata must be counted before shrink/split/seal. This is not permission to raise any quota. Exact approval/required runtime tests are in 📓️coordinator-document-admission-join-2026-08-27.md.

Independent fullReactR21 passes626/626 and strictR30 remains exactly7tutorial joins with six stable captured source hashes. The newer private pending-settlement source independently passes actorR13 95/95; it does not mount public lifecycle output ownership. The kernel fixture oracle independently passes1valid/29negative/6UTF8rows. Native Actor receipt3 plus existing lifecycle5 are actual-output reviewed. Native transport producerDrop/leaseDrop/normal-close each separately reproduced its held-lock RED; its correction remains in flight.
