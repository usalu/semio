# Live Zero-Dependency Summary

Date: 2026-08-26  
Command: `bun ./📜️script.ts verify dependencies summary`  
Exit: `0` for census execution; target verdict remains RED.

| Ecosystem | Raw | Literal external | Production reachable |
| --- | ---: | ---: | ---: |
| Rust | 78 | 78 | 58 |
| JavaScript | 70 | 69 | 31 |
| Go | 2 | 0 | 0 |
| Python | 15 | 0 after composition scope | 0 |
| Total | 165 | 147 | 89 |

The declared zero target is `0`; the live literal-external count is `147`, so `meets-target=false`. The current audited toolchain rows also report `authorized-rows=3`, `unauthorized-rows=2`, `oracle-conflicts=3`, and `toolchain-owner-conflicts=2`. The bounded P9-A leaves removed `rust:serde-wasm-bindgen`, `rust:wasm-bindgen-futures`, the sole direct `rust:ash-window` owner, the sole direct `rust:objc2-core-foundation` owner, the sole direct `rust:nalgebra` owner, the sole direct `rust:objc2-foundation` owner, and the sole direct `rust:windows-sys` owner; each hostile reintroduction restores its exact prior identity and production counts. The coordinator independently reproduced the post-`windows-sys` census at Rust `78/58` and total `147/89`. The private Win32 ABI matches the pinned `windows-sys 0.60.2` oracle for 12 layouts/every named offset, 16 constants, and 21 `extern "system"` signatures, and Windows/native/Wasm consumer checks passed. The independent Terra audit plus coordinator re-audit repaired terminal cleanup so failed ANSI setup/teardown and console/termios restoration remain independently owned and retryable; two native cleanup-state tests and the post-addendum Windows cross-target test check exit 0. `windows-sys 0.60.2` truthfully remains transitively locked through `arboard` and `io-extras`, so no transitive-zero claim is made. Both Foundation identities likewise remain transitively locked through QuartzCore, Metal, and shared framework crates, so these direct-owner leaves are not Metal zero-dependency completion. The accepted objc2-foundation replacement is a private semantic import, `use objc2_metal::MTLDeviceNotificationName as NSString`; all construction and signatures retain the honest `NSString` name, and its exact `TypeId` plus UTF-8/UTF-16 differential oracle passed across four schema-owned cases. Its complete caller census places all 23 `NSString::from_str` allocations in `MetalBackend::from_parts -> Pipelines::new`, during one-time async backend construction and shader compile/link; render, event, resize, and presentation callbacks only read retained pipelines. The executor's fresh native and wasm structural Metal checks both exited 0. Likewise, `nalgebra 0.33.3` remains transitively locked through `parry3d 0.17.6`; Puzzle now names the exact `parry3d::na` re-export, so this is a direct-owner reduction rather than an owned-math completion. The `wasm-bindgen-futures` leaf is accepted after all 16 former owners received exact pass, owner-baseline-blocker, or dependency-baseline-blocker dispositions; no blocker is represented as a pass. The first focused Puzzle library check reached the consumer and found no geometry/nalgebra diagnostics but exited 101 on six concurrent Puzzle5d borrow-check errors. After those live-tree conflicts were corrected, the independent Puzzle semantic executor reran `cargo check --locked -p semio-s-plugin-puzzle --lib` against the same tree and recorded exit 0 (`Finished dev profile`); the direct nalgebra owner leaf therefore has clean consumer-build evidence as well as the differential oracle. The protected consumer cache remains in the ticket for reproducibility.

This is a fresh current-tree census, not a replacement for the last accepted isolated dependency checkpoint. Concurrent work has diverged since that checkpoint, so reductions must be revalidated and attributed before any new baseline is accepted. No dependency gate closure is claimed.
