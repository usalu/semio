# Recovered Fleet and Exact Read Verification

The three existing Sol High executors are running again. The coordinator preserves the sole fleet Rust compilation lease with the retained-publication executor and does not modify production implementation. The master goal and incomplete phase gates remain open. Repository ticket MCP tools are still absent from available tool metadata; no replacement ticket, fabricated MCP transition, or modifying git operation was used.

## Native UI Ownership

The coordinator read the actual seven-law output, all seven test bodies, the added contention law, the changed normal close implementation, and subsequent eight-law/full-library output. The eight-law run passes 8/8, 91 skipped, 0.650 seconds; the full UI-contract run passes 99/99, zero skipped, 0.301 seconds. Evidence: `🧪️member-ui-value-retirement-green-r3-native-2026-08-27.txt` and `🧪️member-ui-value-full-r4-native-2026-08-27.txt`.

The contention regression holds the arena on another thread before and after exact-root retirement starts. Normal close now uses `try_lock` and returns unchanged zero progress on contention, preserving the exact owner; poisoning returns an error without surrendering ownership. The prior actual RED is retained in `🧪️member-ui-value-contention-red-r2-native-2026-08-27.txt`. This proves this primitive's normal path, not every UI producer or aggregate. Ordinary global arena maintenance, exceptional guard handoff, typed component/list/patch descendants, and the final host/renderer/ACK instance witness remain separate work.

## React Read Ownership

Independent coordinator R1 now passes 4/4, 532 skipped, 536 discovered, 19.45 seconds, including the null-ACK correction and source/ID replacement law. Exact command and complete output are in `📓️coordinator-owned-read-lifetime-r1-2026-08-27.md`. This supersedes the pending independent state described below, not the outstanding live aggregate work.

The coordinator read the complete two-slot lease, actual React hook, and three mounted test bodies. The executor's DOM R3 passes 3/3, 532 skipped, 535 discovered, 14.08 seconds; it exercises stable exact-token reads, independent consumers under StrictMode remount, and aborted speculative rendering with no acquired subscription owner. The hook creates leases in subscription lifetime, returns a stable snapshot through `useSyncExternalStore`, and acknowledges the committed token in a layout effect. Unsubscribe delegates retained closure to its source.

An additional hostile runtime `null` acknowledgement actually failed: `null` matched the empty second slot. The executor corrected that comparison and reports a 3/3 rerun; independent verification is running. R3 predates this hostile case. Evidence: `🧪️renderer-read-lease-dom-r3-2026-08-27.txt`, `🧪️renderer-read-lease-null-ack-red-r1-2026-08-27.txt`, and its green sibling. Source/ID replacement and stale layout acknowledgement coverage has been requested. Live UiNodeView adoption and the concrete surface/publication/notification/close owner are not mounted yet.

## Clock and Publication

The actual bare framework-async `wasm32-unknown-unknown` check passes after 7m31; this is compilation, not a consumed-app clock proof. The coordinator read the generated-loader and package-sibling gate's successful output, covering four initializer/import ownership cases against the old Flow binary. The new consumed Flow clock gate actually fails against that old binary because no real startup clock samples occur, after catalogue and lifetime close execute. The canonical target is `semio-framework-os-flow-core:test-browser-clock`.

The demonstrator task owns shared plugin and extension publication, with its engine stage to include `semio-framework-os-flow-core:wasm`. The refactor fleet is not writing those output directories. Current async clock startup, Flow ABI preflight and package sibling binding must be included before rerunning the consumed clock gate. No browser-policy workaround is authorized or attempted.

## Still Red

The original-two-only native cold close remains RED/SIGABRT at 155,839 and 26,938 microseconds inside `BatchJobSession.step`. The five-test combined pass does not supersede the fresh-process failure. The next native gate uses fifteen phase timestamps and retains the strict fault boundary at 8,000 microseconds. No causal diagnosis is inferred from candidate lock/allocation sites.

Independent full React R14 remains 532/532 and source R19 979/979 at their recorded snapshots. Strict React remains seven genuine tutorial joins. The newer full command census is reported RED with 773 rows, 350 admitted bounded rows, 315 BatchOnly, two forbidden, 270 remaining and twelve failure categories. Source admission and unit counts are not all-app functionality, timing or end-to-end completion.
