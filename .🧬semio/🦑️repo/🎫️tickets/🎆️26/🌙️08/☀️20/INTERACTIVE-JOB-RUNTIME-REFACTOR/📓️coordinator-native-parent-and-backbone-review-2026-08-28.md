# Native Original-Parent And Backbone Review

## Reviewed Source-Only Packets

Coordinator read the complete actual-parent report, all275 staged native test lines,64-line reference controller,149-line fixture, and Retained's complete Store-backbone proposal. No native command, canonical include, dependency, production source or resident17 input was changed by this review.

The seven Opening laws are still ticket-only and uncompiled. Dag's scoped reference oracle reports7 cases/39 transitions/2 hostile inputs; earlier relative-path and all-project invocation mistakes are retained in its report. This is declaration/reference evidence, not real parent funding, Send, physical work, callback-tail or constructor execution.

## Accepted Ownership Direction

Reuse the actual RuntimeAppCell and its original registry for Opening, live app and close. Registry backing, cell, typed construction fields and recovery destinations require exact native registration before allocation. The actual VcsArtifactApp four Store fields must be installed in that original parent before a fallible constructor callback; a future or whole-app return with those fields as locals does not provide recovery.

The source/retiring field moves a separately funded stable app-allocation handle, not arbitrary PA by value under a guessed4096-byte extent. The receiver must bind the exact original parent/consumer/field/phase; numeric IDs, same Layout, address containment, mutable Option and a public projection callback are not substitutes.

For backbone retirement, use one typed entry in the same Store FIFO and the same ResidentRecord family. The native RecordNode Layout, changed FIFO entry backing, descriptor URI and exact slot/generation must all be admitted before source take. This replaces the backbone-specific Box rather than allocating a second Box/pool. Existing other FIFO owners are not retroactively certified.

Store and mailbox publication need a jointly prepared, nonallocating commit under real original guards. Current blocking/consuming mailbox send and Tokio broadcast last-receiver destruction are not bounded ownership handoffs. Retain channels and the unsent request through refusal; never resend a committed request after wake-tail failure. No live RuntimeAppCell-to-SyncSession constructor was established by the source census.

## Required Staged-Test Refinements

Before canonical mount, independently compare transfer work with the actual stable-handle Layout, instead of accepting only required_move_bytes reported by the API under test. Preserve the4096 grant.

Capture refusal, changed input and oversized-move observations without an early unwrap/assert unwinding live strict Store owners; close exact original owners before the intended failing assertion. Keep the actual callback-tail interlock, not a completion boolean or unrelated thread.

The64MiB test capacity and100000 test-loop bound must remain explicit isolated setup, not a new live32MiB composition policy or liveness/fit proof. Derive work expectations from declared/captured Layout and source phases. Dag owns these narrow test refinements; no production implementation is authorized by a reference-model pass.

## Queue And Remaining Boundaries

Taxonomy's final source-phase release precedes unchanged native resident17. Only afterward may the reviewed real-parent dependency/test include produce actual missing-API/behavior RED. The six OS-kernel laws retain their separate metadata/compile-first sequence; no await deletion, ignored returned backbone, feature disabling or unsafe Send may force an apparent pass.

Typed shell destruction, backing deallocation, credit refund and original parent release must remain distinct. The candidate's current pre-free refund ordering is a disclosed follow-on, not proof from resident17. Scheduler-issued per-submission tail retirement remains required before final cell publication.

All active inputs, errors and evidence remain preserved; no broad native or end-to-end acceptance is claimed.

