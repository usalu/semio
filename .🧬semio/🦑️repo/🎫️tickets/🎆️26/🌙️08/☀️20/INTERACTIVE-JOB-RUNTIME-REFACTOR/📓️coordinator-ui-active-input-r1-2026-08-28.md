# Independent Active Input Cancellation R1

## Actual Outcome

The registered renderer test-long route actually passed3 tests,727 skipped,730 collected; start04:50:43, duration3.83s, Nx exit0. All88 selected before/after hash lines are byte-identical and untruncated. Both brief source holds were released immediately after the terminal/posthash read; no native command or current source hold remains.

Full command, returned tool chunks and all before/after hashes are preserved in [the evidence](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧪️coordinator-ui-active-input-r1-output-2026-08-28.md). Selection is OwnedResidentActiveInput only, not a full renderer or strict replay. The prior owner49-file release was a single snapshot; this independent gate adds actual selected pre/post comparison.

## Reviewed And Executed Scope

Coordinator read all four cancellation declarations, the three real tests, the parent close dispatch/private matcher and producer input/evidence-detach methods. A combined source display truncated the parent and producer methods; the relevant methods were reread in narrower complete ranges before execution.

The tests execute eight normal/revoked cases across input-held, scalar-latched, scalar-written and reader-active/full-window backpressure states, plus two genuine before/after input-detachment exceptions. The127-byte refusal preserves ownership; a separate128-byte phase clears the direct reader/latch while retaining the original fragment. Actual parent-driven evidence admission handles its declared56/32-byte phases, not an unconditional64-byte child minimum.

Reader/page close precedes input detachment. Exactly one genuine Cancelled token releases the original fragment. Source-detached observation precedes clearing matching builder/capsule variable aliases; source settlement, capsule registration, builder binding and payload registration remain distinct phases. Tests compare exact usage with an Immer oracle and validate language-neutral fixtures/contracts with strict Ajv. Unknown thrown roots remain in their original charged admission owner; they do not obtain terminal/refund credit.

No fields, limits or phases were edited by coordinator. Loop bounds come from declared reader/page/evidence/builder/payload/storage phase lists. Source input remains consumed0 with complete/EOFfalse, including partial destination writes. Cancellation is not copied progress or page-input ACK.

## Excluded Claims

No semantic streaming, copied-evidence/source-advance, native returned-page custody, rawInputAck, live guest/app, full strict/renderer, physical-heap or strict callback-time acceptance follows. Copied-source continuation is the next distinct UI-owned packet. Native Free/Refund and OS compiler work are separate.

The run used existing Bun/Nx registration without configuration/stack/quota changes. All caches, source and historical evidence are preserved.

