# Reader And Rejected-Page Preparation Review

## UI Reader

Root read all four current UI resident reader declarations and the existing page plan through the thirteen-phase admission/binding proposal. The next source-owned implementation remains with UI: reader720/12/12, current intrinsic alias432/8/8, one256-byte destination-page window, original two-field phaseful witness and existing builder reader word. No extra source proof or hidden parent backlink is authorized.

The real semantic decoder must receive the genuine reader while its original builder is still source-incomplete; waiting for whole-field builder-ready would deadlock the bounded page window. Exact validated EOF, same-field no restart, after-read/after-detach fault containment and original/foreign terminal pairing must remain separately tested. No reader runtime result is claimed by this root read. UI reported R101's actual missing-method RED separately.

## Store Rejected Page Preparation66

Root read complete preparation66/root-review reports, strict schema, all three layouts/23 observations,435-line native test file and226-line source/reference controller. Reported57/57 reference and63/63 source checks are peer-executed, not root reruns or native tests.

The proposed top-level Store sibling include beside owned_schema_record_tests is the correct private-parent scope; it must not move inside the unrelated inline tests module. The two genuine wrappers, original field lease/ticket/reclamation and counted token identity are exercised by the proposed bodies. Full4096 grant remains required for a14-byte retained page; reports correctly distinguish granted work granule from actual released bytes. Semantic-page expectations do not change field/lease terminal policy.

Before mount root requested checked teardown bounds derived from maximum page count and actual field/lease/returned-owner phases instead of helper magic8/32 loops. No larger cap or Drop suppression. Taxonomy owns a separate read-only exact path admission review for Store/🧪️tests/🧬️rejected-page-close/{🦀️.rs,🔣️.json,🧬️schema/🔣️.json}, after its current OS publication. No new membership/move is implicitly authorized.

Production wrapper source is unchanged; fixture remains unmounted. Future sole-executor two-law RED follows OS6 coordination, not a concurrent native command. R17 codec/backbone, Fresh decoder/poison/command/Interaction and absent old49 evidence remain excluded. No native process, output publication, restoration or cleanup was started by this review.

