# Renderer R8 Active Wait

The coordinator launched the canonical full React long target after the executor's coherent five-law OwnedWire checkpoint. Unlike the preceding 6.86-second full regression, R8 is still running after approximately 90 seconds.

A read-only process snapshot found the exact root-owned chain: Nx 92207 → script 92232 → Vitest 92234 → one remaining worker 92237. The worker was idle at the snapshot; this does not identify the cause. The actual long-profile arguments retain a 300,000 ms test/hook/teardown timeout. No timeout has been enlarged, no process terminated, and no pass claimed.

Both implementation executors have been notified of a possible asynchronous integration wait. The renderer executor confirms its ordinary decoder source and five-law tests were coherent at launch; the new SurfaceDoc byte-view work remains isolated. Attribution awaits the test result rather than an assumption about concurrent source changes.

The run subsequently exceeded the unchanged 300,000 ms process budget and exited 1. Complete output is retained in `📓️coordinator-renderer-react-full-r8-2026-08-27.md`. The R10 verbose run and executor source diagnosis identified the dropped ordinary empty-response regression; see `📓️coordinator-query-channel-regression-2026-08-27.md`. The earlier import-cycle hypothesis is not a confirmed cause.
