# Numeric Reference Admission

The coordinator identified unchecked reference increments in numeric root capture and AVL rebuilding. Root capture now checks both exact root allocations before incrementing either; aliasing both slots accounts for two increments. `OwnedUiNodeIndex.beginSet` calls that exact source preflight before capturing its node input. There are no application callbacks or asynchronous yields between those admission checks and transfer.

AVL rebuilding now retains a fixed plan of at most three node allocations. It collects cumulative existing-node/entry reservations incrementally (one slot/comparison per step), then rechecks every current counter and reserves every increment in one atomic phase. Only after all checks succeed does allocation begin, one node per step. New child nodes transfer their already-owned references directly into their parent. A later poll cannot invalidate a previously granted reservation.

Cancellation queues the allocation plan before source/work anchors. Each close step clears one spec or reservation, releasing built fragments and unconsumed reserved references through the ordinary numeric retirement queue. The original minimum grant remains 256 bytes; maximum reported plan-creation work is 240 bytes, reservation commit is at most 176 bytes, one node allocation is 96 bytes, and plan retirement is at most 160 bytes. This is logical byte-work accounting, not a browser GC or outer-poll timing certificate.

Schema-first `🔢️numeric/🧪️fixtures/🔣️references.json` defines six saturation laws. The test script uses the existing TypeScript compiler and Node VM to evaluate the exact source with its private probe; no mutable reference counter or projection callback is exported in the production module. The duplicate-owner case fails at MAX_SAFE_INTEGER minus one when two reservations are required. Allocation tests change the count after incremental collection, proving the final atomic phase rechecks current counters.

The first canonical RED attempt failed in the test loader: Bun treats the long data URL as a package path (`NameTooLong`). Its full log preserves the complete pre-repair transpiled module. A supplemental Node-VM execution of that preserved module reproduces both original root failures: capture-ids and capture-order each report rejected=false, preserved=false. This is recorded in `🧪️numeric-reference-preserved-red-oracle-2026-08-27.json`; it is explicitly supplemental, not a successful canonical test run.

After replacing that test-only loader with Node VM, canonical `bun x nx run @semio-tech/value-numeric-index:test --skip-nx-cache` passes: 12 semantic laws, 165 lifecycle/cancellation prefixes (previously 37 before retained allocation phases), 2 ordinal laws, 3072 Immer+Map comparisons, 6 saturation laws, 5 invalid-ID cases, and strict TypeScript zero diagnostics. Full output: `🧪️numeric-reference-r2-2026-08-27.txt`.

The coordinator then identified a per-instance shadow of the public capacity checker. A seventh private fixture reproduced the actual canonical failure: capture rejected after incrementing the ID root, so preservation was false. Internal capture now calls a private checker, and numeric public capabilities are frozen. The canonical shadow GREEN log reports 12 semantic, 165 lifecycle, 2 ordinal, 3072 differential, 7 reference and 5 invalid-ID cases, with strict TypeScript zero diagnostics. Full RED/GREEN logs are `🧪️numeric-reference-shadow-red-2026-08-27.txt` and `🧪️numeric-reference-shadow-green-2026-08-27.txt`.

The owned wrapper's input preservation joins the runtime-tested numeric preflight law with source ordering and its closed-index input-preservation test. No public mutation hook was introduced solely to saturate its private numeric root. The later owned-operation targeted test passes one case, as recorded separately. No live renderer or full-suite pass is inferred.
