Command: cargo check --offline -p semio-s-plugin-draw --lib --message-format short
Exit: 101

The check reached semio-s-plugin-draw after compiling its direct framework and stdio dependencies. It reported 399 errors in the shared Draw tree. The dominant failures predate or are outside the instance-owner packet: missing generated DrawApps, repository-wide async-signature conversions where synchronous protocol/config/mutation/UI traits now return futures, Draw owned-codec type mismatches, and existing panel/UI label mismatches.

The one directly surfaced preview error was:
- DrawSession attempted to derive Clone although fsm::Snapshot<DrawGesture> is not Clone.

That design was removed. Render publication now copies only DrawGesturePreview, a fixed-capacity immutable projection with DRAW_GESTURE_PREVIEW_POINT_CAPACITY=256; it never clones DrawSession or the FSM working state.

No successful Draw runtime/test claim is made while the package-wide compile boundary remains red.

