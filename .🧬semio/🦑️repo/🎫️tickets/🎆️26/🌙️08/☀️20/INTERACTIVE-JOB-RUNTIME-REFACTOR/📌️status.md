# Refactor Status Dashboard

## 2026-08-28 Active Verification Checkpoint

This current checkpoint supersedes the historical snapshots below without rewriting their evidence.

Independent metadataR2 actually passes7 envelope/exact-ledger/short-grant cases and6 hostile kinds; neutralR3 canonical target exits0 with7stableinputs and strictTS0, including5 constructor-return facade recovery cases. Arbitrary thrown-root retention and outer-wrapper handoff remain concrete open defects, not credited by those cases. See `📓️coordinator-ui-metadata-r1-r2-2026-08-27.md` and `📓️coordinator-neutral-resident-r3-2026-08-28.md`.

Native fullPatchTrackerR6 actually29PASS/1FAIL of30,492unselected,.646s. All7mounted-output cases pass including actual two-thread shared-pool admission; the old capacity fixture still fails before its overflow assertion and is being corrected against charged fixed backing, without quota change. Independent canonical runner6 also passes with4stableinputs. Generated awaited registration1PASS is root-output reviewed; the other two awaited outcomes remain executor-reported because their raw logs are missing. See `📓️coordinator-plugin-runner-and-r6-review-2026-08-28.md`.

Native resident metadata is source-released; its actual first compiler gate is1E0432/four missing symbols, zero test bodies executed. Sole-source implementation/native4/Wasm remain in progress. Shared UI pool, typed evidence, binary inbox, native composition/bootstrap and full app/runtime integration are not complete. Latest independent fullReact658 and actor134 predate the current coherent-source cutover; no current full-suite or strict-green claim.

Three exact native raw log paths are newly absent, including R6 read successfully earlier this continuation. Surviving reports/artifacts are retained, no reconstruction or cleanup was executed, and no cause is established. See `📓️coordinator-native-evidence-absence-2026-08-28.md`.


## 2026-08-27 Active Verification Checkpoint

This checkpoint supersedes older completion claims below. Historical implementation reports remain evidence of their exact runs, not proof that the current shared tree passes every gate.

### Current Gate Snapshot

Full runtime R84 actually121PASS/0FAIL/0skip1.632s; canonical wasm R85 compiles wasip2/unknown-unknown2.32s/2.13s,Nx0. Root read actual footers; live PluginR3/R4 compiler joins remain separate, no Plugin behavior or timing inferred. See `📓️coordinator-runtime-r84-r85-review-2026-08-27.md`. Actual host ledger injection/UI pool/evidence adoption is ongoing under `📓️coordinator-composition-bootstrap-and-proof-ownership-2026-08-27.md`.

Runtime direct output receiver R83 actually1PASS/120skip0.018s after4missing-API REDs; root read source, test and full raw output. Exact job/payload retained across short grant, occupied receiver and post-transfer callback panic; declared8936B stays within32768. Live Plugin pool preadmission remains under repair against its original clean saturationRED. See `📓️coordinator-direct-output-receiver-r83-review-2026-08-27.md`.

Newest independent neutral resident gate R2 exits0 with all7 inputs stable, including domainRecord15/recordOverflow3/recordConstructor3 and strictTS0. Actual record registration/constructor/detachment laws pass; original shell identity remains in the stable neutral observation, so typed private domain emptiness must precede detach. Actual actor/UI adoption, inbox custody and timing remain open. See `📓️coordinator-neutral-resident-r2-2026-08-27.md`.

Current complete static census remains RED:773rows/771unique,350source-admitted,315BatchOnly,2forbidden,270remaining,1009self-tests. It reached actual evaluation (not startup failure); root script/project hashes stable, domain sources not atomically frozen. Source admission is not runtime/timing proof. See `📓️coordinator-current-interactivity-census-2026-08-27.md`.

Current native Plugin boundary: R8 compile GREEN44.02s, construction0/2RED→2/2GREEN0.29s. Producer R2 now3PASS/1clean-saturationFAIL0.05s after cancellation/structural retention repairs; shared pool admission still open. Existing registry allocation actual0/1RED reports1024slots/4,202,496bytes before admission. Exact old-R8 fixture24: combined3PASS→SIGABRT; isolated4processPASS(one unawaited/vacuous viewer),19SIGABRT,1ordinaryFAIL. No full24/Plugin acceptance. Root read actual outputs; repairs remain owned and active. See `📓️coordinator-native-r8-behavior-review-2026-08-27.md`.

Current independent actor checkpoint: R20 full134/134,ninefiles3.46s,start23:21:37,exit0,36 selected inputs stable. Neutral resident R1 independently exits0 with7 inputs stable and strict module TS0. These verify byte-framing and standalone intrinsic admission/custody, not domain-metadata/inbox/UI mounting or native InputAck. See `📓️coordinator-actor-r20-2026-08-27.md` and `📓️coordinator-neutral-resident-r1-2026-08-27.md`.

Current complete renderer regression: independent fullReactR24 actually658/658,fivefiles160.83s,start22:57:53,exit0,with55 selected TS/config/JSON hashes stable. No filter/exclusion or source hold. This supersedes R23's640 at the new source boundary; strictR33 remains exactly7tutorial joins. Shared ledger/raw receiver/native lifecycle and all-app/runtime timing remain open. See `📓️coordinator-renderer-react-full-r24-2026-08-27.md`.

Current independent combined checkpoint: ActorR19 actually131/131,ninefiles4.36s,start22:54:17; OwnedPagedR3 actually15PASS/643skip658,fivecollectedfiles11.46s,start22:54:19. All31 selected TS/config/JSON hashes stable, including framing and credit packet. StrictR33 still exactly7tutorial joins, no framing/UI/resident errors. No-copy format framing and reader/observation recovery are verified in this scope; neutral ledger/raw custody/live native mount remain open. See `📓️coordinator-actor-r19-paged-r3-and-strict-r33-2026-08-27.md`.

Current full native runtime: R81 actually120PASS/0FAIL/0skip,3.109s,exit0, including all119 earlier tests plus the independent zero-node credit law. Root read actual R79 semanticRED/R80GREEN, cfg-only census projection and R81 footer. This supersedes R78's count without turning cfg-only usage into physical-work or live Plugin proof. Mutation17 source repair remains unreleased, so PluginR7 and construction/live-output gates have not run. See `📓️transaction-node-credit-r79-r80-native-2026-08-27.md`, `📓️runtime-full-exhaustive-r81-native-2026-08-27.md` and the coordinator native review.

Latest full native runtime: R78 actually119PASS/0FAIL/0skip,1.874s, all original cases executed. Root read actual output and cfg(test)-only transaction cutover; R76's six failures are preserved as the earlier boundary. Real admitted Job/paired output replaces unsealed test candidates; this is not a production transaction route, live Plugin/key admission, or 8ms proof. See `📓️runtime-full-exhaustive-r78-native-2026-08-27.md` and `📓️coordinator-native-r6-r73-r76-review-2026-08-27.md`.

Latest combined private-input check: independent ActorR18 actually129/129,ninefiles2.19s,start22:45:18; OwnedPagedR2 actually13PASS/643skip656,fivecollectedfiles7.75s,start22:45:23. All26 selected source/config hashes stable, holds released. Three newly imported credit JSON files were captured only afterward, not falsely called pre/post proof. Includes actual sole-builder field continuation and after-bind recovery; credit oracle remains model-only. Reader-finalization and wrapper-grant follow-ups, neutral shared budget and live raw/native ownership remain open. See `📓️coordinator-actor-r18-and-paged-r2-2026-08-27.md`.

Latest independent owned-page checkpoint: PagedR1 actually10PASS/643skip653,19.78s,start22:28:43; ResidentR1 actually3PASS/650skip653,16.49s,start22:28:47. All26 selected imported/UI/config hashes stable; all holds released. Exact first-fragment copy/fault/cancellation/constructor laws only, not multi-page/live/raw ACK. New after-bind and sole-driver continuation TDD follows this checkpoint. See `📓️coordinator-paged-and-resident-r1-2026-08-27.md`.

Latest native inventory: PluginR6 compiled far enough to report19 actual fixture/API errors, no tests; ownership routed17Mutation/1Dag/1Retained, all holds released. RuntimeR76 now executed ALL119:113PASS6FAIL0skip,4.889s; six real transaction-oracle failures remain. Actual in-place transferR73 is3PASS and strengthened actual-callback entriesR3 is1PASS, not live Plugin or source retirement. See `📓️coordinator-native-r6-r73-r76-review-2026-08-27.md`.

Current integration decision: one shared neutral composition resident budget plus exact one-response worker/receiver credit, retaining raw backing independently of semantic consumption; neither is presently a live InputAck proof. See `📓️coordinator-response-credit-and-resident-decision-2026-08-27.md`. WGPU four-file snapshot is explicitly NOT released: four mounted Winit watchdog wrappers still need candidate/verdict adoption, complete32 taxonomy pins remain withheld, no WGPU runtime test from this snapshot. See `📓️coordinator-wgpu-source-boundary-2026-08-27.md`.

Newest authored tutorial replay: independent TutorialLocalR1 actually1PASS/547skip548,one file13.73s,start22:18:14,seven stable hashes. Six neutral cases include explicit UTF16 domain ordering and exact three-map changes. This is cold authored composition only; live tutorial7 capture/record/restore joins remain open. See `📓️coordinator-tutorial-local-r1-2026-08-27.md`. Positive paged-copy/admission is now under actual UI TDD, including root-identified partial-child-fault and admission-retention branches; no final page/raw retirement claim.

Newest fixed worker-response gate: independent ReturnResponseR1 actually5PASS/118skip123,nine collected files1.13s,start22:15:21,all11 hashes stable. The actual TS codec and Node transfer/backing cases executed; this is not full123, generated producer mounting, raw retirement or browser timing. See `📓️coordinator-return-response-r1-2026-08-27.md`. Native paired handback preadmissionR71 also actually2PASS after both real saturation REDs; in-place target transfer and live PatchTracker adoption remain open.

Newest framing replay: independent KernelContentR2 actually49/49,two files3.85s,start22:13:27,nine stable hashes. Root's Node24.15 backing oracle independently passes8 cases including actual MessageChannel transfer and cross-realm intrinsic checks, but not browser/retirement/timing. The single fixed worker-response envelope schema is reviewed; host/worker mounting remains open. See `📓️coordinator-kernel-content-r2-2026-08-27.md` and `📓️coordinator-worker-backing-r1-2026-08-27.md`.

Latest native full attempt is runtimeR68:99PASS/1FAIL/15notrun of115. Original inline/root gates passed; first remaining failure is the cfg(test) bulk-projection transaction oracle expecting1patch but receiving0. This is not full green. Kernel source entriesR2 actually4PASS/261skip; root read source/tests/output and flagged that its catch_unwind closure itself did not mutate the entries. Stronger integrated producer-unwind proof is pending. Plugin native inventoryR5 stopped before tests on the contributed fixture's incorrect relative module path; the real fixture exists, all source holds released, Mutation owns repair.

Newest independent transport/input checkpoint: actorR17 actually116/116, eight files7.10s,start21:56:58, with all21 captured source/schema/fixture/config hashes stable. Holds were immediately released. This includes private captured input rejection laws, not positive UI copying or input ACK. The decoded-page public-return escape is explicitly assigned to the peer's next true RED; full stream/raw-root retirement and live guest adoption remain open. See `📓️coordinator-actor-r17-2026-08-27.md`. Current peer strict additionally includes actively staged tutorial helper diagnostics; the historical exact tutorial7 below is not a current strict pass.

Newest native actual-output review: full UI contractR74 is159/159,0skip; runtime static/output-poolR65 is6PASS and original inline lawR66 is1PASS with unchanged expected[0,0]. Static contract390800 + runtime143568 =534368bytes remains charged without dynamic root slots. Full runtimeR67 was running at this checkpoint; no complete runtime green is inferred. Actor return-wireR8 is5PASS and Kernel source layoutR1 is1PASS, explicitly unmounted. The live output adoption target is production PatchTracker, not cfg(test) transaction. See `📓️coordinator-live-patchtracker-and-backing-2026-08-27.md`.

Newest independent UI forwarding checks: OwnedNativeChild3PASS/640skip643 and OwnedWireOperation7PASS/636skip643, with all six captured hashes stable. An earlier combined selector was a shell-pipeline orchestration failure, not a semantic test failure. The corrected separate existing-target runs and raw evidence are in `📓️coordinator-native-child-r1-r2-and-wire-operation-r1-2026-08-27.md`. Positive paged payload admission remains UI-owned and under TDD.

Newest full native output review: UI contractR71 actually158/158 with0skipped; original existing-component runtimeR60 actually2PASS; combined Kernel return-contentR2 actually7PASS. Full runtimeR61 selected109 with no exclusions and ended81PASS/1FAIL/27notrun: unchanged inline-census law still reports[1536,3072] instead of[0,0]. This supersedes R49 as the latest full-runtime attempt, not as full green. The native owner continues the census/transaction-output work. See `📓️coordinator-native-full-r61-r71-review-2026-08-27.md`. Root's borrowed-message oracle independently passes5endpoint records,8193-byte prefix and6invalid spellings with four stable hashes; format only, `📓️coordinator-borrowed-message-oracle-r1-2026-08-27.md`.

Newest combined renderer checkpoint: independent fullReactR23 actually640/640, five files162.90s, with all33 captured UI/renderer/config/Shard/return source hashes stable. StrictR32 remains exactly seven tutorial joins, not a strict pass. The source hold is released; UI owns the next native-child forwarding packet and authored tutorial TS joins, while Dag retains native Interaction/restore. See `📓️coordinator-renderer-react-full-r23-2026-08-27.md` and `📓️coordinator-strict-r32-2026-08-27.md`. New unmounted work after this release is not implicitly included.

Newest independent generated-format checkpoint: OS-dev return mappingR1 actually67/67, two files34.59s, with seven stable hashes. Root WIT parserR3 also exits0 with16,185-byte dummy metadata held only in memory; its two earlier orchestration failures are retained. The canonical return interface is still type-only: current poll signature/generator and live source ownership are not cut over. See `📓️coordinator-osdev-return-mapping-r1-2026-08-27.md` and `📓️coordinator-wit-return-parser-r1-r3-2026-08-27.md`.

Newest reviewed native scope: common Kernel framing/dialect4PASS; compact component comparison3PASS; split copy/allocation/root-transfer6PASS; runtime canonical grants3PASS and layout1PASS. Actual cursor48,552/retained64,992 fit the unchanged48KiB/64KiB bounds, resolving the exact prior R50 layout RED without a Box or limit increase. Existing-component completion no longer borrows the final child's grant; fresh-component/binding siblings are separately assigned. Full runtime, original inline census, simultaneous resident accounting, transaction output admission, native returned-source/ACK ownership and all-app timing remain open. See `📓️coordinator-native-framing-and-ui-transfers-2026-08-27.md`. The earlier full runtimeR49 remains a historical58PASS/2FAIL/46notrun gate, not superseded by a new full green.

Earlier protocol framing check: independent KernelTS return-contentR1 actually46/46,2files1.51s with seven stable hashes; no body/provenance/retirement credit. Native runtime exhaustiveR49 was58PASS/2FAIL/46notrun, and layoutR50 was RED at cursor53,712bytes against unchanged48KiB; the newer scoped repairs above supersede only their exact failure boundaries. See `📓️coordinator-kernel-return-content-r1-2026-08-27.md` and `📓️coordinator-native-runtime-r49-r50-review-2026-08-27.md`.

Newest independent checks: actorR16 passes106/106,8files7.54s with five stable hashes; OwnedSceneR3 passes9/621skip630,156.24s with nine stable hashes including the near-grant parent-retirement fixes. Refined format oraclesR2 pass13fixed results,2pages,8contradictions,224control pairs and19content checks. Root reviewed actual native Actor exhaustiveR7 112/112, superseding the earlier61/1/50 replay-fixture RED, plus actual canonical reconcilerR46/R47 2+2 laws (zero preadmission allocation,35-turn changed-byte copy,nine exact-root readers and old-reader credit at1/64/4096 grants). Shared host resident-pool/final-witness integration, retained native source/encoder, paired transaction output, inline physical census,tutorial7 and all-app timing remain open. See `📓️coordinator-actor-r16-2026-08-27.md`, `📓️coordinator-owned-scene-field-r3-2026-08-27.md`, `📓️coordinator-refined-return-oracles-r2-2026-08-27.md` and `📓️coordinator-native-actor-r7-and-ui-r46-r47-2026-08-27.md`. Historical full628 predates new isolated TDD and is not current full-suite acceptance.

Newest isolated checks: independent actorR15 passes101/101,8files1.43s with five stable control/schema/Shard hashes. IndexedJSONDocumentR1 passes1/629skip630,71.49s with three stable hashes; the near-grant wrapper-retirement review case is newly assigned, not included. Content declaration oracleR1 passes13 format checks; fixed result oracleR1 passes11vectors,2pages,7contradictions and224control combinations. Native ActorPage3 and handback3 actual outputs reviewed; fullUI155 and runtime101/3realREDsexcluded actual footers reviewed, runtime two Wasm targets compile. Native live canonical root cutover, complete page/typed host ownership, tutorial7, Process fit and all-app timing remain open. See `📓️coordinator-actor-r15-2026-08-27.md`, `📓️coordinator-indexed-json-document-r1-2026-08-27.md`, `📓️coordinator-return-content-oracle-r1-2026-08-27.md`, `📓️coordinator-return-result-oracle-r1-2026-08-27.md` and `📓️coordinator-native-r40-r66-review-2026-08-27.md`. The full628 renderer checkpoint below predates subsequent isolated TDD; it is not a claim that every current in-progress test passes.

Latest renderer run: independent fullReactR22 actually passes628/628 in5files64.94s; strictR31 remains exactly7tutorial joins. Independent OwnedSceneR2 passes7/621skip628 with four stable hashes, including private generic-pack roots and child-refusal/over-grant repair. Seven of eight full/strict source hashes are stable; ShardClient drift is explicitly recorded and not promoted to a stable current transport proof. Native canonical root-credit fullUIR61 is actual-output reviewed154/154; runtime shared-ledger R33+R34 passes2+3 focused laws. Actual reconciler old-tree/R30/R31, handback mutex/poison, live UI adoption and all-app verification remain open. Canonical return controls pass independent strict-schema/LEB R3 after two real strict-schema REDs; semantic stream framing and runtime owners remain under construction. See `📓️coordinator-renderer-react-full-r22-2026-08-27.md`, `📓️coordinator-strict-r31-2026-08-27.md`, `📓️coordinator-owned-scene-fields-r2-2026-08-27.md`, `📓️coordinator-canonical-root-credit-r61-2026-08-27.md` and `📓️coordinator-return-control-oracle-r1-r3-2026-08-27.md`.

Newer isolated transport/storage checkpoint: independent actorR14 passes98/98 in7files1.22s with stable page/Shard/output hashes, including the authored command `{cursor,page}` conversion. Independent byte-page3/95skip and schema-word1394/Buffer512 oracles pass. Common Kernel transportR8 is actual-output reviewed18/18, including all three earlier held-lock failures; UiResidentPermit primitiveR57 passes3/3, but runtime shared-ledgerR32 is a genuine semantic RED and canonical root/final-reader credit remains in implementation. Independent owned scene fields6/621skip627 also pass; later generic-pack TDD is separate and is not included in fullReactR21. See `📓️coordinator-actor-r14-2026-08-27.md`, `📓️coordinator-byte-page-r1-2026-08-27.md`, `📓️coordinator-byte-page-word-oracle-r1-2026-08-27.md`, `📓️coordinator-kernel-transport18-and-resident-permit3-2026-08-27.md` and `📓️coordinator-owned-scene-fields-r1-2026-08-27.md`.

Latest combined release: independent fullReactR21 passes626/626 in5files45.92s; strictR30 is exactly7tutorial joins, with six integration hashes stable across both runs. Independent issued-receipt host9/9 and intake5/5 pass; generated bridge receipt1/60skip61 passes with three stable hashes. The later private transport checkpoint independently passes actorR13 95/95 in6files2.19s; it does not yet mount strong output ownership on public lifecycle calls. Native fullUIR53 passes147/147, commonKernel owner/transport regression12/12 and Actor receipt3+existing lifecycle5 are actual-output reviewed. Whole variable return paging, native transport close, root-associated final credit, live renderer adoption and all-app timing remain open. See `📓️coordinator-renderer-react-full-r21-2026-08-27.md`, `📓️coordinator-strict-r30-2026-08-27.md`, `📓️coordinator-actor-r13-2026-08-27.md`, `📓️coordinator-issued-patch-producer-r1-2026-08-27.md` and `📓️coordinator-document-admission-join-2026-08-27.md`. The following checkpoints retain their historical source/run scope.

Newest independent JavaScript checkpoint: fullReactR20 passes616/616 in5files48.03s; host OwnedInstanceR3 passes9/9 in8.61s; actorR10 passes91/91 in6files2.57s with stable pre/post codec, output-foundation and ShardClient hashes. StrictR29 is exactly7tutorial/local-interaction diagnostics. ActorR10 includes the isolated, still-unmounted strong output reservation and35-byte issued-patch codec; the subsequent host issued-receipt adoption is a newer source boundary. Independent owned-root DOMR1 passes1/617skip618, using real host ownership with a controlled worker. Native fullUIR47 passes143/143 and runtimeR29 passes96 with one intentional physical-census RED excluded; actual outputs reviewed. Runtime/guest/live renderer integration remains open. See `📓️coordinator-renderer-react-full-r20-2026-08-27.md`, `📓️coordinator-owned-instance-r3-2026-08-27.md`, `📓️coordinator-actor-r10-2026-08-27.md`, `📓️coordinator-owned-root-source-r1-2026-08-27.md` and `📓️coordinator-ui-transport-authority-review-2026-08-27.md`.

Newest semantic native REDs: the correctly routed common Kernel DropR2 and normal-closeR3 both wait for a held arena. Existing-record runtimeR30 allocates32,768bytes before resident admission, and R31 compares/copies a final-byte-different32KiB component in one turn with zero allocation debit. These actual failures remain assigned for repair; standalone typed-copy/comparison passes do not establish active reconciliation correctness. The unrelated30OS-kernel fixture errors came from the first wrong target and are separately mutation-owned.

Newest close/paging checkpoint: independent host OwnedInstanceR2 passes7/7 at its captured source boundary; the UI owner's subsequent accepted-input R18 passes9/9 and is awaiting the coordinator's independent rerun. Native actor lifecycle codec/outer-turn laws pass5/5; canonical Kernel/WIT receipt types are mounted, but the pre-Open guest aggregate and exact descendant retirement integration remain in flight and are not Plugin-build-ready. Full native UI R40 passes135/135; runtime R24 passes93 with one intentional inline-census RED explicitly excluded. Three canonical UI wasm32 checks pass as compilation only. No fresh guest, live renderer adoption, all-app close, Process fit or timing completion is claimed. See `📓️coordinator-lifecycle-and-component-copy-review-2026-08-27.md`, `📓️coordinator-owned-instance-r2-2026-08-27.md` and `📓️renderer-owned-instance-2026-08-27.md`.

Newest scoped native comparison checkpoint: document-root comparison R44 passes3/3 with exact immutable lease/ordinal authority, all18 variants, cancellation and contention; full256-frame initialization and owner moves require15,224bytes with zero allocation. It is not yet mounted in old-record reconciliation. Component byte candidate R42 passes5/5, including actual65,536byte allocator-overcapacity retention. Final guest shell release is now ordered before the timing verdict in source, but its four native ownership/clock laws remain unrun. New permanent typed-command fixtures pass strict Ajv2020 validation and independent Node Buffer/TextEncoder output7/7; native40-law execution is still pending, and missing ticket evidence was not reconstructed.

Newest full renderer checkpoint: independent R19 passes595/595 in5files77.10s; strictR25 is exactly7tutorial joins. Independent actorR7 passes74/74. The canonical UI wasm32 width target also completed successfully (compilation only). Paired Surface and exact child-close ownership are tested; mounted React scene cleanup/adoption, resident metering and all-app runtime proof remain open. See `📓️coordinator-renderer-react-full-r19-2026-08-27.md` and `📓️coordinator-actor-r7-and-ui-wasm-r20-2026-08-27.md`.

Latest narrow checks: independent owned Read R3 passes5/579skip and SceneBinding R1 passes4/580skip (584 discovered); strictR24 remains exactly7tutorial joins. Full native UI R19 is122/122, runtime allocation/cancel R12 is2/2, and runtime R13 is88PASS with one intentional inline-censusRED explicitly excluded. These native results are actual-output reviewed. Shared fixed-list paging and retained field cloning now precede honest resident metering: current32bindings alone allocate/copy66,304bytes above a32,768byte grant. No Process fit is claimed. See `📓️coordinator-owned-read-r3-and-scene-binding-r1-2026-08-27.md` and `📓️coordinator-native-ui-r19-and-runtime-r13-2026-08-27.md`.

Newest independent renderer R18 passes577/577 in5files; strictR23 remains exactly7tutorial diagnostics. This supersedes R17's10descriptor-admission failures. Source bootstrap is repaired through the surviving native domain fixture/schema: independent R22 passes1009 self-tests. Full census R6 actually completes RED with773rows/771unique,350bounded classifications,315BatchOnly,270remaining and12failure entries. Unknown missing bytes were not reconstructed. See `📓️coordinator-source-r22-and-full-census-r6-2026-08-27.md`, `📓️coordinator-renderer-react-full-r18-2026-08-27.md` and `📓️coordinator-active-evidence-loss-2026-08-27.md`.

New library checkpoint: full native UI R16 actual120PASS and paged storage4PASS are independently output/source reviewed; first patch backing is33992bytes rather than7.3MiB. Native Scene96PASS is output/source reviewed, including checked numeric narrowing. Independent actor R6 passes66tests. Runtime transfer/finalization and exact rejected payload gates pass; runtime R10 passes86 with the known inline-chargeRED explicitly excluded. Runtime resident/work metering, pre-producer admission, pending-operation cancellation and wasm32 compilation remain open. No Process fit or quota increase is claimed. See `📓️coordinator-paged-ui-actor-r6-and-scene-r6-2026-08-27.md`, `process-workshop-quota-ownership-diagnosis.md` and `process-workshop-native-footprint-tdd.md`.

Independent source R21 passes997 self-tests and actor R5 passes64tests. Independent full React R15 passes542tests; owned-read publication5/5, concrete OwnedSurface5/5 and the new OwnedScene4/4 are separately verified. The Scene parser is not mounted live and its deep-fixture progress-bound refinement is pending. Strict R20 remains RED with seven tutorial joins. Typed-document descendants now actually pass6/6 and full native UI R6 passes108/108; four subsequent held-mutex Drop laws each fail0/1/111skipped, with the atomic handback repair awaiting its fresh focused/full gates. Native deterministic close/clock laws pass4/4, but original-two-only cold R6 remains RED/SIGABRT at8,519/19,965microseconds. The4held-mutex telemetry REDs are followed by5actual GREEN laws, full trace20/20 and job22/22; mounted typed-command fault handoff and6RAII-only callback consumers remain open. Cooperative pool/maintenance laws now pass3/3 and full async53/53; the Plugin Wasm helper and fresh GIS rerun remain uncompiled/unverified. Fresh GIS publishes5initial surfaces then stalls its accepted example command through4096turns. Fresh Process workshop exceeds its unchanged8MiB census at8,391,431bytes; exact ownership accounting is under diagnosis. No all-app completion is claimed.

| Gate | Latest evidence |
| --- | --- |
| Exact peer publication / Presence | Native 12/12; coordinator source and output review complete |
| Local interaction wire / OS client | Independent OS R4 216/216; native retained-root2 and atomic-update3 actual-output reviewed. Store/restore/tutorial and fatal-stream lifetime joins remain open |
| Expanded live query cohort | Native R8 19/19; registered binary query/continuation/ACK/close plus cold transaction receipts and honest encoded-route rejection; restore and full host seam remain open |
| Ordinary app command ingress | Sixteen tags lack route-specific paged admission; real encoded transaction Prepare rejects before dispatch; owned decoder cohorts required |
| Full React renderer | Independent fullR23 640/640 in five files,162.90seconds; strictR32 remains7tutorial joins;33 captured hashes stable. Resident pool/instance/surface accounting and scene parser laws are included in that exact run. Live interpreter/WGPU host adoption, canonical native return paging and strong raw-output retirement remain open |
| Static interactivity | RootR8 stopped at stale Puzzle clock mutation; later executor run passes Puzzle hostiles then stops at stale render-fault-wire-erased mutation. Current full findings remain uncompleted; sound cfg reachability repair remains open |
| Current canonical source bootstrap | Independent R25 passes1009 self-tests,33proof owners,255custom/25generic rows; domain fixture/schema join removes the missing ticket-input dependency without reconstructing evidence |
| Full command census | Independent R6 stdout JSON reviewed: RED,773rows/771unique,350bounded classifications,315BatchOnly,two forbidden,270remaining,twelve failures,1009self-tests; counts are overlapping source projections, not runtime completion |
| Exact instance lifetime close | Independent actorR13 95/95 and issued-receipt host9/9+intake5/5. Native receipt3+existing lifecycle5 and commonKernel12 output-reviewed; fullUI147 and runtime96/1intentionalcensusexcluded. Separate native transport3 held-lock laws are newly RED; guest aggregate/descendants and live issued feedback remain in flight. Original-two-only coldR6 stays RED/SIGABRT; live host mount, resident metering, raw-output retirement and final host/guest timing remain open |
| Owned React read lifecycle | Independent ReadR3 5/5, SceneBindingR1 4/4 and managedSurfaceR2 8/8 (589 skipped). Exact child close and runtime mint guards pass; live interpreter/native-wire/15host integration remains open |
| Real Wasm clock | Bare async wasm32 check PASS, actual output reviewed; old Flow loader/package gate PASS, but consumed-clock gate RED at missing startup clock samples. Fresh shared engine rebuild is coordinated with the demonstrator task |
| Actual CAD / Wasm / all-app browser / hard timing | Real CAD close R3 4/4 and constructor R2 1/1 with 25 exact registry rows; actual command execution, fresh Wasm, browser and timing remain open |

The expanded current evidence is in `📓️coordinator-query-channel-regression-2026-08-27.md`, `📓️coordinator-live-query-transport-review-2026-08-27.md` and `📓️coordinator-command-census-review-2026-08-27.md`. Detailed historical checkpoints follow.

Latest actual renderer/strict results: `📓️coordinator-renderer-react-full-r15-2026-08-27.md` and `📓️coordinator-owned-surface-r1-2026-08-27.md`. Native source/output review and remaining watchdog consumers: `📓️coordinator-exact-verdict-and-fixed-list-2026-08-27.md`.

Latest source self-test verification is independently997/997 (`📓️coordinator-source-gate-r21-2026-08-27.md`); the full command census is a separate RED gate. New scoped review: `📓️coordinator-actor-full-r5-2026-08-27.md`, `📓️coordinator-typed-document-native-r2-2026-08-27.md`, `📓️coordinator-cooperative-maintenance-review-2026-08-27.md` and `📓️coordinator-owned-scene-r1-2026-08-27.md`. `📓️coordinator-cad-harness-and-query-receipts-2026-08-27.md` supersedes the earlier native-empty-reply attribution and records the pending receipt/ownership work.

The owned decoder/SurfaceBytePages foundation independently passes 8/8 (`📓️coordinator-owned-wire-r1-2026-08-27.md`). The actual command-ingress gap and distinction between cold branch tests and registered success are recorded in `📓️coordinator-app-command-admission-gaps-2026-08-27.md`.

Current follow-ups: `📓️coordinator-recovery-and-owned-read-review-2026-08-27.md`, `📓️coordinator-close-lifetime-audit-2026-08-27.md`, `📓️coordinator-typed-retirement-review-2026-08-27.md`, `📓️coordinator-cad-constructor-r1-review-2026-08-27.md`, and `📓️coordinator-os-client-full-r3-2026-08-27.md`. Strict TypeScript remains RED at seven tutorial joins; the retained-operation diagnostic is corrected. Exact host clock/deadline integration remains assigned.

The full renderer checkpoint is now independently green in `📓️coordinator-renderer-react-full-r12-2026-08-27.md`; this includes all four typed ownership laws and supersedes the earlier pending full-suite state, not the still-unmounted live integration gates.

Current exact-close and deadline evidence: `📓️coordinator-instance-close-native-r1-review-2026-08-27.md` and `📓️coordinator-monotonic-clock-host-audit-2026-08-27.md`. The microsecond consumer snapshot compiles, but actual host/WASI clock execution, registered 500-microsecond admission, native fault-owner retention and terminal-publication timing remain assigned. Seven-field ownership is independently green; numeric-index saturation admission has an executor green run and is under independent rerun before live operation adoption (`📓️numeric-reference-admission-2026-08-27.md`).

The seven-field ownership rerun is now independently **4/4**, 523 skipped, five discovered files (`📓️coordinator-owned-node-fields-r1-2026-08-27.md`); full 527-test regression and live adoption are not inferred. Independent actor **59/59** evidence and its Node-worker-versus-Wasm scope are in `📓️coordinator-actor-full-r2-2026-08-27.md`.

- Exact peer publication freshness now passes the complete **12/12 native Presence** group: foreign factory, foreign same-factory Store, stale candidate, successful handoff and cooperative late-commit retirement. Coordinator reviewed the real output and source. Independent source self-tests are **898/898** and numeric-index parity is **12 semantic/37 lifecycle/2 ordinal/3,072 differential/5 invalid-ID**, strict TypeScript clean. See `📓️coordinator-peer-publication-freshness-2026-08-27.md`. Live query delivery/restore, full retained renderer, actual CAD and all-app browser/timing gates remain unfinished.

- Subsequent native verification: canonical reader/sealer partial-error accounting **18/18**, distinct original local/peer close factories **11/11**; actual outputs and source reviewed by the coordinator. Exact peer-publication base/factory freshness is a separate newly assigned defect before CAD. The repaired static reconcile predicates now proceed to **19 standalone-test thread findings**; their reachability projection remains to be repaired without filename exemptions. See `📓️coordinator-ownership-gates-followup-2026-08-27.md`. The live query transport, complete retained renderer and all-app gates are still unfinished.

- Latest independent full React regression is now **506/506 in four files**, exit 0 (`📓️coordinator-renderer-react-full-r6-2026-08-27.md`). The default-environment static interactivity target is newly **RED** despite 101/101 launch discovery: three source predicates still name the peer's old reconcile byte constants/error-arm spelling. Exact attribution and the narrow verifier repair assignment are in `📓️coordinator-static-reconcile-drift-2026-08-27.md`. No current static-green claim is made from the older checkpoint.

- New native gates supersede the r16 failure: **latest-wins r17 10/10**, **Presence r2 10/10**, and **local-interaction capture/retirement/query r1 9/9**. Root read the actual retained output, including registered publication and cross-worker ownership DEBUG lines. These are scoped ownership/page tests, not the still-unmounted user-visible query/restore transport. Root independently passes 851 source self-tests, local-interaction source 12 semantic/9 hostile plus retirement/query oracles, and numeric index 10 semantic/37 lifecycle/2 ordinal/3,072 differential/5 invalid-ID laws. The full command census remains RED at 350 source-admitted, 316 BatchOnly and 269 remaining; counts have not changed. See `📓️coordinator-native-renderer-checkpoint-r15-2026-08-27.md` and `📓️coordinator-numeric-index-test-2026-08-27.md`.

- Current continuation: registered latest-wins r15 natively passes one test (432 filtered, 0.04 s). Full r16 then aborts because a fairness fixture supplied a foreign local-Presence retirement factory; its exact-factory fixture correction is pending rerun. The compiler lease remains with the publication executor, currently running the ten Presence laws while plugin page-query code is mounted. Root independently verifies React **484/484**, full typecheck RED with **seven** real tutorial interaction joins, and linkedSessionEngines **2/2**. See `📓️coordinator-native-renderer-checkpoint-r15-2026-08-27.md`. Later external peer code added the actual MutationLeaf macro, so the older missing-export diagnosis below is historical, not the present build state.

- Latest native registered-dispatch r11 is RED before tests: the DSL facade reexports a missing MutationLeaf derive. The exact dependency join is assigned for repair without deleting mutation semantics. No current native Presence/latest-wins/CAD pass is claimed. The coordinator independently passed 834 source self-tests and the eleven-case/nine-hostile local-interaction cold contract; the live producer remains in implementation. See `📓️coordinator-native-r11-dependency-review-2026-08-27.md` and `📓️coordinator-local-interaction-contract-test-2026-08-27.md`.

- Latest independently verified renderer regression is **476/476 in four files**, coordinator r4, exit 0. The executor's latest coherent typecheck r11 remains RED with eight diagnostics: seven depend on the real local-interaction producer and one on the actual Puzzle-owned BoardSession factory/package. Full captured coordinator output is retained in `📓️coordinator-renderer-react-full-r4-2026-08-27.md`; mocked Wasm DOM tests are not fresh Wasm or all-app proof.

- The corrected source census completes RED with 773 rows, 350 source-admitted rows, 316 BatchOnly, two forbidden, 269 remaining registrations, 823 self-tests, and thirteen failure categories. Correcting stale verifier function bindings accounts for the 244-row admission change; these are not 244 newly proven bounded runtime features. A later source-only self-test checkpoint passes 827. Native latest-wins, Presence, Store local-root and peer-final-owner laws remain pending.

- Active-ticket cleanup protection is now source-reviewed and independently tested: the existing library target passes one synthetic test with 58 assertions, without deleting any real files. The previous cleaner's unsafe open-ticket selection was demonstrated, but its involvement in historical artifact loss is unproven. Registered latest-wins r10 never reached tests because Cargo fingerprint state disappeared. The publication executor holds the next native lease once its public Presence seam is coherent; the cache is now cold. The Flow metadata gate previously failed with eighteen source diagnostics, now repaired but not rerun.

- Current continuation checkpoint: full census r2 is RED with 773 rows, 106 bounded, 316 BatchOnly, two forbidden, 513 remaining registrations, 796 self-tests, and eighteen failure categories. The six-entry decrease is a Procedural3d BatchOnly reclassification; accepted interactive rows did not increase. Registered latest-wins r9 reaches real publication but fails fixed applied/revision capacity after a rebasing edit; no ten-test pass exists. Renderer repair is still in progress, with agent-reported typecheck reductions and a filesystem-interrupted test attempt not yet independently verified. The Flow executor holds the fleet's short metadata-check lease. See `📓️coordinator-command-census-review-2026-08-27.md`, `📓️coordinator-exclusive-compiler-queue-2026-08-27.md`, and `📓️coordinator-presence-and-tutorial-integration-review-2026-08-27.md` for the current evidence.

- The live static interactivity/discovery gate passed with 101/101 app contexts launch-covered. See `📓️coordinator-live-interactivity-gate-2026-08-27.md` for the exact scope; launch coverage does not establish that every action is functional or bounded.
- Phase 8 remains open. The latest full command census exited 1 with 773 command rows, 106 bounded rows, 310 BatchOnly rows, two forbidden rows, and 519 remaining registrations across overlapping projections. Its 676 internal tests are not an all-app pass. Later coordinator source-only self-tests reached 785. The expanded native latest-wins cohort is RED at 9 passed/1 failed: contention, rebase, full-registry reclamation, raw cleanup, fairness, and publication boundaries pass; the registered-dispatch fixture is rejected for a missing window declaration before dispatch. Its repair and rerun are pending. See `📓️coordinator-command-census-review-2026-08-27.md` and `📓️coordinator-cancellation-publication-review-2026-08-27.md`.
- Native kernel canonical rerun passes 22/22 after repairing the reader cancellation-close ownership bug; all four new reader laws executed and reader adoption is authorized. Retained-member tests passed 3/3, expanded group-history/cursor tests 4/4, corrected test-filter/artifact-path helper 2/2, and expanded owned ordered-collection tests 16/16. This includes shared-release/shared-input/race and OrderedSet laws. Primitive adoption into Flow/Dictionary is authorized and in progress. Earlier stdio/CAD library checks passed; the real CAD constructor, app cohorts, Wasm, and browser workflows remain queued. Exact evidence is in `📓️coordinator-exclusive-compiler-queue-2026-08-27.md`.
- Production typed Store advances and closes grant one item and 4,096 bytes. Flow's unsatisfiable 16,384-byte cleanup requirement is repaired in source; its complete scene preparation still needs native verification and bounded ordered-map construction. Earlier CAD/Writer/Sequence and other preparations still require maximum-envelope audits. Source-only acceptance is not a runtime pass.
- Neural full regression now passes 43/43, including the added evaluation-owner law, after repairing the earlier 28 failures with explicit cold/error ownership. Retained evaluation and Flow host integration remain unverified. Browser-worker tests pass 32/32. The full React suite passes its recorded 470/470 checkpoint; the coordinator's separate typecheck remains RED at 385 diagnostics, with further renderer repairs in progress. A local Bun diagnostic also measured the current whole-document patch path above 8 ms; it is not an isolated browser benchmark. Renderer type repair and retained patch decomposition remain open. See `📓️coordinator-neural-ownership-review-2026-08-27.md` and `📓️coordinator-renderer-boundary-review-2026-08-27.md`.
- Immutable child-root reads are synchronous and exact-owner. The monolithic typed child-group fallback was removed and the lane remains fail-closed while real grouped visibility/log/undo is completed. Member publication, provisional history/cursor, and the two new compound-envelope read tests now pass their native gates. Store snapshot/revision/generation selection and mounted atomic groups remain separate pending gates.
- Fresh browser testing previously found a CAD startup proof-join trap, rejected interaction state writes, unnamed Generator sliders, and rejected authoritative slider edits. The coordinator's full React renderer suite now passes 470/470 in four files, including graph-host/slider cases across Flow/Procedural2d/Procedural3d. These cover small payloads, keyboard/drag, stale overlay rejection, and pick-target routing. Backends are not yet registered for the new parameter feature; mock-Wasm DOM results are not authoritative-edit proof. The exact plugin factory proof now passes; the real CAD constructor remains pending after its earlier cold-build timeout. Real CAD draft/presence/transient close adoption is now an explicit prerequisite. See `📓️coordinator-generator-retirement-review-2026-08-27.md` and `📓️coordinator-exclusive-compiler-queue-2026-08-27.md`.
- The literal dependency gate now reports 156 external identities (106 production-reachable): Rust 75, JavaScript 66, Python 15. The audited Bun/Nx allowance has no conflicts. See `📓️coordinator-literal-dependency-review-2026-08-27.md`. The zero-external requirement and all-app timing/replay/device/accessibility gates remain open. No master or phase ticket is being closed from these partial results.

## 2026-08-22 Live Gate Checkpoint

This checkpoint supersedes the older progress table below without deleting its diagnostic history.

| Phase | Current gate state |
| --- | --- |
| 0 — Observability and freeze | Met; deny-mode audits and dependency ratchet are installed. |
| 1 — One-pool runtime | Implementation gate met; the process-wide pool, finite timer turns, admission ledger, and interactive compute job boundary are in place. |
| 1.5 — De-async repair | Open; Animate native library and `wasm32-wasip2` compilation are clean, with canonical descriptor/native runtime/strict-warning/release/`wasm32-unknown-unknown` gates still running. The framework strict-warning cohort and 164 tests pass; the exact plugin gate now reaches the distinct OS-plugin cohort. |
| 2 — Job and progress protocol | Met; bounded torture-job, cancellation, checkpoint, replay, and worker-count gates are green. |
| 3 — UI-thread isolation | Open; the static deny audit is green, but the mounted native renderer still builds product/domain frame work from `redraw`. |
| 4 — Puzzle 3D | Open; the exact unchanged 8 ms fill-build watchdog passes with retained cross-worker sessions. The full `j4` suite then exposed cross-document serialization in the first process-wide mutex design; per-app sharded sessions plus persistence/equality and two-document isolation regressions are now implemented and awaiting a stable shared-plugin rerun. |
| 5 — Frame transaction and renderer | Core transaction/layout/prepared-packet jobs are green; final mounted renderer isolation remains coupled to Phase 3. |
| 6 — FEM | Native job graph, numerical, timing, release, WASI-P2 compilation, and the official composite descriptor assembly are green. |
| 7 — WFC, Puzzle 2D, Energy | Implemented and evidenced by the three packet reports; final integration is retained in the master gate run. |
| 8 — Every tool | Production coverage/classification is complete; the exact plugin all-target warning-denial gate remains open upstream. |
| 9 — Runtime dependencies | Open at 63 Rust identities. Owned replacements include triangulation, planar booleans, codecs/compression pieces, schema/error pieces, shader-contract validation, and a bounded WASM interpreter; the owned WASM production-host/default/deletion packet is active, but the retained runtime boundary is not empty. |
| 10 — UI and tooling dependencies | Open at 122 JavaScript identities plus the native UI/render dependency cohort. The ownership-aware parity gate now covers root fallback ownership, nested technology targets, recognized config, re-exports, and template-safe lexical scanning. |

The live freeze gate is **185** third-party identities, down 53 from the 238 baseline, and rejects
additions. This is progress evidence only: zero is still the Phase 9/10 exit condition.

## ⚠️ ACTIVE COLLISION — a second large refactor is running in this tree

A peer Claude session is executing ticket `26/08/20/COMPOSE-TO-PUZZLE5D-MIGRATION` **concurrently**, with its own parallel "lanes", and it is rewriting the plugin tree wholesale. Identified from the content of its own edits (its diffs reference that ticket by name and say "this lane's agent owns `📦️glue.rs`" and "the other artifact lanes running concurrently"), not inferred from file timestamps.

Scale at the time of writing: **2,664 uncommitted files**, concentrated in `🗄️stdio` (758), `📕️norm` (345), `📸️remodel` (205), `🗒️note` (199), `🎥️shooting` (187), `📏️layout` (151), `📐️cad` (121), `🧱️block` (105).

**Consequences for this refactor, and the decisions taken:**
1. **De-async codemod application in plugin territory is STOPPED.** Packet R15 halted on its own initiative when `🗄️stdio` jumped 747→758 modified files mid-verification, none of it matching its journal's runIds. Fixing files that a competing migration is concurrently rewriting is wasted work at best and a merge hazard at worst.
2. **Error counts in plugin crates are not currently meaningful.** Five crates previously verified "cleared and holding" (`mesh-engine`, `math`, `graph`, `os-kernel`, `os-kernel-db`) reappeared with fresh *test-target* errors that are **not** attributable to this session's runIds. Treat as the peer's transient state; re-verify once their migration settles rather than chasing it.
3. **Framework-level work continues safely** — `⏳️async`, `⏱️trace`, `🧵️job`, `🎭️actor`, `🖱️ui/🖥️host` and `📺️renderer` are essentially disjoint from their plugin work, and that is where Phases 3 and 5 live.

This is the hazard the repo's own rules anticipate: never fight a peer session, never use git to revert its work, and poll rather than chase.

Baseline commit `95b8688ee2f62f4056b6403c282bf0c76172c37c`. Host: 10 logical cores ⇒ `cpu_worker_count = 9`.

## Phase progress

| Phase | State | Ticket |
| --- | --- | --- |
| 0 — Observability & dependency freeze | **closed, gate met** | `…/PHASE-0-INTERACTIVITY-OBSERVABILITY-AND-DEPENDENCY-FREEZE` |
| 1 — One-pool worker runtime | **in progress** | `…/PHASE-1-ONE-POOL-WORKER-RUNTIME` |
| 1.5 — De-async repair (NEW, unplanned) | **in progress** | `…/PHASE-1.5-DE-ASYNC-REPAIR-SWEEP` |
| 2 — Job & progress protocol | **core landed, gate met** | `…/PHASE-2-RESUMABLE-JOB-AND-PROGRESS-PROTOCOL` |
| 3 — UI thread isolation | **in progress** | `…/PHASE-3-UI-THREAD-ISOLATION` |
| 4–10 | pending | — |

## Phase 3 progress

Landed (packets P3a, P3b):
- `RenderSnapshot`/`RenderSnapshotSink` — immutable frame publication. The first `Arc`+`AtomicPtr` design had a **real use-after-free** (SIGTRAP under concurrent load) caught by a stress test; it is now a safe `Mutex<Arc<T>>`. Do not "optimise" it back without a proof and that stress test.
- Enqueue-only `EventQueue`/`CoalesceSlot`, plus the `UiThreadToken` / `WorkerContext` capability split (9 tests).
- `OsHost::redraw` split into `build_and_publish_snapshot` / `present_snapshot`; UI and kernel threads registered with the trace crate; a `UiEvent`-stage watchdog added.
- **19 UI-thread-reachable `pollster::block_on(ParallelRuntime::…)` sites removed** — blocking bridges fell **142 → 124**. 15 in `kernel_runtime::KernelThreadState` collapsed into one legitimate entry-point bridge; 4 in `poll_pending_assets` were a genuine synchronous-`ureq`-HTTP-on-the-UI-thread bug, now on the non-blocking `spawn_app_task` path.
- `FrameBuildJob` — a real `semio_framework_job::InteractiveJob` running the World3D wheel-zoom-deadline scan on the worker pool, polled non-blockingly (`try_recv`, never `recv`). Results are re-validated against live state before use, making staleness safe **by construction**.

Two blockers to full frame-building offload, both now precisely diagnosed:
1. `AppRuntime` is `!Send` because of exactly one field — `self_weak: Weak<RefCell<AppRuntime>>`, self-referential. Every other field was checked individually; `window: Arc<Window>` is structurally `Send` but must not be called off the main thread on macOS/Web/iOS.
2. **The subtler one:** `Shell/🧊️component.rs` reads and writes ~15 `thread_local!` statics during chrome rendering. Moving that to a worker would **not fail to compile** — it would silently read and write empty per-thread state. A latent correctness landmine, not a type error.

Still on the UI thread, and named as Phase 5's actual work: chrome layout, text shaping and tessellation (`render_chrome` takes `&mut GpuContext` and touches those thread-locals), and GPU submission (fused encode+submit+present in one `ui_wgpu` method).

Verification caveat: `semio-framework-os-renderer-wgpu` **cannot currently be compiled** because the peer session's migration leaves `os-infinite` (821 errors) and `s-plugin-stdio` (4,824 on wasm32) broken upstream — zero mentions of our files in either error output. Both packets verified instead via standalone path-dependency crates that compile the code under test for real (7/7 tests each, debug+release, including a stall-resistance test proving the UI thread presents at cadence under a 300 ms builder stall). That technique is honest but is not a substitute for compiling the real crate once the tree settles.

## Phase 1 progress

Landed:
- `WorkerPool` in `🧰️framework/🔨️modules/⏳️async/🦀️component.rs` — work-stealing, DRR-fair, admission-controlled, native threads natively and a cooperative pump on wasm. `ThreadPlan`, `thread_plan()`, `ThreadBudget`, `ThreadRole` **deleted**. `PermitLedger` is release-checked and never wraps (closes the Phase 0 defect). `TimerWheel` replaces the epoch-ticker thread. `run_blocking` removed from `HostAsyncRuntime`; `block_on` behind an `entrypoint` feature. `ChannelPolicy` bounds items **and** bytes.
- Services (`🛎️services`) re-hosted: builds no `tokio::runtime::Runtime`; tokio features narrowed to `sync`+`macros`.
- Actor/shards: per-shard `ShardExecutor` OS threads and `semio-os-host-kernel-shard-forward-*` forwarder threads **deleted**; shard turns are pool submissions with affinity as a scheduling constraint. Actor DRR/mailbox/budget/failure-ladder semantics preserved (76/76 actor tests pass).
- Ad-hoc threads: db storage blocking bridge, db engine per-submit thread, store-sync per-document thread + embedded runtime, pack HTTP per-request fetch thread, hub `HubDbRuntime` — all eliminated or bounded.
- Epoch ticker and process-transport heartbeat moved to `Lane::Timer`. Two blocking-pipe I/O threads remain in process-transport, bounded per child process and registered as `ThreadRole::IoBoundary` so the census can see them.
- Renderer re-hosted onto one injected `WorkerPool`; `ParallelRuntime` no longer owns threads; frame callback wrapped in the trace watchdog.

Verified by the coordinator (not merely reported): the five core Phase 1 crates build clean; production shard and forwarder threads are gone; `ThreadPlan`/`ThreadBudget` survive only in doc prose.

Open items before the Phase 1 gate can close:
- ~~`ManualRuntime::drive()` polls with a no-op waker and cannot observe completions from the real cross-thread pool.~~ **Closed 2026-08-21:** task-specific retained wakers plus an epoch/condition-variable wake path now observe real cross-thread completion; plugin-host is 141/0/1 in both debug and release.
- ~~`WorkerPool::shutdown()` deadlock flagged by P1f.~~ **Closed 2026-08-21:** shutdown drains the timer wheel before joining; focused in-flight-timer shutdown coverage passes in debug and release.
- Low-priority admission's check-then-increment race is closed with an atomic RAII reservation held for the whole job. Async is 43/43 and services is 30/30 in both debug and release; async clippy and both wasm targets are clean. Services wasm remains blocked by 15 existing actor-glue errors.
- `ComputePool::run_blocking` retains an opaque `FnOnce` signature (now a pool submission rather than a thread). The governing rule wants opaque blocking closures gone from interactive paths — carry into Phase 3 with the capability tokens.
- P1h removed four of the six residual production sites: pack retry sleep now uses the shared `TimerWheel`; store-sync uses bounded `WorkerPool` actor turns; DB artifact authority and the feature-gated DB actor use injected bounded pool turns. Their owned production census is zero, and every `ArtifactHost::new` callsite now injects a shared pool. Debug/release pack and OS-kernel checks pass natively; OS-kernel sync passes debug/release on `wasm32-unknown-unknown`. The literal "UI thread + pool workers only" gate is still **not met** because Shell identity bootstrap and Shell directory streaming remain, alongside the separately classified renderer-kernel and registered process-I/O boundaries. Exact architecture, commands, current de-async blockers, and census: `PHASE-1-ONE-POOL-WORKER-RUNTIME/📓️p1h-residual-threads.md`.
- P1i removed the two Shell residuals: identity bootstrap is a deadline/cancellation-aware retained-waker `Lane::Io` future, and directory delivery is a Send-capable bounded state machine driven by finite pool turns and `TimerWheel` wakeups. Shell identity/directory production contains zero spawn, blocking receive, or local executor sites; native OS-kernel and browser-wasm kernel library checks pass. Full renderer/test execution remains masked by unrelated Phase 1.5 syntax/stale-await failures. The strongest repository-wide literal gate still sees separately owned renderer-kernel and procedural-WFC CPU threads plus classified process/CLI I/O boundaries. Exact evidence: `PHASE-1-ONE-POOL-WORKER-RUNTIME/📓️p1i-shell-threads.md`.

## CRITICAL FINDING — the repo did not build at baseline

A full `cargo check --workspace --all-targets` reports ~698 errors. Attribution was verified rather than assumed: `semio-framework-ui` (557), `semio-framework-2d` (28), `semio-framework-graph` (8), `semio-framework-machine-derive` (4) and `semio-s-plugin-draw-fsm` (5) are **unmodified in the working tree**, and there are **zero commits since baseline**, so their content *is* baseline content. This breakage pre-dates the refactor.

The error signatures are one bug class:
```
no method named `X` found for opaque type `impl Future<Output = …>`
cannot apply unary operator `!` to type `impl Future<Output = bool>`
the `?` operator can only be applied to values that implement `Try`
`impl Future<Output = Vec<…>>` is not an iterator
cannot subtract `impl Future<Output = i32>` from `impl Future<Output = i32>`
```
That is **`async fn` called without `.await`**, at scale — the mechanical fallout of the repo-wide async convention (`AGENTS.md:44`: "You SHOULD implement everything async when it makes sense"). Phase 0's census measured the same phenomenon from the other side: 88.28% of 53,338 `async fn` are effectively non-suspending.

Already fixed as an unblocker: `✏️s/🔌️plugins/🧩️puzzle/📦️packages/🦀️rust/build.rs` had an illegal `async fn main()` plus an `async fn out_dir()` called without `.await` — it had never compiled.

Note on scoping: `semio-framework-ui` alone builds clean (`cargo check -p semio-framework-ui --all-targets` → 0 errors). The 557 errors appear only in a full-workspace build, i.e. under cargo's cross-member **feature unification**, and the failing set includes `semio-compose-rs` which is explicitly out of scope. The true in-scope breakage must therefore be measured with compose excluded before sizing the repair.

### Consequence for the plan

De-asyncing is promoted from a Phase 6/7 clean-up to **Phase 1.5, a prerequisite**. Phases 3 and 5 rewrite the UI runtime and the frame transaction; they cannot land on crates that do not compile under the workspace feature set. The repair is driven by Phase 0's `🔧️async-census.json` (one record per function, with body extents) and must use **compiler-verified, span-keyed edits — never name-keyed**, which silently hit unrelated production code.

The refactor narrows `AGENTS.md:44` to *genuine suspension only*. `AGENTS.md` must not be edited, so the narrowing is recorded here.

## Phase 1.5 wave 1 results

| Crate | Before | After | Note |
| --- | ---: | ---: | --- |
| `semio-framework-ui` | 557 | **0** | 400 `async fn` → `fn`, **zero** `.await` added. Repro: `cargo check -p semio-framework-ui --features wgpu,wgpu-engine,tui,tui-terminal --lib` |
| `semio-framework-2d` | 28 | **0** | marching-squares, Douglas–Peucker, planar booleans de-asynced |
| `semio-framework-graph` | 8 | **0** | `DslField::to_value`/`from_value` were async against a sync trait; 4 stray `.await` on sync calls removed |
| `semio-framework-machine-derive` | 4 | **0** | proc-macro entry points were `async` — impossible, since a proc macro runs inside `rustc` with no executor. 4 entry points + 47 helpers de-asynced |
| `semio-s-plugin-draw-fsm` | 5 | **0** | 4 missing `.await` on `restore(...)`, 1 missing generic argument |
| `semio-framework-ui-backend-vulkan` | 1 | **0** | not a defect — the crate's intentional non-Linux `compile_error!` gate |
| `semio-hub` | 70 | 18 | 70/70 attributed to pre-existing breakage (0 from the Phase 1 API change, 0 from P1d's edit); remaining 18 are a cross-boundary blocker |

Fixing the derive macro **cascaded**: it had been aborting the build before `rustc` reached several crates, so `framework-ui`, `framework-2d` and `framework-graph` were partly cleared by it too.

### Blockers discovered by wave 1

1. **`semio-framework-machine` (the runtime crate, not `-derive`): 351 errors**, same bug class, previously invisible because the macro crate's failure aborted the build first.
2. **`db` futures are not `Send`**: a `std::sync::MutexGuard` taken from an `if let`/`if` scrutinee is held across an `.await` (edition-2021 temporary-scope rules) in `⚙️engine/🦀️component.rs:885-896,906-907` and `🔒️security/🦀️component.rs:620`. Axum's `on_upgrade` and six REST handlers require `Send` futures, so this blocks hub's last 18 errors. No in-boundary workaround exists.
3. **UI render backend churn**: `🖱️ui/🖼️render/🎯️targets/{webgpu,d3d12}` moved 476→0 and 0→332 without corresponding edits from the packet that observed it. Needs attribution.
4. `semio-framework-ui-backend-d3d12` has an incomplete platform gate (ungated `windows::` import).
5. `semio-framework-ui`'s unittest target has a pre-existing `Label: From<&str>` compile-time-gate violation from an unrelated ticket — not the async bug class.

## Phase 1.5 waves 2–3 results

| Crate | Before | After | Note |
| --- | ---: | ---: | --- |
| `semio-framework-machine` (lib) | 90 | **0** | 160 `async fn` → `fn`, 0 `.await` added, 3 stale `.await` removed |
| `semio-framework-machine` (lib test) | 318 | 3 | remaining 3 need the derive crate updated in lockstep |
| `semio-framework-os-kernel` (lib test) | 16 | **0** | 12 stale `.await` in `🏪️store/🔄️sync` test fixtures on callees already sync in production |
| `semio-framework-ui` (lib test) | 84 | **0** | not the async class at all: 39 `Label: From<&str>` gate violations + 45 `String`/`Label` mismatches, all in fixtures. The deliberate gate itself was left untouched |
| `semio-hub` | 18 | **0** | unblocked by the db `Send` fix |
| `semio-framework-os-kernel-db` | — | **0** | 478/478 tests green across sqlite/postgres/neo4j |
| `semio-framework-ui-backend-webgpu` | "452" | **0** | phantom — wasm32-only crate checked natively; 0 errors on `wasm32-unknown-unknown`. Gated properly |
| `semio-framework-ui-backend-d3d12` | "332" | **0** | phantom — all cascading `E0433` from one ungated `windows::` import |
| `semio-framework-ui-backend-vulkan` / `-metal` | 1 / — | **0** | `compile_error!` gates replaced with cfg-gated empty libs |

### Two measurement traps, now recorded as method

1. **Platform-gated crates must be measured on their own target.** "452" and "332" were pure artifacts of checking a wasm32-only and a Windows-only crate on macOS; zero of those errors mentioned `Future` or `await`. Always confirm an error actually names `Future`/`await` before attributing it to the async bug class.
2. **`compile_error!` platform gates make a clean workspace build impossible forever**, because `cargo check --workspace` visits every member directly regardless of dependency edges. All four render backends now use cfg-gated empty libs instead, with "wrong platform is an error" preserved one layer up at the consumer's target-gated dependency edge.

### Remaining after wave 3

- `semio-framework-machine` (lib test) — 3 errors. `StatechartEvent::event_id`/`event_name` and `Machine::definition` went sync, so the derive crate's `emit()` / `expand_statechart_event()` must drop `async` from 5 generated methods. Separately, `export_wasm_machine!` has pre-existing bugs (stale `.await`, `restore::<M>` missing one of two generics, a missing `wasm_bindgen_futures` dependency) newly visible from the first-ever wasm32 check of that crate. Blast radius is zero external crates.
- `semio-framework-hash` (lib test) — newly surfaced, unmeasured.

### Phase 2 input from R8

`semio-framework-machine`'s `PersistedSnapshot`/`step()` round-trip, its `Command` effect/kernel split, and its `Inspector` event stream are a genuinely strong foundation for `InteractiveJob`. But `run_to_completion` drains to full quiescence bounded by a *count*, not a deadline, with no mid-macrostep yield/resume, no preview channel and no fault channel. Recommendation carried into Phase 2: make the microstep loop budget-aware and resumable (its pending-trigger queue is already a reified value) rather than adopting this kernel as a ready-made universal `InteractiveJob`.

## The reachability-masking iceberg — and the decision it forces

Phase 1.5 was repaired crate by crate, and every fix **unmasked more damage**, because a crate that fails to compile prevents `rustc` from ever reaching its dependents. The sequence actually observed:

1. The proc-macro crate's illegal `async` entry points aborted the build → hid `semio-framework-machine` (408 errors).
2. Fixing `semio-framework-machine` → unmasked `semio-framework-hash` (29).
3. Fixing `semio-framework-hash` → unmasked `semio-framework-os-infinite` (927 lib + 1228 test), `semio-s-plugin-stdio` (**5,545**), `semio-framework-os-mcp` (22), `semio-s-imperative` (6).

None of these later crates reference the earlier ones' symbols; they were simply never reached. **`cargo check --workspace` without `--keep-going` stops early and cannot show the true picture** — that is the methodological error that made this look like a small cleanup. All future measurement must use `--keep-going`.

### Consequence: hand-repair does not scale, build the codemod

Repairing ~8,000+ errors by hand, crate by crate, is the wrong instrument. Phase 0 already built the analysis half (`🔧️async-census.ts`, `🔧️async-census.json`, 30/30 self-test). The repair half should be **compiler-driven**:

- Run `cargo check --workspace --all-targets --keep-going --message-format=json`.
- Consume structured diagnostics (each carries exact spans, and often a machine-applicable suggestion).
- Apply span-keyed edits for the known bug-class shapes: `async functions cannot be used for tests` → drop `async`; anything shaped `impl Future<…>` where a value was expected → either drop the stale `.await` or de-async the callee, preferring **de-async when the callee has no genuine suspension point**.
- Re-check and iterate to a fixpoint, requiring the error count to fall monotonically.

This is exactly the "compiler-verified, span-keyed, never name-keyed" discipline the phase has been enforcing manually, and R12's cascade gave the empirical shape to automate: of ~86 raw grep-matched call sites, only ~14 were real edits, because most call sites sit inside functions that retain other genuine awaits. A name-keyed replace would have corrupted the other ~72 — and R12 confirmed two *unrelated* same-named `hash_bytes` functions (a render fxhash wrapper and a db/state blake3 wrapper) that a blind textual replace would have silently broken.

## The codemod (R13) — built, and it works

Tool: `🔧️r13-deasync-codemod.ts` (68 KB, TypeScript/bun, temporary, in the Phase 1.5 ticket folder). Journal: `📝️r13-journal.jsonl` (3.6 MB, every edit reversible). Compiler-driven: consumes `cargo check --message-format=json`, applies **span-keyed** edits ranked by rustc's own machine-applicable suggestions, iterates to a fixpoint with a **monotonic guard** that reverts and stops if the error count rises.

| Crate | Before | After |
| --- | ---: | ---: |
| `semio-s-plugin-stdio` | 16,725 | 5 (wave 1: 8,327 edits) |
| `semio-framework-os-infinite` | 2,155 | 2,029 |
| `semio-framework-os-kernel` | 145 | **0** |
| `semio-framework-plugin` | 125 | 114 |
| `semio-s-imperative` | 12 | 4 |
| `semio-framework-mesh-engine` | 11 | **0** |
| `semio-framework-math` | 9 | **0** |
| `semio-framework-geometry` | 8 | **0** |

The guard earned its keep twice: it caught a 2,029→2,033 regression and a 5→13,272 jump, reverting both. The second was then *diagnosed rather than retried* — it was a real reachability cascade (fixing `mesh-engine` and `math` unmasked stdio's own calls into now-sync functions), confirmed by reproducing the exact figure deliberately.

### One real corruption, caught and closed

On the first large run, `findFutureExprSpan()` attributed rustc's "found future" label to a *pattern* sub-span in a `let-else` tuple destructure, and since `Some(x)` is lexically indistinguishable from a call, the tool inserted `.await` after a pattern: `let (Some(src_ep).await, …) = …`. Caught by running the real compiler afterwards (a parse error), contained to exactly one line (verified by targeted grep), hand-repaired, then root-caused with a `PATTERN_CONSTRUCTOR_DENYLIST` (`Some`/`None`/`Ok`/`Err`) and a regression self-test. This is precisely why the packet mandated running the compiler after every crate rather than trusting the tool's self-report.

### Second bug class: stacked `.await`s — found, fixed, and honestly unwound

On the larger runs the tool's `findFutureExprSpan()` fallback picked the **method-call** span (`.into_iter()`, ~9 bytes) instead of the Future-producing receiver, which sits in a *separate, unlabeled, non-primary* span whose `byte_end` exactly abuts the primary span's `byte_start` (confirmed byte-for-byte on `semio-framework-surface`: `[16072,16148)` then `[16148,16157)`). So `.await` landed as `X.into_iter().await` instead of `X.await.into_iter()` — which does not fix the type error, so each later run re-diagnosed it and stacked another `.await`. Accumulated **up to 6 deep, across 116 sites in 16 files**, all attributable to this tool's own repeated runs (the stacking pattern matches its journal, not the peer session).

Why the earlier spot-checks missed it: the first validation of the `is not an iterator` shape went through the **de-async** branch (the callee resolved and was de-asynced), so the await-**insertion** path for that shape was never exercised at small scale.

Fixes: (1) span selection now prefers a non-primary span byte-adjacent to the primary one — a structural signal, not a guess; (2) an unconditional `wouldStackAwait()` backstop refuses any insertion where `.await` is already adjacent, so even an undiscovered future span bug surfaces as residue rather than corruption; (3) reproducing self-tests for both. Then a journaled, revertible `strip-stacked-awaits` command removed all stacked awaits at all 116 sites back to zero — verified by `grep -rn "\.await\.await"` returning nothing and no parse-level errors remaining.

Crucially, this **raises** the reported error counts, because the stripped sites return to their original honestly-broken state. The tool refused to report a corrupted-but-lower number. That is the right call, and the numbers below are the honest ones.

### Final Phase 1.5 numbers (codemod wound down at a clean checkpoint)

**At zero, with tests passing:** `os-kernel` (779 tests), `os-kernel-db` (424), `geometry` (57), `graph` (174), `mesh-engine` (20), `math` (191). Confirmed still zero with no regressions: `framework-ui`, `framework-machine`, `framework-2d`, `hub`, `hash`.

**Remaining in scope:** `semio-s-plugin-stdio` 10,450 · `semio-framework-os-infinite` 1,189 (from 2,155) · `semio-framework-surface` 870 (newly unmasked) · `semio-framework-plugin` 146 · `semio-s-imperative` 4 (E0733 recursive-async, documented refusal). Out of scope: `os-mcp` 24 (unrelated missing-API breakage, zero async-class), `semio-compose-rs`.

**Def-use extension: explicitly refused, and correctly so.** Four real bugs surfaced this session in the *narrower* span-local design — one of which corrupted 116 sites across 16 files. A def-use pass has the same "plausible but silently wrong" failure mode and needs its own dedicated, self-tested packet rather than a same-session extension. A documented refusal beats a third corruption.

**Concurrency:** one directly observed interaction with the peer Claude session in `manifest/component.rs`, on non-colliding byte spans, handled safely by design. Dependency ratchet re-verified at 238. No file left mid-edit; every edit journalled.

### Honest state after the strip

| Crate | Errors (lib / lib test) |
| --- | --- |
| `semio-s-plugin-stdio` | 4,827 / 9,537 |
| `semio-framework-os-infinite` | 869 / 1,152 |
| `semio-framework-plugin` | — / 146 |
| `semio-framework-os-mcp` | 22 / 22 (unrelated: `E0425`/`E0405` against plugin-host, not the async class) |
| `semio-framework-os-kernel` | — / 9 |
| `semio-s-imperative` | 2 / 2 (recursive-async residue) |
| 3 × `semio-s-plugin-cad-*` | — / 4 each |
| `semio-compose-rs` | out of scope |

Cleared during the sweep and holding: `semio-framework-graph`, `semio-framework-os-kernel-db`, `semio-framework-geometry`, `semio-framework-mesh-engine`, `semio-framework-math`, `semio-framework-ui`, `semio-framework-machine`, `semio-framework-2d`, `semio-hub`, `semio-framework-hash`.

### The residue is one shape, and it is the real remaining problem

~1,180 diagnostics in `os-infinite` and 67 in `framework-plugin` share a single shape: the Future-typed expression is a **bare `let`-bound local variable**, assigned from a call earlier and used later — not a fresh call expression. The tool deliberately refuses these (no `(` follows the identifier), because guessing is what produced the one corruption. Fixing them needs **def-use / data-flow tracing back to the assignment site**, which is a genuinely harder tool than the span-local one built here. That is the top work-list item.

Also residue: mutually-recursive `async fn` pairs (E0733) in `semio-s-imperative` where cycle members await non-cycle callees of unverified suspension status — needs either extending the cycle or a human `Box::pin` decision.

## Phase 2 — the job protocol has landed and its gate is met

New crate `semio-framework-job` at `🧰️framework/🔨️modules/🧵️job/`, built on the `⏱️trace` module's structural precedent. Public shape:

- `InteractiveJob::step(&mut self, cx: &mut StepContext) -> StepOutcome` — **synchronous and explicitly resumable, deliberately not `async`**, with `StepOutcome::{Yield, PreviewReady, CheckpointReady, Complete, Cancelled, Fault}`. Two-bound budgeting: fuel counter plus absolute wall-clock deadline.
- `Operation`/`RevisionId`/`validate_commit` — operation id, base document revision, input generation, preview sequence, deterministic seed. A commit is accepted only when base revision and generation still match.
- `JobScope`/`ChildJobGuard` — structured child jobs built on the async crate's `CancelToken` parent-chain, no separate registry.
- Ten-event `ProgressEvent` vocabulary with `channel_policy_for`/`default_channel_kind_for` implementing the policy matrix (latest-wins, coalesced, lossless bounded, ring, lossy, byte-credit).
- `drive_step` — the single point where a `StepOutcome` becomes a `semio-framework-trace` `record_*`/`Watchdog` call, so instrumentation is not duplicated.
- `run_to_completion`/`run_on_worker` — the batch adapter, so CLI and headless paths drive the *same* job implementation as the interactive path and cannot diverge.
- `TortureJob` — the conformance job.

**Exit gate verified by the coordinator, not just reported** — `cargo test -p semio-framework-job` 16/16 in debug and release, including all six torture tests: never trips the 8 ms watchdog ceiling; previews continuously; deterministic for identical seed and inputs; observes cancellation within 8 ms at p99 (measured over 40 trials); checkpoint → restore → resume byte-equal to an uninterrupted run; and replays deterministically across **actual** `WorkerPool` worker counts 1/2/4 rather than assuming. Clean on `wasm32-unknown-unknown` and `wasm32-wasip2`. Dependency ratchet unchanged at 238.

A real bug was caught building it: the initial RNG seeding used `seed | 1`, which collapsed adjacent seeds (42 and 43) onto identical state — replaced with a splitmix64 mixing step. Exactly the class of defect the determinism requirement exists to catch.

## Interactivity audit, re-run after Phase 1 (was 180 findings at Phase 0)

Now **198 findings**: 142 non-allowlisted blocking bridges (was 121), 36 sync-filesystem, 8 thread-pool (was 10), 6 sync-clipboard, 6 sync-process.

**Good:** the audit's own staleness detector independently confirms Phase 1's deletion — the `🎠️activation.rs` `block_on` allowlist entry is reported as "no longer matches any finding". That is machine-verified proof the shard-forwarder poll loop is gone, not an agent's self-report.

**Bad, and partly self-inflicted:** blocking bridges rose 121 → 142. A known contributor is packet P1e, which wrapped **17 `ParallelRuntime` call sites in `pollster::block_on`** because the type's methods became async. That runs directly against the rule that `block_on` is confined to approved process and test entry points. Phase 3 must remove these rather than let them settle — flagged here so the increase is not mistaken for drift.

**Remaining literal thread creation outside the pool after P1h:** the pack HTTP retry site is gone. Production Semio-owned sites still include renderer Shell identity bootstrap and directory streaming plus repo CLI; asyncprobe fixture spawns are test fixtures. Separately, the renderer kernel thread and plugin process-transport reader threads remain registered platform/process boundaries. P1h's four owned source files contain zero production spawns; see `PHASE-1-ONE-POOL-WORKER-RUNTIME/📓️p1h-residual-threads.md` for the exact census and classifications.

### Measurement caveat

Workspace-wide error totals are **non-deterministic run to run** (observed 1058 → 569 → 957 → 1145 with no edits in between) because failures cascade through reachability: fixing one crate unmasks others. Per-crate `cargo check -p <crate>` is the only reliable measure, and progress must be tracked per crate, never by workspace total.
