# Plugin Codec Source Oracle Staging

## Current Boundary

Prepared **ticket-only** schema, fixture and independent source-oracle draft for the [reviewed twelve-site plan](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/📓️plugin-codec-method-local-caller-join-plan-2026-08-28.md). No canonical Plugin, Store, Sync, resident, schema/fixture include, package route, launch entry or production file changed. No Cargo/native command, new unregistered script, source-oracle invocation or TypeScript test ran. This is source preparation, not actual RED/GREEN.

Canonical Plugin/📦️codec/🧵️send path and registered source-route admission remain with taxonomy/root. The draft exports `testPluginCodecCallerSource(repoRoot: string): void`; it has no direct CLI entry. A released canonical router must pass its actual repository root. Do not import/execute this ticket draft ad hoc to obtain a result.

## Ticket Files

| Prepared input | SHA-256 |
| --- | --- |
| [Schema](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧪️plugin-codec-send/🧬️schema/🔣️.json) | `4653711cb2163f8765e7cdeef9db228f7866856e683ec1fd6be4cabf3cd6fb54` |
| [Fixture](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧪️plugin-codec-send/🧪️tests/🔣️.json) | `6f9c1d2f30fbbc916ec1caebf97e8ca237550932888ec3bfc24b86fc5313518f` |
| [Oracle draft](/Users/ueli/Documents/semio/.🧬semio/🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️20/INTERACTIVE-JOB-RUNTIME-REFACTOR/🧪️plugin-codec-send/📜️script.ts) | `729147662971128dac2ad70c3789b359697a4281fce919787d92e4db9bdcf273` |

Hashes were read with shasum; no oracle execution is implied. The schema/fixture were authored before the oracle, then visibility declarations were added before the corresponding selector check. All are still unmounted and uncompiled.

## Contract And Independent Check

The language-neutral section distinguishes owned mutation retained across suspension (Send), shared mutation retained across suspension (Sync), codec-local qualification, and Send-only eligibility of the separate non-codec contract. Four route rows declare app-owned, foreign, bare and direct registration obligations. The native section separately names exact source containers, parent function scopes, twelve method headers/bodies, visibility/async shape, erased spec fields and unchanged base-trait/codec fields.

Eight associated mutation sites and four bare mutation sites are explicit. The direct graph walk computes the required set; the independently expressed dev-only Lodash flat-map/unique/sort relation must produce the same authored set. Strict Ajv2020 validates the fixture, including three staged hostile schema cases. This checks the declared graph/model, **not Rust auto-traits or native runtime behavior**.

The actual-source check is separate from that model. It reads the current four named source owners (Plugin main, Plugin builder, Store, protocol Mutation). Its bounded lexical selector skips nested block/line comments and quoted/raw literals, checks exact container uniqueness, and scopes nested codec functions to their original parent. It does not claim Rust parsing, name resolution or macro expansion. Tokenization is reused within each inspection; source edits are simulated in memory only.

Selected bodies are compared as Rust-like token sequences after normalizing only the twelve reviewed local header qualifications. This preserves code/literals/awaits in the source law while deliberately ignoring formatting/comments. **It is not a byte-for-byte body proof**; actual production patch review and selected pre/post source capture must still verify the promised minimal byte delta. No full main/protocol file pin blocks unrelated owner work.

## Intended Registered Invocation Order

1. Validate the neutral fixture and independent graph result.
2. Construct one qualified source candidate **in memory** by adding only eight A::Mutation: Sync and four bare Mutation + Sync qualifications. Check all preserved boundaries on that candidate.
3. Revert each of the twelve sites individually in a separate mutated candidate. Each source must actually differ; the check must identify exactly that missing site and no unrelated failure. These are twelve real input mutations, not twelve fixed assertions over the same model.
4. Exercise the additional staged source guards: removed await; three widened app/editor/viewer traits; unreviewed A/PA/associated Diff bounds; enclosing impl widening; erased fn-pointer replacement; removal of each of the two Send slots. Comment and raw-string decoy declarations are positive lexical-isolation checks.
5. Only then inspect the **unchanged actual source** and print its exact missing/problem lists. Current source is expected to fail the desired-law assertion for twelve missing local qualifications, but that result has **not run** and cannot be reported as actual RED yet. An earlier schema/selector/body mismatch must be reported as the actual failure, not relabeled as intended missing qualifications.

The draft's prospective source-negative inventory is23 mutations (twelve local omissions plus eleven other boundary mutations), alongside two lexical decoy positives, one qualified in-memory positive and three schema hostiles. These are counts of authored branches, not executed/pass counts.

Unexpected/missing/duplicate containers, altered visibility/async shape, bodies, trait bounds or codec fields remain errors. There is no catch that marks malformed source as successful, no runtime local-future fallback and no automatic production edit.

## Separate Compiler Proof

The proposed non-codec Send-but-not-Sync mutation fixture remains a distinct future authored Rust compiler proof. This source oracle only checks that the base trait declaration was not widened; it cannot prove an implementation containing a Cell payload satisfies all of ArtifactApp's actual requirements.

No shared TestApp defaults/metadata were changed, and no copied empty-descriptor shortcut or blanket trait relaxation was introduced. That fixture must be authored independently with real required metadata/codec semantics, then compiled under a separately authorized owner-crate gate. It is not mounted by this source packet and has no passing claim.

The existing two OS Send tests remain untouched and cannot be executed by a Plugin dependency compile. Neither this source preparation nor a future source-oracle GREEN may be credited as their native execution.

## Next Reviewed Boundary

Await taxonomy's exact canonical path/export/registered command release. Then coordinate source-only canonical test packet mounting, capture its actual source closure, and execute only the admitted registered source route. Preserve its complete actual RED and hostile results before any Plugin production qualification edit. No native lease is requested.

No Store/Sync detach or resident/Opening work is included. The original-parent funded receiver remains absent; this qualification packet does not change that.

