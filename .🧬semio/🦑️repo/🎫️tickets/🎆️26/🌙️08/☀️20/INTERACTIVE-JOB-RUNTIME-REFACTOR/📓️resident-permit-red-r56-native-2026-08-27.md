# Neutral Resident Permit — Native Compile RED R56

Canonical UI contract selector `retained_resident_permit_` failed before tests with 33 missing-API errors: `UiResidentPermit`, limits/progress/snapshot/fault types and the fixed ledger constants were not implemented. The three schema-first native laws are unexecuted. Node Buffer/Ajv source oracle passed 63 checks.

```text
error: could not compile `semio-framework-ui-contract` (lib test) due to 33 previous errors; 70 warnings emitted
NX Running target test for project @semio-tech/ui-contract-rs failed
```

Raw output: `🧪️member-ui-resident-permit-red-r56-native-2026-08-27.txt`. The next implementation factors the existing reservation semantics; until runtime is migrated, this module must not become a second independently enforced quota. Canonical root/reader credit binding remains a later integration step in the same packet.
