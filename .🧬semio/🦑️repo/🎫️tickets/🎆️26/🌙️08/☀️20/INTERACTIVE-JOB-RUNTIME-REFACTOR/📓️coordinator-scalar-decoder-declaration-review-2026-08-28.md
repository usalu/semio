# Scalar Decoder Declaration Coordinator Review

## Read Boundary

Root read the full scalar proposal and all four JSON documents (367 contract,428 contract-schema,376 fixture,154 fixture-schema lines). Root also read the current resident reader675–770 and existing wire value scalar branches. No scalar implementation, reader API, metadata price or production file changed in this review.

The five fixed records sum to432 logical bytes; record264 plus cell296 produces992/14/14. Payload+8 and reader+16 are separate proposed parent costs, not covered by that992. Current production prices are not changed by arithmetic agreement. This is not a heap bound or a same-ledger full-field fit proof.

The proposed UI value tags match the existing value cursor. Its finite, negative-zero-rejecting float policy is a UI value profile only: the separate native generic scene-numeric fixture deliberately accepts IEEE negative zero and has a different acceptance boundary. Preserve this distinction and do not rewrite generic codec laws.

## Required Before Source Cutover

1. Make the neutral accepted/expected relation explicit. Current vector schema permits accepted=false with a string result, and the inline oracle compares only actual to expected before counting the unrelated accepted flag. Add desired-law contradictory-flag cases, unique IDs and required profile coverage. Preserve all existing43 values; do not mistake declaration arithmetic or a schema pass for the new algorithm running.

2. Replace ownership-law prose alone with closed executable neutral traces for actual source receipt installation, cursor commit, serial, latch, parse, settlement, replay, cancellation and wrapper faults. Distinguish await-page/await-seal from EOF at every scalar prefix; truncation is not ordinary backpressure.

3. Declare the exact original-reader callable/private consumer verifier and source-owned transaction work charge. Current advanceReader reads into a local primitive, advances offset/consumed and allocates a returned byte object. The proposal must install its preadmitted exact receipt before the external return boundary and guard forward reads by the mandatory original consumer, without an optional old-call path. Explicitly account for receipt/cursor mutation work; merely forwarding the old bytes=1 result cannot explain newly appended wrapper work. A maintenance turn must still forward its original result, and observation occurs later.

4. Specify exact close grant phases and original child contributions. The present23 close names are useful ordering but not yet a checked prefix bound or executable phase/cost oracle. Retain every admitted prefix through first faults, distinguish receipt quiescence from reader terminal, and use the same private witness for both directions. Unknown faults remain funded; negative matching is not a never-installed proof.

## Authorized Next Slice

UI may harden the ticket declaration and stage the corresponding tests in the existing registered renderer route after coordinating any canonical domain admission. This is schema/test-first preparation; no decoder, raw read, caller fallback, price/capacity, semantic output or live renderer mount is authorized yet. Publish exact mandatory callable names and phased vectors before coordinating the actor callers.

The next actual runtime boundary should establish the preadmitted original-reader receipt/binding transaction with its real early-reader256-byte window and loss/replay/close tests. Scalar arithmetic may consume only that canonical transaction once proved; no whole-buffer substitute or second codec grammar. All43 primitive oracle vectors remain available for subsequent bytewise implementation parity. No raw InputAck, native semantic ACK or full typed scene publication follows from this proposal.
