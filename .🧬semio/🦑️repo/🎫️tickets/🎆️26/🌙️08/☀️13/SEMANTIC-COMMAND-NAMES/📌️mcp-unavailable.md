# Repo MCP unavailable

This session's MCP catalog exposed only `cursor-app-control`. `repo://goals`, `ticket_open`, and `ticket_close` were not callable. Goals were listed from `.🦑️repo/🎯️goals`. This ticket is associated with `AI-OPTIMIZED-REPO` (same family as Semantic Mutations Overhaul). No existing open ticket covered command-folder naming; `SEMANTIC-MUTATIONS-OVERHAUL` is mutations-only.
