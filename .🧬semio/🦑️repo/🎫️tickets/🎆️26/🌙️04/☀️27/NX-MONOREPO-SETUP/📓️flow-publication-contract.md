# Flow Publication Contract Follow-Up

The repeat of the actual native renderer build stopped before compilation in the framework graph generator (637 ms; 1/3 completed tasks from cache). The current shared taxonomy added flow-browser-package as an owned generator contract without its required previewTarget. loadCatalogTaxonomy rejects it. This is a new shared-source validation failure following the successful full repo:test run, so the repeat does not qualify native warm reuse. No Flow source or taxonomy edits were made in this inspection.

The current Flow core package has a wasm target that compiles the Flow family via runWasmPackWebBuild, copies native bytes into a second core bindings directory, and writes browser, host and declaration projections. Its separate declarations target also writes the source declaration, core declaration and package manifest while declaring outputs: []. The new owned taxonomy contract includes two roots (ignored family bindings and tracked core bindings), lacks a preview command, and does not include the separate source declaration output. These are actual ownership concerns, not merely a missing metadata string. The durable correction should separate native compilation from finite browser/declaration projections, declare every output owner and prerequisite, and implement a nonmutating preview from the same projection. A fake preview target or weakened schema would hide the conflict.

Inspect the current sources again before changing them because Flow files and the taxonomy are being edited concurrently. Relevant owner: semio-framework-os-flow-core at 🧰️framework/🛍️products/💻️os/🔨️modules/🌊️flow/🫀️core/📦️packages/🦀️rust. Publisher: 🌊️flow/🕸️wasm/🌐️browser/📦️publication/🟦️.ts.
