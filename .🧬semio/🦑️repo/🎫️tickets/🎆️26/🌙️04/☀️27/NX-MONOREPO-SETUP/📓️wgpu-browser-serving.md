# WGPU Browser Serving — 2026-09-13

The browser host now has a dedicated Vite configuration and a narrow serve command. It mounts completed dev/release compiler outputs, generated boot/worker files and the existing plugin/extension stores. The inferred variant/profile targets depend on activation, while the public Nx launcher owns the matching activation watch. The former TrunkServeScript and the OS DevScript that started Trunk/reused or killed occupied ports have been removed. The historical Trunk config/HTML still remain in the lockfile test surface and require retirement; native-run activation remains a separate pending nested-orchestration issue.

The language-neutral fixture initially failed because the standalone browser document was absent (1.1s). Native HTTP exposed missing-module fallthrough (200 HTML rather than 404) and a reused native config object retaining a watcher across failed same-port starts. Explicit route boundaries and per-invocation configuration factories fix both. The focused native Vite test passed in 2.4s, including independent native public-file byte comparison for both profiles, WebSocket full reload after an activation marker, occupied-port rejection, listener shutdown and process exit. A subsequent run adds native WASM execution and prepared-file checks. Native Nx preparation/activation plus matching profile server-command inference also passes. Full suite and actual configuration qualification are pending.

The actual full renderer build ended after 19m28s: its Cargo child received SIGTERM, which Trunk reported as a failed pipeline. No successful publication was recorded; the initiating process is not established. This agent did not terminate that build or shared compiler workers. A new actual-root compiler run is in progress. The compiler's isolated fixture/oracle proofs and prior full repository suite remain passing evidence, not a substitute for the full renderer build.

The historical Trunk.toml/HTML, Trunk copy-route parser and its copier-specific tests have now been retired. The replacement route fixture names canonical mounts, and the lockfile test extracts and parses the finite compiler’s private Trunk configuration. The native Vite + current Trunk contract pair passes in 3.3s. Browser boot selection now accepts the per-server meta selection while preserving explicit query precedence; its Bun/esbuild/native-Node test passes in 11.3s. The actual-root boot generator/editor prerequisites completed in 2m28s.

The renderer retry succeeded in 2m26s, publishing four finite dev files (0/8 task cache hits). The first new full suite failed in 53.9s because another active refactor moved OS Dev’s Vite configuration. Its fixture now names the observed current builder path; no compatibility fallback was added. The dedicated WGPU config was also concurrently relocated to 🌐️server/🎚️config/🟦️.ts, with its launcher adjusted. A fresh full run and actual renderer HTTP checks are in progress.

Actual runtime qualification: the native Nx verification pair completed in 17.3s. The real WGPU Vite configuration returned HTML with HMR and the selected playground, served canonical renderer JavaScript/WASM and both generated worker files with SHA-256/byte counts equal to their published filesystem artifacts, then closed its listener. Four engine module-route Vitest tests also passed (11.24s). This proves HTTP delivery, not a rendered WebGPU application or end-to-end plugin interaction.
