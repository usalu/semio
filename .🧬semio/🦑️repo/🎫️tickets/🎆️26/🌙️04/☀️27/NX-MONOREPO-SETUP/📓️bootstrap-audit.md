# Bootstrap Audit

The current root configuration still references removed Compose-era directories. These paths were checked on disk and are absent: `compose`, `compose/py`, `compose/engine`, `elements/client/lib/geometry/topologic`. The only declared Git submodule is the research repository.

| Surface | Observed Configuration | Required Alignment |
| --- | --- | --- |
| Python | Root workspace lists compose/py and compose/engine; styling and Energy oracle have standalone locks | Keep active Python environments explicit and preserve the Energy oracle's separate Python 3.12 constraint |
| .NET | Monorepo.sln lists removed Compose projects; the current test-host csproj points at nonexistent 🔷️host.cs | Use the current test-host project and its actual 🔷️.cs source |
| CMake | Root adds nonexistent Topologic directory; presets and setup install vcpkg for it | Remove the obsolete product dependency and keep native configuration/provisioning separate |
| Editor | Three Animate-video entries reference missing animate/video/rs; Coda and Trinity entries also need current target mapping | Remove unsupported entries or map supported commands to real Nx targets |

Baseline Python and .NET dependency tasks are being executed to capture their concrete errors before making changes. No bootstrap configuration changes have been made yet.


## Baseline Results and Correction Contract

`workspace:deps-python` completed successfully: uv checked 147 packages. Its frozen invocation does not validate stale workspace membership, so success does not establish that `compose/py` and `compose/engine` are valid projects. The root environment will retain its existing locked dependency groups; independently versioned Python environments remain separate. Repository native adapters continue to use the explicit test coordinator instead of broad pytest discovery.

`workspace:deps-dotnet` failed with eight MSB3202 errors from the obsolete Compose solution. The current .NET support project also names a missing compile leaf (`🔷️host.cs` instead of `🔷️.cs`). The correction will give that existing library a project-owned restore target and a build target that publishes only consumable DLL/PDB/dependency metadata. Mutable MSBuild state will remain outside Nx artifacts. Root setup will depend on its restore through Nx.

The root CMake graph has no surviving application subdirectory. It will retain native tool configuration without referencing deleted Topologic paths or downloading unused vcpkg state. Configure/build/test ordering belongs to Nx; the commands will remain uncached while they manage native configuration state.

The .NET implementation follows Microsoft's documented [explicit restore/build boundary](https://github.com/dotnet/docs/blob/main/docs/core/tools/dotnet-build.md) and [locked restore behavior](https://devblogs.microsoft.com/dotnet/enable-repeatable-package-restores-using-a-lock-file/). The restore and build must use the same intermediate directory.


## Verified Implementation

The .NET project is now an Nx library with uncached locked restoration, cacheable Release compilation, and a staged `dist/build` deliverable tree. Native MSBuild state lives in the repository cache. Its compiler reported zero warnings and errors. An actual Nx cache restoration and independent .NET consumer passed; see `📓️dotnet-artifacts.md`. Source paths are remapped in compilation so artifacts do not embed this checkout's path.

Root Python setup now validates its lock with `--locked` and depends on separate Nx targets for the styling and Energy oracle environments. All three environments synchronized successfully. Styling's Python import test passed through its dependency and canonical generation prerequisites. Energy execution/status targets remain explicitly uncached because they inspect external native tool and fixture state; their Python execution no longer synchronizes dependencies implicitly. The Energy archive provisioning command remains a separate uncached target.

CMake configure, build, and the completion target ran successfully on macOS after removing deleted Topologic configuration and the unused vcpkg installer. CTest reported no registered tests; this is not a native product test pass. Nx now orders all native configuration steps, including forwarding a selected preset to prerequisites.

The language-neutral bootstrap fixture first failed on the missing .NET project, then passed after implementation. Final `repo:test` and `repo:graph-check` passed together: 306 resolved projects and 1,222 dependency edges. The editor seed includes the new restore/build/environment commands; its launch file was regenerated by the canonical registry target.
