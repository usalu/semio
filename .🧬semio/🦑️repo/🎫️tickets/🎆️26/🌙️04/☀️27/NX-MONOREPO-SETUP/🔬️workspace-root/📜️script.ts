import { join, resolve } from "node:path";
const { testWorkspaceRoots } = await import(join(process.cwd(), "🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/⚡️caching/🧪️tests/📜️script.ts"));
testWorkspaceRoots(process.cwd(), resolve(import.meta.dir, "../🗑️generated"));
