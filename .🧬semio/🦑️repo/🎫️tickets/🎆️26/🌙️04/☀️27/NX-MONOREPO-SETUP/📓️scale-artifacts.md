# Scale WASI Component Restoration

Nx restored the deleted staged component directory with identical file hashes and modes. The independent installed Bytecode Alliance component tooling read the restored binary and extracted its reactor, checkpoint, describe and jobs interfaces. This interface proof does not claim native actor execution; the genuine Wasmtime consumer is checked separately.

{
  ".nx-artifact.json": {
    "hash": "41566c2327e18a859523a79c0dfe493e0a138adedc8556c1d193db89c179456a",
    "mode": 420
  },
  "semio_framework_os_scale_fixture.wasm": {
    "hash": "0ecf6554bfb276e75a6c56588e4875a11ab3499c200e9b42c760b90d81629c3c",
    "mode": 420
  }
}

## Shared Schema and Editor Contracts

The Scale producer explicitly hashes the plugin WIT schema outside its package. The language-neutral contract failed before this input was declared and passed afterwards; the real deleted-directory restoration and independent Node/JCO WIT consumer passed again with the corrected input. Native, React and WGPU benchmark selection resolves before Nx constructs the graph. The native variant depends on Scale and WGPU binary producers; browser variants depend on browser provisioning. Benchmark reports default to a renderer-scoped repository cache directory rather than a historical ticket. The generated editor entries were refreshed through the registry generator.

## Native Consumer

The public plugin-host ui-patch-marshalling-native-check completed successfully after the Scale output had been restored. It executed six exact native laws, including three genuine Wasmtime component laws exercising returned/imported patch order, atomic refusal and malformed-import draining. The source/AJV oracle also passed seven source cases, four component cases and eleven operations. Native compilation took 15m28s. Inspection of the receipt exposed a separate remaining storage defect: runExactCargoLaws defaults its Cargo target to the per-ticket evidence directory, duplicating the native compiler store. This helper and its callers must be corrected as part of the remaining storage work.
