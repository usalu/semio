# Report document ownership inspection

Observed on 2026-09-08 in `♻️mit-bestand/📋️bericht/📦️packages/🟦️typescript/📜️script.ts` and its project metadata. The three registered documents are Zwischenbericht, Forschungsbericht and Kompaktbericht. Four build targets remain uncached: the aggregate loops over the three documents internally, while each named target can still receive arbitrary TeX paths and output-directory arguments. `SEMIO_PRINT_OUTPUT_DIR` also redirects outputs. Source staging and compiler intermediates share the final output directory. A custom recursive filesystem watcher serializes rebuilds outside the Nx graph. The `latex` target duplicates this arbitrary-document path.

Forschungsbericht compilation also runs read-only Akteursnetz source/render validation inside its producer. That validation should be a declared cacheable prerequisite. The independent `akteursnetz-render` operation currently writes four tracked fragments and should obtain an owned generator contract, precise inputs and its own cache test. It must remain separate from the PDF compiler.

Existing editor entries invoke the three named build targets and one generic watch target; no active editor entry was found for `latex`. The language-neutral report-family fixture can validate the new catalog identities. Each PDF leaf should publish a fixed `dist/documents/<id>` directory, stage temporary TeX inputs separately, and consume the already declared Print fonts, Tectonic executable, local TeX bundle and token prerequisites. At initial inspection only the report input/toolchain prerequisites had been updated. The implementation checkpoint below supersedes that initial state.

## Fixed document and generator contracts — 2026-09-08

Added a versioned, schema-validated catalog for the three report documents. Each build target now publishes a fixed `dist/documents/<id>` directory containing its PDF and artifact marker. The collection target only verifies the three outputs after Nx completes the prerequisites. The minimal compiler router accepts exactly one declared document and uses the shared pinned Tectonic/font/TeX bundle pipeline, with isolated temporary staging and cancellation. Removed the old arbitrary-output compiler commands, internal document loop, filesystem watcher and duplicate `latex` target.

The Akteursnetz generator now owns exactly four tracked LaTeX fragments through `report-actor-network`. Rendering derives all four files before publishing and checks that its ledger stayed unchanged. Read-only preview emits the owned-generator JSON protocol; progress goes to stderr. Asset existence validation remains in source validation/tests, while the generator is determined by its ledger and implementation. The validation target hashes the checked generated fragments and assets. The report compiler imports neither the generator nor the report tests.

Added seven language-neutral root invocation vectors. The red test caught missing Nx watch coordination; all vectors now pass. Report watch targets retain their own build prerequisite, and the root coordinator selects the corresponding Nx rebuild target. Editor seed entries cover three document watches and generator preview/check, plus the renamed generator command.

The configuration probe passed schema validation through Ajv, the independent existing report-family fixture, fixed output ownership, prerequisites and invocation vectors. The first actual fragment check completed successfully against all four existing files. Permanent report quick checks now include the catalog, producer ownership and an independent esbuild compiler-import closure check. Normal shared-daemon Nx report quick, full report build, repository contracts and editor generation are running; these results remain pending. Clean-output cache restoration, complete PDF consumption and report watch lifecycle are not yet qualified.

All three report PDFs published successfully through the declared outer Nx graph. The aggregate verified all three outputs, and Nx reported 9 tasks, 2 cache hits, 14m29s task-run duration with deliberately constrained parallelism (one report task at a time alongside the separate Print gallery build). Compiler layout warnings remain visible; no compiler errors occurred.

Separated the actor-network implementation into its own minimal `🔨️modules/👥️actor-network/📜️script.ts`. Its cache inputs now omit the report test router and document catalog. Sorting uses an explicit English collation and generation hashes platform/architecture as well as Bun version. The isolated generator's exact four-fragment check passed in 55.9s. The revised report quick suite passed in 513ms of producer time, independently checking esbuild command closures of 13 files for the compiler and 3 for the generator.

The final-source native repeat and clean-output Nx restoration probe is now running for all three PDF directories. Long tests consume completed PDFs through PDF.js using authored page-count/content vectors; they declare the PDF producer and hash its transitive PDF outputs. Neither the new repeat/restore probe nor PDF.js long validation is claimed complete yet.

The final-source native repeat produced byte-identical PDFs for all three documents. The first restoration reported each PDF leaf as a local cache hit and restored every marker/PDF byte, but its overall invocation failed while the concurrent dependency install replaced Nx files. Therefore it is not recorded as a clean restoration pass. All redundant backup bytes were checked before removal, and the probe now reruns after dependency synchronization. Its unchanged-generator assertion accepts Nx's distinct existing-output cache-hit label as well as a restored local-cache label.

The repeat/restore rerun passed cleanly after dependency synchronization. All three leaf targets restored their entire PDF/marker directories byte-for-byte from local Nx cache; the unchanged actor-network generator reused its result. The long validation also passed: independent PDF.js consumed every page of Zwischenbericht (192 pages), Forschungsbericht (205 pages) and Kompaktbericht (6 pages), and found all authored expected text. Nx reported 2m8s task-run duration, 6/10 cache hits. The read-only generator preview completed in 1m46s; its JSON protocol contains exactly four canonical 0644 file nodes whose decoded bytes equal all four tracked outputs, with no stale removals. Actual report-product watch lifecycle remains unqualified.

The scoped-input retry rebuilt all three report leaves in 22m30s and failed the explicit cache-hit assertion. The workspace-root helper changed between its population and restoration runs. A separate comparison confirmed that every rebuilt PDF and ownership marker is byte-identical to the saved prior output. This is another native repeatability result, not cache restoration. Only the redundant probe backup was removed after comparison. A fresh current-input cache retry follows.

The subsequent current-input retry passed: all three report leaves restored from actual Nx local-cache hits after their output directories were removed. Every PDF and ownership marker matched the saved bytes, and actor-network generation reused its cache entry. This qualifies the scoped Bun inputs and final workspace-root helper for these report producers.
