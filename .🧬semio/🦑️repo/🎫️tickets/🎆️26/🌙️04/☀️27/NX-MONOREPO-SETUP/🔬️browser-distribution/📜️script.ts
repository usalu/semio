import { dirname, join } from "node:path";
const { testBrowserDistribution } = await import(join(process.cwd(), "🧰️framework/🛍️products/🦑️repo/🔨️modules/📚️library/⚡️caching/🧪️tests/🌐️browser/📜️script.ts"));
await testBrowserDistribution(process.cwd(), join(dirname(import.meta.dir), "🗑️generated"));
