# Demonstrator Preparation

Read-only inspection on 2026-09-08 confirmed that Demonstrator build/dev call OS Dev plugin builds, registry generation and six pane engine preparations inside one target. Its build has no fixed output contract. Its E2E command starts the raw development script and permits reuse of the fixed port. Vite imports the task router for the runtime layout, and copies ambient global plugin/extension roots in addition to its computed union. These paths remain unqualified for task caching.

The six panes need Demonstrator and Procedural runtime plugins. The existing runtime layout recursively follows component dependencies and consumed extension contributions. Nx playground preparation currently expands contributions only for the root before traversing ordinary dependencies. A fixture and comparison against actual Cargo metadata are needed to determine and fix any omitted preparation edges.

Planned implementation: schema-owned runtime selection; pure component closure and consumer configuration without task-router imports; explicit preparation and output ownership; one Nx-owned server lifecycle; independent cache restoration and runtime consumers. No completed Demonstrator runtime validation is claimed by this inspection.

## Runtime Closure Comparison

Independent fixed-point traversal over the current generated registry found these root selections missing recursively consumed contributors under the previous root-only contribution scan:

- flow-extension-bim: flow-extension-brep, flow-extension-dictionary, flow-extension-draw, flow-extension-list, flow-extension-logic, flow-extension-math, flow-extension-primitive, flow-extension-text
- flow-extension-brep: flow-extension-bim, flow-extension-dictionary, flow-extension-draw, flow-extension-list, flow-extension-logic, flow-extension-math, flow-extension-primitive, flow-extension-text
- flow-extension-dictionary: flow-extension-bim, flow-extension-brep, flow-extension-draw, flow-extension-list, flow-extension-logic, flow-extension-math, flow-extension-primitive, flow-extension-text
- flow-extension-draw: flow-extension-bim, flow-extension-brep, flow-extension-dictionary, flow-extension-list, flow-extension-logic, flow-extension-math, flow-extension-primitive, flow-extension-text
- flow-extension-list: flow-extension-bim, flow-extension-brep, flow-extension-dictionary, flow-extension-draw, flow-extension-logic, flow-extension-math, flow-extension-primitive, flow-extension-text
- flow-extension-logic: flow-extension-bim, flow-extension-brep, flow-extension-dictionary, flow-extension-draw, flow-extension-list, flow-extension-math, flow-extension-primitive, flow-extension-text
- flow-extension-math: flow-extension-bim, flow-extension-brep, flow-extension-dictionary, flow-extension-draw, flow-extension-list, flow-extension-logic, flow-extension-primitive, flow-extension-text
- flow-extension-primitive: flow-extension-bim, flow-extension-brep, flow-extension-dictionary, flow-extension-draw, flow-extension-list, flow-extension-logic, flow-extension-math, flow-extension-text
- flow-extension-text: flow-extension-bim, flow-extension-brep, flow-extension-dictionary, flow-extension-draw, flow-extension-list, flow-extension-logic, flow-extension-math, flow-extension-primitive
- process-extension-concrete: process-extension-metal, process-extension-robotic, process-extension-wood
- process-extension-metal: process-extension-concrete, process-extension-robotic, process-extension-wood
- process-extension-robotic: process-extension-concrete, process-extension-metal, process-extension-wood
- process-extension-wood: process-extension-concrete, process-extension-metal, process-extension-robotic

Reference: [Nx language support](https://nx.dev/docs/kb/add-language-support) assigns target and dependency inference to project graph plugins; [project configuration](https://nx.dev/docs/reference/project-configuration) defines task prerequisites and continuous targets.

## Shared Closure and Configuration Boundary

A pure JavaScript component resolver now supplies Nx preparation and registry session filtering. It closes ordinary runtime dependencies and consumed contributions recursively, validates duplicate/unknown component identities, deduplicates union roots and supports host selection. Five language-neutral positive cases and three invalid cases pass against graphlib traversal. A separate comparison of actual Cargo metadata and graphlib agrees with all 120 inferred playground preparation targets (60 variants, two profiles). The initial red test failed on the absent resolver.

Demonstrator now has an authored runtime catalog and schema for its six pane variants, host and assets. Vite imports the pure runtime description instead of its task router or brand implementation. The independent esbuild probe reports 11 files and no task or brand imports; catalog, pane remapping and module directory assertions pass. The old in-source layout expectations were stale: the current authored deployment directories use emoji vendor/shard names, and the actual metadata closure does not declare Stdio. The new expectations follow those verified source contracts rather than the stale assertions. This configuration extraction does not complete Demonstrator preparation/build/E2E refactoring.

Repository and product Nx tests are pending; their clients currently wait for shared graph construction while other source changes continue. The shared daemon has not been reset.

The isolated daemon-backed Nx dependency fixture passed all eleven actual task/cache scenarios with the extracted helper present. Demonstrator quick tests passed all four tests in two files. Registry quick tests initially failed because its script forwarded the level word `quick` as a Vitest filename filter; the router now resolves the test level before passing remaining arguments and a retry is running. Registry projection tests now also consume the transitive runtime fixture.

The full repository suite passed its command/input/component/activation checks, then failed the editor coverage gate on newly discovered `semio-s-artifact-stdio-contract:build`. The crate exists in the current workspace but had no build/check/test launch entries. Those three canonical Nx entries have been added to the existing seed order; registry regeneration and a final suite retry remain pending. No unrelated crate changes were reverted.

Registry generation passed in 52.1s and emitted the missing native editor entries. The next registry run passed 24 of 25 tests, including the new session closure test; the remaining preview-order assertion lacked the already implemented Report actor-network contract. Its expected order and the seed are now aligned in the existing preview group. Registry tests explicitly depend on their catalog generator and remain uncached while their dynamic whole-catalog/source reads await a complete input audit. A final test run is pending.

## Final Integration Checks

The final `repo:test` passed in 35.9s, including the runtime component fixtures, 120 real preparation targets, 11-file Demonstrator import boundary, source/input contracts, editor coverage and cancellation checks. Registry `test-quick` passed all 25 tests across four files in 23.01s (52.5s Nx run including its two generation prerequisites). These results qualify the pure runtime extraction and registry fixes; the Demonstrator build/dev/E2E pipeline remains to be refactored.

## Next Pipeline Boundary

The planned Demonstrator graph exposes profile preparation as an outer dependency union over the seven authored pane/runtime variants, allowing Nx to deduplicate shared component, engine, support and session producers. Development consumes a completed generator activation receipt only after that union has finished. Production should copy only the declared component/extension/support directories from profile-specific materialized outputs; it must not copy the global legacy module tree or installation timestamps. The application build remains unqualified until its complete Vite source, assets, runtime-output and environment inputs are proven. The E2E runner must consume an Nx-owned continuous server with explicit readiness and lifetime ownership rather than spawn its own dev pipeline or reuse an ambient port.

The installed Nx task environment supplies NX_INVOCATION_ROOT_PID to related tasks. This is useful for tracing ownership but does not alone prove a process lifetime when a PID is reused. Readiness and cleanup need a concrete runtime proof before adopting it as a resource identity. Nx’s [run-commands executor reference](https://nx.dev/docs/reference/nx/executors) and installed 23.2.0 executor/task-orchestrator source are being used to resolve continuous task behavior. No new Demonstrator target graph or server implementation has been applied yet.

## Authored Preparation Targets

`prepare-dev` and `prepare-release` now declare seven OS development preparation prerequisites in the outer Nx graph: aggregator, aussuchen, bearbeiten, generation3d, generator, koordinator and verfolgen. Their thin command reads completed component ownership markers and generated sessions; it never invokes compilers or another repository task. Both aggregate validations are uncached and own no outputs. Their public launch selection sets the selected native build mode before Nx schedules prerequisite engines, and rejects extra compiler arguments. Two editor launch-seed entries expose these preparations. Existing dev/build/E2E consumers have not yet been connected and still contain their old hidden pipeline.

The language-neutral pipeline fixture initially failed with missing dev preparation and now passes. Profile invocation vectors also pass; the attempted red invocation process was still running when the implementation was added, so it observed the updated code and cannot be reported as a reproduced red result. The final root retry passed all ten graph coalescing scenarios, then failed on the intentionally not-yet-authored Demonstrator preparation target; it must be rerun after this implementation. Actual `prepare-dev` runtime validation and launch regeneration are running.

## Browser Distribution Foundation

The existing Preview2 import rewrite was extracted into a dependency-free module, preserving its current generated-import behavior and public exports. Four language-neutral relocation vectors match independent es-module-lexer import spans, and esbuild confirms a one-file production import boundary. A new browser artifact copier reads exact ownership manifests, copies only their declared regular files into caller-owned staging, rewrites extension shim imports, rejects traversal/absolute/marker paths and collisions, reports per-file progress, and respects cancellation. Its initial regression passes three copied files, ambient installation metadata exclusion, corrupt owner/path rejection, and pre-cancel behavior. This is a staging primitive; it does not yet provide cross-process source leases or final publication.

The Demonstrator’s immutable asset plan selects the runtime closure plus Preview2, shard and font outputs, with extension routes separated from plugin routes. It has not yet been wired into Vite. The plan check initially could not run because concurrent schema consolidation moved the runtime schema to `🧬️schema/🔣️.json` and merged the pipeline contract into its `$defs`. The test now reads that current draft-07 schema and validates each definition with the matching Ajv implementation. It does not recreate the old schema files.

The combined repository/registry test invocation was cancelled by Nx after registry generation failed to import the concurrently missing actor `📃️pageSchema/🟦️.ts` module. The repository task had started its coalescing checks and was interrupted; this does not constitute a completed root test. The earlier explicit launch generation did pass and both preparation commands are present in `.vscode/launch.json`. The native Demonstrator preparation remains running.

## Vite Publication and Preparation Interruption

The browser distribution regression now also rejects duplicate manifest entries and symlink roots, observes mid-copy cancellation after one file, and compares regular-file collection against fast-glob. A real installed Vite production build copies exactly the fixture ownership manifest and rejects a mismatched owner. The new Vite publication helper invokes only the selected bundler, collects its regular files and publishes through the existing artifact-owner transaction. Its real CLI fixture verifies the complete marker inventory, preservation of an earlier successful output after a failing config, pre-cancellation and staging cleanup. Both missing-module red tests and their passing implementation runs were observed. References: [Vite plugin hooks](https://vite.dev/guide/api-plugin.html) and [configuration loader](https://vite.dev/config/); validation uses the installed version rather than assuming the current documentation version is installed.

Demonstrator `build` now depends on `prepare-release`, invokes a dedicated site command, and owns `dist/site`. The production config copies only the 24 immutable runtime sources and excludes the development installation store. The former root build implementation was removed. The profile resolver establishes ship/react before prerequisites. The pipeline fixture reproduced the missing dependency and now passes. The target remains explicitly uncached pending real product bundling and complete restoration/input proof. Development and E2E still need their separate lifecycle refactor.

The first real Demonstrator dev preparation ended unsuccessfully after 34m14s. Flow and Surface engine compilation completed, but later Cargo invocations failed because the workspace generation2d member manifest was temporarily absent during concurrent edits. That exact manifest exists again at the next inspection; a fresh offline metadata check is being used before retrying. No manifest source was recreated or rolled back. The Print collection proof remains in its population phase.

## Development Serving and Continuous Task Ownership

The generic Vite service adapter has passed a real loopback HTTP fixture, occupied-port rejection, pre-cancellation, and binding the same port again after cancellation. The language-neutral fixture is owned by the caching/Vite schema. Demonstrator `activate-dev` now validates the exact component union after both the seven preparation targets and generator activation finish. `dev` and `serve` are continuous, uncached Nx leaves depending on that completed activation. Their command only validates the receipt and hosts Vite; plugin/engine build loops were removed from the former root DevScript. The root public `dev` invocation attaches Nx watch to `activate-dev`; `serve` performs no watch scheduling. Vite now uses profile-specific module outputs and the generator's scoped development extensions/receipt, and serves only its declared component routes. The actual production config still loads after this change. New activate/serve editor seed entries are being generated.

Offline Cargo metadata passed after a second transiently missing member (generation3d) reappeared. The real release site build has now been started through Nx with two task workers and CARGO_BUILD_JOBS=2. The target remains uncached and is not yet qualified.

E2E is actively under refactor: its former root helper still refers to the removed raw DevScript and must be replaced before this work is complete. Installed Nx 23.2.0 starts continuous dependents without a readiness barrier and shares continuous tasks across invocations by task ID through RunningTasksService, independently of task input hashes. This rules out assuming either `continuous: true` alone supplies readiness or an invocation PID alone provides an isolated server generation. The next lifecycle contract must explicitly prove readiness/ownership and test concurrent invocations and cancellation. The existing Vite fixture qualifies only the adapter itself, not a complete Nx E2E lifecycle.

## Native Nx Continuous Isolation Reproduction

The private native Nx fixture reproduced actual cross-invocation sharing: first started one server, second logged `Waiting for fixture:serve in another nx process`, and second never received its own readiness identity. The first server then ended when its owning consumer timed out. A candidate patch allows `metadata.semio.continuousSharing: false` to bypass only RunningTasksService registration/reuse for that continuous target. In the native fixture, two servers were created and closing the first invocation preserved the second; both ports were released after completion. A default-sharing scenario still used one server and completed. The first cancellation assertion expected signal exit 143, but installed Nx returned 130 for the interrupted task invocation; the fixture now records that observed Nx outcome and a retry checks port release and the other invocation's survival. The candidate has not yet been installed or appended to the permanent Bun patch.

The E2E readiness design uses an explicit uncached preparation target to create a UUID within its Nx invocation PID directory. The continuous server and consumer share that prepared identity, validate it over HTTP, use an ephemeral port, and conditionally remove only their own generation. This avoids introducing a random graph-affecting environment variable. PID alone is not treated as a generation. The service target requires the tested continuous-sharing opt-out. This design is not implemented yet.

Demonstrator quick tests passed four tests in two files through Nx (24.7s run; 16.13s Vitest). Registry/editor regeneration also passed (2m44s elapsed, two executed tasks). The new activate/serve commands are available in the generated editor configuration.

## Continuous Isolation Installed

All three native candidate scenarios now pass: two independently completed service owners, cancellation of the first while the second remains usable, and unchanged default sharing with one owner. Both completed/cancelled scenarios rebind the released service ports. The fixture records the installed Nx CLI's observed interruption exit code 130; the earlier 143 assertion was a mistaken harness expectation, not a service isolation failure.

The regression is now permanent in the caching service tests and invoked by `repo:test`. The authoritative Bun patch adds a fifth module, `dist/src/tasks-runner/task-orchestrator.js`, with a metadata-controlled sharing opt-out. A completely separate Bun installation followed by frozen-lockfile installation passed all previous diagnostic/environment/log-retention/coalescing checks and all three native service scenarios. The installed workspace module was byte-compared against the captured original and replaced atomically with the byte-identical fresh-Bun-verified candidate. No shared dependency install ran, no daemon was reset, and already-loaded task runners remain untouched. The installed-module retry and complete root suite are now running.

## Readiness Contract Foundation

A schema-owned service-session helper now has a language-neutral fixture covering fresh UUID generations at the same invocation PID, owner/pid/URL validation, rejection of stale-generation publication, real loopback HTTP identity matching, cancellation, and selective cleanup preserving unrelated/newer files. The missing-module red was observed and the initial HTTP fixture passed. Vite middleware integration is the next red test. These helpers do not provide a general cross-process compare-and-swap lock; their current generation checks rely on the explicit Nx preparation ordering and isolated invocation lifecycle. General resource leases and stale-state reclamation remain outstanding.

The installed continuous module passed its three native regressions again. The complete root suite then passed those checks and the component/Demonstrator runtime contracts, but encountered another concurrent schema consolidation: browser import/distribution schemas moved into `os/plugin/🌐️browser-bundle/🧬️schema/🔣️.json`. Their production copier was already updated by that work. The two test consumers now reference the canonical draft-07 definitions (`Preview2ShimImportRewriteV1`, `BrowserArtifactDistributionV1`); no removed schema was recreated. Targeted retries are running. An earlier root invocation omitted SEMIO_TICKET_DIR and failed its ticket guard before tests; the subsequent invocation supplied the ticket correctly.

## E2E Graph Connected

Demonstrator now exposes `prepare-e2e`, `serve-e2e`, and `test-e2e`. Preparation runs after completed dev activation and browser synchronization, then creates a UUID for the Nx invocation. The isolated continuous server reads that generation, binds an ephemeral loopback port, exposes the generation through Vite's readiness middleware, and cleans its session records after shutdown. The test leaf waits for that HTTP identity and invokes Playwright against the returned URL; it never starts a server or compiler. The old root raw-server helper is removed, the Playwright config requires the supplied URL, and editor/package entry points select `test-e2e`. The pipeline/profile fixture reproduced the missing preparation dependency and now passes. Editor regeneration and integrated root/registry checks are running. Real product E2E behavior remains unqualified until its native dev prerequisites finish and the full Playwright consumer executes.

Browser synchronization no longer depends on `deps-javascript`: an E2E browser prerequisite must not run a Bun dependency replacement while other Nx consumers are using those modules. JavaScript environment synchronization remains an explicit setup operation. This narrow graph correction does not claim clean-machine bootstrap qualification or solve general dependency-install serialization. Full native environment qualification remains outstanding.

The readiness Vite red test failed on the missing identity endpoint and then passed with the middleware implemented. Actual Vite HTTP replies match the schema-owned generation and carry `cache-control: no-store`; wrong-generation responses are rejected, cancellation works, and cleanup preserves unrelated/newer records. The helper is now included in the permanent root suite.

## Concurrent Vite Listener Qualification — 2026-09-08

The first native Nx + real Vite two-invocation test failed: Vite mapped requested port `0` to its default `5173`, so the second independently owned server could not bind. Earlier single-server checks did not qualify ephemeral allocation. The combined repo/registry run independently hit this same collision; its registry task also exceeded its existing 15-second process budget without producing test results.

`serveVite` now owns a Node HTTP listener and runs Vite in middleware mode. The operating system allocates port zero atomically. Vite HMR and WebSocket proxies use that same listener. Cancellation closes Vite and destroys remaining owned sockets, including active HMR connections. Explicit occupied ports still reject. This follows the installed Vite API and its inspected listener/client source; the [official middleware API](https://vite.dev/guide/ssr) describes externally owned HTTP integration. The [current server documentation](https://vite.dev/config/server-options) has since moved WebSocket options to `server.ws`; this repository’s installed Vite still implements `server.hmr.server`, which the native tests exercise.

Observed passing checks: native Nx `vite-cancelled` starts two servers, cancels one invocation (Nx exit 130), confirms the other identity remains available, then rebinds both released ports. In parallel, the browser-distribution and readiness suites passed with real Vite. The browser fixture connects using the token from `/@vite/client`, receives Vite’s connected frame and a full-reload frame after an HTML edit, and confirms cancellation closes the still-connected WebSocket. Its fixed-port collision and rebinding checks pass. Logs: `nx-vite-cancellation-retry.log`, `vite-middleware-browser.log`, `vite-middleware-readiness.log` under ticket generated output.

This qualifies the generic listener and fixture graph on this macOS host. It does not yet qualify the full Demonstrator browser/E2E workload, every host platform, session compare-and-swap, or generalized artifact leases. Full Print cache population and the Demonstrator release prerequisite build remain running.

## Current Rust Retry and Lease Integration

The second release attempt progressed past the prior missing dependencies but failed on three `protocol::value::ordered` imports in the newly independent Flow artifact crate. Its root incorrectly aliased `semio_framework_os_kernel` as `protocol`; the existing `semio-framework-replication` dependency owns the required ordered-value module. That alias now points to replication. A focused Nx Flow WASM release prerequisite is running before another full Demonstrator attempt.

Session mutations now use the native resource lease primitive; both the per-invocation native Nx + Vite cancellation test and the delayed-HTTP-response generation test passed. The latter initially accepted a stale server after session replacement; the response is now checked against the current session again before exposing its URL. See `📓️resource-leases.md` for native oracle evidence and remaining integration boundaries.
