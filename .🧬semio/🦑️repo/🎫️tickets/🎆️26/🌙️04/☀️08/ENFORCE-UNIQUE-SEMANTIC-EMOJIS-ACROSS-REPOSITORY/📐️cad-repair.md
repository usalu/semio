# CAD Semantic Review

The pass handpicked the 📐️mutate-cad-1 suite and 16 scenario directories from the actual operations: visibility 🙈, unlocking 🔓, translation 📍, plane width ↔, drawing creation 📐, node appending 🌱, reference media 🖼, list replacement 🔄, model slots 🧱/🏢/🏛/⚡, and vacated slots 🕳/🏚/🔌.

Four additional carrier identities were repaired: ⚙️machine, 🌲️concrete-forest-reference, 🪨️BasePlate, and 🌉️aec.building.structure.classic. The classic structure identity is distinct from its sibling 🏛️aec.building.structure. The thermal hull's 🐚 and cylinder's 🥫 were retained because their shapes directly express the modeled concepts, unlike the removed dolphins, octopus, fruit, and arbitrary color symbols.

Explicit moves and exact references only; no Git modifications or automatic emoji selection. All 20 mutation descriptors agree with their directory identities, and all 20 catalog scenario paths resolve. The catalog's four unchanged scenarios still carried generic old directory names; these now match their reviewed physical names.

Production feature parsing resolves all 100 fixture URIs. The existing independent Python adapter ran 41 scenarios: 41 passed. The Rust suite's old shared-law include was corrected to the verified current Stdio location. TypeScript demo/runtime smoke checks are still being evaluated; no native compilation pass is claimed.

## Final Scope and Demo Verification

The final bounded audit covers 768 files, 534 directories, and 1,264 governed entries with zero findings. All 414 JSON files parse, 228 embedded Rust file references and 271 path mounts resolve, and 20 descriptors match their owners. All 20 scenario catalog paths resolve. Context-aware semantic lookup checks 531 directories, including 21 direct test children, without missing registrations.

The first general JavaScript checks exhausted their 15-second and 30-second runner budgets before producing test results; those were not passing runs. The CAD test configuration now includes the two existing demo files alongside its unchanged in-source domain test selection. The obsolete `🧪️artifact.ts` imports use Vitest, matching the existing runner. The selected demo paths executed through `bun nx run @semio-tech/cad-js:test-long`: two files, two tests, all passed, each reading its committed asset. This is demo evidence, not a claim that all CAD domain suites or native builds passed.

## Pinned CAD Projection Fixture Recovery

The shared `📐️cad-draw-path-projection/🔣️.json` had internally inconsistent CAD mapping data: its stored 209 source/destination rows hashed to `46c5d2cd397e2cc5296fd6881dd861321376bd3660f5774be068d310988ac214`, while the pinned digest was `a09f60c5de5718394ddb856052444b306de7443b2d4ecd546e1e911dc44d40a6`. Independent destination-layout authority reconstructed the original source rows with zero problems and reproduced the pinned digest exactly. Only the 155 contaminated source-coordinate strings were restored. Destination coordinates, pinned digest, fixture payloads, and all Draw rows were preserved.

Frozen normalization input names intentionally differ from current live asset names. A test-only, closed semantic-identity binding now reads each of the 209 scenario payloads from exactly one current file, never probes a historical path, and supplies no production path fallback or alias. The complete current census contains 229 files: the additional 20 building placement actions/interactions are explicitly listed and validated as current-only members rather than silently omitted or added to the frozen mapping. Missing, duplicate, and undeclared identities are rejected; all 209 bound contents are compared with current file bytes. Two focused production Bun tests pass through the existing Nx target, covering the pinned source authority and this complete binding census.

The earlier diagnostic's 196 source-layout rejections concerned feeding already hand-renamed files directly into the frozen canonical-source normalization grammar. No production projection grammar was changed or claimed repaired. The restored frozen source scenario now passes its original authority assertions.

## Existing Draw Contract Boundary

The combined CAD/Draw test completes the CAD checks and then fails because the authored Draw scenario no longer owns the frozen `📦️glue.rs` coordinate. Unlike CAD, the Draw golden's 11 stored rows already reproduce its pinned `1f28fcc6e28e54001a9df6ce98b1c30b565cd42b824ed2491bb9b5e407b7436b`. Read-only `git show HEAD:<path>` evidence shows both the current `sourceArtifactMemberName: "🖍️drawing"` contract and authored scenario's canonical `🦀️.rs` members unchanged from HEAD; the frozen golden still uses `🖍️draw`, `📦️glue.rs`, and `🦀️component.rs`. Independent current authority refuses that owner mismatch. No proof supports changing its pinned rows, so no Draw payload, contract, or digest was altered. This is recorded separately from the passing CAD-only checks.
