# Raw Binary Format Hand Review

Each choice below follows the actual owner content. Mutation identities and sample bytes are not renamed or rewritten inside domain data.

- `🔮️oracle` distinguishes independent reference work from `🧪️tests`.
- `☑️options` distinguishes selectable window options from `🎚️config` in the editor and viewer.
- The before/after specimens become `⬅️before.json` and `➡️after.json`, grouped under `➕️append-bytes`, `✂️splice`, and `🔪️truncate-at`.
- Mutation directories use `📸️set-snapshot`, `✂️replace-byte-range`, `➕️append-bytes`, and `🔪️truncate-at`.
- The general mutation suite is `🔀️mutate-binary-raw`; the middle-byte replacement case is `🪡️rewrites-the-two-middle-bytes` rather than an unrelated kite.
- The snapshot payload schema is `🧬️.schema.json`, distinct from its JSON descriptor sibling.

The pre-edit source-byte hashes are retained in `🗑️generated/binary-before.json`. Moves, current source references, discovery authority and oracle specimen coordinates must be verified before this scope is described as complete. Frozen source-mutation evidence stays unchanged; only current owner coordinates may change.

Twenty-one literal moves now cover those choices and the two existing floor-plan specimen directories, handpicked as `🏘️abbau-aufbau-masterarbeit-grundriss`. The actual shared JPEG is unchanged; its precise locator was updated in the Rust test and seven feature scenarios. The empty same-named artifact-root directory was preserved, not deleted.

The intermediate physical census covers 263 entries and all 166 original files, with zero naming findings. At that point 158 complete hashes were unchanged and eight files differed only in the explicitly patched source/schema/fixture coordinates or comments; the later shared-fixture locator adds one more intentional source change. Current schema ownership and fixture references were repaired without changing mutation IDs, expected data, assertions or frozen source-mutation evidence.

The normal repository fixture verifier now discovers all three binary fixtures at the handpicked locations and reports zero file problems. The first invocation used a nonexistent target name and never ran; the corrected existing `test-fixture-verify` target passed. Final scoped naming, neutral regression and native compilation checks are still being collected.

Final physical verification retains 263 entries and all 166 files: 157 complete file hashes are unchanged; nine files contain the enumerated current-coordinate/comment changes. There are zero naming findings and zero unresolved semantic roles. The existing standard role now recognizes raw-binary format standards below their actual flattened serializer/deserializer owner, as it already did for other artifact-kind members. The taxonomy validates without errors.

All six before/after file digests and sizes match their unchanged manifest records; the snapshot payload schema compiles with Ajv, and the current mutation/scenario coordinates resolve while the frozen `sourceMutationDirectoryName` remains unchanged. The focused language-neutral suite, including the two newly added Binary/Stdio examples, passes all 27 tests and 731 assertions through explicitly selected Nx execution. The initial direct Bun invocation omitted `./` and discovered no tests; it is not counted.

Native Stdio benchmark compilation is waiting on an existing shared build lock. No native binary-format or benchmark success is claimed. The unchanged binary mutation suite also contains a pre-existing `splice`/`replace_byte_range` API inconsistency; that requires separate attribution if compilation reaches it. No assertion or oracle requirement was relaxed.
