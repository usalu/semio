# Workspace Repair

## Current Manual Batches

The bounded TXT, TSV, STL and PLY families now have separate hand-review reports with byte/reference evidence. TXT has 89 manually reviewed owners and no remaining old carrier path. TSV has 24 initial fixture/test moves plus its production snapshot identity; its six absent payload schemas and casing discrepancy remain open. STL has 31 physical owner moves across the three active lanes and 24 main-tree internal moves, with zero current strict or semantic-role findings in the main tree. PLY has 13 manually reviewed physical owners and 30 main-tree findings repaired, with 209 files accounted for and no remaining old carrier path. This is progress evidence, not a repository-wide completion statement: the last read-only global census still contained thousands of findings in untouched trees and is intentionally not represented as current-zero.

The latest TXT native qualification reached compilation with no missing renamed path, then failed before tests on unrelated current Serde/`decode_op` trait errors (`txt-family-native-tests.txt`). No native TXT pass is claimed. The STL and PLY main fixture verifiers and language-neutral naming suites are green; a full Stdio native run is not claimed because the same broader library compilation blockers remain.

The earlier completion report was incorrect. The bulk emoji migration changed runtime values as well as paths. Confirmed examples include Linux platform comparisons, Rust target-OS configuration, and generic test-directory vocabulary. Passing the narrow emoji audit did not establish workspace integrity.

Repair is in progress. No modifying Git command is authorized or used. Historical Git objects and this task's execution log are read-only evidence; current file contents must remain authoritative. Other agents' implementation changes and their repair notes must be preserved.

The completed recovery batches reconstructed unsafe rename passes from exact recorded findings, execution-log samples, and unique current/historical path identities. Their original current-byte snapshots were later deleted by a separate cleanup task; see the incident record below. Retained reports and the recovery input folders outside generated output remain available. Both historical mutation utilities are retired: only individual handpicked moves and precise edits are allowed. No ambiguous destination may overwrite another file, and no whole historical file may replace current contents.

Verification and any unresolved issues will be recorded here before completion is claimed.

## Rechecked Reported Examples

The exact examples supplied by the developer were rechecked on disk after the scoped repairs. Flow's generation command siblings are `🏷️rename-generation`, `🗑️remove-generation`, `🎯️select-generation`, `➕️add-generation`, and `🎚️update-generation-values`. Kernel return content now has one `🧬️schema`, alongside distinct `🧫️fixture`, `📥️input`, `🖼️framing`, `🔌️wire`, `💌️message`, `🗣️dialects` and `📐️fixture-schema` owners. Hub's chunk store is `🌎️hub/🗿️artifact-authority/🧱️chunk-cas/🦀️.rs`; the Rust leaf has a unique format role within that owner. Value's explicitly registered integration test is `🧰️framework/🔨️modules/🌱️value/✨️derive/📦️packages/🦀️rust/🧪️tests/🪗️flatten-with-skip.rs`. These concrete checks do not supersede the remaining global findings or the scoped native limitations in their reports.

Latest scoped verification: async/job/trace have 78 audited entries with zero naming findings and 99 passing native tests. Four additional small framework trees have 25 clean entries after one schema collision repair. Dispatch/Deflate/Hash/ABI/Machine have 54 clean entries after six individually chosen moves; Dispatch's four explicitly named Cargo test targets and library pass 28 tests. Details are in `⏳️async-job-repair.md`, `🎯️small-framework-review.md`, and `🔀️dispatch-deflate-review.md`.

Development publication keeps exact tool-required identities separate from authored emoji names. The completed companion matrix covers all 246 files/82 WASM headers/82 TypeScript resolutions with 3,445 assertions; the exact interface authority covers 82 parents, 33 filenames and 2,027 declarations with 13,330 assertions. Both materialized deployment roots now have zero naming findings across 2,763 entries, while older dist mirrors remain under review. All 26 extension and 56 plugin static import edits preserve their non-import bodies; vendor transport checks cover 345 imports and 12 actual middleware payloads. Details are in `🧷️interface-parent-review.md`, `🧑‍💻️dev-repair.md` and `🪞️extension-import-review.md`.

Architect's 815 individually specified moves are complete across 69 domains and 266 operations, including all schema sibling collisions. Its whole-tree audit covers 6,344 nodes/3,174 files with zero naming findings, but native verification failed: its source-authority validator still assumes immediate-child mutation owners, alongside separate async-test/trait-shape diagnostics. The exact registered domain-owner contract is now being repaired in the native implementation as well; no Architect native pass is claimed. Graph passes 184 native tests and exact preimage checks against all 22 original generated-file hashes; renderer's exhaustive run passes 747 tests. Stdio Binary/WAV/MP3 have completed scoped naming reviews, and the current focused naming suite passes 28 tests/900 assertions. Stdio glTF and other large trees remain under parallel review. See `🏛️architect-repair.md`, `💾️binary-format-review.md`, `🔊️wav-format-review.md`, `🎵️mp3-format-review.md` and the framework scoped reports.

The fresh global snapshot remains non-clean: the full counts and scope caveats are recorded in `📊️remaining-tree-census.md`. It is not superseded by any scoped passing result.

Block is complete as a bounded plugin scope. Its 2D, 3D and 5D trees and publication authority were reviewed owner by owner, with distinct semantic identities selected for every collision and every payload schema moved explicitly beside its descriptor. The final repository-context audit reports zero findings throughout `✏️s/🔌️plugins/🧱️block/`; 160 Block 2D, 224 Block 3D and 219 Block 5D barrel mounts resolve, and all 104 descriptor/schema/oracle triples resolve and parse. The focused shared Nx suite reached 33 passes with two concurrent non-Block failures: Value's then-in-progress keycap oracle implementation and an OS legacy ZWJ output recreated by an active external demonstrator. The keycap oracle now passes its focused two-test/62-assertion run. Details and unchanged aggregate digest evidence are in `🧱️block-repair.md`.

FEM is complete as a bounded plugin scope. Its 2D and 3D subset, command, mutation, fixture, payload-schema, test and generator owners were reviewed individually. The final read-only audit covers 2,242 governed entries with zero findings; all 50 payload descriptors resolve and parse. Both 22-kind carrier manifest targets build and read their exact semantic fixture coordinates, and two Nx geometry smoke runs create only the handpicked directory/file identities. The shared schema now preserves logical subset IDs while resolving exact per-standard physical rosters, preventing future generators and policies from recreating generic `✳️` siblings. Eight verified-empty malformed FEM directories were removed individually without deleting any file. Details and retained digest evidence are in `🏗️fem-repair.md`.

The shared-asset/favicons delivery review now passes 39 fresh Styling tests/1,350 assertions; both asset/Styling canonical freshness checks previously passed. The public asset namespace is explicitly `🖼️assets`, with actual HTTP byte checks and all 42 CSS/25 native cursor coordinates resolved against existing files. The focused native cursor and React logo tests each passed one test. Explicit favicon output names are `🛡️favicon.svg` and `🔖️favicon.ico`, verified through dev/preview HTTP and three parsed HTML sources. Trunk now copies the canonical assets directly, independent of old Vite output. The apparent duplicate `���️renderer` project was reproduced as an upstream Nx split-UTF8 socket-decoding defect, not a physical folder; the repository's existing in-process policy works without deletion. See `🚚️asset-delivery-review.md`, `🔖️favicon-delivery-review.md` and `📡️nx-unicode-transport-review.md`. Surface is complete in scope; Norm's 469 initial findings are under a separate hand review.

TXT's first 25 individually selected moves preserve 193 unchanged active files and five retired competing schemas in ticket recovery. Four active files contain exact reference updates; its scoped 303-entry audit has zero findings, while the arbitrary TXT root and external codec-family names remain open. The five-pair fixture verifier passes, ten canonical schema cases are independently checked, and the shared naming/source-authority suite passes 30 tests/1,013 assertions. Native qualification remains pending. See `🔤️txt-format-review.md`.

The mesh naming/transport repair now preserves all 90 moved GLBs and resolves all 93 public mesh identities through explicit current source authority. The Puzzle example DSL still contains 16 corrupted semantic URL identities repeated 1,408 times. Those values have not been hidden by aliases or bulk-rewritten; this remains an open repair item. Details are in `🧊️metabolism-glb-repair.md`. No whole-workspace completion is claimed.

Framework asset icons now have 249 individually handpicked source paths in 26 domain groups. The final source-plus-generated audit covers 558 entries with zero naming or role findings, all 249 source hashes are unchanged, and the compiled native builder reproduces all IDs and paths. The 286-output asset freshness check passes. Reserved asset README generation now retains the literal README rather than its redundant relocated copy; recovery bytes and frozen historical catalog evidence remain intact. Other asset families remain under review. Details: `🖼️assets-repair.md`.

Latest completed scopes: Actor's 20 handpicked moves have zero naming findings across 140 governed entries; its native route passes 121 tests, and TypeScript matches its prior 198-pass/one-failure baseline. UI root/modules have 12 handpicked moves, 51 clean module entries, 692 passing React tests and 18 passing Admin tests. Nine empty duplicate UI module shells were removed individually without deleting any files. UI elements, styling/assets and the render subtree are still under review. Details: `🎭️actor-repair.md` and `🖱️ui-repair.md`.

The shared reserved-document statute now protects README and LICENSE with any format extension, including `.rst` and `.txt`, rather than only Markdown. A regression failed before this correction and passes afterward through both Bun and TypeScript compilation. Exact fixed-name contracts still require literal basenames; their grammar was not weakened to allow arbitrary wildcard exemptions.

## Clarified Naming Contract

The developer explicitly requires case-by-case, handpicked names with exactly one emoji per non-reserved file or directory. A unique multi-emoji sequence is not compliant. Reserved names, including README and LICENSE, must not be emoji-prefixed. No hash, palette rotation, heuristic, or automatic semantic selector may choose names.

The original ticket script's mutation commands have been disabled. Its historical implementation is retained only as incident evidence.

## Recovery Verification In Progress

- A language-neutral repair fixture passed four exact-output cases using both a trie implementation and a separate regular-expression implementation, with Ajv checking expected outputs.
- The stricter naming fixture was run before implementation and failed as expected because the old checker accepted stacked emojis. This demonstrates the prior audit's gap.
- The recovery applied 4,085 path renames and 22,678 exact reversal substitutions in 3,446 text files. It used historical names as evidence, never historical file contents as replacements. Ambiguous names and non-UTF-8 files were excluded from text rewriting. Remaining single-emoji names still require semantic review.
- A separate same-file historical-literal comparison repaired 4,768 injected runtime/data tokens in 1,479 files, including platform names and domain fields. Relative paths and ambiguous literals were excluded from that pass.
- Three explicit handpicked batches applied 5, 44, and 71 renames. The last batch updated resolved references in 31 files. The recorded decisions are in `🩺️repair/🖐️names.json`; there is no automatic emoji selector in this repair workflow.
- Direct enumeration of all 48 reviewed sibling groups (228 entries) found zero emoji findings and zero unregistered semantic directory names. This is scoped verification, not a claim about the whole workspace.
- Both remaining emoji-prefixed README files in the audit were renamed to `README.md` without overwriting existing files. Global fixed-name contracts now protect README, README.md, LICENSE, and LICENSE.md; the normalizer no longer relocates those reserved names into emoji documentation leaves.
- Added red/green regression coverage for an extra emoji after the semantic stem as well as stacked prefixes. The language-neutral fixture is exercised by TypeScript and Go. The fresh Go Nx run passed (0.773 seconds), and `gofmt -l` reported no formatting changes.
- Repaired three exact filename values that had been changed into relative paths: the submitted-proof fixture, its schema, and the testing README coordinate fixture.
- A runtime Cargo check exposed four stale `✍️🔤️set-text` descriptor owners; these were repaired to their actual current source owners. The descriptor census also identified 35 remodel descriptors using the wrong plugin directory; their exact owner fields were corrected. No compiler validation was weakened.

## Remaining Work

The repair is not complete. The last full strict audit, before the handpicked batches, still reported 3,976 duplicate siblings, 439 multi-emoji names, 183 missing emojis, 53 generic emojis, 219 presentation mismatches, and two prefixed reserved names. Several of those categories have since been reduced, but a new whole-workspace census is required.

The broad repository-library test run had 444 passes and 93 failures before the source-reader configuration fix. These require individual attribution: at least one malformed fixture coordinate already existed in the read-only pre-task baseline, while other failures exposed damage repaired here. Neither the suite nor the workspace may be described as clean. The repaired Flow Cargo check reached seven API/type errors after its path-related failures were repaired. Forty-one compiler-default Cargo source directories were restored before the mutation utilities were retired; current bytes were preserved.

## Parallel Hand Review

All four available agent slots are working on separate scopes. Hub completed 32 precise moves and its TypeScript/admin tests pass; native Rust verification is separate. Kernel completed 36 moves and 49 quick tests pass; its renamed oracle discovery is being verified. Value completed 17 moves, all 68 governed entries pass emoji checks, and four focused Nx routes pass; two native-test blockers are documented without suppressing assertions. Details are retained in `🧭️hub-repair.md`, `🎠️kernel-repair.md`, and `🌱️value-repair.md`.

The next tree reviews are architect, replication, and framework manifest; Flow and shared statutes remain with the parent agent. Architect has a flat verb-by-domain mutation group with hundreds of siblings: no arbitrary palette or extra emoji stack will be used to conceal that unresolved taxonomy problem.

The normalizer's old stacked-name preservation was corrected, and existing handpicked file basenames now remain intact while invalid emoji identities produce violations. The neutral focused Nx regression run passes eight tests, including independent Bun/TypeScript compilation and Ajv/emoji-regex checks. This does not establish whole-workspace completion.

## Current Strict Census and Completed Scopes

The read-only census captured 74,194 files, 41,701 directories, and 88,200 governed entries. After removing blanket exemptions for authored fixtures, assets, examples, public files, and generated material, it reported 5,167 missing emojis, 5,919 duplicate siblings, 419 multiple-emoji names, 43 generic emojis, 219 presentation mismatches, zero prefixed reserved names, and two Unicode-oracle mismatches. This snapshot predates the newest parallel manual repairs and is not a completion measure. Increased findings reflect previously hidden scopes, not permission to rename external/tool-owned names indiscriminately.

Flow's direct final audit covers 813 entries (777 governed) with zero naming findings. Its source-fixture Nx test passes. Before/after graph previews preserve all 23 generated nodes byte-for-byte, and the graph freshness check passes. The Rust watcher now uses the same semantic manifest suffix as TypeScript discovery. Details: `🌊️flow-repair.md`.

The five handpicked framework module-root moves and five mesh-fixture moves are recorded in `🧰️framework-siblings.md`. All incoming Cargo paths and TypeScript imports were updated exactly. The framework TypeScript quick suite passes 88 tests; dedicated native core-module verification passes all 66 tests across four binaries through a registered Nx/launch route.

Shared naming tests now pass 11 cases, covering handpicked projected scenario names, exact owner-specific oracle discovery, graph-manifest discovery, single-emoji preservation, and reserved documentation. Next's literal `page.tsx`, `layout.tsx`, and `route.ts` identities are tested against its installed matcher, while its entire `app` subtree is no longer blanket-exempt. URL route-directory contracts still need exact review before any route-name change.

Kernel and framework-manifest repair checks pass, replication's TypeScript test passes, renderer's React long suite passes 537 tests, and the OS TypeScript suite passes 237 tests. Native checks and canonical generator projection-contract drift remain separately documented by their owners. No old full-suite failure count is being represented as a current passing suite.
# Concurrent Diagnostic Cleanup Incident

On 2026-09-04, the separate **Clean workspace artifacts** task (`01a06c98-e3a2-7d02-9309-b29e0dc7cb1e`) completed a root clean command that reported removing this ticket's `🗑️generated` directory (49,171,518,812 bytes) and the workspace `target`. Its output simultaneously listed this ticket as protected. The active repair agents did not initiate that cleanup. Three native test runs lost their logs/caches while still running; no passing result is inferred for unfinished or unread output.

Completed verification counts and byte-preservation results recorded in the retained Markdown reports remain historical observations. Raw diagnostic files previously cited there may now be absent. The initial current-byte snapshot and retired UI-assets copy under generated output are also absent. The canonical active asset tree remains in place; the original retired UI source pair had previously been verified equal to the canonical source outside import declarations, but their exact retired byte copies can no longer be read from that deleted output directory. The retained input recovery folders `🗃️prior-icon-svgs`, `🌿️prior-metabolism-svgs`, and `📦️wire-recovery` remain outside generated output.

A protection-only coordination notice was sent to that task. No additional cleanup, Git restoration or replacement of current files was requested. New diagnostics will be recreated only for continuing checks; reports and remaining recovery inputs stay outside disposable output.

## Latest Coordinated Format Repairs

All 89 TXT carrier owners are now `🔤️txt`, selected after reviewing their actual text-carrier role and each sibling namespace. The final 371-file audit proves 360 files entirely unchanged and eleven files changed only in their exact root identity, with no missing files, old paths, unexpected content or selected-icon collisions. All explicit mounts and current source-authority references were repaired by hand. The uncached five-pair fixture verifier and 32-test neutral naming/source-authority suite pass. Native qualification is still separate and pending. See `🔤️txt-format-review.md`, `🔤️txt-seven-carriers.md`, and `🆎️txt-carriers.md`.

TSV's first 23 fixture/oracle/options/suite moves preserve all 173 files and pass six fixture-pair checks. Its arbitrary snapshot icon and six absent payload schemas are unresolved; a shared test timeout is also retained rather than hidden. See `📑️tsv-format-review.md`. These bounded results do not establish whole-workspace completion.

STL's triangle identity is now `🔺️stl` across 31 physical owners. Its 230-file audit retains 217 full hashes and limits 13 changes to exact identity/reference fields; all 56 explicit mounts resolve, five canonical fixture pairs agree, and the shared suite passes 32 tests/1,120 assertions. PLY's element-block identity is now `🧱️ply` across 13 physical owners. Its 209-file audit retains 194 full hashes and limits 15 changes to exact identity/reference fields; all 37 explicit mounts resolve, eight canonical fixture pairs agree, and the shared suite passes 32 tests/1,145 assertions. BMP's Windows Bitmap identity is now `🪟️bmp` across seven physical owners, with a 382-entry scoped audit at zero findings, 35 resolved Rust mounts and ten generator-reproduced fixture files byte-identical to the committed set. These results do not claim a native Stdio codec pass. See `🔺️stl-format-review.md`, `🧱️ply-format-review.md` and `🪟️bmp-format-review.md`.

Block 2D now has zero findings in a fresh repository-context audit. Seven sibling-directory collisions were resolved with role-specific identities, and 26 descriptor/schema sibling collisions were repaired by retaining `🔣️.json` for the descriptor and using `🧬️.schema.json` for the schema. The directory moves preserved representative hashes; the 26 schema moves preserve their full ordered content-hash aggregate. All 160 Block 2D Rust mounts resolve, all 26 descriptor schema references resolve and parse, all 26 oracle entries agree, and the old coordinates have zero remaining live references. Block 3D and 5D remain separate open scopes. See `🧱️block-repair.md`.
