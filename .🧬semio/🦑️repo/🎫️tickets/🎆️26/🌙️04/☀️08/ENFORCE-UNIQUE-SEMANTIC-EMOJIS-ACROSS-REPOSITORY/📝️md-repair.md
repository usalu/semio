# Markdown Artifact Emoji Repair

## Scope

This review covers only `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/📝️md` and the one exact Markdown mount in the Stdio Rust oracle barrel. It preserves all payload bytes and semantic mutation IDs. No normalization writer, generated rename plan, Git mutation, or compatibility alias was used.

The pre-repair inventory contained 173 files and 98 descendant directories. The scoped statute audit reported 17 findings: two sibling option/config collisions, one test/oracle collision, four missing fixture-owner identities, eight missing fixture-leaf identities, one generic fixture-document identity, and one incomplete variation-selector identity.

## Handpicked decisions

- `🎚️options → ☑️options` in the editor and viewer windows: the directory contains selected window options, while its sibling `🎚️config` remains configuration.
- `🧪️oracle → 🔮️oracle`: the independent Comrak comparison authority must differ from sibling test cases.
- `insert-block → ➕️insert-block`, `remove-block → ➖️remove-block`, `replace-block → 🔁️replace-block`, and `set-inlines → ✏️set-inlines`: each fixture owner now mirrors the meaning of its corresponding production mutation.
- Each fixture pair uses `⬅️before.json` and `➡️after.json`, which distinguishes input and expected output without changing fixture data.
- `📄️readme.md → 📖️readme.md`: this lowercase file is a pinned 47,607-byte CommonMark input fixture, not the reserved uppercase `README.md` convention. The book identity describes the document being read and is unique among its fixture siblings.
- `✏set-inlines → ✏️set-inlines`: the semantic pencil identity is retained and completed as one emoji grapheme.
- `🟤️set-snapshot → 📸️set-snapshot`: the camera describes capturing/replacing the complete document snapshot; the brown-circle placeholder did not.

The exact fixture catalog paths, mutation descriptors, Rust `#[path]`/`include_str!` coordinates, feature text, fixture input URI, and Stdio oracle barrel mount were updated with those literal coordinates. The two changed mutation descriptors retain `set-snapshot` and `set-inlines` as their wire-level semantic kinds.

## Verification

- All JSON files in the subtree parse with `jq`.
- The exact stale-coordinate scan for the repaired paths returns zero matches.
- Every selected destination was checked absent before its `mv -n` operation.
- The post-repair read-only ticket audit reports 173 files, 103 directories including the scope ancestors, and zero `missing`, `generic`, `presentation`, `spacing`, `duplicate`, `multiple`, `reserved-emoji`, or independent-oracle findings.
- After the root lane registered the exact oracle-directory override, `bun nx run @semio-tech/repo-test-domain:test-fixture-verify -- --artifact s.stdio.md` passed with 4 fixtures and 0 file problems.

## Remaining boundary

The central mapping is the exact owner `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/📝️md/🏅️standards/🔖️commonmark/🪆️subsets/✳️any` to contribution directory `🔮️oracle`; the root lane registered it. No taxonomy or parser source was changed in this lane.
