# Plain Text Format Hand Review

The physical TXT artifact baseline contains 308 entries and 202 files, with 24 strict findings. Every original file hash is retained in `🗑️generated/txt-before.json`. The arbitrary `🦅️txt` artifact identity also requires coordinated review of its exact external codec owners before a root move; that root has not yet been renamed.

The first bounded handpicked batch covers the subset's actual content: two `☑️options` window facets distinct from `🎚️config`; `🔮️oracle` distinct from `🧪️tests`; five literal fixture groups matching their line operation (`📥️insert-line`, `✏️set-line`, `🗑️remove-line`, `🔚️set-line-ending`, `↩️set-trailing-newline`), each with `⬅️before.json` and `➡️after.json`; the spoken interview transcript as `🎙️interview-transkript.tex`; and the text-editing suite as `📝️mutate-txt-utf-8`. Payload values, transcript bytes and mutation wire identifiers must remain unchanged.

Five colliding flat `🔣️.schema.json` files are competing, non-canonical schemas rather than byte-identical copies. The actual descriptor-linked payload authority is `🧬️schema/🔣️.json` in every one of the five leaves, matching `mutationPayloadSchemaAuthority` in the taxonomy. The nested contracts include uint32 bounds and scalar-text constraints missing from the flat files; the flat line-ending enum even uses `Lf`/`CrLf` instead of the actual `lf`/`crLf` contract. No explicit TXT `.schema.json` reference was found across source and task consumers. Those five obsolete schemas will be retained byte-for-byte in the ticket's `📝️txt-schema-recovery`, not deleted, renamed into another competing source contract, or used to relax validation. The five canonical schemas and descriptors remain unchanged.

The oracle's two existing transcript includes used five parents even though the fixture is one sibling away. Their exact paths and suite documentation are now repaired alongside the fixture move, preserving all native assertions. The test adapter also now assigns `📥️input.txt` to its mutable fixture copy; it reads the returned path rather than assuming a raw basename.

All 25 moves in this bounded batch are complete. The active artifact has 303 entries/197 files; 193 complete-file hashes are unchanged and the remaining four files contain the precise catalog/source/feature references described above. All five obsolete schemas are retained with their original hashes outside generated output. The strict scoped audit has zero findings, but it does not establish that the still-arbitrary eagle root is semantically appropriate. Evidence: `txt-subset-audit.json`. The first context-free role diagnostic overreported valid owner-local names; a proper ancestry-aware audit identified the missing exact fixture/schema/module/test registrations. Those current names are now registered and `txt-contextual-roles-final.json` has zero unknown roles.

The uncached normal fixture verifier passes five pairs with zero file problems. Ajv admits/rejects ten exact payload cases through the unchanged descriptor-linked canonical schemas, including uint32 bounds, canonical line-ending casing and boolean flags. The focused shared naming/source-authority suite passes 30 tests/1,013 assertions after the new language-neutral TXT sibling case. The first invocation treated its argument as a test-name filter and found no tests; its corrected explicit relative-path invocation is the passing result (`txt-naming-tests-resolved.txt`). The normal Stdio native target is still running its preliminary catalog-contract checks; no TXT native success is claimed. The root/other TXT codec-family names remain an open coordinated batch.

## Carrier Family Preparation

The physical source census identifies 89 `🦅️txt` owners: the main artifact plus 88 foreign carrier codec directories. Their 371 current files have a new pre-move SHA roster in `txt-family-files-before.json`. The individually selected replacement for this actual plain-text/UTF-8 carrier is `🔤️txt`; it is not occupied by any sibling in the inspected 89 groups. The same format identity may legitimately recur in different sibling groups. All runtime dialect IDs remain `s.stdio.txt`; no basename substitution may alter wire content, field names or labels. Rust declarations and local source documentation distinguish actual carrier implementations from existing explicit stubs; a naming move does not claim those stubs are implemented.

The inventory and incoming-reference evidence are `txt-family-owner-inventory.json`, `txt-codec-content-review.jsonl`, and `txt-root-reference-review.json`. The main root and all 88 carrier directories remain physically unchanged at this preparation point. The kernel agent is asked to prepare the 14 Writer/Mathematical/VCS/Sequence/Reasoning/DAG/Sourcing codec moves after its current Norm batch; the root agent prepares the remaining owners. All moves, explicit mounts, descriptor owners, registry directory identities and current test references must become coherent before qualification. No generated rename script or global replacement is permitted. Inspection of all five glTF references found only explanatory comments describing sibling Stdio formats, not executable imports or local path attributes; no missing TXT owner was established. Those exact comments are included in the coordinated reference update.

## Native Qualification Follow-Up

The uncached Stdio TXT native target exited unsuccessfully. The first two diagnostics exposed missed relative oracle includes in the MP3 and WAV mutation vocabulary tests; both literal paths now select their actual `🔮️oracle/🔣️.json`, without changing assertions. Further diagnostics report `SemioBrepEditCommand::decode_op` and widespread snapshot/mutation Serde trait failures across Stdio. No TXT native test execution or native success is claimed. The complete failed output remains in `txt-native-tests.txt`; the earlier pending status above is superseded by this result.

## Coordinated Carrier Cutover

All 89 individually reviewed TXT owners now use `🔤️txt`: 25 Stdio owners plus six Procedural/Flow owners handled by the root agent, 14 owners in the seven kernel-agent plugin trees, and 44 owners across the hub agent's 15 plugin trees. Every move was an explicit handwritten non-overwriting command. The 371-file pre-cutover witness now proves 360 complete files unchanged; the remaining 11 main TXT files differ only in the exact physical root identity. There are no missing files, surviving old owner paths, unexpected content changes or `🔤️` sibling collisions. Evidence: `txt-family-final-byte-audit.json`; the independent lane reports are `🔤️txt-seven-carriers.md` and the hub lane's retained review.

The main artifact's 303 entries/197 files pass the strict naming audit and ancestry-aware role resolution. All 59 root-lane Rust mounts resolve, including the 52 Stdio mounts, shared oracle mount and six Procedural/Flow mounts. The TypeScript export, artifact directory declarations, registry pointers, five mutation descriptor owner paths, twenty native provenance assertions, exact existing policy-waiver path, taxonomy membership/override, and DSL fixture-sweep coordinates were updated explicitly. Wire IDs remain `s.stdio.txt`; all codec bodies and payload schemas are unchanged. Evidence: `txt-root-current-audit.json` and `txt-root-byte-check.json`.

After cutover, the uncached canonical fixture verifier again passes five fixture pairs with zero file problems. The expanded language-neutral naming/source-authority suite passes 32 tests and 1,057 assertions, including three distinct TXT carrier sibling groups; its independent emoji-regex and Ajv checks remain active. Evidence: `txt-family-fixture-verify.txt` and `txt-family-naming-tests.txt`. A fresh canonical Stdio native run is underway in `txt-family-native-tests.txt`; no native success is claimed. The earlier Hub route retry overlapped the physical move and failed on the old TXT root, so it is not evidence against or for the coherent post-cutover tree and must be rerun.
