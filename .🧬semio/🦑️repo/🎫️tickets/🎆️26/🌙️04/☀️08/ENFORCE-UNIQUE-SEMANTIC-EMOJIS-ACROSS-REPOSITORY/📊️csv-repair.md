# CSV Emoji Repair

Scope: `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/📊️csv`, plus its exact Stdio oracle mount. The sole `✳️any` subset, semantic mutation IDs, fixture bytes, unique format leaves, and reserved names remain unchanged. No generated rename plan, palette, normalization write, migration, or Git mutation was used.

## Decisions And Cutover

The Git-visible baseline contains 167 files and 96 derived directories with 17 statute findings. Eighteen literal destination-absent moves repair those findings and one additional generic snapshot identity:

- editor and viewer `🎚️options→☑️options`;
- `🧪️oracle→🔮️oracle`, distinct from sibling tests;
- fixture owners `insert-record→📥insert-record`, `remove-record→📤remove-record`, `set-field→✏️set-field`, and `set-has-header→🧾set-has-header`, matching their already-authored production operation meanings;
- each fixture's `before.json→⬅️before.json` and `after.json→➡️after.json`;
- production `✏set-field→✏️set-field`, restoring the required explicit pictograph presentation without changing `set-field`;
- production `🟤️set-snapshot→📸️set-snapshot` and its colliding `🔣️.schema.json→🧬️.schema.json`.

The oracle catalog, mutation aggregate, descriptors, payload authority, and native oracle barrel mount now reference the exact new coordinates. Descriptor semantic IDs remain unchanged; the field and snapshot descriptors only update their physical owner, explicit emoji, and snapshot schema filename.

## Verification

Every CSV JSON document parses. Exact scans find no stale local `🎚️options`, `🧪️oracle`, unprefixed fixture pair, `✏set-field`, or `🟤️set-snapshot` coordinate. The post-cutover census covers the same 167 files and 96 directories with zero statute findings. The parent added the exact CSV subset-to-`🔮️oracle` contribution-owner override; canonical verification discovers all four fixture manifests and passes with zero file problems.
