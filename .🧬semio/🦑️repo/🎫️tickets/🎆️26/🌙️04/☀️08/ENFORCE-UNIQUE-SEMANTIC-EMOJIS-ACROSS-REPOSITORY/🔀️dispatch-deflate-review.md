# Dispatch and Deflate Hand Review

Six individually chosen moves repaired the remaining naming findings in the reviewed Dispatch, Deflate, Hash, ABI and Machine trees. Dispatch's four integration files are `🧩️mixed-receivers.rs`, `✍️mutable-receiver.rs`, `📈️scale.rs`, and `🕳️uninhabited.rs`. Deflate's specimen corpus is `🧪️deflate-corpus.json`; terminal/overflow cases are `🏁️deflate-tail-cases.json`.

Cargo's literal `tests` directory remains reserved, but its source filenames are governed. Four explicit `[[test]]` entries preserve their compiler-safe target names and point at the handpicked paths, with automatic target inference disabled. Two source comments and two Deflate includes were patched exactly. No assertion or fixture payload changed. Four of the six moved files retain their complete SHA-256; the other two differ only in the three hand-edited filename mentions in comments. Historical package-purity evidence paths were not rewritten to pretend they are current sources.

The final five-tree physical audit covers 54 entries, 38 files, and 45 governed entries: zero naming findings, independent emoji-regex discrepancies or unresolved semantic directory roles. Hash, ABI and Machine required no filename changes. Their compiler/tool-required manifests and configuration basenames remain literal.

Dispatch's actual exhaustive Nx route passed all 28 tests across its library and four declared integration targets, with zero skipped. The ordinary unused-qualification warning was not suppressed. The route also writes its normal repository coverage metric. The existing framework core-modules Nx route, with the explicit Deflate package added, passed all 76 tests across five binaries with zero skipped, including the unchanged Deflate corpus/miniz oracle checks.

Two language-neutral naming cases now explicitly prove that a reserved Cargo test directory does not exempt an unmarked test source. The first focused suite after that addition had 26 passes and one 30-second timeout in the existing full normalization-reader check; its deadline and assertions were not raised or weakened. The isolated reader rerun passed in 7.79 seconds with the same 30-second deadline. The fresh full suite passed all 27 tests and 701 assertions in 11.19 seconds. Earlier full focused runs passed 27 tests/688 assertions before these two cases.

Diagnostics are retained under `🗑️generated/dispatch-*`, `deflate-core-native.txt`, and `normalization-reader-isolated.txt`. This is a scoped report, not a whole-repository completion claim.
