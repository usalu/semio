# Shooting Semantic Emoji Review

The plugin's local AGENTS document defines shooting as turning 3D assets into icons. All 31 mutation payloads and the full directory identity set were read. The 155 original fixture JSON payloads have combined sorted SHA-256 `44e2cdc7455362ad3fb1eb1f907a1fc666acfa085e72c8226e61086946bf3a9a`.

## Explicit choices

The aggregate suite changes from unrelated penguin to `🎥️mutate-shooting-1`. The asset-creation owner changes from seedling to `➕️create-asset`; all other 30 mutation owners have meaningful, distinct subject/action identities and are retained.

| Scenario | Chosen emoji | Meaning inspected in the payload |
| --- | --- | --- |
| appends-saved-camera-top | 🎥️ | Adds saved camera cam-top |
| repositions-cam-close-view | 📍️ | Replaces camera position and view |
| moves-cam-close-to-front | 🔁️ | Reorders saved cameras |
| rewrites-cam-wide-through-shot-wide | 📷️ | Replaces the shot camera |
| heightens-shot-close-to-768 | ↕️ | Changes vertical output resolution |
| widens-shot-close-to-1024 | ↔️ | Changes horizontal output resolution |
| moves-shot-close-to-front | ⬆️ | Moves the shot to index zero |
| offsets-both-assets-and-skips-a-ghost | 🚚️ | Translates the two present assets |
| activates-shot-close | 🎯️ | Selects the active shot |
| rounds-shot-wide-to-ellipse | ⭕️ | Changes the crop to an ellipse |
| switches-shot-wide-to-svg | 🎨️ | Selects SVG output |
| appends-shot-macro | 📸️ | Creates a shot |
| polishes-scene-material-to-quarter | ✨️ | Reduces material roughness |
| dims-scene-ambient-to-quarter | 🔅️ | Reduces ambient lighting |
| dims-scene-sun-to-half | 💡️ | Reduces direct-light intensity |
| raises-scene-sun-to-60-degrees | 🌅️ | Raises solar elevation |
| switches-scene-sun-off | ☀️ | Disables the sun |
| switches-scene-shadows-off | 🌑️ | Disables shadows |
| turns-scene-sun-to-315-degrees | 🧭️ | Changes solar azimuth |
| doubles-asset-hero-scale | 📏️ | Doubles all scale factors |
| renames-asset-hero-to-lead | 🏷️ | Changes the asset name |
| moves-asset-hero-behind-asset-prop | 🔀️ | Reorders assets |
| removes-trailing-asset-prop | 🗑️ | Deletes the asset |
| spins-asset-hero-about-z | 🔄️ | Rotates about the Z axis |
| activates-asset-prop | 📌️ | Selects the active asset |
| appends-asset-detail | ➕️ | Adds the detail asset |
| points-asset-prop-at-v2-mesh | 🌐️ | Replaces the asset URL |

The already meaningful two deletion/refusal symbols and two label-text symbols remain. These are exact handpicked names, not automatically selected labels. Stable scenario identifiers and all fixture payload content remain unchanged.

## Verification

Completed 29 explicit physical moves and exact scoped source-reference replacements. All 31 leaf descriptors were read; the changed creation descriptor's emoji and an existing URL descriptor mismatch now match their physical owners. The central catalog includes those two exact mutation identities, all 32 aggregate/scenario identities and the 11 reviewed existing editor option identities. Strict loaded-catalog validation returns `[]`.

The final read-only audit covers 583 files, 516 directories and 1,091 governed entries, with zero findings in every category. All 237 JSON files parse and all 540 Rust literal path/include mounts resolve. The 155 fixture payload hashes are unchanged. The actual production parser expands one feature into 63 scenarios, and `fixtureUrisIn` + `resolveFixtures` resolve all 64 declared fixture URIs. This is real fixture-discovery evidence, not a claim that the native mutation handlers ran.

The source-import review found the same removed `🧪️artifact.ts` helper in two existing demo suites as in Draw. Both exact imports now use `bun:test`, and the existing package test router runs those two actual suites instead of printing `shooting ts ok`. Its uncached Nx invocation was observed failing on both missing helpers before the fix, then passed both tests (two assertions). The same existing target is now exposed in `.vscode/launch.json`. Native Shooting compilation was not run while Draw's shared Cargo verification was already active.

The Stdio owner completed canonical `📖️pdf` publication mirror alignment; a fresh post-handoff check resolves all 540 Rust mounts. The test launch entry is now narrowly restored in both `.vscode/launch.json` and its source `.vscode/🧩️launch.seed.jsonc`. Reserved package/build basenames and all input fixture bytes were preserved. No automatic renaming, generated migration or modifying Git operation was used.
