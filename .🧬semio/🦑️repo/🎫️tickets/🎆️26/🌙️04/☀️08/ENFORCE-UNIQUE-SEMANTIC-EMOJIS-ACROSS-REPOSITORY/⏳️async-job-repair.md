# Async, Job and Trace Naming Repair

Twelve literal hand-authored moves separate cooperative scheduling (`🤝️cooperative`) from its sibling clock, the job budget specimen (`🧫️fixture`) from its executable tests, and each clock, binding, budget schema and registry fixture from its siblings. No script chose names, generated rename commands, or rewrote file contents. Trace required no moves.

The budget clock fixture is `🕰️clock.json`, its binding fixture is `🪢️binding.json`, and the three schema leaves are `🪫️budget.json`, `⏱️clock.json`, and `🪢️binding.json`. The fixed-operation schema, law, and Rust cases are respectively `🧬️fixed-operation-registry.schema.json`, `📇️fixed-operation-registry-law.json`, and `🧪️fixed-operation-registry-cases.rs`. Shared action schema/law leaves use `📡️` and `⚖️`.

Exact Rust includes, root verifier/fixture-generator literals, schema self-pointers, one Cargo comment, and the cooperative taxonomy fixture were updated. The budget specimen's schema reference also had an incorrect relative depth; it now resolves from the specimen directory. A native compiler failure exposed a stale telemetry contention include with both wrong ancestry and the former leaf name; it now resolves to the existing grouped trace fixture. Two trace clock readers and the OS plugin host's clock reader follow the current job clock fixture.

The central cooperative role is `🤝️`; the specimen role is scoped to the budget owner only. Two additional language-neutral sibling cases cover these concrete choices. Reserved Cargo and package manifests remain literal. No implementation or assertion was weakened, and no Git mutation was used.

## Verification

- Physical audit: 78 entries, 52 files, 73 governed entries, zero statute findings, zero independent emoji-regex discrepancies, zero unresolved directory roles. The only five literal reserved entries are the actual Cargo/package manifests. Emoji-bearing scripts and project metadata were included in sibling checks, not exempted.
- Async and job retained all 38 files. Twenty-seven hashes are unchanged, including all three cooperative files and the four registry JSON fixtures. Eleven files changed only through the explicit source-reference, schema-pointer, or comment edits described above.
- Native Nx long routes passed: async 54/54, job 22/22, trace 23/23; zero skipped in each. The initial job compilation failed at the stale contention include and was rerun successfully after that exact repair.
- Cooperative maintenance's Ajv/arithmetic oracle passed eight cases; telemetry contention's independent oracle passed eleven.
- Workspace Nx fixed-operation fixture route passed all eight law scenarios. Shared-action fixture route passed twelve production routes, six descriptor laws and twelve hostile laws.
- Focused path-emoji suite passed 27 tests and 688 assertions after the concrete sibling cases were added. The separate package-language handoff directory is fixture data, not an executable Bun test file; no additional suite pass is claimed for it.
- The full microsecond worker-contract self-test still fails at its unmutated `none` source-binding case. The preceding schema/deadline fixture checks ran, but this is not a full microsecond verifier pass; its production token assertions remain unchanged.

The first fixed-operation command used the wrong Nx project name (`semio`); the verified rerun used the actual `workspace` project. Generated diagnostics are in `🗑️generated/async-*`, `job-*`, and `trace-native.txt`. This is a scoped completion report, not a whole-workspace completion claim.
