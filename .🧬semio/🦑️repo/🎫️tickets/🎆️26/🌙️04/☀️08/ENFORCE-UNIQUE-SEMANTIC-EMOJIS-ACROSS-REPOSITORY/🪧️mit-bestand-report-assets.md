# Mit-Bestand Report Asset and Appendix Follow-up

## Preserved Inputs

All 67 newly copied Forschungsbericht project JPEGs were compared by SHA-256 with the corresponding individually named Zwischenbericht JPEG before any move. Every pair matched. The copied paths now use the same canonical names; Pavillon Circulaire's former blue-circle discriminator was replaced by its pavilion identity `🎪️` in both copies. The exact 66 changed Forschungsbericht project references and the Zwischenbericht pavilion reference were patched. No image bytes were regenerated and no duplicate source was deleted.

The Forschungsbericht's bare demonstrator asset directory now has the same `🧺️demonstrator/🧺️aggegator.png` coordinate as the Zwischenbericht. Its compiler-facing spelling remains unchanged.

A further contents review corrected five weak earlier project identities in both reports: BioPartner's bioscience laboratory `🔬️` (not lettuce), Berlin-Schildow's prototype `🧫️`, the explicitly comparative Verbiest/Karreveld cluster `⚖️` (not a bicycle), Kamikatsu's approximately 700 reclaimed windows `🪟️` (not a tower), and the Bestandsverpflanzung pavilion's documented 1972 Olympic Village bungalows `🏅️` (not a palm tree). Exact appendix coordinates were patched; bytes are unchanged.

## Sponsor Identities and Dark Variants

The three reports and web demonstrator each retain their own ten original PNGs. Sponsor directories are handpicked by identity: LUH `🎓️`, UdK `🎨️`, BBSR `🏛️`, BMWSB `🏢️`, Zukunft Bau `🔮️`. Each contains the meaningful pair `☀️logo.png` and `🌙️logo-dark.png`. This removes the earlier owl/brush/nightscape dark-image discriminators.

All 40 before/after byte-length and SHA-256 records match. Report footer references, demonstrator footer constants, and demonstrator brand logo references were patched exactly. The Zukunft Bau TeX class now looks up the corresponding compiler-staged paths, for example `asset/logo/bbsr/logo.png` and `asset/logo/bbsr/logo-dark.png`. No fallback compatibility paths or canonical-name selection algorithm was added.

## Shared Appendices

The current shared appendix owners were preserved while names became semantic: actor figures `🕸️`, tables `📊️`, programs `🎯️`, actor-network introduction `👥️`, input model `📥️`, integration maturity `🔗️`, evidence matrix `🧾️`, scaling `📈️`, candidate ledger `📇️`, and project audit `🔎️`. The Forschungsbericht's project appendix is `🏗️projekte.tex`.

Only exact current producer constants, authored include strings, and the rendered introduction's include strings were patched. The active report router was not overwritten or regenerated. Actor data identities, classifications, coordinates, edge records, and decisions were unchanged.

## Executed Verification

- Full scoped Mit-Bestand statute audit: **1,829 files, 1,047 directories, 2,865 governed entries; zero findings in all eight categories**.
- Full report subtree audit: **1,705 files, 1,013 directories, 2,715 governed entries; zero findings**.
- Uncached demonstrator quick tests: **2 test files and 5 tests passed**.
- Uncached actor-network validation passed twice: after all eleven country repairs and again after the shared appendix renames.
- Uncached report test passed: 20 Zwischenbericht, 10 Forschungsbericht, and one Kompaktbericht source documents plus the actor-network checks.
- Uncached native `@semio-tech/mit-bestand-bericht:build` completed successfully for all three reports and their light/dark passes. Each actual PDF was emitted by the print pipeline. TeX reported layout warnings, not failed asset lookups. The final Nx target exited successfully.
- All 1,504 actor PNGs retain their original byte-length/SHA multiset; all 465 approved image paths resolve in both reports and appear in the figures.

No modifying Git command, workspace reset, automated filename selection, mass rename plan, figure regeneration, or image rewrite was used.
