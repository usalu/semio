# Process Plugin Emoji Repair

## Scope

Owned tree: `✏️s/🔌️plugins/🏭️process`, including the Process3D artifact, editor surfaces, serializers, mutation manifests, fixtures, package mounts, and exact central registrations. The assignment's `⚙️process` path does not exist; `🏭️process` is the repository's Process plugin identity.

The initial strict audit covered 423 files, 355 directories, and 759 governed entries. It found two missing emojis, one generic emoji, and 23 sibling-emoji collisions. Every changed identity was selected and moved explicitly; no automatic emoji chooser, automatic rename planner, migration script, or modifying Git operation was used.

Both editor-window option authorities now use `☑️options`. The artifact command family is `🗿️artifact`, distinct from its sibling `📄️document`. Both Wavefront serializer families use `🗿️obj`, distinct from their sibling `🧊️gltf`. The subset-local contribution is `🔮️oracle`. All 16 mutation payload sidecars are `🧬️.schema.json`.

The two formerly unprefixed artifact tests are `✉️p8yx-process3d-retained-envelope-laws.json` and `🧬️p8yx-process3d-retained-mutation-laws.json`. Their sibling route fixtures are `📐️retained-route-schema.json` and `⚖️retained-route-laws.json`. The plugin-root manifest is `📜️manifest`.

All 16 oracle, Python-adapter, and Gherkin fixture identities were reconciled individually with the existing physical scenarios. Cursor, icon, enablement, provenance, stock label, machine/step creation and deletion, stock transform, renames, step ordering, capability replacement, measure replacement, and solid replacement use the exact handpicked identities recorded in their live oracle and adapter tables.

## Verification

- Final strict scoped audit: 423 files, 355 directories, 759 governed entries; missing, generic, presentation, spacing, duplicate, multiple, reserved-emoji, and oracle findings are all zero.
- `validateTaxonomy(loadCatalogTaxonomy())` returns `[]` after adding the exact Process oracle override and only the two missing shared identities, `🗿️artifact` and `📜️manifest`.
- All 247 Rust `#[path]` package mounts resolve.
- All 16 mutation descriptors exactly match their physical owners and declare `🧬️.schema.json`.
- The oracle manifest's 16 mutation owners, 16 payload schemas, and 16 scenario directories resolve.
- All 16 Python adapter fixture roots and both 16-row Gherkin tables resolve.
- The stale Python oracle rationale was corrected to the physical `🌷️mutate-process3d-1/🐍️.py`; the Gherkin Stdio-law prose now names the physical `🦀️.rs` authority.
- `bun nx run @semio-tech/process-plugin:test-quick`: exited 1 before reaching Process because the shared Stdio crate references a missing `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🧿️semio/🏅️standards/🔖️v1/🪆️subsets/✳️brep/🧬️schema/🧬️mutations/🔗create-edge/🔺️diff/🦀️.rs`; Cargo could not compile `semio-s-plugin-stdio`. This is outside the owned Process scope.

## Exact Central Override

```json
"✏️s/🔌️plugins/🏭️process/🗿️artifacts/🧊️process3d/🏅️standards/🔖️1/🪆️subsets/✳️any": "🔮️oracle"
```
