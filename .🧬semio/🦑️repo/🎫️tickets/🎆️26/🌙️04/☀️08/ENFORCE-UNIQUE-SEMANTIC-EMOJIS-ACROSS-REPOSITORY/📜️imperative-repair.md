# Imperative Emoji Repair

## Scope

Hand-reviewed every strict path-statute finding under `✏️s/🔌️plugins/📜️imperative`, including the entire step-command family, four window configurations, retained-route fixtures, mutation payload schemas, and oracle ownership.

## Result

- Strict audit: 303 files, 213 directories, 494 governed entries.
- Findings: zero missing, generic, presentation, spacing, duplicate, multiple-emoji, reserved-name, and oracle breaches.
- Central taxonomy validation: zero problems; every physical command is registered.
- TypeScript example/runtime tests: 2 passed, 0 failed.
- Exact stale path-reference scan and mojibake scan: zero findings.
- Rust compilation remains to be rerun after the concurrent Stdio repair settles; no passing Rust result is claimed.

## Handpicked identities

- Execution and locale: `🏃️run` and `🗣️set-locale`.
- Creation: `➕️add-step` and targeted `📍️add-step-at`.
- Movement: `🚚️move-step` and targeted `🧭️move-step-at`.
- Removal: `➖️remove-step` and targeted `✂️remove-step-at`.
- Parameter updates: `🎚️set-step-params` and targeted `🎯️set-step-params-at`.
- Window configuration: `⚙️config`, paired with retained `🎚️options`.
- Retained-route fixtures: `🛣️retained-command-routes.json` and `🧬️retained-command-routes.schema.json`.
- Direct mutation schemas: `🧬️.schema.json`.
- Oracle owner: `🔮️oracle`.
