# Deflate Emoji Repair

Scope: `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🗜️deflate`, plus its exact Stdio oracle mount. The existing sole `✳️any` subset, unique format leaves, semantic IDs, fixture bytes, and reserved names remain unchanged. No generated rename plan, palette, migration, Git mutation, or normalization write was used.

## Baseline And Decisions

The Git-visible baseline contains 168 files and 97 derived directories with 16 statute findings. Each has one direct semantic repair:

- editor and viewer `🎚️options→☑️options`, separating option values from sibling configuration;
- `🧪️oracle→🔮️oracle`, separating the independent oracle from sibling tests;
- fixture owners `set-compression-params→🧮️set-compression-params`, `set-payload→📦️set-payload`, and `set-preset-dictionary→📖️set-preset-dictionary`;
- each fixture's `before.json→⬅️before.json` and `after.json→➡️after.json`;
- `📄️readme-level1.zz→🪶️readme-level1.zz` for the fastest/lightest compression and `📄️readme-level9.zz→🗜️readme-level9.zz` for maximum compression;
- `🟤️set-snapshot→📸️set-snapshot` and its colliding schema `🔣️.schema.json→🧬️.schema.json`.

All sixteen moves were literal destination-absent `mv -n` operations. The oracle catalog, mutation aggregate, descriptor, native oracle mount, Gherkin scenarios, and Rust fixture adapter now use the exact new paths. The snapshot descriptor's semantic ID stays `set-snapshot`; only its path identity, explicit emoji, and payload-schema filename changed.

## Verification

The post-cutover census covers the same 168 files and 97 directories with zero statute findings. Every JSON document parses. Exact scans find no stale Deflate `🎚️options`, local `🧪️oracle`, old fixture coordinates, `📄️readme-level*.zz`, or `🟤️set-snapshot` path. The only local `🧪️oracle` text is the intentional plugin-level oracle host path.

The catalog contains three fixture manifests. Before the central exact owner override landed, the canonical verifier reported zero discovered fixtures and zero file problems; that result was correctly rejected as qualification. The parent added `✏️s/🔌️plugins/🗄️stdio/🗿️artifacts/🗜️deflate/🏅️standards/🔖️rfc1950/🪆️subsets/✳️any → 🔮️oracle` to the exact contribution-owner contract. The rerun discovered all three fixtures and passed with zero file problems.
