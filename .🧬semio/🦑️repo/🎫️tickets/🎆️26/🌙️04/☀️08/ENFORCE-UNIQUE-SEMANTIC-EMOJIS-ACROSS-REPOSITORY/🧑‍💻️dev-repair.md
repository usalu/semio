# OS Development Host Hand Repair

Scope: `🧰️framework/🛍️products/💻️os/🔨️modules/🧑️‍💻️dev`. Root and OS instructions apply; no deeper AGENTS file exists. These are individual content-reviewed choices and exact moves, not automated naming. No Git modification, binary rewriting, output regeneration, or old cleanup routine was run.

## Exact Moves

Paths are relative to the development-host root; the parent is its current name after any preceding move.

| Parent | Old | New | Meaning |
| --- | --- | --- | --- |
| . | 🔌️extension-modules | 🧩️extension-modules | Installed extensions, distinct from built plugin modules. |
| . | 🧪️fixtures | 🧫️fixtures | Committed development specimens, distinct from tests. |
| 🧫️fixtures | 🔣️engine-publication.json | 📢️engine-publication.json | Engine publication cases. |
| 🧫️fixtures | 🔣️linked-session-engines.json | 🔗️linked-session-engines.json | Explicit session/engine links. |
| 📦️packages/🟦️typescript | 🧪️fixtures | 🧫️fixtures | Package source specimens, distinct from tests. |
| 📦️packages/🟦️typescript | 🧪️config.test.ts | 🧹️config.test.ts | Leading Bun shebang removal tests. |
| 📦️packages/🟦️typescript | 🧪️bench-web-harness.ts | 📊️bench-web-harness.ts | Browser benchmark measurements. |

Exact producer/readers were patched together: the extension-store install root, development script fixture paths and benchmark entry, package Vitest include, and registry launch-test fixture root. Public plugin/extension IDs are unchanged. Frozen remaining-package-purity coordinates remain unchanged.

Four scoped semantic roles were registered: extension install root under modules, WASM profile policy under fixtures, its v1 specimen, and executable-source test. The current taxonomy loads and each resolves through its actual full ancestry.

The development test exposed a nonexistent demonstrator `📦️index.tsx` coordinate. Its HTML explicitly names `🟦️.tsx`; the test now reads that actual entry. The next run reported 76 passing tests and one declaration/import mismatch: demonstrator metadata named the nonexistent `@semio-tech/puzzle-js/board-session` export. The actual entry imports the factory from the package root and that root exports it. The exact metadata declaration now matches that existing public export. Final development suite: 77 tests pass. Registry regression: 10 pass. Extension-store regression: 3 pass.

The subsequent scale-output review added a read-only test of the two committed 2550-record/50-plugin specimens, including an independent Node parse/count/ID-roster oracle. It failed against the old flat filenames and passes against the actual nested semantic paths. The existing generation function now writes only its two explicit owned leaves and rejects unexpected children instead of recursively deleting them. It was not executed. The existing read-only `scale-fixture-check` reports fresh; native scale-fixture tests pass 12/12.

## Materialized Trees Still Under Review

The current plugin and extension output producers use logical IDs as physical directory names and browser URL segments. Renaming only the existing files would break those readers and future builds. Their exact declared domain identities, catalog mapping, extension-store behavior, and browser URL readers must be updated together before any rematerialization. No blanket generated exemption has been introduced.

Existing `dist`, package `fixture/dist`, plugin-module outputs, extension-module outputs, and JCO artifacts remain preserved, including binaries. Some old worker outputs are explicitly described as obsolete by the current source cleanup routine; that destructive routine was not executed. Third-party JCO ABI names must be supported by actual tool-contract evidence before any narrow reservation. This scope is not claimed fully compliant while these authored/generated boundary issues remain.
