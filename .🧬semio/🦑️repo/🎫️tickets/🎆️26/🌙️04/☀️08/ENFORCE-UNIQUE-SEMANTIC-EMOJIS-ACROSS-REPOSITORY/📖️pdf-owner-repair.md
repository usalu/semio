# PDF Owner and IO Mirror Repair

The primary PDF artifact's previous `🌳️pdf` name had no content justification: its facade declares the PDF document schema and its implementations handle pages, fonts, annotations, archives, publication profiles, and rendering. The final handpicked sibling-unique identity is **`📖️pdf`**, a paged publication. A brief physical `📄️pdf` intermediate was corrected before any reference patch because the explicit statutes reserve that generic document identity.

The primary owner and 24 authorized import/export mirror mounts were moved explicitly. These cover Animate, Writer, GIS, Playbook, Trinity, Draw, Raster, Shooting, Layout, Note, and Stdio's Semio drawing/document subsets. Their format/version paths and inspected deserializer APIs establish PDF ownership. No sibling already used `📖️`. The coordinating agents independently own the Procedural and Puzzle mirrors.

Exact references in 157 active source files were patched: owner definition, registry, generators, oracle descriptors, schemas, native include barrels, policy allowlist, tests, and central taxonomy oracle overrides. No source implementation, API identifier, fixture payload, expected result, or artifact ID was changed. An active no-ignore source scan found no remaining `🌳️pdf` references.

Verification:

- Uncached PDF fixture verification: **105 fixtures, zero file problems**, successful Nx target.
- The Git-pathspec audit sees only three files in this renamed/ignored root and is explicitly **not** sufficient evidence of its contents.
- The same read-only statute logic was also executed in memory against the actual PDF filesystem, excluding only Cargo `target`, `dist`, and `node_modules` build outputs: **1,769 files, 1,045 directories, 2,749 governed entries; zero findings in all eight categories**.
- An unfiltered physical census also exposed existing Cargo build caches below standalone `🦀️lopdf-engine/target`; they are build output, not authored source. Nothing in those caches was renamed or reclassified.
- The remaining central artifact member literal `📄️pdf` was corrected to `📖️pdf`. The actual `loadTaxonomy()` and `semanticDirectoryKindId()` APIs were then exercised over all 29 physical PDF owner/mirror chains: **85 owner, standard, and subset checks; zero failures**, including each IO mirror's `🔖️1.4` or `🔖️1.7` standard and `✳️any` subset.

No permanent script was added or changed. All filename choices were manual; the temporary filesystem enumeration and source-reference patch construction did not select names.
