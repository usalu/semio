# Sourcing Emoji Repair

## Scope

Hand-reviewed every strict path-statute finding under `✏️s/🔌️plugins/🪵️sourcing`, including filters, curation actions, example selection, window configuration, fixtures, mutation schemas, oracle ownership, and presentation normalization.

## Result

- Strict audit: 307 files, 219 directories, 510 governed entries.
- Findings: zero missing, generic, presentation, spacing, duplicate, multiple-emoji, reserved-name, and oracle breaches.
- Central taxonomy validation: zero problems at the time of repair.
- Focused Bun tests remain to be rerun after the concurrent Cargo workload clears; no passing result is claimed.
- Exact stale path-reference scan: zero findings after propagation.

## Handpicked identities

- Expected-stock fixture: `📦️expected-stock.json`.
- Example selection: `🎬️set-active-example`.
- Filters: `📉️set-filter-min-availability`, `🧱️set-filter-module`, `🔎️set-filter-query`, and `🏛️set-filter-typology`.
- Table order: `↕️sort-table`.
- Curation: `➕️curation-add`, `➖️curation-remove`, `🔢️curation-set-count`, retained `🧺️drop-on-curated`, and `🏊️drop-on-pool`.
- Window configuration: `⚙️config`, paired with retained `🎚️options`.
- Oracle owner: `🔮️oracle` at subset level.
- Direct mutation schemas: `🧬️.schema.json`.
- Presentation normalization: `🗃️entries`.

