# Browser Icon Publication Hand Review

The Styling emitter wrote raw `favicon.svg` and `🖼️favicon.ico`, the latter colliding with the newly explicit `🖼️assets` output directory. These are explicit HTML-linked publication files, not implicit browser-reserved basenames. Their individually selected identities are `🛡️favicon.svg` for the shield-shaped emblem and `🔖️favicon.ico` for the bookmark icon. The existing assets, plugin modules, extension modules and proposed bundle directory have different sibling identities. No alias or palette rule was added.

The schema-first authority is `styling/🌐️favicon.json`, validated independently with Ajv against `🧬️favicon.schema.json`. Neutral request vectors include both exact raw and encoded names, query handling, old names, missing presentation selectors, malformed escapes, double encoding, encoded separators and surplus descendants. The real HTTP regression failed first with 404 instead of 200, then passed for both dev and preview middleware. The existing SVG bleed transform and ICO bytes are unchanged; response hashes are compared to those exact sources. The seven write=false/default/true emission guards remain intact.

The emitter, shared head fragment and both authored OS development/harness HTML documents now use the explicit authority. Actual browser encoding exposed the previous ICO middleware defect: it compared raw emoji strings against encoded request paths. The corrected handler decodes once, catches malformed escapes and accepts only the two exact identities. It does not normalize or admit old aliases. Parse5 checks the shared fragment and the two current HTML documents independently.

The first complete Styling run passed 39 tests and 1,347 assertions. A subsequent invocation returned cached output and was not accepted as fresh evidence for the added Parse5 checks. The explicit uncached final run passes 39 tests and 1,350 assertions, including all three parsed documents. Evidence: `🗑️generated/favicon-delivery-fresh-tests.txt`, earlier `favicon-delivery-styling-tests.txt`, and RED `favicon-delivery-red.txt`. Both changed sibling groups contain 18 entries total and have zero strict emoji findings (`favicon-sibling-audit.json`). Existing primary distribution files remain untouched until the parallel output-authority review is complete.

The uncached canonical Styling freshness check also passes after the favicon changes (`favicon-styling-freshness-resolved.txt`). An initial invocation selected the TypeScript test project, which has no `check-generated` target; no check executed in that attempt. The verified generator owner is `@semio-tech/ui-styling-tokens:check-generated`.

One external website URL in the authored Zukunft Bau HTML fixture had also been polluted: `/Icons/🖼️favicon.ico`. Independent live HEAD requests on 2026-09-04 returned 404 for that URL and 200, `image/x-icon`, 1,150 bytes for the actual `/Icons/favicon.ico`. Only that one exact external URL was repaired. The original hash, one-occurrence check and expected-hash witness prove that no other fixture byte changed; see `favicon-remote-fixture-before.json` and `favicon-remote-fixture-after.json`. The uncached normal HTML fixture verifier passes eight fixtures with zero file problems (`favicon-html-fixture-verify.txt`). This is a remote URI, not a local filename subject to emoji naming. Other fixture resource coordinates are outside this bounded change and remain for their own hand review.
