# Block Hand Repair

## Block 2D

The Block 2D subset was reviewed against its actual sibling groups rather than a generated rename plan. Seven directory identities were chosen individually from their roles:

- window choices: `🎚️options` → `☑️options`, distinct from runtime `🎚️config`;
- handle removal command: `🌱️remove-handle` → `➖️remove-handle`, distinct from creation `🌱️add-handle`;
- example-loading command: `🎨️set-active-example` → `🎬️set-active-example`, distinct from `🎨️edit`;
- compatibility-rule removal command: `🔗️remove-compatibility-rule` → `🚫️remove-compatibility-rule`, distinct from `🔗️add-compatibility-rule`;
- handle-kind removal command: `🔘️remove-handle-kind` → `🗑️remove-handle-kind`, distinct from `🔘️add-handle-kind` and from the other removal actions;
- right-hand example: `🎬️hexagonal-cut-concrete-forest-right` → `➡️hexagonal-cut-concrete-forest-right`, distinct from the left example;
- comparison authority: `🧪️oracle` → `🔮️oracle`, distinct from executable `🧪️tests`.

All seven destinations were checked absent before moving. SHA-256 values of representative files were identical before and after every directory move. The Rust barrel, snapshot include, mutation documentation, oracle include and central taxonomy were then patched only at their exact references. The four command contracts and the example contract are registered in `semanticDirectoryMemberKinds`; the owner-specific oracle override and the existing window-facet `☑️options` statute cover the remaining two identities. All seven old 2D coordinates have zero remaining live references. All 160 Rust barrel mounts under Block 2D resolve.

Each of the 26 mutation owners was then reviewed as a descriptor/schema sibling pair. The descriptor remains `🔣️.json`; the JSON Schema now has the schema-kind identity `🧬️.schema.json`. Every file was moved with an explicit owner-specific command after destination-collision checks. The ordered aggregate of all 26 content hashes was identical before and after the moves. All 26 descriptors and all 26 oracle mutation entries now name `🧬️.schema.json`; each reference resolves and all schemas parse as JSON. No `🔣️.schema.json` path or reference remains in this authority.

The fresh repository-context read-only audit reports zero Block 2D findings across every category: missing, generic, presentation, spacing, duplicate, multiple, reserved-name and independent emoji-oracle checks. The audit snapshot is retained at `🗑️generated/block-2d-post.json`. This is a bounded Block 2D result, not a repository-wide completion claim.

## Block 3D

The Block 3D sibling groups were reviewed directly. The two window-choice owners are now `☑️options`, distinct from runtime configuration. Operation identities now distinguish removal (`➖️remove-vortex`), example activation (`🎬️set-active-example`), kind deletion (`🗑️remove-vortex-kind`), leaving (`👋️leave-surface`), placement (`📍️place-vortex`), flip (`🔄️set-brush-flip`), radius (`📏️set-brush-radius`), brush kind (`🧬️set-brush-vortex-kind`), patching (`🩹️patch-representation`), representation disposal (`🚮️remove-representation`), utility selection (`🪛️set-active-utility`), arrangement (`↔️set-window-arrangement`), representation collection (`🖼️set-window-representations`), spacing (`📐️set-window-spacing`) and toggling (`🔀️toggle-window-representation`). The Nakagin example is `🏢️nakagin-capsule`, and comparison authority is `🔮️oracle`. Six existing identities with incomplete presentation were normalized without changing their semantic emoji: `✂️remove-compatibility-rule`, `✒️rename-representation`, `🏔️change-representation-lod`, `🕳️delete-vortex`, `🖋️rename-vortex-kind` and `🗑️delete-representation`.

Each destination was checked absent before its explicit move. The aggregate content digest for all semantic directory moves stayed `838888b…`. All 37 mutation schemas were explicitly moved from the colliding descriptor basename `🔣️.schema.json` to the schema-specific `🧬️.schema.json`; their aggregate digest stayed `2fcac8dc…`. The exact barrel, documentation, include, command-doc and taxonomy references were updated. All 37 descriptor/schema pairs and all 37 oracle entries resolve and parse; all 224 Block 3D barrel mounts resolve; all retired coordinates have zero live references.

## Block 5D

Six presentation-only identities were first repaired explicitly: `✂️remove-compatibility-rule`, `✒️rename-representation`, `🏔️change-representation-lod`, `🕳️delete-grip`, `🖋️rename-grip-kind` and `🗑️delete-representation`. Their aggregate digest stayed `7d4482…`. The two options owners are now `☑️options`; the handpicked command identities are `➖️remove-grip`, `🎬️set-active-example` and `🗑️remove-grip-kind`; the example is `🏢️nakagin-capsule`; comparison authority is `🔮️oracle`. The semantic-directory aggregate digest stayed `a7a4b6…`.

All 41 mutation schemas were explicitly moved to `🧬️.schema.json`; their aggregate digest stayed `7e579e…`. The exact barrel, documentation, include, command-doc and taxonomy coordinates were updated, and retired identities with no physical owner were removed from the taxonomy. All 41 descriptor/schema pairs and all 41 oracle entries resolve and parse; all 219 Block 5D barrel mounts resolve; all retired coordinates have zero live references.

## Publication Authority and Combined Verification

The publication-authority descriptor/schema siblings are now `🔣️.json` and `🧬️.schema.json`. The schema digest stayed `499cc8…`; both files parse and the descriptor resolves the schema. No stale `🔣️.schema.json` coordinate remains anywhere under Block.

The combined fresh repository-context audit snapshot is `🗑️generated/block-post.json`. It reports zero findings under the complete `✏️s/🔌️plugins/🧱️block/` tree across every category. The focused Nx naming suite was also run against the live concurrent workspace: 33 tests passed and two failed outside Block. One failure was Value's in-progress keycap oracle case, since repaired and independently green; the other is an OS legacy ZWJ output root being actively recreated by a separately running demonstrator process. No Block assertion failed, and no global green claim is made.
