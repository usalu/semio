# Stdio Semantic Follow-Up

This pass manually inspected the 31 Stdio artifact trees other than Semio, DXF, OBJ, PPTX, and GLTF. It supplements the earlier structural audits: unique arbitrary animals or colored shapes are not meaningful identities merely because a mechanical audit accepts them.

## Handpicked Identities

| Tree | Explicit semantic corrections |
| --- | --- |
| ZIP, JSON, BCF, XLSX | Mutation suites use `🔀️`; ZIP README scenario `📖️`, BCF clash resolution `🤝️`, XLSX formula expansion `🧮️`. |
| PNG | Mutation suite `🔀️`, direct behavior `🎯️`, Rathaus fixture `🏛️`. |
| IFC | IFC4 suite `🏗️`; IFC2x3 Base `🧱️`, COBie `🏢️`, SAV `🧮️`, CV20 `🤝️`. |
| SVG | Base suite `🎨️`, Basic `🔰️`, Tiny `🔬️`; Basic/Tiny whole snapshots `📸️`. |
| XML | Base suite `📰️`; the already meaningful Valid suite is retained. |
| STEP | Base suite `📐️`, conformance suites `1️⃣` through `6️⃣`; whole snapshots `📸️`; product-name scenario `🏷️`; real concrete-forest fixtures `🌲️`. Standard-level shared oracle is `🔮️`, matching its existing Rust mount. |
| DWG | Both version suites `🖊️`, whole snapshots `📸️`; title and save-counter scenarios remain `✏️` and `🔢️`. |
| DOCX | Base suite `📜️`, Strict `📏️`, Transitional `🔄️`; bold scenario `🅱️`; snapshots `📸️`, main namespace `🌐️`, relationship base `🔗️`, set/remove conformance `✅️`/`🚫️`, insert/remove VML `✒️`/`🧹️`, insert/remove alternate content `🪆️`/`📤️`. |
| LAS | Header suite `🎩️`, Points `📍️`, VLR `📼️`; snapshot `📸️`, raised-point scenario `📈️`. |
| GIF | Round-trip `🔁️`; GIF89a Base suite `🎞️`, Comment `💬️`, Graphic Control `🎛️`, Application `🧩️`; GIF87a suite `🖼️`; snapshots `📸️`, frame-delay scenario `⏱️`. |
| PDF | Base suites `📑️`; PDF/X `🖨️`, PDF/A `🗄️`, PDF/E `📐️`, PDF/H `⚕️`, PDF/VT `🧾️`, PDF/UA `♿️`; minimal append suite `➕️`; concrete-inverse scenarios `🔄️`. |
| MP4, AVI | MP4 suite `🎞️`; AVI HDRL/MOVI/IDX1 suites `🎛️`/`🎞️`/`📇️`; sync/keyframe scenarios `🔑️`. |
| EPW | Suite `🌦️`, warming scenario `🌡️`. |
| BMP | Pixel-data mutation and its fixture `🔲️`, direct behavior `🎯️`. |
| JPG | Document suite `📸️`; existing direct behavior `🎯️` retained. |
| HTML | Suite `🌐️`, document-language scenario `🗣️`, existing-building fixture `🏚️`. |
| Deflate | Zlib round-trip `🔁️`, mutation suite `🗜️`, raised compression hint scenario `📈️`. |
| Markdown | Suite `📝️`, heading-demotion scenario `🔽️`. |
| TIFF | Round-trip `🔁️`, Document suite `🖼️`, Baseline suite `🧱️`, direct behavior `🎯️`. |
| CSV | Suite `📊️`, corrected-cell scenario `✏️`. |

WAV, MP3, TXT, Binary, PLY, TSV, and STL were inspected; their already meaningful suite/scenario identities were retained. Stale WAV/MP3/Binary `sourceMutationDirectoryName` snapshot fields were corrected to the existing `📸️set-snapshot` directory.

All renames were explicit individual moves, with exact reference updates. No automatic emoji selection, migration, Git mutation, or fixture regeneration was used in this follow-up. Existing unrelated content was preserved.

## Reference Verification

Actual-filesystem inspection on 2026-09-05 verified all 70 catalog vector mutation/scenario coordinates. The catalog `directoryName` values now identify the physical semantic scenario directories, including ten JPG direct-behavior records that previously retained `🧪️`.

An exhaustive read-only scan of the owned Rust files found 36 missing literal `#[path]`, `include_str!`, and `include_bytes!` targets. These were real source-coordinate defects left by earlier subset splitting: oracle fixture/test includes still climbed five parent directories, IFC4 demo includes still named `✳️base`, and the STEP shared oracle had not physically moved to the `🔮️` coordinate already declared by its mount. Corrected each to its verified existing subset-local fixture/test or deliberate cross-version target. Recheck: **2,699 literal targets, zero missing**. All **1,953 JSON files parse**.

All 31 scoped statute audits returned zero in every category. Thirty inventories had realistic file counts. PDF's scoped Git-backed inventory returned only three files, so that result is explicitly insufficient for full PDF coverage; the root agent was notified to include PDF in the final unscoped inventory.

## Fixture Integrity Investigation

The first actual-filesystem hash check inspected **640 manifests and 1,578 carriers**. It found one BMP pair whose physical fixture directory still used the old calculator identity; the physical directory was corrected to its explicitly handpicked pixel identity `🔲️`.

The other 110 discrepancies were exclusively STEP CC6 manifest digests. For all 109 metrics records, replacing the already-committed string `🎯️expected.step` with `expected.step` **in memory only** reproduced the exact previously recorded byte length and SHA-256. Thus the stale records were caused solely by a prior semantic filename reference update; numeric measurements were unchanged. The remaining hexagonal source fixture matches HEAD and every one of the seven current subset copies byte-for-byte (`d53c56233f84726e438e595072aabf20a19f6760d69f644a30f1ba226b3eeb12`, 79,984 bytes); its manifest alone retained an older digest. Only these proven checksum/length records were patched. All fixture bytes were left untouched.

Final verification:

- Read-only actual-filesystem fixture check: **640 manifests, 1,578 carriers, zero missing paths or byte/hash discrepancies**.
- Uncached `bun nx run @semio-tech/repo-test-domain:test-fixture-verify --skip-nx-cache -- --artifact s.stdio.step`: **157 fixtures, zero file problems**, exit 0.
- Uncached equivalent BMP target: **5 fixtures, zero file problems**, exit 0.
- Shared Stdio oracle/package source scan contains no stale STEP suite/forest fixture, HTML real-building fixture, or BMP calculator-pixel coordinates.

These checks do not claim a full native Stdio codec execution. The initial BMP/mismatch checks are retained as investigation evidence, not presented as passes.
# Central External STEP Contract Follow-up

The repository-wide contract test subsequently exposed seven stale `external-step-assets.outputRoots` coordinates outside the Stdio tree. Exact paths for AP214 CC1–CC6 and base now use the already handpicked `🌲️hexagonal-cut-concrete-forest-left-ap214` fixture directory. All seven targets were verified to exist. The external classification, null owner/target, fixture bytes, and digests were unchanged. A no-ignore scan of active Stdio TypeScript, Rust, and JSON source found no remaining old `🧪️hexagonal-cut-concrete-forest-left-ap214` references.
# Final TSV Reconciliation

A fresh literal Rust include/path scan after the final plugin reconciliation resolves all 49 TSV targets. The earlier TSV missing schema/snapshot/casing path notes are superseded; no missing TSV literal mount remains. Native compiler or grammar implementation limits are distinct from this verified path result.
