# glTF Semantic Naming Follow-up

The structural repair left arbitrary emoji in nested glTF mutation scenarios and test suites. This follow-up reviews each entity/operation/scenario in the `♾️any` schema, then checks the split subset oracle catalogs against those physical scenarios. Existing meaningful entity and operation names remain the authority.

Five test suites now identify their domain: `🧊️mutate-gltf-2-0`, `🎞️mutate-gltf-2-0-animation`, `🎥️mutate-gltf-2-0-camera`, `💎️mutate-gltf-2-0-material`, and `🦴️mutate-gltf-2-0-skin`.

Topology inferences are individually meaningful: `➰️boundary-loops`, `🥯️handles`, `🔢️genus`, `🕳️holes`, and `🧮️euler-characteristic`. The original balloon, octopus, koala, panda, and magic-wand markers were unrelated to the corresponding inference. Exact owned source and shared Rust mounts were updated with each literal move.

All 120 mutation scenarios were reviewed. Seventy-three arbitrary prefixes were replaced by individually selected action or subject identities: clips use film, cameras use a camera, buffer creation uses a disc, meshes use a mesh, accessors use numbers, primitive creation uses a triangle, weights use a scale, movement uses a truck or directional arrow, ordering uses shuffle, attachment uses links, and rejection uses an appropriate prohibition. Existing meaningful binding, removal, renaming, and metadata scenario identities were retained. Split oracle catalog `directoryName` fields now cite the actual physical scenario, including five previously different slugs; semantic scenario IDs were preserved.

Three generic fixture wrappers were reviewed against their real contents and renamed explicitly: metabolism `🏙️base` contains the 271-mesh building export, demo `🔺️example` contains a three-vertex triangle, and `🌳️base-with-nested-node` expresses the derived hierarchy fixture. Binary contents were not rewritten. Exact local references and the Semio Mesh citation were patched.

Verification completed after these edits:

- `bun nx run @semio-tech/repo-test-domain:test-fixture-verify -- --artifact s.stdio.gltf`: 120 fixtures, zero file problems, exit 0.
- Production taxonomy helpers resolve all 120 catalog scenario directories, zero missing.
- All 985 literal Rust embedded-file references and 171 glTF shared Rust mounts resolve.
- All 1,072 JSON files parse; all 120 operation descriptor emoji fields agree with their owning operation identity.
- Read-only scoped naming audit: 2,486 files, 1,619 directories, 4,098 governed entries; all eight violation counts zero.

The parent agent owns the separate non-topology inference semantic pass; this report claims completion of suites, scenarios, topology, catalog metadata, examples, and fixture coverage only. No Git modification, worktree, generated rename script, automatic emoji selection, or shared barrel regeneration was used.
