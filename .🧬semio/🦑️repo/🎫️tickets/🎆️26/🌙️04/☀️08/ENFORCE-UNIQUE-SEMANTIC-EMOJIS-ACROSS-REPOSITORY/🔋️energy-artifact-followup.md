# Energy Model Artifact Follow-up

## Scope

The Energy model was the smallest untouched bounded non-Norm artifact outside the excluded Hub, Stdio, Kernel and Puzzle lanes. Its local instructions identify a headless building-energy model and require the existing Nx-routed package test. The starting scoped audit covered 196 files and 137 directories, 333 governed entries in total, and found four option/config collisions, one tests/oracle collision, one descriptor/schema collision and one missing-presentation inference owner.

## Handpicked identities

- Four empty option facets are now `☑️options`, distinguishing selectable options from their sibling `🎚️config` facets.
- The subset's reference contribution is now `🔮️oracle`, distinguishing it from executable `🧪️tests`.
- The `replace-model` payload contract is `🧬️.schema.json`, distinguishing the JSON Schema from its adjacent `🔣️.json` mutation descriptor.
- The inference owner is `🗃️entries`. This retains the entries/census meaning while correcting the malformed presentation-less `🗃entries` identity.

Every physical move was explicit and destination-absent. Exact live consumers changed with their owners: the replace-model descriptor, current oracle specification source and mutation row, Python reference doc, Rust schema read/assertion, Energy feature coordinate, Energy Rust mount, and two inference docs. The historical oracle rationale line explicitly marked as kept verbatim retains its original source coordinate; it is not a live resolver. The only cross-scope live reference was a Stdio ZIP documentation comparison, which the Hub lane changed exactly from `🔋️model/🗃entries` to `🔋️model/🗃️entries`.

## Verification

The final scoped audit covers the same 333 governed entries with every category at zero. SHA-256 before/after comparison is exact for all seven moved authored files: four option markers, the payload schema, and both Rust/TypeScript inference files. Ajv compiles the moved Draft-07 schema and an independent check confirms the descriptor semantic kind, physical schema and oracle mutation row agree. The current Nx route `@semio-tech/energy-js:test-quick` passes.

The exact central contribution override required is `✏️s/🔌️plugins/🔋️energy/🗿️artifacts/🔋️model/🏅️standards/🔖️1/🪆️subsets/✳️any` → `🔮️oracle`. No shared root, central taxonomy, compatibility alias, generated rename plan or Git state was changed.
