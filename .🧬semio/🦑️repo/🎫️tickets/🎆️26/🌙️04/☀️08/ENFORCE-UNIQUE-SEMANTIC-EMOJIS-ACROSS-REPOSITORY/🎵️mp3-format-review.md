# MP3 Format Hand Review

The initial inventory contains 239 entries and 158 files. Current-byte hashes were recorded in `🗑️generated/mp3-before.json` before any move. This review is not a whole-workspace completion claim.

Twenty moves are individually selected from their actual contents:

- `🔮️oracle` separates the independent ID3/frame reader from `🧪️tests`.
- Each viewer/editor window uses `☑️options`, distinct from its `🎚️config` sibling.
- Three fixture owners are `🎼️set-frames` for the MPEG frame sequence, `🔖️set-id3v1` for the fixed trailer, and `🏷️set-id3v2` for the extensible tag. Their six states are `⬅️before.json` and `➡️after.json`.
- The three corresponding mutation owners retain those same meaningful identities with canonical presentation.
- A full-state replacement is `📸️set-snapshot`, with a distinct `🧬️.schema.json` beside its descriptor. Its real title-tag test is `🏷️retitles-the-id3v2-tit2-frame`, replacing an unrelated panda.
- The mutation suite is `🎛️mutate-mp3-mpeg1-layer3`, replacing an unrelated fox. The duration inference retains its stopwatch as `⏱️duration`.

All current includes, descriptor owners and fixture coordinates must follow these exact names. Frozen source-mutation evidence remains unchanged. The shared encoded MP3 and all fixture values remain untouched. The oracle's existing five-parent feature include does not resolve from its physical source; the corrected one-parent include will be checked against the real feature file without changing any assertion.

All twenty manual moves are complete. The final physical census has the same 239 entries and 158 files, with zero naming findings, unknown directory roles or taxonomy errors. All 148 files not requiring explicit coordinates/comments retain their exact original hashes; the ten changed files are enumerated in `🗑️generated/mp3-final-audit-verified.json`.

The normal Nx fixture verifier passes all three pairs with zero file problems. All six byte counts and SHA-256 values remain unchanged. The moved snapshot JSON Schema compiles with independent Ajv. An initial local diagnostic compared a bare SHA digest against the manifest's `sha256:`-prefixed value; its false mismatch output is retained with the explicit correction in the verified diagnostic, not concealed by changing fixture metadata.

The source aggregate's four leaf mounts, case mount and duration mount resolve current names, as do all four descriptor owners and the shared oracle mount. The catalog's current snapshot/scenario fields follow the handpicked paths while `sourceMutationDirectoryName` remains frozen evidence. The exact oracle feature include now resolves one parent away. A sibling WAV documentation reference was corrected to the actual MP3 suite. No assertion, sample value or wire identity was changed.

The separate end-to-end task's in-flight compilation observed old MP3 leaf spans while these moves occurred. It was notified when the complete production source/mount/descriptor batch was ready; no fallback directories or derive bypass were introduced. This review does not claim native MP3 test success. The three pre-existing non-snapshot payload-schema pointers still refer to absent files; they remain an explicit unresolved ownership issue.

The focused naming suite now passes 28 tests and 900 assertions through Nx, including the new language-neutral tag/frame/snapshot sibling case and the independent emoji-regex/Ajv checks. Both unchanged encoded MP3 assets were opened by FFmpeg's `ffprobe`: MPEG audio, 44.1 kHz, mono; the shared specimen is 193,275 bytes/12 seconds and the bundled example is 1,725 bytes/0.104250 seconds. Diagnostics are `mp3-naming-tests.txt`, `mp3-fixture-ffprobe.json` and `mp3-demo-ffprobe.json`. The native Stdio benchmark target also passed Cargo check; no native MP3 test assertion is claimed from that compile-only result.
