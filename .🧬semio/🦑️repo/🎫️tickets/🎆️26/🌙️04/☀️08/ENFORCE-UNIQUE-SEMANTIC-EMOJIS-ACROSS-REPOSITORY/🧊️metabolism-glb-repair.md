# Metabolism GLB Identity Review

Scope: the 90 GLBs under `🧰️framework/🔨️modules/🖼️assets/🌱️metabolism/🎨️representation`, their public asset identity, source delivery, and direct consumers. The parent separately moved 156 CAD files into explicit family/shape scopes. All 90 GLBs are now moved to their individually chosen catalog destinations, with every SHA-256 preserved. No authored DSL URL or generated descriptor/hash edit has occurred.

## Source Authority

Puzzle's `🔣️.json` is the canonical descriptor emitter's readable mirror, paired with `🛂️.descriptor.semio` and component/core/descriptor hashes. Do not handpatch either output. Its seven `manifest.examples` entries originate in each example's Rust `ExampleSource`, whose `document_json()` parses the authored DSL and serializes the resulting projection. Puzzle's `describe` task builds the actual WASI component and runs the canonical emitter. The separate platform task confirmed it does not own Puzzle example DSL or descriptor emission.

Three authored Puzzle DSL files repeat mesh URLs in both kind catalogs and placed objects: 3D tower 192 URL-bearing lines, 5D tower 180, and 5D dream 2,880. Those lines contain approximately 2.59 MB of retained semantic data. Dream has malformed stacked URL identities, including `/mesh/🧊️cylindric-🧊️capital.glb`; these are corruption, not names to admit as aliases.

The parent approved a hand-authored 90-row representation catalog separating stable public URL identity from physical source path, analogous to icon IDs. Source paths will be explicit family/shape entries, never derived from an emoji or basename heuristic. Static output must retain those nested physical identities; both browser and native delivery need the same authority. The existing `/mesh` collection also includes Abbau-Aufbau and a placeholder, so authority composition must reject unknown IDs without hijacking those other declared sources.

## Independently Completed Copier Repair

The prior static mesh copier read only root-level GLBs. The existing HTTP middleware already resolved nested paths, producing a dev/static mismatch. A language-neutral fixture at `ui/🎨️styling/🧪️tests/🧊️mesh-collection.json` declares two source roots, overlapping exact paths, identical basenames in different directories, a non-delivered CAD file, and a placeholder. Existing third-party `fast-glob` independently enumerates the expected path/byte map.

The regression runs the actual HTTP middleware and static build plugin. Before implementation, it passed HTTP checks and failed because all four nested output files were missing. After the precise recursive copier repair, the Nx-exec Bun suite passed all 33 tests with 602 assertions. It preserves relative paths, first-root precedence, and source bytes; symlink entries are not traversed. There are no old-name aliases or flattened output duplicates.

The existing separate in-source Vitest quick target again failed to produce useful test results within its budget, matching the earlier styling report. The direct Bun suite is the successful result, not that Vitest target. An initial Nx-exec attempt with the long ticket TMPDIR failed Unix socket-length validation before tests; setting TMPDIR only for the child Bun test correctly keeps fixture files in this ticket while leaving Nx's socket handling unchanged.

## Preflight Evidence

Read-only Puzzle `cargo check -p semio-s-plugin-puzzle --target wasm32-wasip2` completed through Nx with a new isolated ticket cache: exit 0, 8m32s. This confirms the current WASI source graph checks; actual descriptor emission has not yet run.

Exact malformed dream URL inventory: 16 distinct names, 1,408 total source occurrences. One cylindric capital, six ellipsoid capsules, three cylindric tambours, and six trapezoid capsules have an extra inner `🧊️` before `capital`, `capsule`, or `tambour`. No such aliases are registered. This inventory preceded all GLB moves; the DSL values remain unchanged.

## Retained Corruption Evidence

All rows below occur in the authored 5D capsule-dream DSL. Each intended identity is independently present among the 90 actual GLBs and in the new hand-authored representation catalog. This table is input evidence, not an executable replacement specification. The 1,408 semantic values remain unchanged while delivery is repaired; the parent owns the constrained source-correction decision and subsequent canonical descriptor/hash emission.

| Corrupt public URL | Intended existing public URL | Occurrences |
|---|---|---:|
| `/mesh/🧊️cylindric-🧊️capital.glb` | `/mesh/🧊️cylindric-capital.glb` | 16 |
| `/mesh/🧊️ellipsoid-🧊️capsule_backslash.glb` | `/mesh/🧊️ellipsoid-capsule_backslash.glb` | 124 |
| `/mesh/🧊️ellipsoid-🧊️capsule_L.glb` | `/mesh/🧊️ellipsoid-capsule_L.glb` | 104 |
| `/mesh/🧊️ellipsoid-🧊️capsule_J.glb` | `/mesh/🧊️ellipsoid-capsule_J.glb` | 132 |
| `/mesh/🧊️ellipsoid-🧊️capsule_slash.glb` | `/mesh/🧊️ellipsoid-capsule_slash.glb` | 160 |
| `/mesh/🧊️ellipsoid-🧊️capsule_s.glb` | `/mesh/🧊️ellipsoid-capsule_s.glb` | 52 |
| `/mesh/🧊️ellipsoid-🧊️capsule_p.glb` | `/mesh/🧊️ellipsoid-capsule_p.glb` | 44 |
| `/mesh/🧊️cylindric-🧊️tambour_first-storey.glb` | `/mesh/🧊️cylindric-tambour_first-storey.glb` | 16 |
| `/mesh/🧊️cylindric-🧊️tambour.glb` | `/mesh/🧊️cylindric-tambour.glb` | 128 |
| `/mesh/🧊️cylindric-🧊️tambour_last-storey.glb` | `/mesh/🧊️cylindric-tambour_last-storey.glb` | 16 |
| `/mesh/🧊️trapezoid-🧊️capsule_backslash.glb` | `/mesh/🧊️trapezoid-capsule_backslash.glb` | 124 |
| `/mesh/🧊️trapezoid-🧊️capsule_L.glb` | `/mesh/🧊️trapezoid-capsule_L.glb` | 104 |
| `/mesh/🧊️trapezoid-🧊️capsule_J.glb` | `/mesh/🧊️trapezoid-capsule_J.glb` | 132 |
| `/mesh/🧊️trapezoid-🧊️capsule_slash.glb` | `/mesh/🧊️trapezoid-capsule_slash.glb` | 160 |
| `/mesh/🧊️trapezoid-🧊️capsule_s.glb` | `/mesh/🧊️trapezoid-capsule_s.glb` | 52 |
| `/mesh/🧊️trapezoid-🧊️capsule_p.glb` | `/mesh/🧊️trapezoid-capsule_p.glb` | 44 |

## Handpicked Physical Authority

`representation/📇️catalog.json` contains all 90 individually written public-URL/source-path decisions. It preserves the existing ASCII stems and case, separates nine semantic families and their existing handpicked shapes/storey variants, gives each render mesh `🧊️`, and each physics collision mesh `💥️` beside the existing CAD/collider leaves. The schema is `representation/🧬️catalog.schema.json`. All 90 destinations were materialized using 90 exact, individually written non-overwriting moves. The read-only before/after bijection verified every original SHA-256, every destination, and zero remaining flat GLBs.

## Delivery Cutover and Verification

`assets/🥽️mesh/📇️catalog.json` is the current composition authority: the explicit 90-row Metabolism catalog, two individually declared Abbau-Aufbau rows, and the explicit placeholder. Each entry has a public URL identity, exact repository source, and exact nested output path. Left/right forest outputs use `👈️`/`👉️`, separating their sibling meanings; public URLs retain their existing values. No physical source name is inferred from a URL, no malformed URL is admitted, and no old-name file alias is emitted.

Paired `🥽️mesh/🟦️.ts` and `🥽️mesh/🦀️.rs` resolvers validate the catalog. The neutral fixture and independent Ajv/serde checks cover output mapping, unknown IDs, duplicate IDs/sources/destinations, traversal, unknown fields, and absent source catalogs. TypeScript: 2 tests, 27 assertions. Rust: 2 tests. The first TypeScript run failed because the resolver did not exist; the completed implementation passed. The catalogue-driven copier regression also failed before its implementation and passed afterward.

Four authored Cargo asset metadata rows now name the catalog rather than roots/placeholder/inert filtering: Shooting, Puzzle, Block, and Demonstrator. The registry's scoped asset-row parser/emitter/validation consumes this one field and rejects unrelated mesh-row fields. Normal registry generation succeeded (59 plugin crates, 60 playgrounds, 45 framework packages), and its generated catalog/launch freshness check passed.

The exact fetch boundaries are World3dHost's two GLTF loader calls, WGPU's browser frame-worker fetch, and `native_renderer_asset_path`. Non-mesh URL namespaces retain their current owners. Two direct mesh-engine `include_bytes!` fixtures now point to the moved capsule-J source.

The complete styling Bun suite passed 34 tests / 979 assertions. Its actual dev HTTP middleware and static build plugin delivered all 93 current public mesh IDs with source-identical SHA-256 values, exact model MIME type, and exactly 93 nested static files. Unknown IDs and a non-catalog CAD request were rejected. A standalone compiled harness extracted the actual native path function unchanged, set the real WGPU Cargo package directory, and resolved every one of the same 93 IDs to its existing canonical repository source. That harness passed 3 tests, including the two Rust catalog tests.

The central WGPU closed-input contract now includes exactly the three mesh runtime inputs and five new deployment/extension validator inputs from the neighboring agent. Canonical generation succeeded with six exact outputs / one changed bundle; canonical check then reported six outputs / zero changed. Direct frame-worker generation and browser-worker freshness check succeeded. Current naming/semantic-role audit: 309 entries across representation and mesh authority, zero emoji-statute findings and zero unresolved directory roles.

Downstream checks: framework kernel quick 49 tests, framework package quick 88 tests, renderer quick 4 tests, and mesh-engine native 35 tests passed. The renderer exhaustive target subsequently finished within its bounded verification window: 7 files / 747 tests passed in 878.27 seconds; its timeout was not changed and no cancellation was needed. The mesh-engine suite decodes the real moved capsule-J GLB and compares it with the independent `gltf` crate oracle. Scoped `git diff --check` passed; no modifying Git command was used.

After the neighboring vendor owner confirmed its physical cutover, the kernel's exact default font-fetch path was refreshed to `🪞️vendor/🔤️guestslim-typst-fonts.bin`. Canonical WGPU generation then passed with six artifacts / one changed bundle, followed by canonical check with six artifacts / zero changed. Direct frame-worker generation and browser-worker freshness both passed again. These latest worker checks include the final vendor filename; the earlier exhaustive renderer run predates that last URL-only change.

## Explicit Remaining Limit

The 1,408 corrupted DSL URL occurrences listed above remain unchanged and are rejected as unknown IDs. This is not a completed semantic-data repair. The parent owns the constrained correction and canonical Puzzle descriptor/hash regeneration.

Native source-path resolution is verified, not full native streaming/rendering. The preexisting `push_renderer_asset_page` implementation returns `native renderer asset page requires a zero-copy retained-page handoff that is not mounted` for populated payloads. That unrelated platform handoff was not altered or claimed working. Initial standalone Rust attempts used the workspace's split-metadata artifacts and failed before linking; an isolated dependency build with embedded metadata enabled allowed the actual tests to compile and pass without changing workspace Cargo configuration.
