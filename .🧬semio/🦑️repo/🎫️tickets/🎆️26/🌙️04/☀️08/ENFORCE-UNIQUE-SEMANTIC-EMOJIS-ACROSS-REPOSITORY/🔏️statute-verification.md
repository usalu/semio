# Naming Statute Verification

The permanent contract requires exactly one meaningful, handpicked emoji for every nonreserved file or directory, unique in the combined file-and-directory sibling namespace. README, LICENSE and exact tool-owned filenames remain literal. Statute metadata explicitly disables automatic correction and requires updating only resolved path references. The retired ticket mutation utility now refuses every command except read-only audit.

The final whole-workspace read-only checkpoint covers 51,828 files, 37,893 directories and 88,437 governed entries. Missing, generic, presentation, spacing, duplicate, multiple, reserved-emoji and independent-oracle counts are all zero. This fresh final rerun includes new concurrent files and supersedes the earlier 88,427-entry clean checkpoint. An intervening census caught GIS's inference/mutation map collision; its measured-extents fixture was individually renamed `📐️infer-gismap-1` before both clean reruns. This is a naming checkpoint, not a claim that all runtime code passes. Meaning was reviewed manually in the per-tree reports; the mechanical generic blacklist alone cannot prove meaning.

The census enumerates tracked and nonignored untracked paths that physically exist, together with their directory ancestors. Fixed tool contracts, declared exclusions, and reserved metadata/dependency subtrees retain their separate scope; Git's absent historical paths are not treated as live files. Scope-specific physical reviews additionally cover the affected materialized mirrors, including the PDF owners that a renamed tracked-only pathspec could miss. This is not a claim about arbitrary ignored build output or third-party dependency contents.

Fresh uncached TypeScript/Bun statute verification after the final catalog updates and five added neutral vectors passes 38 tests and 1,356 assertions (17.10 seconds). The independent leading-grapheme suite passes nine tests and 33,713 assertions over 5,375 current catalog members (2.97 seconds), comparing the owned parser with existing Unicode/library oracles. These supersede the earlier successful runs and include the last 147 registered handpicked test names.

The actual Go CLI shared-fixture target initially rejected four otherwise valid before/after vectors because its existing rune recognizer omitted the U+2B05..U+2B07 directional-arrow range. The exact range is now included; no expected vector changed. The fresh focused `@semio-tech/repo-client:test` run, using the supported long-level environment budget, passes `TestPathEmojiStatutesLanguageNeutralFixture` (0.671 seconds). A cold compile exceeded the 15-second fundamental budget before the long rerun. `gofmt -l` reports no formatting change. The separate `repo-go-lib:test-quick` target reports no test files and is explicitly not counted as Go verification.

All commands use existing Nx targets and existing `📜️script.ts` routers. There is no new automatic emoji selector, palette, migration script, runtime library dependency or Git mutation.

Five additional neutral vectors exercise country flags, keycaps, a rejected two-flag stack, plain digits versus keycap presentation, and a text-default directional arrow. The owned TypeScript classifier and installed independent emoji-regex oracle agree. Actual Go red runs reproduced the missing flag/keycap support and omitted presentation findings. Its existing recognizer now consumes one regional-indicator pair or one complete keycap, retains ordinary digits as nonemoji, and requires the selector on keycaps/text-default arrows. No expected outcome was weakened and no external runtime dependency was added. The final uncached Go CLI run passes the complete shared fixture in 0.440 seconds. The final TypeScript/Bun suite above exercises the same five additions.

Four additional existing Go regression functions were executed individually through the same uncached Nx target: FileKindEmoji (1.034 seconds), DefinitionKindTestEmoji (1.369 seconds), SessionKindEmoji (0.453 seconds) and EmojiVariationAutofix (final 0.490 seconds) all pass. A combined-filter invocation was rejected by Nx's shell argument forwarding before Go started; it is not counted as a test run. EmojiVariationAutofix initially exposed four contradictory expected literals already present in read-only HEAD: its “strips VS16” cases expected the selector to remain. Those four exact expected strings now explicitly encode their unqualified emoji code points; its mislabeled VS15 input now actually contains U+FE0E. Runtime stripping behavior was not changed. Independent Unicode Emoji_Presentation and installed emoji-regex checks validate all four base identities. The six-subtest rerun passes, and gofmt plus the scoped diff whitespace check are clean. Nx's historical flaky label follows the intentional red/green runs; no automatic retry was used.
