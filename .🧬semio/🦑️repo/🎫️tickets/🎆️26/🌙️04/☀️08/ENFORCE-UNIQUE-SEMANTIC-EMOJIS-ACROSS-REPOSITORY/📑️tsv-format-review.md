# Tab-Separated Table Hand Review

The physical TSV artifact baseline contains 263 entries/173 files and 22 strict naming findings, retained with every file's SHA-256 in `🗑️generated/tsv-before.json`. The current `📑️tsv` root is distinct among the Stdio artifact siblings and identifies tabulated records; no root move is proposed.

The first bounded handpicked batch is 23 moves: two window `☑️options` facets distinct from their `🎚️config`; `🔮️oracle` distinct from `🧪️tests`; six fixture groups (`📥️insert-row`, `🗑️remove-row`, `🔲️set-cell`, `📸️set-snapshot`, `🔚️set-line-ending`, `↩️set-trailing-newline`), each with `⬅️before.json`/`➡️after.json`; `♻️reuse-marketplaces.tsv` for the real European building-component reuse survey; and `📊️mutate-tsv-iana` for the table-mutation suite. The table contents and all fixture hashes must remain unchanged. Incoming evidence is `tsv-incoming-review.txt`.

The arbitrary brown `🟤️set-snapshot` production leaf will require a separate exact move to `📸️set-snapshot`, its actual descriptor and module references, and the existing scenario directory name must be reflected accurately in the catalog. That production mutation change is held until the current Stdio native qualification has loaded/finished; no production path is moved during an active dependent compilation.

All six TSV mutation descriptors and the secondary oracle declarations currently point to absent `🔣️.schema.json` payload files. This is a real pre-existing source-contract problem, not resolved by naming checks, fixture-byte verification, or a passing current catalog count. The aggregate schema and actual leaf payload types are being reviewed before selecting a proper owner-relative schema authority; no permissive replacement schema or validation bypass is authorized.

## First Bounded Batch

All 23 planned non-production-leaf moves are complete. The before/after audit accounts for all 173 files: 169 complete-file hashes are unchanged; the four changed files are the oracle catalog, its documentation, and the suite's feature/adapter fixture coordinates. The catalog's already-stale scenario name now matches the existing physical `✏️renames-the-alpha-row-and-switches-to-crlf`. The exact shared oracle mount and owner-specific discovery override select the current oracle directory. The suite's mutable copy label is `📥️input.tsv`, and its returned path is still used directly.

The normal uncached fixture verifier passes all six fixture pairs with zero file problems (`tsv-fixture-verify.txt`). The strict 263-entry naming audit has zero findings; this does not validate the semantic suitability of the still-brown snapshot leaf, whose separate move remains open. Exact fixture, test and existing row-operation roles were registered; the new neutral fixture covers seven distinct sibling identities and a before/after pair. The first combined byte/role diagnostic process exited 143 before output; it is not passing evidence. Separate completed diagnostics are `tsv-first-batch-bytes.json` and `tsv-first-batch-naming-resolved.json`.

The expanded shared naming test run completed with 30 passes and two timeout failures: the existing normalization-reader test exceeded 30 seconds, and the existing taxonomy identity test exceeded its default timeout. Native compilation was concurrent; the cause of the timing change is not established. No timeout or assertion was relaxed. The previously completed TXT-era 32-test pass must not be represented as a fresh full TSV-era pass; focused unchanged-gate retries and a final run remain required. All six absent payload schemas and the production snapshot identity remain open.

The unchanged focused retry now passes both timeout cases (37 assertions): normalization reader 3.60 seconds and taxonomy identity 0.80 seconds, recorded in `tsv-reader-identity-retry.txt`. The prior reader failure was waiting inside Git's untracked enumeration, not an assertion identifying a bad reserved-name contract. A bounded read-only Git probe measured the literal README scope at 81 ms, with the existing compose exclusion globs at 3.04 seconds; no inventory implementation, assertion, or timeout was changed. A fresh complete naming run is still required.

The missing payload files have an additional contract discrepancy: aggregate JSON declares camelCase payload keys, while current `SetCell`, `SetLineEnding`, and `SetTrailingNewline` leaf value derives have no casing declaration and therefore use snake_case Rust field names. The snapshot itself explicitly uses camelCase and Rust defaults that its JSON schema currently requires. These are recorded separately from naming repair; no speculative schema or payload behavior change has been made.

The full unchanged shared naming suite now passes all 32 tests and 1,076 assertions in 12.05 seconds (`tsv-naming-current-retry.txt`). This replaces only the pending full-run qualification, not the native or missing-schema findings. No assertion or timeout was modified to obtain this result.

## Snapshot Identity

After the dependent Stdio native qualification finished, the single production owner was moved to `📸️set-snapshot`. Both exact Rust module paths, the descriptor's owner/emoji, and both oracle source directory fields were updated. The stale mutation-catalog documentation now names the actual oracle. All 14 moved files are accounted for: 13 full-file hashes unchanged, with only the descriptor's identity fields changed. Payload code, scenario bytes, mutation/inverse/diff bodies and assertions are unchanged (`tsv-snapshot-byte-audit.json`).

The canonical fixture verifier passes all six pairs after this move (`tsv-snapshot-fixture-verify.txt`). The added language-neutral mutation-sibling case passes with the independent emoji/Ajv checks: two focused tests, 195 assertions (`tsv-snapshot-neutral-tests.txt`). The absent payload-schema and casing discrepancies remain explicitly unresolved; this naming repair does not introduce a schema bypass or claim native execution.

## Payload Schema Authority

The six dangling descriptor authorities are now real owner-local `🧬️.schema.json` files, distinct from each sibling descriptor's `🔣️.json`. Their Draft-07 contracts cover insert/remove row, set cell, set snapshot, set line ending and set trailing newline with closed payload objects. All six descriptor fields and all six oracle-manifest declarations name the same exact schema leaf. The three multiword payload fields are camel-case in both schema and Rust value derivation; the same explicit derive policy is present on all six leaf records for one consistent boundary.

A new repository language-neutral contract first failed on the former `🔣️.schema.json` declaration. After implementation it validates six accepted and six rejected payloads with Ajv, applies the repository's independent Draft-07 authority checks, verifies the six Rust casing declarations and checks the oracle roster. The first complete post-change run passed 34 tests and 1,211 assertions. A later run again passed this TSV contract, but the overall suite failed in an unrelated taxonomy loader while the separately assigned ZWJ path cutover had physically moved OS/Renderer owners before their generator-contract coordinates; that concurrent run is not a clean-suite claim. Thirteen changed JSON authorities parse successfully and no TSV reference to the absent `🔣️.schema.json` remains. Native Stdio compilation is deferred until Hub's active glTF owner cutover is coherent.
