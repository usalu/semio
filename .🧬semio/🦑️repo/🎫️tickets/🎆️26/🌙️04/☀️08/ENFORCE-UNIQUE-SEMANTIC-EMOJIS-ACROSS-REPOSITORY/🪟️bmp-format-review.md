# Windows Bitmap Hand Review

BMP and TIFF currently share `🖼️` in all seven physical artifact sibling namespaces. The BMP tree is specifically the Windows Bitmap v3 format: its snapshot models the bitmap header, bottom-up or top-down row order, palette entries and canonical RGBA pixel bytes. The handpicked identity is therefore `🪟️bmp`, using the window identity for Windows Bitmap rather than the generic image identity. TIFF remains unchanged in this batch and will receive its own tagged-format review.

The physical BMP owners are the main Stdio artifact, paired Semio image carriers, paired Raster carriers and paired Shooting carriers. `🪟️bmp` is absent from every affected file-and-directory sibling namespace. Each owner will be moved explicitly with no generated rename plan, and only exact BMP source-authority references will change. Internal BMP operations, tests, fixtures and schemas remain a separate case-by-case review within the same bounded family.

## Verified Result

All seven physical owners now use `🪟️bmp`. The internal review selected distinct identities for the editor/viewer option owners, local oracle, direct-contract test, two suites, pixel-data operation, architectural sample, five generated fixture bundles, ten directional fixture files and five payload schemas. The standalone Rust codec entry is `src/🦀️.rs` with its Cargo path updated explicitly. No automatic name selector or bulk path replacement was used.

The current main BMP tree contains 382 entries and 232 files when the compiler-owned Cargo target is excluded. The full repository-context read-only statute audit reports zero findings under this owner. All 35 BMP Rust `#[path]` mounts in the Stdio package barrel and oracle host resolve, and no source path still contains the former `🖼️bmp` root.

Both registered generator routes compile and run through Bun/Nx. Their four image-rs mutation bundles plus the Pillow header bundle reproduced all ten committed BMP files byte-for-byte at the five handpicked fixture coordinates. Both manifest routes emitted the same coordinates and digests. The shared language-neutral statute/source-authority target passes 33 tests and 1,175 assertions. The artifact-filtered repository fixture verifier selected zero registered fixtures, so it is recorded as non-evidence rather than presented as a fixture pass. No full Stdio native codec pass is claimed.
