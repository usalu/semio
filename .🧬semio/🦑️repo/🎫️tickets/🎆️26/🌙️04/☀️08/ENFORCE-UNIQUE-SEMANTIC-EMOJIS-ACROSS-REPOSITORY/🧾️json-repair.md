# JSON Handpicked Emoji Repair

The Stdio artifact root is now `🧾️json`, representing structured records and avoiding the sibling inventory file's `🔣️` identity. Its RFC8259 subsets are `🧱️base` and `🛜️i-json` (Internet JSON). Local oracles use `🔮️` and window options `☑️`.

Base mutations and fixtures distinguish member editing `✏️`, scalar values `🔢️`, array insertion `📥️`, array removal `📤️`, and member removal `🗑️`. Payload schema files are `🧬️.schema.json` beside `🔣️.json` metadata. Internet JSON fixtures use insertion `📥️`, removal from arrays `📤️`, member removal `➖️`, renaming `🏷️`, safe number `🔢️`, whole snapshot `📸️`, string `🔤️`, top-level tree `🌳️`, and member insertion/update `➕️`. All carriers use `⬅️before.json` and `➡️after.json`.

Updated owned source and metadata, shared Stdio exports/registries/oracle mounts, exact fully-qualified external references, and the dependency index. Nested serializer folders named `🔣️json` are different coordinates and were preserved. The Base generator now authors the physical directory separately from semantic mutation IDs, supports the existing `SEMIO_FIXTURE_OUT` convention, and selects the Windows executable suffix.

Verification on 2026-09-05:

- Pre-parent-move scoped audit: 285 files, 172 directories, 451 governed entries; zero findings except the one subset collision resolved by the final parent moves.
- Fixture verifier: 14 fixtures, zero problems, exit 0.
- All 65 JSON files parse; all 14 manifests and 28 referenced files have exact recorded byte length and SHA-256.
- Nx Base generator completed and emitted the five selected fixture directories with ten files under the ticket generated folder. All ten regenerated files match their recorded SHA-256 and bytes exactly.
- Final renamed-root findings are covered by the root agent's full repository audit; central override paths were supplied to that agent.

The final scoped audit also counts the complete renamed tree (285 files, 172 directories, 451 governed entries), with every finding category zero. Both mutation suites now use `🔀️` rather than the arbitrary frog/wolf identities found during the semantic follow-up.
