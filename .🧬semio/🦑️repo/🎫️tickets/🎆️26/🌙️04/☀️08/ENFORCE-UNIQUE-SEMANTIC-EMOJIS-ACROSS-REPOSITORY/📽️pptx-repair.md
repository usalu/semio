# PPTX Emoji Repair

The artifact root is `📽️pptx`, distinct from sibling GIF. Its three subset folders are `🧱️base`, `🔒️strict`, and `🌉️transitional`. Oracles use `🔮️oracle`; options use `☑️options`. All mutation, suite and scenario choices were reviewed individually. Namespace, relationship, conformance, VML and alternate-content operations now describe those roles instead of carrying unrelated tool or arithmetic markers.

All 23 fixture folders have explicit handpicked names and paired `⬅️before.pptx` / `➡️after.pptx` carriers. Source imports, fixture catalogs, descriptor owner/emoji fields, scenario declarations, registry entries, MIME authority and central oracle overrides were updated. Existing ticket fixture-generation inputs now declare their exact output directory names and resolve the repository portably; no migration or emoji-assignment script was introduced.

Verification:

- Scoped audit: 349 files, 238 directories, 587 governed entries; all eight structural categories zero.
- Repository fixture verifier: 23 fixtures, zero problems.
- All 43 JSON documents parse; catalog source-mutation, mutation and scenario coordinates resolve.
- All 94 literal Rust include/path dependencies resolve, after repairing four over-deep fixture/feature paths. Shared OBJ, DXF and PPTX mounts were checked together; their three remaining PPTX oracle mounts were corrected.
- The existing Python generators ran only in this ticket's generated directory using python-pptx 1.0.2. All 46 generated carriers matched the original archives' 1,764 uncompressed OOXML members exactly; ZIP metadata was not rebaselined. The independent library also read all 15 Base presentation fixtures.
- Twelve stale descriptor emoji values were updated to their reviewed physical owners. Across PPTX, OBJ and DXF, all 63 inspected mutation descriptors now use the matching emoji identity.

No Git state or workspace fixture bytes were modified. Nested PPTX serializer directories elsewhere retain their separate identities. Workspace-wide completion remains pending the other trees and final audit.
