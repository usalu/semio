# Stereolithography Hand Review

The arbitrary purple square `🟪️stl` does not describe this triangle-mesh format. The handpicked identity is `🔺️stl`: its actual snapshot consists of triangles, with individual triangle insertion/removal, normal and vertex changes. Before moving each owner, its siblings must be checked for the triangle identity and its source bytes recorded. Format IDs, MIME, codec behavior and payload schemas remain unchanged.

Root owns the main Stdio artifact, two Semio mesh carriers, two Procedural generation3d carriers, and two CAD carriers. The Hub lane owns its GIS, FEM, Lowpoly, Process, Puzzle, Block and Remodel carriers plus their local documentation. The Kernel lane owns the two Sourcing carriers. Every move is literal and manually selected; the read-only incoming review (`stl-incoming-review.txt`) does not select names or mutate files.

The STL main artifact also contains unresolved internal naming and source-contract issues, to be reviewed after the root identity cutover. Root identity consistency alone is not a clean-tree or runtime-success claim.

## Internal Choices

The next 24 exact moves choose two `☑️options` facets; `🔮️oracle`; `🔺️mutate-stl-ascii` and `🔁️create-and-round-trip-stl`; `🏛️hexagonal-cut-concrete-forest-left` for the real architectural mesh; five fixture groups (`📥️insert-triangle`, `🗑️remove-triangle`, `🏷️set-solid-name`, `🧭️set-triangle-normal`, `📐️set-triangle-vertices`) and ten `⬅️before.json`/`➡️after.json` files; `📸️set-snapshot`; its `🧬️.schema.json` distinct from descriptor JSON; and the missing emoji-presentation selector on the existing `🏷️set-solid-name` production leaf. The existing bounding-box `📦bounds` identity was inspected against its actual min/max triangle-soup extent calculation and remains a specific box meaning, not an arbitrary package assignment.

The checked-in demo `🧊️.stl` contains only a text placeholder, and the codec-retention test already falls back to a synthetic snapshot. Its stale physical path was corrected without changing that pre-existing test behavior. The real 958-triangle architectural fixture is separate and remains byte-preserved. Five payload-schema files are absent; the existing snapshot leaf's flat schema describes bare snapshot data, not its wrapper payload. Renaming that file preserves its exact schema bytes and does not claim to solve the contract mismatch.

## Verified Result

The complete family cutover accounts for 29 original physical owners and two pre-existing empty FEM import scaffolds. The root lane moved seven owners, Kernel moved two Sourcing owners, and Hub moved 22 owners including the two empty scaffolds. Across the original 230 files, 217 full SHA-256 values are unchanged and 13 files contain only the listed identity/reference corrections (`stl-family-final-audit.json`). The new triangle identity was absent from every affected sibling namespace before the moves.

All 56 exact Rust `#[path]` mounts across 17 source files resolve (`stl-family-mounts.json`). No non-ticket source reference to `🟪️stl` remains. The main tree has 283 entries/176 files, zero strict naming findings and zero unknown semantic-directory roles (`stl-final-naming-resolved.json`). The fresh canonical verifier finds five fixture pairs and zero file problems (`stl-current-fixture-verify.txt`). The fresh shared naming suite passes all 32 tests and 1,120 assertions (`stl-naming-tests.txt`). These checks do not convert the placeholder demo into real STL or create the five absent payload schemas.
