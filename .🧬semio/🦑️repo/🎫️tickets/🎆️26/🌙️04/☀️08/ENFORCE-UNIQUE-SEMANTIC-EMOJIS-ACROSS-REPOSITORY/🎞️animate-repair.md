# Animate Emoji Repair

## Scope

Hand-reviewed every strict path-statute finding under `✏️s/🔌️plugins/🎞️animate`, including all editor command siblings, both tile-editor window configurations, retained-command fixtures, mutation presentation, direct payload schemas, and both oracle owners.

## Result

- Strict audit: 334 files, 278 directories, 605 governed entries.
- Findings: zero missing, generic, presentation, spacing, duplicate, multiple-emoji, reserved-name, and oracle breaches.
- Central taxonomy validation: zero problems.
- TypeScript example/runtime tests: 2 passed, 0 failed.
- Exact stale path-reference scan and mojibake scan: zero findings.
- Rust compilation remains to be rerun after the concurrent Stdio brep repair settles; no passing Rust result is claimed.

## Handpicked identities

- Engagement commands: retained `⌨️engagement-input`; `📤️engagement-submit`.
- Tile commands: retained `🀄️add-tile`; `🚮️delete-selection`, `🗑️delete-tile`, `✂️patch-tile-crops`, and `🏷️rename-tiles`.
- Grid commands: `🧹️clear-tiles`, `↩️reset-grid`, and `🌱️seed-grid`.
- Prompt and media commands: `📋️copy-prompt` and `🎥️export-video-from-deck`.
- Interaction commands: `👇️canvas-pointer-down`, `⏸️no-operation`, and `🗣️set-locale`.
- Selection commands: `🎬️set-active-example`, retained `🖼️set-frame`, and `📥️set-source`.
- Window configuration: `⚙️config`, paired with retained `🎚️options`.
- Retained-command schema: `🧬️.schema.json`, paired with retained `🔣️.json`.
- Mutation directories retain distinct semantic identities with normalized emoji presentation; every direct payload schema is `🧬️.schema.json`.
- Oracle owners: `🔮️oracle` at artifact and subset levels.
