{
  "name": "Kit Store Architecture (Contracts-First, Multi-Backbone, Pointer-Based RS Core)",
  "description": "Replace incomplete kit persistence with one root snapshot per kit graph, ordered semantic changes, async semio/rs core, dev JSON and local .semio/ backbones, GraphQL IPC, semio/js facade with RxJS, semio/react useSyncExternalStore hooks, and sketchpad integration.",
  "branchName": "ralph/kit-store-architecture-contracts-multi-backbone",
  "userStories": [
    {
      "id": "US-001",
      "title": "Align Kit Asset Contracts With Snapshot + Change History Model",
      "description": "As a contributor, I want the canonical kit asset shapes under semio/assets/semio (including patterns exemplified by semio/assets/semio/metabolism.new.kit.semio.json) to match the target architecture so contracts and fixtures are unambiguous for codegen, tests, and GraphQL mapping.",
      "acceptanceCriteria": [
        "Document and encode the intended kit graph model as one root snapshot + ordered semantic changes, consistent with how checkpoints/drafts/transactions reference history (same ordered op persistence model, differing by metadata/lifecycle).",
        "Update or replace incompatible example assets/schemas in semio/assets/semio needed for the new model (greenfield replacement, no legacy compatibility requirement).",
        "Provide at least one minimal golden fixture pair: (persisted op log inputs) → (expected derived snapshot hash or structural invariant checks) suitable for automated testing (exact mechanism chosen in RS tests).",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes."
      ],
      "priority": 1,
      "passes": true,
      "notes": "",
      "dependsOn": [],
      "labels": [
        "assets",
        "contracts",
        "fixtures"
      ],
      "completionNotes": "Completed by agent"
    },
    {
      "id": "US-002",
      "title": "Finalize semio/graphql Contract For Kits, Changes, Diffs, Connections, Operations, And Backbone Lifecycle",
      "description": "As an integrator, I want semio/graphql/schema.graphql to fully specify the kit store surface (Relay-style patterns already present: owners/children/hashes) so semio/rs can implement a single GraphQL target without ad hoc gaps.",
      "acceptanceCriteria": [
        "GraphQL supports querying readable states broadly (not one global active kit): selectors/parameters explicitly identify which readable state/graph projection is queried.",
        "GraphQL supports initiating writes against explicitly identified writable states (draft/transaction flows), producing async-command style outcomes consistent with existing patterns like Command / commandSucceeded already present in the schema direction.",
        "Schema covers snapshots, diffs, connections, semantic operations, ordered change sequences, and checkpoint/draft/transaction linkage consistent with the asset model.",
        "Schema includes backbone operations needed for this epic: attach, detach, and capability discovery sufficient for dev JSON vs local .semio/ backends (without exposing backbone-internal SQL details to clients).",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes."
      ],
      "priority": 2,
      "passes": true,
      "notes": "",
      "dependsOn": [
        "US-001"
      ],
      "labels": [
        "graphql",
        "contracts",
        "ipc"
      ],
      "completionNotes": "Completed by agent"
    },
    {
      "id": "US-003",
      "title": "Implement semio/rs Core Kit Graph Engine (Async, Pointer-Internal, Multi-State)",
      "description": "As a library author, I want semio/rs to represent kit graphs internally with stable pointers/handles (not ID-string resolution as the primary internal mechanism) and to allow reads on any readable state and writes on any writable state, so hosts can implement complex parallel editing workflows safely.",
      "acceptanceCriteria": [
        "Internal graph resolution uses pointer/handle-based referencing for hot paths; any externally-facing IDs are translated at GraphQL/IPC boundaries only.",
        "Applying a semantic operation computes a kit diff deterministically from operation + input (diff not persisted).",
        "Core APIs are async for operations that may touch execution queues, IO readiness, or backbone-mediated persistence scheduling.",
        "Remove or rewrite incompatible surfaces aggressively per greenfield rule (no legacy API parity).",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes."
      ],
      "priority": 3,
      "passes": true,
      "notes": "",
      "dependsOn": [
        "US-002"
      ],
      "labels": [
        "rust",
        "core",
        "async"
      ],
      "completionNotes": "Completed by agent"
    },
    {
      "id": "US-004",
      "title": "Implement Attachable Backbones — Dev JSON And Local .semio/ Multi-DB + Blobs",
      "description": "As a user/developer, I want two backbone implementations so kits can be developed locally with a single file while still supporting a realistic on-disk layout for local workspaces.",
      "acceptanceCriteria": [
        "Dev backbone persists and replays operation + input records from one JSON file (atomic rewrite strategy documented; crash safety expectations documented).",
        "Local backbone persists under .semio/ using separate SQLite databases: wip.db, staged.db, authoritative.db, conflicts.db, plus blobs/BLOBHASH.EXT for referenced payloads.",
        "Backbones can be attached/detached at runtime without restarting sketchpad in dev workflows (detach semantics define what remains cached vs cleared).",
        "Replay from persisted ops reproduces the same derived snapshot/diff behavior as in-memory application (golden test hooked to US-001 fixture).",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes."
      ],
      "priority": 4,
      "passes": false,
      "notes": "",
      "dependsOn": [
        "US-003"
      ],
      "labels": [
        "rust",
        "persistence",
        "backbone"
      ]
    },
    {
      "id": "US-005",
      "title": "Implement GraphQL Target In semio/rs Matching semio/graphql/schema.graphql",
      "description": "As a host integrator, I want semio/rs to implement the GraphQL target directly so all IPC semantics remain in schema, not bespoke channels.",
      "acceptanceCriteria": [
        "async-graphql schema/types implement the updated semio/graphql/schema.graphql surface required by US-002 for this epic’s scope.",
        "Computed fields (diffs, derived views, caches) resolve without requiring clients to persist diffs; caching semantics remain explicit (what is memoized, what invalidates).",
        "GraphQL export/build wiring remains consistent with pnpm exec nx build semio/graphql.",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes."
      ],
      "priority": 5,
      "passes": false,
      "notes": "",
      "dependsOn": [
        "US-002",
        "US-003",
        "US-004"
      ],
      "labels": [
        "rust",
        "graphql",
        "async-graphql"
      ]
    },
    {
      "id": "US-006",
      "title": "Rebuild semio/js Facade Over semio/rs Using RxJS Without JS-Layer Kit Caching",
      "description": "As an app developer, I want semio/js to expose clean OO APIs that hide RS’s internal architecture while preserving correct async behavior.",
      "acceptanceCriteria": [
        "Public JS API does not introduce a second source of truth for kit graphs (no kit state machine duplicated in JS beyond ergonomic wrappers).",
        "Uses RxJS internally for eventing/streams where appropriate; external API remains conventional OO ergonomics (methods + objects), not Rx-first required for consumers.",
        "Extend existing embedded tests in semio/js (do not add new test files) to cover new facade behaviors related to subscriptions/progress/error channels as applicable.",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes."
      ],
      "priority": 6,
      "passes": false,
      "notes": "",
      "dependsOn": [
        "US-005"
      ],
      "labels": [
        "typescript",
        "js",
        "rxjs",
        "facade"
      ]
    },
    {
      "id": "US-007",
      "title": "Rebuild semio/react Hooks Over semio/js With useSyncExternalStore Kit Subscriptions And Tuple Hooks",
      "description": "As a UI developer, I want React bindings that avoid over-fetching and match the repo’s hook conventions.",
      "acceptanceCriteria": [
        "Hooks consume only semio/js (no direct RS imports from React code).",
        "Settable hooks follow [STATE, SETSTATE, STATUS] = use*(…) naming/shape where applicable.",
        "Kit state uses useSyncExternalStore wired to subscribe + snapshot callbacks provided by semio/js.",
        "Demonstrably granular updates: reading a narrow projection (example: a single piece flat plane) triggers React updates only when that projection’s backing RS signal reports an update (test strategy may use deterministic RS emitters + React testing utilities).",
        "Extend existing embedded tests in semio/react (do not add new test files).",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes."
      ],
      "priority": 7,
      "passes": false,
      "notes": "",
      "dependsOn": [
        "US-006"
      ],
      "labels": [
        "typescript",
        "react",
        "hooks",
        "useSyncExternalStore"
      ]
    },
    {
      "id": "US-008",
      "title": "Sketchpad Integration Demonstrating End-To-End Store Workflows",
      "description": "As a reviewer, I want semio/sketchpad to prove checkpoints/drafts/transactions workflows plus backbone attach/detach against real GraphQL/Relay-shaped usage patterns.",
      "acceptanceCriteria": [
        "Sketchpad demonstrates creating/navigating checkpoint/draft/transaction timelines using ordered semantic changes (UX minimal but real; avoid decorative mocks).",
        "Sketchpad demonstrates switching between dev JSON backbone and local .semio/ backbone (attach/detach), without violating layering rules.",
        "Extend existing sketchpad tests (Playwright embedded tests) rather than introducing new test harness files.",
        "Verify in browser using the dev-browser skill: at least one happy path and one failure/conflict path (e.g., conflict surfaced via conflicts.db semantics).",
        "Repository root defines pnpm typecheck and pnpm lint that validate all packages and files touched by this story (epic may introduce root pnpm workspace config and scripts until this is real).",
        "pnpm typecheck && pnpm lint passes when run from repo root.",
        "cargo test --manifest-path semio/rs/Cargo.toml passes.",
        "cargo clippy --manifest-path semio/rs/Cargo.toml --all-targets -- -D warnings passes.",
        "pnpm exec nx build semio/graphql passes.",
        "pnpm --filter @semio/js test passes.",
        "pnpm --filter @semio/react test passes.",
        "pnpm --filter @semio/sketchpad build passes."
      ],
      "priority": 8,
      "passes": false,
      "notes": "",
      "dependsOn": [
        "US-004",
        "US-007"
      ],
      "labels": [
        "sketchpad",
        "integration",
        "ui",
        "e2e"
      ]
    }
  ],
  "metadata": {
    "updatedAt": "2026-05-06T21:34:07.358Z"
  }
}