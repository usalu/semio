/** 🧩 energy-model 🗣️dsl WASM facade. */
export function parseDsl(text: string): unknown {
  throw new Error("wire energy-model 🗣️dsl parseDsl to plugin WASM");
}
export function printDsl(value: unknown): string {
  throw new Error("wire energy-model 🗣️dsl printDsl to plugin WASM");
}
