{
  "$id": "https://semio.tech/schema/s/energy/model/mutation.json",
  "title": "EnergyModelMutation",
  "oneOf": [
    { "$ref": "#/$defs/ReplaceModel" }
  ],
  "$defs": {
    "ReplaceModel": {
      "title": "ReplaceModel",
      "type": "object",
      "additionalProperties": false,
      "required": ["mutation", "newModelJson"],
      "properties": {
        "mutation": { "const": "replace-model" },
        "newModelJson": { "type": "string" }
      }
    }
  }
}
