/** 🧬️ EnergyModel artifact schema — every field with its state class. */

export interface EnergyModelArtifact {
  /** @state artifact */
  schema: string;
  /** @state artifact */
  modelJson: string;
  /** @state artifact */
  resultsJson: string;
}
