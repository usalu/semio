{
  "schema": "semio.energy.retained-wire-mutations/1",
  "source": "simulation/engine/sim/component.rs",
  "mutations": [
    { "id": "magic", "replace": "SMENERGY -> BADWIRE!", "mustFail": "header" },
    { "id": "version", "replace": "ENERGY_WIRE_VERSION -> ENERGY_WIRE_VERSION + 1", "mustFail": "version" },
    { "id": "kind", "replace": "Checkpoint -> Preview", "mustFail": "kind" },
    { "id": "operation", "replace": "identity.operation -> identity.operation + 1", "mustFail": "identity" },
    { "id": "generation", "replace": "identity.generation -> identity.generation + 1", "mustFail": "stale-generation" },
    { "id": "items-max-plus-one", "replace": "declared_items -> declared_items + 1", "mustFail": "items" },
    { "id": "pages-max-plus-one", "replace": "declared_pages -> declared_pages + 1", "mustFail": "pages" },
    { "id": "operations-max-plus-one", "replace": "declared_operations -> 2", "mustFail": "operations" },
    { "id": "process-jobs-max-plus-one", "replace": "declared_jobs -> 2", "mustFail": "process-jobs" },
    { "id": "trailing-byte", "replace": "checkpoint_bytes -> checkpoint_bytes + 1", "mustFail": "trailing" },
    { "id": "fragment-fuel", "replace": "consume_fuel(1) -> consume_fuel(0)", "mustFail": "one-fragment-per-grant" },
    { "id": "page-fuel", "replace": "begin_staged_page -> begin_staged_page twice", "mustFail": "one-page-per-grant" },
    { "id": "preview-retirement", "replace": "retire-before-install -> overwrite", "mustFail": "one-owner-latest-wins" },
    { "id": "checkpoint-saturation", "replace": "return Err(packet) -> drop(packet)", "mustFail": "lossless-order-and-identity" },
    { "id": "queue-head-rotation", "replace": "take keeps head -> take advances head and retry appends tail", "mustFail": "same-owner-same-head-order" },
    { "id": "matching-packet-injection", "replace": "retry provenance lease -> accept matching identity packet", "mustFail": "exact-in-flight-owner" },
    { "id": "lease-drop-recovery", "replace": "Abandoned(token, packet) -> Vacant", "mustFail": "exact-in-flight-owner-recovery" },
    { "id": "terminal-compute", "replace": "queue.take -> encode_output_step", "mustFail": "prepared-terminal" },
    { "id": "cancel-order", "replace": "cancel-check-after-drive-wire", "mustFail": "interrupt-before-mutation" },
    { "id": "deadline-order", "replace": "deadline-check-after-drive-wire", "mustFail": "interrupt-before-allocation" },
    { "id": "drop-recovery", "replace": "Abandoned -> Vacant", "mustFail": "exact-owner-recovery" },
    { "id": "lossy-fresh-restore", "replace": "finish installs replayed EnergyJob -> finish calls EnergyJob::admit", "mustFail": "complete-numerical-authority" },
    { "id": "ignored-decoded-stage-tier-rng-cursors-digest", "replace": "replay target comparison -> hour-only comparison", "mustFail": "restored-graph-parity" },
    { "id": "ignored-decoded-time-series-count", "replace": "retain and compare bytes 132..140 -> cap-check then discard", "mustFail": "restored-table-length-and-backing-parity" },
    { "id": "ignored-decoded-meter-count", "replace": "retain and compare bytes 140..148 -> cap-check then discard", "mustFail": "restored-table-length-and-backing-parity" },
    { "id": "ignored-decoded-history-count", "replace": "retain and compare bytes 148..156 -> cap-check then discard", "mustFail": "restored-history-length-and-backing-parity" },
    { "id": "unacknowledged-checkpoint-adoption", "replace": "provenance lease plus explicit ACK -> public adopt advances head", "mustFail": "lost-owner-requeues-to-unchanged-head" },
    { "id": "zero-preview-facility-total", "replace": "live retained electricity accumulator -> 0.0", "mustFail": "substantive-preview" },
    { "id": "split-preview-authority", "replace": "typed view decoded from installed SMENERGY packet -> separately constructed pending preview", "mustFail": "one-schema-authority" },
    { "id": "legacy-preview-magic", "replace": "SMENERGY preview -> ENERGYP1 encoder", "mustFail": "one-versioned-wire-schema" },
    { "id": "restore-stale-step", "replace": "identity check before replay step -> identity check after replay step", "mustFail": "stale-before-mutation" },
    { "id": "restore-stale-install", "replace": "identity check immediately before finish -> no install guard", "mustFail": "stale-before-atomic-install" },
    { "id": "whole-commit-scan", "replace": "CommitCensusWork cursor -> values().try_fold", "mustFail": "one-record-or-character-per-grant" },
    { "id": "late-commit-reserve", "replace": "reservation before writer creation -> begin_staged_page before census completes", "mustFail": "simultaneous-admission-before-copy" },
    { "id": "unreserved-commit-page", "replace": "commit_pages_mounted < reservation.pages -> unconditional begin_staged_page", "mustFail": "exact-page-reservation" }
  ]
}
