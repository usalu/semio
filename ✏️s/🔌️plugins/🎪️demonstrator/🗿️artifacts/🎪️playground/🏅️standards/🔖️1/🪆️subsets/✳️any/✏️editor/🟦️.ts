/** ✏️ Playground editor — surface root: namespaced re-export of the one window's typed twin. Not a
 * blanket `export *` — every other migrated surface in this ticket follows the same convention even
 * with a single window, so a second window added later never becomes an ambiguous re-export. */

export * as mainWindow from "./🎭️modes/✏️edit/🪟️windows/🪟️main/🟦️.ts";
