//! 🪦️ Orphaned by 26/08/12/SEMANTIC-MUTATIONS-OVERHAUL — `NoMutation` is banned outright (see
//! `../🦠️mutation/🦀️component.rs`'s doc comment). Stays present only because `📦️glue.rs` still
//! `#[path]`-wires it; see this ticket's wave2 report `sharedFileRequests`.
