syntax = "proto3";
package semio.s.demonstrator.playground.mutation;

// 🧬️ Playground mutation schema — persistent fields only.

message PlaygroundMutation {
  // @state artifact
  string schema = 1;
}
