syntax = "proto3";
package semio.s.demonstrator.playground.snapshot;

// 🧬️ Playground snapshot schema — persistent fields only.

message PlaygroundSnapshot {
  // @state artifact
  string schema = 1;
}
