syntax = "proto3";
package semio.s.demonstrator.playground.artifact;

// 🧬️ Playground artifact schema — every field with its state class.

message PlaygroundArtifact {
  // @state persistent
  string schema = 1;
}
