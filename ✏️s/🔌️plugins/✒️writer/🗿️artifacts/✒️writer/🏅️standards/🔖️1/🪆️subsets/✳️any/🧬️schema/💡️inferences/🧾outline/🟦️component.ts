/** 🧾 `outline` — one named inference: this document's own outline (markdown headings + word/line counts). */

export interface WriterOutline {
  sectionOutline: string[];
  wordCount: number;
  lineCount: number;
}
