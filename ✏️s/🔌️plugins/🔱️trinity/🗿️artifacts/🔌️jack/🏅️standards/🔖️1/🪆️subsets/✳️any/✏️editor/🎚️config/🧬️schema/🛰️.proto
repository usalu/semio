syntax = "proto3";
package semio.app.trinity.jack;
message JackConfig {
  // @state config
  Camera camera = 2;
  // @state config
  // @state config
  string jack_query = 4;
  // @state config
  // @state config
  // @state config
  // @state config
  // @state config
  // @state config
  map<string, string> lod_mode_by_window = 11;
  // @state config
  // @state config
}

message Camera {
  double x = 1;
  double y = 2;
  double zoom = 3;
}
