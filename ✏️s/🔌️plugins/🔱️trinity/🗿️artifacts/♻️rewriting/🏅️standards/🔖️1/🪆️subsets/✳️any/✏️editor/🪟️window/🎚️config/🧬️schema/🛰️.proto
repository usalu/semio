syntax = "proto3";
package semio.trinity.rewriting.window.config;
message RewritingWindowCamera { double x = 1; double y = 2; double zoom = 3; }
message RewritingWindowConfig {
  // @state config
  optional RewritingWindowCamera camera = 1;
  // @state config
  string lod_mode = 2;
}
