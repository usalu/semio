# 💡️ Cad inference schema — object/vertex counts + 3d bounding box across every pane.

type CadInference {
  objectCount: Int! @derived
  vertexCount: Int! @derived
  bounds: CadBounds @derived
}

type CadBounds {
  min: [Float!]!
  max: [Float!]!
}
