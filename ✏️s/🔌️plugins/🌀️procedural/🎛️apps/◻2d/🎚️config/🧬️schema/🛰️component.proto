syntax = "proto3";
package semio.app.procedural.2d;
message Procedural2dConfig {
  // @state config
  repeated string selected_ids = 1;
  // @state config
  CameraJson camera = 2;
  // @state config
  string show_mode = 3;
  // @state config
  optional string selected_generation_id = 4;
  // @state config
  optional string generation_preview_text = 5;
  // @state config
  string locale = 6;
}
message CameraJson {
  double x = 1;
  double y = 2;
  double zoom = 3;
}
