{
  "$id": "https://semio.tech/schema/s/procedural/procedural2d/inference.json",
  "title": "Procedural2dInference",
  "type": "object",
  "additionalProperties": false,
  "required": ["topology"],
  "properties": {
    "topology": {
      "$ref": "#/$defs/Procedural2dTopology",
      "x-semio-derived": true
    }
  },
  "$defs": {
    "Procedural2dTopology": {
      "type": "object",
      "additionalProperties": false,
      "required": ["nodeCount", "edgeCount", "topoOrder", "depth", "cycleFree"],
      "properties": {
        "nodeCount": { "type": "integer", "minimum": 0 },
        "edgeCount": { "type": "integer", "minimum": 0 },
        "topoOrder": { "type": "array", "items": { "type": "string" } },
        "depth": { "type": "integer", "minimum": 0 },
        "cycleFree": { "type": "boolean" }
      }
    }
  }
}
