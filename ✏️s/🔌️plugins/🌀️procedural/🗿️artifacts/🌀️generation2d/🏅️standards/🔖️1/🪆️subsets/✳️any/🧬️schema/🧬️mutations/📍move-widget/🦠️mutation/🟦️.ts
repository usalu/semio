/** 📍 generation2d move-widget payload — mirrors `MoveWidget` (…/📍move-widget/🦠️mutation/🦀️.rs:16-19). */
export interface WidgetLayout {
  x: number;
  y: number;
}

export interface MoveWidget {
  id: string;
  layout: WidgetLayout;
}
