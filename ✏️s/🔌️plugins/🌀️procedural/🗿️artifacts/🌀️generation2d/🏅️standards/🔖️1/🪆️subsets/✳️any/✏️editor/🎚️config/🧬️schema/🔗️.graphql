type Generation2dConfig {
  camera: CameraJson! @state(class: CONFIG)
  showMode: String! @state(class: CONFIG)
  selectedGenerationId: String @state(class: CONFIG)
}
type CameraJson {
  x: Float!
  y: Float!
  zoom: Float!
}
