/** 🫧️ Generation3d app-local computed preview shared by all generation windows. */
export interface Generation3dTransient {
  generationPreviewText?: string;
}

export interface SetGenerationPreview {
  previewText?: string;
}
