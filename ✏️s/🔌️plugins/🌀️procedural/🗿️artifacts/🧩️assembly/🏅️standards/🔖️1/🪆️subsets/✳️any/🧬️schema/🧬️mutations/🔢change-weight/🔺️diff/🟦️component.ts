/** 🔢 assembly change-weight/🔺️diff — real diff shape mirror: an id-keyed weight upsert. */
import type { AssemblyModuleWeight } from "../../../📸️snapshot/🟦️component";
export interface ChangeWeightDiff { weightsUpserted: AssemblyModuleWeight[]; }
