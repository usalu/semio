/** 🔗 assembly connect-slots/🔺️diff — real diff shape mirror: a single-edge upsert. */
import type { AssemblySlotEdge } from "../../../📸️snapshot/🟦️component";
export interface ConnectSlotsDiff { edgesUpserted: [number, AssemblySlotEdge][]; }
