/** 🌱 assembly create-rule/🔺️diff — real diff shape mirror: a single-rule upsert. */
import type { AssemblyRule } from "../../../📸️snapshot/🟦️component";
export interface CreateRuleDiff { rulesUpserted: [number, AssemblyRule][]; }
