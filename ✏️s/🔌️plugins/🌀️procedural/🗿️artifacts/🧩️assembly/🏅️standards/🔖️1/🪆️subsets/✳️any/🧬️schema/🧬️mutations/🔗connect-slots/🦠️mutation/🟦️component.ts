/** 🔗 assembly connect-slots/🦠️mutation — real payload shape mirror. */
import type { AssemblySlotEdge } from "../../../📸️snapshot/🟦️component";
export interface ConnectSlots { index: number; edge: AssemblySlotEdge; }
