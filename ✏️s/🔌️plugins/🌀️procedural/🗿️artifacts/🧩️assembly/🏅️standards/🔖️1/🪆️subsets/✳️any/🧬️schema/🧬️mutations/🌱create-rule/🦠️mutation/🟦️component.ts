/** 🌱 assembly create-rule/🦠️mutation — real payload shape mirror. */
import type { AssemblyRule } from "../../../📸️snapshot/🟦️component";
export interface CreateRule { index: number; rule: AssemblyRule; }
