/** 🌱 assembly create-slot/🔺️diff — real diff shape mirror: a single-slot upsert. */
import type { AssemblySlot } from "../../../📸️snapshot/🟦️component";
export interface CreateSlotDiff { slotsUpserted: [number, AssemblySlot][]; }
