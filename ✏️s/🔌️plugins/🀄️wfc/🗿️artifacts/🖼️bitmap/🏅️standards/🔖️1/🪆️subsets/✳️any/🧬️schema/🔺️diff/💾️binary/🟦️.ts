/** 📦️ Binary facet declaration for `s.wfc.bitmap` diffs. Declaration-only on both sides, for the
 * same reason the text facet is. */
export type BitmapDiffBinary = Uint8Array;
export const BITMAP_DIFF_PROTOCOL_ID = "wfc.bitmap.diff";
