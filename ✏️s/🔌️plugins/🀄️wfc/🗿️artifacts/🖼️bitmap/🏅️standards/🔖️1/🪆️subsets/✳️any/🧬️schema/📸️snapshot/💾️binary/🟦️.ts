/** 📦️ Binary facet declaration for `s.wfc.bitmap` snapshots — the normative protocol lives beside
 * this file as `📡️.protocol.semio`; the TS twin names the carrier, never the pack codec. */
export type BitmapSnapshotBinary = Uint8Array;
export const BITMAP_SNAPSHOT_PROTOCOL_ID = "wfcbitmap.snapshot";
export const BITMAP_SNAPSHOT_ENVELOPE_ID = "wfc.bitmap";
