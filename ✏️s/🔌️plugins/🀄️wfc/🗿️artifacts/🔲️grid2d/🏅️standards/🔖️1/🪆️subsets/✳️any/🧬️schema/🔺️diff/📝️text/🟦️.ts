/** 🔺️ Text facet declaration for `s.wfc.grid2d` diffs — this subset publishes the diff through the
 * pack/JSON carriers only, so the text form is the JSON projection of `Grid2dDiff`. */
export type Grid2dDiffText = string;
export const GRID2D_DIFF_GRAMMAR_ID = "wfc.grid2d.diff";
