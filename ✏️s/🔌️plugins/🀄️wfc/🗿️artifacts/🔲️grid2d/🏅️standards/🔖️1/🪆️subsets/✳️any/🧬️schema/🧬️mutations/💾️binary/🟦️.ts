/** ⚖️ Binary facet declaration for `s.wfc.grid2d` mutations — the wire TAG of an operation is its
 * position in `GRID2D_OP_KEYWORDS`, the same roster the text grammar reads. */
export type Grid2dMutationBinary = Uint8Array;
export const GRID2D_OP_PROTOCOL_ID = "wfc.grid2d.op";
export const GRID2D_OP_PROTOCOL_VERSION = 1;
