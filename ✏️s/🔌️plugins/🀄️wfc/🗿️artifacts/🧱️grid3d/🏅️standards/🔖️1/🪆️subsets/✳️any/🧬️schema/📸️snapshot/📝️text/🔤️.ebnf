(* 🔤 wfc.grid3d.snapshot — derived from 📖️.grammar.semio; rule names are the grammar's own, kebab folded to spaces. *)

document = header body ;
header = "schema" SP "wfc.grid3d.snapshot" NL ;
body = payload NL? ;
payload = OCTET+ ;
