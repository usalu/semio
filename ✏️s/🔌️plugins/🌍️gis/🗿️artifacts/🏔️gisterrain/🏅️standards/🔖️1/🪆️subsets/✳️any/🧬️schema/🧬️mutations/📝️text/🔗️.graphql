# gis.gisterrain mutations — one input type per semantic mutation slug (tags 1..2 match protocol.semio order).
scalar Bytes

input ChangeExaggeration { newExaggeration: Float! }
input ChangeImportedFeatures {  }

union GisterrainMutation = ChangeExaggeration | ChangeImportedFeatures
