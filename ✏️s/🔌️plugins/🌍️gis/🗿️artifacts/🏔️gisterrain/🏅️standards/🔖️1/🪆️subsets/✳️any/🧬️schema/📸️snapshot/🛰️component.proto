syntax = "proto3";
package semio.s.gis.gisterrain.snapshot;

// 🧬️ GIS terrain snapshot schema — persistent fields only.

message GisTerrainSnapshot {
  // @state artifact
  double exaggeration = 1;
  // @state artifact
  string imported_features_json = 2;
}
