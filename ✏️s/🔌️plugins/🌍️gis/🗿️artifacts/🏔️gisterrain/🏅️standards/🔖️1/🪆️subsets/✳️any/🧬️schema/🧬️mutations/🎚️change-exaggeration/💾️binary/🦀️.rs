//! 🎚 Binary identity owned by the direct `change-exaggeration` leaf.

pub const BINARY_TAG: u8 = dsl::protocol_record::tag_u8(include_str!("../../💾️binary/📡️.protocol.semio"), "change-exaggeration");
