syntax = "proto3";
package semio.s.gis.gisterrain.mutation;

// 🧬️ GIS terrain mutation schema — artifact-lane fields only.

message GisTerrainMutation {
  // @state artifact
  double exaggeration = 1;
  // @state artifact
  string imported_features_json = 2;
}
