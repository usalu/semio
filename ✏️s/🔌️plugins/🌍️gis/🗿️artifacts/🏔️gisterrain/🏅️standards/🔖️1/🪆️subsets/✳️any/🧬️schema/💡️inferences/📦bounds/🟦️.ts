/** 📦 `bounds` — one named inference: geographic bounding box across the `map:in` overlay. */

export interface GisTerrainBounds {
  lonMin: number;
  lonMax: number;
  latMin: number;
  latMax: number;
}
