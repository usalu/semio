syntax = "proto3";
package semio.app.gis.gis3d;
message Gis3dConfig {
  // @state config
  string camera_json = 1;
  // @state config
  repeated string selected_ids = 2;
  // @state config
  string locale = 3;
}
