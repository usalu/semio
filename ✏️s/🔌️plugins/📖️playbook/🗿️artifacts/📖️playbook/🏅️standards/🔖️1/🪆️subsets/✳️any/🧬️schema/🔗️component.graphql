# 🧬️ Playbook artifact schema — every field with its state class.

type PlaybookArtifact {
  schema: String! @state(class: ARTIFACT)
  id: String! @state(class: ARTIFACT)
  version: String! @state(class: ARTIFACT)
  title: String @state(class: ARTIFACT)
  steps: [PlaybookStep!]! @state(class: ARTIFACT)
  selectedIds: [String!]! @state(class: PRESENCE)
  locale: String! @state(class: CONFIG)
  contributionsJson: String! @state(class: CONFIG)
}

type PlaybookStep {
  id: String!
  title: String!
  description: String
  blocks: [PlaybookBlock!]!
}

type PlaybookBlock {
  id: String!
  label: String!
  kind: String!
}
