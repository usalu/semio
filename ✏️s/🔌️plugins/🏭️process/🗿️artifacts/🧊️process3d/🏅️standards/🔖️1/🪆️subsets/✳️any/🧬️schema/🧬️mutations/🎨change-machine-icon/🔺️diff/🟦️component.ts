/** 🔺️ Sparse diff construction for `change-machine-icon`. */
import type { ChangeMachineIcon } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeMachineIcon` directly from `(payload, base)` — mirrors the Rust `diff`. */
export declare function diff(payload: ChangeMachineIcon, base: unknown): unknown;
