# 🧬️ Process3d snapshot schema — persistent fields only.

type Process3dSnapshot {
  workshop: Workshop! @state(class: ARTIFACT)
  stock: Stock! @state(class: ARTIFACT)
  steps: [ProcessStep!]! @state(class: ARTIFACT)
  resolvedUpTo: Int @state(class: ARTIFACT)
}
