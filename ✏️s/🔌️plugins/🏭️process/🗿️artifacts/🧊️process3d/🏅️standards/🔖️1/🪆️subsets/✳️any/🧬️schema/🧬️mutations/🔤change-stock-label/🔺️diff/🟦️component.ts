/** 🔺️ Sparse diff construction for `change-stock-label`. */
import type { ChangeStockLabel } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeStockLabel` directly from `(payload, base)` — mirrors the Rust `diff`. */
export declare function diff(payload: ChangeStockLabel, base: unknown): unknown;
