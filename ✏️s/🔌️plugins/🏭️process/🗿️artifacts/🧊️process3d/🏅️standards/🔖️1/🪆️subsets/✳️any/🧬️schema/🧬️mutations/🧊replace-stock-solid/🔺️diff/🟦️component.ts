/** 🔺️ Sparse diff construction for `replace-stock-solid`. */
import type { ReplaceStockSolid } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ReplaceStockSolid` directly from `(payload, base)` — mirrors the Rust `diff`. */
export declare function diff(payload: ReplaceStockSolid, base: unknown): unknown;
