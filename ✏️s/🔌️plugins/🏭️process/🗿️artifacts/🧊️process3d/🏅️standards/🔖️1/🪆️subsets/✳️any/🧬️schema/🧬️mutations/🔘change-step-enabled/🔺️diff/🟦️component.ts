/** 🔺️ Sparse diff construction for `change-step-enabled`. */
import type { ChangeStepEnabled } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeStepEnabled` directly from `(payload, base)` — mirrors the Rust `diff`. */
export declare function diff(payload: ChangeStepEnabled, base: unknown): unknown;
