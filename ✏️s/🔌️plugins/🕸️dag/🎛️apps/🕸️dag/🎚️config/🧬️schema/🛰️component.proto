syntax = "proto3";
package semio.app.dag.dag;
message DagConfig {
  // @state config
  repeated string selected_node_ids = 1;
  // @state config
  double camera_x = 2;
  // @state config
  double camera_y = 3;
  // @state config
  double camera_zoom = 4;
  // @state config
  string locale = 5;
}
