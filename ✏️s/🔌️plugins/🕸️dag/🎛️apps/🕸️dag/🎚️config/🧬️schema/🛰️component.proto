syntax = "proto3";
package semio.app.dag.dag;
message DagConfig {
  reserved 1;
  // @state config
  double camera_x = 2;
  // @state config
  double camera_y = 3;
  // @state config
  double camera_zoom = 4;
  // @state config
  string locale = 5;
}
