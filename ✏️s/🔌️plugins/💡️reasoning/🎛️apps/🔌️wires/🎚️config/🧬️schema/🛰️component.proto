syntax = "proto3";
package semio.app.reasoning.wires;
message WiresConfig {
  // @state config
  optional string drag_node_id = 1;
  // @state config
  double drag_last_x = 2;
  // @state config
  double drag_last_y = 3;
  // @state config
  string locale = 4;
}
