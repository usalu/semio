syntax = "proto3";
package semio.s.reasoning.wires.snapshot;

// 🧬️ Wires snapshot schema.

message WiresSnapshot {
  // @state artifact
  DslValue wires_fixture = 1;
  // @state artifact
  DslValue board_fixture = 2;
}

message WiresStringList {
  repeated string values = 1;
}

message DslValue {}
