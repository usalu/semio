# 💡️ Wires inference schema — topology (node/edge/component counts + cycle-freedom) over the board graph.

type WiresInference {
  topology: WiresTopology! @state(class: INFERRED)
}

type WiresTopology {
  nodeCount: Int!
  edgeCount: Int!
  componentCount: Int!
  cycleFree: Boolean!
}
