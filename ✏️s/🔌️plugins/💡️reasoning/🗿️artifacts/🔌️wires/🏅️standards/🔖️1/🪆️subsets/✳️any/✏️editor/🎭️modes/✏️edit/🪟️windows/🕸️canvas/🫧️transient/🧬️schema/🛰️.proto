syntax = "proto3";
package semio.app.reasoning.wires;
message WiresCanvasTransient {
  // @state transient
  optional string drag_node_id = 1;
  // @state transient
  double drag_start_x = 2;
  // @state transient
  double drag_start_y = 3;
  // @state transient
  double drag_last_x = 4;
  // @state transient
  double drag_last_y = 5;
  // @state transient
  double drag_zoom = 6;
  // @state transient
}
