# 💡️ Wires inference schema — topology (node/edge/component counts + cycle-freedom) over the board graph.

type WiresInference {
  topology: WiresTopology! @derived
}

type WiresTopology {
  nodeCount: Int!
  edgeCount: Int!
  componentCount: Int!
  cycleFree: Boolean!
}
