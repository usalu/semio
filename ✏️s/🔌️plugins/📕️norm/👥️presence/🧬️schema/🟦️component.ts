/** 🧬️ NormPresence — empty: every norm family app keeps its only view state in its config (`selectedCheckIndex`); there is no separate shareable live surface to broadcast. Mirrors `NormPresence` in `../🦀️component.rs`. */
export interface NormPresence {}
