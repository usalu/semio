# 💡️ En1996 inference schema — document outline (field/section list + entry count).

type En1996Inference {
  outline: En1996Outline! @state(class: INFERRED)
}

type En1996Outline {
  sectionOutline: [String!]!
  fieldCount: Int!
  entryCount: Int!
}
