/** 🦒 `change-sigma-ed-shell-mpa` mutation payload — mirrors `ChangeSigmaEdShellMpa` (`En1999Mutation::ChangeSigmaEdShellMpa`). */
export interface ChangeSigmaEdShellMpa {
  newSigmaEdShellMpa: number;
}
