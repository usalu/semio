# 💡️ En1990 inference schema — document outline (field/section list + entry count).

type En1990Inference {
  outline: En1990Outline! @derived
}

type En1990Outline {
  sectionOutline: [String!]!
  fieldCount: Int!
  entryCount: Int!
}
