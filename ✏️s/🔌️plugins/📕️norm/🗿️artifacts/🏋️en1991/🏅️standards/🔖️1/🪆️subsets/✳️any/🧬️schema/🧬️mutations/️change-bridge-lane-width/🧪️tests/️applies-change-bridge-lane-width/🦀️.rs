//! Scenario for `change-bridge-lane-width`.
#[test]
fn applies_change_bridge_lane_width() {
    let base = crate::En1991Snapshot::default();
    // smoke: construct payload type exists
    let _ = base.annex;
}
