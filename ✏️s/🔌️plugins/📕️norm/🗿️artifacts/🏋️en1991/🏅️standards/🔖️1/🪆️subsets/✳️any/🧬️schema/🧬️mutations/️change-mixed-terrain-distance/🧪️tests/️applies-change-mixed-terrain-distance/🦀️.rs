//! Scenario for `change-mixed-terrain-distance`.
#[test]
fn applies_change_mixed_terrain_distance() {
    let base = crate::En1991Snapshot::default();
    // smoke: construct payload type exists
    let _ = base.annex;
}
