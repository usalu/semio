/** ↩️ inverse — its restored mutation has the same payload shape as the forward one. */
import type { ChangeExchangeProcess } from "../🦠️mutation/🟦️.ts";

export type ChangeExchangeProcessInverse = ChangeExchangeProcess;
