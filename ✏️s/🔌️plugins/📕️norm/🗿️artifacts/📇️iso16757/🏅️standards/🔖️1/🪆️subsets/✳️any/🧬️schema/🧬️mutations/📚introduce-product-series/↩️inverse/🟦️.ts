/** ↩️ inverse for `IntroduceProductSeries` — same payload shape restores the prior state. */
import type { IntroduceProductSeries } from "../🦠️mutation/🟦️.ts";

export type IntroduceProductSeriesInverse = IntroduceProductSeries;
