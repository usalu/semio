/** ↩️ inverse for `RetireProductSeries` — same payload shape restores the prior state. */
import type { RetireProductSeries } from "../🦠️mutation/🟦️.ts";

export type RetireProductSeriesInverse = RetireProductSeries;
