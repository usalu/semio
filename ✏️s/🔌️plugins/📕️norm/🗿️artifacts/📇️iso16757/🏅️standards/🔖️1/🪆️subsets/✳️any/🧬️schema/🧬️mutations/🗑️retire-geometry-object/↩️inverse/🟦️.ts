/** ↩️ inverse for `RetireGeometryObject` — same payload shape restores the prior state. */
import type { RetireGeometryObject } from "../🦠️mutation/🟦️.ts";

export type RetireGeometryObjectInverse = RetireGeometryObject;
