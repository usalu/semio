/** ↩️ inverse for `RetireProductIndex` — same payload shape restores the prior state. */
import type { RetireProductIndex } from "../🦠️mutation/🟦️.ts";

export type RetireProductIndexInverse = RetireProductIndex;
