/** ↩️ inverse for `RetireProductClass` — same payload shape restores the prior state. */
import type { RetireProductClass } from "../🦠️mutation/🟦️.ts";

export type RetireProductClassInverse = RetireProductClass;
