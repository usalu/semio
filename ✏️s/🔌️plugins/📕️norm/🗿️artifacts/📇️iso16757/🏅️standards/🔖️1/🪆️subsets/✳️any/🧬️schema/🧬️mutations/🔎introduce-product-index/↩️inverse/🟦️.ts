/** ↩️ inverse for `IntroduceProductIndex` — same payload shape restores the prior state. */
import type { IntroduceProductIndex } from "../🦠️mutation/🟦️.ts";

export type IntroduceProductIndexInverse = IntroduceProductIndex;
