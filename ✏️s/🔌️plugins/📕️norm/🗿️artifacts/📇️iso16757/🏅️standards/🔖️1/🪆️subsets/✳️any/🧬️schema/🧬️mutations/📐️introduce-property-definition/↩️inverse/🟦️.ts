/** ↩️ inverse for `IntroducePropertyDefinition` — same payload shape restores the prior state. */
import type { IntroducePropertyDefinition } from "../🦠️mutation/🟦️.ts";

export type IntroducePropertyDefinitionInverse = IntroducePropertyDefinition;
