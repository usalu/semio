/** ↩️ inverse for `IntroduceGeometryObject` — same payload shape restores the prior state. */
import type { IntroduceGeometryObject } from "../🦠️mutation/🟦️.ts";

export type IntroduceGeometryObjectInverse = IntroduceGeometryObject;
