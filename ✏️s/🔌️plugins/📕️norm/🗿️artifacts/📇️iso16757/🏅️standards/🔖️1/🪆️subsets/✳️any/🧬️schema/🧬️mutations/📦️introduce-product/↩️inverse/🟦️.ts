/** ↩️ inverse for `IntroduceProduct` — same payload shape restores the prior state. */
import type { IntroduceProduct } from "../🦠️mutation/🟦️.ts";

export type IntroduceProductInverse = IntroduceProduct;
