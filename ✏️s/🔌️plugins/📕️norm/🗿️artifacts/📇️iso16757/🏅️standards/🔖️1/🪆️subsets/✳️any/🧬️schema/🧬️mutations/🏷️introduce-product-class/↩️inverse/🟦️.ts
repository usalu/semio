/** ↩️ inverse for `IntroduceProductClass` — same payload shape restores the prior state. */
import type { IntroduceProductClass } from "../🦠️mutation/🟦️.ts";

export type IntroduceProductClassInverse = IntroduceProductClass;
