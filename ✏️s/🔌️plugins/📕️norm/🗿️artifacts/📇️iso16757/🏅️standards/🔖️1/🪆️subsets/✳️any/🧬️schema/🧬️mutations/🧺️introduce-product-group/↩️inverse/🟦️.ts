/** ↩️ inverse for `IntroduceProductGroup` — same payload shape restores the prior state. */
import type { IntroduceProductGroup } from "../🦠️mutation/🟦️.ts";

export type IntroduceProductGroupInverse = IntroduceProductGroup;
