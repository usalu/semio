/** ↩️ inverse — its restored mutation has the same payload shape as the forward one. */
import type { ReplacePartNumberRule } from "../🦠️mutation/🟦️.ts";

export type ReplacePartNumberRuleInverse = ReplacePartNumberRule;
