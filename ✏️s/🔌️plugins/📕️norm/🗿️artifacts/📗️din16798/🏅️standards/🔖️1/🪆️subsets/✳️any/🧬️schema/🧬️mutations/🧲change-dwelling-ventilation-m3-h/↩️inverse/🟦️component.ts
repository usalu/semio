/** ↩️ `change-dwelling-ventilation-m3-h` inverse — its restored mutation has the same payload shape as the forward one. */
export type ChangeDwellingVentilationM3HInverse = ChangeDwellingVentilationM3H;
