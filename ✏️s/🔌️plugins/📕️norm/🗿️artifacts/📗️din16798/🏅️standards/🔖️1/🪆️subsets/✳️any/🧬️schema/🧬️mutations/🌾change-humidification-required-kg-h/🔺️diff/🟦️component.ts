/** 🔺 `change-humidification-required-kg-h` diff fragment — the sparse `Din16798Diff` field this mutation writes. */
export interface ChangeHumidificationRequiredKgHDiff {
  humidificationRequiredKgH?: number;
}
