# 💡️ Din16798 inference schema — document outline (field/section list + entry count).

type Din16798Inference {
  outline: Din16798Outline! @state(class: INFERRED)
}

type Din16798Outline {
  sectionOutline: [String!]!
  fieldCount: Int!
  entryCount: Int!
}
