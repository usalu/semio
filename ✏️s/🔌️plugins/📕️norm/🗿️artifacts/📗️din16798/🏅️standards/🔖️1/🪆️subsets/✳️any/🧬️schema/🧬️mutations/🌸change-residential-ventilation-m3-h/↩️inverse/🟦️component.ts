/** ↩️ `change-residential-ventilation-m3-h` inverse — its restored mutation has the same payload shape as the forward one. */
export type ChangeResidentialVentilationM3HInverse = ChangeResidentialVentilationM3H;
