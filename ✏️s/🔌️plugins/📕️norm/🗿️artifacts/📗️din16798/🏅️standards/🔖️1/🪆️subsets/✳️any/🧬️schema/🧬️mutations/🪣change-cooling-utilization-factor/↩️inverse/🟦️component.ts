/** ↩️ `change-cooling-utilization-factor` inverse — its restored mutation has the same payload shape as the forward one. */
export type ChangeCoolingUtilizationFactorInverse = ChangeCoolingUtilizationFactor;
