/** ↩️ `change-humidification-required-kg-h` inverse — its restored mutation has the same payload shape as the forward one. */
export type ChangeHumidificationRequiredKgHInverse = ChangeHumidificationRequiredKgH;
