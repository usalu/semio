/** 🔧 `change-sfp-required-class` mutation payload — mirrors `ChangeSfpRequiredClass` (`Din16798Mutation::ChangeSfpRequiredClass`). */
export interface ChangeSfpRequiredClass {
  newSfpRequiredClass: number;
}
