/** 🔺 `change-dwelling-ventilation-m3-h` diff fragment — the sparse `Din16798Diff` field this mutation writes. */
export interface ChangeDwellingVentilationM3HDiff {
  dwellingVentilationM3H?: number;
}
