/** 🔺 `change-generation-reference-kwh` diff fragment — the sparse `Din16798Diff` field this mutation writes. */
export interface ChangeGenerationReferenceKwhDiff {
  generationReferenceKwh?: number;
}
