/** 🔺 `change-infiltration-allowance-m3-h` diff fragment — the sparse `Din16798Diff` field this mutation writes. */
export interface ChangeInfiltrationAllowanceM3HDiff {
  infiltrationAllowanceM3H?: number;
}
