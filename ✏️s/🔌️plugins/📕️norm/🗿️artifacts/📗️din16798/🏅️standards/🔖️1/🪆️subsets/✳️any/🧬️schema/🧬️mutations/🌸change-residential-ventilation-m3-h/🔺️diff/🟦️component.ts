/** 🔺 `change-residential-ventilation-m3-h` diff fragment — the sparse `Din16798Diff` field this mutation writes. */
export interface ChangeResidentialVentilationM3HDiff {
  residentialVentilationM3H?: number;
}
