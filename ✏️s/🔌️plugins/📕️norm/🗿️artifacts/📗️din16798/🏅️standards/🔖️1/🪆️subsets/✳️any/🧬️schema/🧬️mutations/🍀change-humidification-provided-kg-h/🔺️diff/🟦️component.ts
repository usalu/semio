/** 🔺 `change-humidification-provided-kg-h` diff fragment — the sparse `Din16798Diff` field this mutation writes. */
export interface ChangeHumidificationProvidedKgHDiff {
  humidificationProvidedKgH?: number;
}
