/** 🔺 `change-cooling-utilization-factor` diff fragment — the sparse `Din16798Diff` field this mutation writes. */
export interface ChangeCoolingUtilizationFactorDiff {
  coolingUtilizationFactor?: number;
}
