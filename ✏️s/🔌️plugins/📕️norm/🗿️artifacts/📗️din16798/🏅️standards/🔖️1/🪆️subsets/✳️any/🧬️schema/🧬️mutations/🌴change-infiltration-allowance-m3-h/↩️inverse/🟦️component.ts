/** ↩️ `change-infiltration-allowance-m3-h` inverse — its restored mutation has the same payload shape as the forward one. */
export type ChangeInfiltrationAllowanceM3HInverse = ChangeInfiltrationAllowanceM3H;
