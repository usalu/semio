{
  "artifact": "s.norm.vdi3805",
  "standard": "1",
  "subsets": {
    "catalog": {},
    "geometry": {
      "name": "The `geometry` register — keyed `ParametricGeometry` entries (add-geometry, remove-geometry, resize-geometry, change-geometry-parameters, add/remove-geometry-connection), organized in the artifact's own `🦀️.rs` under its own `Geometry` code region, separate from `Part1`'s catalogue types."
    },
    "curves": {
      "name": "The `curves` register — keyed `CharacteristicCurve` entries (add-curve, remove-curve, change-curve-points), organized under the artifact's own `Functions` code region, separate from the catalogue and from geometry."
    }
  }
}
