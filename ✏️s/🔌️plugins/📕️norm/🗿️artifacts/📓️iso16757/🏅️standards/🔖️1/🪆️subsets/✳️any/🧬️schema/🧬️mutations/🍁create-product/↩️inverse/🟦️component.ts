/** ↩️ inverse for `CreateProduct` — same payload shape restores the prior state. */
import type { CreateProduct } from "../🦠️mutation/🟦️component.ts";

export type CreateProductInverse = CreateProduct;
