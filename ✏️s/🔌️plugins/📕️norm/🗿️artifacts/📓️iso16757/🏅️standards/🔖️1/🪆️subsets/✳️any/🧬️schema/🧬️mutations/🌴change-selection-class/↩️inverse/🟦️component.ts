/** ↩️ inverse — its restored mutation has the same payload shape as the forward one. */
import type { ChangeSelectionClass } from "../🦠️mutation/🟦️component.ts";

export type ChangeSelectionClassInverse = ChangeSelectionClass;
