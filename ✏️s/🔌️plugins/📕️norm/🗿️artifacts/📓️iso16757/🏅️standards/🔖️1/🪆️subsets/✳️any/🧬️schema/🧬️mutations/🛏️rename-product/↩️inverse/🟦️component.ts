/** ↩️ inverse for `RenameProduct` — same payload shape restores the prior state. */
import type { RenameProduct } from "../🦠️mutation/🟦️component.ts";

export type RenameProductInverse = RenameProduct;
