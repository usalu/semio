{
  "$schema": "../../../../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/🧪️test/🧬️schema/🔣️component.json",
  "schemaVersion": 1,
  "_comment": "🧩️ This subset's own contribution. A mutation vocabulary belongs to ONE subset of ONE standard of ONE artifact, so the (non-)oracle registration and the catalog live here rather than in a shared manifest. `iso16757-1-any` is generated from each triad leaf's own `SemanticDescriptor.kind`, in `#[derive(dsl::Mutations)]`'s declaration order, and `kinds_match_the_enum_and_the_catalog` in `../🧬️schema/🧬️mutations/🦀️component.rs` fails the moment this list and the enum part company.",
  "oracles": [],
  "noOracleDecisions": [
    {
      "id": "iso16757-1-mutation-semantics",
      "capabilities": [
        "iso16757-1-mutate"
      ],
      "rationale": "`s.norm.iso16757` is a semio-NATIVE artifact: the document is this repository's own `.dsl.semio`/`.pack.semio` envelope over a ISO 16757 — product data for building services, the catalogue/dictionary exchange standard model, and no third-party library — in any ecosystem — reads or writes it, let alone one that could be authoritative over this subset's `Iso16757Mutation` vocabulary. That vocabulary IS this subset's own specification (derived from `Iso16757Snapshot`'s shape by `26/08/12/SEMANTIC-MUTATIONS-OVERHAUL`'s `📓️derivation-rules.md`), not a fact an external implementation could confirm or refute; a crate that parsed ISO 16757 data from some other interchange format would be judging a different document. Registering a weak or tangential crate here would be worse than registering none. Confidence comes instead from the 21 committed, independently handcrafted per-kind specification vectors under `../🧬️schema/🧬️mutations/<kind>/🧪️tests/<fixture>/` — a `(before, mutation, after, diff, outcome)` quintet per kind, already unit-tested inside the production crate itself — re-exercised end to end here through `apply_iso16757_mutation`, plus the inverse law as a metamorphic property: applying a mutation and then its own computed inverse must restore the exact pre-mutation document.",
      "substitutes": [
        "specification-vectors",
        "metamorphic-laws"
      ]
    }
  ],
  "mutationCatalogs": [
    {
      "id": "iso16757-1-any",
      "capability": "iso16757-1-mutate",
      "kinds": [
        "change-exchange-process",
        "update-script-limits",
        "replace-part-number-rule",
        "change-part-number-input",
        "remove-part-number-input",
        "change-selection-class",
        "change-selection-series",
        "add-selection-constraint",
        "remove-selection-constraint",
        "rename-catalogue",
        "rename-manufacturer",
        "create-product-group",
        "delete-product-group",
        "rename-product-group",
        "create-product",
        "delete-product",
        "rename-product",
        "create-property-definition",
        "delete-property-definition",
        "create-subject",
        "delete-subject"
      ]
    }
  ]
}
