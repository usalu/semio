/** ↩️ inverse — its restored mutation has the same payload shape as the forward one. */
import type { RenameManufacturer } from "../🦠️mutation/🟦️component.ts";

export type RenameManufacturerInverse = RenameManufacturer;
