/** ↩️ inverse for `CreateSubject` — same payload shape restores the prior state. */
import type { CreateSubject } from "../🦠️mutation/🟦️component.ts";

export type CreateSubjectInverse = CreateSubject;
