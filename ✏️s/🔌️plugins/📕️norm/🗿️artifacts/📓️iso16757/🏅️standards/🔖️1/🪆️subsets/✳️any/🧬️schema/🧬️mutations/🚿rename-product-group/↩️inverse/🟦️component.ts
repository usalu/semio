/** ↩️ inverse for `RenameProductGroup` — same payload shape restores the prior state. */
import type { RenameProductGroup } from "../🦠️mutation/🟦️component.ts";

export type RenameProductGroupInverse = RenameProductGroup;
