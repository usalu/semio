/** ↩️ inverse for `CreateProductGroup` — same payload shape restores the prior state. */
import type { CreateProductGroup } from "../🦠️mutation/🟦️component.ts";

export type CreateProductGroupInverse = CreateProductGroup;
