/** ↩️ inverse — its restored mutation has the same payload shape as the forward one. */
import type { ChangeSelectionSeries } from "../🦠️mutation/🟦️component.ts";

export type ChangeSelectionSeriesInverse = ChangeSelectionSeries;
