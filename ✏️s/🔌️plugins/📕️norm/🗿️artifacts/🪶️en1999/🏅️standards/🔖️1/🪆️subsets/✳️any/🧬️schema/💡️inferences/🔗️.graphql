# 💡️ En1999 inference schema — document outline (field/section list + entry count).

type En1999Inference {
  outline: En1999Outline! @derived
}

type En1999Outline {
  sectionOutline: [String!]!
  fieldCount: Int!
  entryCount: Int!
}
