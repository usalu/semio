# 💡️ Din4108 inference schema — document outline (field/section list + entry count).

type Din4108Inference {
  outline: Din4108Outline! @state(class: INFERRED)
}

type Din4108Outline {
  sectionOutline: [String!]!
  fieldCount: Int!
  entryCount: Int!
}
