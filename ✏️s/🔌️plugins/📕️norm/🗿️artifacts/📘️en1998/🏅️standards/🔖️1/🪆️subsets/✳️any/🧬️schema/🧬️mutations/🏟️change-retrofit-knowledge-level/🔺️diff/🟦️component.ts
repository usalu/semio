/** 🔺️ `change-retrofit-knowledge-level` diff fragment — the sparse `En1998Diff` field this mutation writes. */
export interface ChangeRetrofitKnowledgeLevelDiff {
  retrofitknowledgelevel?: string;
}
