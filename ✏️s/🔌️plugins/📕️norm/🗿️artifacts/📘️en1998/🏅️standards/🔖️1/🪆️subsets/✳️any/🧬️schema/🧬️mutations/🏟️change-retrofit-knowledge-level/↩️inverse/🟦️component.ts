/** ↩️ `change-retrofit-knowledge-level` inverse — its restored mutation has the same payload shape as the forward one. */
export type ChangeRetrofitKnowledgeLevelInverse = ChangeRetrofitKnowledgeLevel;
