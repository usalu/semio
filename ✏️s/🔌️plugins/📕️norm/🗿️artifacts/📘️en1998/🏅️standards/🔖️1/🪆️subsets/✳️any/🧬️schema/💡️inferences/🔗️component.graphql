# 💡️ En1998 inference schema — document outline (field/section list + entry count).

type En1998Inference {
  outline: En1998Outline! @state(class: INFERRED)
}

type En1998Outline {
  sectionOutline: [String!]!
  fieldCount: Int!
  entryCount: Int!
}
