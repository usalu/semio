/** 🔺️ `change-multiple-resisting-systems` diff fragment — the sparse `En1998Diff` field this mutation writes. */
export interface ChangeMultipleResistingSystemsDiff {
  multipleresistingsystems?: boolean;
}
