/** 🌊 `change-structural-system` mutation payload — mirrors `ChangeStructuralSystem` (`En1998Mutation::ChangeStructuralSystem`). */
export interface ChangeStructuralSystem {
  newStructuralSystem: string;
}
