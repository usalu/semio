/** 🌰 `change-importance-class` mutation payload — mirrors `ChangeImportanceClass` (`En1998Mutation::ChangeImportanceClass`). */
export interface ChangeImportanceClass {
  newImportanceClass: string;
}
