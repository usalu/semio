/** ↩️ `change-multiple-resisting-systems` inverse — its restored mutation has the same payload shape as the forward one. */
export type ChangeMultipleResistingSystemsInverse = ChangeMultipleResistingSystems;
