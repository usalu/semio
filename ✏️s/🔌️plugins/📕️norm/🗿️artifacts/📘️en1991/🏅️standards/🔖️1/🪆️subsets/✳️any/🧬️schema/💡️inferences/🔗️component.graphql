# 💡️ En1991 inference schema — document outline (field/section list + entry count).

type En1991Inference {
  outline: En1991Outline! @state(class: INFERRED)
}

type En1991Outline {
  sectionOutline: [String!]!
  fieldCount: Int!
  entryCount: Int!
}
