syntax = "proto3";
package semio.app.forms.forms;
message FormsConfig {
  // @state config
  uint32 current_step_index = 1;
  // @state config
  string try_values_json = 2;
  // @state config
  string locale = 3;
  // @state config
  string contributions_json = 4;
}
