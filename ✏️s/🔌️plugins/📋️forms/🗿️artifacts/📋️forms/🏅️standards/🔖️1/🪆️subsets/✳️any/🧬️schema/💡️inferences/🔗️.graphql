# 💡️ Forms inference schema — topology (step/block dependency order).

type FormsInference {
  topology: FormsTopology! @derived
}

type FormsTopology {
  topoOrder: [String!]!
  depth: [FormsTopologyDepthEntry!]!
  cycleFree: Boolean!
  nodeCount: Int!
}

type FormsTopologyDepthEntry {
  nodeId: String!
  depth: Int!
}
