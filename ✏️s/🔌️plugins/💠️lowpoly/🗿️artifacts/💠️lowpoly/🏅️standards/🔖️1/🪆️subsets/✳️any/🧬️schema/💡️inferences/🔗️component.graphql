# 💡️ Lowpoly inference schema — object count + 3d bounding box across every object's transform
# position.

type LowpolyInference {
  objectCount: Int! @state(class: INFERRED)
  bounds: LowpolyBounds @state(class: INFERRED)
}

type LowpolyBounds {
  min: [Float!]!
  max: [Float!]!
}
