# 🧬️ Lowpoly snapshot schema — artifact-lane fields only.
# import LowpolyObject from "../🔗️.graphql"

type LowpolySnapshot {
  schema: String! @state(class: ARTIFACT)
  objects: [LowpolyObject!]! @state(class: ARTIFACT)
}
