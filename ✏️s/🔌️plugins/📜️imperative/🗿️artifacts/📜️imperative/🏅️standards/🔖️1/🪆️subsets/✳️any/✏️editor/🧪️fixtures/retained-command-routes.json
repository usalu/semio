{
  "schema": "imperative.retained-command-routes.v1",
  "maximumRawBytes": 8192,
  "maximumWorkItems": 1,
  "routes": [
    { "id": "addStep", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "fresh-id derivation clones and recursively scans the flow child" },
    { "id": "addStepAt", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "fresh-id and owner resolution clone and scan the flow child" },
    { "id": "removeStep", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "step resolution clones and scans the flow child" },
    { "id": "removeStepAt", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "nested step resolution clones and scans the flow child" },
    { "id": "moveStep", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "step resolution clones and scans the flow child" },
    { "id": "moveStepAt", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "nested step resolution clones and scans the flow child" },
    { "id": "setStepParams", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "step resolution clones and scans the flow child" },
    { "id": "setStepParamsAt", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "nested step resolution clones and scans the flow child" },
    { "id": "run", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "execution clones both children and has no child-job stepping seam" },
    { "id": "setLocale", "disposition": "migrated", "lanes": ["config"], "reason": "one byte-bounded config mutation" },
    { "id": "setContributions", "disposition": "batch-only-pending-rewrite", "lanes": [], "reason": "the mutation synchronizes a process-global imperative module registry before Store publication" }
  ]
}
