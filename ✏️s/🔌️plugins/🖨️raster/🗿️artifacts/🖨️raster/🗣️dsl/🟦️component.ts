/** 🧩 raster 🗣️dsl WASM facade — parse/print delegates to the plugin Rust crate. */
export function parseDsl(text: string): unknown {
  throw new Error("wire raster 🗣️dsl parseDsl to plugin WASM");
}
export function printDsl(value: unknown): string {
  throw new Error("wire raster 🗣️dsl printDsl to plugin WASM");
}
