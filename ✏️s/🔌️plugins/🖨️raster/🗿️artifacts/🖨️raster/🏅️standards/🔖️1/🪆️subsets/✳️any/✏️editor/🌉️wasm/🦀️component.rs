//! 🌉️ Raster retained document-load bridge. The wasm-bindgen `RasterArtifactVcs`/
//! `RasterEnvelopeLoadHandle` bridge that used to live here was deleted — its playground engine is
//! the framework's `🗺️surface` crate, not this plugin's own crate; nothing ever built this bridge
//! for `wasm32-unknown-unknown` — see
//! `26/09/01/RUNTIME-DEPENDENCY-ELIMINATION-FOR-S-PLUGINS-AND-ARTIFACTS`.
