syntax = "proto3";
package semio.app.draw.draw;
message DrawConfig {
  // @state config
  repeated string selected_ids = 1;
  // @state config
  optional string hovered_id = 2;
  // @state config
  string engagement_input = 3;
  // @state config
  DrawCamera camera = 4;
  // @state config
  string active_utility_id = 5;
  // @state config
  string locale = 6;
}
message DrawCamera { double x = 1; double y = 2; double zoom = 3; }
