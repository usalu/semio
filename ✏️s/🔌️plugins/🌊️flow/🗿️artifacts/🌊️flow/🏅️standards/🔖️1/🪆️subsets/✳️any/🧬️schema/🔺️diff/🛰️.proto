syntax = "proto3";
package semio.s.flow.flow.diff;
import "🧰️framework/🛍️products/💻️os/🔨️modules/🏪️store/🪆️child/🧬️schema/🛰️.proto";

// 🔺️ Sparse Flow document delta.
message FlowDiff {
  optional FlowArtifact artifact = 1;
  optional string schema = 2;
  optional semio.os.store.ArtifactChild content = 3; // child s.stdio.semio@v1/flow
}
message FlowArtifact {
  string schema = 1;
  semio.os.store.ArtifactChild content = 2;
}
