# 💡️ Flow inference schema — topology (topological order + depth + cycle-freedom) over widgets/synapses.

type FlowInference {
  topology: FlowTopology! @derived
}

type FlowTopology {
  topoOrder: [String!]!
  depth: [FlowDepthEntry!]!
  cycleFree: Boolean!
  nodeCount: Int!
}

type FlowDepthEntry {
  widgetId: String!
  depth: Int!
}
