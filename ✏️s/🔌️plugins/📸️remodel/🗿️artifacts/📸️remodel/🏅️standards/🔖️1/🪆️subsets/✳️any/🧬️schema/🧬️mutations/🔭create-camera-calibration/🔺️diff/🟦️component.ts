/** 🔺 create-camera-calibration diff — populates `RemodelDiff.calibration` with the full state. */
export interface CreateCameraCalibrationDiff {
  calibration: { cameras: unknown[]; rig: unknown[] };
}
