/** 🚧 📷️png 📥️import — not implemented. The Rust leaf refuses this hop at fidelity `Lossy` with a
 *  reason; a remodeling document has no inline geometry or raster a TypeScript leaf could reach,
 *  so writing one here would claim a conversion that did not happen. See `🚪️io/🟦️.ts`. */
export {};
