/** 🚧 txt export — not implemented. The import direction is a real DSL reader; the export direction
 *  needs a DSL *printer*, and `dsl::print`'s layout rules have no TypeScript twin anywhere in the
 *  repo. See `🚪️io/🟦️.ts`. */
export {};
