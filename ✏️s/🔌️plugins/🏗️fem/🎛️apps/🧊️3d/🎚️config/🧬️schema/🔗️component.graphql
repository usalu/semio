type FemCamera {
  json: String!
}

type Fem3dConfig {
  resultSourceId: String @state(class: CONFIG)
  resultMode: String! @state(class: CONFIG)
  resultModeIndex: Int! @state(class: CONFIG)
  camera: FemCamera! @state(class: CONFIG)
}
