# 💡️ Fem2d inference schema — bounds (plan-view extent + node/element counts).

type Fem2dInference {
  bounds: Fem2dBounds! @derived
}

type Fem2dBounds {
  boundingBox: Fem2dBoundingBox!
  nodeCount: Int!
  elementCount: Int!
}

type Fem2dBoundingBox {
  min: [Float!]!
  max: [Float!]!
}
