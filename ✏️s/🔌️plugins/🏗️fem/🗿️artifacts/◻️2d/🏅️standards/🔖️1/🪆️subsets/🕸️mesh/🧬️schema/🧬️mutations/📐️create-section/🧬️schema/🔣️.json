{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://semio.tech/schema/s/fem/2d/1/mesh/mutation/create-section.json",
  "title": "CreateSection",
  "description": "The `create-section` mutation payload as it travels the wire: internally tagged with `\"mutation\": \"createSection\"`, camelCase throughout. Structural only — the plausibility and geometry bounds this kind enforces live in the SNAPSHOT schema, because they are properties of a valid document rather than of a well-formed payload; each is annotated there with the `mutations::guards` function that raises them.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "section"
  ],
  "properties": {
    "mutation": {
      "const": "createSection",
      "description": "The internally tagged discriminator `#[value(tag = \"mutation\")]` puts on the wire."
    },
    "section": {
      "title": "FemSection",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "name",
        "area",
        "iy"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "area": {
          "type": "number",
          "format": "double"
        },
        "iy": {
          "type": "number",
          "format": "double"
        }
      }
    }
  }
}
