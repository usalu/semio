syntax = "proto3";
package semio.app.fem.3d.config;
message FemCamera {
  string json = 1;
}
message Fem3dConfig {
  // @state config
  optional string result_source_id = 1;
  // @state config
  string result_mode = 2;
  // @state config
  uint32 result_mode_index = 3;
  // @state config
  FemCamera camera = 4;
}
