/**
 * 🚪️ Twin of the sibling `🦀️.rs` leaf: `CsvIntoFem3d`, the `s.stdio.csv@rfc4180/*` hop that reads that envelope's `payload` cell back (`Exact`).
 *
 * Declaration-only — the codec body lives in Rust, and the row this leaf contributes to the
 * `io_mechanism` registry is listed in `🚪️io/🟦️.ts`'s `FEM3D_IO_ENTRIES`. No TS codec here:
 * this package's vitest config only discovers `📚️examples/**/🧪️tests/🟦️.ts`, so nothing under
 * `🚪️io/` could be pinned against the Rust output by a runnable parity test (ticket `📓️w4-io.md`).
 */
export {};
