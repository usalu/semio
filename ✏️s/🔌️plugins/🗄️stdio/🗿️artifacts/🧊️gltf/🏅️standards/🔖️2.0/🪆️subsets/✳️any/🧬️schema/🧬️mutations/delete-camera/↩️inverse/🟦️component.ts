/** ↩️ delete-camera leaf-owned inverse reconstruction from the exact base. */
import type { GltfCamera, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfDeleteCamera, type GltfDeleteCameraPayload } from '../../delete-camera/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteCameraInverseDescriptor = { id: 's.stdio.gltf.mutation.delete-camera.v1.inverse', version: 1, touchedPathPattern: 'document/cameras' } as const;
export type GltfDeleteCameraInverseOperation = GltfStructuralOperation<GltfCamera>;
export interface GltfDeleteCameraInverse { readonly id: 's.stdio.gltf.mutation.delete-camera.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfDeleteCameraPayload; readonly operation: GltfDeleteCameraInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfDeleteCameraPayload): GltfDeleteCameraInverseOperation => ({ kind: 'insert', position: payload.index, item: base.document.cameras[payload.index]! });
const inverseTouchedPaths = (payload: GltfDeleteCameraPayload): readonly string[] => [`document/cameras/${payload.index}`];
export const validateGltfDeleteCameraInverse = (inverse: GltfDeleteCameraInverse): GltfMutationRejection | undefined => inverse.id !== GltfDeleteCameraInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfDeleteCameraInverse = (after: GltfSnapshot, inverse: GltfDeleteCameraInverse): GltfStructuralResult => { const rejection = validateGltfDeleteCameraInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'cameras', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfDeleteCameraInverse = (inverse: GltfDeleteCameraInverse): string => JSON.stringify(inverse);
export const deriveGltfDeleteCameraInverse = (base: GltfSnapshot, payload: GltfDeleteCameraPayload): { accepted: true; inverse: GltfDeleteCameraInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfDeleteCamera(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.delete-camera.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'cameras') }, touchedPaths: paths }; };
