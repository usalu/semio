/** ↩️ move-mesh leaf-owned inverse reconstruction from the exact base. */
import type { GltfMesh, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfMoveMesh, type GltfMoveMeshPayload } from '../../move-mesh/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfMoveMeshInverseDescriptor = { id: 's.stdio.gltf.mutation.move-mesh.v1.inverse', version: 1, touchedPathPattern: 'document/meshes' } as const;
export type GltfMoveMeshInverseOperation = GltfStructuralOperation<GltfMesh>;
export interface GltfMoveMeshInverse { readonly id: 's.stdio.gltf.mutation.move-mesh.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfMoveMeshPayload; readonly operation: GltfMoveMeshInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfMoveMeshPayload): GltfMoveMeshInverseOperation => ({ kind: 'move', index: payload.position, position: payload.index });
const inverseTouchedPaths = (payload: GltfMoveMeshPayload): readonly string[] => [`document/meshes/${payload.index}`, `document/meshes/${payload.position}`];
export const validateGltfMoveMeshInverse = (inverse: GltfMoveMeshInverse): GltfMutationRejection | undefined => inverse.id !== GltfMoveMeshInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfMoveMeshInverse = (after: GltfSnapshot, inverse: GltfMoveMeshInverse): GltfStructuralResult => { const rejection = validateGltfMoveMeshInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'meshes', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfMoveMeshInverse = (inverse: GltfMoveMeshInverse): string => JSON.stringify(inverse);
export const deriveGltfMoveMeshInverse = (base: GltfSnapshot, payload: GltfMoveMeshPayload): { accepted: true; inverse: GltfMoveMeshInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfMoveMesh(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.move-mesh.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'meshes') }, touchedPaths: paths }; };
