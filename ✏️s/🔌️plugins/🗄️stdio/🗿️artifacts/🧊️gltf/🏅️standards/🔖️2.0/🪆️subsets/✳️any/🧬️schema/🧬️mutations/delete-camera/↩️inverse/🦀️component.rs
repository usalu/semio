//! ↩️ delete-camera leaf-owned inverse reconstruction from exact base values.
use serde::{Deserialize, Serialize};
use crate::artifacts::gltf::GltfSnapshot;
use crate::artifacts::gltf::schema::snapshot::GltfCamera;
use crate::artifacts::gltf::schema::mutations::delete_camera::mutation::{validate, GltfDeleteCameraPayload};
use crate::artifacts::gltf::schema::mutations::top_level_collections_private::{cameras_op, reject, repair, Change, GltfTopLevelFamily, GltfTopLevelMutationRejection};
pub const ID: &str = "s.stdio.gltf.mutation.delete-camera.v1.inverse";
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)] #[serde(tag = "kind", rename_all = "camelCase")]
pub enum GltfDeleteCameraInverseOperation { Insert { position: usize, item: GltfCamera }, Delete { index: usize, removed: GltfCamera }, Move { index: usize, position: usize }, Reorder { order: Vec<usize> } }
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)] #[serde(rename_all = "camelCase")]
pub struct GltfDeleteCameraInverse { pub id: String, pub version: u32, pub touched_paths: Vec<String>, pub payload: GltfDeleteCameraPayload, pub operation: GltfDeleteCameraInverseOperation }
fn operation(payload: &GltfDeleteCameraPayload, base: &GltfSnapshot) -> GltfDeleteCameraInverseOperation { GltfDeleteCameraInverseOperation::Insert { position: payload.index, item: base.document.cameras[payload.index].clone() } }
fn touched_paths(payload: &GltfDeleteCameraPayload) -> Vec<String> { vec![format!("document/cameras/{}", payload.index)] }
pub fn validate_inverse(inverse: &GltfDeleteCameraInverse) -> Result<(), GltfTopLevelMutationRejection> { if inverse.id != ID || inverse.version != 1 { return Err(reject("gltf.mutation.invalid-inverse-envelope", "inverse", "descriptor identity does not match")); } if inverse.touched_paths != touched_paths(&inverse.payload) { return Err(reject("gltf.mutation.invalid-touched-paths", "inverse/touchedPaths", "paths must be concrete payload-derived locations")); } Ok(()) }
pub fn apply_inverse(inverse: &GltfDeleteCameraInverse, after: &GltfSnapshot) -> Result<GltfSnapshot, GltfTopLevelMutationRejection> { validate_inverse(inverse)?; let operation = &inverse.operation; match operation { GltfDeleteCameraInverseOperation::Insert { position, .. } if *position > after.document.cameras.len() => return Err(reject("gltf.mutation.insert-out-of-range", "document/cameras", "position must be within the collection")), GltfDeleteCameraInverseOperation::Delete { index, .. } if *index >= after.document.cameras.len() => return Err(reject("gltf.mutation.index-out-of-range", "document/cameras", "index must address an item")), GltfDeleteCameraInverseOperation::Move { index, position } if *index >= after.document.cameras.len() || *position >= after.document.cameras.len() => return Err(reject("gltf.mutation.index-out-of-range", "document/cameras", "indices must address items")), GltfDeleteCameraInverseOperation::Reorder { order } if order.is_empty() || order.len() != after.document.cameras.len() || order.iter().collect::<std::collections::BTreeSet<_>>().len() != order.len() || order.iter().any(|index| *index >= after.document.cameras.len()) => return Err(reject("gltf.mutation.invalid-permutation", "document/cameras", "order must be a non-empty permutation")), _ => {} } let mut next = after.clone(); match operation { GltfDeleteCameraInverseOperation::Insert { position, item } => { repair(&mut next.document, GltfTopLevelFamily::Cameras, &Change::Insert(*position))?; next.document.cameras.insert(*position, item.clone());  }, GltfDeleteCameraInverseOperation::Delete { index, .. } => { cameras_op(&mut next, GltfTopLevelFamily::Cameras, *index, None, None)?;  }, GltfDeleteCameraInverseOperation::Move { index, position } => { cameras_op(&mut next, GltfTopLevelFamily::Cameras, *index, Some(*position), None)?;  }, GltfDeleteCameraInverseOperation::Reorder { order } => { cameras_op(&mut next, GltfTopLevelFamily::Cameras, order[0], None, Some(order))?;  } } Ok(next) }
pub fn encode(inverse: &GltfDeleteCameraInverse) -> Result<Vec<u8>, GltfTopLevelMutationRejection> { serde_json::to_vec(inverse).map_err(|error| reject("gltf.mutation.encode-failed", "inverse", error.to_string())) }
pub fn derive(payload: &GltfDeleteCameraPayload, base: &GltfSnapshot) -> Result<GltfDeleteCameraInverse, GltfTopLevelMutationRejection> { validate(payload, base)?; Ok(GltfDeleteCameraInverse { id: ID.into(), version: 1, touched_paths: touched_paths(payload), payload: payload.clone(), operation: operation(payload, base) }) }
