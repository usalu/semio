/** 🔺️ reorder-images leaf-owned typed sparse operation diff. */
import type { GltfImage, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfReorderImages, validateGltfReorderImages, type GltfReorderImagesPayload } from '../../reorder-images/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfReorderImagesDescriptor = { id: 's.stdio.gltf.mutation.reorder-images.v1', version: 1, touchedPathPattern: 'document/images' } as const;
export type GltfReorderImagesOperation = GltfStructuralOperation<GltfImage>;
export interface GltfReorderImagesDiff { readonly id: 's.stdio.gltf.mutation.reorder-images.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfReorderImagesPayload; readonly operation: GltfReorderImagesOperation; }
const operation = (base: GltfSnapshot, payload: GltfReorderImagesPayload): GltfReorderImagesOperation => ({ kind: 'reorder', order: [...payload.order] });
const touchedPaths = (payload: GltfReorderImagesPayload): readonly string[] => payload.order.map(index => `document/images/${index}`);
export const validateGltfReorderImagesDiff = (diff: GltfReorderImagesDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfReorderImagesDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfReorderImages(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfReorderImagesDiff = (base: GltfSnapshot, diff: GltfReorderImagesDiff): GltfStructuralResult => { const rejection = validateGltfReorderImagesDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'images', diff.operation); };
export const encodeGltfReorderImagesDiff = (diff: GltfReorderImagesDiff): string => JSON.stringify(diff);
export const deriveGltfReorderImagesDiff = (base: GltfSnapshot, payload: GltfReorderImagesPayload): { accepted: true; diff: GltfReorderImagesDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfReorderImages(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.reorder-images.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfReorderImagesDiff above. */
export const deriveTransitionalGltfReorderImagesGltfDiff = (base: GltfSnapshot, payload: GltfReorderImagesPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfReorderImages(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('images', base, applied.snapshot) } : applied; };
