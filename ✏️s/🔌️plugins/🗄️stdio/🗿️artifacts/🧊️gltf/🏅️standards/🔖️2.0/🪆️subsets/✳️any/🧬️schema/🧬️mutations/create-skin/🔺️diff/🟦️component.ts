/** 🔺️ create-skin leaf-owned typed sparse operation diff. */
import type { GltfSkin, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfCreateSkin, validateGltfCreateSkin, type GltfCreateSkinPayload } from '../../create-skin/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfCreateSkinDescriptor = { id: 's.stdio.gltf.mutation.create-skin.v1', version: 1, touchedPathPattern: 'document/skins' } as const;
export type GltfCreateSkinOperation = GltfStructuralOperation<GltfSkin>;
export interface GltfCreateSkinDiff { readonly id: 's.stdio.gltf.mutation.create-skin.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfCreateSkinPayload; readonly operation: GltfCreateSkinOperation; }
const operation = (base: GltfSnapshot, payload: GltfCreateSkinPayload): GltfCreateSkinOperation => ({ kind: 'insert', position: payload.position, item: { joints: [] } });
const touchedPaths = (payload: GltfCreateSkinPayload): readonly string[] => [`document/skins/${payload.position}`];
export const validateGltfCreateSkinDiff = (diff: GltfCreateSkinDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfCreateSkinDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfCreateSkin(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfCreateSkinDiff = (base: GltfSnapshot, diff: GltfCreateSkinDiff): GltfStructuralResult => { const rejection = validateGltfCreateSkinDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'skins', diff.operation); };
export const encodeGltfCreateSkinDiff = (diff: GltfCreateSkinDiff): string => JSON.stringify(diff);
export const deriveGltfCreateSkinDiff = (base: GltfSnapshot, payload: GltfCreateSkinPayload): { accepted: true; diff: GltfCreateSkinDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfCreateSkin(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.create-skin.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfCreateSkinDiff above. */
export const deriveTransitionalGltfCreateSkinGltfDiff = (base: GltfSnapshot, payload: GltfCreateSkinPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfCreateSkin(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('skins', base, applied.snapshot) } : applied; };
