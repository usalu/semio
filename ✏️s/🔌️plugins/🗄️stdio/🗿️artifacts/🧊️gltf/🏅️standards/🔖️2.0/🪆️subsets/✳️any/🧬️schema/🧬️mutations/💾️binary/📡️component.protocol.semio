dialect protocol stdio.gltf.mutations
; Binary wire form IS the mutation's own internally-tagged JSON text (see sibling 📝️text
; grammar.semio), UTF-8 encoded -- OpBinary::encode_op/decode_op delegate straight to
; serde_json::to_vec/from_slice. No separate binary framing exists.
document = json_utf8_text:bytes(eof)
