/** 🦠️ move-primitive-attribute: cohesive atomic mesh mutation. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget } from '../../📸️snapshot/🟦️.ts';
import { run, reject, positionIn, itemIndex, permutation, moveItem, type GltfLeafResult, type GltfMutationRejection } from './🟦️';
export const GltfMovePrimitiveAttributeDescriptor = { id: 's.stdio.gltf.mutation.move-primitive-attribute.v1', version: 1, kind: 'move', touchedPaths: ["document/meshes/*/primitives/*/attributes"], referencePolicy: 'moves one semantic-keyed attribute in its stable ordered map' } as const;
export interface GltfMovePrimitiveAttributePayload { mesh: number; primitive: number; semantic: string; position: number }
export type GltfMovePrimitiveAttributeResult = GltfLeafResult;
export const validateGltfMovePrimitiveAttribute = (payload: GltfMovePrimitiveAttributePayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const mesh = itemIndex(payload.mesh, base.document.meshes.length, 'document/meshes'); if (mesh) return mesh; const primitive = itemIndex(payload.primitive, base.document.meshes[payload.mesh]!.primitives.length, `document/meshes/${payload.mesh}/primitives`); if (primitive) return primitive; const keys = Object.keys(base.document.meshes[payload.mesh]!.primitives[payload.primitive]!.attributes); const index = keys.indexOf(payload.semantic); if (index < 0) return reject('gltf.mutation.relation-absent', 'document/meshes/primitives/attributes', 'semantic is not bound'); const position = itemIndex(payload.position, keys.length, 'document/meshes/primitives/attributes'); if (position) return position; if (position && index === payload.position) return position; return undefined; };
export const applyGltfMovePrimitiveAttribute = (base: GltfSnapshot, payload: GltfMovePrimitiveAttributePayload): GltfMovePrimitiveAttributeResult => run(base, payload, validateGltfMovePrimitiveAttribute, (next, payload) => { const attributes = next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.attributes; const entries = Object.entries(attributes); moveItem(entries, entries.findIndex(([semantic]) => semantic === payload.semantic), payload.position); next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.attributes = Object.fromEntries(entries); }, GltfMovePrimitiveAttributeDescriptor.touchedPaths);
