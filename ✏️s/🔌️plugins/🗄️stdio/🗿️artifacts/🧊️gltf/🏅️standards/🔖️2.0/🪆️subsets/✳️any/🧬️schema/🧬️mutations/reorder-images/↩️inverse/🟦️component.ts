/** ↩️ reorder-images leaf-owned inverse reconstruction from the exact base. */
import type { GltfImage, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfReorderImages, type GltfReorderImagesPayload } from '../../reorder-images/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfReorderImagesInverseDescriptor = { id: 's.stdio.gltf.mutation.reorder-images.v1.inverse', version: 1, touchedPathPattern: 'document/images' } as const;
export type GltfReorderImagesInverseOperation = GltfStructuralOperation<GltfImage>;
export interface GltfReorderImagesInverse { readonly id: 's.stdio.gltf.mutation.reorder-images.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfReorderImagesPayload; readonly operation: GltfReorderImagesInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfReorderImagesPayload): GltfReorderImagesInverseOperation => ({ kind: 'reorder', order: payload.order.map((_, position) => payload.order.indexOf(position)) });
const inverseTouchedPaths = (payload: GltfReorderImagesPayload): readonly string[] => payload.order.map(index => `document/images/${index}`);
export const validateGltfReorderImagesInverse = (inverse: GltfReorderImagesInverse): GltfMutationRejection | undefined => inverse.id !== GltfReorderImagesInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfReorderImagesInverse = (after: GltfSnapshot, inverse: GltfReorderImagesInverse): GltfStructuralResult => { const rejection = validateGltfReorderImagesInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'images', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfReorderImagesInverse = (inverse: GltfReorderImagesInverse): string => JSON.stringify(inverse);
export const deriveGltfReorderImagesInverse = (base: GltfSnapshot, payload: GltfReorderImagesPayload): { accepted: true; inverse: GltfReorderImagesInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfReorderImages(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.reorder-images.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'images') }, touchedPaths: paths }; };
