/** 🔺️ move-sampler leaf-owned typed sparse operation diff. */
import type { GltfSampler, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfMoveSampler, validateGltfMoveSampler, type GltfMoveSamplerPayload } from '../../move-sampler/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfMoveSamplerDescriptor = { id: 's.stdio.gltf.mutation.move-sampler.v1', version: 1, touchedPathPattern: 'document/samplers' } as const;
export type GltfMoveSamplerOperation = GltfStructuralOperation<GltfSampler>;
export interface GltfMoveSamplerDiff { readonly id: 's.stdio.gltf.mutation.move-sampler.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfMoveSamplerPayload; readonly operation: GltfMoveSamplerOperation; }
const operation = (base: GltfSnapshot, payload: GltfMoveSamplerPayload): GltfMoveSamplerOperation => ({ kind: 'move', index: payload.index, position: payload.position });
const touchedPaths = (payload: GltfMoveSamplerPayload): readonly string[] => [`document/samplers/${payload.index}`, `document/samplers/${payload.position}`];
export const validateGltfMoveSamplerDiff = (diff: GltfMoveSamplerDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfMoveSamplerDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfMoveSampler(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfMoveSamplerDiff = (base: GltfSnapshot, diff: GltfMoveSamplerDiff): GltfStructuralResult => { const rejection = validateGltfMoveSamplerDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'samplers', diff.operation); };
export const encodeGltfMoveSamplerDiff = (diff: GltfMoveSamplerDiff): string => JSON.stringify(diff);
export const deriveGltfMoveSamplerDiff = (base: GltfSnapshot, payload: GltfMoveSamplerPayload): { accepted: true; diff: GltfMoveSamplerDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfMoveSampler(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.move-sampler.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfMoveSamplerDiff above. */
export const deriveTransitionalGltfMoveSamplerGltfDiff = (base: GltfSnapshot, payload: GltfMoveSamplerPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfMoveSampler(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('samplers', base, applied.snapshot) } : applied; };
