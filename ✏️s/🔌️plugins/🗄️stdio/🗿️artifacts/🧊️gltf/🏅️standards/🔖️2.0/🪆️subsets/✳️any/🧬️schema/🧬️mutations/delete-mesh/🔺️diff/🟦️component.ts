/** 🔺️ delete-mesh leaf-owned typed sparse operation diff. */
import type { GltfMesh, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfDeleteMesh, validateGltfDeleteMesh, type GltfDeleteMeshPayload } from '../../delete-mesh/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteMeshDescriptor = { id: 's.stdio.gltf.mutation.delete-mesh.v1', version: 1, touchedPathPattern: 'document/meshes' } as const;
export type GltfDeleteMeshOperation = GltfStructuralOperation<GltfMesh>;
export interface GltfDeleteMeshDiff { readonly id: 's.stdio.gltf.mutation.delete-mesh.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfDeleteMeshPayload; readonly operation: GltfDeleteMeshOperation; }
const operation = (base: GltfSnapshot, payload: GltfDeleteMeshPayload): GltfDeleteMeshOperation => ({ kind: 'delete', index: payload.index, removed: base.document.meshes[payload.index]! });
const touchedPaths = (payload: GltfDeleteMeshPayload): readonly string[] => [`document/meshes/${payload.index}`];
export const validateGltfDeleteMeshDiff = (diff: GltfDeleteMeshDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfDeleteMeshDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfDeleteMesh(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfDeleteMeshDiff = (base: GltfSnapshot, diff: GltfDeleteMeshDiff): GltfStructuralResult => { const rejection = validateGltfDeleteMeshDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'meshes', diff.operation); };
export const encodeGltfDeleteMeshDiff = (diff: GltfDeleteMeshDiff): string => JSON.stringify(diff);
export const deriveGltfDeleteMeshDiff = (base: GltfSnapshot, payload: GltfDeleteMeshPayload): { accepted: true; diff: GltfDeleteMeshDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfDeleteMesh(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.delete-mesh.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfDeleteMeshDiff above. */
export const deriveTransitionalGltfDeleteMeshGltfDiff = (base: GltfSnapshot, payload: GltfDeleteMeshPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfDeleteMesh(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('meshes', base, applied.snapshot) } : applied; };
