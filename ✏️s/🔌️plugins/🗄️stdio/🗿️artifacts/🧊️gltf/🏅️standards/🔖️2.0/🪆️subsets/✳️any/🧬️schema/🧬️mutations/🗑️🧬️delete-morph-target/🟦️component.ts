/** 🦠️ delete-morph-target: cohesive atomic mesh mutation. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget } from '../../📸️snapshot/🟦️component.ts';
import { run, reject, positionIn, itemIndex, permutation, moveItem, type GltfLeafResult, type GltfMutationRejection } from '../../🔨️modules/🧬️mutation-support/📚️top-level/🟦️component.ts';
export const GltfDeleteMorphTargetDescriptor = { id: 's.stdio.gltf.mutation.delete-morph-target.v1', version: 1, kind: 'delete', touchedPaths: ["document/meshes/*/primitives/*/targets"], referencePolicy: 'removes one target only when mesh target-count coherence survives' } as const;
export interface GltfDeleteMorphTargetPayload { mesh: number; primitive: number; target: number }
export type GltfDeleteMorphTargetResult = GltfLeafResult;
export const validateGltfDeleteMorphTarget = (payload: GltfDeleteMorphTargetPayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const mesh = itemIndex(payload.mesh, base.document.meshes.length, 'document/meshes'); if (mesh) return mesh; const primitive = itemIndex(payload.primitive, base.document.meshes[payload.mesh]!.primitives.length, `document/meshes/${payload.mesh}/primitives`); if (primitive) return primitive; const target = itemIndex(payload.target, base.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets.length, 'document/meshes/primitives/targets'); if (target) return target; if (base.document.meshes[payload.mesh]!.primitives.length !== 1) return reject('gltf.mutation.morph-target-arity', 'document/meshes/primitives/targets', 'delete is rejected until every primitive can remain target-count coherent'); if (base.document.meshes[payload.mesh]!.weights.length) return reject('gltf.mutation.morph-weight-arity', 'document/meshes/weights', 'clear or change mesh weights before deleting a target'); return undefined; };
export const applyGltfDeleteMorphTarget = (base: GltfSnapshot, payload: GltfDeleteMorphTargetPayload): GltfDeleteMorphTargetResult => run(base, payload, validateGltfDeleteMorphTarget, (next, payload) => { next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets.splice(payload.target, 1); }, GltfDeleteMorphTargetDescriptor.touchedPaths);
