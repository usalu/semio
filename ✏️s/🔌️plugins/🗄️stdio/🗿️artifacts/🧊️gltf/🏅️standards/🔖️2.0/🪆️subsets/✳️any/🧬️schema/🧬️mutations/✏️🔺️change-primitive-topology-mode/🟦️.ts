/** 🦠️ change-primitive-topology-mode: cohesive atomic mesh mutation. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget } from '../../📸️snapshot/🟦️.ts';
import { run, reject, positionIn, itemIndex, permutation, moveItem, type GltfLeafResult, type GltfMutationRejection } from './🟦️';
export const GltfChangePrimitiveTopologyModeDescriptor = { id: 's.stdio.gltf.mutation.change-primitive-topology-mode.v1', version: 1, kind: 'change', touchedPaths: ["document/meshes/*/primitives/*/mode"], referencePolicy: 'accepts only the glTF primitive topology domain 0 through 6' } as const;
export interface GltfChangePrimitiveTopologyModePayload { mesh: number; primitive: number; mode: number }
export type GltfChangePrimitiveTopologyModeResult = GltfLeafResult;
export const validateGltfChangePrimitiveTopologyMode = (payload: GltfChangePrimitiveTopologyModePayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const mesh = itemIndex(payload.mesh, base.document.meshes.length, 'document/meshes'); if (mesh) return mesh; const primitive = itemIndex(payload.primitive, base.document.meshes[payload.mesh]!.primitives.length, `document/meshes/${payload.mesh}/primitives`); if (primitive) return primitive; if (!Number.isInteger(payload.mode) || payload.mode < 0 || payload.mode > 6) return reject('gltf.mutation.invalid-topology-mode', 'document/meshes/primitives/mode', 'mode must be in the glTF topology domain'); return undefined; };
export const applyGltfChangePrimitiveTopologyMode = (base: GltfSnapshot, payload: GltfChangePrimitiveTopologyModePayload): GltfChangePrimitiveTopologyModeResult => run(base, payload, validateGltfChangePrimitiveTopologyMode, (next, payload) => { next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.mode = payload.mode; }, GltfChangePrimitiveTopologyModeDescriptor.touchedPaths);
