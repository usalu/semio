/** 🦠️ reorder-morph-target-attributes: cohesive atomic mesh mutation. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget } from '../../📸️snapshot/🟦️.ts';
import { run, reject, positionIn, itemIndex, permutation, moveItem, type GltfLeafResult, type GltfMutationRejection } from '../../🔨️modules/🧬️mutation-support/📚️top-level/🟦️.ts';
export const GltfReorderMorphTargetAttributesDescriptor = { id: 's.stdio.gltf.mutation.reorder-morph-target-attributes.v1', version: 1, kind: 'reorder', touchedPaths: ["document/meshes/*/primitives/*/targets/*/attributes"], referencePolicy: 'accepts only the complete target semantic permutation' } as const;
export interface GltfReorderMorphTargetAttributesPayload { mesh: number; primitive: number; target: number; order: string[] }
export type GltfReorderMorphTargetAttributesResult = GltfLeafResult;
export const validateGltfReorderMorphTargetAttributes = (payload: GltfReorderMorphTargetAttributesPayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const mesh = itemIndex(payload.mesh, base.document.meshes.length, 'document/meshes'); if (mesh) return mesh; const primitive = itemIndex(payload.primitive, base.document.meshes[payload.mesh]!.primitives.length, `document/meshes/${payload.mesh}/primitives`); if (primitive) return primitive; const target = itemIndex(payload.target, base.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets.length, 'document/meshes/primitives/targets'); if (target) return target; const keys = Object.keys(base.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets[payload.target]!); if (payload.order.length !== keys.length || new Set(payload.order).size !== keys.length || payload.order.some(semantic => !keys.includes(semantic))) return reject('gltf.mutation.invalid-permutation', 'document/meshes/primitives/targets', 'order must contain every semantic once'); return undefined; };
export const applyGltfReorderMorphTargetAttributes = (base: GltfSnapshot, payload: GltfReorderMorphTargetAttributesPayload): GltfReorderMorphTargetAttributesResult => run(base, payload, validateGltfReorderMorphTargetAttributes, (next, payload) => { const target = next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets[payload.target]!; next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets[payload.target] = Object.fromEntries(payload.order.map(semantic => [semantic, target[semantic]! ])); }, GltfReorderMorphTargetAttributesDescriptor.touchedPaths);
