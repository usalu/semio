/** 🦠️ change-node-morph-weights is an atomic, typed glTF 2.0 command. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget, GltfAccessor, GltfSparseAccessor, GltfSparseIndices, GltfSparseValues } from '../../📸️snapshot/🟦️component.ts';
import { run, reject, positionIn, itemIndex, moveItem, type GltfLeafResult, type GltfMutationRejection } from '../../🔨️modules/🧬️mutation-support/📚️top-level/🟦️component.ts';
export const GltfChangeNodeMorphWeightsDescriptor = { id: 's.stdio.gltf.mutation.change-node-morph-weights.v1', version: 1, kind: 'change', touchedPaths: ["document/nodes/*/weights"], referencePolicy: 'requires finite weights and verifies the bound mesh target arity' } as const;
export interface GltfChangeNodeMorphWeightsPayload { node: number; weights: number[] }
export type GltfChangeNodeMorphWeightsResult = GltfLeafResult;
export const validateGltfChangeNodeMorphWeights = (payload: GltfChangeNodeMorphWeightsPayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const node = itemIndex(payload.node, base.document.nodes.length, 'document/nodes'); if (node) return node; if (!payload.weights.every(Number.isFinite)) return reject('gltf.mutation.invalid-morph-weights', `document/nodes/${payload.node}/weights`, 'weights must be finite'); const mesh = base.document.nodes[payload.node]!.mesh; if (payload.weights.length && mesh === undefined) return reject('gltf.mutation.missing-mesh', `document/nodes/${payload.node}/mesh`, 'morph weights require a mesh'); if (mesh !== undefined && base.document.meshes[mesh]!.primitives.some(primitive => primitive.targets.length !== payload.weights.length)) return reject('gltf.mutation.morph-weight-arity', `document/nodes/${payload.node}/weights`, 'weights must match every primitive target count'); return undefined; };
export const applyGltfChangeNodeMorphWeights = (base: GltfSnapshot, payload: GltfChangeNodeMorphWeightsPayload): GltfChangeNodeMorphWeightsResult => run(base, payload, validateGltfChangeNodeMorphWeights, (next, payload) => { next.document.nodes[payload.node]!.weights = [...payload.weights]; }, GltfChangeNodeMorphWeightsDescriptor.touchedPaths);
