/** ↩️ change-material-double-sided reconstructs the prior render-state value. */
import type { GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts'; import { validateGltfChangeMaterialDoubleSided, type GltfChangeMaterialDoubleSidedPayload, type GltfChangeMaterialDoubleSidedRejection } from '../../change-material-double-sided/🦠️mutation/🟦️component.ts';
export interface GltfChangeMaterialDoubleSidedInverse { material: number; expectedDoubleSided: boolean; doubleSided: boolean; touchedPaths: readonly string[] }
export const touchedPathsGltfChangeMaterialDoubleSidedInverse = (inverse: GltfChangeMaterialDoubleSidedInverse): string[] => [`document/materials/${inverse.material}/doubleSided`];
export const reconstructGltfChangeMaterialDoubleSidedInverse = (base: GltfSnapshot, payload: GltfChangeMaterialDoubleSidedPayload): { accepted: true; inverse: GltfChangeMaterialDoubleSidedInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfChangeMaterialDoubleSidedRejection } => { const rejection = validateGltfChangeMaterialDoubleSided(payload, base); if (rejection) return { accepted: false, rejection }; const touchedPaths = [`document/materials/${payload.material}/doubleSided`]; const inverse = { material: payload.material, expectedDoubleSided: payload.doubleSided, doubleSided: base.document.materials[payload.material].doubleSided, touchedPaths }; return { accepted: true, inverse, touchedPaths }; };
export const applyGltfChangeMaterialDoubleSidedInverse = (snapshot: GltfSnapshot, inverse: GltfChangeMaterialDoubleSidedInverse): GltfChangeMaterialDoubleSidedRejection | undefined => { const expectedPaths = touchedPathsGltfChangeMaterialDoubleSidedInverse(inverse); if (JSON.stringify(inverse.touchedPaths) !== JSON.stringify(expectedPaths)) return { code: 'gltf.mutation.invalid-touched-paths', path: 'inverse/touchedPaths', detail: 'touched paths must equal the concrete material double-sided path' }; const material = snapshot.document.materials[inverse.material]; if (!material) return { code: 'gltf.mutation.index-out-of-range', path: 'document/materials', detail: 'the addressed index must exist' }; if (material.doubleSided !== inverse.expectedDoubleSided) return { code: 'gltf.mutation.stale-inverse', path: `document/materials/${inverse.material}/doubleSided`, detail: 'current double-sided value does not equal the planned forward result' }; material.doubleSided = inverse.doubleSided; };
