{
  "id": "s.stdio.gltf.mutation.bind-node-skin.v1",
  "command": "bind-node-skin",
  "laws": [
    "typed payload validation and typed rejection",
    "direct sparse diff only at declared touched paths",
    "inverse derives from exact base and restores that base when applied",
    "reference integrity is repaired or rejected at the named typed paths",
    "apply and replay are deterministic"
  ],
  "referencePolicy": "validates the typed skin reference",
  "touchedPaths": [
    "document/nodes/*/skin"
  ]
}
