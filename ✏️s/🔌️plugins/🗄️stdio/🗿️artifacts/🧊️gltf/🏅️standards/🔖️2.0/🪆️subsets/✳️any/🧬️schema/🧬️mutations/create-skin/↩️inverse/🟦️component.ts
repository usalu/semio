/** ↩️ create-skin leaf-owned inverse reconstruction from the exact base. */
import type { GltfSkin, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfCreateSkin, type GltfCreateSkinPayload } from '../../create-skin/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfCreateSkinInverseDescriptor = { id: 's.stdio.gltf.mutation.create-skin.v1.inverse', version: 1, touchedPathPattern: 'document/skins' } as const;
export type GltfCreateSkinInverseOperation = GltfStructuralOperation<GltfSkin>;
export interface GltfCreateSkinInverse { readonly id: 's.stdio.gltf.mutation.create-skin.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfCreateSkinPayload; readonly operation: GltfCreateSkinInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfCreateSkinPayload): GltfCreateSkinInverseOperation => ({ kind: 'delete', index: payload.position, removed: { joints: [] } });
const inverseTouchedPaths = (payload: GltfCreateSkinPayload): readonly string[] => [`document/skins/${payload.position}`];
export const validateGltfCreateSkinInverse = (inverse: GltfCreateSkinInverse): GltfMutationRejection | undefined => inverse.id !== GltfCreateSkinInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfCreateSkinInverse = (after: GltfSnapshot, inverse: GltfCreateSkinInverse): GltfStructuralResult => { const rejection = validateGltfCreateSkinInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'skins', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfCreateSkinInverse = (inverse: GltfCreateSkinInverse): string => JSON.stringify(inverse);
export const deriveGltfCreateSkinInverse = (base: GltfSnapshot, payload: GltfCreateSkinPayload): { accepted: true; inverse: GltfCreateSkinInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfCreateSkin(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.create-skin.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'skins') }, touchedPaths: paths }; };
