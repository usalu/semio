# 💡️ Gltf inference schema — mesh-primitive POSITION-accessor-derived bounding box.

type GltfInference {
  bounds: GltfBounds! @derived
}

type GltfBounds {
  min: [Float!]!
  max: [Float!]!
  vertexCount: Int!
  meshCount: Int!
  primitiveCount: Int!
}
