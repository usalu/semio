/** ↩️ delete-mesh leaf-owned inverse reconstruction from the exact base. */
import type { GltfMesh, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfDeleteMesh, type GltfDeleteMeshPayload } from '../../delete-mesh/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteMeshInverseDescriptor = { id: 's.stdio.gltf.mutation.delete-mesh.v1.inverse', version: 1, touchedPathPattern: 'document/meshes' } as const;
export type GltfDeleteMeshInverseOperation = GltfStructuralOperation<GltfMesh>;
export interface GltfDeleteMeshInverse { readonly id: 's.stdio.gltf.mutation.delete-mesh.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfDeleteMeshPayload; readonly operation: GltfDeleteMeshInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfDeleteMeshPayload): GltfDeleteMeshInverseOperation => ({ kind: 'insert', position: payload.index, item: base.document.meshes[payload.index]! });
const inverseTouchedPaths = (payload: GltfDeleteMeshPayload): readonly string[] => [`document/meshes/${payload.index}`];
export const validateGltfDeleteMeshInverse = (inverse: GltfDeleteMeshInverse): GltfMutationRejection | undefined => inverse.id !== GltfDeleteMeshInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfDeleteMeshInverse = (after: GltfSnapshot, inverse: GltfDeleteMeshInverse): GltfStructuralResult => { const rejection = validateGltfDeleteMeshInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'meshes', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfDeleteMeshInverse = (inverse: GltfDeleteMeshInverse): string => JSON.stringify(inverse);
export const deriveGltfDeleteMeshInverse = (base: GltfSnapshot, payload: GltfDeleteMeshPayload): { accepted: true; inverse: GltfDeleteMeshInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfDeleteMesh(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.delete-mesh.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'meshes') }, touchedPaths: paths }; };
