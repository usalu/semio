/** ↩️ delete-image leaf-owned inverse reconstruction from the exact base. */
import type { GltfImage, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfDeleteImage, type GltfDeleteImagePayload } from '../../delete-image/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteImageInverseDescriptor = { id: 's.stdio.gltf.mutation.delete-image.v1.inverse', version: 1, touchedPathPattern: 'document/images' } as const;
export type GltfDeleteImageInverseOperation = GltfStructuralOperation<GltfImage>;
export interface GltfDeleteImageInverse { readonly id: 's.stdio.gltf.mutation.delete-image.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfDeleteImagePayload; readonly operation: GltfDeleteImageInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfDeleteImagePayload): GltfDeleteImageInverseOperation => ({ kind: 'insert', position: payload.index, item: base.document.images[payload.index]! });
const inverseTouchedPaths = (payload: GltfDeleteImagePayload): readonly string[] => [`document/images/${payload.index}`];
export const validateGltfDeleteImageInverse = (inverse: GltfDeleteImageInverse): GltfMutationRejection | undefined => inverse.id !== GltfDeleteImageInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfDeleteImageInverse = (after: GltfSnapshot, inverse: GltfDeleteImageInverse): GltfStructuralResult => { const rejection = validateGltfDeleteImageInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'images', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfDeleteImageInverse = (inverse: GltfDeleteImageInverse): string => JSON.stringify(inverse);
export const deriveGltfDeleteImageInverse = (base: GltfSnapshot, payload: GltfDeleteImagePayload): { accepted: true; inverse: GltfDeleteImageInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfDeleteImage(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.delete-image.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'images') }, touchedPaths: paths }; };
