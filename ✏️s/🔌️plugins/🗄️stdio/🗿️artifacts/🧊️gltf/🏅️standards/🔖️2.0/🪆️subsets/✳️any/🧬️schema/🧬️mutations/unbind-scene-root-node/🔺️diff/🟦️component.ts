/** 🔺️ Exact scene-root removal patch. */
import type { GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfUnbindSceneRootNode, type GltfUnbindSceneRootNodePayload } from '../../unbind-scene-root-node/🦠️mutation/🟦️component.ts';
import type { GltfMutationRejection } from '../../🔒️top-level-private/🟦️component.ts';
export interface GltfUnbindSceneRootNodeDiff { scene: number; node: number; position: number; touchedPaths: readonly [string] }
export type GltfUnbindSceneRootNodeDiffResult = { accepted: true; diff: GltfUnbindSceneRootNodeDiff } | { accepted: false; rejection: GltfMutationRejection };
export type GltfUnbindSceneRootNodeDiffApplyResult = { accepted: true; snapshot: GltfSnapshot } | { accepted: false; rejection: GltfMutationRejection };
export const deriveGltfUnbindSceneRootNodeDiff = (base: GltfSnapshot, payload: GltfUnbindSceneRootNodePayload): GltfUnbindSceneRootNodeDiffResult => { const rejection = validateGltfUnbindSceneRootNode(payload, base); if (rejection) return { accepted: false, rejection }; const position = base.document.scenes[payload.scene]!.nodes.indexOf(payload.node); return { accepted: true, diff: { scene: payload.scene, node: payload.node, position, touchedPaths: [`document/scenes/${payload.scene}/nodes/${position}`] } }; };
export const applyGltfUnbindSceneRootNodeDiff = (base: GltfSnapshot, diff: GltfUnbindSceneRootNodeDiff): GltfUnbindSceneRootNodeDiffApplyResult => { const path = `document/scenes/${diff.scene}/nodes/${diff.position}`; if (diff.touchedPaths.length !== 1 || diff.touchedPaths[0] !== path) return { accepted: false, rejection: { code: 'gltf.mutation.invalid-touched-path', path, detail: 'patch touched path does not match its root coordinates' } }; const nodes = base.document.scenes[diff.scene]?.nodes; if (!nodes || nodes[diff.position] !== diff.node) return { accepted: false, rejection: { code: 'gltf.mutation.stale-diff', path, detail: 'node is not at the recorded removal position' } }; const snapshot = structuredClone(base); snapshot.document.scenes[diff.scene]!.nodes.splice(diff.position, 1); return { accepted: true, snapshot }; };
export const encodeGltfUnbindSceneRootNodeDiff = (diff: GltfUnbindSceneRootNodeDiff): string => JSON.stringify(diff);
