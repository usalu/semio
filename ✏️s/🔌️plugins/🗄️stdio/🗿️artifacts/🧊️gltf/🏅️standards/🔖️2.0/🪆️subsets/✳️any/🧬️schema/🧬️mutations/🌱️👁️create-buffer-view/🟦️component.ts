/** 🦠️ create-buffer-view executable structural glTF command. */
import type { GltfOrthographic, GltfPerspective, GltfSnapshot } from '../../📸️snapshot/🟦️component.ts';
import { clone, insert, order, position, reject, remove, relocate, reorder, repair, type GltfMutationRejection, type GltfStructuralResult } from '../../🔨️modules/🧬️mutation-support/🗂️top-level-collections/🟦️component.ts';
export const GltfCreateBufferViewDescriptor = { id: 's.stdio.gltf.mutation.create-buffer-view.v1', version: 1, touchedPathPattern: 'document/bufferViews', referencePolicy: 'all typed buffer-view references are remapped, repaired, or rejected' } as const;
export interface GltfCreateBufferViewPayload { position: number; buffer: number; byteOffset: number; byteLength: number }
export const validateGltfCreateBufferView = (payload: GltfCreateBufferViewPayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const index = position(payload.position, base.document.bufferViews.length, 'document/bufferViews', true); if (index) return index;  if (position(payload.buffer, base.document.buffers.length, 'document/buffers')) return reject('gltf.mutation.index-out-of-range', 'document/buffers', 'backing buffer must exist'); if (!Number.isInteger(payload.byteOffset) || payload.byteOffset < 0 || !Number.isInteger(payload.byteLength) || payload.byteLength < 0) return reject('gltf.mutation.invalid-buffer-view-layout', 'document/bufferViews', 'offset and length must be non-negative integers');  return undefined; };
export const applyGltfCreateBufferView = (base: GltfSnapshot, payload: GltfCreateBufferViewPayload): GltfStructuralResult => { const rejection = validateGltfCreateBufferView(payload, base); if (rejection) return { accepted: false, rejection }; try { const next = clone(base); insert(next, 'bufferViews', payload.position, { buffer: payload.buffer, byteOffset: payload.byteOffset, byteLength: payload.byteLength }); return { accepted: true, snapshot: clone(next) }; } catch (error) { return { accepted: false, rejection: typeof error === 'object' && error && 'code' in error ? error as GltfMutationRejection : reject('gltf.mutation.apply-failed', 'document/bufferViews', String(error)) }; } };
