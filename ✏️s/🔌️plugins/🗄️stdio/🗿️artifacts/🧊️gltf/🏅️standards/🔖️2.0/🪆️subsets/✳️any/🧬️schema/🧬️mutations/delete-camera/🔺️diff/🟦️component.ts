/** 🔺️ delete-camera leaf-owned typed sparse operation diff. */
import type { GltfCamera, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfDeleteCamera, validateGltfDeleteCamera, type GltfDeleteCameraPayload } from '../../delete-camera/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteCameraDescriptor = { id: 's.stdio.gltf.mutation.delete-camera.v1', version: 1, touchedPathPattern: 'document/cameras' } as const;
export type GltfDeleteCameraOperation = GltfStructuralOperation<GltfCamera>;
export interface GltfDeleteCameraDiff { readonly id: 's.stdio.gltf.mutation.delete-camera.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfDeleteCameraPayload; readonly operation: GltfDeleteCameraOperation; }
const operation = (base: GltfSnapshot, payload: GltfDeleteCameraPayload): GltfDeleteCameraOperation => ({ kind: 'delete', index: payload.index, removed: base.document.cameras[payload.index]! });
const touchedPaths = (payload: GltfDeleteCameraPayload): readonly string[] => [`document/cameras/${payload.index}`];
export const validateGltfDeleteCameraDiff = (diff: GltfDeleteCameraDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfDeleteCameraDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfDeleteCamera(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfDeleteCameraDiff = (base: GltfSnapshot, diff: GltfDeleteCameraDiff): GltfStructuralResult => { const rejection = validateGltfDeleteCameraDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'cameras', diff.operation); };
export const encodeGltfDeleteCameraDiff = (diff: GltfDeleteCameraDiff): string => JSON.stringify(diff);
export const deriveGltfDeleteCameraDiff = (base: GltfSnapshot, payload: GltfDeleteCameraPayload): { accepted: true; diff: GltfDeleteCameraDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfDeleteCamera(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.delete-camera.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfDeleteCameraDiff above. */
export const deriveTransitionalGltfDeleteCameraGltfDiff = (base: GltfSnapshot, payload: GltfDeleteCameraPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfDeleteCamera(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('cameras', base, applied.snapshot) } : applied; };
