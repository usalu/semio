/** 🔺️ reorder-samplers leaf-owned typed sparse operation diff. */
import type { GltfSampler, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfReorderSamplers, validateGltfReorderSamplers, type GltfReorderSamplersPayload } from '../../reorder-samplers/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfReorderSamplersDescriptor = { id: 's.stdio.gltf.mutation.reorder-samplers.v1', version: 1, touchedPathPattern: 'document/samplers' } as const;
export type GltfReorderSamplersOperation = GltfStructuralOperation<GltfSampler>;
export interface GltfReorderSamplersDiff { readonly id: 's.stdio.gltf.mutation.reorder-samplers.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfReorderSamplersPayload; readonly operation: GltfReorderSamplersOperation; }
const operation = (base: GltfSnapshot, payload: GltfReorderSamplersPayload): GltfReorderSamplersOperation => ({ kind: 'reorder', order: [...payload.order] });
const touchedPaths = (payload: GltfReorderSamplersPayload): readonly string[] => payload.order.map(index => `document/samplers/${index}`);
export const validateGltfReorderSamplersDiff = (diff: GltfReorderSamplersDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfReorderSamplersDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfReorderSamplers(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfReorderSamplersDiff = (base: GltfSnapshot, diff: GltfReorderSamplersDiff): GltfStructuralResult => { const rejection = validateGltfReorderSamplersDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'samplers', diff.operation); };
export const encodeGltfReorderSamplersDiff = (diff: GltfReorderSamplersDiff): string => JSON.stringify(diff);
export const deriveGltfReorderSamplersDiff = (base: GltfSnapshot, payload: GltfReorderSamplersPayload): { accepted: true; diff: GltfReorderSamplersDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfReorderSamplers(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.reorder-samplers.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfReorderSamplersDiff above. */
export const deriveTransitionalGltfReorderSamplersGltfDiff = (base: GltfSnapshot, payload: GltfReorderSamplersPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfReorderSamplers(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('samplers', base, applied.snapshot) } : applied; };
