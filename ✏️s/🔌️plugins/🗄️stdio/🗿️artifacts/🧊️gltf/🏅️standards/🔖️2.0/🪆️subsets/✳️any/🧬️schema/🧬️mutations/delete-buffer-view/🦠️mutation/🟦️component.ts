/** 🦠️ delete-buffer-view executable structural glTF command. */
import type { GltfOrthographic, GltfPerspective, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { clone, insert, order, position, reject, remove, relocate, reorder, repair, type GltfMutationRejection, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteBufferViewDescriptor = { id: 's.stdio.gltf.mutation.delete-buffer-view.v1', version: 1, touchedPathPattern: 'document/bufferViews', referencePolicy: 'all typed buffer-view references are remapped, repaired, or rejected' } as const;
export interface GltfDeleteBufferViewPayload { index: number }
export const validateGltfDeleteBufferView = (payload: GltfDeleteBufferViewPayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const index = position(payload.index, base.document.bufferViews.length, 'document/bufferViews'); if (index) return index;  return undefined; };
export const applyGltfDeleteBufferView = (base: GltfSnapshot, payload: GltfDeleteBufferViewPayload): GltfStructuralResult => { const rejection = validateGltfDeleteBufferView(payload, base); if (rejection) return { accepted: false, rejection }; try { const next = clone(base); remove(next, 'bufferViews', payload.index); return { accepted: true, snapshot: clone(next) }; } catch (error) { return { accepted: false, rejection: typeof error === 'object' && error && 'code' in error ? error as GltfMutationRejection : reject('gltf.mutation.apply-failed', 'document/bufferViews', String(error)) }; } };
