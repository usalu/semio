/** 🔺️ create-camera leaf-owned typed sparse operation diff. */
import type { GltfCamera, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfCreateCamera, validateGltfCreateCamera, type GltfCreateCameraPayload } from '../../create-camera/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfCreateCameraDescriptor = { id: 's.stdio.gltf.mutation.create-camera.v1', version: 1, touchedPathPattern: 'document/cameras' } as const;
export type GltfCreateCameraOperation = GltfStructuralOperation<GltfCamera>;
export interface GltfCreateCameraDiff { readonly id: 's.stdio.gltf.mutation.create-camera.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfCreateCameraPayload; readonly operation: GltfCreateCameraOperation; }
const operation = (base: GltfSnapshot, payload: GltfCreateCameraPayload): GltfCreateCameraOperation => ({ kind: 'insert', position: payload.position, item: { ...payload.projection } });
const touchedPaths = (payload: GltfCreateCameraPayload): readonly string[] => [`document/cameras/${payload.position}`];
export const validateGltfCreateCameraDiff = (diff: GltfCreateCameraDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfCreateCameraDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfCreateCamera(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfCreateCameraDiff = (base: GltfSnapshot, diff: GltfCreateCameraDiff): GltfStructuralResult => { const rejection = validateGltfCreateCameraDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'cameras', diff.operation); };
export const encodeGltfCreateCameraDiff = (diff: GltfCreateCameraDiff): string => JSON.stringify(diff);
export const deriveGltfCreateCameraDiff = (base: GltfSnapshot, payload: GltfCreateCameraPayload): { accepted: true; diff: GltfCreateCameraDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfCreateCamera(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.create-camera.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfCreateCameraDiff above. */
export const deriveTransitionalGltfCreateCameraGltfDiff = (base: GltfSnapshot, payload: GltfCreateCameraPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfCreateCamera(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('cameras', base, applied.snapshot) } : applied; };
