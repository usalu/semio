/** ↩️ move-skin leaf-owned inverse reconstruction from the exact base. */
import type { GltfSkin, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfMoveSkin, type GltfMoveSkinPayload } from '../../move-skin/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfMoveSkinInverseDescriptor = { id: 's.stdio.gltf.mutation.move-skin.v1.inverse', version: 1, touchedPathPattern: 'document/skins' } as const;
export type GltfMoveSkinInverseOperation = GltfStructuralOperation<GltfSkin>;
export interface GltfMoveSkinInverse { readonly id: 's.stdio.gltf.mutation.move-skin.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfMoveSkinPayload; readonly operation: GltfMoveSkinInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfMoveSkinPayload): GltfMoveSkinInverseOperation => ({ kind: 'move', index: payload.position, position: payload.index });
const inverseTouchedPaths = (payload: GltfMoveSkinPayload): readonly string[] => [`document/skins/${payload.index}`, `document/skins/${payload.position}`];
export const validateGltfMoveSkinInverse = (inverse: GltfMoveSkinInverse): GltfMutationRejection | undefined => inverse.id !== GltfMoveSkinInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfMoveSkinInverse = (after: GltfSnapshot, inverse: GltfMoveSkinInverse): GltfStructuralResult => { const rejection = validateGltfMoveSkinInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'skins', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfMoveSkinInverse = (inverse: GltfMoveSkinInverse): string => JSON.stringify(inverse);
export const deriveGltfMoveSkinInverse = (base: GltfSnapshot, payload: GltfMoveSkinPayload): { accepted: true; inverse: GltfMoveSkinInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfMoveSkin(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.move-skin.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'skins') }, touchedPaths: paths }; };
