/** ↩️ reorder-materials leaf-owned inverse reconstruction from the exact base. */
import type { GltfMaterial, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfReorderMaterials, type GltfReorderMaterialsPayload } from '../../reorder-materials/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfReorderMaterialsInverseDescriptor = { id: 's.stdio.gltf.mutation.reorder-materials.v1.inverse', version: 1, touchedPathPattern: 'document/materials' } as const;
export type GltfReorderMaterialsInverseOperation = GltfStructuralOperation<GltfMaterial>;
export interface GltfReorderMaterialsInverse { readonly id: 's.stdio.gltf.mutation.reorder-materials.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfReorderMaterialsPayload; readonly operation: GltfReorderMaterialsInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfReorderMaterialsPayload): GltfReorderMaterialsInverseOperation => ({ kind: 'reorder', order: payload.order.map((_, position) => payload.order.indexOf(position)) });
const inverseTouchedPaths = (payload: GltfReorderMaterialsPayload): readonly string[] => payload.order.map(index => `document/materials/${index}`);
export const validateGltfReorderMaterialsInverse = (inverse: GltfReorderMaterialsInverse): GltfMutationRejection | undefined => inverse.id !== GltfReorderMaterialsInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfReorderMaterialsInverse = (after: GltfSnapshot, inverse: GltfReorderMaterialsInverse): GltfStructuralResult => { const rejection = validateGltfReorderMaterialsInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'materials', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfReorderMaterialsInverse = (inverse: GltfReorderMaterialsInverse): string => JSON.stringify(inverse);
export const deriveGltfReorderMaterialsInverse = (base: GltfSnapshot, payload: GltfReorderMaterialsPayload): { accepted: true; inverse: GltfReorderMaterialsInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfReorderMaterials(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.reorder-materials.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'materials') }, touchedPaths: paths }; };
