/** 🔺️ reorder-skins leaf-owned typed sparse operation diff. */
import type { GltfSkin, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfReorderSkins, validateGltfReorderSkins, type GltfReorderSkinsPayload } from '../../reorder-skins/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfReorderSkinsDescriptor = { id: 's.stdio.gltf.mutation.reorder-skins.v1', version: 1, touchedPathPattern: 'document/skins' } as const;
export type GltfReorderSkinsOperation = GltfStructuralOperation<GltfSkin>;
export interface GltfReorderSkinsDiff { readonly id: 's.stdio.gltf.mutation.reorder-skins.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfReorderSkinsPayload; readonly operation: GltfReorderSkinsOperation; }
const operation = (base: GltfSnapshot, payload: GltfReorderSkinsPayload): GltfReorderSkinsOperation => ({ kind: 'reorder', order: [...payload.order] });
const touchedPaths = (payload: GltfReorderSkinsPayload): readonly string[] => payload.order.map(index => `document/skins/${index}`);
export const validateGltfReorderSkinsDiff = (diff: GltfReorderSkinsDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfReorderSkinsDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfReorderSkins(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfReorderSkinsDiff = (base: GltfSnapshot, diff: GltfReorderSkinsDiff): GltfStructuralResult => { const rejection = validateGltfReorderSkinsDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'skins', diff.operation); };
export const encodeGltfReorderSkinsDiff = (diff: GltfReorderSkinsDiff): string => JSON.stringify(diff);
export const deriveGltfReorderSkinsDiff = (base: GltfSnapshot, payload: GltfReorderSkinsPayload): { accepted: true; diff: GltfReorderSkinsDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfReorderSkins(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.reorder-skins.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfReorderSkinsDiff above. */
export const deriveTransitionalGltfReorderSkinsGltfDiff = (base: GltfSnapshot, payload: GltfReorderSkinsPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfReorderSkins(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('skins', base, applied.snapshot) } : applied; };
