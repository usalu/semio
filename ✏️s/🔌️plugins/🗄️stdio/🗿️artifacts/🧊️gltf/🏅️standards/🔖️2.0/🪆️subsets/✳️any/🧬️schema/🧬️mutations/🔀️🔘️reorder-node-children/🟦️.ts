/** 🦠️ reorder-node-children is an atomic, typed glTF 2.0 command. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget, GltfAccessor, GltfSparseAccessor, GltfSparseIndices, GltfSparseValues } from '../../📸️snapshot/🟦️.ts';
import { run, reject, positionIn, itemIndex, moveItem, type GltfLeafResult, type GltfMutationRejection } from '../../🔨️modules/🧬️mutation-support/📚️top-level/🟦️.ts';
export const GltfReorderNodeChildrenDescriptor = { id: 's.stdio.gltf.mutation.reorder-node-children.v1', version: 1, kind: 'reorder', touchedPaths: ["document/nodes/*/children"], referencePolicy: 'accepts only a complete permutation of current child identities' } as const;
export interface GltfReorderNodeChildrenPayload { parent: number; order: number[] }
export type GltfReorderNodeChildrenResult = GltfLeafResult;
export const validateGltfReorderNodeChildren = (payload: GltfReorderNodeChildrenPayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const parent = itemIndex(payload.parent, base.document.nodes.length, 'document/nodes'); if (parent) return parent; const children = base.document.nodes[payload.parent]!.children; if (payload.order.length !== children.length || new Set(payload.order).size !== children.length || payload.order.some(child => !children.includes(child))) return reject('gltf.mutation.invalid-permutation', `document/nodes/${payload.parent}/children`, 'order must contain every child identity once'); if (payload.order.every((child, index) => child === children[index])) return reject('gltf.mutation.no-observable-change', `document/nodes/${payload.parent}/children`, 'reorder must change order'); return undefined; };
export const applyGltfReorderNodeChildren = (base: GltfSnapshot, payload: GltfReorderNodeChildrenPayload): GltfReorderNodeChildrenResult => run(base, payload, validateGltfReorderNodeChildren, (next, payload) => { next.document.nodes[payload.parent]!.children = [...payload.order]; }, GltfReorderNodeChildrenDescriptor.touchedPaths);
