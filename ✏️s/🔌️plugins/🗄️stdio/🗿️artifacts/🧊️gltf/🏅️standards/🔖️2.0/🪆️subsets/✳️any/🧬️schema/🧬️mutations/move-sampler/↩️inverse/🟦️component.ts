/** ↩️ move-sampler leaf-owned inverse reconstruction from the exact base. */
import type { GltfSampler, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfMoveSampler, type GltfMoveSamplerPayload } from '../../move-sampler/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfMoveSamplerInverseDescriptor = { id: 's.stdio.gltf.mutation.move-sampler.v1.inverse', version: 1, touchedPathPattern: 'document/samplers' } as const;
export type GltfMoveSamplerInverseOperation = GltfStructuralOperation<GltfSampler>;
export interface GltfMoveSamplerInverse { readonly id: 's.stdio.gltf.mutation.move-sampler.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfMoveSamplerPayload; readonly operation: GltfMoveSamplerInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfMoveSamplerPayload): GltfMoveSamplerInverseOperation => ({ kind: 'move', index: payload.position, position: payload.index });
const inverseTouchedPaths = (payload: GltfMoveSamplerPayload): readonly string[] => [`document/samplers/${payload.index}`, `document/samplers/${payload.position}`];
export const validateGltfMoveSamplerInverse = (inverse: GltfMoveSamplerInverse): GltfMutationRejection | undefined => inverse.id !== GltfMoveSamplerInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfMoveSamplerInverse = (after: GltfSnapshot, inverse: GltfMoveSamplerInverse): GltfStructuralResult => { const rejection = validateGltfMoveSamplerInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'samplers', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfMoveSamplerInverse = (inverse: GltfMoveSamplerInverse): string => JSON.stringify(inverse);
export const deriveGltfMoveSamplerInverse = (base: GltfSnapshot, payload: GltfMoveSamplerPayload): { accepted: true; inverse: GltfMoveSamplerInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfMoveSampler(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.move-sampler.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'samplers') }, touchedPaths: paths }; };
