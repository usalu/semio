/** 🔺️ delete-image leaf-owned typed sparse operation diff. */
import type { GltfImage, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfDeleteImage, validateGltfDeleteImage, type GltfDeleteImagePayload } from '../../delete-image/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteImageDescriptor = { id: 's.stdio.gltf.mutation.delete-image.v1', version: 1, touchedPathPattern: 'document/images' } as const;
export type GltfDeleteImageOperation = GltfStructuralOperation<GltfImage>;
export interface GltfDeleteImageDiff { readonly id: 's.stdio.gltf.mutation.delete-image.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfDeleteImagePayload; readonly operation: GltfDeleteImageOperation; }
const operation = (base: GltfSnapshot, payload: GltfDeleteImagePayload): GltfDeleteImageOperation => ({ kind: 'delete', index: payload.index, removed: base.document.images[payload.index]! });
const touchedPaths = (payload: GltfDeleteImagePayload): readonly string[] => [`document/images/${payload.index}`];
export const validateGltfDeleteImageDiff = (diff: GltfDeleteImageDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfDeleteImageDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfDeleteImage(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfDeleteImageDiff = (base: GltfSnapshot, diff: GltfDeleteImageDiff): GltfStructuralResult => { const rejection = validateGltfDeleteImageDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'images', diff.operation); };
export const encodeGltfDeleteImageDiff = (diff: GltfDeleteImageDiff): string => JSON.stringify(diff);
export const deriveGltfDeleteImageDiff = (base: GltfSnapshot, payload: GltfDeleteImagePayload): { accepted: true; diff: GltfDeleteImageDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfDeleteImage(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.delete-image.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfDeleteImageDiff above. */
export const deriveTransitionalGltfDeleteImageGltfDiff = (base: GltfSnapshot, payload: GltfDeleteImagePayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfDeleteImage(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('images', base, applied.snapshot) } : applied; };
