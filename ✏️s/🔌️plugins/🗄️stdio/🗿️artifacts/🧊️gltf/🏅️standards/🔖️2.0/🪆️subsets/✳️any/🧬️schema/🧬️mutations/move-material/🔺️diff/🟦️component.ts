/** 🔺️ move-material leaf-owned typed sparse operation diff. */
import type { GltfMaterial, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfMoveMaterial, validateGltfMoveMaterial, type GltfMoveMaterialPayload } from '../../move-material/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfMoveMaterialDescriptor = { id: 's.stdio.gltf.mutation.move-material.v1', version: 1, touchedPathPattern: 'document/materials' } as const;
export type GltfMoveMaterialOperation = GltfStructuralOperation<GltfMaterial>;
export interface GltfMoveMaterialDiff { readonly id: 's.stdio.gltf.mutation.move-material.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfMoveMaterialPayload; readonly operation: GltfMoveMaterialOperation; }
const operation = (base: GltfSnapshot, payload: GltfMoveMaterialPayload): GltfMoveMaterialOperation => ({ kind: 'move', index: payload.index, position: payload.position });
const touchedPaths = (payload: GltfMoveMaterialPayload): readonly string[] => [`document/materials/${payload.index}`, `document/materials/${payload.position}`];
export const validateGltfMoveMaterialDiff = (diff: GltfMoveMaterialDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfMoveMaterialDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfMoveMaterial(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfMoveMaterialDiff = (base: GltfSnapshot, diff: GltfMoveMaterialDiff): GltfStructuralResult => { const rejection = validateGltfMoveMaterialDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'materials', diff.operation); };
export const encodeGltfMoveMaterialDiff = (diff: GltfMoveMaterialDiff): string => JSON.stringify(diff);
export const deriveGltfMoveMaterialDiff = (base: GltfSnapshot, payload: GltfMoveMaterialPayload): { accepted: true; diff: GltfMoveMaterialDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfMoveMaterial(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.move-material.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfMoveMaterialDiff above. */
export const deriveTransitionalGltfMoveMaterialGltfDiff = (base: GltfSnapshot, payload: GltfMoveMaterialPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfMoveMaterial(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('materials', base, applied.snapshot) } : applied; };
