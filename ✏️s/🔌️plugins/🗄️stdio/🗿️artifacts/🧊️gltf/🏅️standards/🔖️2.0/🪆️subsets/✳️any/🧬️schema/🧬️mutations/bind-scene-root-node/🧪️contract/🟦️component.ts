/** 🧪️ Mutation-law probe for bind-scene-root-node. */
import vector from './🔣️component.json' with { type: 'json' };
import type { GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { applyGltfBindSceneRootNode, type GltfBindSceneRootNodePayload } from '../../bind-scene-root-node/🦠️mutation/🟦️component.ts';
import { applyGltfBindSceneRootNodeDiff, deriveGltfBindSceneRootNodeDiff } from '../../bind-scene-root-node/🔺️diff/🟦️component.ts';
import { applyGltfBindSceneRootNodeInverse, deriveGltfBindSceneRootNodeInverse } from '../../bind-scene-root-node/↩️inverse/🟦️component.ts';
export const assertGltfBindSceneRootNodeLaws = (base: GltfSnapshot, payload: GltfBindSceneRootNodePayload) => { const first = applyGltfBindSceneRootNode(base, payload); const direct = deriveGltfBindSceneRootNodeDiff(base, payload); const inverse = deriveGltfBindSceneRootNodeInverse(base, payload); if (!first.accepted || !direct.accepted || !inverse.accepted) return !first.accepted ? first : !direct.accepted ? direct : inverse; const applied = applyGltfBindSceneRootNodeDiff(base, direct.diff); if (!applied.accepted) throw new Error('bind-scene-root-node direct patch was rejected'); const replay = applyGltfBindSceneRootNodeDiff(base, direct.diff); const undo = applyGltfBindSceneRootNodeInverse(applied.snapshot, inverse.inverse); const stale = applyGltfBindSceneRootNodeDiff(applied.snapshot, direct.diff); const prematureInverse = applyGltfBindSceneRootNodeInverse(base, inverse.inverse); if (!replay.accepted || !undo.accepted || stale.accepted || prematureInverse.accepted || JSON.stringify(applied.snapshot) !== JSON.stringify(first.snapshot) || JSON.stringify(replay.snapshot) !== JSON.stringify(applied.snapshot) || JSON.stringify(undo.snapshot) !== JSON.stringify(base) || direct.diff.touchedPaths[0] !== `document/scenes/${payload.scene}/nodes/${payload.position}` || inverse.inverse.touchedPaths[0] !== direct.diff.touchedPaths[0]) throw new Error('bind-scene-root-node violates sparse planning, replay, undo, path, or stale-state laws'); return { first, direct, inverse, applied, undo }; };
export const assertGltfBindSceneRootNodeCanonicalVector = () => { const item = vector.vectors[0] as unknown as { base: GltfSnapshot; mutation: GltfBindSceneRootNodePayload; diff: { scene: number; node: number; position: number; touchedPaths: readonly [string] }; inverse: { scene: number; node: number; position: number; touchedPaths: readonly [string] }; after: GltfSnapshot }; const result = assertGltfBindSceneRootNodeLaws(item.base, item.mutation); const forgedDiff = applyGltfBindSceneRootNodeDiff(item.base, { ...item.diff, touchedPaths: ['document/forged'] }); const forgedInverse = applyGltfBindSceneRootNodeInverse(item.after, { ...item.inverse, touchedPaths: ['document/forged'] }); if (!('applied' in result) || forgedDiff.accepted || forgedInverse.accepted || JSON.stringify(result.direct.diff) !== JSON.stringify(item.diff) || JSON.stringify(result.inverse.inverse) !== JSON.stringify(item.inverse) || JSON.stringify(result.applied.snapshot) !== JSON.stringify(item.after)) throw new Error('bind-scene-root-node canonical vector mismatch'); return result; };
