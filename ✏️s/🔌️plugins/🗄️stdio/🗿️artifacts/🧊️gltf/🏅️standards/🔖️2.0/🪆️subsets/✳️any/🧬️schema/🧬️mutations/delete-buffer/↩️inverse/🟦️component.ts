/** ↩️ delete-buffer leaf-owned inverse reconstruction from the exact base. */
import type { GltfBuffer, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfDeleteBuffer, type GltfDeleteBufferPayload } from '../../delete-buffer/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfDeleteBufferInverseDescriptor = { id: 's.stdio.gltf.mutation.delete-buffer.v1.inverse', version: 1, touchedPathPattern: 'document/buffers' } as const;
export type GltfDeleteBufferInverseOperation = GltfStructuralOperation<GltfBuffer>;
export interface GltfDeleteBufferInverse { readonly id: 's.stdio.gltf.mutation.delete-buffer.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfDeleteBufferPayload; readonly operation: GltfDeleteBufferInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfDeleteBufferPayload): GltfDeleteBufferInverseOperation => ({ kind: 'insert', position: payload.index, item: base.document.buffers[payload.index]!, bytes: [...base.buffers[payload.index]!] });
const inverseTouchedPaths = (payload: GltfDeleteBufferPayload): readonly string[] => [`document/buffers/${payload.index}`, `buffers/${payload.index}`];
export const validateGltfDeleteBufferInverse = (inverse: GltfDeleteBufferInverse): GltfMutationRejection | undefined => inverse.id !== GltfDeleteBufferInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfDeleteBufferInverse = (after: GltfSnapshot, inverse: GltfDeleteBufferInverse): GltfStructuralResult => { const rejection = validateGltfDeleteBufferInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'buffers', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfDeleteBufferInverse = (inverse: GltfDeleteBufferInverse): string => JSON.stringify(inverse);
export const deriveGltfDeleteBufferInverse = (base: GltfSnapshot, payload: GltfDeleteBufferPayload): { accepted: true; inverse: GltfDeleteBufferInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfDeleteBuffer(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.delete-buffer.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'buffers') }, touchedPaths: paths }; };
