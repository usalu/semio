/** ↩️ create-camera leaf-owned inverse reconstruction from the exact base. */
import type { GltfCamera, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfCreateCamera, type GltfCreateCameraPayload } from '../../create-camera/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfCreateCameraInverseDescriptor = { id: 's.stdio.gltf.mutation.create-camera.v1.inverse', version: 1, touchedPathPattern: 'document/cameras' } as const;
export type GltfCreateCameraInverseOperation = GltfStructuralOperation<GltfCamera>;
export interface GltfCreateCameraInverse { readonly id: 's.stdio.gltf.mutation.create-camera.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfCreateCameraPayload; readonly operation: GltfCreateCameraInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfCreateCameraPayload): GltfCreateCameraInverseOperation => ({ kind: 'delete', index: payload.position, removed: { ...payload.projection } });
const inverseTouchedPaths = (payload: GltfCreateCameraPayload): readonly string[] => [`document/cameras/${payload.position}`];
export const validateGltfCreateCameraInverse = (inverse: GltfCreateCameraInverse): GltfMutationRejection | undefined => inverse.id !== GltfCreateCameraInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfCreateCameraInverse = (after: GltfSnapshot, inverse: GltfCreateCameraInverse): GltfStructuralResult => { const rejection = validateGltfCreateCameraInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'cameras', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfCreateCameraInverse = (inverse: GltfCreateCameraInverse): string => JSON.stringify(inverse);
export const deriveGltfCreateCameraInverse = (base: GltfSnapshot, payload: GltfCreateCameraPayload): { accepted: true; inverse: GltfCreateCameraInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfCreateCamera(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.create-camera.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'cameras') }, touchedPaths: paths }; };
