/** ↩️ create-image leaf-owned inverse reconstruction from the exact base. */
import type { GltfImage, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfCreateImage, type GltfCreateImagePayload } from '../../create-image/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfCreateImageInverseDescriptor = { id: 's.stdio.gltf.mutation.create-image.v1.inverse', version: 1, touchedPathPattern: 'document/images' } as const;
export type GltfCreateImageInverseOperation = GltfStructuralOperation<GltfImage>;
export interface GltfCreateImageInverse { readonly id: 's.stdio.gltf.mutation.create-image.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfCreateImagePayload; readonly operation: GltfCreateImageInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfCreateImagePayload): GltfCreateImageInverseOperation => ({ kind: 'delete', index: payload.position, removed: {} });
const inverseTouchedPaths = (payload: GltfCreateImagePayload): readonly string[] => [`document/images/${payload.position}`];
export const validateGltfCreateImageInverse = (inverse: GltfCreateImageInverse): GltfMutationRejection | undefined => inverse.id !== GltfCreateImageInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfCreateImageInverse = (after: GltfSnapshot, inverse: GltfCreateImageInverse): GltfStructuralResult => { const rejection = validateGltfCreateImageInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'images', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfCreateImageInverse = (inverse: GltfCreateImageInverse): string => JSON.stringify(inverse);
export const deriveGltfCreateImageInverse = (base: GltfSnapshot, payload: GltfCreateImagePayload): { accepted: true; inverse: GltfCreateImageInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfCreateImage(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.create-image.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'images') }, touchedPaths: paths }; };
