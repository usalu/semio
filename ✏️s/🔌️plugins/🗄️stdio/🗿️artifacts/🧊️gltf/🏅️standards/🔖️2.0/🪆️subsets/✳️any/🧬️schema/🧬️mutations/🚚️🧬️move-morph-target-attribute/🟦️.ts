/** 🦠️ move-morph-target-attribute: cohesive atomic mesh mutation. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget } from '../../📸️snapshot/🟦️.ts';
import { run, reject, positionIn, itemIndex, permutation, moveItem, type GltfLeafResult, type GltfMutationRejection } from '../../🔨️modules/🧬️mutation-support/📚️top-level/🟦️.ts';
export const GltfMoveMorphTargetAttributeDescriptor = { id: 's.stdio.gltf.mutation.move-morph-target-attribute.v1', version: 1, kind: 'move', touchedPaths: ["document/meshes/*/primitives/*/targets/*/attributes"], referencePolicy: 'moves one target semantic in its stable ordered map' } as const;
export interface GltfMoveMorphTargetAttributePayload { mesh: number; primitive: number; target: number; semantic: string; position: number }
export type GltfMoveMorphTargetAttributeResult = GltfLeafResult;
export const validateGltfMoveMorphTargetAttribute = (payload: GltfMoveMorphTargetAttributePayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const mesh = itemIndex(payload.mesh, base.document.meshes.length, 'document/meshes'); if (mesh) return mesh; const primitive = itemIndex(payload.primitive, base.document.meshes[payload.mesh]!.primitives.length, `document/meshes/${payload.mesh}/primitives`); if (primitive) return primitive; const target = itemIndex(payload.target, base.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets.length, 'document/meshes/primitives/targets'); if (target) return target; const entries = Object.keys(base.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets[payload.target]!); const index = entries.indexOf(payload.semantic); if (index < 0) return reject('gltf.mutation.relation-absent', 'document/meshes/primitives/targets', 'semantic is not bound'); const position = itemIndex(payload.position, entries.length, 'document/meshes/primitives/targets'); if (position) return position; if (index === payload.position) return reject('gltf.mutation.no-observable-change', 'document/meshes/primitives/targets', 'destination equals source'); return undefined; };
export const applyGltfMoveMorphTargetAttribute = (base: GltfSnapshot, payload: GltfMoveMorphTargetAttributePayload): GltfMoveMorphTargetAttributeResult => run(base, payload, validateGltfMoveMorphTargetAttribute, (next, payload) => { const target = next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets[payload.target]!; const entries = Object.entries(target); moveItem(entries, entries.findIndex(([semantic]) => semantic === payload.semantic), payload.position); next.document.meshes[payload.mesh]!.primitives[payload.primitive]!.targets[payload.target] = Object.fromEntries(entries); }, GltfMoveMorphTargetAttributeDescriptor.touchedPaths);
