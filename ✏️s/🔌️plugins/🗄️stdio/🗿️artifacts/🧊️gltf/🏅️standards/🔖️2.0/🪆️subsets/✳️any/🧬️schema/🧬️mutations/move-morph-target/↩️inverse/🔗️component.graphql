# move-morph-target inverse facet.
input GltfMoveMorphTargetPayloadV1 {
  mesh: Int!
  primitive: Int!
  target: Int!
  position: Int!
}
type GltfMoveMorphTargetInverseV1 { id: ID!, version: Int!, accepted: Boolean!, touchedPaths: [String!]!, rejectionCode: String }
