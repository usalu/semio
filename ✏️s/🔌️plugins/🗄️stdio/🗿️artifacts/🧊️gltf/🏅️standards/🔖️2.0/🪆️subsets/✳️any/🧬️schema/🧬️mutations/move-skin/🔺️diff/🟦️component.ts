/** 🔺️ move-skin leaf-owned typed sparse operation diff. */
import type { GltfSkin, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfMoveSkin, validateGltfMoveSkin, type GltfMoveSkinPayload } from '../../move-skin/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfMoveSkinDescriptor = { id: 's.stdio.gltf.mutation.move-skin.v1', version: 1, touchedPathPattern: 'document/skins' } as const;
export type GltfMoveSkinOperation = GltfStructuralOperation<GltfSkin>;
export interface GltfMoveSkinDiff { readonly id: 's.stdio.gltf.mutation.move-skin.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfMoveSkinPayload; readonly operation: GltfMoveSkinOperation; }
const operation = (base: GltfSnapshot, payload: GltfMoveSkinPayload): GltfMoveSkinOperation => ({ kind: 'move', index: payload.index, position: payload.position });
const touchedPaths = (payload: GltfMoveSkinPayload): readonly string[] => [`document/skins/${payload.index}`, `document/skins/${payload.position}`];
export const validateGltfMoveSkinDiff = (diff: GltfMoveSkinDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfMoveSkinDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfMoveSkin(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfMoveSkinDiff = (base: GltfSnapshot, diff: GltfMoveSkinDiff): GltfStructuralResult => { const rejection = validateGltfMoveSkinDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'skins', diff.operation); };
export const encodeGltfMoveSkinDiff = (diff: GltfMoveSkinDiff): string => JSON.stringify(diff);
export const deriveGltfMoveSkinDiff = (base: GltfSnapshot, payload: GltfMoveSkinPayload): { accepted: true; diff: GltfMoveSkinDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfMoveSkin(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.move-skin.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfMoveSkinDiff above. */
export const deriveTransitionalGltfMoveSkinGltfDiff = (base: GltfSnapshot, payload: GltfMoveSkinPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfMoveSkin(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('skins', base, applied.snapshot) } : applied; };
