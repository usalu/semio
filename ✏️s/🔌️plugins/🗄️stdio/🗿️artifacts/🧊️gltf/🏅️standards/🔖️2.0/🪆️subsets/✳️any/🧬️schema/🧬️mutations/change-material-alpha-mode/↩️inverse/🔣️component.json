{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "s.stdio.gltf.mutation.change-material-alpha-mode.v1.inverse",
  "type": "object",
  "additionalProperties": false,
  "required": ["material", "expectedAlphaMode", "alphaMode", "touchedPaths"],
  "properties": {
    "material": { "type": "integer", "minimum": 0 },
    "expectedAlphaMode": { "type": "string", "enum": ["OPAQUE", "MASK", "BLEND"] },
    "alphaMode": { "type": "string", "enum": ["OPAQUE", "MASK", "BLEND"] },
    "touchedPaths": { "type": "array", "minItems": 1, "maxItems": 1, "items": { "type": "string" } }
  },
  "x-semio": { "id": "s.stdio.gltf.mutation.change-material-alpha-mode.v1", "version": 1, "phase": "inverse", "touchedPaths": ["document/materials/{material}/alphaMode"] }
}
