/** 🔺️ create-image leaf-owned typed sparse operation diff. */
import type { GltfImage, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfCreateImage, validateGltfCreateImage, type GltfCreateImagePayload } from '../../create-image/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfCreateImageDescriptor = { id: 's.stdio.gltf.mutation.create-image.v1', version: 1, touchedPathPattern: 'document/images' } as const;
export type GltfCreateImageOperation = GltfStructuralOperation<GltfImage>;
export interface GltfCreateImageDiff { readonly id: 's.stdio.gltf.mutation.create-image.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfCreateImagePayload; readonly operation: GltfCreateImageOperation; }
const operation = (base: GltfSnapshot, payload: GltfCreateImagePayload): GltfCreateImageOperation => ({ kind: 'insert', position: payload.position, item: {} });
const touchedPaths = (payload: GltfCreateImagePayload): readonly string[] => [`document/images/${payload.position}`];
export const validateGltfCreateImageDiff = (diff: GltfCreateImageDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfCreateImageDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfCreateImage(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfCreateImageDiff = (base: GltfSnapshot, diff: GltfCreateImageDiff): GltfStructuralResult => { const rejection = validateGltfCreateImageDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'images', diff.operation); };
export const encodeGltfCreateImageDiff = (diff: GltfCreateImageDiff): string => JSON.stringify(diff);
export const deriveGltfCreateImageDiff = (base: GltfSnapshot, payload: GltfCreateImagePayload): { accepted: true; diff: GltfCreateImageDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfCreateImage(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.create-image.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfCreateImageDiff above. */
export const deriveTransitionalGltfCreateImageGltfDiff = (base: GltfSnapshot, payload: GltfCreateImagePayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfCreateImage(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('images', base, applied.snapshot) } : applied; };
