/** 🦠️ move-scene-root-node is an atomic, typed glTF 2.0 command. */
import type { GltfJson, GltfSnapshot, GltfPrimitive, GltfMorphTarget, GltfAccessor, GltfSparseAccessor, GltfSparseIndices, GltfSparseValues } from '../../📸️snapshot/🟦️.ts';
import { run, reject, positionIn, itemIndex, moveItem, type GltfLeafResult, type GltfMutationRejection } from '../../🔨️modules/🧬️mutation-support/📚️top-level/🟦️.ts';
export const GltfMoveSceneRootNodeDescriptor = { id: 's.stdio.gltf.mutation.move-scene-root-node.v1', version: 1, kind: 'move', touchedPaths: ["document/scenes/*/nodes"], referencePolicy: 'reorders one identified scene root without retargeting any node reference' } as const;
export interface GltfMoveSceneRootNodePayload { scene: number; node: number; position: number }
export type GltfMoveSceneRootNodeResult = GltfLeafResult;
export const validateGltfMoveSceneRootNode = (payload: GltfMoveSceneRootNodePayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const scene = itemIndex(payload.scene, base.document.scenes.length, 'document/scenes'); if (scene) return scene; const roots = base.document.scenes[payload.scene]!.nodes; const index = roots.indexOf(payload.node); if (index < 0) return reject('gltf.mutation.relation-absent', `document/scenes/${payload.scene}/nodes`, 'node is not a root of this scene'); const position = itemIndex(payload.position, roots.length, `document/scenes/${payload.scene}/nodes`); if (position) return position; if (index === payload.position) return reject('gltf.mutation.no-observable-change', `document/scenes/${payload.scene}/nodes`, 'destination equals source'); return undefined; };
export const applyGltfMoveSceneRootNode = (base: GltfSnapshot, payload: GltfMoveSceneRootNodePayload): GltfMoveSceneRootNodeResult => run(base, payload, validateGltfMoveSceneRootNode, (next, payload) => { const roots = next.document.scenes[payload.scene]!.nodes; moveItem(roots, roots.indexOf(payload.node), payload.position); }, GltfMoveSceneRootNodeDescriptor.touchedPaths);
