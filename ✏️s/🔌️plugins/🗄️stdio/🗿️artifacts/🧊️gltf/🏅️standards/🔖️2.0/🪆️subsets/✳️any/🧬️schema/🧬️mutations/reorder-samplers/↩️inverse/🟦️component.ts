/** ↩️ reorder-samplers leaf-owned inverse reconstruction from the exact base. */
import type { GltfSampler, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import { validateGltfReorderSamplers, type GltfReorderSamplersPayload } from '../../reorder-samplers/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, captureReferenceRestore, reject, restoreReferences, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult, type GltfTopLevelReferenceRestore } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfReorderSamplersInverseDescriptor = { id: 's.stdio.gltf.mutation.reorder-samplers.v1.inverse', version: 1, touchedPathPattern: 'document/samplers' } as const;
export type GltfReorderSamplersInverseOperation = GltfStructuralOperation<GltfSampler>;
export interface GltfReorderSamplersInverse { readonly id: 's.stdio.gltf.mutation.reorder-samplers.v1.inverse'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfReorderSamplersPayload; readonly operation: GltfReorderSamplersInverseOperation; readonly referenceRestore: GltfTopLevelReferenceRestore; }
const inverseOperation = (base: GltfSnapshot, payload: GltfReorderSamplersPayload): GltfReorderSamplersInverseOperation => ({ kind: 'reorder', order: payload.order.map((_, position) => payload.order.indexOf(position)) });
const inverseTouchedPaths = (payload: GltfReorderSamplersPayload): readonly string[] => payload.order.map(index => `document/samplers/${index}`);
export const validateGltfReorderSamplersInverse = (inverse: GltfReorderSamplersInverse): GltfMutationRejection | undefined => inverse.id !== GltfReorderSamplersInverseDescriptor.id || inverse.version !== 1 ? reject('gltf.mutation.invalid-inverse-envelope', 'inverse', 'descriptor identity does not match') : !same(inverse.touchedPaths, inverseTouchedPaths(inverse.payload)) ? reject('gltf.mutation.invalid-touched-paths', 'inverse/touchedPaths', 'paths must be concrete payload-derived locations') : undefined;
export const applyGltfReorderSamplersInverse = (after: GltfSnapshot, inverse: GltfReorderSamplersInverse): GltfStructuralResult => { const rejection = validateGltfReorderSamplersInverse(inverse); if (rejection) return { accepted: false, rejection }; const applied = applyStructuralOperation(after, 'samplers', inverse.operation); return applied.accepted ? { accepted: true, snapshot: restoreReferences(applied.snapshot, inverse.referenceRestore) } : applied; };
export const encodeGltfReorderSamplersInverse = (inverse: GltfReorderSamplersInverse): string => JSON.stringify(inverse);
export const deriveGltfReorderSamplersInverse = (base: GltfSnapshot, payload: GltfReorderSamplersPayload): { accepted: true; inverse: GltfReorderSamplersInverse; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfReorderSamplers(payload, base); const paths = inverseTouchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, inverse: { id: 's.stdio.gltf.mutation.reorder-samplers.v1.inverse', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: inverseOperation(base, payload), referenceRestore: captureReferenceRestore(base.document, 'samplers') }, touchedPaths: paths }; };
