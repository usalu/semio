/** 🔺️ reorder-materials leaf-owned typed sparse operation diff. */
import type { GltfMaterial, GltfSnapshot } from '../../../📸️snapshot/🟦️component.ts';
import type { GltfDiff } from '../../../🔺️diff/🟦️component.ts';
import { applyGltfReorderMaterials, validateGltfReorderMaterials, type GltfReorderMaterialsPayload } from '../../reorder-materials/🦠️mutation/🟦️component.ts';
import { applyStructuralOperation, familyDiff, reject, same, type GltfMutationRejection, type GltfStructuralOperation, type GltfStructuralResult } from '../../🔒️top-level-collections-private/🟦️component.ts';
export const GltfReorderMaterialsDescriptor = { id: 's.stdio.gltf.mutation.reorder-materials.v1', version: 1, touchedPathPattern: 'document/materials' } as const;
export type GltfReorderMaterialsOperation = GltfStructuralOperation<GltfMaterial>;
export interface GltfReorderMaterialsDiff { readonly id: 's.stdio.gltf.mutation.reorder-materials.v1'; readonly version: 1; readonly touchedPaths: readonly string[]; readonly payload: GltfReorderMaterialsPayload; readonly operation: GltfReorderMaterialsOperation; }
const operation = (base: GltfSnapshot, payload: GltfReorderMaterialsPayload): GltfReorderMaterialsOperation => ({ kind: 'reorder', order: [...payload.order] });
const touchedPaths = (payload: GltfReorderMaterialsPayload): readonly string[] => payload.order.map(index => `document/materials/${index}`);
export const validateGltfReorderMaterialsDiff = (diff: GltfReorderMaterialsDiff, base: GltfSnapshot): GltfMutationRejection | undefined => { if (diff.id !== GltfReorderMaterialsDescriptor.id || diff.version !== 1) return reject('gltf.mutation.invalid-diff-envelope', 'diff', 'descriptor identity does not match'); const rejection = validateGltfReorderMaterials(diff.payload, base); if (rejection) return rejection; if (!same(diff.touchedPaths, touchedPaths(diff.payload))) return reject('gltf.mutation.invalid-touched-paths', 'diff/touchedPaths', 'paths must be concrete payload-derived locations'); return same(diff.operation, operation(base, diff.payload)) ? undefined : reject('gltf.mutation.invalid-sparse-operation', 'diff/operation', 'operation must equal the direct typed delta'); };
export const applyGltfReorderMaterialsDiff = (base: GltfSnapshot, diff: GltfReorderMaterialsDiff): GltfStructuralResult => { const rejection = validateGltfReorderMaterialsDiff(diff, base); return rejection ? { accepted: false, rejection } : applyStructuralOperation(base, 'materials', diff.operation); };
export const encodeGltfReorderMaterialsDiff = (diff: GltfReorderMaterialsDiff): string => JSON.stringify(diff);
export const deriveGltfReorderMaterialsDiff = (base: GltfSnapshot, payload: GltfReorderMaterialsPayload): { accepted: true; diff: GltfReorderMaterialsDiff; touchedPaths: readonly string[] } | { accepted: false; rejection: GltfMutationRejection } => { const rejection = validateGltfReorderMaterials(payload, base); const paths = touchedPaths(payload); return rejection ? { accepted: false, rejection } : { accepted: true, diff: { id: 's.stdio.gltf.mutation.reorder-materials.v1', version: 1, touchedPaths: paths, payload: structuredClone(payload), operation: operation(base, payload) }, touchedPaths: paths }; };
/** 🔬 Transitional evidence only; dispatch must consume GltfReorderMaterialsDiff above. */
export const deriveTransitionalGltfReorderMaterialsGltfDiff = (base: GltfSnapshot, payload: GltfReorderMaterialsPayload): { accepted: true; diff: GltfDiff } | { accepted: false; rejection: GltfMutationRejection } => { const applied = applyGltfReorderMaterials(base, payload); return applied.accepted ? { accepted: true, diff: familyDiff('materials', base, applied.snapshot) } : applied; };
