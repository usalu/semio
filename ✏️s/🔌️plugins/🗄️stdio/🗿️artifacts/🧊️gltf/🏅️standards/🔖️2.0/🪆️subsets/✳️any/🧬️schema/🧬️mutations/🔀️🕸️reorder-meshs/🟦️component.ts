/** 🦠️ reorder-meshs executable structural glTF command. */
import type { GltfOrthographic, GltfPerspective, GltfSnapshot } from '../../📸️snapshot/🟦️component.ts';
import { clone, insert, order, position, reject, remove, relocate, reorder, repair, type GltfMutationRejection, type GltfStructuralResult } from '../../🔨️modules/🧬️mutation-support/🗂️top-level-collections/🟦️component.ts';
export const GltfReorderMeshsDescriptor = { id: 's.stdio.gltf.mutation.reorder-meshs.v1', version: 1, touchedPathPattern: 'document/meshes', referencePolicy: 'all typed mesh references are remapped, repaired, or rejected' } as const;
export interface GltfReorderMeshsPayload { order: number[] }
export const validateGltfReorderMeshs = (payload: GltfReorderMeshsPayload, base: GltfSnapshot): GltfMutationRejection | undefined => { const permutation = order(payload.order, base.document.meshes.length, 'document/meshes'); if (permutation) return permutation; if (payload.order.every((value, index) => value === index)) return reject('gltf.mutation.no-observable-change', 'document/meshes', 'order already matches');  return undefined; };
export const applyGltfReorderMeshs = (base: GltfSnapshot, payload: GltfReorderMeshsPayload): GltfStructuralResult => { const rejection = validateGltfReorderMeshs(payload, base); if (rejection) return { accepted: false, rejection }; try { const next = clone(base); reorder(next, 'meshes', payload.order); return { accepted: true, snapshot: clone(next) }; } catch (error) { return { accepted: false, rejection: typeof error === 'object' && error && 'code' in error ? error as GltfMutationRejection : reject('gltf.mutation.apply-failed', 'document/meshes', String(error)) }; } };
