/** 🏗️ BinaryBuilder (raw/✳️any) meta -- the real materializer; artifact/standard levels delegate here. */
export const meta = {
  artifactKind: "s.stdio.binary",
  standard: "raw",
  subset: "*",
} as const;
