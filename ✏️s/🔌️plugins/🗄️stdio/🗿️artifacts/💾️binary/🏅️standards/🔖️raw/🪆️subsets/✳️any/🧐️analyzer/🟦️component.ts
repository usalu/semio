/** 🧐️ BinaryAnalyzer (raw/✳️any) meta -- the real analyzer; artifact/standard levels delegate here. */
export const meta = {
  artifactKind: "s.stdio.binary",
  standard: "raw",
  subset: "*",
} as const;
