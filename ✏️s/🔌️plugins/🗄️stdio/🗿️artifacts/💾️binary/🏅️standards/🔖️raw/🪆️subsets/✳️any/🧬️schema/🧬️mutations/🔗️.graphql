# 🧬️ BinaryMutation — imperative content mutation for `stdio.binary`. Discriminated on
# `mutation`; only the fields relevant to that variant are set.
type BinaryMutation {
  mutation: String!
  snapshot: BinarySnapshot
  offset: Int
  removeLen: Int
  insert: [Int!]
  data: [Int!]
}
