/** 👁️ `jpg` view (baseline) — Main window: typed twin of
 * `🦀️component.rs`'s view-model, mirroring the frozen `framework.window.image` window kit's own payload shape. */

export const JPGBASELINE_VIEW_WINDOW_KIND_ID = "framework.window.image" as const;
export const JPGBASELINE_VIEW_BODY_KEY = "framework.window.image" as const;
