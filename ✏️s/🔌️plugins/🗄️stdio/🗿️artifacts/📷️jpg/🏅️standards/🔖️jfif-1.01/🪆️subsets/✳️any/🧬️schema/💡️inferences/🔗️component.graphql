# 💡️ Jpg inference schema — canonical raster geometry.

type JpgInference {
  dimensions: JpgDimensions! @derived
}

type JpgDimensions {
  width: Int!
  height: Int!
  bitDepth: Int!
  hasAlpha: Boolean!
  pixelCount: Int!
}
