dialect grammar stdio.jpg.diff
root = document
document = payload
; JpgDiff's text representation IS its JSON serialization (see the sibling ../🔣️component.json
; for the JpgDiff JSON Schema) -- serde(rename_all = "camelCase"). No custom textual grammar
; beyond JSON's own (well-known, not reproduced here).
payload = json-value
