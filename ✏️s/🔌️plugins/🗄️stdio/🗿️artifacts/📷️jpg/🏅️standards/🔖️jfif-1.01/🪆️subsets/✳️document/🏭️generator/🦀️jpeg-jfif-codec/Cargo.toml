# 🧭️ Own workspace root, exactly like the sibling avi reference crate
# (../../../../../📼️avi/🏅️standards/🔖️1.0/🪆️subsets/✳️any/🏭️generator/🦀️riff-avi-codec/Cargo.toml)
# — never joins the repository's root workspace or its Cargo.lock, and never links against
# `semio-s-plugin-stdio` (currently non-compiling, unrelated peer migration).
[workspace]

[package]
name = "jpeg-jfif-codec"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25.10", default-features = false, features = ["jpeg"] }
