dialect protocol
protocol None
version 1
schema None
start op
framing record

# Real `Mp3Mutation` op frame (`dsl::tagged_value_binary`): `format u8` (`OP_BINARY_FORMAT`) then `tag varint`, then
# the variant's `ToValue` tree without its variant name, as one framework wire value (`pack_rt::encode_wire_value`).
# Each record is one mutation kind at its wire tag; this file is the only source of those tags.
header fixed 2
field format u8
field tag varint
record set-snapshot tag=0
field payload bytes
record set-id3v2 tag=1
field payload bytes
record set-frames tag=2
field payload bytes
record set-id3v1 tag=3
field payload bytes
