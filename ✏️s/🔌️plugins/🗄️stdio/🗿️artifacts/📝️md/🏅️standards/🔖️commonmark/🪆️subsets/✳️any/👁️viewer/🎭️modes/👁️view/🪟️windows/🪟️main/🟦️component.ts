/** 👁️ `md` view (any) — Main window: typed twin of
 * `🦀️component.rs`'s view-model, mirroring the frozen `framework.window.text` window kit's own payload shape. */

export const MD_VIEW_WINDOW_KIND_ID = "framework.window.text" as const;
export const MD_VIEW_BODY_KEY = "framework.window.text" as const;
