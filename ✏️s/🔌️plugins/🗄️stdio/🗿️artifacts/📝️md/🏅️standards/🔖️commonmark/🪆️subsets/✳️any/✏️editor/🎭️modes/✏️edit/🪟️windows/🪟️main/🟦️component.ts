/** ✏️ `md` edit (any) — Main window: typed twin of
 * `🦀️component.rs`'s view-model, mirroring the frozen `framework.window.text` window kit's own payload shape. */

export const MD_EDIT_WINDOW_KIND_ID = "framework.window.text" as const;
export const MD_EDIT_BODY_KEY = "framework.window.text" as const;
