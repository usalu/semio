module Stdio_obj_diff;

# ObjDiff's wire binary IS its serde_json encoding (protocol::OpBinary); the shared
# `.semio` envelope wraps that JSON payload.
type Document = unit {
    magic: bytes &size=8;
    token_len: uint32 &byte-order=spicy::ByteOrder::Little;
    token: bytes &size=self.token_len;   # UTF-8 "stdio.obj.diff v1"
    payload: bytes &eod;                 # UTF-8 JSON bytes of ObjDiff
};
