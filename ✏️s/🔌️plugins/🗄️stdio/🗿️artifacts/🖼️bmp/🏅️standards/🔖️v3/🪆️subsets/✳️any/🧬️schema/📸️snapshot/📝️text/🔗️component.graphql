# stdio.bmp snapshot text grammar schema: a lowercase-hex dump of the real on-disk BMP
# bytes (after the `semio ...` preamble line is stripped). See ../💾️binary/ for the same
# bytes' field-by-field structure.
type Document {
  schema: String!
  hexPayload: String!
}
