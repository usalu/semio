/** 📝️ Text representation for `stdio.bmp` (snapshot): after the `semio ...` preamble line is
 * stripped, the body IS a lowercase-hex dump of the real on-disk BMP bytes — see
 * ../💾️binary/🟦️.ts for the same bytes' binary-envelope representation. */
export type BmpSnapshotText = string;
