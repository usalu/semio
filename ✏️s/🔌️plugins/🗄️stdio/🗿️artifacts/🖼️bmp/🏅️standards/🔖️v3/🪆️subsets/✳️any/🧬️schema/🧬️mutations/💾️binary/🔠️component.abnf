; abnf stdio.bmp mutations (binary) — protocol::OpBinary: raw UTF-8 JSON bytes of
; BmpMutation, NOT wrapped in the shared `.semio` envelope.
document = payload
payload = *OCTET   ; UTF-8 JSON object per sibling ../📝️text/📖️component.grammar.semio
