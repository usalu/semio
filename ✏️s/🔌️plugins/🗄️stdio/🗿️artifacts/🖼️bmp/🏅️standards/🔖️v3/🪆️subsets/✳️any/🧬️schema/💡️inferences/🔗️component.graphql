# 💡️ Bmp inference schema — BITMAPINFOHEADER-derived raster geometry.

type BmpInference {
  dimensions: BmpDimensions! @state(class: INFERRED)
}

type BmpDimensions {
  width: Int!
  height: Int!
  bitDepth: Int!
  hasAlpha: Boolean!
  pixelCount: Int!
}
