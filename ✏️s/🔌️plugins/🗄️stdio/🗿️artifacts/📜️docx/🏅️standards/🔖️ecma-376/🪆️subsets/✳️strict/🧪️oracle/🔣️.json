{
  "$schema": "../../../../../../../../../../🧰️framework/🛍️products/🦑️repo/🔨️modules/🧪️test/🧬️schema/🔣️.json",
  "schemaVersion": 2,
  "_comment": "🧩️ DOCX ECMA-376/✳️strict's own contribution to the repository test platform: the CONFORMANCE-CLASS mutation vocabulary this subset owns, and the reference implementation that performs it independently. The ✳️any subset owns the document vocabulary (sheets, cells, blocks, runs, slides); this subset owns the ISO/IEC 29500-1 Strict class, which is a property of the OPC package and of no document object — one kind per axis its own check_strict_conformance reads, and the two vocabularies are disjoint. Reference: `quick-xml` 0.42 over the `zip` 6 container codec — a generic, namespace-agnostic OPC + XML implementation, which is what this axis needs. The obvious alternative, the Python `python-docx` distribution on the venv host that landed with `pypdf-pdf-1-4-text`, was considered and NOT chosen: it is a document object model over the WordprocessingML schema, exposing sheets/paragraphs/slides rather than the package's conformance class, so it could neither perform nor observe a single kind in this catalog. That is a statement about the shape of its API, not a claim about its quality, and it was not verified against an installed copy in the session that wrote this file.",
  "oracles": [
    {
      "id": "docx-ecma-376-strict-mutate",
      "ecosystem": "rust",
      "package": "quick-xml",
      "version": "0.42",
      "capabilities": [
        "docx-ecma-376-strict-mutate"
      ],
      "comparisonProfiles": [
        "semantic-ooxml-docx-strict-v1"
      ],
      "license": "MIT",
      "testOnly": true,
      "homepage": "https://github.com/tafia/quick-xml",
      "rationale": "🩹️ RECLASSIFIED (ticket SEPARATE-ARTIFACT-STANDARD-SUBSET-IMPLEMENTATIONS-AND-FIXTURE-TEST-EVERY-MUTATION, shard A10, 2026-09-02). This entry was registered `third-party-library` on the strength of a real crate that does genuine codec work, but the mutation's expected RESULT is computed by this owner's own `🦀️oracle.rs` -- its dispatch function's catch-all arm is `\"mutation kind {...} has no oracle implementation\"`, proof that every OTHER arm is an implementation, not a reading. Both halves of the comparison then descend from the same specification, so a shared misreading produces two agreeing wrong answers -- the one failure a differential oracle exists to catch. This is a `cross-semio-implementation`: a required SUPPLEMENT (metamorphic/inverse/round-trip evidence), never a substitute. A qualifying third-party reference for the mutation semantics is still owed. See $TICKET/📓️a10-oracle-honesty.md.\n\nThe reference streaming XML reader/writer of the Rust ecosystem, paired with `zip` 6 (MIT/Apache-2.0, already a registered oracle of this owner) for the OPC container. Together they read a real ECMA-376 package, perform every conformance-class edit this catalog declares — namespace declarations, the officeDocument relationship base, the root conformance attribute, legacy VML parts and mc:AlternateContent markup — and reassemble the whole container from the parts alone. That is a genuine second producer for every declared kind, which is why every scenario is @mode-differential. Registered under `quick-xml` because it performs and observes the edit; `zip` is the paired container codec and is exercised by every green scenario. Neither crate is production-reachable: this repository's own OPC and XML codecs are hand-written and link neither.",
      "hostPath": "✏️s/🔌️plugins/🗄️stdio/🧪️oracle/📦️packages/🦀️rust",
      "kind": "cross-semio-implementation",
      "engine": {
        "family": "quick-xml",
        "implementation": "quick-xml Rust streaming XML reader/writer",
        "version": "0.42"
      },
      "productionReachable": false,
      "networkDuringExecution": false
    }
  ],
  "mutationCatalogs": [
    {
      "id": "docx-ecma-376-strict",
      "capability": "docx-ecma-376-strict-mutate",
      "standardDirectoryName": "🔖️ecma-376",
      "subsetDirectoryName": "✳️strict",
      "vectors": [],
      "kinds": [
        "no-mutation",
        "set-snapshot",
        "set-main-namespace",
        "set-relationship-base",
        "set-conformance-attribute",
        "remove-conformance-attribute",
        "insert-vml-part",
        "remove-vml-part",
        "insert-alternate-content",
        "remove-alternate-content"
      ]
    }
  ],
  "noOracleDecisions": [],
  "comparisonProfiles": [
    {
      "id": "semantic-ooxml-docx-strict-v1",
      "description": "An ECMA-376 package compared by its CONFORMANCE-CLASS surface, every field read back out of the bytes by the independent `zip` + `quick-xml` implementation: the part inventory with each part's resolved content type, the main part (word/document.xml on the real fixture, resolved through the root officeDocument relationship rather than assumed), that part's root element name and full attribute list, every OOXML-family namespace declared anywhere in the package, every relationship type, and the inventory of parts carrying mc:AlternateContent. Document CONTENT is deliberately absent — cell values, paragraphs and slide shapes belong to the ✳️any subset's own profile, and a conformance class says nothing about them. Compression method, entry order, attribute order within an element and XML whitespace are writer choices and are normalised away by projecting sorted, typed facts instead of bytes. No numeric field, hence no tolerance.",
      "tolerance": 0,
      "ignoreKeys": []
    }
  ]
}
