/** 🧬️ DocxSnapshot schema (📏️strict subset) meta — reuses the ✳️any subset's schema verbatim. */
export const meta = {
  artifactKind: "s.stdio.docx",
  standard: "ecma-376",
  subset: "strict",
} as const;
