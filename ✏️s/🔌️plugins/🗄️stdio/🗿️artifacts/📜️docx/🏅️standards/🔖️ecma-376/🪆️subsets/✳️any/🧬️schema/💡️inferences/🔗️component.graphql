# 💡️ Docx inference schema — document outline (paragraph/table/word counts).

type DocxInference {
  outline: DocxOutline! @derived
}

type DocxOutline {
  paragraphCount: Int!
  tableCount: Int!
  wordCount: Int!
}
