/** 🧾 `outline` — one named inference: this WordprocessingML document's own paragraph/table/word structure. */

export interface DocxOutline {
  paragraphCount: number;
  tableCount: number;
  wordCount: number;
}
