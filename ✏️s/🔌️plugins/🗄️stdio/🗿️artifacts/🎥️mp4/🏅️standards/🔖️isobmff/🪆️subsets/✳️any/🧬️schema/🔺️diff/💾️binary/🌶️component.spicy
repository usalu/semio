module Stdio_mp4_diff;
# Op-binary payload: one JSON-serialized Mp4Mutation per record (see 🧬️mutations/🦀️component.rs's
# hand-rolled OpBinary impl — this is a documentation leaf, not a distinct wire codec).
public type Op = unit {
    json_utf8: bytes &eod;
};
