(* ebnf stdio.html diff -- hand-rolled bracket-token grammar, see the sibling
   ../📖️component.grammar.semio for the full production rules. *)
document = { token, " " } ;
token = "doctype=", tristateString | "root=", nodeDiff ;
