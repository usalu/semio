module Stdio_html_snapshot;

# `pack` payload: UTF-8 HTML5 document text (canonical form), same grammar as the sibling text facet.
type Document = unit {
    payload: bytes &eod &convert=$$.decode("utf-8");
};
