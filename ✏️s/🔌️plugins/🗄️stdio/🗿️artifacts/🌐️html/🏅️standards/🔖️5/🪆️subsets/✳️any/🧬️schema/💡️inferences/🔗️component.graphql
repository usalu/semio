# 💡️ Html inference schema — document outline (element count, max depth, text length).

type HtmlInference {
  outline: HtmlOutline! @derived
}

type HtmlOutline {
  elementCount: Int!
  maxDepth: Int!
  textLength: Int!
}
