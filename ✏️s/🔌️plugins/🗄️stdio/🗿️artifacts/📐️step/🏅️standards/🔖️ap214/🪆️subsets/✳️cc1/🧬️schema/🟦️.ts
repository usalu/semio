/** 🧬️ StepSnapshot schema (✳️cc1 subset) meta — reuses the ✳️base subset's schema verbatim. */
export const meta = {
  artifactKind: "s.stdio.step",
  standard: "ap214",
  subset: "cc1",
} as const;
