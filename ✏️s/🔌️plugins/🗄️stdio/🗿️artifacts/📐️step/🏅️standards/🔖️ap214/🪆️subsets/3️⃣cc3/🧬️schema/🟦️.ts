/** 🧬️ StepSnapshot schema (3️⃣cc3 subset) meta — reuses the 🧱️base subset's schema verbatim. */
export const meta = {
  artifactKind: "s.stdio.step",
  standard: "ap214",
  subset: "cc3",
} as const;
