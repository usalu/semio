/** 🧬️ StepSnapshot schema (✳️cc5 subset) meta — reuses the ✳️base subset's schema verbatim. */
export const meta = {
  artifactKind: "s.stdio.step",
  standard: "ap214",
  subset: "cc5",
} as const;
