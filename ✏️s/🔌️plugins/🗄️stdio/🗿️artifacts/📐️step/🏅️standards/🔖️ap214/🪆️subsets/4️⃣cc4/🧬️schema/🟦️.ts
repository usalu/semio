/** 🧬️ StepSnapshot schema (4️⃣cc4 subset) meta — reuses the 🧱️base subset's schema verbatim. */
export const meta = {
  artifactKind: "s.stdio.step",
  standard: "ap214",
  subset: "cc4",
} as const;
