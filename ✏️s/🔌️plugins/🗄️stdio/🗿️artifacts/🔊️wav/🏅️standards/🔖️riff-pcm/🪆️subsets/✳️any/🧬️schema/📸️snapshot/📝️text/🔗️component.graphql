# 🚧 scaffolded by W1b — generic facet mirror.
type Semio_wav_snapshotEntry {
  key: String!
  value: String!
}
type Semio_wav_snapshot {
  schema: String! @state(class: PERSISTENT)
  entries: [Semio_wav_snapshotEntry!]! @state(class: PERSISTENT)
}
