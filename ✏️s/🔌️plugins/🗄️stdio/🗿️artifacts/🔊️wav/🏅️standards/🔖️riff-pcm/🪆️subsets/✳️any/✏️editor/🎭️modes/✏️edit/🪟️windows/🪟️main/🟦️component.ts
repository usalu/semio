/** ✏️ `wav` edit (any) — Main window: typed twin of
 * `🦀️component.rs`'s view-model, mirroring the frozen `framework.window.media` window kit's own payload shape. */

export const WAV_EDIT_WINDOW_KIND_ID = "framework.window.media" as const;
export const WAV_EDIT_BODY_KEY = "framework.window.media" as const;
