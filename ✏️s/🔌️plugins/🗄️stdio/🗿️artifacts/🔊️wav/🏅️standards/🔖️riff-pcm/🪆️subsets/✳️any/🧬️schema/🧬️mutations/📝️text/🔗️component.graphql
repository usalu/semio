# 🚧 scaffolded by W1b — generic facet mirror.
type Semio_wav_mutationsEntry {
  key: String!
  value: String!
}
type Semio_wav_mutations {
  schema: String! @state(class: PERSISTENT)
  entries: [Semio_wav_mutationsEntry!]! @state(class: PERSISTENT)
}
