/** 💾️ Binary representation for `stdio.json` (diff) — envelope-wrapped UTF-8 JSON text of the
 *  tagged `JsonDiff` struct (see ../🟦️component.ts for its shape). */
export type JsonDiffBinary = Uint8Array;
