dialect grammar
grammar json.mutations
extension json
start document

# Direct descriptor identities: set-member, remove-member, insert-array-element, remove-array-element, set-scalar.
document = "json-mutation" "payload" "=" hex
