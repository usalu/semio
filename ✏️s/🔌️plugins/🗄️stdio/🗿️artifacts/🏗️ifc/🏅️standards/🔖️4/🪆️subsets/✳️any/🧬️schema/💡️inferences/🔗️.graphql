# 💡️ IFC4 inference schema — IFCCARTESIANPOINT-derived spatial bounding box.

type IfcInference {
  bounds: IfcBounds! @derived
}

type IfcBounds {
  min: [Float!]!
  max: [Float!]!
  pointCount: Int!
}
