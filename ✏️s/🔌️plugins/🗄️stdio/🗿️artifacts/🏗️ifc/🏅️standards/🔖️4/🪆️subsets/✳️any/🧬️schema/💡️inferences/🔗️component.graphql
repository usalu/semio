# 💡️ IFC4 inference schema — IFCCARTESIANPOINT-derived spatial bounding box.

type IfcInference {
  bounds: IfcBounds! @state(class: INFERRED)
}

type IfcBounds {
  min: [Float!]!
  max: [Float!]!
  pointCount: Int!
}
