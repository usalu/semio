# 💡️ IFC2X3 inference schema — IFCCARTESIANPOINT-derived spatial bounding box.

type Ifc2x3Inference {
  bounds: Ifc2x3Bounds! @state(class: INFERRED)
}

type Ifc2x3Bounds {
  min: [Float!]!
  max: [Float!]!
  pointCount: Int!
}
