# 💡️ Png inference schema — IHDR-derived raster geometry.

type PngInference {
  dimensions: PngDimensions! @derived
}

type PngDimensions {
  width: Int!
  height: Int!
  bitDepth: Int!
  hasAlpha: Boolean!
  pixelCount: Int!
}
