dialect protocol stdio.png.diff
; protocol::OpBinary raw JSON encoding of PngDiff — NOT wrapped in the shared `.semio`
; binary envelope (contrast with ../../📸️snapshot/💾️binary/component.protocol.semio). No
; length prefix: the entire op body IS the JSON document.
json-object = *OCTET   ; UTF-8 JSON object (see ../📝️text/📖️component.grammar.semio)
