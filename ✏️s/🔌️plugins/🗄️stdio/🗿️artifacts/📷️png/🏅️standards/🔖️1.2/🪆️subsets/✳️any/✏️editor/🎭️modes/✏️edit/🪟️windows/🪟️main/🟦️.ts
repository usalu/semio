/** ✏️ `png` edit (any) — Main window: typed twin of
 * `🦀️.rs`'s view-model, mirroring the frozen `framework.window.image` window kit's own payload shape. */

export const PNG_EDIT_WINDOW_KIND_ID = "framework.window.image" as const;
export const PNG_EDIT_BODY_KEY = "framework.window.image" as const;
