/** 🧬️ replace-text-chunk direct payload. */
import type { PngTextChunk } from '../../📸️snapshot/🟦️.ts';
export interface ReplaceTextChunkMutation {
  readonly index: number;
  readonly chunk: PngTextChunk;
}
