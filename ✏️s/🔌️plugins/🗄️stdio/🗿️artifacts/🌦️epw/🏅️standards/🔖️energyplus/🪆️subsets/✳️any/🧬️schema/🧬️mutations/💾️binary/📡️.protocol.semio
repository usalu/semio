dialect protocol stdio.epw.mutations
; protocol::OpBinary raw text encoding of EpwMutation — NOT wrapped in the shared `.semio`
; binary envelope
payload = *OCTET   ; UTF-8 hand-rolled mutation text (see ../📝️text/📖️.grammar.semio)
