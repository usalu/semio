dialect protocol stdio.epw.diff
; protocol::OpBinary raw text encoding of EpwDiff — NOT wrapped in the shared `.semio`
; binary envelope (contrast with ../../📸️snapshot/💾️binary/component.protocol.semio)
payload = *OCTET   ; UTF-8 hand-rolled diff text (see ../📝️text/📖️.grammar.semio)
