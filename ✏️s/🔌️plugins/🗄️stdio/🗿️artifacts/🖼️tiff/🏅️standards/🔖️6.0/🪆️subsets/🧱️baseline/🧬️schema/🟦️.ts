/** 🧬️ TiffSnapshot schema (🧱️baseline subset) meta — reuses the ✳️any subset's schema verbatim. */
export const meta = {
  artifactKind: "s.stdio.tiff",
  standard: "6.0",
  subset: "baseline",
} as const;
