# 💡️ Tiff inference schema — IFD 0 baseline-tag-derived raster geometry.

type TiffInference {
  dimensions: TiffDimensions! @state(class: INFERRED)
}

type TiffDimensions {
  width: Int!
  height: Int!
  bitDepth: Int!
  hasAlpha: Boolean!
  pixelCount: Int!
}
