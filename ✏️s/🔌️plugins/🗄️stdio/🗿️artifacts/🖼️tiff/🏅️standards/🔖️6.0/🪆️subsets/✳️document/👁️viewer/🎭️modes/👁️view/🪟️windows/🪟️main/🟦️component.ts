/** 👁️ `tiff` view (any) — Main window: typed twin of
 * `🦀️component.rs`'s view-model, mirroring the frozen `framework.window.image` window kit's own payload shape. */

export const TIFFANY_VIEW_WINDOW_KIND_ID = "framework.window.image" as const;
export const TIFFANY_VIEW_BODY_KEY = "framework.window.image" as const;
