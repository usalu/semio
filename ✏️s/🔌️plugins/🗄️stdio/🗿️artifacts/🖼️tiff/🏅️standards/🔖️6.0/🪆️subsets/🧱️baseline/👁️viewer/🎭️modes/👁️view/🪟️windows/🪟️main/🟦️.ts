/** 👁️ `tiff` view (baseline) — Main window: typed twin of
 * `🦀️.rs`'s view-model, mirroring the frozen `framework.window.image` window kit's own payload shape. */

export const TIFFBASELINE_VIEW_WINDOW_KIND_ID = "framework.window.image" as const;
export const TIFFBASELINE_VIEW_BODY_KEY = "framework.window.image" as const;
