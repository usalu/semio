# 💡️ Las inference schema — header-declared bounding box and point count.

type LasInference {
  bounds: LasBounds! @derived
}

type LasBounds {
  minX: Float!
  minY: Float!
  minZ: Float!
  maxX: Float!
  maxY: Float!
  maxZ: Float!
  pointCount: Int!
}
