# 🧬️ LasArtifact
type LasArtifact {
  schema: String! @state(class: PERSISTENT)
  header: LasHeader! @state(class: PERSISTENT)
  vlrs: [LasVlr!]! @state(class: PERSISTENT)
  points: [LasPoint!]! @state(class: PERSISTENT)
}
