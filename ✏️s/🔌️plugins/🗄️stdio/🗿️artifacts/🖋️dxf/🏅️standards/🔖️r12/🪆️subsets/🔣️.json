{
  "artifact": "s.stdio.dxf",
  "standard": "r12",
  "subsets": {
    "header": {
      "name": "AutoCAD DXF R12 HEADER section (Autodesk DXF Reference, 1992) -- document-wide system variables ($ACADVER etc.), plus the whole-document set-snapshot/no-mutation surface. The shared reader/writer for the whole file lives here; the other sections reuse it (family-module pattern) rather than duplicating it."
    },
    "tables": {
      "name": "AutoCAD DXF R12 TABLES section -- LAYER, STYLE and LTYPE (linetype) table records."
    },
    "blocks": {
      "name": "AutoCAD DXF R12 BLOCKS section -- block definition records."
    },
    "entities": {
      "name": "AutoCAD DXF R12 ENTITIES section -- drawing entity records."
    }
  }
}
