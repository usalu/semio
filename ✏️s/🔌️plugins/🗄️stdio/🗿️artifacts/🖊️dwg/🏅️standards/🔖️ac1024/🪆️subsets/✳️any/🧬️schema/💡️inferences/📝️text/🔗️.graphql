# s.stdio.dwg.inference (ac1024) structured text schema
type DwgInferenceText {
  schema: String!
  structure: DwgStructureText!
}

type DwgStructureText {
  layerCount: Int!
  entityCount: Int!
  geometryValueCount: Int!
  geometryIndexCount: Int!
  textCharacterCount: Int!
  codepage: Int!
  version: String!
}
