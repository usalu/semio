dialect protocol
protocol stdio.dwg.ac1024.inference
version 1
schema s.stdio.dwg.inference
start frame
framing magic 0x8953f83f7d340d0b

struct DwgStructure { layer_count u64 entity_count u64 geometry_value_count u64 geometry_index_count u64 text_character_count u64 codepage u64 version utf8 }
struct DwgInference { structure DwgStructure }

header fixed 32
field format_major u16
field format_minor u16
field flags u32
field domain_tag u32
field header_crc32 u32
segment payload varint bytes
footer fixed 64
field artifact_mark utf8
field body_crc32 u32
