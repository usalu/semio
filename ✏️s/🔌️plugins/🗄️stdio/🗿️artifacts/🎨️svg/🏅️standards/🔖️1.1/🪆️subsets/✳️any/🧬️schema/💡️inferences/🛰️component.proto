syntax = "proto3";
package semio.s.stdio.svg.inference;

// 💡️ Svg inference schema — root `<svg>` intrinsic size.

message SvgInference {
  // @derived
  SvgDimensions dimensions = 1;
}

message SvgDimensions {
  double width = 1;
  double height = 2;
}
