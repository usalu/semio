/** ↩️ inverse for `ChangeMaterialMetallic` — undoes to another `ChangeMaterialMetallic` restoring the prior factor. */
export interface ChangeMaterialMetallicInverseChangeMaterialMetallic {
  id: string;
  newMetallic: number;
}
