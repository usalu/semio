# DSL text envelope for a SemioDocumentSnapshot -- opaque to GraphQL (a preamble line + hex body),
# exposed as a scalar rather than fabricating structure GraphQL callers never parse directly.
scalar SemioDocumentSnapshotDslText
