# 🧬️ SemioAnimationArtifact schema — full artifact state, mirrors SemioAnimationSnapshot field for
# field (see `📸️snapshot/🔗️.graphql` for the nested `AnimTimeline` shape).
type SemioAnimationArtifact {
  schema: String! @state(class: ARTIFACT)
  timelines: [AnimTimeline!]! @state(class: ARTIFACT)
}
