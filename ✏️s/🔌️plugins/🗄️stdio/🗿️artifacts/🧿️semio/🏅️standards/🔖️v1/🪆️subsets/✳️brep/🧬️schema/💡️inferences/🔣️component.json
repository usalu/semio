{
  "$id": "https://semio.tech/schema/s.stdio.semio.brep.inference/inferences.json",
  "title": "SemioBrepInference",
  "description": "Derived, non-persisted computation over a SemioBrepSnapshot. See the sibling 🦀️component.rs for the real source of truth. tessellation/massProperties are deliberately absent (see that file's module doc comment).",
  "type": "object",
  "properties": {
    "validationReport": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "code": { "type": "string" },
          "message": { "type": "string" }
        },
        "required": ["code", "message"]
      }
    }
  },
  "required": ["validationReport"]
}
