; stdio.semio.video diff binary layout: UTF-8 bytes of the text grammar (../📝️text) verbatim, no
; separate binary framing — protocol::DiffCodec::encode_diff is documented to equal
; print_diff().into_bytes() (matching GifDiff/SvgDiff/DocxDiff's own hand-rolled codecs).
document = *(UTF8-OCTET)
