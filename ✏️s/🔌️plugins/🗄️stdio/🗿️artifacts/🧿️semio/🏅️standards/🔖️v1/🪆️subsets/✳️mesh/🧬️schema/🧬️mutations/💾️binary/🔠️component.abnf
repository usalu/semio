; abnf s.stdio.semio.mesh mutation (binary) — identical to the TEXT grammar, UTF-8 encoded, no
; additional framing (encode_op = print_op().into_bytes()).
document = *OCTET   ; UTF-8 bytes of the ../📝️text/ print_op() grammar
