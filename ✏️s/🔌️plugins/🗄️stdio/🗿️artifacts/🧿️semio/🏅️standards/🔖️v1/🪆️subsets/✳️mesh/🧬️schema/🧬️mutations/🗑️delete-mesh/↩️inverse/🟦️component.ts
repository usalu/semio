/** ↩️ inverse for `DeleteMesh` — undoes to one or more `CreateMesh` (position-preserving). */
export interface DeleteMeshInverseCreateMesh {
  mesh: import("../../../📸️snapshot/🟦️component.ts").SemioMesh;
}
