dialect protocol stdio.semio.document.snapshot
; Pack binary form: store::semio_format envelope (magic + component tag + version + u32 length)
; then exactly that many bytes of UTF-8 JSON.
magic = "SEMI"
componentTag = OCTET
envelopeVersion = OCTET
bodyLen = U32BE
jsonBody = bodyLen OCTET
