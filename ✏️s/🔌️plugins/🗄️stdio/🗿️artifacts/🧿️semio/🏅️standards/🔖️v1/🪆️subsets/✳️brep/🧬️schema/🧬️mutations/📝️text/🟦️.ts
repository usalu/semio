/** 📝️ Text representation for `stdio.semio.brep` (mutations): one line of compact JSON matching
 * the tagged `SemioBrepMutation` shape (see ../🟦️.ts). */
export type SemioBrepMutationText = string;
