# 💾️ Binary-facet grammar mirror (descriptive) — see ../🔗️component.graphql for the canonical
# facet schema; this file describes the OP PACK-BINARY ENCODING header of the same shape.
type SemioGraphOpFrameHeader {
  format: Int!
  tag: Int!
  payload: String!
}
