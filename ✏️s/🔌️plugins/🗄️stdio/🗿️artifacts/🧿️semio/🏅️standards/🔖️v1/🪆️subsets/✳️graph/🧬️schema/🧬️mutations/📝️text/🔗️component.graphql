# 📝️ Text-facet grammar mirror, GraphQL SDL shape (descriptive) — see ../🔗️component.graphql for
# the canonical facet schema; this file describes the OP-TEXT ENCODING of the same shape.
type SemioGraphOpDsl {
  keyword: String!
  args: String!
}
