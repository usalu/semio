/** ↩️ inverse for `ChangeStrokeWidth` — always `ChangeStrokeWidth` with the captured old width. */
export interface ChangeStrokeWidthInverseChangeStrokeWidth {
  styleName: string;
  newWidth?: number;
}
