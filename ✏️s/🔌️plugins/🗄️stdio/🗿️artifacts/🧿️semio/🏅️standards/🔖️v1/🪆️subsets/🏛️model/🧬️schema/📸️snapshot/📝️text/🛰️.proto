syntax = "proto3";
package semio.stdio_semio_model.snapshot_text;
message Artifact { string preamble = 1; bytes body = 2; }

message SemioModelSnapshotText {
  optional string preamble = 1;
  optional string body = 2;
}
