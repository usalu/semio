/** ↩️ inverse for `ReplaceFill` — always `ReplaceFill` with the captured old fill. */
export interface ReplaceFillInverseReplaceFill {
  styleName: string;
  newFill?: { r: number; g: number; b: number; a: number };
}
