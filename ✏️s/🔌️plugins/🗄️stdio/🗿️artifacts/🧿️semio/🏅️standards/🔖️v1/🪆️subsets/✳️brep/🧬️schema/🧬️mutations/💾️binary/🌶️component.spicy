module Stdio_semio_brep_mutations;

# Binary payload: UTF-8 compact-JSON bytes of a SemioBrepMutation (serde_json::to_vec), same
# grammar as the sibling text facet.
type Document = unit {
    payload: bytes &eod &convert=$$.decode("utf-8");
};
