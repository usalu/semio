# 🧬️ SemioDrawingArtifact — mirrors the real Rust `SemioDrawingArtifact`. See
# 📸️snapshot/🔗️component.graphql for DrawCanvas/DrawStyle/DrawLayer/DrawNode.
type SemioDrawingArtifact {
  schema: String! @state(class: PERSISTENT)
  canvas: DrawCanvas! @state(class: PERSISTENT)
  styles: [DrawStyle!]! @state(class: PERSISTENT)
  layers: [DrawLayer!]! @state(class: PERSISTENT)
}
