dialect grammar
grammar stdio.semio.animation.inference
extension semio.animation.inference
start document

document = header body
header = "schema" SP "s.stdio.semio.animation.inference" NL
body = payload NL?
payload = OCTET+
