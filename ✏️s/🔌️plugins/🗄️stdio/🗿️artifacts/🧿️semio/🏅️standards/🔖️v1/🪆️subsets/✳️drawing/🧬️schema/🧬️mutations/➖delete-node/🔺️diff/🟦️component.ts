/** 🔺️ diff fragment for `DeleteNode` — nested `Group.children.removed` reached via `diff_at_path`. */
export interface DeleteNodeDiff {
  layers?: { modified: { index: number; diff: unknown }[] };
}
