; stdio.semio.video mutations binary layout: UTF-8 bytes of the text op grammar (../📝️text)
; verbatim, no separate binary framing — protocol::OpBinary::encode_op is documented to equal
; print_op().into_bytes() (matching DocxMutation's own hand-rolled codec).
document = *(UTF8-OCTET)
