# 💾️ SemioTableSnapshot binary frame — descriptive twin of 📡️component.protocol.semio.
type SemioTableSnapshotFrame {
  format: Int!
  schema: String!
  payload: String! # opaque base64: columns + rows
}
