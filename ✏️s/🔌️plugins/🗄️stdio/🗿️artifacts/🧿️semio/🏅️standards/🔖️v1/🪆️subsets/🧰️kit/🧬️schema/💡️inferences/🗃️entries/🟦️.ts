/** 🗃 `entries` — the semio kit's own catalog census. */

export interface SemioKitEntries {
  typeCount: number;
  designCount: number;
  pieceCount: number;
  connectionCount: number;
  objectCount: number;
  modelCount: number;
  hasProperties: boolean;
  representationCount: number;
}
