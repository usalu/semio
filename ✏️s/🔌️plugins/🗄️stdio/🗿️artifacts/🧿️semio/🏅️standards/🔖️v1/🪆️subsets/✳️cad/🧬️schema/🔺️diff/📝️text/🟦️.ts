/** 📝️ Text (DiffCodec) representation for `s.stdio.semio.cad.diff` — no semio envelope
 * (`encode_diff` is `print_diff().into_bytes()`); see ../🟦️.ts for the decoded diff's
 * real schema. */
export type SemioCadDiffText = string;
