/** 📝️ Text-facet grammar mirror (descriptive) — see ../🟦️component.ts for the canonical facet
 * schema; this file describes the DSL-text ENCODING of the diff shape. */
export interface SemioTextDiffDsl {
  runsWire?: string | null;
}
