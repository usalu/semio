# 🚧 scaffolded by W1b — generic facet mirror.
type Semio_semio_model_snapshotEntry {
  key: String!
  value: String!
}
type Semio_semio_model_snapshot {
  schema: String! @state(class: PERSISTENT)
  entries: [Semio_semio_model_snapshotEntry!]! @state(class: PERSISTENT)
}
