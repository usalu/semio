# 🚧 scaffolded by W1b — generic facet mirror.
type SemioAudioSnapshotEntry {
  key: String!
  value: String!
}
type SemioAudioSnapshot {
  schema: String! @state(class: PERSISTENT)
  entries: [SemioAudioSnapshotEntry!]! @state(class: PERSISTENT)
}
