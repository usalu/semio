/** mutation payload — mirrors `SetPrimitiveTopology`. */
export interface SetPrimitiveTopology {
  meshId: string;
  primitiveId: string;
  topology: import("../../../📸️snapshot/🟦️component.ts").SemioTopology;
}
