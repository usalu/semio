# 🧬️ SemioValueArtifact schema — mirrors `SemioValueSnapshot` field for field.
type SemioValueArtifact {
  schema: String! @state(class: PERSISTENT)
  root: SemioValue! @state(class: PERSISTENT)
  nodes: [SemioValueNode!]! @state(class: PERSISTENT)
}
