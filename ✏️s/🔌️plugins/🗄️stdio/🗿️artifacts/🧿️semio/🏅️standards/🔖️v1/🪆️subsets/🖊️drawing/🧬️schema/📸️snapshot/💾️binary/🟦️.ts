/** 💾️ Binary-wire representation for `stdio.semio.drawing` (snapshot): the real
 * `semio_format::wrap_binary` envelope. Real shape: `📡️.protocol.semio`. */
export type SemioDrawingSnapshotBinary = Uint8Array;
