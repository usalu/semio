# 🚧 scaffolded by W1b — generic facet mirror.
type Semio_semio_audio_diffEntry {
  key: String!
  value: String!
}
type Semio_semio_audio_diff {
  schema: String! @state(class: PERSISTENT)
  entries: [Semio_semio_audio_diffEntry!]! @state(class: PERSISTENT)
}
