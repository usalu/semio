syntax = "proto3";
// 💾️ Binary-facet grammar mirror (descriptive) — see ../🛰️.proto for the canonical facet
// schema; this file describes the OP PACK-BINARY ENCODING header of the mutation dispatch shape.
package semio.s_stdio_semio_text_mutations.binary;
message SemioTextMutationOpFrameHeader {
  uint32 format = 1;
  uint32 tag = 2;
  bytes payload = 3;
}
