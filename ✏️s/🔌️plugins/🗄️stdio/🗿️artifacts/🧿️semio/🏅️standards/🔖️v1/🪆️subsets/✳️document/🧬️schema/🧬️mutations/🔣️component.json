{
  "$id": "https://semio.tech/schema/s.stdio.semio.document/mutations.json",
  "title": "SemioDocumentMutation",
  "description": "Named-variant mutation enum, tagged on `mutation`.",
  "type": "object",
  "required": ["mutation"],
  "properties": {
    "mutation": {
      "enum": [
        "setSnapshot", "insertBlock", "removeBlock", "setBlockContent",
        "setParagraphStyle", "setHeadingLevel", "setListOrdered", "setRunText", "setRunStyle",
        "setImageBlock", "insertStyle", "removeStyle", "setStyleName", "setStyleBasedOn",
        "insertImage", "removeImage", "setImageBytes"
      ]
    }
  }
}
