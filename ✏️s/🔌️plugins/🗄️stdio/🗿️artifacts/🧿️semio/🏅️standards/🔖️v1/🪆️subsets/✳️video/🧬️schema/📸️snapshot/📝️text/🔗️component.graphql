# 📝️ Text representation mirror for `stdio.semio.video` (snapshot) — envelope header + hex(JSON)
# body; the JSON body's own structure is ../🔗️component.graphql's SemioVideoSnapshot.
type SemioVideoSnapshotTextEnvelope {
  header: String!
  bodyHex: String!
}
