# 🧬️ SemioVideoArtifact — full artifact state, mirrors SemioVideoSnapshot field for field (see
# 📸️snapshot/🔗️component.graphql for SemioVideoStream/SemioVideoSample/SemioRational).
type SemioVideoArtifact {
  schema: String! @state(class: PERSISTENT)
  streams: [SemioVideoStream!]! @state(class: PERSISTENT)
}
