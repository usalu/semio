# 🚧 scaffolded by W1b — generic facet mirror.
type SemioVideoArtifactEntry {
  key: String!
  value: String!
}
type SemioVideoArtifact {
  schema: String! @state(class: PERSISTENT)
  entries: [SemioVideoArtifactEntry!]! @state(class: PERSISTENT)
}
