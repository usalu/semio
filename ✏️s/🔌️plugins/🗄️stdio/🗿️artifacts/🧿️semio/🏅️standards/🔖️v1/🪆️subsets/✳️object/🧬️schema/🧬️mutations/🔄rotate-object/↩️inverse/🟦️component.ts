/** ↩️ `rotate-object` inverse — self-inverse, another RotateObject restoring the prior rotation. */
export interface RotateObjectInverse {
  rotation: { x: number; y: number; z: number; w: number };
}
