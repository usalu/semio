module Semio_workflow_mutations;
# `s.stdio.semio.workflow` mutation BINARY wire format — UTF-8 bytes of the mutation TEXT wire
# format verbatim (see the doc comment on 🧬️mutations/💾️binary/🥋️component.ksy).
type Document = unit {
    op_line_utf8: bytes &eod;
};
