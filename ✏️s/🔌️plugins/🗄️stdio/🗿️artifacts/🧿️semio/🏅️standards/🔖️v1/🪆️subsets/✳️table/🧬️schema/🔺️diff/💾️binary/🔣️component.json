{
  "$id": "https://semio.tech/schema/s.stdio.semio.table/diff.binary.json",
  "title": "SemioTableDiffBinaryFrame",
  "description": "Descriptive twin of 📡️component.protocol.semio — format u8, presence u8 (bit0=columns, bit1=rows), then an opaque payload tail.",
  "type": "object",
  "properties": {
    "format": { "type": "integer" },
    "presence": { "type": "integer" },
    "payload": { "type": "string", "contentEncoding": "base64" }
  }
}
