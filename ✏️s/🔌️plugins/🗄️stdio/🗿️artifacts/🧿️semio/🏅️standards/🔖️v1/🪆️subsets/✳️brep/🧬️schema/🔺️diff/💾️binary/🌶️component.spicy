module Stdio_semio_brep_diff;

# Binary payload: UTF-8 diff-line text verbatim (protocol::DiffCodec::encode_diff is
# print_diff().into_bytes()), same grammar as the sibling text facet.
type Document = unit {
    payload: bytes &eod &convert=$$.decode("utf-8");
};
