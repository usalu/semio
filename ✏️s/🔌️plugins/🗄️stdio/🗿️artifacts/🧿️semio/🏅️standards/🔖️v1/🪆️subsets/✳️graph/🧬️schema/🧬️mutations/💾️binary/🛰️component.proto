syntax = "proto3";
// 💾️ Binary-facet grammar mirror (descriptive) — see ../🛰️component.proto for the canonical facet
// schema; this file describes the OP PACK-BINARY ENCODING header of the same shape.
package semio.s_stdio_semio_graph_mutations.binary;

message SemioGraphOpFrameHeader {
  uint32 format = 1;
  uint32 tag = 2;
  bytes payload = 3;
}
