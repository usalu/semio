module Semio_semio_drawing_diff;
# no separate binary envelope -- bytes ARE the UTF-8 text-facet grammar verbatim (json's own
# stdio.json.diff binary leaf precedent).
type Document = unit {
    text_bytes: bytes &eod;
};
