# 📝️ Text-facet grammar mirror, GraphQL SDL shape (descriptive) — see ../🔗️.graphql for
# the canonical facet schema; this file describes the DIFF-TEXT ENCODING of the same shape.
type SemioGraphDiffDsl {
  nodesWire: String
  edgesWire: String
}
