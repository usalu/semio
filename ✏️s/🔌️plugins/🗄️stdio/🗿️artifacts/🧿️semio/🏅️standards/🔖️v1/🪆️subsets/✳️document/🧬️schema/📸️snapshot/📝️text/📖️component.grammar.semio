dialect grammar stdio.semio.document.snapshot
; DSL text form: preamble line naming the envelope, then the JSON body hex-encoded 2 digits/byte.
root = document
document = preamble NL hexBody
preamble = "stdio.semio.document.dsl.v1"
hexBody = *HEXPAIR
HEXPAIR = HEXDIG HEXDIG
