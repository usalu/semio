# 💡️ Semio envelope inference schema — the wrapped subset's dispatch tag/ordinal.

type SemioInference {
  kind: SemioKind! @state(class: INFERRED)
}

type SemioKind {
  tag: String!
  ordinal: Int!
}
