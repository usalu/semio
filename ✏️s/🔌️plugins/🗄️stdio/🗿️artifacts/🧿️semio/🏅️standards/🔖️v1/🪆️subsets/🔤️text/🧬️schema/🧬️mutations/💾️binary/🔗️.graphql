# 💾️ Binary-facet grammar mirror (descriptive) — see ../🔗️.graphql for the canonical
# facet schema; this file describes the OP PACK-BINARY ENCODING header of the mutation shape.
type SemioTextMutationOpFrameHeader {
  format: Int!
  tag: Int!
  payload: String!
}
