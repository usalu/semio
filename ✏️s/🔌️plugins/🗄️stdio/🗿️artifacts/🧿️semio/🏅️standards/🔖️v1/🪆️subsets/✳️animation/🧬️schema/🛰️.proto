syntax = "proto3";
// 🧬️ SemioAnimationArtifact schema — full artifact state, mirrors SemioAnimationSnapshot field for
// field.
package semio.s_stdio_semio_animation.artifact;

import "📸️snapshot/🛰️.proto";

message SemioAnimationArtifact {
  string schema = 1;
  repeated semio.s_stdio_semio_animation.snapshot.AnimTimeline timelines = 2;
}
