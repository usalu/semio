syntax = "proto3";
// 📝️ SemioTableSnapshot text body — descriptive twin of 📖️.grammar.semio.
package semio.s_stdio_semio_table.snapshot.text;

message SemioTableSnapshotTextBody {
  string body = 1; // "schema=<hex>\ncolumns=[<col>,...]\nrows=[<row>,...]"
}
