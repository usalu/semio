{
  "$id": "https://semio.tech/schema/s.stdio.semio.graph.mutations/binary.json",
  "title": "SemioGraphOpFrameHeader",
  "description": "Pack-binary op-frame header mirror — see ../🔣️.json for the canonical facet schema.",
  "type": "object",
  "properties": {
    "format": { "type": "integer" },
    "tag": { "type": "integer", "description": "variant ordinal, 0-10" },
    "payload": { "type": "string", "contentEncoding": "hex" }
  }
}
