# 🚧 scaffolded by W1b — generic facet mirror.
type SemioAudioDiffEntry {
  key: String!
  value: String!
}
type SemioAudioDiff {
  schema: String! @state(class: PERSISTENT)
  entries: [SemioAudioDiffEntry!]! @state(class: PERSISTENT)
}
