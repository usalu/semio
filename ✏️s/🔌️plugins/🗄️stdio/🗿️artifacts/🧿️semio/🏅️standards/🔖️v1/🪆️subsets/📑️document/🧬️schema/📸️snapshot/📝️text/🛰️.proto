syntax = "proto3";
// DSL text envelope for a SemioDocumentSnapshot: the whole `preamble\nhexbody` string verbatim.
package semio.s_stdio_semio_document.snapshot.text;
message SemioDocumentSnapshotDslText {
  string preamble = 1;
  string hex_body = 2;
}
