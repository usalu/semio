/** 💾️ Binary-wire representation for `stdio.semio.drawing` (mutation): `serde_json::to_vec`, no
 * separate envelope. Real shape: `../📝️text/🅰️.g4`. */
export type SemioDrawingMutationBinary = Uint8Array;
