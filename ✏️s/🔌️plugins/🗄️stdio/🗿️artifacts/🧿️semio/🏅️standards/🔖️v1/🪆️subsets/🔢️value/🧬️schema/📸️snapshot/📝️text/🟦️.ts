/** 📝️ Text representation for `stdio.semio.value` (snapshot): the semio envelope preamble
 * wrapping a hex-encoded compact-JSON `SemioValueSnapshot`. */
export type SemioValueSnapshotText = string;
