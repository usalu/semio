module Stdio_semio_model_mutation;

# No envelope: the binary form is exactly serde_json::to_vec of the tagged SemioModelMutation
# enum (same grammar as the sibling text facet).
type Document = unit {
    json: bytes &eod &convert=$$.decode("utf-8");
};
