# 💡️ Semio animation inference schema — keyframe-derived playback duration.

type SemioAnimationInference {
  duration: SemioAnimationDuration! @derived
}

type SemioAnimationDuration {
  durationSeconds: Float!
  timelineCount: Int!
  channelCount: Int!
  keyframeCount: Int!
}
