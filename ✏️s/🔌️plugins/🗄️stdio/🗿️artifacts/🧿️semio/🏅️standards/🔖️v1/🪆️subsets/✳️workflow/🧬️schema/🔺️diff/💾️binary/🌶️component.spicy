module Semio_workflow_diff;
# `s.stdio.semio.workflow` diff BINARY wire format — UTF-8 bytes of the diff TEXT wire format
# verbatim (see the doc comment on 🔺️diff/💾️binary/🥋️component.ksy).
type Document = unit {
    diff_line_utf8: bytes &eod;
};
