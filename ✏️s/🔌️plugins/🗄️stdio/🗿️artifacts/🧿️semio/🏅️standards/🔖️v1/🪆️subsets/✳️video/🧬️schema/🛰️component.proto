syntax = "proto3";
// 🧬️ SemioVideoArtifact — full artifact state, mirrors SemioVideoSnapshot field for field.
package semio.s_stdio_semio_video.artifact;
import "./📸️snapshot/🛰️component.proto";

message SemioVideoArtifact {
  string schema = 1;
  repeated semio.s_stdio_semio_video.snapshot.SemioVideoStream streams = 2;
}
