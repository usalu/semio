/** 💾️ Binary-wire representation for `stdio.semio.drawing` (diff): no separate envelope, the
 * bytes ARE the UTF-8 text-facet grammar verbatim. Real shape: `../📝️text/🅰️.g4`. */
export type SemioDrawingDiffBinary = Uint8Array;
