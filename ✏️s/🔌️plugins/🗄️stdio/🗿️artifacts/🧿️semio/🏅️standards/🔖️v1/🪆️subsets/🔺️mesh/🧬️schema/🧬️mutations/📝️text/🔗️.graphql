# 🔗️ GraphQL mirror of `s.stdio.semio.mesh`'s hand-rolled OpText TEXT grammar.
type SemioMeshMutationTextArg {
  name: String!
  value: String!
}
type SemioMeshMutationText {
  keyword: String!   # "no-mutation" | "set-snapshot" | "add-mesh" | ... (16 total)
  args: [SemioMeshMutationTextArg!]!
}
