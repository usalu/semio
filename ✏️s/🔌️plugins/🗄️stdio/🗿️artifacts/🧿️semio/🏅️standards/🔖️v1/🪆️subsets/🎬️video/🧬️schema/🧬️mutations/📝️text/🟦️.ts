/** 📝️ Text representation mirror for `stdio.semio.video` (mutations) — one line: "no-mutation" or
 * "<keyword> arg=value ...". Structural shape is `../🟦️.ts`'s `SemioVideoMutation`. */
export type SemioVideoMutationTextLine = string;
