/** ↩️ inverse for `DeletePrimitive` — undoes to one or more `CreatePrimitive` (position-preserving). */
export interface DeletePrimitiveInverseCreatePrimitive {
  meshId: string;
  primitive: import("../../../📸️snapshot/🟦️component.ts").SemioPrimitive;
}
