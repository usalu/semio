/** ↩️ inverse for `DeleteTexture` — undoes to one or more `CreateTexture` (position-preserving). */
export interface DeleteTextureInverseCreateTexture {
  texture: import("../../../📸️snapshot/🟦️component.ts").SemioTexture;
}
