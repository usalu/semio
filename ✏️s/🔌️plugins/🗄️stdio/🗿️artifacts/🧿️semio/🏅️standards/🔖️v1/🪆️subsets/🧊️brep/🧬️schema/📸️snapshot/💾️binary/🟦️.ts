/** 💾️ Binary (pack) representation for `stdio.semio.brep` (snapshot): the semio_format envelope
 * wrapping JSON-pack bytes of a `SemioBrepSnapshot`. */
export type SemioBrepSnapshotPack = Uint8Array;
