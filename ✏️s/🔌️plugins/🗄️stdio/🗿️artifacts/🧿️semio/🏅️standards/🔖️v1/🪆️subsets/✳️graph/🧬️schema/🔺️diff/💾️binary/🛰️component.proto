syntax = "proto3";
// 💾️ Binary-facet grammar mirror (descriptive) — see ../🛰️component.proto for the canonical facet
// schema; this file describes the DIFF PACK-BINARY ENCODING header of the same shape.
package semio.s_stdio_semio_graph_diff.binary;

message SemioGraphDiffFrameHeader {
  uint32 format = 1;
  uint32 presence = 2;
  bytes payload = 3;
}
