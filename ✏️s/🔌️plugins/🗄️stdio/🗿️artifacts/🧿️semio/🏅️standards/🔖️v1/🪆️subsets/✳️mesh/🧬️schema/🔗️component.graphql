# 🚧 scaffolded by W1b — generic facet mirror.
type SemioMeshArtifactEntry {
  key: String!
  value: String!
}
type SemioMeshArtifact {
  schema: String! @state(class: PERSISTENT)
  entries: [SemioMeshArtifactEntry!]! @state(class: PERSISTENT)
}
