# 🧬️ SemioMeshArtifact schema — full artifact state, mirrors SemioMeshSnapshot field for field.
type SemioMeshArtifact {
  schema: String! @state(class: PERSISTENT)
  meshes: [SemioMesh!]! @state(class: PERSISTENT)
  materials: [SemioMaterial!]! @state(class: PERSISTENT)
  textures: [SemioTexture!]! @state(class: PERSISTENT)
}
