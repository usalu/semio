module Stdio_semio_object_snapshot;

# `pack` payload: UTF-8 compact-JSON `SemioObjectSnapshot`, same grammar as the sibling text facet.
type Document = unit {
    payload: bytes &eod &convert=$$.decode("utf-8");
};
