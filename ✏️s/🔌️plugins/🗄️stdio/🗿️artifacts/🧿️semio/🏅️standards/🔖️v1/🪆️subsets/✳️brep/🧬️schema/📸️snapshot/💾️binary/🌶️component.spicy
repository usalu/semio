module Stdio_semio_brep_snapshot;

# `pack` payload: JSON-pack bytes of a SemioBrepSnapshot (see ../../🔣️component.json for its
# structure), same envelope framing every stdio semio subset's binary pack form uses.
type Document = unit {
    payload: bytes &eod;
};
