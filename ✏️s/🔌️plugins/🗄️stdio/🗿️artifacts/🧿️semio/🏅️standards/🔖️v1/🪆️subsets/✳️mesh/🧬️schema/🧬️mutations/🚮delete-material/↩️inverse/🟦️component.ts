/** ↩️ inverse for `DeleteMaterial` — undoes to one or more `CreateMaterial` (position-preserving). */
export interface DeleteMaterialInverseCreateMaterial {
  material: import("../../../📸️snapshot/🟦️component.ts").SemioMaterial;
}
