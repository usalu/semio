module Stdio_semio_model_diff;

# No envelope: the binary form is exactly the UTF-8 bytes of the hand-rolled `print_diff()`
# one-line text (same grammar as the sibling text facet).
type Document = unit {
    line: bytes &eod &convert=$$.decode("utf-8");
};
