# Zeek Spicy mirror (descriptive, not test-parsed — the real byte-level walker is
# ../📡️.protocol.semio, walked by dsl::walk_protocol) for the SemioTableDiff binary frame.
module StdioSemioTableDiff;

public type DiffFrame = unit {
    format: uint8;
    presence: uint8;
    payload: bytes &eod;
};
