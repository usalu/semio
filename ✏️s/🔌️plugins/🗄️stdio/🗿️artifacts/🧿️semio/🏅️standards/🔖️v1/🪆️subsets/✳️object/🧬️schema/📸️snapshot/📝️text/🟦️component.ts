/** 📝️ Text representation for `stdio.semio.object` (snapshot): the semio envelope preamble
 * wrapping a hex-encoded compact-JSON `SemioObjectSnapshot`. */
export type SemioObjectSnapshotText = string;
