dialect grammar
grammar stdio.semio.document.inference
extension semio.document.inference
start document

document = header body
header = "schema" SP "s.stdio.semio.document.inference" NL
body = payload NL?
payload = OCTET+
