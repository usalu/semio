/** 📝️ Text representation mirror for `stdio.semio.video` (diff) — one line, empty if unchanged,
 * `streams=` followed by the index-keyed triple. Structural shape is `../🟦️component.ts`'s
 * `SemioVideoDiff`; this leaf is the line-oriented wire form specifically. */
export type SemioVideoDiffTextLine = string; // "" | `streams=${string}`
