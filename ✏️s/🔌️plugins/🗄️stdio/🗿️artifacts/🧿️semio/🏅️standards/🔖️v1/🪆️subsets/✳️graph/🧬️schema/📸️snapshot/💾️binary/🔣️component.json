{
  "$id": "https://semio.tech/schema/s.stdio.semio.graph.snapshot/binary.json",
  "title": "SemioGraphSnapshotPackHeader",
  "description": "Pack-binary-encoding header mirror of the snapshot facet — see ../🔣️component.json for the canonical facet schema.",
  "type": "object",
  "properties": {
    "format": { "type": "integer" },
    "schemaLen": { "type": "integer" },
    "schemaBytes": { "type": "string" },
    "payload": { "type": "string", "contentEncoding": "hex" }
  }
}
