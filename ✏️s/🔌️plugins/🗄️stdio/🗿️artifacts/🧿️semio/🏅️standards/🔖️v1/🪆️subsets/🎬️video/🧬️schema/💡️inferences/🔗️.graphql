# 💡️ Semio video inference schema — real per-stream max-pts elapsed time.

type SemioVideoInference {
  duration: SemioVideoDuration! @derived
}

type SemioVideoDuration {
  durationSeconds: Float!
  streamCount: Int!
  sampleCount: Int!
}
