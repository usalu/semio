dialect protocol stdio.semio.mesh.mutations
; identical to the TEXT grammar (../📝️text/📖️component.grammar.semio), UTF-8 encoded, no
; additional binary framing — encode_op() is print_op().into_bytes() verbatim.
document = UTF8(*)
