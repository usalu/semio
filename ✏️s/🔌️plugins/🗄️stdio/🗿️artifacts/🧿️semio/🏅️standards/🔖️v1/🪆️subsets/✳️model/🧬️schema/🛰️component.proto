syntax = "proto3";
// SemioModelArtifact schema -- full artifact state, mirrors SemioModelSnapshot field for field.
package semio.s_stdio_semio_model.artifact;
message SemioModelArtifact {
  string schema = 1;
  repeated bytes spatial = 2;
  repeated bytes elements = 3;
  repeated bytes relations = 4;
}
