; abnf s.stdio.semio.mesh diff (binary) — identical to the TEXT grammar, UTF-8 encoded, no
; additional framing (encode_diff = print_diff().into_bytes()).
document = *OCTET   ; UTF-8 bytes of the ../📝️text/ print_diff() grammar
