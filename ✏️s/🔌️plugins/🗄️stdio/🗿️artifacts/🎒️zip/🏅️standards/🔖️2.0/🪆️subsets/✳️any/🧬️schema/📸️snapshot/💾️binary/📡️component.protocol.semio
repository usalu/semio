dialect protocol
protocol stdio.zip.snapshot
version 1
schema stdio.zip
start snapshot
framing record

# 🎒️ Schema-first pack record: schema id, archive comment, then name-keyed decompressed member records.
field format u8
chain snapshot bytes
