dialect grammar stdio.xml.diff
root = document
document = payload
; XmlDiff's text representation IS its JSON serialization (see the sibling
; ../🔣️component.json for the XmlDiff JSON Schema) -- serde(rename_all = "camelCase"),
; tag-discriminated unions via a "kind"/"mutation" field. No custom textual grammar beyond JSON's
; own (well-known, not reproduced here).
payload = json-value
