/** 💾️ Binary (pack) representation for `stdio.xml` (snapshot): the semio envelope wrapping the
 * JSON serialization of `XmlDocument` (see `../📸️snapshot/🟦️.ts`) -- NOT raw XML text. */
export type XmlSnapshotBinary = Uint8Array;
