# 💡️ Xml inference schema — document outline (element count, max depth, hasDoctype).

type XmlInference {
  outline: XmlOutline! @derived
}

type XmlOutline {
  elementCount: Int!
  maxDepth: Int!
  hasDoctype: Boolean!
}
