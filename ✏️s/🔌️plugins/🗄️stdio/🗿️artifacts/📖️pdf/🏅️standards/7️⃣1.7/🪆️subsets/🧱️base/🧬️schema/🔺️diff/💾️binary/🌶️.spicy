module Stdio_pdf_1_7_Diff;

# Derive-owned PdfDiff frame: the shared op format byte, then the container-less pack record
# body of the sparse typed diff value.
public type Document = unit {
    format: uint8;
    diff: bytes &eod;
};
