; abnf stdio.pdf.1.7 diff -- derive-owned binary frame emitted by DiffCodec::encode_diff.
; The format octet is the shared op format; the body is the container-less pack record of
; the sparse typed PdfDiff value (varint-counted fields, length-prefixed strings and bytes).
pdf-diff = format diff
format = %x01
diff = *OCTET
