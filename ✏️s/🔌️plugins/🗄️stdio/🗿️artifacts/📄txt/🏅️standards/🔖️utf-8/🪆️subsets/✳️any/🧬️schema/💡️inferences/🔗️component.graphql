# 💡️ Txt inference schema — document outline (line/word/char counts).

type TxtInference {
  outline: TxtOutline! @derived
}

type TxtOutline {
  lineCount: Int!
  wordCount: Int!
  charCount: Int!
}
