# 💡️ Txt inference schema — document outline (line/word/char counts).

type TxtInference {
  outline: TxtOutline! @state(class: INFERRED)
}

type TxtOutline {
  lineCount: Int!
  wordCount: Int!
  charCount: Int!
}
