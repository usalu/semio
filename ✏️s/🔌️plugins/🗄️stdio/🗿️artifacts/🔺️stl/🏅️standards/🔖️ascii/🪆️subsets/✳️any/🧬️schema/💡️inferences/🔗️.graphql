# 💡️ Stl inference schema — triangle-soup bounding box and triangle count.

type StlInference {
  bounds: StlBounds! @derived
}

type StlBounds {
  min: [Float!]!
  max: [Float!]!
  triangleCount: Int!
}
