/** 🗒️ `topicStats` — bcf's topic/comment/viewpoint/author counts, derived from `topics`. */

export interface BcfTopicStats {
  topicCount: number;
  commentCount: number;
  viewpointCount: number;
  authorCount: number;
}
