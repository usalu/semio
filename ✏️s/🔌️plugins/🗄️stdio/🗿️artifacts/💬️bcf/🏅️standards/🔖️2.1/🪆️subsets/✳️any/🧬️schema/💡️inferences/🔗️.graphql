# 💡️ Bcf inference schema — topic/comment/viewpoint/author counts derived from the topic tree.

type BcfInference {
  topicStats: BcfTopicStats! @derived
}

type BcfTopicStats {
  topicCount: Int!
  commentCount: Int!
  viewpointCount: Int!
  authorCount: Int!
}
