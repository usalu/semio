dialect protocol stdio.csv.diff
; protocol::OpBinary raw JSON encoding of CsvDiff — NOT wrapped in the shared `.semio`
; binary envelope (contrast with ../../📸️snapshot/💾️binary/component.protocol.semio)
payload = *OCTET   ; UTF-8 JSON object (see ../📝️text/📖️component.grammar.semio)
