/** 💾️ Binary representation for `stdio.csv` (mutations): `protocol::OpBinary::encode_op` /
 * `decode_op` — raw `serde_json::to_vec`/`from_slice` of `CsvMutation`, no envelope header. */
export type CsvMutationsBinary = Uint8Array;
