/** 🧾 `outline` — one named inference: this PresentationML deck's own slide/shape/word structure. */

export interface PptxOutline {
  slideCount: number;
  shapeCount: number;
  wordCount: number;
}
