dialect protocol
protocol None
version 1
schema None
start op
framing record

# Real `TsvMutation` op frame (`dsl::tagged_value_binary`): `format u8` (`OP_BINARY_FORMAT`) then `tag varint`, then
# the variant's `ToValue` tree without its variant name, as one framework wire value (`pack_rt::encode_wire_value`).
# Each record is one mutation kind at its wire tag; this file is the only source of those tags.
header fixed 2
field format u8
field tag varint
record set-snapshot tag=0
field payload bytes
record set-trailing-newline tag=1
field payload bytes
record set-line-ending tag=2
field payload bytes
record insert-row tag=3
field payload bytes
record remove-row tag=4
field payload bytes
record set-cell tag=5
field payload bytes
