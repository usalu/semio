syntax = "proto3";
package semio.s_stdio_tsv.snapshot;

enum TsvLineEnding {
  LF = 0;
  CRLF = 1;
}

message TsvRow {
  repeated string fields = 1;
}

message TsvSnapshot {
  string schema = 1;
  repeated TsvRow records = 2;
  bool trailing_newline = 3;
  TsvLineEnding line_ending = 4;
}
