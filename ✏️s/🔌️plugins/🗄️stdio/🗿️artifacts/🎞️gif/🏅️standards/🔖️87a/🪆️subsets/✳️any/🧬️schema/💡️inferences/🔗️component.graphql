# 💡️ Gif (87a) inference schema — logical screen geometry.

type GifInference {
  dimensions: GifDimensions! @derived
}

type GifDimensions {
  width: Int!
  height: Int!
  bitDepth: Int!
  hasAlpha: Boolean!
  pixelCount: Int!
}
