# 💡️ Gif (89a) inference schema — logical screen geometry.

type GifInference {
  dimensions: GifDimensions! @state(class: INFERRED)
}

type GifDimensions {
  width: Int!
  height: Int!
  bitDepth: Int!
  hasAlpha: Boolean!
  pixelCount: Int!
}
