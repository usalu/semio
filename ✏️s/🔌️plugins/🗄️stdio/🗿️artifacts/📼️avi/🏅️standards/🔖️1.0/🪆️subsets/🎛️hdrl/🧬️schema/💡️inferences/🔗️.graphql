# 💡️ avi inference schema — real `avih` MainAVIHeader-derived playback duration.

type AviInference {
  duration: AviDuration! @derived
}

type AviDuration {
  durationSeconds: Float!
  streamCount: Int!
  totalFrames: Int!
}
