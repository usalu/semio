# 💡️ Obj inference schema — vertex-derived bounding box.

type ObjInference {
  bounds: ObjBounds! @derived
}

type ObjBounds {
  min: [Float!]!
  max: [Float!]!
  vertexCount: Int!
  faceCount: Int!
  groupCount: Int!
}
