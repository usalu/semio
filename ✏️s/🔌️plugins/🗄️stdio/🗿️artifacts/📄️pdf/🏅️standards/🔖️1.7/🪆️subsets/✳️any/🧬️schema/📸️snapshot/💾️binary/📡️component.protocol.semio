dialect protocol
protocol stdio.pdf.1.7.snapshot
version 1
schema stdio.pdf.1.7
start header

# The Semio envelope is removed before walking this deterministic logical snapshot codec.
# Native PDF headers, xref offsets, lexical forms, and encoded stream payloads are never stored.
framing record

chain logical_snapshot bytes
