/** 🎹️ PdfAComposer (✳️a subset) meta. This leaf was missing on the pre-restructure ✳️a-2b pilot
 * (a standing breach against 🔣️taxonomy.json's `subsetChildDirs`) -- W2 of ticket
 * 26/08/11/ARTIFACT-STANDARD-SUBSETS-REAL-VOCABULARIES adds it. */
export const meta = {
  artifactKind: "s.stdio.pdf",
  standard: "1.7",
  subset: "a",
} as const;
