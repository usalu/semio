module Stdio_pdf_1_7_Mutations;

# Structural logical mutation frame. The payload is decoded by mutation-tag into varints,
# length-prefixed semantic values, recursive COS objects, and typed stream-filter records.
public type Document = unit {
    format: uint8;
    mutation_tag: uint8;
    structural_payload: bytes &eod;
};
