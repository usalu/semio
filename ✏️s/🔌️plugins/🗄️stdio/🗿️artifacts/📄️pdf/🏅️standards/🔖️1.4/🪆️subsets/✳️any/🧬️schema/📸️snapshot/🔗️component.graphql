# 🧬️ PdfSnapshot (1.4) schema — the intentionally-frozen pre-real-codec stub.
type PageDoc {
  width: Float!
  height: Float!
  text: String!
}
type PdfSnapshot {
  schema: String! @state(class: ARTIFACT)
  page: PageDoc! @state(class: ARTIFACT)
}
