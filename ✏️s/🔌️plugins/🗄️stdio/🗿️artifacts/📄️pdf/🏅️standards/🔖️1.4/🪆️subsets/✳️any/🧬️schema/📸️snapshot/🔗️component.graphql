# 🧬️ PdfSnapshot (1.4) schema — the document's real page tree.
type PageDoc {
  width: Float!
  height: Float!
  text: String!
}
type PdfSnapshot {
  schema: String! @state(class: ARTIFACT)
  pages: [PageDoc!]! @state(class: ARTIFACT)
}
