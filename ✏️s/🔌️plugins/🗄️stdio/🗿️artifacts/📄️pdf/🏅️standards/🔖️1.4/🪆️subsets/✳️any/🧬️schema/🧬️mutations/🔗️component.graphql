# 🧬️ PdfMutation (1.4) union — see ../📸️snapshot/🔗️component.graphql for PageDoc/PdfSnapshot.
type PdfMutation {
  mutation: PdfMutationKind!
  snapshot: PdfSnapshot
}

enum PdfMutationKind {
  NO_MUTATION
  SET_SNAPSHOT
}
