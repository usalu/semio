# 💡️ Pdf (1.4) inference schema — document outline (page count, word/char counts).

type PdfInference {
  outline: PdfOutline! @derived
}

type PdfOutline {
  pageCount: Int!
  wordCount: Int!
  charCount: Int!
}
