# 💡️ Json inference schema — document outline (node count, max depth, root kind).

type JsonInference {
  outline: JsonOutline! @derived
}

type JsonOutline {
  nodeCount: Int!
  maxDepth: Int!
  rootKind: String!
}
