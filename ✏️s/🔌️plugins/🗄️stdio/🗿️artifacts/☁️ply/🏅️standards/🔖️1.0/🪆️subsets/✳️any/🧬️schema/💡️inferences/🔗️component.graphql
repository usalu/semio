# 💡️ Ply inference schema — vertex-element bounding box plus vertex/face counts.

type PlyInference {
  bounds: PlyBounds! @state(class: INFERRED)
}

type PlyBounds {
  min: [Float!]!
  max: [Float!]!
  vertexCount: Int!
  faceCount: Int!
}
