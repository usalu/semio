# 💡️ Xlsx inference schema — document outline (sheet names/count, cell count).

type XlsxInference {
  outline: XlsxOutline! @derived
}

type XlsxOutline {
  sheetNames: [String!]!
  sheetCount: Int!
  cellCount: Int!
}
