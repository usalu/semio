/** 🧬️ XlsxSnapshot schema (✳️transitional subset) meta — reuses the ✳️any subset's schema verbatim. */
export const meta = {
  artifactKind: "s.stdio.xlsx",
  standard: "ecma-376",
  subset: "transitional",
} as const;
