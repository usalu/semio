/** ↩️ block5d rename-representation/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RenameRepresentation } from "../🦠️mutation/🟦️component";
import type { Block5dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RenameRepresentation, base: Block5dSnapshot): unknown[];
