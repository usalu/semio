/** 🎬 block5d move-camera3d/🦠️mutation — the 3D-projection window camera's position + look-at target together. */
export interface MoveCamera3d {
  newPosition: [number, number, number];
  newTarget: [number, number, number];
}
