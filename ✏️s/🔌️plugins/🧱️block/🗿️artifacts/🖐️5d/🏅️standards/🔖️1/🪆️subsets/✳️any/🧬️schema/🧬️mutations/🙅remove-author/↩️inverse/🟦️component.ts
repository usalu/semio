/** ↩️ block5d remove-author/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RemoveAuthor } from "../🦠️mutation/🟦️component";
import type { Block5dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RemoveAuthor, base: Block5dSnapshot): unknown[];
