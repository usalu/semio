/** ↩️ block5d rename-part-kind/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RenamePartKind } from "../🦠️mutation/🟦️component";
import type { Block5dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RenamePartKind, base: Block5dSnapshot): unknown[];
