/** 🖌️ block5d update-part-2d/🦠️mutation — the whole 2D-projection presentation facet atomically — mirrors block2d's `update-presentation` (same shape, same no-identity-field reasoning). */
export interface UpdatePart2d {
  newShape?: string;
  newRadius?: number;
  newWidth?: number;
  newHeight?: number;
  newColor?: string;
  newIconKind?: string;
}
