/** 🧭 block5d move-grip-3d/🦠️mutation — a grip's 3D position + facing direction together. */
export interface MoveGrip3d {
  id: string;
  newPosition: [number, number, number];
  newDirection: [number, number, number];
}
