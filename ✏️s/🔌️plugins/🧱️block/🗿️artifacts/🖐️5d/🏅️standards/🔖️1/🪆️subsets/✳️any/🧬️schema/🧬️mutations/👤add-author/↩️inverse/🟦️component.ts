/** ↩️ block5d add-author/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { AddAuthor } from "../🦠️mutation/🟦️component";
import type { Block5dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: AddAuthor, base: Block5dSnapshot): unknown[];
