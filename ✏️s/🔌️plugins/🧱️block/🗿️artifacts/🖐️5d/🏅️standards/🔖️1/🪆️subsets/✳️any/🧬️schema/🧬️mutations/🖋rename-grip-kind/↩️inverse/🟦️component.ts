/** ↩️ block5d rename-grip-kind/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RenameGripKind } from "../🦠️mutation/🟦️component";
import type { Block5dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RenameGripKind, base: Block5dSnapshot): unknown[];
