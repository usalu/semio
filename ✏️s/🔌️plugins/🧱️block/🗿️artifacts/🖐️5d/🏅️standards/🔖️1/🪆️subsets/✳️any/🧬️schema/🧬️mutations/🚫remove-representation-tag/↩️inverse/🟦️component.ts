/** ↩️ block5d remove-representation-tag/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RemoveRepresentationTag } from "../🦠️mutation/🟦️component";
import type { Block5dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RemoveRepresentationTag, base: Block5dSnapshot): unknown[];
