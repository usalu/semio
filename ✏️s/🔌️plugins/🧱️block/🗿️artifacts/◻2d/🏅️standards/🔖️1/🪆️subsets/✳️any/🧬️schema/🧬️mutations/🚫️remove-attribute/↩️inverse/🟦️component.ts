/** ↩️ block2d remove-attribute/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RemoveAttribute } from "../🦠️mutation/🟦️component";
import type { Block2dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RemoveAttribute, base: Block2dSnapshot): unknown[];
