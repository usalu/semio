/** ↩️ block2d rename-node-kind/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RenameNodeKind } from "../🦠️mutation/🟦️component";
import type { Block2dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RenameNodeKind, base: Block2dSnapshot): unknown[];
