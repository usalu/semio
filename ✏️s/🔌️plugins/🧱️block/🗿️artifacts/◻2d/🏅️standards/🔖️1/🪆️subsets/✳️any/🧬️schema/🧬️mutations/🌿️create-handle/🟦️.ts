/** 🌿️ `CreateHandle` mutation payload — mirrors `🦀️.rs`. */

import type { Block2dHandleTemplate } from "../../../../../../../🟦️component";

export interface CreateHandle {
  handle: Block2dHandleTemplate;
}
