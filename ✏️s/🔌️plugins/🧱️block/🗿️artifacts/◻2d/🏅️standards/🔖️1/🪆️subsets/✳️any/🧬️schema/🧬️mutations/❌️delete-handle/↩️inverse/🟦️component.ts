/** ↩️ block2d delete-handle/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { DeleteHandle } from "../🦠️mutation/🟦️component";
import type { Block2dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: DeleteHandle, base: Block2dSnapshot): unknown[];
