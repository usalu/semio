/** ↩️ block2d update-presentation/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { UpdatePresentation } from "../🦠️mutation/🟦️component";
import type { Block2dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: UpdatePresentation, base: Block2dSnapshot): unknown[];
