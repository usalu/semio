/** ↩️ block2d move-handle/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { MoveHandle } from "../🦠️mutation/🟦️component";
import type { Block2dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: MoveHandle, base: Block2dSnapshot): unknown[];
