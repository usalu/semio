/** ↩️ block2d change-handle-kind-color/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { ChangeHandleKindColor } from "../🦠️mutation/🟦️component";
import type { Block2dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: ChangeHandleKindColor, base: Block2dSnapshot): unknown[];
