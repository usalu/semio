/** ↩️ block2d add-attribute/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { AddAttribute } from "../🦠️mutation/🟦️component";
import type { Block2dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: AddAttribute, base: Block2dSnapshot): unknown[];
