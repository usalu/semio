/** ↩️ block3d add-representation-tag/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { AddRepresentationTag } from "../🦠️mutation/🟦️component";
import type { Block3dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: AddRepresentationTag, base: Block3dSnapshot): unknown[];
