/** ↩️ block3d rename-representation/↩️inverse facade — inverse-mutation builder signature (real, non-stub). */
import type { RenameRepresentation } from "../🦠️mutation/🟦️component";
import type { Block3dSnapshot } from "../../../📸️snapshot/🟦️component";

export declare function inverse(payload: RenameRepresentation, base: Block3dSnapshot): unknown[];
