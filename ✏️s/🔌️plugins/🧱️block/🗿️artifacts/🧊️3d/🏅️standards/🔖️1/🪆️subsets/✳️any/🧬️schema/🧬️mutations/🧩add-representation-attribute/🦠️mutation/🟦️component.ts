/** 🧩 block3d add-representation-attribute/🦠️mutation — a member of a representation's nested `attributes`. */
export interface AddRepresentationAttribute {
  id: string;
  attribute: BlockAttribute;
}
