/** 🧬️ HomePresence — empty: the home launcher keeps panel tab and locale in `HomeConfig`; there is no multi-user shareable live surface on the launcher. Mirrors `HomePresence` in `../🦀️component.rs`. */
export interface HomePresence {}
