syntax = "proto3";
package semio.s.space.home.mutation;

// 🧬️ S Home mutation schema — artifact-lane fields only.

message SHomeMutation {
  // @state artifact
  string schema = 1;
  // @state artifact
  uint64 catalog_generation = 2;
}
