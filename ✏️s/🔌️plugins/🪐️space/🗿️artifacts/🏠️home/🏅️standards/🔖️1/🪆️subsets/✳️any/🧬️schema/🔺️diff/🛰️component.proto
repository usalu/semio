syntax = "proto3";
package semio.s.space.home.diff;

// 🧬️ S Home diff schema — sparse field delta over the artifact.

message SHomeDiff {
  // @state artifact
  optional string schema = 1;
  // @state artifact
  optional uint64 catalog_generation = 2;
  // @state config
  optional string active_panel_tab = 3;
  // @state config
  optional string locale = 4;
}
