syntax = "proto3";
package semio.s.space.home.snapshot;

// 🧬️ S Home snapshot schema — artifact-lane fields only.

message SHomeSnapshot {
  // @state artifact
  string schema = 1;
  // @state artifact
  uint64 catalog_generation = 2;
}
