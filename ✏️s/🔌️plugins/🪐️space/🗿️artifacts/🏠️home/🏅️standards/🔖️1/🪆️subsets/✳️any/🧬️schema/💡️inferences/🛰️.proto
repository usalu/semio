syntax = "proto3";
package semio.s.space.home.inference;

// 💡️ SHome inference schema — contentDigest fingerprint of the persisted document.

message SHomeInference {
  // @derived
  string content_digest = 1;
}
