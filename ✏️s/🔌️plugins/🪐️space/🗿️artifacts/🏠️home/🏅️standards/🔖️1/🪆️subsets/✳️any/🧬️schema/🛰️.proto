syntax = "proto3";
package semio.s.space.home.artifact;

// 🧬️ S Home artifact schema — every field with its state class.

message SHomeArtifact {
  // @state artifact
  string schema = 1;
  // @state artifact
  uint64 catalog_generation = 2;
  // @state config
  string active_panel_tab = 3;
  // @state config
  string locale = 4;
}
