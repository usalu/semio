{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://semio.tech/schema/s/puzzle/puzzle2d/mutations/delete-node.json",
  "title": "DeleteNode",
  "description": "The `delete-node` mutation record — internally tagged `\"mutation\": \"deleteNode\"`, the one branch the aggregate `../🔣️.json` union carries for this kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "id"
  ],
  "properties": {
    "mutation": {
      "const": "deleteNode"
    },
    "id": {
      "type": "string"
    }
  }
}
