{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://semio.tech/schema/s/puzzle/puzzle2d/mutations/change-edge-locked.json",
  "title": "ChangeEdgeLocked",
  "description": "The `change-edge-locked` mutation record — internally tagged `\"mutation\": \"changeEdgeLocked\"`, the one branch the aggregate `../🔣️.json` union carries for this kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "id"
  ],
  "properties": {
    "mutation": {
      "const": "changeEdgeLocked"
    },
    "id": {
      "type": "string"
    },
    "newLocked": {
      "type": [
        "boolean",
        "null"
      ]
    }
  }
}
