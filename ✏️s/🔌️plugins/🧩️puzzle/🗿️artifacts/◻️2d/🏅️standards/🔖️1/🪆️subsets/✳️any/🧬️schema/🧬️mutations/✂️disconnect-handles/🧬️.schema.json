{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://semio.tech/schema/s/puzzle/puzzle2d/mutations/disconnect-handles.json",
  "title": "DisconnectHandles",
  "description": "The `disconnect-handles` mutation record — internally tagged `\"mutation\": \"disconnectHandles\"`, the one branch the aggregate `../🔣️.json` union carries for this kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "id"
  ],
  "properties": {
    "mutation": {
      "const": "disconnectHandles"
    },
    "id": {
      "type": "string"
    }
  }
}
