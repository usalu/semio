{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://semio.tech/schema/s/puzzle/puzzle2d/mutation/disconnect-kind-compatibility/schema.json",
  "title": "DisconnectKindCompatibility",
  "description": "The `disconnect-kind-compatibility` mutation record — internally tagged `\"mutation\": \"disconnectKindCompatibility\"`, the one branch the aggregate `../🔣️.json` union carries for this kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "source",
    "target"
  ],
  "properties": {
    "mutation": {
      "const": "disconnectKindCompatibility"
    },
    "source": {
      "type": "string"
    },
    "target": {
      "type": "string"
    }
  }
}
