{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://semio.tech/schema/s/puzzle/puzzle2d/mutations/change-edge-kind.json",
  "title": "ChangeEdgeKind",
  "description": "The `change-edge-kind` mutation record — internally tagged `\"mutation\": \"changeEdgeKind\"`, the one branch the aggregate `../🔣️.json` union carries for this kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "id"
  ],
  "properties": {
    "mutation": {
      "const": "changeEdgeKind"
    },
    "id": {
      "type": "string"
    },
    "newEdgeKind": {
      "type": [
        "string",
        "null"
      ]
    }
  }
}
