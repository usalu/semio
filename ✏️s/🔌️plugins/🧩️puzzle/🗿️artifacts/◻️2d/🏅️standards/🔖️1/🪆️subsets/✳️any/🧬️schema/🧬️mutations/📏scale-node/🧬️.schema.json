{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://semio.tech/schema/s/puzzle/puzzle2d/mutations/scale-node.json",
  "title": "ScaleNode",
  "description": "The `scale-node` mutation record — internally tagged `\"mutation\": \"scaleNode\"`, the one branch the aggregate `../🔣️.json` union carries for this kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "id"
  ],
  "properties": {
    "mutation": {
      "const": "scaleNode"
    },
    "id": {
      "type": "string"
    },
    "newScale": {
      "type": [
        "number",
        "null"
      ]
    }
  }
}
