{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://json.schemas.assets.semio-tech.com/s/puzzle/puzzle2d/mutation/edit-target-region-label/schema.json",
  "title": "EditTargetRegionLabel",
  "description": "The `edit-target-region-label` mutation record — internally tagged `\"mutation\": \"editTargetRegionLabel\"`, the one branch the aggregate `../🔣️.json` union carries for this kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mutation",
    "id"
  ],
  "properties": {
    "mutation": {
      "const": "editTargetRegionLabel"
    },
    "id": {
      "type": "string"
    },
    "newLabel": {
      "type": [
        "string",
        "null"
      ]
    }
  }
}
