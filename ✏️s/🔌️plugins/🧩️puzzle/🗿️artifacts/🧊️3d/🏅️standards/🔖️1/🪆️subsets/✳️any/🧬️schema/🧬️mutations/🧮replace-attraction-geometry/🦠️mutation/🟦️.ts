/** 🧮 `replace-attraction-geometry` payload — mirrors Rust `ReplaceAttractionGeometry`
 * (`../🦀️.rs:13`). */
export interface ReplaceAttractionGeometry {
  id: string;
  newGap: number;
  newShift: number;
  newRise: number;
  newRotation: number;
  newTurn: number;
  newTilt: number;
  newX: number;
  newY: number;
}
