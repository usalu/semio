/** 🔗 `connect-vortices` payload — mirrors Rust `ConnectVortices` (`../🦀️.rs:15`). */
export interface ConnectVortices {
  id: string;
  attracting: string;
  attracted: string;
  gap: number;
  shift: number;
  rise: number;
  rotation: number;
  turn: number;
  tilt: number;
  x: number;
  y: number;
}
