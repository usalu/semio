/** 🖇 `replace-reference-source` payload — mirrors Rust `ReplaceReferenceSource` (`../🦀️.rs:13`). */
import type { Puzzle3dReferenceSource } from "../../🟦️component.ts";

export interface ReplaceReferenceSource {
  id: string;
  newSource: Puzzle3dReferenceSource;
}
