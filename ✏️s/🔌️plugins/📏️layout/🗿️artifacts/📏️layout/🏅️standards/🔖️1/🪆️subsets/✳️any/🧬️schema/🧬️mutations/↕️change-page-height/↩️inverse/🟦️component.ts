/** ↩ Inverse constructor for `change-page-height` — reconstructed from captured BASE state. */
import type { ChangePageHeight } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ChangePageHeight` from `base` (pre-state) — mirrors the Rust `inverse_change_page_height`. */
export declare function inverseChangePageHeight(payload: ChangePageHeight, base: unknown): unknown[];
