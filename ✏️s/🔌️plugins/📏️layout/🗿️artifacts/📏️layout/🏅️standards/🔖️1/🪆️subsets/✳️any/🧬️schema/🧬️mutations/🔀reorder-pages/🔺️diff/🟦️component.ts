/** 🔺 Diff constructor for `reorder-pages`. */
import type { ReorderPages } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ReorderPages` directly from `(payload, base)` — mirrors the Rust `diff_reorder_pages`. */
export declare function diffReorderPages(payload: ReorderPages, base: unknown): unknown;
