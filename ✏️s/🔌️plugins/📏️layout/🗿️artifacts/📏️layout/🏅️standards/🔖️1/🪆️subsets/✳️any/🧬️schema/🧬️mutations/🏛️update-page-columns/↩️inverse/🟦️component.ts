/** ↩ Inverse constructor for `update-page-columns` — reconstructed from captured BASE state. */
import type { UpdatePageColumns } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `UpdatePageColumns` from `base` (pre-state) — mirrors the Rust `inverse_update_page_columns`. */
export declare function inverseUpdatePageColumns(payload: UpdatePageColumns, base: unknown): unknown[];
