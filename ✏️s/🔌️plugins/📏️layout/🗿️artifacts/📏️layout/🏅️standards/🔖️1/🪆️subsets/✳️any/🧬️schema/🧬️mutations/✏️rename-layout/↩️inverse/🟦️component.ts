/** ↩ Inverse constructor for `rename-layout` — reconstructed from captured BASE state. The document root always exists, so this never returns `Vec::new()`. */
import type { RenameLayout } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `RenameLayout` from `base` (pre-state) — mirrors the Rust `inverse_rename_layout`. */
export declare function inverseRenameLayout(payload: RenameLayout, base: unknown): unknown[];
