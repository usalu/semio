/** 🔺 Diff constructor for `create-link`. */
import type { CreateLink } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `CreateLink` directly from `(payload, base)` — mirrors the Rust `diff_create_link`. */
export declare function diffCreateLink(payload: CreateLink, base: unknown): unknown;
