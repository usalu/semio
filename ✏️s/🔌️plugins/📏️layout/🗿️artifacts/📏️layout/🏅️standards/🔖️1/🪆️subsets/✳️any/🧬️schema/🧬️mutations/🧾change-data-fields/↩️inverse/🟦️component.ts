/** ↩ Inverse constructor for `change-data-fields` — reconstructed from captured BASE state. */
import type { ChangeDataFields } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ChangeDataFields` from `base` (pre-state) — mirrors the Rust `inverse_change_data_fields`. */
export declare function inverseChangeDataFields(payload: ChangeDataFields, base: unknown): unknown[];
