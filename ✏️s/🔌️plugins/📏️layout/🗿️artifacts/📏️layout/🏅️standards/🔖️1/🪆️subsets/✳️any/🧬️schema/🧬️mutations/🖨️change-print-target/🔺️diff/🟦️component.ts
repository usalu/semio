/** 🔺 Diff constructor for `change-print-target`. */
import type { ChangePrintTarget } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangePrintTarget` directly from `(payload, base)` — mirrors the Rust `diff_change_print_target`. */
export declare function diffChangePrintTarget(payload: ChangePrintTarget, base: unknown): unknown;
