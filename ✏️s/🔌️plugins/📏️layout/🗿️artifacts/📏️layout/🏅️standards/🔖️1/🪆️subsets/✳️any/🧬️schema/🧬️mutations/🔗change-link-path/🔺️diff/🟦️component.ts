/** 🔺 Diff constructor for `change-link-path`. */
import type { ChangeLinkPath } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeLinkPath` directly from `(payload, base)` — mirrors the Rust `diff_change_link_path`. */
export declare function diffChangeLinkPath(payload: ChangeLinkPath, base: unknown): unknown;
