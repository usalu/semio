/** 🔺 Diff constructor for `delete-link`. */
import type { DeleteLink } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `DeleteLink` directly from `(payload, base)` — mirrors the Rust `diff_delete_link`. */
export declare function diffDeleteLink(payload: DeleteLink, base: unknown): unknown;
