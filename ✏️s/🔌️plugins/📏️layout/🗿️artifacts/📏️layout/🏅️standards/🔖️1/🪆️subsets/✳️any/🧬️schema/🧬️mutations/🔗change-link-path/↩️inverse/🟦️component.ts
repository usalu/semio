/** ↩ Inverse constructor for `change-link-path` — reconstructed from captured BASE state. */
import type { ChangeLinkPath } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ChangeLinkPath` from `base` (pre-state) — mirrors the Rust `inverse_change_link_path`. */
export declare function inverseChangeLinkPath(payload: ChangeLinkPath, base: unknown): unknown[];
