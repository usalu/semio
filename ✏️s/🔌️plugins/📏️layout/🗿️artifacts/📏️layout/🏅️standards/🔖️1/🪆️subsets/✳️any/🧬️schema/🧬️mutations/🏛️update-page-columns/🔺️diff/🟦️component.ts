/** 🔺 Diff constructor for `update-page-columns`. */
import type { UpdatePageColumns } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `UpdatePageColumns` directly from `(payload, base)` — mirrors the Rust `diff_update_page_columns`. */
export declare function diffUpdatePageColumns(payload: UpdatePageColumns, base: unknown): unknown;
