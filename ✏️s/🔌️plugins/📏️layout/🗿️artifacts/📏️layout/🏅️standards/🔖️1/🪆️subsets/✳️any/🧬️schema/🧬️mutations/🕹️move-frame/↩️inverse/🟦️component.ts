/** ↩ Inverse constructor for `move-frame` — reconstructed from captured BASE state. */
import type { MoveFrame } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `MoveFrame` from `base` (pre-state) — mirrors the Rust `inverse_move_frame`. */
export declare function inverseMoveFrame(payload: MoveFrame, base: unknown): unknown[];
