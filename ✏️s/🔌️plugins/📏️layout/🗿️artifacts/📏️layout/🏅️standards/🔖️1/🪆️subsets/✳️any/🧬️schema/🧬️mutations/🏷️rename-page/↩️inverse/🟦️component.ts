/** ↩ Inverse constructor for `rename-page` — reconstructed from captured BASE state. */
import type { RenamePage } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `RenamePage` from `base` (pre-state) — mirrors the Rust `inverse_rename_page`. */
export declare function inverseRenamePage(payload: RenamePage, base: unknown): unknown[];
