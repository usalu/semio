/** ↩ Inverse constructor for `change-frame-wrap-mode` — reconstructed from captured BASE state. A non-text frame has nothing to undo (the field-patch was a no-op). */
import type { ChangeFrameWrapMode } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ChangeFrameWrapMode` from `base` (pre-state) — mirrors the Rust `inverse_change_frame_wrap_mode`. */
export declare function inverseChangeFrameWrapMode(payload: ChangeFrameWrapMode, base: unknown): unknown[];
