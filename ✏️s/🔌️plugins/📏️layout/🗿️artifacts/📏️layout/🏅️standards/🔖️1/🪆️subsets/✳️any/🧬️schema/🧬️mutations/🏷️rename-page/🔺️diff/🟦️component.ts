/** 🔺 Diff constructor for `rename-page`. */
import type { RenamePage } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `RenamePage` directly from `(payload, base)` — mirrors the Rust `diff_rename_page`. */
export declare function diffRenamePage(payload: RenamePage, base: unknown): unknown;
