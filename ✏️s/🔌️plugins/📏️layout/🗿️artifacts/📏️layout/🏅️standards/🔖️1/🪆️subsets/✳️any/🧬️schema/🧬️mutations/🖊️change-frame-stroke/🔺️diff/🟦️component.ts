/** 🔺 Diff constructor for `change-frame-stroke`. */
import type { ChangeFrameStroke } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeFrameStroke` directly from `(payload, base)` — mirrors the Rust `diff_change_frame_stroke`. */
export declare function diffChangeFrameStroke(payload: ChangeFrameStroke, base: unknown): unknown;
