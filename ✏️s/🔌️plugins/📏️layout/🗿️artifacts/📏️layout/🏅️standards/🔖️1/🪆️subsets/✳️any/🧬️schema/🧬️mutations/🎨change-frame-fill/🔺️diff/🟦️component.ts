/** 🔺 Diff constructor for `change-frame-fill`. */
import type { ChangeFrameFill } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeFrameFill` directly from `(payload, base)` — mirrors the Rust `diff_change_frame_fill`. */
export declare function diffChangeFrameFill(payload: ChangeFrameFill, base: unknown): unknown;
