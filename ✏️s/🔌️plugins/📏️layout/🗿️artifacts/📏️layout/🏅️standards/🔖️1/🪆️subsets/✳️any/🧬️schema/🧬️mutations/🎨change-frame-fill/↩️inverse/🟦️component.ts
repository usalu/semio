/** ↩ Inverse constructor for `change-frame-fill` — reconstructed from captured BASE state. A non-rect frame has nothing to undo (the field-patch was a no-op). */
import type { ChangeFrameFill } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ChangeFrameFill` from `base` (pre-state) — mirrors the Rust `inverse_change_frame_fill`. */
export declare function inverseChangeFrameFill(payload: ChangeFrameFill, base: unknown): unknown[];
