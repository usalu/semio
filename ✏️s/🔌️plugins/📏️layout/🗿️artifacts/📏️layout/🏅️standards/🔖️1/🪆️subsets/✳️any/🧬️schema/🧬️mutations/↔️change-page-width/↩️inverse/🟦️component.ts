/** ↩ Inverse constructor for `change-page-width` — reconstructed from captured BASE state. */
import type { ChangePageWidth } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ChangePageWidth` from `base` (pre-state) — mirrors the Rust `inverse_change_page_width`. */
export declare function inverseChangePageWidth(payload: ChangePageWidth, base: unknown): unknown[];
