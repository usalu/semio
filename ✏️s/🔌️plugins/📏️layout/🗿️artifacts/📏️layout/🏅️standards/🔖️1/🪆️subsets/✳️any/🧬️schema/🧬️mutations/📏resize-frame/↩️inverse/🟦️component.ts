/** ↩ Inverse constructor for `resize-frame` — reconstructed from captured BASE state. */
import type { ResizeFrame } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ResizeFrame` from `base` (pre-state) — mirrors the Rust `inverse_resize_frame`. */
export declare function inverseResizeFrame(payload: ResizeFrame, base: unknown): unknown[];
