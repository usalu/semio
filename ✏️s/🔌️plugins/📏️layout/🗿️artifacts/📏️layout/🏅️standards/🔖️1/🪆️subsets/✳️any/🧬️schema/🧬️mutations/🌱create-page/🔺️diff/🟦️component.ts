/** 🔺 Diff constructor for `create-page`. */
import type { CreatePage } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `CreatePage` directly from `(payload, base)` — mirrors the Rust `diff_create_page`. */
export declare function diffCreatePage(payload: CreatePage, base: unknown): unknown;
