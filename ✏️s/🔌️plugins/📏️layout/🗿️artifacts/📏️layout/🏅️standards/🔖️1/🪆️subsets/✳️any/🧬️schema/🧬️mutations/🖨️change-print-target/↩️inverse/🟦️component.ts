/** ↩ Inverse constructor for `change-print-target` — reconstructed from captured BASE state. */
import type { ChangePrintTarget } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `ChangePrintTarget` from `base` (pre-state) — mirrors the Rust `inverse_change_print_target`. */
export declare function inverseChangePrintTarget(payload: ChangePrintTarget, base: unknown): unknown[];
