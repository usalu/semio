/** ↩ Inverse constructor for `edit-story` — reconstructed from captured BASE state. */
import type { EditStory } from "../🦠️mutation/🟦️component.ts";

/** Reconstructs the inverse mutation list for `EditStory` from `base` (pre-state) — mirrors the Rust `inverse_edit_story`. */
export declare function inverseEditStory(payload: EditStory, base: unknown): unknown[];
