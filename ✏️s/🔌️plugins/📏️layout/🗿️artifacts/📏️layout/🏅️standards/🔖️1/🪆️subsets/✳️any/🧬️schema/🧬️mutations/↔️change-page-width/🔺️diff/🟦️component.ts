/** 🔺 Diff constructor for `change-page-width`. */
import type { ChangePageWidth } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangePageWidth` directly from `(payload, base)` — mirrors the Rust `diff_change_page_width`. */
export declare function diffChangePageWidth(payload: ChangePageWidth, base: unknown): unknown;
