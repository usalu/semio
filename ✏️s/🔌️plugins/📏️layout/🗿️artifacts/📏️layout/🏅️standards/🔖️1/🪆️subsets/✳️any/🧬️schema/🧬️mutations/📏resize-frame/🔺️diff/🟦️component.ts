/** 🔺 Diff constructor for `resize-frame`. */
import type { ResizeFrame } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ResizeFrame` directly from `(payload, base)` — mirrors the Rust `diff_resize_frame`. */
export declare function diffResizeFrame(payload: ResizeFrame, base: unknown): unknown;
