/** 🔺 Diff constructor for `delete-page`. */
import type { DeletePage } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `DeletePage` directly from `(payload, base)` — mirrors the Rust `diff_delete_page`. */
export declare function diffDeletePage(payload: DeletePage, base: unknown): unknown;
