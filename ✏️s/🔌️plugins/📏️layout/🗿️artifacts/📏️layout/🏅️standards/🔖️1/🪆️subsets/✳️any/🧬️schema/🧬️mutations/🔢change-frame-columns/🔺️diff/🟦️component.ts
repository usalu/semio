/** 🔺 Diff constructor for `change-frame-columns`. */
import type { ChangeFrameColumns } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeFrameColumns` directly from `(payload, base)` — mirrors the Rust `diff_change_frame_columns`. */
export declare function diffChangeFrameColumns(payload: ChangeFrameColumns, base: unknown): unknown;
