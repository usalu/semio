/** 🔺 Diff constructor for `update-page-margins`. */
import type { UpdatePageMargins } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `UpdatePageMargins` directly from `(payload, base)` — mirrors the Rust `diff_update_page_margins`. */
export declare function diffUpdatePageMargins(payload: UpdatePageMargins, base: unknown): unknown;
