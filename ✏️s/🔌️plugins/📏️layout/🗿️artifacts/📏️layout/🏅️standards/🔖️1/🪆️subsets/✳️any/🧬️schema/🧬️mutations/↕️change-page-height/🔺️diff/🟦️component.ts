/** 🔺 Diff constructor for `change-page-height`. */
import type { ChangePageHeight } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangePageHeight` directly from `(payload, base)` — mirrors the Rust `diff_change_page_height`. */
export declare function diffChangePageHeight(payload: ChangePageHeight, base: unknown): unknown;
