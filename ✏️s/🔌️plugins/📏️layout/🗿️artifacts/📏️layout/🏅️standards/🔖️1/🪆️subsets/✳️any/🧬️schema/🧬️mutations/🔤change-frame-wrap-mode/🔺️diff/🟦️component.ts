/** 🔺 Diff constructor for `change-frame-wrap-mode`. */
import type { ChangeFrameWrapMode } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ChangeFrameWrapMode` directly from `(payload, base)` — mirrors the Rust `diff_change_frame_wrap_mode`. */
export declare function diffChangeFrameWrapMode(payload: ChangeFrameWrapMode, base: unknown): unknown;
