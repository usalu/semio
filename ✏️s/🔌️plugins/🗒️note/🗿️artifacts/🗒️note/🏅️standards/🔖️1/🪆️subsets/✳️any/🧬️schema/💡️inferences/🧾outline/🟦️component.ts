/** 🧾 `outline` — one named inference: this document's own outline (flattened block names + word/block counts). */

export interface NoteOutline {
  sectionOutline: string[];
  blockCount: number;
  wordCount: number;
}
