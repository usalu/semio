export interface NoteCamera { x: number; y: number; zoom: number; }
export interface NoteCompositeWindowConfig { camera: NoteCamera; }
export interface NoteCompositeWindowTransient { engagementInput: string; }
