# Note Editor Transient Facet

🫧️ Intentionally empty — `NotePlayApp::Transient`/`TransientMutation` are the framework's
`NoTransient`/`NoTransientMutation`; this surface declares no custom ephemeral state of its own.
