# 💡️ Note inference schema — block outline (flattened block names + word/block counts).

type NoteInference {
  outline: NoteOutline! @derived
}

type NoteOutline {
  sectionOutline: [String!]!
  blockCount: Int!
  wordCount: Int!
}
