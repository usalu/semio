syntax = "proto3";
package semio.app.note.note;
message NotePresence {
  // @state shared-ui
  repeated string selected_block_ids = 1;
  // @state shared-ui
  double camera_x = 2;
  // @state shared-ui
  double camera_y = 3;
  // @state shared-ui
  double camera_zoom = 4;
  // @state shared-ui
  optional string hovered_block_id = 5;
  // @state shared-ui
  string active_utility_id = 6;
}
