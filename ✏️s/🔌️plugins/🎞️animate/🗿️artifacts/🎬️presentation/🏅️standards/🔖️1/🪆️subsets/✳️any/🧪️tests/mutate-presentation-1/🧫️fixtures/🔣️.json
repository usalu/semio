{
  "_provenance": "🧫️ Derived ONCE from this vocabulary's own committed leaf fixtures, never invented here. `t-hero` is the tile `create-tile`'s committed payload creates (🧬️schema/🧬️mutations/🆕create-tile/🧪️tests/rejects-a-duplicate-tile-id/🦠️mutation/🔣️.json) and the tile `rename-tile`, `resize-tile-crop` and `reorder-tiles` each address in their own committed payloads, carried over verbatim including its crop. `t-alpha` and `t-omega` are the two ids `delete-tiles`' committed payload addresses (🧹delete-tiles/🧪️tests/rejects-when-every-addressed-tile-is-missing); that fixture pins the branch where BOTH are missing, so it commits their ids and no names or crops — the two crops below are this case's own derivation, chosen as the opposite corners of the normalized figure so a reorder is visible in the projection. The source is not committed here at all: it is whatever the real committed artifact `📚️examples/🎬️demo/🖼️assets/🗣️.dsl.semio` decodes to through production's own reader.",
  "tiles": [
    {
      "id": "t-hero",
      "name": "Hero",
      "crop": {
        "x": 0.25,
        "y": 0.25,
        "width": 0.5,
        "height": 0.5
      }
    },
    {
      "id": "t-alpha",
      "name": "Alpha",
      "crop": {
        "x": 0.0,
        "y": 0.0,
        "width": 0.25,
        "height": 0.25
      }
    },
    {
      "id": "t-omega",
      "name": "Omega",
      "crop": {
        "x": 0.75,
        "y": 0.75,
        "width": 0.25,
        "height": 0.25
      }
    }
  ]
}
