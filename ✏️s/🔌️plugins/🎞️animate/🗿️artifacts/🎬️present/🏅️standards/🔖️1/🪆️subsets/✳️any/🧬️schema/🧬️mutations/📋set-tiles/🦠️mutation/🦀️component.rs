//! 🪦️ Superseded stub — the generic `SetTiles { tiles }` variant this leaf backed was deleted by
//! the SEMANTIC-MUTATIONS-OVERHAUL fan-out (ticket
//! `.🦑️repo/🎫️tickets/🎆️26/🌙️08/☀️12/SEMANTIC-MUTATIONS-OVERHAUL`, `animate-present-1-any` facet
//! report) in favor of `replace-tiles` (see the `🧬️mutations/🔁replace-tiles/` sibling). Kept as an
//! empty placeholder only because `📦️glue.rs` (plugin-shared, out of this facet's edit boundary)
//! still `#[path]`-wires this exact file; the report's `sharedFileRequests` asks a later pass to
//! delete this directory once `📦️glue.rs` is repointed.
