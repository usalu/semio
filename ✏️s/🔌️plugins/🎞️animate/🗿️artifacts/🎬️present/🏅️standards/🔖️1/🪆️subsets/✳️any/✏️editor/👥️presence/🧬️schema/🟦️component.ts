/** 🧬️ PresentPresence — empty: tile selection/hover broadcast through the framework's typed `PresenceInteraction` ("tiles" domain, ticket 26/08/14/FIRST-CLASS-HOVER-AND-SELECTION-MECHANISM); present has no other app-specific ephemeral field left to carry. Mirrors `PresentPresence` in `../🦀️component.rs`. */
export interface PresentPresence {}
