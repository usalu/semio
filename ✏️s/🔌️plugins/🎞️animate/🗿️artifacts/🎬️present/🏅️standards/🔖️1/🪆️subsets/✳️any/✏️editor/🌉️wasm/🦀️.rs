//! 🕸️ Animate present app — the wasm-bindgen `PresentArtifactVcs` VCS bridge that used to live here
//! was deleted — nothing ever built it for `wasm32-unknown-unknown` (no engine entry, no `wasm`
//! script target) — see `26/09/01/RUNTIME-DEPENDENCY-ELIMINATION-FOR-S-PLUGINS-AND-ARTIFACTS`.
