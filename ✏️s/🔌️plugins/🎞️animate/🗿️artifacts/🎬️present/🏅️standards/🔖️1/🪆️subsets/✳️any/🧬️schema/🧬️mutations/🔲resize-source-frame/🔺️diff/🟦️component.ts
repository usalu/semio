/** 🔺️ Sparse diff construction for `resize-source-frame`. */
import type { ResizeSourceFrame } from "../🦠️mutation/🟦️component.ts";

/** Builds the sparse artifact diff for `ResizeSourceFrame` directly from `(payload, base)` — mirrors the Rust `diff`. */
export declare function diff(payload: ResizeSourceFrame, base: unknown): unknown;
