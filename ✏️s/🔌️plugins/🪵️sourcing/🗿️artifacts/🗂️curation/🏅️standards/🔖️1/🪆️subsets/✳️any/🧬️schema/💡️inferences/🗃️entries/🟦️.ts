/** 🗃 `entries` — one named inference: a real census over the curation document's stock/curated lists. */

export interface CurationEntries {
  stockCount: number;
  entryCount: number;
  totalCount: number;
}
