# 💡️ Curate inference schema — a real census over the stock catalog and curated bill of quantities.

type CurateInference {
  entries: CurateEntries! @derived
}

type CurateEntries {
  stockCount: Int!
  entryCount: Int!
  totalCount: Int!
}
