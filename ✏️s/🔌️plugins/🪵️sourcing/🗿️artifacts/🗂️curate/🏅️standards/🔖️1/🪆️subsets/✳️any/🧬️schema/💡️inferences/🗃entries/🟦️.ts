/** 🗃 `entries` — one named inference: a real census over the curate document's stock/curated lists. */

export interface CurateEntries {
  stockCount: number;
  entryCount: number;
  totalCount: number;
}
