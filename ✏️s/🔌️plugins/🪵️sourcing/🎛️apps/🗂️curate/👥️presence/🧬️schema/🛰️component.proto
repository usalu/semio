syntax = "proto3";
package semio.app.sourcing.curate;
message SourcingCuratePresence {
  // @state presence
  repeated double world_camera_position = 2;
  // @state presence
  repeated double world_camera_target = 3;
  // @state presence
  double world_camera_fov = 4;
}
