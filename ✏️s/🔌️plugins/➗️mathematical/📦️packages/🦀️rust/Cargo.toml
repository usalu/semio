[package]
name = "semio-s-plugin-mathematical"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Mathematical plugin - one crate for the mathematical artifact (diff/op/dsl/pack/spr/engine) and the mathematical play app (commands/modes/windows)"

[lints]
workspace = true

[package.metadata.component]
package = "semio:mathematical"

[package.metadata.semio]
role = "plugin"

[[package.metadata.semio.playground]]
variant = "mathematical"
app = "mathematical-play"
aliases = ["mathematical", "math"]
ports = { react = 6084, wgpu = 6184 }

[lib]
crate-type = ["cdylib", "rlib"]
path = "📦️lib.rs"

[dependencies]
math = { path = "../../../../../🧰️framework/🔨️modules/🧮️math/📦️packages/🦀️rust", package = "semio-framework-math" }
# 🧭️ `workspace = true` only works when this crate's local dependency KEY matches the key in root
# `[workspace.dependencies]` verbatim — Cargo resolves workspace inheritance by that key, and a `package =`
# override does NOT redirect the lookup (verified empirically: `cargo check` errors
# "dependency.dsl was not found in workspace.dependencies" when trying `dsl = { workspace = true, package =
# "semio-framework-os-kernel-dsl" }`). So the renamed internal aliases below stay plain `path`+`package`
# deps; only the four whose local name already equals the workspace key use `workspace = true`.
dsl = { path = "../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🗣️dsl/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-dsl" }
store = { path = "../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/🏪️store/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-store" }
protocol = { path = "../../../../../🧰️framework/🛍️products/💻️os/🔨️modules/📡️protocol/⚡️implementations/🦀️rust", package = "semio-framework-os-kernel-protocol" }
ui_wgpu = {path = "../../../../../🧰️framework/🔨️modules/🖱️ui/📦️packages/🦀️rust", package = "semio-framework-ui", features = ["wgpu"]}
semio-framework-core = { workspace = true }
semio-framework-plugin = { workspace = true, features = ["component-guest"] }
serde = { workspace = true }
serde_json = { workspace = true }
