/** mathematical op — thin WASM op-line facade. */
export function parseOp(line: string): unknown {
  throw new Error("wire to plugin WASM");
}
export function printOp(value: unknown): string {
  throw new Error("wire to plugin WASM");
}
