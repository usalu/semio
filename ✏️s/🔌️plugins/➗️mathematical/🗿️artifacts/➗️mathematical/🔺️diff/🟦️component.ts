/** mathematical diff — thin WASM diff facade. */
export function parseDiff(text: string): unknown {
  throw new Error("wire to plugin WASM");
}
export function printDiff(value: unknown): string {
  throw new Error("wire to plugin WASM");
}
