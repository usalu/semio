# 💡️ VCS inference schema — a scalar summary digest of the tags/notes free-form fields.

type VcsInference {
  summary: VcsSummary! @derived
}

type VcsSummary {
  tagCount: Int!
  notesWordCount: Int!
  hasNotes: Boolean!
}
