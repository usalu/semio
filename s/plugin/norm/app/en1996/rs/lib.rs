//! 🧱 EN 1996 app — document entities (constitutional: general).

use norm_core::{AnnexChoice, DesignSituation};
use serde::{Deserialize, Serialize};

// #region 🔖Types
/// 🧱 Masonry manufacturing-control class underlying the EN-recommended γ_M table (EN 1996-1-1 Table 2.1-style).
#[derive(Clone, Copy, Debug, PartialEq, Eq, Serialize, Deserialize, dsl::DslScalar)]
pub enum MasonryClass {
    Class1,
    Class2,
    Class3,
    Class4,
    Class5,
}

impl MasonryClass {
    pub fn gamma_m_en(self) -> f64 {
        match self {
            Self::Class1 => 1.5,
            Self::Class2 => 1.7,
            Self::Class3 => 2.0,
            Self::Class4 => 2.2,
            Self::Class5 => 2.5,
        }
    }
}

impl Default for MasonryClass {
    fn default() -> Self {
        Self::Class3
    }
}

pub mod part_2 {
    use serde::{Deserialize, Serialize};

    /// 🌦️ Masonry durability exposure class (EN 1996-1-1 Annex B-style categorisation MX1–MX5).
    #[derive(Clone, Copy, Debug, PartialEq, Eq, Serialize, Deserialize, dsl::DslScalar)]
    pub enum ExposureClass {
        Mx1,
        Mx2,
        Mx3,
        Mx4,
        Mx5,
    }

    /// 🧪 General-purpose mortar compressive-strength class per EN 998-2.
    #[derive(Clone, Copy, Debug, PartialEq, Eq, Serialize, Deserialize, dsl::DslScalar)]
    pub enum MortarClass {
        M1,
        /// 🔡 `M2_5` auto-kebabs to `m2-5` (digit-underscore-digit), but the standard's own class
        /// label is `M2.5`/`M2_5` with no internal dash — kept as a genuine rename.
        #[dsl(key = "m2_5")]
        M2_5,
        M5,
        M10,
        M20,
    }

    impl MortarClass {
        pub fn compressive_strength_mpa(self) -> f64 {
            match self {
                Self::M1 => 1.0,
                Self::M2_5 => 2.5,
                Self::M5 => 5.0,
                Self::M10 => 10.0,
                Self::M20 => 20.0,
            }
        }
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize, dsl::DslDocument)]
#[serde(rename_all = "camelCase")]
#[dsl(extension = "en1996", layout = "lines")]
pub struct Document {
    pub m_ed_knm: f64,
    pub n_ed_kn: f64,
    pub v_ed_kn: f64,
    pub h_ed_kn: f64,
    pub z_mm3: f64,
    pub area_mm2: f64,
    pub shear_area_mm2: f64,
    pub f_k_mpa: f64,
    pub f_vk_mpa: f64,
    pub annex: AnnexChoice,
    pub masonry_class: MasonryClass,
    pub design_situation: DesignSituation,
    pub mu: f64,
    pub wall_thickness_mm: f64,
    pub fire_resistance_min: u32,
    pub unit: String,
    pub exposure: part_2::ExposureClass,
    pub mortar: part_2::MortarClass,
    pub bed_joint_thickness_mm: f64,
    pub storeys: u32,
    pub h_ef_mm: f64,
    pub t_ef_mm: f64,
}

impl Default for Document {
    fn default() -> Self {
        Self {
            m_ed_knm: 8.0,
            n_ed_kn: 200.0,
            v_ed_kn: 35.0,
            h_ed_kn: 20.0,
            z_mm3: 8_000_000.0,
            area_mm2: 500_000.0,
            shear_area_mm2: 300_000.0,
            f_k_mpa: 5.0,
            f_vk_mpa: 0.15,
            annex: AnnexChoice::De,
            masonry_class: MasonryClass::default(),
            design_situation: DesignSituation::Persistent,
            mu: 0.4,
            wall_thickness_mm: 240.0,
            fire_resistance_min: 60,
            unit: "clay".into(),
            exposure: part_2::ExposureClass::Mx1,
            mortar: part_2::MortarClass::M5,
            bed_joint_thickness_mm: 12.0,
            storeys: 2,
            h_ef_mm: 2500.0,
            t_ef_mm: 240.0,
        }
    }
}
// #endregion 🔖Types

// `impl store::DocumentDsl for Document` and `MasonryClass`/`ExposureClass`/`MortarClass`'s scalar-tag
// mappings are generated by `#[derive(dsl::DslDocument)]`/`#[derive(dsl::DslScalar)]` on the type
// definitions above. Note: `Document::annex_params()` from the pre-split monolith moved to the
// `engine` crate as a free fn (`annex_params(document)`) — it constructs the compute-layer
// `AnnexParams` struct, which lives in `engine`, and an inherent impl on a foreign type is not
// possible across the crate boundary (Rust's orphan rule).
