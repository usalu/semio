[package]
name = "semio-s-app-playbook-engine"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Playbook-play app - headless compute (constitutional: engine)"

[lints]
workspace = true
[lib]
path = "lib.rs"

[dependencies]
playbook = { path = "../../rs", package = "semio-s-app-playbook" }
