[package]
name = "semio-s-kernel-pack-io"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Pack binary document format native IO — file source/sink, atomic writes, streaming writer, recovery"


[lints]
workspace = true
[lib]
path = "lib.rs"

[dependencies]
pack_core = { path = "../../core/rs", package = "semio-s-kernel-pack-core" }
pack_format = { path = "../../format/rs", package = "semio-s-kernel-pack-format" }
