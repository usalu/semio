[package]
name = "semio-s-kernel-db-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Db foundation — ids, errors, limits, durability classes, frontier/fencing types, priority, config, VersionGraph seam, Emit trait"

[lints]
workspace = true
[lib]
path = "lib.rs"

[dependencies]
pack_core = { path = "../../../pack/core/rs", package = "semio-s-kernel-pack-core" }
thiserror = "2.0.12"
serde = { version = "1", features = ["derive"] }
