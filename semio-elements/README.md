# Summary

Semio Elements contains shared React UI bundles for reusable semio interface elements and their visual documentation.

### Specs

- The `semio-elements` technology owns reusable UI elements that are consumed by other technologies instead of living inside feature bundles.
- Shared element development and Storybook hosting belong in `semio-elements/ui`.
